package:
  name: flask-snooze
  version: "0.1.6"

source:
  fn: Flask-Snooze-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Snooze/Flask-Snooze-0.1.6.tar.gz
  md5: 0ec50a6d5ad51636a2abc413d750ebb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-snooze = flask-snooze:main
    #
    # Would create an entry point called flask-snooze that calls flask-snooze.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ahri/flask-snooze
  license: MIT License
  summary: 'Backend agnostic REST API provider for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
