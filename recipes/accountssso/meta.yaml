package:
  name: accountssso
  version: "0.0.7"

source:
  fn: accountsSSO-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/a/accountsSSO/accountsSSO-0.0.7.tar.gz
  md5: 48cad9d3903b329e82c4cdb6fa83d03d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - accountssso = accountssso:main
    #
    # Would create an entry point called accountssso that calls accountssso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - accountssso

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/inean/python-accounts-sso
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'AccountSSO bindings for Harmattan platform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
