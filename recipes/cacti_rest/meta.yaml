package:
  name: cacti_rest
  version: "0.2.2"

source:
  fn: cacti_rest-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cacti_rest/cacti_rest-0.2.2.tar.gz
  md5: 32beb53dd2d43b36844b772fc8e90adf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cacti_rest = cacti_rest:main
    #
    # Would create an entry point called cacti_rest that calls cacti_rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5
    - simplejson
    - mysql-python

  run:
    - python
    - django ==1.5
    - simplejson
    - mysql-python

test:
  # Python imports
  imports:
    - cacti_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.digitalhigh.es
  license: BSD License
  summary: 'A Django app to publish via REST the cacti collected data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
