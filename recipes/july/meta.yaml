package:
  name: july
  version: "0.9.2"

source:
  fn: july-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/j/july/july-0.9.2.tar.gz
  md5: ebc25215ff9b5148c3addf476deb84d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - july = july:main
    #
    # Would create an entry point called july that calls july.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - july
    - july.auth
    - july.ext
    - july.ext.mail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://project.lepture.com/july/
  license: BSD License
  summary: 'July Tornado: a better way to organize your tornado project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
