package:
  name: django_ip_authn
  version: "0.3.0.1"

source:
  fn: django_ip_authn-0.3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_ip_authn/django_ip_authn-0.3.0.1.tar.gz
  md5: 2be797519886a4bbb4ac8f848f7d015e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_ip_authn = django_ip_authn:main
    #
    # Would create an entry point called django_ip_authn that calls django_ip_authn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_ip_authn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Felspar/django-ip-authn
  license: Boost Software License - Version 1.0 - August 17th, 2003
  summary: 'IP number based authentication for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
