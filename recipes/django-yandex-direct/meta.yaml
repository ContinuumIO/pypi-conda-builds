package:
  name: django-yandex-direct
  version: "1.0"

source:
  fn: django-yandex-direct-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yandex-direct/django-yandex-direct-1.0.tar.gz
  md5: 3b701b45d073a0338a21f27774e1e299
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yandex-direct = django-yandex-direct:main
    #
    # Would create an entry point called django-yandex-direct that calls django-yandex-direct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0
    - simplejson >=2.2.1

  run:
    - python
    - requests >=2.3.0
    - simplejson >=2.2.1

test:
  # Python imports
  imports:
    - yandex_direct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LPgenerator/django-yandex-direct
  license: GNU General Public License (GPL)
  summary: 'Simple wrapper for the Yandex.Direct API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
