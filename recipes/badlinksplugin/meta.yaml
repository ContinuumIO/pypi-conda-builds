package:
  name: badlinksplugin
  version: "0.1.1"

source:
  fn: BadLinksPlugin-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BadLinksPlugin/BadLinksPlugin-0.1.1.tar.gz
  md5: b24279a688883dfb82d3e1d3f450c65c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - badlinksplugin = badlinksplugin:main
    #
    # Would create an entry point called badlinksplugin that calls badlinksplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - badlinks
    - badlinksplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mitar.tnode.com/
  license: AGPLv3
  summary: 'Log bad local links found in wiki content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
