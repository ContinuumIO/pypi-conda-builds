package:
  name: kantonzugpdf
  version: "0.2.1"

source:
  fn: kantonzugpdf-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kantonzugpdf/kantonzugpdf-0.2.1.tar.gz
  md5: d412e111b01476e7b9f4b20f7a38496e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kantonzugpdf = kantonzugpdf:main
    #
    # Would create an entry point called kantonzugpdf that calls kantonzugpdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pdfdocument
    - reportlab

  run:
    - python
    - pdfdocument
    - reportlab

test:
  # Python imports
  imports:
    - kantonzugpdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seantis/kantonzugpdf
  license: GNU General Public License v2 (GPLv2)
  summary: 'A library to create PDF reports for the Canton of Zug'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
