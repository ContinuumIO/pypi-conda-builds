package:
  name: collective.recipe.pydevproject
  version: "0.1"

source:
  fn: collective.recipe.pydevproject-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.pydevproject/collective.recipe.pydevproject-0.1.zip
  md5: b9a5054aac88a37dff7e4c01450ff456
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.pydevproject = collective.recipe.pydevproject:main
    #
    # Would create an entry point called collective.recipe.pydevproject that calls collective.recipe.pydevproject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.pydevproject
    - collective.recipe.pydevproject.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.recipe.pydevproject
  license: GNU General Public License (GPL)
  summary: 'zc.buildout recipe that creates an Eclipse PyDev Project config with PYTHONPATH pointing to some eggs and their dependencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
