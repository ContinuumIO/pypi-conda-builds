package:
  name: webkit2png
  version: "0.8.2"

source:
  fn: webkit2png-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/w/webkit2png/webkit2png-0.8.2.tar.gz
  md5: 2b3009ffb6a334a42197e9ceb7031fea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - webkit2png = webkit2png:main
    #
    # Would create an entry point called webkit2png that calls webkit2png.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - webkit2png

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/AdamN/python-webkit2png
  license: GNU General Public License (GPL)
  summary: 'Takes snapshot of webpages using Webkit and Qt4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
