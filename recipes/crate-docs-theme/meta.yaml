package:
  name: crate-docs-theme
  version: "0.3.8"

source:
  fn: crate-docs-theme-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/c/crate-docs-theme/crate-docs-theme-0.3.8.tar.gz
  md5: 094679171587f99dd961453e6dda5200
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crate-docs-theme = crate-docs-theme:main
    #
    # Would create an entry point called crate-docs-theme that calls crate-docs-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx
    - sphinxcontrib-plantuml

  run:
    - python
    - setuptools
    - sphinx
    - sphinxcontrib-plantuml

test:
  # Python imports
  imports:
    - crate
    - crate.theme
    - crate.theme.rtd
    - crate.theme.rtd.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/crate/crate-docs-theme
  license: Apache Software License
  summary: 'Crate Sphinx Theme for ReadTheDocs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
