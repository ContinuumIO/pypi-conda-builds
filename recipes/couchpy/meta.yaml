package:
  name: couchpy
  version: "0.2dev"

source:
  fn: couchpy-0.2dev.tar.gz
  url: https://pypi.python.org/packages/source/c/couchpy/couchpy-0.2dev.tar.gz
  md5: 760f5b95f33708f679fcbb03d6d50fc4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchpy = couchpy:main
    #
    # Would create an entry point called couchpy that calls couchpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments

  run:
    - python
    - pygments

test:
  # Python imports
  imports:
    - couchpy
    - couchpy.db
    - docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://couchpy.pluggdapps.com
  license: BSD License
  summary: 'Data modeling for CouchDB database management systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
