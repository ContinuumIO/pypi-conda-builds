package:
  name: oauth-python-twitter
  version: "1.0"

source:
  fn: oauth-python-twitter-1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oauth-python-twitter/oauth-python-twitter-1.0.tar.gz
  md5: 0c5cf7a70aa1ce67a7b2d8ba7291d160
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oauth-python-twitter = oauth-python-twitter:main
    #
    # Would create an entry point called oauth-python-twitter that calls oauth-python-twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/oauth-python-twitter/
  license: Apache
  summary: 'OAuth implementation for python-twitter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
