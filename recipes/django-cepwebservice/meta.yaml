package:
  name: django-cepwebservice
  version: "0.0.2"

source:
  fn: django-cepwebservice-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cepwebservice/django-cepwebservice-0.0.2.tar.gz
  md5: cd0023dcf614e45633ed61e2793287b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cepwebservice = django-cepwebservice:main
    #
    # Would create an entry point called django-cepwebservice that calls django-cepwebservice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cepwebservice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adonescunha/django-cepwebservice
  license: MIT License
  summary: 'WebService de CEP para Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
