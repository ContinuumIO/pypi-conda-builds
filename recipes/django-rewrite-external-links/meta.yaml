package:
  name: django-rewrite-external-links
  version: "0.4.3"

source:
  fn: django-rewrite-external-links-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rewrite-external-links/django-rewrite-external-links-0.4.3.tar.gz
  md5: a9e3e58d947219d00b91c1ae3bc6aeab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rewrite-external-links = django-rewrite-external-links:main
    #
    # Would create an entry point called django-rewrite-external-links that calls django-rewrite-external-links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rewrite_external_links

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/django-rewrite-external-links
  license: UNKNOWN
  summary: 'Rewrite all external (off-site) links to go via a message page, using a middleware class.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
