package:
  name: cubicweb-card
  version: "0.5.5"

source:
  fn: cubicweb-card-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-card/cubicweb-card-0.5.5.tar.gz
  md5: 701f7a29b934b5156b97227321146c3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-card = cubicweb-card:main
    #
    # Would create an entry point called cubicweb-card that calls cubicweb-card.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.0
    - docutils
    - cubicweb-seo
    - cubicweb-preview

  run:
    - python
    - cubicweb >=3.16.0
    - docutils
    - cubicweb-seo
    - cubicweb-preview

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-card
  license: LGPL
  summary: 'card/wiki component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
