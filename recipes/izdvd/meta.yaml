package:
  name: izdvd
  version: "0.1.2"

source:
  fn: izdvd-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/izdvd/izdvd-0.1.2.tar.gz
  md5: 35965d3455290b64f466e68d84083bcd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - izdvd = izdvd:main
    #
    # Would create an entry point called izdvd that calls izdvd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - izdvd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/izzilly/izdvd
  license: BSD License
  summary: 'A set of python scripts for authoring DVDs and/or DVD menus with little or no user interaction.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
