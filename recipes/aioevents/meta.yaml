package:
  name: aioevents
  version: "0.1"

source:
  fn: aioevents-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aioevents/aioevents-0.1.tar.gz
  md5: aad4e2adc8b846254826151cd88a8da1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aioevents = aioevents:main
    #
    # Would create an entry point called aioevents that calls aioevents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - asyncio

  run:
    - python
    - asyncio

test:
  # Python imports
  imports:
    - aioevents

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/astronouth7303/aioevents
  license: MIT License
  summary: 'Events for asyncio (PEP 3156)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
