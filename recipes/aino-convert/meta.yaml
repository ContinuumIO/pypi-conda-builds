package:
  name: aino-convert
  version: "0.1.0.14"

source:
  fn: aino-convert-0.1.0.14.tar.gz
  url: https://pypi.python.org/packages/source/a/aino-convert/aino-convert-0.1.0.14.tar.gz
  md5: f9251f2f601d41c5d58a6c83d6cfaf15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aino-convert = aino-convert:main
    #
    # Would create an entry point called aino-convert that calls aino-convert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - convert
    - convert.conf
    - convert.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/aino/aino-convert/
  license: BSD License
  summary: 'Magick for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
