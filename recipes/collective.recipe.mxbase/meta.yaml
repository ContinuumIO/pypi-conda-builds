package:
  name: collective.recipe.mxbase
  version: "0.1"

source:
  fn: collective.recipe.mxbase-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.mxbase/collective.recipe.mxbase-0.1.tar.gz
  md5: ff669b48ce45598638818bdae522a749
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.mxbase = collective.recipe.mxbase:main
    #
    # Would create an entry point called collective.recipe.mxbase that calls collective.recipe.mxbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.mxbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/collective.recipe.mxbase
  license: GNU General Public License (GPL)
  summary: 'A buildout recipe to install eGenix mx.base (mx.DateTime, mx.TextTools...)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
