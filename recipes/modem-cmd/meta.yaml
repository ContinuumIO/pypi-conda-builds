package:
  name: modem-cmd
  version: "0.0.1"

source:
  fn: modem-cmd-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/modem-cmd/modem-cmd-0.0.1.tar.gz
  md5: 892cd320156665cdecb270c21193b752
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modem-cmd = modem-cmd:main
    #
    # Would create an entry point called modem-cmd that calls modem-cmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial >=2.6

  run:
    - python
    - pyserial >=2.6

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imZack/modem-cmd
  license: MIT License
  summary: 'Send arbitrary AT commands to your modem'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
