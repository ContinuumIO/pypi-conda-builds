package:
  name: django-tastypie-crust
  version: "0.2"

source:
  fn: django-tastypie-crust-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tastypie-crust/django-tastypie-crust-0.2.tar.gz
  md5: afdd0f4cd18261ab8739aeffe55cfac6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tastypie-crust = django-tastypie-crust:main
    #
    # Would create an entry point called django-tastypie-crust that calls django-tastypie-crust.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-tastypie

  run:
    - python
    - django-tastypie

test:
  # Python imports
  imports:
    - tastycrust

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/uranusjr/django-tastypie-crust
  license: BSD License
  summary: 'Goodies for Tastypie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
