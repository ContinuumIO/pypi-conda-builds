package:
  name: jsb.recipes.jsonbot
  version: "0.1a1"

source:
  fn: jsb.recipes.jsonbot-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsb.recipes.jsonbot/jsb.recipes.jsonbot-0.1a1.tar.gz
  md5: eefe1025f653e6b9bfe1479baf1d3061
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsb.recipes.jsonbot = jsb.recipes.jsonbot:main
    #
    # Would create an entry point called jsb.recipes.jsonbot that calls jsb.recipes.jsonbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout >=1.4.3
    - z3c.recipe.scripts

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout >=1.4.3
    - z3c.recipe.scripts

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jsonbot.googlecode.com/
  license: MIT License
  summary: 'The bot for you!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
