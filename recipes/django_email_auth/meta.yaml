package:
  name: django_email_auth
  version: "0.1.5"

source:
  fn: django_email_auth-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django_email_auth/django_email_auth-0.1.5.tar.gz
  md5: ee5b7b0237cab18de354d651d1492c7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_email_auth = django_email_auth:main
    #
    # Would create an entry point called django_email_auth that calls django_email_auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_email_auth
    - email_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jcbagneris/django_email_auth
  license: BSD License
  summary: 'Yet another email auth system for the Django web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
