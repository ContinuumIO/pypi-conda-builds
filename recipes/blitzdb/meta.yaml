package:
  name: blitzdb
  version: "0.2.12"

source:
  fn: blitzdb-0.2.12.tar.gz
  url: https://pypi.python.org/packages/source/b/blitzdb/blitzdb-0.2.12.tar.gz
  md5: 49e06ccbe85f17f1ec7e6943e88d087d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blitzdb = blitzdb:main
    #
    # Would create an entry point called blitzdb that calls blitzdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - blitzdb
    - blitzdb.backends
    - blitzdb.backends.file
    - blitzdb.backends.mongo
    - blitzdb.backends.sql
    - blitzdb.tests
    - blitzdb.tests.helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adewes/blitzdb
  license: MIT
  summary: 'A document-oriented database written purely in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
