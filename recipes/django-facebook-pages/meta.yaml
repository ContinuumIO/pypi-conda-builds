package:
  name: django-facebook-pages
  version: "0.4.0"

source:
  fn: django-facebook-pages-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-pages/django-facebook-pages-0.4.0.tar.gz
  md5: cd3391cb025ac3154b6dab4a01011dd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-pages = django-facebook-pages:main
    #
    # Would create an entry point called django-facebook-pages that calls django-facebook-pages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-facebook-api >=0.4.0

  run:
    - python
    - django-facebook-api >=0.4.0

test:
  # Python imports
  imports:
    - facebook_pages
    - facebook_pages.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-facebook-pages
  license: BSD License
  summary: 'Django implementation for Facebook Graph API Pages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
