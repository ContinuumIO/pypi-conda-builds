package:
  name: pelican-diminuendo
  version: "0.2.1"

source:
  fn: pelican-diminuendo-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-diminuendo/pelican-diminuendo-0.2.1.tar.gz
  md5: fc042cbf1b2c02baba3349226703aaed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-diminuendo = pelican-diminuendo:main
    #
    # Would create an entry point called pelican-diminuendo that calls pelican-diminuendo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican
    - python-diminuendo

  run:
    - python
    - pelican
    - python-diminuendo

test:
  # Python imports
  imports:
    - pelican_diminuendo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hrbonz/pelican-diminuendo
  license: BSD License
  summary: 'HTML minifier plugin for Pelican'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
