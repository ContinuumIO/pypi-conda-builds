package:
  name: dupes
  version: "0.2.0"

source:
  fn: dupes-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dupes/dupes-0.2.0.tar.gz
  md5: f5226a91f004de36ed17fd176fa80a15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dupes = dupes:main
    #
    # Would create an entry point called dupes that calls dupes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - click

  run:
    - python
    - click

test:
  # Python imports
  imports:
    - dupes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dwwkelly/dupes
  license: GNU General Public License v2 (GPLv2)
  summary: 'Identify duplicate files (by SHA1) and remove them if needed'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
