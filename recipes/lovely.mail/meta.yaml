package:
  name: lovely.mail
  version: "0.3.1"

source:
  fn: lovely.mail-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.mail/lovely.mail-0.3.1.tar.gz
  md5: 2ce646c0f0b304f64fe78b1735f9b66c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.mail = lovely.mail:main
    #
    # Would create an entry point called lovely.mail that calls lovely.mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.schema
    - zope.sendmail
    - lovely.remotetask

  run:
    - python
    - setuptools
    - zope.component
    - zope.schema
    - zope.sendmail
    - lovely.remotetask

test:
  # Python imports
  imports:
    - lovely
    - lovely.mail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/lovely.mail
  license: Zope Public License
  summary: 'sending emails via remotetask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
