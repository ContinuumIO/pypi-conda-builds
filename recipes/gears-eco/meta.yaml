package:
  name: gears-eco
  version: "0.1"

source:
  fn: gears-eco-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gears-eco/gears-eco-0.1.tar.gz
  md5: 7d67055d3da2f3bd510d5ed4e4ecd119
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gears-eco = gears-eco:main
    #
    # Would create an entry point called gears-eco that calls gears-eco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gears_eco

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ISC License (ISCL)
  summary: 'Eco: Embedded CoffeeScript templates for Gears'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
