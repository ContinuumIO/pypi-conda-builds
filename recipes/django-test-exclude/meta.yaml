package:
  name: django-test-exclude
  version: "0.1.0"

source:
  fn: django-test-exclude-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-test-exclude/django-test-exclude-0.1.0.tar.gz
  md5: 5f8f5dfd9998180bd8f118da91b4e54b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-test-exclude = django-test-exclude:main
    #
    # Would create an entry point called django-test-exclude that calls django-test-exclude.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_test_exclude

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Easy application excludes for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
