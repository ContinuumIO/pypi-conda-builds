package:
  name: hurry.extjs
  version: "2.2.1"

source:
  fn: hurry.extjs-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.extjs/hurry.extjs-2.2.1.tar.gz
  md5: 32cb806d341b125b42838414b3163760
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.extjs = hurry.extjs:main
    #
    # Would create an entry point called hurry.extjs that calls hurry.extjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hurry.resource

  run:
    - python
    - setuptools
    - hurry.resource

test:
  # Python imports
  imports:
    - hurry
    - hurry.extjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'ExtJS for hurry.resource.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
