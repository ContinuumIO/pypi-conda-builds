package:
  name: django-closable_admin_filter
  version: "0.1.0"

source:
  fn: django-closable_admin_filter-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-closable_admin_filter/django-closable_admin_filter-0.1.0.tar.gz
  md5: 70366bc2756fd250688d170f86c73c4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-closable_admin_filter = django-closable_admin_filter:main
    #
    # Would create an entry point called django-closable_admin_filter that calls django-closable_admin_filter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - closable_admin_filter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eternalfame/django-closable_admin_filter
  license: MIT License
  summary: 'A simple Django app to add hide/show ability to the filter in Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
