package:
  name: burrahobbit
  version: "0.1.1"

source:
  fn: burrahobbit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/burrahobbit/burrahobbit-0.1.1.tar.gz
  md5: 35d8d7793223fff73490d84d0078e144
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - burrahobbit = burrahobbit:main
    #
    # Would create an entry point called burrahobbit that calls burrahobbit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - burrahobbit
    - burrahobbit.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://segfaulthunter.github.com/burrahobbit
  license: MIT
  summary: 'Persistent data-structures in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
