package:
  name: kolekto
  version: "1.3"

source:
  fn: kolekto-1.3.tar.gz
  url: https://pypi.python.org/packages/source/k/kolekto/kolekto-1.3.tar.gz
  md5: 7c3060db2897f9c8370497ec5a477375
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kolekto = kolekto:main
    #
    # Would create an entry point called kolekto that calls kolekto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - confiture
    - kaa-metadata
    - progressbar
    - requests
    - lxml

  run:
    - python
    - setuptools
    - confiture
    - kaa-metadata
    - progressbar
    - requests
    - lxml

test:
  # Python imports
  imports:
    - kolekto
    - kolekto.commands
    - kolekto.datasources
    - kolekto.profiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/NaPs/Kolekto
  license: MIT License
  summary: 'A really KISS movie catalog software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
