package:
  name: asv_media
  version: "dev-20121101-03"

source:
  fn: asv_media-dev-20121101-03.tar.gz
  url: https://pypi.python.org/packages/source/a/asv_media/asv_media-dev-20121101-03.tar.gz
  md5: c9a9c2c9f049132297aa9276061b7929
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asv_media = asv_media:main
    #
    # Would create an entry point called asv_media that calls asv_media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - asv_utils >=dev-20121101-01

  run:
    - python
    - asv_utils >=dev-20121101-01

test:
  # Python imports
  imports:
    - asv_media
    - asv_media.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/xenolog/asv_media/wiki/Home
  license: GPL
  summary: '\nThis is a set of media files (js libs, css, pictures) for other asv.* libs.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
