package:
  name: django-central-message
  version: "0.1.3"

source:
  fn: django-central-message-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-central-message/django-central-message-0.1.3.tar.gz
  md5: ad1168133dae0162b4e07ad2589784a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-central-message = django-central-message:main
    #
    # Would create an entry point called django-central-message that calls django-central-message.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pytz >=2014.2
    - django-messages-extends >=0.2

  run:
    - python
    - django
    - pytz >=2014.2
    - django-messages-extends >=0.2

test:
  # Python imports
  imports:
    - central_message
    - central_message.migrations
    - central_message.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Wrapper for extends-messages to send to multiple users (all for now) from the admin page..'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
