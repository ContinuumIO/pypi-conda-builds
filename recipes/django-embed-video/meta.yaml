package:
  name: django-embed-video
  version: "1.0.0"

source:
  fn: django-embed-video-1.0.0.zip
  url: https://pypi.python.org/packages/source/d/django-embed-video/django-embed-video-1.0.0.zip
  md5: 665b6114bae6fd94565f31315a7d8241
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-embed-video = django-embed-video:main
    #
    # Would create an entry point called django-embed-video that calls django-embed-video.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - django >=1.5

  run:
    - python
    - requests >=1.2.3
    - django >=1.5

test:
  # Python imports
  imports:
    - embed_video
    - embed_video.templatetags
    - embed_video.tests
    - embed_video.tests.backends
    - embed_video.tests.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yetty/django-embed-video
  license: MIT License
  summary: 'Django app for easy embeding YouTube and Vimeo videos and music from\nSoundCloud.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
