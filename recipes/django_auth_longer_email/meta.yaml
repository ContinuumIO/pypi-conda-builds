package:
  name: django_auth_longer_email
  version: "1.0.0"

source:
  fn: django_auth_longer_email-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_auth_longer_email/django_auth_longer_email-1.0.0.tar.gz
  md5: 2b252f80d9e12793b67fe736deca192c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_auth_longer_email = django_auth_longer_email:main
    #
    # Would create an entry point called django_auth_longer_email that calls django_auth_longer_email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_auth_longer_email

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mikexstudios/django_auth_longer_email
  license: MIT License
  summary: "Simple django app that patches auth.User's email field to 254 char."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
