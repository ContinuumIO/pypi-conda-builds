package:
  name: django-pagination-py3
  version: "1.1.1"

source:
  fn: django-pagination-py3-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pagination-py3/django-pagination-py3-1.1.1.tar.gz
  md5: 53988bf7b62635c79cdb8af7b816e444
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pagination-py3 = django-pagination-py3:main
    #
    # Would create an entry point called django-pagination-py3 that calls django-pagination-py3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pagination
    - pagination.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'django-pagination-py3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
