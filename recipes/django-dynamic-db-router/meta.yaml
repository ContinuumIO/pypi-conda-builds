package:
  name: django-dynamic-db-router
  version: "0.1.1"

source:
  fn: django-dynamic-db-router-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamic-db-router/django-dynamic-db-router-0.1.1.tar.gz
  md5: 8130fca45c30a9014332da6ce7d4c006
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-db-router = django-dynamic-db-router:main
    #
    # Would create an entry point called django-dynamic-db-router that calls django-dynamic-db-router.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - dynamic_db_router
    - dynamic_db_router.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-dynamic-db-router
  license: MIT License
  summary: 'Simply route complex django queries to multiple databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
