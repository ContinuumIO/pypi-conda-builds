package:
  name: ckanext-nationdata
  version: "0.1"

source:
  fn: ckanext-nationdata-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-nationdata/ckanext-nationdata-0.1.tar.gz
  md5: 3c9f332aeaf2b623e494fb7c5f5da9d5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-nationdata = ckanext-nationdata:main
    #
    # Would create an entry point called ckanext-nationdata that calls ckanext-nationdata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ckanext
    - ckanext.nationdata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.afrigeo.com
  license: GPL 2
  summary: 'Ckan Extension for the Nation Media Group data portal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
