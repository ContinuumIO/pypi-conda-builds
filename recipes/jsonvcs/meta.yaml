package:
  name: jsonvcs
  version: "0.0.1"

source:
  fn: jsonvcs-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonvcs/jsonvcs-0.0.1.tar.gz
  md5: f289061abdd0e02a94a45ecfa18e8921
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonvcs = jsonvcs:main
    #
    # Would create an entry point called jsonvcs that calls jsonvcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonpatch >=0.11

  run:
    - python
    - jsonpatch >=0.11

test:
  # Python imports
  imports:
    - jsonvcs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/slang800/jsonvcs
  license: LICENSE
  summary: 'version control for structured documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
