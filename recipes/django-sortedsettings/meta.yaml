package:
  name: django-sortedsettings
  version: "1.0"

source:
  fn: django-sortedsettings-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sortedsettings/django-sortedsettings-1.0.tar.gz
  md5: def3db165243d29c2f7cedba517acf90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sortedsettings = django-sortedsettings:main
    #
    # Would create an entry point called django-sortedsettings that calls django-sortedsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/specialunderwear/django-sortedsettings
  license: GNU General Public License (GPL)
  summary: "Sort the django settings module's entries alphabetically"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
