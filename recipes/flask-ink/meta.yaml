package:
  name: flask-ink
  version: "2.2.1"

source:
  fn: Flask-Ink-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Ink/Flask-Ink-2.2.1.tar.gz
  md5: 66449207d45f75e8fffa7f4d0150e0b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-ink = flask-ink:main
    #
    # Would create an entry point called flask-ink that calls flask-ink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1

  run:
    - python
    - flask >=0.10.1

test:
  # Python imports
  imports:
    - flask_ink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/diogoosorio/flask-sapo-ink
  license: MIT
  summary: "Easily integrate Sapo Ink's framework in your Flask project."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
