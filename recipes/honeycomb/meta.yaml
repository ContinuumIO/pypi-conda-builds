package:
  name: honeycomb
  version: "0.2.3"

source:
  fn: honeycomb-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/h/honeycomb/honeycomb-0.2.3.tar.gz
  md5: 38aba9133a70ae1802bfdf3a563001aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - honeycomb = honeycomb:main
    #
    # Would create an entry point called honeycomb that calls honeycomb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - honeycomb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'a tiny and smart redis data structure wrapper of honey project based on Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
