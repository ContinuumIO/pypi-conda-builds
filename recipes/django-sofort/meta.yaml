package:
  name: django-sofort
  version: "0.0.1"

source:
  fn: django-sofort-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sofort/django-sofort-0.0.1.tar.gz
  md5: 5f8c64040dc1c2317d8f2e5c13b1299e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sofort = django-sofort:main
    #
    # Would create an entry point called django-sofort that calls django-sofort.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - dict2xml >=1.1
    - py-moneyed >=0.5
    - beautifulsoup4 >=4.0
    - python-dateutil >=2.2
    - django-appconf >=0.6

  run:
    - python
    - django >=1.3.1
    - dict2xml >=1.1
    - py-moneyed >=0.5
    - beautifulsoup4 >=4.0
    - python-dateutil >=2.2
    - django-appconf >=0.6

test:
  # Python imports
  imports:
    - sofort

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codingjoe/django-sofort
  license: MIT License
  summary: 'Django implementation of the Sofort.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
