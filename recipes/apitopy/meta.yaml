package:
  name: apitopy
  version: "0.3.0"

source:
  fn: apitopy-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/apitopy/apitopy-0.3.0.tar.gz
  md5: af66d903a5446b90715d81c405a4c5bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apitopy = apitopy:main
    #
    # Would create an entry point called apitopy that calls apitopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - supermutes

  run:
    - python
    - requests
    - supermutes

test:
  # Python imports
  imports:
    - apitopy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/txels/apitopy
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Pythonic access to HTTP APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
