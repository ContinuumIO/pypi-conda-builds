package:
  name: django-supervisor
  version: "0.3.2"

source:
  fn: django-supervisor-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-supervisor/django-supervisor-0.3.2.tar.gz
  md5: 820fd67123658fec56835730c2ff32fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-supervisor = django-supervisor:main
    #
    # Would create an entry point called django-supervisor that calls django-supervisor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - supervisor
    - watchdog

  run:
    - python
    - supervisor
    - watchdog

test:
  # Python imports
  imports:
    - djsupervisor
    - djsupervisor.management
    - djsupervisor.management.commands
    - djsupervisor.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rfk/django-supervisor
  license: MIT License
  summary: 'easy integration between djangocl and supervisord'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
