package:
  name: cubicweb-comment
  version: "1.11.1"

source:
  fn: cubicweb-comment-1.11.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-comment/cubicweb-comment-1.11.1.tar.gz
  md5: 6f88ca6e8ad3b9f4edf9cb6bd31db9ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-comment = cubicweb-comment:main
    #
    # Would create an entry point called cubicweb-comment that calls cubicweb-comment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0

  run:
    - python
    - cubicweb >=3.19.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-comment
  license: LGPL
  summary: 'commenting system for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
