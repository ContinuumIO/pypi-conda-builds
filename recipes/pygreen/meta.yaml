package:
  name: pygreen
  version: "2.0.0"

source:
  fn: pygreen-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pygreen/pygreen-2.0.0.tar.gz
  md5: 981e68122835a754be35a0f22da3696d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygreen = pygreen:main
    #
    # Would create an entry point called pygreen that calls pygreen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - mako >=0.8.0
    - argparse
    - markdown

  run:
    - python
    - flask >=0.10.1
    - mako >=0.8.0
    - argparse
    - markdown

test:
  # Python imports
  imports:
    - pygreen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pygreen.neoname.eu
  license: MIT
  summary: 'PyGreen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
