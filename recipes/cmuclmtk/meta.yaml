package:
  name: cmuclmtk
  version: "0.1.5"

source:
  fn: cmuclmtk-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cmuclmtk/cmuclmtk-0.1.5.tar.gz
  md5: 87a2e2bf1f08a51509df558d7ad72def
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmuclmtk = cmuclmtk:main
    #
    # Would create an entry point called cmuclmtk that calls cmuclmtk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmuclmtk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Holzhaus/python-cmuclmtk
  license: BSD
  summary: 'Wrapper library for accessing the language model tools for CMU Sphinx (CMUCLMTK).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
