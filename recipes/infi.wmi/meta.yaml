package:
  name: infi.wmi
  version: "0.1.27"

source:
  fn: infi.wmi-0.1.27.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.wmi/infi.wmi-0.1.27.tar.gz
  md5: 51da385d0560f6535b65b5fd9de3078c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.wmi = infi.wmi:main
    #
    # Would create an entry point called infi.wmi that calls infi.wmi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - comtypes
    - setuptools

  run:
    - python
    - comtypes
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.wmi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.wmi
  license: Python Software Foundation License
  summary: 'monkeypatched fork of comptypes to support faster WMI access'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
