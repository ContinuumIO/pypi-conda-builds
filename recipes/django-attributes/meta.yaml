package:
  name: django-attributes
  version: "0.2.0"

source:
  fn: django-attributes-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-attributes/django-attributes-0.2.0.tar.gz
  md5: 7a98f3eb5d752b87e391b06cfb7be6ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-attributes = django-attributes:main
    #
    # Would create an entry point called django-attributes that calls django-attributes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - attributes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-attributes
  license: BSD License
  summary: 'A reusable applicaton to add arbitrary attributes to a model. Idea yoinked from Satchmo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
