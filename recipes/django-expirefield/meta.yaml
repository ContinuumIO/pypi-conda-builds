package:
  name: django-expirefield
  version: "0.0.2"

source:
  fn: django-expirefield-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-expirefield/django-expirefield-0.0.2.tar.gz
  md5: edca92caf15d69a824fb7a0a029063b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-expirefield = django-expirefield:main
    #
    # Would create an entry point called django-expirefield that calls django-expirefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - expirefield
    - expirefield.management
    - expirefield.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/malloc47/django-expirefield
  license: LICENSE.txt
  summary: 'ExpireField for django models that will remove fields at regular intervals'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
