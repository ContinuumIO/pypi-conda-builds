package:
  name: asset_bundles
  version: "0.1.3"

source:
  fn: asset_bundles-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/asset_bundles/asset_bundles-0.1.3.tar.gz
  md5: 1549fc6646cf48169344d05f5027d975
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asset_bundles = asset_bundles:main
    #
    # Would create an entry point called asset_bundles that calls asset_bundles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - asset_bundles
    - asset_bundles.management
    - asset_bundles.management.commands
    - asset_bundles.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'simple clone of django_assets compatible with django 1.3 and staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
