package:
  name: geobricksgeocoding
  version: "0.1.3"

source:
  fn: GeobricksGeocoding-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksGeocoding/GeobricksGeocoding-0.1.3.tar.gz
  md5: 1e132636e08a6e759a1fa279ebe6172b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobricksgeocoding = geobricksgeocoding:main
    #
    # Would create an entry point called geobricksgeocoding that calls geobricksgeocoding.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - watchdog
    - flask
    - flask-cors
    - geopy

  run:
    - python
    - watchdog
    - flask
    - flask-cors
    - geopy

test:
  # Python imports
  imports:
    - geobricks_geocoding
    - geobricks_geocoding.config
    - geobricks_geocoding.core
    - geobricks_geocoding.rest
    - geobricks_geocoding.utils
    - geobricksgeocoding

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksGeocoding/
  license: LICENSE.txt
  summary: 'Geobricks library to manage geocoding.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
