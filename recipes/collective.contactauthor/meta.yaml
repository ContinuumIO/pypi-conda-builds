package:
  name: collective.contactauthor
  version: "1.1.0"

source:
  fn: collective.contactauthor-1.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.contactauthor/collective.contactauthor-1.1.0.zip
  md5: 38bec508927185961ebb5950fb42432d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contactauthor = collective.contactauthor:main
    #
    # Would create an entry point called collective.contactauthor that calls collective.contactauthor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.recaptcha
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - collective.recaptcha
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.contactauthor
    - collective.contactauthor.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.contactauthor
  license: GPL
  summary: 'A customization for Plone author form: anonymous users can send message to authors with captcha protection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
