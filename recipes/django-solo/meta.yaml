package:
  name: django-solo
  version: "1.1.0"

source:
  fn: django-solo-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-solo/django-solo-1.1.0.tar.gz
  md5: 079c0f0bc3d8a495114ef4d76179c4a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-solo = django-solo:main
    #
    # Would create an entry point called django-solo that calls django-solo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - solo
    - solo.templatetags
    - solo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lazybird/django-solo/
  license: Creative Commons Attribution 3.0 Unported
  summary: 'django-solo helps working with singletons: things like global settings that you want to edit from the admin site.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
