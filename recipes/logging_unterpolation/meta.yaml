package:
  name: logging_unterpolation
  version: "0.2.0"

source:
  fn: logging_unterpolation-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logging_unterpolation/logging_unterpolation-0.2.0.tar.gz
  md5: d0d4de0763be1a1cf044e7ddcce8e1a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logging_unterpolation = logging_unterpolation:main
    #
    # Would create an entry point called logging_unterpolation that calls logging_unterpolation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logging_unterpolation
    - logging_unterpolation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/rdennis463/logging_unterpolation
  license: MIT License
  summary: 'patch logging module to accept PEP-3101 formatting syntax'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
