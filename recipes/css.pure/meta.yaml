package:
  name: css.pure
  version: "0.2.1"

source:
  fn: css.pure-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/css.pure/css.pure-0.2.1.tar.gz
  md5: 341b8c10e32bc13e67cd82b50609671e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - css.pure = css.pure:main
    #
    # Would create an entry point called css.pure that calls css.pure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - css
    - css.pure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fanstatic/js.jquery
  license: BSD
  summary: 'Pure CSS for Fanstatic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
