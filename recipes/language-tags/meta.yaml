package:
  name: language-tags
  version: "0.3.1"

source:
  fn: language_tags-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/l/language-tags/language_tags-0.3.1.tar.gz
  md5: 098ed48bcdaba80a14bed602fcba100e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - language-tags = language-tags:main
    #
    # Would create an entry point called language-tags that calls language-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - language_tags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OnroerendErfgoed/language-tags
  license: MIT License
  summary: 'This project is a Python version of the language-tags Javascript project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
