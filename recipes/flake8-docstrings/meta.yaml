package:
  name: flake8-docstrings
  version: "0.2.1.post1"

source:
  fn: flake8-docstrings-0.2.1.post1.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-docstrings/flake8-docstrings-0.2.1.post1.tar.gz
  md5: 66ac2e0ce7c40870d528fa2bff4b6ef6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-docstrings = flake8-docstrings:main
    #
    # Would create an entry point called flake8-docstrings that calls flake8-docstrings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8
    - pep257

  run:
    - python
    - setuptools
    - flake8
    - pep257

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/pycqa/flake8-docstrings
  license: MIT License
  summary: 'Extension for flake8 which uses pep257 to check docstrings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
