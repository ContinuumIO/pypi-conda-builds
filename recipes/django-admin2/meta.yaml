package:
  name: django-admin2
  version: "0.6.1"

source:
  fn: django-admin2-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin2/django-admin2-0.6.1.tar.gz
  md5: 893393fc624b8a18558a1875f3927f5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin2 = django-admin2:main
    #
    # Would create an entry point called django-admin2 that calls django-admin2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0
    - django-braces >=1.0.0
    - django-extra-views >=0.6.2
    - djangorestframework >=2.3.3
    - django-floppyforms >=1.1
    - django-filter >=0.6
    - django-crispy-forms >=1.3.2

  run:
    - python
    - django >=1.5.0
    - django-braces >=1.0.0
    - django-extra-views >=0.6.2
    - djangorestframework >=2.3.3
    - django-floppyforms >=1.1
    - django-filter >=0.6
    - django-crispy-forms >=1.3.2

test:
  # Python imports
  imports:
    - djadmin2
    - djadmin2/templatetags
    - djadmin2/tests
    - djadmin2/themes
    - djadmin2/themes/djadmin2theme_default

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://djangoadmin.org
  license: BSD License
  summary: "An introspective interface for Django's ORM."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
