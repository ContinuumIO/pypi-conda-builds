package:
  name: diagnostics
  version: "0.2.4"

source:
  fn: diagnostics-0.2.4.zip
  url: https://pypi.python.org/packages/source/d/diagnostics/diagnostics-0.2.4.zip
  md5: 328ebcb606868877883d4d01194b9ce9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diagnostics = diagnostics:main
    #
    # Would create an entry point called diagnostics that calls diagnostics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - diagnostics
    - diagnostics.formatters
    - diagnostics.logging
    - diagnostics.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/miso-belica/diagnostics
  license: Apache Software License
  summary: "Alternative to Python's module `cgitb` with template inspired by Nette and Django"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
