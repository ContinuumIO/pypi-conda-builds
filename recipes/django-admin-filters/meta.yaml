package:
  name: django-admin-filters
  version: "0.1.3"

source:
  fn: django-admin-filters-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-filters/django-admin-filters-0.1.3.tar.gz
  md5: 2b8b2ea48b05a39493d43c2923a31381
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-filters = django-admin-filters:main
    #
    # Would create an entry point called django-admin-filters that calls django-admin-filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adminfilters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-admin-filters
  license: UNKNOWN
  summary: 'Django Admin Filters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
