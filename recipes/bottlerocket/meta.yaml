package:
  name: bottlerocket
  version: "0.5.2"

source:
  fn: bottlerocket-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bottlerocket/bottlerocket-0.5.2.tar.gz
  md5: b9350b978a16043942096b4400dfd090
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottlerocket = bottlerocket:main
    #
    # Would create an entry point called bottlerocket that calls bottlerocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pystaggregator >=0.3.1
    - bottle >=0.12.0

  run:
    - python
    - pystaggregator >=0.3.1
    - bottle >=0.12.0

test:
  # Python imports
  imports:
    - bottlerocket
    - bottlerocket.instrumentation
    - bottlerocket.patch
    - bottlerocket.startup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robtandy/bottlerocket
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
