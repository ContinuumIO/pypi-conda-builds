package:
  name: calflate
  version: "0.4.1"

source:
  fn: calflate-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/calflate/calflate-0.4.1.tar.gz
  md5: 13b899191a9c6bcc3e32045c872195da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - calflate = calflate:main
    #
    # Would create an entry point called calflate that calls calflate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - calflate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chripo/calflate/
  license: BSD revised
  summary: 'import external CalDAV and CardDAV entries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
