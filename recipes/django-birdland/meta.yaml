package:
  name: django-birdland
  version: "0.1a1"

source:
  fn: django-birdland-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-birdland/django-birdland-0.1a1.tar.gz
  md5: 5118a61a193a0ab0145dfd0342406381
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-birdland = django-birdland:main
    #
    # Would create an entry point called django-birdland that calls django-birdland.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - birdland
    - birdland.templatetags
    - birdland.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jeffkistler/django-backlinks
  license: BSD License
  summary: 'A simple blog app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
