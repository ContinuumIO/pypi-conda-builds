package:
  name: hanabirc
  version: "1.2.05"

source:
  fn: HanabIRC-1.2.05.tar.gz
  url: https://pypi.python.org/packages/source/H/HanabIRC/HanabIRC-1.2.05.tar.gz
  md5: 6d7b06ae4ee7cd066d2a44c65da573fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hanabirc = hanabirc:main
    #
    # Would create an entry point called hanabirc that calls hanabirc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - irc
    - pyyaml

  run:
    - python
    - irc
    - pyyaml

test:
  # Python imports
  imports:
    - hanabIRC
    - hanabirc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/philsstein/hanabIRC
  license: FreeBSD License
  summary: 'An IRC bot that organizes and plays the card game Hanabi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
