package:
  name: pelican-autostatic
  version: "0.2.2"

source:
  fn: pelican-autostatic-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-autostatic/pelican-autostatic-0.2.2.tar.gz
  md5: 561677abbd9cdb8374c5e9aa4eff941c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-autostatic = pelican-autostatic:main
    #
    # Would create an entry point called pelican-autostatic that calls pelican-autostatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican >=3.4.0

  run:
    - python
    - pelican >=3.4.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AlexJF/pelican-autostatic
  license: Apache
  summary: 'A generator for Pelican allowing flexible referencing of static content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
