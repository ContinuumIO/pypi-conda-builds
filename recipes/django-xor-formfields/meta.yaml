package:
  name: django-xor-formfields
  version: "0.0.4"

source:
  fn: django-xor-formfields-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xor-formfields/django-xor-formfields-0.0.4.tar.gz
  md5: 8737c092f2e19f2d2129d69ec8b4ab5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xor-formfields = django-xor-formfields:main
    #
    # Would create an entry point called django-xor-formfields that calls django-xor-formfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - requests

  run:
    - python
    - django
    - requests

test:
  # Python imports
  imports:
    - xorformfields
    - xorformfields.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dschep/django-mutuallyexclusive-formfields
  license: MIT License
  summary: 'Mutually Exclusive form field wigets for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
