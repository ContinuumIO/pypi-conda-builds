package:
  name: collect-exceptions
  version: "0.0.5"

source:
  fn: collect-exceptions-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collect-exceptions/collect-exceptions-0.0.5.tar.gz
  md5: eb5c0a4417a86effebec1b70a67a7c1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collect-exceptions = collect-exceptions:main
    #
    # Would create an entry point called collect-exceptions that calls collect-exceptions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - collect_exceptions
    - collect_exceptions.contrib
    - collect_exceptions.contrib.celery
    - collect_exceptions.contrib.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yemsheng/collect-exceptions
  license: UNKNOWN
  summary: 'python exception collector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
