package:
  name: django-batchform
  version: "0.2.3"

source:
  fn: django-batchform-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-batchform/django-batchform-0.2.3.tar.gz
  md5: 2befa1275fdf2ccd705f1e63370f0f65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-batchform = django-batchform:main
    #
    # Would create an entry point called django-batchform that calls django-batchform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - batchform
    - batchform.parsers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rbarrois/django-batchform
  license: BSD
  summary: 'Fill a batch of django forms from an uploaded file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
