package:
  name: onegov.foundation
  version: "0.0.2"

source:
  fn: onegov.foundation-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/o/onegov.foundation/onegov.foundation-0.0.2.tar.gz
  md5: ed670242b1d22d2fe9861530ea2dd9f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onegov.foundation = onegov.foundation:main
    #
    # Would create an entry point called onegov.foundation that calls onegov.foundation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - csscompressor
    - pyscss

  run:
    - python
    - csscompressor
    - pyscss

test:
  # Python imports
  imports:
    - onegov
    - onegov.foundation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seantis/onegov.foundation
  license: GNU General Public License v2 (GPLv2)
  summary: 'Provides a base for Zurb Foundation themes used in OneGov.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
