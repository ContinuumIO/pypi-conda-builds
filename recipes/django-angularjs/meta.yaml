package:
  name: django-angularjs
  version: "1.0.2"

source:
  fn: django-angularjs-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-angularjs/django-angularjs-1.0.2.tar.gz
  md5: 1852af0dc10845aba818b5e25039e35a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-angularjs = django-angularjs:main
    #
    # Would create an entry point called django-angularjs that calls django-angularjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - angularjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/graingert/django-static-libs/tree/master/angular-1.0.2
  license: UNKNOWN
  summary: 'Angularjs packaged in a handy django app to speed up new applications and reduce duplicaiton.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
