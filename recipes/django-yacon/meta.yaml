package:
  name: django-yacon
  version: "0.7.0"

source:
  fn: django-yacon-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yacon/django-yacon-0.7.0.tar.gz
  md5: fa2b803e030e7c1fc0793d80f0c1236b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yacon = django-yacon:main
    #
    # Would create an entry point called django-yacon that calls django-yacon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - pillow >=2.7
    - bleach >=1.4
    - django-treebeard >=3

  run:
    - python
    - django >=1.7
    - pillow >=2.7
    - bleach >=1.4
    - django-treebeard >=3

test:
  # Python imports
  imports:
    - yacon
    - yacon.examples
    - yacon.management
    - yacon.management.commands
    - yacon.models
    - yacon.pagination
    - yacon.templatetags
    - yacon.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cltrudeau/django-yacon
  license: MIT License
  summary: 'Django based Content Managment building framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
