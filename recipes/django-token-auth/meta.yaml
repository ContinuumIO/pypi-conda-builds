package:
  name: django-token-auth
  version: "1.3.2b4"

source:
  fn: django-token-auth-1.3.2b4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-token-auth/django-token-auth-1.3.2b4.tar.gz
  md5: daafc50ca623140289381332855a6a21
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-token-auth = django-token-auth:main
    #
    # Would create an entry point called django-token-auth that calls django-token-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - token_auth
    - token_auth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mogga/django-token-auth/
  license: BSD License
  summary: 'app that provides limited authentication via hash-type URL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
