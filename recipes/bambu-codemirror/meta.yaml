package:
  name: bambu-codemirror
  version: "0.1"

source:
  fn: bambu-codemirror-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-codemirror/bambu-codemirror-0.1.tar.gz
  md5: 9c25d989f105ef44cfaacacd0fe72a65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-codemirror = bambu-codemirror:main
    #
    # Would create an entry point called bambu-codemirror that calls bambu-codemirror.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bambu.codemirror

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-codemirror
  license: UNKNOWN
  summary: 'Adds static files for the CodeMirror editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
