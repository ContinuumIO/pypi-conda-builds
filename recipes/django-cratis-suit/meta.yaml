package:
  name: django-cratis-suit
  version: "0.1.3"

source:
  fn: django-cratis-suit-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cratis-suit/django-cratis-suit-0.1.3.tar.gz
  md5: 533bd05e8c4efc25e5a2ca99976d557a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cratis-suit = django-cratis-suit:main
    #
    # Would create an entry point called django-cratis-suit that calls django-cratis-suit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-suit
    - django-cratis-base

  run:
    - python
    - django-suit
    - django-cratis-base

test:
  # Python imports
  imports:
    - cratis_admin_suit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: 'Django-suit integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
