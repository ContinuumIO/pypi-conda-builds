package:
  name: importtools
  version: "0.1.5"

source:
  fn: importtools-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/i/importtools/importtools-0.1.5.tar.gz
  md5: fdf825111afe6a401d603cfe5bc139e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importtools = importtools:main
    #
    # Would create an entry point called importtools that calls importtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - importtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pbs/importtools
  license: UNKNOWN
  summary: 'Import tools for various data sets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
