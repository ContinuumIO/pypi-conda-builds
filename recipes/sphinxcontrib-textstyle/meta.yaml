package:
  name: sphinxcontrib-textstyle
  version: "0.2.0"

source:
  fn: sphinxcontrib-textstyle-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-textstyle/sphinxcontrib-textstyle-0.2.0.tar.gz
  md5: e24cdc85bc1353a04aa9e3cf25046b21
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-textstyle = sphinxcontrib-textstyle:main
    #
    # Would create an entry point called sphinxcontrib-textstyle that calls sphinxcontrib-textstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sphinx >=0.6
    - setuptools

  run:
    - python
    - sphinx >=0.6
    - setuptools

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r_rudi/sphinxcontrib-textstyle
  license: BSD License
  summary: 'Sphinx "textstyle" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
