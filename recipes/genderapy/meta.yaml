package:
  name: genderapy
  version: "0.1.1"

source:
  fn: genderapy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/genderapy/genderapy-0.1.1.tar.gz
  md5: e94f776025c09fab91426834a39574d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genderapy = genderapy:main
    #
    # Would create an entry point called genderapy that calls genderapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.0

  run:
    - python
    - requests >=2.4.0

test:
  # Python imports
  imports:
    - genderapy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tttthomasssss/genderapy
  license: MIT License
  summary: 'gender-api.com connector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
