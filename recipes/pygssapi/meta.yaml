package:
  name: pygssapi
  version: "1.0.0"

source:
  fn: PyGSSAPI-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/P/PyGSSAPI/PyGSSAPI-1.0.0.tar.gz
  md5: 68045469d2ab0976eab174a13bbbaf96
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygssapi = pygssapi:main
    #
    # Would create an entry point called pygssapi that calls pygssapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flufl.enum >=4.0

  run:
    - python
    - flufl.enum >=4.0

test:
  # Python imports
  imports:
    - gssapi
    - gssapi.base
    - gssapi.tests
    - pygssapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/directxman12/python-gssapi
  license: BSD License
  summary: 'Python GSSAPI Wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
