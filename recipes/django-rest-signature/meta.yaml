package:
  name: django-rest-signature
  version: "0.0.2"

source:
  fn: django-rest-signature-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-signature/django-rest-signature-0.0.2.tar.gz
  md5: 565cbbe4c2275a19010e0db58668c4db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-signature = django-rest-signature:main
    #
    # Would create an entry point called django-rest-signature that calls django-rest-signature.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rest_signature

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.example.com/
  license: BSD License
  summary: 'django-rest-signature for multisite'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
