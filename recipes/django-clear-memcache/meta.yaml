package:
  name: django-clear-memcache
  version: "1.1.0"

source:
  fn: django-clear-memcache-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clear-memcache/django-clear-memcache-1.1.0.tar.gz
  md5: 8c9855e5f1bc7ac75679a49ec0bf78a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clear-memcache = django-clear-memcache:main
    #
    # Would create an entry point called django-clear-memcache that calls django-clear-memcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_clear_memcache
    - django_clear_memcache.management
    - django_clear_memcache.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eht16/django-clear-memcache
  license: BSD License
  summary: "Allow to clear Memcache items for the current site (according to the cache key prefix)\nor even to clear the whole configured Memcache server.\nThe app integrates into Django's admin interface for easy use.\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
