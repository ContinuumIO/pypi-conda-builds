package:
  name: raptus.multilanguageurls
  version: "1.0a8"

source:
  fn: raptus.multilanguageurls-1.0a8.zip
  url: https://pypi.python.org/packages/source/r/raptus.multilanguageurls/raptus.multilanguageurls-1.0a8.zip
  md5: 5cf2b337b91d7d85afd619574765008c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.multilanguageurls = raptus.multilanguageurls:main
    #
    # Would create an entry point called raptus.multilanguageurls that calls raptus.multilanguageurls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - raptus.multilanguagefields
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - raptus.multilanguagefields
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - raptus
    - raptus.multilanguageurls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Raptus/raptus.multilanguageurls
  license: GPL
  summary: 'Provides multilanguage URLs for Plone sites using raptus.multilanguagefields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
