package:
  name: icemac.truncatetext
  version: "0.3"

source:
  fn: icemac.truncatetext-0.3.zip
  url: https://pypi.python.org/packages/source/i/icemac.truncatetext/icemac.truncatetext-0.3.zip
  md5: 4245cfd9a809a01f5891847d1993db69
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icemac.truncatetext = icemac.truncatetext:main
    #
    # Would create an entry point called icemac.truncatetext that calls icemac.truncatetext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - icemac
    - icemac.truncatetext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/icemac.truncatetext
  license: Zope Public License
  summary: 'Nice, intelligent truncation of text.\n\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
