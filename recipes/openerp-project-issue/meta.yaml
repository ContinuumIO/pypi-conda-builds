package:
  name: openerp-project-issue
  version: "7.0.406"

source:
  fn: openerp-project-issue-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-project-issue/openerp-project-issue-7.0.406.tar.gz
  md5: 14e91628b12a19e1bbf85fd06cc5a71a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-project-issue = openerp-project-issue:main
    #
    # Would create an entry point called openerp-project-issue that calls openerp-project-issue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-base-status
    - openerp-crm
    - openerp-project

  run:
    - python
    - openerp-base-status
    - openerp-crm
    - openerp-project

test:
  # Python imports
  imports:
    - openerp.addons.project_issue
    - openerp.addons.project_issue.report

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openerp.com
  license: GNU Affero General Public License v3
  summary: 'Issue Tracker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
