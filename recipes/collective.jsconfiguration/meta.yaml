package:
  name: collective.jsconfiguration
  version: "0.1.0"

source:
  fn: collective.jsconfiguration-0.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.jsconfiguration/collective.jsconfiguration-0.1.0.zip
  md5: 2818c825dad9a7d9c3eb2e940e966d6b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jsconfiguration = collective.jsconfiguration:main
    #
    # Would create an entry point called collective.jsconfiguration that calls collective.jsconfiguration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

test:
  # Python imports
  imports:
    - collective
    - collective.jsconfiguration
    - collective.jsconfiguration.browser
    - collective.jsconfiguration.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keul/collective.jsconfiguration
  license: GPL
  summary: 'General approach for adding JavaScript configuration (and i18n data) to Plone products'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
