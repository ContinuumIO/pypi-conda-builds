package:
  name: lazr.testing
  version: "0.1.2"

source:
  fn: lazr.testing-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lazr.testing/lazr.testing-0.1.2.tar.gz
  md5: b4db0be34e8bde1fa795764c1534a25c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazr.testing = lazr.testing:main
    #
    # Would create an entry point called lazr.testing that calls lazr.testing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - mocker
    - setuptools
    - zope.exceptions
    - zope.interface
    - zope.testing

  run:
    - python
    - mocker
    - setuptools
    - zope.exceptions
    - zope.interface
    - zope.testing

test:
  # Python imports
  imports:
    - lazr
    - lazr.testing
    - lazr.testing.docs
    - lazr.testing.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lazr.testing
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A collection of testing helpers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
