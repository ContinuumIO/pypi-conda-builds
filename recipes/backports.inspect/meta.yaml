package:
  name: backports.inspect
  version: "0.0.3"

source:
  fn: backports.inspect-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/backports.inspect/backports.inspect-0.0.3.tar.gz
  md5: bac63c83151cc5119ccce443e78f084c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backports.inspect = backports.inspect:main
    #
    # Would create an entry point called backports.inspect that calls backports.inspect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - funcsigs >=0.4

  run:
    - python
    - funcsigs >=0.4

test:
  # Python imports
  imports:
    - backports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agoraplex/backports.inspect
  license: BSD
  summary: 'Backport (some) Python 3 `inspect` module features to 2.7 (extending aliles/funcsigs)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
