package:
  name: geobricksgisvector
  version: "0.0.2"

source:
  fn: GeobricksGISVector-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksGISVector/GeobricksGISVector-0.0.2.tar.gz
  md5: 4653dc2a990aec61aa5c11ece414104d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobricksgisvector = geobricksgisvector:main
    #
    # Would create an entry point called geobricksgisvector that calls geobricksgisvector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - watchdog
    - flask
    - flask-cors
    - fiona
    - geobrickscommon
    - geobricksproj4toepsg

  run:
    - python
    - watchdog
    - flask
    - flask-cors
    - fiona
    - geobrickscommon
    - geobricksproj4toepsg

test:
  # Python imports
  imports:
    - geobricks_gis_vector
    - geobricks_gis_vector.config
    - geobricks_gis_vector.core
    - geobricksgisvector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksGISVector/
  license: LICENSE.txt
  summary: 'Geobricks library to process vector layers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
