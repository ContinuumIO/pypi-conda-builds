package:
  name: armor_api
  version: "0.1.1"

source:
  fn: armor_api-0.1.1.zip
  url: https://pypi.python.org/packages/source/a/armor_api/armor_api-0.1.1.zip
  md5: e275a18debb881c5927e70868b385b26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armor_api = armor_api:main
    #
    # Would create an entry point called armor_api that calls armor_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appier

  run:
    - python
    - appier

test:
  # Python imports
  imports:
    - armor
    - armor_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://armor_api.hive.pt
  license: GNU General Public License (GPL)
  summary: 'Armor API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
