package:
  name: django-transaction-signals-do-not-use
  version: "1.0"

source:
  fn: django-transaction-signals-do-not-use-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-transaction-signals-do-not-use/django-transaction-signals-do-not-use-1.0.tar.gz
  md5: a9f912d145b52c8cce0994612f47afa0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-transaction-signals-do-not-use = django-transaction-signals-do-not-use:main
    #
    # Would create an entry point called django-transaction-signals-do-not-use that calls django-transaction-signals-do-not-use.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - transaction_signals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aaugustin/django-transaction-signals
  license: BSD License
  summary: "Transaction signals for Django. Don't use this!"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
