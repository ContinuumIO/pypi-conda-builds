package:
  name: django-extra-form-fields
  version: "0.0.1"

source:
  fn: django-extra-form-fields-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extra-form-fields/django-extra-form-fields-0.0.1.tar.gz
  md5: e884ae42f31996ce155cec230977380f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extra-form-fields = django-extra-form-fields:main
    #
    # Would create an entry point called django-extra-form-fields that calls django-extra-form-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - multi-registry

  run:
    - python
    - multi-registry

test:
  # Python imports
  imports:
    - django_extra_form_fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://askbot.org
  license: BSD License
  summary: 'Additional form fields for Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
