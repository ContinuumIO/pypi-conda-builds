package:
  name: collective.portlets.reverseordering
  version: "0.1"

source:
  fn: collective.portlets.reverseordering-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.portlets.reverseordering/collective.portlets.reverseordering-0.1.zip
  md5: 7034237135a720d3d4e57fa8bd4d9a6e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlets.reverseordering = collective.portlets.reverseordering:main
    #
    # Would create an entry point called collective.portlets.reverseordering that calls collective.portlets.reverseordering.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.portlets >=1.1,<1.2

  run:
    - python
    - setuptools
    - setuptools
    - plone.portlets >=1.1,<1.2

test:
  # Python imports
  imports:
    - collective
    - collective.portlets
    - collective.portlets.reverseordering

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: LGPL
  summary: 'Reverse the acquisition ordering, having the global portlets on top and local at the bottom.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
