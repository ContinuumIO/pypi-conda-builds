package:
  name: django-cked
  version: "0.1.0"

source:
  fn: django-cked-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-cked/django-cked-0.1.0.zip
  md5: fc51c55d132933be8a07849a2eef30f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cked = django-cked:main
    #
    # Would create an entry point called django-cked that calls django-cked.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cked

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dead23angel/django-cked
  license: BSD License
  summary: 'CKEditor and elFinder integration for Django Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
