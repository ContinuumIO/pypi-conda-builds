package:
  name: ensmime
  version: "2014.05.13.1"

source:
  fn: ensmime-2014.05.13.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ensmime/ensmime-2014.05.13.1.tar.gz
  md5: fd3b15aa90d5f2be2ad5cf50f4413341
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ensmime = ensmime:main
    #
    # Would create an entry point called ensmime that calls ensmime.main()

    - ensmime = _ensmime.ensmime:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - _ensmime
    - ensmime

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - ensmime --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'ensmime - openssl smime encryption helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
