package:
  name: collective.seeder
  version: "1.1"

source:
  fn: collective.seeder-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.seeder/collective.seeder-1.1.zip
  md5: c8b6cee7ed1c52d219e01fb987ed82b8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.seeder = collective.seeder:main
    #
    # Would create an entry point called collective.seeder that calls collective.seeder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - bittorrent-bencode

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - bittorrent-bencode

test:
  # Python imports
  imports:
    - collective
    - collective.seeder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plumi/collective.seeder
  license: GPL
  summary: 'A torrent creation and seeding product for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
