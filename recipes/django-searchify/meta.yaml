package:
  name: django-searchify
  version: "0.4"

source:
  fn: django-searchify-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-searchify/django-searchify-0.4.tar.gz
  md5: f312788b36b4fb2fc48d261d7e2081bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-searchify = django-searchify:main
    #
    # Would create an entry point called django-searchify that calls django-searchify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - searchify
    - searchify.clients
    - searchify.management
    - searchify.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Search integration for Django with a focus on indexing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
