package:
  name: mamba-runner
  version: "0.0.2"

source:
  fn: mamba-runner-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mamba-runner/mamba-runner-0.0.2.tar.gz
  md5: 01ef6bf3d3828bb25ba869c344ef7942
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mamba-runner = mamba-runner:main
    #
    # Would create an entry point called mamba-runner that calls mamba-runner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blessings

  run:
    - python
    - blessings

test:
  # Python imports
  imports:
    - mamba_runner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattack108/mamba-runner
  license: MIT License
  summary: 'Measure time execution for unittest tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
