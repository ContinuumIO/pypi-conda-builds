package:
  name: zc.resourcelibrary
  version: "1.3.4"

source:
  fn: zc.resourcelibrary-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.resourcelibrary/zc.resourcelibrary-1.3.4.tar.gz
  md5: bebe49f3e930f896a8ea75531bf3fae8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.resourcelibrary = zc.resourcelibrary:main
    #
    # Would create an entry point called zc.resourcelibrary that calls zc.resourcelibrary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.app.publication
    - zope.browserpage
    - zope.browserresource
    - zope.component
    - zope.configuration
    - zope.interface
    - zope.publisher
    - zope.security
    - zope.tales
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.app.publication
    - zope.browserpage
    - zope.browserresource
    - zope.component
    - zope.configuration
    - zope.interface
    - zope.publisher
    - zope.security
    - zope.tales
    - zope.traversing

test:
  # Python imports
  imports:
    - zc
    - zc.resourcelibrary
    - zc.resourcelibrary.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.resourcelibrary
  license: Zope Public License
  summary: 'Post-rendering Resource Inclusion'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
