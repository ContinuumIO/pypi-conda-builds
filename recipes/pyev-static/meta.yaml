package:
  name: pyev-static
  version: "0.9.5"

source:
  fn: pyev-static-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pyev-static/pyev-static-0.9.5.tar.gz
  md5: 32b8925a25d4ef3f5c80f5b2f5250fd0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyev-static = pyev-static:main
    #
    # Would create an entry point called pyev-static that calls pyev-static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyev
    - utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/pyev/
  license: BSD License or GNU General Public License (GPL)
  summary: 'Python bindings to libev.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
