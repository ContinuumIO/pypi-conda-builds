package:
  name: django-exchange
  version: "0.7.3"

source:
  fn: django-exchange-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-exchange/django-exchange-0.7.3.tar.gz
  md5: 25fa575fa0372668cf8fa8591cd0da31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-exchange = django-exchange:main
    #
    # Would create an entry point called django-exchange that calls django-exchange.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openexchangerates

  run:
    - python
    - openexchangerates

test:
  # Python imports
  imports:
    - exchange
    - exchange.adapters
    - exchange.management
    - exchange.management.commands
    - exchange.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/metglobal/django-exchange
  license: MIT License
  summary: 'currency, exchange rates and conversions support for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
