package:
  name: chardetails
  version: "0.3"

source:
  fn: chardetails-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/chardetails/chardetails-0.3.tar.gz
  md5: 11c753f3a641a5a18541056401ec5ffb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chardetails = chardetails:main
    #
    # Would create an entry point called chardetails that calls chardetails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - chardetails
    - chardetails.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://silpa.org.in/chardetails
  license: LGPL-3.0
  summary: 'Unicode character details Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
