package:
  name: django-journal
  version: "1.24.1"

source:
  fn: django-journal-1.24.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-journal/django-journal-1.24.1.tar.gz
  md5: ebe69e14f946ff75aae49304429c8085
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-journal = django-journal:main
    #
    # Would create an entry point called django-journal that calls django-journal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - django-model-utils

  run:
    - python
    - django >=1.4.2
    - django-model-utils

test:
  # Python imports
  imports:
    - django_journal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.entrouvert.org/projects/django-journal/
  license: AGPLv3
  summary: 'Keep a structured -- i.e. not just log strings -- journal of events in your applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
