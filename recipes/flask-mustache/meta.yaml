package:
  name: flask-mustache
  version: "0.4.1"

source:
  fn: Flask-Mustache-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Mustache/Flask-Mustache-0.4.1.tar.gz
  md5: 7bfeca2f744d904e92d27d87376970a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mustache = flask-mustache:main
    #
    # Would create an entry point called flask-mustache that calls flask-mustache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ahri/flask-mustache
  license: MIT License
  summary: 'Mustache for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
