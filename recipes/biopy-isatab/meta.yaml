package:
  name: biopy-isatab
  version: "0.1.1"

source:
  fn: biopy-isatab-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/biopy-isatab/biopy-isatab-0.1.1.tar.gz
  md5: 6e5f77a9054a04fe8f2225ef29ef5308
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biopy-isatab = biopy-isatab:main
    #
    # Would create an entry point called biopy-isatab that calls biopy-isatab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bcbio
    - bcbio.isatab

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ISA-tools/biopy-isatab
  license: MIT
  summary: 'Python parser for ISAtab, a biological file format for experimental metadata'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
