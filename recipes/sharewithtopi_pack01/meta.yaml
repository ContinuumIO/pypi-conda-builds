package:
  name: sharewithtopi_pack01
  version: "1.0.0"

source:
  fn: ShareWithTopi_Pack01-1.0.0.zip
  url: https://pypi.python.org/packages/source/S/ShareWithTopi_Pack01/ShareWithTopi_Pack01-1.0.0.zip
  md5: 58da812520a61de31331f5792a3c19af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sharewithtopi_pack01 = sharewithtopi_pack01:main
    #
    # Would create an entry point called sharewithtopi_pack01 that calls sharewithtopi_pack01.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sharewithtopi_pack01

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: dammina.blogspot.com
  license: UNKNOWN
  summary: 'My first damn python code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
