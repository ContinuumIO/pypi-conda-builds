package:
  name: advancedhtmlparser
  version: "3.0.1"

source:
  fn: AdvancedHTMLParser-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/A/AdvancedHTMLParser/AdvancedHTMLParser-3.0.1.tar.gz
  md5: 84d677cb0fa36ffd4a43708ba2b33f1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - advancedhtmlparser = advancedhtmlparser:main
    #
    # Would create an entry point called advancedhtmlparser that calls advancedhtmlparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - AdvancedHTMLParser
    - advancedhtmlparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'A Powerful HTML Parser/Writer that constructs a modifiable, searchable DOM tree, and includes many standard JS DOM functions (getElementsBy*, appendChild, etc)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
