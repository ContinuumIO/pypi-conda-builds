package:
  name: redsolutioncms.django-utilities
  version: "0.1.5"

source:
  fn: redsolutioncms.django-utilities-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-utilities/redsolutioncms.django-utilities-0.1.5.tar.gz
  md5: 09dd3062aeb0ede55228ab4e1dcdbd78
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-utilities = redsolutioncms.django-utilities:main
    #
    # Would create an entry point called redsolutioncms.django-utilities that calls redsolutioncms.django-utilities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - utilities
    - utilities.management
    - utilities.management.commands
    - utilities.redsolution_setup
    - utilities.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: "Django's set of utilities. Includes admin ImageField with preview, override for username,\nLocalized date filter, ConsoleException middleware and broken link highlight template."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
