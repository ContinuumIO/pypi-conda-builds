package:
  name: fabgis
  version: "0.17.1"

source:
  fn: fabgis-0.17.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fabgis/fabgis-0.17.1.tar.gz
  md5: 3c01b541f6bf4b6cc49c4cdb1a74929f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fabgis = fabgis:main
    #
    # Would create an entry point called fabgis that calls fabgis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - fabric ==1.6.0
    - fabtools >=0.15.0

  run:
    - python
    - fabric ==1.6.0
    - fabtools >=0.15.0

test:
  # Python imports
  imports:
    - fabgis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/fabgis/
  license: LICENSE.txt
  summary: 'Fabric tools for a busy FOSSGIS user.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
