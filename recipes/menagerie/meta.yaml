package:
  name: menagerie
  version: "0.1.0"

source:
  fn: menagerie-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/menagerie/menagerie-0.1.0.tar.gz
  md5: e2efbf260fb8db195e817d3b05a53111
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - menagerie = menagerie:main
    #
    # Would create an entry point called menagerie that calls menagerie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2,<1.5
    - kazoo >=0.5,<0.9

  run:
    - python
    - django >=1.2,<1.5
    - kazoo >=0.5,<0.9

test:
  # Python imports
  imports:
    - menagerie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/disqus/menagerie
  license: Apache License 2.0
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
