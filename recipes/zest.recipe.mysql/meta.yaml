package:
  name: zest.recipe.mysql
  version: "1.0.4"

source:
  fn: zest.recipe.mysql-1.0.4.zip
  url: https://pypi.python.org/packages/source/z/zest.recipe.mysql/zest.recipe.mysql-1.0.4.zip
  md5: 9e7c964019757ee820aa57e4e58f9678
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zest.recipe.mysql = zest.recipe.mysql:main
    #
    # Would create an entry point called zest.recipe.mysql that calls zest.recipe.mysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - zest
    - zest.recipe
    - zest.recipe.mysql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.zestsoftware.nl/svn/zest/zest.recipe.mysql/trunk
  license: Zope Public License
  summary: 'A Buildout recipe to setup a MySQL database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
