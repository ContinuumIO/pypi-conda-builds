package:
  name: marie
  version: "1.0.5"

source:
  fn: Marie-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/M/Marie/Marie-1.0.5.tar.gz
  md5: 9895d0362fbc82294ae3b4fdda8287ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marie = marie:main
    #
    # Would create an entry point called marie that calls marie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.trydyingtolive.com/marie/
  license: GNU General Public License (GPL)
  summary: 'A WSGI micro-framework for websites in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
