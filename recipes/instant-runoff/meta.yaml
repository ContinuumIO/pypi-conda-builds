package:
  name: instant-runoff
  version: "0.1"

source:
  fn: instant-runoff-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/instant-runoff/instant-runoff-0.1.tar.gz
  md5: 40dfe2ff05258e487871ab9389f63863
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instant-runoff = instant-runoff:main
    #
    # Would create an entry point called instant-runoff that calls instant-runoff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://readmes.numm.org/instant-runoff/
  license: AGPL
  summary: 'Write emails in groff.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
