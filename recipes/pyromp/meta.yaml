package:
  name: pyromp
  version: "1.0"

source:
  fn: PyroMP-1.0.zip
  url: https://pypi.python.org/packages/source/P/PyroMP/PyroMP-1.0.zip
  md5: fa139626f0139715c5a1b41ef488f182
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyromp = pyromp:main
    #
    # Would create an entry point called pyromp that calls pyromp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyro4 >=4.20
    - mock
    - six

  run:
    - python
    - pyro4 >=4.20
    - mock
    - six

test:
  # Python imports
  imports:
    - PyroMP
    - PyroMP.tests
    - pyromp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/PBrimmers/PyroMP
  license: pyromsgui
  summary: 'Easy muliprocessing combining Pyro4 and the built-in multiprocessing module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
