package:
  name: image_slicer
  version: "0.1.1"

source:
  fn: image_slicer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/image_slicer/image_slicer-0.1.1.tar.gz
  md5: 81ba09d8e5ace5945c833186a785510f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - image_slicer = image_slicer:main
    #
    # Would create an entry point called image_slicer that calls image_slicer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow >=2.0.0

  run:
    - python
    - pillow >=2.0.0

test:
  # Python imports
  imports:
    - image_slicer
    - image_slicer.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://samdobson.github.io/image_slicer
  license: MIT License
  summary: 'Cut images into tiles and reassemble them..'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
