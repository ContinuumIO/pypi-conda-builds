package:
  name: auroraplot
  version: "0.3.1"

source:
  fn: auroraplot-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/auroraplot/auroraplot-0.3.1.tar.gz
  md5: 898e778b37de476c8ed7a1ee449d94c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auroraplot = auroraplot:main
    #
    # Would create an entry point called auroraplot that calls auroraplot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib
    - numpy >=1.7
    - scipy

  run:
    - python
    - matplotlib
    - numpy >=1.7
    - scipy

test:
  # Python imports
  imports:
    - auroraplot
    - auroraplot.datasets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stevemarple/auroraplot
  license: Python Software Foundation License
  summary: 'Analyse and plot space weather datasets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
