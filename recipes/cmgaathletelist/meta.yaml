package:
  name: cmgaathletelist
  version: "1.0.0"

source:
  fn: CMGAathletelist-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/C/CMGAathletelist/CMGAathletelist-1.0.0.tar.gz
  md5: 146d3243aa6d1bbe26fa2fd50dde5519
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmgaathletelist = cmgaathletelist:main
    #
    # Would create an entry point called cmgaathletelist that calls cmgaathletelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cmgaathletelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://caiomga.tumblr.com/
  license: UNKNOWN
  summary: 'HeadFirst AthleteList class and functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
