package:
  name: cgi_wsgi
  version: "0.1"

source:
  fn: cgi_wsgi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cgi_wsgi/cgi_wsgi-0.1.tar.gz
  md5: 89f09a398e1cd9cdf542b2617e2f52c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cgi_wsgi = cgi_wsgi:main
    #
    # Would create an entry point called cgi_wsgi that calls cgi_wsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cgi_wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/red56/cgi_wsgi
  license: MIT
  summary: 'A CGI-like http interface for WSGI (for educational purposes only)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
