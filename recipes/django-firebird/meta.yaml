package:
  name: django-firebird
  version: "1.6.1.final.0"

source:
  fn: django-firebird-1.6.1.final.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-firebird/django-firebird-1.6.1.final.0.tar.gz
  md5: 666b8260a5b22f752e3af0eb1213c33d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-firebird = django-firebird:main
    #
    # Would create an entry point called django-firebird that calls django-firebird.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - firebird

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxirobaina/django-firebird
  license: BSD License
  summary: 'Firebird backend for Django 1.6.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
