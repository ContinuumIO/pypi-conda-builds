package:
  name: django-pastedeploy-settings
  version: "1.0"

source:
  fn: django-pastedeploy-settings-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pastedeploy-settings/django-pastedeploy-settings-1.0.tar.gz
  md5: 8f27f47c7ba9d675a98227d0edb7f31a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pastedeploy-settings = django-pastedeploy-settings:main
    #
    # Would create an entry point called django-pastedeploy-settings that calls django-pastedeploy-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pastedeploy >=1.3.3
    - paste >=1.7.2
    - setuptools

  run:
    - python
    - setuptools
    - django >=1.4
    - pastedeploy >=1.3.3
    - paste >=1.7.2
    - setuptools

test:
  # Python imports
  imports:
    - django_pastedeploy_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/django-pastedeploy-settings/
  license: BSD License
  summary: 'Conversion of Paste Deployment configuration to Django settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
