package:
  name: django-mptt-treechangelist
  version: "0.1.1"

source:
  fn: django-mptt-treechangelist-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mptt-treechangelist/django-mptt-treechangelist-0.1.1.tar.gz
  md5: f871ef948c846e8d51c6d214dba6a800
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mptt-treechangelist = django-mptt-treechangelist:main
    #
    # Would create an entry point called django-mptt-treechangelist that calls django-mptt-treechangelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=2.1.0

  run:
    - python
    - simplejson >=2.1.0

test:
  # Python imports
  imports:
    - mptt_treechangelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blancltd/django-mptt-treechangelist
  license: BSD License
  summary: 'MPTT Tree Change List Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
