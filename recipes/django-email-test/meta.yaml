package:
  name: django-email-test
  version: "0.0.3"

source:
  fn: django-email-test-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-test/django-email-test-0.0.3.tar.gz
  md5: 34f78ef363712cb127087cde58c1c510
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-test = django-email-test:main
    #
    # Would create an entry point called django-email-test that calls django-email-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django_email_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://seanhayes.name/
  license: BSD License
  summary: 'An app for sending test emails via the admin site to make sure your email server is working.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
