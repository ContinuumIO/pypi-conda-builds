package:
  name: config_resolver
  version: "4.2.2"

source:
  fn: config_resolver-4.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/config_resolver/config_resolver-4.2.2.tar.gz
  md5: 42eec8c547f304144a7a0831f59dad1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - config_resolver = config_resolver:main
    #
    # Would create an entry point called config_resolver that calls config_resolver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - config_resolver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/exhuma/config_resolver
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'A small package to automatically find a configuration file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
