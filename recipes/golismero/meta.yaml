package:
  name: golismero
  version: "2.0.3-1"

source:
  fn: golismero-2.0.3-1.tar.gz
  url: https://pypi.python.org/packages/source/g/golismero/golismero-2.0.3-1.tar.gz
  md5: e4e029f832c20c746074a17ea8182730
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - golismero = golismero:main
    #
    # Would create an entry point called golismero that calls golismero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils
    - pymongo
    - simplejson
    - argcomplete

  run:
    - python
    - docutils
    - pymongo
    - simplejson
    - argcomplete

test:
  # Python imports
  imports:
    - golismero

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/golismero
  license: GNU General Public License v2 (GPLv2)
  summary: "GoLismero: The web knife. GoLismero is an open source framework for security testing.It's currently     geared towards web security, but it can easily be expanded to other kinds of scans."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
