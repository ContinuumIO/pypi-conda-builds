package:
  name: lribeiro.cherrypy.authorizer.mongoengine
  version: "1.0.1"

source:
  fn: lribeiro.cherrypy.authorizer.mongoengine-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lribeiro.cherrypy.authorizer.mongoengine/lribeiro.cherrypy.authorizer.mongoengine-1.0.1.tar.gz
  md5: a4f222917b7e09fd715ad5d5b2dc7217
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lribeiro.cherrypy.authorizer.mongoengine = lribeiro.cherrypy.authorizer.mongoengine:main
    #
    # Would create an entry point called lribeiro.cherrypy.authorizer.mongoengine that calls lribeiro.cherrypy.authorizer.mongoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy
    - mongoengine
    - bcrypt
    - lribeiro.cherrypy.authorizer

  run:
    - python
    - cherrypy
    - mongoengine
    - bcrypt
    - lribeiro.cherrypy.authorizer

test:
  # Python imports
  imports:
    - lribeiro
    - lribeiro.cherrypy
    - lribeiro.cherrypy.authorizer
    - lribeiro.cherrypy.authorizer.mongoengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/livioribeiro/cherrypy-authorizer-mongoengine
  license: BSD License
  summary: 'Basic user model, authenticator and authorizer for lribeiro.cherrypy.authorizer and mongoengine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
