package:
  name: collective.dms.scanbehavior
  version: "0.3"

source:
  fn: collective.dms.scanbehavior-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.dms.scanbehavior/collective.dms.scanbehavior-0.3.zip
  md5: cdbc268e03f582000b434d9441427eb9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dms.scanbehavior = collective.dms.scanbehavior:main
    #
    # Would create an entry point called collective.dms.scanbehavior that calls collective.dms.scanbehavior.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.api
    - setuptools
    - plone.behavior
    - plone.directives.form
    - zope.schema
    - zope.interface
    - zope.component
    - rwproperty

  run:
    - python
    - setuptools
    - plone.api
    - setuptools
    - plone.behavior
    - plone.directives.form
    - zope.schema
    - zope.interface
    - zope.component
    - rwproperty

test:
  # Python imports
  imports:
    - collective
    - collective.dms
    - collective.dms.scanbehavior
    - collective.dms.scanbehavior.behaviors
    - collective.dms.scanbehavior.browser
    - collective.dms.scanbehavior.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.dms.scanbehavior
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
