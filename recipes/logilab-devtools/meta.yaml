package:
  name: logilab-devtools
  version: "0.24.0"

source:
  fn: logilab-devtools-0.24.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logilab-devtools/logilab-devtools-0.24.0.tar.gz
  md5: 68c29f538ddd4caeb69a7050ea439e69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logilab-devtools = logilab-devtools:main
    #
    # Would create an entry point called logilab-devtools that calls logilab-devtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - logilab-common

  run:
    - python
    - logilab-common

test:
  # Python imports
  imports:
    - hgext.jpl
    - logilab.devtools
    - logilab.devtools.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.logilab.org/project/logilab-devtools
  license: GPL
  summary: 'set of development tools used at Logilab'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
