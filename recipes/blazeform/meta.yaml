package:
  name: blazeform
  version: "0.3.7"

source:
  fn: BlazeForm-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/B/BlazeForm/BlazeForm-0.3.7.tar.gz
  md5: 4cdc400eb1231303c4a1b229349d20af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blazeform = blazeform:main
    #
    # Would create an entry point called blazeform that calls blazeform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - formencode >=1.2.2
    - blazeutils >=0.3.0
    - webhelpers >=1.0

  run:
    - python
    - formencode >=1.2.2
    - blazeutils >=0.3.0
    - webhelpers >=1.0

test:
  # Python imports
  imports:
    - blazeform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BlazeForm
  license: BSD License
  summary: 'A library for generating and validating HTML forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
