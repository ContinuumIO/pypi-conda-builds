package:
  name: django-badges
  version: "0.1.11"

source:
  fn: django-badges-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-badges/django-badges-0.1.11.tar.gz
  md5: a33e2e580ce55a8e8c9daa333a25be34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-badges = django-badges:main
    #
    # Would create an entry point called django-badges that calls django-badges.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - pillow

  run:
    - python
    - django >=1.6
    - pillow

test:
  # Python imports
  imports:
    - badges
    - badges.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://djangobadges.com
  license: GNU General Public License (GPL)
  summary: 'An easy to use app that provides Stack Overflow style badgeswith a minimum ammount of effort in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
