package:
  name: django-user-roles
  version: "0.1.0"

source:
  fn: django-user-roles-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-roles/django-user-roles-0.1.0.tar.gz
  md5: 68ccbf2cada3ef15e4206eeb426ebf8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-roles = django-user-roles:main
    #
    # Would create an entry point called django-user-roles that calls django-user-roles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - userroles
    - userroles/migrations
    - userroles/testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dabapps/django-user-roles
  license: BSD
  summary: 'Simple role-based user permissions for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
