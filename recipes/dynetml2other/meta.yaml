package:
  name: dynetml2other
  version: "0.11"

source:
  fn: dynetml2other-0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/dynetml2other/dynetml2other-0.11.tar.gz
  md5: f4bdba9ee8d07161bc6662940d1274d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynetml2other = dynetml2other:main
    #
    # Would create an entry point called dynetml2other that calls dynetml2other.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dynetml2other

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pmlandwehr.github.io/dynetml2other/
  license: BSD
  summary: 'DyNetML parsing and manipulation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
