package:
  name: mkschroot
  version: "0.3.4.1"

source:
  fn: mkschroot-0.3.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mkschroot/mkschroot-0.3.4.1.tar.gz
  md5: 58d1e37fb450a699b9a7fd07697cc22a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mkschroot = mkschroot:main
    #
    # Would create an entry point called mkschroot that calls mkschroot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - mkschroot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/KayEss/mkschroot
  license: Boost Software License - Version 1.0 - August 17th, 2003
  summary: 'A simple script for making schroot environments from a JSON configuration file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
