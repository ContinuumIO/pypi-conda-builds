package:
  name: django-xstatic
  version: "0.0.2"

source:
  fn: django-xstatic-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xstatic/django-xstatic-0.0.2.tar.gz
  md5: bd0ebce698353379055b5bcd07b7f643
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xstatic = django-xstatic:main
    #
    # Would create an entry point called django-xstatic that calls django-xstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_xstatic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gautier/django-xstatic
  license: MIT License
  summary: 'Django helpers to use XStatic packages in Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
