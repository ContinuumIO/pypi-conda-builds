package:
  name: django-browserid
  version: "1.0.0"

source:
  fn: django-browserid-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-browserid/django-browserid-1.0.0.tar.gz
  md5: 4464f0603544f4ea1b5692885552dd54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-browserid = django-browserid:main
    #
    # Would create an entry point called django-browserid that calls django-browserid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0

  run:
    - python
    - requests >=1.0.0

test:
  # Python imports
  imports:
    - django_browserid
    - django_browserid.templatetags
    - django_browserid.tests
    - docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/django-browserid
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Django application for adding BrowserID support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
