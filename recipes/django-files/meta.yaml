package:
  name: django-files
  version: "1.1"

source:
  fn: django-files-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-files/django-files-1.1.tar.gz
  md5: 968076e5049c89eae4835f10409957f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-files = django-files:main
    #
    # Would create an entry point called django-files that calls django-files.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - django-braces

  run:
    - python
    - django
    - django-braces

test:
  # Python imports
  imports:
    - files
    - files.management
    - files.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rhblind/django-files
  license: Apache Software License
  summary: 'Django attachments ContentTypes micro framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
