package:
  name: django-shop-gestpay
  version: "0.0.1"

source:
  fn: django-shop-gestpay-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-gestpay/django-shop-gestpay-0.0.1.tar.gz
  md5: 2d2959440d2c95a3bddb69eb6dceb005
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-gestpay = django-shop-gestpay:main
    #
    # Would create an entry point called django-shop-gestpay that calls django-shop-gestpay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-shop
    - gestpypay

  run:
    - python
    - django >=1.4
    - django-shop
    - gestpypay

test:
  # Python imports
  imports:
    - shop_gestpay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/albertojacini/django-shop-gestpay
  license: BSD License
  summary: 'A Banca Sella Gestpay payment backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
