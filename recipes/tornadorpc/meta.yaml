package:
  name: tornadorpc
  version: "0.1.1"

source:
  fn: tornadorpc-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadorpc/tornadorpc-0.1.1.tar.gz
  md5: 9504bfd8e9526157ae79d07cf26ffc7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadorpc = tornadorpc:main
    #
    # Would create an entry point called tornadorpc that calls tornadorpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - jsonrpclib

  run:
    - python
    - tornado
    - jsonrpclib

test:
  # Python imports
  imports:
    - tornadorpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/tornadorpc/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'TornadoRPC is a an implementation of both JSON-RPC and XML-RPC handlers for the Tornado framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
