package:
  name: js.fanstatictools
  version: "0.1"

source:
  fn: js.fanstatictools-0.1.zip
  url: https://pypi.python.org/packages/source/j/js.fanstatictools/js.fanstatictools-0.1.zip
  md5: 7a9630a7894dc334262d718dbbfdb088
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.fanstatictools = js.fanstatictools:main
    #
    # Would create an entry point called js.fanstatictools that calls js.fanstatictools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.namespace
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.namespace
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.fanstatictools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic related javascript tools packaged as a fanstatic library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
