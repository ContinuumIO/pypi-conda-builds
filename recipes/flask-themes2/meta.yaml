package:
  name: flask-themes2
  version: "0.1.4"

source:
  fn: Flask-Themes2-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Themes2/Flask-Themes2-0.1.4.tar.gz
  md5: 7cbc8e0017625ce5ef70b9e54bcbd866
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-themes2 = flask-themes2:main
    #
    # Would create an entry point called flask-themes2 that calls flask-themes2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.6
    - simplejson

  run:
    - python
    - flask >=0.6
    - simplejson

test:
  # Python imports
  imports:
    - flask_themes2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sysr-q/flask-themes2
  license: MIT License
  summary: 'Provides infrastructure for theming Flask applications                     (and supports Flask>=0.6!)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
