package:
  name: isotoma.recipe.ldap
  version: "0.0.1"

source:
  fn: isotoma.recipe.ldap-0.0.1.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.ldap/isotoma.recipe.ldap-0.0.1.zip
  md5: 0633308e928597c96c099f8aa737483c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.ldap = isotoma.recipe.ldap:main
    #
    # Would create an entry point called isotoma.recipe.ldap that calls isotoma.recipe.ldap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - jinja2
    - isotoma.recipe.gocaptain

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - jinja2
    - isotoma.recipe.gocaptain

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.ldap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.slapd
  license: Apache Software License
  summary: 'Buildout recipes to configure slapd'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
