package:
  name: django-hashlink
  version: "0.1.1"

source:
  fn: django-hashlink-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hashlink/django-hashlink-0.1.1.tar.gz
  md5: ed74e043bf2f12cb079ae731bf2c73e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hashlink = django-hashlink:main
    #
    # Would create an entry point called django-hashlink that calls django-hashlink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - mongoengine >=0.6.11

  run:
    - python
    - django >=1.3
    - mongoengine >=0.6.11

test:
  # Python imports
  imports:
    - hashlink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mitar/django-hashlink
  license: GNU Affero General Public License v3
  summary: 'Store and retrieve URL hash (fragment) based webpage state in MongoDB through Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
