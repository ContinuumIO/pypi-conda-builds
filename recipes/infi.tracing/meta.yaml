package:
  name: infi.tracing
  version: "0.2.9"

source:
  fn: infi.tracing-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.tracing/infi.tracing-0.2.9.tar.gz
  md5: 8b47ec1932526be0aca3fe5f08211079
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.tracing = infi.tracing:main
    #
    # Would create an entry point called infi.tracing that calls infi.tracing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cython
    - greenlet
    - infi.pyutils
    - setuptools

  run:
    - python
    - cython
    - greenlet
    - infi.pyutils
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.tracing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.tracing
  license: Python Software Foundation License
  summary: 'short description here'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
