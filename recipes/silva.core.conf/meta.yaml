package:
  name: silva.core.conf
  version: "3.0.3"

source:
  fn: silva.core.conf-3.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.core.conf/silva.core.conf-3.0.3.tar.gz
  md5: d5b4c0926ef4ffcd6f67b7eb7e3447f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.core.conf = silva.core.conf:main
    #
    # Would create an entry point called silva.core.conf that calls silva.core.conf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - five.grok
    - grokcore.component
    - grokcore.view
    - grokcore.viewlet
    - martian >=0.11
    - setuptools
    - silva.core.interfaces
    - silva.core.services
    - silva.translations
    - zope.container
    - zope.cachedescriptors
    - zope.component
    - zope.configuration
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.publisher
    - zope.schema

  run:
    - python
    - five.grok
    - grokcore.component
    - grokcore.view
    - grokcore.viewlet
    - martian >=0.11
    - setuptools
    - silva.core.interfaces
    - silva.core.services
    - silva.translations
    - zope.container
    - zope.cachedescriptors
    - zope.component
    - zope.configuration
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.publisher
    - zope.schema

test:
  # Python imports
  imports:
    - silva
    - silva.core
    - silva.core.conf
    - silva.core.conf.martiansupport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.core.conf
  license: BSD License
  summary: 'Configuration machinery for Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
