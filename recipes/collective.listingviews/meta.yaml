package:
  name: collective.listingviews
  version: "1.0beta3"

source:
  fn: collective.listingviews-1.0beta3.zip
  url: https://pypi.python.org/packages/source/c/collective.listingviews/collective.listingviews-1.0beta3.zip
  md5: b4f8bf1aecdff44cfd7fac105f0dcaad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.listingviews = collective.listingviews:main
    #
    # Would create an entry point called collective.listingviews that calls collective.listingviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.listingviews
    - collective.listingviews.browser
    - collective.listingviews.browser.portlets
    - collective.listingviews.browser.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.listingviews
  license: GPL
  summary: 'Listing views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
