package:
  name: boxsdk
  version: "1.1.6"

source:
  fn: boxsdk-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/b/boxsdk/boxsdk-1.1.6.tar.gz
  md5: 8c8945efde8d90f8fdd27523fb54820d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boxsdk = boxsdk:main
    #
    # Would create an entry point called boxsdk that calls boxsdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.3
    - six >=1.4.0
    - enum34 >=1.0.4

  run:
    - python
    - requests >=2.4.3
    - six >=1.4.0
    - enum34 >=1.0.4

test:
  # Python imports
  imports:
    - boxsdk
    - boxsdk.auth
    - boxsdk.network
    - boxsdk.object
    - boxsdk.session
    - boxsdk.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://opensource.box.com
  license: Apache Software License
  summary: u'Official Box Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
