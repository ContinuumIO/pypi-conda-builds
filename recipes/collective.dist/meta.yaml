package:
  name: collective.dist
  version: "0.2.5"

source:
  fn: collective.dist-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.dist/collective.dist-0.2.5.tar.gz
  md5: d892285cbfcb63678e97989664edb197
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dist = collective.dist:main
    #
    # Would create an entry point called collective.dist that calls collective.dist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - docutils

  run:
    - python
    - setuptools
    - setuptools
    - docutils

test:
  # Python imports
  imports:
    - collective
    - collective.dist
    - collective.dist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.dist
  license: GPL
  summary: 'Distutils commands to upload files to several servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
