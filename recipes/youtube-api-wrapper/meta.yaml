package:
  name: youtube-api-wrapper
  version: "0.2"

source:
  fn: youtube-api-wrapper-0.2.tar.gz
  url: https://pypi.python.org/packages/source/y/youtube-api-wrapper/youtube-api-wrapper-0.2.tar.gz
  md5: 7da4c1f0d4b49fbed4fc0b588d57d349
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - youtube-api-wrapper = youtube-api-wrapper:main
    #
    # Would create an entry point called youtube-api-wrapper that calls youtube-api-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso8601
    - requests

  run:
    - python
    - iso8601
    - requests

test:
  # Python imports
  imports:
    - youtube
    - youtube.data
    - youtube.data.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lalo73/youtube-api/
  license: BSD License
  summary: 'Python Youtube Data API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
