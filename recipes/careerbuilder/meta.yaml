package:
  name: careerbuilder
  version: "0.1"

source:
  fn: careerbuilder-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/careerbuilder/careerbuilder-0.1.tar.gz
  md5: c7b163ae61a0f8389eb4767488b4c6dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - careerbuilder = careerbuilder:main
    #
    # Would create an entry point called careerbuilder that calls careerbuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests ==2.1.0
    - xmltodict ==0.8.3

  run:
    - python
    - requests ==2.1.0
    - xmltodict ==0.8.3

test:
  # Python imports
  imports:
    - careerbuilder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neutroninteractive/careerbuilder-python.git
  license: UNKNOWN
  summary: 'CareerBuilder API Wrapper for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
