package:
  name: django-cloud-browser
  version: "0.2.3"

source:
  fn: django-cloud-browser-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cloud-browser/django-cloud-browser-0.2.3.tar.gz
  md5: 5467db50afb1f2fa8689bc185af1815f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cloud-browser = django-cloud-browser:main
    #
    # Would create an entry point called django-cloud-browser that calls django-cloud-browser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - cloud_browser
    - cloud_browser.cloud
    - cloud_browser.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ryan-roemer.github.com/django-cloud-browser
  license: MIT License
  summary: 'Django Cloud Browser application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
