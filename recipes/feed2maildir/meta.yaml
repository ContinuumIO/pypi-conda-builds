package:
  name: feed2maildir
  version: "0.3.5.2"

source:
  fn: feed2maildir-0.3.5.2.tar.gz
  url: https://pypi.python.org/packages/source/f/feed2maildir/feed2maildir-0.3.5.2.tar.gz
  md5: 837b9e7dbf4244f0403af40bb00821b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feed2maildir = feed2maildir:main
    #
    # Would create an entry point called feed2maildir that calls feed2maildir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feedparser >=5.1.3
    - python-dateutil >=2.2

  run:
    - python
    - feedparser >=5.1.3
    - python-dateutil >=2.2

test:
  # Python imports
  imports:
    - feed2maildir

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sulami/feed2maildir
  license: ISC License (ISCL)
  summary: 'Convert feeds to maildirs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
