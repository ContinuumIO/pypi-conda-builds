package:
  name: tornpg
  version: "0.1.0"

source:
  fn: tornpg-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornpg/tornpg-0.1.0.tar.gz
  md5: 3d9e018dfacb104b539bc3405393d841
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornpg = tornpg:main
    #
    # Would create an entry point called tornpg that calls tornpg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2 >=2.5.4

  run:
    - python
    - psycopg2 >=2.5.4

test:
  # Python imports
  imports:
    - tornpg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markgao/tornpg
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'A lightweight PostgresSQL database adapter wrappered around psycopg2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
