package:
  name: toolboxassistant
  version: "0.1"

source:
  fn: ToolBoxAssistant-0.1.tar.gz
  url: https://pypi.python.org/packages/source/T/ToolBoxAssistant/ToolBoxAssistant-0.1.tar.gz
  md5: c459a70e5dd01709229b06aef072acbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - toolboxassistant = toolboxassistant:main
    #
    # Would create an entry point called toolboxassistant that calls toolboxassistant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ToolBoxAssistant
    - toolboxassistant

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattoufoutu/ToolBoxAssistant
  license: GNU General Public License v3 (GPLv3)
  summary: 'An utility to easily manage your toolbox applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
