package:
  name: django-stachoutils
  version: "1.9.1"

source:
  fn: django-stachoutils-1.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stachoutils/django-stachoutils-1.9.1.tar.gz
  md5: 78897b8acfa995fef1dd19de30a050d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stachoutils = django-stachoutils:main
    #
    # Would create an entry point called django-stachoutils that calls django-stachoutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_stachoutils
    - django_stachoutils.forms
    - django_stachoutils.management
    - django_stachoutils.management.commands
    - django_stachoutils.templatetags
    - django_stachoutils.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Starou/django-stachoutils
  license: BSD License
  summary: 'Commons for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
