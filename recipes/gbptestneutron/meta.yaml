package:
  name: gbptestneutron
  version: "0.9.0"

source:
  fn: gbptestneutron-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gbptestneutron/gbptestneutron-0.9.0.tar.gz
  md5: fb41f5d49cf369b65ffd35bc68898622
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gbptestneutron = gbptestneutron:main
    #
    # Would create an entry point called gbptestneutron that calls gbptestneutron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gbptestneutron

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Gbp Test OpenStack Networking'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
