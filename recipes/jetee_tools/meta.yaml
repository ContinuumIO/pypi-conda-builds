package:
  name: jetee_tools
  version: "0.1.1"

source:
  fn: jetee_tools-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jetee_tools/jetee_tools-0.1.1.tar.gz
  md5: de991ba42431eecd9c2f42271d91a36b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jetee_tools = jetee_tools:main
    #
    # Would create an entry point called jetee_tools that calls jetee_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pydns

  run:
    - python
    - pydns

test:
  # Python imports
  imports:
    - jetee_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WhackoJacko/JeteeTools
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
