package:
  name: uploadview
  version: "0.2.7"

source:
  fn: UploadView-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/U/UploadView/UploadView-0.2.7.tar.gz
  md5: 657894323d0b5028447c45c2c7eb8b78
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uploadview = uploadview:main
    #
    # Would create an entry point called uploadview that calls uploadview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob
    - paste
    - pastescript

  run:
    - python
    - setuptools
    - webob
    - paste
    - pastescript

test:
  # Python imports
  imports:
    - uploader
    - uploadview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org/hg/uploader
  license: GPL
  summary: 'a file uploader app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
