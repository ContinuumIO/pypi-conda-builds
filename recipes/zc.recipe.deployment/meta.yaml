package:
  name: zc.recipe.deployment
  version: "1.2.0"

source:
  fn: zc.recipe.deployment-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.recipe.deployment/zc.recipe.deployment-1.2.0.tar.gz
  md5: 111b63423379ffdd4da6a74bf7a1a61d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.recipe.deployment = zc.recipe.deployment:main
    #
    # Would create an entry point called zc.recipe.deployment that calls zc.recipe.deployment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.recipe
    - zc.recipe.deployment

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/zc.recipe.deployment
  license: ZPL 2.1
  summary: 'ZC Buildout recipe for Unix deployments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
