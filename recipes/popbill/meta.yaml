package:
  name: popbill
  version: "1.1.1"

source:
  fn: popbill-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/popbill/popbill-1.1.1.tar.gz
  md5: d68200ce11bd951ebb3ad4271dc9fc38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - popbill = popbill:main
    #
    # Would create an entry point called popbill that calls popbill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - linkhub

  run:
    - python
    - linkhub

test:
  # Python imports
  imports:
    - popbill

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/linkhub-sdk/Popbill.py
  license: MIT License
  summary: 'Popbill API SDK Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
