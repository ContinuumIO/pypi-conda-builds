package:
  name: django-treeadmin-fork-alt-storage
  version: "0.5"

source:
  fn: django-treeadmin-fork-alt-storage-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-treeadmin-fork-alt-storage/django-treeadmin-fork-alt-storage-0.5.tar.gz
  md5: 192941aca9582416992bb7b69c111774
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-treeadmin-fork-alt-storage = django-treeadmin-fork-alt-storage:main
    #
    # Would create an entry point called django-treeadmin-fork-alt-storage that calls django-treeadmin-fork-alt-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-mptt >=0.5

  run:
    - python
    - django >=1.4
    - django-mptt >=0.5

test:
  # Python imports
  imports:
    - treeadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/KristianOellegaard/django-treeadmin
  license: BSD License
  summary: 'Tree UI for mptt-managed models, extracted from FeinCMS. This is a fork with support for alternative storage engines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
