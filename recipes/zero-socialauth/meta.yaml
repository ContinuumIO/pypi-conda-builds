package:
  name: zero-socialauth
  version: "0.0.2"

source:
  fn: zero-socialauth-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zero-socialauth/zero-socialauth-0.0.2.tar.gz
  md5: 75a296bc27defa7bd8ad957f9b3b5e63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zero-socialauth = zero-socialauth:main
    #
    # Would create an entry point called zero-socialauth that calls zero-socialauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zero-common >=0.1.2
    - django >=1.3.1
    - south >=0.7.3

  run:
    - python
    - zero-common >=0.1.2
    - django >=1.3.1
    - south >=0.7.3

test:
  # Python imports
  imports:
    - socialauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/josezambrana/Zero-Socialauth
  license: apache license v2.0
  summary: 'Simple app to add social authentication in django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
