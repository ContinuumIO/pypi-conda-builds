package:
  name: django-citations
  version: "0.3"

source:
  fn: django-citations-0.3.zip
  url: https://pypi.python.org/packages/source/d/django-citations/django-citations-0.3.zip
  md5: c4ea49c5cd96c7d1c4baaa9b097eece6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-citations = django-citations:main
    #
    # Would create an entry point called django-citations that calls django-citations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - citations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.williamhart.info
  license: MIT License
  summary: 'A simple Django app to include citations and reference lists in your templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
