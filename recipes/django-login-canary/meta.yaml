package:
  name: django-login-canary
  version: "0.1.0"

source:
  fn: django-login-canary-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-login-canary/django-login-canary-0.1.0.tar.gz
  md5: be4a8d7dd20c36e3cd1e45eb9effea93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-login-canary = django-login-canary:main
    #
    # Would create an entry point called django-login-canary that calls django-login-canary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.8

  run:
    - python
    - django >=1.8

test:
  # Python imports
  imports:
    - login_canary
    - login_canary.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/MarkusH/django-login-canary
  license: MIT License
  summary: 'django-login-canary is a reusable Django application to notify users upon successful and failed logins.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
