package:
  name: products.takeaction
  version: "1.0"

source:
  fn: Products.takeaction-1.0.zip
  url: https://pypi.python.org/packages/source/P/Products.takeaction/Products.takeaction-1.0.zip
  md5: ebbab17a68d020915d82b0217e47510f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.takeaction = products.takeaction:main
    #
    # Would create an entry point called products.takeaction that calls products.takeaction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - Products
    - Products.takeaction
    - Products.takeaction.browser
    - Products.takeaction.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/takeaction
  license: GPL
  summary: 'takeaction, content as actions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
