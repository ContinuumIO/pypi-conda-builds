package:
  name: dev_db
  version: "0.3.2"

source:
  fn: dev_db-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dev_db/dev_db-0.3.2.tar.gz
  md5: 4dfecf6d34f0d2855ba362411b07e530
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dev_db = dev_db:main
    #
    # Would create an entry point called dev_db that calls dev_db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dev_db
    - dev_db.management
    - dev_db.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tschellenbach/dev_db
  license: BSD License
  summary: 'Tool to automatically create a development database for local development by sampling your production database.\nIt maintains referential integrity by looking up the dependencies for the selected rows.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
