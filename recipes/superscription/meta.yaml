package:
  name: superscription
  version: "0.1.2"

source:
  fn: superscription-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/superscription/superscription-0.1.2.tar.gz
  md5: 6e13bc3f0888446063328aa9683b8f69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - superscription = superscription:main
    #
    # Would create an entry point called superscription that calls superscription.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - superscription

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shrikant-joshi/superscription
  license: BSD License
  summary: 'Superscriptions: A (super-)thin Python 2.7 wrapper around the Superfeedr PubSubHubbub API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
