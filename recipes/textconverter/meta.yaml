package:
  name: textconverter
  version: "0.1.1"

source:
  fn: textconverter-0.1.1.zip
  url: https://pypi.python.org/packages/source/t/textconverter/textconverter-0.1.1.zip
  md5: ac82207f43dfb16e7d660f629db6c2f5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - textconverter = textconverter:main
    #
    # Would create an entry point called textconverter that calls textconverter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chardet

  run:
    - python
    - setuptools
    - chardet

test:
  # Python imports
  imports:
    - textconverter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ukyo/textconverter
  license: Apache
  summary: 'convert text encoding utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
