package:
  name: irckit
  version: "0.1.1"

source:
  fn: irckit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/irckit/irckit-0.1.1.tar.gz
  md5: c7cd2d234c08ed0d54f0538f5b9535ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - irckit = irckit:main
    #
    # Would create an entry point called irckit that calls irckit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - botnet
    - bots
    - irckit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coleifer/irc/
  license: MIT License
  summary: 'tinkering with a made-from-scratch irc library in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
