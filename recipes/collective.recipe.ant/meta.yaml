package:
  name: collective.recipe.ant
  version: "1.0"

source:
  fn: collective.recipe.ant-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.ant/collective.recipe.ant-1.0.tar.gz
  md5: 8781d51e45e95d49f5e4dfe29c3cc9d4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.ant = collective.recipe.ant:main
    #
    # Would create an entry point called collective.recipe.ant that calls collective.recipe.ant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.ant
    - collective.recipe.ant.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/collective.recipe.ant/
  license: Zope Public License
  summary: 'zc.buildout recipe for building ant (java) projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
