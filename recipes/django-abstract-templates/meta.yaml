package:
  name: django-abstract-templates
  version: "1.2.0"

source:
  fn: django-abstract-templates-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-abstract-templates/django-abstract-templates-1.2.0.tar.gz
  md5: 51f68308c60702eba9c93539140767b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-abstract-templates = django-abstract-templates:main
    #
    # Would create an entry point called django-abstract-templates that calls django-abstract-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - abstract_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tswicegood/django-abstract-templates/
  license: BSD License
  summary: 'Basic structure for building reusable templates in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
