package:
  name: corpkit
  version: "0.22"

source:
  fn: corpkit-0.22.tar.gz
  url: https://pypi.python.org/packages/source/c/corpkit/corpkit-0.22.tar.gz
  md5: 13b22003a3a447bdf648bf601aa037ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corpkit = corpkit:main
    #
    # Would create an entry point called corpkit that calls corpkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib >=1.4.3
    - nltk >=3.0.0

  run:
    - python
    - matplotlib >=1.4.3
    - nltk >=3.0.0

test:
  # Python imports
  imports:
    - corpkit
    - dictionaries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/interrogator/corpkit
  license: MIT
  summary: 'A toolkit for working with parsed corpora'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
