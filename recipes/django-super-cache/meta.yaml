package:
  name: django-super-cache
  version: "0.1a1"

source:
  fn: django-super-cache-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-super-cache/django-super-cache-0.1a1.tar.gz
  md5: 2f28a3cd15d2b644de59c563f6aea864
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-super-cache = django-super-cache:main
    #
    # Would create an entry point called django-super-cache that calls django-super-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - setuptools
    - django >=1.6

test:
  # Python imports
  imports:
    - django_super_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youngershen/django-super-cache
  license: MIT License
  summary: 'a page cache plugin, enable you to cache your single page to file or redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
