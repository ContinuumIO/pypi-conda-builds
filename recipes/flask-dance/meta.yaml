package:
  name: flask-dance
  version: "0.5.1"

source:
  fn: Flask-Dance-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Dance/Flask-Dance-0.5.1.tar.gz
  md5: 42ec2b8d58389eb712b6594fd5a13cad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dance = flask-dance:main
    #
    # Would create an entry point called flask-dance that calls flask-dance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0
    - oauthlib
    - requests-oauthlib
    - flask >=0.7
    - urlobject
    - six
    - lazy

  run:
    - python
    - requests >=2.0
    - oauthlib
    - requests-oauthlib
    - flask >=0.7
    - urlobject
    - six
    - lazy

test:
  # Python imports
  imports:
    - flask_dance
    - flask_dance.consumer
    - flask_dance.consumer.backend
    - flask_dance.contrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/singingwolfboy/flask-dance
  license: MIT License
  summary: u'Doing the OAuth dance with style using Flask, requests, and oauthlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
