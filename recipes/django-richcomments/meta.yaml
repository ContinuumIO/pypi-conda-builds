package:
  name: django-richcomments
  version: "0.0.2"

source:
  fn: django-richcomments-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-richcomments/django-richcomments-0.0.2.tar.gz
  md5: 4e056ef6b63c2b434540f0198431a296
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-richcomments = django-richcomments:main
    #
    # Would create an entry point called django-richcomments that calls django-richcomments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - richcomments
    - richcomments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-richcomments
  license: BSD License
  summary: 'Django app extending the builtin comments framework for AJAX style commenting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
