package:
  name: gevent_inotifyx
  version: "0.1.1"

source:
  fn: gevent_inotifyx-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gevent_inotifyx/gevent_inotifyx-0.1.1.tar.gz
  md5: 05ebaf8c3d69b92283da93a846f78190
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gevent_inotifyx = gevent_inotifyx:main
    #
    # Would create an entry point called gevent_inotifyx that calls gevent_inotifyx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gevent
    - inotifyx

  run:
    - python
    - gevent
    - inotifyx

test:
  # Python imports
  imports:
    - gevent_inotifyx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trendels/gevent_inotifyx
  license: MIT
  summary: 'gevent compatibility for inotifyx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
