package:
  name: collective.portlet.similarcontent
  version: "1.5"

source:
  fn: collective.portlet.similarcontent-1.5.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.similarcontent/collective.portlet.similarcontent-1.5.zip
  md5: 8e5c04721fe41b939c4a3a54bbb387bd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.similarcontent = collective.portlet.similarcontent:main
    #
    # Would create an entry point called collective.portlet.similarcontent that calls collective.portlet.similarcontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.similarcontent
    - collective.portlet.similarcontent.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: ZPL 2.1
  summary: "A Plone portlet that uses the catalog internals to find 'similar' content to the page you are looking at"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
