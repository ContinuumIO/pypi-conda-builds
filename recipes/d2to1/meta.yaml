package:
  name: d2to1
  version: "0.2.11"

source:
  fn: d2to1-0.2.11.tar.gz
  url: https://pypi.python.org/packages/source/d/d2to1/d2to1-0.2.11.tar.gz
  md5: 81addef3dde584ab89b35ada8177c0d0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - d2to1 = d2to1:main
    #
    # Would create an entry point called d2to1 that calls d2to1.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - d2to1
    - d2to1.extern

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/d2to1
  license: BSD License
  summary: "Allows using distutils2-like setup.cfg files for a package's metadata with a distribute/setuptools setup.py"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
