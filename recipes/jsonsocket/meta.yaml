package:
  name: jsonsocket
  version: "1.1"

source:
  fn: JsonSocket-1.1.tar.gz
  url: https://pypi.python.org/packages/source/J/JsonSocket/JsonSocket-1.1.tar.gz
  md5: 8d9c2b8d7461c9a67bbfa058e3ca7dc0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonsocket = jsonsocket:main
    #
    # Would create an entry point called jsonsocket that calls jsonsocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsonsocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cpiekarski.com/2011/05/09/super-easy-python-json-client-server/
  license: Apache Software License
  summary: 'Python JSON Server & Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
