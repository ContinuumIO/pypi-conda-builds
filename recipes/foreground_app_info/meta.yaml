package:
  name: foreground_app_info
  version: "1.20150502.141250"

source:
  fn: foreground_app_info-1.20150502.141250.tar.gz
  url: https://pypi.python.org/packages/source/f/foreground_app_info/foreground_app_info-1.20150502.141250.tar.gz
  md5: 635ebdfd22a44bd416812587a76e4bd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foreground_app_info = foreground_app_info:main
    #
    # Would create an entry point called foreground_app_info that calls foreground_app_info.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - foreground_app_info

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/albertz/foreground_app_info
  license: BSD License
  summary: 'Get details about the application and opened URL which is in foreground'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
