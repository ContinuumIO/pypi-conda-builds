package:
  name: django-url-utils
  version: "1.0.0"

source:
  fn: django-url-utils-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-utils/django-url-utils-1.0.0.tar.gz
  md5: 1d88cc6fc3cd802a2922eee254654522
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-utils = django-url-utils:main
    #
    # Would create an entry point called django-url-utils that calls django-url-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - url_utils
    - url_utils/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brightinteractive/django-url-utils/
  license: BSD
  summary: 'Django template tags for manipulating URLs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
