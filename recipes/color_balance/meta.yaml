package:
  name: color_balance
  version: "0.1.45"

source:
  fn: color_balance-0.1.45.tar.gz
  url: https://pypi.python.org/packages/source/c/color_balance/color_balance-0.1.45.tar.gz
  md5: 892141e190beae96ca95892f1a702df0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - color_balance = color_balance:main
    #
    # Would create an entry point called color_balance that calls color_balance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6
    - gdal >=1.11

  run:
    - python
    - numpy >=1.6
    - gdal >=1.11

test:
  # Python imports
  imports:
    - color_balance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/planetlabs/color_balance
  license: Apache Software License
  summary: 'Color balancing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
