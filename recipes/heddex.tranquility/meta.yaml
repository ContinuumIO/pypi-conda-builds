package:
  name: heddex.tranquility
  version: "1.1"

source:
  fn: heddex.tranquility-1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/heddex.tranquility/heddex.tranquility-1.1.tar.gz
  md5: 2f56c617bb0fa8886e1518e5eb2e2735
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heddex.tranquility = heddex.tranquility:main
    #
    # Would create an entry point called heddex.tranquility that calls heddex.tranquility.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - heddex
    - heddex.tranquility
    - heddex.tranquility.browser
    - heddex.tranquility.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'An installable theme for Plone 3.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
