package:
  name: aldryn-quote
  version: "1.0.2"

source:
  fn: aldryn-quote-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-quote/aldryn-quote-1.0.2.tar.gz
  md5: fc152849d88bbab107ec63b2f4a3722c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-quote = aldryn-quote:main
    #
    # Would create an entry point called aldryn-quote that calls aldryn-quote.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - aldryn_quote
    - aldryn_quote.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/philipp-x/aldryn-quote
  license: BSD License
  summary: 'Easy to use quote plugin for Aldryn and django CMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
