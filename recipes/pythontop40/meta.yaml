package:
  name: pythontop40
  version: "0.1.6"

source:
  fn: pythontop40-0.1.6.zip
  url: https://pypi.python.org/packages/source/p/pythontop40/pythontop40-0.1.6.zip
  md5: 375bd5a773f982000aad5cfdbd103677
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythontop40 = pythontop40:main
    #
    # Would create an entry point called pythontop40 that calls pythontop40.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - arrow ==0.4.4
    - booby >=0.7.0
    - six ==1.8.0
    - future ==0.14.2
    - requests-cache ==0.4.8

  run:
    - python
    - arrow ==0.4.4
    - booby >=0.7.0
    - six ==1.8.0
    - future ==0.14.2
    - requests-cache ==0.4.8

test:
  # Python imports
  imports:
    - pythontop40

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dannygoodall/pythontop40
  license: Apache v2.0
  summary: u'A Python API to access the UK Top 40 album and single charts (Intended to be used as a teaching aid in UK schools)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
