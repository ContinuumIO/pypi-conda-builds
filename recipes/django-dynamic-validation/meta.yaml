package:
  name: django-dynamic-validation
  version: "1.2.3"

source:
  fn: django-dynamic-validation-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamic-validation/django-dynamic-validation-1.2.3.tar.gz
  md5: 6b6c9bc2f177ae30b7e15bd6048190f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-validation = django-dynamic-validation:main
    #
    # Would create an entry point called django-dynamic-validation that calls django-dynamic-validation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5.0
    - django-fields
    - django-autoload
    - django-dynamic-rules >=0.2.0

  run:
    - python
    - django <1.5.0
    - django-fields
    - django-autoload
    - django-dynamic-rules >=0.2.0

test:
  # Python imports
  imports:
    - dynamic_validation
    - dynamic_validation.migrations
    - dynamic_validation.templatetags
    - dynamic_validation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imtapps/django-dynamic-validation
  license: BSD License
  summary: 'Define user generated validation requirements for django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
