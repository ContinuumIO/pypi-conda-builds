package:
  name: django-superview
  version: "0.3.7"

source:
  fn: django-superview-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-superview/django-superview-0.3.7.tar.gz
  md5: b09f29621f37944da13e81903274c163
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-superview = django-superview:main
    #
    # Would create an entry point called django-superview that calls django-superview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - superview
    - superview.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jespino/django-superview
  license: BSD License
  summary: 'Generic View class with extra funionality for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
