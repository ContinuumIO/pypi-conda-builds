package:
  name: apigen
  version: "0.1.5"

source:
  fn: apigen-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/a/apigen/apigen-0.1.5.tar.gz
  md5: 7fda40b7deeaf2187e9d7a8642926159
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apigen = apigen:main
    #
    # Would create an entry point called apigen that calls apigen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-jsonrpc ==0.7.3
    - argparse ==1.2.1

  run:
    - python
    - python-jsonrpc ==0.7.3
    - argparse ==1.2.1

test:
  # Python imports
  imports:
    - apigen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/F483/apigen/
  license: MIT
  summary: 'Easily create a CLI and JSON-RPC interface from a common API definition.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
