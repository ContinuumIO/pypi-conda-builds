package:
  name: django-clean
  version: "0.1.2"

source:
  fn: django-clean-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clean/django-clean-0.1.2.tar.gz
  md5: 820e763ed37c72178e3fe25d993a5294
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clean = django-clean:main
    #
    # Would create an entry point called django-clean that calls django-clean.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clean
    - clean.management
    - clean.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ashchristopher/django-clean
  license: BSD License
  summary: 'Clean out files in a django app using regular expressions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
