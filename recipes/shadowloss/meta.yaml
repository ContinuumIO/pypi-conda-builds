package:
  name: shadowloss
  version: "0.1.1"

source:
  fn: shadowloss-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/shadowloss/shadowloss-0.1.1.tar.gz
  md5: 5f0ff710c6fa155b64556791c2253751
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shadowloss = shadowloss:main
    #
    # Would create an entry point called shadowloss that calls shadowloss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - shadowloss
    - shadowloss.builtinstickfigures
    - shadowloss.external

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metanohi.org/projects/shadowloss/
  license: GNU General Public License (GPL)
  summary: 'A stickman-oriented game against time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
