package:
  name: redis_natives
  version: "0.12"

source:
  fn: redis_natives-0.12.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_natives/redis_natives-0.12.tar.gz
  md5: 0b6b1f9cc9e8547d5a978d156f6d6a6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_natives = redis_natives:main
    #
    # Would create an entry point called redis_natives that calls redis_natives.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_natives

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/peta/redis-natives-py
  license: MIT License or BSD License
  summary: 'Exposes Redis entities as native Python datatypes. Simple, plain but powerful. Supports namespacing, indexing, and some more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
