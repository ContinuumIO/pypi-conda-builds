package:
  name: django-antivirus-field
  version: "0.2.0"

source:
  fn: django-antivirus-field-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-antivirus-field/django-antivirus-field-0.2.0.tar.gz
  md5: 9a70978da546b7f8036bf7eb606fd26f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-antivirus-field = django-antivirus-field:main
    #
    # Would create an entry point called django-antivirus-field that calls django-antivirus-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pyclamd
    - south

  run:
    - python
    - django
    - pyclamd
    - south

test:
  # Python imports
  imports:
    - django_antivirus_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/budurli/django-antivirus-field
  license: MIT
  summary: 'Django FileField with ClamAV protection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
