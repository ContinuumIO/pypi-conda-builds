package:
  name: django-openzoom
  version: "0.1.1"

source:
  fn: django-openzoom-0.1.1.tgz
  url: https://pypi.python.org/packages/source/d/django-openzoom/django-openzoom-0.1.1.tgz
  md5: 0cea6b26a76bcd4263b79b7a938c7feb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-openzoom = django-openzoom:main
    #
    # Would create an entry point called django-openzoom that calls django-openzoom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - openzoom
    - openzoom.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/franckinux/django-openzoom
  license: GNU General Public License (GPL)
  summary: 'Django application for displaying very high resolution images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
