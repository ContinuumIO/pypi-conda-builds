package:
  name: cmakelint
  version: "1.2.01"

source:
  fn: cmakelint-1.2.01.tar.gz
  url: https://pypi.python.org/packages/source/c/cmakelint/cmakelint-1.2.01.tar.gz
  md5: d4c28393b81dca318f4c5ca326c55820
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmakelint = cmakelint:main
    #
    # Would create an entry point called cmakelint that calls cmakelint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmakelint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/richq/cmake-lint
  license: Apache Software License
  summary: 'Static code checker for CMake files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
