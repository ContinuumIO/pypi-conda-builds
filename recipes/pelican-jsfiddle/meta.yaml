package:
  name: pelican-jsfiddle
  version: "0.1.0"

source:
  fn: pelican-jsfiddle-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-jsfiddle/pelican-jsfiddle-0.1.0.tar.gz
  md5: 6449b1465f0d1c42a0d147d810b58fc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-jsfiddle = pelican-jsfiddle:main
    #
    # Would create an entry point called pelican-jsfiddle that calls pelican-jsfiddle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.11

  run:
    - python
    - docutils >=0.11

test:
  # Python imports
  imports:
    - pelican_jsfiddle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/if1live/pelican-jsfiddle
  license: MIT License
  summary: 'Easily embed JSFiddle in your articles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
