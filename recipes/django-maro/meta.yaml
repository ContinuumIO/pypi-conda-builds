package:
  name: django-maro
  version: "0.0.2"

source:
  fn: django-maro-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-maro/django-maro-0.0.2.tar.gz
  md5: c507a344e676537b8dd06fdfc7774513
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-maro = django-maro:main
    #
    # Would create an entry point called django-maro that calls django-maro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - maro
    - maro.love
    - maro.love.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/imagawa_yakata/django-maro
  license: PSL
  summary: '`django-maro` is utility for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
