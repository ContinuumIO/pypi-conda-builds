package:
  name: django-social-poster
  version: "0.1"

source:
  fn: django-social-poster-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-poster/django-social-poster-0.1.tar.gz
  md5: e99ea4377a0422b98dedc3bfeb44aa51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-poster = django-social-poster:main
    #
    # Would create an entry point called django-social-poster that calls django-social-poster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - facepy
    - python-twitter

  run:
    - python
    - django
    - facepy
    - python-twitter

test:
  # Python imports
  imports:
    - social_poster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/raphaa/django-social-poster
  license: BSD License
  summary: 'Signals for posting messages on Facebook and Twitter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
