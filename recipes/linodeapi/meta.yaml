package:
  name: linodeapi
  version: "1.7"

source:
  fn: linodeapi-1.7.tar.gz
  url: https://pypi.python.org/packages/source/l/linodeapi/linodeapi-1.7.tar.gz
  md5: b75841f52d82ba0754fe0f95cee5ba08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linodeapi = linodeapi:main
    #
    # Would create an entry point called linodeapi that calls linodeapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - linodeapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RevelSystems/linodeapi
  license: UNKNOWN
  summary: 'Python API for Linode DNS management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
