package:
  name: koansys.recipe.elasticsearch
  version: "1.0.2"

source:
  fn: koansys.recipe.elasticsearch-1.0.2.zip
  url: https://pypi.python.org/packages/source/k/koansys.recipe.elasticsearch/koansys.recipe.elasticsearch-1.0.2.zip
  md5: 6277944ee1335fd139066b53cbbb3b97
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - koansys.recipe.elasticsearch = koansys.recipe.elasticsearch:main
    #
    # Would create an entry point called koansys.recipe.elasticsearch that calls koansys.recipe.elasticsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - koansys
    - koansys.recipe
    - koansys.recipe.elasticsearch
    - koansys.recipe.elasticsearch.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/koansys.recipe.elasticsearch
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'ZC Buildout recipe for setting up elasticsearch.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
