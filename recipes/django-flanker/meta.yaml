package:
  name: django-flanker
  version: "0.3.0"

source:
  fn: django-flanker-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flanker/django-flanker-0.3.0.tar.gz
  md5: 79f067f7d38062e789ccb8abe25734cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flanker = django-flanker:main
    #
    # Would create an entry point called django-flanker that calls django-flanker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - flanker

  run:
    - python
    - django
    - flanker

test:
  # Python imports
  imports:
    - django_flanker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dmpayton/django-flanker
  license: MIT License
  summary: 'Extended EmailField validation using flanker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
