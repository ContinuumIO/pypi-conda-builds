package:
  name: django-axes-login-actions
  version: "1.0.0"

source:
  fn: django-axes-login-actions-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-axes-login-actions/django-axes-login-actions-1.0.0.tar.gz
  md5: aeb6322cd3efd96974d95bc7c6367602
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-axes-login-actions = django-axes-login-actions:main
    #
    # Would create an entry point called django-axes-login-actions that calls django-axes-login-actions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-axes

  run:
    - python
    - django-axes

test:
  # Python imports
  imports:
    - axes_login_actions
    - axes_login_actions.actions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eht16/django-axes-login-actions
  license: BSD License
  summary: 'Perform one or more actions if someone performed a login, e.g. to the admin interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
