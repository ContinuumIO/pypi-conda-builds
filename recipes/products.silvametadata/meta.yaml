package:
  name: products.silvametadata
  version: "3.0.1"

source:
  fn: Products.SilvaMetadata-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SilvaMetadata/Products.SilvaMetadata-3.0.1.tar.gz
  md5: 2384284827ef03fe6a4128cef61bd974
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.silvametadata = products.silvametadata:main
    #
    # Would create an entry point called products.silvametadata that calls products.silvametadata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - products.formulator
    - five.grok
    - setuptools
    - silva.core.conf
    - silva.core.services
    - silva.core.views
    - zope.interface
    - zope.annotation
    - zope.component
    - zope.event

  run:
    - python
    - products.formulator
    - five.grok
    - setuptools
    - silva.core.conf
    - silva.core.services
    - silva.core.views
    - zope.interface
    - zope.annotation
    - zope.component
    - zope.event

test:
  # Python imports
  imports:
    - Products
    - Products.SilvaMetadata
    - Products.SilvaMetadata.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/Products.SilvaMetadata
  license: BSD License
  summary: 'Metadata support for Silva'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
