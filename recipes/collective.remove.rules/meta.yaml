package:
  name: collective.remove.rules
  version: "0.1"

source:
  fn: collective.remove.rules-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.remove.rules/collective.remove.rules-0.1.zip
  md5: 5476e44f02ae3479180e54239377d33b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.remove.rules = collective.remove.rules:main
    #
    # Would create an entry point called collective.remove.rules that calls collective.remove.rules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.remove
    - collective.remove.rules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.remove/rules/trunk
  license: GPL
  summary: 'Remove rules system from plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
