package:
  name: djhcup_core
  version: "0.20150403"

source:
  fn: djhcup_core-0.20150403.tar.gz
  url: https://pypi.python.org/packages/source/d/djhcup_core/djhcup_core-0.20150403.tar.gz
  md5: a24c4aee187aa515e0160ca0420409ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djhcup_core = djhcup_core:main
    #
    # Would create an entry point called djhcup_core that calls djhcup_core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djhcup_core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'A django-based interface for warehousing HCUP data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
