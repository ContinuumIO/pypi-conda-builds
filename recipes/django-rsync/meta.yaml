package:
  name: django-rsync
  version: "0.1"

source:
  fn: django-rsync-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rsync/django-rsync-0.1.tar.gz
  md5: c35d0b89ea1ce04dd3b3495ded893dec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rsync = django-rsync:main
    #
    # Would create an entry point called django-rsync that calls django-rsync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_rsync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hisie/django-rsync
  license: Apache Software License
  summary: 'Simple deployment script using rsync for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
