package:
  name: plone.recipe.alltests
  version: "1.5"

source:
  fn: plone.recipe.alltests-1.5.zip
  url: https://pypi.python.org/packages/source/p/plone.recipe.alltests/plone.recipe.alltests-1.5.zip
  md5: 1aa29994a0df659fa68c9c51959d6134
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.alltests = plone.recipe.alltests:main
    #
    # Would create an entry point called plone.recipe.alltests that calls plone.recipe.alltests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.alltests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.recipe.alltests
  license: Zope Public License
  summary: 'Buildout recipe for running tests isolated at package boundaries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
