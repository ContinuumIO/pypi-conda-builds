package:
  name: django-outbox
  version: "0.6.0"

source:
  fn: django-outbox-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-outbox/django-outbox-0.6.0.tar.gz
  md5: 39fcaec929fd8e085c050dbad1075914
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-outbox = django-outbox:main
    #
    # Would create an entry point called django-outbox that calls django-outbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_outbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/poiati/django-outbox
  license: MIT License
  summary: 'Django Outbox is an app that enable you to see the emails sent by your project through the web browser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
