package:
  name: file_encryptor
  version: "0.2.9"

source:
  fn: file_encryptor-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/f/file_encryptor/file_encryptor-0.2.9.tar.gz
  md5: e909519331b31cf0507fe90afbd3bbe9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - file_encryptor = file_encryptor:main
    #
    # Would create an entry point called file_encryptor that calls file_encryptor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto >=2.6.1

  run:
    - python
    - pycrypto >=2.6.1

test:
  # Python imports
  imports:
    - file_encryptor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Storj/file-encryptor
  license: FileFields
  summary: 'Tool for convergently encrypting files used by MetaDisk.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
