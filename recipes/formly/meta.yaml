package:
  name: formly
  version: "0.6"

source:
  fn: formly-0.6.tar.gz
  url: https://pypi.python.org/packages/source/f/formly/formly-0.6.tar.gz
  md5: b76c57f6ad1c6cf8b45e80c709c1c11b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formly = formly:main
    #
    # Would create an entry point called formly that calls formly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formly
    - formly.forms
    - formly.utils
    - formly.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eldarion/formly
  license: BSD License
  summary: 'a dynamic form generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
