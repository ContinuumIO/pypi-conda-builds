package:
  name: ckanext-esdstandards
  version: "0.0.6"

source:
  fn: ckanext-esdstandards-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-esdstandards/ckanext-esdstandards-0.0.6.tar.gz
  md5: 36230b39df8fea34e3f5610af517f7aa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-esdstandards = ckanext-esdstandards:main
    #
    # Would create an entry point called ckanext-esdstandards that calls ckanext-esdstandards.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ckanext
    - ckanext-esdstandards-data
    - ckanext.esdstandards
    - ckanext.esdstandards.tests
    - ckanext.esdstandards.tests.mockplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/okfn/ckanext-esdstandards
  license: AGPL
  summary: "Helpers for working with LGA's ESD standards on CKAN"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
