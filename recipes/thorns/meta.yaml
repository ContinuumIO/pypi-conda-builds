package:
  name: thorns
  version: "0.7.1"

source:
  fn: thorns-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/t/thorns/thorns-0.7.1.tar.gz
  md5: 87b271d47264f6101c40c2b9ee40627c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thorns = thorns:main
    #
    # Would create an entry point called thorns that calls thorns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - pandas
    - scipy

  run:
    - python
    - numpy
    - pandas
    - scipy

test:
  # Python imports
  imports:
    - thorns
    - thorns.datasets
    - thorns.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mrkrd/thorns
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Spike analysis software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
