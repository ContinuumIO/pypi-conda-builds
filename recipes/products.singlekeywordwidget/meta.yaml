package:
  name: products.singlekeywordwidget
  version: "1.0a1"

source:
  fn: Products.SingleKeywordWidget-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SingleKeywordWidget/Products.SingleKeywordWidget-1.0a1.tar.gz
  md5: 7cec8463f2da8926864a82b180f6a9d1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.singlekeywordwidget = products.singlekeywordwidget:main
    #
    # Would create an entry point called products.singlekeywordwidget that calls products.singlekeywordwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.SingleKeywordWidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://raptus.com
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
