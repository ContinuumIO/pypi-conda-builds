package:
  name: chinese-rfid-access-control-library
  version: "0.0.6"

source:
  fn: Chinese-RFID-Access-Control-Library-0.0.6.zip
  url: https://pypi.python.org/packages/source/C/Chinese-RFID-Access-Control-Library/Chinese-RFID-Access-Control-Library-0.0.6.zip
  md5: 823f0ee22faa6bd834801acdf586b12a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chinese-rfid-access-control-library = chinese-rfid-access-control-library:main
    #
    # Would create an entry point called chinese-rfid-access-control-library that calls chinese-rfid-access-control-library.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pawl/Chinese-RFID-Access-Control-Library
  license: chinese-slugify
  summary: 'A library for interfacing with one of the most common RFID Access Control System sold in China.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
