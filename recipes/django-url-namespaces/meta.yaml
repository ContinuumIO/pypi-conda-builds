package:
  name: django-url-namespaces
  version: "0.1.0"

source:
  fn: django-url-namespaces-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-namespaces/django-url-namespaces-0.1.0.tar.gz
  md5: 4fef15ca007f4107d3d2014f408a7131
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-namespaces = django-url-namespaces:main
    #
    # Would create an entry point called django-url-namespaces that calls django-url-namespaces.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - url_namespaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fish2000/django-url-namespaces/
  license: MIT License
  summary: 'Declarative class-based datastructures, for simple and flexible URLConfs in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
