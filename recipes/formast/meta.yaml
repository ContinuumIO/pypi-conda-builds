package:
  name: formast
  version: "0.1.0a1"

source:
  fn: formast-0.1.0a1.tar.bz2
  url: https://pypi.python.org/packages/source/f/formast/formast-0.1.0a1.tar.bz2
  md5: 5dd0a747ab3039c350705066cc0945c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formast = formast:main
    #
    # Would create an entry point called formast that calls formast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formast

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amorilia/formast
  license: BSD License
  summary: u'FormAST exposes file format descriptions through a simple API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
