package:
  name: kitchensinkrpc
  version: "0.2"

source:
  fn: kitchensinkRPC-0.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kitchensinkRPC/kitchensinkRPC-0.2.tar.gz
  md5: ad30f318c0cd51e3591d267a6b775f82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kitchensinkrpc = kitchensinkrpc:main
    #
    # Would create an entry point called kitchensinkrpc that calls kitchensinkrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - kitchensink
    - kitchensink.admin
    - kitchensink.api
    - kitchensink.clients
    - kitchensink.data
    - kitchensink.rpc
    - kitchensink.scripts
    - kitchensink.serialization
    - kitchensink.taskqueue
    - kitchensink.testutils
    - kitchensink.utils
    - kitchensinkrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hhuuggoo/kitchensink
  license: New BSD
  summary: 'Kitchen Sink'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
