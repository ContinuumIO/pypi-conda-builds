package:
  name: django-shop-saferpay
  version: "1.2.1"

source:
  fn: django-shop-saferpay-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-saferpay/django-shop-saferpay-1.2.1.tar.gz
  md5: 8146131077fb3cf6701030aefb65418b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-saferpay = django-shop-saferpay:main
    #
    # Would create an entry point called django-shop-saferpay that calls django-shop-saferpay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-shop
    - requests

  run:
    - python
    - django >=1.2
    - django-shop
    - requests

test:
  # Python imports
  imports:
    - saferpay_backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piquadrat/django-shop-saferpay
  license: BSD License
  summary: 'A django SHOP payment backend for SaferPay'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
