package:
  name: lazr.lifecycle
  version: "0.1"

source:
  fn: lazr.lifecycle-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lazr.lifecycle/lazr.lifecycle-0.1.tar.gz
  md5: 483583598048fee2c60b6ed1f0a6b948
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazr.lifecycle = lazr.lifecycle:main
    #
    # Would create an entry point called lazr.lifecycle that calls lazr.lifecycle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.lifecycleevent
    - zope.security

  run:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.lifecycleevent
    - zope.security

test:
  # Python imports
  imports:
    - lazr
    - lazr.lifecycle
    - lazr.lifecycle.docs
    - lazr.lifecycle.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lazr.lifecycle
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Richer lifecycle events API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
