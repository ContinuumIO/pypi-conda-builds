package:
  name: verktyg
  version: "0.4.0"

source:
  fn: verktyg-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/v/verktyg/verktyg-0.4.0.tar.gz
  md5: b7aab9590c360af72fd6cea962655a92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - verktyg = verktyg:main
    #
    # Would create an entry point called verktyg that calls verktyg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.9,<0.10
    - python-mimeparse >=0.1.4

  run:
    - python
    - werkzeug >=0.9,<0.10
    - python-mimeparse >=0.1.4

test:
  # Python imports
  imports:
    - verktyg
    - verktyg.testsuite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bwhmather/verktyg
  license: BSD License
  summary: 'A web framework based on Werkzeug'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
