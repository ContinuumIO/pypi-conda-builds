package:
  name: django-gravatar-tags
  version: "1.0.0"

source:
  fn: django-gravatar-tags-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gravatar-tags/django-gravatar-tags-1.0.0.tar.gz
  md5: 16b3cec3db20611747ebc553f042de7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gravatar-tags = django-gravatar-tags:main
    #
    # Would create an entry point called django-gravatar-tags that calls django-gravatar-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_gravatar_tags
    - django_gravatar_tags.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/DrMegahertz/django-gravatar-tags
  license: BSD License
  summary: 'Easy to use Gravatar template tags for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
