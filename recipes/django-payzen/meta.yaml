package:
  name: django-payzen
  version: "1.0.1"

source:
  fn: django-payzen-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-payzen/django-payzen-1.0.1.tar.gz
  md5: 19269e6127d76e074f76b430911b2148
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-payzen = django-payzen:main
    #
    # Would create an entry point called django-payzen that calls django-payzen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_payzen
    - django_payzen.migrations
    - django_payzen.templatetags
    - django_payzen.tests
    - django_payzen.tests.payzen_init
    - django_payzen.tests.payzen_response
    - django_payzen.tests.request_data
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bsvetchine/django-payzen
  license: MIT
  summary: 'Django app to manage payments with Payzen ETP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
