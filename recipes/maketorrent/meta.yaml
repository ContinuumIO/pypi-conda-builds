package:
  name: maketorrent
  version: "0.11"

source:
  fn: makeTorrent-0.11.tar.gz
  url: https://pypi.python.org/packages/source/m/makeTorrent/makeTorrent-0.11.tar.gz
  md5: 4f01a0282692f204a83c0cc9d1bd23f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maketorrent = maketorrent:main
    #
    # Would create an entry point called maketorrent that calls maketorrent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - bencode

  run:
    - python
    - bencode

test:
  # Python imports
  imports:
    - makeTorrent
    - maketorrent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stewrutledge/makeTorrent
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'Basic library for creating torrents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
