package:
  name: abe
  version: "0.7.2"

source:
  fn: Abe-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/A/Abe/Abe-0.7.2.tar.gz
  md5: 046467ee8723adda26c1bf988ca0d2f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abe = abe:main
    #
    # Would create an entry point called abe that calls abe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - Abe
    - abe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jtobey/bitcoin-abe
  license: GNU Affero General Public License v3
  summary: 'Abe: a free block chain browser for Bitcoin-based currencies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
