package:
  name: redturtle.catalogmount
  version: "0.0.3"

source:
  fn: redturtle.catalogmount-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.catalogmount/redturtle.catalogmount-0.0.3.tar.gz
  md5: b265a124ebaafb4d9cc95dc98b883bdd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.catalogmount = redturtle.catalogmount:main
    #
    # Would create an entry point called redturtle.catalogmount that calls redturtle.catalogmount.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.catalogmount
    - redturtle.catalogmount.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.redturtle.net
  license: GPL
  summary: 'Small package which helps mount portal_catalog to separate ZODB.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
