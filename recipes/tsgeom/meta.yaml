package:
  name: tsgeom
  version: "0.1.5"

source:
  fn: TSGeom-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/T/TSGeom/TSGeom-0.1.5.tar.gz
  md5: 8c066f45888b03445097cf0fc28c60a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tsgeom = tsgeom:main
    #
    # Would create an entry point called tsgeom that calls tsgeom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.8.0

  run:
    - python
    - numpy >=1.8.0

test:
  # Python imports
  imports:
    - tsgeom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbn/TSGeom
  license: BSD-new license
  summary: 'Python package for computing simple geometries on TS data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
