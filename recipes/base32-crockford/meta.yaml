package:
  name: base32-crockford
  version: "0.3.0"

source:
  fn: base32-crockford-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/base32-crockford/base32-crockford-0.3.0.tar.gz
  md5: c6e65112793ffab0150e33739c030cb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - base32-crockford = base32-crockford:main
    #
    # Would create an entry point called base32-crockford that calls base32-crockford.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbittel/base32-crockford
  license: BSD License
  summary: "A Python implementation of Douglas Crockford's base32 encoding scheme"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
