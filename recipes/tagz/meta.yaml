package:
  name: tagz
  version: "0.0.12"

source:
  fn: tagz-0.0.12.tar.gz
  url: https://pypi.python.org/packages/source/t/tagz/tagz-0.0.12.tar.gz
  md5: a9c7a99f2cf1cb2f57930ea7665844d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tagz = tagz:main
    #
    # Would create an entry point called tagz that calls tagz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argh >=0.15.1
    - pybean >=0.1.1
    - hsaudiotag

  run:
    - python
    - argh >=0.15.1
    - pybean >=0.1.1
    - hsaudiotag

test:
  # Python imports
  imports:
    - tagz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Music collection utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
