package:
  name: pyphylip
  version: "0.0.2"

source:
  fn: pyphylip-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyphylip/pyphylip-0.0.2.tar.gz
  md5: ee1abeed9fabd9724b804114e72a7edf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyphylip = pyphylip:main
    #
    # Would create an entry point called pyphylip that calls pyphylip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyphylip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/pyphylip
  license: MIT
  summary: 'a python module to read and write phylip alignment files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
