package:
  name: chartio
  version: "5.0.0"

source:
  fn: chartio-5.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chartio/chartio-5.0.0.tar.gz
  md5: d58e2ff6ed7c4922ebe38da7c01b908b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chartio = chartio:main
    #
    # Would create an entry point called chartio that calls chartio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chartio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://chartio.com/
  license: UNKNOWN
  summary: 'Setup wizard and connection client for connecting MySQL/PostgreSQL databases to Chartio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
