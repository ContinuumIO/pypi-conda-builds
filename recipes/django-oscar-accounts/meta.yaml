package:
  name: django-oscar-accounts
  version: "0.3"

source:
  fn: django-oscar-accounts-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oscar-accounts/django-oscar-accounts-0.3.tar.gz
  md5: 79d98de704e75237ff5e9d72f108a17e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oscar-accounts = django-oscar-accounts:main
    #
    # Would create an entry point called django-oscar-accounts that calls django-oscar-accounts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-oscar >=0.5
    - python-dateutil >=2.1,<2.2

  run:
    - python
    - django-oscar >=0.5
    - python-dateutil >=2.1,<2.2

test:
  # Python imports
  imports:
    - accounts
    - accounts.api
    - accounts.checkout
    - accounts.dashboard
    - accounts.management
    - accounts.management.commands
    - accounts.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Managed accounts for django-oscar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
