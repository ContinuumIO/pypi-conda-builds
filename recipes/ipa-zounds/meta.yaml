package:
  name: ipa-zounds
  version: "3.1"

source:
  fn: ipa-zounds-3.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ipa-zounds/ipa-zounds-3.1.tar.gz
  md5: ab5c9faba5ef24c760d11e3fd01cac5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipa-zounds = ipa-zounds:main
    #
    # Would create an entry point called ipa-zounds that calls ipa-zounds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ipazounds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zounds.artefact.org.nz/
  license: GNU General Public License (GPL)
  summary: 'IPA Zounds sound change applier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
