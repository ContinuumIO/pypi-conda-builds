package:
  name: django-yapi
  version: "0.3.4"

source:
  fn: django-yapi-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yapi/django-yapi-0.3.4.tar.gz
  md5: 638c86950d0e7991be2c834de7c24f5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yapi = django-yapi:main
    #
    # Would create an entry point called django-yapi that calls django-yapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - yapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A mini-framework for Django for creating RESTful APIs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
