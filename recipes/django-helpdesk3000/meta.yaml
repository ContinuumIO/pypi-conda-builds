package:
  name: django-helpdesk3000
  version: "0.2.5"

source:
  fn: django-helpdesk3000-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-helpdesk3000/django-helpdesk3000-0.2.5.tar.gz
  md5: 10492299319b1fac87f715b8db5c5fe9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-helpdesk3000 = django-helpdesk3000:main
    #
    # Would create an entry point called django-helpdesk3000 that calls django-helpdesk3000.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0
    - six >=1.7.2

  run:
    - python
    - django >=1.4.0
    - six >=1.7.2

test:
  # Python imports
  imports:
    - helpdesk
    - helpdesk.management
    - helpdesk.management.commands
    - helpdesk.migrations
    - helpdesk.templatetags
    - helpdesk.tests
    - helpdesk.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisspen/django-helpdesk3000
  license: BSD License
  summary: 'Django-powered ticket tracker for your helpdesk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
