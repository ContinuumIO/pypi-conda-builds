package:
  name: django-dumpdata-field
  version: "0.1"

source:
  fn: django-dumpdata-field-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dumpdata-field/django-dumpdata-field-0.1.tar.gz
  md5: eec9909a70f20bd65852adbb25d2f361
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dumpdata-field = django-dumpdata-field:main
    #
    # Would create an entry point called django-dumpdata-field that calls django-dumpdata-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dumpdata_field
    - dumpdata_field.management
    - dumpdata_field.management.commands
    - dumpdata_field.tests
    - dumpdata_field.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-dumpdata-field
  license: The MIT License
  summary: 'custom dumpdata and loaddata commands that allow to export given fields of a model\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
