package:
  name: django-yandex-money
  version: "1.1.3"

source:
  fn: django-yandex-money-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yandex-money/django-yandex-money-1.1.3.tar.gz
  md5: bc915ead23d256630fc87f3e78fa9205
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yandex-money = django-yandex-money:main
    #
    # Would create an entry point called django-yandex-money that calls django-yandex-money.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >1.5
    - south ==0.8.4
    - lxml ==3.3.4
    - django-webtest ==1.7.7
    - webtest ==2.0.15
    - django-annoying

  run:
    - python
    - django >1.5
    - south ==0.8.4
    - lxml ==3.3.4
    - django-webtest ==1.7.7
    - webtest ==2.0.15
    - django-annoying

test:
  # Python imports
  imports:
    - yandex_money

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DrMartiner/django-yandex-money
  license: MIT License
  summary: 'Integrating django project with yandex-money'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
