package:
  name: collective.editmodeswitcher
  version: "1.0.1"

source:
  fn: collective.editmodeswitcher-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.editmodeswitcher/collective.editmodeswitcher-1.0.1.zip
  md5: dcab845324421f1d559a25712f1dbbb7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.editmodeswitcher = collective.editmodeswitcher:main
    #
    # Would create an entry point called collective.editmodeswitcher that calls collective.editmodeswitcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope2
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope2
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.editmodeswitcher
    - collective.editmodeswitcher.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/collective.editmodeswitcher
  license: GPL2
  summary: 'Allows to toggle edit mode in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
