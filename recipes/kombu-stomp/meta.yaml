package:
  name: kombu-stomp
  version: "0.0.0.dev4"

source:
  fn: kombu-stomp-0.0.0.dev4.tar.gz
  url: https://pypi.python.org/packages/source/k/kombu-stomp/kombu-stomp-0.0.0.dev4.tar.gz
  md5: 5aaa47dd52d065cca0673cc575ae1bae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kombu-stomp = kombu-stomp:main
    #
    # Would create an entry point called kombu-stomp that calls kombu-stomp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stomp.py
    - six
    - kombu

  run:
    - python
    - stomp.py
    - six
    - kombu

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
