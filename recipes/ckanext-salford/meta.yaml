package:
  name: ckanext-salford
  version: "0.0.2"

source:
  fn: ckanext-salford-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-salford/ckanext-salford-0.0.2.tar.gz
  md5: 16958a7d6ac6845223bfb235a7e90ad9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-salford = ckanext-salford:main
    #
    # Would create an entry point called ckanext-salford that calls ckanext-salford.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ckanext
    - ckanext.salford
    - ckanext.salford.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/okfn/ckanext-salford
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
