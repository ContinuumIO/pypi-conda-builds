package:
  name: sqlite3dbm
  version: "0.1.4"

source:
  fn: sqlite3dbm-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlite3dbm/sqlite3dbm-0.1.4.tar.gz
  md5: fc2f8fb09a4bbc0260b97e835b369184
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlite3dbm = sqlite3dbm:main
    #
    # Would create an entry point called sqlite3dbm that calls sqlite3dbm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlite3dbm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Yelp/sqlite3dbm/
  license: Apache Software License
  summary: 'sqlite-backed dictionary'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
