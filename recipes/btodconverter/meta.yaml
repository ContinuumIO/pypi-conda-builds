package:
  name: btodconverter
  version: "0.1"

source:
  fn: BtoDConverter-0.1.zip
  url: https://pypi.python.org/packages/source/B/BtoDConverter/BtoDConverter-0.1.zip
  md5: 50a89dd4a563e84b8eff82abaf85db7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - btodconverter = btodconverter:main
    #
    # Would create an entry point called btodconverter that calls btodconverter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - nose

test:
  # Python imports
  imports:
    - BtoDConverter
    - btodconverter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: none
  license: UNKNOWN
  summary: 'Converts a binary number to a decimal number'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
