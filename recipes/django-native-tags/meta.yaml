package:
  name: django-native-tags
  version: "0.5.3"

source:
  fn: django-native-tags-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-native-tags/django-native-tags-0.5.3.tar.gz
  md5: 0d99dc2a5de534b543021d4f3b0583c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-native-tags = django-native-tags:main
    #
    # Would create an entry point called django-native-tags that calls django-native-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - native_tags
    - native_tags.contrib
    - native_tags.management
    - native_tags.management.commands
    - native_tags.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/justquick/django-native-tags
  license: BSD License
  summary: 'Native, Pythonic Templatetags for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
