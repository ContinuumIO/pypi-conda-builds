package:
  name: ub-split-map
  version: "0.1.1"

source:
  fn: ub-split-map-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/u/ub-split-map/ub-split-map-0.1.1.tar.gz
  md5: 25815cb042c9caba120186597a4e67c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ub-split-map = ub-split-map:main
    #
    # Would create an entry point called ub-split-map that calls ub-split-map.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stuffivelearned.org
  license: GNU General Public License v2 (GPLv2)
  summary: 'A python module for Unbound to dynamically map external IPs to internal RFC 1918 addresses to avoid split-horizon DNS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
