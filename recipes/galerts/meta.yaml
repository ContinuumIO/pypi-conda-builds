package:
  name: galerts
  version: "0.2.4dev"

source:
  fn: galerts-0.2.4dev.tar.gz
  url: https://pypi.python.org/packages/source/g/galerts/galerts-0.2.4dev.tar.gz
  md5: 88e2a4b32abe5b0a592931f0d1c40ebe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - galerts = galerts:main
    #
    # Would create an entry point called galerts that calls galerts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup

  run:
    - python
    - beautifulsoup

test:
  # Python imports
  imports:
    - galerts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/galerts
  license: MIT License
  summary: 'Python libary for managing Google Alerts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
