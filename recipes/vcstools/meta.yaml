package:
  name: vcstools
  version: "0.1.36"

source:
  fn: vcstools-0.1.36.tar.gz
  url: https://pypi.python.org/packages/source/v/vcstools/vcstools-0.1.36.tar.gz
  md5: e3ef997da464cf284ca36fd60e993ed6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vcstools = vcstools:main
    #
    # Would create an entry point called vcstools that calls vcstools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - python-dateutil

  run:
    - python
    - pyyaml
    - python-dateutil

test:
  # Python imports
  imports:
    - vcstools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.ros.org/vcstools
  license: BSD License
  summary: 'VCS/SCM source control library for svn, git, hg, and bzr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
