package:
  name: bernoulli
  version: "0.1.6"

source:
  fn: bernoulli-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/b/bernoulli/bernoulli-0.1.6.tar.gz
  md5: 6739f161e0e02ab86e97a218689443b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bernoulli = bernoulli:main
    #
    # Would create an entry point called bernoulli that calls bernoulli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - bernoulli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bernoulli-metrics/bernoulli-python
  license: MIT
  summary: 'A Python API for Bernoulli'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
