package:
  name: django-bigfoot
  version: "0.2"

source:
  fn: django-bigfoot-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bigfoot/django-bigfoot-0.2.tar.gz
  md5: fe81f7318ccc37daa07b6df2d6c45ab2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bigfoot = django-bigfoot:main
    #
    # Would create an entry point called django-bigfoot that calls django-bigfoot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-tables2 ==0.13

  run:
    - python
    - django-tables2 ==0.13

test:
  # Python imports
  imports:
    - bigfoot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/setaris/django-bigfoot
  license: UNKNOWN
  summary: 'A Django library for defining HTML building blocks (e.g., forms, form fields, tables) that can be easily rendered into HTML.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
