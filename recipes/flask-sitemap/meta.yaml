package:
  name: flask-sitemap
  version: "0.1.0"

source:
  fn: Flask-Sitemap-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Sitemap/Flask-Sitemap-0.1.0.tar.gz
  md5: 42a9669d1cec9d351540159dcb4b5eab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sitemap = flask-sitemap:main
    #
    # Would create an entry point called flask-sitemap that calls flask-sitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - blinker

  run:
    - python
    - flask
    - blinker

test:
  # Python imports
  imports:
    - flask_sitemap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/inveniosoftware/flask-sitemap/
  license: BSD License
  summary: 'Flask extension that helps with sitemap generation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
