package:
  name: automium_web
  version: "0.1.1"

source:
  fn: automium_web-0.1.1.zip
  url: https://pypi.python.org/packages/source/a/automium_web/automium_web-0.1.1.zip
  md5: d063163d76de8541016a0609c540e15c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - automium_web = automium_web:main
    #
    # Would create an entry point called automium_web that calls automium_web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - automium
    - flask

  run:
    - python
    - automium
    - flask

test:
  # Python imports
  imports:
    - automium_web
    - models
    - quorum
    - views
    - views.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://automium.com
  license: GNU General Public License (GPL)
  summary: 'Automium System Web Interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
