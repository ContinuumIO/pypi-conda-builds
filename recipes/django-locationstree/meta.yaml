package:
  name: django-locationstree
  version: "0.0.0.3"

source:
  fn: django-locationstree-0.0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-locationstree/django-locationstree-0.0.0.3.tar.gz
  md5: b4bab6cb6c22d2037c07a3ada1b7fb89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-locationstree = django-locationstree:main
    #
    # Would create an entry point called django-locationstree that calls django-locationstree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-mptt >=0.5.5

  run:
    - python
    - django >=1.4
    - django-mptt >=0.5.5

test:
  # Python imports
  imports:
    - locationstree
    - locationstree.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/witoi/django-locationstree
  license: UNKNOWN
  summary: u'Simple Django App for location tree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
