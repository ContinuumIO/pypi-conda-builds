package:
  name: middlecoords
  version: "1.0.0"

source:
  fn: middlecoords-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/middlecoords/middlecoords-1.0.0.tar.gz
  md5: d573ba21f3a596c95e18d1c9538912a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - middlecoords = middlecoords:main
    #
    # Would create an entry point called middlecoords that calls middlecoords.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - middlecoords

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Cybran111/MiddleCoords
  license: BSD License
  summary: 'Function which calculates coordinates between given positions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
