package:
  name: circulardependencyb
  version: "0.0.0"

source:
  fn: CircularDependencyB-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/C/CircularDependencyB/CircularDependencyB-0.0.0.tar.gz
  md5: 7fe4305707468f58c3b2380968f8ac6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circulardependencyb = circulardependencyb:main
    #
    # Would create an entry point called circulardependencyb that calls circulardependencyb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - circulardependencya

  run:
    - python
    - circulardependencya

test:
  # Python imports
  imports:
    - circulardependencyb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/msabramo/CircularDependencyA
  license: MIT
  summary: 'CircularDependency{A,B} depend on each other; for testing packaging tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
