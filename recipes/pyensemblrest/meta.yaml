package:
  name: pyensemblrest
  version: "0.1.7b"

source:
  fn: pyensemblrest-0.1.7b.tar.gz
  url: https://pypi.python.org/packages/source/p/pyensemblrest/pyensemblrest-0.1.7b.tar.gz
  md5: 2f6608cce21240d7993208948afbdcad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyensemblrest = pyensemblrest:main
    #
    # Would create an entry point called pyensemblrest that calls pyensemblrest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0,<2.0.0

  run:
    - python
    - requests >=1.0.0,<2.0.0

test:
  # Python imports
  imports:
    - ensemblrest
    - pyensemblrest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pyopensci/pyensemblrest/tree/master
  license: GNU General Public License v3 (GPLv3)
  summary: 'An easy way to access EnsEMBL data with Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
