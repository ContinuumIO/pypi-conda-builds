package:
  name: django-addresses
  version: "0.1"

source:
  fn: django-addresses-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-addresses/django-addresses-0.1.tar.gz
  md5: cb8eb10fae579442fb28e84355e18f54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-addresses = django-addresses:main
    #
    # Would create an entry point called django-addresses that calls django-addresses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - addressbook
    - addressbook.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simonluijk/django-addresses
  license: BSD License
  summary: 'Some forms around a few models to manage addresses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
