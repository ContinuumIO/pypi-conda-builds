package:
  name: flask-metaroute
  version: "1.3.0"

source:
  fn: Flask-MetaRoute-1.3.0.zip
  url: https://pypi.python.org/packages/source/F/Flask-MetaRoute/Flask-MetaRoute-1.3.0.zip
  md5: 3cf3c7162c28872eac10569b18859285
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-metaroute = flask-metaroute:main
    #
    # Would create an entry point called flask-metaroute that calls flask-metaroute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - setuptools
    - flask

test:
  # Python imports
  imports:
    - flask_metaroute

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/deep-orca/Flask-MetaRoute
  license: BSD License
  summary: 'Extra routing capabilities for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
