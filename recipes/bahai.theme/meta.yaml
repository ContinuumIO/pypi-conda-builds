package:
  name: bahai.theme
  version: "1.0.3"

source:
  fn: bahai.theme-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bahai.theme/bahai.theme-1.0.3.tar.gz
  md5: 991311b8e7a1cfeaf47524de5637b87b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bahai.theme = bahai.theme:main
    #
    # Would create an entry point called bahai.theme that calls bahai.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plonetheme.classic

  run:
    - python
    - setuptools
    - setuptools
    - plonetheme.classic

test:
  # Python imports
  imports:
    - bahai
    - bahai.theme
    - bahai.theme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Theme for Bahai.dk site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
