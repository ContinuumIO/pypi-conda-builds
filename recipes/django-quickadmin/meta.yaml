package:
  name: django-quickadmin
  version: "0.1.2"

source:
  fn: django-quickadmin-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quickadmin/django-quickadmin-0.1.2.tar.gz
  md5: 89f3ce011c7fd4f7f29c383aaa22936f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quickadmin = django-quickadmin:main
    #
    # Would create an entry point called django-quickadmin that calls django-quickadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - quickadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zniper/django-quickadmin
  license: MIT
  summary: 'Django application automatically registers all found models into admin area'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
