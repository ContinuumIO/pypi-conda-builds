package:
  name: collective.archiveviewer
  version: "1.0b2"

source:
  fn: collective.archiveviewer-1.0b2.zip
  url: https://pypi.python.org/packages/source/c/collective.archiveviewer/collective.archiveviewer-1.0b2.zip
  md5: 3eafe7620be3afdc527181fd9d2c91c0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.archiveviewer = collective.archiveviewer:main
    #
    # Would create an entry point called collective.archiveviewer that calls collective.archiveviewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.archiveviewer
    - collective.archiveviewer.browser
    - collective.archiveviewer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.archiveviewer
  license: GPL
  summary: 'Allows to upload archive files (check readme for supported types)       and access their contents trough the web, without having to download and extract them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
