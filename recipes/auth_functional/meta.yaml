package:
  name: auth_functional
  version: "0.1.0dev"

source:
  fn: auth_functional-0.1.0dev.tar.gz
  url: https://pypi.python.org/packages/source/a/auth_functional/auth_functional-0.1.0dev.tar.gz
  md5: 940bd30e31e2eed37ed159ce611ec523
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auth_functional = auth_functional:main
    #
    # Would create an entry point called auth_functional that calls auth_functional.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - auth_functional

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ikame/django-auth-functional
  license: MIT License
  summary: 'Functional library for working with authentication and authorization in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
