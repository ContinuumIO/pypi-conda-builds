package:
  name: docbook2epub
  version: "1.0.1dev-r138"

source:
  fn: docbook2epub-1.0.1dev-r138.tar.gz
  url: https://pypi.python.org/packages/source/d/docbook2epub/docbook2epub-1.0.1dev-r138.tar.gz
  md5: e0fbeb90caa0775c493aaecd8091cd8b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docbook2epub = docbook2epub:main
    #
    # Would create an entry point called docbook2epub that calls docbook2epub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.1.2
    - epubtools

  run:
    - python
    - setuptools
    - lxml >=2.1.2
    - epubtools

test:
  # Python imports
  imports:
    - docbook2epub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/epub-tools/
  license: New BSD
  summary: 'Converts DocBook 4/5 documents into ePub. Requires the DocBook XSL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
