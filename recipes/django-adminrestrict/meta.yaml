package:
  name: django-adminrestrict
  version: "1.0.1"

source:
  fn: django-adminrestrict-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adminrestrict/django-adminrestrict-1.0.1.tar.gz
  md5: 67511325da2a06c54a874de483226edb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adminrestrict = django-adminrestrict:main
    #
    # Would create an entry point called django-adminrestrict that calls django-adminrestrict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adminrestrict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robromano/django-adminrestrict
  license: MIT License
  summary: 'Block .'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
