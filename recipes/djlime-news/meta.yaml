package:
  name: djlime-news
  version: "0.9.1"

source:
  fn: djlime-news-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djlime-news/djlime-news-0.9.1.tar.gz
  md5: b39118257fa0f94dbd3d17e2eb429d72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djlime-news = djlime-news:main
    #
    # Would create an entry point called djlime-news that calls djlime-news.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djlime >=0.0.8
    - django-imagekit ==2.0.1

  run:
    - python
    - djlime >=0.0.8
    - django-imagekit ==2.0.1

test:
  # Python imports
  imports:
    - news

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/whitespy/djlime-news
  license: BSD License
  summary: 'The application of news.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
