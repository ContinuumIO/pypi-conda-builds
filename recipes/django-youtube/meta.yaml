package:
  name: django-youtube
  version: "0.2"

source:
  fn: django-youtube-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-youtube/django-youtube-0.2.tar.gz
  md5: 954702fcb430f768cc44d34a1c649a08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-youtube = django-youtube:main
    #
    # Would create an entry point called django-youtube that calls django-youtube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_youtube
    - django_youtube.tastypie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/laplacesdemon/django-youtube
  license: BSD licence, see LICENCE.txt
  summary: 'Youtube API wrapper app for Django. It helps to upload, display, delete, update videos from Youtube'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
