package:
  name: barbeque
  version: "0.2.1"

source:
  fn: barbeque-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/barbeque/barbeque-0.2.1.tar.gz
  md5: d31491f54564fb0ea7c74f9d20cff2b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barbeque = barbeque:main
    #
    # Would create an entry point called barbeque that calls barbeque.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.8

  run:
    - python
    - django >=1.6,<1.8

test:
  # Python imports
  imports:
    - barbeque
    - barbeque.cms
    - barbeque.commands
    - barbeque.management
    - barbeque.management.commands
    - barbeque.templatetags
    - barbeque.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/moccu/barbeque/
  license: BSD License
  summary: 'Helper and tools collection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
