package:
  name: django-nanolog
  version: "0.5"

source:
  fn: django-nanolog-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nanolog/django-nanolog-0.5.tar.gz
  md5: ecdfcf5e54bb16ceac752022bca4d432
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nanolog = django-nanolog:main
    #
    # Would create an entry point called django-nanolog that calls django-nanolog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nanolog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/DNX/django-nanolog
  license: BSD License
  summary: 'A simple Django app to manage simple logs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
