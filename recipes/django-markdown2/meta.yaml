package:
  name: django-markdown2
  version: "0.2.1"

source:
  fn: django-markdown2-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markdown2/django-markdown2-0.2.1.tar.gz
  md5: 519205edb4c09490eac9534435080118
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markdown2 = django-markdown2:main
    #
    # Would create an entry point called django-markdown2 that calls django-markdown2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown2

  run:
    - python
    - markdown2

test:
  # Python imports
  imports:
    - django_markdown2
    - django_markdown2.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-markdown2/
  license: BSD License
  summary: 'This is a simple app, which supplies a single template tag for markdown markup.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
