package:
  name: django-anysign
  version: "0.3"

source:
  fn: django-anysign-0.3.zip
  url: https://pypi.python.org/packages/source/d/django-anysign/django-anysign-0.3.zip
  md5: d1266885c6a205f2f8658041b739175a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-anysign = django-anysign:main
    #
    # Would create an entry point called django-anysign that calls django-anysign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django <1.7
    - setuptools

  run:
    - python
    - django <1.7
    - setuptools

test:
  # Python imports
  imports:
    - django_anysign
    - django_dummysign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Django application to manage online signature in a generic way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
