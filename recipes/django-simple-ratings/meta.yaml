package:
  name: django-simple-ratings
  version: "0.3.3"

source:
  fn: django-simple-ratings-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-ratings/django-simple-ratings-0.3.3.tar.gz
  md5: 775717d41e6bbb44aa9a49faf18490a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-ratings = django-simple-ratings:main
    #
    # Would create an entry point called django-simple-ratings that calls django-simple-ratings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-generic-aggregation

  run:
    - python
    - django-generic-aggregation

test:
  # Python imports
  imports:
    - ratings
    - ratings.management
    - ratings.management.commands
    - ratings.migrations
    - ratings.ratings_tests
    - ratings.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/django-de/django-simple-ratings/tree/master
  license: MIT License
  summary: 'a simple, extensible rating system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
