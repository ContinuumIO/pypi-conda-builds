package:
  name: pystretch
  version: "0.2.1"

source:
  fn: PyStretch-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/P/PyStretch/PyStretch-0.2.1.tar.gz
  md5: ff349b79b458b02c5f05391cda0cf45e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystretch = pystretch:main
    #
    # Would create an entry point called pystretch that calls pystretch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pystretch
    - pystretch.core
    - pystretch.custom
    - pystretch.filter
    - pystretch.linear
    - pystretch.masks
    - pystretch.nonlinear
    - pystretch.plot
    - pystretch.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/PyStretch/
  license: LICENSE.txt
  summary: 'Python image analysis and manipulation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
