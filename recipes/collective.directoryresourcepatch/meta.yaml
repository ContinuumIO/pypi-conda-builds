package:
  name: collective.directoryresourcepatch
  version: "1.1"

source:
  fn: collective.directoryresourcepatch-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.directoryresourcepatch/collective.directoryresourcepatch-1.1.tar.gz
  md5: 214022d12c8ed281e06963aa85058eca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.directoryresourcepatch = collective.directoryresourcepatch:main
    #
    # Would create an entry point called collective.directoryresourcepatch that calls collective.directoryresourcepatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - collective
    - collective.directoryresourcepatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.directoryresourcepatch/
  license: GPL
  summary: 'Patch for DirectoryResource to allow traversal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
