package:
  name: lastopt
  version: "0.0.2"

source:
  fn: lastopt-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lastopt/lastopt-0.0.2.tar.gz
  md5: f78393c73c86b7e6b1bab6b69434ae10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lastopt = lastopt:main
    #
    # Would create an entry point called lastopt that calls lastopt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lastopt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cablehead/lastopt
  license: MIT
  summary: 'lastopt is a Python utility for quickly creating command line utilities using'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
