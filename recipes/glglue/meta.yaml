package:
  name: glglue
  version: "0.3.1"

source:
  fn: glglue-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/g/glglue/glglue-0.3.1.tar.gz
  md5: 735148818d73dff0366299cc26fffd66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glglue = glglue:main
    #
    # Would create an entry point called glglue that calls glglue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - glglue
    - glglue.sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: zlib/libpng License
  summary: 'The glue code which mediates between OpenGL and some GUI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
