package:
  name: ajenti.plugin.ace
  version: "0.14"

source:
  fn: ajenti.plugin.ace-0.14.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.ace/ajenti.plugin.ace-0.14.tar.gz
  md5: dbc18d34f4cd8d412d3226fee09b12f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.ace = ajenti.plugin.ace:main
    #
    # Would create an entry point called ajenti.plugin.ace that calls ajenti.plugin.ace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core

  run:
    - python
    - aj
    - ajenti.plugin.core

test:
  # Python imports
  imports:
    - ajenti_plugin_ace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Ace editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
