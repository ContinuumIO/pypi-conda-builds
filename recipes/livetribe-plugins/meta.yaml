package:
  name: livetribe-plugins
  version: "1.0a1"

source:
  fn: livetribe-plugins-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/l/livetribe-plugins/livetribe-plugins-1.0a1.tar.gz
  md5: e3d34e7287b63eb12d4550131ec8e307
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - livetribe-plugins = livetribe-plugins:main
    #
    # Would create an entry point called livetribe-plugins that calls livetribe-plugins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - livetribe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/livetribe/livetribe-plugins/
  license: Apache Software License
  summary: 'A simple Python plugin framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
