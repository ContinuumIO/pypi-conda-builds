package:
  name: django-mynewsdesk
  version: "0.4"

source:
  fn: django-mynewsdesk-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mynewsdesk/django-mynewsdesk-0.4.tar.gz
  md5: 9a8c77a712daf7a32184b53c5b7ff879
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mynewsdesk = django-mynewsdesk:main
    #
    # Would create an entry point called django-mynewsdesk that calls django-mynewsdesk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - requests >=1.0.3

  run:
    - python
    - django >=1.4.3
    - requests >=1.0.3

test:
  # Python imports
  imports:
    - mynewsdesk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pkozlov/django-mynewsdesk
  license: MIT License
  summary: 'Django package for MyNewsDesk API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
