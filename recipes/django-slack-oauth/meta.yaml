package:
  name: django-slack-oauth
  version: "0.1"

source:
  fn: django-slack-oauth-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-slack-oauth/django-slack-oauth-0.1.tar.gz
  md5: 561ba6c3e8c2ec908909f27a4b19273e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-slack-oauth = django-slack-oauth:main
    #
    # Would create an entry point called django-slack-oauth that calls django-slack-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.1
    - requests

  run:
    - python
    - django >=1.6.1
    - requests

test:
  # Python imports
  imports:
    - django_slack_oauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Handles OAuth and stores slack token'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
