package:
  name: djb.headerproxy
  version: "0.1.1"

source:
  fn: djb.headerproxy-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/djb.headerproxy/djb.headerproxy-0.1.1.zip
  md5: 5f4cdaf9c2a48a4fc290041ccc25f1b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djb.headerproxy = djb.headerproxy:main
    #
    # Would create an entry point called djb.headerproxy that calls djb.headerproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - paste

  run:
    - python
    - setuptools
    - setuptools
    - paste

test:
  # Python imports
  imports:
    - djb
    - djb.headerproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/davidjb/djb.headerproxy/
  license: gpl
  summary: 'Paste proxies with the ability to proxy to a location based upon an incoming header'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
