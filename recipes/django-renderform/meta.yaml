package:
  name: django-renderform
  version: "0.1"

source:
  fn: django-renderform-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-renderform/django-renderform-0.1.tar.gz
  md5: 94d34fec41754e387a464dd52f4ddcbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-renderform = django-renderform:main
    #
    # Would create an entry point called django-renderform that calls django-renderform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - renderform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-renderform/
  license: MIT License
  summary: 'Helpers for rendering Django forms in a flexible way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
