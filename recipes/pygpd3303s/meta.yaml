package:
  name: pygpd3303s
  version: "1.1.3"

source:
  fn: PyGPD3303S-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/P/PyGPD3303S/PyGPD3303S-1.1.3.tar.gz
  md5: 97bd92e6e5b1823be611e18c2408e401
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygpd3303s = pygpd3303s:main
    #
    # Would create an entry point called pygpd3303s that calls pygpd3303s.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - pygpd3303s

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://sourceforge.net/p/pygpd3303s/
  license: BSD License
  summary: 'Python Interface for DC power supply GPD-3303S'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
