package:
  name: products.salesforcebaseconnector
  version: "1.4"

source:
  fn: Products.salesforcebaseconnector-1.4.zip
  url: https://pypi.python.org/packages/source/P/Products.salesforcebaseconnector/Products.salesforcebaseconnector-1.4.zip
  md5: a809d4905b9d17d383477feee0f3a696
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.salesforcebaseconnector = products.salesforcebaseconnector:main
    #
    # Would create an entry point called products.salesforcebaseconnector that calls products.salesforcebaseconnector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beatbox >=16.0dev

  run:
    - python
    - setuptools
    - setuptools
    - beatbox >=16.0dev

test:
  # Python imports
  imports:
    - Products
    - Products.salesforcebaseconnector
    - Products.salesforcebaseconnector.interfaces
    - Products.salesforcebaseconnector.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://groups.google.com/group/plonesf
  license: GPL
  summary: 'Provides access to the Salesforce API within Zope 2, based on the beatbox library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
