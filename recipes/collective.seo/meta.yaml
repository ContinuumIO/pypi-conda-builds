package:
  name: collective.seo
  version: "1.1"

source:
  fn: collective.seo-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.seo/collective.seo-1.1.tar.gz
  md5: 898f4e89f7aa6f6c1e34b670c8edf963
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.seo = collective.seo:main
    #
    # Would create an entry point called collective.seo that calls collective.seo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.configviews

  run:
    - python
    - setuptools
    - setuptools
    - collective.configviews

test:
  # Python imports
  imports:
    - collective
    - collective.seo
    - collective.seo.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.seo
  license: GPL
  summary: 'SEO add-on let you set values of some html header tags.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
