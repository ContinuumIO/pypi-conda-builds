package:
  name: google-api-python-client-py3
  version: "1.2"

source:
  fn: google-api-python-client-py3-1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/google-api-python-client-py3/google-api-python-client-py3-1.2.tar.gz
  md5: fc91ac855f30e56c2dc3aa258cea7163
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google-api-python-client-py3 = google-api-python-client-py3:main
    #
    # Would create an entry point called google-api-python-client-py3 that calls google-api-python-client-py3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.8

  run:
    - python
    - httplib2 >=0.8

test:
  # Python imports
  imports:
    - apiclient
    - oauth2client
    - uritemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/google-api-python-client/
  license: Apache Software License
  summary: 'Google API Client Library for Python (python 3x port)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
