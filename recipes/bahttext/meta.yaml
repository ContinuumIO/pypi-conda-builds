package:
  name: bahttext
  version: "1.0.1"

source:
  fn: bahttext-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bahttext/bahttext-1.0.1.tar.gz
  md5: cec3d29c871f6381d91d96aed48390e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bahttext = bahttext:main
    #
    # Would create an entry point called bahttext that calls bahttext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bahttext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/morangesolution/bahttext
  license: BSD
  summary: 'Tool for convert currency number to Thai text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
