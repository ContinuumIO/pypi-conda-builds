package:
  name: django-generic-images
  version: "0.36.1"

source:
  fn: django-generic-images-0.36.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-images/django-generic-images-0.36.1.tar.gz
  md5: 3a39b51c41296617cdcd1700b9f83aa5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-images = django-generic-images:main
    #
    # Would create an entry point called django-generic-images that calls django-generic-images.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - generic_images
    - generic_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-generic-images/
  license: BSD License
  summary: 'Generic images pluggable django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
