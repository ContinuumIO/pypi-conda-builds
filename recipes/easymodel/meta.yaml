package:
  name: easymodel
  version: "0.3.0"

source:
  fn: easymodel-0.3.0.zip
  url: https://pypi.python.org/packages/source/e/easymodel/easymodel-0.3.0.zip
  md5: 2158a68bfb0f9daafb9e46eab568a5a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easymodel = easymodel:main
    #
    # Would create an entry point called easymodel that calls easymodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyside

  run:
    - python
    - pyside

test:
  # Python imports
  imports:
    - easymodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/storax/easymodel
  license: BSD License
  summary: 'Qt Models and Views made easy with general purpose Model and a Widget delegate.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
