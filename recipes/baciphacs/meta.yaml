package:
  name: baciphacs
  version: "1.0"

source:
  fn: baciphacs-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/baciphacs/baciphacs-1.0.tar.gz
  md5: affdbad32dd02592f0de3f90030316e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baciphacs = baciphacs:main
    #
    # Would create an entry point called baciphacs that calls baciphacs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - baciphacs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tibonihoo/baciphacs
  license: BSD License
  summary: 'Bar Charts In Pure HTML And CSS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
