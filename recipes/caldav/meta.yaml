package:
  name: caldav
  version: "0.4.0"

source:
  fn: caldav-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/caldav/caldav-0.4.0.tar.gz
  md5: 0ebae509fb16e1ee7c113639c1567645
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caldav = caldav:main
    #
    # Would create an entry point called caldav that calls caldav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vobject
    - lxml
    - nose
    - coverage
    - requests
    - six

  run:
    - python
    - vobject
    - lxml
    - nose
    - coverage
    - requests
    - six

test:
  # Python imports
  imports:
    - caldav
    - caldav.elements
    - caldav.lib
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/cyrilrbt/caldav
  license: GNU General Public License (GPL) or Apache Software License
  summary: 'CalDAV (RFC4791) client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
