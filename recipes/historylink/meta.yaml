package:
  name: historylink
  version: "0.0.1"

source:
  fn: historylink-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/historylink/historylink-0.0.1.tar.gz
  md5: d5366500c9902219aeccc9244c1e14c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - historylink = historylink:main
    #
    # Would create an entry point called historylink that calls historylink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - historylink
    - historylink.management
    - historylink.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oxys-net/django-historylink
  license: UNKNOWN
  summary: 'Store model url change, od a permanent redirect on 404'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
