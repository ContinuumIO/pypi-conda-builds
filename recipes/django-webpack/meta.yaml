package:
  name: django-webpack
  version: "3.1.1"

source:
  fn: django-webpack-3.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webpack/django-webpack-3.1.1.tar.gz
  md5: d430f1fa74e38b0bc49f998974749353
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webpack = django-webpack:main
    #
    # Would create an entry point called django-webpack that calls django-webpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-node ==4.0.0
    - optional-django ==0.1.0

  run:
    - python
    - django
    - django-node ==4.0.0
    - optional-django ==0.1.0

test:
  # Python imports
  imports:
    - django_webpack
    - django_webpack.services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markfinger/django-webpack
  license: UNKNOWN
  summary: 'Generate Webpack bundles from a Django application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
