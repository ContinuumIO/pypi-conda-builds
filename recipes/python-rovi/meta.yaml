package:
  name: python-rovi
  version: "0.0.3"

source:
  fn: python-rovi-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/python-rovi/python-rovi-0.0.3.tar.gz
  md5: 3c821858c4cf969993d9c69d6df9422e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-rovi = python-rovi:main
    #
    # Would create an entry point called python-rovi that calls python-rovi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=0.13.3

  run:
    - python
    - requests >=0.13.3

test:
  # Python imports
  imports:
    - rovi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GetBlimp/rovi-tv-listings
  license: MIT License
  summary: 'Python Wrapper for Rovi TV Listings API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
