package:
  name: django-chalk
  version: "0.2.1"

source:
  fn: django-chalk-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chalk/django-chalk-0.2.1.tar.gz
  md5: d071ceeacf9247072ee823f83f88f848
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chalk = django-chalk:main
    #
    # Would create an entry point called django-chalk that calls django-chalk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.9
    - pygments >=1.4

  run:
    - python
    - docutils >=0.9
    - pygments >=1.4

test:
  # Python imports
  imports:
    - chalk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kevindias.com/projects/chalk/
  license: BSD License
  summary: 'Simple reStructuredText blogging for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
