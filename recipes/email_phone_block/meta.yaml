package:
  name: email_phone_block
  version: "0.5.1"

source:
  fn: email_phone_block-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/e/email_phone_block/email_phone_block-0.5.1.tar.gz
  md5: d224487426e720f98b087184c3c595a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - email_phone_block = email_phone_block:main
    #
    # Would create an entry point called email_phone_block that calls email_phone_block.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - email_phone_block

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mdgart/email_phone_block
  license: UNKNOWN
  summary: 'Substitute emails and phone numbers with dummy in your text.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
