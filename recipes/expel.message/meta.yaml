package:
  name: expel.message
  version: "0.2"

source:
  fn: expel.message-0.2.zip
  url: https://pypi.python.org/packages/source/e/expel.message/expel.message-0.2.zip
  md5: 5db9e9cc0799c20d8c3e7828e5ac4a29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expel.message = expel.message:main
    #
    # Would create an entry point called expel.message that calls expel.message.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing

  run:
    - python
    - pyparsing

test:
  # Python imports
  imports:
    - expel
    - expel.message

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sffjunkie.co.uk/python-expel.html
  license: Apache Software License
  summary: 'expel.message is part of the expel namespace package and\n    provides classes to manipulate xPL messages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
