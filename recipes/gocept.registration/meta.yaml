package:
  name: gocept.registration
  version: "0.3.0"

source:
  fn: gocept.registration-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.registration/gocept.registration-0.3.0.tar.gz
  md5: f54585320f9eba88b6077fee7738d14c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.registration = gocept.registration:main
    #
    # Would create an entry point called gocept.registration that calls gocept.registration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.app.container
    - zope.component
    - zope.interface
    - zope.sendmail
    - z3c.form

  run:
    - python
    - setuptools
    - zope.app.container
    - zope.component
    - zope.interface
    - zope.sendmail
    - z3c.form

test:
  # Python imports
  imports:
    - gocept
    - gocept.registration
    - gocept.registration.browser
    - gocept.registration.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.registration/
  license: Zope Public License
  summary: 'User self-registration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
