package:
  name: birdhousebuilder.recipe.tomcat
  version: "0.2.1"

source:
  fn: birdhousebuilder.recipe.tomcat-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/birdhousebuilder.recipe.tomcat/birdhousebuilder.recipe.tomcat-0.2.1.tar.gz
  md5: b59f368315585f9a1b43688653835862
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - birdhousebuilder.recipe.tomcat = birdhousebuilder.recipe.tomcat:main
    #
    # Would create an entry point called birdhousebuilder.recipe.tomcat that calls birdhousebuilder.recipe.tomcat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - mako
    - birdhousebuilder.recipe.conda
    - birdhousebuilder.recipe.supervisor

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - mako
    - birdhousebuilder.recipe.conda
    - birdhousebuilder.recipe.supervisor

test:
  # Python imports
  imports:
    - birdhousebuilder
    - birdhousebuilder.recipe
    - birdhousebuilder.recipe.tomcat
    - birdhousebuilder.recipe.tomcat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bird-house/birdhousebuilder.recipe.tomcat
  license: BSD License
  summary: 'A Buildout recipe to install and configure tomcat for Anaconda.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
