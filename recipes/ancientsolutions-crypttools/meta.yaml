package:
  name: ancientsolutions-crypttools
  version: "0.1"

source:
  fn: ancientsolutions-crypttools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/ancientsolutions-crypttools/ancientsolutions-crypttools-0.1.tar.gz
  md5: 049a67ededcd01b2afc34c9960949be2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ancientsolutions-crypttools = ancientsolutions-crypttools:main
    #
    # Would create an entry point called ancientsolutions-crypttools that calls ancientsolutions-crypttools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ancientsolutions.crypttools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://python.ancient-solutions.com/crypttools/
  license: BSD License
  summary: 'Python Cryptography Utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
