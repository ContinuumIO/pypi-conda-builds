package:
  name: easyirc
  version: "0.1.1"

source:
  fn: easyirc-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyirc/easyirc-0.1.1.tar.gz
  md5: fd126706325b3433bb336daecb9c56e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyirc = easyirc:main
    #
    # Would create an entry point called easyirc that calls easyirc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - prettyexc
    - cacheobj ==0.8.2

  run:
    - python
    - distribute
    - prettyexc
    - cacheobj ==0.8.2

test:
  # Python imports
  imports:
    - easyirc
    - easyirc/client
    - easyirc/command
    - easyirc/event

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youknowone/easyirc
  license: UNKNOWN
  summary: 'Easy IRC is an IRC toolkit to develop IRC client or bot, especially for Python/IRC beginner.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
