package:
  name: combination_all
  version: "1.0.5"

source:
  fn: combination_all-1.0.5.zip
  url: https://pypi.python.org/packages/source/c/combination_all/combination_all-1.0.5.zip
  md5: 45659e60a0301ced17156ba30accf9c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - combination_all = combination_all:main
    #
    # Would create an entry point called combination_all that calls combination_all.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - combination_all

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cadcam.yonsei.ac.kr
  license: UNKNOWN
  summary: 'If you have list that contains "n" data, you will get nC1 + nC2 + ... nCn for output'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
