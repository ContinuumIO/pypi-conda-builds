package:
  name: zope.componentvocabulary
  version: "2.0.0"

source:
  fn: zope.componentvocabulary-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.componentvocabulary/zope.componentvocabulary-2.0.0.tar.gz
  md5: f998e280d868050bd5d829eb70c316d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.componentvocabulary = zope.componentvocabulary:main
    #
    # Would create an entry point called zope.componentvocabulary that calls zope.componentvocabulary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.security

  run:
    - python
    - setuptools
    - six
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.security

test:
  # Python imports
  imports:
    - zope
    - zope.componentvocabulary
    - zope.componentvocabulary.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.componentvocabulary
  license: Zope Public License
  summary: 'Component vocabularies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
