package:
  name: isotoma.recipe.apache
  version: "1.0.4"

source:
  fn: isotoma.recipe.apache-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.apache/isotoma.recipe.apache-1.0.4.tar.gz
  md5: 49fd66bea51d3ed106ec104929b2ec91
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.apache = isotoma.recipe.apache:main
    #
    # Would create an entry point called isotoma.recipe.apache that calls isotoma.recipe.apache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - jinja2
    - missingbits >=0.0.14

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - jinja2
    - missingbits >=0.0.14

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.apache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.apache
  license: Apache Software License
  summary: 'Buildout recipes for apache.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
