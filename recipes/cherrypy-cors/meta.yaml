package:
  name: cherrypy-cors
  version: "1.3"

source:
  fn: cherrypy-cors-1.3.zip
  url: https://pypi.python.org/packages/source/c/cherrypy-cors/cherrypy-cors-1.3.zip
  md5: b1c4fca6932bf83e03eb61da4c07c94b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherrypy-cors = cherrypy-cors:main
    #
    # Would create an entry point called cherrypy-cors that calls cherrypy-cors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy >=3,<4
    - httpagentparser >=1.5

  run:
    - python
    - cherrypy >=3,<4
    - httpagentparser >=1.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/yougov/cherrypy-cors/
  license: Apache License 2.0
  summary: 'CORS handling as a cherrypy tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
