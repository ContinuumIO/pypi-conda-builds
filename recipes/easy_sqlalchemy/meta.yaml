package:
  name: easy_sqlalchemy
  version: "0.1.9"

source:
  fn: easy_sqlalchemy-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/e/easy_sqlalchemy/easy_sqlalchemy-0.1.9.tar.gz
  md5: de7ba8fd0445cedf83a5fd5cc1449c8e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy_sqlalchemy = easy_sqlalchemy:main
    #
    # Would create an entry point called easy_sqlalchemy that calls easy_sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - zope.sqlalchemy
    - ipy

  run:
    - python
    - setuptools
    - sqlalchemy
    - zope.sqlalchemy
    - ipy

test:
  # Python imports
  imports:
    - easy_sqlalchemy
    - easy_sqlalchemy.types

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lxneng/easy_sqlalchemy
  license: BSD License
  summary: 'SQLAlchemy wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
