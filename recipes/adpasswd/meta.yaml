package:
  name: adpasswd
  version: "0.2"

source:
  fn: adpasswd-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/adpasswd/adpasswd-0.2.tar.gz
  md5: e97db53f58bedc2646f7bc6218d92188
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adpasswd = adpasswd:main
    #
    # Would create an entry point called adpasswd that calls adpasswd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - adpasswd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/adpasswd
  license: GNU General Public License (GPL)
  summary: 'adpasswd.py Pure Python Command line interface to change Active Directory Passwords via LDAP.\n\tusage: adpasswd.py username [password]'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
