package:
  name: coaster
  version: "0.4.3"

source:
  fn: coaster-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/c/coaster/coaster-0.4.3.tar.gz
  md5: b807fbc3b26f7e6f9893e76491b2787e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coaster = coaster:main
    #
    # Would create an entry point called coaster that calls coaster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - flask
    - markupsafe
    - werkzeug
    - simplejson
    - flask-assets
    - semantic_version
    - pytz
    - markdown >=2.4.1
    - bleach
    - pygments
    - docflow
    - sqlalchemy
    - flask-sqlalchemy
    - flask-script ==0.5.3
    - webassets
    - flask-assets
    - pyexecjs
    - uglipyjs
    - tldextract
    - unidecode
    - py-bcrypt

  run:
    - python
    - six
    - flask
    - markupsafe
    - werkzeug
    - simplejson
    - flask-assets
    - semantic_version
    - pytz
    - markdown >=2.4.1
    - bleach
    - pygments
    - docflow
    - sqlalchemy
    - flask-sqlalchemy
    - flask-script ==0.5.3
    - webassets
    - flask-assets
    - pyexecjs
    - uglipyjs
    - tldextract
    - unidecode
    - py-bcrypt

test:
  # Python imports
  imports:
    - coaster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hasgeek/coaster
  license: BSD License
  summary: 'Coaster for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
