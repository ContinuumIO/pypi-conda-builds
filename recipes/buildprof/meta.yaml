package:
  name: buildprof
  version: "0.1.8"

source:
  fn: buildprof-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/b/buildprof/buildprof-0.1.8.tar.gz
  md5: c256d2e0c5d2b19e564557c05df4260f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildprof = buildprof:main
    #
    # Would create an entry point called buildprof that calls buildprof.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - buildprof

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/buildprof
  license: GNU GPL
  summary: u'Build profiling tools and UI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
