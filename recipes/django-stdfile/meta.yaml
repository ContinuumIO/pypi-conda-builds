package:
  name: django-stdfile
  version: "0.5"

source:
  fn: django-stdfile-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stdfile/django-stdfile-0.5.tar.gz
  md5: 08cd37a2df7d2136430a764ca463eae0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stdfile = django-stdfile:main
    #
    # Would create an entry point called django-stdfile that calls django-stdfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stdfile
    - stdfile.db
    - stdfile.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://tracpub.yaco.es/djangoapps/wiki/StdFile
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application that improves management of FileFields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
