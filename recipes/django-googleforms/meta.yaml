package:
  name: django-googleforms
  version: "0.8"

source:
  fn: django-googleforms-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googleforms/django-googleforms-0.8.tar.gz
  md5: 8106864eb6ad28ab3c84e727bcc01dc4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googleforms = django-googleforms:main
    #
    # Would create an entry point called django-googleforms that calls django-googleforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - googleforms
    - googleforms.forms
    - googleforms.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matllubos/django-googleforms
  license: GNU General Public License (GPL)
  summary: 'Application which help with parsing google forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
