package:
  name: schachtlervs
  version: "1.0.0"

source:
  fn: schachtlervs-1.0.0.zip
  url: https://pypi.python.org/packages/source/s/schachtlervs/schachtlervs-1.0.0.zip
  md5: b5a9d359b94f16d733d88f587f16e554
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - schachtlervs = schachtlervs:main
    #
    # Would create an entry point called schachtlervs that calls schachtlervs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - schachtlervs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wwww.aguynamedvincent.com
  license: UNKNOWN
  summary: 'Einfache Ausgabe geschachtelter Lsiten'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
