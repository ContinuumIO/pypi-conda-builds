package:
  name: ezdxf
  version: "0.6.5"

source:
  fn: ezdxf-0.6.5.tar.gz
  url: https://pypi.python.org/packages/source/e/ezdxf/ezdxf-0.6.5.tar.gz
  md5: bd471bd7d433e085b8f6d11ab39055b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezdxf = ezdxf:main
    #
    # Would create an entry point called ezdxf that calls ezdxf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing >=2.0.1

  run:
    - python
    - pyparsing >=2.0.1

test:
  # Python imports
  imports:
    - ezdxf
    - ezdxf.legacy
    - ezdxf.modern

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mozman/ezdxf
  license: MIT License
  summary: 'A Python package to create/manipulate DXF drawings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
