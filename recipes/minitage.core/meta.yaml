package:
  name: minitage.core
  version: "2.0.57"

source:
  fn: minitage.core-2.0.57.zip
  url: https://pypi.python.org/packages/source/m/minitage.core/minitage.core-2.0.57.zip
  md5: 073b25550a0fb0e65f2ae507ea09ab9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minitage.core = minitage.core:main
    #
    # Would create an entry point called minitage.core that calls minitage.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - minitage

  run:
    - python
    - minitage

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/minitage.core
  license: BSD
  summary: 'moved to minitage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
