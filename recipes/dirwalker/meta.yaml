package:
  name: dirwalker
  version: "0.4.1"

source:
  fn: dirwalker-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dirwalker/dirwalker-0.4.1.tar.gz
  md5: a4e9f415be8b5b38df0c4ef8e22f8039
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dirwalker = dirwalker:main
    #
    # Would create an entry point called dirwalker that calls dirwalker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dirwalker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/questrail/dirwalker
  license: MIT License
  summary: 'Python Directory Walker module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
