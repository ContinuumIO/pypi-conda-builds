package:
  name: haikunator
  version: "0.2.1"

source:
  fn: haikunator-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/haikunator/haikunator-0.2.1.tar.gz
  md5: 838c8c6e04ab00248e26fbc4b10a7a42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haikunator = haikunator:main
    #
    # Would create an entry point called haikunator that calls haikunator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - haikunator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Atrox/haikunatorpy
  license: GNU General Public License v3 (GPLv3)
  summary: 'Heroku-like random name generator for python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
