package:
  name: ftw.tika
  version: "2.0.1"

source:
  fn: ftw.tika-2.0.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.tika/ftw.tika-2.0.1.zip
  md5: 779607e7b155900b9ded883b03a98dba
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.tika = ftw.tika:main
    #
    # Would create an entry point called ftw.tika that calls ftw.tika.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - requests
    - zope.component
    - zope.interface
    - zope.schema
    - zodb3
    - products.genericsetup
    - products.portaltransforms

  run:
    - python
    - setuptools
    - setuptools
    - requests
    - zope.component
    - zope.interface
    - zope.schema
    - zodb3
    - products.genericsetup
    - products.portaltransforms

test:
  # Python imports
  imports:
    - ftw
    - ftw.tika
    - ftw.tika.mimetypes
    - ftw.tika.tests
    - ftw.tika.transforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.tika
  license: GNU General Public License (GPL)
  summary: 'Apache Tika integration for Plone using portal transforms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
