package:
  name: safe_access
  version: "1.1"

source:
  fn: safe_access-1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/safe_access/safe_access-1.1.tar.gz
  md5: cc571c11b621ae5ab1d4b2fa9dae588b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - safe_access = safe_access:main
    #
    # Would create an entry point called safe_access that calls safe_access.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - safe_access

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jwegan/safe_access_py
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Tool to make drilling into Python objects easy & painless'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
