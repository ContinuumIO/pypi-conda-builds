package:
  name: django-change-email
  version: "0.1.3"

source:
  fn: django-change-email-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-change-email/django-change-email-0.1.3.tar.gz
  md5: 93e91750f15129bc8c754926ad5bed86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-change-email = django-change-email:main
    #
    # Would create an entry point called django-change-email that calls django-change-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5,<=1.6
    - django-easysettings

  run:
    - python
    - django >=1.5,<=1.6
    - django-easysettings

test:
  # Python imports
  imports:
    - change_email
    - change_email.management
    - change_email.management.commands
    - change_email.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tarak/django-change-email
  license: UNKNOWN
  summary: 'A Django application to enable users to change their e-mail address.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
