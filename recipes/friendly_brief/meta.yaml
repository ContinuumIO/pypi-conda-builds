package:
  name: friendly_brief
  version: "0.0.5"

source:
  fn: friendly_brief-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/friendly_brief/friendly_brief-0.0.5.tar.gz
  md5: 99119979ec93bf353ca6495c044ce926
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - friendly_brief = friendly_brief:main
    #
    # Would create an entry point called friendly_brief that calls friendly_brief.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - unidecode >=0.04.16
    - sliding_window

  run:
    - python
    - unidecode >=0.04.16
    - sliding_window

test:
  # Python imports
  imports:
    - friendly_brief

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/friendly_brief
  license: AGPL
  summary: 'Find amici curiae in briefs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
