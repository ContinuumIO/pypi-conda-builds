package:
  name: django-asyncmailer
  version: "0.3"

source:
  fn: django-asyncmailer-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-asyncmailer/django-asyncmailer-0.3.tar.gz
  md5: dfb225ef5fe477f644eed272152566d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-asyncmailer = django-asyncmailer:main
    #
    # Would create an entry point called django-asyncmailer that calls django-asyncmailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jsonfield
    - django
    - celery

  run:
    - python
    - django-jsonfield
    - django
    - celery

test:
  # Python imports
  imports:
    - asyncmailer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yzmtf2008/django-asyncmailer
  license: MIT License
  summary: 'Async email solution w/ multiple SMTP credentials, load-balancing and blacklist.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
