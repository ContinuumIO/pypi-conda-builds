package:
  name: django-flux
  version: "0.3.0"

source:
  fn: django-flux-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flux/django-flux-0.3.0.tar.gz
  md5: a94c476f31473405ffdbe339197ae3b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flux = django-flux:main
    #
    # Would create an entry point called django-flux that calls django-flux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - python-twitter
    - python-dateutil
    - feedparser
    - fbconsole
    - mechanize

  run:
    - python
    - django
    - python-twitter
    - python-dateutil
    - feedparser
    - fbconsole
    - mechanize

test:
  # Python imports
  imports:
    - flux
    - flux.conf
    - flux.management
    - flux.management.commands
    - flux.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datascopeanalytics/django-flux
  license: MIT, see LICENSE.rst
  summary: 'locally fetch, store, and present social media flux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
