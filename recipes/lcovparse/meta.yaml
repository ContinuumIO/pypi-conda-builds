package:
  name: lcovparse
  version: "0.0.4"

source:
  fn: lcovparse-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/l/lcovparse/lcovparse-0.0.4.tar.gz
  md5: e8e03f1ffe44af5de5ee55381e937baf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lcovparse = lcovparse:main
    #
    # Would create an entry point called lcovparse that calls lcovparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lcovparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codecov/lcov-parse
  license: Apache Software License
  summary: 'lcov to json'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
