package:
  name: auto2to3
  version: "0.3"

source:
  fn: auto2to3-0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/auto2to3/auto2to3-0.3.tar.gz
  md5: 5ba21fb48347ae6a2df64c7d4be89889
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auto2to3 = auto2to3:main
    #
    # Would create an entry point called auto2to3 that calls auto2to3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - auto2to3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bdarnell/auto2to3/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Wrapper to run 2to3 automatically at import time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
