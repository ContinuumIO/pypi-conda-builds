package:
  name: collective.project
  version: "1.0.3"

source:
  fn: collective.project-1.0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.project/collective.project-1.0.3.zip
  md5: aaf74585a11966fd87ab202ef6e27753
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.project = collective.project:main
    #
    # Would create an entry point called collective.project that calls collective.project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity[grok]

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity[grok]

test:
  # Python imports
  imports:
    - collective
    - collective.project
    - collective.project.browser
    - collective.project.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://collective.github.com/collective.project/
  license: ZPL
  summary: 'Demo app: project management with dexterity content types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
