package:
  name: indeed
  version: "0.0.4"

source:
  fn: indeed-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/indeed/indeed-0.0.4.tar.gz
  md5: 8d8ebf5b651eb9b95f75f9edf9b37baf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indeed = indeed:main
    #
    # Would create an entry point called indeed that calls indeed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests ==2.0.0

  run:
    - python
    - requests ==2.0.0

test:
  # Python imports
  imports:
    - indeed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/indeedlabs/indeed-python
  license: UNKNOWN
  summary: 'Indeed Job Search Python Api Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
