package:
  name: flask-rauth
  version: "0.3.2"

source:
  fn: Flask-Rauth-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Rauth/Flask-Rauth-0.3.2.tar.gz
  md5: f22a03e558611740d60fc0d4a60e5a09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-rauth = flask-rauth:main
    #
    # Would create an entry point called flask-rauth that calls flask-rauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - rauth

  run:
    - python
    - flask
    - rauth

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joelverhagen/flask-rauth
  license: BSD License
  summary: 'Adds OAuth 1.0/a, 2.0, and Ofly consumer support for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
