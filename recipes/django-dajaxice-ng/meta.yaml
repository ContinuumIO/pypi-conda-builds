package:
  name: django-dajaxice-ng
  version: "0.7.0.1"

source:
  fn: django-dajaxice-ng-0.7.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dajaxice-ng/django-dajaxice-ng-0.7.0.1.tar.gz
  md5: 5310d8b66825415dd976c3cb3b4a9877
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dajaxice-ng = django-dajaxice-ng:main
    #
    # Would create an entry point called django-dajaxice-ng that calls django-dajaxice-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - dajaxice
    - dajaxice.core
    - dajaxice.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ifanrx/django-dajaxice
  license: BSD License
  summary: 'Agnostic and easy to use ajax library for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
