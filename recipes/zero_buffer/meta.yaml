package:
  name: zero_buffer
  version: "0.5.1"

source:
  fn: zero_buffer-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zero_buffer/zero_buffer-0.5.1.tar.gz
  md5: b7ec5c9206fe31efab4b00b8f0dd55e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zero_buffer = zero_buffer:main
    #
    # Would create an entry point called zero_buffer that calls zero_buffer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zero_buffer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'A fast, no-copy, buffer for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
