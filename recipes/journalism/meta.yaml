package:
  name: journalism
  version: "0.4.0"

source:
  fn: journalism-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/j/journalism/journalism-0.4.0.tar.gz
  md5: dcd1a099e2b0bec6d8c5e1c743f8ec5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - journalism = journalism:main
    #
    # Would create an entry point called journalism that calls journalism.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.6.1
    - python-dateutil >=2.2

  run:
    - python
    - six ==1.6.1
    - python-dateutil >=2.2

test:
  # Python imports
  imports:
    - journalism

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://journalism.readthedocs.org/
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
