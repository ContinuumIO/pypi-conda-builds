package:
  name: hiatus
  version: "0.0.1"

source:
  fn: hiatus-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hiatus/hiatus-0.0.1.tar.gz
  md5: 7fb9e029c2adc313453f0f615aff743a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hiatus = hiatus:main
    #
    # Would create an entry point called hiatus that calls hiatus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hiatus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jesusabdullah/hiatus
  license: MIT License
  summary: 'set_timeout/clear_timeout and set_interval/clear_intervalimplemented with the threading module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
