package:
  name: sitetool
  version: "0.3.0"

source:
  fn: SiteTool-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SiteTool/SiteTool-0.3.0.tar.gz
  md5: 914047124f10dff44d196bb8844ce718
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sitetool = sitetool:main
    #
    # Would create an entry point called sitetool that calls sitetool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - commandtool >=0.3.0
    - docutils >=0.5.0
    - gallery >=0.1.0
    - barenecessities

  run:
    - python
    - setuptools
    - commandtool >=0.3.0
    - docutils >=0.5.0
    - gallery >=0.1.0
    - barenecessities

test:
  # Python imports
  imports:
    - sitetool
    - sitetool.convert
    - sitetool.export
    - sitetool.template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/sitetool/index.html
  license: GPLv3
  summary: 'Tools for working with HTML pages in the context of managing a website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
