package:
  name: collective.js.ui.multiselect
  version: "1.0.1"

source:
  fn: collective.js.ui.multiselect-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.ui.multiselect/collective.js.ui.multiselect-1.0.1.zip
  md5: 82e806d55a7fbfb0f1ab6550b7af5c75
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.ui.multiselect = collective.js.ui.multiselect:main
    #
    # Would create an entry point called collective.js.ui.multiselect that calls collective.js.ui.multiselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade

  run:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.ui
    - collective.js.ui.multiselect
    - collective.js.ui.multiselect.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/collective.js.ui.multiselect
  license: GNU General Public License (GPL)
  summary: 'Integrates the multiselect widget into plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
