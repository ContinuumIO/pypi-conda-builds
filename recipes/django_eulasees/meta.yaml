package:
  name: django_eulasees
  version: "0.2.5"

source:
  fn: django_eulasees-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django_eulasees/django_eulasees-0.2.5.tar.gz
  md5: dbbdf7b419ae6a677c0c6378187ef6a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_eulasees = django_eulasees:main
    #
    # Would create an entry point called django_eulasees that calls django_eulasees.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_eulasees

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/swfiua/django_eulasees
  license: BSD License
  summary: 'Team Bermuda django app for koding hackathon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
