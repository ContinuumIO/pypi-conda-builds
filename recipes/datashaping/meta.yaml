package:
  name: datashaping
  version: "0.3.2"

source:
  fn: datashaping-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/datashaping/datashaping-0.3.2.tar.gz
  md5: 0b63c0713c7456a565ad55ea6f37bcf4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datashaping = datashaping:main
    #
    # Would create an entry point called datashaping that calls datashaping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datashaping
    - datashaping.storage
    - datashaping.utils
    - tests
    - tests.storage
    - tests.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/neithere/datashaping/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A pythonic query API for structured data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
