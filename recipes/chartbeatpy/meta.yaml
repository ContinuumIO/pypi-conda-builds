package:
  name: chartbeatpy
  version: "0.2.1"

source:
  fn: chartbeatpy-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chartbeatpy/chartbeatpy-0.2.1.tar.gz
  md5: 801d49471501540dd794dab97c2f43fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chartbeatpy = chartbeatpy:main
    #
    # Would create an entry point called chartbeatpy that calls chartbeatpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - chartbeatpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/detroit-media-partnership/chartbeatpy
  license: MIT License
  summary: 'A charbeat API wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
