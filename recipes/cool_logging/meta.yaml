package:
  name: cool_logging
  version: "0.3"

source:
  fn: cool_logging-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cool_logging/cool_logging-0.3.tar.gz
  md5: 1fce0796aafe4ed1a4e607648045935e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cool_logging = cool_logging:main
    #
    # Would create an entry point called cool_logging that calls cool_logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - termcolor

  run:
    - python
    - termcolor

test:
  # Python imports
  imports:
    - cool_logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rshk/cool-logging
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Nice colorful formatter for Python logging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
