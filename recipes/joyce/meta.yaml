package:
  name: joyce
  version: "0.1.9"

source:
  fn: joyce-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/j/joyce/joyce-0.1.9.tar.gz
  md5: 7071e119e4a02711e4e6d94af2250ba0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - joyce = joyce:main
    #
    # Would create an entry point called joyce that calls joyce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.3
    - sarah >=0.1.2
    - mirte >=0.1.3
    - poster >=0.7.0

  run:
    - python
    - docutils >=0.3
    - sarah >=0.1.2
    - mirte >=0.1.3
    - poster >=0.7.0

test:
  # Python imports
  imports:
    - joyce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bwesterb/py-joyce/
  license: UNKNOWN
  summary: 'JSON channels'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
