package:
  name: eventlet_inotifyx
  version: "0.0.1"

source:
  fn: eventlet_inotifyx-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/eventlet_inotifyx/eventlet_inotifyx-0.0.1.tar.gz
  md5: 45a116ccf83310357a9df58dc55a5d60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eventlet_inotifyx = eventlet_inotifyx:main
    #
    # Would create an entry point called eventlet_inotifyx that calls eventlet_inotifyx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eventlet
    - inotifyx

  run:
    - python
    - eventlet
    - inotifyx

test:
  # Python imports
  imports:
    - eventlet_inotifyx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/little-arhat/eventlet_inotifyx
  license: MIT License
  summary: 'Thin wrapper around inotifyx to use with eventlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
