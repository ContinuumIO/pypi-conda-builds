package:
  name: lltk
  version: "0.2.4"

source:
  fn: lltk-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/l/lltk/lltk-0.2.4.tar.gz
  md5: b2246171ba010c73e657be1e8985b3f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lltk = lltk:main
    #
    # Would create an entry point called lltk that calls lltk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - lxml
    - pattern
    - functools32
    - textblob

  run:
    - python
    - requests
    - lxml
    - pattern
    - functools32
    - textblob

test:
  # Python imports
  imports:
    - lltk
    - lltk.audiosamples
    - lltk.caches
    - lltk.config
    - lltk.de
    - lltk.de.scrapers
    - lltk.en
    - lltk.en.scrapers
    - lltk.es
    - lltk.es.scrapers
    - lltk.fr
    - lltk.fr.scrapers
    - lltk.images
    - lltk.it
    - lltk.it.scrapers
    - lltk.locale
    - lltk.nl
    - lltk.nl.scrapers
    - lltk.scrapers
    - lltk.textsamples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lltk/lltk
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'The Language Learning Toolkit (LLTK) performs a variety of tasks useful for (human) language learning.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
