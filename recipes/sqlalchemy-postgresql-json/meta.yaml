package:
  name: sqlalchemy-postgresql-json
  version: "0.4.5"

source:
  fn: sqlalchemy-postgresql-json-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-postgresql-json/sqlalchemy-postgresql-json-0.4.5.tar.gz
  md5: 8af86d679312cfd9a58a5a2951ac7a17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-postgresql-json = sqlalchemy-postgresql-json:main
    #
    # Would create an entry point called sqlalchemy-postgresql-json that calls sqlalchemy-postgresql-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2
    - sqlalchemy

  run:
    - python
    - psycopg2
    - sqlalchemy

test:
  # Python imports
  imports:
    - sqlalchemy_postgresql_json

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DisruptiveLabs/sqlalchemy_postgresql_json
  license: UNKNOWN
  summary: 'Postgresql JSON Extension for sqlalchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
