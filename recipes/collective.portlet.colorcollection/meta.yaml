package:
  name: collective.portlet.colorcollection
  version: "0.3.0"

source:
  fn: collective.portlet.colorcollection-0.3.0.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.colorcollection/collective.portlet.colorcollection-0.3.0.zip
  md5: 8d55ff134dff8406b87d5e7a0039d8f2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.colorcollection = collective.portlet.colorcollection:main
    #
    # Would create an entry point called collective.portlet.colorcollection that calls collective.portlet.colorcollection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.portlet.collection

  run:
    - python
    - setuptools
    - setuptools
    - plone.portlet.collection

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.colorcollection
    - collective.portlet.colorcollection.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.portlet.colorcollection
  license: GNU General Public License (GPL)
  summary: 'Replace some code of the Plone collection portlet to add links color'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
