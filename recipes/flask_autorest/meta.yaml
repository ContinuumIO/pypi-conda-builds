package:
  name: flask_autorest
  version: "0.1.5"

source:
  fn: flask_autorest-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_autorest/flask_autorest-0.1.5.tar.gz
  md5: 4bf271cc4fd74b62654cebb02b36437d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_autorest = flask_autorest:main
    #
    # Would create an entry point called flask_autorest that calls flask_autorest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - dataset

  run:
    - python
    - flask
    - dataset

test:
  # Python imports
  imports:
    - flask_autorest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/flask_autorest
  license: MIT
  summary: 'auto create restful apis for database, with the help of dataset.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
