package:
  name: keepassdb
  version: "0.2.1"

source:
  fn: keepassdb-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/k/keepassdb/keepassdb-0.2.1.tar.gz
  md5: 291d796ccdd472b32ff3cd5783b154dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - keepassdb = keepassdb:main
    #
    # Would create an entry point called keepassdb that calls keepassdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto >=2.6,<3.0dev

  run:
    - python
    - pycrypto >=2.6,<3.0dev

test:
  # Python imports
  imports:
    - keepassdb
    - keepassdb.export
    - keepassdb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hozn/keepassdb
  license: GNU General Public License (GPL)
  summary: 'Python library for reading and writing KeePass 1.x databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
