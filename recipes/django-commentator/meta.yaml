package:
  name: django-commentator
  version: "0.0.4"

source:
  fn: django-commentator-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-commentator/django-commentator-0.0.4.tar.gz
  md5: b21350fe4a1d675d7111c0f0dd488bfa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-commentator = django-commentator:main
    #
    # Would create an entry point called django-commentator that calls django-commentator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - setuptools

  run:
    - python
    - django
    - setuptools

test:
  # Python imports
  imports:
    - commentator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smidth/django-commentator
  license: BSD License
  summary: 'Commentaries system for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
