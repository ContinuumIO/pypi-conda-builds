package:
  name: jmbo-foundry
  version: "2.0.0a1"

source:
  fn: jmbo-foundry-2.0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-foundry/jmbo-foundry-2.0.0a1.tar.gz
  md5: 49d01d2331dc73cdc3120b71e512dbcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-foundry = jmbo-foundry:main
    #
    # Would create an entry point called jmbo-foundry that calls jmbo-foundry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.7
    - django_compressor
    - django-dfp >=0.3.3
    - django-export
    - django-googlesearch
    - django-layers-hr >=0.4
    - django-object-tools >=0.0.5
    - django-pagination
    - django-publisher
    - django-simple-autocomplete
    - django-social-auth ==0.7.18
    - django-snippetscream
    - jmbo ==2.0.0a2
    - jmbo-analytics
    - jmbo-contact ==2.0.0a2
    - jmbo-post ==2.0.0a2
    - beautifulsoup
    - jellyfish
    - python-memcached
    - pyjwt
    - requests
    - zope.interface

  run:
    - python
    - django >=1.6,<1.7
    - django_compressor
    - django-dfp >=0.3.3
    - django-export
    - django-googlesearch
    - django-layers-hr >=0.4
    - django-object-tools >=0.0.5
    - django-pagination
    - django-publisher
    - django-simple-autocomplete
    - django-social-auth ==0.7.18
    - django-snippetscream
    - jmbo ==2.0.0a2
    - jmbo-analytics
    - jmbo-contact ==2.0.0a2
    - jmbo-post ==2.0.0a2
    - beautifulsoup
    - jellyfish
    - python-memcached
    - pyjwt
    - requests
    - zope.interface

test:
  # Python imports
  imports:
    - foundry
    - foundry.management
    - foundry.management.commands
    - foundry.migrations
    - foundry.templatetags
    - foundry.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-foundry
  license: BSD License
  summary: 'Jmbo Foundry ties together the various Jmbo products enabling you to rapidly build multilingual web and mobi sites with the minimum amount of code and customization.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
