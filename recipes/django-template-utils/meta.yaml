package:
  name: django-template-utils
  version: "0.1.1"

source:
  fn: django-template-utils-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-utils/django-template-utils-0.1.1.tar.gz
  md5: 998642414cf7badb2640026893ec494e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-utils = django-template-utils:main
    #
    # Would create an entry point called django-template-utils that calls django-template-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - template_utils
    - template_utils.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gerardo-orozco/django-template-utils
  license: BSD License
  summary: 'Just a collection of useful template tags and filters gathered in a single app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
