package:
  name: django_wtfd
  version: "0.1.4"

source:
  fn: django_wtfd-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django_wtfd/django_wtfd-0.1.4.tar.gz
  md5: 7b1dd46e7d643a49cf3ecfc758defe14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_wtfd = django_wtfd:main
    #
    # Would create an entry point called django_wtfd that calls django_wtfd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_wtfd
    - django_wtfd.management
    - django_wtfd.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/postatum/django-wtfd
  license: UNKNOWN
  summary: 'Forcing developers to write docstrings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
