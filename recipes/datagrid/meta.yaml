package:
  name: datagrid
  version: "0.3.5"

source:
  fn: DataGrid-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/D/DataGrid/DataGrid-0.3.5.tar.gz
  md5: 8a623a369bb2739466328091b23752ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datagrid = datagrid:main
    #
    # Would create an entry point called datagrid that calls datagrid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datagrid
    - datagrid.renderer
    - datagrid.server
    - tests
    - tests.mocks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/redventures-oss/DataGrid
  license: LGPL 3
  summary: 'Tabular data rendering library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
