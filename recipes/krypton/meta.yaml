package:
  name: krypton
  version: "0.1.2"

source:
  fn: Krypton-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/K/Krypton/Krypton-0.1.2.tar.gz
  md5: 8cf7e15185b212af9be1f1d05ac339bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - krypton = krypton:main
    #
    # Would create an entry point called krypton that calls krypton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pil
    - qrcode
    - six
    - tornado
    - pymongo
    - pgpdump
    - m2crypto

  run:
    - python
    - pil
    - qrcode
    - six
    - tornado
    - pymongo
    - pgpdump
    - m2crypto

test:
  # Python imports
  imports:
    - krypton
    - krypton.hkpserver
    - krypton.hkpserver.controllers
    - krypton.hkpserver.libs
    - krypton.hkpserver.libs.gossip
    - krypton.hkpserver.libs.gpgjsonparser
    - krypton.hkpserver.libs.gpgmongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zerodine/krypton
  license: MIT
  summary: 'GPG/PGP Keyserver with many enhancements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
