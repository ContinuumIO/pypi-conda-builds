package:
  name: ipython-pip
  version: "0.3"

source:
  fn: ipython-pip-0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/ipython-pip/ipython-pip-0.3.tar.gz
  md5: af32ab49204df73fc9cbd4a226f24bd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipython-pip = ipython-pip:main
    #
    # Would create an entry point called ipython-pip that calls ipython-pip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ipythonpip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jdfreder/ipython-pip
  license: New BSD License
  summary: 'Allows IPython notebook extension authors to make their extension pip installable!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
