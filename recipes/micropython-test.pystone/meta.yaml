package:
  name: micropython-test.pystone
  version: "1.0"

source:
  fn: micropython-test.pystone-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-test.pystone/micropython-test.pystone-1.0.tar.gz
  md5: 4bbf5404b279c04f89c27dab5900cd27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-test.pystone = micropython-test.pystone:main
    #
    # Would create an entry point called micropython-test.pystone that calls micropython-test.pystone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: Python
  summary: 'CPython test.pystone module ported to MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
