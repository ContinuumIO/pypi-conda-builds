package:
  name: collective.discussionintegration.plonegazette
  version: "1.0.0"

source:
  fn: collective.discussionintegration.plonegazette-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.discussionintegration.plonegazette/collective.discussionintegration.plonegazette-1.0.0.tar.gz
  md5: 5e15e3d513c13e59d668d6e4884c0ea0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.discussionintegration.plonegazette = collective.discussionintegration.plonegazette:main
    #
    # Would create an entry point called collective.discussionintegration.plonegazette that calls collective.discussionintegration.plonegazette.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.discussion
    - products.plonegazette

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.discussion
    - products.plonegazette

test:
  # Python imports
  imports:
    - collective
    - collective.discussionintegration
    - collective.discussionintegration.plonegazette

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.discussionintegration.plonegazette
  license: GPL
  summary: 'Makes possible the creation of the PloneGazette content types if there is an installation of plone.app.discussion in your instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
