package:
  name: authtkt
  version: "0.3.3"

source:
  fn: AuthTkt-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/A/AuthTkt/AuthTkt-0.3.3.tar.gz
  md5: 381f17bf4da04f93830a811b6476aa9a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authtkt = authtkt:main
    #
    # Would create an entry point called authtkt that calls authtkt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - authtkt
    - authtkt.app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Apache Software License
  summary: 'Python implementation of the mod_auth_tkt handler and Perl scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
