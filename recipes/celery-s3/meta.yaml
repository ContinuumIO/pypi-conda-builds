package:
  name: celery-s3
  version: "0.1.1"

source:
  fn: celery-s3-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/celery-s3/celery-s3-0.1.1.tar.gz
  md5: 95b81e2e82215f4f775bb7539c84a7d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celery-s3 = celery-s3:main
    #
    # Would create an entry point called celery-s3 that calls celery-s3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.8.0,<3.0

  run:
    - python
    - boto >=2.8.0,<3.0

test:
  # Python imports
  imports:
    - celery_s3
    - celery_s3.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robgolding63/celery-s3
  license: BSD License
  summary: 'An S3 result store backend for Celery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
