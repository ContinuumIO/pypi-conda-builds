package:
  name: dynect
  version: "0.0.8"

source:
  fn: dynect-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/dynect/dynect-0.0.8.tar.gz
  md5: 940534a1c1488224631ac8192ac10330
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynect = dynect:main
    #
    # Would create an entry point called dynect that calls dynect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - requests
    - decorator

  run:
    - python
    - distribute
    - requests
    - decorator

test:
  # Python imports
  imports:
    - dynect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jorgeecardona/dynect
  license: UNKNOWN
  summary: 'Wrapper library to Dynect API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
