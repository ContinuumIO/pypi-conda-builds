package:
  name: collective.z3cform.datagridfield
  version: "1.1"

source:
  fn: collective.z3cform.datagridfield-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.datagridfield/collective.z3cform.datagridfield-1.1.zip
  md5: 5eaeb420942c45b568d86d18c4253a15
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.datagridfield = collective.z3cform.datagridfield:main
    #
    # Would create an entry point called collective.z3cform.datagridfield that calls collective.z3cform.datagridfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - plone.app.z3cform
    - plone.directives.form
    - setuptools
    - z3c.form >=2.4.3dev
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - five.grok
    - plone.app.z3cform
    - plone.directives.form
    - setuptools
    - z3c.form >=2.4.3dev
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.datagridfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.z3cform.datagridfield
  license: GPL
  summary: 'Version of DataGridField for use with Dexterity / z3c.form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
