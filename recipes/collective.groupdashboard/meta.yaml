package:
  name: collective.groupdashboard
  version: "1.0b2"

source:
  fn: collective.groupdashboard-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.groupdashboard/collective.groupdashboard-1.0b2.tar.gz
  md5: 25c141179030cb848b71c170f53faa7a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.groupdashboard = collective.groupdashboard:main
    #
    # Would create an entry point called collective.groupdashboard that calls collective.groupdashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - collective.autopermission
    - five.grok

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - collective.autopermission
    - five.grok

test:
  # Python imports
  imports:
    - collective
    - collective.groupdashboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.groupdashboard
  license: GPL
  summary: "Assign portlets to users' dashboards on a per-group basis"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
