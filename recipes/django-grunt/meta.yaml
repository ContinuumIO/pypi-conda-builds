package:
  name: django-grunt
  version: "1.1.0"

source:
  fn: django-grunt-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-grunt/django-grunt-1.1.0.tar.gz
  md5: cc153dd75344c278ec0d2198d314b8fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-grunt = django-grunt:main
    #
    # Would create an entry point called django-grunt that calls django-grunt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-commando >=0.1.1

  run:
    - python
    - django >=1.4
    - django-commando >=0.1.1

test:
  # Python imports
  imports:
    - grunt
    - grunt.management
    - grunt.management.commands
    - grunt.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skibblenybbles/django-grunt
  license: MIT License
  summary: 'Django tools for integrating Grunt into your workflow'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
