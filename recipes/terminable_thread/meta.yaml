package:
  name: terminable_thread
  version: "0.7.1"

source:
  fn: terminable_thread-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/t/terminable_thread/terminable_thread-0.7.1.tar.gz
  md5: f4f052ea5c1efa98889f0fd10335ae5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - terminable_thread = terminable_thread:main
    #
    # Would create an entry point called terminable_thread that calls terminable_thread.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - terminable_thread
    - terminable_thread.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/intuited/terminable_thread
  license: http://sam.zoy.org/wtfpl/
  summary: ' Provides a subclass of Thread with facilities to raise an exception in the thread or terminate the thread from another thread.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
