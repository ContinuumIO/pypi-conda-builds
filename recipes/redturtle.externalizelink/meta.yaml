package:
  name: redturtle.externalizelink
  version: "0.1.0alpha"

source:
  fn: redturtle.externalizelink-0.1.0alpha.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.externalizelink/redturtle.externalizelink-0.1.0alpha.tar.gz
  md5: 436618553f015621fe7a2c32b556f925
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.externalizelink = redturtle.externalizelink:main
    #
    # Would create an entry point called redturtle.externalizelink that calls redturtle.externalizelink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.externalizelink
    - redturtle.externalizelink.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/redturtle.externalizelink
  license: GPL
  summary: 'A Plone product that open external link on different browser window in accessible way (even using XHTML Strict)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
