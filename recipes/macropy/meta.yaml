package:
  name: macropy
  version: "1.0.3"

source:
  fn: MacroPy-1.0.3.zip
  url: https://pypi.python.org/packages/source/M/MacroPy/MacroPy-1.0.3.zip
  md5: f403377b06ed303cfc5cad0d8ce2f29d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - macropy = macropy:main
    #
    # Would create an entry point called macropy that calls macropy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - macropy
    - macropy.core
    - macropy.experimental

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lihaoyi/macropy
  license: MIT
  summary: 'Macros for Python: Quasiquotes, Case Classes, LINQ and more!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
