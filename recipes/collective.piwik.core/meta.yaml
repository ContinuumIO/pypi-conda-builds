package:
  name: collective.piwik.core
  version: "0.4"

source:
  fn: collective.piwik.core-0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.piwik.core/collective.piwik.core-0.4.zip
  md5: 393abb09fd68f6af2a904a40beb96f10
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.piwik.core = collective.piwik.core:main
    #
    # Would create an entry point called collective.piwik.core that calls collective.piwik.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.piwik
    - collective.piwik.core
    - collective.piwik.core.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plumi/collective.piwik.core
  license: GPL
  summary: 'Analytics support for pages using Piwik (core package)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
