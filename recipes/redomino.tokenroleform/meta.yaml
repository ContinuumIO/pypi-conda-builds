package:
  name: redomino.tokenroleform
  version: "0.5"

source:
  fn: redomino.tokenroleform-0.5.zip
  url: https://pypi.python.org/packages/source/r/redomino.tokenroleform/redomino.tokenroleform-0.5.zip
  md5: 74f14f947e2858b6b645c9e7224c2f03
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.tokenroleform = redomino.tokenroleform:main
    #
    # Would create an entry point called redomino.tokenroleform that calls redomino.tokenroleform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - redomino.tokenrole

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - redomino.tokenrole

test:
  # Python imports
  imports:
    - redomino
    - redomino.tokenroleform
    - redomino.tokenroleform.browser
    - redomino.tokenroleform.content
    - redomino.tokenroleform.interfaces
    - redomino.tokenroleform.portlets
    - redomino.tokenroleform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.tokenroleform
  license: GNU General Public License (GPL)
  summary: 'Token Role Form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
