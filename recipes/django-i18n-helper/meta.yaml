package:
  name: django-i18n-helper
  version: "0.1.1"

source:
  fn: django-i18n-helper-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-i18n-helper/django-i18n-helper-0.1.1.tar.gz
  md5: 6b5eab76264240b420baa86e8e68298e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-i18n-helper = django-i18n-helper:main
    #
    # Would create an entry point called django-i18n-helper that calls django-i18n-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - i18n_helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/machinalis/django-i18n-helper
  license: BSD License
  summary: u'A internationalization helper that highlights translated strings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
