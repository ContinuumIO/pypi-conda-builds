package:
  name: benchmark-places
  version: "0.0.8"

source:
  fn: benchmark-places-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/b/benchmark-places/benchmark-places-0.0.8.tar.gz
  md5: 8fde9b1b480bb74abb16b71969c7bcce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - benchmark-places = benchmark-places:main
    #
    # Would create an entry point called benchmark-places that calls benchmark-places.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rauth
    - requests

  run:
    - python
    - rauth
    - requests

test:
  # Python imports
  imports:
    - benchmarkplaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/usebenchmark/benchmarkplaces.git
  license: MIT licence, see LICENCE.txt
  summary: 'Place abstraction'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
