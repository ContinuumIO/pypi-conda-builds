package:
  name: jupyterdrive
  version: "1.1.0"

source:
  fn: jupyterdrive-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jupyterdrive/jupyterdrive-1.1.0.tar.gz
  md5: 2b26ba66fcd734c86a7f4cc249cef986
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jupyterdrive = jupyterdrive:main
    #
    # Would create an entry point called jupyterdrive that calls jupyterdrive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython

  run:
    - python
    - ipython

test:
  # Python imports
  imports:
    - jupyterdrive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jupyter/jupyter-drive
  license: BSD License
  summary: 'Integration of IPython/Jupyter with Google drive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
