package:
  name: infrae.maildrophost
  version: "2.2"

source:
  fn: infrae.maildrophost-2.2.tar.gz
  url: https://pypi.python.org/packages/source/i/infrae.maildrophost/infrae.maildrophost-2.2.tar.gz
  md5: d0e293acda025925639c8e66b11b3fe7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infrae.maildrophost = infrae.maildrophost:main
    #
    # Would create an entry point called infrae.maildrophost that calls infrae.maildrophost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - setuptools
    - psutil

  run:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - setuptools
    - psutil

test:
  # Python imports
  imports:
    - infrae
    - infrae.maildrophost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/infrae.maildrophost
  license: ZPL 2.1
  summary: 'Recipe to install and setup maildrophost server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
