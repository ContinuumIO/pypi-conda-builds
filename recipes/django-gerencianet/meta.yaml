package:
  name: django-gerencianet
  version: "0.1.2"

source:
  fn: django-gerencianet-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gerencianet/django-gerencianet-0.1.2.tar.gz
  md5: 9208d773bd28d0d0ee4e635201a70405
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gerencianet = django-gerencianet:main
    #
    # Would create an entry point called django-gerencianet that calls django-gerencianet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gerencianet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.starlinetecnologia.com.br/
  license: BSD License
  summary: 'Uma aplica\xc3\xa7\xc3\xa3o django para comunicar com o gateway de pagamento Gerencianet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
