package:
  name: ganymede
  version: "0.1.1"

source:
  fn: ganymede-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/ganymede/ganymede-0.1.1.tar.gz
  md5: 4809d45db03d7d5ac383c01dae8882e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ganymede = ganymede:main
    #
    # Would create an entry point called ganymede that calls ganymede.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - path.py >=7.0
    - ipython >=3.0

  run:
    - python
    - path.py >=7.0
    - ipython >=3.0

test:
  # Python imports
  imports:
    - ganymede
    - ganymede.static

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/userzimmermann/ganymede
  license: GNU General Public License (GPL)
  summary: "Hacking Jupyter's atmosphere"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
