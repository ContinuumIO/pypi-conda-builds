package:
  name: horae.core
  version: "1.0a1"

source:
  fn: horae.core-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.core/horae.core-1.0a1.tar.gz
  md5: 8ed3c28cb27f9fc9d624c73f418e7a5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.core = horae.core:main
    #
    # Would create an entry point called horae.core that calls horae.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - zope.app.locales
    - z3c.taskqueue
    - horae.cache

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - zope.app.locales
    - z3c.taskqueue
    - horae.cache

test:
  # Python imports
  imports:
    - horae
    - horae.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Basic functionality for Horae'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
