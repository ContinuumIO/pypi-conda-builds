package:
  name: chembl_clippy
  version: "0.1.6"

source:
  fn: chembl_clippy-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/chembl_clippy/chembl_clippy-0.1.6.tar.gz
  md5: fb68ad4ad8dc4caae3a19c2a5c6a0740
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chembl_clippy = chembl_clippy:main
    #
    # Would create an entry point called chembl_clippy that calls chembl_clippy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - chembl_clippy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.ebi.ac.uk/chembl/
  license: MIT License
  summary: 'A cross-platform chemical structure rendering desktop app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
