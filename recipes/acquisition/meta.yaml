package:
  name: acquisition
  version: "4.2.2"

source:
  fn: Acquisition-4.2.2.tar.gz
  url: https://pypi.python.org/packages/source/A/Acquisition/Acquisition-4.2.2.tar.gz
  md5: 8d57d152ce38c7639657998c113a4f12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acquisition = acquisition:main
    #
    # Would create an entry point called acquisition that calls acquisition.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - extensionclass >=4.1.1
    - zope.interface

  run:
    - python
    - extensionclass >=4.1.1
    - zope.interface

test:
  # Python imports
  imports:
    - Acquisition

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zopefoundation/Acquisition
  license: Zope Public License
  summary: "Acquisition is a mechanism that allows objects to obtain attributes from the containment hierarchy they're in."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
