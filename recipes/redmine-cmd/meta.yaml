package:
  name: redmine-cmd
  version: "0.11.0"

source:
  fn: redmine-cmd-0.11.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redmine-cmd/redmine-cmd-0.11.0.tar.gz
  md5: e09e9f9749ac85f2c25f00fe94d3cb0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redmine-cmd = redmine-cmd:main
    #
    # Would create an entry point called redmine-cmd that calls redmine-cmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - simplejson
    - texttable
    - slugify

  run:
    - python
    - httplib2
    - simplejson
    - texttable
    - slugify

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/psaavedra/redmine-cmd
  license: MIT License
  summary: 'redmine-cmd.cfg.example'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
