package:
  name: beebotte
  version: "0.3.1"

source:
  fn: beebotte-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/b/beebotte/beebotte-0.3.1.tar.gz
  md5: 070546c5cad4d597ff58cf428495b94a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beebotte = beebotte:main
    #
    # Would create an entry point called beebotte that calls beebotte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - beebotte

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://beebotte.com
  license: MIT License
  summary: 'Python library for interfacing with Beebotte'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
