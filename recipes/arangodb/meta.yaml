package:
  name: arangodb
  version: "0.0.12"

source:
  fn: arangodb-0.0.12.tar.gz
  url: https://pypi.python.org/packages/source/a/arangodb/arangodb-0.0.12.tar.gz
  md5: 06232d98f2a2ea6ef03dbfb18d28011a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arangodb = arangodb:main
    #
    # Would create an entry point called arangodb that calls arangodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - arangodb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/diefans/python-arangodb
  license: Apache Software License
  summary: 'A Python client for ArangoDB REST API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
