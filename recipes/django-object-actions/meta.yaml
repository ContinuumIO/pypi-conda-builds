package:
  name: django-object-actions
  version: "0.5.1"

source:
  fn: django-object-actions-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-object-actions/django-object-actions-0.5.1.tar.gz
  md5: 4e20e8772da8e1f319238f89006eb565
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-object-actions = django-object-actions:main
    #
    # Would create an entry point called django-object-actions that calls django-object-actions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_object_actions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/texastribune/django-object-actions
  license: Apache Software License
  summary: 'A Django app for adding object tools for models in the admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
