package:
  name: bambu-dataportability
  version: "0.1"

source:
  fn: bambu-dataportability-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-dataportability/bambu-dataportability-0.1.tar.gz
  md5: 52c24708322a32637be4a38c02811e1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-dataportability = bambu-dataportability:main
    #
    # Would create an entry point called bambu-dataportability that calls bambu-dataportability.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - bambu-mail

  run:
    - python
    - django >=1.4
    - bambu-mail

test:
  # Python imports
  imports:
    - bambu.dataportability
    - bambu.dataportability.management
    - bambu.dataportability.management.commands
    - bambu.dataportability.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-dataportability
  license: UNKNOWN
  summary: 'Generic import and export tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
