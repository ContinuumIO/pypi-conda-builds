package:
  name: cygwinreg
  version: "1.0"

source:
  fn: cygwinreg-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cygwinreg/cygwinreg-1.0.tar.gz
  md5: 80fc2aaad507e02934026228163cb576
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cygwinreg = cygwinreg:main
    #
    # Would create an entry point called cygwinreg that calls cygwinreg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cygwinreg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.akoha.com/python-cygwinreg/
  license: Python Software Foundation License
  summary: 'Windows registry access for the Cygwin toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
