package:
  name: html2list
  version: "0.1.6"

source:
  fn: html2list-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/h/html2list/html2list-0.1.6.tar.gz
  md5: d4f684d97ce38ffe89766f76b0951d9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html2list = html2list:main
    #
    # Would create an entry point called html2list that calls html2list.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html5lib
    - lxml

  run:
    - python
    - html5lib
    - lxml

test:
  # Python imports
  imports:
    - html2list

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aanari/html2list
  license: MIT License
  summary: 'A library for converting html markup from an email or webpage into a list'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
