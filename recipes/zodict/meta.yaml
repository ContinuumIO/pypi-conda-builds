package:
  name: zodict
  version: "1.9.3"

source:
  fn: zodict-1.9.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zodict/zodict-1.9.3.tar.gz
  md5: dce7c57b6b5e2f60d4725a4cc0400885
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zodict = zodict:main
    #
    # Would create an entry point called zodict that calls zodict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - uuid
    - odict
    - zope.location
    - zope.lifecycleevent
    - zope.deprecation

  run:
    - python
    - setuptools
    - uuid
    - odict
    - zope.location
    - zope.lifecycleevent
    - zope.deprecation

test:
  # Python imports
  imports:
    - zodict
    - zodict.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/archetypes/AGX/zodict
  license: Python Software Foundation License
  summary: 'zope.interface compliant ordered dictionary and zope.location aware node.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
