package:
  name: pyteomics
  version: "3.0.1"

source:
  fn: pyteomics-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyteomics/pyteomics-3.0.1.tar.gz
  md5: 2c838cc1c16dce69148662b883c755b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyteomics = pyteomics:main
    #
    # Would create an entry point called pyteomics that calls pyteomics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyteomics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.theorchromo.ru/pyteomics
  license: License :: OSI Approved :: Apache Software License
  summary: 'A framework for proteomics data analysis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
