package:
  name: anderson.django-custom-user
  version: "0.3.3"

source:
  fn: anderson.django-custom-user-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/a/anderson.django-custom-user/anderson.django-custom-user-0.3.3.tar.gz
  md5: 3eed75747e9ea5b2e18dd48e19005165
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anderson.django-custom-user = anderson.django-custom-user:main
    #
    # Would create an entry point called anderson.django-custom-user that calls anderson.django-custom-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - custom_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jochangmin/django-custom-user
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
