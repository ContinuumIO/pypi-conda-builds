package:
  name: django-autoreports
  version: "0.8.6"

source:
  fn: django-autoreports-0.8.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-autoreports/django-autoreports-0.8.6.tar.gz
  md5: ce1e443df95c97bba96db2697ccec3f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-autoreports = django-autoreports:main
    #
    # Would create an entry point called django-autoreports that calls django-autoreports.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-configfield >=0.1.0-beta
    - django-form-admin >=0.3

  run:
    - python
    - django-configfield >=0.1.0-beta
    - django-form-admin >=0.3

test:
  # Python imports
  imports:
    - autoreports
    - autoreports.migrations
    - autoreports.templatetags
    - testing
    - testing.multimediaresources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yaco-Sistemas/django-autoreports
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application that lets you create reports'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
