package:
  name: intermine-bio
  version: "0.99.00"

source:
  fn: intermine-bio-0.99.00.tar.gz
  url: https://pypi.python.org/packages/source/i/intermine-bio/intermine-bio-0.99.00.tar.gz
  md5: 7af4085f55b30d722f6a5de4a20b6910
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intermine-bio = intermine-bio:main
    #
    # Would create an entry point called intermine-bio that calls intermine-bio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - interminebio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.intermine.org
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Biological Extensions to the InterMine WebService client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
