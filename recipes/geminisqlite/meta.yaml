package:
  name: geminisqlite
  version: "0.11.0"

source:
  fn: geminisqlite-0.11.0.tar.gz
  url: https://pypi.python.org/packages/source/g/geminisqlite/geminisqlite-0.11.0.tar.gz
  md5: fc7b1a2a9cafa4e1c734b4c57b02f5fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geminisqlite = geminisqlite:main
    #
    # Would create an entry point called geminisqlite that calls geminisqlite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.7.1
    - pyparsing >=1.5.6,<=1.5.7
    - pysam >=0.6
    - cyvcf >=0.1.10
    - pyyaml >=3.10
    - pybedtools >=0.6.2
    - jinja2 >=2.7.1
    - python-graph-core >=1.8.2
    - python-graph-dot >=1.8.2
    - bottle >=0.11.6
    - ipython-cluster-helper >=0.2.23
    - bx-python >=0.7.1
    - pandas >=0.11.0
    - openpyxl >=1.6.1,<2.0.0
    - scipy >=0.12.0
    - unidecode >=0.04.14

  run:
    - python
    - numpy >=1.7.1
    - pyparsing >=1.5.6,<=1.5.7
    - pysam >=0.6
    - cyvcf >=0.1.10
    - pyyaml >=3.10
    - pybedtools >=0.6.2
    - jinja2 >=2.7.1
    - python-graph-core >=1.8.2
    - python-graph-dot >=1.8.2
    - bottle >=0.11.6
    - ipython-cluster-helper >=0.2.23
    - bx-python >=0.7.1
    - pandas >=0.11.0
    - openpyxl >=1.6.1,<2.0.0
    - scipy >=0.12.0
    - unidecode >=0.04.14

test:
  # Python imports
  imports:
    - geminisqlite
    - geminisqlite.data
    - geminisqlite.scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://geminisqlite.readthedocs.org
  license: GNU General Public License (GPL)
  summary: 'A database framework for exploring genetic variation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
