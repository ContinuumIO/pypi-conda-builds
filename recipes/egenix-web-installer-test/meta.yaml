package:
  name: egenix-web-installer-test
  version: "0.2.0"

source:
  fn: egenix-web-installer-test-0.2.0.zip
  url: https://pypi.python.org/packages/source/e/egenix-web-installer-test/egenix-web-installer-test-0.2.0.zip
  md5: d53e3d5f707f84faea626f85037ce620
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - egenix-web-installer-test = egenix-web-installer-test:main
    #
    # Would create an entry point called egenix-web-installer-test that calls egenix-web-installer-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mx
    - mx.DateTime
    - mx.DateTime.Examples
    - mx.DateTime.mxDateTime
    - mx.Misc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.egenix.com/
  license: Python License (CNRI Python License)
  summary: 'eGenix Web Installer Test Package '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
