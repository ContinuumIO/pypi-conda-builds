package:
  name: django-group-access
  version: "1.1.17"

source:
  fn: django-group-access-1.1.17.tar.gz
  url: https://pypi.python.org/packages/source/d/django-group-access/django-group-access-1.1.17.tar.gz
  md5: d6c63c3c2b7f95af0e1951bbf3d3cd75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-group-access = django-group-access:main
    #
    # Would create an entry point called django-group-access that calls django-group-access.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_group_access
    - django_group_access.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-group-access
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Base Django model to add access control, via groups, to objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
