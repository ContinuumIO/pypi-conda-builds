package:
  name: git_credit
  version: "0.0.6"

source:
  fn: git_credit-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/g/git_credit/git_credit-0.0.6.tar.gz
  md5: 342ae21006594467c0d09cebc385efc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git_credit = git_credit:main
    #
    # Would create an entry point called git_credit that calls git_credit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bladerunner

  run:
    - python
    - bladerunner

test:
  # Python imports
  imports:
    - git_credit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/a-tal/git_credit
  license: BSD License
  summary: 'A pretty way to show committer stats for git repos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
