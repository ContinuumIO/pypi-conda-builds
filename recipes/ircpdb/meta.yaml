package:
  name: ircpdb
  version: "1.7.1"

source:
  fn: ircpdb-1.7.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ircpdb/ircpdb-1.7.1.tar.gz
  md5: 35e3cf5830b360e0956a3ebcf491b01f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ircpdb = ircpdb:main
    #
    # Would create an entry point called ircpdb that calls ircpdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - irc <10.0,>=9.0
    - six
    - requests >=1.0.0

  run:
    - python
    - irc <10.0,>=9.0
    - six
    - requests >=1.0.0

test:
  # Python imports
  imports:
    - ircpdb
    - ircpdb.django
    - ircpdb.django.templatetags
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coddingtonbear/ircpdb
  license: ISC License (ISCL)
  summary: 'Remotely and collaboratively debug your Python application via IRC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
