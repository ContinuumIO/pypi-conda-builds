package:
  name: django-rforum
  version: "0.0.24"

source:
  fn: django-rforum-0.0.24.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rforum/django-rforum-0.0.24.tar.gz
  md5: 9c67c5fe955eef25cd75cbd77c08310c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rforum = django-rforum:main
    #
    # Would create an entry point called django-rforum that calls django-rforum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5
    - south ==0.8.1
    - django-tinymce >=1.5.1b2
    - django-allauth ==0.13.0
    - pil ==1.1.7
    - django-filebrowser-no-grappelli ==3.1.1
    - django-tagging ==0.3.1
    - django-compressor ==1.3
    - sorl-thumbnail ==11.12
    - django-mailer

  run:
    - python
    - django <1.5
    - south ==0.8.1
    - django-tinymce >=1.5.1b2
    - django-allauth ==0.13.0
    - pil ==1.1.7
    - django-filebrowser-no-grappelli ==3.1.1
    - django-tagging ==0.3.1
    - django-compressor ==1.3
    - sorl-thumbnail ==11.12
    - django-mailer

test:
  # Python imports
  imports:
    - rforum
    - rforum.migrations
    - rforum.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r0sk/django-rforum
  license: BSD License
  summary: 'Yet another Django Forum App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
