package:
  name: collective.plone3bugfixes
  version: "0.9.4"

source:
  fn: collective.plone3bugfixes-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.plone3bugfixes/collective.plone3bugfixes-0.9.4.tar.gz
  md5: 9ec465fb2e38a114bda7a23a50d7b85b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.plone3bugfixes = collective.plone3bugfixes:main
    #
    # Would create an entry point called collective.plone3bugfixes that calls collective.plone3bugfixes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.plone3bugfixes
    - collective.plone3bugfixes.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.brainson.de/
  license: GPL
  summary: 'Plone 3 Bugfixes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
