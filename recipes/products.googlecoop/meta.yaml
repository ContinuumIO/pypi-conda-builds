package:
  name: products.googlecoop
  version: "2.1"

source:
  fn: Products.googlecoop-2.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.googlecoop/Products.googlecoop-2.1.tar.gz
  md5: 54679315746a42b6c2f687e79c6c6a1a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.googlecoop = products.googlecoop:main
    #
    # Would create an entry point called products.googlecoop that calls products.googlecoop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - Products
    - Products.googlecoop
    - Products.googlecoop.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/google-co-op
  license: GNU General Public License (GPL)
  summary: 'Google Coop for Plone makes it easy for you to integrate a Google Co-op Custom Search engine in your plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
