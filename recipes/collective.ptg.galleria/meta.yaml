package:
  name: collective.ptg.galleria
  version: "1.3.0"

source:
  fn: collective.ptg.galleria-1.3.0.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.galleria/collective.ptg.galleria-1.3.0.zip
  md5: 5f3473bfacb9a158ebeddf3d490d3ca4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.galleria = collective.ptg.galleria:main
    #
    # Would create an entry point called collective.ptg.galleria that calls collective.ptg.galleria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.galleria

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.galleria
  license: GPL
  summary: 'galleria integration with plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
