package:
  name: requery
  version: "0.3.5.1"

source:
  fn: requery-0.3.5.1.zip
  url: https://pypi.python.org/packages/source/r/requery/requery-0.3.5.1.zip
  md5: 54b5b1f43e277ac4b6894b37e46b8914
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requery = requery:main
    #
    # Would create an entry point called requery that calls requery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - pygments >=1.6
    - six >=1.7

  run:
    - python
    - django >=1.7
    - pygments >=1.6
    - six >=1.7

test:
  # Python imports
  imports:
    - requery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ebertti/requery/
  license: MIT License
  summary: 'Simple way to store and use queries in database for use of DBA for Django Admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
