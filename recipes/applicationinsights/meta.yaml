package:
  name: applicationinsights
  version: "0.10.0"

source:
  fn: applicationinsights-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/a/applicationinsights/applicationinsights-0.10.0.tar.gz
  md5: 37e852ef3c8022e53afb4fa5e0d42718
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - applicationinsights = applicationinsights:main
    #
    # Would create an entry point called applicationinsights that calls applicationinsights.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - applicationinsights
    - applicationinsights.channel
    - applicationinsights.channel.contracts
    - applicationinsights.exceptions
    - applicationinsights.logging
    - applicationinsights.requests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Microsoft/AppInsights-Python
  license: MIT License
  summary: 'This project extends the Application Insights API surface to support Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
