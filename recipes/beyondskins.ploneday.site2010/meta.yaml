package:
  name: beyondskins.ploneday.site2010
  version: "1.0.3"

source:
  fn: beyondskins.ploneday.site2010-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/beyondskins.ploneday.site2010/beyondskins.ploneday.site2010-1.0.3.tar.gz
  md5: 2a3fc93ba383021a485ade06c96be2f7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beyondskins.ploneday.site2010 = beyondskins.ploneday.site2010:main
    #
    # Would create an entry point called beyondskins.ploneday.site2010 that calls beyondskins.ploneday.site2010.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - beyondskins
    - beyondskins.ploneday
    - beyondskins.ploneday.site2010
    - beyondskins.ploneday.site2010.browser
    - beyondskins.ploneday.site2010.doc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'World Plone Day 2010 theme.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
