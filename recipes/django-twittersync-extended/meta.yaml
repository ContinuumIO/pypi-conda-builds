package:
  name: django-twittersync-extended
  version: "0.2.1"

source:
  fn: django-twittersync-extended-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twittersync-extended/django-twittersync-extended-0.2.1.tar.gz
  md5: f46ac87c5dd76e22fcdcc82af433028e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twittersync-extended = django-twittersync-extended:main
    #
    # Would create an entry point called django-twittersync-extended that calls django-twittersync-extended.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - django

  run:
    - python
    - python-dateutil
    - django

test:
  # Python imports
  imports:
    - twittersync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mpiz/django-twittersync
  license: BSD License
  summary: "Django app to sync Twitter stream to local DB with extendible  models. Fork of Peter Sanchez's Twitter sync"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
