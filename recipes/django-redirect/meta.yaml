package:
  name: django-redirect
  version: "0.2"

source:
  fn: django-redirect-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redirect/django-redirect-0.2.tar.gz
  md5: 81556ecd451c5e1ea41c4405bf946e10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redirect = django-redirect:main
    #
    # Would create an entry point called django-redirect that calls django-redirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - redirect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glenbot/django-redirect
  license: MIT License
  summary: 'A slightly more robust version of the native django redirect'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
