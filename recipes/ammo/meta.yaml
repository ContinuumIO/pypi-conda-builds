package:
  name: ammo
  version: "0.0.7"

source:
  fn: ammo-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/a/ammo/ammo-0.0.7.tar.gz
  md5: d496ca607a4e6c38b67e26c11a789ab1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ammo = ammo:main
    #
    # Would create an entry point called ammo that calls ammo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ammo
    - ammo.lunapark
    - ammo.phantom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/greggyNapalm/ammo
  license: BSD License
  summary: 'Input data generator for different load tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
