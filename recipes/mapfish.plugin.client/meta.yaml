package:
  name: mapfish.plugin.client
  version: "0.2"

source:
  fn: mapfish.plugin.client-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mapfish.plugin.client/mapfish.plugin.client-0.2.tar.gz
  md5: 58d6ed00c8c424bd8c45af1bb3e30a36
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mapfish.plugin.client = mapfish.plugin.client:main
    #
    # Would create an entry point called mapfish.plugin.client that calls mapfish.plugin.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pastescript >=1.7.2,<=1.7.99
    - tempita >=0.4,<=0.4.99
    - jstools >=0.1.5,<=0.1.99

  run:
    - python
    - setuptools
    - pastescript >=1.7.2,<=1.7.99
    - tempita >=0.4,<=0.4.99
    - jstools >=0.1.5,<=0.1.99

test:
  # Python imports
  imports:
    - mapfishpluginclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.mapfish.org
  license: GNU General Public License (GPL)
  summary: 'Client plugin of the MapFish Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
