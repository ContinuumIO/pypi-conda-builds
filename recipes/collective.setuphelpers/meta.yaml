package:
  name: collective.setuphelpers
  version: "0.6.5"

source:
  fn: collective.setuphelpers-0.6.5.zip
  url: https://pypi.python.org/packages/source/c/collective.setuphelpers/collective.setuphelpers-0.6.5.zip
  md5: acbef1c4aea368f559191941b50ec32b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.setuphelpers = collective.setuphelpers:main
    #
    # Would create an entry point called collective.setuphelpers that calls collective.setuphelpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.setuphelpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fry-it.com
  license: GPL
  summary: "Various simple functions to be used in Plone's setuphandlers"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
