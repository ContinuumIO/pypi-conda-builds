package:
  name: django-organizations
  version: "0.5.1"

source:
  fn: django-organizations-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-organizations/django-organizations-0.5.1.tar.gz
  md5: 743d32313b93e234efd3f69241119538
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-organizations = django-organizations:main
    #
    # Would create an entry point called django-organizations that calls django-organizations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - organizations
    - organizations.backends
    - organizations.south_migrations
    - organizations.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bennylope/django-organizations/
  license: BSD License
  summary: 'Group accounts for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
