package:
  name: django-foundation
  version: "0.1"

source:
  fn: django-foundation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-foundation/django-foundation-0.1.tar.gz
  md5: 9289d9cf461dc3cccb9c8af41e240d3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-foundation = django-foundation:main
    #
    # Would create an entry point called django-foundation that calls django-foundation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - foundation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/petry/django-foundation
  license: BSD License
  summary: 'Django Integration with foundation front-end.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
