package:
  name: djinn
  version: "1.0.6"

source:
  fn: djinn-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/djinn/djinn-1.0.6.tar.gz
  md5: 9f22c12555fce572e4c33103747fa839
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinn = djinn:main
    #
    # Would create an entry point called djinn that calls djinn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djinn
    - djinn.datastore
    - djinn.stats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/palmhold/djinn
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Djinn is a micro-framework which wraps Tornado to make it easy to write a web application using Tornado.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
