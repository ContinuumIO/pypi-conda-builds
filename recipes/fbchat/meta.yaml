package:
  name: fbchat
  version: "0.0.2"

source:
  fn: fbchat-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fbchat/fbchat-0.0.2.tar.gz
  md5: d93a36385a46685c63c8f2c266d601b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fbchat = fbchat:main
    #
    # Would create an entry point called fbchat that calls fbchat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - bs4

  run:
    - python
    - requests
    - bs4

test:
  # Python imports
  imports:
    - fbchat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carpedm20/fbchat/
  license: BSD License or BSD License
  summary: 'Facebook Chat (Messenger) for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
