package:
  name: django-cratis-common
  version: "0.1.0"

source:
  fn: django-cratis-common-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cratis-common/django-cratis-common-0.1.0.tar.gz
  md5: e14eec166566837b161a2c6786bd8dd4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cratis-common = django-cratis-common:main
    #
    # Would create an entry point called django-cratis-common that calls django-cratis-common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-cratis
    - setuptools

  run:
    - python
    - django-cratis
    - setuptools

test:
  # Python imports
  imports:
    - cratis_common

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Integrates django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
