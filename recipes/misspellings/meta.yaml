package:
  name: misspellings
  version: "2.0c"

source:
  fn: misspellings-2.0c.tar.gz
  url: https://pypi.python.org/packages/source/m/misspellings/misspellings-2.0c.tar.gz
  md5: d194d51a086fc97cc819a9f1b3d4c57f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - misspellings = misspellings:main
    #
    # Would create an entry point called misspellings that calls misspellings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - misspellings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lyda/misspell-check
  license: GNU General Public License v3 (GPLv3)
  summary: 'A tool to detect misspellings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
