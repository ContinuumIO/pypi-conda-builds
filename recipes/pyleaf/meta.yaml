package:
  name: pyleaf
  version: "1.0.1.1"

source:
  fn: pyleaf-1.0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyleaf/pyleaf-1.0.1.1.tar.gz
  md5: 03bbae615eb88834f915e1b0ecccfc35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyleaf = pyleaf:main
    #
    # Would create an entry point called pyleaf that calls pyleaf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyleaf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.francesconapolitano.it/leaf/downloads/pyleaf-1.0.1.tar.gz
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'ASCII-ART Data Analysis Pipeline Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
