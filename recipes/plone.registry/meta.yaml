package:
  name: plone.registry
  version: "1.0.2"

source:
  fn: plone.registry-1.0.2.zip
  url: https://pypi.python.org/packages/source/p/plone.registry/plone.registry-1.0.2.zip
  md5: 100bffff963eed54200fb7da1bebd501
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.registry = plone.registry:main
    #
    # Would create an entry point called plone.registry that calls plone.registry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zodb3
    - zope.schema
    - zope.interface
    - zope.component
    - zope.dottedname
    - zope.event
    - zope.testing

  run:
    - python
    - setuptools
    - setuptools
    - zodb3
    - zope.schema
    - zope.interface
    - zope.component
    - zope.dottedname
    - zope.event
    - zope.testing

test:
  # Python imports
  imports:
    - plone
    - plone.registry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.registry
  license: GPL
  summary: 'A debconf-like (or about:config-like) registry for storing application settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
