package:
  name: lmj.plot
  version: "0.4.1"

source:
  fn: lmj.plot-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lmj.plot/lmj.plot-0.4.1.tar.gz
  md5: 9f25823c5aaf8c0fe9f508883451bcb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lmj.plot = lmj.plot:main
    #
    # Would create an entry point called lmj.plot that calls lmj.plot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - prettyplotlib
    - climate

  run:
    - python
    - prettyplotlib
    - climate

test:
  # Python imports
  imports:
    - lmj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lmjohns3/py-plot
  license: MIT License
  summary: 'A command line tool for plotting data from text files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
