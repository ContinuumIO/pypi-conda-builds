package:
  name: timegaps
  version: "0.1.1"

source:
  fn: timegaps-0.1.1.zip
  url: https://pypi.python.org/packages/source/t/timegaps/timegaps-0.1.1.zip
  md5: f3ace6ac2687ba5f9959baba9ced698d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - timegaps = timegaps:main
    #
    # Would create an entry point called timegaps that calls timegaps.main()

    - timegaps = timegaps.main:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - timegaps

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - timegaps --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gehrcke.de/timegaps
  license: MIT License
  summary: 'Accept or reject items based on age categorization.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
