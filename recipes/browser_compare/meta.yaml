package:
  name: browser_compare
  version: "0.0.1"

source:
  fn: browser_compare-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/browser_compare/browser_compare-0.0.1.tar.gz
  md5: 321c5b07daf5025a469ba1a6455d77ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - browser_compare = browser_compare:main
    #
    # Would create an entry point called browser_compare that calls browser_compare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httpagentparser >=1.6.0

  run:
    - python
    - httpagentparser >=1.6.0

test:
  # Python imports
  imports:
    - browser_compare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ojarva/browser-compare
  license: MIT License
  summary: 'Library for detecting browser changes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
