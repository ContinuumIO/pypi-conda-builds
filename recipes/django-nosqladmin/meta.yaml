package:
  name: django-nosqladmin
  version: "0.1.1"

source:
  fn: django-nosqladmin-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nosqladmin/django-nosqladmin-0.1.1.tar.gz
  md5: 2f0dc67ea309610ec73fc857c4060a05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nosqladmin = django-nosqladmin:main
    #
    # Would create an entry point called django-nosqladmin that calls django-nosqladmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo ==2.1.1

  run:
    - python
    - pymongo ==2.1.1

test:
  # Python imports
  imports:
    - nosqladmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pydanny/django-nosqladmin
  license: BSD License
  summary: 'An introspective interface for Django and Document Based NoSQL databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
