package:
  name: js.xeditable
  version: "1.4.5"

source:
  fn: js.xeditable-1.4.5.zip
  url: https://pypi.python.org/packages/source/j/js.xeditable/js.xeditable-1.4.5.zip
  md5: c25ab8f0d813f5f4b15aa61b28dab1ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.xeditable = js.xeditable:main
    #
    # Would create an entry point called js.xeditable that calls js.xeditable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jqueryui
    - js.bootstrap
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jqueryui
    - js.bootstrap
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.xeditable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RedTurtle/js.xeditable
  license: BSD
  summary: 'fanstatic xeditable.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
