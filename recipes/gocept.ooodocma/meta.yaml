package:
  name: gocept.ooodocma
  version: "0.9"

source:
  fn: gocept.ooodocma-0.9.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.ooodocma/gocept.ooodocma-0.9.tar.gz
  md5: f36a960b850cb12a9ad76499c12d239a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.ooodocma = gocept.ooodocma:main
    #
    # Would create an entry point called gocept.ooodocma that calls gocept.ooodocma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - gocept.download
    - zc.recipe.egg
    - zdaemon

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - gocept.download
    - zc.recipe.egg
    - zdaemon

test:
  # Python imports
  imports:
    - gocept
    - gocept.ooodocma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ftp://ftp.gocept.com/OOoDocmaServer/
  license: ZPL 2.1
  summary: 'zc.buildout recipe for installing an OpenOffice.org/DocmaServer package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
