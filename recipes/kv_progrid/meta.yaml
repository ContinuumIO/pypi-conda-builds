package:
  name: kv_progrid
  version: "0.1.2.11"

source:
  fn: kv_progrid-0.1.2.11.tar.gz
  url: https://pypi.python.org/packages/source/k/kv_progrid/kv_progrid-0.1.2.11.tar.gz
  md5: 4f57c2f6bdcf4e9fce24e703a131d9fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kv_progrid = kv_progrid:main
    #
    # Would create an entry point called kv_progrid that calls kv_progrid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kv_progrid
    - progrid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Cuuuurzel/kivy-pro-grid
  license: UNKNOWN
  summary: 'Easy to use grid for kivy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
