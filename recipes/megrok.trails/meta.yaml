package:
  name: megrok.trails
  version: "1.0"

source:
  fn: megrok.trails-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.trails/megrok.trails-1.0.tar.gz
  md5: 3349c3e47d51b6d0b2f9a027404d575b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.trails = megrok.trails:main
    #
    # Would create an entry point called megrok.trails that calls megrok.trails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok

  run:
    - python
    - setuptools
    - setuptools
    - grok

test:
  # Python imports
  imports:
    - megrok
    - megrok.trails
    - megrok.trails.ftests
    - megrok.trails.ftests.traversal
    - megrok.trails.ftests.url

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'URL patterns for Grok applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
