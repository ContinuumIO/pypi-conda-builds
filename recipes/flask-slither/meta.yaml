package:
  name: flask-slither
  version: "0.5.2"

source:
  fn: Flask-Slither-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Slither/Flask-Slither-0.5.2.tar.gz
  md5: 8dd2468add99c60639b912b30cd0bdc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-slither = flask-slither:main
    #
    # Would create an entry point called flask-slither that calls flask-slither.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.10.1
    - pymongo ==2.6

  run:
    - python
    - flask ==0.10.1
    - pymongo ==2.6

test:
  # Python imports
  imports:
    - flask_slither

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gevious/flask_slither
  license: MIT License
  summary: 'A small library between MongoDB and JSON API endpoints'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
