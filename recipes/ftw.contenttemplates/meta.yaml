package:
  name: ftw.contenttemplates
  version: "1.2.1"

source:
  fn: ftw.contenttemplates-1.2.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.contenttemplates/ftw.contenttemplates-1.2.1.zip
  md5: 173beb2c1384df60eebd58368fbd4aa0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.contenttemplates = ftw.contenttemplates:main
    #
    # Would create an entry point called ftw.contenttemplates that calls ftw.contenttemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.contentmenu

  run:
    - python
    - setuptools
    - setuptools
    - ftw.contentmenu

test:
  # Python imports
  imports:
    - ftw
    - ftw.contenttemplates
    - ftw.contenttemplates.browser
    - ftw.contenttemplates.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.contenttemplates
  license: GPL2
  summary: 'Create content objects from templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
