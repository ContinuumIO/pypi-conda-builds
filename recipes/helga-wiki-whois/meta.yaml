package:
  name: helga-wiki-whois
  version: "0.1.0"

source:
  fn: helga-wiki-whois-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-wiki-whois/helga-wiki-whois-0.1.0.tar.gz
  md5: 6126c7e8657419e0435865e03ca98b6a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-wiki-whois = helga-wiki-whois:main
    #
    # Would create an entry point called helga-wiki-whois that calls helga-wiki-whois.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-wiki-whois
  license: MIT License
  summary: 'A helga command to generate confluence-type URLs for users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
