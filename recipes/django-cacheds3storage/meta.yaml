package:
  name: django-cacheds3storage
  version: "0.1.1"

source:
  fn: django-cacheds3storage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cacheds3storage/django-cacheds3storage-0.1.1.tar.gz
  md5: 86acd368b248a7072249b5ef866cff5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cacheds3storage = django-cacheds3storage:main
    #
    # Would create an entry point called django-cacheds3storage that calls django-cacheds3storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil

  run:
    - python
    - python-dateutil

test:
  # Python imports
  imports:
    - cacheds3storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ccnmtl/django-cacheds3storage
  license: BSD
  summary: 'S3 cached storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
