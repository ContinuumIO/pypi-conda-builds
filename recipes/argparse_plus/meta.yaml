package:
  name: argparse_plus
  version: "0.1.0"

source:
  fn: argparse_plus-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/argparse_plus/argparse_plus-0.1.0.tar.gz
  md5: fad0bffea437e992f5cebcbccc9012b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argparse_plus = argparse_plus:main
    #
    # Would create an entry point called argparse_plus that calls argparse_plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - argparse_plus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pnpnpn/argparse-plus
  license: MIT License
  summary: 'ArgumentParser Plus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
