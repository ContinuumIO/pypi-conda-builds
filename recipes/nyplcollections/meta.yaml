package:
  name: nyplcollections
  version: "1.1"

source:
  fn: nyplcollections-1.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nyplcollections/nyplcollections-1.1.tar.gz
  md5: a33b723f3770b4ba779bf251092ced15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nyplcollections = nyplcollections:main
    #
    # Would create an entry point called nyplcollections that calls nyplcollections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - nyplcollections

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nmohoric/nypl-digital-collections
  license: UNKNOWN
  summary: 'new york public library image collections api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
