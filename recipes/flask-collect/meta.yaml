package:
  name: flask-collect
  version: "1.2.1"

source:
  fn: Flask-Collect-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Collect/Flask-Collect-1.2.1.tar.gz
  md5: bc50e99a3bce16fc26bf655f59547ea9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-collect = flask-collect:main
    #
    # Would create an entry point called flask-collect that calls flask-collect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1

  run:
    - python
    - flask >=0.10.1

test:
  # Python imports
  imports:
    - flask_collect
    - flask_collect.storage
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home:  http://github.com/klen/Flask-Collect
  license: BSD License
  summary: 'Flask-Collect -- Collect static files in Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
