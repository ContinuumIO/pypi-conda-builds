package:
  name: mysql_helper
  version: "1.0.0"

source:
  fn: mysql_helper-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mysql_helper/mysql_helper-1.0.0.tar.gz
  md5: 035c52019a4385e5da7276c0070e1359
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mysql_helper = mysql_helper:main
    #
    # Would create an entry point called mysql_helper that calls mysql_helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mysql_helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Um m\xc3\xb3dulo para auxiliar opera\xc3\xa7\xc3\xb5es com o MySQL e MySQLdb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
