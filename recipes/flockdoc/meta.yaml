package:
  name: flockdoc
  version: "0.0.4"

source:
  fn: flockdoc-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/flockdoc/flockdoc-0.0.4.tar.gz
  md5: b2f28d0418ec39c42c37a92c8b7131e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flockdoc = flockdoc:main
    #
    # Would create an entry point called flockdoc that calls flockdoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown ==2.3.1
    - pyyaml ==3.10
    - jinja2 ==2.7
    - pygments ==1.6

  run:
    - python
    - markdown ==2.3.1
    - pyyaml ==3.10
    - jinja2 ==2.7
    - pygments ==1.6

test:
  # Python imports
  imports:
    - flockdoc
    - flockdoc.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brianhicks/flockdoc
  license: Apache Software License
  summary: 'Create example API documentation in multiple languages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
