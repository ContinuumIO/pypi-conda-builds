package:
  name: cmsplugin-accordion
  version: "0.2.2"

source:
  fn: cmsplugin-accordion-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-accordion/cmsplugin-accordion-0.2.2.tar.gz
  md5: 2af1b3a2bdac3d501b1a25758a02a1a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-accordion = cmsplugin-accordion:main
    #
    # Would create an entry point called cmsplugin-accordion that calls cmsplugin-accordion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cmsplugin_accordion
    - cmsplugin_accordion.migrations
    - cmsplugin_accordion.tests
    - cmsplugin_accordion.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/cmsplugin-accordion
  license: The MIT License
  summary: 'django-cms plugin that allow to create accordions on the frontend.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
