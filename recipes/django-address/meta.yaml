package:
  name: django-address
  version: "0.1.1"

source:
  fn: django-address-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-address/django-address-0.1.1.tar.gz
  md5: 053c6510f7bfb81cf78e4b41b2558bac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-address = django-address:main
    #
    # Would create an entry point called django-address that calls django-address.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - address
    - address.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/furious-luke/django-address
  license: BSD License
  summary: 'A django application for describing addresses.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
