package:
  name: collective.tablepage
  version: "0.10.1"

source:
  fn: collective.tablepage-0.10.1.zip
  url: https://pypi.python.org/packages/source/c/collective.tablepage/collective.tablepage-0.10.1.zip
  md5: a97ec13aa636ffa49debab7b9adc254f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.tablepage = collective.tablepage:main
    #
    # Would create an entry point called collective.tablepage that calls collective.tablepage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.atcontenttypes
    - collective.datagridcolumns >=0.6.1
    - collective.autopermission
    - products.tinymce
    - products.advancedquery
    - products.datagridfield >=1.9.0

  run:
    - python
    - setuptools
    - setuptools
    - products.atcontenttypes
    - collective.datagridcolumns >=0.6.1
    - collective.autopermission
    - products.tinymce
    - products.advancedquery
    - products.datagridfield >=1.9.0

test:
  # Python imports
  imports:
    - collective
    - collective.tablepage
    - collective.tablepage.browser
    - collective.tablepage.content
    - collective.tablepage.fields
    - collective.tablepage.fields.computed
    - collective.tablepage.search
    - collective.tablepage.storage
    - collective.tablepage.tests
    - collective.tablepage.validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.tablepage
  license: GNU General Public License (GPL)
  summary: 'A Plone page with an editable table as main content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
