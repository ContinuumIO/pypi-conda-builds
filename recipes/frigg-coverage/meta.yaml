package:
  name: frigg-coverage
  version: "1.0.0"

source:
  fn: frigg-coverage-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/f/frigg-coverage/frigg-coverage-1.0.0.tar.gz
  md5: ae6155c263afc5219ddf63c0a518c45a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frigg-coverage = frigg-coverage:main
    #
    # Would create an entry point called frigg-coverage that calls frigg-coverage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - frigg_coverage
    - frigg_coverage.parsers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frigg/frigg-coverage
  license: MIT
  summary: 'Coverage report reading util'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
