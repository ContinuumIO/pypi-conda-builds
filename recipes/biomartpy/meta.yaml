package:
  name: biomartpy
  version: "0.1.2"

source:
  fn: biomartpy-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/biomartpy/biomartpy-0.1.2.tar.gz
  md5: 23eda607e826dce6ef12dacd7e9d42fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biomartpy = biomartpy:main
    #
    # Would create an entry point called biomartpy that calls biomartpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas
    - rpy2

  run:
    - python
    - pandas
    - rpy2

test:
  # Python imports
  imports:
    - biomartpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: none
  license: UNKNOWN
  summary: '\nSimple interface for accessing biomaRt from Python (via rpy2 and pandas)\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
