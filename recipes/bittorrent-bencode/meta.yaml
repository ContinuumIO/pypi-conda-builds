package:
  name: bittorrent-bencode
  version: "5.0.8.1"

source:
  fn: BitTorrent-bencode-5.0.8.1.zip
  url: https://pypi.python.org/packages/source/B/BitTorrent-bencode/BitTorrent-bencode-5.0.8.1.zip
  md5: 264d91c9611713a79f8f1ccdc86ec390
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bittorrent-bencode = bittorrent-bencode:main
    #
    # Would create an entry point called bittorrent-bencode that calls bittorrent-bencode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bittorrent.com/
  license: BitTorrent Open Source License
  summary: 'The BitTorrent bencode module as leight-weight, standalone package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
