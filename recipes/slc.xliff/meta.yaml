package:
  name: slc.xliff
  version: "2.0b2"

source:
  fn: slc.xliff-2.0b2.zip
  url: https://pypi.python.org/packages/source/s/slc.xliff/slc.xliff-2.0b2.zip
  md5: 9e49b84563f76cda4eecaa6ae3daa892
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.xliff = slc.xliff:main
    #
    # Would create an entry point called slc.xliff that calls slc.xliff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - zope.component
    - plone.app.contenttypes
    - plone.app.multilingual[archetypes,dexterity]

  run:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - zope.component
    - plone.app.contenttypes
    - plone.app.multilingual[archetypes,dexterity]

test:
  # Python imports
  imports:
    - slc
    - slc.xliff
    - slc.xliff.adapters
    - slc.xliff.browser
    - slc.xliff.templates
    - slc.xliff.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/slc.xliff
  license: GNU General Public License (GPL) or European Union Public Licence 1.1 (EUPL 1.1)
  summary: 'A framework to allow im and export of xliff files for translations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
