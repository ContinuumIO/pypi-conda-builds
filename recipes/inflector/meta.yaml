package:
  name: inflector
  version: "2.0.11"

source:
  fn: Inflector-2.0.11.tar.gz
  url: https://pypi.python.org/packages/source/I/Inflector/Inflector-2.0.11.tar.gz
  md5: 553aa5a6f381882768741702c63240c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inflector = inflector:main
    #
    # Would create an entry point called inflector that calls inflector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - inflector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ixmatus/inflector
  license: Python Software Foundation License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
