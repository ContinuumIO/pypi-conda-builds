package:
  name: pymunk
  version: "4.0.0"

source:
  fn: pymunk-4.0.0.zip
  url: https://pypi.python.org/packages/source/p/pymunk/pymunk-4.0.0.zip
  md5: 0b1fe07990840661e22d128c1a696009
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pymunk = pymunk:main
    #
    # Would create an entry point called pymunk that calls pymunk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pymunk
    - pymunkoptions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pymunk.org
  license: MIT License
  summary: 'pymunk is a easy-to-use pythonic 2d physics library built on top of Chipmunk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
