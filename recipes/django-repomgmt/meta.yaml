package:
  name: django-repomgmt
  version: "0.1.1"

source:
  fn: django-repomgmt-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-repomgmt/django-repomgmt-0.1.1.tar.gz
  md5: cf21ff11e7c864c9af112d3bc8cbb6ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-repomgmt = django-repomgmt:main
    #
    # Would create an entry point called django-repomgmt that calls django-repomgmt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - repomgmt
    - repomgmt.management
    - repomgmt.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sorenh/python-django-repomgmt
  license: Apache Software License
  summary: 'APT repo management, buildd, etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
