package:
  name: mpldatacursor
  version: "0.5"

source:
  fn: mpldatacursor-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mpldatacursor/mpldatacursor-0.5.tar.gz
  md5: 6038e0e35c2db7cc1d43f453c1342a0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpldatacursor = mpldatacursor:main
    #
    # Would create an entry point called mpldatacursor that calls mpldatacursor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib >=0.9
    - numpy >=1.1

  run:
    - python
    - matplotlib >=0.9
    - numpy >=1.1

test:
  # Python imports
  imports:
    - mpldatacursor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joferkington/mpldatacursor/
  license: MIT
  summary: 'Interactive data cursors for Matplotlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
