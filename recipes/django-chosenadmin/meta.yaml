package:
  name: django-chosenadmin
  version: "0.2.0"

source:
  fn: django-chosenadmin-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chosenadmin/django-chosenadmin-0.2.0.tar.gz
  md5: 146bfb75fd0dcf34e53665e87ddb9f45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chosenadmin = django-chosenadmin:main
    #
    # Would create an entry point called django-chosenadmin that calls django-chosenadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chosenadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradmontgomery/django-chosenadmin/archive/v0.2.0.tar.gz
  license: MIT License
  summary: 'Adds Chosen.js to the Django Admin app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
