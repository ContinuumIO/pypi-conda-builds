package:
  name: django-ipgeo
  version: "0.0.2"

source:
  fn: django-ipgeo-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ipgeo/django-ipgeo-0.0.2.tar.gz
  md5: 80fa996ff716b3403ede75ccf4cacbde
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ipgeo = django-ipgeo:main
    #
    # Would create an entry point called django-ipgeo that calls django-ipgeo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-common

  run:
    - python
    - django-common

test:
  # Python imports
  imports:
    - ipgeo
    - ipgeo.management
    - ipgeo.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/django-ipgeo/
  license: BSD License
  summary: 'API to ipgeobase.ru database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
