package:
  name: django-servee
  version: "0.7a1.dev1"

source:
  fn: django-servee-0.7a1.dev1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-servee/django-servee-0.7a1.dev1.tar.gz
  md5: 15f5a837d070154fb324b5b77c090906
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-servee = django-servee:main
    #
    # Would create an entry point called django-servee that calls django-servee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.5
    - django-classy-tags

  run:
    - python
    - django >=1.2.5
    - django-classy-tags

test:
  # Python imports
  imports:
    - servee
    - servee.frontendadmin
    - servee.frontendadmin.templatetags
    - servee.frontendadmin.tests
    - servee.wysiwyg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/servee/servee/
  license: BSD License
  summary: 'Inline Wysiwyg editing making use of a subclassed Django Admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
