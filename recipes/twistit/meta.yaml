package:
  name: twistit
  version: "0.2.0"

source:
  fn: twistit-0.2.0.zip
  url: https://pypi.python.org/packages/source/t/twistit/twistit-0.2.0.zip
  md5: b8d6a618addb9574e0d72afb34188645
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - twistit = twistit:main
    #
    # Would create an entry point called twistit that calls twistit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - utwist

  run:
    - python
    - twisted
    - utwist

test:
  # Python imports
  imports:
    - twistit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smurn/twistit
  license: UNKNOWN
  summary: 'Handy tools for the twisted framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
