package:
  name: products.validation
  version: "2.0.1"

source:
  fn: Products.validation-2.0.1.zip
  url: https://pypi.python.org/packages/source/P/Products.validation/Products.validation-2.0.1.zip
  md5: 7fcfda235a566027fa1a49178d6bd23d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.validation = products.validation:main
    #
    # Would create an entry point called products.validation that calls products.validation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - acquisition
    - datetime
    - zope2

  run:
    - python
    - setuptools
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - acquisition
    - datetime
    - zope2

test:
  # Python imports
  imports:
    - Products
    - Products.validation
    - Products.validation.interfaces
    - Products.validation.tests
    - Products.validation.validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.validation
  license: GPL
  summary: 'Data validation package for Archetypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
