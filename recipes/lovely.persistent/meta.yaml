package:
  name: lovely.persistent
  version: "0.1.1"

source:
  fn: lovely.persistent-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.persistent/lovely.persistent-0.1.1.tar.gz
  md5: 8c9910f2cf366f51e6e41e6c7fb5fe2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.persistent = lovely.persistent:main
    #
    # Would create an entry point called lovely.persistent that calls lovely.persistent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3

  run:
    - python
    - setuptools
    - zodb3

test:
  # Python imports
  imports:
    - lovely
    - lovely.persistent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/lovely.persistent
  license: Zope Public License
  summary: 'improved ZODB persistence'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
