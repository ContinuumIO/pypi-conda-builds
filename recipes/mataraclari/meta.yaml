package:
  name: mataraclari
  version: "0.13"

source:
  fn: mataraclari-0.13.tar.gz
  url: https://pypi.python.org/packages/source/m/mataraclari/mataraclari-0.13.tar.gz
  md5: e496e380aa10a6b146c542eb34895c79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mataraclari = mataraclari:main
    #
    # Would create an entry point called mataraclari that calls mataraclari.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mataraclari

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://yasar.serveblog.net
  license: GNU General Public License (GPL)
  summary: 'Provides some math utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
