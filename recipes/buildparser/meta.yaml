package:
  name: buildparser
  version: "0.1.1"

source:
  fn: buildparser-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/buildparser/buildparser-0.1.1.tar.gz
  md5: 682df479eb4358ca4f497afec03a8cac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildparser = buildparser:main
    #
    # Would create an entry point called buildparser that calls buildparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.11

  run:
    - python
    - pyyaml ==3.11

test:
  # Python imports
  imports:
    - buildparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brettlangdon/buildparser
  license: MIT License
  summary: 'Generic job build specification parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
