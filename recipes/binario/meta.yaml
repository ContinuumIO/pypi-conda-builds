package:
  name: binario
  version: "0.0.4"

source:
  fn: binario-0.0.4.zip
  url: https://pypi.python.org/packages/source/b/binario/binario-0.0.4.zip
  md5: 54a599545c1f24e272280924b5a0272e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - binario = binario:main
    #
    # Would create an entry point called binario that calls binario.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - binario

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/asaskevich/binario
  license: MIT
  summary: 'Package that lets an application read/write primitive data types from an underlying input/output stream as binary data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
