package:
  name: django-simple-mixins
  version: "0.1.0"

source:
  fn: django-simple-mixins-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-simple-mixins/django-simple-mixins-0.1.0.zip
  md5: 1b0a75c2688164bae8ab1899f47e240d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-mixins = django-simple-mixins:main
    #
    # Would create an entry point called django-simple-mixins that calls django-simple-mixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplemixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drager/django-simple-mixins
  license: BSD License
  summary: 'Mixins to use for Djangos Class Based Views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
