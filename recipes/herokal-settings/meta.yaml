package:
  name: herokal-settings
  version: "0.0.1"

source:
  fn: herokal-settings-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/herokal-settings/herokal-settings-0.0.1.tar.gz
  md5: 5b270416c31903ab95f91a0e8fa6e6ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - herokal-settings = herokal-settings:main
    #
    # Would create an entry point called herokal-settings that calls herokal-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dj-database-url

  run:
    - python
    - dj-database-url

test:
  # Python imports
  imports:
    - herokal
    - herokal.management
    - herokal.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drinks/herokal-settings
  license: BSD
  summary: 'Make your django local_settings play nice with Heroku'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
