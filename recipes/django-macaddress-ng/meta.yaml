package:
  name: django-macaddress-ng
  version: "1.1.1"

source:
  fn: django-macaddress-ng-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-macaddress-ng/django-macaddress-ng-1.1.1.tar.gz
  md5: 39ac3f8e829beedcd38427472a25074f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-macaddress-ng = django-macaddress-ng:main
    #
    # Would create an entry point called django-macaddress-ng that calls django-macaddress-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netaddr

  run:
    - python
    - netaddr

test:
  # Python imports
  imports:
    - macaddress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kra3/django-macaddress
  license: BSD License
  summary: 'MAC address model and form fields for Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
