package:
  name: zope.app.i18nfile
  version: "3.4.1"

source:
  fn: zope.app.i18nfile-3.4.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.i18nfile/zope.app.i18nfile-3.4.1.tar.gz
  md5: 295553b12f664ff88fcb3ff87d840a5b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.i18nfile = zope.app.i18nfile:main
    #
    # Would create an entry point called zope.app.i18nfile that calls zope.app.i18nfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.app.file
    - zope.i18n
    - zope.interface
    - zope.size

  run:
    - python
    - setuptools
    - zodb3
    - zope.app.file
    - zope.i18n
    - zope.interface
    - zope.size

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.i18nfile
    - zope.app.i18nfile.browser
    - zope.app.i18nfile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.i18nfile
  license: Zope Public License
  summary: 'I18n File and Image -- Zope 3 Content Components'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
