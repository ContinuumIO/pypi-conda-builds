package:
  name: greenbalance
  version: "0.2.0"

source:
  fn: greenbalance-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/greenbalance/greenbalance-0.2.0.tar.gz
  md5: 0a7d05a3aaab0001d951320d71829a24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - greenbalance = greenbalance:main
    #
    # Would create an entry point called greenbalance that calls greenbalance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - wr
    - gevent

  run:
    - python
    - wr
    - gevent

test:
  # Python imports
  imports:
    - greenbalance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/waawal/greenbalance
  license: GNU General Public License (GPL) or GNU General Public License v3 or later (GPLv3+)
  summary: 'Load Balancer for TCP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
