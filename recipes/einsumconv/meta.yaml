package:
  name: einsumconv
  version: "0.0.2"

source:
  fn: EinSumConv-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/E/EinSumConv/EinSumConv-0.0.2.tar.gz
  md5: 344391e7ee090449d62f435d68ec12e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - einsumconv = einsumconv:main
    #
    # Would create an entry point called einsumconv that calls einsumconv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sympy >=0.7.5

  run:
    - python
    - sympy >=0.7.5

test:
  # Python imports
  imports:
    - EinSumConv
    - einsumconv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LindaLinsefors/EinSumConv
  license: BSD
  summary: 'The EinSumConv package builds on sympy and supports calculations according to Einstein summation convention.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
