package:
  name: frla
  version: "0.0.1dev-r2"

source:
  fn: frla.tar.gz
  url: https://pypi.python.org/packages/source/f/frla/frla.tar.gz
  md5: 306113f77d270cbd5c9343db0c66a9bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frla = frla:main
    #
    # Would create an entry point called frla that calls frla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylons >=0.9.6.2

  run:
    - python
    - setuptools
    - pylons >=0.9.6.2

test:
  # Python imports
  imports:
    - frla
    - frla.config
    - frla.controllers
    - frla.lib
    - frla.model
    - frla.tests
    - frla.tests.functional

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3
  summary: 'FreeRadius Log Analyser for PostGreSQL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
