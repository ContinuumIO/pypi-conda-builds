package:
  name: django_options
  version: "r6"

source:
  fn: django_options-r6.tar.gz
  url: https://pypi.python.org/packages/source/d/django_options/django_options-r6.tar.gz
  md5: d955b6d4d3a44682dd81d5c99d399fb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_options = django_options:main
    #
    # Would create an entry point called django_options that calls django_options.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django.contrib.options
    - django_options

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.haagenti.com/
  license: GPL
  summary: 'MetaOptions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
