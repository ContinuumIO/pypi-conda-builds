package:
  name: ftw.pdfgenerator
  version: "1.3.7"

source:
  fn: ftw.pdfgenerator-1.3.7.zip
  url: https://pypi.python.org/packages/source/f/ftw.pdfgenerator/ftw.pdfgenerator-1.3.7.zip
  md5: 6ca4a3f32e6074fa53d57b5e3921204c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.pdfgenerator = ftw.pdfgenerator:main
    #
    # Would create an entry point called ftw.pdfgenerator that calls ftw.pdfgenerator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2
    - zope.annotation
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - products.archetypes
    - products.cmfcore
    - mako
    - beautifulsoup !=4.0b

  run:
    - python
    - setuptools
    - setuptools
    - zope2
    - zope.annotation
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - products.archetypes
    - products.cmfcore
    - mako
    - beautifulsoup !=4.0b

test:
  # Python imports
  imports:
    - ftw
    - ftw.pdfgenerator
    - ftw.pdfgenerator.browser
    - ftw.pdfgenerator.html2latex
    - ftw.pdfgenerator.html2latex.subconverters
    - ftw.pdfgenerator.layout
    - ftw.pdfgenerator.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.pdfgenerator
  license: GNU General Public License (GPL)
  summary: 'A library for generating PDF representations of Plone objects with LaTeX.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
