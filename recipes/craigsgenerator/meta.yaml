package:
  name: craigsgenerator
  version: "0.1.1"

source:
  fn: craigsgenerator-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/craigsgenerator/craigsgenerator-0.1.1.tar.gz
  md5: 6e50bbd0702f73e93e9710f60a5d5e83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - craigsgenerator = craigsgenerator:main
    #
    # Would create an entry point called craigsgenerator that calls craigsgenerator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - lxml
    - python-dateutil

  run:
    - python
    - requests
    - lxml
    - python-dateutil

test:
  # Python imports
  imports:
    - craigsgenerator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/craigsgenerator.git
  license: AGPL
  summary: "Iterate through Craig's list"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
