package:
  name: django-rest-api
  version: "0.1.1"

source:
  fn: django-rest-api-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-rest-api/django-rest-api-0.1.1.zip
  md5: 730cd6411927b0b23c538234b3d66581
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-api = django-rest-api:main
    #
    # Would create an entry point called django-rest-api that calls django-rest-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - rest_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skies-io/django-rest-api
  license: MIT License
  summary: 'Django REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
