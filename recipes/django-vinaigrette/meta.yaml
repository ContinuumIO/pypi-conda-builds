package:
  name: django-vinaigrette
  version: "0.4.0"

source:
  fn: django-vinaigrette-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vinaigrette/django-vinaigrette-0.4.0.tar.gz
  md5: c249869d0b338384ee2a538dacff7714
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vinaigrette = django-vinaigrette:main
    #
    # Would create an entry point called django-vinaigrette that calls django-vinaigrette.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vinaigrette
    - vinaigrette.management
    - vinaigrette.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ecometrica/django-vinaigrette/
  license: BSD License
  summary: 'Translate Django model data using gettext'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
