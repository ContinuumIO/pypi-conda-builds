package:
  name: django-webcam
  version: "0.1b1"

source:
  fn: django-webcam-0.1b1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webcam/django-webcam-0.1b1.tar.gz
  md5: 02d59e58cdbcf8d1c8905e2e8e5542e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webcam = django-webcam:main
    #
    # Would create an entry point called django-webcam that calls django-webcam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - uuid

  run:
    - python
    - uuid

test:
  # Python imports
  imports:
    - webcam
    - webcam.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saxix/django-webcam
  license: BSD
  summary: 'django fields easy store webcam snaphot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
