package:
  name: gadjo
  version: "0.4.1"

source:
  fn: gadjo-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gadjo/gadjo-0.4.1.tar.gz
  md5: 922848faf4d23ac223fb0b27783a8a12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gadjo = gadjo:main
    #
    # Would create an entry point called gadjo that calls gadjo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xstatic
    - xstatic_font_awesome
    - xstatic_jquery
    - xstatic_jquery_ui

  run:
    - python
    - xstatic
    - xstatic_font_awesome
    - xstatic_jquery
    - xstatic_jquery_ui

test:
  # Python imports
  imports:
    - gadjo
    - gadjo.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Django base template tailored for management interfaces'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
