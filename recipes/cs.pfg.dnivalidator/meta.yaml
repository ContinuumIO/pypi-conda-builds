package:
  name: cs.pfg.dnivalidator
  version: "0.3"

source:
  fn: cs.pfg.dnivalidator-0.3.zip
  url: https://pypi.python.org/packages/source/c/cs.pfg.dnivalidator/cs.pfg.dnivalidator-0.3.zip
  md5: 7ae368c46c0ce8af4bfe32a5163e039e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.pfg.dnivalidator = cs.pfg.dnivalidator:main
    #
    # Would create an entry point called cs.pfg.dnivalidator that calls cs.pfg.dnivalidator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - cs
    - cs.pfg
    - cs.pfg.dnivalidator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.codesyntax.com/private/cs.pfg.dnivalidator
  license: GPL
  summary: 'A package that adds a Spanish ID card number validator for PloneFormGen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
