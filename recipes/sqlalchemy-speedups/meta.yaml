package:
  name: sqlalchemy-speedups
  version: "1.0"

source:
  fn: SQLAlchemy-speedups-1.0.zip
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-speedups/SQLAlchemy-speedups-1.0.zip
  md5: 4739fbdcfed4765f30370e7d1019b6e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-speedups = sqlalchemy-speedups:main
    #
    # Would create an entry point called sqlalchemy-speedups that calls sqlalchemy-speedups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlalchemy_speedups

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sqlalchemy.org
  license: MIT License
  summary: 'Database Abstraction Library C Extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
