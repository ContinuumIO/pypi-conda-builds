package:
  name: django-stackato
  version: "1.0"

source:
  fn: django-stackato-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stackato/django-stackato-1.0.tar.gz
  md5: 497d264780ab2e0c275ff3427fcdb6ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stackato = django-stackato:main
    #
    # Would create an entry point called django-stackato that calls django-stackato.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_stackato
    - django_stackato.management
    - django_stackato.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ActiveState/django-stackato
  license: MIT License
  summary: 'Django Stackato Extensions -- non-interactive changepassword'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
