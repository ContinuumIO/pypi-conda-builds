package:
  name: zpkg
  version: "1.0.0"

source:
  fn: zpkg-1.0.0.tgz
  url: https://pypi.python.org/packages/source/z/zpkg/zpkg-1.0.0.tgz
  md5: 14e3aad26367aa3132a9282c046eb8f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zpkg = zpkg:main
    #
    # Would create an entry point called zpkg that calls zpkg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ZConfig
    - ZConfig.components
    - ZConfig.components.basic
    - ZConfig.components.basic.tests
    - ZConfig.components.logger
    - ZConfig.components.logger.tests
    - ZConfig.tests
    - ZConfig.tests.library
    - ZConfig.tests.library.thing
    - ZConfig.tests.library.widget
    - zpkg
    - zpkgsetup
    - zpkgsetup.tests
    - zpkgtools
    - zpkgtools.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.zope.org/Members/fdrake/zpkgtools/
  license: Zope Public License
  summary: 'Zope Packaging Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
