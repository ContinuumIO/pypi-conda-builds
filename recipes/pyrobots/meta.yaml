package:
  name: pyrobots
  version: "2.0"

source:
  fn: pyRobots-2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyRobots/pyRobots-2.0.tar.gz
  md5: d9d819c8f9e0c1efd6eae7e98286e8fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyrobots = pyrobots:main
    #
    # Would create an entry point called pyrobots that calls pyrobots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - futures
    - numpy

  run:
    - python
    - futures
    - numpy

test:
  # Python imports
  imports:
    - pyrobots
    - robots
    - robots.concurrency
    - robots.events
    - robots.helpers
    - robots.mw
    - robots.poses
    - robots.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chili-epfl/pyrobots
  license: ISC License (ISCL)
  summary: 'A Python toolset for event-based, asynchronous programming of robot controllers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
