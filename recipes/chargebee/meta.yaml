package:
  name: chargebee
  version: "1.4.6"

source:
  fn: chargebee-1.4.6.tar.gz
  url: https://pypi.python.org/packages/source/c/chargebee/chargebee-1.4.6.tar.gz
  md5: 957493cb90a3bb3ad10e393c8b8e2322
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chargebee = chargebee:main
    #
    # Would create an entry point called chargebee that calls chargebee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chargebee
    - chargebee.models
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://apidocs.chargebee.com/docs/api?lang=python
  license: UNKNOWN
  summary: 'Python wrapper for the ChargeBee Subscription Billing API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
