package:
  name: gu-django-filebrowser-no-grappelli
  version: "3.1.24"

source:
  fn: gu-django-filebrowser-no-grappelli-3.1.24.tar.gz
  url: https://pypi.python.org/packages/source/g/gu-django-filebrowser-no-grappelli/gu-django-filebrowser-no-grappelli-3.1.24.tar.gz
  md5: 484c85d2ad41309473ec8496d1e57a74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gu-django-filebrowser-no-grappelli = gu-django-filebrowser-no-grappelli:main
    #
    # Would create an entry point called gu-django-filebrowser-no-grappelli that calls gu-django-filebrowser-no-grappelli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - filebrowser
    - filebrowser.management
    - filebrowser.management.commands
    - filebrowser.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agushuley/gu-django-filebrowser-no-grappelli
  license: BSD License
  summary: 'Media-Management with the Django Admin-Interface. Without django-grappelli requirement.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
