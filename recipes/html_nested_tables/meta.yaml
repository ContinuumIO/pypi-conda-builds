package:
  name: html_nested_tables
  version: "0.2.0"

source:
  fn: html_nested_tables-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/html_nested_tables/html_nested_tables-0.2.0.tar.gz
  md5: 2150147ba7f7969c0a1e8054b330f2bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html_nested_tables = html_nested_tables:main
    #
    # Would create an entry point called html_nested_tables that calls html_nested_tables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - html_nested_tables

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BertrandBordage/html_nested_tables
  license: BSD License
  summary: u'Python module that generates nested HTML tables from nested association lists.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
