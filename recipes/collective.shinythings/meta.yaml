package:
  name: collective.shinythings
  version: "0.6dev-r117588"

source:
  fn: collective.shinythings-0.6dev-r117588.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.shinythings/collective.shinythings-0.6dev-r117588.tar.gz
  md5: 7dd20d9d8a74a4bb90aa971ce0789c04
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.shinythings = collective.shinythings:main
    #
    # Would create an entry point called collective.shinythings that calls collective.shinythings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.shinythings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GPL
  summary: 'Jazz up your Plone theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
