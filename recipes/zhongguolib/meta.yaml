package:
  name: zhongguolib
  version: "0.0.1"

source:
  fn: ZhongGuoLib-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/Z/ZhongGuoLib/ZhongGuoLib-0.0.1.tar.gz
  md5: a9dc050acc3d73effae4d93035e0c175
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zhongguolib = zhongguolib:main
    #
    # Would create an entry point called zhongguolib that calls zhongguolib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - zhongguolib
    - zhongguolib.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zhongguolib/
  license: LICENSE.txt
  summary: 'Useful chinese language stuff.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
