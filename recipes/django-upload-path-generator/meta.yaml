package:
  name: django-upload-path-generator
  version: "0.0.2"

source:
  fn: django-upload-path-generator-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-upload-path-generator/django-upload-path-generator-0.0.2.tar.gz
  md5: 1fc8ce6eda9ef67cf30b1003e07589ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-upload-path-generator = django-upload-path-generator:main
    #
    # Would create an entry point called django-upload-path-generator that calls django-upload-path-generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/limijiaoyin/djang-upload-path-generator
  license: UNKNOWN
  summary: "upload path generator for django model's FielField"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
