package:
  name: influx-content-client
  version: "0.0.7"

source:
  fn: influx-content-client-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/i/influx-content-client/influx-content-client-0.0.7.tar.gz
  md5: 7c5c3e0b404880da234e8fe6d42630d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - influx-content-client = influx-content-client:main
    #
    # Would create an entry point called influx-content-client that calls influx-content-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - influxdb ==0.1.13

  run:
    - python
    - influxdb ==0.1.13

test:
  # Python imports
  imports:
    - influx_content_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theonion/influx-content-client
  license: UNKNOWN
  summary: 'a thin wrapper around the influxdb client to work with content series -- see https://github.com/theonion/influx-trending'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
