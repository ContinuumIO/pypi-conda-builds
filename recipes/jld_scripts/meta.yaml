package:
  name: jld_scripts
  version: "0.8.9"

source:
  fn: jld_scripts-0.8.9.tar.gz
  url: https://pypi.python.org/packages/source/j/jld_scripts/jld_scripts-0.8.9.tar.gz
  md5: 556261c6af98027e9c344187a534470d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jld_scripts = jld_scripts:main
    #
    # Would create an entry point called jld_scripts that calls jld_scripts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jld_scripts
    - jld_scripts.agents
    - jld_scripts.music
    - jld_scripts.music.lastfm
    - jld_scripts.res
    - jld_scripts.scripts
    - jld_scripts.system

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.systemical.com/doc/opensource/jld_scripts
  license: UNKNOWN
  summary: 'Collection of python utility scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
