package:
  name: doapfiend-html
  version: "0.1.1"

source:
  fn: doapfiend-html-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/doapfiend-html/doapfiend-html-0.1.1.tar.gz
  md5: 1cec6a834f1a723e760c5a040a14848f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doapfiend-html = doapfiend-html:main
    #
    # Would create an entry point called doapfiend-html that calls doapfiend-html.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.0.2
    - doapfiend >=0.3.0

  run:
    - python
    - setuptools
    - lxml >=2.0.2
    - doapfiend >=0.3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.doapspace.org/doapfiend/wiki/DfHtml
  license: BSD-2
  summary: 'Doapfiend plugin. Serializes DOAP as HTML/CSS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
