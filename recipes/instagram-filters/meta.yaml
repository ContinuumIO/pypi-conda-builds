package:
  name: instagram-filters
  version: "0.1"

source:
  fn: instagram-filters-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/instagram-filters/instagram-filters-0.1.tar.gz
  md5: e5e4a2b0bf38d49d9591eac30b16897d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instagram-filters = instagram-filters:main
    #
    # Would create an entry point called instagram-filters that calls instagram-filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil

  run:
    - python
    - pil

test:
  # Python imports
  imports:
    - instagram_filters
    - instagram_filters.decorations
    - instagram_filters.filters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/acoomans/instagram-filters
  license: LICENSE.txt
  summary: 'Instagram-like image filters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
