package:
  name: kotti_bootswatch
  version: "0.1.2"

source:
  fn: kotti_bootswatch-0.1.2.zip
  url: https://pypi.python.org/packages/source/k/kotti_bootswatch/kotti_bootswatch-0.1.2.zip
  md5: c348b116f62cf9a83fdd45c6d41906c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_bootswatch = kotti_bootswatch:main
    #
    # Would create an entry point called kotti_bootswatch that calls kotti_bootswatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.10b1

  run:
    - python
    - setuptools
    - kotti >=0.10b1

test:
  # Python imports
  imports:
    - kotti_bootswatch
    - kotti_bootswatch.scaffolds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mete0r/kotti_bootswatch
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Kotti bootswatch theme / theme generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
