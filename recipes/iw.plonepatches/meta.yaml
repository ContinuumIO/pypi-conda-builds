package:
  name: iw.plonepatches
  version: "0.1.1"

source:
  fn: iw.plonepatches-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.plonepatches/iw.plonepatches-0.1.1.tar.gz
  md5: 6bb1bc4c2df6e3451485992735ab543f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.plonepatches = iw.plonepatches:main
    #
    # Would create an entry point called iw.plonepatches that calls iw.plonepatches.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - iw
    - iw.plonepatches

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://ingeniweb.svn.sourceforge.net/svnroot/ingeniweb/iw.plonepatches
  license: GPL
  summary: 'Patches for Plone 3.x'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
