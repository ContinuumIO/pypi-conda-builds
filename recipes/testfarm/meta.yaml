package:
  name: testfarm
  version: "2.0"

source:
  fn: testfarm-2.0.tar.gz
  url: https://pypi.python.org/packages/source/t/testfarm/testfarm-2.0.tar.gz
  md5: 18df6c49bd464dd79ebb99b28b60f47b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testfarm = testfarm:main
    #
    # Would create an entry point called testfarm that calls testfarm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator
    - webob
    - argparse
    - wsgi_intercept
    - wsgiref

  run:
    - python
    - decorator
    - webob
    - argparse
    - wsgi_intercept
    - wsgiref

test:
  # Python imports
  imports:
    - testfarm
    - testfarm.v1

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/clam-project/testfarm/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Testfarm, a continuous integration tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
