package:
  name: django-readernaut
  version: "0.1"

source:
  fn: django-readernaut-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-readernaut/django-readernaut-0.1.tar.gz
  md5: 5e5dc67b7f16d2c004ef85eefec0da9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-readernaut = django-readernaut:main
    #
    # Would create an entry point called django-readernaut that calls django-readernaut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djangonaut
    - djangonaut.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/oscarduignan/django-readernaut/
  license: BSD
  summary: 'Django application which provides a template tag interface to the pyreadernaut package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
