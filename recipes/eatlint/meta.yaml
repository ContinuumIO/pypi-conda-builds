package:
  name: eatlint
  version: "1.1.0"

source:
  fn: EatLint-1.1.0.zip
  url: https://pypi.python.org/packages/source/E/EatLint/EatLint-1.1.0.zip
  md5: a46a33131a9793d7e4a6a1dcb16576c9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eatlint = eatlint:main
    #
    # Would create an entry point called eatlint that calls eatlint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - eatlint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://echospiral.com/trac/eatlint
  license: BSD License
  summary: 'PyLint Summary and Graph Generators for Bitten.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
