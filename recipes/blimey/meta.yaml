package:
  name: blimey
  version: "0.9.4"

source:
  fn: blimey-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/b/blimey/blimey-0.9.4.tar.gz
  md5: e6ae0f88c49f3f3b0033377dab11654b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blimey = blimey:main
    #
    # Would create an entry point called blimey that calls blimey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbkdf2
    - pycrypto
    - jinja2

  run:
    - python
    - pbkdf2
    - pycrypto
    - jinja2

test:
  # Python imports
  imports:
    - blimey
    - blimey.abstract
    - blimey.agile_keychain
    - blimey.agile_keychain._manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/openpassword/blimey
  license: MIT License
  summary: 'A password management library with AgileKeychain (1Password) support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
