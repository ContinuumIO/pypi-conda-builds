package:
  name: thorlabspm100
  version: "1.0"

source:
  fn: ThorlabsPM100-1.0.tar.gz
  url: https://pypi.python.org/packages/source/T/ThorlabsPM100/ThorlabsPM100-1.0.tar.gz
  md5: 1ac004ed326b8c085655780b98750d15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thorlabspm100 = thorlabspm100:main
    #
    # Would create an entry point called thorlabspm100 that calls thorlabspm100.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ThorlabsPM100
    - thorlabspm100

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/RigolDG5000
  license: BSD License
  summary: 'Interface to the PM100A/D power meter from Thorlabs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
