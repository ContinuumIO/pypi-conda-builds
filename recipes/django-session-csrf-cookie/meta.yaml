package:
  name: django-session-csrf-cookie
  version: "0.1"

source:
  fn: django-session-csrf-cookie-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-session-csrf-cookie/django-session-csrf-cookie-0.1.tar.gz
  md5: e93fbba1f289324796f7b46c3b9834a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-session-csrf-cookie = django-session-csrf-cookie:main
    #
    # Would create an entry point called django-session-csrf-cookie that calls django-session-csrf-cookie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-session-csrf

  run:
    - python
    - django
    - django-session-csrf

test:
  # Python imports
  imports:
    - session_csrf_cookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/trustcentric/django-session-csrf-cookie
  license: BSD License
  summary: 'Django middleware that works with session-csrf and sends a CSRF token cookie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
