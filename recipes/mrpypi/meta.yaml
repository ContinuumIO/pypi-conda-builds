package:
  name: mrpypi
  version: "0.1.6"

source:
  fn: mrpypi-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/m/mrpypi/mrpypi-0.1.6.tar.gz
  md5: 9fc2662fad3a7248040f0cf4ea14e445
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mrpypi = mrpypi:main
    #
    # Would create an entry point called mrpypi that calls mrpypi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chisel >=0.8.4
    - pymongo >=2.6

  run:
    - python
    - chisel >=0.8.4
    - pymongo >=2.6

test:
  # Python imports
  imports:
    - mrpypi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/craigahobbs/mrpypi
  license: MIT License
  summary: 'Simple, reliable local pypy cache.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
