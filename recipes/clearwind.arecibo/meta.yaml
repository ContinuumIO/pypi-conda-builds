package:
  name: clearwind.arecibo
  version: "0.7"

source:
  fn: clearwind.arecibo-0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/clearwind.arecibo/clearwind.arecibo-0.7.tar.gz
  md5: d204bf07aeffb531b47a20d07528b7c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clearwind.arecibo = clearwind.arecibo:main
    #
    # Would create an entry point called clearwind.arecibo that calls clearwind.arecibo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - clearwind
    - clearwind.arecibo
    - clearwind.arecibo.browser
    - clearwind.arecibo.lib
    - clearwind.arecibo.lib.simplejson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://clearwind.ca
  license: BSD
  summary: 'Connector from Plone to Arecibo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
