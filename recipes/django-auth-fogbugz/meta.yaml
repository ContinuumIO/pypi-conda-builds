package:
  name: django-auth-fogbugz
  version: "1.2.0"

source:
  fn: django-auth-fogbugz-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-fogbugz/django-auth-fogbugz-1.2.0.tar.gz
  md5: e43c13a4f2d1a2a120cbf49e2a58b69b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-fogbugz = django-auth-fogbugz:main
    #
    # Would create an entry point called django-auth-fogbugz that calls django-auth-fogbugz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fogbugz

  run:
    - python
    - fogbugz

test:
  # Python imports
  imports:
    - django_auth_fogbugz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dougn/django-auth-fogbugz/
  license: BSD License
  summary: 'Django FogBugz authentication backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
