package:
  name: deliverance.rewrite
  version: "0.6.3"

source:
  fn: Deliverance.Rewrite-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/D/Deliverance.Rewrite/Deliverance.Rewrite-0.6.3.tar.gz
  md5: b7c790dad5e9e5660a1d9ed0cb329b7e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deliverance.rewrite = deliverance.rewrite:main
    #
    # Would create an entry point called deliverance.rewrite that calls deliverance.rewrite.main()

    - deliverance-proxy=deliverance.proxycommand:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - webob ==1.1.1
    - wsgiproxy
    - tempita
    - pygments
    - weberror
    - devauth
    - cssselect
    - paste
    - pastescript
    - wsgifilter
    - chardet
    - simplejson

  run:
    - python
    - setuptools
    - lxml
    - webob ==1.1.1
    - wsgiproxy
    - tempita
    - pygments
    - weberror
    - devauth
    - cssselect
    - paste
    - pastescript
    - wsgifilter
    - chardet
    - simplejson

test:
  # Python imports
  imports:
    - deliverance
    - deliverance.editor
    - deliverance.util

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - deliverance-proxy --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/deliverance/Deliverance/
  license: MIT License
  summary: 'Deliverance transforms HTML to theme pages, fork with separate header rewrite'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
