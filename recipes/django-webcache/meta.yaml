package:
  name: django-webcache
  version: "0.1.8"

source:
  fn: django-webcache-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webcache/django-webcache-0.1.8.tar.gz
  md5: fa8af13ba862ae076060a20ff10e2784
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webcache = django-webcache:main
    #
    # Would create an entry point called django-webcache that calls django-webcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - webcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dcthomson/django-webcache
  license: MIT License
  summary: 'A django web caching app that supports throttling, expiration and compression.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
