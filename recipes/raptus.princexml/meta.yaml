package:
  name: raptus.princexml
  version: "1.0a3"

source:
  fn: raptus.princexml-1.0a3.zip
  url: https://pypi.python.org/packages/source/r/raptus.princexml/raptus.princexml-1.0a3.zip
  md5: 857251847941d3729ea76040fde51f57
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.princexml = raptus.princexml:main
    #
    # Would create an entry point called raptus.princexml that calls raptus.princexml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

test:
  # Python imports
  imports:
    - raptus
    - raptus.princexml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Raptus/raptus.princexml
  license: GPL
  summary: 'Provides a simple bridge to create PDFs from HTML views using PrinceXML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
