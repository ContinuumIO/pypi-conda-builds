package:
  name: django-threaded-multihost
  version: "1.4-1"

source:
  fn: django-threaded-multihost-1.4-1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-threaded-multihost/django-threaded-multihost-1.4-1.tar.gz
  md5: a455c87f6f4eca7bfc172f85e4aabd29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-threaded-multihost = django-threaded-multihost:main
    #
    # Would create an entry point called django-threaded-multihost that calls django-threaded-multihost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - threaded_multihost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/bkroeze/django-threaded-multihost/
  license: BSD License
  summary: 'Django Threaded Multihost'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
