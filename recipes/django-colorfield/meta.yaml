package:
  name: django-colorfield
  version: "0.1.4"

source:
  fn: django-colorfield-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-colorfield/django-colorfield-0.1.4.tar.gz
  md5: d6f2b7a4e7cee5cd8267257ad1654080
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-colorfield = django-colorfield:main
    #
    # Would create an entry point called django-colorfield that calls django-colorfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - colorfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'A small app providing a colorpicker field for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
