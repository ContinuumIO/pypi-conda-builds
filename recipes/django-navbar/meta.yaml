package:
  name: django-navbar
  version: "0.3.0"

source:
  fn: django-navbar-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-navbar/django-navbar-0.3.0.tar.gz
  md5: f4725cd19b5bcde61fbab0f568664524
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-navbar = django-navbar:main
    #
    # Would create an entry point called django-navbar that calls django-navbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - examples
    - examples.dbgp
    - examples.dbgp._logging
    - navbar
    - navbar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-navbar/
  license: MIT License
  summary: 'Reusable django application managing navigation menues with permissions, auto selection and crumbs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
