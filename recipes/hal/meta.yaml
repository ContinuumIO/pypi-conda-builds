package:
  name: hal
  version: "0.0.3"

source:
  fn: HAL-0.0.3.zip
  url: https://pypi.python.org/packages/source/H/HAL/HAL-0.0.3.zip
  md5: 802aa1824d88673141c4e856d40b9575
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hal = hal:main
    #
    # Would create an entry point called hal that calls hal.main()

    - hal = HAL.mainentry:main
    - whal = HAL.tkgui:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stemming

  run:
    - python
    - stemming

test:
  # Python imports
  imports:
    - HAL
    - HAL.engine
    - hal

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - hal --help
    - whal --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.halbot.co.cc/
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Powerful AI Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
