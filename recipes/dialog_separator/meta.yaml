package:
  name: dialog_separator
  version: "1.3.2"

source:
  fn: dialog_separator-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dialog_separator/dialog_separator-1.3.2.tar.gz
  md5: ad65d0ec575bb857a7f33841483b69e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dialog_separator = dialog_separator:main
    #
    # Would create an entry point called dialog_separator that calls dialog_separator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dialog_separator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A simple dialog separator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
