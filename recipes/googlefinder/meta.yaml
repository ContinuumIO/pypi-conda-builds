package:
  name: googlefinder
  version: "0.1"

source:
  fn: googlefinder-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/googlefinder/googlefinder-0.1.tar.gz
  md5: 86d7f537359a3bd7ca78677728a00c3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googlefinder = googlefinder:main
    #
    # Would create an entry point called googlefinder that calls googlefinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.4.3
    - beautifulsoup4 >=4.3.2

  run:
    - python
    - requests >=2.4.3
    - beautifulsoup4 >=4.3.2

test:
  # Python imports
  imports:
    - googlefinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peterldowns/mypackage
  license: UNKNOWN
  summary: 'Google finder returns google search results against a list of search parameters. Results consist of a 2 part tuple of url and result description'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
