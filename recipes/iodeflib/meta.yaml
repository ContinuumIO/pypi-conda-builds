package:
  name: iodeflib
  version: "0.07"

source:
  fn: iodeflib-0.07.zip
  url: https://pypi.python.org/packages/source/i/iodeflib/iodeflib-0.07.zip
  md5: 3e86064122729d49bd812e58d895715c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iodeflib = iodeflib:main
    #
    # Would create an entry point called iodeflib that calls iodeflib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iodeflib
    - iodeflib.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.decalage.info/python/iodeflib
  license: BSD License
  summary: 'a python library to create, parse and edit cyber incident reports using the IODEF XML format (RFC 5070)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
