package:
  name: mercurialrecipe
  version: "0.1.3"

source:
  fn: MercurialRecipe-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/M/MercurialRecipe/MercurialRecipe-0.1.3.tar.gz
  md5: e1145d26f8c51551167b47305e1b2e9d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mercurialrecipe = mercurialrecipe:main
    #
    # Would create an entry point called mercurialrecipe that calls mercurialrecipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial

  run:
    - python
    - setuptools
    - mercurial

test:
  # Python imports
  imports:
    - mercurialrecipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tawm/mercurial-recipe/
  license: UNKNOWN
  summary: 'zc.buildout recipe for cloning and/or pulling a Mercurial repository'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
