package:
  name: django-multiple-email-backends
  version: "0.1a4"

source:
  fn: django-multiple-email-backends-0.1a4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multiple-email-backends/django-multiple-email-backends-0.1a4.tar.gz
  md5: 93528d2cd2ca820c1b4b14e676da92b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multiple-email-backends = django-multiple-email-backends:main
    #
    # Would create an entry point called django-multiple-email-backends that calls django-multiple-email-backends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_multiple_email_backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/KristianOellegaard/django-multiple-email-backends
  license: BSD
  summary: 'Want to send through more than one interface? E.g. SMTP and database? This is your solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
