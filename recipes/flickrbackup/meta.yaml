package:
  name: flickrbackup
  version: "0.8.2"

source:
  fn: flickrbackup-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flickrbackup/flickrbackup-0.8.2.tar.gz
  md5: 2d43a884c295ae4ca520eae17eaef0c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flickrbackup = flickrbackup:main
    #
    # Would create an entry point called flickrbackup that calls flickrbackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flickrapi
    - threadpool

  run:
    - python
    - flickrapi
    - threadpool

test:
  # Python imports
  imports:
    - flickrbackup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/optilude/flickrbackup
  license: BSD
  summary: 'Flickr backup utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
