package:
  name: django-batch-requests
  version: "0.1.0"

source:
  fn: django-batch-requests-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-batch-requests/django-batch-requests-0.1.0.tar.gz
  md5: 3cbade860177239b6be4a795b98605ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-batch-requests = django-batch-requests:main
    #
    # Would create an entry point called django-batch-requests that calls django-batch-requests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - batch_requests
    - batch_requests/migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tanwanirahul/django-batch-requests
  license: MIT License
  summary: 'Create batch APIs for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
