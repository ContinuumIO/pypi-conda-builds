package:
  name: flask-kale
  version: "0.1"

source:
  fn: Flask-kale-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-kale/Flask-kale-0.1.tar.gz
  md5: 03a190f9c3d3a7ca8754677af9e2e92c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-kale = flask-kale:main
    #
    # Would create an entry point called flask-kale that calls flask-kale.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - kale

  run:
    - python
    - flask
    - kale

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/uniphil/flask-kale
  license: dwtfyw with optional coffee stipulation (one large dark black)
  summary: 'Use kale models on a flask project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
