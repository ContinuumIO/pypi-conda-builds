package:
  name: django-debug-panel
  version: "0.8.1"

source:
  fn: django-debug-panel-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-panel/django-debug-panel-0.8.1.tar.gz
  md5: ff0a69af35708ee02b1e8b45306393d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-panel = django-debug-panel:main
    #
    # Would create an entry point called django-debug-panel that calls django-debug-panel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-debug-toolbar >=1.0

  run:
    - python
    - django-debug-toolbar >=1.0

test:
  # Python imports
  imports:
    - debug_panel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/recamshak/django-debug-panel
  license: BSD
  summary: 'django-debug-toolbar in WebKit DevTools. Works fine with background Ajax requests and non-HTML responses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
