package:
  name: isbn_hyphenate
  version: "1.0.3"

source:
  fn: isbn_hyphenate-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/isbn_hyphenate/isbn_hyphenate-1.0.3.tar.gz
  md5: 916f14e1e85c3c805e64c66c1595806e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isbn_hyphenate = isbn_hyphenate:main
    #
    # Would create an entry point called isbn_hyphenate that calls isbn_hyphenate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - isbn_hyphenate
    - isbn_hyphenate.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TorKlingberg/isbn_hyphenate
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'a Python library to add hyphens in the right place to an ISBN (International Standard Book Number)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
