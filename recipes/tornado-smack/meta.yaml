package:
  name: tornado-smack
  version: "1.0.4"

source:
  fn: tornado-smack-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-smack/tornado-smack-1.0.4.tar.gz
  md5: 70bcc49615a9cf12064254326c556e45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-smack = tornado-smack:main
    #
    # Would create an entry point called tornado-smack that calls tornado-smack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - werkzeug

  run:
    - python
    - tornado
    - werkzeug

test:
  # Python imports
  imports:
    - tornado_smack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Hipo/tornado_smack
  license: UNKNOWN
  summary: 'Syntactic sugar for tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
