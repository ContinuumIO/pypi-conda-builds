package:
  name: genty
  version: "1.2.1"

source:
  fn: genty-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/genty/genty-1.2.1.tar.gz
  md5: 2372dc3b42efe3dd334330a611e695b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genty = genty:main
    #
    # Would create an entry point called genty that calls genty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - genty
    - genty.private

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/box/genty
  license: Apache Software License
  summary: u'Allows you to run a test with multiple data sets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
