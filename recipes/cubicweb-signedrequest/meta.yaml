package:
  name: cubicweb-signedrequest
  version: "0.1.3"

source:
  fn: cubicweb-signedrequest-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-signedrequest/cubicweb-signedrequest-0.1.3.tar.gz
  md5: 6bc05d05b94ec1192d7d288984848f94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-signedrequest = cubicweb-signedrequest:main
    #
    # Would create an entry point called cubicweb-signedrequest that calls cubicweb-signedrequest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.0

  run:
    - python
    - cubicweb >=3.17.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-signedrequest
  license: LGPL
  summary: 'rest api for cubicweb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
