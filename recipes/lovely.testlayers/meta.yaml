package:
  name: lovely.testlayers
  version: "0.6.2"

source:
  fn: lovely.testlayers-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.testlayers/lovely.testlayers-0.6.2.tar.gz
  md5: 7bb1cf4c23c1e1f0b0437d9f2cd5e773
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.testlayers = lovely.testlayers:main
    #
    # Would create an entry point called lovely.testlayers that calls lovely.testlayers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - lovely
    - lovely.testlayers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lovelysystems/lovely.testlayers
  license: Apache License 2.0
  summary: 'mysql, postgres nginx, memcached cassandra test layers for use with zope.testrunner'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
