package:
  name: django-request-log
  version: "0.0.10"

source:
  fn: django-request-log-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-request-log/django-request-log-0.0.10.tar.gz
  md5: a45670a62d97999c5adfdaf983088ce4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-request-log = django-request-log:main
    #
    # Would create an entry point called django-request-log that calls django-request-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - log

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orcasgit/django-request-log
  license: Apache Software License
  summary: 'Pluggable Django applications to record user login and logout events and URL views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
