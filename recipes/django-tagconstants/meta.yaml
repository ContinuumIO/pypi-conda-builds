package:
  name: django-tagconstants
  version: "0.1"

source:
  fn: django-tagconstants-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-tagconstants/django-tagconstants-0.1.zip
  md5: 7693f2692e9993849586288ad9eed7a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tagconstants = django-tagconstants:main
    #
    # Would create an entry point called django-tagconstants that calls django-tagconstants.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - tagconstants
    - tagconstants.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nalourie/django-tagconstants
  license: MIT License
  summary: 'Define constants in your settings, and make them available in your templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
