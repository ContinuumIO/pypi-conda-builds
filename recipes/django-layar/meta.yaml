package:
  name: django-layar
  version: "0.2.0"

source:
  fn: django-layar-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-layar/django-layar-0.2.0.tar.gz
  md5: 190cd8c39efd353c4ed76d3373bca88c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-layar = django-layar:main
    #
    # Would create an entry point called django-layar that calls django-layar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - layar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/django-layar/
  license: BSD License
  summary: 'helper for publishing data to Layar augmented reality browser from Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
