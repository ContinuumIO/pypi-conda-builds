package:
  name: chessmind.grok
  version: "0.3"

source:
  fn: chessmind.grok-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/chessmind.grok/chessmind.grok-0.3.tar.gz
  md5: dc668c7981d87baeae513bfc5fac626e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chessmind.grok = chessmind.grok:main
    #
    # Would create an entry point called chessmind.grok that calls chessmind.grok.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - chessmind.core

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - chessmind.core

test:
  # Python imports
  imports:
    - chessmind
    - chessmind.grok

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Grok UI for ChessMind'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
