package:
  name: django-hs-sessions
  version: "0.1.1"

source:
  fn: django-hs-sessions-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hs-sessions/django-hs-sessions-0.1.1.tar.gz
  md5: 1bdbe336dac426daa3428052c32768f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hs-sessions = django-hs-sessions:main
    #
    # Would create an entry point called django-hs-sessions that calls django-hs-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-handler-socket

  run:
    - python
    - python-handler-socket

test:
  # Python imports
  imports:
    - hs_sessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dchaplinsky/django-hs-sessions
  license: MIT License
  summary: 'HandlerSocket session backend for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
