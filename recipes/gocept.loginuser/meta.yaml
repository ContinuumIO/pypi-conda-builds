package:
  name: gocept.loginuser
  version: "1.1"

source:
  fn: gocept.loginuser-1.1.zip
  url: https://pypi.python.org/packages/source/g/gocept.loginuser/gocept.loginuser-1.1.zip
  md5: 12cf5b47531dd78393ff5b2f6276e378
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.loginuser = gocept.loginuser:main
    #
    # Would create an entry point called gocept.loginuser that calls gocept.loginuser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - bcrypt

  run:
    - python
    - setuptools
    - sqlalchemy
    - bcrypt

test:
  # Python imports
  imports:
    - gocept
    - gocept.loginuser
    - gocept.loginuser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/gocept.loginuser/
  license: Zope Public License
  summary: 'Sqlalchemy user object and password management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
