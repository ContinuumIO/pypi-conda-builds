package:
  name: sphinxtools
  version: "0.2"

source:
  fn: sphinxtools-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxtools/sphinxtools-0.2.tar.gz
  md5: 709e03ddadd3736b12b4b98d6bd2a41b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxtools = sphinxtools:main
    #
    # Would create an entry point called sphinxtools that calls sphinxtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paver >=1.0

  run:
    - python
    - paver >=1.0

test:
  # Python imports
  imports:
    - sphinxtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpvanhal/sphinxtools
  license: BSD License
  summary: 'Paver tasks for Sphinx Search server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
