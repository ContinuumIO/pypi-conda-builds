package:
  name: betahaus.viewcomponent
  version: "0.4.1"

source:
  fn: betahaus.viewcomponent-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/b/betahaus.viewcomponent/betahaus.viewcomponent-0.4.1.tar.gz
  md5: 04ef8aa4d92091966a42fdf4f5f3cd68
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betahaus.viewcomponent = betahaus.viewcomponent:main
    #
    # Would create an entry point called betahaus.viewcomponent that calls betahaus.viewcomponent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid >=1.2
    - venusian
    - zope.interface

  run:
    - python
    - setuptools
    - pyramid >=1.2
    - venusian
    - zope.interface

test:
  # Python imports
  imports:
    - betahaus
    - betahaus.viewcomponent
    - betahaus.viewcomponent.fixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robinharms/betahaus.viewcomponent
  license: GNU General Public License v3 (GPLv3)
  summary: 'Plugin structure for menus, JSON responses or similar.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
