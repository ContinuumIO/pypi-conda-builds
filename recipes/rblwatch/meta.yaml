package:
  name: rblwatch
  version: "0.2.2"

source:
  fn: rblwatch-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/r/rblwatch/rblwatch-0.2.2.tar.gz
  md5: 7d5001fba5e7b90934b160d74eb744fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rblwatch = rblwatch:main
    #
    # Would create an entry point called rblwatch that calls rblwatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - ipy ==0.81
    - dnspython ==1.11.1

  run:
    - python
    - ipy ==0.81
    - dnspython ==1.11.1

test:
  # Python imports
  imports:
    - rblwatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drscream/rblwatch
  license: UNKNOWN
  summary: 'RBL lookups with Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
