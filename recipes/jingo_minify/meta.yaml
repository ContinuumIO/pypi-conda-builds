package:
  name: jingo_minify
  version: "0.5"

source:
  fn: jingo_minify-0.5.tar.gz
  url: https://pypi.python.org/packages/source/j/jingo_minify/jingo_minify-0.5.tar.gz
  md5: fa90596c2949c7dedef5f2fefeb44ee1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jingo_minify = jingo_minify:main
    #
    # Would create an entry point called jingo_minify that calls jingo_minify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - examples
    - jingo_minify
    - jingo_minify.management
    - jingo_minify.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsocol/jingo-minify
  license: BSD License
  summary: 'A Django app that will concat and minify JS and CSS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
