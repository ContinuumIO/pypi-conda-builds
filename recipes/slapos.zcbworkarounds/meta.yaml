package:
  name: slapos.zcbworkarounds
  version: "0.1"

source:
  fn: slapos.zcbworkarounds-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/slapos.zcbworkarounds/slapos.zcbworkarounds-0.1.tar.gz
  md5: adc2a76531f991a93e47f97779f2f2fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slapos.zcbworkarounds = slapos.zcbworkarounds:main
    #
    # Would create an entry point called slapos.zcbworkarounds that calls slapos.zcbworkarounds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - slapos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3
  summary: 'A zc.buildout extensions to workaround zc.buildout issues which are impacting ERP5 Appliance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
