package:
  name: sunnytrail
  version: "0.1.1"

source:
  fn: sunnytrail-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sunnytrail/sunnytrail-0.1.1.tar.gz
  md5: 414b51487b4807cd27267b4a9963dc05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sunnytrail = sunnytrail:main
    #
    # Would create an entry point called sunnytrail that calls sunnytrail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sunnytrail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.thesunnytrail.com/
  license: Apache Software License
  summary: 'Python Wrapper for the Sunnytrail API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
