package:
  name: collective.opensearch
  version: "1.5"

source:
  fn: collective.opensearch-1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.opensearch/collective.opensearch-1.5.tar.gz
  md5: a3d8077e56c579e797fae9978244603c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.opensearch = collective.opensearch:main
    #
    # Would create an entry point called collective.opensearch that calls collective.opensearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - feedparser
    - chardet
    - htmllaundry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - feedparser
    - chardet
    - htmllaundry

test:
  # Python imports
  imports:
    - collective
    - collective.opensearch
    - collective.opensearch.browser
    - collective.opensearch.content
    - collective.opensearch.interfaces
    - collective.opensearch.portlets
    - collective.opensearch.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plos
  license: GNU General Public License (GPL)
  summary: 'Collective Opensearch Collective.opensearch adds\n        the ability to produce search results in the simple OpenSearch\n        format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
