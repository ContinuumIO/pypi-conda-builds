package:
  name: bitcoinwebrpc
  version: "0.101"

source:
  fn: bitcoinWebRPC-0.101.tar.gz
  url: https://pypi.python.org/packages/source/b/bitcoinWebRPC/bitcoinWebRPC-0.101.tar.gz
  md5: e256eaa6356aa9997fae33408c7a0c87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitcoinwebrpc = bitcoinwebrpc:main
    #
    # Would create an entry point called bitcoinwebrpc that calls bitcoinwebrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bitcoinwebrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benjyz/bitcoinWebRPC
  license: MIT
  summary: 'Bitcoin WebRPC for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
