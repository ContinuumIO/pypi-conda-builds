package:
  name: datamash
  version: "0.1.0"

source:
  fn: datamash-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/datamash/datamash-0.1.0.tar.gz
  md5: 760278256346823340ccf534ca5e078c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datamash = datamash:main
    #
    # Would create an entry point called datamash that calls datamash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil ==1.5
    - requests ==1.0
    - simplejson

  run:
    - python
    - python-dateutil ==1.5
    - requests ==1.0
    - simplejson

test:
  # Python imports
  imports:
    - datamash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datamash
  license: UNKNOWN
  summary: 'A client for the DatamashIO API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
