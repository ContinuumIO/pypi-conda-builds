package:
  name: collective.zestreleaser.aftercheckoutaction
  version: "0.1.0"

source:
  fn: collective.zestreleaser.aftercheckoutaction-0.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.zestreleaser.aftercheckoutaction/collective.zestreleaser.aftercheckoutaction-0.1.0.zip
  md5: 9be210c759022d522c03c80d93983db5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.zestreleaser.aftercheckoutaction = collective.zestreleaser.aftercheckoutaction:main
    #
    # Would create an entry point called collective.zestreleaser.aftercheckoutaction that calls collective.zestreleaser.aftercheckoutaction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zest.releaser >=3.12

  run:
    - python
    - setuptools
    - setuptools
    - zest.releaser >=3.12

test:
  # Python imports
  imports:
    - collective
    - collective.zestreleaser
    - collective.zestreleaser.aftercheckoutaction

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/do3cc/collective.zestreleaser.aftercheckoutaction
  license: BSD License
  summary: 'Execute a user defined shell action after clean checkout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
