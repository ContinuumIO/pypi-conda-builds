package:
  name: djangorestframework-extensions
  version: "0.0.3"

source:
  fn: djangorestframework-extensions-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-extensions/djangorestframework-extensions-0.0.3.tar.gz
  md5: 004c28e46813508ba6b88c4ea3eeffb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-extensions = djangorestframework-extensions:main
    #
    # Would create an entry point called djangorestframework-extensions that calls djangorestframework-extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangorestframework_extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com.org/bolster/djangorestframework_extensions
  license: BSD License
  summary: 'Extensions to django-rest-framework, which is a lightweight REST framework for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
