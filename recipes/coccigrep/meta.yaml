package:
  name: coccigrep
  version: "1.13"

source:
  fn: coccigrep-1.13.tar.gz
  url: https://pypi.python.org/packages/source/c/coccigrep/coccigrep-1.13.tar.gz
  md5: aebea51670b22a383ebd2f010769b7d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coccigrep = coccigrep:main
    #
    # Would create an entry point called coccigrep that calls coccigrep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coccigrep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://home.regit.org/software/coccigrep/
  license: GNU General Public License (GPL)
  summary: 'Semantic grep for C based on coccinelle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
