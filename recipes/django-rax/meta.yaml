package:
  name: django-rax
  version: "0.6"

source:
  fn: django-rax-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rax/django-rax-0.6.tar.gz
  md5: 75d91ccdab899b7e2eb99cbc91c1e091
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rax = django-rax:main
    #
    # Would create an entry point called django-rax that calls django-rax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pyrax ==1.3.6

  run:
    - python
    - django >=1.4
    - pyrax ==1.3.6

test:
  # Python imports
  imports:
    - rax
    - rax.management
    - rax.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dustinfarris/django-rax
  license: BSD
  summary: 'PyRax implementation for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
