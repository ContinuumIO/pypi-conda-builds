package:
  name: django-cities
  version: "0.4.1"

source:
  fn: django-cities-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cities/django-cities-0.4.1.tar.gz
  md5: 024bf0e9e9d699032cb14835f55c6cfd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cities = django-cities:main
    #
    # Would create an entry point called django-cities that calls django-cities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cities
    - cities.management
    - cities.management.commands
    - cities.plugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coderholic/django-cities
  license: MIT License
  summary: 'Place models and worldwide place data for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
