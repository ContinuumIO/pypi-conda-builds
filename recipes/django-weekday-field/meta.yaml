package:
  name: django-weekday-field
  version: "1.1.0"

source:
  fn: django-weekday-field-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-weekday-field/django-weekday-field-1.1.0.tar.gz
  md5: 46ba22e9e02422b9cf0e4291ea9214d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-weekday-field = django-weekday-field:main
    #
    # Would create an entry point called django-weekday-field that calls django-weekday-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - weekday_field
    - weekday_field.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-weekday-field/
  license: BSD License
  summary: 'Weekday field for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
