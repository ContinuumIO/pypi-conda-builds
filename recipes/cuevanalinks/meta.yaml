package:
  name: cuevanalinks
  version: "0.5"

source:
  fn: CuevanaLinks-0.5.tar.gz
  url: https://pypi.python.org/packages/source/C/CuevanaLinks/CuevanaLinks-0.5.tar.gz
  md5: cae8bab24603a403dd159c55bf27bd36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cuevanalinks = cuevanalinks:main
    #
    # Would create an entry point called cuevanalinks that calls cuevanalinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cuevanalib
    - plac >=0.8
    - progressbar

  run:
    - python
    - cuevanalib
    - plac >=0.8
    - progressbar

test:
  # Python imports
  imports:
    - cuevanalinks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tin_nqn/cuevanalinks
  license: GNU General Public License (GPL)
  summary: 'A program to retrieve movies and series (or its links)                    from cuevana.tv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
