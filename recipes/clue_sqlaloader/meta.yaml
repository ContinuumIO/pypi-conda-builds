package:
  name: clue_sqlaloader
  version: "0.1.1"

source:
  fn: clue_sqlaloader-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clue_sqlaloader/clue_sqlaloader-0.1.1.tar.gz
  md5: 26f6ec440de13c4b2613c9354da3a077
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clue_sqlaloader = clue_sqlaloader:main
    #
    # Would create an entry point called clue_sqlaloader that calls clue_sqlaloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.6.1
    - pyyaml
    - zope.dottedname

  run:
    - python
    - sqlalchemy >=0.6.1
    - pyyaml
    - zope.dottedname

test:
  # Python imports
  imports:
    - clue_sqlaloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clueproject/clue_sqlaloader
  license: BSD License
  summary: 'Data loader for SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
