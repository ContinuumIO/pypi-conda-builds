package:
  name: django-emailform
  version: "0.1.6"

source:
  fn: django-emailform-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailform/django-emailform-0.1.6.tar.gz
  md5: e515806ef12627e3c9d3f748506e88b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailform = django-emailform:main
    #
    # Would create an entry point called django-emailform that calls django-emailform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - emailform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/glennmohre/django-emailform
  license: MIT
  summary: 'django-email forms is an application for generating multi input forms, customized using the django admin, with emailing of entries to a supplied list of email addresses.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
