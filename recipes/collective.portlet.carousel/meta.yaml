package:
  name: collective.portlet.carousel
  version: "1.0"

source:
  fn: collective.portlet.carousel-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.carousel/collective.portlet.carousel-1.0.tar.gz
  md5: 1acafdc3d2020cb4ecf8575d7472c72b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.carousel = collective.portlet.carousel:main
    #
    # Would create an entry point called collective.portlet.carousel that calls collective.portlet.carousel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - collective.panels
    - plone.app.portlets

  run:
    - python
    - setuptools
    - distribute
    - collective.panels
    - plone.app.portlets

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.carousel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Carousel for collective.panels'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
