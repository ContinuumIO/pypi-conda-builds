package:
  name: collective.js.dygraphs
  version: "1.0.1"

source:
  fn: collective.js.dygraphs-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.dygraphs/collective.js.dygraphs-1.0.1.tar.gz
  md5: b40da46aae9008e678ac940a46cdaf7e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.dygraphs = collective.js.dygraphs:main
    #
    # Would create an entry point called collective.js.dygraphs that calls collective.js.dygraphs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.dygraphs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/collective.js.dygraphs
  license: GNU General Public License (GPL)
  summary: 'Dygraphs JS Library Packaged for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
