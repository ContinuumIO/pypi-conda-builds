package:
  name: django-versionfield3
  version: "0.1.1"

source:
  fn: django-versionfield3-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versionfield3/django-versionfield3-0.1.1.tar.gz
  md5: da69c8363f892e6c1cdc528cb0a8f4f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versionfield3 = django-versionfield3:main
    #
    # Would create an entry point called django-versionfield3 that calls django-versionfield3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - future >=0.14.3

  run:
    - python
    - future >=0.14.3

test:
  # Python imports
  imports:
    - versionfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Crystalnix/django-versionfield3
  license: BSD License
  summary: 'A DB Independent Custom Django Field for storing Version numbers for fast indexing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
