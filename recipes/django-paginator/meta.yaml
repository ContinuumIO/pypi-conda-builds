package:
  name: django-paginator
  version: "0.2a3"

source:
  fn: django-paginator-0.2a3.zip
  url: https://pypi.python.org/packages/source/d/django-paginator/django-paginator-0.2a3.zip
  md5: efbb0ee5707a36a8599df578de0dab9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paginator = django-paginator:main
    #
    # Would create an entry point called django-paginator that calls django-paginator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - paginator
    - paginator.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-paginator
  license: BSD License
  summary: "A template tag that works with Django's Paginator class"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
