package:
  name: tornwrap
  version: "0.2.15"

source:
  fn: tornwrap-0.2.15.tar.gz
  url: https://pypi.python.org/packages/source/t/tornwrap/tornwrap-0.2.15.tar.gz
  md5: a8dbd4dc690e3e7946efb776000cbc64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornwrap = tornwrap:main
    #
    # Would create an entry point called tornwrap that calls tornwrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=4.0.0
    - valideer >=0.3.1
    - timestring >=1.6.1

  run:
    - python
    - tornado >=4.0.0
    - valideer >=0.3.1
    - timestring >=1.6.1

test:
  # Python imports
  imports:
    - tornwrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stevepeak/tornwrap
  license: Apache Software License
  summary: 'tornado decorators and wrappers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
