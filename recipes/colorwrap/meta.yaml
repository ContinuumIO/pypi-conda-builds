package:
  name: colorwrap
  version: "0.1.5"

source:
  fn: colorwrap-0.1.5.zip
  url: https://pypi.python.org/packages/source/c/colorwrap/colorwrap-0.1.5.zip
  md5: 09ce177876b4aad96a82fa70da835a00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorwrap = colorwrap:main
    #
    # Would create an entry point called colorwrap that calls colorwrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorwrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/konvexum/colorwrap
  license: MIT License
  summary: 'Wraps a shell command to make unix/ansi escape codes work on Windows.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
