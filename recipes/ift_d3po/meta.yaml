package:
  name: ift_d3po
  version: "1.0.2"

source:
  fn: ift_d3po-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/ift_d3po/ift_d3po-1.0.2.tar.gz
  md5: eda048094eb71bf46e93a535067c4954
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ift_d3po = ift_d3po:main
    #
    # Would create an entry point called ift_d3po that calls ift_d3po.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - d3po
    - ift_d3po

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.mpa-garching.mpg.de/ift/d3po/
  license: GPLv3
  summary: 'Deonoiseing, Deconvolving, and Decomposing Photon Observations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
