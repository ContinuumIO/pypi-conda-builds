package:
  name: django-meta-mixin
  version: "0.1.1"

source:
  fn: django-meta-mixin-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-meta-mixin/django-meta-mixin-0.1.1.tar.gz
  md5: ac6b406ff043fbe765c372003ab42a69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-meta-mixin = django-meta-mixin:main
    #
    # Would create an entry point called django-meta-mixin that calls django-meta-mixin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-meta
    - django_sekizai

  run:
    - python
    - django
    - django-meta
    - django_sekizai

test:
  # Python imports
  imports:
    - meta_mixin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/django-meta-mixin
  license: BSD License
  summary: 'Social meta tags mixin for django-meta'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
