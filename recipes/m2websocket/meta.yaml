package:
  name: m2websocket
  version: "0.2.2"

source:
  fn: m2websocket-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/m/m2websocket/m2websocket-0.2.2.tar.gz
  md5: e1d9682d05434905dfa6911305acc3b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m2websocket = m2websocket:main
    #
    # Would create an entry point called m2websocket that calls m2websocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - m2websocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Jaymon/m2websocket
  license: MIT License
  summary: 'Makes websockets with Mongrel2 and Python super easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
