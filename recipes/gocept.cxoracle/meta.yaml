package:
  name: gocept.cxoracle
  version: "0.1.1"

source:
  fn: gocept.cxoracle-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.cxoracle/gocept.cxoracle-0.1.1.tar.gz
  md5: dbf8ddb5e4cf88d6cbd014e5f87c96d0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.cxoracle = gocept.cxoracle:main
    #
    # Would create an entry point called gocept.cxoracle that calls gocept.cxoracle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.cxoracle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.cxoracle
  license: Zope Public License
  summary: 'zc.buildout recipe for installing cx_Oracle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
