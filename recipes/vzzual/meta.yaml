package:
  name: vzzual
  version: "0.0.3"

source:
  fn: vzzual-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/v/vzzual/vzzual-0.0.3.tar.gz
  md5: 7fcac601157fcdcf4dd43e8e07f0770c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vzzual = vzzual:main
    #
    # Would create an entry point called vzzual that calls vzzual.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1
    - python-dateutil
    - simplejson
    - nose2 >=0.4.7
    - httpretty >=0.8.0

  run:
    - python
    - requests >=2.2.1
    - python-dateutil
    - simplejson
    - nose2 >=0.4.7
    - httpretty >=0.8.0

test:
  # Python imports
  imports:
    - vzzual

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vzzual/vzzual-python
  license: MIT License
  summary: 'Official python wrapper for the Vzzual API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
