package:
  name: compress_pyscripts
  version: "0.0.1"

source:
  fn: compress_pyscripts-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/compress_pyscripts/compress_pyscripts-0.0.1.tar.gz
  md5: 2c6f5dca6bc638ceebe8ecfb27053069
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compress_pyscripts = compress_pyscripts:main
    #
    # Would create an entry point called compress_pyscripts that calls compress_pyscripts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - compress_pyscripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rpq/compress_pyscripts
  license: MIT License
  summary: 'Compression related scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
