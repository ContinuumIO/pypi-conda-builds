package:
  name: tenthousandfeet
  version: "0.1.1"

source:
  fn: tenthousandfeet-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tenthousandfeet/tenthousandfeet-0.1.1.tar.gz
  md5: 27e23f1c07ba7cb43556fe65e0e14a43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tenthousandfeet = tenthousandfeet:main
    #
    # Would create an entry point called tenthousandfeet that calls tenthousandfeet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - python-dateutil

  run:
    - python
    - requests
    - python-dateutil

test:
  # Python imports
  imports:
    - tenthousandfeet
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cooper-software/python-10000ft
  license: UNKNOWN
  summary: 'A client for the 10000ft.com service (http://10kft.github.io/api-documentation/).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
