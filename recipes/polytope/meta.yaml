package:
  name: polytope
  version: "0.1.0"

source:
  fn: polytope-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/polytope/polytope-0.1.0.tar.gz
  md5: 1eca56d647340acab6314431c568319f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - polytope = polytope:main
    #
    # Would create an entry point called polytope that calls polytope.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6
    - scipy
    - networkx >=1.6
    - cvxopt

  run:
    - python
    - numpy >=1.6
    - scipy
    - networkx >=1.6
    - cvxopt

test:
  # Python imports
  imports:
    - polytope

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tulip-control.org
  license: BSD
  summary: 'Polytope Toolbox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
