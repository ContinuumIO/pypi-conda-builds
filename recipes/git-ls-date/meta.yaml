package:
  name: git-ls-date
  version: "0.1.1"

source:
  fn: git-ls-date-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/git-ls-date/git-ls-date-0.1.1.tar.gz
  md5: 3f26f4fb01d67bf29e0bd64ab249f0c9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-ls-date = git-ls-date:main
    #
    # Would create an entry point called git-ls-date that calls git-ls-date.main()

    - git-ls-date = git_ls_date:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  # imports:

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - git-ls-date --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ton1517/git-ls-date
  license: MIT License
  summary: 'git-ls-date is git sub command shows first and last commit date.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
