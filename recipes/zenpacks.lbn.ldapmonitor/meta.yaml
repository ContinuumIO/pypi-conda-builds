package:
  name: zenpacks.lbn.ldapmonitor
  version: "4.0.1"

source:
  fn: ZenPacks.lbn.LDAPMonitor-4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/Z/ZenPacks.lbn.LDAPMonitor/ZenPacks.lbn.LDAPMonitor-4.0.1.tar.gz
  md5: cca0f4481fd8a13560594fa0fdc7b5cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zenpacks.lbn.ldapmonitor = zenpacks.lbn.ldapmonitor:main
    #
    # Would create an entry point called zenpacks.lbn.ldapmonitor that calls zenpacks.lbn.ldapmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zenpacks.lbn.base >=4.0.3
    - python-ldap

  run:
    - python
    - setuptools
    - zenpacks.lbn.base >=4.0.3
    - python-ldap

test:
  # Python imports
  imports:
    - ZenPacks
    - ZenPacks.lbn
    - ZenPacks.lbn.LDAPMonitor
    - ZenPacks.lbn.LDAPMonitor.datasources
    - ZenPacks.lbn.LDAPMonitor.services
    - ZenPacks.lbn.LDAPMonitor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://linux.last-bastion.net/LBN/up2date/monitor
  license: Zope Public License
  summary: 'ZenPack to monitor LDAP Server stats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
