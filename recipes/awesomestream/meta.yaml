package:
  name: awesomestream
  version: "0.1"

source:
  fn: awesomestream-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/awesomestream/awesomestream-0.1.tar.gz
  md5: faba9ebcb66b08373258337d8ce8dd8f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - awesomestream = awesomestream:main
    #
    # Would create an entry point called awesomestream that calls awesomestream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - awesomestream
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ericflo/awesomestream
  license: BSD
  summary: 'AwesomeStream makes awesome streams'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
