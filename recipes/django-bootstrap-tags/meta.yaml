package:
  name: django-bootstrap-tags
  version: "1.2.0"

source:
  fn: django-bootstrap-tags-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-tags/django-bootstrap-tags-1.2.0.tar.gz
  md5: d0638d7dfc51ac3943a8bda35c450501
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-tags = django-bootstrap-tags:main
    #
    # Would create an entry point called django-bootstrap-tags that calls django-bootstrap-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - django_bootstrap_tags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://thegoods.aj7may.com/django-bootstrap-tags
  license: Creative Commons Attribution-ShareAlike 4.0 International License
  summary: 'A django form widget and tag model to add typeahead tagging to any form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
