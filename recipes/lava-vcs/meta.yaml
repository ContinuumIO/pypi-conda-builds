package:
  name: lava-vcs
  version: "0.1"

source:
  fn: lava-vcs-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-vcs/lava-vcs-0.1.tar.gz
  md5: fb28500c47ce84d246b21b7c3d17b223
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-vcs = lava-vcs:main
    #
    # Would create an entry point called lava-vcs that calls lava-vcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lava-server >=0.7
    - lava-celery >=0.1.1
    - versiontools >=1.8

  run:
    - python
    - setuptools
    - lava-server >=0.7
    - lava-celery >=0.1.1
    - versiontools >=1.8

test:
  # Python imports
  imports:
    - lava
    - lava.vcs
    - lava.vcs.adapters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
