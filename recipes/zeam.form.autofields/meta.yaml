package:
  name: zeam.form.autofields
  version: "1.0.1"

source:
  fn: zeam.form.autofields-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zeam.form.autofields/zeam.form.autofields-1.0.1.tar.gz
  md5: b7244d33466eb6f2ba782c3c34667b10
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeam.form.autofields = zeam.form.autofields:main
    #
    # Would create an entry point called zeam.form.autofields that calls zeam.form.autofields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - martian
    - zope.browser
    - zope.interface
    - zope.component
    - grokcore.component >=2.5
    - grokcore.view
    - zeam.form.base >=1.2
    - zeam.form.ztk >=1.2

  run:
    - python
    - setuptools
    - martian
    - zope.browser
    - zope.interface
    - zope.component
    - grokcore.component >=2.5
    - grokcore.view
    - zeam.form.base >=1.2
    - zeam.form.ztk >=1.2

test:
  # Python imports
  imports:
    - zeam
    - zeam.form
    - zeam.form.autofields
    - zeam.form.autofields.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zeam.form.autofields
  license: BSD
  summary: 'Automatically collect fields for a Zeam Form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
