package:
  name: ftw.publisher.example
  version: "1.0"

source:
  fn: ftw.publisher.example-1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/ftw.publisher.example/ftw.publisher.example-1.0.tar.gz
  md5: 4624b2f6fae24755940aad07b2764e39
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.publisher.example = ftw.publisher.example:main
    #
    # Would create an entry point called ftw.publisher.example that calls ftw.publisher.example.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ftw
    - ftw.publisher
    - ftw.publisher.example
    - ftw.publisher.example.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.publisher.example
  license: GPL2
  summary: "Example workflow integration for ftw.publisher's staging and publishing system"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
