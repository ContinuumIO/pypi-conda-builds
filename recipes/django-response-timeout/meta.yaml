package:
  name: django-response-timeout
  version: "0.1.0"

source:
  fn: django-response-timeout-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-response-timeout/django-response-timeout-0.1.0.tar.gz
  md5: 8113ca70244e341e73adc47629f1b5ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-response-timeout = django-response-timeout:main
    #
    # Would create an entry point called django-response-timeout that calls django-response-timeout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.7

  run:
    - python
    - django >=1.4,<1.7

test:
  # Python imports
  imports:
    - response_timeout
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/saulshanabrook/django-response-timeout
  license: MIT License
  summary: 'Django global response timeout middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
