package:
  name: bitvector
  version: "3.4.2"

source:
  fn: BitVector-3.4.2.tar.gz
  url: https://engineering.purdue.edu/kak/dist/BitVector-3.4.2.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitvector = bitvector:main
    #
    # Would create an entry point called bitvector that calls bitvector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - BitVector
    - bitvector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://engineering.purdue.edu/kak/dist/BitVector-3.4.2.html
  license: Python Software Foundation License
  summary: 'A pure-Python memory-efficient packed representation for bit arrays'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
