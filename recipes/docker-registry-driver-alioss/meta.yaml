package:
  name: docker-registry-driver-alioss
  version: "0.1.1"

source:
  fn: docker-registry-driver-alioss-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-registry-driver-alioss/docker-registry-driver-alioss-0.1.1.tar.gz
  md5: b0e0de5c769aff67b48e3c717a3e6348
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-registry-driver-alioss = docker-registry-driver-alioss:main
    #
    # Would create an entry point called docker-registry-driver-alioss that calls docker-registry-driver-alioss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docker-registry-core >=2,<3
    - oss ==0.1.3

  run:
    - python
    - docker-registry-core >=2,<3
    - oss ==0.1.3

test:
  # Python imports
  imports:
    - docker_registry
    - docker_registry.drivers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chris-jin/docker-registry-driver-alioss
  license: Apache Software License
  summary: 'Docker registry ali oss driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
