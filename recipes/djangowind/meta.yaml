package:
  name: djangowind
  version: "0.13.3"

source:
  fn: djangowind-0.13.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangowind/djangowind-0.13.3.tar.gz
  md5: 931f1124ca0668805acb99f39c2f129f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangowind = djangowind:main
    #
    # Would create an entry point called djangowind that calls djangowind.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangowind

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ccnmtl/djangowind
  license: BSD
  summary: 'Django Authentication backend for CU WIND'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
