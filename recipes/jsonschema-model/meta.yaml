package:
  name: jsonschema-model
  version: "1.3.0"

source:
  fn: jsonschema-model-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonschema-model/jsonschema-model-1.3.0.tar.gz
  md5: 2d47743ea57c2f61d530b7cf5fbb1fdc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonschema-model = jsonschema-model:main
    #
    # Would create an entry point called jsonschema-model that calls jsonschema-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Json schema Model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
