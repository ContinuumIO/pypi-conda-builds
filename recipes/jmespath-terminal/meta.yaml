package:
  name: jmespath-terminal
  version: "0.1.1"

source:
  fn: jmespath-terminal-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmespath-terminal/jmespath-terminal-0.1.1.tar.gz
  md5: 72c22209d9dc9b3475c768095dcce8ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmespath-terminal = jmespath-terminal:main
    #
    # Would create an entry point called jmespath-terminal that calls jmespath-terminal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmespath >=0.4.1,<=1.0.0
    - pygments >=1.6,<=2.0
    - urwid ==1.2.2

  run:
    - python
    - jmespath >=0.4.1,<=1.0.0
    - pygments >=1.6,<=2.0
    - urwid ==1.2.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmespath/jmespath.terminal
  license: Apache Software License
  summary: 'JMESPath Terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
