package:
  name: appengine
  version: "0.2"

source:
  fn: appengine-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/appengine/appengine-0.2.tar.gz
  md5: 21eb570473a6527c99c247e8102ef4a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appengine = appengine:main
    #
    # Would create an entry point called appengine that calls appengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - appengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidwtbuxton/appengine.py
  license: MIT License
  summary: 'Google App Engine re-packaged for PyPI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
