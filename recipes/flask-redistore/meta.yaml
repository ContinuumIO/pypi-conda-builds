package:
  name: flask-redistore
  version: "1.0.1"

source:
  fn: Flask-Redistore-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Redistore/Flask-Redistore-1.0.1.tar.gz
  md5: 5414569ca40c741bf5cf21f97fdf8922
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-redistore = flask-redistore:main
    #
    # Would create an entry point called flask-redistore that calls flask-redistore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - redis

  run:
    - python
    - flask
    - redis

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Adds Redis support to your Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
