package:
  name: mrhooker
  version: "0.1"

source:
  fn: mrhooker-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mrhooker/mrhooker-0.1.tar.gz
  md5: b451f8a27396b241249bc58efdc2b66f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mrhooker = mrhooker:main
    #
    # Would create an entry point called mrhooker that calls mrhooker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mrhooker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/axw/mrhooker
  license: MIT License
  summary: 'MrHooker is a command-line tool for hooking function calls in Cython,\nusing LD_PRELOAD. Hooks are written in `Cython <http://cython.org>`_ (a dialect\nof Python), and compiled on the fly. MrHooker will take care of setting up the\nenvironment, initializing Python in the child process, and cleaning up\nafterwards.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
