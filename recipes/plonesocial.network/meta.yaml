package:
  name: plonesocial.network
  version: "0.6.1"

source:
  fn: plonesocial.network-0.6.1.zip
  url: https://pypi.python.org/packages/source/p/plonesocial.network/plonesocial.network-0.6.1.zip
  md5: 7eaa88b350f38e62f65ac8f85bad19ff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonesocial.network = plonesocial.network:main
    #
    # Would create an entry point called plonesocial.network that calls plonesocial.network.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.layout
    - products.cmfcore
    - products.cmfplone >=4.2
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - plone.app.layout
    - products.cmfcore
    - products.cmfplone >=4.2
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - plonesocial
    - plonesocial.network
    - plonesocial.network.browser
    - plonesocial.network.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cosent/plonesocial.network
  license: GNU General Public License v2 (GPLv2)
  summary: 'Personal profile + follow/unfollow functionality for the Plonesocial suite'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
