package:
  name: flask-stats
  version: "1.0.1"

source:
  fn: Flask-Stats-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Stats/Flask-Stats-1.0.1.tar.gz
  md5: f7308438c37bbb6cdfe8f038505664a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-stats = flask-stats:main
    #
    # Would create an entry point called flask-stats that calls flask-stats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - statsd

  run:
    - python
    - flask
    - statsd

test:
  # Python imports
  imports:
    - flask_stats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/solarnz/Flask-Stats
  license: BSD License
  summary: 'A flask plugin to keep stats about your application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
