package:
  name: holytransaction
  version: "0.1"

source:
  fn: HolyTransaction-0.1.tar.gz
  url: https://pypi.python.org/packages/source/H/HolyTransaction/HolyTransaction-0.1.tar.gz
  md5: 0baeb74254b7635db3794be31361c75e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - holytransaction = holytransaction:main
    #
    # Would create an entry point called holytransaction that calls holytransaction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.8
    - requests >=1.1.0

  run:
    - python
    - httplib2 >=0.8
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - holytransaction

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/noveltylab/ht-client-python
  license: MIT License
  summary: 'Python client for HolyTransaction API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
