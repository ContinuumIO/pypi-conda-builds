package:
  name: gaq_hub
  version: "0.0.13"

source:
  fn: gaq_hub-0.0.13.tar.gz
  url: https://pypi.python.org/packages/source/g/gaq_hub/gaq_hub-0.0.13.tar.gz
  md5: b068ed09df94ebd66c5bced06e2a979e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaq_hub = gaq_hub:main
    #
    # Would create an entry point called gaq_hub that calls gaq_hub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - metadata_utils >=0.0.1

  run:
    - python
    - metadata_utils >=0.0.1

test:
  # Python imports
  imports:
    - gaq_hub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jvanasco/gaq_hub
  license: MIT License
  summary: 'Lightweight Google Analytics support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
