package:
  name: collective.portlet.collectionmultiview
  version: "2.2"

source:
  fn: collective.portlet.collectionmultiview-2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.collectionmultiview/collective.portlet.collectionmultiview-2.2.zip
  md5: b6c3a16c443c31777f64bcea70d57760
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.collectionmultiview = collective.portlet.collectionmultiview:main
    #
    # Would create an entry point called collective.portlet.collectionmultiview that calls collective.portlet.collectionmultiview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.portlet.collection >=2.0

  run:
    - python
    - setuptools
    - setuptools
    - plone.portlet.collection >=2.0

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.collectionmultiview
    - collective.portlet.collectionmultiview.renderers
    - collective.portlet.collectionmultiview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.inigo-tech.com/
  license: GPLv2+
  summary: 'A collection portlet product which supports multiple views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
