package:
  name: python-kubernetes
  version: "0.2"

source:
  fn: python-kubernetes-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-kubernetes/python-kubernetes-0.2.tar.gz
  md5: e1114103db3407db886e3b6beadd397e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-kubernetes = python-kubernetes:main
    #
    # Would create an entry point called python-kubernetes that calls python-kubernetes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - kubernetes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cosmos-io/python-kubernetes
  license: Apache Software License
  summary: 'A Python wrapper around the Kubernetes API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
