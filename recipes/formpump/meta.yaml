package:
  name: formpump
  version: "0.1.8"

source:
  fn: formpump-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/f/formpump/formpump-0.1.8.tar.gz
  md5: 75f112ddb20e1b1f5f0ebc28a46f748e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formpump = formpump:main
    #
    # Would create an entry point called formpump that calls formpump.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formpump
    - formpump.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mankyd/formpump
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'FormPump is a tool that integrates with popular templating engines, allowing you to cleanly fill in HTML forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
