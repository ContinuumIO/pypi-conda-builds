package:
  name: django-extra-views-ng
  version: "0.3.3"

source:
  fn: django-extra-views-ng-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extra-views-ng/django-extra-views-ng-0.3.3.tar.gz
  md5: 0df6f5599ee5770a25c4c22335124599
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extra-views-ng = django-extra-views-ng:main
    #
    # Would create an entry point called django-extra-views-ng that calls django-extra-views-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - extra_views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hovel/django-extra-views
  license: MIT
  summary: 'Extra class-based views for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
