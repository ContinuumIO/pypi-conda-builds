package:
  name: django-userprofile
  version: "0.0.6"

source:
  fn: django-userprofile-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-userprofile/django-userprofile-0.0.6.tar.gz
  md5: 73379d9bc8b5b2988e3022630fda80dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-userprofile = django-userprofile:main
    #
    # Would create an entry point called django-userprofile that calls django-userprofile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-photologue
    - django-registration

  run:
    - python
    - django-photologue
    - django-registration

test:
  # Python imports
  imports:
    - userprofile
    - userprofile.backends
    - userprofile.backends.simple
    - userprofile.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-userprofile
  license: BSD License
  summary: 'Django user profile app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
