package:
  name: collective.orderedmultiselectwidget
  version: "0.1"

source:
  fn: collective.orderedmultiselectwidget-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.orderedmultiselectwidget/collective.orderedmultiselectwidget-0.1.zip
  md5: 74d5bd77d7a21d5f2349d1fe2894da04
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.orderedmultiselectwidget = collective.orderedmultiselectwidget:main
    #
    # Would create an entry point called collective.orderedmultiselectwidget that calls collective.orderedmultiselectwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.orderedmultiselectwidget
    - collective.orderedmultiselectwidget.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Provides SecureOrderedMultiSelectWidget which fixes an acquisition bug in OrderedMultiSelectWidget in zope.app.form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
