package:
  name: sumologic
  version: "0.1.2"

source:
  fn: sumologic-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sumologic/sumologic-0.1.2.tar.gz
  md5: f4d50eee1349eb0dc9e786e3bbda3243
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sumologic = sumologic:main
    #
    # Would create an entry point called sumologic that calls sumologic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - sumologic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sijis/sumologic-python
  license: UNKNOWN
  summary: 'Python library for working with the Sumo Logic api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
