package:
  name: django-events-watcher
  version: "0.3.3"

source:
  fn: django-events-watcher-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-events-watcher/django-events-watcher-0.3.3.tar.gz
  md5: b81f170f05b4c388d1425a40ae64829e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-events-watcher = django-events-watcher:main
    #
    # Would create an entry point called django-events-watcher that calls django-events-watcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - events_watcher
    - events_watcher.backends
    - events_watcher.backends.database
    - events_watcher.backends.database.south_migrations
    - events_watcher.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-events-watcher
  license: MIT License
  summary: 'Events watcher is an event packaging library for Django to track changes made in your models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
