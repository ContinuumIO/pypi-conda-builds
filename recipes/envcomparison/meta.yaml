package:
  name: envcomparison
  version: "0.1.5"

source:
  fn: EnvComparison-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/E/EnvComparison/EnvComparison-0.1.5.tar.gz
  md5: 8786b7dab9a9eda209f6a1c025c35f5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - envcomparison = envcomparison:main
    #
    # Would create an entry point called envcomparison that calls envcomparison.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko
    - tornado

  run:
    - python
    - paramiko
    - tornado

test:
  # Python imports
  imports:
    - EnvComparison
    - envcomparison

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamar/EnvComparison
  license: UNKNOWN
  summary: 'Linux Environment Comparison Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
