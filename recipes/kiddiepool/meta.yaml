package:
  name: kiddiepool
  version: "1.0.2"

source:
  fn: kiddiepool-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kiddiepool/kiddiepool-1.0.2.tar.gz
  md5: 633e18067df36d17aec511b9f762aa5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kiddiepool = kiddiepool:main
    #
    # Would create an entry point called kiddiepool that calls kiddiepool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kazoo ==1.3.1

  run:
    - python
    - kazoo ==1.3.1

test:
  # Python imports
  imports:
    - kiddiepool
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/urbanairship/kiddiepool
  license: Apache Software License
  summary: 'An extensible driver framework with pooling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
