package:
  name: django-simple-sso
  version: "0.9.3"

source:
  fn: django-simple-sso-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-sso/django-simple-sso-0.9.3.tar.gz
  md5: cb2293e0a941c26cd66651d6ea62b9d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-sso = django-simple-sso:main
    #
    # Would create an entry point called django-simple-sso that calls django-simple-sso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - itsdangerous

  run:
    - python
    - itsdangerous

test:
  # Python imports
  imports:
    - simple_sso
    - simple_sso.sso_client
    - simple_sso.sso_server
    - simple_sso.sso_server.migrations
    - simple_sso.test_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aldryn/django-simple-sso
  license: BSD License
  summary: 'Simple SSO for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
