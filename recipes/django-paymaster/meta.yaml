package:
  name: django-paymaster
  version: "0.1.1"

source:
  fn: django-paymaster-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paymaster/django-paymaster-0.1.1.tar.gz
  md5: 27e78e0738d3b15b1be36b9e9c88378a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paymaster = django-paymaster:main
    #
    # Would create an entry point called django-paymaster that calls django-paymaster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - paymaster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/scailer/django-paymaster/
  license: MIT License
  summary: 'Application for integration PayMaster payment system in Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
