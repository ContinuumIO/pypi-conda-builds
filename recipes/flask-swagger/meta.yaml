package:
  name: flask-swagger
  version: "0.2.5"

source:
  fn: flask-swagger-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-swagger/flask-swagger-0.2.5.tar.gz
  md5: 60ed0e95930f3d5636c41c0726f19c29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-swagger = flask-swagger:main
    #
    # Would create an entry point called flask-swagger that calls flask-swagger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10
    - pyyaml >=3.0

  run:
    - python
    - flask >=0.10
    - pyyaml >=3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gangverk/flask-swagger
  license: MIT
  summary: 'Extract swagger specs from your flask project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
