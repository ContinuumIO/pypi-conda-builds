package:
  name: delightedpython
  version: "0.0.1.6"

source:
  fn: DelightedPython-0.0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/D/DelightedPython/DelightedPython-0.0.1.6.tar.gz
  md5: cacd6e508f44cf84e55ccf2057e550ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - delightedpython = delightedpython:main
    #
    # Would create an entry point called delightedpython that calls delightedpython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - delighted
    - delighted.test
    - delightedpython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/DelightedPython/
  license: LICENSE.txt
  summary: 'Python wrapper for the Delighted API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
