package:
  name: jterator
  version: "0.0.1"

source:
  fn: Jterator-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/J/Jterator/Jterator-0.0.1.tar.gz
  md5: 5dcf67a822cb623b4afef02778294a13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jterator = jterator:main
    #
    # Would create an entry point called jterator that calls jterator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - h5py >=2.3.0

  run:
    - python
    - h5py >=2.3.0

test:
  # Python imports
  imports:
    - jterator
    - jterator.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ewiger/Jterator
  license: MIT License
  summary: 'A minimalistic pipeline engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
