package:
  name: django-groove
  version: "0.4.0"

source:
  fn: django-groove-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-groove/django-groove-0.4.0.tar.gz
  md5: a75c7ca233689e60b11748a3ddd91d90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-groove = django-groove:main
    #
    # Would create an entry point called django-groove that calls django-groove.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - groove
    - groove.auth
    - groove.context_processors
    - groove.email
    - groove.http
    - groove.models
    - groove.storage
    - groove.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/funkbit/django-groove
  license: BSD License
  summary: 'Various utilities for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
