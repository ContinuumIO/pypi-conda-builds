package:
  name: fitnoise
  version: "2.2"

source:
  fn: fitnoise-2.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fitnoise/fitnoise-2.2.tar.gz
  md5: 086463265d907488fa047a75834af457
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fitnoise = fitnoise:main
    #
    # Would create an entry point called fitnoise that calls fitnoise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fitnoise

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pfh/fitnoise
  license: UNKNOWN
  summary: 'Statistical analysis of RNA-Seq, PAT-Seq, and microarray data using linear models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
