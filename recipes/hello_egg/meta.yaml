package:
  name: hello_egg
  version: "0.1"

source:
  fn: hello_egg-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hello_egg/hello_egg-0.1.tar.gz
  md5: 8bc4747a0e27a05b104d5c787089f9f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hello_egg = hello_egg:main
    #
    # Would create an entry point called hello_egg that calls hello_egg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hello_egg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jizhouli
  license: http://opensource.org/licenses/mit-license.php
  summary: 'say hello to the egg'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
