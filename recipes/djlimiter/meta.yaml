package:
  name: djlimiter
  version: "0.1.1"

source:
  fn: djlimiter-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djlimiter/djlimiter-0.1.1.tar.gz
  md5: 004191982f3acc5d29299668b3e292f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djlimiter = djlimiter:main
    #
    # Would create an entry point called djlimiter that calls djlimiter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - limits
    - django
    - six >=1.4.1

  run:
    - python
    - limits
    - django
    - six >=1.4.1

test:
  # Python imports
  imports:
    - djlimiter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://djlimiter.readthedocs.org
  license: MIT License
  summary: 'Rate limiting for django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
