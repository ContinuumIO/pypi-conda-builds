package:
  name: brubeck-oauth
  version: "0.1.11"

source:
  fn: brubeck-oauth-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/b/brubeck-oauth/brubeck-oauth-0.1.11.tar.gz
  md5: 121258ab00b88422fad29fbe21cccebf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brubeck-oauth = brubeck-oauth:main
    #
    # Would create an entry point called brubeck-oauth that calls brubeck-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brubeckoauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sethmurphy/BrubeckOAuth
  license: UNKNOWN
  summary: 'Brubeck OAuth module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
