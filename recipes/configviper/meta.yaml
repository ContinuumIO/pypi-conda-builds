package:
  name: configviper
  version: "0.5"

source:
  fn: ConfigViper-0.5.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfigViper/ConfigViper-0.5.tar.gz
  md5: 5aba6c1ad4f2d86098645860afc1c64b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configviper = configviper:main
    #
    # Would create an entry point called configviper that calls configviper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - configviper
    - configviper.lockfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/danielgoncalves/configviper
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Handles configuration saved as JSON files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
