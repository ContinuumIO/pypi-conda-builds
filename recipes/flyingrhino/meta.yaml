package:
  name: flyingrhino
  version: "0.1.2"

source:
  fn: flyingrhino-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flyingrhino/flyingrhino-0.1.2.tar.gz
  md5: 30707639cb01b8468472953c825fd286
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flyingrhino = flyingrhino:main
    #
    # Would create an entry point called flyingrhino that calls flyingrhino.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas
    - numpy
    - pymavlink

  run:
    - python
    - pandas
    - numpy
    - pymavlink

test:
  # Python imports
  imports:
    - flyingrhino

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/foobarbecue/flyingrhino
  license: Apache 2.0, see LICENCE.txt
  summary: 'Tools for working with unmanned aerial vechicle data using python-pandas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
