package:
  name: django-ldapdb
  version: "0.3.2"

source:
  fn: django-ldapdb-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ldapdb/django-ldapdb-0.3.2.tar.gz
  md5: 649d2661cb33bc4851b7fb803ccc31be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ldapdb = django-ldapdb:main
    #
    # Would create an entry point called django-ldapdb that calls django-ldapdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - python-ldap >=2.0

  run:
    - python
    - django >=1.2
    - python-ldap >=2.0

test:
  # Python imports
  imports:
    - ldapdb
    - ldapdb.backends
    - ldapdb.backends.ldap
    - ldapdb.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jlaine/django-ldapdb
  license: UNKNOWN
  summary: u'An LDAP database backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
