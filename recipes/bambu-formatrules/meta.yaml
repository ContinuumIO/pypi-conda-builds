package:
  name: bambu-formatrules
  version: "2.0"

source:
  fn: bambu-formatrules-2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-formatrules/bambu-formatrules-2.0.tar.gz
  md5: a9211146e7c11900c04718acbf41d26a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-formatrules = bambu-formatrules:main
    #
    # Would create an entry point called bambu-formatrules that calls bambu-formatrules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - markdown

  run:
    - python
    - django >=1.4
    - markdown

test:
  # Python imports
  imports:
    - bambu_formatrules
    - bambu_formatrules.formatters
    - bambu_formatrules.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-formatrules
  license: UNKNOWN
  summary: 'Syntactical sugar ontop of Markdown for adding extra formatting, expressed in a human-readable way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
