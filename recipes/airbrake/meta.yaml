package:
  name: airbrake
  version: "1.1.4"

source:
  fn: airbrake-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/a/airbrake/airbrake-1.1.4.tar.gz
  md5: 4607c987c8abbb1915f620b4774f0910
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - airbrake = airbrake:main
    #
    # Would create an entry point called airbrake that calls airbrake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - airbrake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/airbrake/airbrake-python
  license: Apache License (2.0)
  summary: 'Airbrake API implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
