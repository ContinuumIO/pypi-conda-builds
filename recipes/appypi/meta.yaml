package:
  name: appypi
  version: "0.2.0"

source:
  fn: appypi-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/appypi/appypi-0.2.0.tar.gz
  md5: 0dcfc894b8ac78e67e19d0b995f65cac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appypi = appypi:main
    #
    # Would create an entry point called appypi that calls appypi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blessings
    - fabric
    - sqlalchemy

  run:
    - python
    - blessings
    - fabric
    - sqlalchemy

test:
  # Python imports
  imports:
    - appypi
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stephanepechard.github.com/appypi
  license: GNU General Public License v3 (GPLv3)
  summary: 'appypi: sandboxing apps from Pypi packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
