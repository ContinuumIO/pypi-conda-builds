package:
  name: slapos.tool.nosqltester
  version: "0.0.4.dev-r45972"

source:
  fn: slapos.tool.nosqltester-0.0.4.dev-r45972.tar.gz
  url: https://pypi.python.org/packages/source/s/slapos.tool.nosqltester/slapos.tool.nosqltester-0.0.4.dev-r45972.tar.gz
  md5: e1eccb71eb0448de82517c22a7a9dcdc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slapos.tool.nosqltester = slapos.tool.nosqltester:main
    #
    # Would create an entry point called slapos.tool.nosqltester that calls slapos.tool.nosqltester.main()

    - nosqltester=slapos.tool.nosqltester:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - flask

  run:
    - python
    - setuptools
    - setuptools
    - flask

test:
  # Python imports
  imports:
    - slapos
    - slapos.tool
    - slapos.tool.nosqltester

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - nosqltester --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'NoSQL systems tester'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
