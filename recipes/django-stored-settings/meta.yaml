package:
  name: django-stored-settings
  version: "0.2.0"

source:
  fn: django-stored-settings-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stored-settings/django-stored-settings-0.2.0.tar.gz
  md5: 0a0dafa3ace3d3313aadbe9cfe77aaad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stored-settings = django-stored-settings:main
    #
    # Would create an entry point called django-stored-settings that calls django-stored-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stored_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zvadim/django-stored-settings
  license: MIT License
  summary: 'Module allowing dynamically to create and further on to edit via django-admin settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
