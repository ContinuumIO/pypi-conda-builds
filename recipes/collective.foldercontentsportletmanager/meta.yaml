package:
  name: collective.foldercontentsportletmanager
  version: "0.2.1"

source:
  fn: collective.FolderContentsPortletManager-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.FolderContentsPortletManager/collective.FolderContentsPortletManager-0.2.1.tar.gz
  md5: b152b44edb75e711599fca32a77dd106
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.foldercontentsportletmanager = collective.foldercontentsportletmanager:main
    #
    # Would create an entry point called collective.foldercontentsportletmanager that calls collective.foldercontentsportletmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.FolderContentsPortletManager
    - collective.FolderContentsPortletManager.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Portlet manager for the folder_contents (contents tab). Usefull to add a mass uploader portlet for example'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
