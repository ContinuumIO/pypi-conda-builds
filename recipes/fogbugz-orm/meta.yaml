package:
  name: fogbugz-orm
  version: "0.2.9"

source:
  fn: fogbugz-orm-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/f/fogbugz-orm/fogbugz-orm-0.2.9.tar.gz
  md5: 87cd4652721ea23190dca900b0c545ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fogbugz-orm = fogbugz-orm:main
    #
    # Would create an entry point called fogbugz-orm that calls fogbugz-orm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fogbugz
    - jsontree

  run:
    - python
    - fogbugz
    - jsontree

test:
  # Python imports
  imports:
    - fborm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dougn/fogbugz-orm/
  license: BSD License
  summary: 'FogBugz API Object Relational Mapper (ORM)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
