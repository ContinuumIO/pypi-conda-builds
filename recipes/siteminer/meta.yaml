package:
  name: siteminer
  version: "0.1.6"

source:
  fn: SiteMiner-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/S/SiteMiner/SiteMiner-0.1.6.tar.gz
  md5: 67d00ba4eb42de49c1a45180c6bf5070
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - siteminer = siteminer:main
    #
    # Would create an entry point called siteminer that calls siteminer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - SiteMiner
    - siteminer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yangsibai/SiteMiner
  license: MIT License
  summary: 'crawl a site and find out all link http status code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
