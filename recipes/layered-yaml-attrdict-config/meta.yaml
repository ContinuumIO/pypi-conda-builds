package:
  name: layered-yaml-attrdict-config
  version: "15.5.2"

source:
  fn: layered-yaml-attrdict-config-15.5.2.tar.gz
  url: https://pypi.python.org/packages/source/l/layered-yaml-attrdict-config/layered-yaml-attrdict-config-15.5.2.tar.gz
  md5: 14c50ba1e3c37c4432af63dabcacf255
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - layered-yaml-attrdict-config = layered-yaml-attrdict-config:main
    #
    # Would create an entry point called layered-yaml-attrdict-config that calls layered-yaml-attrdict-config.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - lya

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mk-fg/layered-yaml-attrdict-config
  license: WTFPL
  summary: 'YAML-based configuration module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
