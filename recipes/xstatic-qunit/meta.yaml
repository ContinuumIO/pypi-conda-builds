package:
  name: xstatic-qunit
  version: "1.14.0.2"

source:
  fn: XStatic-QUnit-1.14.0.2.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-QUnit/XStatic-QUnit-1.14.0.2.tar.gz
  md5: 21d48252d3301bd7ae530bfeffa3d108
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-qunit = xstatic-qunit:main
    #
    # Would create an entry point called xstatic-qunit that calls xstatic-qunit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.qunit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://qunitjs.com
  license: (same as QUnit)
  summary: 'QUnit 1.14.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
