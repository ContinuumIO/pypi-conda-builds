package:
  name: django-ssl-redirect
  version: "1.0"

source:
  fn: django-ssl-redirect-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-ssl-redirect/django-ssl-redirect-1.0.zip
  md5: b91f6e9fce6f43b4da82b7a43c345ef7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ssl-redirect = django-ssl-redirect:main
    #
    # Would create an entry point called django-ssl-redirect that calls django-ssl-redirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - ssl_redirect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-ssl-redirect
  license: BSD License
  summary: 'A middleware that secures URLS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
