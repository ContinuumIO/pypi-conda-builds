package:
  name: python-translate
  version: "1.0.9"

source:
  fn: python-translate-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/p/python-translate/python-translate-1.0.9.tar.gz
  md5: bf795827c90568e5ec51fa184b0a573d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-translate = python-translate:main
    #
    # Would create an entry point called python-translate that calls python-translate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.11
    - codegen ==1.0
    - polib ==1.0.6

  run:
    - python
    - pyyaml ==3.11
    - codegen ==1.0
    - polib ==1.0.6

test:
  # Python imports
  imports:
    - python_translate
    - python_translate.extractors
    - python_translate.tests
    - python_translate.tests.dumpers
    - python_translate.tests.loaders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamziel/python_translate
  license: MIT
  summary: 'Non-gettext translations for python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
