package:
  name: brubeck-mysql
  version: "0.2.8"

source:
  fn: brubeck-mysql-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/b/brubeck-mysql/brubeck-mysql-0.2.8.tar.gz
  md5: f89baceb5ce56cae4b1580b821d87c04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brubeck-mysql = brubeck-mysql:main
    #
    # Would create an entry point called brubeck-mysql that calls brubeck-mysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brubeckmysql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sethmurphy/BrubeckMySQL
  license: UNKNOWN
  summary: 'Brubeck module for MySQL Querysets using Schematics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
