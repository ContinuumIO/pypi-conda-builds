package:
  name: apache_conf_parser
  version: "1.0.1"

source:
  fn: apache_conf_parser-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/apache_conf_parser/apache_conf_parser-1.0.1.tar.gz
  md5: 827aedf2343e7a88c4310f55a74327e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apache_conf_parser = apache_conf_parser:main
    #
    # Would create an entry point called apache_conf_parser that calls apache_conf_parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - apache_conf_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ericsnowcurrently/apache_conf_parser/overview
  license: BSD License
  summary: 'Parse and manipulate apache conf files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
