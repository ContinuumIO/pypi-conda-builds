package:
  name: cubicweb-relationwidget
  version: "0.2.0"

source:
  fn: cubicweb-relationwidget-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-relationwidget/cubicweb-relationwidget-0.2.0.tar.gz
  md5: 8cdf0f44d1facb61ae593a40af8929bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-relationwidget = cubicweb-relationwidget:main
    #
    # Would create an entry point called cubicweb-relationwidget that calls cubicweb-relationwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18.0
    - cwtags >=0.1.1

  run:
    - python
    - cubicweb >=3.18.0
    - cwtags >=0.1.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-relationwidget
  license: LGPL
  summary: 'Provide a generic and ergonomic relation widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
