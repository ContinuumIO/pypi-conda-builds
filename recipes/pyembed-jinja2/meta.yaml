package:
  name: pyembed-jinja2
  version: "1.1.0"

source:
  fn: pyembed-jinja2-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyembed-jinja2/pyembed-jinja2-1.1.0.tar.gz
  md5: 3f1f710c190a9265f3cb2f4e06f914c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyembed-jinja2 = pyembed-jinja2:main
    #
    # Would create an entry point called pyembed-jinja2 that calls pyembed-jinja2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyembed
    - jinja2

  run:
    - python
    - pyembed
    - jinja2

test:
  # Python imports
  imports:
    - pyembed.jinja2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyembed.github.io
  license: MIT License
  summary: 'PyEmbed plugin for rendering embeddings using Jinja2 templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
