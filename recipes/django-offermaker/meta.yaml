package:
  name: django-offermaker
  version: "0.9.8"

source:
  fn: django-offermaker-0.9.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-offermaker/django-offermaker-0.9.8.tar.gz
  md5: 575ed7618bfa45a87996cc5822059b19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-offermaker = django-offermaker:main
    #
    # Would create an entry point called django-offermaker that calls django-offermaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonfield >=1.0.0

  run:
    - python
    - jsonfield >=1.0.0

test:
  # Python imports
  imports:
    - offermaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://offermaker.kjw.pt
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Django application that allows creation, preview and usage of custom multivariant restrictions applied on the forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
