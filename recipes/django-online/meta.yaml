package:
  name: django-online
  version: "0.2.1"

source:
  fn: django-online-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-online/django-online-0.2.1.tar.gz
  md5: 27ef9220279e430b898b6c33cf07938a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-online = django-online:main
    #
    # Would create an entry point called django-online that calls django-online.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - online
    - online.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-online
  license: MIT License
  summary: 'Standalone Django application to know if a django user is online or not.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
