package:
  name: threadfix_api
  version: "1.1.0"

source:
  fn: threadfix_api-1.1.0.zip
  url: https://pypi.python.org/packages/source/t/threadfix_api/threadfix_api-1.1.0.zip
  md5: 8c0695164b4b37232815aa15c557755e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - threadfix_api = threadfix_api:main
    #
    # Would create an entry point called threadfix_api that calls threadfix_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - threadfix_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aparsons/threadfix_api
  license: MIT License
  summary: 'An API wrapper to facilitate interactions with ThreadFix.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
