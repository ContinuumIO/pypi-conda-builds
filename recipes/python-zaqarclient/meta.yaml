package:
  name: python-zaqarclient
  version: "0.1.0"

source:
  fn: python-zaqarclient-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-zaqarclient/python-zaqarclient-0.1.0.tar.gz
  md5: c9beeeaf6c4b087507764c7e5694b048
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-zaqarclient = python-zaqarclient:main
    #
    # Would create an entry point called python-zaqarclient that calls python-zaqarclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Client Library for OpenStack Zaqar Queueing API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
