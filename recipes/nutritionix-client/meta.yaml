package:
  name: nutritionix-client
  version: "1.0.0.dev1"

source:
  fn: nutritionix-client-1.0.0.dev1.tar.gz
  url: https://pypi.python.org/packages/source/n/nutritionix-client/nutritionix-client-1.0.0.dev1.tar.gz
  md5: 6dba298c31bc253bdcedbdeec784f8c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nutritionix-client = nutritionix-client:main
    #
    # Would create an entry point called nutritionix-client that calls nutritionix-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - nutritionix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nutritionix/library-python
  license: MIT License
  summary: 'Official Nutritionix Python Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
