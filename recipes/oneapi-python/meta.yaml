package:
  name: oneapi-python
  version: "0.03"

source:
  fn: oneapi-python-0.03.tar.gz
  url: https://pypi.python.org/packages/source/o/oneapi-python/oneapi-python-0.03.tar.gz
  md5: 392f8c19e333a61bf690cddac46ae095
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oneapi-python = oneapi-python:main
    #
    # Would create an entry point called oneapi-python that calls oneapi-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - oneapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/infobip/oneapi-python
  license: Apache License, Version 2.0
  summary: 'Infobip OneApi Python library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
