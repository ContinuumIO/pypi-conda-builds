package:
  name: django-hautomation
  version: "2.0.4"

source:
  fn: django-hautomation-2.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hautomation/django-hautomation-2.0.4.tar.gz
  md5: b9de254e3679e7f084822a963ec9cf30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hautomation = django-hautomation:main
    #
    # Would create an entry point called django-hautomation that calls django-hautomation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5
    - simplejson ==2.6.2

  run:
    - python
    - django ==1.5
    - simplejson ==2.6.2

test:
  # Python imports
  imports:
    - hacore
    - harest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.digitalhigh.es
  license: BSD License
  summary: 'A Django app which provides a REST API for home automation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
