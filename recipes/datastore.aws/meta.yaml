package:
  name: datastore.aws
  version: "0.1.2"

source:
  fn: datastore.aws-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/datastore.aws/datastore.aws-0.1.2.tar.gz
  md5: d7aa194b220238f8099aa1ba2a861787
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datastore.aws = datastore.aws:main
    #
    # Would create an entry point called datastore.aws that calls datastore.aws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datastore >=0.3.3
    - boto >=2.5.2

  run:
    - python
    - datastore >=0.3.3
    - boto >=2.5.2

test:
  # Python imports
  imports:
    - datastore
    - datastore.aws

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datastore/datastore.aws
  license: MIT License
  summary: 'aws datastore implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
