package:
  name: collective.jqcookie
  version: "1.1.0"

source:
  fn: collective.jqcookie-1.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.jqcookie/collective.jqcookie-1.1.0.zip
  md5: fb145a63765bb4308b5212d6697dc10c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jqcookie = collective.jqcookie:main
    #
    # Would create an entry point called collective.jqcookie that calls collective.jqcookie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.jqcookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/collective.jqcookie
  license: GPL
  summary: 'Collective jqcookie Package (Maintainer: Victor baumann)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
