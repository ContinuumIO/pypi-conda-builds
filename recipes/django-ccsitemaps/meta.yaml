package:
  name: django-ccsitemaps
  version: "1.1.2"

source:
  fn: django-ccsitemaps-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ccsitemaps/django-ccsitemaps-1.1.2.tar.gz
  md5: 69a95d76aaf6cc7daffd585250400e56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ccsitemaps = django-ccsitemaps:main
    #
    # Would create an entry point called django-ccsitemaps that calls django-ccsitemaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ccsitemaps
    - ccsitemaps.management
    - ccsitemaps.management.commands
    - ccsitemaps.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/designcc/django-ccsitemaps
  license: BSD 3 Clause
  summary: 'A static sitemap generator for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
