package:
  name: buildout.eggnest
  version: "0.2"

source:
  fn: buildout.eggnest-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.eggnest/buildout.eggnest-0.2.tar.gz
  md5: c84e878b7c7fbf599c389ab9346b4749
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.eggnest = buildout.eggnest:main
    #
    # Would create an entry point called buildout.eggnest that calls buildout.eggnest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - buildout
    - buildout.eggnest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/buildout.eggnest
  license: GPL
  summary: 'buildout extension to auto load eggs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
