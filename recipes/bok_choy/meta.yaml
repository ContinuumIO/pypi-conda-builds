package:
  name: bok_choy
  version: "0.3.3"

source:
  fn: bok_choy-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bok_choy/bok_choy-0.3.3.tar.gz
  md5: 35a6a80347c2634e4b585866a8e5b531
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bok_choy = bok_choy:main
    #
    # Would create an entry point called bok_choy that calls bok_choy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - selenium
    - browsermob-proxy ==0.6.0
    - psutil ==2.2.1

  run:
    - python
    - selenium
    - browsermob-proxy ==0.6.0
    - psutil ==2.2.1

test:
  # Python imports
  imports:
    - bok_choy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/edx/bok-choy
  license: Apache Software License
  summary: 'UI-level acceptance test framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
