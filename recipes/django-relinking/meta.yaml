package:
  name: django-relinking
  version: "0.0.2"

source:
  fn: django-relinking-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-relinking/django-relinking-0.0.2.tar.gz
  md5: 4df900ced332a1bbf7c644c3cd486cbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-relinking = django-relinking:main
    #
    # Would create an entry point called django-relinking that calls django-relinking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - south >=0.7.6

  run:
    - python
    - django >=1.4.5
    - south >=0.7.6

test:
  # Python imports
  imports:
    - django_relinking
    - django_relinking.migrations
    - django_relinking.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-relinking.beslave.net/
  license: BSD License
  summary: 'Provide relinking features.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
