package:
  name: horae.ticketing
  version: "1.0a1"

source:
  fn: horae.ticketing-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.ticketing/horae.ticketing-1.0a1.tar.gz
  md5: a1ae697720eb4fbfc35b64bfe3bac4a2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.ticketing = horae.ticketing:main
    #
    # Would create an entry point called horae.ticketing that calls horae.ticketing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - hurry.query
    - horae.auth
    - horae.cache
    - horae.core
    - horae.layout
    - horae.lifecycle
    - horae.properties
    - horae.search
    - horae.timeaware

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - hurry.query
    - horae.auth
    - horae.cache
    - horae.core
    - horae.layout
    - horae.lifecycle
    - horae.properties
    - horae.search
    - horae.timeaware

test:
  # Python imports
  imports:
    - horae
    - horae.ticketing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Implements the different models and their hierarchy for the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
