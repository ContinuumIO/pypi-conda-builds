package:
  name: ely.tinymce_kupu_useragent_patch
  version: "1.0"

source:
  fn: ely.tinymce_kupu_useragent_patch-1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/ely.tinymce_kupu_useragent_patch/ely.tinymce_kupu_useragent_patch-1.0.tar.gz
  md5: 445d33fb14d5534ed0c1a977ce5b561f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ely.tinymce_kupu_useragent_patch = ely.tinymce_kupu_useragent_patch:main
    #
    # Would create an entry point called ely.tinymce_kupu_useragent_patch that calls ely.tinymce_kupu_useragent_patch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - ely
    - ely.tinymce_kupu_useragent_patch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'remove user agent test from the test for isTinyMCEnabled and isKupuEnabled'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
