package:
  name: django-nested-formset
  version: "0.1.4"

source:
  fn: django-nested-formset-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nested-formset/django-nested-formset-0.1.4.tar.gz
  md5: 005452157765dc6b51150eefd0574770
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nested-formset = django-nested-formset:main
    #
    # Would create an entry point called django-nested-formset that calls django-nested-formset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - nested_formset
    - nested_formset.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nyergler/nested-formset
  license: BSD License
  summary: 'Nest Django formsets for multi-level hierarchical editing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
