package:
  name: jcl
  version: "0.1b1"

source:
  fn: jcl-0.1b1.tar.gz
  url: https://pypi.python.org/packages/source/j/jcl/jcl-0.1b1.tar.gz
  md5: ffeb8cfd71694b4ae469c6d1385970ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jcl = jcl:main
    #
    # Would create an entry point called jcl that calls jcl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlobject >=0.8
    - pyxmpp >=1.0
    - pysqlite >=2.0

  run:
    - python
    - sqlobject >=0.8
    - pyxmpp >=1.0
    - pysqlite >=2.0

test:
  # Python imports
  imports:
    - jcl
    - jcl.jabber
    - jcl.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://people.happycoders.org/dax/projects/jcl
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Jabber Component Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
