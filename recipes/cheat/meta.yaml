package:
  name: cheat
  version: "2.1.8"

source:
  fn: cheat-2.1.8.tar.gz
  url: https://pypi.python.org/packages/source/c/cheat/cheat-2.1.8.tar.gz
  md5: 5657374e1dfa65a71da7c9de466395ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cheat = cheat:main
    #
    # Would create an entry point called cheat that calls cheat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - docopt >=0.6.1
    - pygments >=1.6.0

  run:
    - python
    - docopt >=0.6.1
    - pygments >=1.6.0

test:
  # Python imports
  imports:
    - cheat
    - cheat.cheatsheets
    - cheat.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisallenlane/cheat
  license: GPL3
  summary: 'cheat allows you to create and view interactive cheatsheets on the command-line. It was designed to help remind *nix system administrators of options for commands that they use frequently, but not frequently enough to remember.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
