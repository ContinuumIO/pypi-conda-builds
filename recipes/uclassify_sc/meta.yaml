package:
  name: uclassify_sc
  version: "0.1.4"

source:
  fn: uclassify_sc-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/u/uclassify_sc/uclassify_sc-0.1.4.tar.gz
  md5: 87ffd71b488546973baca6327d51092a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uclassify_sc = uclassify_sc:main
    #
    # Would create an entry point called uclassify_sc that calls uclassify_sc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - uclassify
    - uclassify_sc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/psibi/pyuClassify
  license: GNU General Public License v3 (GPLv3)
  summary: 'Python Wrapper for accessing uClassify services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
