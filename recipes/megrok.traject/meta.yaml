package:
  name: megrok.traject
  version: "1.0"

source:
  fn: megrok.traject-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.traject/megrok.traject-1.0.tar.gz
  md5: 389f813b7778ef54e7f9e68beea14e2c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.traject = megrok.traject:main
    #
    # Would create an entry point called megrok.traject that calls megrok.traject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - traject
    - martian
    - grokcore.component
    - zope.publisher
    - zope.component

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - traject
    - martian
    - grokcore.component
    - zope.publisher
    - zope.component

test:
  # Python imports
  imports:
    - megrok
    - megrok.traject
    - megrok.traject.ftests
    - megrok.traject.ftests.traject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'Traject integration for Grok applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
