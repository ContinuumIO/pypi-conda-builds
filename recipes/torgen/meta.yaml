package:
  name: torgen
  version: "0.2.5"

source:
  fn: torgen-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/t/torgen/torgen-0.2.5.tar.gz
  md5: cd06dc315b0920b223b4b4e2cf714ab5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torgen = torgen:main
    #
    # Would create an entry point called torgen that calls torgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - torgen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BeardyBear/tornado-generic-handlers
  license: UNKNOWN
  summary: "This package contains Django's CBVs adapted to use with Tornado framework."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
