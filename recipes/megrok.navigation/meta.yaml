package:
  name: megrok.navigation
  version: "0.6.2"

source:
  fn: megrok.navigation-0.6.2.zip
  url: https://pypi.python.org/packages/source/m/megrok.navigation/megrok.navigation-0.6.2.zip
  md5: cc1193bd1a00290586f31654b6b816c6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.navigation = megrok.navigation:main
    #
    # Would create an entry point called megrok.navigation that calls megrok.navigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - martian
    - setuptools
    - zope.viewlet
    - zope.lifecycleevent
    - zope.pagetemplate
    - zope.traversing
    - grokcore.viewlet

  run:
    - python
    - martian
    - setuptools
    - zope.viewlet
    - zope.lifecycleevent
    - zope.pagetemplate
    - zope.traversing
    - grokcore.viewlet

test:
  # Python imports
  imports:
    - megrok
    - megrok.navigation
    - megrok.navigation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/megrok.navigation
  license: GNU General Public License (GPL)
  summary: 'Navigation Menus for Grok'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
