package:
  name: obelus
  version: "0.1"

source:
  fn: obelus-0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/obelus/obelus-0.1.tar.gz
  md5: 0290406b7460c42a1b88e67eb6c6376f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - obelus = obelus:main
    #
    # Would create an entry point called obelus that calls obelus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - obelus
    - obelus.agi
    - obelus.ami
    - obelus.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/obelus/
  license: MIT License
  summary: 'Protocol implementation of the Asterisk Manager Interface and Asterisk Gateway Interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
