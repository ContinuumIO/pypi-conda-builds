package:
  name: officedissector
  version: "1.0"

source:
  fn: officedissector-1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/officedissector/officedissector-1.0.tar.gz
  md5: 8f8dc85d774cad7f4bdcd4dd2dcdb049
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - officedissector = officedissector:main
    #
    # Would create an entry point called officedissector that calls officedissector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - officedissector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.officedissector.com/
  license: UNKNOWN
  summary: 'OfficeDissector is a parser library for static security analysis of OOXML documents.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
