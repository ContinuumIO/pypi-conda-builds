package:
  name: tornadokick
  version: "0.3.0"

source:
  fn: tornadokick-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadokick/tornadokick-0.3.0.tar.gz
  md5: 272b80f43f0af9d80845d7170fefe5cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadokick = tornadokick:main
    #
    # Would create an entry point called tornadokick that calls tornadokick.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - tornadokick
    - tornadokick.example
    - tornadokick.service

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/multoncore/tornadokick
  license: Apache Software License
  summary: 'The Tornado Kick Web Toolkit for Tornado Web Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
