package:
  name: plone.formwidget.datetime
  version: "1.1"

source:
  fn: plone.formwidget.datetime-1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.formwidget.datetime/plone.formwidget.datetime-1.1.tar.gz
  md5: 226ab4e0e97e17bf39db675e14721a80
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.formwidget.datetime = plone.formwidget.datetime:main
    #
    # Would create an entry point called plone.formwidget.datetime that calls plone.formwidget.datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - datetime
    - products.cmfplone
    - plone.app.jquerytools
    - pytz
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - datetime
    - products.cmfplone
    - plone.app.jquerytools
    - pytz
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - plone
    - plone.formwidget
    - plone.formwidget.datetime
    - plone.formwidget.datetime.at
    - plone.formwidget.datetime.at.browser
    - plone.formwidget.datetime.at.tests
    - plone.formwidget.datetime.at.tests.examples
    - plone.formwidget.datetime.tests
    - plone.formwidget.datetime.z3cform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.formwidget.datetime
  license: GPL
  summary: 'Datetime widgets for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
