package:
  name: django-alert
  version: "0.7.7"

source:
  fn: django-alert-0.7.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-alert/django-alert-0.7.7.tar.gz
  md5: 26ef98413223660836ce1b9748f87339
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-alert = django-alert:main
    #
    # Would create an entry point called django-alert that calls django-alert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - alert
    - alert.management
    - alert.management.commands
    - alert.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://djangoalert.com
  license: MIT License
  summary: 'Send alerts, notifications, and messages based on events in your django application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
