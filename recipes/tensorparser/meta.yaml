package:
  name: tensorparser
  version: "0.12"

source:
  fn: tensorparser-0.12.tar.gz
  url: https://pypi.python.org/packages/source/t/tensorparser/tensorparser-0.12.tar.gz
  md5: 7b33b503b4e20f3391ebc697e7406352
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tensorparser = tensorparser:main
    #
    # Would create an entry point called tensorparser that calls tensorparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tensorparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlamadon/tensorparser
  license: MIT
  summary: 'Generate code from tensor expressions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
