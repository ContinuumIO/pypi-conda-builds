package:
  name: django-auth-gapps
  version: ".1"

source:
  fn: django-auth-gapps-.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-gapps/django-auth-gapps-.1.tar.gz
  md5: 4c1155f9afaefebb01628a4c405ae78f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-gapps = django-auth-gapps:main
    #
    # Would create an entry point called django-auth-gapps that calls django-auth-gapps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-auth-gapps/
  license: BSD License
  summary: 'Authentication with Google Apps Provisioning api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
