package:
  name: maelstrom-py
  version: "0.1.5"

source:
  fn: maelstrom-py-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/m/maelstrom-py/maelstrom-py-0.1.5.tar.gz
  md5: ca3d3566ce2c1083fce825d0723e2ce0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maelstrom-py = maelstrom-py:main
    #
    # Would create an entry point called maelstrom-py that calls maelstrom-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cassandra-driver ==2.0.2
    - nose >=1.3.1
    - rednose

  run:
    - python
    - cassandra-driver ==2.0.2
    - nose >=1.3.1
    - rednose

test:
  # Python imports
  imports:
    - maelstrom
    - maelstrom.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gradfly/maelstrom
  license: LICENSE
  summary: 'A powerful, model based Cassandra wrapper for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
