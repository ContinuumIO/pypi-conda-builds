package:
  name: hostlists
  version: "0.7.94"

source:
  fn: hostlists-0.7.94.tar.gz
  url: https://pypi.python.org/packages/source/h/hostlists/hostlists-0.7.94.tar.gz
  md5: 5e99f9bef22f74268a0dc71daef02f27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hostlists = hostlists:main
    #
    # Would create an entry point called hostlists that calls hostlists.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dnspython

  run:
    - python
    - dnspython

test:
  # Python imports
  imports:
    - hostlists
    - hostlists.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yahoo/hostlists
  license: Apache Software License
  summary: 'A python library to obtain lists of hosts from various systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
