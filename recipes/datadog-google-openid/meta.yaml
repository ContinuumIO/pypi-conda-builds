package:
  name: datadog-google-openid
  version: "2.2.5-datadog"

source:
  fn: datadog-google-openid-2.2.5-datadog.tar.gz
  url: https://pypi.python.org/packages/source/d/datadog-google-openid/datadog-google-openid-2.2.5-datadog.tar.gz
  md5: a74298018b554118717133d888baf571
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datadog-google-openid = datadog-google-openid:main
    #
    # Would create an entry point called datadog-google-openid that calls datadog-google-openid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - openid
    - openid.consumer
    - openid.extensions
    - openid.extensions.draft
    - openid.server
    - openid.store
    - openid.yadis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/openid/python-openid
  license: Apache Software License
  summary: 'OpenID support for servers and consumers with google apps support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
