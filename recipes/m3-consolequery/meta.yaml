package:
  name: m3-consolequery
  version: "1.0"

source:
  fn: m3-consolequery-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-consolequery/m3-consolequery-1.0.tar.gz
  md5: a1f836b99a4fa35bf3f73fce43e5d7e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-consolequery = m3-consolequery:main
    #
    # Would create an entry point called m3-consolequery that calls m3-consolequery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m3_users ==1.0

  run:
    - python
    - m3_users ==1.0

test:
  # Python imports
  imports:
    - m3_consolequery
    - m3_consolequery.migrations
    - tests
    - tests.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barsgroup/m3-consolequery
  license: MIT License
  summary: u'\u041a\u043e\u043d\u0441\u043e\u043b\u044c \u0437\u0430\u043f\u0440\u043e\u0441\u043e\u0432'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
