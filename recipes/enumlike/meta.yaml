package:
  name: enumlike
  version: "0.2"

source:
  fn: enumlike-0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/enumlike/enumlike-0.2.tar.gz
  md5: d550f34212028caa73473f801caf3d07
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - enumlike = enumlike:main
    #
    # Would create an entry point called enumlike that calls enumlike.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - enumlike

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oyakata/enumlike
  license: MIT License
  summary: 'An enum like object for using django-form-choces.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
