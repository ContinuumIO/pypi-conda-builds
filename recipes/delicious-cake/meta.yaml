package:
  name: delicious-cake
  version: "0.0.10"

source:
  fn: delicious-cake-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/delicious-cake/delicious-cake-0.0.10.tar.gz
  md5: 8b71e03874e9ba6e6a04d84035e10e7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - delicious-cake = delicious-cake:main
    #
    # Would create an entry point called delicious-cake that calls delicious-cake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mimeparse
    - python_dateutil >=1.5,!=2.0

  run:
    - python
    - mimeparse
    - python_dateutil >=1.5,!=2.0

test:
  # Python imports
  imports:
    - delicious_cake
    - delicious_cake.management
    - delicious_cake.management.commands
    - delicious_cake.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/itemshoppe/delicious-cake
  license: BSD License
  summary: 'A flexible REST framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
