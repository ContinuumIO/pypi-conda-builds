package:
  name: jscov8
  version: "0.1.0"

source:
  fn: jscov8-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jscov8/jscov8-0.1.0.tar.gz
  md5: 0dd42e01d9bd7b97a50f7b89704502e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jscov8 = jscov8:main
    #
    # Would create an entry point called jscov8 that calls jscov8.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jscov8

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/damoti/jscov8
  license: Apache Software License
  summary: 'JavaScript coverage tool built with Python and V8.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
