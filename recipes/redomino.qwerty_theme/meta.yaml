package:
  name: redomino.qwerty_theme
  version: "1.3"

source:
  fn: redomino.qwerty_theme-1.3.tar.gz
  url: https://pypi.python.org/packages/source/r/redomino.qwerty_theme/redomino.qwerty_theme-1.3.tar.gz
  md5: 80cb27be8262fa70601677e6c6a167ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.qwerty_theme = redomino.qwerty_theme:main
    #
    # Would create an entry point called redomino.qwerty_theme that calls redomino.qwerty_theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - redomino.css3theme

  run:
    - python
    - setuptools
    - setuptools
    - redomino.css3theme

test:
  # Python imports
  imports:
    - redomino
    - redomino.qwerty_theme
    - redomino.qwerty_theme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.qwerty_theme
  license: GPL
  summary: 'An installable theme for based on redomino.css3theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
