package:
  name: django-flexible-content
  version: "0.9.1"

source:
  fn: django-flexible-content-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flexible-content/django-flexible-content-0.9.1.tar.gz
  md5: ba2539daa3b01bd9adf05efa19af4fd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flexible-content = django-flexible-content:main
    #
    # Would create an entry point called django-flexible-content that calls django-flexible-content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-model-utils
    - requests

  run:
    - python
    - django >=1.4
    - django-model-utils
    - requests

test:
  # Python imports
  imports:
    - flexible_content
    - flexible_content.default_item_types
    - mock_project
    - mock_project.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hjkelly/django-flexible-content
  license: UNKNOWN
  summary: "Allows granular, typed, extensible control over a page's content."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
