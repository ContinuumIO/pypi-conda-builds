package:
  name: slimish_jinja
  version: "1.0.2"

source:
  fn: slimish_jinja-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/slimish_jinja/slimish_jinja-1.0.2.tar.gz
  md5: 650be50291fa4dfd123cde40093c7699
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slimish_jinja = slimish_jinja:main
    #
    # Would create an entry point called slimish_jinja that calls slimish_jinja.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - jinja2

  run:
    - python
    - distribute
    - jinja2

test:
  # Python imports
  imports:
    - slimish_jinja

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thoughtnirvana/slimish-jinja2
  license: BSD
  summary: 'Slim templates syntax for Jinja2 templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
