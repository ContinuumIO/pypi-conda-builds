package:
  name: iotlabcli
  version: "1.5.3"

source:
  fn: iotlabcli-1.5.3.tar.gz
  url: https://pypi.python.org/packages/source/i/iotlabcli/iotlabcli-1.5.3.tar.gz
  md5: 9404cdce3fc1bfd9ce8651902f5152aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iotlabcli = iotlabcli:main
    #
    # Would create an entry point called iotlabcli that calls iotlabcli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - requests >2.4.2

  run:
    - python
    - argparse
    - requests >2.4.2

test:
  # Python imports
  imports:
    - iotlabcli
    - iotlabcli.parser
    - iotlabcli.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.iot-lab.info
  license: UNKNOWN
  summary: 'IoT-LAB testbed command-line client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
