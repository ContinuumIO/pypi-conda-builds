package:
  name: django-watermark
  version: "0.1.6-pre1"

source:
  fn: django-watermark-0.1.6-pre1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-watermark/django-watermark-0.1.6-pre1.tar.gz
  md5: 0ac1c1f0d2a7237b962f13e037972721
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-watermark = django-watermark:main
    #
    # Would create an entry point called django-watermark that calls django-watermark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - watermarker
    - watermarker.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/codekoala/django-watermark/
  license: BSD License
  summary: 'Quick and efficient way to apply watermarks to images in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
