package:
  name: ageliaco.rd
  version: "0.2.6"

source:
  fn: ageliaco.rd-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/a/ageliaco.rd/ageliaco.rd-0.2.6.tar.gz
  md5: 8d2ab918d7795a2ed28d3e489d660b15
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ageliaco.rd = ageliaco.rd:main
    #
    # Would create an entry point called ageliaco.rd that calls ageliaco.rd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - collective.autopermission
    - plone
    - plone.principalsource
    - plone.app.users >=1.0b7
    - collective.wtf
    - plone.namedfile[blobs]
    - plone.formwidget.namedfile
    - ageliaco.p10userdata

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - collective.autopermission
    - plone
    - plone.principalsource
    - plone.app.users >=1.0b7
    - collective.wtf
    - plone.namedfile[blobs]
    - plone.formwidget.namedfile
    - ageliaco.p10userdata

test:
  # Python imports
  imports:
    - ageliaco
    - ageliaco.rd
    - ageliaco.rd.behavior
    - ageliaco.rd.browser
    - ageliaco.rd.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
