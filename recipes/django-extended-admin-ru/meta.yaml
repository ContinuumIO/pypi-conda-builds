package:
  name: django-extended-admin-ru
  version: "1.0.0"

source:
  fn: django-extended-admin-ru-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extended-admin-ru/django-extended-admin-ru-1.0.0.tar.gz
  md5: f4d988b0dd7bbd4bb48c3b9098378917
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extended-admin-ru = django-extended-admin-ru:main
    #
    # Would create an entry point called django-extended-admin-ru that calls django-extended-admin-ru.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-extended-admin
    - pymorphy

  run:
    - python
    - django-extended-admin
    - pymorphy

test:
  # Python imports
  imports:
    - admin_extended_ru
    - admin_extended_ru.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/masterzeb/django-extended-admin-ru/
  license: BSD
  summary: u'\u041f\u043e\u043b\u043d\u043e\u0441\u0442\u044c\u044e \u043f\u0435\u0440\u0435\u0432\u0435\u0434\u0435\u043d\u043d\u044b\u0439 \u043d\u0430 \u0440\u0443\u0441\u0441\u043a\u0438\u0439 \u044f\u0437\u044b\u043a \u0430\u0434\u043c\u0438\u043d\u0438\u0441\u0442\u0440\u0430\u0442\u0438\u0432\u043d\u044b\u0439 \u0438\u043d\u0442\u0435\u0440\u0444\u0435\u0439\u0441 Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
