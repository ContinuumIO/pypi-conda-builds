package:
  name: fpconst
  version: "0.7.2"

source:
  fn: fpconst-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fpconst/fpconst-0.7.2.tar.gz
  md5: 10ba9e04129af23108d24c22c3a698b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fpconst = fpconst:main
    #
    # Would create an entry point called fpconst that calls fpconst.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fpconst

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.analytics.washington.edu/statcomp/projects/rzope/fpconst/
  license: UNKNOWN
  summary: 'Utilities for handling IEEE 754 floating point special values'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
