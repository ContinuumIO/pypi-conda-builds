package:
  name: jarn.xmpp.collaboration
  version: "0.1b3"

source:
  fn: jarn.xmpp.collaboration-0.1b3.zip
  url: https://pypi.python.org/packages/source/j/jarn.xmpp.collaboration/jarn.xmpp.collaboration-0.1b3.zip
  md5: dfd6b02fed4f10bbe86e47850cbd17a6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jarn.xmpp.collaboration = jarn.xmpp.collaboration:main
    #
    # Would create an entry point called jarn.xmpp.collaboration that calls jarn.xmpp.collaboration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - jarn.xmpp.core

  run:
    - python
    - setuptools
    - setuptools
    - jarn.xmpp.core

test:
  # Python imports
  imports:
    - jarn
    - jarn.xmpp
    - jarn.xmpp.collaboration
    - jarn.xmpp.collaboration.adapters
    - jarn.xmpp.collaboration.browser
    - jarn.xmpp.collaboration.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ggozad/jarn.xmpp.collaboration
  license: GPL
  summary: 'Collaborative editing for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
