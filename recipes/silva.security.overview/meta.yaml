package:
  name: silva.security.overview
  version: "1.1.1"

source:
  fn: silva.security.overview-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.security.overview/silva.security.overview-1.1.1.tar.gz
  md5: 6c7254acbab10921a607ea8bd9a2dc0a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.security.overview = silva.security.overview:main
    #
    # Would create an entry point called silva.security.overview that calls silva.security.overview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - five.grok
    - five.intid
    - setuptools
    - silva.batch
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.views
    - silva.fanstatic
    - zeam.form.silva
    - zeam.utils.batch >=1.0
    - zope.cachedescriptors
    - zope.catalog
    - zope.component
    - zope.container
    - zope.index
    - zope.interface
    - zope.intid
    - zope.lifecycleevent
    - zope.schema

  run:
    - python
    - five.grok
    - five.intid
    - setuptools
    - silva.batch
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.views
    - silva.fanstatic
    - zeam.form.silva
    - zeam.utils.batch >=1.0
    - zope.cachedescriptors
    - zope.catalog
    - zope.component
    - zope.container
    - zope.index
    - zope.interface
    - zope.intid
    - zope.lifecycleevent
    - zope.schema

test:
  # Python imports
  imports:
    - silva
    - silva.security
    - silva.security.overview
    - silva.security.overview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.security.overview
  license: BSD License
  summary: 'Extension providing a global overview of the permissions granted to users in the Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
