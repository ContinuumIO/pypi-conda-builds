package:
  name: jitviewer
  version: "0.1.1"

source:
  fn: JitViewer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/J/JitViewer/JitViewer-0.1.1.tar.gz
  md5: c446923dacf28c487261ff36f3158512
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jitviewer = jitviewer:main
    #
    # Would create an entry point called jitviewer that calls jitviewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pygments
    - simplejson
    - jinja2 >=2.6

  run:
    - python
    - flask
    - pygments
    - simplejson
    - jinja2 >=2.6

test:
  # Python imports
  imports:
    - _jitviewer
    - jitviewer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypy.org
  license: UNKNOWN
  summary: "Viewer for pypy's jit traces"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
