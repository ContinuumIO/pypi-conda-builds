package:
  name: jctconv
  version: "0.1"

source:
  fn: jctconv-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jctconv/jctconv-0.1.tar.gz
  md5: 23ed5de30c0bf735fe037c5fbb96a142
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jctconv = jctconv:main
    #
    # Would create an entry point called jctconv that calls jctconv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jctconv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ikegami-yukino/jctconv
  license: MIT License
  summary: 'Pure-Python Japanese character interconverter for Hiragana, Katakana, Hankaku and Zenkaku'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
