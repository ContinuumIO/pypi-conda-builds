package:
  name: djungle-hyphenator
  version: "0.0.5"

source:
  fn: djungle-hyphenator-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djungle-hyphenator/djungle-hyphenator-0.0.5.tar.gz
  md5: 096f583e2afd1f0abf93e6458ae0d32a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djungle-hyphenator = djungle-hyphenator:main
    #
    # Would create an entry point called djungle-hyphenator that calls djungle-hyphenator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - djungle
    - djungle.hyphenator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djungle/djungle-hyphenator
  license: GNU General Public License (GPL)
  summary: 'Hyphenator.js as Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
