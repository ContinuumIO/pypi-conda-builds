package:
  name: plonerelations.atfield
  version: "1.0"

source:
  fn: plonerelations.ATField-1.0.zip
  url: https://pypi.python.org/packages/source/p/plonerelations.ATField/plonerelations.ATField-1.0.zip
  md5: 12330c1fbf5a9f9a13dc72e47847cdc8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonerelations.atfield = plonerelations.atfield:main
    #
    # Would create an entry point called plonerelations.atfield that calls plonerelations.atfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.relations

  run:
    - python
    - setuptools
    - plone.app.relations

test:
  # Python imports
  imports:
    - plonerelations
    - plonerelations.ATField

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plonerelations.ATField
  license: GPL
  summary: 'ATField for plone.relations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
