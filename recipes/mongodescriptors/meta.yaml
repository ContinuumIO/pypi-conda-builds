package:
  name: mongodescriptors
  version: "0.2.2"

source:
  fn: MongoDescriptors-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/M/MongoDescriptors/MongoDescriptors-0.2.2.tar.gz
  md5: 1756157da762ef73671e7ee0848cafe4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongodescriptors = mongodescriptors:main
    #
    # Would create an entry point called mongodescriptors that calls mongodescriptors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymongo >=2.0.0

  run:
    - python
    - pymongo >=2.0.0

test:
  # Python imports
  imports:
    - mongodescriptors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nightpool/mongo-descriptors
  license: LICENSE.txt
  summary: 'Some really basic descriptor-based utils for interacting with Mongo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
