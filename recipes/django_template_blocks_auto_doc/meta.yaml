package:
  name: django_template_blocks_auto_doc
  version: "0.1"

source:
  fn: django_template_blocks_auto_doc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_template_blocks_auto_doc/django_template_blocks_auto_doc-0.1.tar.gz
  md5: 1723c24aba41c97a0ab01e4208da80d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_template_blocks_auto_doc = django_template_blocks_auto_doc:main
    #
    # Would create an entry point called django_template_blocks_auto_doc that calls django_template_blocks_auto_doc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml ==3.1.1
    - pygments ==1.6

  run:
    - python
    - lxml ==3.1.1
    - pygments ==1.6

test:
  # Python imports
  imports:
    - django_template_blocks_auto_doc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/perenecabuto/django-template-blocks-auto-doc
  license: GNU General Public License v3 (GPLv3)
  summary: 'Generates hirarquical django base templates documentation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
