package:
  name: plonefoldercontentstopbottomlinks
  version: "2.0"

source:
  fn: PloneFolderContentsTopBottomLinks-2.0.tar.gz
  url: https://pypi.python.org/packages/source/P/PloneFolderContentsTopBottomLinks/PloneFolderContentsTopBottomLinks-2.0.tar.gz
  md5: 68b7db058fc45ca0c8408fa4f624c5a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonefoldercontentstopbottomlinks = plonefoldercontentstopbottomlinks:main
    #
    # Would create an entry point called plonefoldercontentstopbottomlinks that calls plonefoldercontentstopbottomlinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.content

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.content

test:
  # Python imports
  imports:
    - PloneFolderContentsTopBottomLinks
    - plonefoldercontentstopbottomlinks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/PloneFolderContentsTopBottomLinks
  license: GNU General Public License (GPL)
  summary: 'Patch folder contents to show top and bottom links (Plone 4 compatible release)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
