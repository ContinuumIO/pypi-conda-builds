package:
  name: codplayer
  version: "1.1"

source:
  fn: codplayer-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/codplayer/codplayer-1.1.tar.gz
  md5: 589b078064fa17196e37e5f9b9aa1a86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codplayer = codplayer:main
    #
    # Would create an entry point called codplayer that calls codplayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-daemon
    - bottle
    - lockfile
    - python-musicbrainz2
    - musicbrainzngs >=0.5
    - pyzmq

  run:
    - python
    - python-daemon
    - bottle
    - lockfile
    - python-musicbrainz2
    - musicbrainzngs >=0.5
    - pyzmq

test:
  # Python imports
  imports:
    - codplayer
    - codplayer.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/petli/codplayer
  license: MIT
  summary: 'Complicated CD player'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
