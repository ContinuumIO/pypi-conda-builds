package:
  name: darts.util.events
  version: "0.4"

source:
  fn: darts.util.events-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/darts.util.events/darts.util.events-0.4.tar.gz
  md5: bea09eaef990f7fdc53850d5cb06c617
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - darts.util.events = darts.util.events:main
    #
    # Would create an entry point called darts.util.events that calls darts.util.events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - darts
    - darts.lib
    - darts.lib.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Simple C#-style event dispatcher'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
