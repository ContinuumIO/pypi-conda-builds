package:
  name: coffee-compressor-compiler
  version: "0.2.0"

source:
  fn: coffee-compressor-compiler-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coffee-compressor-compiler/coffee-compressor-compiler-0.2.0.tar.gz
  md5: e5e400508f28d86ee92171ec48be23ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coffee-compressor-compiler = coffee-compressor-compiler:main
    #
    # Would create an entry point called coffee-compressor-compiler that calls coffee-compressor-compiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyexecjs
    - django_compressor

  run:
    - python
    - pyexecjs
    - django_compressor

test:
  # Python imports
  imports:
    - coffeecompressorcompiler
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Juvenal1228/coffee-compressor-compiler
  license: MIT License
  summary: 'django_compressor filter to compile coffee-script'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
