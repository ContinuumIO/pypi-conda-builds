package:
  name: modelicares
  version: "0.12.2"

source:
  fn: ModelicaRes-0.12.2.tar.gz
  url: https://pypi.python.org/packages/source/M/ModelicaRes/ModelicaRes-0.12.2.tar.gz
  md5: 5b084f902ba3238353f7d6e2af2f94a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modelicares = modelicares:main
    #
    # Would create an entry point called modelicares that calls modelicares.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - matplotlib >=1.3.1
    - pandas
    - control
    - six

  run:
    - python
    - numpy
    - matplotlib >=1.3.1
    - pandas
    - control
    - six

test:
  # Python imports
  imports:
    - modelicares
    - modelicares._io
    - modelicares.exps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kdavies4.github.io/ModelicaRes/
  license: BSD License
  summary: 'Utilities to set up and analyze Modelica simulation experiments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
