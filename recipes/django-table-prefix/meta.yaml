package:
  name: django-table-prefix
  version: "0.0.5"

source:
  fn: django-table-prefix-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-table-prefix/django-table-prefix-0.0.5.tar.gz
  md5: cd0a5d71a3750de10ac15d65732f1258
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-table-prefix = django-table-prefix:main
    #
    # Would create an entry point called django-table-prefix that calls django-table-prefix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - table_prefix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'experimental application for making table prefixes fortables that django creates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
