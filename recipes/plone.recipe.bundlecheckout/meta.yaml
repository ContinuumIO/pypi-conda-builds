package:
  name: plone.recipe.bundlecheckout
  version: "1.1"

source:
  fn: plone.recipe.bundlecheckout-1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.bundlecheckout/plone.recipe.bundlecheckout-1.1.tar.gz
  md5: 7d1a99e37ce2ecc183afce602b77a42e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.bundlecheckout = plone.recipe.bundlecheckout:main
    #
    # Would create an entry point called plone.recipe.bundlecheckout that calls plone.recipe.bundlecheckout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.bundlecheckout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/plone.recipe.bundlecheckout
  license: Zope Public License
  summary: 'ZC Buildout recipe for installing a package or bundle checkout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
