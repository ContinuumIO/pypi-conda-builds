package:
  name: django-analyze-sessions
  version: "0.1.1"

source:
  fn: django-analyze-sessions-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-analyze-sessions/django-analyze-sessions-0.1.1.tar.gz
  md5: 1bf3155b9e596bb3ccc7fc3d8a93dde6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-analyze-sessions = django-analyze-sessions:main
    #
    # Would create an entry point called django-analyze-sessions that calls django-analyze-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3.0

  run:
    - python
    - django >=1.3.0

test:
  # Python imports
  imports:
    - analyze_sessions
    - analyze_sessions.management
    - analyze_sessions.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/isnotajoke/django-analyze-sessions
  license: LICENSE.txt
  summary: 'Tools to analyze Django DB sessions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
