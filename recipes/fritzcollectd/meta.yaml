package:
  name: fritzcollectd
  version: "0.1.0"

source:
  fn: fritzcollectd-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fritzcollectd/fritzcollectd-0.1.0.tar.gz
  md5: 2fe76c3695c0309dffeb0de9993dd84f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fritzcollectd = fritzcollectd:main
    #
    # Would create an entry point called fritzcollectd that calls fritzcollectd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fritzconnection

  run:
    - python
    - fritzconnection

test:
  # Python imports
  imports:
    - fritzcollectd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fetzerch/fritzcollectd
  license: MIT License
  summary: 'A collectd plugin for monitoring AVM FRITZ!Box routers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
