package:
  name: mpwebstatus
  version: "0.2"

source:
  fn: mpwebstatus-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mpwebstatus/mpwebstatus-0.2.tar.gz
  md5: fa77796458b15a968d1d7d14482fe8c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpwebstatus = mpwebstatus:main
    #
    # Would create an entry point called mpwebstatus that calls mpwebstatus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mpwebstatus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cmutel/mpwebstatus
  license: BSD 2-clause; LICENSE.txt
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
