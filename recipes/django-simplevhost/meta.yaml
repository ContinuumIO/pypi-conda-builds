package:
  name: django-simplevhost
  version: "0.2.2"

source:
  fn: django-simplevhost-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simplevhost/django-simplevhost-0.2.2.tar.gz
  md5: b904d4338ecf14ab889f92f736223b8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simplevhost = django-simplevhost:main
    #
    # Would create an entry point called django-simplevhost that calls django-simplevhost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplevhost
    - simplevhost.migrations
    - simplevhost.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/russellhay/django-simplevhost
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
