package:
  name: django-carrousel
  version: "1.0"

source:
  fn: django-carrousel-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-carrousel/django-carrousel-1.0.tar.gz
  md5: 342e689fa9ee26750b86dca2c80abf8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-carrousel = django-carrousel:main
    #
    # Would create an entry point called django-carrousel that calls django-carrousel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - south >=0.7.2
    - django-positions >=0.5.2
    - easy-thumbnails >=2.1

  run:
    - python
    - django >=1.4
    - south >=0.7.2
    - django-positions >=0.5.2
    - easy-thumbnails >=2.1

test:
  # Python imports
  imports:
    - carrousel
    - carrousel.migrations
    - carrousel.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/devartis/django-carrousel
  license: Apache Software License
  summary: 'A django application that allows adding carrousel with slides on your web site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
