package:
  name: typequery
  version: "0.1.4"

source:
  fn: TypeQuery-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/T/TypeQuery/TypeQuery-0.1.4.tar.gz
  md5: 457d54c37a83e4b89164b9f2cdc89612
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typequery = typequery:main
    #
    # Would create an entry point called typequery that calls typequery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - typequery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dahlia/typequery
  license: MIT License
  summary: 'A simple and dirty way to define generic methods to existing types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
