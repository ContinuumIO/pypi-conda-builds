package:
  name: django-inbound-email
  version: "0.8"

source:
  fn: django-inbound-email-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inbound-email/django-inbound-email-0.8.tar.gz
  md5: 4250f8d81165616ad95ec51f3403a4ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inbound-email = django-inbound-email:main
    #
    # Would create an entry point called django-inbound-email that calls django-inbound-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - django_inbound_email
    - django_inbound_email.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yunojuno/django-inbound-email
  license: MIT License
  summary: 'A Django app for receiving inbound emails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
