package:
  name: gdshortener
  version: "0.0.2"

source:
  fn: gdshortener-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gdshortener/gdshortener-0.0.2.tar.gz
  md5: ef617efc11436f72e2fc9cfba16be60f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gdshortener = gdshortener:main
    #
    # Would create an entry point called gdshortener that calls gdshortener.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gdshortener

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/torre76/gd_shortener
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A module that provides access to .gd URL Shortener'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
