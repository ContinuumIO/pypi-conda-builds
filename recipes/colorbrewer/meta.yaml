package:
  name: colorbrewer
  version: "0.1.1"

source:
  fn: colorbrewer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colorbrewer/colorbrewer-0.1.1.tar.gz
  md5: b271dd4e93a2b2498c89760bca025412
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorbrewer = colorbrewer:main
    #
    # Would create an entry point called colorbrewer that calls colorbrewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colorbrewer
    - colorbrewer.data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://noble.gs.washington.edu/~mmh1/software/colorbrewer/
  license: UNKNOWN
  summary: "constants from Cynthia Brewer's ColorBrewer"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
