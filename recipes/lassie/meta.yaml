package:
  name: lassie
  version: "0.5.2"

source:
  fn: lassie-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lassie/lassie-0.5.2.tar.gz
  md5: 88fa65ec370f5da56b35edc1a8ec3b54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lassie = lassie:main
    #
    # Would create an entry point called lassie that calls lassie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.6.0
    - beautifulsoup4 ==4.3.2
    - html5lib ==1.0b3

  run:
    - python
    - requests ==2.6.0
    - beautifulsoup4 ==4.3.2
    - html5lib ==1.0b3

test:
  # Python imports
  imports:
    - lassie
    - lassie.filters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/michaelhelmick/lassie/tree/master
  license: MIT License
  summary: 'Lassie is a Python library for retrieving basic content from websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
