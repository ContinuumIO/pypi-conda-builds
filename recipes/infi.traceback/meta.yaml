package:
  name: infi.traceback
  version: "0.3.11"

source:
  fn: infi.traceback-0.3.11.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.traceback/infi.traceback-0.3.11.tar.gz
  md5: 6ce9007ffd15cd0743133aa2c8765b42
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.traceback = infi.traceback:main
    #
    # Would create an entry point called infi.traceback that calls infi.traceback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - infi.exceptools
    - infi.pyutils
    - mock
    - nose
    - setuptools

  run:
    - python
    - setuptools
    - infi.exceptools
    - infi.pyutils
    - mock
    - nose
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.traceback

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.traceback
  license: Python Software Foundation License
  summary: 'better tracebacks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
