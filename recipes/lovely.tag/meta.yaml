package:
  name: lovely.tag
  version: "1.1.1"

source:
  fn: lovely.tag-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.tag/lovely.tag-1.1.1.tar.gz
  md5: b1e87d4ad0c3bc4b49fe865bb47ce086
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.tag = lovely.tag:main
    #
    # Would create an entry point called lovely.tag that calls lovely.tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - pytz
    - z3c.configurator
    - zope.app.component
    - zope.app.container
    - zope.app.folder
    - zope.app.generations
    - zope.app.intid
    - zope.app.pagetemplate
    - zope.app.zopeappgenerations
    - zope.cachedescriptors
    - zope.component
    - zope.dottedname
    - zope.formlib
    - zope.i18nmessageid
    - zope.index
    - zope.interface
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema
    - zope.security

  run:
    - python
    - setuptools
    - zodb3
    - pytz
    - z3c.configurator
    - zope.app.component
    - zope.app.container
    - zope.app.folder
    - zope.app.generations
    - zope.app.intid
    - zope.app.pagetemplate
    - zope.app.zopeappgenerations
    - zope.cachedescriptors
    - zope.component
    - zope.dottedname
    - zope.formlib
    - zope.i18nmessageid
    - zope.index
    - zope.interface
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema
    - zope.security

test:
  # Python imports
  imports:
    - lovely
    - lovely.tag
    - lovely.tag.browser
    - lovely.tag.generations
    - lovely.tag.performance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/lovely.tag
  license: Zope Public License
  summary: 'A tagging engine for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
