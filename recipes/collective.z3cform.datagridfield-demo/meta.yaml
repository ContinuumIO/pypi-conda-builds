package:
  name: collective.z3cform.datagridfield-demo
  version: "0.6"

source:
  fn: collective.z3cform.datagridfield_demo-0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.z3cform.datagridfield-demo/collective.z3cform.datagridfield_demo-0.6.tar.gz
  md5: 5de6ea38c479e707a5fbe64a5c2d9ca3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.datagridfield-demo = collective.z3cform.datagridfield-demo:main
    #
    # Would create an entry point called collective.z3cform.datagridfield-demo that calls collective.z3cform.datagridfield-demo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok
    - plone.directives.form
    - plone.app.z3cform
    - collective.z3cform.datagridfield

  run:
    - python
    - setuptools
    - setuptools
    - five.grok
    - plone.directives.form
    - plone.app.z3cform
    - collective.z3cform.datagridfield

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.datagridfield_demo
    - collective.z3cform.datagridfield_demo.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.z3cform.datagridfield_demo
  license: GPL
  summary: 'Version of DataGridField for use with Dexterity / z3c.form - Demo views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
