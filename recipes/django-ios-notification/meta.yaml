package:
  name: django-ios-notification
  version: "1.0.4"

source:
  fn: django-ios-notification-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ios-notification/django-ios-notification-1.0.4.tar.gz
  md5: b885dded7c67dedd12466d53d2c42b43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ios-notification = django-ios-notification:main
    #
    # Would create an entry point called django-ios-notification that calls django-ios-notification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ios_notification

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matheussl/django-ios-notification
  license: Apache License 2.0
  summary: 'A Django Application for contacting the Apple Push Service and maintaining a list of iOS devices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
