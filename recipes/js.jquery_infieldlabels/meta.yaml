package:
  name: js.jquery_infieldlabels
  version: "0.1"

source:
  fn: js.jquery_infieldlabels-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_infieldlabels/js.jquery_infieldlabels-0.1.tar.gz
  md5: 9f94f702a8beefa30beb185e321d7355
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_infieldlabels = js.jquery_infieldlabels:main
    #
    # Would create an entry point called js.jquery_infieldlabels that calls js.jquery_infieldlabels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_infieldlabels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
