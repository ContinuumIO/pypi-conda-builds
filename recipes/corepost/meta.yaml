package:
  name: corepost
  version: "0.0.16"

source:
  fn: CorePost-0.0.16.tar.gz
  url: https://pypi.python.org/packages/source/C/CorePost/CorePost-0.0.16.tar.gz
  md5: e505612363eaba0e807f31a9bcf9b2da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corepost = corepost:main
    #
    # Would create an entry point called corepost that calls corepost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=12.0.0
    - formencode >=1.2.4
    - pyyaml >=3.1.0

  run:
    - python
    - twisted >=12.0.0
    - formencode >=1.2.4
    - pyyaml >=3.1.0

test:
  # Python imports
  imports:
    - corepost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jacek99/corepost
  license: BSD License
  summary: 'A Twisted Web REST micro-framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
