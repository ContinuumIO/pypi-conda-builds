package:
  name: collective.portlet.dropdownbox
  version: "0.1.1"

source:
  fn: collective.portlet.dropdownbox-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.dropdownbox/collective.portlet.dropdownbox-0.1.1.tar.gz
  md5: 7c27ec608755222a512a3169747b7c30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.dropdownbox = collective.portlet.dropdownbox:main
    #
    # Would create an entry point called collective.portlet.dropdownbox that calls collective.portlet.dropdownbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.dropdownbox
    - collective.portlet.dropdownbox.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
