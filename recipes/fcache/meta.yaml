package:
  name: fcache
  version: "0.4.4"

source:
  fn: fcache-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/f/fcache/fcache-0.4.4.tar.gz
  md5: ad95c16d8774bc570bea0c2857d916af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fcache = fcache:main
    #
    # Would create an entry point called fcache that calls fcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appdirs

  run:
    - python
    - appdirs

test:
  # Python imports
  imports:
    - fcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tsroten/fcache
  license: MIT License
  summary: 'a dictionary-like, file-based cache module for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
