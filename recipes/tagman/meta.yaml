package:
  name: tagman
  version: "0.1.11"

source:
  fn: tagman-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/t/tagman/tagman-0.1.11.tar.gz
  md5: 54704955cb0caf7cb6240485ef55b182
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tagman = tagman:main
    #
    # Would create an entry point called tagman that calls tagman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tagman
    - tagman.migrations
    - tagman.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Rethought/tagman.git
  license: BSD License
  summary: 'Curated tagging app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
