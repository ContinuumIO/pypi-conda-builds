package:
  name: iservices.controlpanel
  version: "0.3.1"

source:
  fn: iservices.controlpanel-0.3.1.zip
  url: https://pypi.python.org/packages/source/i/iservices.controlpanel/iservices.controlpanel-0.3.1.zip
  md5: da793791668263e702f571bca94c3f91
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iservices.controlpanel = iservices.controlpanel:main
    #
    # Would create an entry point called iservices.controlpanel that calls iservices.controlpanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.registry

test:
  # Python imports
  imports:
    - iservices
    - iservices.controlpanel
    - iservices.controlpanel.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/iservices.controlpanel
  license: GPL
  summary: 'A generic control panel for themers and site integrators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
