package:
  name: aino-utkik
  version: "0.8.0"

source:
  fn: aino-utkik-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aino-utkik/aino-utkik-0.8.0.tar.gz
  md5: a017531c27f2ab65b5fd01c9a81f3c36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aino-utkik = aino-utkik:main
    #
    # Would create an entry point called aino-utkik that calls aino-utkik.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - utkik

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/aino-utkik
  license: BSD License
  summary: 'Small, clean code with a lazy view dispatcher and class based views for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
