package:
  name: django-memcache-status
  version: "1.1"

source:
  fn: django-memcache-status-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-memcache-status/django-memcache-status-1.1.tar.gz
  md5: 7d3e65e97e35c9df62cc1e69986b0ec3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-memcache-status = django-memcache-status:main
    #
    # Would create an entry point called django-memcache-status that calls django-memcache-status.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - memcache_status
    - memcache_status.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bartTC/django-memcache-status
  license: BSD License
  summary: 'A django application that displays the load and some other statistics about your memcached instances in the admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
