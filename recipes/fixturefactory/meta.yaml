package:
  name: fixturefactory
  version: "0.2b"

source:
  fn: fixturefactory-0.2b.tar.gz
  url: https://pypi.python.org/packages/source/f/fixturefactory/fixturefactory-0.2b.tar.gz
  md5: f2a2dc000824ec77023aa72cbf765090
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fixturefactory = fixturefactory:main
    #
    # Would create an entry point called fixturefactory that calls fixturefactory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - fixturefactory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: git@github.com:adgaudio/fixturefactory.git
  license: UNKNOWN
  summary: 'Factory to generate Django model objects.  Easier than factoryboy and factorygirl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
