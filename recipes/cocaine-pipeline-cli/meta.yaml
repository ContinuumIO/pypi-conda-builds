package:
  name: cocaine-pipeline-cli
  version: "0.11.9.5"

source:
  fn: cocaine-pipeline-cli-0.11.9.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cocaine-pipeline-cli/cocaine-pipeline-cli-0.11.9.5.tar.gz
  md5: fc371acfa61fe18ad369aa5af42b3bf7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cocaine-pipeline-cli = cocaine-pipeline-cli:main
    #
    # Would create an entry point called cocaine-pipeline-cli that calls cocaine-pipeline-cli.main()

    - cocaine-pipeline-cli = cocaine.pipeline:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cocaine >=0.11.1.0
    - tornado
    - simplejson

  run:
    - python
    - cocaine >=0.11.1.0
    - tornado
    - simplejson

test:
  # Python imports
  imports:
    - cocaine.pipeline

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - cocaine-pipeline-cli --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LGPLv3+
  summary: 'Cocaine Tools for Cocaine Application Cloud.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
