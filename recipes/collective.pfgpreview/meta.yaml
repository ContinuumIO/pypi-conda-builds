package:
  name: collective.pfgpreview
  version: "1.1"

source:
  fn: collective.pfgpreview-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.pfgpreview/collective.pfgpreview-1.1.zip
  md5: 4f0e586e4beab53aeb6ba04bd5e8c29c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pfgpreview = collective.pfgpreview:main
    #
    # Would create an entry point called collective.pfgpreview that calls collective.pfgpreview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen

test:
  # Python imports
  imports:
    - collective
    - collective.pfgpreview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.pfgpreview
  license: GPL
  summary: 'Preview page for PloneFormGen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
