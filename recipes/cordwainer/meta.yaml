package:
  name: cordwainer
  version: "0.0.2"

source:
  fn: cordwainer-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cordwainer/cordwainer-0.0.2.tar.gz
  md5: e84887fb4a64ce1066050a01d8671181
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cordwainer = cordwainer:main
    #
    # Would create an entry point called cordwainer that calls cordwainer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.7

  run:
    - python
    - six >=1.7

test:
  # Python imports
  imports:
    - cordwainer
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caktus/cordwainer
  license: Apache Software License
  summary: 'A better CSV module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
