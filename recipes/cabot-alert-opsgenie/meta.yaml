package:
  name: cabot-alert-opsgenie
  version: "0.0.1"

source:
  fn: cabot-alert-opsgenie-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cabot-alert-opsgenie/cabot-alert-opsgenie-0.0.1.tar.gz
  md5: da80e89b76f6f4c991d67330ae0bc55f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cabot-alert-opsgenie = cabot-alert-opsgenie:main
    #
    # Would create an entry point called cabot-alert-opsgenie that calls cabot-alert-opsgenie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cabot_alert_opsgenie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dibits
  license: UNKNOWN
  summary: 'OpsGenie plugin for Cabot by Arachnys'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
