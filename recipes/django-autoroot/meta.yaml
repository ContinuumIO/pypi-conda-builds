package:
  name: django-autoroot
  version: "0.2.1"

source:
  fn: django-autoroot-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-autoroot/django-autoroot-0.2.1.tar.gz
  md5: 9298feeb2fdf288ea1357434c3864f2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-autoroot = django-autoroot:main
    #
    # Would create an entry point called django-autoroot that calls django-autoroot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - autoroot
    - autoroot.management

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/spinus/django-autoroot
  license: BSD License
  summary: 'automaticly creates `root` user after syncdb if not exists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
