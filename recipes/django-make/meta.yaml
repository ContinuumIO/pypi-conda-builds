package:
  name: django-make
  version: "0.0.3"

source:
  fn: django-make-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-make/django-make-0.0.3.tar.gz
  md5: 79b28b80fa0aa42cb818d01321f86dc4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-make = django-make:main
    #
    # Would create an entry point called django-make that calls django-make.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_make

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kelonye/django-make
  license: MIT License
  summary: 'Django development environment `$ make` middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
