package:
  name: django-dbarray
  version: "0.2"

source:
  fn: django-dbarray-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbarray/django-dbarray-0.2.tar.gz
  md5: 65a5ebaa1b7af9fedb8da894941666f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbarray = django-dbarray:main
    #
    # Would create an entry point called django-dbarray that calls django-dbarray.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbarray
    - dbarray.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ecometrica/django-dbarray/
  license: BSD License
  summary: 'Django ORM field for Postgres array types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
