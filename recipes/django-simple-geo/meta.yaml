package:
  name: django-simple-geo
  version: "0.1.7"

source:
  fn: django-simple-geo-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-geo/django-simple-geo-0.1.7.tar.gz
  md5: cee71d527b2e2b0741eee1a15bed7c94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-geo = django-simple-geo:main
    #
    # Would create an entry point called django-simple-geo that calls django-simple-geo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simple_geo
    - simple_geo.management
    - simple_geo.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jaddison/django-simple-geo
  license: BSD License
  summary: 'Simple Django city, state, country geo-spatially aware model and utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
