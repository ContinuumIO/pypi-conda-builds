package:
  name: djangoquickapi
  version: "0.1"

source:
  fn: DjangoQuickAPI-0.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoQuickAPI/DjangoQuickAPI-0.1.tar.gz
  md5: 9071c4884f088227cd664026f2d44e6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoquickapi = djangoquickapi:main
    #
    # Would create an entry point called djangoquickapi that calls djangoquickapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework

  run:
    - python
    - djangorestframework

test:
  # Python imports
  imports:
    - djangoquickapi
    - quickapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/seanmavley/DjangoQuickAPI.git
  license: MIT License
  summary: 'A simple django app to serve api requests for local development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
