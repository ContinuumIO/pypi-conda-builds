package:
  name: django-freeplay
  version: "0.1.91"

source:
  fn: django-freeplay-0.1.91.tar.gz
  url: https://pypi.python.org/packages/source/d/django-freeplay/django-freeplay-0.1.91.tar.gz
  md5: 2e4668033c1ff29a1d1d4b49e0c4f0b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-freeplay = django-freeplay:main
    #
    # Would create an entry point called django-freeplay that calls django-freeplay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-model-utils ==1.1.0
    - django-relatedadminwidget ==0.0.2
    - django-imagekit >=3.0.4

  run:
    - python
    - django-model-utils ==1.1.0
    - django-relatedadminwidget ==0.0.2
    - django-imagekit >=3.0.4

test:
  # Python imports
  imports:
    - freeplay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pullswitch/django-freeplay
  license: MIT License
  summary: 'Easily add CMS functionality to your Django site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
