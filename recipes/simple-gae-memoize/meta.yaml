package:
  name: simple-gae-memoize
  version: "0.0.2"

source:
  fn: simple-gae-memoize-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/simple-gae-memoize/simple-gae-memoize-0.0.2.tar.gz
  md5: 29d4c3fc465c42b7e298bbd370955a47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple-gae-memoize = simple-gae-memoize:main
    #
    # Would create an entry point called simple-gae-memoize that calls simple-gae-memoize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - memoize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/simple_gae_memoize
  license: BSD License
  summary: 'A decorator to memoize App Engine functions using the memcache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
