package:
  name: django-macaddress
  version: "1.3.0"

source:
  fn: django-macaddress-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-macaddress/django-macaddress-1.3.0.tar.gz
  md5: 95dca10caa3cce9d91800d14bfc90ed5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-macaddress = django-macaddress:main
    #
    # Would create an entry point called django-macaddress that calls django-macaddress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netaddr

  run:
    - python
    - netaddr

test:
  # Python imports
  imports:
    - macaddress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tubaman/django-macaddress
  license: BSD License
  summary: 'MAC address model and form fields for Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
