package:
  name: gruffy
  version: "0.3.4"

source:
  fn: gruffy-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/g/gruffy/gruffy-0.3.4.tar.gz
  md5: f172c99d476eb20b9dabaa773e041b39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gruffy = gruffy:main
    #
    # Would create an entry point called gruffy that calls gruffy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pgmagick

  run:
    - python
    - pgmagick

test:
  # Python imports
  imports:
    - gruffy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gruffy/
  license: MIT License
  summary: 'Gruffy is Python implemetation of Gruff(Ruby Graphing Library)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
