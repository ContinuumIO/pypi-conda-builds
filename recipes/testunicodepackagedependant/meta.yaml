package:
  name: testunicodepackagedependant
  version: "1.0"

source:
  fn: testunicodepackagedependant-1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/testunicodepackagedependant/testunicodepackagedependant-1.0.tar.gz
  md5: 25bc682c12cc78dbd73a242fd9480cc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testunicodepackagedependant = testunicodepackagedependant:main
    #
    # Would create an entry point called testunicodepackagedependant that calls testunicodepackagedependant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - testunicodepackage ==1.0

  run:
    - python
    - testunicodepackage ==1.0

test:
  # Python imports
  imports:
    - testunicodepackagedependant

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/testunicodepackagedependant/
  license: MIT License
  summary: 'Test package dependant on a package with Unicode filenames'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
