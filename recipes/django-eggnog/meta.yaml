package:
  name: django-eggnog
  version: "0.0.1"

source:
  fn: django-eggnog-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-eggnog/django-eggnog-0.0.1.tar.gz
  md5: d0fa401f8e505921cca97578eeb44912
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-eggnog = django-eggnog:main
    #
    # Would create an entry point called django-eggnog that calls django-eggnog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - south >=0.7.2
    - yolk
    - apscheduler

  run:
    - python
    - django >=1.4
    - south >=0.7.2
    - yolk
    - apscheduler

test:
  # Python imports
  imports:
    - eggnog
    - eggnog.management.commands
    - eggnog.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/mridang/django-eggnog
  license: BSD License
  summary: 'Displays available egg updates from PyPi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
