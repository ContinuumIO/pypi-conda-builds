package:
  name: django-excalibur
  version: "0.0.1"

source:
  fn: django-excalibur-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-excalibur/django-excalibur-0.0.1.tar.gz
  md5: 8967f1545bed9c3885e8bc01ecc53bb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-excalibur = django-excalibur:main
    #
    # Would create an entry point called django-excalibur that calls django-excalibur.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - excalibur
    - britney
    - britney-utils

  run:
    - python
    - excalibur
    - britney
    - britney-utils

test:
  # Python imports
  imports:
    - django_excalibur

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unistra/django-excalibur
  license: GNU General Public License v2 (GPLv2)
  summary: 'Django Rest Framework implementation for Excalibur : A tool to manage plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
