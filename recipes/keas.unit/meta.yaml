package:
  name: keas.unit
  version: "1.0.0"

source:
  fn: keas.unit-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/k/keas.unit/keas.unit-1.0.0.tar.gz
  md5: 0d593ac639e5d5423d984c022bc8d681
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - keas.unit = keas.unit:main
    #
    # Would create an entry point called keas.unit that calls keas.unit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - keas
    - keas.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/keas.unit
  license: Zope Public License
  summary: "A simple wrapper around the 'units' shell command"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
