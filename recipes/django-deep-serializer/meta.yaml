package:
  name: django-deep-serializer
  version: "0.1.3"

source:
  fn: django-deep-serializer-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-deep-serializer/django-deep-serializer-0.1.3.tar.gz
  md5: 532d138549bff2e4b73a1ce25a08d690
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-deep-serializer = django-deep-serializer:main
    #
    # Would create an entry point called django-deep-serializer that calls django-deep-serializer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - deep_serializer
    - deep_serializer.serializers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goinnn/django-deep-serializer
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Serialize/Deserialize an object and its relations through class definitions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
