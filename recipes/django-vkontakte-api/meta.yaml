package:
  name: django-vkontakte-api
  version: "0.7.11"

source:
  fn: django-vkontakte-api-0.7.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-api/django-vkontakte-api-0.7.11.tar.gz
  md5: 364adc9a5d21082cf963c1ab4e9d54f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-api = django-vkontakte-api:main
    #
    # Would create an entry point called django-vkontakte-api that calls django-vkontakte-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-annoying
    - django-picklefield
    - django-oauth-tokens >=0.5.0
    - vkontakte
    - simplejson
    - beautifulsoup4

  run:
    - python
    - django
    - django-annoying
    - django-picklefield
    - django-oauth-tokens >=0.5.0
    - vkontakte
    - simplejson
    - beautifulsoup4

test:
  # Python imports
  imports:
    - vkontakte_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-vkontakte-api
  license: BSD License
  summary: 'Django implementation for vkontakte API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
