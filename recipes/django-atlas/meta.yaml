package:
  name: django-atlas
  version: "0.1"

source:
  fn: django-atlas-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-atlas/django-atlas-0.1.tar.gz
  md5: 87302c5047ee0f2d966e30bdbfc1fc67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-atlas = django-atlas:main
    #
    # Would create an entry point called django-atlas that calls django-atlas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.7
    - django-tastypie >=0.10,<0.12
    - django-photologue >=3.1
    - django-category

  run:
    - python
    - django <1.7
    - django-tastypie >=0.10,<0.12
    - django-photologue >=3.1
    - django-category

test:
  # Python imports
  imports:
    - atlas
    - atlas.migrations
    - atlas.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Rizziepit/django-atlas
  license: BSD License
  summary: 'Geolocation models, data and tools using GeoDjango'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
