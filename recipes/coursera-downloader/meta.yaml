package:
  name: coursera-downloader
  version: "0.2"

source:
  fn: coursera-downloader-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/coursera-downloader/coursera-downloader-0.2.tar.gz
  md5: dc37a82fe1a07c915bcc48b3838e9b84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coursera-downloader = coursera-downloader:main
    #
    # Would create an entry point called coursera-downloader that calls coursera-downloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyquery
    - selenium

  run:
    - python
    - pyquery
    - selenium

test:
  # Python imports
  imports:
    - cdl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/huanghao/coursera-downloader
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
