package:
  name: django-data-importer
  version: "0.1.0-p3"

source:
  fn: django-data-importer-0.1.0-p3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-data-importer/django-data-importer-0.1.0-p3.tar.gz
  md5: d82779721a72964eeffa9b9c69b7e8de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-data-importer = django-data-importer:main
    #
    # Would create an entry point called django-data-importer that calls django-data-importer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.4
    - openpyxl
    - xlrd

  run:
    - python
    - django >=1.3.4
    - openpyxl
    - xlrd

test:
  # Python imports
  imports:
    - data_importer
    - data_importer.readers
    - data_importer.tests
    - sampleproject
    - sampleproject.data_importer
    - sampleproject.data_importer.readers
    - sampleproject.data_importer.tests
    - sampleproject.sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chronossc/django-data-importer
  license: BSD License
  summary: 'Generic, easy to use, file reader and importer with validations like Django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
