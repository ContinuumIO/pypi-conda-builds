package:
  name: sc.contentrules.movebyattribute
  version: "0.5"

source:
  fn: sc.contentrules.movebyattribute-0.5.tar.gz
  url: https://pypi.python.org/packages/source/s/sc.contentrules.movebyattribute/sc.contentrules.movebyattribute-0.5.tar.gz
  md5: 0a9834a9a4e13aeb69c1acdd78cac1a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.contentrules.movebyattribute = sc.contentrules.movebyattribute:main
    #
    # Would create an entry point called sc.contentrules.movebyattribute that calls sc.contentrules.movebyattribute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - sc
    - sc.contentrules
    - sc.contentrules.movebyattribute
    - sc.contentrules.movebyattribute.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br
  license: GPL
  summary: 'Plone content rule to move an item based on an attribute'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
