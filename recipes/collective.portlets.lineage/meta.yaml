package:
  name: collective.portlets.lineage
  version: "1.3.0"

source:
  fn: collective.portlets.lineage-1.3.0.zip
  url: https://pypi.python.org/packages/source/c/collective.portlets.lineage/collective.portlets.lineage-1.3.0.zip
  md5: 66cf22b8acbb20d70fab636052395c58
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlets.lineage = collective.portlets.lineage:main
    #
    # Would create an entry point called collective.portlets.lineage that calls collective.portlets.lineage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.lineage
    - products.advancedquery

  run:
    - python
    - setuptools
    - setuptools
    - collective.lineage
    - products.advancedquery

test:
  # Python imports
  imports:
    - collective
    - collective.portlets
    - collective.portlets.lineage
    - collective.portlets.lineage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.portlets.lineage
  license: GPL
  summary: 'A collection of collective.lineage aware portlets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
