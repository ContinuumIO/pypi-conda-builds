package:
  name: django-smartagent
  version: "0.1.1"

source:
  fn: django-smartagent-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-smartagent/django-smartagent-0.1.1.zip
  md5: 578b0acf4fc212c5e5be5c0514af34de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smartagent = django-smartagent:main
    #
    # Would create an entry point called django-smartagent that calls django-smartagent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smartagent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jamespacileo/django-smartagent/
  license: BSD License
  summary: 'django-smartagent is the fastest and most complete user agent parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
