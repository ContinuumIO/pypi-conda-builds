package:
  name: gocept.cache
  version: "0.6.1"

source:
  fn: gocept.cache-0.6.1.zip
  url: https://pypi.python.org/packages/source/g/gocept.cache/gocept.cache-0.6.1.zip
  md5: bb5186173f6c2e0d4d043527f8ba2596
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.cache = gocept.cache:main
    #
    # Would create an entry point called gocept.cache that calls gocept.cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - decorator
    - setuptools
    - transaction
    - zope.testing

  run:
    - python
    - decorator
    - setuptools
    - transaction
    - zope.testing

test:
  # Python imports
  imports:
    - gocept
    - gocept.cache
    - gocept.cache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.cache
  license: ZPL 2.1
  summary: 'Cache descriptors for Python and Zope'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
