package:
  name: dex
  version: "0.6"

source:
  fn: Dex-0.6.tar.gz
  url: https://pypi.python.org/packages/source/D/Dex/Dex-0.6.tar.gz
  md5: 3fe32c254ebff3ba8ca81fbbd0741060
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dex = dex:main
    #
    # Would create an entry point called dex that calls dex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymongo
    - pyyaml
    - dargparse >=0.2.2
    - ordereddict

  run:
    - python
    - pymongo
    - pyyaml
    - dargparse >=0.2.2
    - ordereddict

test:
  # Python imports
  imports:
    - dex
    - dex.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mongolab/dex
  license: MIT
  summary: 'Index and query analyzer for MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
