package:
  name: hurry.zopetinymce
  version: "0.1"

source:
  fn: hurry.zopetinymce-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.zopetinymce/hurry.zopetinymce-0.1.tar.gz
  md5: 56ed104512beb45f856699bc500d0c1a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.zopetinymce = hurry.zopetinymce:main
    #
    # Would create an entry point called hurry.zopetinymce that calls hurry.zopetinymce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hurry.zoperesource
    - hurry.tinymce

  run:
    - python
    - setuptools
    - hurry.zoperesource
    - hurry.tinymce

test:
  # Python imports
  imports:
    - hurry
    - hurry.zopetinymce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Zope integration for hurry.tinymce.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
