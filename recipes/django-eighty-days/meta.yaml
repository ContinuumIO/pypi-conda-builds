package:
  name: django-eighty-days
  version: "0.3.0"

source:
  fn: django-eighty-days-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-eighty-days/django-eighty-days-0.3.0.tar.gz
  md5: 25b076be086a538d88945398e6b3344a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-eighty-days = django-eighty-days:main
    #
    # Would create an entry point called django-eighty-days that calls django-eighty-days.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_eighty_days

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/swfiua/django_eighty_days
  license: BSD License
  summary: 'A django app for around the world in 80 days'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
