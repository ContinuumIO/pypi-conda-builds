package:
  name: django-livejs2
  version: "1.0"

source:
  fn: django-livejs2-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livejs2/django-livejs2-1.0.tar.gz
  md5: 4136627468ff7cc04062629de34fd6de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livejs2 = django-livejs2:main
    #
    # Would create an entry point called django-livejs2 that calls django-livejs2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_livejs2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SAPikachu/django-livejs2
  license: MIT License
  summary: 'Integrate live.js into django site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
