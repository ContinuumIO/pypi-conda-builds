package:
  name: django-sr
  version: "0.0.4"

source:
  fn: django-sr-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sr/django-sr-0.0.4.tar.gz
  md5: 1a11c2181de2140b46cc491ca68d4e3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sr = django-sr:main
    #
    # Would create an entry point called django-sr that calls django-sr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0

  run:
    - python
    - django >=1.3.0

test:
  # Python imports
  imports:
    - sr
    - sr.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jespino/django-sr
  license: BSD License
  summary: 'Django settings resolver.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
