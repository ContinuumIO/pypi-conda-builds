package:
  name: free-tele
  version: "0.2.1"

source:
  fn: Free-Tele-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Free-Tele/Free-Tele-0.2.1.tar.gz
  md5: 2e3f59bc7a39d6c8732b93dfd4702c82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - free-tele = free-tele:main
    #
    # Would create an entry point called free-tele that calls free-tele.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wxpython
    - beautifulsoup4

  run:
    - python
    - wxpython
    - beautifulsoup4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kaveensblog.wordpress.com
  license: GNU General Public License v3 (GPLv3)
  summary: 'Search, Download Tv episodes via Torrent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
