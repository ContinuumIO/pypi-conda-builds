package:
  name: django-dynamicsites-lite
  version: "0.1"

source:
  fn: django-dynamicsites-lite-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamicsites-lite/django-dynamicsites-lite-0.1.tar.gz
  md5: d3a3f329951fd74255c9f1366702c6ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamicsites-lite = django-dynamicsites-lite:main
    #
    # Would create an entry point called django-dynamicsites-lite that calls django-dynamicsites-lite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dynamicsiteslite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/wardi/django-dynamicsites-lite
  license: UNKNOWN
  summary: 'Host multiple sites from a single django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
