package:
  name: incuna-groups
  version: "0.4.0"

source:
  fn: incuna-groups-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/i/incuna-groups/incuna-groups-0.4.0.tar.gz
  md5: bdc47ecddf84ddd017e041070bcf6130
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incuna-groups = incuna-groups:main
    #
    # Would create an entry point called incuna-groups that calls incuna-groups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_crispy_forms ==1.4.0
    - django-polymorphic ==0.6.1

  run:
    - python
    - django_crispy_forms ==1.4.0
    - django-polymorphic ==0.6.1

test:
  # Python imports
  imports:
    - groups
    - groups.migrations
    - groups.templatetags
    - groups.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/incuna-groups
  license: UNKNOWN
  summary: 'Generic group/forum framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
