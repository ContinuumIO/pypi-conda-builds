package:
  name: setuptools-yaml
  version: "0.4"

source:
  fn: setuptools-yaml-0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-yaml/setuptools-yaml-0.4.tar.gz
  md5: 4a58b0f3da76802676ad81f416182155
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-yaml = setuptools-yaml:main
    #
    # Would create an entry point called setuptools-yaml that calls setuptools-yaml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - setuptools
    - pyyaml

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sudarkoff/setuptools-yaml
  license: MIT
  summary: 'Use YAML file for your project metadata'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
