package:
  name: iiif-validator
  version: "1.0.0"

source:
  fn: iiif-validator-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/iiif-validator/iiif-validator-1.0.0.tar.gz
  md5: 2c83bafb26b456884d96b3b84f14c938
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iiif-validator = iiif-validator:main
    #
    # Would create an entry point called iiif-validator that calls iiif-validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - bottle
    - python-magic
    - lxml

  run:
    - python
    - pillow
    - bottle
    - python-magic
    - lxml

test:
  # Python imports
  imports:
    - iiif_validator
    - iiif_validator.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/IIIF/image-api
  license: UNKNOWN
  summary: 'IIIF Image API Validator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
