package:
  name: pytest-pythonpath
  version: "0.6"

source:
  fn: pytest-pythonpath-0.6.tar.gz
  url: https://pypi.python.org/packages/source/p/pytest-pythonpath/pytest-pythonpath-0.6.tar.gz
  md5: 4acd0b5e06fa5c00ca7ff627f7f7081b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pytest-pythonpath = pytest-pythonpath:main
    #
    # Would create an entry point called pytest-pythonpath that calls pytest-pythonpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest >=2.5.2

  run:
    - python
    - setuptools
    - pytest >=2.5.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bigsassy/pytest-pythonpath
  license: UNKNOWN
  summary: 'pytest plugin for adding to the PYTHONPATH from command line or configs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
