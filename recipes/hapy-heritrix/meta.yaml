package:
  name: hapy-heritrix
  version: "0.1.1"

source:
  fn: hapy-heritrix-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hapy-heritrix/hapy-heritrix-0.1.1.tar.gz
  md5: 126764d8b91f63a3641d264872a1ae63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hapy-heritrix = hapy-heritrix:main
    #
    # Would create an entry point called hapy-heritrix that calls hapy-heritrix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - requests

  run:
    - python
    - nose
    - requests

test:
  # Python imports
  imports:
    - hapy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WilliamMayor/hapy
  license: UNKNOWN
  summary: 'A Python wrapper around the Heritrix API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
