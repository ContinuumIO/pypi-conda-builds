package:
  name: applpy
  version: "0.3.0"

source:
  fn: APPLPy-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/A/APPLPy/APPLPy-0.3.0.tar.gz
  md5: cc7b6529d910950877968ae20f61264f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - applpy = applpy:main
    #
    # Would create an entry point called applpy that calls applpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sympy >=0.7.0
    - scipy >=0.14.0
    - matplotlib >=1.3.1

  run:
    - python
    - sympy >=0.7.0
    - scipy >=0.14.0
    - matplotlib >=1.3.1

test:
  # Python imports
  imports:
    - applpy
    - applpy.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/APPLPy/
  license: LICENSE.txt
  summary: 'open source computational probability software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
