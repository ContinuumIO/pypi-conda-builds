package:
  name: datagrepper
  version: "0.6.0"

source:
  fn: datagrepper-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/datagrepper/datagrepper-0.6.0.tar.gz
  md5: c0aada7c37799015a07987c96bc7eb92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datagrepper = datagrepper:main
    #
    # Would create an entry point called datagrepper that calls datagrepper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - arrow
    - flask
    - datanommer.models >=0.6.0
    - fedmsg >=0.10.0
    - docutils
    - dogpile.cache
    - fedmsg_meta_fedora_infrastructure
    - pygal

  run:
    - python
    - arrow
    - flask
    - datanommer.models >=0.6.0
    - fedmsg >=0.10.0
    - docutils
    - dogpile.cache
    - fedmsg_meta_fedora_infrastructure
    - pygal

test:
  # Python imports
  imports:
    - datagrepper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fedora-infra/datagrepper/
  license: GPLv2+
  summary: 'A webapp to query fedmsg history'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
