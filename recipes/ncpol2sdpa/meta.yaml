package:
  name: ncpol2sdpa
  version: "1.7"

source:
  fn: ncpol2sdpa-1.7.tar.gz
  url: https://pypi.python.org/packages/source/n/ncpol2sdpa/ncpol2sdpa-1.7.tar.gz
  md5: 35505e0001b3f16351421c1c496526c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ncpol2sdpa = ncpol2sdpa:main
    #
    # Would create an entry point called ncpol2sdpa that calls ncpol2sdpa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sympy >=0.7.2
    - numpy

  run:
    - python
    - sympy >=0.7.2
    - numpy

test:
  # Python imports
  imports:
    - ncpol2sdpa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://peterwittek.github.io/ncpol2sdpa/
  license: GNU General Public License v3 (GPLv3)
  summary: 'A converter from polynomial optimization problems of                 noncommutative variables to sparse SDPA input format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
