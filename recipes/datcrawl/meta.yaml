package:
  name: datcrawl
  version: "0.3.0"

source:
  fn: datCrawl-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/datCrawl/datCrawl-0.3.0.tar.gz
  md5: 283b8a9d8f8dcff473d813a052293154
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datcrawl = datcrawl:main
    #
    # Would create an entry point called datcrawl that calls datcrawl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - datCrawl
    - datcrawl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fmartingr/datCrawl/
  license: GNU General Public License v2 (GPLv2)
  summary: 'Crawl as is you were using an API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
