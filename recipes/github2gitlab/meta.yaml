package:
  name: github2gitlab
  version: "1.5.0"

source:
  fn: github2gitlab-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/g/github2gitlab/github2gitlab-1.5.0.tar.gz
  md5: 233b3eb162b32fd20852862861614b34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github2gitlab = github2gitlab:main
    #
    # Would create an entry point called github2gitlab that calls github2gitlab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - github2gitlab

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'command line tool to migrate projects from GitHub to GitLab'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
