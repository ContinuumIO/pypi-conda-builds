package:
  name: django-filternaut
  version: "0.0.4"

source:
  fn: django-filternaut-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filternaut/django-filternaut-0.0.4.tar.gz
  md5: 7a883ced6347c2dddb3e248e59d50da2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filternaut = django-filternaut:main
    #
    # Would create an entry point called django-filternaut that calls django-filternaut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.9.0

  run:
    - python
    - six ==1.9.0

test:
  # Python imports
  imports:
    - filternaut

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bennullgraham/django-filternaut/
  license: BSD License
  summary: 'Construct arbitrarily complex Django "Q" filters from flat data, such as query parameters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
