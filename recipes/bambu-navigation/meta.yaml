package:
  name: bambu-navigation
  version: "0.1"

source:
  fn: bambu-navigation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-navigation/bambu-navigation-0.1.tar.gz
  md5: c0813cacf6896765e2b93b5978c59d29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-navigation = bambu-navigation:main
    #
    # Would create an entry point called bambu-navigation that calls bambu-navigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bambu.navigation
    - bambu.navigation.management
    - bambu.navigation.management.commands
    - bambu.navigation.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-navigation
  license: UNKNOWN
  summary: 'A set of template tags and settings for creating automatically-built menus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
