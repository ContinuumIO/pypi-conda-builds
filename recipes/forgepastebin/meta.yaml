package:
  name: forgepastebin
  version: "0.3.0"

source:
  fn: ForgePastebin-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/ForgePastebin/ForgePastebin-0.3.0.tar.gz
  md5: a3332ce756aa8cca93f7621cafd36f99
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - forgepastebin = forgepastebin:main
    #
    # Would create an entry point called forgepastebin that calls forgepastebin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - forgepastebin
    - forgepastebin.controllers
    - forgepastebin.model
    - forgepastebin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sf.net/p/forgepastebin
  license: Apache 2.0
  summary: 'Pastebin plugin for the SourceForge Allura platform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
