package:
  name: django-model-utils
  version: "2.2"

source:
  fn: django-model-utils-2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-utils/django-model-utils-2.2.tar.gz
  md5: c81cb247d00a1afc89ab50475fea47ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-utils = django-model-utils:main
    #
    # Would create an entry point called django-model-utils that calls django-model-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - model_utils
    - model_utils.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carljm/django-model-utils/
  license: BSD License
  summary: 'Django model mixins and utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
