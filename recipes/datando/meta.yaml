package:
  name: datando
  version: "0.5"

source:
  fn: datando-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/datando/datando-0.5.tar.gz
  md5: f8781f167563e4087f574767dc199bfa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datando = datando:main
    #
    # Would create an entry point called datando that calls datando.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datando

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.diegognesi.it
  license: GPLv3
  summary: 'This library allows to convert between calendars and to manage datetime variables and intervals for a period of +/-5 billions of years and a precision of 1 microsecond.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
