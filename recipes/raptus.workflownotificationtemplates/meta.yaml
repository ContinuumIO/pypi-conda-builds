package:
  name: raptus.workflownotificationtemplates
  version: "1.0b1"

source:
  fn: raptus.workflownotificationtemplates-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.workflownotificationtemplates/raptus.workflownotificationtemplates-1.0b1.tar.gz
  md5: ef558c6b77d4a75e53af4d2b4c139958
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.workflownotificationtemplates = raptus.workflownotificationtemplates:main
    #
    # Would create an entry point called raptus.workflownotificationtemplates that calls raptus.workflownotificationtemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - raptus.workflownotification

  run:
    - python
    - setuptools
    - setuptools
    - raptus.workflownotification

test:
  # Python imports
  imports:
    - raptus
    - raptus.workflownotificationtemplates
    - raptus.workflownotificationtemplates.Extensions
    - raptus.workflownotificationtemplates.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://raptus.com
  license: GPL
  summary: 'Provides a configlet to write notification templates ttw'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
