package:
  name: django-kenny
  version: "0.1.0"

source:
  fn: django-kenny-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kenny/django-kenny-0.1.0.tar.gz
  md5: 0ec4419fd6893b229d6625feb8f7b72c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kenny = django-kenny:main
    #
    # Would create an entry point called django-kenny that calls django-kenny.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kenny
    - kenny.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'An extremely simple dynamic TemplateView for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
