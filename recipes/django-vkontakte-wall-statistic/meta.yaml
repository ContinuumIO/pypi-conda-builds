package:
  name: django-vkontakte-wall-statistic
  version: "0.7.2"

source:
  fn: django-vkontakte-wall-statistic-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-wall-statistic/django-vkontakte-wall-statistic-0.7.2.tar.gz
  md5: cbb68642715609788c6f51a40c8c5391
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-wall-statistic = django-vkontakte-wall-statistic:main
    #
    # Would create an entry point called django-vkontakte-wall-statistic that calls django-vkontakte-wall-statistic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-vkontakte-api >=0.4.0
    - django-vkontakte-wall >=0.7.2

  run:
    - python
    - django-vkontakte-api >=0.4.0
    - django-vkontakte-wall >=0.7.2

test:
  # Python imports
  imports:
    - vkontakte_wall_statistic
    - vkontakte_wall_statistic.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-vkontakte-wall-statistic
  license: BSD License
  summary: 'Django implementation for vkontakte API Wall Statistic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
