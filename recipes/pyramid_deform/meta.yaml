package:
  name: pyramid_deform
  version: "0.2"

source:
  fn: pyramid_deform-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_deform/pyramid_deform-0.2.tar.gz
  md5: cd2818f1b5f19062798dbb947a01497c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_deform = pyramid_deform:main
    #
    # Would create an entry point called pyramid_deform that calls pyramid_deform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - deform >=0.8.1

  run:
    - python
    - setuptools
    - pyramid
    - deform >=0.8.1

test:
  # Python imports
  imports:
    - pyramid_deform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://docs.pylonshq.com
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Bindings to the Deform form library for the Pyramid web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
