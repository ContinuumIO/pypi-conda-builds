package:
  name: aioauth-client
  version: "0.2.1"

source:
  fn: aioauth-client-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aioauth-client/aioauth-client-0.2.1.tar.gz
  md5: 368429f3ec6db703feab35184c9ad3be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aioauth-client = aioauth-client:main
    #
    # Would create an entry point called aioauth-client that calls aioauth-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aiohttp

  run:
    - python
    - aiohttp

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klen/aioauth-client
  license: BSD License
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
