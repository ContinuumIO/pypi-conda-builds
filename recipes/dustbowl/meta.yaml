package:
  name: dustbowl
  version: "1.0.1"

source:
  fn: Dustbowl-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Dustbowl/Dustbowl-1.0.1.tar.gz
  md5: 302d156ea761d5c3f1451c1f19cbc719
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dustbowl = dustbowl:main
    #
    # Would create an entry point called dustbowl that calls dustbowl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - colorama

  run:
    - python
    - setuptools
    - sqlalchemy
    - colorama

test:
  # Python imports
  imports:
    - dustbowl
    - dustbowl.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pacopablo.github.com/dustbowl
  license: MIT
  summary: 'Dustbowl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
