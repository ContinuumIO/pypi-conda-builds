package:
  name: arcpylogger
  version: "1.0.0"

source:
  fn: arcpylogger-1.0.0.zip
  url: https://pypi.python.org/packages/source/a/arcpylogger/arcpylogger-1.0.0.zip
  md5: 1cadaa8935a49cf46eb951726b8b9d05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arcpylogger = arcpylogger:main
    #
    # Would create an entry point called arcpylogger that calls arcpylogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - arcpylogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gisinc/ArcPyLogger
  license: MIT
  summary: 'Classes to define ArcPy message stream handlers and a helper function for us noobs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
