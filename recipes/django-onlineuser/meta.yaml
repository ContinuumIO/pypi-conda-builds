package:
  name: django-onlineuser
  version: "0.8.7"

source:
  fn: django-onlineuser-0.8.7.zip
  url: https://pypi.python.org/packages/source/d/django-onlineuser/django-onlineuser-0.8.7.zip
  md5: d10f13bd75ed4f341db4888a2290bac9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-onlineuser = django-onlineuser:main
    #
    # Would create an entry point called django-onlineuser that calls django-onlineuser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south >=0.7.2

  run:
    - python
    - south >=0.7.2

test:
  # Python imports
  imports:
    - onlineuser
    - onlineuser.management
    - onlineuser.management.commands
    - onlineuser.migrations
    - onlineuser.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vicalloy/onlineuser/
  license: BSD
  summary: 'A django app to count online users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
