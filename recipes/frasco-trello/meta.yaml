package:
  name: frasco-trello
  version: "0.2"

source:
  fn: frasco-trello-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-trello/frasco-trello-0.2.tar.gz
  md5: 21ad07b63cab8dd4549a58d1ab08f0a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-trello = frasco-trello:main
    #
    # Would create an entry point called frasco-trello that calls frasco-trello.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco-users
    - python-dateutil ==2.4.0
    - requests_oauthlib

  run:
    - python
    - frasco-users
    - python-dateutil ==2.4.0
    - requests_oauthlib

test:
  # Python imports
  imports:
    - frasco_trello

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-trello
  license: MIT
  summary: 'Trello integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
