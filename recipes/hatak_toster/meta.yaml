package:
  name: hatak_toster
  version: "0.2.1"

source:
  fn: Hatak_Toster-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hatak_Toster/Hatak_Toster-0.2.1.tar.gz
  md5: caad0980eb3fa9641266aecd247bc40c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hatak_toster = hatak_toster:main
    #
    # Would create an entry point called hatak_toster that calls hatak_toster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hatak >=0.2.3
    - toster
    - hatak_sql
    - formskit >=0.5.0

  run:
    - python
    - hatak >=0.2.3
    - toster
    - hatak_sql
    - formskit >=0.5.0

test:
  # Python imports
  imports:
    - haplugin
    - haplugin.toster
    - hatak_toster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'Toster plugin for Hatak.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
