package:
  name: flask-esclient
  version: "0.1.1"

source:
  fn: Flask-ESClient-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-ESClient/Flask-ESClient-0.1.1.tar.gz
  md5: 298ae09b0ef526f3cf1e937e5ec5e769
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-esclient = flask-esclient:main
    #
    # Would create an entry point called flask-esclient that calls flask-esclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - esclient

  run:
    - python
    - setuptools
    - flask
    - esclient

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/baijum/flask-esclient
  license: BSD License
  summary: 'Flask extension for ESClient (elasticsearch client)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
