package:
  name: products.simplereference
  version: "0.1"

source:
  fn: Products.SimpleReference-0.1.zip
  url: https://pypi.python.org/packages/source/P/Products.SimpleReference/Products.SimpleReference-0.1.zip
  md5: 9ed752ba5ce3e71c91adac058bb17dcd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.simplereference = products.simplereference:main
    #
    # Would create an entry point called products.simplereference that calls products.simplereference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.richdocument

  run:
    - python
    - setuptools
    - setuptools
    - products.richdocument

test:
  # Python imports
  imports:
    - Products
    - Products.SimpleReference
    - Products.SimpleReference.browser
    - Products.SimpleReference.content
    - Products.SimpleReference.interfaces
    - Products.SimpleReference.portlets
    - Products.SimpleReference.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License (GPL)
  summary: 'SimpleReference Content Type for RichDocument to enable Image/File referencing support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
