package:
  name: django-generic-mail
  version: "0.2.3"

source:
  fn: django-generic-mail-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-mail/django-generic-mail-0.2.3.tar.gz
  md5: 466fa8b119b55f8d30ebd213076b9614
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-mail = django-generic-mail:main
    #
    # Would create an entry point called django-generic-mail that calls django-generic-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.2
    - markdown

  run:
    - python
    - django >=1.2
    - markdown

test:
  # Python imports
  imports:
    - generic_mail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevinrenskers/django-generic-mail
  license: BSD License
  summary: 'Easy to use, class based email for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
