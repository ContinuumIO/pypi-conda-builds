package:
  name: django-jenkins
  version: "0.17.0"

source:
  fn: django-jenkins-0.17.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jenkins/django-jenkins-0.17.0.tar.gz
  md5: 27a7f19482c5e916d362f934ff338ac2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jenkins = django-jenkins:main
    #
    # Would create an entry point called django-jenkins that calls django-jenkins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - django_jenkins
    - django_jenkins.management
    - django_jenkins.management.commands
    - django_jenkins.tasks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kmmbvnr/django-jenkins
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Plug and play continuous integration with django and jenkins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
