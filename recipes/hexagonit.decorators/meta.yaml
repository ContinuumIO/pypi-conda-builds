package:
  name: hexagonit.decorators
  version: "0.2"

source:
  fn: hexagonit.decorators-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hexagonit.decorators/hexagonit.decorators-0.2.tar.gz
  md5: 047cc5e3bd82bc592b900e0dedb334ac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hexagonit.decorators = hexagonit.decorators:main
    #
    # Would create an entry point called hexagonit.decorators that calls hexagonit.decorators.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - hexagonit
    - hexagonit.decorators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hexagonit.decorators
  license: GNU General Public License (GPL)
  summary: 'Collection of useful decorators for Zope development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
