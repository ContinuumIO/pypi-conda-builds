package:
  name: django-ajax-validation
  version: "0.1.3"

source:
  fn: django-ajax-validation-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-validation/django-ajax-validation-0.1.3.tar.gz
  md5: 96ee5c948c21c3cdc1984d42b3120906
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-validation = django-ajax-validation:main
    #
    # Would create an entry point called django-ajax-validation that calls django-ajax-validation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajax_validation
    - ajax_validation.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alex/django-ajax-validation/tree/master
  license: BSD License
  summary: 'Provides support for doing validation using Ajax(currently with jQuery) using your existing Django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
