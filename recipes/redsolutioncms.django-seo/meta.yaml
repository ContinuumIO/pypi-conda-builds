package:
  name: redsolutioncms.django-seo
  version: "0.3.5"

source:
  fn: redsolutioncms.django-seo-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-seo/redsolutioncms.django-seo-0.3.5.tar.gz
  md5: d5963c1395972c1209cc709c2cf4f7da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-seo = redsolutioncms.django-seo:main
    #
    # Would create an entry point called redsolutioncms.django-seo that calls redsolutioncms.django-seo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - seo
    - seo.migrations
    - seo.redsolution_setup
    - seo.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Adds generic SEO fields for objects in your site or specific urls'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
