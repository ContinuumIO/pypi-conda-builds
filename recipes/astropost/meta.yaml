package:
  name: astropost
  version: "0.1"

source:
  fn: astropost-0.1.zip
  url: https://pypi.python.org/packages/source/a/astropost/astropost-0.1.zip
  md5: b1cfe4f11e7b60fb343411f370096c47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astropost = astropost:main
    #
    # Would create an entry point called astropost that calls astropost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mechanize
    - faker

  run:
    - python
    - mechanize
    - faker

test:
  # Python imports
  imports:
    - astropost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/murodese/astropost
  license: MIT License
  summary: 'Commenting API for Australian news sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
