package:
  name: goescalibration
  version: "0.0.6"

source:
  fn: goescalibration-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/g/goescalibration/goescalibration-0.0.6.tar.gz
  md5: 42ce1f0bb71545489ae3bb7a5db049c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goescalibration = goescalibration:main
    #
    # Would create an entry point called goescalibration that calls goescalibration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent ==1.0.1
    - requests ==2.3.0
    - grequests ==0.2.0
    - beautifulsoup4 ==4.3.2
    - pytz ==2014.4
    - netcdf ==0.0.32

  run:
    - python
    - gevent ==1.0.1
    - requests ==2.3.0
    - grequests ==0.2.0
    - beautifulsoup4 ==4.3.2
    - pytz ==2014.4
    - netcdf ==0.0.32

test:
  # Python imports
  imports:
    - goescalibration
    - goescalibration.channels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gersolar/goescalibration
  license: MIT
  summary: 'A python library that allow to calibrate netcdf images of the NOAA CLASS (Comprehensive Large Array-Data Stewardship System).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
