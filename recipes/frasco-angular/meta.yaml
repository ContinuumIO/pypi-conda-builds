package:
  name: frasco-angular
  version: "0.1.4"

source:
  fn: frasco-angular-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-angular/frasco-angular-0.1.4.tar.gz
  md5: 6203880ae152639474ed4f3841b2a73c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-angular = frasco-angular:main
    #
    # Would create an entry point called frasco-angular that calls frasco-angular.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - frasco-assets

  run:
    - python
    - frasco
    - frasco-assets

test:
  # Python imports
  imports:
    - frasco_angular

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-angular
  license: MIT
  summary: 'Angular integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
