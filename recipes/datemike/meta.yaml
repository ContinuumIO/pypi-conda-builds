package:
  name: datemike
  version: "0.1.1"

source:
  fn: datemike-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/datemike/datemike-0.1.1.tar.gz
  md5: 19bfe556f0f3216f5bd4700a032c323e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datemike = datemike:main
    #
    # Would create an entry point called datemike that calls datemike.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - datemike

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/angstwad/datemike
  license: Apache v2.0
  summary: 'Create Ansible tasks, plays, and playbooks in pure Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
