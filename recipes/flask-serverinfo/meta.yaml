package:
  name: flask-serverinfo
  version: "0.1.1"

source:
  fn: Flask-ServerInfo-0.1.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-ServerInfo/Flask-ServerInfo-0.1.1.zip
  md5: 714f91640a828138d34dbdc54632b987
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-serverinfo = flask-serverinfo:main
    #
    # Would create an entry point called flask-serverinfo that calls flask-serverinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.5

  run:
    - python
    - flask >=0.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/salsita/flask-serverinfo
  license: MIT License
  summary: 'Flask server info view for inspecting server app and user requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
