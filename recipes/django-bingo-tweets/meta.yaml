package:
  name: django-bingo-tweets
  version: "1.0.1"

source:
  fn: django-bingo-tweets-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bingo-tweets/django-bingo-tweets-1.0.1.tar.gz
  md5: 412a62a5cb0f15934c21b2eedd5383e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bingo-tweets = django-bingo-tweets:main
    #
    # Would create an entry point called django-bingo-tweets that calls django-bingo-tweets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-bingo
    - tweepy

  run:
    - python
    - django-bingo
    - tweepy

test:
  # Python imports
  imports:
    - bingo_tweets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allo-/django-bingo-tweet
  license: GNU Affero General Public License v3
  summary: 'Bingo Tweets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
