package:
  name: south_mysqlndb
  version: "0.1"

source:
  fn: south_mysqlndb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/south_mysqlndb/south_mysqlndb-0.1.tar.gz
  md5: 89a4fb913c67ae98e44d2dd762bc6ed2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - south_mysqlndb = south_mysqlndb:main
    #
    # Would create an entry point called south_mysqlndb that calls south_mysqlndb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - south_mysqlndb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jimmykobe1171/south-mysqlndb
  license: MIT
  summary: 'south database adapter for mysqlndb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
