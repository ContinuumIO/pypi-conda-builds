package:
  name: appomatic_django_vcs
  version: "0.0.30"

source:
  fn: appomatic_django_vcs-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_django_vcs/appomatic_django_vcs-0.0.30.tar.gz
  md5: bfd151049b60f71da6c5184667ab5718
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_django_vcs = appomatic_django_vcs:main
    #
    # Would create an entry point called appomatic_django_vcs that calls appomatic_django_vcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_vcs
    - dulwich
    - pygments
    - appomaticcore ==0.0.30

  run:
    - python
    - django_vcs
    - dulwich
    - pygments
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_django_vcs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-vcs/
  license: GPL
  summary: 'A way to integrate VCS-agnostic repository browsing into Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
