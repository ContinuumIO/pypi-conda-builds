package:
  name: django-celery-model
  version: "0.1"

source:
  fn: django-celery-model-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery-model/django-celery-model-0.1.tar.gz
  md5: 386f0190030d57b2eaf144c3ce7c1c3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery-model = django-celery-model:main
    #
    # Would create an entry point called django-celery-model that calls django-celery-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=3.1.10

  run:
    - python
    - celery >=3.1.10

test:
  # Python imports
  imports:
    - djcelery_model
    - djcelery_model.migrations
    - djcelery_model.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mback2k/django-celery-model
  license: MIT License
  summary: u'django-celery-model is an extension to Celery and django-celery which adds support for tracking Celery tasks assigned to Django model instances.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
