package:
  name: bt-manager
  version: "0.3.0"

source:
  fn: BT-Manager-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BT-Manager/BT-Manager-0.3.0.tar.gz
  md5: e808054ca2d7015fb22ef7e67b585ddc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bt-manager = bt-manager:main
    #
    # Would create an entry point called bt-manager that calls bt-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi >=0.7

  run:
    - python
    - setuptools
    - cffi >=0.7

test:
  # Python imports
  imports:
    - bt_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/liamw9534/bt-manager
  license: Apache Software License
  summary: u'Python-based Bluetooth Device Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
