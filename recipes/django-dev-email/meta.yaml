package:
  name: django-dev-email
  version: "1.0.2"

source:
  fn: django-dev-email-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dev-email/django-dev-email-1.0.2.tar.gz
  md5: d3bb4a86fc996534d7164d36a97ddd86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dev-email = django-dev-email:main
    #
    # Would create an entry point called django-dev-email that calls django-dev-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dev_email
    - dev_email.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstegelman/django-dev-email
  license: MIT License
  summary: 'Send all mail to a desired address during development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
