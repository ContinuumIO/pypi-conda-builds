package:
  name: django-bookmarker
  version: "0.1.0"

source:
  fn: django-bookmarker-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bookmarker/django-bookmarker-0.1.0.tar.gz
  md5: 9776a19e3150945525026c5f94fcaebf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bookmarker = django-bookmarker:main
    #
    # Would create an entry point called django-bookmarker that calls django-bookmarker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bookmarker
    - bookmarker.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/django-bookmarker
  license: BSD License
  summary: 'Social bookmark linsk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
