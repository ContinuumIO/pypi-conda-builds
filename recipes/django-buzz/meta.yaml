package:
  name: django-buzz
  version: "0.4b"

source:
  fn: django-buzz-0.4b.tar.gz
  url: https://pypi.python.org/packages/source/d/django-buzz/django-buzz-0.4b.tar.gz
  md5: efe093a2df3bcd548f2046bb95d42111
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-buzz = django-buzz:main
    #
    # Would create an entry point called django-buzz that calls django-buzz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pystories
    - pystories.migrations
    - pystories.services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/agiliq/django-buzz
  license: UNKNOWN
  summary: 'Django app to find What is hot and buzzing in a topic.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
