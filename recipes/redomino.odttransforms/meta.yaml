package:
  name: redomino.odttransforms
  version: "0.4"

source:
  fn: redomino.odttransforms-0.4.zip
  url: https://pypi.python.org/packages/source/r/redomino.odttransforms/redomino.odttransforms-0.4.zip
  md5: 3b4ceed6502487d687636f0f86eef5d4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.odttransforms = redomino.odttransforms:main
    #
    # Would create an entry point called redomino.odttransforms that calls redomino.odttransforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ooopy

  run:
    - python
    - setuptools
    - setuptools
    - ooopy

test:
  # Python imports
  imports:
    - redomino
    - redomino.odttransforms
    - redomino.odttransforms.tests
    - redomino.odttransforms.transforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.odttransforms
  license: GNU General Public License (GPL)
  summary: 'ODT templating transforms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
