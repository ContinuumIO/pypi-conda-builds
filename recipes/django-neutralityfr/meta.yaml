package:
  name: django-neutralityfr
  version: "0.1.2"

source:
  fn: django-neutralityFR-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-neutralityFR/django-neutralityFR-0.1.2.tar.gz
  md5: 6adb768d8d9f9d8029fe23ffeebbad48
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-neutralityfr = django-neutralityfr:main
    #
    # Would create an entry point called django-neutralityfr that calls django-neutralityfr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - neutralityFR
    - neutralityFR.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberj/django-neutralityFR
  license: MIT License
  summary: 'Middleware Django anti-netneutrality to give an example to some people.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
