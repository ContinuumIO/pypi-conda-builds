package:
  name: django-fanout
  version: "1.1.1"

source:
  fn: django-fanout-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fanout/django-fanout-1.1.1.tar.gz
  md5: e646c4823e7870448d9b873f4efad456
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fanout = django-fanout:main
    #
    # Would create an entry point called django-fanout that calls django-fanout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanout >=1.1.1

  run:
    - python
    - fanout >=1.1.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fanout/django-fanout
  license: MIT License
  summary: 'Fanout.io library for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
