package:
  name: django-tsearch2
  version: "0.3.0"

source:
  fn: django-tsearch2-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tsearch2/django-tsearch2-0.3.0.tar.gz
  md5: 6789f4fb06ae8d8d3542b4e6048c540d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tsearch2 = django-tsearch2:main
    #
    # Would create an entry point called django-tsearch2 that calls django-tsearch2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tsearch2
    - tsearch2.management
    - tsearch2.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hcarvalhoalves/django-tsearch2
  license: BSD License
  summary: "Postgresql's full text search support for Django"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
