package:
  name: django-setmagic
  version: "0.3"

source:
  fn: django-setmagic-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-setmagic/django-setmagic-0.3.tar.gz
  md5: b9775055bf7a705ccac2120409d7fb3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-setmagic = django-setmagic:main
    #
    # Would create an entry point called django-setmagic that calls django-setmagic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - setmagic
    - setmagic.migrations
    - setmagic.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/7ws/django-setmagic
  license: MIT License
  summary: 'Magically editable settings for winged pony lovers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
