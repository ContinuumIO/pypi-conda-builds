package:
  name: collective.viewlet.pythonscript
  version: "0.1"

source:
  fn: collective.viewlet.pythonscript-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.viewlet.pythonscript/collective.viewlet.pythonscript-0.1.zip
  md5: 16c480c6060e359c6b5bc1f0e4119465
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.viewlet.pythonscript = collective.viewlet.pythonscript:main
    #
    # Would create an entry point called collective.viewlet.pythonscript that calls collective.viewlet.pythonscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfcore
    - products.cmfplone >=4.1
    - products.genericsetup
    - products.statusmessages
    - archetypes.schemaextender
    - collective.portlet.pythonscript
    - setuptools
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - products.cmfcore
    - products.cmfplone >=4.1
    - products.genericsetup
    - products.statusmessages
    - archetypes.schemaextender
    - collective.portlet.pythonscript
    - setuptools
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.viewlet
    - collective.viewlet.pythonscript
    - collective.viewlet.pythonscript.browser
    - collective.viewlet.pythonscript.schema
    - collective.viewlet.pythonscript.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.viewlet.pythonscript
  license: GNU General Public License v2 (GPLv2)
  summary: 'Viewlet rendering collection of items returned by customizable Python scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
