package:
  name: zeam.form.rdb
  version: "1.0.1"

source:
  fn: zeam.form.rdb-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zeam.form.rdb/zeam.form.rdb-1.0.1.tar.gz
  md5: 5f8f303c822befc04c594120924988e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeam.form.rdb = zeam.form.rdb:main
    #
    # Would create an entry point called zeam.form.rdb that calls zeam.form.rdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grokcore.component
    - setuptools
    - sqlalchemy
    - z3c.saconfig
    - zeam.form.base >=1.0
    - zeam.form.ztk
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - grokcore.component
    - setuptools
    - sqlalchemy
    - z3c.saconfig
    - zeam.form.base >=1.0
    - zeam.form.ztk
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - zeam
    - zeam.form
    - zeam.form.rdb
    - zeam.form.rdb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zeam.form.rdb
  license: BSD
  summary: 'Extra SQLAlchemy support for zeam.form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
