package:
  name: django-xforwardedfor-middleware
  version: "1.0"

source:
  fn: django-xforwardedfor-middleware-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xforwardedfor-middleware/django-xforwardedfor-middleware-1.0.tar.gz
  md5: 9132e555e58762f6039e5cec4feeeb39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xforwardedfor-middleware = django-xforwardedfor-middleware:main
    #
    # Would create an entry point called django-xforwardedfor-middleware that calls django-xforwardedfor-middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - x_forwarded_for

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allo-/django-xforwardedfor-middleware
  license: UNKNOWN
  summary: 'Django X-Forwarded-For Middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
