package:
  name: authform_middleware
  version: "0.2.3"

source:
  fn: authform_middleware-0.2.3.zip
  url: https://pypi.python.org/packages/source/a/authform_middleware/authform_middleware-0.2.3.zip
  md5: e6668696671364dfb4053e9e988952eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authform_middleware = authform_middleware:main
    #
    # Would create an entry point called authform_middleware that calls authform_middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - paste >=1.4
    - beaker >=0.7.3

  run:
    - python
    - distribute
    - paste >=1.4
    - beaker >=0.7.3

test:
  # Python imports
  imports:
    - authform_middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/Dalius/authform-middleware/
  license: MIT
  summary: 'Form authentication middleware for WSGI applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
