package:
  name: pumpkin
  version: "0.1.10"

source:
  fn: pumpkin-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/p/pumpkin/pumpkin-0.1.10.tar.gz
  md5: 02606d59451429de3a1490f77308c0c1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pumpkin = pumpkin:main
    #
    # Would create an entry point called pumpkin that calls pumpkin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap
    - python-dateutil

  run:
    - python
    - setuptools
    - python-ldap
    - python-dateutil

test:
  # Python imports
  imports:
    - pumpkin
    - pumpkin.contrib
    - pumpkin.contrib.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3
  summary: 'Simple library for working with ldap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
