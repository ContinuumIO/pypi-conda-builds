package:
  name: junos-netconify
  version: "1.0.0"

source:
  fn: junos-netconify-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/junos-netconify/junos-netconify-1.0.0.tar.gz
  md5: 718603dcbd9c6f5b0679e3904fa08f6b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - junos-netconify = junos-netconify:main
    #
    # Would create an entry point called junos-netconify that calls junos-netconify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial
    - lxml

  run:
    - python
    - pyserial
    - lxml

test:
  # Python imports
  imports:
    - netconify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/Juniper/py-junos-netconify
  license: Apache Software License
  summary: 'Junos console/bootstrap automation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
