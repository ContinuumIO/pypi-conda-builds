package:
  name: html5forms
  version: "0.0.3"

source:
  fn: html5forms-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/html5forms/html5forms-0.0.3.tar.gz
  md5: 07b33db8240b3764d40b100f3ee984f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html5forms = html5forms:main
    #
    # Would create an entry point called html5forms that calls html5forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - html5forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamcupial/django-html5-forms
  license: MIT License
  summary: 'HTML5 forms for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
