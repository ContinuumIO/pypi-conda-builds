package:
  name: djangoproj
  version: "0.3"

source:
  fn: djangoproj-0.3.zip
  url: https://pypi.python.org/packages/source/d/djangoproj/djangoproj-0.3.zip
  md5: 72eed4eae545d9359124c27afd9b2098
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoproj = djangoproj:main
    #
    # Would create an entry point called djangoproj that calls djangoproj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-1.6.5
    - django-bootstrap-toolkit-2.15.0

  run:
    - python
    - django-1.6.5
    - django-bootstrap-toolkit-2.15.0

test:
  # Python imports
  imports:
    - djangoproj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'mencoba'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
