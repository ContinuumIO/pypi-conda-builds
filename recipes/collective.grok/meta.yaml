package:
  name: collective.grok
  version: "1.0a2"

source:
  fn: collective.grok-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.grok/collective.grok-1.0a2.tar.gz
  md5: 7261464f37c72844aab0ab66a994f065
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.grok = collective.grok:main
    #
    # Would create an entry point called collective.grok that calls collective.grok.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok >=1.2

  run:
    - python
    - setuptools
    - setuptools
    - five.grok >=1.2

test:
  # Python imports
  imports:
    - collective
    - collective.grok
    - collective.grok.gs
    - collective.grok.i18n
    - collective.grok.tests
    - collective.grok.tests.gs
    - collective.grok.tests.i18n

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br
  license: GNU General Public License (GPL)
  summary: 'Configuring and registering Plone components, w/o ZCML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
