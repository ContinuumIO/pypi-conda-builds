package:
  name: flask-memcache-session
  version: "2.0"

source:
  fn: flask-memcache-session-2.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-memcache-session/flask-memcache-session-2.0.tar.gz
  md5: 62f92774d63a4478c25e0b7831910ea1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-memcache-session = flask-memcache-session:main
    #
    # Would create an entry point called flask-memcache-session that calls flask-memcache-session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flask_memcache_session

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unk2k/Flask-memcache-session
  license: BSD License
  summary: 'Use memcache for store session data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
