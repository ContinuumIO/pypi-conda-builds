package:
  name: django-donottrack
  version: "0.1"

source:
  fn: django-donottrack-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-donottrack/django-donottrack-0.1.tar.gz
  md5: e84ee9945a5e2c575549f770f162dfcf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-donottrack = django-donottrack:main
    #
    # Would create an entry point called django-donottrack that calls django-donottrack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - donottrack
    - donottrack.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benspaulding/django-donottrack/
  license: BSD License
  summary: 'Django utilities for honoring the Do Not Track HTTP header.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
