package:
  name: flask-orientdb
  version: "0.1"

source:
  fn: Flask-OrientDB-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-OrientDB/Flask-OrientDB-0.1.tar.gz
  md5: cc16285d02d0ba02ab90511ac1fcbc49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-orientdb = flask-orientdb:main
    #
    # Would create an entry point called flask-orientdb that calls flask-orientdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pyorient ==1.4

  run:
    - python
    - flask
    - pyorient ==1.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/calthoff/flask-orientdb
  license: BSD License
  summary: 'A Flask extension for using OrientDB with Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
