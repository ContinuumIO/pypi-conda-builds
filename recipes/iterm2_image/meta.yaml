package:
  name: iterm2_image
  version: "0.1"

source:
  fn: iterm2_image-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iterm2_image/iterm2_image-0.1.tar.gz
  md5: 033a0f094eb75a0ee1a490b42298fe93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iterm2_image = iterm2_image:main
    #
    # Would create an entry point called iterm2_image that calls iterm2_image.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iterm2_image

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zakx/iterm2_image
  license: UNKNOWN
  summary: 'Prints images and sends files using the iTerm2 image extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
