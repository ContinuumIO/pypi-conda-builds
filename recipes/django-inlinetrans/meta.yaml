package:
  name: django-inlinetrans
  version: "0.6.0"

source:
  fn: django-inlinetrans-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inlinetrans/django-inlinetrans-0.6.0.tar.gz
  md5: f729527eedc067f6b29c822505f0a3bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inlinetrans = django-inlinetrans:main
    #
    # Would create an entry point called django-inlinetrans that calls django-inlinetrans.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - polib ==1.0.3

  run:
    - python
    - polib ==1.0.3

test:
  # Python imports
  imports:
    - inlinetrans
    - inlinetrans.management
    - inlinetrans.management.commands
    - inlinetrans.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yaco-Sistemas/django-inlinetrans
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Is a django application that allows the translation of django templtes from the rendered html in the browser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
