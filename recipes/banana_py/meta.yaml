package:
  name: banana_py
  version: "0.1.2"

source:
  fn: banana_py-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/banana_py/banana_py-0.1.2.tar.gz
  md5: a2610e0d3676e2424b6c2e4302bdaadd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - banana_py = banana_py:main
    #
    # Would create an entry point called banana_py that calls banana_py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2
    - simplejson
    - django

  run:
    - python
    - oauth2
    - simplejson
    - django

test:
  # Python imports
  imports:
    - banana_py

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennethlove/Banana-Py/
  license: BSD
  summary: 'OAuth2 Backend for MailChimp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
