package:
  name: mongodbpipe
  version: "0.1.0"

source:
  fn: MongoDBPipe-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/M/MongoDBPipe/MongoDBPipe-0.1.0.tar.gz
  md5: f297b9a369545e85eb74a4a0a6f28b31
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongodbpipe = mongodbpipe:main
    #
    # Would create an entry point called mongodbpipe that calls mongodbpipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - mongodbpipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/mongodbpipe/index.html
  license: GNU Affero General Public License v3
  summary: 'A pipe to connect MongoDB to a PipeStack application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
