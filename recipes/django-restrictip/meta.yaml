package:
  name: django-restrictip
  version: "1.0.1"

source:
  fn: django-restrictip-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restrictip/django-restrictip-1.0.1.tar.gz
  md5: 523be6b462821670f9f8b333a6a41d1d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restrictip = django-restrictip:main
    #
    # Would create an entry point called django-restrictip that calls django-restrictip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - restrictip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pitcons/django-restrictip
  license: GNU General Public License (GPL)
  summary: 'An ip blocker by regexp and ip range for Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
