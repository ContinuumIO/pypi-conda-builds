package:
  name: butterfly
  version: "1.5.10"

source:
  fn: butterfly-1.5.10.tar.gz
  url: https://pypi.python.org/packages/source/b/butterfly/butterfly-1.5.10.tar.gz
  md5: 2dcc4b10f7dab8d9df951796b4630563
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - butterfly = butterfly:main
    #
    # Would create an entry point called butterfly that calls butterfly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.2
    - pyopenssl
    - tornado_systemd

  run:
    - python
    - tornado >=3.2
    - pyopenssl
    - tornado_systemd

test:
  # Python imports
  imports:
    - butterfly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/paradoxxxzero/butterfly
  license: GNU General Public License v3 (GPLv3)
  summary: 'A sleek web based terminal emulator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
