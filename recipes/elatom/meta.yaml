package:
  name: elatom
  version: "1.0.10"

source:
  fn: Elatom-1.0.10.tar.gz
  url: https://pypi.python.org/packages/source/E/Elatom/Elatom-1.0.10.tar.gz
  md5: d32ca436b981e74fa7ce71e773632d7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elatom = elatom:main
    #
    # Would create an entry point called elatom that calls elatom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - elatom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/elatom
  license: GNU General Public License (GPL)
  summary: 'Podcast downloader in Python 3 with Curses and Tkinter interfaces.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
