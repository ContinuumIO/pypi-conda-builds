package:
  name: topp.featurelets
  version: "0.3.0"

source:
  fn: topp.featurelets-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/t/topp.featurelets/topp.featurelets-0.3.0.tar.gz
  md5: 9ec2c494158fb19d2262ba12834bf117
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - topp.featurelets = topp.featurelets:main
    #
    # Would create an entry point called topp.featurelets that calls topp.featurelets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - topp.utils >=0.5

  run:
    - python
    - setuptools
    - topp.utils >=0.5

test:
  # Python imports
  imports:
    - topp
    - topp.featurelets
    - topp.featurelets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.openplans.org/svn/topp.featurelets/
  license: GNU General Public License (GPL)
  summary: 'Base support for plone featurelets (i.e. feature plugins)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
