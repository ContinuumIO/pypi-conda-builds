package:
  name: django-profile
  version: "0.0.3"

source:
  fn: django-profile-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-profile/django-profile-0.0.3.tar.gz
  md5: 729982801ad1392bcf776ac4e66269b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-profile = django-profile:main
    #
    # Would create an entry point called django-profile that calls django-profile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-photologue
    - django-registration

  run:
    - python
    - django-photologue
    - django-registration

test:
  # Python imports
  imports:
    - profile
    - profile.backends
    - profile.backends.simple
    - profile.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-profile
  license: BSD License
  summary: 'Django user profile app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
