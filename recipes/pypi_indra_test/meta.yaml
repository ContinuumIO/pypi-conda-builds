package:
  name: pypi_indra_test
  version: "1.1.0"

source:
  fn: pypi_indra_test-1.1.0.zip
  url: https://pypi.python.org/packages/source/p/pypi_indra_test/pypi_indra_test-1.1.0.zip
  md5: a40fd29d13a8ddd66f235503286343b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypi_indra_test = pypi_indra_test:main
    #
    # Would create an entry point called pypi_indra_test that calls pypi_indra_test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pypi_indra_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.soddy.com
  license: UNKNOWN
  summary: 'Simple print shit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
