package:
  name: flask-sl
  version: "0.0.4"

source:
  fn: Flask-SL-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SL/Flask-SL-0.0.4.tar.gz
  md5: e59f54d09e6ecca294487b9167c77ac5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sl = flask-sl:main
    #
    # Would create an entry point called flask-sl that calls flask-sl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - netaddr

  run:
    - python
    - flask
    - netaddr

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nivardus/flask-sl
  license: BSD License
  summary: 'Basic recognition of Second Life\xc2\xae requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
