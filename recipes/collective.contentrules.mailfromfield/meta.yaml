package:
  name: collective.contentrules.mailfromfield
  version: "0.4.0"

source:
  fn: collective.contentrules.mailfromfield-0.4.0.zip
  url: https://pypi.python.org/packages/source/c/collective.contentrules.mailfromfield/collective.contentrules.mailfromfield-0.4.0.zip
  md5: de77d5d9b896891148d811a64a475b4e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.mailfromfield = collective.contentrules.mailfromfield:main
    #
    # Would create an entry point called collective.contentrules.mailfromfield that calls collective.contentrules.mailfromfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.contentrules

  run:
    - python
    - setuptools
    - setuptools
    - plone.contentrules

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.mailfromfield
    - collective.contentrules.mailfromfield.actions
    - collective.contentrules.mailfromfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.contentrules.mailfromfield
  license: GNU General Public License (GPL)
  summary: 'A Plone content rule for send e-mail to addresses taken from the content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
