package:
  name: collective.flowplayer_toolbar
  version: "1.0.0"

source:
  fn: collective.flowplayer_toolbar-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.flowplayer_toolbar/collective.flowplayer_toolbar-1.0.0.tar.gz
  md5: 89ccb88ee4a1cab5d74e61f101aa2bb6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.flowplayer_toolbar = collective.flowplayer_toolbar:main
    #
    # Would create an entry point called collective.flowplayer_toolbar that calls collective.flowplayer_toolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.flowplayer >3.0dev

  run:
    - python
    - setuptools
    - setuptools
    - collective.flowplayer >3.0dev

test:
  # Python imports
  imports:
    - collective
    - collective.flowplayer_toolbar
    - collective.flowplayer_toolbar.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.flowplayer_toolbar
  license: GPL
  summary: 'A Plone module which add an accessible Javascript controlsbar to collective.flowplayer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
