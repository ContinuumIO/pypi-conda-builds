package:
  name: django_keyval
  version: "0.1.2"

source:
  fn: django_keyval-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_keyval/django_keyval-0.1.2.tar.gz
  md5: dd4e3528bc50673df69060b741e59f86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_keyval = django_keyval:main
    #
    # Would create an entry point called django_keyval that calls django_keyval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1.1

  run:
    - python
    - django >=1.1.1

test:
  # Python imports
  imports:
    - django_keyval
    - django_keyval.management
    - django_keyval.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/EdwinvO/django_keyval
  license: LICENSE.txt
  summary: 'Simple django key-value store'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
