package:
  name: indexed.py
  version: "0.0.1"

source:
  fn: indexed.py-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/indexed.py/indexed.py-0.0.1.tar.gz
  md5: 47f0517b630018bed6dfe44ba50a9873
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indexed.py = indexed.py:main
    #
    # Would create an entry point called indexed.py that calls indexed.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - indexed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/niklasf/indexed.py
  license: GNU General Public License (GPL)
  summary: 'A dictionary that is indexed by insertion order.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
