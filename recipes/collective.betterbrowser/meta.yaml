package:
  name: collective.betterbrowser
  version: "0.4"

source:
  fn: collective.betterbrowser-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.betterbrowser/collective.betterbrowser-0.4.tar.gz
  md5: 9c008319104667762d835d3368c12bf3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.betterbrowser = collective.betterbrowser:main
    #
    # Would create an entry point called collective.betterbrowser that calls collective.betterbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.testing

  run:
    - python
    - setuptools
    - setuptools
    - plone.testing

test:
  # Python imports
  imports:
    - collective
    - collective.betterbrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.betterbrowser
  license: MIT
  summary: 'Inspect the contents of your plone.testing browser instance in a real browser (like Firefox), and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
