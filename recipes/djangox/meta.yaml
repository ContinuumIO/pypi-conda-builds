package:
  name: djangox
  version: "0.0.3"

source:
  fn: djangox-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangox/djangox-0.0.3.tar.gz
  md5: 67283ff9083e4dbe506b200c3adb7dc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangox = djangox:main
    #
    # Would create an entry point called djangox that calls djangox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangox
    - djangox.http
    - djangox.mako
    - djangox.route

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youngrok/djangox
  license: BSD License
  summary: 'Another way of using django. Convention over configuration for urls, mako template language support,json response, REST style API, convenient scripts, ...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
