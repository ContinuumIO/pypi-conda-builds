package:
  name: django-smart-extends
  version: "0.7.0"

source:
  fn: django-smart-extends-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smart-extends/django-smart-extends-0.7.0.tar.gz
  md5: 0ebb9b411e0b437da4f5ed938ccc8836
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smart-extends = django-smart-extends:main
    #
    # Would create an entry point called django-smart-extends that calls django-smart-extends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smartextends
    - smartextends.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goinnn/django-smart-extends
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'This application is useful when you want to overwrite a template of a application in your project. Currently this in Django produce infinite recursion '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
