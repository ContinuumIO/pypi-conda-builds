package:
  name: flask-conditional
  version: "0.1"

source:
  fn: flask-conditional-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-conditional/flask-conditional-0.1.tar.gz
  md5: 608ec860c179d5533afb7d1585a6ef2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-conditional = flask-conditional:main
    #
    # Would create an entry point called flask-conditional that calls flask-conditional.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alfg/flask-conditional
  license: MIT License
  summary: 'Conditional decorators for Flask routes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
