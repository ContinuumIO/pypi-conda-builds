package:
  name: django-redis-status
  version: "1.0.0"

source:
  fn: django-redis-status-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis-status/django-redis-status-1.0.0.tar.gz
  md5: d7ec3d7c458429179a53bb1cf7605966
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis-status = django-redis-status:main
    #
    # Would create an entry point called django-redis-status that calls django-redis-status.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_status
    - redis_status.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicholasserra/django-redis-status
  license: BSD License
  summary: 'A django application that displays some statistics about your redis instance in the admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
