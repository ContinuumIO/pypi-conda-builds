package:
  name: django-critic
  version: "0.2.4"

source:
  fn: django-critic-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-critic/django-critic-0.2.4.tar.gz
  md5: b02ba741c3bb4a374e14e55fa49d42d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-critic = django-critic:main
    #
    # Would create an entry point called django-critic that calls django-critic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - critic
    - critic.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/josesoa/django-critic
  license: Apache Software License
  summary: 'A rating application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
