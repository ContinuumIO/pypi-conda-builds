package:
  name: logprism
  version: "0.1.2"

source:
  fn: logprism-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/logprism/logprism-0.1.2.tar.gz
  md5: 2c7bc46577793b8277d21fdc04a99af5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logprism = logprism:main
    #
    # Would create an entry point called logprism that calls logprism.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - logprism
    - prism

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peterhil/prism
  license: BSD License
  summary: 'Prism \xe2\x80\x93 Colourise log files (with ANSI characters codes)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
