package:
  name: kryptomime
  version: "0.4.1"

source:
  fn: kryptomime-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kryptomime/kryptomime-0.4.1.tar.gz
  md5: d13bebc527f3d84e4c9c1df003e89396
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kryptomime = kryptomime:main
    #
    # Would create an entry point called kryptomime that calls kryptomime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gnupg >=1.3.2
    - six >=1.4.1

  run:
    - python
    - gnupg >=1.3.2
    - six >=1.4.1

test:
  # Python imports
  imports:
    - kryptomime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ttanner/kryptomime
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Python support for E-Mail kryptography'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
