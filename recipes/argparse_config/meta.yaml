package:
  name: argparse_config
  version: "0.5.1"

source:
  fn: argparse_config-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/a/argparse_config/argparse_config-0.5.1.tar.gz
  md5: 55907c9ee57fb6f42d0889d7d7e17afc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argparse_config = argparse_config:main
    #
    # Would create an entry point called argparse_config that calls argparse_config.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - argparse_config
    - argparse_config.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tikitu/argparse_config
  license: MIT License
  summary: 'Default values for argparse commandline args read from a config file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
