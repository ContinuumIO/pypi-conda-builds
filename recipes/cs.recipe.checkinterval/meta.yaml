package:
  name: cs.recipe.checkinterval
  version: "1.0"

source:
  fn: cs.recipe.checkinterval-1.0.zip
  url: https://pypi.python.org/packages/source/c/cs.recipe.checkinterval/cs.recipe.checkinterval-1.0.zip
  md5: 4a949caea88cb0962a03b77fac136558
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.recipe.checkinterval = cs.recipe.checkinterval:main
    #
    # Would create an entry point called cs.recipe.checkinterval that calls cs.recipe.checkinterval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - jarn.checkinterval

  run:
    - python
    - setuptools
    - setuptools
    - jarn.checkinterval

test:
  # Python imports
  imports:
    - cs
    - cs.recipe
    - cs.recipe.checkinterval

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codesyntax/cs.recipe.checkinterval
  license: GPL
  summary: 'Buildout recipe that calculates the python-check-interval value for your zope instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
