package:
  name: medialog.issuu
  version: "0.3.3"

source:
  fn: medialog.issuu-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.issuu/medialog.issuu-0.3.3.tar.gz
  md5: dd4fe2ebffd1f6d2a8ad7baadf12c560
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.issuu = medialog.issuu:main
    #
    # Would create an entry point called medialog.issuu that calls medialog.issuu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - requests >=0.9,<1.0

  run:
    - python
    - setuptools
    - setuptools
    - requests >=0.9,<1.0

test:
  # Python imports
  imports:
    - medialog
    - medialog.issuu
    - medialog.issuu.browser
    - medialog.issuu.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenmn/medialog.issuu
  license: GNU General Public License (GPL)
  summary: "The goal is to integrate issuu.com's API with plone"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
