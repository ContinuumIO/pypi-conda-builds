package:
  name: django-master
  version: "0.2.7"

source:
  fn: django-master-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-master/django-master-0.2.7.tar.gz
  md5: b83e49d4a76fa9b8dbe66283097002fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-master = django-master:main
    #
    # Would create an entry point called django-master that calls django-master.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - ordereddict >=1.1

  run:
    - python
    - django >=1.4
    - ordereddict >=1.1

test:
  # Python imports
  imports:
    - djangomaster
    - djangomaster.master
    - djangomaster.tests
    - mock_master
    - mock_master.migrations
    - mock_master.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/django-master/django-master
  license: LICENSE
  summary: 'django master dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
