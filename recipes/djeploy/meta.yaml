package:
  name: djeploy
  version: "0.2.5-dev"

source:
  fn: djeploy-0.2.5-dev.tar.gz
  url: https://pypi.python.org/packages/source/d/djeploy/djeploy-0.2.5-dev.tar.gz
  md5: c95b12e51395787a8757a307907cb8ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djeploy = djeploy:main
    #
    # Would create an entry point called djeploy that calls djeploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djeploy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/petersanchez/djeploy/
  license: BSD License
  summary: 'Common tasks used to deploy Django powered websites via Fabric.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
