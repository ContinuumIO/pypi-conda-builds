package:
  name: ift_nifty
  version: "1.0.6"

source:
  fn: ift_nifty-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/i/ift_nifty/ift_nifty-1.0.6.tar.gz
  md5: 313724ae6d09e3327b0e7ed17abc9de0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ift_nifty = ift_nifty:main
    #
    # Would create an entry point called ift_nifty that calls ift_nifty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ift_nifty
    - nifty
    - nifty.demos
    - nifty.lm
    - nifty.rg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.mpa-garching.mpg.de/ift/nifty/
  license: GPLv3
  summary: 'Numerical Information Field Theory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
