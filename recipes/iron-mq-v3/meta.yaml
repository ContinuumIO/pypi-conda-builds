package:
  name: iron-mq-v3
  version: "0.6"

source:
  fn: iron-mq-v3-0.6.tar.gz
  url: https://pypi.python.org/packages/source/i/iron-mq-v3/iron-mq-v3-0.6.tar.gz
  md5: 6fc2ca2febcf59836f7739aefb033828
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iron-mq-v3 = iron-mq-v3:main
    #
    # Would create an entry point called iron-mq-v3 that calls iron-mq-v3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iron_core

  run:
    - python
    - iron_core

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iron-io/iron_mq_python
  license: BSD License
  summary: 'Client library for IronMQ, a message queue in the cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
