package:
  name: crispy-forms-foundation
  version: "0.5.1"

source:
  fn: crispy-forms-foundation-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/c/crispy-forms-foundation/crispy-forms-foundation-0.5.1.tar.gz
  md5: e4e54edcd2b267a88f14754bc40b8ff2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crispy-forms-foundation = crispy-forms-foundation:main
    #
    # Would create an entry point called crispy-forms-foundation that calls crispy-forms-foundation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-crispy-forms >=1.4

  run:
    - python
    - django-crispy-forms >=1.4

test:
  # Python imports
  imports:
    - crispy_forms_foundation
    - crispy_forms_foundation.layout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/crispy-forms-foundation
  license: BSD License
  summary: "\nDjango application to add 'django-crispy-forms' layout objects for Foundation\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
