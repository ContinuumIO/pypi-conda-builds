package:
  name: instantauth
  version: "0.8.8"

source:
  fn: instantauth-0.8.8.tar.gz
  url: https://pypi.python.org/packages/source/i/instantauth/instantauth-0.8.8.tar.gz
  md5: 64b08dbe31e805c5e8d639caceffd0fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instantauth = instantauth:main
    #
    # Would create an entry point called instantauth that calls instantauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - prettyexc

  run:
    - python
    - prettyexc

test:
  # Python imports
  imports:
    - instantauth
    - instantauth/coders
    - instantauth/cryptors
    - instantauth/verifiers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youknowone/instantauth
  license: UNKNOWN
  summary: 'Not very credential authentication model.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
