package:
  name: configmgr
  version: "0.0"

source:
  fn: configmgr-0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/configmgr/configmgr-0.0.tar.gz
  md5: 884458ed83ae81d72b4a6200b6523fb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configmgr = configmgr:main
    #
    # Would create an entry point called configmgr that calls configmgr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appdirs
    - py

  run:
    - python
    - appdirs
    - py

test:
  # Python imports
  imports:
    - configmgr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/grantor61/configmgr
  license: MIT License
  summary: 'simple configuration management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
