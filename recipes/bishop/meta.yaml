package:
  name: bishop
  version: "0.2.0"

source:
  fn: bishop-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bishop/bishop-0.2.0.tar.gz
  md5: e07aac05a4532a1517c6992fa6293efe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bishop = bishop:main
    #
    # Would create an entry point called bishop that calls bishop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bishop
    - bishop.mako
    - bishop.mako.ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.chadwickmorris.com/bishop/
  license: MIT License
  summary: 'Simple Configuration Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
