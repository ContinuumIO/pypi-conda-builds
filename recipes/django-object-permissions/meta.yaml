package:
  name: django-object-permissions
  version: "1.4.6"

source:
  fn: django-object-permissions-1.4.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-object-permissions/django-object-permissions-1.4.6.tar.gz
  md5: e1fc52b4da6bc23f685e836b79c44aa4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-object-permissions = django-object-permissions:main
    #
    # Would create an entry point called django-object-permissions that calls django-object-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - object_permissions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.osuosl.org/projects/object-permissions
  license: MIT License
  summary: 'A method for adding object-level or row-level permissions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
