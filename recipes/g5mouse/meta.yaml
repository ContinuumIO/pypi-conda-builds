package:
  name: g5mouse
  version: "0.1"

source:
  fn: g5mouse-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/g5mouse/g5mouse-0.1.tar.gz
  md5: d3347dbf261135049cb6e5a6be133355
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - g5mouse = g5mouse:main
    #
    # Would create an entry point called g5mouse that calls g5mouse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - g5mouse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/g5mouse/
  license: BSD License
  summary: 'A pure python code to control logitech G3,G5,G7,G9 mouse on linux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
