package:
  name: js.html5boilerplate
  version: "1.0"

source:
  fn: js.html5boilerplate-1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.html5boilerplate/js.html5boilerplate-1.0.tar.gz
  md5: 23a5571cd5b48c1d5decbf4003b0dcbf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.html5boilerplate = js.html5boilerplate:main
    #
    # Would create an entry point called js.html5boilerplate that calls js.html5boilerplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - js.modernizr
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - js.modernizr
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.html5boilerplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of html5boilerplate'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
