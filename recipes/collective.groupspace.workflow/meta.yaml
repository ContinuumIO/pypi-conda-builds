package:
  name: collective.groupspace.workflow
  version: "1.0.4"

source:
  fn: collective.groupspace.workflow-1.0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.groupspace.workflow/collective.groupspace.workflow-1.0.4.zip
  md5: 28d1e96bdaca5d0c894617d673ab3f23
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.groupspace.workflow = collective.groupspace.workflow:main
    #
    # Would create an entry point called collective.groupspace.workflow that calls collective.groupspace.workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.groupspace
    - collective.groupspace.workflow
    - collective.groupspace.workflow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.groupspace.workflow
  license: GNU General Public License (GPL)
  summary: 'Plone collective.groupspace local workflow for securing private content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
