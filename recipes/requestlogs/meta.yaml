package:
  name: requestlogs
  version: "1.0dev"

source:
  fn: RequestLogs-1.0dev.tar.gz
  url: https://pypi.python.org/packages/source/R/RequestLogs/RequestLogs-1.0dev.tar.gz
  md5: a13f077df7e93061ada9560be1626161
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requestlogs = requestlogs:main
    #
    # Would create an entry point called requestlogs that calls requestlogs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3.0
    - pymongo >=2.5.1
    - celery
    - elasticsearch ==1.0.0

  run:
    - python
    - django >=1.3.0
    - pymongo >=2.5.1
    - celery
    - elasticsearch ==1.0.0

test:
  # Python imports
  imports:
    - requestlogs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dayski/requestlogs
  license: MIT
  summary: 'Request Log middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
