package:
  name: bgapi
  version: "0.3"

source:
  fn: bgapi-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bgapi/bgapi-0.3.tar.gz
  md5: 1dc85799bf19f86af45d521086ba7a5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bgapi = bgapi:main
    #
    # Would create an entry point called bgapi that calls bgapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - bgapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjbrown/bgapi
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Interface library for BlueGiga BLE112 and BLE113 modules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
