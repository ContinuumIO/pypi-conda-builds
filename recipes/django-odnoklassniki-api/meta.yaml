package:
  name: django-odnoklassniki-api
  version: "0.2.3"

source:
  fn: django-odnoklassniki-api-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-odnoklassniki-api/django-odnoklassniki-api-0.2.3.tar.gz
  md5: a51792c5c75191b83ff1e4df351bdab0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-odnoklassniki-api = django-odnoklassniki-api:main
    #
    # Would create an entry point called django-odnoklassniki-api that calls django-odnoklassniki-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-annoying
    - django-picklefield
    - django-oauth-tokens >=0.5.0
    - odnoklassniki
    - simplejson
    - pytz

  run:
    - python
    - django
    - django-annoying
    - django-picklefield
    - django-oauth-tokens >=0.5.0
    - odnoklassniki
    - simplejson
    - pytz

test:
  # Python imports
  imports:
    - odnoklassniki_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-odnoklassniki-api
  license: BSD License
  summary: 'Django implementation for odnoklassniki API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
