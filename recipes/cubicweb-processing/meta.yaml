package:
  name: cubicweb-processing
  version: "0.6.1"

source:
  fn: cubicweb-processing-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-processing/cubicweb-processing-0.6.1.tar.gz
  md5: 1c71e5c2e7341c2f869d697a9e3dbf98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-processing = cubicweb-processing:main
    #
    # Would create an entry point called cubicweb-processing that calls cubicweb-processing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.4
    - cubicweb-wireit >=0.3.0
    - cubicweb-file >=1.11.0

  run:
    - python
    - cubicweb >=3.17.4
    - cubicweb-wireit >=0.3.0
    - cubicweb-file >=1.11.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-processing
  license: LGPL
  summary: 'cube that describes simple data processing workflows'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
