package:
  name: plone.recipe.pound
  version: "0.5.5"

source:
  fn: plone.recipe.pound-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.pound/plone.recipe.pound-0.5.5.tar.gz
  md5: ba8b0c65722347b5dbb7ed9c63c10466
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.pound = plone.recipe.pound:main
    #
    # Would create an entry point called plone.recipe.pound that calls plone.recipe.pound.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.cmmi
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.cmmi
    - cheetah

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.pound
    - plone.recipe.pound.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plone-recipes
  license: GPL
  summary: 'Recipe to install and configure Pound'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
