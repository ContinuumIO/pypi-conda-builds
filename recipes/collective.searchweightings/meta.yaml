package:
  name: collective.searchweightings
  version: "1.1"

source:
  fn: collective.searchweightings-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.searchweightings/collective.searchweightings-1.1.tar.gz
  md5: 0bcee0451266ccb2550d8981a0fbeac5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.searchweightings = collective.searchweightings:main
    #
    # Would create an entry point called collective.searchweightings that calls collective.searchweightings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - collective
    - collective.searchweightings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Allow arbitrary adapters to be registered to weight search results differently'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
