package:
  name: gluino
  version: "0.4"

source:
  fn: Gluino-0.4.tar.gz
  url: https://pypi.python.org/packages/source/G/Gluino/Gluino-0.4.tar.gz
  md5: 5561f706eae64978630f852ac3455b72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gluino = gluino:main
    #
    # Would create an entry point called gluino that calls gluino.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gluino

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mdipierro/gluino
  license: lgpl
  summary: 'port of web2py libs to bottle, flask, pyramid, tornado (includes copy of modules from the web2py framework)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
