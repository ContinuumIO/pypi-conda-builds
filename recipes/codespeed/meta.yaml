package:
  name: codespeed
  version: "0.10.0"

source:
  fn: codespeed-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/c/codespeed/codespeed-0.10.0.tar.gz
  md5: 80758ba8af938bd790bd82f17e0489bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codespeed = codespeed:main
    #
    # Would create an entry point called codespeed that calls codespeed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - isodate
    - south <=2.0

  run:
    - python
    - django >=1.4
    - isodate
    - south <=2.0

test:
  # Python imports
  imports:
    - codespeed
    - codespeed.migrations
    - codespeed.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tobami/codespeed
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'A web application to monitor and analyze the performance of your code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
