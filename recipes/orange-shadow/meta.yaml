package:
  name: orange-shadow
  version: "1.0"

source:
  fn: Orange-Shadow-1.0.21.tar.gz
  url: https://pypi.python.org/packages/source/O/Orange-Shadow/Orange-Shadow-1.0.21.tar.gz
  md5: 76ac9cae1d34e2aef7b8bcd4aa98b0a0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - orange-shadow = orange-shadow:main
    #
    # Would create an entry point called orange-shadow that calls orange-shadow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - orangecontrib
    - orangecontrib.shadow
    - orangecontrib.shadow.menu
    - orangecontrib.shadow.util
    - orangecontrib.shadow.widgets
    - orangecontrib.shadow.widgets.experimental_elements
    - orangecontrib.shadow.widgets.gui
    - orangecontrib.shadow.widgets.loop_management
    - orangecontrib.shadow.widgets.optical_elements
    - orangecontrib.shadow.widgets.plots
    - orangecontrib.shadow.widgets.preprocessor
    - orangecontrib.shadow.widgets.sources
    - orangecontrib.shadow.widgets.utility

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://orange.biolab.si/
  license: GPLv3
  summary: 'Shadow, Ray-tracing simulation software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
