package:
  name: cm-stv-statistics
  version: "0.2"

source:
  fn: cm-stv-statistics-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cm-stv-statistics/cm-stv-statistics-0.2.tar.gz
  md5: a91a5ff692dfbe448ecc27fca6aeea38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cm-stv-statistics = cm-stv-statistics:main
    #
    # Would create an entry point called cm-stv-statistics that calls cm-stv-statistics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cmstv_statistics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/creativemedia/cm-stv/
  license: UNKNOWN
  summary: 'Statistics generator for cmstv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
