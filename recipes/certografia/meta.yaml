package:
  name: certografia
  version: "0.1b"

source:
  fn: certografia-0.1b.tar.gz
  url: https://pypi.python.org/packages/source/c/certografia/certografia-0.1b.tar.gz
  md5: 17aaf8c9347d2eeebf2c5af139f13d10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - certografia = certografia:main
    #
    # Would create an entry point called certografia that calls certografia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - elementtree ==1.2.6-20050316
    - hunspell ==0.1
    - numpy ==1.7.1
    - pyyaml ==3.10
    - nltk ==2.0.4
    - sqlalchemy ==0.8.3

  run:
    - python
    - elementtree ==1.2.6-20050316
    - hunspell ==0.1
    - numpy ==1.7.1
    - pyyaml ==3.10
    - nltk ==2.0.4
    - sqlalchemy ==0.8.3

test:
  # Python imports
  imports:
    - certografia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://edpittol.github.io
  license: GNU General Public License v3 (GPLv3)
  summary: 'Spell checker for Portuguese language.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
