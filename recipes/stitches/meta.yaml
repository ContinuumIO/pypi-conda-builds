package:
  name: stitches
  version: "0.9"

source:
  fn: stitches-0.9.tar.gz
  url: https://pypi.python.org/packages/source/s/stitches/stitches-0.9.tar.gz
  md5: bb54625840d0618da9f09b14ea529de1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - stitches = stitches:main
    #
    # Would create an entry point called stitches that calls stitches.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko >=1.10
    - nose
    - pyyaml
    - plumbum
    - rpyc

  run:
    - python
    - paramiko >=1.10
    - nose
    - pyyaml
    - plumbum
    - rpyc

test:
  # Python imports
  imports:
    - stitches

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RedHatQE/python-stitches
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Multihost actions toolbox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
