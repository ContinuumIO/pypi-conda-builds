package:
  name: oapackage
  version: "1.9.318"

source:
  fn: OApackage-1.9.318.tar.gz
  url: https://pypi.python.org/packages/source/O/OApackage/OApackage-1.9.318.tar.gz
  md5: fc500174610da45d6ce84d3add662183
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oapackage = oapackage:main
    #
    # Would create an entry point called oapackage that calls oapackage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oapackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pietereendebak.nl/oapackage/index.html
  license: BSD
  summary: 'Package to generate and analyse orthogonal arrays and optimal designs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
