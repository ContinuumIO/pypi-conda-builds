package:
  name: django-request-provider
  version: "1.0.2"

source:
  fn: django-request-provider-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-request-provider/django-request-provider-1.0.2.tar.gz
  md5: eff54ab735a0c3a014a3e16ea5bb345a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-request-provider = django-request-provider:main
    #
    # Would create an entry point called django-request-provider that calls django-request-provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - request_provider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tzulberti/django-request-provider
  license: BSD License
  summary: 'access django request object whenever you need it'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
