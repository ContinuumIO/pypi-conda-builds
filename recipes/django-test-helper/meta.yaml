package:
  name: django-test-helper
  version: "0.1a2"

source:
  fn: django-test-helper-0.1a2.zip
  url: https://pypi.python.org/packages/source/d/django-test-helper/django-test-helper-0.1a2.zip
  md5: f5ede457aef17879f1e3a77ac199b816
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-test-helper = django-test-helper:main
    #
    # Would create an entry point called django-test-helper that calls django-test-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - test_helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-test-helper
  license: BSD License
  summary: 'Utilites for Django tests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
