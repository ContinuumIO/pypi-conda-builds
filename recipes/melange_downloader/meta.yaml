package:
  name: melange_downloader
  version: "0.5"

source:
  fn: melange_downloader-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/melange_downloader/melange_downloader-0.5.tar.gz
  md5: cd0f738fa24183743208b3437788efb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - melange_downloader = melange_downloader:main
    #
    # Would create an entry point called melange_downloader that calls melange_downloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup

  run:
    - python
    - beautifulsoup

test:
  # Python imports
  imports:
    - melange_downloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bhavul/testimonials-downloader
  license: MIT
  summary: 'Download testimonials in a sorted way from BITS Melange website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
