package:
  name: django-multilingual-events
  version: "1.3.1"

source:
  fn: django-multilingual-events-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multilingual-events/django-multilingual-events-1.3.1.tar.gz
  md5: d40064fc90a2a1c25b012af72aaaa765
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multilingual-events = django-multilingual-events:main
    #
    # Would create an entry point called django-multilingual-events that calls django-multilingual-events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-cms
    - south
    - django-countries ==2.1.2
    - django-hvad >=0.4
    - django-document-library

  run:
    - python
    - django
    - django-cms
    - south
    - django-countries ==2.1.2
    - django-hvad >=0.4
    - django-document-library

test:
  # Python imports
  imports:
    - multilingual_events
    - multilingual_events.migrations
    - multilingual_events.templatetags
    - multilingual_events.tests
    - multilingual_events.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-multilingnual-events
  license: The MIT License
  summary: 'A simple event app to create event pages for events such as conferences.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
