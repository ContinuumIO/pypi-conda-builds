package:
  name: flattery
  version: "0.1"

source:
  fn: flattery-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flattery/flattery-0.1.tar.gz
  md5: cb623a4ecb8525a08e9dac7f757dbdfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flattery = flattery:main
    #
    # Would create an entry point called flattery that calls flattery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - flattery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/acg/python-flattery
  license: BSD License
  summary: 'flattery - fast flattening and unflattening of nested Python data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
