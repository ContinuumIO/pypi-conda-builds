package:
  name: manysim
  version: "0.0-1"

source:
  fn: manysim-0.0-1.tar.gz
  url: https://pypi.python.org/packages/source/m/manysim/manysim-0.0-1.tar.gz
  md5: 167373f33c15119c9c2d97c0b4cb2204
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - manysim = manysim:main
    #
    # Would create an entry point called manysim that calls manysim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - manysim

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/manysim/
  license: Apache Software License
  summary: 'Perform Simulations on the Amazon Elastic Compute Cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
