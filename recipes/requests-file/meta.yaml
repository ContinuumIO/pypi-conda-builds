package:
  name: requests-file
  version: "1.3.1"

source:
  fn: requests-file-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/r/requests-file/requests-file-1.3.1.tar.gz
  md5: 35c06e4bbd64556adc102680d1ffb007
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requests-file = requests-file:main
    #
    # Would create an entry point called requests-file that calls requests-file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=1.0.0
    - six

  run:
    - python
    - requests >=1.0.0
    - six

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dashea/requests-file
  license: Apache Software License
  summary: 'File transport adapter for Requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
