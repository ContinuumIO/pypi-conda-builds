package:
  name: django-mockups
  version: "0.4.8"

source:
  fn: django-mockups-0.4.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mockups/django-mockups-0.4.8.tar.gz
  md5: ea5a636f527f35af4ae971769021038a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mockups = django-mockups:main
    #
    # Would create an entry point called django-mockups that calls django-mockups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mockups
    - mockups.contrib
    - mockups.management
    - mockups.management.commands
    - mockups.tests
    - mockups.tests.generator_test
    - mockups.tests.mockups_test
    - mockups.tests.sample_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sorl/django-mockups
  license: BSD License
  summary: 'Provides tools to auto generate content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
