package:
  name: django-chunks2
  version: "2.8.3"

source:
  fn: django-chunks2-2.8.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chunks2/django-chunks2-2.8.3.tar.gz
  md5: 08220591a3244f42d6e822e95c3477e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chunks2 = django-chunks2:main
    #
    # Would create an entry point called django-chunks2 that calls django-chunks2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - easy_thumbnails

  run:
    - python
    - easy_thumbnails

test:
  # Python imports
  imports:
    - chunks
    - chunks.migrations
    - chunks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gotlium/django-chunks2
  license: BSD License
  summary: 'Keyed blocks of content for use in your Django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
