package:
  name: exscript
  version: "v2.1.451"

source:
  fn: Exscript-v2.1.451.tar.gz
  url: https://pypi.python.org/packages/source/E/Exscript/Exscript-v2.1.451.tar.gz
  md5: e60cc9816163888a965f9df2adc4219e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exscript = exscript:main
    #
    # Would create an entry point called exscript that calls exscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko

  run:
    - python
    - paramiko

test:
  # Python imports
  imports:
    - Exscript
    - Exscript.emulators
    - Exscript.external
    - Exscript.external.otp
    - Exscript.interpreter
    - Exscript.parselib
    - Exscript.protocols
    - Exscript.protocols.drivers
    - Exscript.servers
    - Exscript.stdlib
    - Exscript.util
    - Exscript.workqueue
    - Exscriptd
    - Exscriptd.config
    - TkExscript
    - TkExscript.compat
    - exscript

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/knipknap/exscript/
  license: GNU General Public License (GPL)
  summary: 'Template language for automating Telnet and SSH'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
