package:
  name: neuroshare
  version: "0.9.2"

source:
  fn: neuroshare-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/n/neuroshare/neuroshare-0.9.2.tar.gz
  md5: 03b3f6aa5f574b0cf28b2a05ec0c79ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - neuroshare = neuroshare:main
    #
    # Would create an entry point called neuroshare that calls neuroshare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - neuroshare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.g-node.org/neuroshare-tools
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python Bindings for Neuroshare'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
