package:
  name: bosonnlp
  version: "0.5.0"

source:
  fn: bosonnlp-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bosonnlp/bosonnlp-0.5.0.tar.gz
  md5: fb56f234234a09e0d9e51b2a64de3a18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bosonnlp = bosonnlp:main
    #
    # Would create an entry point called bosonnlp that calls bosonnlp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.0

  run:
    - python
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - bosonnlp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bosonnlp-py.readthedocs.org/
  license: MIT License
  summary: u'BosonNLP.com API wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
