package:
  name: ass.ets
  version: "0.1.1"

source:
  fn: ass.ets-0.1.1.zip
  url: https://pypi.python.org/packages/source/a/ass.ets/ass.ets-0.1.1.zip
  md5: 549134c01907b1542708fd409a6f7bb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ass.ets = ass.ets:main
    #
    # Would create an entry point called ass.ets that calls ass.ets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - useless.pipes

  run:
    - python
    - useless.pipes

test:
  # Python imports
  imports:
    - ass
    - ass.ets
    - ass.ets.filters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kaste/ass.ets
  license: BSD License
  summary: 'Asset management for webapps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
