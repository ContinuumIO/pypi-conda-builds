package:
  name: glyphviewer
  version: "0.3.1"

source:
  fn: glyphviewer-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/g/glyphviewer/glyphviewer-0.3.1.tar.gz
  md5: 327b0c904542e2e77e3c5ee12c615d19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glyphviewer = glyphviewer:main
    #
    # Would create an entry point called glyphviewer that calls glyphviewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - glyphviewer
    - glyphviewer.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/glyphviewer/
  license: GNU General Public License v3 (GPLv3)
  summary: 'A Django app for examining web fonts and the characters they contain.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
