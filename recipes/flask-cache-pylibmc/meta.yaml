package:
  name: flask-cache-pylibmc
  version: "0.1"

source:
  fn: Flask-Cache-PyLibMC-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Cache-PyLibMC/Flask-Cache-PyLibMC-0.1.tar.gz
  md5: 20ef7b74857f67be1db70a19807504a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-cache-pylibmc = flask-cache-pylibmc:main
    #
    # Would create an entry point called flask-cache-pylibmc that calls flask-cache-pylibmc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug
    - flask-cache
    - pylibmc

  run:
    - python
    - werkzeug
    - flask-cache
    - pylibmc

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/radiosilence/flask-cache-pylibmc/
  license: BSD License
  summary: 'PyLibMC cache for Flask-Cache, supports multiple operations\nand other awesome things.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
