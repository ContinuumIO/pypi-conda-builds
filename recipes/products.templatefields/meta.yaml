package:
  name: products.templatefields
  version: "1.2.5"

source:
  fn: Products.TemplateFields-1.2.5.zip
  url: https://pypi.python.org/packages/source/P/Products.TemplateFields/Products.TemplateFields-1.2.5.zip
  md5: bb0905e14e218077fc5f6cb6fe126439
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.templatefields = products.templatefields:main
    #
    # Would create an entry point called products.templatefields that calls products.templatefields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.TemplateFields
    - Products.TemplateFields.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/scriptablefields
  license: GPL
  summary: 'Supplies an Archetypes field useful for editing and storing Zope Page Templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
