package:
  name: collective.pece
  version: "0.0.8"

source:
  fn: collective.pece-0.0.8.zip
  url: https://pypi.python.org/packages/source/c/collective.pece/collective.pece-0.0.8.zip
  md5: bcc03a8856fd5d1f5cbc4755704415f8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pece = collective.pece:main
    #
    # Would create an entry point called collective.pece that calls collective.pece.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.cover
    - collective.documentviewer
    - five.grok
    - pyzotero
    - z3c.jbot

  run:
    - python
    - setuptools
    - setuptools
    - collective.cover
    - collective.documentviewer
    - five.grok
    - pyzotero
    - z3c.jbot

test:
  # Python imports
  imports:
    - collective
    - collective.pece
    - collective.pece.browser
    - collective.pece.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.pece
  license: GPL
  summary: 'The Platform for Experimental Collaborative Ethnography (PECE)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
