package:
  name: jadm
  version: "1.0"

source:
  fn: jadm-1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jadm/jadm-1.0.tar.gz
  md5: 19d0e3cbc5fdabde2f2e3461fc9eb817
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jadm = jadm:main
    #
    # Would create an entry point called jadm that calls jadm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tabulate >=0.7
    - netifaces >=0.6
    - ipaddress >=1.0.6
    - paramiko >=1.13.0

  run:
    - python
    - tabulate >=0.7
    - netifaces >=0.6
    - ipaddress >=1.0.6
    - paramiko >=1.13.0

test:
  # Python imports
  imports:
    - jadm
    - man8

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/NikolayDachev/jadm
  license: BSD 3-Clause
  summary: 'FreeBSD jail framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
