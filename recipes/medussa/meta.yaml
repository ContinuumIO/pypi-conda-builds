package:
  name: medussa
  version: "1.0.1"

source:
  fn: medussa-1.0.1.zip
  url: https://pypi.python.org/packages/source/m/medussa/medussa-1.0.1.zip
  md5: 65c3602f6842e80a94d691f5ff3b6cbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medussa = medussa:main
    #
    # Would create an entry point called medussa that calls medussa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.3

  run:
    - python
    - numpy >=1.3

test:
  # Python imports
  imports:
    - medussa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.medussa.us
  license: GNU General Public License (GPL)
  summary: 'Medussa: A cross-platform high-level audio library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
