package:
  name: import_or_pip
  version: "0.1.2"

source:
  fn: import_or_pip-0.1.2.zip
  url: https://pypi.python.org/packages/source/i/import_or_pip/import_or_pip-0.1.2.zip
  md5: b14ba846c385eaaa3cb638929b7c5a78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - import_or_pip = import_or_pip:main
    #
    # Would create an entry point called import_or_pip that calls import_or_pip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - import_or_pip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ubershmekel/import_or_pip
  license: Public Domain
  summary: "import a module or pip install it if it isn't found"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
