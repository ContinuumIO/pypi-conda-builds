package:
  name: countenvir
  version: "1.0.3"

source:
  fn: countenvir-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/countenvir/countenvir-1.0.3.tar.gz
  md5: 7fc0b675aff0168643cc8172595f2f1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - countenvir = countenvir:main
    #
    # Would create an entry point called countenvir that calls countenvir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - countenvir

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.csdemmer.de
  license: GPLv3
  summary: 'A simple environment to count the occurrences of the 26 latin letters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
