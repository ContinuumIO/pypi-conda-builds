package:
  name: mongoassist
  version: "1.2"

source:
  fn: mongoassist-1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mongoassist/mongoassist-1.2.tar.gz
  md5: ff8acd30e0d4a7871b5df36da10b336b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoassist = mongoassist:main
    #
    # Would create an entry point called mongoassist that calls mongoassist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - mongoassist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guyingbo/mongoassist
  license: MIT License
  summary: 'A very simple mongodb client wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
