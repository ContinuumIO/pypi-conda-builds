package:
  name: django-goodies
  version: "0.1.0"

source:
  fn: django-goodies-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-goodies/django-goodies-0.1.0.tar.gz
  md5: 95cbc6995e61cbc95f0981ffd9db3348
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-goodies = django-goodies:main
    #
    # Would create an entry point called django-goodies that calls django-goodies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_goodies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oogles/django-goodies
  license: BSD
  summary: 'A collection of useful stuff for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
