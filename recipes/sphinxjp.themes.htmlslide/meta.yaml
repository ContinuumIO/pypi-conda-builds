package:
  name: sphinxjp.themes.htmlslide
  version: "0.1.4"

source:
  fn: sphinxjp.themes.htmlslide-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.themes.htmlslide/sphinxjp.themes.htmlslide-0.1.4.tar.gz
  md5: 33b9721d6f6406e977348d0c756b867e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.themes.htmlslide = sphinxjp.themes.htmlslide:main
    #
    # Would create an entry point called sphinxjp.themes.htmlslide that calls sphinxjp.themes.htmlslide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - docutils
    - sphinx
    - sphinxjp.themecore

  run:
    - python
    - setuptools
    - setuptools
    - docutils
    - sphinx
    - sphinxjp.themecore

test:
  # Python imports
  imports:
    - sphinxjp
    - sphinxjp.themes
    - sphinxjp.themes.htmlslide

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/shimizukawa/sphinxjp.themes.htmlslide
  license: MIT License
  summary: 'A sphinx theme for generate HTML Slide presentation. #sphinxjp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
