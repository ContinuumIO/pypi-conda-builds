package:
  name: m01.grid
  version: "0.5.0"

source:
  fn: m01.grid-0.5.0.zip
  url: https://pypi.python.org/packages/source/m/m01.grid/m01.grid-0.5.0.zip
  md5: aaa5e8efc5f28155c8c627461a0b62ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m01.grid = m01.grid:main
    #
    # Would create an entry point called m01.grid that calls m01.grid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bson
    - m01.mongo
    - pymongo
    - z3c.form
    - zope.component
    - zope.contenttype
    - zope.datetime
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.site

  run:
    - python
    - setuptools
    - bson
    - m01.mongo
    - pymongo
    - z3c.form
    - zope.component
    - zope.contenttype
    - zope.datetime
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.site

test:
  # Python imports
  imports:
    - m01
    - m01.grid
    - m01.grid.testing
    - m01.grid.widget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/m01.grid
  license: Zope Public License
  summary: 'MongoDB grid file implementation for Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
