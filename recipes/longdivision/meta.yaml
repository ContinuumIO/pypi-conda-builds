package:
  name: longdivision
  version: "0.1"

source:
  fn: longdivision-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/longdivision/longdivision-0.1.tar.gz
  md5: b58a903648999adbe60280543adc8cc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - longdivision = longdivision:main
    #
    # Would create an entry point called longdivision that calls longdivision.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - longdivision

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sirex/longdivision
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Script that breaks down a division problem into a series of easier steps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
