package:
  name: cosmid
  version: "0.4.9.1"

source:
  fn: cosmid-0.4.9.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cosmid/cosmid-0.4.9.1.tar.gz
  md5: 7e60d2f4e8a96a0345a940baf7282807
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cosmid = cosmid:main
    #
    # Would create an entry point called cosmid that calls cosmid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - path.py
    - pyyaml
    - fuzzywuzzy
    - termcolor
    - sh

  run:
    - python
    - docopt
    - path.py
    - pyyaml
    - fuzzywuzzy
    - termcolor
    - sh

test:
  # Python imports
  imports:
    - cosmid
    - cosmid.resources
    - cosmid.servers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robinandeer/cosmid
  license: MIT License
  summary: 'A genomics resource manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
