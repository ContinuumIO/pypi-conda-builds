package:
  name: py-config-parser
  version: "1.1"

source:
  fn: py-config-parser-1.1.zip
  url: https://pypi.python.org/packages/source/p/py-config-parser/py-config-parser-1.1.zip
  md5: 22cc03ed790a0cf47db3c5230632d363
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-config-parser = py-config-parser:main
    #
    # Would create an entry point called py-config-parser that calls py-config-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - config_parser
    - examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/damirazo/py-config-parser
  license: MIT License
  summary: 'Simple python json configuration file parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
