package:
  name: django_qbe
  version: "0.3.0"

source:
  fn: django_qbe-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_qbe/django_qbe-0.3.0.tar.gz
  md5: d163042f2cc58ba9cbcdf969228d9042
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_qbe = django_qbe:main
    #
    # Would create an entry point called django_qbe that calls django_qbe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-picklefield

  run:
    - python
    - django-picklefield

test:
  # Python imports
  imports:
    - django_qbe
    - django_qbe.savedqueries
    - django_qbe.savedqueries.management
    - django_qbe.savedqueries.management.commands
    - django_qbe.savedqueries.migrations
    - django_qbe.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://versae.github.com/qbe/
  license: MIT License
  summary: 'Django admin tool for custom reports'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
