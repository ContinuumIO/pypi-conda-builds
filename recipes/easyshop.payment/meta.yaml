package:
  name: easyshop.payment
  version: "0.1a1"

source:
  fn: easyshop.payment-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.payment/easyshop.payment-0.1a1.tar.gz
  md5: a10dca2b599ea776e1fbc4eb80b0b797
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.payment = easyshop.payment:main
    #
    # Would create an entry point called easyshop.payment that calls easyshop.payment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.payment
    - easyshop.payment.adapters
    - easyshop.payment.browser
    - easyshop.payment.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'Payment for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
