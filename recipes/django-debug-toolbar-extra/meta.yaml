package:
  name: django-debug-toolbar-extra
  version: "0.0.2"

source:
  fn: django-debug-toolbar-extra-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-toolbar-extra/django-debug-toolbar-extra-0.0.2.tar.gz
  md5: 84048103d689be33460d5189d0d6b614
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-toolbar-extra = django-debug-toolbar-extra:main
    #
    # Would create an entry point called django-debug-toolbar-extra that calls django-debug-toolbar-extra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - debug_toolbar_extra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://tracpub.yaco.es/djangoapps/wiki/DjangoDebugToolbarExtra
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application that allows add funcionality to the django-debug-toolbar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
