package:
  name: django-social-friends-finder
  version: "0.2"

source:
  fn: django-social-friends-finder-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-friends-finder/django-social-friends-finder-0.2.tar.gz
  md5: 632cb07b3b52dde709774edbac7dcda4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-friends-finder = django-social-friends-finder:main
    #
    # Would create an entry point called django-social-friends-finder that calls django-social-friends-finder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - social_friends_finder
    - social_friends_finder.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/laplacesdemon/django-social-friends-finder.git
  license: BSD licence, see LICENCE.txt
  summary: 'An extension app for django-social-auth or django-allauth that fetches your friends from different social-networks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
