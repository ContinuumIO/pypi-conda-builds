package:
  name: gomobile.supporter
  version: "0.9.3"

source:
  fn: gomobile.supporter-0.9.3.zip
  url: https://pypi.python.org/packages/source/g/gomobile.supporter/gomobile.supporter-0.9.3.zip
  md5: 5cc73e9dd852f0f665cc19c2a19e2780
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gomobile.supporter = gomobile.supporter:main
    #
    # Would create an entry point called gomobile.supporter that calls gomobile.supporter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - gomobile
    - gomobile.supporter
    - gomobile.supporter.discussion
    - gomobile.supporter.easytemplate
    - gomobile.supporter.feedfeeder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://webandmobile.mfabrik.com
  license: GNU General Public License (GPL)
  summary: 'Add mobile support for various Plone add on products'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
