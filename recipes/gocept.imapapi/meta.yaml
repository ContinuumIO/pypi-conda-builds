package:
  name: gocept.imapapi
  version: "0.5"

source:
  fn: gocept.imapapi-0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.imapapi/gocept.imapapi-0.5.tar.gz
  md5: 97272b43917378afb415f6d9cfc18f1a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.imapapi = gocept.imapapi:main
    #
    # Would create an entry point called gocept.imapapi that calls gocept.imapapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - gocept
    - gocept.imapapi
    - gocept.imapapi.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'Object-oriented API for accessing IMAP accounts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
