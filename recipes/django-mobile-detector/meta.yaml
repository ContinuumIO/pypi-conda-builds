package:
  name: django-mobile-detector
  version: "0.1.3"

source:
  fn: django-mobile-detector-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mobile-detector/django-mobile-detector-0.1.3.tar.gz
  md5: 133efc02ded8533e4fe3e4f517f97882
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobile-detector = django-mobile-detector:main
    #
    # Would create an entry point called django-mobile-detector that calls django-mobile-detector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - mobile_detector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madisona/django-mobile-detector
  license: BSD License
  summary: 'A small app to detect mobile browsers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
