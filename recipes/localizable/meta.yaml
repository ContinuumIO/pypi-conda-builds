package:
  name: localizable
  version: "0.1.2"

source:
  fn: localizable-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/localizable/localizable-0.1.2.tar.gz
  md5: 967f078acaab2947ed7a87a8b9aaa035
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - localizable = localizable:main
    #
    # Would create an entry point called localizable that calls localizable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - chardet

  run:
    - python
    - chardet

test:
  # Python imports
  imports:
    - localizable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisballinger/python-localizable
  license: GPLv2
  summary: 'Localizable.strings parser for Apple-specific localization files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
