package:
  name: html-tree-diff
  version: "0.1.2"

source:
  fn: html-tree-diff-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/html-tree-diff/html-tree-diff-0.1.2.tar.gz
  md5: 6bc02e9b3dac3fb443b3b5691b74c0f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html-tree-diff = html-tree-diff:main
    #
    # Would create an entry point called html-tree-diff that calls html-tree-diff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - htmltreediff

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/christian-oudard/htmltreediff/
  license: BSD License
  summary: 'Structure-aware diff for html and xml documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
