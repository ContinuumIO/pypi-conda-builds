package:
  name: pythonicgrib
  version: "0.2.0"

source:
  fn: PythonicGRIB-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/P/PythonicGRIB/PythonicGRIB-0.2.0.tar.gz
  md5: 753722deadc1f1f287df6df4f8ce2cca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythonicgrib = pythonicgrib:main
    #
    # Would create an entry point called pythonicgrib that calls pythonicgrib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyth_grib
    - pythonicgrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erget/PythonicGRIB
  license: Apache
  summary: "A Pythonic interface for the ECMWF's GRIB API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
