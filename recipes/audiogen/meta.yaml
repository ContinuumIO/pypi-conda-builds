package:
  name: audiogen
  version: "0.0.2"

source:
  fn: audiogen-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/audiogen/audiogen-0.0.2.tar.gz
  md5: a620d701f6a795d5f52de5f8e2326050
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audiogen = audiogen:main
    #
    # Would create an entry point called audiogen that calls audiogen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - audiogen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/casebeer/audiogen
  license: BSD License
  summary: 'Generator based tools for working with audio clips.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
