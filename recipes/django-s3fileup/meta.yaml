package:
  name: django-s3fileup
  version: "0.5"

source:
  fn: django-s3fileup-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-s3fileup/django-s3fileup-0.5.tar.gz
  md5: fea6219ec0cd9134109ba42b0f516eff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-s3fileup = django-s3fileup:main
    #
    # Would create an entry point called django-s3fileup that calls django-s3fileup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.7.0
    - djangorestframework >=2.3.6

  run:
    - python
    - boto >=2.7.0
    - djangorestframework >=2.3.6

test:
  # Python imports
  imports:
    - s3fileup
    - s3fileup/api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/roadhead/s3fileup
  license: UNKNOWN
  summary: 'Django app that exposes a file upload API to S3, direct from the client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
