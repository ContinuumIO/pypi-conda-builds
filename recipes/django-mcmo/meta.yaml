package:
  name: django-mcmo
  version: "2.1.2"

source:
  fn: django-mcmo-2.1.2.zip
  url: https://pypi.python.org/packages/source/d/django-mcmo/django-mcmo-2.1.2.zip
  md5: f68264e3089baec4a838b270db09f8a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mcmo = django-mcmo:main
    #
    # Would create an entry point called django-mcmo that calls django-mcmo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - mcmo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tkhyn/django-mcmo
  license: MIT License
  summary: 'Allows multiple apps to override the same management command in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
