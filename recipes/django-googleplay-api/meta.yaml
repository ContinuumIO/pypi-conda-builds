package:
  name: django-googleplay-api
  version: "1.1.2"

source:
  fn: django-googleplay-api-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googleplay-api/django-googleplay-api-1.1.2.tar.gz
  md5: 3a94a1ab863daebfb63766334f191c06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googleplay-api = django-googleplay-api:main
    #
    # Would create an entry point called django-googleplay-api that calls django-googleplay-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata
    - django-preferences
    - protobuf
    - pycurl
    - lxml
    - grab

  run:
    - python
    - gdata
    - django-preferences
    - protobuf
    - pycurl
    - lxml
    - grab

test:
  # Python imports
  imports:
    - djgpa
    - djgpa.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gotlium/django-googleplay-api
  license: GNU General Public License (GPL)
  summary: 'Google Play API, with configuration on Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
