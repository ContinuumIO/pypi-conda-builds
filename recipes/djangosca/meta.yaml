package:
  name: djangosca
  version: "1.3i"

source:
  fn: DjangoSCA-1.3i.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoSCA/DjangoSCA-1.3i.tar.gz
  md5: 137187d3c138a73084f58cfbea2029f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangosca = djangosca:main
    #
    # Would create an entry point called djangosca that calls djangosca.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangoSCAclasses
    - djangosca

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jsthyer/djangosca
  license: GPLv3
  summary: 'Django Static Source Code Analyzer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
