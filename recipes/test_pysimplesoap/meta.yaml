package:
  name: test_pysimplesoap
  version: "1.0"

source:
  fn: test_pysimplesoap-1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/test_pysimplesoap/test_pysimplesoap-1.0.tar.gz
  md5: 4d79900234a4aef77177c5cf33c04974
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - test_pysimplesoap = test_pysimplesoap:main
    #
    # Would create an entry point called test_pysimplesoap that calls test_pysimplesoap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pysimplesoap
    - test_pysimplesoap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/pysimplesoap
  license: UNKNOWN
  summary: 'Python Simple SOAP Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
