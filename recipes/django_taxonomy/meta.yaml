package:
  name: django_taxonomy
  version: "0.0.2"

source:
  fn: django_taxonomy-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_taxonomy/django_taxonomy-0.0.2.tar.gz
  md5: d14c7a9220b79b39d72b2d1e14b89613
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_taxonomy = django_taxonomy:main
    #
    # Would create an entry point called django_taxonomy that calls django_taxonomy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mptt
    - django

  run:
    - python
    - django-mptt
    - django

test:
  # Python imports
  imports:
    - django_taxonomy
    - taxonomy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ohmypixel.com/
  license: LGPL
  summary: 'Taxonomy Model to use for django categories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
