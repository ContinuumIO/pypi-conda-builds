package:
  name: inkpy
  version: "0.1.0-alpha"

source:
  fn: inkpy-0.1.0-alpha.tar.gz
  url: https://pypi.python.org/packages/source/i/inkpy/inkpy-0.1.0-alpha.tar.gz
  md5: b0a6383f0bbe28c06b3c86cd1b709995
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inkpy = inkpy:main
    #
    # Would create an entry point called inkpy that calls inkpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.13
    - django_rq ==0.4.5

  run:
    - python
    - django >=1.4.13
    - django_rq ==0.4.5

test:
  # Python imports
  imports:
    - inkpy
    - inkpy.management
    - inkpy.management.commands
    - inkpy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quamilek/InkPy
  license: BSD License
  summary: 'InkPy - provide tool to fill Django style template in odt file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
