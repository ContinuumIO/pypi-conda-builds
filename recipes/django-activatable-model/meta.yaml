package:
  name: django-activatable-model
  version: "0.4.1"

source:
  fn: django-activatable-model-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-activatable-model/django-activatable-model-0.4.1.tar.gz
  md5: df9099dbb3d6395e0181edc906354d1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-activatable-model = django-activatable-model:main
    #
    # Would create an entry point called django-activatable-model that calls django-activatable-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-manager-utils >=0.8.2

  run:
    - python
    - django >=1.7
    - django-manager-utils >=0.8.2

test:
  # Python imports
  imports:
    - activatable_model
    - activatable_model.tests
    - activatable_model.tests.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-activatable-model
  license: MIT License
  summary: 'Django Activatable Model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
