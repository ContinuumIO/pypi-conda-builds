package:
  name: omnifile
  version: "1.0"

source:
  fn: OmniFile-1.0.tar.gz
  url: https://pypi.python.org/packages/source/O/OmniFile/OmniFile-1.0.tar.gz
  md5: 2037f46b31326cf48968ccea576dc448
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omnifile = omnifile:main
    #
    # Would create an entry point called omnifile that calls omnifile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - omnifile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Opens files from file names, file objects, or pre-read data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
