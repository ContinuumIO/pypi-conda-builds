package:
  name: collective.validator.xhtmlstrict
  version: "1.2.0"

source:
  fn: collective.validator.xhtmlStrict-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.validator.xhtmlStrict/collective.validator.xhtmlStrict-1.2.0.tar.gz
  md5: 6c72ca6fed57191f460ca791fbc62fb0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.validator.xhtmlstrict = collective.validator.xhtmlstrict:main
    #
    # Would create an entry point called collective.validator.xhtmlstrict that calls collective.validator.xhtmlstrict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.validator
    - collective.validator.xhtmlStrict
    - collective.validator.xhtmlStrict.interfaces
    - collective.validator.xhtmlStrict.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.validator.xhtmlStrict
  license: GPL
  summary: 'XHTML Strict Validator for collective.validator.base'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
