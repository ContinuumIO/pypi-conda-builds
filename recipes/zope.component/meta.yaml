package:
  name: zope.component
  version: "4.2.1"

source:
  fn: zope.component-4.2.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.component/zope.component-4.2.1.tar.gz
  md5: cf55d496428d976f9329a4a2330a8b54
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.component = zope.component:main
    #
    # Would create an entry point called zope.component that calls zope.component.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface >=4.1.0
    - zope.event

  run:
    - python
    - setuptools
    - zope.interface >=4.1.0
    - zope.event

test:
  # Python imports
  imports:
    - zope
    - zope.component
    - zope.component.testfiles
    - zope.component.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.component
  license: Zope Public License
  summary: 'Zope Component Architecture'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
