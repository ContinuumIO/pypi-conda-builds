package:
  name: fez.atcsv
  version: "0.2"

source:
  fn: fez.atcsv-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fez.atcsv/fez.atcsv-0.2.tar.gz
  md5: 0cb96e70f2e95196f5585a31348a1021
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fez.atcsv = fez.atcsv:main
    #
    # Would create an entry point called fez.atcsv that calls fez.atcsv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - fez
    - fez.atcsv
    - fez.atcsv.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fezconsulting.com
  license: BSD License
  summary: 'Simple AT CSV processing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
