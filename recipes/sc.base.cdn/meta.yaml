package:
  name: sc.base.cdn
  version: "0.6-"

source:
  fn: sc.base.cdn-0.6-.zip
  url: https://pypi.python.org/packages/source/s/sc.base.cdn/sc.base.cdn-0.6-.zip
  md5: 02eedde866f139bdd36343274fb6412f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.base.cdn = sc.base.cdn:main
    #
    # Would create an entry point called sc.base.cdn that calls sc.base.cdn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - sc
    - sc.base
    - sc.base.cdn
    - sc.base.cdn.controlpanel
    - sc.base.cdn.docs
    - sc.base.cdn.providers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
