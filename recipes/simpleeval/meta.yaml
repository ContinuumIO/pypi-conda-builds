package:
  name: simpleeval
  version: "0.8.2"

source:
  fn: simpleeval-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/s/simpleeval/simpleeval-0.8.2.tar.gz
  md5: dc070c46f1e7ee1745b71100e5a1bcfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleeval = simpleeval:main
    #
    # Would create an entry point called simpleeval that calls simpleeval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simpleeval

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danthedeckie/simpleeval
  license: MIT License
  summary: 'A simple, safe single expression evaluator library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
