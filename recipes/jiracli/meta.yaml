package:
  name: jiracli
  version: "0.4.1"

source:
  fn: jiracli-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jiracli/jiracli-0.4.1.tar.gz
  md5: c99ca3d9f84a670171c31e2db6795ad7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jiracli = jiracli:main
    #
    # Would create an entry point called jiracli that calls jiracli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jiracli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License or GNU General Public License v3 (GPLv3)
  summary: 'command line interface for jira'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
