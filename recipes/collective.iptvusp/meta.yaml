package:
  name: collective.iptvusp
  version: "0.1a1"

source:
  fn: collective.iptvusp-0.1a1.zip
  url: https://pypi.python.org/packages/source/c/collective.iptvusp/collective.iptvusp-0.1a1.zip
  md5: 6329a297bd96051504077244c128c2e4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.iptvusp = collective.iptvusp:main
    #
    # Would create an entry point called collective.iptvusp that calls collective.iptvusp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone >=4.2
    - plone.app.dexterity[grok]

  run:
    - python
    - setuptools
    - setuptools
    - plone >=4.2
    - plone.app.dexterity[grok]

test:
  # Python imports
  imports:
    - collective
    - collective.iptvusp
    - collective.iptvusp.Extensions
    - collective.iptvusp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Use videos from http://iptv.usp.br/ in a Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
