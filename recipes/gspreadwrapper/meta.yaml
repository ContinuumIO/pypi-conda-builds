package:
  name: gspreadwrapper
  version: "0.1.3"

source:
  fn: gspreadwrapper-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gspreadwrapper/gspreadwrapper-0.1.3.tar.gz
  md5: c9899216091b85b1a6822dd484ec2dfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gspreadwrapper = gspreadwrapper:main
    #
    # Would create an entry point called gspreadwrapper that calls gspreadwrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gspread >=0.0.15

  run:
    - python
    - gspread >=0.0.15

test:
  # Python imports
  imports:
    - gspreadwrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://about.me/david_rossellat
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'mostly having fun with google spreadsheets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
