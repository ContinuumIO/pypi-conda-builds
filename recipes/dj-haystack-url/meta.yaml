package:
  name: dj-haystack-url
  version: "0.2.0"

source:
  fn: dj-haystack-url-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dj-haystack-url/dj-haystack-url-0.2.0.tar.gz
  md5: 26f071a15ce9ee619fee7975fcaf12f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj-haystack-url = dj-haystack-url:main
    #
    # Would create an entry point called dj-haystack-url that calls dj-haystack-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simpleenergy/dj-haystack-url
  license: BSD License
  summary: 'Haystack configuration from environment variable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
