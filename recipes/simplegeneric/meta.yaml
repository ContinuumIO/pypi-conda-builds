package:
  name: simplegeneric
  version: "0.8.1"

source:
  fn: simplegeneric-0.8.1.zip
  url: https://pypi.python.org/packages/source/s/simplegeneric/simplegeneric-0.8.1.zip
  md5: f9c1fab00fd981be588fc32759f474e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplegeneric = simplegeneric:main
    #
    # Would create an entry point called simplegeneric that calls simplegeneric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/simplegeneric
  license: Zope Public License
  summary: "Simple generic functions (similar to Python's own len(), pickle.dump(), etc.)"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
