package:
  name: django-basecontact
  version: "0.1.0"

source:
  fn: django-basecontact-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-basecontact/django-basecontact-0.1.0.tar.gz
  md5: 829b6cf31cfcfeae536970c80a3952aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-basecontact = django-basecontact:main
    #
    # Would create an entry point called django-basecontact that calls django-basecontact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - contact
    - default_contact
    - default_contact_with_phone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.de-nieuwbouwmakelaar.nl
  license: BSD License
  summary: 'Abstract contact form classes to provide a contact form backend and mailing functionality'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
