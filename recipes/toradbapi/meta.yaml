package:
  name: toradbapi
  version: "0.1.3"

source:
  fn: toradbapi-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/t/toradbapi/toradbapi-0.1.3.tar.gz
  md5: 5ad8c68e27c3bab0272e54847ecfd6ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - toradbapi = toradbapi:main
    #
    # Would create an entry point called toradbapi that calls toradbapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - twisted

  run:
    - python
    - tornado
    - twisted

test:
  # Python imports
  imports:
    - toradbapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/geerk/toradbapi
  license: MIT License
  summary: 'Wrapper for twisted.enterprise.adbapi.ConnectionPool to use with tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
