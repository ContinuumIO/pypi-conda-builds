package:
  name: experimental.ulocalized_time
  version: "0.1.0"

source:
  fn: experimental.ulocalized_time-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/experimental.ulocalized_time/experimental.ulocalized_time-0.1.0.tar.gz
  md5: 43f2c261180eb52838f8faddeded9cb6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.ulocalized_time = experimental.ulocalized_time:main
    #
    # Would create an entry point called experimental.ulocalized_time that calls experimental.ulocalized_time.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - experimental
    - experimental.ulocalized_time

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/experimental.ulocalized_time
  license: GNU General Public License (GPL)
  summary: 'Experimental version for ulocalized_time Plone function, that extends the date/time formats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
