package:
  name: mopidy-mpris
  version: "1.2.0"

source:
  fn: Mopidy-MPRIS-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-MPRIS/Mopidy-MPRIS-1.2.0.tar.gz
  md5: 516ec485a0925af102b129f80e4b3bb7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-mpris = mopidy-mpris:main
    #
    # Would create an entry point called mopidy-mpris that calls mopidy-mpris.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

test:
  # Python imports
  imports:
    - mopidy_mpris

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mopidy/mopidy-mpris
  license: Apache Software License
  summary: u'Mopidy extension for controlling Mopidy through the MPRIS D-Bus interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
