package:
  name: dap.responses.html
  version: "0.1"

source:
  fn: dap.responses.html-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.responses.html/dap.responses.html-0.1.tar.gz
  md5: 5461e708f2d8a4281f89016cd62b6e47
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.responses.html = dap.responses.html:main
    #
    # Would create an entry point called dap.responses.html that calls dap.responses.html.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dap[server] >=2.2.5

  run:
    - python
    - setuptools
    - dap[server] >=2.2.5

test:
  # Python imports
  imports:
    - dap
    - dap.responses
    - dap.responses.html

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydap.org/responses/html
  license: MIT License
  summary: 'Simple HTML form for pydap server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
