package:
  name: mr.crabby
  version: "1.5"

source:
  fn: mr.crabby-1.5.zip
  url: https://pypi.python.org/packages/source/m/mr.crabby/mr.crabby-1.5.zip
  md5: 7a1616aa9249566bfd4a9c3c790c5a7b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.crabby = mr.crabby:main
    #
    # Would create an entry point called mr.crabby that calls mr.crabby.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - mr
    - mr.crabby

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/davisagli/mr.crabby
  license: GPL
  summary: 'Mr. Crabby is your blue crabby friend for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
