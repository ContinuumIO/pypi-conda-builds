package:
  name: audiolazy
  version: "0.05"

source:
  fn: audiolazy-0.05.tar.gz
  url: https://pypi.python.org/packages/source/a/audiolazy/audiolazy-0.05.tar.gz
  md5: e681fe245bb63b9bac9782f956f25298
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audiolazy = audiolazy:main
    #
    # Would create an entry point called audiolazy that calls audiolazy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - audiolazy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danilobellini/audiolazy
  license: GNU General Public License v3 (GPLv3)
  summary: 'Real-Time Expressive Digital Signal Processing (DSP) Package for Python!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
