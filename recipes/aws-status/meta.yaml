package:
  name: aws-status
  version: "0.0.13"

source:
  fn: aws-status-0.0.13.tar.gz
  url: https://pypi.python.org/packages/source/a/aws-status/aws-status-0.0.13.tar.gz
  md5: c9869713d39ee589d5f7bd36e0520f1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aws-status = aws-status:main
    #
    # Would create an entry point called aws-status that calls aws-status.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feedparser >=5.1.3

  run:
    - python
    - feedparser >=5.1.3

test:
  # Python imports
  imports:
    - aws_status

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jbbarth/aws-status
  license: MIT License
  summary: 'Wraps AWS status informations obtained via the status page.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
