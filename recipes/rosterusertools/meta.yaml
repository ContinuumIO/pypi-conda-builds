package:
  name: rosterusertools
  version: "0.17"

source:
  fn: RosterUserTools-0.17.tar.gz
  url: https://pypi.python.org/packages/source/R/RosterUserTools/RosterUserTools-0.17.tar.gz
  md5: df37e2b162db78f96a278a5919ab6b9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rosterusertools = rosterusertools:main
    #
    # Would create an entry point called rosterusertools that calls rosterusertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - roster_user_tools
    - rosterusertools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/roster-dns-management/
  license: BSD License
  summary: 'RosterUserTools are a set of command line tools for Roster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
