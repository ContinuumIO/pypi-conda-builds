package:
  name: django-form-models
  version: "0.1.5"

source:
  fn: django-form-models-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-form-models/django-form-models-0.1.5.tar.gz
  md5: 37515ae8b9f4ac330bf07b5cf7e43f4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-form-models = django-form-models:main
    #
    # Would create an entry point called django-form-models that calls django-form-models.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-orderable >=1.0.1
    - django-appconf >=0.5

  run:
    - python
    - django-orderable >=1.0.1
    - django-appconf >=0.5

test:
  # Python imports
  imports:
    - form_models
    - form_models.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://incuna.com/
  license: UNKNOWN
  summary: 'Sample models for modelling django forms and fields, and integrate with crispy forms layout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
