package:
  name: balanced-ach
  version: "0.1.5"

source:
  fn: balanced-ach-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/balanced-ach/balanced-ach-0.1.5.tar.gz
  md5: 5c3937accd9f55fb9609c1bba6f426a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - balanced-ach = balanced-ach:main
    #
    # Would create an entry point called balanced-ach that calls balanced-ach.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso8601 >=0.1.4
    - simplejson >=2.3.2
    - wac >=0.11

  run:
    - python
    - iso8601 >=0.1.4
    - simplejson >=2.3.2
    - wac >=0.11

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/balanced/balanced-ach-python
  license: BSD License
  summary: 'Balanced ACH client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
