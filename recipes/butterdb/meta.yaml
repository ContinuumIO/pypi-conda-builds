package:
  name: butterdb
  version: "0.1.4"

source:
  fn: butterdb-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/b/butterdb/butterdb-0.1.4.tar.gz
  md5: 3472b5bd2d2a591b56364dea1d5cde67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - butterdb = butterdb:main
    #
    # Would create an entry point called butterdb that calls butterdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gspread

  run:
    - python
    - gspread

test:
  # Python imports
  imports:
    - butterdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Widdershin/butterdb
  license: MIT
  summary: 'butterdb is a Python ORM for Google Drive Spreadsheets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
