package:
  name: jungleweb
  version: "0.1"

source:
  fn: jungleweb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jungleweb/jungleweb-0.1.tar.gz
  md5: 7ca87a45d523f089c090409c333facfd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jungleweb = jungleweb:main
    #
    # Would create an entry point called jungleweb that calls jungleweb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jungleweb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apparentlymart/python-jungleweb
  license: junitxml
  summary: 'Utilities for storing webpage summary information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
