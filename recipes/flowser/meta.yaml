package:
  name: flowser
  version: "0.1.0"

source:
  fn: flowser-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flowser/flowser-0.1.0.tar.gz
  md5: 69306e6aa6efd2171918b14928d49816
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flowser = flowser:main
    #
    # Would create an entry point called flowser that calls flowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.4.1

  run:
    - python
    - boto >=2.4.1

test:
  # Python imports
  imports:
    - flowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pilt/flowser/
  license: MIT License
  summary: 'High-level interface for Amazon Simple Workflow'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
