package:
  name: django-validatorchain
  version: "0.1"

source:
  fn: django-validatorchain-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-validatorchain/django-validatorchain-0.1.tar.gz
  md5: 5b48005a1e6cc15c39e3e9d66d1db9ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-validatorchain = django-validatorchain:main
    #
    # Would create an entry point called django-validatorchain that calls django-validatorchain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zerok/django-validatorchain
  license: BSD
  summary: 'Helper for making form validators skippable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
