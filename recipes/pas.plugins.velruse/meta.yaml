package:
  name: pas.plugins.velruse
  version: "0.1.0a1"

source:
  fn: pas.plugins.velruse-0.1.0a1.zip
  url: https://pypi.python.org/packages/source/p/pas.plugins.velruse/pas.plugins.velruse-0.1.0a1.zip
  md5: b6c7e117ff6c29c7606f875308973fe7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pas.plugins.velruse = pas.plugins.velruse:main
    #
    # Would create an entry point called pas.plugins.velruse that calls pas.plugins.velruse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - z3c.form
    - plone.app.z3cform
    - products.pluggableauthservice
    - products.plonepas
    - requests

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - z3c.form
    - plone.app.z3cform
    - products.pluggableauthservice
    - products.plonepas
    - requests

test:
  # Python imports
  imports:
    - pas
    - pas.plugins
    - pas.plugins.velruse
    - pas.plugins.velruse.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RedTurtle/pas.plugins.velruse
  license: GPL
  summary: 'PAS plugin for Plone. Allow users to login using social networks through Velruse'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
