package:
  name: menhir.contenttype.folder
  version: "0.4"

source:
  fn: menhir.contenttype.folder-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/menhir.contenttype.folder/menhir.contenttype.folder-0.4.tar.gz
  md5: 0bee4bc138c00d8f9f48e0d30e4b22c1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - menhir.contenttype.folder = menhir.contenttype.folder:main
    #
    # Would create an entry point called menhir.contenttype.folder that calls menhir.contenttype.folder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - dolmen.app.container >=1.0b2
    - dolmen.app.content >=1.0b1
    - dolmen.app.layout
    - dolmen.app.viewselector
    - dolmen.content >=0.7
    - dolmen.menu
    - grokcore.view
    - megrok.layout
    - setuptools
    - zope.component
    - zope.container
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher

  run:
    - python
    - dolmen.app.container >=1.0b2
    - dolmen.app.content >=1.0b1
    - dolmen.app.layout
    - dolmen.app.viewselector
    - dolmen.content >=0.7
    - dolmen.menu
    - grokcore.view
    - megrok.layout
    - setuptools
    - zope.component
    - zope.container
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher

test:
  # Python imports
  imports:
    - menhir
    - menhir.contenttype
    - menhir.contenttype.folder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dolmen-project.org/
  license: GNU General Public License (GPL)
  summary: 'Dolmen contenttype extension : folder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
