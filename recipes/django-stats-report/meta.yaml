package:
  name: django-stats-report
  version: "0.0.1beta"

source:
  fn: django-stats-report-0.0.1beta.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stats-report/django-stats-report-0.0.1beta.tar.gz
  md5: 88a0cb554ca95f71c0698338bbee847a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stats-report = django-stats-report:main
    #
    # Would create an entry point called django-stats-report that calls django-stats-report.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_stats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Proprietay Software
  summary: 'Generate statistics about view usage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
