package:
  name: attest
  version: "0.5.3"

source:
  fn: Attest-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/A/Attest/Attest-0.5.3.tar.gz
  md5: ea70bf4784f0010fe5282cc666f344cf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - attest = attest:main
    #
    # Would create an entry point called attest that calls attest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - progressbar >=2.3
    - pygments

  run:
    - python
    - setuptools
    - progressbar >=2.3
    - pygments

test:
  # Python imports
  imports:
    - attest
    - attest.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dag/attest
  license: BSD License
  summary: 'Modern, Pythonic unit testing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
