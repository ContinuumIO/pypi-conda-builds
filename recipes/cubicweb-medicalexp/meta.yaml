package:
  name: cubicweb-medicalexp
  version: "0.12.3"

source:
  fn: cubicweb-medicalexp-0.12.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-medicalexp/cubicweb-medicalexp-0.12.3.tar.gz
  md5: c2900a6748e0527cf4d6a9e2eae35be4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-medicalexp = cubicweb-medicalexp:main
    #
    # Would create an entry point called cubicweb-medicalexp that calls cubicweb-medicalexp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18.0
    - cubicweb-container >=2.4.0
    - cubicweb-file

  run:
    - python
    - cubicweb >=3.18.0
    - cubicweb-container >=2.4.0
    - cubicweb-file

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-medicalexp
  license: LGPL
  summary: 'Cube for managing medical experiment data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
