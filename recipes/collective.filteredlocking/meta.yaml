package:
  name: collective.filteredlocking
  version: "1.2.0"

source:
  fn: collective.filteredlocking-1.2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.filteredlocking/collective.filteredlocking-1.2.0.zip
  md5: f23a8127f43fd9b008a00a7ce58485e6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.filteredlocking = collective.filteredlocking:main
    #
    # Would create an entry point called collective.filteredlocking that calls collective.filteredlocking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.locking

  run:
    - python
    - setuptools
    - setuptools
    - plone.locking

test:
  # Python imports
  imports:
    - collective
    - collective.filteredlocking

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'A new specific permission that allows unlock of Plone contents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
