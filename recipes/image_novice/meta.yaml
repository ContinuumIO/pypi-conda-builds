package:
  name: image_novice
  version: "0.1.3"

source:
  fn: image_novice-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/image_novice/image_novice-0.1.3.tar.gz
  md5: 6469ab80b113c35d097b1ab85d7299bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - image_novice = image_novice:main
    #
    # Would create an entry point called image_novice that calls image_novice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.7.1
    - pillow >=2.1.0

  run:
    - python
    - numpy >=1.7.1
    - pillow >=2.1.0

test:
  # Python imports
  imports:
    - image_novice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/image_novice/
  license: LICENSE.txt
  summary: 'A special Python image submodule for beginners.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
