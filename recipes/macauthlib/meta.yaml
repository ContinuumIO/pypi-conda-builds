package:
  name: macauthlib
  version: "0.6.0"

source:
  fn: macauthlib-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/m/macauthlib/macauthlib-0.6.0.tar.gz
  md5: f40df69905af19db3897edd59c94902d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - macauthlib = macauthlib:main
    #
    # Would create an entry point called macauthlib that calls macauthlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob

  run:
    - python
    - webob

test:
  # Python imports
  imports:
    - macauthlib
    - macauthlib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla-services/macauthlib
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'macauth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
