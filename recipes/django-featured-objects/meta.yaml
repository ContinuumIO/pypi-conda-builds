package:
  name: django-featured-objects
  version: "0.1.1"

source:
  fn: django-featured-objects-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-featured-objects/django-featured-objects-0.1.1.tar.gz
  md5: b36f19e1a9a1c6d55ee7d0aa146ef63e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-featured-objects = django-featured-objects:main
    #
    # Would create an entry point called django-featured-objects that calls django-featured-objects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - featured
    - featured.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://desarrollo.witoi.com/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Django app for making any object featured'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
