package:
  name: tai64n
  version: "0.3.3"

source:
  fn: tai64n-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tai64n/tai64n-0.3.3.tar.gz
  md5: b491f8034cf7f14d71c509139b469632
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tai64n = tai64n:main
    #
    # Would create an entry point called tai64n that calls tai64n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - tai64n

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/hinnerk/py-tai64n/
  license: BSD License
  summary: 'Converts TAI64(n) string to datetime.datetime (UTC) object.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
