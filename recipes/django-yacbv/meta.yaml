package:
  name: django-yacbv
  version: "0.0.1"

source:
  fn: django-yacbv-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yacbv/django-yacbv-0.0.1.tar.gz
  md5: 7473ab8c63c8c061f055319eef2ea5d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yacbv = django-yacbv:main
    #
    # Would create an entry point called django-yacbv that calls django-yacbv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5.5

  run:
    - python
    - django ==1.5.5

test:
  # Python imports
  imports:
    - yacbv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hirokiky/django-yacbv
  license: MIT License
  summary: "Replacement for Django's ClassBasedView."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
