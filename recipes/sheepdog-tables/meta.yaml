package:
  name: sheepdog-tables
  version: "1.2.0"

source:
  fn: sheepdog-tables-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sheepdog-tables/sheepdog-tables-1.2.0.tar.gz
  md5: 954d231295d5d67c7974ff706414e127
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sheepdog-tables = sheepdog-tables:main
    #
    # Would create an entry point called sheepdog-tables that calls sheepdog-tables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - doc
    - sheepdog_tables
    - sheepdog_tables.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SheepDogInc/sheepdog_tables
  license: BSD License
  summary: 'Easy to use tables API for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
