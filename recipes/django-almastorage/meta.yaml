package:
  name: django-almastorage
  version: "0.1.2"

source:
  fn: django-almastorage-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-almastorage/django-almastorage-0.1.2.tar.gz
  md5: afc068530b4e3b17af9f63900353d6f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-almastorage = django-almastorage:main
    #
    # Would create an entry point called django-almastorage that calls django-almastorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - python-swiftclient
    - django-tastypie
    - requests

  run:
    - python
    - django >=1.5
    - python-swiftclient
    - django-tastypie
    - requests

test:
  # Python imports
  imports:
    - almastorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aaannnyyy/django-almastorage
  license: UNKNOWN
  summary: 'A simple Django app to use SwiftStack Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
