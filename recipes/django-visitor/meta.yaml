package:
  name: django-visitor
  version: "0.1.7"

source:
  fn: django-visitor-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-visitor/django-visitor-0.1.7.tar.gz
  md5: 095ef010b9b00a28bb038a0196e82ae2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-visitor = django-visitor:main
    #
    # Would create an entry point called django-visitor that calls django-visitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - visitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kencochrane/django-visitor/
  license: UNKNOWN
  summary: 'Track visitors across your site using a cookie'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
