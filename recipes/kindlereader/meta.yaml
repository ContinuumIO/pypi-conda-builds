package:
  name: kindlereader
  version: "0.0.2"

source:
  fn: kindlereader-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kindlereader/kindlereader-0.0.2.tar.gz
  md5: 9f0964d486c31844be5f924282dd879f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kindlereader = kindlereader:main
    #
    # Would create an entry point called kindlereader that calls kindlereader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - yakindlestrip >=1.35
    - librssreader >=0.0.5
    - tornado >=3.0
    - pytz >=2014.9
    - beautifulsoup >=3.2.1
    - python-gflags >=2.0

  run:
    - python
    - yakindlestrip >=1.35
    - librssreader >=0.0.5
    - tornado >=3.0
    - pytz >=2014.9
    - beautifulsoup >=3.2.1
    - python-gflags >=2.0

test:
  # Python imports
  imports:
    - kindlereader
    - krlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fireinice/kindlereader2
  license: kindlestrip
  summary: 'generate news for kindle reader from rss'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
