package:
  name: django-celery_clearcache
  version: "0.1"

source:
  fn: django-celery_clearcache-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery_clearcache/django-celery_clearcache-0.1.tar.gz
  md5: b9b012e96a1129151b2df78527f812ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery_clearcache = django-celery_clearcache:main
    #
    # Would create an entry point called django-celery_clearcache that calls django-celery_clearcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - celery_clearcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://v.licheni.net/drc/django-celer_clearcache.git
  license: GNU General Public License v3 (GPLv3)
  summary: 'Clear cached stale sessions with celery task'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
