package:
  name: plone.app.discussion
  version: "2.4.2"

source:
  fn: plone.app.discussion-2.4.2.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.discussion/plone.app.discussion-2.4.2.tar.gz
  md5: 5151e6303dfbbbc8a0fc99d28e107ed2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.discussion = plone.app.discussion:main
    #
    # Would create an entry point called plone.app.discussion that calls plone.app.discussion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - plone.app.layout
    - plone.app.registry
    - plone.app.uuid
    - plone.app.z3cform
    - plone.indexer
    - plone.registry
    - plone.z3cform
    - products.cmfplone
    - zodb3
    - zope.interface
    - zope.component
    - zope.annotation
    - zope.event
    - zope.container
    - zope.lifecycleevent
    - zope.site
    - z3c.form >=2.3.3

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - plone.app.layout
    - plone.app.registry
    - plone.app.uuid
    - plone.app.z3cform
    - plone.indexer
    - plone.registry
    - plone.z3cform
    - products.cmfplone
    - zodb3
    - zope.interface
    - zope.component
    - zope.annotation
    - zope.event
    - zope.container
    - zope.lifecycleevent
    - zope.site
    - z3c.form >=2.3.3

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.discussion
    - plone.app.discussion.browser
    - plone.app.discussion.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.discussion
  license: GPL
  summary: 'Enhanced discussion support for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
