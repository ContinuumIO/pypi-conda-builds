package:
  name: colorcells
  version: "0.1.1"

source:
  fn: colorcells-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colorcells/colorcells-0.1.1.tar.gz
  md5: 070dbf10e6c3f558207567168f5d6d4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorcells = colorcells:main
    #
    # Would create an entry point called colorcells that calls colorcells.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorcells

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://synhak.org/
  license: UNKNOWN
  summary: 'Serial interface to the ColorCells CC784 LED signs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
