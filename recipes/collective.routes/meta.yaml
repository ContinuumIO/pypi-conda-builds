package:
  name: collective.routes
  version: "1.1a2"

source:
  fn: collective.routes-1.1a2.zip
  url: https://pypi.python.org/packages/source/c/collective.routes/collective.routes-1.1a2.zip
  md5: 70ca807bbf7469dee30ccf6e4ab02593
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.routes = collective.routes:main
    #
    # Would create an entry point called collective.routes that calls collective.routes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.routes
    - collective.routes.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.routes
  license: GPL
  summary: 'ability to add route configuration for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
