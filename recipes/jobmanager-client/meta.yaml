package:
  name: jobmanager-client
  version: "0.5.2"

source:
  fn: jobmanager-client-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jobmanager-client/jobmanager-client-0.5.2.tar.gz
  md5: 83bfc1c29fa2931cc229dca77d095862
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobmanager-client = jobmanager-client:main
    #
    # Would create an entry point called jobmanager-client that calls jobmanager-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tbx
    - jobmanager-common
    - six >=1.8.0
    - pymongo <3.0
    - mongoengine
    - blinker
    - flask

  run:
    - python
    - tbx
    - jobmanager-common
    - six >=1.8.0
    - pymongo <3.0
    - mongoengine
    - blinker
    - flask

test:
  # Python imports
  imports:
    - jobmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ronhanson/python-jobmanager-client
  license: MIT License
  summary: 'Job Manager Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
