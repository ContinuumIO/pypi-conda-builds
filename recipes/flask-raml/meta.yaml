package:
  name: flask-raml
  version: "0.1.5"

source:
  fn: Flask-RAML-0.1.5.zip
  url: https://pypi.python.org/packages/source/F/Flask-RAML/Flask-RAML-0.1.5.zip
  md5: 14552b9cf6fb750fd1aa7011f9c70164
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-raml = flask-raml:main
    #
    # Would create an entry point called flask-raml that calls flask-raml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.5
    - flask-mime-encoders >=0.1.1
    - pyraml >=0.1.6

  run:
    - python
    - flask >=0.5
    - flask-mime-encoders >=0.1.1
    - pyraml >=0.1.6

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/salsita/flask-raml
  license: MIT License
  summary: 'Flask-RAML (REST API Markup Language) API server with parameter conversion, response encoding, and examples'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
