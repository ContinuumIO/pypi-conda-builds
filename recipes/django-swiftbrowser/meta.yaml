package:
  name: django-swiftbrowser
  version: "0.2"

source:
  fn: django-swiftbrowser-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-swiftbrowser/django-swiftbrowser-0.2.tar.gz
  md5: a5a69b02b0e82156b2d2f993f219e8b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-swiftbrowser = django-swiftbrowser:main
    #
    # Would create an entry point called django-swiftbrowser that calls django-swiftbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - python-swiftclient

  run:
    - python
    - django >=1.5
    - python-swiftclient

test:
  # Python imports
  imports:
    - swiftbrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cschwede/django-swiftbrowser
  license: Apache Software License
  summary: 'A simple Django app to access Openstack Swift'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
