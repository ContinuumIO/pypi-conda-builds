package:
  name: device-proxy
  version: "0.4.1"

source:
  fn: device-proxy-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/device-proxy/device-proxy-0.4.1.tar.gz
  md5: 3a2bbb03aea9a1768cdbb081faf7ecee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - device-proxy = device-proxy:main
    #
    # Would create an entry point called device-proxy that calls device-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - pywurfl
    - pyyaml

  run:
    - python
    - twisted
    - pywurfl
    - pyyaml

test:
  # Python imports
  imports:
    - devproxy
    - twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/device-proxy
  license: BSD License
  summary: 'Device Proxy. A reverse HTTP Proxy that can inspect and manipulate HTTP Headers before sending upstream.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
