package:
  name: flask-exceptional
  version: "0.5.4"

source:
  fn: Flask-Exceptional-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Exceptional/Flask-Exceptional-0.5.4.tar.gz
  md5: 57316ff7b48e218c157c36cb1589067e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-exceptional = flask-exceptional:main
    #
    # Would create an entry point called flask-exceptional that calls flask-exceptional.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jzempel/flask-exceptional
  license: BSD License
  summary: 'Adds Exceptional support to Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
