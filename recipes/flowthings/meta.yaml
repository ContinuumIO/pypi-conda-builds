package:
  name: flowthings
  version: "1.0.1"

source:
  fn: flowthings-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flowthings/flowthings-1.0.1.tar.gz
  md5: e61891f047cfc51bc1370949cccb34bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flowthings = flowthings:main
    #
    # Would create an entry point called flowthings that calls flowthings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - websocket-client

  run:
    - python
    - requests
    - websocket-client

test:
  # Python imports
  imports:
    - flowthings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/flowthings/python-client
  license: UNKNOWN
  summary: 'API client for the flowthings.io platform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
