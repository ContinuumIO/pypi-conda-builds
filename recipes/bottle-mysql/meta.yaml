package:
  name: bottle-mysql
  version: "0.2.2"

source:
  fn: bottle-mysql-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-mysql/bottle-mysql-0.2.2.tar.gz
  md5: c342e8e4e08f6d111da9c4b2158b3f24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-mysql = bottle-mysql:main
    #
    # Would create an entry point called bottle-mysql that calls bottle-mysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tg123/bottle-mysql
  license: MIT License
  summary: 'MySQL integration for Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
