package:
  name: ffx
  version: "1.3.4"

source:
  fn: ffx-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/f/ffx/ffx-1.3.4.tar.gz
  md5: 98af7674a62f3b925a895a68df294fb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ffx = ffx:main
    #
    # Would create an entry point called ffx that calls ffx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ffx
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/natekupp/ffx
  license: See readme
  summary: 'Fast Function Extraction: A fast, scalable, and deterministic symbolic regression tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
