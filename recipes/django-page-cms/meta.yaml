package:
  name: django-page-cms
  version: "1.8.0"

source:
  fn: django-page-cms-1.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-page-cms/django-page-cms-1.8.0.tar.gz
  md5: d39fce2752378812a2a952729faca5fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-page-cms = django-page-cms:main
    #
    # Would create an entry point called django-page-cms that calls django-page-cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.8,<1.9
    - django-mptt >=0.7.2
    - six >=1.5.2
    - pillow

  run:
    - python
    - django >=1.8,<1.9
    - django-mptt >=0.7.2
    - six >=1.5.2
    - pillow

test:
  # Python imports
  imports:
    - pages
    - pages.admin
    - pages.management
    - pages.management.commands
    - pages.plugins
    - pages.plugins.jsonexport
    - pages.plugins.jsonexport.management
    - pages.plugins.jsonexport.management.commands
    - pages.plugins.pofiles
    - pages.plugins.pofiles.management
    - pages.plugins.pofiles.management.commands
    - pages.templatetags
    - pages.testproj
    - pages.testproj.documents
    - pages.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django-page-cms/
  license: BSD License
  summary: 'A tree based Django CMS application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
