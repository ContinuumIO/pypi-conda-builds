package:
  name: gottwall-backend-redis
  version: "0.0.1"

source:
  fn: gottwall-backend-redis-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gottwall-backend-redis/gottwall-backend-redis-0.0.1.tar.gz
  md5: 6cb276e7f247ed2bdbac6397d821e898
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gottwall-backend-redis = gottwall-backend-redis:main
    #
    # Would create an entry point called gottwall-backend-redis that calls gottwall-backend-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis ==2.9.0
    - tornado-redis ==2.4.15
    - gottwall ==0.5.0

  run:
    - python
    - redis ==2.9.0
    - tornado-redis ==2.4.15
    - gottwall ==0.5.0

test:
  # Python imports
  imports:
    - gw_backend_redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GottWall/gottwall-backend-redis
  license: BSD License
  summary: 'Redis transport backend for GottWall statistics aggregator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
