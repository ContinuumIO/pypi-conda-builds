package:
  name: imdbpie
  version: "2.1.0"

source:
  fn: imdbpie-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/imdbpie/imdbpie-2.1.0.tar.gz
  md5: 2b3ffff4c4439026a35ed4f6ea11fe3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imdbpie = imdbpie:main
    #
    # Would create an entry point called imdbpie that calls imdbpie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - six

  run:
    - python
    - requests
    - six

test:
  # Python imports
  imports:
    - imdbpie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'Python IMDB client using the IMDB json web service made available for their iOS app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
