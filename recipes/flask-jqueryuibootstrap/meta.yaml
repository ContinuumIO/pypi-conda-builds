package:
  name: flask-jqueryuibootstrap
  version: "0.5.0.4"

source:
  fn: Flask-JqueryUiBootstrap-0.5.0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-JqueryUiBootstrap/Flask-JqueryUiBootstrap-0.5.0.4.tar.gz
  md5: 9cc607d7290e9c794c5cea7ad3cfd1c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-jqueryuibootstrap = flask-jqueryuibootstrap:main
    #
    # Would create an entry point called flask-jqueryuibootstrap that calls flask-jqueryuibootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - SAMPLE_PROJECT
    - flask_jqueryuibootstrap
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lightningwolf/Flask-JqueryUiBootstrap
  license: MIT License
  summary: 'Flask jQuery UI Bootstrap minimalistic fork of Flask-Bootsrap extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
