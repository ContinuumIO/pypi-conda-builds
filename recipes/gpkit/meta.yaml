package:
  name: gpkit
  version: "0.1.1"

source:
  fn: gpkit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gpkit/gpkit-0.1.1.tar.gz
  md5: e264f87785b7878d23be46684ea32881
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gpkit = gpkit:main
    #
    # Would create an entry point called gpkit that calls gpkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - ctypesgen >=0.r125

  run:
    - python
    - numpy
    - ctypesgen >=0.r125

test:
  # Python imports
  imports:
    - gpkit
    - gpkit._mosek
    - gpkit.interactive
    - gpkit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/convexopt/gpkit
  license: gpk-voicing
  summary: 'Package for defining and manipulating geometric programming models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
