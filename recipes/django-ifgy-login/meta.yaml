package:
  name: django-ifgy-login
  version: "0.3"

source:
  fn: django-ifgy-login-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ifgy-login/django-ifgy-login-0.3.tar.gz
  md5: 3aa9d6cced0e97d21161f825e1f28eaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ifgy-login = django-ifgy-login:main
    #
    # Would create an entry point called django-ifgy-login that calls django-ifgy-login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ifgy_login

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infranology
  license: BSD License
  summary: 'A simple Django app to conduct login/logout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
