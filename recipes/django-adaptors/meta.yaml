package:
  name: django-adaptors
  version: "0.2.5"

source:
  fn: django-adaptors-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adaptors/django-adaptors-0.2.5.tar.gz
  md5: 9bea90077c303a43291b03f4e15cac38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adaptors = django-adaptors:main
    #
    # Would create an entry point called django-adaptors that calls django-adaptors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - adaptor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Convert CSV/XML files into python object or django model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
