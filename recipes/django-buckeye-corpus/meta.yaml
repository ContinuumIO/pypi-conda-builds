package:
  name: django-buckeye-corpus
  version: "0.3.55"

source:
  fn: django-buckeye-corpus-0.3.55.tar.gz
  url: https://pypi.python.org/packages/source/d/django-buckeye-corpus/django-buckeye-corpus-0.3.55.tar.gz
  md5: 76e56c1166e177de68ad61dff90bc2a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-buckeye-corpus = django-buckeye-corpus:main
    #
    # Would create an entry point called django-buckeye-corpus that calls django-buckeye-corpus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - django-celery
    - python-praat-scripts
    - pillow
    - linguistic-helper-functions
    - django-picklefield

  run:
    - python
    - django
    - django-celery
    - python-praat-scripts
    - pillow
    - linguistic-helper-functions
    - django-picklefield

test:
  # Python imports
  imports:
    - buckeyebrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-buckeye-corpus/
  license: LICENSE.txt
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
