package:
  name: flask-ratelimiter
  version: "0.2.0"

source:
  fn: Flask-RateLimiter-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-RateLimiter/Flask-RateLimiter-0.2.0.tar.gz
  md5: d94f0dfc9c030225300cd9deefa30d01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-ratelimiter = flask-ratelimiter:main
    #
    # Would create an entry point called flask-ratelimiter that calls flask-ratelimiter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - six

  run:
    - python
    - flask
    - six

test:
  # Python imports
  imports:
    - flask_ratelimiter
    - flask_ratelimiter.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/inveniosoftware/flask-ratelimiter/
  license: BSD License
  summary: 'Flask-RateLimiter is an extension for Flask that adds support for rate limiting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
