package:
  name: django-asterisk
  version: "0.1"

source:
  fn: django-asterisk-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-asterisk/django-asterisk-0.1.tar.gz
  md5: 24204f3a9b980d84f01793d54708c4d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-asterisk = django-asterisk:main
    #
    # Would create an entry point called django-asterisk that calls django-asterisk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_asterisk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxiberta/django-asterisk
  license: MIT License
  summary: 'Phone call queuing and management for the Django web framework using an Asterisk server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
