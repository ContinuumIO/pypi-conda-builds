package:
  name: dhp
  version: "0.0.5"

source:
  fn: dhp-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dhp/dhp-0.0.5.tar.gz
  md5: eb483a84bdbccacb74b68786c8d4f07e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dhp = dhp:main
    #
    # Would create an entry point called dhp that calls dhp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dhp
    - dhp.VI
    - dhp.test
    - dhp.xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dundeemt/dhp
  license: BSD License
  summary: 'A collection of handy modules, almost guaranteed to get you into trouble.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
