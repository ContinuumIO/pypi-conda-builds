package:
  name: django-migration-fixture
  version: "0.1.4"

source:
  fn: django-migration-fixture-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-migration-fixture/django-migration-fixture-0.1.4.tar.gz
  md5: 0fe790c14f2d5b86f84b412d39f05af7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-migration-fixture = django-migration-fixture:main
    #
    # Would create an entry point called django-migration-fixture that calls django-migration-fixture.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_migration_fixture
    - django_migration_fixture.management
    - django_migration_fixture.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alexhayes/django-migration-fixture
  license: MIT License
  summary: 'Django app to easily turn initial_data.* fixtures into Django 1.7 data migrations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
