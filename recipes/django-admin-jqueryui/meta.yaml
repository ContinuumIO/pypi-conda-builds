package:
  name: django-admin-jqueryui
  version: "1.8.22"

source:
  fn: django-admin-jqueryui-1.8.22.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-jqueryui/django-admin-jqueryui-1.8.22.tar.gz
  md5: bcb15777d9a13dd2079923ccb1530cf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-jqueryui = django-admin-jqueryui:main
    #
    # Would create an entry point called django-admin-jqueryui that calls django-admin-jqueryui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_jqueryui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hint/django-admin-jqueryui
  license: BSD License
  summary: 'Simply adds a jquery ui to the admin panel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
