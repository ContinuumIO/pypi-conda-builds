package:
  name: django-counter-field
  version: "0.3.1"

source:
  fn: django-counter-field-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-counter-field/django-counter-field-0.3.1.tar.gz
  md5: 23d17f96182dff55ae7264aed25c7e04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-counter-field = django-counter-field:main
    #
    # Would create an entry point called django-counter-field that calls django-counter-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-model-changes >=0.15

  run:
    - python
    - django-model-changes >=0.15

test:
  # Python imports
  imports:
    - django_counter_field
    - django_counter_field.management
    - django_counter_field.management.commands
    - docs
    - docs.source

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kajic/django-counter-field
  license: MIT License
  summary: 'django-counter-field makes it extremely easy to denormalize and keep track of related model counts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
