package:
  name: erepapi
  version: "0.1.0"

source:
  fn: erepapi-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/erepapi/erepapi-0.1.0.tar.gz
  md5: 4a72141ca64c78b6627d3c86c64a1a26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - erepapi = erepapi:main
    #
    # Would create an entry point called erepapi that calls erepapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - erepapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Nikola-K/erepapi
  license: MIT License
  summary: 'Unofficial python wrapper for erepublik api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
