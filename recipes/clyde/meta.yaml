package:
  name: clyde
  version: "0.9.1"

source:
  fn: clyde-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clyde/clyde-0.9.1.tar.gz
  md5: b9d570ac22b329ca11335b3c6be980e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clyde = clyde:main
    #
    # Would create an entry point called clyde that calls clyde.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - clyde
    - clyde.command
    - clyde.formatter
    - clyde.helpers
    - clyde.mixin
    - clyde.parser
    - clyde.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clyde-hub/clyde
  license: MIT License
  summary: 'Clyde is a command line interface designer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
