package:
  name: pulse
  version: "0.1.2"

source:
  fn: pulse-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pulse/pulse-0.1.2.tar.gz
  md5: 567471302578cfb09fdaf83e36f57cc8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pulse = pulse:main
    #
    # Would create an entry point called pulse that calls pulse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob >=-0.9.6
    - pastedeploy >=1.3.3

  run:
    - python
    - setuptools
    - webob >=-0.9.6
    - pastedeploy >=1.3.3

test:
  # Python imports
  imports:
    - pulse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT Licence
  summary: 'Simple scheduling WSGI middleware.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
