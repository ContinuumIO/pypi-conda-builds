package:
  name: django-embed9
  version: "0.3"

source:
  fn: django-embed9-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-embed9/django-embed9-0.3.tar.gz
  md5: dfb19287ea36c3b2e9cc8b746135c60c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-embed9 = django-embed9:main
    #
    # Would create an entry point called django-embed9 that calls django-embed9.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.4

  run:
    - python
    - django >=1.7.4

test:
  # Python imports
  imports:
    - embed9

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/9dev/django-embed9
  license: MIT License
  summary: 'Django app that make model objects embeddable on 3rd party websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
