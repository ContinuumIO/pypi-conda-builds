package:
  name: django-pushserver
  version: "0.3.4"

source:
  fn: django-pushserver-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pushserver/django-pushserver-0.3.4.tar.gz
  md5: f421acffe633abf23b2fb57fa2b50c54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pushserver = django-pushserver:main
    #
    # Would create an entry point called django-pushserver that calls django-pushserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - py_hbpush ==0.1.4
    - tornado <3

  run:
    - python
    - django >=1.2
    - py_hbpush ==0.1.4
    - tornado <3

test:
  # Python imports
  imports:
    - pushserver
    - pushserver.management
    - pushserver.management.commands
    - pushserver.templatetags
    - pushserver.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mitar/django-pushserver
  license: GNU Affero General Public License v3
  summary: "Push server for Django based on Leo Ponomarev's Basic HTTP Push Relay Protocol."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
