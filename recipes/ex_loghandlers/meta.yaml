package:
  name: ex_loghandlers
  version: "0.1"

source:
  fn: ex_loghandlers-0.1.zip
  url: https://pypi.python.org/packages/source/e/ex_loghandlers/ex_loghandlers-0.1.zip
  md5: a9553687951ecf4d9600f40aadc2049e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ex_loghandlers = ex_loghandlers:main
    #
    # Would create an entry point called ex_loghandlers that calls ex_loghandlers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ex_loghandlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://victorlin@bitbucket.org/victorlin/ex_loghandlers
  license: MIT
  summary: 'A set of extended logging handlers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
