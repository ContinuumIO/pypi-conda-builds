package:
  name: zoop
  version: "0.1.1"

source:
  fn: zoop-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zoop/zoop-0.1.1.tar.gz
  md5: 63cbfe0bb90d8f7baf8583bcf41c1158
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zoop = zoop:main
    #
    # Would create an entry point called zoop that calls zoop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - zoop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/davidmiller/zoop
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Pythonic API and common paradigms for Zookeeper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
