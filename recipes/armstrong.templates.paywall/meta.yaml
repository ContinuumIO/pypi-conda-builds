package:
  name: armstrong.templates.paywall
  version: "1.0.0"

source:
  fn: armstrong.templates.paywall-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.templates.paywall/armstrong.templates.paywall-1.0.0.tar.gz
  md5: b9b3d81cf58db2e12cf8f1462b9696bc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.templates.paywall = armstrong.templates.paywall:main
    #
    # Would create an entry point called armstrong.templates.paywall that calls armstrong.templates.paywall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - armstrong.templates.paywall

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Provides a basic example of a paywall inside Armstrong'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
