package:
  name: antigate
  version: "1.3.1"

source:
  fn: antigate-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/antigate/antigate-1.3.1.tar.gz
  md5: 69e3190c37c3e4c68f374004e6e7050a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - antigate = antigate:main
    #
    # Would create an entry point called antigate that calls antigate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycurl
    - lxml
    - grab
    - xmltodict

  run:
    - python
    - pycurl
    - lxml
    - grab
    - xmltodict

test:
  # Python imports
  imports:
    - antigate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gotlium/antigate
  license: GNU General Public License (GPL)
  summary: 'Easy wrapper for antigate.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
