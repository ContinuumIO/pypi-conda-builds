package:
  name: gocept.rdbmanagement
  version: "0.1.2"

source:
  fn: gocept.rdbmanagement-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.rdbmanagement/gocept.rdbmanagement-0.1.2.tar.gz
  md5: 1d37a16248e649ab0267fdfd8b2cea74
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.rdbmanagement = gocept.rdbmanagement:main
    #
    # Would create an entry point called gocept.rdbmanagement that calls gocept.rdbmanagement.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - psycopg2 >=2.0.6
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - psycopg2 >=2.0.6
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - gocept
    - gocept.rdbmanagement

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.rdbmanagement
  license: Zope Public License
  summary: 'Recipe for managing RDB schemas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
