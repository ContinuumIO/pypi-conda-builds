package:
  name: jmbo-generic
  version: "0.0.2"

source:
  fn: jmbo-generic-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-generic/jmbo-generic-0.0.2.tar.gz
  md5: 710ea6da412948760d6a898882894fdd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-generic = jmbo-generic:main
    #
    # Would create an entry point called jmbo-generic that calls jmbo-generic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-category
    - django-preferences
    - django-snippetscream
    - django-ckeditor
    - jmbo
    - south

  run:
    - python
    - django-category
    - django-preferences
    - django-snippetscream
    - django-ckeditor
    - jmbo
    - south

test:
  # Python imports
  imports:
    - generic
    - generic.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-generic
  license: BSD License
  summary: 'Jmbo generic behaviour/templates app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
