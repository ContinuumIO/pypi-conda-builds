package:
  name: goulib
  version: "1.9.0"

source:
  fn: Goulib-1.9.0.zip
  url: https://pypi.python.org/packages/source/G/Goulib/Goulib-1.9.0.zip
  md5: 3da3b3a9cb6d288d6b66e00ddbd82816
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goulib = goulib:main
    #
    # Would create an entry point called goulib that calls goulib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - Goulib
    - goulib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/goulu/goulib
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'library of useful Python code for scientific + technical applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
