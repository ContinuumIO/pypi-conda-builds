package:
  name: diff_match_patch_python
  version: "1.0.1"

source:
  fn: diff_match_patch_python-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/diff_match_patch_python/diff_match_patch_python-1.0.1.tar.gz
  md5: f984c5dce6c5df4aa54b2cea757b23a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diff_match_patch_python = diff_match_patch_python:main
    #
    # Would create an entry point called diff_match_patch_python that calls diff_match_patch_python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - diff_match_patch_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JoshData/diff_match_patch-python
  license: CC0 (copyright waived)
  summary: u"A Python extension module that wraps Google's diff_match_patch C++ implementation for very fast string comparisons."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
