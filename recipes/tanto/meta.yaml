package:
  name: tanto
  version: "1.1"

source:
  fn: tanto-1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tanto/tanto-1.1.tar.gz
  md5: 480a1549803eb96cf221847ac10c6d3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tanto = tanto:main
    #
    # Would create an entry point called tanto that calls tanto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - configobj >=4.7.2
    - pynsca >=1.2
    - requests >=0.10.1

  run:
    - python
    - configobj >=4.7.2
    - pynsca >=1.2
    - requests >=0.10.1

test:
  # Python imports
  imports:
    - monitoring_agent
    - monitoring_agent.inputs
    - monitoring_agent.outputs
    - tanto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Eyepea/tanto
  license: GNU Affero General Public License v3
  summary: 'Takes monitoring data from Nagios-plugins to push with NSCA (Nagios or Icinga) or WS-Shinken.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
