package:
  name: json-test
  version: "0.1"

source:
  fn: json-test-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/json-test/json-test-0.1.tar.gz
  md5: d635952e79b523a930d67775aae7b1c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-test = json-test:main
    #
    # Would create an entry point called json-test that calls json-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - json_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/inkmonk/json-test
  license: BSD3
  summary: 'A python library for assisting you in writing tests against a JSON structure.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
