package:
  name: easydialogs-gtk
  version: "0.1"

source:
  fn: easydialogs-gtk-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/easydialogs-gtk/easydialogs-gtk-0.1.tar.gz
  md5: 65105bcd8eb711b1f8c54e9d19c4bce7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easydialogs-gtk = easydialogs-gtk:main
    #
    # Would create an entry point called easydialogs-gtk that calls easydialogs-gtk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygtk >=2.0

  run:
    - python
    - pygtk >=2.0

test:
  # Python imports
  imports:
    - EasyDialogs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/easydialogs-gtk/
  license: http://www.opensource.org/licenses/bsd-license.php
  summary: 'EasyDialogs for Linux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
