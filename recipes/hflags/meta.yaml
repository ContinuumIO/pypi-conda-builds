package:
  name: hflags
  version: "1.0"

source:
  fn: hflags-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hflags/hflags-1.0.tar.gz
  md5: 4e05a2c1966751a5b2dd3e20138ca4d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hflags = hflags:main
    #
    # Would create an entry point called hflags that calls hflags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-gflags

  run:
    - python
    - python-gflags

test:
  # Python imports
  imports:
    - hflags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/termie/hflags
  license: Apache 2.0
  summary: 'Extended Google Commandline Flags Module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
