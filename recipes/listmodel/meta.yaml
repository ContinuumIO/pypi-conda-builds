package:
  name: listmodel
  version: "0.2.1"

source:
  fn: listmodel-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/l/listmodel/listmodel-0.2.1.tar.gz
  md5: f07f163b88396e98eeaa23a23e836c8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - listmodel = listmodel:main
    #
    # Would create an entry point called listmodel that calls listmodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonpath_rw
    - lxml
    - pyyaml

  run:
    - python
    - jsonpath_rw
    - lxml
    - pyyaml

test:
  # Python imports
  imports:
    - listmodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jackuess/listmodel
  license: LGPL v3
  summary: 'Listmodel is a Python library for object mappings for various list sources (XML documents, CSV documents, text documents, JSON/YAML objects) in a unified manner.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
