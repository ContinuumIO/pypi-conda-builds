package:
  name: ao.social
  version: "1.0.2"

source:
  fn: ao.social-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/ao.social/ao.social-1.0.2.tar.gz
  md5: c5a959552a1b521437283950bcdb0007
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ao.social = ao.social:main
    #
    # Would create an entry point called ao.social that calls ao.social.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beaker
    - oauth
    - pyfacebook
    - python-openid >=2.2.5

  run:
    - python
    - beaker
    - oauth
    - pyfacebook
    - python-openid >=2.2.5

test:
  # Python imports
  imports:
    - ao
    - ao.social
    - ao.social.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aatiis/ao.social
  license: GNU General Public License (GPL)
  summary: 'Social networking middleware for Facebook, Twitter, LinkedIn and Google (OpenID + Buzz)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
