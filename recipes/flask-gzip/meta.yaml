package:
  name: flask-gzip
  version: "0.1"

source:
  fn: Flask-gzip-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-gzip/Flask-gzip-0.1.tar.gz
  md5: 019281380d1fd4c4c90b5e6d8fc02549
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-gzip = flask-gzip:main
    #
    # Would create an entry point called flask-gzip that calls flask-gzip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elasticsales/flask-gzip
  license: BSD License
  summary: 'Compress responses in your Flask app with gzip.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
