package:
  name: django12factor
  version: "1.3"

source:
  fn: django12factor-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django12factor/django12factor-1.3.tar.gz
  md5: abc4a0f137a43af759d8baabee823219
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django12factor = django12factor:main
    #
    # Would create an entry point called django12factor that calls django12factor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dj-database-url ==0.3.0
    - dj-email-url ==0.0.2
    - django-cache-url ==0.8.0
    - six

  run:
    - python
    - dj-database-url ==0.3.0
    - dj-email-url ==0.0.2
    - django-cache-url ==0.8.0
    - six

test:
  # Python imports
  imports:
    - django12factor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/doismellburning/django12factor
  license: MIT License
  summary: 'django12factor: Bringing 12factor to Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
