package:
  name: auth-userpass
  version: "0.5.0"

source:
  fn: auth-userpass-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/a/auth-userpass/auth-userpass-0.5.0.tar.gz
  md5: 5b3600470136e8e9af944cbbd098302a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auth-userpass = auth-userpass:main
    #
    # Would create an entry point called auth-userpass that calls auth-userpass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.10
    - pycrypto >=2.6.1

  run:
    - python
    - pyyaml >=3.10
    - pycrypto >=2.6.1

test:
  # Python imports
  imports:
    - userpass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/francisluong/py-auth-userpass
  license: LICENSE.txt
  summary: 'Simple but not so secure username and password storage and retrieval'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
