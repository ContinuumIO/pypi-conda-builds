package:
  name: collective.formwidget.uberselect
  version: "0.1"

source:
  fn: collective.formwidget.uberselect-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.formwidget.uberselect/collective.formwidget.uberselect-0.1.tar.gz
  md5: 97845c34549a29d71e103e65bf7d3988
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.formwidget.uberselect = collective.formwidget.uberselect:main
    #
    # Would create an entry point called collective.formwidget.uberselect that calls collective.formwidget.uberselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.form
    - zope.interface
    - zope.schema
    - zope.i18nmessageid
    - z3c.formwidget.query

  run:
    - python
    - setuptools
    - z3c.form
    - zope.interface
    - zope.schema
    - zope.i18nmessageid
    - z3c.formwidget.query

test:
  # Python imports
  imports:
    - collective
    - collective.formwidget
    - collective.formwidget.uberselect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License or GNU Library or Lesser General Public License (LGPL)
  summary: 'An uberselection widget for z3c.form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
