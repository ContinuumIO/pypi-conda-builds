package:
  name: m01.logger
  version: "0.5.0"

source:
  fn: m01.logger-0.5.0.zip
  url: https://pypi.python.org/packages/source/m/m01.logger/m01.logger-0.5.0.zip
  md5: 84a173feaec7540784415d41574ff9e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m01.logger = m01.logger:main
    #
    # Would create an entry point called m01.logger that calls m01.logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo
    - m01.mongo
    - m01.stub

  run:
    - python
    - setuptools
    - pymongo
    - m01.mongo
    - m01.stub

test:
  # Python imports
  imports:
    - m01
    - m01.logger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/m01.logger
  license: Zope Public License
  summary: 'Blocking python logging handler and formatter using mongodb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
