package:
  name: django-user-sessions
  version: "1.1.0"

source:
  fn: django-user-sessions-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-sessions/django-user-sessions-1.1.0.tar.gz
  md5: 6f4e0cca845907059ca273928b96ab57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-sessions = django-user-sessions:main
    #
    # Would create an entry point called django-user-sessions that calls django-user-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - user_sessions
    - user_sessions.backends
    - user_sessions.migrations
    - user_sessions.south_migrations
    - user_sessions.templatetags
    - user_sessions.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Bouke/django-user-sessions
  license: MIT License
  summary: 'Django sessions with a foreign key to the user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
