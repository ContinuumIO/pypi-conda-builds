package:
  name: cubicweb-postgis
  version: "0.3.1"

source:
  fn: cubicweb-postgis-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-postgis/cubicweb-postgis-0.3.1.tar.gz
  md5: e2308be529b417e07ada5d5cc06fe478
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-postgis = cubicweb-postgis:main
    #
    # Would create an entry point called cubicweb-postgis that calls cubicweb-postgis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.15.1
    - cwtags >=1.1.0

  run:
    - python
    - cubicweb >=3.15.1
    - cwtags >=1.1.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-postgis
  license: LGPL
  summary: 'Test for postgis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
