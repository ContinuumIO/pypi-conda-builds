package:
  name: links-devops
  version: "0.1.6"

source:
  fn: links-devops-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/l/links-devops/links-devops-0.1.6.tar.gz
  md5: 743a36f1231d8d8eb0124782af258039
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - links-devops = links-devops:main
    #
    # Would create an entry point called links-devops that calls links-devops.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dapper-devops >=0.1.6

  run:
    - python
    - dapper-devops >=0.1.6

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://linkscreative.codebasehq.com/projects/links-creative/repositories/devops
  license: BSD License
  summary: 'Fabric deployment script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
