package:
  name: methylpurify
  version: "2.0-20141116"

source:
  fn: MethylPurify-2.0-20141116.tar.gz
  url: https://pypi.python.org/packages/source/M/MethylPurify/MethylPurify-2.0-20141116.tar.gz
  md5: 2eb38ab215dcede4fdd99798dc68b9db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - methylpurify = methylpurify:main
    #
    # Would create an entry point called methylpurify that calls methylpurify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyfasta

  run:
    - python
    - pyfasta

test:
  # Python imports
  imports:
    - methylpurify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Python Software Foundation License
  summary: 'Methylation subclone detection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
