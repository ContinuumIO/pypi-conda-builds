package:
  name: flask-mongorest
  version: "0.1.1"

source:
  fn: Flask-MongoRest-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MongoRest/Flask-MongoRest-0.1.1.tar.gz
  md5: 4bed7f085b8d5f80e0709a79dd1a5752
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mongorest = flask-mongorest:main
    #
    # Would create an entry point called flask-mongorest that calls flask-mongorest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flask_mongorest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/elasticsales/flask-mongorest
  license: BSD License
  summary: 'Flask restful API framework for MongoDB/MongoEngine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
