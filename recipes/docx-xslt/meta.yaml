package:
  name: docx-xslt
  version: "1.0.4"

source:
  fn: docx-xslt-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/docx-xslt/docx-xslt-1.0.4.tar.gz
  md5: e4434312c28c86063a8a82f94ab1a6f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docx-xslt = docx-xslt:main
    #
    # Would create an entry point called docx-xslt that calls docx-xslt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.3.2
    - unidecode >=0.04.17

  run:
    - python
    - lxml >=2.3.2
    - unidecode >=0.04.17

test:
  # Python imports
  imports:
    - docxxslt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/backbohne/docx-xslt
  license: MIT License
  summary: 'XSL transformation for Microsoft Word .docx files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
