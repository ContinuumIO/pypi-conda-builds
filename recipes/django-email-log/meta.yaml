package:
  name: django-email-log
  version: "0.2.0"

source:
  fn: django-email-log-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-log/django-email-log-0.2.0.tar.gz
  md5: 4dafb129e0a1050665e00062d34acba2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-log = django-email-log:main
    #
    # Would create an entry point called django-email-log that calls django-email-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - email_log
    - email_log.migrations
    - email_log.south_migrations
    - email_log.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/treyhunner/django-email-log
  license: MIT License
  summary: 'Django email backend that logs all emails'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
