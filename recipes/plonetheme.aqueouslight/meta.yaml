package:
  name: plonetheme.aqueouslight
  version: "1.0"

source:
  fn: plonetheme.aqueouslight-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.aqueouslight/plonetheme.aqueouslight-1.0.tar.gz
  md5: ae86eac95ff14afd9649c7dfdcf3c643
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.aqueouslight = plonetheme.aqueouslight:main
    #
    # Would create an entry point called plonetheme.aqueouslight that calls plonetheme.aqueouslight.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.aqueouslight

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stxnext.pl
  license: gpl
  summary: 'An installable Diazo theme for Plone 4.1'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
