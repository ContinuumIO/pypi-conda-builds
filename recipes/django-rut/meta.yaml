package:
  name: django-rut
  version: "1.1"

source:
  fn: django-rut-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rut/django-rut-1.1.tar.gz
  md5: 2dd8bcdfc027be2f86d6152d17309fec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rut = django-rut:main
    #
    # Would create an entry point called django-rut that calls django-rut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_rut
    - django_rut.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/edwardoyarzun/django-rut
  license: UNKNOWN
  summary: 'A simple Django template filter to format/unformat Chilean RUT'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
