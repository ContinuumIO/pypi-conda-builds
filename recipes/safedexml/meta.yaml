package:
  name: safedexml
  version: "0.5.1"

source:
  fn: safedexml-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/s/safedexml/safedexml-0.5.1.tar.gz
  md5: ac8abdd74bd3d3d86ef9100862365698
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - safedexml = safedexml:main
    #
    # Would create an entry point called safedexml that calls safedexml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - defusedxml

  run:
    - python
    - defusedxml

test:
  # Python imports
  imports:
    - safedexml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/dexml
  license: MIT License
  summary: 'a defusedxml version of dead-simple Object-XML mapper for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
