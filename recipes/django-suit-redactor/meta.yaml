package:
  name: django-suit-redactor
  version: "0.0.2"

source:
  fn: django-suit-redactor-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-suit-redactor/django-suit-redactor-0.0.2.tar.gz
  md5: 615bb6ee8edc72f775518a04d229b2d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-suit-redactor = django-suit-redactor:main
    #
    # Would create an entry point called django-suit-redactor that calls django-suit-redactor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - suit_redactor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/darklow/django-suit-redactor
  license: UNKNOWN
  summary: 'Imperavi Redactor (WYSIWYG editor) integration app for Django admin. http://imperavi.com/redactor/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
