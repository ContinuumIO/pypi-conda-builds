package:
  name: django-oopviews
  version: "0.2.0"

source:
  fn: django-oopviews-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oopviews/django-oopviews-0.2.0.tar.gz
  md5: c0c3b4954d962b942dba29bd0572da29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oopviews = django-oopviews:main
    #
    # Would create an entry point called django-oopviews that calls django-oopviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_oopviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zerok/django-oopviews/
  license: BSD License
  summary: 'django-oopviews provides a simple way to write Django-views in an object-oriented manner.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
