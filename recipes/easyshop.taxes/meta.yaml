package:
  name: easyshop.taxes
  version: "0.1a1"

source:
  fn: easyshop.taxes-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.taxes/easyshop.taxes-0.1a1.tar.gz
  md5: 2d5e5c52163425b7c5839f128ac705da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.taxes = easyshop.taxes:main
    #
    # Would create an entry point called easyshop.taxes that calls easyshop.taxes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.taxes
    - easyshop.taxes.adapters
    - easyshop.taxes.browser
    - easyshop.taxes.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'Tax management for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
