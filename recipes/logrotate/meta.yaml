package:
  name: logrotate
  version: "0.1.0"

source:
  fn: logrotate-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logrotate/logrotate-0.1.0.tar.gz
  md5: 4f5d4a9f346f4d1822edfded7498e976
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logrotate = logrotate:main
    #
    # Would create an entry point called logrotate that calls logrotate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - logrotate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org
  license: GNU Affero General Public License v3
  summary: "A small, simple logrotate implementation for the Python logging module that only rotates files (it doesn't create and delete them) and therefore maintains owner, group and permission information"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
