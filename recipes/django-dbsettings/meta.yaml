package:
  name: django-dbsettings
  version: "0.8.0"

source:
  fn: django-dbsettings-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbsettings/django-dbsettings-0.8.0.tar.gz
  md5: 9fd5e3c9b088e46c21cff70ced2ae3b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbsettings = django-dbsettings:main
    #
    # Would create an entry point called django-dbsettings that calls django-dbsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.11

  run:
    - python
    - django >=1.4.11

test:
  # Python imports
  imports:
    - dbsettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zlorf/django-dbsettings
  license: BSD License
  summary: 'Application settings whose values can be updated while a project is up and running.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
