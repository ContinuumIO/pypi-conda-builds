package:
  name: iptcinfo
  version: "1.9.5-6"

source:
  fn: IPTCInfo-1.9.5-6.tar.gz
  url: https://pypi.python.org/packages/source/I/IPTCInfo/IPTCInfo-1.9.5-6.tar.gz
  md5: 62976f0228efa517298dd6dc7669e56e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iptcinfo = iptcinfo:main
    #
    # Would create an entry point called iptcinfo that calls iptcinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iptcinfo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/gthomas/iptcinfo/downloads
  license: Artistic License or GNU General Public License (GPL)
  summary: "IPTCInfo: extract and modify IPTC (metadata) information on images - port of IPTCInfo.pm by Josh Carter <josh@multipart-mixed.com>'\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
