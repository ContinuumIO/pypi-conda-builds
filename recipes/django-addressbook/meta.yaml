package:
  name: django-addressbook
  version: "0.1.1"

source:
  fn: django-addressbook-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-addressbook/django-addressbook-0.1.1.tar.gz
  md5: aca90451f62512b7a24a87140850b78a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-addressbook = django-addressbook:main
    #
    # Would create an entry point called django-addressbook that calls django-addressbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vobject
    - django-uni-form

  run:
    - python
    - vobject
    - django-uni-form

test:
  # Python imports
  imports:
    - addressbook
    - addressbook.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iiilx/addressbook
  license: BSD license, see LICENSE.txt
  summary: 'A django app that allows users to create contact groups and contacts, as well as views for displaying a contact in hcard format, downloading a vcard for the contact, a gravatar, and a QR code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
