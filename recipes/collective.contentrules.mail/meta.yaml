package:
  name: collective.contentrules.mail
  version: "0.4"

source:
  fn: collective.contentrules.mail-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentrules.mail/collective.contentrules.mail-0.4.tar.gz
  md5: cbfcc2c06fac4d5ab75a111b9439d2c1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.mail = collective.contentrules.mail:main
    #
    # Would create an entry point called collective.contentrules.mail that calls collective.contentrules.mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.mail
    - collective.contentrules.mail.actions
    - collective.contentrules.mail.browser
    - collective.contentrules.mail.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.contentrules.mail
  license: GPL
  summary: 'Flexible mail content rule'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
