package:
  name: midentify
  version: "0.1"

source:
  fn: midentify-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/midentify/midentify-0.1.tar.gz
  md5: cf993ab33136cff7f9e96df550850464
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - midentify = midentify:main
    #
    # Would create an entry point called midentify that calls midentify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - midentify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/linkinpark342/midentify-py/
  license: UNKNOWN
  summary: "Identification of a file using MPlayer's midentify.sh script"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
