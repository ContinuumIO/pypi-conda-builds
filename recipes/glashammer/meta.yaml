package:
  name: glashammer
  version: "0.2.1"

source:
  fn: Glashammer-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/G/Glashammer/Glashammer-0.2.1.tar.gz
  md5: b56f62c4346ea891eca39c15df1630dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glashammer = glashammer:main
    #
    # Would create an entry point called glashammer that calls glashammer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - glashammer
    - glashammer.bundles
    - glashammer.bundles.auth
    - glashammer.bundles.forms
    - glashammer.bundles.i18n
    - glashammer.bundles.jquery
    - glashammer.tools
    - glashammer.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://glashammer.org
  license: GNU General Public License (GPL)
  summary: 'Full stack web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
