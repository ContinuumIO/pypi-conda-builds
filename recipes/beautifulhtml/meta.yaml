package:
  name: beautifulhtml
  version: "1.2.0"

source:
  fn: BeautifulHTML-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BeautifulHTML/BeautifulHTML-1.2.0.tar.gz
  md5: c0cffe910ee59a2bd8bc970404441102
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beautifulhtml = beautifulhtml:main
    #
    # Would create an entry point called beautifulhtml that calls beautifulhtml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - beautifulhtml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: NYSL
  summary: 'Can write a Beautiful HTML Code in python script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
