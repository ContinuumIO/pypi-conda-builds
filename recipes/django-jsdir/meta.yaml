package:
  name: django-jsdir
  version: "0.2.1"

source:
  fn: django-jsdir-0.2.1.zip
  url: https://pypi.python.org/packages/source/d/django-jsdir/django-jsdir-0.2.1.zip
  md5: fef5620a34f6ceb7e3db20b310732cc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsdir = django-jsdir:main
    #
    # Would create an entry point called django-jsdir that calls django-jsdir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - jsdir
    - jsdir.management
    - jsdir.management.commands
    - jsdir.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tkhyn/django-jsdir/
  license: MIT License
  summary: 'Eases the management of JS files in a django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
