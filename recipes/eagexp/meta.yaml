package:
  name: eagexp
  version: "0.1.0"

source:
  fn: eagexp-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/eagexp/eagexp-0.1.0.tar.gz
  md5: 8dad1642b914fe7d521571c3f36f6d8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eagexp = eagexp:main
    #
    # Would create an entry point called eagexp that calls eagexp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - easyprocess
    - entrypoint2
    - pyvirtualdisplay
    - path.py

  run:
    - python
    - easyprocess
    - entrypoint2
    - pyvirtualdisplay
    - path.py

test:
  # Python imports
  imports:
    - eagexp
    - eagexp.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ponty/eagexp
  license: BSD License
  summary: 'export eagle schematic or board to image or partlist'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
