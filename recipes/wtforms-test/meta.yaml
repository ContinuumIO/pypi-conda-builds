package:
  name: wtforms-test
  version: "0.1.1"

source:
  fn: WTForms-Test-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/W/WTForms-Test/WTForms-Test-0.1.1.tar.gz
  md5: e330b46ea282647a22b2188b495df517
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-test = wtforms-test:main
    #
    # Would create an entry point called wtforms-test that calls wtforms-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms >=1.0.2

  run:
    - python
    - wtforms >=1.0.2

test:
  # Python imports
  imports:
    - wtforms_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/wtforms-test
  license: BSD License
  summary: 'Various unit test helpers for WTForms based forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
