package:
  name: django-microblogging
  version: "0.1.2"

source:
  fn: django-microblogging-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-microblogging/django-microblogging-0.1.2.tar.gz
  md5: 334c47cef199a5586a6cc01916b85aaf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-microblogging = django-microblogging:main
    #
    # Would create an entry point called django-microblogging that calls django-microblogging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - microblogging
    - microblogging.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-microblogging/
  license: MIT
  summary: 'django-microblogging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
