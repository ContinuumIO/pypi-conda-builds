package:
  name: django-filebrowser-no-grappelli-nph
  version: "3.4.8"

source:
  fn: django-filebrowser-no-grappelli-nph-3.4.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filebrowser-no-grappelli-nph/django-filebrowser-no-grappelli-nph-3.4.8.tar.gz
  md5: e8155d45bf7ba7a4309bf5073fb9fab3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filebrowser-no-grappelli-nph = django-filebrowser-no-grappelli-nph:main
    #
    # Would create an entry point called django-filebrowser-no-grappelli-nph that calls django-filebrowser-no-grappelli-nph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - filebrowser
    - filebrowser.management
    - filebrowser.management.commands
    - filebrowser.templatetags
    - filebrowser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Media-Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
