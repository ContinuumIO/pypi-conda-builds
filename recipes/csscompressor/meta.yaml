package:
  name: csscompressor
  version: "0.9.3"

source:
  fn: csscompressor-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/c/csscompressor/csscompressor-0.9.3.tar.gz
  md5: 8dcbdfd131def953dbf1e649eaa5db92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csscompressor = csscompressor:main
    #
    # Would create an entry point called csscompressor that calls csscompressor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - csscompressor
    - csscompressor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sprymix/csscompressor
  license: BSD License
  summary: 'A python port of YUI CSS Compressor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
