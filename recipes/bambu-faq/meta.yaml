package:
  name: bambu-faq
  version: "0.1"

source:
  fn: bambu-faq-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-faq/bambu-faq-0.1.tar.gz
  md5: e7f091b0aa004185c2af2664593dae02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-faq = bambu-faq:main
    #
    # Would create an entry point called bambu-faq that calls bambu-faq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bambu.faq
    - bambu.faq.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-faq
  license: UNKNOWN
  summary: 'A simple set of models for a Frequently-Asked-Questions site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
