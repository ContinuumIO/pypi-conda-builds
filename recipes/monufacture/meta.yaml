package:
  name: monufacture
  version: "1.0.0"

source:
  fn: Monufacture-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Monufacture/Monufacture-1.0.0.tar.gz
  md5: 8b232dbc7d9ae174e982d801b1728bb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monufacture = monufacture:main
    #
    # Would create an entry point called monufacture that calls monufacture.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo
    - pytz

  run:
    - python
    - pymongo
    - pytz

test:
  # Python imports
  imports:
    - monufacture

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gamechanger/monufacture
  license: BSD
  summary: 'A lightweight factory framework for easily generating test data in MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
