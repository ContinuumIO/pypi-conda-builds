package:
  name: log2jsconsole
  version: "0.4"

source:
  fn: log2jsconsole-0.4.tar.gz
  url: https://pypi.python.org/packages/source/l/log2jsconsole/log2jsconsole-0.4.tar.gz
  md5: ce67c99c84482ef1ba25ea722322bdb0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - log2jsconsole = log2jsconsole:main
    #
    # Would create an entry point called log2jsconsole that calls log2jsconsole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug

  run:
    - python
    - setuptools
    - werkzeug

test:
  # Python imports
  imports:
    - log2jsconsole

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/StyleShare/log2jsconsole
  license: MIT License
  summary: 'Simple WSGI middleware that helps to log messages into JavaScript console object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
