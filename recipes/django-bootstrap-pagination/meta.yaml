package:
  name: django-bootstrap-pagination
  version: "1.5.1"

source:
  fn: django-bootstrap-pagination-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-pagination/django-bootstrap-pagination-1.5.1.tar.gz
  md5: 6d3fb7d63085853aa118fe253bdf724f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-pagination = django-bootstrap-pagination:main
    #
    # Would create an entry point called django-bootstrap-pagination that calls django-bootstrap-pagination.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap_pagination
    - bootstrap_pagination.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmcclell/django-bootstrap-pagination
  license: MIT License
  summary: 'Render Django Page objects as Bootstrap 3.x Pagination compatible HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
