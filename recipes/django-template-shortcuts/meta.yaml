package:
  name: django-template-shortcuts
  version: "0.1.1"

source:
  fn: django-template-shortcuts-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-shortcuts/django-template-shortcuts-0.1.1.tar.gz
  md5: c0908c2a74b0cd5f2ec97e411b59e25c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-shortcuts = django-template-shortcuts:main
    #
    # Would create an entry point called django-template-shortcuts that calls django-template-shortcuts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - template_shortcuts
    - template_shortcuts.providers
    - template_shortcuts.templatetags
    - template_shortcuts.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/comandrei/django-template-shortcuts/
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
