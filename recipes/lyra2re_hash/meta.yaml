package:
  name: lyra2re_hash
  version: "1.1.1"

source:
  fn: lyra2re_hash-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lyra2re_hash/lyra2re_hash-1.1.1.tar.gz
  md5: 8b27bc26070df585c9004143475796c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lyra2re_hash = lyra2re_hash:main
    #
    # Would create an entry point called lyra2re_hash that calls lyra2re_hash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lyra2re_hash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Bindings for Lyra2RE proof of work used by Vertcoin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
