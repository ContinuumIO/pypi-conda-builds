package:
  name: djangorestframework-chain
  version: "0.1.3"

source:
  fn: djangorestframework-chain-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-chain/djangorestframework-chain-0.1.3.tar.gz
  md5: 6af90db8685cd256b9fad69d14964a06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-chain = djangorestframework-chain:main
    #
    # Would create an entry point called djangorestframework-chain that calls djangorestframework-chain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework
    - django-filter

  run:
    - python
    - djangorestframework
    - django-filter

test:
  # Python imports
  imports:
    - rest_framework_chain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/philipn/django-rest-framework-chain
  license: BSD License
  summary: 'Chained relations and filtering for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
