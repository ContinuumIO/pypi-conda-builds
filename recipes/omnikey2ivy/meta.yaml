package:
  name: omnikey2ivy
  version: "0.1.2"

source:
  fn: omnikey2ivy-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/o/omnikey2ivy/omnikey2ivy-0.1.2.tar.gz
  md5: 257b8a8d484c9116dd0052c179503a9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omnikey2ivy = omnikey2ivy:main
    #
    # Would create an entry point called omnikey2ivy that calls omnikey2ivy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ivy-python >=2.1

  run:
    - python
    - ivy-python >=2.1

test:
  # Python imports
  imports:
    - omnikey2ivy
    - omnikey2ivy.RFIDIOt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/omnikey2ivy/
  license: GNU General Public License (GPL)
  summary: 'emit card id on Ivy software bus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
