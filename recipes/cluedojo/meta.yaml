package:
  name: cluedojo
  version: "1.4.3-1"

source:
  fn: ClueDojo-1.4.3-1.tar.gz
  url: https://pypi.python.org/packages/source/C/ClueDojo/ClueDojo-1.4.3-1.tar.gz
  md5: 169de92ee043203a486913a4a6aae015
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cluedojo = cluedojo:main
    #
    # Would create an entry point called cluedojo that calls cluedojo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - paste

  run:
    - python
    - setuptools
    - setuptools
    - paste

test:
  # Python imports
  imports:
    - cluedojo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://dev.serverzen.com/svn/cluemapper/ClueDojo/
  license: BSD License
  summary: 'Simple Python library to access the Dojo Javascript toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
