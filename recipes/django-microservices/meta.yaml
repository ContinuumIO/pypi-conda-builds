package:
  name: django-microservices
  version: "0.1"

source:
  fn: django-microservices-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-microservices/django-microservices-0.1.tar.gz
  md5: f807f5fd3517ab4cc8a70c0dfa8ee951
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-microservices = django-microservices:main
    #
    # Would create an entry point called django-microservices that calls django-microservices.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - microservices
    - microservices.management
    - microservices.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lander2k2/django-microservices
  license: MIT
  summary: 'A framework for building microservices with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
