package:
  name: zope.dottedname
  version: "4.1.0"

source:
  fn: zope.dottedname-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.dottedname/zope.dottedname-4.1.0.tar.gz
  md5: 503ed09f1a3675e8c413cba81c79da4e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.dottedname = zope.dottedname:main
    #
    # Would create an entry point called zope.dottedname that calls zope.dottedname.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.dottedname

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.dottedname
  license: Zope Public License
  summary: 'Resolver for Python dotted names.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
