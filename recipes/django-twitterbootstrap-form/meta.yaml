package:
  name: django-twitterbootstrap-form
  version: "0.3.2"

source:
  fn: django-twitterbootstrap-form-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twitterbootstrap-form/django-twitterbootstrap-form-0.3.2.tar.gz
  md5: 8c8ea88da6b57be6868b11947672706a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twitterbootstrap-form = django-twitterbootstrap-form:main
    #
    # Would create an entry point called django-twitterbootstrap-form that calls django-twitterbootstrap-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-widget-tweaks

  run:
    - python
    - django-widget-tweaks

test:
  # Python imports
  imports:
    - geelweb
    - geelweb.django
    - geelweb.django.twitter_bootstrap_form
    - geelweb.django.twitter_bootstrap_form.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/geelweb/django-twitter-bootstrap-form
  license: MIT
  summary: 'Render Django forms as described using the twitter bootstrap HTML layout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
