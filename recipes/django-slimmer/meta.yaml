package:
  name: django-slimmer
  version: "0.0.2"

source:
  fn: django-slimmer-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-slimmer/django-slimmer-0.0.2.tar.gz
  md5: 5c44777af2fed3219daf5c68846f78ad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-slimmer = django-slimmer:main
    #
    # Would create an entry point called django-slimmer that calls django-slimmer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - slimmer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hbussell/django-slimmer
  license: MIT
  summary: 'Html compression as middleware and view decorators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
