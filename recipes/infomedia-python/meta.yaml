package:
  name: infomedia-python
  version: "1.2"

source:
  fn: infomedia-python-1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/infomedia-python/infomedia-python-1.2.tar.gz
  md5: dc41a3ed5b1d11fc6500be2f95543b1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infomedia-python = infomedia-python:main
    #
    # Would create an entry point called infomedia-python that calls infomedia-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - infomedia
    - infomedia.hash2cfg
    - infomedia.hash2cfg.test
    - infomedia.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://infomedia.it/dev
  license: Python Software Foundation License
  summary: 'Infomedia Python Utility Module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
