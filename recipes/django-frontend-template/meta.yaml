package:
  name: django-frontend-template
  version: "1.2.0"

source:
  fn: django-frontend-template-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-frontend-template/django-frontend-template-1.2.0.tar.gz
  md5: 30066aa43183e550af00fa664f02254f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-frontend-template = django-frontend-template:main
    #
    # Would create an entry point called django-frontend-template that calls django-frontend-template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djfrontend
    - djfrontend.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jonfaustman/django-frontend-template
  license: MIT License
  summary: 'A basic Django template built on HTML5 Boilerplate.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
