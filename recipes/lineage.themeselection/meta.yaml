package:
  name: lineage.themeselection
  version: "1.4"

source:
  fn: lineage.themeselection-1.4.zip
  url: https://pypi.python.org/packages/source/l/lineage.themeselection/lineage.themeselection-1.4.zip
  md5: 218470a9ac6a6c72802836e4f1fc29ba
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lineage.themeselection = lineage.themeselection:main
    #
    # Would create an entry point called lineage.themeselection that calls lineage.themeselection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.lineage
    - lineage.registry
    - archetypes.schemaextender
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - collective.lineage
    - lineage.registry
    - archetypes.schemaextender
    - plone.app.theming

test:
  # Python imports
  imports:
    - lineage
    - lineage.themeselection

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/lineage.themeselection
  license: GNU General Public License (GPL)
  summary: 'Lineage Addon: Theme Selection on Subsites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
