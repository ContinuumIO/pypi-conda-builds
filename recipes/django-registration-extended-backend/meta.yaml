package:
  name: django-registration-extended-backend
  version: "0.1.a2"

source:
  fn: django-registration-extended-backend-0.1.a2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-extended-backend/django-registration-extended-backend-0.1.a2.tar.gz
  md5: 67befd449177c9c3f2f6395790ff7388
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-extended-backend = django-registration-extended-backend:main
    #
    # Would create an entry point called django-registration-extended-backend that calls django-registration-extended-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - extendedregistrationbackend
    - extendedregistrationbackend.extended

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/huseyinyilmaz/django-registration-extended-backend
  license: GPLv3
  summary: 'Backend for django registration that sends email in HTML and does email normalization'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
