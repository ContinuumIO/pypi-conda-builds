package:
  name: django-dbstorage
  version: "1.3"

source:
  fn: django-dbstorage-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbstorage/django-dbstorage-1.3.tar.gz
  md5: ff76d0fea0dfaa075e7d08125b640c75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbstorage = django-dbstorage:main
    #
    # Would create an entry point called django-dbstorage that calls django-dbstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - django_dbstorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/akoha/django-dbstorage/
  license: MIT License
  summary: 'A Django file storage backend for files in the database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
