package:
  name: django-definition-tag
  version: "0.1.0"

source:
  fn: django-definition-tag-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-definition-tag/django-definition-tag-0.1.0.tar.gz
  md5: fef9b0088036cad598c3255e0eaae0f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-definition-tag = django-definition-tag:main
    #
    # Would create an entry point called django-definition-tag that calls django-definition-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/defcube/django-definition-tag/
  license: UNKNOWN
  summary: 'Allows easy creation of template tags that define a context variable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
