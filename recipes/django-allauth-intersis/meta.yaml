package:
  name: django-allauth-intersis
  version: "0.5.5"

source:
  fn: django-allauth-intersis-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-allauth-intersis/django-allauth-intersis-0.5.5.tar.gz
  md5: 328c4fe092b844adaa00dacee3bece5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-allauth-intersis = django-allauth-intersis:main
    #
    # Would create an entry point called django-allauth-intersis that calls django-allauth-intersis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-allauth >=0.19.0

  run:
    - python
    - django >=1.7
    - django-allauth >=0.19.0

test:
  # Python imports
  imports:
    - allauth_intersis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.intersis.org/
  license: GNU General Public License v3 (GPLv3)
  summary: 'OAuth2.0 InterSIS access module for projects using django-allauth.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
