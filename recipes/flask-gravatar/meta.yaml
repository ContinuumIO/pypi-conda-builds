package:
  name: flask-gravatar
  version: "0.4.2"

source:
  fn: Flask-Gravatar-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Gravatar/Flask-Gravatar-0.4.2.tar.gz
  md5: 97a6b4443c487e9dad95ab5ab8566314
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-gravatar = flask-gravatar:main
    #
    # Would create an entry point called flask-gravatar that calls flask-gravatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_gravatar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zzzsochi/Flask-Gravatar/
  license: BSD License
  summary: 'Small extension for Flask to make usage of Gravatar service easy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
