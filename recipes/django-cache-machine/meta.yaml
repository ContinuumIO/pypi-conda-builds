package:
  name: django-cache-machine
  version: "0.8"

source:
  fn: django-cache-machine-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-machine/django-cache-machine-0.8.tar.gz
  md5: 64b7deba5b06b425d041b69bf22d67ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-machine = django-cache-machine:main
    #
    # Would create an entry point called django-cache-machine that calls django-cache-machine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - caching
    - caching.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jbalogh/django-cache-machine
  license: BSD License
  summary: 'Automatic caching and invalidation for Django models through the ORM.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
