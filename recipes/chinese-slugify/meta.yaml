package:
  name: chinese-slugify
  version: "0.1.0"

source:
  fn: master.zip
  url: https://github.com/oldcai/chinese-slugify/archive/master.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chinese-slugify = chinese-slugify:main
    #
    # Would create an entry point called chinese-slugify that calls chinese-slugify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - awesome-slugify
    - jieba
    - pypinyin

  run:
    - python
    - awesome-slugify
    - jieba
    - pypinyin

test:
  # Python imports
  imports:
    - chinese_slugify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oldcai/chinese-slugify
  license: GNU General Public License v3 (GPLv3)
  summary: 'A more powerful slugify module for Chinese'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
