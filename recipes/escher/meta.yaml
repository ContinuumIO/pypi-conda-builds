package:
  name: escher
  version: "1.1.2"

source:
  fn: Escher-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/E/Escher/Escher-1.1.2.tar.gz
  md5: 0bb83b60879ba5c3c50bfda2c6e67d30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - escher = escher:main
    #
    # Would create an entry point called escher that calls escher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.7.3
    - tornado >=4.0.2
    - pytest >=2.6.2
    - jsonschema >=2.4.0

  run:
    - python
    - jinja2 >=2.7.3
    - tornado >=4.0.2
    - pytest >=2.6.2
    - jsonschema >=2.4.0

test:
  # Python imports
  imports:
    - escher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://escher.github.io
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
