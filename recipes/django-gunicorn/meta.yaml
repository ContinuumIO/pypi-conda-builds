package:
  name: django-gunicorn
  version: "0.1.1"

source:
  fn: django-gunicorn-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gunicorn/django-gunicorn-0.1.1.tar.gz
  md5: a6084ed304a066496f76db05d2ddbdaf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gunicorn = django-gunicorn:main
    #
    # Would create an entry point called django-gunicorn that calls django-gunicorn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gunicorn >=19.0.0

  run:
    - python
    - gunicorn >=19.0.0

test:
  # Python imports
  imports:
    - django_gunicorn
    - django_gunicorn.management
    - django_gunicorn.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brosner/django-gunicorn
  license: BSD License
  summary: 'simple Django app to replace runserver with Gunicorn'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
