package:
  name: django-latest-tweets
  version: "0.3"

source:
  fn: django-latest-tweets-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-latest-tweets/django-latest-tweets-0.3.tar.gz
  md5: f134458c50817b69105cea4e0c6631e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-latest-tweets = django-latest-tweets:main
    #
    # Would create an entry point called django-latest-tweets that calls django-latest-tweets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twitter >=1.9.1

  run:
    - python
    - twitter >=1.9.1

test:
  # Python imports
  imports:
    - latest_tweets
    - latest_tweets.management
    - latest_tweets.management.commands
    - latest_tweets.migrations
    - latest_tweets.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blancltd/django-latest-tweets
  license: BSD License
  summary: 'Latest Tweets for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
