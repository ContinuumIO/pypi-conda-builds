package:
  name: more-itertools
  version: "2.2"

source:
  fn: more-itertools-2.2.tar.gz
  url: https://pypi.python.org/packages/source/m/more-itertools/more-itertools-2.2.tar.gz
  md5: b8d328a33f966bf40bb829bcf8da35ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - more-itertools = more-itertools:main
    #
    # Would create an entry point called more-itertools that calls more-itertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - more_itertools
    - more_itertools.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erikrose/more-itertools
  license: MIT License
  summary: 'More routines for operating on iterables, beyond itertools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
