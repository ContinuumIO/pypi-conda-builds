package:
  name: pelican-delicious
  version: "0.0.1"

source:
  fn: pelican-delicious-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-delicious/pelican-delicious-0.0.1.tar.gz
  md5: 5f3a617fc4f015eaa9436c947805d2aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-delicious = pelican-delicious:main
    #
    # Would create an entry point called pelican-delicious that calls pelican-delicious.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

test:
  # Python imports
  imports:
    - pelican_delicious

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/2xyo/pelican-delicious
  license: BSD
  summary: 'Easily embed delicious bookmarks in your Pelican articles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
