package:
  name: django-email-login
  version: "0.2.4"

source:
  fn: django-email-login-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-login/django-email-login-0.2.4.tar.gz
  md5: b92263447586474aa0430b50cf2b60eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-login = django-email-login:main
    #
    # Would create an entry point called django-email-login that calls django-email-login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_login

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tino/django-email-login
  license: MIT License
  summary: 'A Django application that enables you to have your users use their email address instead of their username'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
