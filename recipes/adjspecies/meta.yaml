package:
  name: adjspecies
  version: "0.1.3"

source:
  fn: adjspecies-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/adjspecies/adjspecies-0.1.3.tar.gz
  md5: e744c75f0649a2852aaf7af6fb5a32e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adjspecies = adjspecies:main
    #
    # Would create an entry point called adjspecies that calls adjspecies.main()

    - adjspecies = adjspecies:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adjspecies

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - adjspecies --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hipikat/adjspecies/
  license: BSD License
  summary: '\nPrint the name of a random adjective/species, more or less\xe2\x80\xa6\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
