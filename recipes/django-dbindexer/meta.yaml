package:
  name: django-dbindexer
  version: "1.6.1"

source:
  fn: django-dbindexer-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbindexer/django-dbindexer-1.6.1.tar.gz
  md5: 9fe35e7ecc68fd2252d878dcbb444587
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbindexer = django-dbindexer:main
    #
    # Would create an entry point called django-dbindexer that calls django-dbindexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbindexer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django-nonrel/django-dbindexer
  license: BSD License
  summary: 'Expressive NoSQL for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
