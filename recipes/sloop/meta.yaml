package:
  name: sloop
  version: "0.3.6.3"

source:
  fn: sloop-0.3.6.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sloop/sloop-0.3.6.3.tar.gz
  md5: d8a29c3a38cc82c243ef62aa8688c3a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sloop = sloop:main
    #
    # Would create an entry point called sloop that calls sloop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - sloop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hipo/django-sloop
  license: UNKNOWN
  summary: 'Django package of the sloop push notification service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
