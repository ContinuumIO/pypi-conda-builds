package:
  name: django-unjoinify
  version: "0.1.0"

source:
  fn: django-unjoinify-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-unjoinify/django-unjoinify-0.1.0.tar.gz
  md5: c16bf6e6c881e721d1c68c752a34bff7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-unjoinify = django-unjoinify:main
    #
    # Would create an entry point called django-unjoinify that calls django-unjoinify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - unjoinify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gasman/django-unjoinify
  license: BSD License
  summary: 'A helper for efficiently retrieving deeply-nested data sets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
