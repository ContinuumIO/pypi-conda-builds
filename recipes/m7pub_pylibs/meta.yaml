package:
  name: m7pub_pylibs
  version: "1.1.4"

source:
  fn: m7pub_pylibs-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/m/m7pub_pylibs/m7pub_pylibs-1.1.4.tar.gz
  md5: 70854f065f184af0ba9b8166151311ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m7pub_pylibs = m7pub_pylibs:main
    #
    # Would create an entry point called m7pub_pylibs that calls m7pub_pylibs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - m7pub_pylibs
    - m7pub_pylibs.dev
    - m7pub_pylibs.jp
    - m7pub_pylibs.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: LICENSE.txt
  summary: 'm7pub_pylibs are libraries used by MARIMORE and are opened sourced'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
