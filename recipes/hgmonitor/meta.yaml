package:
  name: hgmonitor
  version: "0.5"

source:
  fn: hgmonitor-0.5.tar.gz
  url: https://pypi.python.org/packages/source/h/hgmonitor/hgmonitor-0.5.tar.gz
  md5: 5cc3add2fe5bee2c2f88818f3ceaa4f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgmonitor = hgmonitor:main
    #
    # Would create an entry point called hgmonitor that calls hgmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hgmonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jbmohler/hg-monitor
  license: GNU General Public License (GPL)
  summary: 'mercurial repository monitoring and handling as a group'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
