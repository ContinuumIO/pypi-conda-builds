package:
  name: email_extractor
  version: "v1"

source:
  fn: email_extractor-v1.tar.gz
  url: https://pypi.python.org/packages/source/e/email_extractor/email_extractor-v1.tar.gz
  md5: 64028c5dca39cd0455be0390f3cfe7d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - email_extractor = email_extractor:main
    #
    # Would create an entry point called email_extractor that calls email_extractor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - email_extractor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jaist.ac.jp/~s1010205/email_extractor/html/index.html
  license: BSD
  summary: 'Extract Emails by using webcrawl Extract Url links from a site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
