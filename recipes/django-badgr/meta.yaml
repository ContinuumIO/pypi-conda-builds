package:
  name: django-badgr
  version: "1.0"

source:
  fn: django-badgr-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-badgr/django-badgr-1.0.tar.gz
  md5: 34ebe791092c11c86c72b84396c5c483
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-badgr = django-badgr:main
    #
    # Would create an entry point called django-badgr that calls django-badgr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flickrapi

  run:
    - python
    - flickrapi

test:
  # Python imports
  imports:
    - badgr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pigmonkey/django-badgr
  license: BSD License
  summary: 'A resuable Django application used to display a Flickr badge.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
