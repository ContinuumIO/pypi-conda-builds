package:
  name: loredis
  version: "0.1.1"

source:
  fn: loredis-0.1.1.zip
  url: https://pypi.python.org/packages/source/l/loredis/loredis-0.1.1.zip
  md5: 8729825ad7abad3938e620b77840bdfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loredis = loredis:main
    #
    # Would create an entry point called loredis that calls loredis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - loredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/huyx/loredis
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'loredis, compare with hiredis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
