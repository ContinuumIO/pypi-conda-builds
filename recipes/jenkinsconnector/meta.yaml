package:
  name: jenkinsconnector
  version: "0.1.1"

source:
  fn: jenkinsconnector-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jenkinsconnector/jenkinsconnector-0.1.1.tar.gz
  md5: 5e758491733d5f199e4c1f2dbde5427f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jenkinsconnector = jenkinsconnector:main
    #
    # Would create an entry point called jenkinsconnector that calls jenkinsconnector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyquery

  run:
    - python
    - pyquery

test:
  # Python imports
  imports:
    - jenkinsconnector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AxiaCore/jenkins-connector
  license: MIT
  summary: 'A library to communicate with jenkins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
