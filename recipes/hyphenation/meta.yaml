package:
  name: hyphenation
  version: "2.0"

source:
  fn: hyphenation-2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hyphenation/hyphenation-2.0.tar.gz
  md5: 707570643455909adf0d75b7e500bedb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyphenation = hyphenation:main
    #
    # Would create an entry point called hyphenation that calls hyphenation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - guesslanguage

  run:
    - python
    - setuptools
    - guesslanguage

test:
  # Python imports
  imports:
    - hyphenation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitorious.org/hyphenator
  license: LGLP 3.0
  summary: 'Hyphenation module for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
