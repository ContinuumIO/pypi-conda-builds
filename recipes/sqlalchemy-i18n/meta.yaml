package:
  name: sqlalchemy-i18n
  version: "1.0.1"

source:
  fn: SQLAlchemy-i18n-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-i18n/SQLAlchemy-i18n-1.0.1.tar.gz
  md5: 836596ec99eaf9ef98742fe74453975b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-i18n = sqlalchemy-i18n:main
    #
    # Would create an entry point called sqlalchemy-i18n that calls sqlalchemy-i18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9
    - sqlalchemy-utils >=0.25.3
    - six >=1.4.1

  run:
    - python
    - sqlalchemy >=0.9
    - sqlalchemy-utils >=0.25.3
    - six >=1.4.1

test:
  # Python imports
  imports:
    - sqlalchemy_i18n

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/sqlalchemy-i18n
  license: BSD License
  summary: 'Internationalization extension for SQLAlchemy models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
