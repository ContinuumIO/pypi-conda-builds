package:
  name: django-sqlpaginator
  version: "0.1.5"

source:
  fn: django-sqlpaginator-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sqlpaginator/django-sqlpaginator-0.1.5.tar.gz
  md5: eec077531972e09027d3cb70be599474
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sqlpaginator = django-sqlpaginator:main
    #
    # Would create an entry point called django-sqlpaginator that calls django-sqlpaginator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlparse

  run:
    - python
    - sqlparse

test:
  # Python imports
  imports:
    - sqlpaginator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bulkan/django-sqlpaginator
  license: UNKNOWN
  summary: 'django app that does pagination and ordering using raw\nsql on a Model. It has the same API as the django.core.pagination.Paginator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
