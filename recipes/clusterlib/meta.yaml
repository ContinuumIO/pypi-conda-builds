package:
  name: clusterlib
  version: "0.1"

source:
  fn: clusterlib-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clusterlib/clusterlib-0.1.tar.gz
  md5: 8af007507dbdd6a06fdcac3a1ee56475
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clusterlib = clusterlib:main
    #
    # Would create an entry point called clusterlib that calls clusterlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - clusterlib
    - clusterlib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://clusterlib.github.io/clusterlib/
  license: BSD License
  summary: 'Tools to manage jobs on cluster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
