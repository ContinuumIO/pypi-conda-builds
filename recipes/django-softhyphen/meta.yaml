package:
  name: django-softhyphen
  version: "1.0.1"

source:
  fn: django-softhyphen-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-softhyphen/django-softhyphen-1.0.1.tar.gz
  md5: b0056e6e12cabc22a405005bc4c24a21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-softhyphen = django-softhyphen:main
    #
    # Would create an entry point called django-softhyphen that calls django-softhyphen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.3.2
    - six >=1.5.1

  run:
    - python
    - beautifulsoup4 >=4.3.2
    - six >=1.5.1

test:
  # Python imports
  imports:
    - softhyphen
    - softhyphen.dicts
    - softhyphen.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datadesk/django-softhyphen/
  license: UNKNOWN
  summary: 'A Python library for hyphenating HTML in your Django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
