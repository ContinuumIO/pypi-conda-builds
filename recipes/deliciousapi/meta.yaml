package:
  name: deliciousapi
  version: "1.6.7"

source:
  fn: DeliciousAPI-1.6.7.tar.gz
  url: https://pypi.python.org/packages/source/D/DeliciousAPI/DeliciousAPI-1.6.7.tar.gz
  md5: 4fbd077bfbc7c299d74d2cee7896c3aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deliciousapi = deliciousapi:main
    #
    # Would create an entry point called deliciousapi that calls deliciousapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - simplejson >=1.9.2
    - beautifulsoup >=3.0.7

  run:
    - python
    - simplejson >=1.9.2
    - beautifulsoup >=3.0.7

test:
  # Python imports
  imports:
    - deliciousapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.michael-noll.com/wiki/Del.icio.us_Python_API
  license: GNU General Public License (GPL)
  summary: 'Unofficial Python API for retrieving data from Delicious.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
