package:
  name: gist-up
  version: "0.3"

source:
  fn: gist-up-0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gist-up/gist-up-0.3.tar.gz
  md5: 814847219b47bdfa38eb9efd6dfd07a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gist-up = gist-up:main
    #
    # Would create an entry point called gist-up that calls gist-up.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pygithub ==1.25.2
    - pyyaml ==3.11
    - argparse ==1.2.2
    - termcolor ==1.1.0

  run:
    - python
    - pygithub ==1.25.2
    - pyyaml ==3.11
    - argparse ==1.2.2
    - termcolor ==1.1.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sourcec0de/gistup
  license: UNKNOWN
  summary: 'A command line utility for publishing a directory and updating gists on your github account.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
