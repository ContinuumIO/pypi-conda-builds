package:
  name: zest.cachetuning
  version: "0.4"

source:
  fn: zest.cachetuning-0.4.zip
  url: https://pypi.python.org/packages/source/z/zest.cachetuning/zest.cachetuning-0.4.zip
  md5: e96e2e60af9d747f0ec7051ce7936b40
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zest.cachetuning = zest.cachetuning:main
    #
    # Would create an entry point called zest.cachetuning that calls zest.cachetuning.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zest
    - zest.cachetuning
    - zest.cachetuning.browser
    - zest.cachetuning.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'This Plone product allows some cache tuning for your site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
