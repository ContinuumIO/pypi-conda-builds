package:
  name: citylink-utils
  version: "0.8"

source:
  fn: citylink-utils-0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/citylink-utils/citylink-utils-0.8.tar.gz
  md5: a3fcb689632ba4d9a0666aab00643dd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - citylink-utils = citylink-utils:main
    #
    # Would create an entry point called citylink-utils that calls citylink-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mechanize
    - lxml
    - mailer

  run:
    - python
    - mechanize
    - lxml
    - mailer

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OdinsHat/citylink-utils
  license: BSD License
  summary: 'Simple utility package to handle CityLink dispatches for commercial customers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
