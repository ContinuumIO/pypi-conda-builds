package:
  name: att
  version: "0.1.6"

source:
  fn: att-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/a/att/att-0.1.6.tar.gz
  md5: 4207176cb02df52ba75e8eb7426fbd0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - att = att:main
    #
    # Would create an entry point called att that calls att.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - arrow
    - beautifulsoup4
    - requests
    - docopt

  run:
    - python
    - arrow
    - beautifulsoup4
    - requests
    - docopt

test:
  # Python imports
  imports:
    - att

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alaingilbert/att
  license: MIT License
  summary: 'Extract informations from att website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
