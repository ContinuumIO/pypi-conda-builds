package:
  name: collective.kssinline
  version: "0.9.4"

source:
  fn: collective.kssinline-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.kssinline/collective.kssinline-0.9.4.tar.gz
  md5: b5b048ed0a26c629afe19453f12107c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.kssinline = collective.kssinline:main
    #
    # Would create an entry point called collective.kssinline that calls collective.kssinline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.kssinline
    - collective.kssinline.browser
    - collective.kssinline.browser.adapters
    - collective.kssinline.browser.resources
    - collective.kssinline.browser.utilities
    - collective.kssinline.browser.viewlets
    - collective.kssinline.browser.views
    - collective.kssinline.examples
    - collective.kssinline.examples.adapters
    - collective.kssinline.examples.utilities
    - collective.kssinline.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Edit and create content without navigating away from listings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
