package:
  name: blockext-sphero
  version: "0.2.4"

source:
  fn: blockext-sphero-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/b/blockext-sphero/blockext-sphero-0.2.4.tar.gz
  md5: 893bb5957f76bd640ffe13253ffd1e27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockext-sphero = blockext-sphero:main
    #
    # Would create an entry point called blockext-sphero that calls blockext-sphero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockext ==0.2.0a2

  run:
    - python
    - blockext ==0.2.0a2

test:
  # Python imports
  imports:
    - blockext_sphero

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blockext/sphero
  license: MIT
  summary: 'Orbotix Sphero extension for Scratch 2.0 and Snap!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
