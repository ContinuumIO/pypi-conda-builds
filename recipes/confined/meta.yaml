package:
  name: confined
  version: "0.1.10"

source:
  fn: confined-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/c/confined/confined-0.1.10.tar.gz
  md5: fb9f90f91e505af0913d9d9de8c9207d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confined = confined:main
    #
    # Would create an entry point called confined that calls confined.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - confined

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/jul/confined
  license: Python Software Foundation License
  summary: 'Sage templating language with bounded resource'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
