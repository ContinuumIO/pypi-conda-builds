package:
  name: django-google-credentials
  version: "0.0.2"

source:
  fn: django-google-credentials-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-google-credentials/django-google-credentials-0.0.2.tar.gz
  md5: 30281c4dfdba7574eb773c904ba1a780
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-credentials = django-google-credentials:main
    #
    # Would create an entry point called django-google-credentials that calls django-google-credentials.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - google-api-python-client
    - south

  run:
    - python
    - django
    - google-api-python-client
    - south

test:
  # Python imports
  imports:
    - google_credentials

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-google-credentials
  license: BSD License
  summary: 'Django app storing Google API OAuth credentials.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
