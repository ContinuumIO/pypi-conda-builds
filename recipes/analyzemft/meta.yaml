package:
  name: analyzemft
  version: "2.0.15"

source:
  fn: analyzeMFT-2.0.15.tar.gz
  url: https://pypi.python.org/packages/source/a/analyzeMFT/analyzeMFT-2.0.15.tar.gz
  md5: 9f48b11bd64bee807a3f1135c20e8975
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - analyzemft = analyzemft:main
    #
    # Would create an entry point called analyzemft that calls analyzemft.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - analyzemft

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dkovar/analyzeMFT
  license: LICENSE.txt
  summary: 'Analyze the $MFT from a NTFS filesystem.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
