package:
  name: django-bootstrap-forms
  version: "0.1"

source:
  fn: django-bootstrap-forms-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-forms/django-bootstrap-forms-0.1.tar.gz
  md5: af2c31e6bd566e430e4c4792de681eb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-forms = django-bootstrap-forms:main
    #
    # Would create an entry point called django-bootstrap-forms that calls django-bootstrap-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/earle/django-bootstrap
  license: BSD License
  summary: 'Twitter Bootstrap ready Django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
