package:
  name: django-noun-project
  version: "0.1"

source:
  fn: django-noun-project-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-noun-project/django-noun-project-0.1.tar.gz
  md5: 1a1a5fff111345186593f0a36f385616
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-noun-project = django-noun-project:main
    #
    # Would create an entry point called django-noun-project that calls django-noun-project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-classy-tags
    - slumber

  run:
    - python
    - django-classy-tags
    - slumber

test:
  # Python imports
  imports:
    - noun_project
    - noun_project.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/philipn/django-noun-project/
  license: BSD License
  summary: 'Support for the Noun Project inside Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
