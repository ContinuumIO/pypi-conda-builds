package:
  name: anything
  version: "0.1.0"

source:
  fn: anything-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/anything/anything-0.1.0.tar.gz
  md5: 72341526ad74036dd745fc3fbb0e5874
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anything = anything:main
    #
    # Would create an entry point called anything that calls anything.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - anything

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vilcans/anything
  license: MIT
  summary: 'A Python constant that considers itself equal to everything else. Useful for unit testing and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
