package:
  name: silva.app.shorturl
  version: "3.0.2"

source:
  fn: silva.app.shorturl-3.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.app.shorturl/silva.app.shorturl-3.0.2.tar.gz
  md5: b5931c5d783dbe5570a0a395439e01ef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.app.shorturl = silva.app.shorturl:main
    #
    # Would create an entry point called silva.app.shorturl that calls silva.app.shorturl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - products.silva
    - five.grok
    - setuptools
    - silva.app.forest
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.smi
    - silva.core.views
    - silva.fanstatic
    - silva.translations
    - silva.ui
    - zope.component
    - zope.interface
    - zope.schema
    - zope.publisher
    - zope.intid

  run:
    - python
    - products.silva
    - five.grok
    - setuptools
    - silva.app.forest
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.smi
    - silva.core.views
    - silva.fanstatic
    - silva.translations
    - silva.ui
    - zope.component
    - zope.interface
    - zope.schema
    - zope.publisher
    - zope.intid

test:
  # Python imports
  imports:
    - silva
    - silva.app
    - silva.app.shorturl
    - silva.app.shorturl.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.app.shorturl
  license: BSD License
  summary: 'Short url generation for Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
