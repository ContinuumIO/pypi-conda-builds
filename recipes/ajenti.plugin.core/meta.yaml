package:
  name: ajenti.plugin.core
  version: "0.33"

source:
  fn: ajenti.plugin.core-0.33.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.core/ajenti.plugin.core-0.33.tar.gz
  md5: 0f42c6817c58917ca960749680e8c896
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.core = ajenti.plugin.core:main
    #
    # Would create an entry point called ajenti.plugin.core that calls ajenti.plugin.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - gevent
    - gipc
    - jadi >=1.0.0
    - setproctitle

  run:
    - python
    - aj
    - gevent
    - gipc
    - jadi >=1.0.0
    - setproctitle

test:
  # Python imports
  imports:
    - ajenti_plugin_core
    - ajenti_plugin_core.api
    - ajenti_plugin_core.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Core'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
