package:
  name: products.rpcauth
  version: "0.1"

source:
  fn: Products.rpcauth-0.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.rpcauth/Products.rpcauth-0.1.tar.gz
  md5: 6894891dfe1d7b764b5a749e66895dd9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.rpcauth = products.rpcauth:main
    #
    # Would create an entry point called products.rpcauth that calls products.rpcauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.pluggableauthservice

  run:
    - python
    - setuptools
    - products.pluggableauthservice

test:
  # Python imports
  imports:
    - Products
    - Products.rpcauth
    - Products.rpcauth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL
  summary: 'Authentication queries to Zope over XML-RPC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
