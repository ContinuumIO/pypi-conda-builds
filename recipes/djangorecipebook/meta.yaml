package:
  name: djangorecipebook
  version: "1.2.6"

source:
  fn: djangorecipebook-1.2.6.zip
  url: https://pypi.python.org/packages/source/d/djangorecipebook/djangorecipebook-1.2.6.zip
  md5: d0968586cf428cb0307832f00ab76d6e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorecipebook = djangorecipebook:main
    #
    # Would create an entry point called djangorecipebook that calls djangorecipebook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - django >=1.4

  run:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - django >=1.4

test:
  # Python imports
  imports:
    - djangorecipebook
    - djangorecipebook.recipes
    - djangorecipebook.scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tkhyn/djangorecipebook/
  license: MIT License
  summary: 'Buildout recipes for django development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
