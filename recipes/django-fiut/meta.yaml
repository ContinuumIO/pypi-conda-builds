package:
  name: django-fiut
  version: "0.1.7"

source:
  fn: django-FIUT-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-FIUT/django-FIUT-0.1.7.tar.gz
  md5: bef2e6d02ee445afd411b0c6feb4de06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fiut = django-fiut:main
    #
    # Would create an entry point called django-fiut that calls django-fiut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fiut

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/v0y/django-FIUT
  license: MIT
  summary: 'Fine, Incredibly Useful Tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
