package:
  name: ewb_case.portlet.get_involved
  version: "0.1.1"

source:
  fn: ewb_case.portlet.get_involved-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ewb_case.portlet.get_involved/ewb_case.portlet.get_involved-0.1.1.tar.gz
  md5: 479f595e3eea4d40514c9a303fb519a0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ewb_case.portlet.get_involved = ewb_case.portlet.get_involved:main
    #
    # Would create an entry point called ewb_case.portlet.get_involved that calls ewb_case.portlet.get_involved.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone >=4.0
    - zope.i18nmessageid

  run:
    - python
    - setuptools
    - setuptools
    - plone >=4.0
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - ewb_case
    - ewb_case.portlet
    - ewb_case.portlet.get_involved
    - ewb_case.portlet.get_involved.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mattbierner/ewb_case.portlet.get_involved/
  license: GPL
  summary: 'Static portlet showing ways to get involved in EWB Case.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
