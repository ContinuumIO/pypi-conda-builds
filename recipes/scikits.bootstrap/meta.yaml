package:
  name: scikits.bootstrap
  version: "0.3.2"

source:
  fn: scikits.bootstrap-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/s/scikits.bootstrap/scikits.bootstrap-0.3.2.tar.gz
  md5: c4e55db940f078759b52b32cdf394224
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - scikits.bootstrap = scikits.bootstrap:main
    #
    # Would create an entry point called scikits.bootstrap that calls scikits.bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy

  run:
    - python
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - scikits
    - scikits.bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/cgevans/scikits-bootstrap
  license: BSD License
  summary: 'Bootstrap confidence interval estimation routines for SciPy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
