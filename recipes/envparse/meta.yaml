package:
  name: envparse
  version: "0.1.5"

source:
  fn: envparse-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/e/envparse/envparse-0.1.5.tar.gz
  md5: e136d2f0c76af982921c5c1780283248
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - envparse = envparse:main
    #
    # Would create an entry point called envparse that calls envparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - envparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rconradharris/envparse
  license: MIT License
  summary: 'Simple Environment Variable Parsing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
