package:
  name: dropbox2
  version: "1.7.1"

source:
  fn: dropbox2-1.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dropbox2/dropbox2-1.7.1.tar.gz
  md5: 6ac8c9d16dd249790872d27b4c9f6067
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dropbox2 = dropbox2:main
    #
    # Would create an entry point called dropbox2 that calls dropbox2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil

  run:
    - python
    - python-dateutil

test:
  # Python imports
  imports:
    - dropbox
    - dropbox2
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wol.ph/
  license: UNKNOWN
  summary: 'Dropbox REST API Client with more consistent responses.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
