package:
  name: django_location_choices
  version: "1.0"

source:
  fn: django_location_choices-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_location_choices/django_location_choices-1.0.tar.gz
  md5: 7cc312762c897ca6990af12622deafb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_location_choices = django_location_choices:main
    #
    # Would create an entry point called django_location_choices that calls django_location_choices.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_location_choices

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bmelton/django_location_choices/
  license: UNKNOWN
  summary: 'Django location choices is a simple tuple, prepopulated with countries and states for use in Django models as CHOICES values'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
