package:
  name: comdet
  version: "0.4"

source:
  fn: comdet-0.4.zip
  url: https://pypi.python.org/packages/source/c/comdet/comdet-0.4.zip
  md5: e77405d322f42816b59b95059e2181ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - comdet = comdet:main
    #
    # Would create an entry point called comdet that calls comdet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - comdet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pankajr141/comdet
  license: MIT
  summary: 'comdet is used to detect community among graph nodes. Its based on lovain method'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
