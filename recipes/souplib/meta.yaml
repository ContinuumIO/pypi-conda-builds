package:
  name: souplib
  version: "0.2.0"

source:
  fn: souplib-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/souplib/souplib-0.2.0.tar.gz
  md5: dccce0ee3f29bd7cc158b45e4bcbb463
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - souplib = souplib:main
    #
    # Would create an entry point called souplib that calls souplib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - six

  run:
    - python
    - beautifulsoup4
    - six

test:
  # Python imports
  imports:
    - souplib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clld/souplib
  license: BSD License
  summary: 'Utilities for HTML manipulation using BeautifulSoup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
