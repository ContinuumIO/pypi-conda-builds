package:
  name: django-quicky
  version: "0.6.4"

source:
  fn: django-quicky-0.6.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quicky/django-quicky-0.6.4.tar.gz
  md5: 0d6f891df56cdef7a0601e14f8927d98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quicky = django-quicky:main
    #
    # Would create an entry point called django-quicky that calls django-quicky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_quicky
    - django_quicky.management
    - django_quicky.management.commands
    - django_quicky.namegen
    - django_quicky.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sametmax/django-quicky
  license: zlib/libpng License
  summary: 'A collection of tools to use to quickly setup Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
