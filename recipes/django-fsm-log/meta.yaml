package:
  name: django-fsm-log
  version: "1.2.0"

source:
  fn: django-fsm-log-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fsm-log/django-fsm-log-1.2.0.tar.gz
  md5: d8c776e1f29ad6683dc11eb8675ada5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fsm-log = django-fsm-log:main
    #
    # Would create an entry point called django-fsm-log that calls django-fsm-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django_fsm >=2
    - django_appconf

  run:
    - python
    - django >=1.6
    - django_fsm >=2
    - django_appconf

test:
  # Python imports
  imports:
    - django_fsm_log
    - django_fsm_log.migrations
    - django_fsm_log.south_migrations
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gizmag/django_fsm_log
  license: UNKNOWN
  summary: 'Logging for django-fsm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
