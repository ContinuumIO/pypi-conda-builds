package:
  name: js.ehynds_multiselect
  version: "1.13"

source:
  fn: js.ehynds_multiselect-1.13.zip
  url: https://pypi.python.org/packages/source/j/js.ehynds_multiselect/js.ehynds_multiselect-1.13.zip
  md5: 929c00603a2dc4c7a3b6c1c6f256962d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.ehynds_multiselect = js.ehynds_multiselect:main
    #
    # Would create an entry point called js.ehynds_multiselect that calls js.ehynds_multiselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery >=1.7.1
    - js.jqueryui >=1.8.0
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery >=1.7.1
    - js.jqueryui >=1.8.0
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.ehynds_multiselect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/js.ehynds_multiselect
  license: BSD
  summary: 'Fanstatic packaging of ehynds/jquery-ui-multiselect-widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
