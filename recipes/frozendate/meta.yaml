package:
  name: frozendate
  version: "0.1.2"

source:
  fn: frozendate-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/frozendate/frozendate-0.1.2.tar.gz
  md5: 5c5f80adbd46a0a062a8ad365b9f866a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frozendate = frozendate:main
    #
    # Would create an entry point called frozendate that calls frozendate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock

  run:
    - python
    - mock

test:
  # Python imports
  imports:
    - frozendate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ollyc/frozendate/
  license: BSD License
  summary: 'Frozen date: freeze datetime.now at a known value for testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
