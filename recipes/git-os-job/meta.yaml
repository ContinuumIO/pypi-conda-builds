package:
  name: git-os-job
  version: "1.0.1"

source:
  fn: git-os-job-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/git-os-job/git-os-job-1.0.1.tar.gz
  md5: 4b46a11b16fc3f1ff13c3a266858f28e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-os-job = git-os-job:main
    #
    # Would create an entry point called git-os-job that calls git-os-job.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License (2.0)
  summary: 'Show the OpenStack job logs for a commit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
