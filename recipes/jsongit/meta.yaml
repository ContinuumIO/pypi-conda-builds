package:
  name: jsongit
  version: "0.0.7"

source:
  fn: jsongit-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/j/jsongit/jsongit-0.0.7.tar.gz
  md5: c5a8d2bfcf693e0915e33339b3bc9163
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsongit = jsongit:main
    #
    # Would create an entry point called jsongit that calls jsongit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygit2 >=0.16.1
    - json_diff >=1.2.9

  run:
    - python
    - pygit2 >=0.16.1
    - json_diff >=1.2.9

test:
  # Python imports
  imports:
    - jsongit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/talos/jsongit
  license: BSD License
  summary: 'Git for JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
