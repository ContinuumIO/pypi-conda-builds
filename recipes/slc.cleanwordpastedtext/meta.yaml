package:
  name: slc.cleanwordpastedtext
  version: "1.2.8"

source:
  fn: slc.cleanwordpastedtext-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/s/slc.cleanwordpastedtext/slc.cleanwordpastedtext-1.2.8.tar.gz
  md5: 5ab89f735e822552648f68d1d828da8e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.cleanwordpastedtext = slc.cleanwordpastedtext:main
    #
    # Would create an entry point called slc.cleanwordpastedtext that calls slc.cleanwordpastedtext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.form
    - htmllaundry
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - z3c.form
    - htmllaundry
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - slc
    - slc.cleanwordpastedtext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.example
  license: GPL
  summary: "Clean up the HTML formatting problems introduced by pasting content from MSWord into Plone's RichText fields."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
