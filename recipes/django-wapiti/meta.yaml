package:
  name: django-wapiti
  version: "0.0.2"

source:
  fn: django-wapiti-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wapiti/django-wapiti-0.0.2.tar.gz
  md5: 75e5514555643317788e3031cf911b30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wapiti = django-wapiti:main
    #
    # Would create an entry point called django-wapiti that calls django-wapiti.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - wapiti
    - wapiti.migrations
    - wapiti.tests
    - wapiti.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ecometrica/django-wapiti/
  license: BSD License
  summary: 'Django API layer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
