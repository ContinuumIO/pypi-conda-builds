package:
  name: everysport
  version: "1.2.1"

source:
  fn: everysport-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/e/everysport/everysport-1.2.1.tar.gz
  md5: 4a27b2da4459cc15ce6995958c68f402
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - everysport = everysport:main
    #
    # Would create an entry point called everysport that calls everysport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - everysport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peterstark72/everysport
  license: UNKNOWN
  summary: 'A Python wrapper for the Everysport API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
