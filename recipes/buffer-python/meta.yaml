package:
  name: buffer-python
  version: "1.06"

source:
  fn: buffer-python-1.06.tar.gz
  url: https://pypi.python.org/packages/source/b/buffer-python/buffer-python-1.06.tar.gz
  md5: 351ce6513d36954f99e008ef2081364a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buffer-python = buffer-python:main
    #
    # Would create an entry point called buffer-python that calls buffer-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - colorama ==0.2.7
    - coverage ==3.7.1
    - mock ==1.0.1
    - nose ==1.3.0
    - rauth ==0.6.2
    - requests ==1.2.3

  run:
    - python
    - colorama ==0.2.7
    - coverage ==3.7.1
    - mock ==1.0.1
    - nose ==1.3.0
    - rauth ==0.6.2
    - requests ==1.2.3

test:
  # Python imports
  imports:
    - buffpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vtemian/buffpy
  license: UNKNOWN
  summary: 'Python library for Buffer App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
