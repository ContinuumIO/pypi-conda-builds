package:
  name: collective.powertoken.core
  version: "0.3.0"

source:
  fn: collective.powertoken.core-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.powertoken.core/collective.powertoken.core-0.3.0.tar.gz
  md5: 7defb93c71e1b52ac0d26816f9686328
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.powertoken.core = collective.powertoken.core:main
    #
    # Would create an entry point called collective.powertoken.core that calls collective.powertoken.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.powertoken
    - collective.powertoken.core
    - collective.powertoken.core.browser
    - collective.powertoken.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.powertoken.core
  license: GPL
  summary: 'A mechanism for bypass Plone security, performing operations normally unauthorized, if a secret token is know (core package)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
