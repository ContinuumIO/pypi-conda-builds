package:
  name: django-teryt
  version: "0.2.0"

source:
  fn: django-teryt-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-teryt/django-teryt-0.2.0.tar.gz
  md5: c3144c462fa42cd150489ac4fd1ad3e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-teryt = django-teryt:main
    #
    # Would create an entry point called django-teryt that calls django-teryt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - teryt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/scibi/django-teryt
  license: BSD License
  summary: 'TERYT database implementation for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
