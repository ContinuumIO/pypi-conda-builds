package:
  name: django-knowledge
  version: "0.2.0"

source:
  fn: django-knowledge-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-knowledge/django-knowledge-0.2.0.tar.gz
  md5: d7b757fc5793b53ea71b1640b3833539
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-knowledge = django-knowledge:main
    #
    # Would create an entry point called django-knowledge that calls django-knowledge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - markdown >=2.1.1
    - django >=1.4

  run:
    - python
    - markdown >=2.1.1
    - django >=1.4

test:
  # Python imports
  imports:
    - knowledge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zapier/django-knowledge
  license: BSD License
  summary: 'A simple frontend and admin interface for dealing with help\n        knowledge tickets and issues, including public and private responses and searching.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
