package:
  name: djangocms-styledlink
  version: "0.2.2"

source:
  fn: djangocms-styledlink-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-styledlink/djangocms-styledlink-0.2.2.tar.gz
  md5: 159a5d95dc74ae530a3b20724704c457
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-styledlink = djangocms-styledlink:main
    #
    # Would create an entry point called djangocms-styledlink that calls djangocms-styledlink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_styledlink
    - djangocms_styledlink.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mkoistinen/djangocms-styledlink
  license: BSD License
  summary: 'A Universal, Styled Link Plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
