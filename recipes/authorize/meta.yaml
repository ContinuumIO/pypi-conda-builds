package:
  name: authorize
  version: "0.1.0"

source:
  fn: authorize-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/authorize/authorize-0.1.0.tar.gz
  md5: e04f8c310d03e2a6041b1bab5e3c5245
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authorize = authorize:main
    #
    # Would create an entry point called authorize that calls authorize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - authorize
    - authorize.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://adroll.com/labs
  license: MIT License
  summary: 'An API to connect to authorize.net(TM) payment services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
