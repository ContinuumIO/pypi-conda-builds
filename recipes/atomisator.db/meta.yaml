package:
  name: atomisator.db
  version: "0.3.4"

source:
  fn: atomisator.db-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/a/atomisator.db/atomisator.db-0.3.4.tar.gz
  md5: 3c60ae4ba75dd13b85e913fb63efd7e5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomisator.db = atomisator.db:main
    #
    # Would create an entry point called atomisator.db that calls atomisator.db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sqlalchemy
    - pysqlite

  run:
    - python
    - setuptools
    - setuptools
    - sqlalchemy
    - pysqlite

test:
  # Python imports
  imports:
    - atomisator
    - atomisator.db
    - atomisator.db.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atomisator.ziade.org
  license: GPL
  summary: 'Expert Python Programming - Manages the Atomisator storage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
