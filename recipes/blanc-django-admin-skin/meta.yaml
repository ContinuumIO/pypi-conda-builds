package:
  name: blanc-django-admin-skin
  version: "0.1.4"

source:
  fn: blanc-django-admin-skin-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/b/blanc-django-admin-skin/blanc-django-admin-skin-0.1.4.tar.gz
  md5: 4e2019e1d0b983858e71d830e2f81cd5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blanc-django-admin-skin = blanc-django-admin-skin:main
    #
    # Would create an entry point called blanc-django-admin-skin that calls blanc-django-admin-skin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - blanc_django_admin_skin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.blanctools.com/
  license: BSD License
  summary: 'Blanc Admin Skin for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
