package:
  name: sverigesradio
  version: "0.3"

source:
  fn: sverigesradio-0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sverigesradio/sverigesradio-0.3.tar.gz
  md5: a7e591783723322ea12e7636180d6eb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sverigesradio = sverigesradio:main
    #
    # Would create an entry point called sverigesradio that calls sverigesradio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0

  run:
    - python
    - requests >=2.1.0

test:
  # Python imports
  imports:
    - sverigesradio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tiwilliam/sverigesradio
  license: MIT License
  summary: 'Python bindings for Sveriges Radio API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
