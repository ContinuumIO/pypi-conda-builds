package:
  name: sphinxcontrib-sadisplay
  version: "0.3.2dev-20130518"

source:
  fn: sphinxcontrib-sadisplay-0.3.2dev-20130518.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-sadisplay/sphinxcontrib-sadisplay-0.3.2dev-20130518.tar.gz
  md5: 6658b79a071d4732e2a3a481ac4679a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-sadisplay = sphinxcontrib-sadisplay:main
    #
    # Would create an entry point called sphinxcontrib-sadisplay that calls sphinxcontrib-sadisplay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6
    - sadisplay >=0.3

  run:
    - python
    - sphinx >=0.6
    - sadisplay >=0.3

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/birkenfeld/sphinx-contrib
  license: BSD License
  summary: 'Sphinx "sadisplay" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
