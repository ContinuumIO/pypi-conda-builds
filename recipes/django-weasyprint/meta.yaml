package:
  name: django-weasyprint
  version: "0.1"

source:
  fn: django-weasyprint-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-weasyprint/django-weasyprint-0.1.tar.gz
  md5: 973f9d2f6b5207ff8215c6f3d035320a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-weasyprint = django-weasyprint:main
    #
    # Would create an entry point called django-weasyprint that calls django-weasyprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - weasyprint

  run:
    - python
    - django >=1.4
    - weasyprint

test:
  # Python imports
  imports:
    - django_weasyprint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Django weasyprint app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
