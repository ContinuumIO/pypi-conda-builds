package:
  name: taggd
  version: "0.2.2"

source:
  fn: taggd-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/t/taggd/taggd-0.2.2.tar.gz
  md5: b89e34058c773aefb872d2db943c2c4b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taggd = taggd:main
    #
    # Would create an entry point called taggd that calls taggd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysam
    - numpy

  run:
    - python
    - setuptools
    - pysam
    - numpy

test:
  # Python imports
  imports:
    - taggd
    - taggd.core
    - taggd.io
    - taggd.misc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JoelSjostrand/taggd
  license: Open BSD
  summary: 'Bioinformatics genetic barcode demultiplexing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
