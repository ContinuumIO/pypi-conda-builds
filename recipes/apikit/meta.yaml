package:
  name: apikit
  version: "0.3.2"

source:
  fn: apikit-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/a/apikit/apikit-0.3.2.tar.gz
  md5: bb381ac3142f629e3d5d242e72637dd0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apikit = apikit:main
    #
    # Would create an entry point called apikit that calls apikit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - apikit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pudo/apikit
  license: BSD License
  summary: 'A set of utility functions for RESTful Flask applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
