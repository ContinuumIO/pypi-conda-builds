package:
  name: sc.contentrules.localrole
  version: "1.0b3"

source:
  fn: sc.contentrules.localrole-1.0b3.zip
  url: https://pypi.python.org/packages/source/s/sc.contentrules.localrole/sc.contentrules.localrole-1.0b3.zip
  md5: 30d983183227cfbc9d76b54d88f0b8c6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.contentrules.localrole = sc.contentrules.localrole:main
    #
    # Would create an entry point called sc.contentrules.localrole that calls sc.contentrules.localrole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.contentrules
    - plone.contentrules
    - plone.stringinterp
    - products.cmfcore
    - products.statusmessages
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - plone.app.contentrules
    - plone.contentrules
    - plone.stringinterp
    - products.cmfcore
    - products.statusmessages
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - sc
    - sc.contentrules
    - sc.contentrules.localrole
    - sc.contentrules.localrole.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br
  license: GPL
  summary: 'Plone content rule action to apply local roles to a content object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
