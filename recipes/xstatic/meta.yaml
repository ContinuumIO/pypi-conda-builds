package:
  name: xstatic
  version: "1.0.1"

source:
  fn: XStatic-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic/XStatic-1.0.1.tar.gz
  md5: 7064b5ea7fd1ee990e0ae0538301d9bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic = xstatic:main
    #
    # Would create an entry point called xstatic that calls xstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http:/bitbucket.org/thomaswaldmann/xstatic
  license: MIT License
  summary: 'XStatic base package with minimal support code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
