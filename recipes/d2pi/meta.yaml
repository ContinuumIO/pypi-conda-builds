package:
  name: d2pi
  version: "0.0.9.2"

source:
  fn: d2pi-0.0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/d2pi/d2pi-0.0.9.2.tar.gz
  md5: 4660990ad3c5fccc3a9eeeb03a659a61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - d2pi = d2pi:main
    #
    # Would create an entry point called d2pi that calls d2pi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - watchdog
    - dropbox
    - pyyaml

  run:
    - python
    - watchdog
    - dropbox
    - pyyaml

test:
  # Python imports
  imports:
    - d2pi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/GuoJing/Drop2PI
  license: GNU General Public License v3 (GPLv3)
  summary: 'Python library for Dropbox to Raspberry PI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
