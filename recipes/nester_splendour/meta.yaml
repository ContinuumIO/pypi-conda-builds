package:
  name: nester_splendour
  version: "1.3.0"

source:
  fn: nester_splendour-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nester_splendour/nester_splendour-1.3.0.tar.gz
  md5: cf209b2176afc56bce8491fdd32600d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nester_splendour = nester_splendour:main
    #
    # Would create an entry point called nester_splendour that calls nester_splendour.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nester_splendour

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://splendourhui.com
  license: UNKNOWN
  summary: 'A simple printer of a list'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
