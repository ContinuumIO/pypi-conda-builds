package:
  name: edmunds
  version: "0.1.4"

source:
  fn: edmunds-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/e/edmunds/edmunds-0.1.4.tar.gz
  md5: 31ec3854dcb60cdacf5fff16210951c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edmunds = edmunds:main
    #
    # Would create an entry point called edmunds that calls edmunds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=0.8.6

  run:
    - python
    - requests >=0.8.6

test:
  # Python imports
  imports:
    - edmunds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EdmundsAPI/sdk-python
  license: LICENSE
  summary: 'This is an awesome Python 2 wrapper for the Edmunds.com API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
