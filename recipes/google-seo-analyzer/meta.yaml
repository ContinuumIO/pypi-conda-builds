package:
  name: google-seo-analyzer
  version: "0.1.1"

source:
  fn: google-seo-analyzer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/google-seo-analyzer/google-seo-analyzer-0.1.1.tar.gz
  md5: edc6a752fc232fc04b8b4b9942918dc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google-seo-analyzer = google-seo-analyzer:main
    #
    # Would create an entry point called google-seo-analyzer that calls google-seo-analyzer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.2.3

  run:
    - python
    - requests ==1.2.3

test:
  # Python imports
  imports:
    - seo_analyzer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/victorpantoja/google-seo-analyzer
  license: MIT
  summary: u'This simple script parses Google Web Master Tools report and analyzes results.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
