package:
  name: il2fb-difficulty
  version: "1.2.0"

source:
  fn: il2fb-difficulty-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/il2fb-difficulty/il2fb-difficulty-1.2.0.tar.gz
  md5: db89650290947d4a13f96b224ad3ac9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - il2fb-difficulty = il2fb-difficulty:main
    #
    # Would create an entry point called il2fb-difficulty that calls il2fb-difficulty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - candv >=1.2.0
    - il2fb-commons >=0.9.8
    - six >=1.6.1
    - stringlike >=0.3.3
    - verboselib >=0.1.0

  run:
    - python
    - candv >=1.2.0
    - il2fb-commons >=0.9.8
    - six >=1.6.1
    - stringlike >=0.3.3
    - verboselib >=0.1.0

test:
  # Python imports
  imports:
    - il2fb.config.difficulty
    - il2fb.config.difficulty.settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IL2HorusTeam/il2fb-difficulty/
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Parser and emitter of difficulty settings for IL-2 FB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
