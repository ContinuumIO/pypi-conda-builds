package:
  name: django-rwiki
  version: "0.0.3"

source:
  fn: django-rwiki-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rwiki/django-rwiki-0.0.3.tar.gz
  md5: c6a76eb65de27d3234fe65723bc3aff8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rwiki = django-rwiki:main
    #
    # Would create an entry point called django-rwiki that calls django-rwiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5
    - south ==0.7.5
    - pil ==1.1.7
    - django-filebrowser-no-grappelli ==3.1.1
    - sorl-thumbnail ==11.12
    - django-compressor ==1.1.2
    - django-reversion ==1.6.3
    - diff-match-patch ==20120106
    - django-mptt ==0.6.0

  run:
    - python
    - django <1.5
    - south ==0.7.5
    - pil ==1.1.7
    - django-filebrowser-no-grappelli ==3.1.1
    - sorl-thumbnail ==11.12
    - django-compressor ==1.1.2
    - django-reversion ==1.6.3
    - diff-match-patch ==20120106
    - django-mptt ==0.6.0

test:
  # Python imports
  imports:
    - rwiki
    - rwiki.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r0sk/django-rwiki
  license: BSD License
  summary: 'Yet another Django Wiki App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
