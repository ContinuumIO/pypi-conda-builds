package:
  name: juju-deployerizer
  version: "0.1.4"

source:
  fn: juju-deployerizer-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/j/juju-deployerizer/juju-deployerizer-0.1.4.tar.gz
  md5: be4ce7bc976b4f144df88b77d63412b3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - juju-deployerizer = juju-deployerizer:main
    #
    # Would create an entry point called juju-deployerizer that calls juju-deployerizer.main()

    - juju-deployerizer=juju_deployerizer.cli:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - setuptools
    - pyyaml

test:
  # Python imports
  imports:
    - juju_deployerizer

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - juju-deployerizer --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
