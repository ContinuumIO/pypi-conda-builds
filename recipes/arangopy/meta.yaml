package:
  name: arangopy
  version: "0.5.6"

source:
  fn: ArangoPy-0.5.6.tar.gz
  url: https://pypi.python.org/packages/source/A/ArangoPy/ArangoPy-0.5.6.tar.gz
  md5: 2c2f57ebab621607ef2bcd8b79f2cf84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arangopy = arangopy:main
    #
    # Would create an entry point called arangopy that calls arangopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - arangodb
    - arangodb.index
    - arangodb.orm
    - arangodb.query
    - arangodb.query.utils
    - arangodb.server
    - arangodb.tests
    - arangodb.tests.api
    - arangodb.tests.orm
    - arangodb.tests.orm.fields
    - arangodb.tests.query
    - arangodb.transaction
    - arangodb.transaction.javascript
    - arangopy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saeschdivara/ArangoPy
  license: MIT
  summary: 'Driver for ArangoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
