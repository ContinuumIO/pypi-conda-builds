package:
  name: django-keen
  version: "0.1.3"

source:
  fn: django-keen-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-keen/django-keen-0.1.3.tar.gz
  md5: 2837bea08e55315c40e1ee1b6f0397e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-keen = django-keen:main
    #
    # Would create an entry point called django-keen that calls django-keen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - keen ==0.3.0
    - django >=1.6

  run:
    - python
    - keen ==0.3.0
    - django >=1.6

test:
  # Python imports
  imports:
    - dkeen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-keen/
  license: LICENSE.txt
  summary: 'Simple wrapper for django around the official keen.io client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
