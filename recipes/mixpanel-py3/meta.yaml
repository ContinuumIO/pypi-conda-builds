package:
  name: mixpanel-py3
  version: "3.0.4"

source:
  fn: mixpanel-py3-3.0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mixpanel-py3/mixpanel-py3-3.0.4.tar.gz
  md5: b026887c759ae92a20f6b64b1c9a4a8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixpanel-py3 = mixpanel-py3:main
    #
    # Would create an entry point called mixpanel-py3 that calls mixpanel-py3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mixpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MyGGaN/mixpanel-python
  license: Apache Software License
  summary: 'Mixpanel library for Python 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
