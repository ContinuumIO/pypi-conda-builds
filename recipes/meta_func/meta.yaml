package:
  name: meta_func
  version: "0.1.3"

source:
  fn: meta_func-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/meta_func/meta_func-0.1.3.tar.gz
  md5: 534e7ed272ed00fb3537a82bc92bee84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meta_func = meta_func:main
    #
    # Would create an entry point called meta_func that calls meta_func.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator

  run:
    - python
    - decorator

test:
  # Python imports
  imports:
    - meta_func

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/gstaubli/meta_func
  license: UNKNOWN
  summary: 'Python decorator function to track metadata on function calls'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
