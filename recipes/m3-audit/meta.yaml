package:
  name: m3-audit
  version: "2.0.1"

source:
  fn: m3-audit-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-audit/m3-audit-2.0.1.tar.gz
  md5: c931cfc3841cd1221bd82c2d4b17aea6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-audit = m3-audit:main
    #
    # Would create an entry point called m3-audit that calls m3-audit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - m3_audit
    - m3_audit.migrations
    - tests
    - tests.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barsgroup/m3_audit
  license: MIT License
  summary: u'\u0410\u0443\u0434\u0438\u0442 \u043e\u043f\u0435\u0440\u0430\u0446\u0438\u0439 \u0432 \u043f\u0440\u0438\u043a\u043b\u0430\u0434\u043d\u043e\u0439 \u0441\u0438\u0441\u0442\u0435\u043c\u0435'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
