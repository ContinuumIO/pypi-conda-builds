package:
  name: arduino_scons
  version: "0.1"

source:
  fn: arduino_scons-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/arduino_scons/arduino_scons-0.1.tar.gz
  md5: 71dc1f5452e99119fba68cbb23c754c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arduino_scons = arduino_scons:main
    #
    # Would create an entry point called arduino_scons that calls arduino_scons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - arduino_scons
    - path_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cfobel/path_helpers
  license: MIT License
  summary: 'Helper class and functions for working with file path'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
