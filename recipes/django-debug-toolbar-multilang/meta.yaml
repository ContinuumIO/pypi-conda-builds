package:
  name: django-debug-toolbar-multilang
  version: "1.0.1"

source:
  fn: django-debug-toolbar-multilang-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-toolbar-multilang/django-debug-toolbar-multilang-1.0.1.tar.gz
  md5: 1b207be3835b06c2c1859eca21c7aa4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-toolbar-multilang = django-debug-toolbar-multilang:main
    #
    # Would create an entry point called django-debug-toolbar-multilang that calls django-debug-toolbar-multilang.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock
    - django >=1.7.1
    - django-debug-toolbar

  run:
    - python
    - mock
    - django >=1.7.1
    - django-debug-toolbar

test:
  # Python imports
  imports:
    - debug_toolbar_multilang
    - debug_toolbar_multilang.panels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/matt3o12/django-debug-toolbar-multilang
  license: MIT License
  summary: 'Language panel for django-debug-toolbar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
