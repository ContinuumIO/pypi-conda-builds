package:
  name: django-shpaml-template-loader
  version: "0.2.3"

source:
  fn: django-shpaml-template-loader-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shpaml-template-loader/django-shpaml-template-loader-0.2.3.tar.gz
  md5: ecab3a3294198c45b6ff582010c060a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shpaml-template-loader = django-shpaml-template-loader:main
    #
    # Would create an entry point called django-shpaml-template-loader that calls django-shpaml-template-loader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - shpaml_loader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jiaaro/django-shpaml-template-loader/
  license: BSD License
  summary: 'A Django template loader for loading and converting SHPAML markup to HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
