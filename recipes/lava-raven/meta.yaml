package:
  name: lava-raven
  version: "0.1.1"

source:
  fn: lava-raven-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-raven/lava-raven-0.1.1.tar.gz
  md5: 77e9c23fc3a48bbc7d4e16b31777dfa9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-raven = lava-raven:main
    #
    # Would create an entry point called lava-raven that calls lava-raven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lava-server >=0.9.1
    - raven >=1.4.2
    - versiontools >=1.8

  run:
    - python
    - setuptools
    - lava-server >=0.9.1
    - raven >=1.4.2
    - versiontools >=1.8

test:
  # Python imports
  imports:
    - lava
    - lava.raven

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lava-raven
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Raven/Sentry support for LAVA Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
