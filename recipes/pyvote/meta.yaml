package:
  name: pyvote
  version: "0.0.1"

source:
  fn: pyvote-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyvote/pyvote-0.0.1.tar.gz
  md5: 58de3ca8a1d0f39e3b31891b70796060
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvote = pyvote:main
    #
    # Would create an entry point called pyvote that calls pyvote.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - hiredis

  run:
    - python
    - redis
    - hiredis

test:
  # Python imports
  imports:
    - pyvote

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/pyvote
  license: ISC License (ISCL)
  summary: 'Python voting with a redis backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
