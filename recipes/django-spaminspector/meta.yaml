package:
  name: django-spaminspector
  version: "0.1rc4"

source:
  fn: django-spaminspector-0.1rc4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spaminspector/django-spaminspector-0.1rc4.tar.gz
  md5: c78afcc4337093168695b195312db9d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spaminspector = django-spaminspector:main
    #
    # Would create an entry point called django-spaminspector that calls django-spaminspector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - spaminspector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-spaminspector
  license: BSD License
  summary: 'django-spaminspector is a generic spam inspector of Django via Akismet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
