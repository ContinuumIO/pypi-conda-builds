package:
  name: crange
  version: "0.1.2"

source:
  fn: crange-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/crange/crange-0.1.2.tar.gz
  md5: e73caa6cab4bc44eba2def091bb05832
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crange = crange:main
    #
    # Would create an entry point called crange that calls crange.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - clang >=3.5
    - tabulate >=0.7

  run:
    - python
    - clang >=3.5
    - tabulate >=0.7

test:
  # Python imports
  imports:
    - crange

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/crange/crange
  license: BSD
  summary: 'Crange is a tool to index and cross-reference C/C++ source code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
