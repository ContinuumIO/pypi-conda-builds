package:
  name: conekta
  version: "1.2"

source:
  fn: conekta-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/conekta/conekta-1.2.tar.gz
  md5: d550eaf8c9b22a48c8a3700f799e2cf1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - conekta = conekta:main
    #
    # Would create an entry point called conekta that calls conekta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - simplejson
    - nose

  run:
    - python
    - httplib2
    - simplejson
    - nose

test:
  # Python imports
  imports:
    - conekta
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/conekta/conekta-python
  license: MIT License
  summary: 'Easy Conekta python wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
