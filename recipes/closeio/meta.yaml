package:
  name: closeio
  version: "0.1"

source:
  fn: closeio-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/closeio/closeio-0.1.tar.gz
  md5: 682a55b0ce40cbdcf40c2a954b7b4153
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - closeio = closeio:main
    #
    # Would create an entry point called closeio that calls closeio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.10.8

  run:
    - python
    - requests >=0.10.8

test:
  # Python imports
  imports:
    - closeio
    - closeio_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elasticsales/closeio-api/
  license: UNKNOWN
  summary: 'Close.io Python Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
