package:
  name: ofxstatement-postfinance
  version: "0.0.1"

source:
  fn: ofxstatement-postfinance-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/ofxstatement-postfinance/ofxstatement-postfinance-0.0.1.tar.gz
  md5: 763010ff84872fb860738b498c79bc55
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ofxstatement-postfinance = ofxstatement-postfinance:main
    #
    # Would create an entry point called ofxstatement-postfinance that calls ofxstatement-postfinance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ofxstatement

  run:
    - python
    - setuptools
    - ofxstatement

test:
  # Python imports
  imports:
    - ofxstatement
    - ofxstatement.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxandersen/ofxstatement-postfinance
  license: GNU Affero General Public License v3
  summary: 'ofxstatement for Post Finance (Swiss)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
