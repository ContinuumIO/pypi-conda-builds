package:
  name: django-hashphrase
  version: "0.4"

source:
  fn: django-hashphrase-0.4.zip
  url: https://pypi.python.org/packages/source/d/django-hashphrase/django-hashphrase-0.4.zip
  md5: f0b535f00d38c7d0f91ce0cfc25d7893
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hashphrase = django-hashphrase:main
    #
    # Would create an entry point called django-hashphrase that calls django-hashphrase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hashphrase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: n/a
  license: BSD License
  summary: 'A Django app to facilitate email links'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
