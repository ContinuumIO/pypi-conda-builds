package:
  name: json_serializable
  version: "1.0.1"

source:
  fn: json_serializable-1.0.1.zip
  url: https://pypi.python.org/packages/source/j/json_serializable/json_serializable-1.0.1.zip
  md5: a38e4e43fa971c6df8b97a19ce78106a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_serializable = json_serializable:main
    #
    # Would create an entry point called json_serializable that calls json_serializable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - json_serializable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/icoxfog417/json_serializable.git
  license: UNKNOWN
  summary: 'enables python class to be serializable/deserializable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
