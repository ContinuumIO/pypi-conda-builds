package:
  name: git-archive
  version: "0.3.0"

source:
  fn: git-archive-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/g/git-archive/git-archive-0.3.0.tar.gz
  md5: 7c17a45550c9e1c2bd67949b7aaf4a00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-archive = git-archive:main
    #
    # Would create an entry point called git-archive that calls git-archive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - git_archive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EricChiang/git-archive
  license: MIT
  summary: 'Go back into the git archive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
