package:
  name: pelican.bitly
  version: "0.1"

source:
  fn: pelican.bitly-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican.bitly/pelican.bitly-0.1.tar.gz
  md5: 3a8183f17c8adf0dc96ff2690c3fb000
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican.bitly = pelican.bitly:main
    #
    # Would create an entry point called pelican.bitly that calls pelican.bitly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican >=3.0
    - bitlyapi

  run:
    - python
    - pelican >=3.0
    - bitlyapi

test:
  # Python imports
  imports:
    - pelican-bitly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rach/pelican.bitly
  license: UNKNOWN
  summary: 'Plugin for pelican to add bit.ly short urls to pages and articles. Useful for analytics of retweet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
