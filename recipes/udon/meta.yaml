package:
  name: udon
  version: "0.1.1"

source:
  fn: udon-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/u/udon/udon-0.1.1.tar.gz
  md5: 74de8a48e53725693617d5e5bf944d09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - udon = udon:main
    #
    # Would create an entry point called udon that calls udon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - udon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ikegami-yukino/udon
  license: MIT License
  summary: 'Normalizing English lengthened expression having repeating letters. (e.g., "cooooooooooooooollllllllllllll" to "cool")'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
