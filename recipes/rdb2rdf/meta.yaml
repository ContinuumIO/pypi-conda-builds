package:
  name: rdb2rdf
  version: "0.1.3"

source:
  fn: rdb2rdf-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/r/rdb2rdf/rdb2rdf-0.1.3.tar.gz
  md5: d69bcf4b170f576966093d2cf9be6d40
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rdb2rdf = rdb2rdf:main
    #
    # Would create an entry point called rdb2rdf that calls rdb2rdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rdflib
    - spruce-collections
    - spruce-datetime
    - spruce-iri
    - spruce-types
    - sqlalchemy >=0.9.1

  run:
    - python
    - setuptools
    - rdflib
    - spruce-collections
    - spruce-datetime
    - spruce-iri
    - spruce-types
    - sqlalchemy >=0.9.1

test:
  # Python imports
  imports:
    - rdb2rdf
    - rdb2rdf.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: u'PyRDB2RDF'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
