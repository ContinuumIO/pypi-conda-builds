package:
  name: leftronic
  version: "1.5.0"

source:
  fn: leftronic-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/l/leftronic/leftronic-1.5.0.tar.gz
  md5: e9ac2eca9aa9494165f07a5d8279b443
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leftronic = leftronic:main
    #
    # Would create an entry point called leftronic that calls leftronic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - leftronic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.leftronic.com/api/
  license: leftronicd
  summary: 'A Python package to interface with the Leftronic API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
