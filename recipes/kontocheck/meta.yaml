package:
  name: kontocheck
  version: "5.5.11"

source:
  fn: kontocheck-5.5.11.tar.gz
  url: https://pypi.python.org/packages/source/k/kontocheck/kontocheck-5.5.11.tar.gz
  md5: 8734e3c1599a7aec0a933d830d925575
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kontocheck = kontocheck:main
    #
    # Would create an entry point called kontocheck that calls kontocheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - kontocheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.joonis.de/software/fintech/kontocheck
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: u'Python ctypes wrapper of the konto_check library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
