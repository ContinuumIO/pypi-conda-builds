package:
  name: moecache
  version: "0.2"

source:
  fn: moecache-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/moecache/moecache-0.2.tar.gz
  md5: 101c3679142f3bd8d54bc4b7f1c96488
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moecache = moecache:main
    #
    # Would create an entry point called moecache that calls moecache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moecache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lichray/moecache
  license: Apache Software License
  summary: 'A memcached client with a different shading strategy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
