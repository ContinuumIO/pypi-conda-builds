package:
  name: galaxygetopt
  version: "0.9.8"

source:
  fn: galaxygetopt-0.9.8.tar.gz
  url: https://pypi.python.org/packages/source/g/galaxygetopt/galaxygetopt-0.9.8.tar.gz
  md5: 344a11a08f5a4e5475c68531e864e5d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - galaxygetopt = galaxygetopt:main
    #
    # Would create an entry point called galaxygetopt that calls galaxygetopt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - galaxygetopt
    - galaxygetopt.parameter
    - galaxygetopt.writer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://cpt.tamu.edu/gitlab/cpt/pygetoptwrapper/
  license: GPL3
  summary: 'Argparse Wrapper for Galaxy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
