package:
  name: greenapple
  version: "0.1.2"

source:
  fn: greenapple-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/greenapple/greenapple-0.1.2.tar.gz
  md5: 6b1c46938d6c19e489a3c05e69451346
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - greenapple = greenapple:main
    #
    # Would create an entry point called greenapple that calls greenapple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - greenapple
    - greenapple.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/greenapple/
  license: LICENSE.txt
  summary: 'Useful Database Layer Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
