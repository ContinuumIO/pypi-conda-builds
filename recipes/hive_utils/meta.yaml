package:
  name: hive_utils
  version: "0.0.1"

source:
  fn: hive_utils-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hive_utils/hive_utils-0.0.1.tar.gz
  md5: 83cc79c26c3011b87d8cc77cd5e81586
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hive_utils = hive_utils:main
    #
    # Would create an entry point called hive_utils that calls hive_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hive-thrift-py

  run:
    - python
    - hive-thrift-py

test:
  # Python imports
  imports:
    - hive_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eventbrite/python_hive_utils
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
