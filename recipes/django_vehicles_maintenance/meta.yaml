package:
  name: django_vehicles_maintenance
  version: "0.0.6"

source:
  fn: django_vehicles_maintenance-0.0.6.zip
  url: https://pypi.python.org/packages/source/d/django_vehicles_maintenance/django_vehicles_maintenance-0.0.6.zip
  md5: 7bbcad524a9dcaff7a29f1c8a8b3034c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_vehicles_maintenance = django_vehicles_maintenance:main
    #
    # Would create an entry point called django_vehicles_maintenance that calls django_vehicles_maintenance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.4.3
    - south ==0.7.6

  run:
    - python
    - django ==1.4.3
    - south ==0.7.6

test:
  # Python imports
  imports:
    - django_vehicles_maintenance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: '`mantenimiento de vehiculos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
