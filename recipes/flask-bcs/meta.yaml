package:
  name: flask-bcs
  version: "0.9.1"

source:
  fn: Flask-BCS-0.9.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-BCS/Flask-BCS-0.9.1.zip
  md5: 1e15bcd467c173b0a4c6502c166aaf76
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bcs = flask-bcs:main
    #
    # Would create an entry point called flask-bcs that calls flask-bcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - setuptools
    - flask

test:
  # Python imports
  imports:
    - pybcs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/csuzhangxc/Flask-BCS
  license: MIT License
  summary: 'Baidu Cloud Storage for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
