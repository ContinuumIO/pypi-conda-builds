package:
  name: moncov
  version: "0.5.16"

source:
  fn: moncov-0.5.16.tar.gz
  url: https://pypi.python.org/packages/source/m/moncov/moncov-0.5.16.tar.gz
  md5: ce1736e850963e233f298820102ba965
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moncov = moncov:main
    #
    # Would create an entry point called moncov that calls moncov.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - hiredis
    - aaargh
    - pyyaml
    - lxml
    - coverage

  run:
    - python
    - redis
    - hiredis
    - aaargh
    - pyyaml
    - lxml
    - coverage

test:
  # Python imports
  imports:
    - moncov
    - moncov/stats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RedHatQE/python-moncov
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Python remote code coverage using Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
