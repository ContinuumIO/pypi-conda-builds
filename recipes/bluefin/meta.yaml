package:
  name: bluefin
  version: "1.4"

source:
  fn: bluefin-1.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bluefin/bluefin-1.4.tar.gz
  md5: 94e869a444394c50bf82b7d03c15b59b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bluefin = bluefin:main
    #
    # Would create an entry point called bluefin that calls bluefin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - bluefin
    - bluefin.dataretrieval
    - bluefin.directmode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/duointeractive/python-bluefin/
  license: MIT License
  summary: 'A simple Bluefin Payment System API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
