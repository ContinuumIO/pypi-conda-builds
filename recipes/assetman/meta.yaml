package:
  name: assetman
  version: "0.0.4"

source:
  fn: assetman-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/assetman/assetman-0.0.4.tar.gz
  md5: d4e8064997526b0c793285b75acf8f2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - assetman = assetman:main
    #
    # Would create an entry point called assetman that calls assetman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - assetman
    - assetman.core
    - assetman.service
    - assetman.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/petermelias/assetman
  license: MIT License
  summary: 'Generic blob-data asset manager that provides an API, CLI interface, for managing S3 assets and the respective CloudFront delivery setup.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
