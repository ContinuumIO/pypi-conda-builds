package:
  name: modgrammar
  version: "0.10"

source:
  fn: modgrammar-0.10.tar.gz
  url: https://pypi.python.org/packages/source/m/modgrammar/modgrammar-0.10.tar.gz
  md5: 8d4b335a40fd88eef58dce32bb61e2f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modgrammar = modgrammar:main
    #
    # Would create an entry point called modgrammar that calls modgrammar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - modgrammar
    - modgrammar.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/modgrammar
  license: BSD License
  summary: 'Modular grammar-parsing engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
