package:
  name: python-ceilometerclient
  version: "1.0.14"

source:
  fn: python-ceilometerclient-1.0.14.tar.gz
  url: https://pypi.python.org/packages/source/p/python-ceilometerclient/python-ceilometerclient-1.0.14.tar.gz
  md5: 3f46b08979f10239a7d6f03bf7294deb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-ceilometerclient = python-ceilometerclient:main
    #
    # Would create an entry point called python-ceilometerclient that calls python-ceilometerclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbr

  run:
    - python
    - pbr

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Telemetry API Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
