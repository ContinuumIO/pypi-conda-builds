package:
  name: django-coverage
  version: "1.2.4"

source:
  fn: django-coverage-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-coverage/django-coverage-1.2.4.tar.gz
  md5: 20c853910783e56e544775c61cf64ef6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-coverage = django-coverage:main
    #
    # Would create an entry point called django-coverage that calls django-coverage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_coverage
    - django_coverage.management
    - django_coverage.management.commands
    - django_coverage.utils
    - django_coverage.utils.coverage_report
    - django_coverage.utils.coverage_report.templates
    - django_coverage.utils.module_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kmike/django-coverage/
  license: Apache Software License
  summary: 'Django Test Coverage App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
