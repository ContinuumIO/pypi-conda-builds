package:
  name: jsonchkstore
  version: "0.99f5191420dd2749efb3a7c54a9280183983f485"

source:
  fn: jsonchkstore-0.99f5191420dd2749efb3a7c54a9280183983f485.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonchkstore/jsonchkstore-0.99f5191420dd2749efb3a7c54a9280183983f485.tar.gz
  md5: adc52e0c2f121beb226e55b81626f04e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonchkstore = jsonchkstore:main
    #
    # Would create an entry point called jsonchkstore that calls jsonchkstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chkstore >=0

  run:
    - python
    - chkstore >=0

test:
  # Python imports
  imports:
    - jsonchkstore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nejucomo/jsonchkstore
  license: GPLv3
  summary: 'Persistent directed acyclic graph storage with JSON and binary blobs using chkstore.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
