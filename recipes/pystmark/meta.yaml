package:
  name: pystmark
  version: "0.4.4"

source:
  fn: pystmark-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pystmark/pystmark-0.4.4.tar.gz
  md5: a50afd4c38d3ece62f59f0350ccd6acb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystmark = pystmark:main
    #
    # Would create an entry point called pystmark that calls pystmark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.0

  run:
    - python
    - requests >=1.2.0

test:
  # Python imports
  imports:
    - pystmark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xsleonard/pystmark
  license: MIT License
  summary: 'A Python library for the Postmark API (http://developer.postmarkapp.com/).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
