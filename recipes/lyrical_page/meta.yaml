package:
  name: lyrical_page
  version: "3.0.0"

source:
  fn: lyrical_page-3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lyrical_page/lyrical_page-3.0.0.tar.gz
  md5: 4673a363ca60fb5ae543dda5d515cc5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lyrical_page = lyrical_page:main
    #
    # Would create an entry point called lyrical_page that calls lyrical_page.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lyrical
    - lyrical.site_calendar
    - lyrical.site_content
    - lyrical.site_content.management
    - lyrical.site_content.management.commands
    - lyrical.site_content.migrations
    - lyrical.site_content.south_migrations
    - lyrical.site_content.templatetags
    - lyrical.site_news
    - lyrical.site_news.migrations
    - lyrical.site_news.south_migrations
    - lyrical.site_seo
    - lyrical.site_seo.migrations
    - lyrical.site_seo.south_migrations
    - lyrical.site_slider
    - lyrical.site_slider.migrations
    - lyrical.site_slider.south_migrations
    - lyrical.site_slider.templatetags
    - lyrical.site_tracking
    - lyrical.site_tracking.migrations
    - lyrical.site_tracking.south_migrations
    - lyrical.site_tracking.templatetags
    - lyrical.site_util
    - lyrical_page

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.lyrical.net/projects/lyrical_page/
  license: Apache Software License
  summary: 'Website content system based on a mashup of ideas from the Django contrib flatpage appp, joomla!, and many years of systems development..'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
