package:
  name: hamlish-jinja
  version: "0.3.2"

source:
  fn: Hamlish-Jinja-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/H/Hamlish-Jinja/Hamlish-Jinja-0.3.2.tar.gz
  md5: 38b84ff2e62e9aeb790c8b4737c41424
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hamlish-jinja = hamlish-jinja:main
    #
    # Would create an entry point called hamlish-jinja that calls hamlish-jinja.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Pitmairen/hamlish-jinja
  license: BSD License
  summary: 'Haml-ish syntax for jinja2 templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
