package:
  name: blueox
  version: "0.10.0"

source:
  fn: blueox-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/b/blueox/blueox-0.10.0.tar.gz
  md5: 9727bca80e070c47d29daf7a14947cb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blueox = blueox:main
    #
    # Would create an entry point called blueox that calls blueox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyzmq
    - msgpack_python

  run:
    - python
    - pyzmq
    - msgpack_python

test:
  # Python imports
  imports:
    - blueox
    - blueox.contrib
    - blueox.contrib.celery
    - blueox.contrib.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rhettg/BlueOx
  license: ISC
  summary: 'A library for python-based application logging and data collection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
