package:
  name: adi.simplestructure
  version: "0.4"

source:
  fn: adi.simplestructure-0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/adi.simplestructure/adi.simplestructure-0.4.tar.gz
  md5: 678b4d8e035864afcd693a3664f5ed60
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adi.simplestructure = adi.simplestructure:main
    #
    # Would create an entry point called adi.simplestructure that calls adi.simplestructure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.contentwellportlets

  run:
    - python
    - setuptools
    - setuptools
    - products.contentwellportlets

test:
  # Python imports
  imports:
    - adi
    - adi.simplestructure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Removes top and footer viewlets and adds portlets instead.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
