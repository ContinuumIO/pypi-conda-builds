package:
  name: cadvisor-metrics
  version: "0.1.3"

source:
  fn: cadvisor-metrics-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cadvisor-metrics/cadvisor-metrics-0.1.3.tar.gz
  md5: c4918a49365a6f1ed586eafd6056bbb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cadvisor-metrics = cadvisor-metrics:main
    #
    # Would create an entry point called cadvisor-metrics that calls cadvisor-metrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - falcon
    - gunicorn
    - py-dateutil
    - requests

  run:
    - python
    - redis
    - falcon
    - gunicorn
    - py-dateutil
    - requests

test:
  # Python imports
  imports:
    - collector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/catalyzeio/cadvisor-metrics
  license: Apache 2.0
  summary: 'cAdvisor metrics collection from Docker containers running on multiple hosts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
