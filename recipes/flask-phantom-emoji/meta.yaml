package:
  name: flask-phantom-emoji
  version: "0.1.4"

source:
  fn: flask-phantom-emoji-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-phantom-emoji/flask-phantom-emoji-0.1.4.tar.gz
  md5: 3a3179ce9a964d3c58a10b702987754a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-phantom-emoji = flask-phantom-emoji:main
    #
    # Would create an entry point called flask-phantom-emoji that calls flask-phantom-emoji.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_phantom_emoji

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nickw444/flask-phantom-emoji
  license: UNKNOWN
  summary: "Add phantom emoji's to your flask application"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
