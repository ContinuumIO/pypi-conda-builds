package:
  name: casjobs
  version: "0.0.1"

source:
  fn: casjobs-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/casjobs/casjobs-0.0.1.tar.gz
  md5: a12c6460dc5385415a0e3e95ff0d01c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - casjobs = casjobs:main
    #
    # Would create an entry point called casjobs that calls casjobs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - casjobs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dfm/casjobs
  license: MIT License
  summary: 'An interface to CasJobs for Humans.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
