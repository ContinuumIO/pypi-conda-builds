package:
  name: setuptools-metadata
  version: "0.1.5"

source:
  fn: setuptools-metadata-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-metadata/setuptools-metadata-0.1.5.tar.gz
  md5: aa80d03d3eba25df8d49fa8c7149426c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-metadata = setuptools-metadata:main
    #
    # Would create an entry point called setuptools-metadata that calls setuptools-metadata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - setuptools_metadata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/j0057/setuptools-metadata
  license: setuptools_mtn
  summary: 'Adds a command to setup.py for displaying metadata about the package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
