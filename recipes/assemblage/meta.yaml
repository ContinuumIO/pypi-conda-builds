package:
  name: assemblage
  version: "0.1"

source:
  fn: assemblage-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/assemblage/assemblage-0.1.tar.gz
  md5: 85cd492c7f3e44cb7868e29626ecdea7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - assemblage = assemblage:main
    #
    # Would create an entry point called assemblage that calls assemblage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - assemblage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pmatiello/assemblage
  license: MIT License
  summary: 'A small Python framework for building objects in dependency-injection style.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
