package:
  name: bchelpers
  version: "0.3.0"

source:
  fn: bchelpers-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bchelpers/bchelpers-0.3.0.tar.gz
  md5: b1057493d90a62fb025b7183467d2f4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bchelpers = bchelpers:main
    #
    # Would create an entry point called bchelpers that calls bchelpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - botocore >=0.49.0
    - jmespath >=0.4.1

  run:
    - python
    - botocore >=0.49.0
    - jmespath >=0.4.1

test:
  # Python imports
  imports:
    - bchelpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/garnaat/bchelpers
  license: Apache Software License
  summary: 'Handy helpers to make botocore easier to use'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
