package:
  name: collective.twitter.portlets
  version: "1.0b5"

source:
  fn: collective.twitter.portlets-1.0b5.zip
  url: https://pypi.python.org/packages/source/c/collective.twitter.portlets/collective.twitter.portlets-1.0b5.zip
  md5: 62fa0f517e27af13fce8976f353df409
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.twitter.portlets = collective.twitter.portlets:main
    #
    # Would create an entry point called collective.twitter.portlets that calls collective.twitter.portlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.prettydate >=1.1
    - collective.twitter.accounts >=1.0.3

  run:
    - python
    - setuptools
    - setuptools
    - collective.prettydate >=1.1
    - collective.twitter.accounts >=1.0.3

test:
  # Python imports
  imports:
    - collective
    - collective.twitter
    - collective.twitter.portlets
    - collective.twitter.portlets.Extensions
    - collective.twitter.portlets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.twitter.portlets
  license: GNU General Public License (GPL)
  summary: 'This product allows you to add different kinds of portlets to your site to get tweets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
