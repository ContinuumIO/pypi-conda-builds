package:
  name: infi.conf
  version: "0.0.15"

source:
  fn: infi.conf-0.0.15.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.conf/infi.conf-0.0.15.tar.gz
  md5: e9d4eff36e1128ff377cfd19964d5475
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.conf = infi.conf:main
    #
    # Would create an entry point called infi.conf that calls infi.conf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sentinels
    - six

  run:
    - python
    - sentinels
    - six

test:
  # Python imports
  imports:
    - infi
    - infi.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vmalloc/infi.conf
  license: BSD License
  summary: 'Generic configuration mechanism'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
