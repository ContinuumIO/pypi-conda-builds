package:
  name: django-monon
  version: "0.0.1-dev"

source:
  fn: django-monon-0.0.1-dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-monon/django-monon-0.0.1-dev.tar.gz
  md5: 867bd95e5ce0d6e0c667180c63188bc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-monon = django-monon:main
    #
    # Would create an entry point called django-monon that calls django-monon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - pymonon

  run:
    - python
    - django >=1.3
    - pymonon

test:
  # Python imports
  imports:
    - monon
    - monon.forms
    - monon.models
    - monon.tests
    - monon.tests.dummyapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pedroburon/django-monon
  license: UNKNOWN
  summary: u'Django fields for use with pymonon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
