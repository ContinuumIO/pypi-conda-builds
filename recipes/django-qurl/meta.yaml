package:
  name: django-qurl
  version: "0.1.1"

source:
  fn: django-qurl-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qurl/django-qurl-0.1.1.tar.gz
  md5: 690462fc4d4258efe6e652cb60602b6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qurl = django-qurl:main
    #
    # Would create an entry point called django-qurl that calls django-qurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - qurl
    - qurl.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/obiwanus/django-qurl/
  license: MIT License
  summary: 'A set of tools to append, remove or replace query string parameters from a url (originally created by Sophilabs)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
