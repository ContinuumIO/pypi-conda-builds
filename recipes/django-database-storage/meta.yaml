package:
  name: django-database-storage
  version: "0.1.2"

source:
  fn: django-database-storage-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-database-storage/django-database-storage-0.1.2.tar.gz
  md5: 03d97880ee664372f29094fb2d98368c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-database-storage = django-database-storage:main
    #
    # Would create an entry point called django-database-storage that calls django-database-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - database_storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
