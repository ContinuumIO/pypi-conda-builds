package:
  name: django-heroku-memcacheify
  version: "0.8"

source:
  fn: django-heroku-memcacheify-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-heroku-memcacheify/django-heroku-memcacheify-0.8.tar.gz
  md5: 729e72966ba7522d79b05161a959943e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-heroku-memcacheify = django-heroku-memcacheify:main
    #
    # Would create an entry point called django-heroku-memcacheify that calls django-heroku-memcacheify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-pylibmc ==0.5.0

  run:
    - python
    - django-pylibmc ==0.5.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/django-heroku-memcacheify
  license: UNLICENSE
  summary: 'Automatic Django memcached configuration on Heroku.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
