package:
  name: python-instagram
  version: "1.3.1"

source:
  fn: python-instagram-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-instagram/python-instagram-1.3.1.tar.gz
  md5: 6a47ba8a004c06cc6e1cc3c01d909164
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-instagram = python-instagram:main
    #
    # Would create an entry point called python-instagram that calls python-instagram.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - httplib2
    - six

  run:
    - python
    - simplejson
    - httplib2
    - six

test:
  # Python imports
  imports:
    - instagram

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Instagram/python-instagram
  license: MIT
  summary: 'Instagram API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
