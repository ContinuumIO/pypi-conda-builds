package:
  name: django-fasttest
  version: "0.1"

source:
  fn: django-fasttest-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fasttest/django-fasttest-0.1.tar.gz
  md5: 68ed22c9ec561aab9fe6c05bb07439bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fasttest = django-fasttest:main
    #
    # Would create an entry point called django-fasttest that calls django-fasttest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_fasttest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/machinalis/django-fasttest
  license: BSD License
  summary: u'A variant on django.test.TestCase optimized for postgres'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
