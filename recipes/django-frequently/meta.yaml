package:
  name: django-frequently
  version: "1.3.2"

source:
  fn: django-frequently-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-frequently/django-frequently-1.3.2.tar.gz
  md5: e0a9387a3207f340b5083fcf437be65c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-frequently = django-frequently:main
    #
    # Would create an entry point called django-frequently that calls django-frequently.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.1
    - django-libs
    - south

  run:
    - python
    - django >=1.6.1
    - django-libs
    - south

test:
  # Python imports
  imports:
    - frequently
    - frequently.migrations
    - frequently.templatetags
    - frequently.tests
    - frequently.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-frequently
  license: The MIT License
  summary: 'A reusable Django app that can be used as a FAQ.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
