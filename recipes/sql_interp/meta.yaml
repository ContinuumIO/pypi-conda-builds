package:
  name: sql_interp
  version: "0.1.0"

source:
  fn: sql_interp-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sql_interp/sql_interp-0.1.0.tar.gz
  md5: 9489cd96261ea3776cdb39d2e0fec7ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sql_interp = sql_interp:main
    #
    # Would create an entry point called sql_interp that calls sql_interp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sql_interp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mirhampt/sql_interp/
  license: MIT
  summary: 'Interpolate Python variables into SQL statements.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
