package:
  name: manuelpi.fakemodule
  version: "0.1b1"

source:
  fn: manuelpi.fakemodule-0.1b1.tar.gz
  url: https://pypi.python.org/packages/source/m/manuelpi.fakemodule/manuelpi.fakemodule-0.1b1.tar.gz
  md5: c49d45a62475df9ad223fe3f2de568f2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - manuelpi.fakemodule = manuelpi.fakemodule:main
    #
    # Would create an entry point called manuelpi.fakemodule that calls manuelpi.fakemodule.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - manuel

  run:
    - python
    - setuptools
    - manuel

test:
  # Python imports
  imports:
    - manuelpi
    - manuelpi.fakemodule

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/manuelpi.fakemodule
  license: Zope Public License
  summary: 'Module support in doctests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
