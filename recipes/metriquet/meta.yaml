package:
  name: metriquet
  version: "0.2.7-1a"

source:
  fn: metriquet-0.2.7-1a.tar.gz
  url: https://pypi.python.org/packages/source/m/metriquet/metriquet-0.2.7-1a.tar.gz
  md5: 62313f2731494abf0e53e90cf6953562
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metriquet = metriquet:main
    #
    # Would create an entry point called metriquet that calls metriquet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - metriqueu
    - tornado ==3.2

  run:
    - python
    - metriqueu
    - tornado ==3.2

test:
  # Python imports
  imports:
    - metriquet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drpoovilleorg/metrique
  license: GNU General Public License v3 (GPLv3)
  summary: 'Metrique - Generic Tornado Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
