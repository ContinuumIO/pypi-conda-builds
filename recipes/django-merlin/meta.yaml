package:
  name: django-merlin
  version: "0.8"

source:
  fn: django-merlin-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-merlin/django-merlin-0.8.tar.gz
  md5: 8bd87a8b49f3885a95db335fd5fd775f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-merlin = django-merlin:main
    #
    # Would create an entry point called django-merlin that calls django-merlin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - merlin
    - merlin.tests
    - merlin.tests.fixtures
    - merlin.tests.fixtures.testproject
    - merlin.wizards

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/supercodepoet/django-merlin
  license: BSD License
  summary: 'Providing alternate form wizards for the Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
