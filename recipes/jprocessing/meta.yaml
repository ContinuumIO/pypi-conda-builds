package:
  name: jprocessing
  version: "0.1"

source:
  fn: jProcessing-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jProcessing/jProcessing-0.1.tar.gz
  md5: a2ea28adf26506763e50320cebc04111
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jprocessing = jprocessing:main
    #
    # Would create an entry point called jprocessing that calls jprocessing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jNlp
    - jNlp.data
    - jprocessing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jaist.ac.jp/~s1010205
  license: LICENSE.txt
  summary: 'Japanese NLP Utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
