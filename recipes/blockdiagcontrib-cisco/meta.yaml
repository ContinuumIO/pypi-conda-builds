package:
  name: blockdiagcontrib-cisco
  version: "0.1.8"

source:
  fn: blockdiagcontrib-cisco-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-cisco/blockdiagcontrib-cisco-0.1.8.tar.gz
  md5: 636bfc9d71625d7d5e50e88f14ebce1e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-cisco = blockdiagcontrib-cisco:main
    #
    # Would create an entry point called blockdiagcontrib-cisco that calls blockdiagcontrib-cisco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.3.0
    - setuptools

  run:
    - python
    - setuptools
    - blockdiag >=1.3.0
    - setuptools

test:
  # Python imports
  imports:
    - blockdiagcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tk0miya.bitbucket.org/blockdiag/build/html/index.html
  license: Python Software Foundation License
  summary: 'noderenderer plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
