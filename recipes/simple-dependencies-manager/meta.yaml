package:
  name: simple-dependencies-manager
  version: "0.6.0"

source:
  fn: simple-dependencies-manager-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simple-dependencies-manager/simple-dependencies-manager-0.6.0.tar.gz
  md5: 76e5a985525d4f2e153fd6b1f6977332
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple-dependencies-manager = simple-dependencies-manager:main
    #
    # Would create an entry point called simple-dependencies-manager that calls simple-dependencies-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.10

  run:
    - python
    - pyyaml ==3.10

test:
  # Python imports
  imports:
    - simple_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/victorpantoja/simple-dependencies-manager
  license: Apache License 2.0
  summary: u'A simple script that helps you take control of your projects so you do not get crazy!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
