package:
  name: django-galicia
  version: "0.11"

source:
  fn: django-galicia-0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-galicia/django-galicia-0.11.tar.gz
  md5: ed65c5dcda2c259cb1d1a9fd41838bea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-galicia = django-galicia:main
    #
    # Would create an entry point called django-galicia that calls django-galicia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_galicia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fonso/django-galicia
  license: BSD License
  summary: 'Django helpers for Galicia (Spain).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
