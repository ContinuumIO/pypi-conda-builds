package:
  name: mogilefs_storage
  version: "0.2"

source:
  fn: mogilefs_storage-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mogilefs_storage/mogilefs_storage-0.2.tar.gz
  md5: cb85aa0be4f837366e7b141588271ae6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mogilefs_storage = mogilefs_storage:main
    #
    # Would create an entry point called mogilefs_storage that calls mogilefs_storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - mogilefs_storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'MogileFS FileStorage for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
