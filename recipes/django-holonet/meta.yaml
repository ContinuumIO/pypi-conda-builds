package:
  name: django-holonet
  version: "0.0.2"

source:
  fn: django-holonet-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-holonet/django-holonet-0.0.2.tar.gz
  md5: 465e580b845423397eb9744c105de06c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-holonet = django-holonet:main
    #
    # Would create an entry point called django-holonet that calls django-holonet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - holonet_django
    - holonet_django.management
    - holonet_django.management.commands
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/webkom/django-holonet
  license: MIT License
  summary: 'This package is used to control holonet from a django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
