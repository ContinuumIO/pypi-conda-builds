package:
  name: mongoalchemy
  version: "0.19"

source:
  fn: MongoAlchemy-0.19.tar.gz
  url: https://pypi.python.org/packages/source/M/MongoAlchemy/MongoAlchemy-0.19.tar.gz
  md5: f441cfee7aa17672d59d9af93af5a457
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoalchemy = mongoalchemy:main
    #
    # Would create an entry point called mongoalchemy that calls mongoalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - mongoalchemy
    - mongoalchemy.fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mongoalchemy.org/
  license: MIT License
  summary: 'Document-Object Mapper/Toolkit for Mongo Databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
