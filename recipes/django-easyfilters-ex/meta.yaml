package:
  name: django-easyfilters-ex
  version: "0.7.0"

source:
  fn: django-easyfilters-ex-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easyfilters-ex/django-easyfilters-ex-0.7.0.tar.gz
  md5: 83c76d005f3bb07a5fb8223716ed5d8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easyfilters-ex = django-easyfilters-ex:main
    #
    # Would create an entry point called django-easyfilters-ex that calls django-easyfilters-ex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - python-dateutil
    - six

  run:
    - python
    - django >=1.3
    - python-dateutil
    - six

test:
  # Python imports
  imports:
    - django_easyfilters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionelmc/django-easyfilters/
  license: MIT License
  summary: 'Easy creation of link-based filtering for a list of Django model objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
