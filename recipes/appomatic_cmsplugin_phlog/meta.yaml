package:
  name: appomatic_cmsplugin_phlog
  version: "0.0.30"

source:
  fn: appomatic_cmsplugin_phlog-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_cmsplugin_phlog/appomatic_cmsplugin_phlog-0.0.30.tar.gz
  md5: 13177d514e907dabe6bd28c2d5612680
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_cmsplugin_phlog = appomatic_cmsplugin_phlog:main
    #
    # Would create an entry point called appomatic_cmsplugin_phlog that calls appomatic_cmsplugin_phlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appomatic_django_cms ==0.0.30
    - appomatic_photolog ==0.0.30
    - cmsplugin-phlog
    - appomaticcore ==0.0.30

  run:
    - python
    - appomatic_django_cms ==0.0.30
    - appomatic_photolog ==0.0.30
    - cmsplugin-phlog
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_cmsplugin_phlog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benliles/cmsplugin-phlog
  license: GPL
  summary: 'Integrates django-cms and Photologue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
