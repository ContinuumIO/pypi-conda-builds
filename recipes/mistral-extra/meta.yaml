package:
  name: mistral-extra
  version: "2015.1.0"

source:
  fn: mistral-extra-2015.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mistral-extra/mistral-extra-2015.1.0.tar.gz
  md5: 0c732c440edc37a7310ed02dce0120ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mistral-extra = mistral-extra:main
    #
    # Would create an entry point called mistral-extra that calls mistral-extra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: mistune
  summary: 'Mistral Extras'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
