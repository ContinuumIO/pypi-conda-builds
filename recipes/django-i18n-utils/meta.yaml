package:
  name: django-i18n-utils
  version: "1.5.1"

source:
  fn: django-i18n-utils-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-i18n-utils/django-i18n-utils-1.5.1.tar.gz
  md5: 79d61aa4d4dd9766d23eebdb0153e5be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-i18n-utils = django-i18n-utils:main
    #
    # Would create an entry point called django-i18n-utils that calls django-i18n-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_i18n_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/django-i18n-utils
  license: MIT License
  summary: 'Utilities for localized Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
