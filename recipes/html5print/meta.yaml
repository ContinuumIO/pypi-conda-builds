package:
  name: html5print
  version: "0.1"

source:
  fn: html5print-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/html5print/html5print-0.1.tar.gz
  md5: 732dce2c57afd9f89ca9591f5866a4de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html5print = html5print:main
    #
    # Would create an entry point called html5print that calls html5print.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.3.2
    - chardet >=2.2.1
    - html5lib >=0.999
    - requests >=2.3.5
    - slimit >=0.8.1
    - tinycss2 >=0.4

  run:
    - python
    - beautifulsoup4 >=4.3.2
    - chardet >=2.2.1
    - html5lib >=0.999
    - requests >=2.3.5
    - slimit >=0.8.1
    - tinycss2 >=0.4

test:
  # Python imports
  imports:
    - html5print

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/berniey/html5print
  license: Apache Software License
  summary: u'HTML5, CSS, Javascript Pretty Print'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
