package:
  name: django-image-diet
  version: "0.1.1"

source:
  fn: django-image-diet-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-image-diet/django-image-diet-0.1.1.tar.gz
  md5: 8c190b6cbd3e83b8454437ebfac1d383
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-image-diet = django-image-diet:main
    #
    # Would create an entry point called django-image-diet that calls django-image-diet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - image_diet
    - image_diet.management
    - image_diet.management.commands
    - image_diet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ArabellaTech/django-image-diet
  license: MIT License
  summary: 'Remove unnecessary bytes from images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
