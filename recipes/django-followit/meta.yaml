package:
  name: django-followit
  version: "0.0.7"

source:
  fn: django-followit-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-followit/django-followit-0.0.7.tar.gz
  md5: c5bc0bb227ddf84103698ed5fc464aa9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-followit = django-followit:main
    #
    # Would create an entry point called django-followit that calls django-followit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - followit
    - followit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ASKBOT/django-followit
  license: BSD License
  summary: 'A Django application that allows users to follow django model objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
