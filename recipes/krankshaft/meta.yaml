package:
  name: krankshaft
  version: "0.3.13"

source:
  fn: krankshaft-0.3.13.tar.gz
  url: https://pypi.python.org/packages/source/k/krankshaft/krankshaft-0.3.13.tar.gz
  md5: d54591af1356d736a1d3b8a914b551f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - krankshaft = krankshaft:main
    #
    # Would create an entry point called krankshaft that calls krankshaft.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mimeparse ==0.1.3

  run:
    - python
    - mimeparse ==0.1.3

test:
  # Python imports
  imports:
    - krankshaft
    - krankshaft.compat
    - tests
    - tests.krankshaft

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dlamotte/krankshaft
  license: MIT License
  summary: 'A Web API Framework (with Django, ...)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
