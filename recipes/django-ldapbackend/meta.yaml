package:
  name: django-ldapbackend
  version: "0.0.1.1"

source:
  fn: django-ldapbackend-0.0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ldapbackend/django-ldapbackend-0.0.1.1.tar.gz
  md5: 750bbefb95164cd392c7b31acc5340ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ldapbackend = django-ldapbackend:main
    #
    # Would create an entry point called django-ldapbackend that calls django-ldapbackend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0
    - python-ldap >=2.3.9

  run:
    - python
    - django >=1.0
    - python-ldap >=2.3.9

test:
  # Python imports
  imports:
    - django_ldapbackend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-ldapbackend.googlecode.com
  license: BSD License
  summary: 'LDAP Backend for Django (1.0 or higher)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
