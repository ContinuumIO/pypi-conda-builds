package:
  name: collective.recipe.vimproject
  version: "0.3.2"

source:
  fn: collective.recipe.vimproject-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.vimproject/collective.recipe.vimproject-0.3.2.tar.gz
  md5: cb1778b558f3d56ef505e7ca9b8dcbd1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.vimproject = collective.recipe.vimproject:main
    #
    # Would create an entry point called collective.recipe.vimproject that calls collective.recipe.vimproject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mkvimproject

  run:
    - python
    - setuptools
    - setuptools
    - mkvimproject

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.vimproject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/buildout/collective.recipe.vimproject/trunk
  license: GPL
  summary: 'Set up a VIM development environment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
