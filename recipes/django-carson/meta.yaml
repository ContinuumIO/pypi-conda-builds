package:
  name: django-carson
  version: "0.2"

source:
  fn: django-carson-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-carson/django-carson-0.2.tar.gz
  md5: 597ba54b950e425f35720a7a56d6fffd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-carson = django-carson:main
    #
    # Would create an entry point called django-carson that calls django-carson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz ==2011h
    - oauth2 ==1.5.170
    - requests ==0.6.1

  run:
    - python
    - pytz ==2011h
    - oauth2 ==1.5.170
    - requests ==0.6.1

test:
  # Python imports
  imports:
    - carson
    - carson.management
    - carson.management.commands
    - carson.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edavis/django-carson
  license: BSD License
  summary: 'Easily store and display tweets from a set of users or tweets about a given topic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
