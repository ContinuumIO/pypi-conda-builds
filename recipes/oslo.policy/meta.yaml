package:
  name: oslo.policy
  version: "0.4.0"

source:
  fn: oslo.policy-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oslo.policy/oslo.policy-0.4.0.tar.gz
  md5: a0ddfa5782191c218e1e100234550295
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oslo.policy = oslo.policy:main
    #
    # Would create an entry point called oslo.policy that calls oslo.policy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Oslo Policy library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
