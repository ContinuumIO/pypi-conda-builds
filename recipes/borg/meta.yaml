package:
  name: borg
  version: "2012.4.01"

source:
  fn: borg-2012.4.01.tar.gz
  url: https://pypi.python.org/packages/source/b/borg/borg-2012.4.01.tar.gz
  md5: 0b3a8324c20037b18bda97bb8ab122ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - borg = borg:main
    #
    # Would create an entry point called borg that calls borg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cython >=0.15.1
    - numpy >=1.6.1
    - plac >=0.9.0
    - scipy >=0.10.0

  run:
    - python
    - cython >=0.15.1
    - numpy >=1.6.1
    - plac >=0.9.0
    - scipy >=0.10.0

test:
  # Python imports
  imports:
    - borg
    - borg.domains
    - borg.domains.asp
    - borg.domains.max_sat
    - borg.domains.max_sat.test
    - borg.domains.pb
    - borg.domains.pb.test
    - borg.domains.sat
    - borg.domains.sat.test
    - borg.experiments
    - borg.test
    - borg.tools
    - borg.unix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nn.cs.utexas.edu/pages/research/borg/
  license: MIT
  summary: 'the borg algorithm portfolio toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
