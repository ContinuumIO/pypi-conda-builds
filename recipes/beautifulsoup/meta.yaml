package:
  name: beautifulsoup
  version: "3.2.1"

source:
  fn: BeautifulSoup-3.2.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BeautifulSoup/BeautifulSoup-3.2.1.tar.gz
  md5: 44656527ef3ac9874ac4d1c9f35f70ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beautifulsoup = beautifulsoup:main
    #
    # Would create an entry point called beautifulsoup that calls beautifulsoup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - beautifulsoup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.crummy.com/software/BeautifulSoup/
  license: Python Software Foundation License
  summary: 'HTML/XML parser for quick-turnaround applications like screen-scraping.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
