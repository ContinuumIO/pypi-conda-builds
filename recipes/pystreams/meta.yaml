package:
  name: pystreams
  version: "0.6"

source:
  fn: pystreams-0.6.tar.gz
  url: https://pypi.python.org/packages/source/p/pystreams/pystreams-0.6.tar.gz
  md5: 9762e9c0d9a98488824751a9e46adfb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystreams = pystreams:main
    #
    # Would create an entry point called pystreams that calls pystreams.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.6.1
    - repoze.lru ==0.6
    - futures ==2.1.6

  run:
    - python
    - six ==1.6.1
    - repoze.lru ==0.6
    - futures ==2.1.6

test:
  # Python imports
  imports:
    - pystreams
    - streams
    - streams.executors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/9seconds/streams/
  license: MIT License
  summary: 'Streams inspired by Java 8'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
