package:
  name: birdcage
  version: "0.9.0"

source:
  fn: birdcage-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/b/birdcage/birdcage-0.9.0.tar.gz
  md5: e28b642875161a6bf59eedc1af6717db
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - birdcage = birdcage:main
    #
    # Would create an entry point called birdcage that calls birdcage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - birdcage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://thesoftwarestudio.com/birdcage/
  license: MIT License
  summary: 'library to squeeze text fragments into a phrase of a specified length.Used mainly to create tweets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
