package:
  name: cubicweb-nosylist
  version: "0.6.0"

source:
  fn: cubicweb-nosylist-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-nosylist/cubicweb-nosylist-0.6.0.tar.gz
  md5: b2911b730edaf83af0e4da2498fd55ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-nosylist = cubicweb-nosylist:main
    #
    # Would create an entry point called cubicweb-nosylist that calls cubicweb-nosylist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.20.3

  run:
    - python
    - cubicweb >=3.20.3

test:
  # Python imports
  imports:
    - nosylist
    - nosylist.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-nosylist
  license: LGPL
  summary: 'roundup like nosylist component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
