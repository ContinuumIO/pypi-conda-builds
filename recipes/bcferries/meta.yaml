package:
  name: bcferries
  version: "0.1.2"

source:
  fn: bcferries-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bcferries/bcferries-0.1.2.tar.gz
  md5: ce9eec7b54a5e9822d9395b367514f6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bcferries = bcferries:main
    #
    # Would create an entry point called bcferries that calls bcferries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4
    - python-dateutil
    - functools32
    - python-levenshtein
    - geopy

  run:
    - python
    - requests
    - beautifulsoup4
    - python-dateutil
    - functools32
    - python-levenshtein
    - geopy

test:
  # Python imports
  imports:
    - bcferries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://yasyf.github.io/bcferries/
  license: MIT License
  summary: 'BC Ferries Python Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
