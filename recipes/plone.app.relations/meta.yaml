package:
  name: plone.app.relations
  version: "2.0"

source:
  fn: plone.app.relations-2.0.zip
  url: https://pypi.python.org/packages/source/p/plone.app.relations/plone.app.relations-2.0.zip
  md5: 8c68c3c6eed2a8089e301aaac4a1c26d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.relations = plone.app.relations:main
    #
    # Would create an entry point called plone.app.relations that calls plone.app.relations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.relations
    - zope.site
    - zope.intid
    - zope2 >=2.13

  run:
    - python
    - setuptools
    - setuptools
    - plone.relations
    - zope.site
    - zope.intid
    - zope2 >=2.13

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.relations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.relations
  license: GNU General Public License (GPL)
  summary: 'A set of components to provide a content centric API for the engine from plone.relations, as well as a few different core relationship types and policies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
