package:
  name: vowpal_porpoise
  version: "0.3"

source:
  fn: vowpal_porpoise-0.3.tar.gz
  url: https://pypi.python.org/packages/source/v/vowpal_porpoise/vowpal_porpoise-0.3.tar.gz
  md5: 5fac104b8153e5d0e03eebd6d1ad499f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vowpal_porpoise = vowpal_porpoise:main
    #
    # Would create an entry point called vowpal_porpoise that calls vowpal_porpoise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scikit-learn

  run:
    - python
    - numpy
    - scikit-learn

test:
  # Python imports
  imports:
    - vowpal_porpoise

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/josephreisinger/vowpal_porpoise
  license: BSD License
  summary: 'Lightweight vowpal wabbit wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
