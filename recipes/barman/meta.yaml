package:
  name: barman
  version: "1.4.1"

source:
  fn: barman-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/b/barman/barman-1.4.1.tar.gz
  md5: c055c1168e3ee941a94cddfdf74af5cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barman = barman:main
    #
    # Would create an entry point called barman that calls barman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2
    - argh >=0.21.2
    - python-dateutil
    - argcomplete

  run:
    - python
    - psycopg2
    - argh >=0.21.2
    - python-dateutil
    - argcomplete

test:
  # Python imports
  imports:
    - barman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pgbarman.org/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Backup and Recovery Manager for PostgreSQL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
