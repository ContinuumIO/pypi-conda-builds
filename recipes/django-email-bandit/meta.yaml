package:
  name: django-email-bandit
  version: "1.0"

source:
  fn: django-email-bandit-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-bandit/django-email-bandit-1.0.tar.gz
  md5: edd262bd7584ab802a6964a498bd42ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-bandit = django-email-bandit:main
    #
    # Would create an entry point called django-email-bandit that calls django-email-bandit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bandit
    - bandit.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caktus/django-email-bandit
  license: BSD License
  summary: 'django-email-bandit is a Django email backend for hijacking email sending in a test environment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
