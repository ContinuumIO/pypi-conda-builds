package:
  name: tornado-mysql
  version: "0.5"

source:
  fn: Tornado-MySQL-0.5.tar.gz
  url: https://pypi.python.org/packages/source/T/Tornado-MySQL/Tornado-MySQL-0.5.tar.gz
  md5: 031ee612435136abe8954d8a8876506d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-mysql = tornado-mysql:main
    #
    # Would create an entry point called tornado-mysql that calls tornado-mysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=4.0

  run:
    - python
    - tornado >=4.0

test:
  # Python imports
  imports:
    - tornado_mysql
    - tornado_mysql.constants
    - tornado_mysql.tests
    - tornado_mysql.tests.thirdparty
    - tornado_mysql.tests.thirdparty.test_MySQLdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PyMySQL/Tornado-MySQL
  license: MIT License
  summary: 'Pure Python MySQL Driver for Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
