package:
  name: collective.xmltestreport
  version: "1.3.2"

source:
  fn: collective.xmltestreport-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.xmltestreport/collective.xmltestreport-1.3.2.tar.gz
  md5: 86bb01b31c085ad3537bc5095b4d12a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.xmltestreport = collective.xmltestreport:main
    #
    # Would create an entry point called collective.xmltestreport that calls collective.xmltestreport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zope.testrunner
    - zc.recipe.egg
    - z3c.recipe.scripts

  run:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zope.testrunner
    - zc.recipe.egg
    - z3c.recipe.scripts

test:
  # Python imports
  imports:
    - collective
    - collective.xmltestreport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.xmltestreport
  license: ZPL 2.1
  summary: 'A test runner which can output an XML report compatible with JUnit and Jenkins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
