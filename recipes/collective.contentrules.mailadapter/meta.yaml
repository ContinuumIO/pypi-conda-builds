package:
  name: collective.contentrules.mailadapter
  version: "1.0"

source:
  fn: collective.contentrules.mailadapter-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentrules.mailadapter/collective.contentrules.mailadapter-1.0.tar.gz
  md5: 1fc8394553269b1783d66fc4599e0e72
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.mailadapter = collective.contentrules.mailadapter:main
    #
    # Would create an entry point called collective.contentrules.mailadapter that calls collective.contentrules.mailadapter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.mailadapter
    - collective.contentrules.mailadapter.actions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contentrules.mailadapter
  license: GPL
  summary: 'A mail content rule action using an adapter for emails resolving'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
