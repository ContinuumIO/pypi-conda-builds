package:
  name: amenukit
  version: "0.1a1"

source:
  fn: amenukit-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/a/amenukit/amenukit-0.1a1.tar.gz
  md5: 4b2d5ecb31a28f32f549bd82a2e3c7ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amenukit = amenukit:main
    #
    # Would create an entry point called amenukit that calls amenukit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - amenukit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://amenukit.sourceforge.net/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Cross gui-menu building utility.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
