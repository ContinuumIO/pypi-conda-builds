package:
  name: ewb_case.casclient
  version: "0.1.3"

source:
  fn: ewb_case.casclient-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/e/ewb_case.casclient/ewb_case.casclient-0.1.3.tar.gz
  md5: 14d05e1bb37f0b602b438e3eafd96c57
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ewb_case.casclient = ewb_case.casclient:main
    #
    # Would create an entry point called ewb_case.casclient that calls ewb_case.casclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.i18nmessageid
    - anz.casclient

  run:
    - python
    - setuptools
    - setuptools
    - zope.i18nmessageid
    - anz.casclient

test:
  # Python imports
  imports:
    - ewb_case
    - ewb_case.casclient
    - ewb_case.casclient.browser
    - ewb_case.casclient.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattbierner/ewb_case.casclient
  license: GPL
  summary: 'Provides CAS authentication for EWB Case website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
