package:
  name: collective.taghelper
  version: "0.3.1"

source:
  fn: collective.taghelper-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.taghelper/collective.taghelper-0.3.1.tar.gz
  md5: 1ff394df7a2d11011a06063a988a1997
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.taghelper = collective.taghelper:main
    #
    # Would create an entry point called collective.taghelper that calls collective.taghelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - yql
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - yql
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.taghelper
    - collective.taghelper.browser
    - collective.taghelper.interfaces
    - collective.taghelper.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.taghelper/
  license: GNU General Public License (GPL)
  summary: 'The act of tagging content is tedious and humans will often fail to do it. Taghelper examines the content and extracts the keywords that are most relevant.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
