package:
  name: humbug-forensics
  version: "0.0.0"

source:
  fn: humbug-forensics-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/humbug-forensics/humbug-forensics-0.0.0.tar.gz
  md5: e8228ee54a527b223bd96ff612e9b077
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - humbug-forensics = humbug-forensics:main
    #
    # Would create an entry point called humbug-forensics that calls humbug-forensics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - python-gflags
    - pysox
    - python-dateutil

  run:
    - python
    - requests
    - python-gflags
    - pysox
    - python-dateutil

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamdeprince/humbug_forensics
  license: UNKNOWN
  summary: 'Simple utility to fake "hum forensics"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
