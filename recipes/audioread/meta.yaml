package:
  name: audioread
  version: "1.2.1"

source:
  fn: audioread-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/audioread/audioread-1.2.1.tar.gz
  md5: 01a80357f38dbd9bf8d7403802df89ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audioread = audioread:main
    #
    # Would create an entry point called audioread that calls audioread.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - audioread

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sampsyo/audioread
  license: MIT
  summary: 'multi-library, cross-platform audio decoding'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
