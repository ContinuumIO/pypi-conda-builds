package:
  name: js.jquery_elastic
  version: "1.6.11"

source:
  fn: js.jquery_elastic-1.6.11.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_elastic/js.jquery_elastic-1.6.11.tar.gz
  md5: ad3d0d88f9b211c5378e7efc0d1aecea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_elastic = js.jquery_elastic:main
    #
    # Would create an entry point called js.jquery_elastic that calls js.jquery_elastic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_elastic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jQuery Elastic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
