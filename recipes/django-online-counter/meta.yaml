package:
  name: django-online-counter
  version: "1.0"

source:
  fn: django-online-counter-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-online-counter/django-online-counter-1.0.tar.gz
  md5: 75e93afe0f6cc9529d54896ad476e75b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-online-counter = django-online-counter:main
    #
    # Would create an entry point called django-online-counter that calls django-online-counter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - onlinecounter
    - onlinecounter.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.metehan.us/
  license: GNU Affero General Public License v3
  summary: 'Django online visitor counter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
