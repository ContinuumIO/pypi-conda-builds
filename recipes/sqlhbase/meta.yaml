package:
  name: sqlhbase
  version: "0.4"

source:
  fn: SqlHBase-0.4.tar.gz
  url: https://pypi.python.org/packages/source/S/SqlHBase/SqlHBase-0.4.tar.gz
  md5: 0bbd8b2ab80513b94ac160f92547396c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlhbase = sqlhbase:main
    #
    # Would create an entry point called sqlhbase that calls sqlhbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - happybase >=0.4
    - hive-thrift-py ==0.0.1

  run:
    - python
    - happybase >=0.4
    - hive-thrift-py ==0.0.1

test:
  # Python imports
  imports:
    - sqlhbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.rocket-internet.de
  license: GPL 3
  summary: 'MySQLDump to HBase, ETL scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
