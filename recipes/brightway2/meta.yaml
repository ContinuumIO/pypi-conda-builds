package:
  name: brightway2
  version: "0.14"

source:
  fn: brightway2-0.14.tar.gz
  url: https://pypi.python.org/packages/source/b/brightway2/brightway2-0.14.tar.gz
  md5: ba826fc6095271cea9737892cfe1a71d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brightway2 = brightway2:main
    #
    # Would create an entry point called brightway2 that calls brightway2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - numpy
    - scipy
    - nose
    - progressbar-ipython
    - voluptuous
    - fuzzywuzzy
    - flask
    - docopt
    - bw2calc >=0.14
    - bw2ui >=0.12.1
    - bw2analyzer >=0.5
    - bw2data >=0.16
    - requests >=1.1.0
    - stats_arrays >=0.2.4

  run:
    - python
    - lxml
    - numpy
    - scipy
    - nose
    - progressbar-ipython
    - voluptuous
    - fuzzywuzzy
    - flask
    - docopt
    - bw2calc >=0.14
    - bw2ui >=0.12.1
    - bw2analyzer >=0.5
    - bw2data >=0.16
    - requests >=1.1.0
    - stats_arrays >=0.2.4

test:
  # Python imports
  imports:
    - brightway2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cmutel/brightway2
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
