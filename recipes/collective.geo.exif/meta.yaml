package:
  name: collective.geo.exif
  version: "0.1"

source:
  fn: collective.geo.exif-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.geo.exif/collective.geo.exif-0.1.tar.gz
  md5: 4652d4f725beb4da521d5820bcb21b6a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geo.exif = collective.geo.exif:main
    #
    # Would create an entry point called collective.geo.exif that calls collective.geo.exif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.geo.settings
    - collective.geo.contentlocations

  run:
    - python
    - setuptools
    - setuptools
    - collective.geo.settings
    - collective.geo.contentlocations

test:
  # Python imports
  imports:
    - collective
    - collective.geo
    - collective.geo.exif
    - collective.geo.exif.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.geo.exif
  license: GNU General Public License (GPL)
  summary: 'Geoannotate Images with EXIF information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
