package:
  name: python-graph-dot
  version: "1.8.2"

source:
  fn: python-graph-dot-1.8.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-graph-dot/python-graph-dot-1.8.2.tar.gz
  md5: 0ecd251a9fd210a9135e1d1957689709
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-graph-dot = python-graph-dot:main
    #
    # Would create an entry point called python-graph-dot that calls python-graph-dot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-graph-core ==1.8.2
    - pydot

  run:
    - python
    - python-graph-core ==1.8.2
    - pydot

test:
  # Python imports
  imports:
    - pygraph
    - pygraph/readwrite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-graph/
  license: MIT License
  summary: 'DOT support for python-graph'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
