package:
  name: nimoy
  version: "0.5.1"

source:
  fn: nimoy-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nimoy/nimoy-0.5.1.tar.gz
  md5: 05db42335a1fd06f88904f9fd1878a85
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nimoy = nimoy:main
    #
    # Would create an entry point called nimoy that calls nimoy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mongomock
    - pymongo

  run:
    - python
    - mongomock
    - pymongo

test:
  # Python imports
  imports:
    - nimoy
    - nimoy.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/laco/nimoy/
  license: BSD License
  summary: 'Python3 Database Access Layer with Simple Functions and Dictionaries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
