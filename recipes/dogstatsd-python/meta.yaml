package:
  name: dogstatsd-python
  version: "0.5.6"

source:
  fn: dogstatsd-python-0.5.6.tar.gz
  url: https://pypi.python.org/packages/source/d/dogstatsd-python/dogstatsd-python-0.5.6.tar.gz
  md5: 36be46793dc33b55d47603d33243e399
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dogstatsd-python = dogstatsd-python:main
    #
    # Would create an entry point called dogstatsd-python that calls dogstatsd-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.datadoghq.com
  license: BSD
  summary: "Python bindings to Datadog's API and a user-facing command line tool."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
