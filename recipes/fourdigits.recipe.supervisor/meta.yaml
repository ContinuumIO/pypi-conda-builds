package:
  name: fourdigits.recipe.supervisor
  version: "0.18dev"

source:
  fn: fourdigits.recipe.supervisor-0.18dev.tar.gz
  url: https://pypi.python.org/packages/source/f/fourdigits.recipe.supervisor/fourdigits.recipe.supervisor-0.18dev.tar.gz
  md5: f0f9e1ed7e82a7eb7dacc21cf59c0dcb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fourdigits.recipe.supervisor = fourdigits.recipe.supervisor:main
    #
    # Would create an entry point called fourdigits.recipe.supervisor that calls fourdigits.recipe.supervisor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - fourdigits
    - fourdigits.recipe
    - fourdigits.recipe.supervisor
    - fourdigits.recipe.supervisor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/fourdigits.recipe.supervisor
  license: Zope Public License
  summary: 'A buildout recipe to install supervisor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
