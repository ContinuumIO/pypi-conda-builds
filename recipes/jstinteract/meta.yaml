package:
  name: jstinteract
  version: "0.01"

source:
  fn: JSTInteract-0.01.tar.gz
  url: https://pypi.python.org/packages/source/J/JSTInteract/JSTInteract-0.01.tar.gz
  md5: 7de88544f267d397155078dc79321e8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jstinteract = jstinteract:main
    #
    # Would create an entry point called jstinteract that calls jstinteract.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - JSTInteract
    - jstinteract

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svicario/PhyloHWS
  license: MIT License
  summary: 'JST Client for running phylogenetic web services from IPython Notebook'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
