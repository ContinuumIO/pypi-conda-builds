package:
  name: dj-email-url
  version: "0.0.4"

source:
  fn: dj-email-url-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dj-email-url/dj-email-url-0.0.4.tar.gz
  md5: a64628e885cb3bd80e395c52c56d701c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj-email-url = dj-email-url:main
    #
    # Would create an entry point called dj-email-url that calls dj-email-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/migonzalvar/dj-email-url
  license: BSD License
  summary: 'Use an URL to configure email backend settings in your Django Application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
