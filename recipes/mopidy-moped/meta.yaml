package:
  name: mopidy-moped
  version: "0.5.0"

source:
  fn: Mopidy-Moped-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Moped/Mopidy-Moped-0.5.0.tar.gz
  md5: dfbf67b13a99dff82e32f53991b84f2e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-moped = mopidy-moped:main
    #
    # Would create an entry point called mopidy-moped that calls mopidy-moped.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0.0

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0.0

test:
  # Python imports
  imports:
    - mopidy_moped

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martijnboland/moped
  license: MIT License
  summary: u'Responsive Web client for Mopidy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
