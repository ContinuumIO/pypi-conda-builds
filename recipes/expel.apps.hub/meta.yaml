package:
  name: expel.apps.hub
  version: "0.1"

source:
  fn: expel.apps.hub-0.1.zip
  url: https://pypi.python.org/packages/source/e/expel.apps.hub/expel.apps.hub-0.1.zip
  md5: af5ac10bee829e828e2d58e6e7fe49b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expel.apps.hub = expel.apps.hub:main
    #
    # Would create an entry point called expel.apps.hub that calls expel.apps.hub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - expel.lib
    - expel.message

  run:
    - python
    - expel.lib
    - expel.message

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sffjunkie.co.uk/python-expel.html#hub
  license: Apache-2.0
  summary: 'xPL Hub'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
