package:
  name: js.bootstrap_image_gallery
  version: "3.1.1-1"

source:
  fn: js.bootstrap_image_gallery-3.1.1-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.bootstrap_image_gallery/js.bootstrap_image_gallery-3.1.1-1.tar.gz
  md5: 789cfad24d6dbb89d930ef26bfecbcb0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.bootstrap_image_gallery = js.bootstrap_image_gallery:main
    #
    # Would create an entry point called js.bootstrap_image_gallery that calls js.bootstrap_image_gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - js.bootstrap
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - js.bootstrap
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.bootstrap_image_gallery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/disko/js.bootstrap_image_gallery
  license: BSD
  summary: 'Fanstatic packaging of Bootstrap Image Gallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
