package:
  name: sevencow
  version: "0.2.0"

source:
  fn: sevencow-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sevencow/sevencow-0.2.0.tar.gz
  md5: 0d7a51b24ba338cbbe714821622e37d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sevencow = sevencow:main
    #
    # Would create an entry point called sevencow that calls sevencow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - sevencow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yueyoum/seven-cow
  license: BSD License
  summary: 'Yet another qiniu cloud storage Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
