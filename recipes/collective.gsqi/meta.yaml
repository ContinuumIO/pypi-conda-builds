package:
  name: collective.gsqi
  version: "0.3"

source:
  fn: collective.gsqi-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.gsqi/collective.gsqi-0.3.tar.gz
  md5: f127f6bd9a4caff10320370bd75125a4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.gsqi = collective.gsqi:main
    #
    # Would create an entry point called collective.gsqi that calls collective.gsqi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - collective
    - collective.gsqi
    - collective.gsqi.content
    - collective.gsqi.group
    - collective.gsqi.gsml
    - collective.gsqi.refs
    - collective.gsqi.workflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.gsqi
  license: GPL
  summary: 'Various GenericSetup handlers and CMFQuickInstaller enhancements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
