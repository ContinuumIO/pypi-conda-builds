package:
  name: attribute_wrapper
  version: "0.2.0"

source:
  fn: attribute_wrapper-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/attribute_wrapper/attribute_wrapper-0.2.0.tar.gz
  md5: 6fbd164c0b8f8e1039eab2dff29d95bc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - attribute_wrapper = attribute_wrapper:main
    #
    # Would create an entry point called attribute_wrapper that calls attribute_wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - setuptools
    - requests

test:
  # Python imports
  imports:
    - attribute_wrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Bystroushaak/attribute_wrapper
  license: MIT License
  summary: 'Class wrapper, which maps attribute calls to HTTP API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
