package:
  name: htmlentity2ascii
  version: "1.0"

source:
  fn: htmlentity2ascii-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/htmlentity2ascii/htmlentity2ascii-1.0.tar.gz
  md5: 149e0342029c632ad61b089efaa38f69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmlentity2ascii = htmlentity2ascii:main
    #
    # Would create an entry point called htmlentity2ascii that calls htmlentity2ascii.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - htmlentity2ascii

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stackoverflow.com/questions/1197981/convert-html-entities-to-ascii-in-python/1582036#1582036
  license: GNU General Public License (GPL)
  summary: 'Python module to convert html entities to ascii'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
