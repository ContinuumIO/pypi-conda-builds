package:
  name: data-importer
  version: "2.2.1"

source:
  fn: data-importer-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/data-importer/data-importer-2.2.1.tar.gz
  md5: fa50b3d3310ddb89ccab92be41617f43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - data-importer = data-importer:main
    #
    # Would create an entry point called data-importer that calls data-importer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - openpyxl >=2.1.4
    - xlrd >=0.9.3

  run:
    - python
    - django >=1.4
    - openpyxl >=2.1.4
    - xlrd >=0.9.3

test:
  # Python imports
  imports:
    - data_importer
    - data_importer.core
    - data_importer.importers
    - data_importer.migrations
    - data_importer.readers
    - data_importer.writers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/valdergallo/data-importer
  license: BSD
  summary: 'Simple library to easily import data with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
