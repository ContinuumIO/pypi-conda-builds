package:
  name: p4a.ploneimage
  version: "0.2"

source:
  fn: p4a.ploneimage-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/p4a.ploneimage/p4a.ploneimage-0.2.tar.gz
  md5: 29e7922700688bd555e10efb7c10c8bf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.ploneimage = p4a.ploneimage:main
    #
    # Would create an entry point called p4a.ploneimage that calls p4a.ploneimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - p4a.common
    - p4a.image

  run:
    - python
    - setuptools
    - p4a.common
    - p4a.image

test:
  # Python imports
  imports:
    - p4a
    - p4a.ploneimage
    - p4a.ploneimage.atphoto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.example
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
