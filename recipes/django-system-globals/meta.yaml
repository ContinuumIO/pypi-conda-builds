package:
  name: django-system-globals
  version: "0.0.3"

source:
  fn: django-system-globals-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-system-globals/django-system-globals-0.0.3.tar.gz
  md5: 6eff64e9bfa3be7dbbd1a608f2f969e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-system-globals = django-system-globals:main
    #
    # Would create an entry point called django-system-globals that calls django-system-globals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - system_globals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andres-torres-marroquin/django-system-globals
  license: UNKNOWN
  summary: 'A Django App to manage any system globals from a DB table.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
