package:
  name: django-infinite-scroll-pagination
  version: "0.1.3"

source:
  fn: django-infinite-scroll-pagination-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-infinite-scroll-pagination/django-infinite-scroll-pagination-0.1.3.tar.gz
  md5: 7478868487ce69f2286e7fecc0fe0357
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-infinite-scroll-pagination = django-infinite-scroll-pagination:main
    #
    # Would create an entry point called django-infinite-scroll-pagination that calls django-infinite-scroll-pagination.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - infinite_scroll_pagination

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nitely/django-infinite-scroll-pagination
  license: MIT License
  summary: 'infinite-scroll-pagination is a Django app that implements the *seek method* for scalable pagination..'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
