package:
  name: django-model-history
  version: "0.1.2"

source:
  fn: django-model-history-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-history/django-model-history-0.1.2.tar.gz
  md5: 6f8d86376e721ce044175a6dc1a95e59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-history = django-model-history:main
    #
    # Would create an entry point called django-model-history that calls django-model-history.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - model_history

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goinnn/django-model-history
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Enable in any Model class an change history of all inserts, updates and deletes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
