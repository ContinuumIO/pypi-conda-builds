package:
  name: django-cronjobs
  version: "0.2.3"

source:
  fn: django-cronjobs-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cronjobs/django-cronjobs-0.2.3.tar.gz
  md5: 2458615410997e7fded0ca900f9961ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cronjobs = django-cronjobs:main
    #
    # Would create an entry point called django-cronjobs that calls django-cronjobs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cronjobs
    - cronjobs.management
    - cronjobs.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsocol/django-cronjobs
  license: BSD License
  summary: 'A simple Django app for running cron jobs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
