package:
  name: chatbot
  version: "1.5.2b"

source:
  fn: chatbot-1.5.2b.tar.gz
  url: https://pypi.python.org/packages/source/c/chatbot/chatbot-1.5.2b.tar.gz
  md5: dee4c3c4d59e45e169c5a172f892170b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chatbot = chatbot:main
    #
    # Would create an entry point called chatbot that calls chatbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chatbot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/chatbot/
  license: MIT License
  summary: 'Allows users on wikia wikis to create a chatbot for Special:Chat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
