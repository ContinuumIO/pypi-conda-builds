package:
  name: django-crystal-big
  version: "2011.20.10"

source:
  fn: django-crystal-big-2011.20.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crystal-big/django-crystal-big-2011.20.10.tar.gz
  md5: 2444ea09c253d90e7ea05a6c3dfd66ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crystal-big = django-crystal-big:main
    #
    # Would create an entry point called django-crystal-big that calls django-crystal-big.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django_crystal_big

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.everaldo.com/crystal/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: "Everaldo's Crystal icons (big sizes) bundled for direct consumption from Django applications"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
