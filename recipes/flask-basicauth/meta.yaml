package:
  name: flask-basicauth
  version: "0.2.0"

source:
  fn: Flask-BasicAuth-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-BasicAuth/Flask-BasicAuth-0.2.0.tar.gz
  md5: 6c7d204e06c5133f42e4171cfb2f42e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-basicauth = flask-basicauth:main
    #
    # Would create an entry point called flask-basicauth that calls flask-basicauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpvanhal/flask-basicauth
  license: BSD License
  summary: 'HTTP basic access authentication for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
