package:
  name: bambu-fileupload
  version: "2.2.1"

source:
  fn: bambu-fileupload-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-fileupload/bambu-fileupload-2.2.1.tar.gz
  md5: b7420d576d019b43b4e782c9fbd76874
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-fileupload = bambu-fileupload:main
    #
    # Would create an entry point called bambu-fileupload that calls bambu-fileupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4django-bower

  run:
    - python
    - django >=1.4django-bower

test:
  # Python imports
  imports:
    - bambu_fileupload
    - bambu_fileupload.migrations
    - bambu_fileupload.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-fileupload
  license: UNKNOWN
  summary: 'A wrapper around the jQuery.fileupload library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
