package:
  name: jsmapper
  version: "0.1.9"

source:
  fn: jsmapper-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/j/jsmapper/jsmapper-0.1.9.tar.gz
  md5: 0900d3222acdcde4106495dca90c793b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsmapper = jsmapper:main
    #
    # Would create an entry point called jsmapper that calls jsmapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema ==2.4.0

  run:
    - python
    - jsonschema ==2.4.0

test:
  # Python imports
  imports:
    - jsmapper
    - jsmapper.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yosida95/python-jsmapper
  license: MIT License
  summary: 'A Object - JSON Schema Mapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
