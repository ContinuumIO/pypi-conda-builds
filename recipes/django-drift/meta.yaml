package:
  name: django-drift
  version: "0.1.2"

source:
  fn: django-drift-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-drift/django-drift-0.1.2.tar.gz
  md5: fbf01511757ca3e7d354b1c9b1af6b98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-drift = django-drift:main
    #
    # Would create an entry point called django-drift that calls django-drift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=3.1.6

  run:
    - python
    - celery >=3.1.6

test:
  # Python imports
  imports:
    - drift
    - drift.management
    - drift.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/monokrome/django-drift
  license: UNKNOWN
  summary: 'Takes files and turns them into recrods in models. HOORAY!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
