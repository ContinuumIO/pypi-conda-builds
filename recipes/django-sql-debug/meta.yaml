package:
  name: django-sql-debug
  version: "0.0.4"

source:
  fn: django-sql-debug-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sql-debug/django-sql-debug-0.0.4.tar.gz
  md5: af5f5cd657fc58250bf75083b49c5f0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sql-debug = django-sql-debug:main
    #
    # Would create an entry point called django-sql-debug that calls django-sql-debug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-debug-toolbar >=1.2.1

  run:
    - python
    - django-debug-toolbar >=1.2.1

test:
  # Python imports
  imports:
    - django_sql_debug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/socketubs/django-sql-debug
  license: MIT License
  summary: u'Django Sql Debug'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
