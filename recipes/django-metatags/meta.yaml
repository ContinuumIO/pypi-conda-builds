package:
  name: django-metatags
  version: "0.1"

source:
  fn: django-metatags-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-metatags/django-metatags-0.1.tar.gz
  md5: 77f8357fafb07f35d7a1c6f9a8a4c477
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-metatags = django-metatags:main
    #
    # Would create an entry point called django-metatags that calls django-metatags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - metatags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cpharmston/django-metatags
  license: GNU General Public License (GPL) or MIT License
  summary: 'A simple, pluggable app for attaching meta tags to objects with their own views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
