package:
  name: cloudmade
  version: "0.2"

source:
  fn: CloudMade-0.2.tar.gz
  url: https://pypi.python.org/packages/source/C/CloudMade/CloudMade-0.2.tar.gz
  md5: fad2c5d2c3d73cca36b3162afa234fa1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudmade = cloudmade:main
    #
    # Would create an entry point called cloudmade that calls cloudmade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cloudmade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://developers.cloudmade.com/projects/show/python-lib
  license: LGPL
  summary: "CloudMade's online services client"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
