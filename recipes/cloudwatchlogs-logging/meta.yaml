package:
  name: cloudwatchlogs-logging
  version: "0.0.1"

source:
  fn: cloudwatchlogs-logging-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudwatchlogs-logging/cloudwatchlogs-logging-0.0.1.tar.gz
  md5: c0fbb2c349f84066c1fac53945a9d3ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudwatchlogs-logging = cloudwatchlogs-logging:main
    #
    # Would create an entry point called cloudwatchlogs-logging that calls cloudwatchlogs-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - docopt

  run:
    - python
    - boto
    - docopt

test:
  # Python imports
  imports:
    - cloudwatchlogs_logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ImmobilienScout24/python-cloudwatchlogs-logging
  license: Apache License 2.0
  summary: 'Handler for easy logging to AWS CloudWatchLogs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
