package:
  name: django-apollo
  version: "0.2.1"

source:
  fn: django-apollo-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-apollo/django-apollo-0.2.1.tar.gz
  md5: d792b06a3f594db3020b43c61278f141
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-apollo = django-apollo:main
    #
    # Would create an entry point called django-apollo that calls django-apollo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-ckeditor >=4.4.4
    - django-widget-tweaks >=1.3

  run:
    - python
    - django-ckeditor >=4.4.4
    - django-widget-tweaks >=1.3

test:
  # Python imports
  imports:
    - apollo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dbroudy/django-apollo/
  license: MIT License
  summary: 'A simple landing page Django app with quick setup, minimal dependencies, but still managable from Django admin pages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
