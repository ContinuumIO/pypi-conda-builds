package:
  name: django-redirect-plus
  version: "0.0.1"

source:
  fn: django-redirect-plus-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redirect-plus/django-redirect-plus-0.0.1.tar.gz
  md5: 5d42b12cf58f0955e919cda5d3a8895d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redirect-plus = django-redirect-plus:main
    #
    # Would create an entry point called django-redirect-plus that calls django-redirect-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - redirect_plus
    - redirect_plus.management
    - redirect_plus.management.commands
    - redirect_plus.templatetags
    - redirect_plus.tests
    - redirect_plus.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arteria/django-redirect-plus
  license: The MIT License
  summary: "Django's built-in redirect app with some extras.\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
