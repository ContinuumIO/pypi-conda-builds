package:
  name: jsonwidget
  version: "0.1.7"

source:
  fn: jsonwidget-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonwidget/jsonwidget-0.1.7.tar.gz
  md5: d30dc07b9eca986740a06698dd0c95db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonwidget = jsonwidget:main
    #
    # Would create an entry point called jsonwidget that calls jsonwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simpleparse

  run:
    - python
    - simpleparse

test:
  # Python imports
  imports:
    - jsonwidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://robla.net/jsonwidget
  license: BSD License
  summary: 'jsonwidget is a general-purpose JSON validation and manipulation, and form-building library for terminal windows and web pages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
