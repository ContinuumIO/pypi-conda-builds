package:
  name: js-host
  version: "0.1.3"

source:
  fn: js-host-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/j/js-host/js-host-0.1.3.tar.gz
  md5: 68a6cb163c7a998f60901df01f48a405
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js-host = js-host:main
    #
    # Would create an entry point called js-host that calls js-host.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.5.0
    - optional-django ==0.3.0

  run:
    - python
    - requests >=2.5.0
    - optional-django ==0.3.0

test:
  # Python imports
  imports:
    - js_host

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markfinger/python-js-host
  license: UNKNOWN
  summary: 'Python bindings to a performant JavaScript environment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
