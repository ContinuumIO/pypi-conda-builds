package:
  name: django-misery
  version: "0.0.3"

source:
  fn: django-misery-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-misery/django-misery-0.0.3.tar.gz
  md5: df544f2463e530036648cbfc334e4758
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-misery = django-misery:main
    #
    # Would create an entry point called django-misery that calls django-misery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_misery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fschwebel/django-misery/
  license: MIT License
  summary: 'A simple ban system for Django, that does nasty stuff to trolls wandering on your website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
