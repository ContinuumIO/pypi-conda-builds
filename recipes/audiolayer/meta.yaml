package:
  name: audiolayer
  version: "0.1.1"

source:
  fn: audiolayer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/audiolayer/audiolayer-0.1.1.tar.gz
  md5: 4933985ccd398e8fc77fb04aa3d2af8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audiolayer = audiolayer:main
    #
    # Would create an entry point called audiolayer that calls audiolayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - audiolayer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RemcoHaszing/python-audiolayer
  license: BSD 3-Clause License
  summary: 'This module provides a wrapper around the libav C library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
