package:
  name: pyrise
  version: "0.5.3"

source:
  fn: pyrise-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pyrise/pyrise-0.5.3.tar.gz
  md5: 2e43e80cb52643f39b3714e200597cf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyrise = pyrise:main
    #
    # Would create an entry point called pyrise that calls pyrise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - requests

  run:
    - python
    - httplib2
    - requests

test:
  # Python imports
  imports:
    - pyrise

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/feedmagnet/pyrise
  license: MIT License
  summary: 'Python wrapper for 37Signals Highrise'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
