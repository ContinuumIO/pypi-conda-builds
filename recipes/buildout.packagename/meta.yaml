package:
  name: buildout.packagename
  version: "1.0"

source:
  fn: buildout.packagename-1.0.zip
  url: https://pypi.python.org/packages/source/b/buildout.packagename/buildout.packagename-1.0.zip
  md5: 307f0fdd8abbe2690ec5c801d7eaafd1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.packagename = buildout.packagename:main
    #
    # Would create an entry point called buildout.packagename that calls buildout.packagename.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - buildout
    - buildout.packagename

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/witsch/buildout.packagename
  license: BSD
  summary: 'A `zc.buildout` extension to extract the package name from an adjacent `setup.py`.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
