package:
  name: django-filebrowser-django13
  version: "3.0"

source:
  fn: django-filebrowser-django13-3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filebrowser-django13/django-filebrowser-django13-3.0.tar.gz
  md5: 5c03523e147a2860653ee7e2620b1d61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filebrowser-django13 = django-filebrowser-django13:main
    #
    # Would create an entry point called django-filebrowser-django13 that calls django-filebrowser-django13.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - filebrowser
    - filebrowser.management
    - filebrowser.management.commands
    - filebrowser.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/msaelices/django-filebrowser-no-grappelli-for-django13
  license: BSD License
  summary: 'Media-Management with the Django Admin-Interface. Package for using without django-grapelli in Django 1.3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
