package:
  name: django-compat
  version: "1.0.6"

source:
  fn: django-compat-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-compat/django-compat-1.0.6.tar.gz
  md5: 831c10d7088f1b099eb420a98370dd59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-compat = django-compat:main
    #
    # Would create an entry point called django-compat that calls django-compat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.9
    - six

  run:
    - python
    - django >=1.4,<1.9
    - six

test:
  # Python imports
  imports:
    - compat
    - compat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'For- and backwards compatibility layer for Django 1.4 to 1.8'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
