package:
  name: couchbasekit
  version: "0.2.2"

source:
  fn: couchbasekit-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/couchbasekit/couchbasekit-0.2.2.tar.gz
  md5: 36ab609762cb00e7bd11d7d4e1bbf8bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchbasekit = couchbasekit:main
    #
    # Would create an entry point called couchbasekit that calls couchbasekit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - couchbase
    - jsonpickle
    - python-dateutil

  run:
    - python
    - couchbase
    - jsonpickle
    - python-dateutil

test:
  # Python imports
  imports:
    - couchbasekit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kirpit/couchbasekit
  license: LICENSE.txt
  summary: 'A wrapper around CouchBase Python driver for document validation and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
