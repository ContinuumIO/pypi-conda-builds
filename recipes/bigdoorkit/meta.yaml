package:
  name: bigdoorkit
  version: "0.1.4"

source:
  fn: bigdoorkit-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bigdoorkit/bigdoorkit-0.1.4.tar.gz
  md5: 35493bfffeb27e5aa38072aa4ad9982f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigdoorkit = bigdoorkit:main
    #
    # Would create an entry point called bigdoorkit that calls bigdoorkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - restkit

  run:
    - python
    - restkit

test:
  # Python imports
  imports:
    - bigdoorkit
    - bigdoorkit.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/splee/bigdoorkit
  license: MIT License
  summary: 'Client library for the BigDoor API, based on RESTkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
