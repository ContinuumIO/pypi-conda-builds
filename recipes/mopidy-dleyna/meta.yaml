package:
  name: mopidy-dleyna
  version: "0.3.1"

source:
  fn: Mopidy-dLeyna-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-dLeyna/Mopidy-dLeyna-0.3.1.tar.gz
  md5: bcc9f865a1d0ccbfd2f4055b921820b7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-dleyna = mopidy-dleyna:main
    #
    # Would create an entry point called mopidy-dleyna that calls mopidy-dleyna.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - uritools >=0.12

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - uritools >=0.12

test:
  # Python imports
  imports:
    - mopidy_dleyna

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tkem/mopidy-dleyna
  license: Apache Software License
  summary: u'Mopidy extension for playing music from Digital Media Servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
