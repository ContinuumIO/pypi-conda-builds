package:
  name: flask-multi-session
  version: "0.1.7"

source:
  fn: Flask-Multi-Session-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Multi-Session/Flask-Multi-Session-0.1.7.tar.gz
  md5: bd295d9ec580d145b6cbcb08d162b73f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-multi-session = flask-multi-session:main
    #
    # Would create an entry point called flask-multi-session that calls flask-multi-session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - python-dateutil >=2.4.0
    - redis >=2.10.3

  run:
    - python
    - flask >=0.10.1
    - python-dateutil >=2.4.0
    - redis >=2.10.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/atkin1450/flask-multi-session
  license: MIT License
  summary: 'Redis multi-sessions for Flask apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
