package:
  name: django-simpleimages
  version: "1.2.0"

source:
  fn: django-simpleimages-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simpleimages/django-simpleimages-1.2.0.tar.gz
  md5: 0933bb4fd5f74eabd10341eb4e513fa2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simpleimages = django-simpleimages:main
    #
    # Would create an entry point called django-simpleimages that calls django-simpleimages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5,<=1.8
    - six
    - pillow
    - clint

  run:
    - python
    - django >=1.5,<=1.8
    - six
    - pillow
    - clint

test:
  # Python imports
  imports:
    - simpleimages
    - simpleimages.management
    - simpleimages.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/saulshanabrook/django-simpleimages
  license: MIT License
  summary: 'Opinionated Django image transforms on models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
