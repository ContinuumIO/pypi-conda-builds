package:
  name: django-sortedm2m
  version: "0.9.5"

source:
  fn: django-sortedm2m-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sortedm2m/django-sortedm2m-0.9.5.tar.gz
  md5: 8a82b32e1b47a24884f3b79ab37564e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sortedm2m = django-sortedm2m:main
    #
    # Would create an entry point called django-sortedm2m that calls django-sortedm2m.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sortedm2m

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gregmuellegger/django-sortedm2m
  license: BSD License
  summary: "Drop-in replacement for django's many to many field with sorted relations."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
