package:
  name: django-mssql
  version: "1.7"

source:
  fn: django-mssql-1.7.zip
  url: https://pypi.python.org/packages/source/d/django-mssql/django-mssql-1.7.zip
  md5: 91c3292d60feb556704fce8c842b19ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mssql = django-mssql:main
    #
    # Would create an entry point called django-mssql that calls django-mssql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlserver_ado
    - sqlserver_ado.models
    - sqlserver_ado.sql_app
    - sqlserver_ado.sql_app.management
    - sqlserver_ado.sql_app.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/Manfre/django-mssql
  license: MIT
  summary: 'Django backend database support for MS SQL Server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
