package:
  name: gaecookie
  version: "0.7"

source:
  fn: gaecookie-0.7.tar.gz
  url: https://pypi.python.org/packages/source/g/gaecookie/gaecookie-0.7.tar.gz
  md5: f8ae327fb8523c81ada9a6c5fe5be437
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaecookie = gaecookie:main
    #
    # Would create an entry point called gaecookie that calls gaecookie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gaebusiness >=4.0
    - gaegraph >=2.3
    - tekton >=3.1.1

  run:
    - python
    - gaebusiness >=4.0
    - gaegraph >=2.3
    - tekton >=3.1.1

test:
  # Python imports
  imports:
    - gaecookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renzon/gaecookie
  license: BSD License
  summary: 'A library for signing string and validating cookies. cd ..'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
