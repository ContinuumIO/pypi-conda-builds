package:
  name: plone.synchronize
  version: "1.0.1"

source:
  fn: plone.synchronize-1.0.1.zip
  url: https://pypi.python.org/packages/source/p/plone.synchronize/plone.synchronize-1.0.1.zip
  md5: d25e86ace8daa0816861296c3288c4fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.synchronize = plone.synchronize:main
    #
    # Would create an entry point called plone.synchronize that calls plone.synchronize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.synchronize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.synchronize
  license: BSD License
  summary: 'Simple decorators to support synchronized methods'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
