package:
  name: soundcloud-cli
  version: "0.2.5"

source:
  fn: soundcloud-cli-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/s/soundcloud-cli/soundcloud-cli-0.2.5.tar.gz
  md5: aed8fd5f89f5f68c30c69820aae46eea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - soundcloud-cli = soundcloud-cli:main
    #
    # Would create an entry point called soundcloud-cli that calls soundcloud-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - soundcloud

  run:
    - python
    - soundcloud

test:
  # Python imports
  imports:
    - soundcloud_cli
    - soundcloud_cli.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zeekay/soundcloud-cli
  license: MIT License
  summary: 'Soundcloud command-line utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
