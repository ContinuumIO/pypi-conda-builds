package:
  name: aws-trade-in
  version: "0.1"

source:
  fn: aws-trade-in-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aws-trade-in/aws-trade-in-0.1.tar.gz
  md5: 64cc3f62d95f8e2d64d62d8d15e3b7f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aws-trade-in = aws-trade-in:main
    #
    # Would create an entry point called aws-trade-in that calls aws-trade-in.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottlenose
    - argparse

  run:
    - python
    - bottlenose
    - argparse

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dlecocq/aws-trade-in
  license: UNKNOWN
  summary: 'Amazon Trade-In Helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
