package:
  name: django-livesettings
  version: "1.4-14"

source:
  fn: django-livesettings-1.4-14.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livesettings/django-livesettings-1.4-14.tar.gz
  md5: be56db4e8464b93134d83906d039303d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livesettings = django-livesettings:main
    #
    # Would create an entry point called django-livesettings that calls django-livesettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - livesettings
    - livesettings.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/bkroeze/django-livesettings/
  license: BSD License
  summary: 'livesettings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
