package:
  name: django-devsafe
  version: "0.3.0"

source:
  fn: django-devsafe-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-devsafe/django-devsafe-0.3.0.tar.gz
  md5: 78a68c5c9f72cc6a93d54613f0ef0280
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-devsafe = django-devsafe:main
    #
    # Would create an entry point called django-devsafe that calls django-devsafe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - devsafe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Miserlou/django-devsafe
  license: Apache Software License
  summary: 'Safely work with a production database in an insecure environment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
