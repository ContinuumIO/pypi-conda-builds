package:
  name: bubbles
  version: "0.1"

source:
  fn: bubbles-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bubbles/bubbles-0.1.tar.gz
  md5: 8c934d1609c700d3180107871b10d6d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bubbles = bubbles:main
    #
    # Would create an entry point called bubbles that calls bubbles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bubbles
    - bubbles.backends
    - bubbles.backends.sql
    - bubbles.backends.text

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bubbles.databrewery.org
  license: MIT License
  summary: 'Virtual Data Object framework for data processing (ETL) and quality monitoring'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
