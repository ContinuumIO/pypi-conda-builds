package:
  name: tranchitella.recipe.testrunner
  version: "0.4"

source:
  fn: tranchitella.recipe.testrunner-0.4.tar.gz
  url: https://pypi.python.org/packages/source/t/tranchitella.recipe.testrunner/tranchitella.recipe.testrunner-0.4.tar.gz
  md5: 6bb9323bdb2cddb547a143e9b60b26d6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tranchitella.recipe.testrunner = tranchitella.recipe.testrunner:main
    #
    # Would create an entry point called tranchitella.recipe.testrunner that calls tranchitella.recipe.testrunner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - zope.testrunner

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - zope.testrunner

test:
  # Python imports
  imports:
    - tranchitella
    - tranchitella.recipe
    - tranchitella.recipe.testrunner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/tranchitella.recipe.testrunner
  license: GNU General Public License (GPL)
  summary: 'Buildout recipe to create a zope.testing test runner'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
