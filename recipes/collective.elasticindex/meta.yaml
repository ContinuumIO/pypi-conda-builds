package:
  name: collective.elasticindex
  version: "1.2.5"

source:
  fn: collective.elasticindex-1.2.5.zip
  url: https://pypi.python.org/packages/source/c/collective.elasticindex/collective.elasticindex-1.2.5.zip
  md5: 57257bd74999b7d2fbc8eee59302786d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.elasticindex = collective.elasticindex:main
    #
    # Would create an entry point called collective.elasticindex that calls collective.elasticindex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - acquisition
    - products.cmfcore
    - products.cmfplone
    - products.statusmessages
    - plone.app.controlpanel
    - plone.app.portlets
    - plone.portlets
    - pyes
    - setuptools
    - transaction
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.traversing

  run:
    - python
    - acquisition
    - products.cmfcore
    - products.cmfplone
    - products.statusmessages
    - plone.app.controlpanel
    - plone.app.portlets
    - plone.portlets
    - pyes
    - setuptools
    - transaction
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.traversing

test:
  # Python imports
  imports:
    - collective
    - collective.elasticindex
    - collective.elasticindex.browser
    - collective.elasticindex.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/infrae/collective.elasticindex
  license: BSD
  summary: 'Index Plone content in Elastic Search'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
