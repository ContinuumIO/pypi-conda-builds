package:
  name: django-ymaps-admin-widget
  version: "1.0.12"

source:
  fn: django-ymaps-admin-widget-1.0.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ymaps-admin-widget/django-ymaps-admin-widget-1.0.12.tar.gz
  md5: a10a375c0c16b20cd6bfe5207c4552db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ymaps-admin-widget = django-ymaps-admin-widget:main
    #
    # Would create an entry point called django-ymaps-admin-widget that calls django-ymaps-admin-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - yandexmaps_widget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DrMartiner/django-ymaps-admin-widget
  license: MIT License
  summary: 'Widget admin for Yandex Maps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
