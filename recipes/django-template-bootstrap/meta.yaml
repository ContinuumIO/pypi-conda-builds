package:
  name: django-template-bootstrap
  version: "0.2.15"

source:
  fn: django-template-bootstrap-0.2.15.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-bootstrap/django-template-bootstrap-0.2.15.tar.gz
  md5: 88da255997e6cc9daa741a4e7df0007b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-bootstrap = django-template-bootstrap:main
    #
    # Would create an entry point called django-template-bootstrap that calls django-template-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - template_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adieu/django-template-bootstrap
  license: BSD
  summary: "A django template based on twitter's bootstrap project."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
