package:
  name: django-qunit
  version: "0.1.1"

source:
  fn: django-qunit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qunit/django-qunit-0.1.1.tar.gz
  md5: b90714d6b3bf7d76e0c07d93cab2bb7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qunit = django-qunit:main
    #
    # Would create an entry point called django-qunit that calls django-qunit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_qunit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codysoyland/django-qunit/
  license: BSD License
  summary: 'QUnit Javascript testing integration for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
