package:
  name: cubicweb-squareui
  version: "1.0.1"

source:
  fn: cubicweb-squareui-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-squareui/cubicweb-squareui-1.0.1.tar.gz
  md5: 1e685c82d16cc255714a348944cfb813
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-squareui = cubicweb-squareui:main
    #
    # Would create an entry point called cubicweb-squareui that calls cubicweb-squareui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0
    - cubicweb-bootstrap >=1.0.0

  run:
    - python
    - cubicweb >=3.19.0
    - cubicweb-bootstrap >=1.0.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-squareui
  license: LGPL
  summary: 'data-centric user interface for cubicweb based on bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
