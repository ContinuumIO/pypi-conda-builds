package:
  name: cuckooapi
  version: "1.0.0"

source:
  fn: cuckooapi-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cuckooapi/cuckooapi-1.0.0.tar.gz
  md5: 6f22b3c67fa312c2041a6ca250feb709
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cuckooapi = cuckooapi:main
    #
    # Would create an entry point called cuckooapi that calls cuckooapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - drest ==0.9.12

  run:
    - python
    - drest ==0.9.12

test:
  # Python imports
  imports:
    - cuckooapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nilp0inter/cuckooapi
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Cuckoo REST API client for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
