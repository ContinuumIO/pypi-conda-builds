package:
  name: chembl_webresource_client
  version: "0.8.6"

source:
  fn: chembl_webresource_client-0.8.6.tar.gz
  url: https://pypi.python.org/packages/source/c/chembl_webresource_client/chembl_webresource_client-0.8.6.tar.gz
  md5: d9216729e045ca5a57eb7c2ae407831b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chembl_webresource_client = chembl_webresource_client:main
    #
    # Would create an entry point called chembl_webresource_client that calls chembl_webresource_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - urllib3
    - requests ==2.5.3
    - requests-cache >=0.4.7
    - grequests ==0.2.0
    - easydict

  run:
    - python
    - six
    - urllib3
    - requests ==2.5.3
    - requests-cache >=0.4.7
    - grequests ==0.2.0
    - easydict

test:
  # Python imports
  imports:
    - chembl_webresource_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.ebi.ac.uk/chembldb/index.php/ws
  license: Apache Software License
  summary: 'Python client fot accessing ChEMBL webservices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
