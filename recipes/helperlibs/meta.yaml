package:
  name: helperlibs
  version: "0.1.5"

source:
  fn: helperlibs-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/h/helperlibs/helperlibs-0.1.5.tar.gz
  md5: 9be095bf2c687819de0a0e9b70a28fdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helperlibs = helperlibs:main
    #
    # Would create an entry point called helperlibs that calls helperlibs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - biopython >=1.62

  run:
    - python
    - biopython >=1.62

test:
  # Python imports
  imports:
    - helperlibs
    - helperlibs.bio
    - helperlibs.tests
    - helperlibs.tests.bio
    - helperlibs.tests.wrappers
    - helperlibs.wrappers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kblin/bioinf-helperlibs/wiki
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A collection of bioinformatics-related helper functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
