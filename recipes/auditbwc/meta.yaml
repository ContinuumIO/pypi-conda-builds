package:
  name: auditbwc
  version: "0.1.1"

source:
  fn: AuditBWC-0.1.1.zip
  url: https://pypi.python.org/packages/source/A/AuditBWC/AuditBWC-0.1.1.zip
  md5: 70062c40a8dfde38a10fda9056d16a20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auditbwc = auditbwc:main
    #
    # Would create an entry point called auditbwc that calls auditbwc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemybwc
    - webgrid
    - blazeweb

  run:
    - python
    - sqlalchemybwc
    - webgrid
    - blazeweb

test:
  # Python imports
  imports:
    - auditbwc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/blazelibs/auditbwc/
  license: BSD License
  summary: 'A record history and comparison component for the BlazeWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
