package:
  name: indra.base
  version: "1.0"

source:
  fn: indra.base-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/indra.base/indra.base-1.0.tar.gz
  md5: c1b0f67e2ca2e996221bfb1282a9c900
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indra.base = indra.base:main
    #
    # Would create an entry point called indra.base that calls indra.base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - indra.util

  run:
    - python
    - setuptools
    - setuptools
    - indra.util

test:
  # Python imports
  imports:
    - indra
    - indra.base
    - indra.base.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.secondlife.com/wiki
  license: MIT License
  summary: 'Second Life/OGP support library for handling uuid and llsd'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
