package:
  name: first_pypi_practice
  version: "1.0.1"

source:
  fn: first_pypi_practice-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/first_pypi_practice/first_pypi_practice-1.0.1.tar.gz
  md5: 5b578642ce1a9bb3101600ed492b0c72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - first_pypi_practice = first_pypi_practice:main
    #
    # Would create an entry point called first_pypi_practice that calls first_pypi_practice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dogs
    - first_pypi_practice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chardet.feedparser.org/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Universal encoding detector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
