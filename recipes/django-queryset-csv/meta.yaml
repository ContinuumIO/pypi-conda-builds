package:
  name: django-queryset-csv
  version: "0.3.0"

source:
  fn: django-queryset-csv-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-queryset-csv/django-queryset-csv-0.3.0.tar.gz
  md5: 4e2b794a25e4ac4c2b4eaf5170b2319f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-queryset-csv = django-queryset-csv:main
    #
    # Would create an entry point called django-queryset-csv that calls django-queryset-csv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - djqscsv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/azavea/django-queryset-csv
  license: GNU General Public License (GPL)
  summary: 'A simple python module for writing querysets to csv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
