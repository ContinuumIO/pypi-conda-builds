package:
  name: caesium
  version: "0.3.1"

source:
  fn: Caesium-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/C/Caesium/Caesium-0.3.1.tar.gz
  md5: 376781232a92688631b302756f634c39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caesium = caesium:main
    #
    # Would create an entry point called caesium that calls caesium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.1
    - motor >=0.3
    - jsonschema >=2.3.0

  run:
    - python
    - tornado >=3.1
    - motor >=0.3
    - jsonschema >=2.3.0

test:
  # Python imports
  imports:
    - caesium

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://urbn.github.io/Caesium
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Caesium document revision and RESTful tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
