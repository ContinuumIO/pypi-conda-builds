package:
  name: django-dumper
  version: "0.2.5"

source:
  fn: django-dumper-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dumper/django-dumper-0.2.5.tar.gz
  md5: 766723d814391decce3ef84edd81395a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dumper = django-dumper:main
    #
    # Would create an entry point called django-dumper that calls django-dumper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.7
    - six

  run:
    - python
    - django >=1.4,<1.7
    - six

test:
  # Python imports
  imports:
    - dumper
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/saulshanabrook/django-dumper
  license: MIT License
  summary: 'Django URL cache invalidation from model saves'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
