package:
  name: collective.setdefaulteditor
  version: "1.5"

source:
  fn: collective.setdefaulteditor-1.5.zip
  url: https://pypi.python.org/packages/source/c/collective.setdefaulteditor/collective.setdefaulteditor-1.5.zip
  md5: 7636d4a9ac4ed80a1af7b215ce329f1f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.setdefaulteditor = collective.setdefaulteditor:main
    #
    # Would create an entry point called collective.setdefaulteditor that calls collective.setdefaulteditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.setdefaulteditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/collective.setdefaulteditor
  license: GPL
  summary: 'Set the default editor in Plone for all existing members.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
