package:
  name: django-simple-social
  version: "0.1.10"

source:
  fn: django-simple-social-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-social/django-simple-social-0.1.10.tar.gz
  md5: b002cc85ee04415c601e1803b117f1a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-social = django-simple-social:main
    #
    # Would create an entry point called django-simple-social that calls django-simple-social.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-social-user
    - facebook-sdk
    - linkedin-api-json-client
    - twython

  run:
    - python
    - django >=1.3
    - django-social-user
    - facebook-sdk
    - linkedin-api-json-client
    - twython

test:
  # Python imports
  imports:
    - django_simple_social

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattsnider/Django-Simple-Social
  license: MIT License
  summary: 'A generic system for interacting with remote APIs that need to create Django socials.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
