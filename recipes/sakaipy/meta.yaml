package:
  name: sakaipy
  version: "2.0.0"

source:
  fn: SakaiPy-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SakaiPy/SakaiPy-2.0.0.tar.gz
  md5: 7657bdd9016863f2e873c5607dcd327f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sakaipy = sakaipy:main
    #
    # Would create an entry point called sakaipy that calls sakaipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - SakaiPy
    - SakaiPy.SakaiTools
    - sakaipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/willkara/SakaiPy
  license: MIT License
  summary: "Python interface to the Sakai RESTful API's"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
