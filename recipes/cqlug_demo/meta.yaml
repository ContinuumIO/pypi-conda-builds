package:
  name: cqlug_demo
  version: "0.1.1"

source:
  fn: cqlug_demo-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cqlug_demo/cqlug_demo-0.1.1.tar.gz
  md5: dd1abba17172da768486cdfa376d3e49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cqlug_demo = cqlug_demo:main
    #
    # Would create an entry point called cqlug_demo that calls cqlug_demo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cqlug_demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cqlug_demo/
  license: BSD License
  summary: 'Demo package for the cqlug demo, August 2012'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
