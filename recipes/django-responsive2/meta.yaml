package:
  name: django-responsive2
  version: "0.1.1"

source:
  fn: django-responsive2-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-responsive2/django-responsive2-0.1.1.tar.gz
  md5: 4fbc9cce0d11fcfcc30cdb09937c5546
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-responsive2 = django-responsive2:main
    #
    # Would create an entry point called django-responsive2 that calls django-responsive2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf
    - six

  run:
    - python
    - django-appconf
    - six

test:
  # Python imports
  imports:
    - responsive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mishbahr/django-responsive2
  license: BSD License
  summary: 'Utilities for building responsive websites in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
