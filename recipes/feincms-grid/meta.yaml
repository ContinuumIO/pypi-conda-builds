package:
  name: feincms-grid
  version: "0.1.2"

source:
  fn: feincms-grid-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/feincms-grid/feincms-grid-0.1.2.tar.gz
  md5: dd093c08113212ba74c16ff7bd33603b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-grid = feincms-grid:main
    #
    # Would create an entry point called feincms-grid that calls feincms-grid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - feincms >=1.10.1

  run:
    - python
    - django >=1.7
    - feincms >=1.10.1

test:
  # Python imports
  imports:
    - feincms_grid
    - feincms_grid.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joshuajonah/feincms_grid
  license: BSD License
  summary: 'Integrate CSS grids with FeinCMS contenttypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
