package:
  name: django-news
  version: "0.2.5"

source:
  fn: django-news-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-news/django-news-0.2.5.tar.gz
  md5: b863c1b951b0639207a2cf26fd5ea5fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-news = django-news:main
    #
    # Would create an entry point called django-news that calls django-news.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - news
    - news.management
    - news.management.commands
    - news.migrations
    - news.news_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coleifer/django-news/tree/master
  license: MIT License
  summary: 'rss feed aggregation with django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
