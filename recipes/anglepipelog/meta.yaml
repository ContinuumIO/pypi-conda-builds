package:
  name: anglepipelog
  version: "0.3"

source:
  fn: anglepipelog-0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/anglepipelog/anglepipelog-0.3.tar.gz
  md5: e1b2d062942a5f9974f0520b35e6202e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anglepipelog = anglepipelog:main
    #
    # Would create an entry point called anglepipelog that calls anglepipelog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - anglepipelog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markeganfuller/pyanglepipelog
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Library for parsing Angle-Pipe format logs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
