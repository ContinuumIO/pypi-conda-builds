package:
  name: beatbox
  version: "32.1"

source:
  fn: beatbox-32.1.zip
  url: https://pypi.python.org/packages/source/b/beatbox/beatbox-32.1.zip
  md5: db2cfbd6194b0862d7e63ac8de00c661
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beatbox = beatbox:main
    #
    # Would create an entry point called beatbox that calls beatbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - beatbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/salesforce-beatbox/
  license: GNU GENERAL PUBLIC LICENSE Version 2
  summary: 'A Python library for querying/updating Saleforce.com data via SOAP API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
