package:
  name: djenga
  version: "0.0.5dev"

source:
  fn: djenga-0.0.5dev.tar.gz
  url: https://pypi.python.org/packages/source/d/djenga/djenga-0.0.5dev.tar.gz
  md5: c1f456e20449aff412134fd78dbc69c3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djenga = djenga:main
    #
    # Would create an entry point called djenga that calls djenga.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.7.3
    - cssutils ==1.0
    - beautifulsoup4 ==4.3.2
    - pycrypto ==2.6.1

  run:
    - python
    - setuptools
    - django ==1.7.3
    - cssutils ==1.0
    - beautifulsoup4 ==4.3.2
    - pycrypto ==2.6.1

test:
  # Python imports
  imports:
    - djenga
    - djenga.csv
    - djenga.currency
    - djenga.dateutil
    - djenga.db
    - djenga.email
    - djenga.encryption
    - djenga.exceptions
    - djenga.inlinecss
    - djenga.loggers
    - djenga.management
    - djenga.management.commands
    - djenga.mixins
    - djenga.profiling

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/2ps/djenga
  license: BSD
  summary: 'Useful building blocks for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
