package:
  name: collective.deletepermission
  version: "1.2.0"

source:
  fn: collective.deletepermission-1.2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.deletepermission/collective.deletepermission-1.2.0.zip
  md5: 8f1a978930c14bc6ff61c7b233432a91
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.deletepermission = collective.deletepermission:main
    #
    # Would create an entry point called collective.deletepermission that calls collective.deletepermission.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - products.archetypes
    - products.cmfcore
    - products.cmfplone
    - products.genericsetup
    - products.pythonscripts
    - zodb3
    - zope2
    - collective.monkeypatcher
    - ftw.upgrade
    - setuptools
    - zope.container
    - zope.event
    - zope.lifecycleevent

  run:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - products.archetypes
    - products.cmfcore
    - products.cmfplone
    - products.genericsetup
    - products.pythonscripts
    - zodb3
    - zope2
    - collective.monkeypatcher
    - ftw.upgrade
    - setuptools
    - zope.container
    - zope.event
    - zope.lifecycleevent

test:
  # Python imports
  imports:
    - collective
    - collective.deletepermission
    - collective.deletepermission.tests
    - collective.deletepermission.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/collective.deletepermission
  license: GPL2
  summary: "Implements a new permission 'Delete portal content'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
