package:
  name: django_timedeltatemplatefilter
  version: "0.1.2"

source:
  fn: django_timedeltatemplatefilter-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_timedeltatemplatefilter/django_timedeltatemplatefilter-0.1.2.tar.gz
  md5: 68bf9d5d8ec0e4f62703c9831f4e8ec1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_timedeltatemplatefilter = django_timedeltatemplatefilter:main
    #
    # Would create an entry point called django_timedeltatemplatefilter that calls django_timedeltatemplatefilter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_timedeltatemplatefilter
    - timedeltatemplatefilter
    - timedeltatemplatefilter.templatetags
    - timedeltatemplatefilter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django_timedeltatemplatefilter
  license: The Unlicense
  summary: 'Format timedelta values nicely in your Django templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
