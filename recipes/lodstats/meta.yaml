package:
  name: lodstats
  version: "0.3.1"

source:
  fn: lodstats-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lodstats/lodstats-0.3.1.tar.gz
  md5: d2496b6e8f16d9f1d713d89b0496f5ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lodstats = lodstats:main
    #
    # Would create an entry point called lodstats that calls lodstats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - bitarray
    - sparqlwrapper

  run:
    - python
    - requests
    - bitarray
    - sparqlwrapper

test:
  # Python imports
  imports:
    - lodstats
    - lodstats.stats
    - lodstats.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aksw.org/projects/LODStats
  license: GNU General Public License (GPL)
  summary: 'Gather statistics from RDF files in various formats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
