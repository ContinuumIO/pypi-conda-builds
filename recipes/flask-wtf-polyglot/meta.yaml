package:
  name: flask-wtf-polyglot
  version: "0.1"

source:
  fn: Flask-WTF-Polyglot-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-WTF-Polyglot/Flask-WTF-Polyglot-0.1.tar.gz
  md5: 2eb6dc619ca9c27a62aa9e4ce34211b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-wtf-polyglot = flask-wtf-polyglot:main
    #
    # Would create an entry point called flask-wtf-polyglot that calls flask-wtf-polyglot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-wtf
    - wtforms-polyglot

  run:
    - python
    - flask-wtf
    - wtforms-polyglot

test:
  # Python imports
  imports:
    - flask_wtf_polyglot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yggi49/flask-wtf-polyglot
  license: BSD License
  summary: 'Flask-WTF companion library providing `PolyglotForm` for polyglot HTML output'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
