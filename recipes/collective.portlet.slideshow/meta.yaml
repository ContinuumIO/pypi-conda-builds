package:
  name: collective.portlet.slideshow
  version: "0.0.1"

source:
  fn: collective.portlet.slideshow-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.slideshow/collective.portlet.slideshow-0.0.1.tar.gz
  md5: 6e7d977515b2aa2ca2ed70e9a847c54f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.slideshow = collective.portlet.slideshow:main
    #
    # Would create an entry point called collective.portlet.slideshow that calls collective.portlet.slideshow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.slideshow
    - collective.portlet.slideshow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/intk/collective.portlet.slideshow
  license: GPL
  summary: 'A simple portlet that displays a slideshow.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
