package:
  name: list_nested
  version: "1.4.0"

source:
  fn: list_nested-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/l/list_nested/list_nested-1.4.0.tar.gz
  md5: 3673075283ea24a2ac0b18e26cf5544e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - list_nested = list_nested:main
    #
    # Would create an entry point called list_nested that calls list_nested.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - list_nested

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bagsandwallet.weebly.com
  license: UNKNOWN
  summary: 'Final upload of nested list printer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
