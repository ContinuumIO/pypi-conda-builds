package:
  name: kalapy
  version: "0.4.2"

source:
  fn: KalaPy-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/K/KalaPy/KalaPy-0.4.2.tar.gz
  md5: f627fa6eecc0a5eb0261dd6b5ba8c026
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kalapy = kalapy:main
    #
    # Would create an entry point called kalapy that calls kalapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.6.2
    - jinja2 >=2.4.1
    - babel >=0.9.5
    - pytz >=2010h
    - pygments >=1.3.1
    - simplejson >=2.1.1

  run:
    - python
    - werkzeug >=0.6.2
    - jinja2 >=2.4.1
    - babel >=0.9.5
    - pytz >=2010h
    - pygments >=1.3.1
    - simplejson >=2.1.1

test:
  # Python imports
  imports:
    - kalapy
    - kalapy.admin
    - kalapy.admin.commands
    - kalapy.admin.package_template
    - kalapy.conf
    - kalapy.contrib
    - kalapy.contrib.sessions
    - kalapy.contrib.sessions.engines
    - kalapy.core
    - kalapy.db
    - kalapy.db.engines
    - kalapy.db.engines.dummy
    - kalapy.db.engines.gae
    - kalapy.db.engines.mysql
    - kalapy.db.engines.postgresql
    - kalapy.db.engines.sqlite3
    - kalapy.i18n
    - kalapy.test
    - kalapy.utils
    - kalapy.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.kalapy.org/
  license: BSD License
  summary: 'A highly modular web application framework for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
