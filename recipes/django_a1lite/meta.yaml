package:
  name: django_a1lite
  version: "0.5"

source:
  fn: django_a1lite-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django_a1lite/django_a1lite-0.5.tar.gz
  md5: f26f61427f4984d4c2462e84d44b4ee3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_a1lite = django_a1lite:main
    #
    # Would create an entry point called django_a1lite that calls django_a1lite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - a1lite
    - django_a1lite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dgk/django_a1lite
  license: BSD License
  summary: 'django application for using http://a1pay.ru/ payment gateway'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
