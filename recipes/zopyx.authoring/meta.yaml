package:
  name: zopyx.authoring
  version: "2.4.0"

source:
  fn: zopyx.authoring-2.4.0.zip
  url: https://pypi.python.org/packages/source/z/zopyx.authoring/zopyx.authoring-2.4.0.zip
  md5: 1a874463964369b996202d0ef50af68c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.authoring = zopyx.authoring:main
    #
    # Would create an entry point called zopyx.authoring that calls zopyx.authoring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pp.client-python
    - products.datagridfield
    - zopyx.smartprintng.plone
    - beautifulsoup
    - simpledropbox
    - ordereddict
    - cssutils
    - lxml
    - z3c.jbot

  run:
    - python
    - setuptools
    - setuptools
    - pp.client-python
    - products.datagridfield
    - zopyx.smartprintng.plone
    - beautifulsoup
    - simpledropbox
    - ordereddict
    - cssutils
    - lxml
    - z3c.jbot

test:
  # Python imports
  imports:
    - zopyx
    - zopyx.authoring
    - zopyx.authoring.browser
    - zopyx.authoring.browser.tests
    - zopyx.authoring.browser.types
    - zopyx.authoring.content
    - zopyx.authoring.interfaces
    - zopyx.authoring.portlets
    - zopyx.authoring.subscribers
    - zopyx.authoring.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.produce-and-publish.com
  license: GNU General Public License (GPL)
  summary: 'Produce & Publish Authoring Environment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
