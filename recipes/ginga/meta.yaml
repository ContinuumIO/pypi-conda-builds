package:
  name: ginga
  version: "2.3.20150430212838"

source:
  fn: ginga-2.3.20150430212838.tar.gz
  url: https://pypi.python.org/packages/source/g/ginga/ginga-2.3.20150430212838.tar.gz
  md5: 2f6cc91b03c47b3e523a667a312fd591
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ginga = ginga:main
    #
    # Would create an entry point called ginga that calls ginga.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - ginga
    - ginga.aggw
    - ginga.base
    - ginga.cairow
    - ginga.canvas
    - ginga.doc
    - ginga.gtkw
    - ginga.gtkw.plugins
    - ginga.gtkw.tests
    - ginga.icons
    - ginga.misc
    - ginga.misc.plugins
    - ginga.mockw
    - ginga.mplw
    - ginga.qtw
    - ginga.qtw.plugins
    - ginga.qtw.tests
    - ginga.tests
    - ginga.tkw
    - ginga.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ejeschke.github.com/ginga
  license: BSD License
  summary: 'An astronomical image viewer and toolkit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
