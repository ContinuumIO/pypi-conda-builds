package:
  name: collective.harlequin
  version: "1.0b4"

source:
  fn: collective.harlequin-1.0b4.zip
  url: https://pypi.python.org/packages/source/c/collective.harlequin/collective.harlequin-1.0b4.zip
  md5: 0388888b1bb3dc81dd0e23bce7e3b36e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.harlequin = collective.harlequin:main
    #
    # Would create an entry point called collective.harlequin that calls collective.harlequin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.harlequin
    - collective.harlequin.Extensions
    - collective.harlequin.browser
    - collective.harlequin.example
    - collective.harlequin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.harlequin
  license: GPL
  summary: 'System based on annotation to make a configurable browser view by Makina Corpus.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
