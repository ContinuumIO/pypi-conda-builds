package:
  name: hifive
  version: "1.0.3"

source:
  fn: hifive-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hifive/hifive-1.0.3.tar.gz
  md5: d23e7305da8fd256ab1024afea09b4cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hifive = hifive:main
    #
    # Would create an entry point called hifive that calls hifive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - h5py

  run:
    - python
    - numpy
    - scipy
    - h5py

test:
  # Python imports
  imports:
    - hifive
    - hifive.commands
    - hifive.libraries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bxlab/hifive
  license: UNKNOWN
  summary: 'Python library for normalizing and analyzing HiC and 5C data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
