package:
  name: husoftm
  version: "0.65p2"

source:
  fn: huSoftM-0.65p2.tar.gz
  url: https://pypi.python.org/packages/source/h/huSoftM/huSoftM-0.65p2.tar.gz
  md5: 8c22d98c256a8db110ba3570fd308674
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - husoftm = husoftm:main
    #
    # Would create an entry point called husoftm that calls husoftm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cs
    - simplejson
    - couchdb
    - python-memcached
    - hutools >=0.38p2
    - pyro

  run:
    - python
    - cs
    - simplejson
    - couchdb
    - python-memcached
    - hutools >=0.38p2
    - pyro

test:
  # Python imports
  imports:
    - husoftm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hudora/huSoftM#readme
  license: BSD License
  summary: 'communication with the SoftM Suite on an AS/400'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
