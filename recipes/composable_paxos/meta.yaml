package:
  name: composable_paxos
  version: "1.0.0"

source:
  fn: composable_paxos-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/composable_paxos/composable_paxos-1.0.0.tar.gz
  md5: 5ea78ffe24d8e524aabe2b014d3380a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - composable_paxos = composable_paxos:main
    #
    # Would create an entry point called composable_paxos that calls composable_paxos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - composable_paxos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cocagne/python-composable-paxos
  license: MIT License
  summary: 'Implements the core Paxos algorithm as a set of composable classes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
