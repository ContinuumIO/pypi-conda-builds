package:
  name: sail
  version: "0.3.0"

source:
  fn: sail-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sail/sail-0.3.0.tar.gz
  md5: 3eca3409ad9bf029c3064e12bcc07244
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sail = sail:main
    #
    # Would create an entry point called sail that calls sail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.3
    - tabulate ==0.7.2
    - python-dateutil ==2.2
    - requests ==2.3.0
    - argparse ==1.2.1
    - argcomplete ==0.8.1
    - pyaml ==14.05.7

  run:
    - python
    - docutils >=0.3
    - tabulate ==0.7.2
    - python-dateutil ==2.2
    - requests ==2.3.0
    - argparse ==1.2.1
    - argcomplete ==0.8.1
    - pyaml ==14.05.7

test:
  # Python imports
  imports:
    - sail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://labs.runabove.com/
  license: BSD License
  summary: 'Sailabove CLI - Docker hosting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
