package:
  name: django-file-keeper
  version: "0.0.1"

source:
  fn: django-file-keeper-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-file-keeper/django-file-keeper-0.0.1.tar.gz
  md5: ba63d3278a65214bf19a5575c68e1614
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-file-keeper = django-file-keeper:main
    #
    # Would create an entry point called django-file-keeper that calls django-file-keeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - keeper
    - keeper.management
    - keeper.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spulec/django-file-keeper
  license: UNKNOWN
  summary: 'file storage for management command in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
