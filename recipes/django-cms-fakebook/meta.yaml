package:
  name: django-cms-fakebook
  version: "0.1.0"

source:
  fn: django-cms-fakebook-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-fakebook/django-cms-fakebook-0.1.0.tar.gz
  md5: bf54d5e11c95d1af25a775f4b5499443
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-fakebook = django-cms-fakebook:main
    #
    # Would create an entry point called django-cms-fakebook that calls django-cms-fakebook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cms_facebook
    - cms_facebook.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-cms-facebook
  license: UNKNOWN
  summary: 'Django CMS Facebook Plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
