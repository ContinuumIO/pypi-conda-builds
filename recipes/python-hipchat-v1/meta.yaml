package:
  name: python-hipchat-v1
  version: "0.3.5"

source:
  fn: python-hipchat-v1-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hipchat-v1/python-hipchat-v1-0.3.5.tar.gz
  md5: 2f2f95afd5c8ace31a33f29012e21942
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hipchat-v1 = python-hipchat-v1:main
    #
    # Would create an entry point called python-hipchat-v1 that calls python-hipchat-v1.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hipchat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/greenietea/python-hipchat-v1
  license: MIT License
  summary: "Easy peasy wrapper for HipChat's v1 API (modified version and credits to kurttheviking)"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
