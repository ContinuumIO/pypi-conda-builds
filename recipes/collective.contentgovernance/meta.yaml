package:
  name: collective.contentgovernance
  version: "1.1"

source:
  fn: collective.contentgovernance-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.contentgovernance/collective.contentgovernance-1.1.zip
  md5: 24b37c07e581cd2590b6ab4579188c64
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentgovernance = collective.contentgovernance:main
    #
    # Would create an entry point called collective.contentgovernance that calls collective.contentgovernance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.testing

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.testing

test:
  # Python imports
  imports:
    - collective
    - collective.contentgovernance
    - collective.contentgovernance.browser
    - collective.contentgovernance.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contentgovernance
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
