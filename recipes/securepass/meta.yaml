package:
  name: securepass
  version: "0.3.6"

source:
  fn: securepass-0.3.6.tar.gz
  url: https://pypi.python.org/packages/source/s/securepass/securepass-0.3.6.tar.gz
  md5: f46ebfe9b1534479f07d5906df51a678
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - securepass = securepass:main
    #
    # Would create an entry point called securepass that calls securepass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycurl >=7.19.0

  run:
    - python
    - pycurl >=7.19.0

test:
  # Python imports
  imports:
    - securepass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/garlsecurity/securepass-tools
  license: LICENSE.txt
  summary: 'SecurePass tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
