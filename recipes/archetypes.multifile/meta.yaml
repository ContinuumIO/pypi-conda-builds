package:
  name: archetypes.multifile
  version: "1.5"

source:
  fn: archetypes.multifile-1.5.zip
  url: https://pypi.python.org/packages/source/a/archetypes.multifile/archetypes.multifile-1.5.zip
  md5: 8b486b05092ed6590c78dc20b5b292f9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.multifile = archetypes.multifile:main
    #
    # Would create an entry point called archetypes.multifile that calls archetypes.multifile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.multifile
    - archetypes.multifile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.multifile
  license: GPL
  summary: 'This package adds a field to upload multiple files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
