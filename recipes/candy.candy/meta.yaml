package:
  name: candy.candy
  version: "0.1.2"

source:
  fn: candy.candy-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/candy.candy/candy.candy-0.1.2.tar.gz
  md5: f946b2acd9b15280769e1d168b3f184c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - candy.candy = candy.candy:main
    #
    # Would create an entry point called candy.candy that calls candy.candy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

test:
  # Python imports
  imports:
    - candy
    - candy.candy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://candy-candy.googlecode.com/
  license: GPL
  summary: 'Grabs videos from youtube'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
