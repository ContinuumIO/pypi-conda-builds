package:
  name: djangocms-admin-style
  version: "0.2.5"

source:
  fn: djangocms-admin-style-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-admin-style/djangocms-admin-style-0.2.5.tar.gz
  md5: 9419d90877dca4d7d83a2b37060faea0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-admin-style = djangocms-admin-style:main
    #
    # Would create an entry point called djangocms-admin-style that calls djangocms-admin-style.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_admin_style

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.django-cms.org/
  license: BSD License
  summary: 'Adds pretty CSS styles for the django CMS admin interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
