package:
  name: django-knockout-modeler
  version: "0.6.3"

source:
  fn: django-knockout-modeler-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-knockout-modeler/django-knockout-modeler-0.6.3.tar.gz
  md5: 095abdc79abef0d1cda25e3ea840bf66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-knockout-modeler = django-knockout-modeler:main
    #
    # Would create an entry point called django-knockout-modeler that calls django-knockout-modeler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - knockout_modeler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Miserlou/django-knockout-modeler
  license: Apache Software License
  summary: 'Super easy ModelViews for knockout.js for your Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
