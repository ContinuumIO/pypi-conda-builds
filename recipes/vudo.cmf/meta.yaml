package:
  name: vudo.cmf
  version: "0.2"

source:
  fn: vudo.cmf-0.2.tar.gz
  url: https://pypi.python.org/packages/source/v/vudo.cmf/vudo.cmf-0.2.tar.gz
  md5: 9c6e617c36800358faf7a5b62c35efa2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vudo.cmf = vudo.cmf:main
    #
    # Would create an entry point called vudo.cmf that calls vudo.cmf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.dublincore
    - repoze.bfg
    - repoze.formapi
    - repoze.bfg.skins
    - repoze.bfg.httprequest
    - plone.transforms
    - markdown

  run:
    - python
    - setuptools
    - setuptools
    - zope.dublincore
    - repoze.bfg
    - repoze.formapi
    - repoze.bfg.skins
    - repoze.bfg.httprequest
    - plone.transforms
    - markdown

test:
  # Python imports
  imports:
    - vudo
    - vudo.cmf
    - vudo.cmf.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'Content management framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
