package:
  name: mopidy-http-kuechenradio
  version: "0.4.2"

source:
  fn: Mopidy-HTTP-Kuechenradio-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-HTTP-Kuechenradio/Mopidy-HTTP-Kuechenradio-0.4.2.tar.gz
  md5: c76c89c6de74b4c9d53ef0863f4ccfc8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-http-kuechenradio = mopidy-http-kuechenradio:main
    #
    # Would create an entry point called mopidy-http-kuechenradio that calls mopidy-http-kuechenradio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19

test:
  # Python imports
  imports:
    - mopidy_http_kuechenradio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tkem/mopidy-http-kuechenradio
  license: Apache Software License
  summary: u'A deliberately simple Mopidy Web client for mobile devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
