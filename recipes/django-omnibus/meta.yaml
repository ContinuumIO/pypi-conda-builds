package:
  name: django-omnibus
  version: "0.2.0"

source:
  fn: django-omnibus-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-omnibus/django-omnibus-0.2.0.tar.gz
  md5: becb93c70f2c885d8b8448a5363aab7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-omnibus = django-omnibus:main
    #
    # Would create an entry point called django-omnibus that calls django-omnibus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pyzmq ==14.1.1
    - tornado ==3.1.1
    - sockjs-tornado >=1.0.0

  run:
    - python
    - django >=1.4
    - pyzmq ==14.1.1
    - tornado ==3.1.1
    - sockjs-tornado >=1.0.0

test:
  # Python imports
  imports:
    - omnibus
    - omnibus.management
    - omnibus.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/moccu/django-omnibus/
  license: BSD License
  summary: 'Django/JavaScript WebSocket Connections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
