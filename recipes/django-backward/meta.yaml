package:
  name: django-backward
  version: "0.3.5"

source:
  fn: django-backward-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-backward/django-backward-0.3.5.tar.gz
  md5: 729b0fb9efcfea4c6784d213f8be619c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-backward = django-backward:main
    #
    # Would create an entry point called django-backward that calls django-backward.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - backward
    - backward.backends
    - backward.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-backward
  license: MIT License
  summary: 'A Django application to store your previous history and action in your session engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
