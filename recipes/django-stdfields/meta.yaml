package:
  name: django-stdfields
  version: "1.0.2"

source:
  fn: django-stdfields-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stdfields/django-stdfields-1.0.2.tar.gz
  md5: ef01ab6dc1b080cb5156e21a058c871a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stdfields = django-stdfields:main
    #
    # Would create an entry point called django-stdfields that calls django-stdfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - stdfields
    - stdfields.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/roam/django-stdfields
  license: BSD licence, see LICENCE
  summary: 'Extra model and form fields for Django: minutes and enumerations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
