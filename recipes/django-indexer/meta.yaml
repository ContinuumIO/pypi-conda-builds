package:
  name: django-indexer
  version: "0.3.0"

source:
  fn: django-indexer-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-indexer/django-indexer-0.3.0.tar.gz
  md5: 3e10959319901d5f27d356bdd377b0cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-indexer = django-indexer:main
    #
    # Would create an entry point called django-indexer that calls django-indexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - indexer
    - indexer.migrations
    - indexer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcramer/django-indexer
  license: UNKNOWN
  summary: 'Key/Value Indexer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
