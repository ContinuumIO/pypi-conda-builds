package:
  name: django-web-utils
  version: "0.0.5"

source:
  fn: django-web-utils-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-web-utils/django-web-utils-0.0.5.tar.gz
  md5: d2b0bd90137b4a7bb1d73dc58f53c603
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-web-utils = django-web-utils:main
    #
    # Would create an entry point called django-web-utils that calls django-web-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - web_utils
    - web_utils.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madisona/django-web-utils
  license: BSD License
  summary: 'Django helpers for working with the web.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
