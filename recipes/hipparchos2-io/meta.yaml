package:
  name: hipparchos2-io
  version: "1.0.2"

source:
  fn: hipparchos2-io-1.0.2.zip
  url: https://pypi.python.org/packages/source/h/hipparchos2-io/hipparchos2-io-1.0.2.zip
  md5: 1472cc7f9aad1181b164a00abb56db7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hipparchos2-io = hipparchos2-io:main
    #
    # Would create an entry point called hipparchos2-io that calls hipparchos2-io.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hip2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JohnHBrock/hipparchos2-io
  license: MIT License
  summary: 'A Python library for reading Hipparcos new reduction data, and for writing Hipparcos new reduction data to a CSV.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
