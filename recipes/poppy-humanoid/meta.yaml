package:
  name: poppy-humanoid
  version: "1.0.1"

source:
  fn: poppy-humanoid-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/poppy-humanoid/poppy-humanoid-1.0.1.tar.gz
  md5: 4d0b2f7ba578e08153bb557826d87261
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - poppy-humanoid = poppy-humanoid:main
    #
    # Would create an entry point called poppy-humanoid that calls poppy-humanoid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - poppy-creature >=1.0
    - pypot >=2.2.2

  run:
    - python
    - poppy-creature >=1.0
    - pypot >=2.2.2

test:
  # Python imports
  imports:
    - poppy_humanoid
    - poppy_humanoid.primitives
    - poppy_humanoid.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/poppy-project/poppy-creature
  license: GNU GENERAL PUBLIC LICENSE Version 3
  summary: 'Poppy Humanoid Software Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
