package:
  name: macaron
  version: "0.3.1"

source:
  fn: macaron-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/m/macaron/macaron-0.3.1.tar.gz
  md5: 9b02d293a2fdef5d1e965d5d1be05988
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - macaron = macaron:main
    #
    # Would create an entry point called macaron that calls macaron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - macaron

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nobrin.github.com/macaron
  license: MIT License
  summary: 'Simple object-relational mapper for SQLite3, includes plugin for Bottle web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
