package:
  name: estruct
  version: "0.1.3"

source:
  fn: EStruct-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/E/EStruct/EStruct-0.1.3.tar.gz
  md5: e0f1247c02259b038b199251e376bebe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - estruct = estruct:main
    #
    # Would create an entry point called estruct that calls estruct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils

  run:
    - python
    - docutils

test:
  # Python imports
  imports:
    - estruct
    - estruct.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/EStruct/
  license: LICENSE.txt
  summary: "Python package to extend the capabilities of the standard library's Struct package."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
