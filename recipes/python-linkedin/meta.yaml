package:
  name: python-linkedin
  version: "4.1"

source:
  fn: python-linkedin-4.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-linkedin/python-linkedin-4.1.tar.gz
  md5: 10574e7c6836f3db4fb06feb9e3b04d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-linkedin = python-linkedin:main
    #
    # Would create an entry point called python-linkedin that calls python-linkedin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1.0
    - requests-oauthlib >=0.3

  run:
    - python
    - requests >=1.1.0
    - requests-oauthlib >=0.3

test:
  # Python imports
  imports:
    - linkedin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ozgur.github.com/python-linkedin/
  license: MIT License
  summary: 'Python Interface to the LinkedIn API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
