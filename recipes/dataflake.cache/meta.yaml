package:
  name: dataflake.cache
  version: "1.4"

source:
  fn: dataflake.cache-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dataflake.cache/dataflake.cache-1.4.tar.gz
  md5: 5da22ec2cf9714eea08b0134888fd2d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataflake.cache = dataflake.cache:main
    #
    # Would create an entry point called dataflake.cache that calls dataflake.cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface

  run:
    - python
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - dataflake
    - dataflake.cache
    - dataflake.cache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/dataflake.cache
  license: Zope Public License
  summary: 'Simple caching library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
