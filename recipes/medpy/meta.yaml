package:
  name: medpy
  version: "0.2.2"

source:
  fn: MedPy-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/M/MedPy/MedPy-0.2.2.tar.gz
  md5: ee90bd5f7bf10ce956ce66bbda679a06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medpy = medpy:main
    #
    # Would create an entry point called medpy that calls medpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scipy >=0.9.0
    - numpy >=1.6.1

  run:
    - python
    - scipy >=0.9.0
    - numpy >=1.6.1

test:
  # Python imports
  imports:
    - medpy
    - medpy.core
    - medpy.features
    - medpy.filter
    - medpy.graphcut
    - medpy.io
    - medpy.itkvtk
    - medpy.itkvtk.filter
    - medpy.itkvtk.utilities
    - medpy.metric
    - medpy.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/loli/medpy
  license: GNU General Public License (GPL)
  summary: 'Medical image processing in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
