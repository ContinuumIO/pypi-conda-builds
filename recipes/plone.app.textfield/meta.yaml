package:
  name: plone.app.textfield
  version: "1.2.5"

source:
  fn: plone.app.textfield-1.2.5.zip
  url: https://pypi.python.org/packages/source/p/plone.app.textfield/plone.app.textfield-1.2.5.zip
  md5: 3b0609e4d8b8b8dd1a2c65f780e90038
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.textfield = plone.app.textfield:main
    #
    # Would create an entry point called plone.app.textfield that calls plone.app.textfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.interface
    - zope.component
    - zodb3 >=3.8.1

  run:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.interface
    - zope.component
    - zodb3 >=3.8.1

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.textfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.textfield
  license: GPL
  summary: 'Text field with MIME type support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
