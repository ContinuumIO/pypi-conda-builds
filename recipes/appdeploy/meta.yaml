package:
  name: appdeploy
  version: "1.0.3"

source:
  fn: appdeploy-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/appdeploy/appdeploy-1.0.3.tar.gz
  md5: dddcf16f351a5b3e8b47830057611f5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appdeploy = appdeploy:main
    #
    # Would create an entry point called appdeploy that calls appdeploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - appdeploy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jbq/appdeploy
  license: UNKNOWN
  summary: 'Application deployment tool I use and maintain for years, to be used in a console (uses an ncurses UI)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
