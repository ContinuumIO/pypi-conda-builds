package:
  name: harpy
  version: "0.2.0"

source:
  fn: HARPy-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/H/HARPy/HARPy-0.2.0.tar.gz
  md5: 843f2c115a309da6dfb377060719fee2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - harpy = harpy:main
    #
    # Would create an entry point called harpy that calls harpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - harpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamgoucher/HARPy
  license: Apache Software License
  summary: 'HARPy: a simple library for interrogating an HTTP Archive (HAR) file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
