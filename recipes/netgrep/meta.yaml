package:
  name: netgrep
  version: "0.3.7"

source:
  fn: netgrep-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/n/netgrep/netgrep-0.3.7.tar.gz
  md5: c72daedd8b42e3a624dd1148452268d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netgrep = netgrep:main
    #
    # Would create an entry point called netgrep that calls netgrep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - publicsuffix >=1.0.0
    - httplib2 >=0.6.0
    - bulkwhois >=0.2.1
    - adns-python >=1.2.1
    - argparse

  run:
    - python
    - publicsuffix >=1.0.0
    - httplib2 >=0.6.0
    - bulkwhois >=0.2.1
    - adns-python >=1.2.1
    - argparse

test:
  # Python imports
  imports:
    - netgrep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/netgrep/
  license: LICENSE.txt
  summary: 'Grep-like filter for files based on country codes and ASN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
