package:
  name: greeking
  version: "1.1.1"

source:
  fn: greeking-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/greeking/greeking-1.1.1.tar.gz
  md5: 9fda1c23db4aef3e4d590e5e2ed657e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - greeking = greeking:main
    #
    # Would create an entry point called greeking that calls greeking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.5.2

  run:
    - python
    - six >=1.5.2

test:
  # Python imports
  imports:
    - greeking

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/palewire/django-greeking
  license: MIT License
  summary: 'Django template tools for printing filler, a     technique from the days of hot type known as greeking.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
