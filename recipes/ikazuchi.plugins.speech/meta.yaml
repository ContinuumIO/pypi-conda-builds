package:
  name: ikazuchi.plugins.speech
  version: "0.1.2"

source:
  fn: ikazuchi.plugins.speech-0.1.2.tar.bz2
  url: https://pypi.python.org/packages/source/i/ikazuchi.plugins.speech/ikazuchi.plugins.speech-0.1.2.tar.bz2
  md5: 18a81817cce5fb90d2c5af41cacf0cc8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ikazuchi.plugins.speech = ikazuchi.plugins.speech:main
    #
    # Would create an entry point called ikazuchi.plugins.speech that calls ikazuchi.plugins.speech.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - pyglet
    - ikazuchi >=0.5.2

  run:
    - python
    - setuptools
    - distribute
    - pyglet
    - ikazuchi >=0.5.2

test:
  # Python imports
  imports:
    - ikazuchi
    - ikazuchi.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://t2y.bitbucket.org/ikazuchi/build/html/index.html
  license: Apache Software License
  summary: 'Speech handler with ikazuchi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
