package:
  name: poolana
  version: "0.9.2"

source:
  fn: PoolAna-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/P/PoolAna/PoolAna-0.9.2.tar.gz
  md5: b1cf6ef339cb4f2af27a2e5049c15faf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - poolana = poolana:main
    #
    # Would create an entry point called poolana that calls poolana.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - poolana

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/PoolAna/
  license: GPLv2
  summary: 'Worker process pool based analysis framework for use with PyROOT.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
