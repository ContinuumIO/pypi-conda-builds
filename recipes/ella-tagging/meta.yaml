package:
  name: ella-tagging
  version: "1.0.0"

source:
  fn: Ella-Tagging-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/E/Ella-Tagging/Ella-Tagging-1.0.0.tar.gz
  md5: bde58f67362834c36d3fe2d4bd0d4212
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ella-tagging = ella-tagging:main
    #
    # Would create an entry point called ella-tagging that calls ella-tagging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - ella >=3.0.0
    - django-tagging

  run:
    - python
    - setuptools >=0.6b1
    - ella >=3.0.0
    - django-tagging

test:
  # Python imports
  imports:
    - ella_tagging
    - test_ella_tagging.settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ella.github.com/
  license: BSD License
  summary: 'django-tagging wrapper for Ella CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
