package:
  name: launchkey-pyramid
  version: "0.1"

source:
  fn: launchkey-pyramid-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/launchkey-pyramid/launchkey-pyramid-0.1.tar.gz
  md5: 2aacc9d6b314ebc7c6f01d8cb9791b2e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - launchkey-pyramid = launchkey-pyramid:main
    #
    # Would create an entry point called launchkey-pyramid that calls launchkey-pyramid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - pyramid_chameleon
    - pyramid_debugtoolbar
    - waitress
    - launchkey-python

  run:
    - python
    - setuptools
    - pyramid
    - pyramid_chameleon
    - pyramid_debugtoolbar
    - waitress
    - launchkey-python

test:
  # Python imports
  imports:
    - pyramid_launchkey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LaunchKey/launchkey-pyramid
  license: MIT
  summary: 'LaunchKey authentication extension for Pyramid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
