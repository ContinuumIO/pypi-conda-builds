package:
  name: django-documentation
  version: "1.1"

source:
  fn: django-documentation-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-documentation/django-documentation-1.1.tar.gz
  md5: 55f15c4a34b8c246b3709383298a15a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-documentation = django-documentation:main
    #
    # Would create an entry point called django-documentation that calls django-documentation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.0.7

  run:
    - python
    - sphinx >=1.0.7

test:
  # Python imports
  imports:
    - documentation
    - documentation.management
    - documentation.management.commands
    - example_documentation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Narsil/django-documentation
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Provides a way to integrate a protected sphinx based\n    documentation within your django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
