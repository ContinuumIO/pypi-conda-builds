package:
  name: anthill.customexport
  version: "0.3"

source:
  fn: anthill.customexport-0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/anthill.customexport/anthill.customexport-0.3.tar.gz
  md5: 569f0487c1efeb257f4cab0c49d4d3e8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anthill.customexport = anthill.customexport:main
    #
    # Would create an entry point called anthill.customexport that calls anthill.customexport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - anthill
    - anthill.customexport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Makes it possible to export custom folder contents (from portal_skins/custom and portal_view_customizations) to filesystem'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
