package:
  name: fotografering
  version: "0.3"

source:
  fn: Fotografering-0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Fotografering/Fotografering-0.3.tar.gz
  md5: ea7b4b3f9663719397edf7faac0def15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fotografering = fotografering:main
    #
    # Would create an entry point called fotografering that calls fotografering.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - requests

  run:
    - python
    - docopt
    - requests

test:
  # Python imports
  imports:
    - fotografering

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ## Set url
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Fotografering, linkify and beautify your photos.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
