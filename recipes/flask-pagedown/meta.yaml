package:
  name: flask-pagedown
  version: "0.1.5"

source:
  fn: Flask-PageDown-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-PageDown/Flask-PageDown-0.1.5.tar.gz
  md5: 73a4fb383fcd4e1f48344c15148ac3a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-pagedown = flask-pagedown:main
    #
    # Would create an entry point called flask-pagedown that calls flask-pagedown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - wtforms

  run:
    - python
    - flask
    - wtforms

test:
  # Python imports
  imports:
    - flask_pagedown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/miguelgrinberg/flask-pagedown/
  license: MIT License
  summary: 'Implementation of StackOverflow\'s "PageDown" markdown editor for Flask-WTF.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
