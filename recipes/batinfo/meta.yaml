package:
  name: batinfo
  version: "0.3"

source:
  fn: batinfo-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/batinfo/batinfo-0.3.tar.gz
  md5: f9ad72f0e63b466e5f67ab45214b310e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - batinfo = batinfo:main
    #
    # Would create an entry point called batinfo that calls batinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - batinfo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicolargo/batinfo
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'A simple Python lib to retreive battery information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
