package:
  name: cubicweb-clinipath
  version: "0.2.2"

source:
  fn: cubicweb-clinipath-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-clinipath/cubicweb-clinipath-0.2.2.tar.gz
  md5: 807f469b3ae4be1204b4089848b482f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-clinipath = cubicweb-clinipath:main
    #
    # Would create an entry point called cubicweb-clinipath that calls cubicweb-clinipath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18.4

  run:
    - python
    - cubicweb >=3.18.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-clinipath
  license: LGPL
  summary: 'Cube for modeling and handling of clinical pathology data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
