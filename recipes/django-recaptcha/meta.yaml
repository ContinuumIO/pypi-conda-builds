package:
  name: django-recaptcha
  version: "1.0.4"

source:
  fn: django-recaptcha-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-recaptcha/django-recaptcha-1.0.4.tar.gz
  md5: 65c7c8d62688fb569525ef380f0008c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-recaptcha = django-recaptcha:main
    #
    # Would create an entry point called django-recaptcha that calls django-recaptcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - captcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-recaptcha
  license: BSD License
  summary: 'Django recaptcha form field/widget app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
