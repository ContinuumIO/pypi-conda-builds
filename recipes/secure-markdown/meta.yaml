package:
  name: secure-markdown
  version: "2.3.dev"

source:
  fn: secure-Markdown-2.3.dev.tar.gz
  url: https://pypi.python.org/packages/source/s/secure-Markdown/secure-Markdown-2.3.dev.tar.gz
  md5: b172fea5d93c45070a75d4fda03acb1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secure-markdown = secure-markdown:main
    #
    # Would create an entry point called secure-markdown that calls secure-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - markdown
    - markdown.extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/waylan/Python-Markdown/issues/186
  license: BSD License
  summary: 'Python implementation of Markdown.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
