package:
  name: hipchav
  version: "0.1.3"

source:
  fn: hipchav-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hipchav/hipchav-0.1.3.tar.gz
  md5: 008a1cde94afd4994570be07f78f88c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hipchav = hipchav:main
    #
    # Would create an entry point called hipchav that calls hipchav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3

  run:
    - python
    - requests >=1.2.3

test:
  # Python imports
  imports:
    - hipchav

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/larsyencken/hipchav/
  license: ISC
  summary: 'A minimal command-line HipChat client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
