package:
  name: clyent
  version: "0.3.4"

source:
  fn: clyent-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/c/clyent/clyent-0.3.4.tar.gz
  md5: 11c0b1659ae85819bba65470c1c96fa8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clyent = clyent:main
    #
    # Would create an entry point called clyent that calls clyent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clyent
    - clyent.logs
    - clyent.logs.colors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/binstar/clyent
  license: BSD
  summary: 'Command line client Library for windwos and posix'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
