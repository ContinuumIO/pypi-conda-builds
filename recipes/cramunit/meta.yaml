package:
  name: cramunit
  version: "0.8"

source:
  fn: CramUnit-0.8.tar.gz
  url: https://pypi.python.org/packages/source/C/CramUnit/CramUnit-0.8.tar.gz
  md5: c6d04c13cd4213fcf69bd6152bff94ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cramunit = cramunit:main
    #
    # Would create an entry point called cramunit that calls cramunit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cram
    - nose
    - xmlbuilder

  run:
    - python
    - cram
    - nose
    - xmlbuilder

test:
  # Python imports
  imports:
    - cram_unit
    - cramunit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mpkocher/CramUnit
  license: BSD
  summary: 'Tool to create Unittests/Xunit output from cram (*.t) tests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
