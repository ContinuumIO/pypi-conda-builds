package:
  name: arquiapgw
  version: "1.0.0"

source:
  fn: arquiapgw-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/arquiapgw/arquiapgw-1.0.0.tar.gz
  md5: c29c52c610157bdb37136efc82c18c7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arquiapgw = arquiapgw:main
    #
    # Would create an entry point called arquiapgw that calls arquiapgw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pydes

  run:
    - python
    - pydes

test:
  # Python imports
  imports:
    - arquiapgw

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gisce.net
  license: General Public Licence 2
  summary: 'A client to submit payment orders to the Arquia\n      service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
