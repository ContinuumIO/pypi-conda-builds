package:
  name: aiocron
  version: "0.2"

source:
  fn: aiocron-0.2.zip
  url: https://pypi.python.org/packages/source/a/aiocron/aiocron-0.2.zip
  md5: 272eaa820004233923cb5a4e9a4353ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiocron = aiocron:main
    #
    # Would create an entry point called aiocron that calls aiocron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - croniter
    - trollius
    - futures

  run:
    - python
    - croniter
    - trollius
    - futures

test:
  # Python imports
  imports:
    - aiocron

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gawel/aiocron/
  license: MIT License
  summary: 'Crontabs for asyncio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
