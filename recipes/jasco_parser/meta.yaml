package:
  name: jasco_parser
  version: "0.1.0"

source:
  fn: jasco_parser-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jasco_parser/jasco_parser-0.1.0.tar.gz
  md5: e23f4c4e8ae92e7cca147dcca816d3be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jasco_parser = jasco_parser:main
    #
    # Would create an entry point called jasco_parser that calls jasco_parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - app_version

  run:
    - python
    - app_version

test:
  # Python imports
  imports:
    - jasco_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/jasco_parser
  license: MIT License
  summary: 'A enhanced permission system which enable logical permissionsystems to complex permissions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
