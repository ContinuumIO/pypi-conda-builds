package:
  name: jsonconfig
  version: "0.1.2"

source:
  fn: jsonConfig-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonConfig/jsonConfig-0.1.2.tar.gz
  md5: 88f7c092b7dca99344ac289189ef3f07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonconfig = jsonconfig:main
    #
    # Would create an entry point called jsonconfig that calls jsonconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonConfig
    - jsonconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RyanHope/jsonConfig
  license: GNU General Public License (GPL)
  summary: 'Sparse JSON based config files with GUI editor.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
