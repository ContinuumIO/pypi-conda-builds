package:
  name: memcacheinspector
  version: "0.2.0"

source:
  fn: memcacheinspector-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/memcacheinspector/memcacheinspector-0.2.0.tar.gz
  md5: a5858f78e6529aa6bdd97d9f9a202bf9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memcacheinspector = memcacheinspector:main
    #
    # Would create an entry point called memcacheinspector that calls memcacheinspector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached

  run:
    - python
    - python-memcached

test:
  # Python imports
  imports:
    - memcacheinspector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jayclassless/memcacheinspector
  license: MIT License
  summary: 'Memcache Inspection Module and Command-Line Utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
