package:
  name: django-djam
  version: "0.1.0"

source:
  fn: django-djam-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-djam/django-djam-0.1.0.tar.gz
  md5: 6746e85ba684a34f78dd29dadff47ca9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-djam = django-djam:main
    #
    # Would create an entry point called django-djam that calls django-djam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-floppyforms >=1.1

  run:
    - python
    - django >=1.5
    - django-floppyforms >=1.1

test:
  # Python imports
  imports:
    - djam
    - djam.riffs
    - djam.templatetags
    - djam.views
    - test_project
    - test_project.test_project
    - test_project.test_project.example_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django-djam/django-djam
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
