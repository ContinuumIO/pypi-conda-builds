package:
  name: jukebox-live-indexer
  version: "0.1.1"

source:
  fn: jukebox-live-indexer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jukebox-live-indexer/jukebox-live-indexer-0.1.1.tar.gz
  md5: 2fa2d9f40c0dac7207d54a087a7e7847
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jukebox-live-indexer = jukebox-live-indexer:main
    #
    # Would create an entry point called jukebox-live-indexer that calls jukebox-live-indexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jukebox >=0.3.7
    - python-daemon ==1.6
    - watchdog >=0.6.0

  run:
    - python
    - jukebox >=0.3.7
    - python-daemon ==1.6
    - watchdog >=0.6.0

test:
  # Python imports
  imports:
    - jukebox_live_indexer
    - jukebox_live_indexer.management
    - jukebox_live_indexer.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jensnistler.de/
  license: MIT License
  summary: 'Background library indexing service for jukebox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
