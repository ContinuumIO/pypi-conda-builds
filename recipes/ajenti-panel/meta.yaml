package:
  name: ajenti-panel
  version: "2.0.45"

source:
  fn: ajenti-panel-2.0.45.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti-panel/ajenti-panel-2.0.45.tar.gz
  md5: dca37fcef2e1ad6cd4869e3a0a4db425
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti-panel = ajenti-panel:main
    #
    # Would create an entry point called ajenti-panel that calls ajenti-panel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj ==2.0.45
    - pyyaml

  run:
    - python
    - aj ==2.0.45
    - pyyaml

test:
  # Python imports
  imports:
    - aj
    - aj.api
    - aj.gate
    - aj.security
    - aj.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org/
  license: UNKNOWN
  summary: 'Ajenti core based panel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
