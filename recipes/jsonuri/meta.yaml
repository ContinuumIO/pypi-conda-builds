package:
  name: jsonuri
  version: "0.1.22"

source:
  fn: jsonuri-0.1.22.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonuri/jsonuri-0.1.22.tar.gz
  md5: 7f12a26f4d992352a5763569c44f8d66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonuri = jsonuri:main
    #
    # Would create an entry point called jsonuri that calls jsonuri.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonuri

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/guidj/jsonuri-py
  license: Apache Software License
  summary: 'Package to serialize python dictionaries/JSON into URI GET parameters, and reverse.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
