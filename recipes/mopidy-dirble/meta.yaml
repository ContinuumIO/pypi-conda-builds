package:
  name: mopidy-dirble
  version: "1.1.0"

source:
  fn: Mopidy-Dirble-1.1.0_1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Dirble/Mopidy-Dirble-1.1.0_1.tar.gz
  md5: 5314420b82ca51465e16f3c36f9c9196
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-dirble = mopidy-dirble:main
    #
    # Would create an entry point called mopidy-dirble that calls mopidy-dirble.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - pycountry
    - requests

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - pycountry
    - requests

test:
  # Python imports
  imports:
    - mopidy_dirble

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mopidy/mopidy-dirble
  license: Apache Software License
  summary: u'Mopidy extension for Dirble open radio directory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
