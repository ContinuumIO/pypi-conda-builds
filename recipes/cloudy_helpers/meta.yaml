package:
  name: cloudy_helpers
  version: "0.0.7"

source:
  fn: cloudy_helpers-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudy_helpers/cloudy_helpers-0.0.7.tar.gz
  md5: 256223d95520db8be80d591c99f6192b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudy_helpers = cloudy_helpers:main
    #
    # Would create an entry point called cloudy_helpers that calls cloudy_helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cloudy_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Stupeflix/cloudy_helpers
  license: MIT
  summary: 'Cloudy helpers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
