package:
  name: fulcrum
  version: "1.4.2"

source:
  fn: fulcrum-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fulcrum/fulcrum-1.4.2.tar.gz
  md5: fde60d77a3f3b868a8929e9a7f1ae558
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fulcrum = fulcrum:main
    #
    # Would create an entry point called fulcrum that calls fulcrum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0

  run:
    - python
    - requests >=2.1.0

test:
  # Python imports
  imports:
    - fulcrum
    - fulcrum.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fulcrumapp/fulcrum-python
  license: Apache License
  summary: 'A python wrapper for the Fulcrum API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
