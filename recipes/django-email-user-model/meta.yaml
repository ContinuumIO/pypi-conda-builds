package:
  name: django-email-user-model
  version: "0.1.4"

source:
  fn: django-email-user-model-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-user-model/django-email-user-model-0.1.4.tar.gz
  md5: 6a88baa949608678b944efd2ccd445e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-user-model = django-email-user-model:main
    #
    # Would create an entry point called django-email-user-model that calls django-email-user-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_email_user_model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Django custom user model that enable E-Mail as username'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
