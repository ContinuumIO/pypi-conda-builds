package:
  name: isotoma.egglayer
  version: "0.0.5"

source:
  fn: isotoma.egglayer-0.0.5.zip
  url: https://pypi.python.org/packages/source/i/isotoma.egglayer/isotoma.egglayer-0.0.5.zip
  md5: 15f56fcd593c1cb3d1d3607adf554ec2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.egglayer = isotoma.egglayer:main
    #
    # Would create an entry point called isotoma.egglayer that calls isotoma.egglayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - setuptools
    - jinja2

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.egglayer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.egglayer
  license: Apache Software License
  summary: 'Utility for packaging things as eggs without setuptools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
