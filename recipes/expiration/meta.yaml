package:
  name: expiration
  version: "0.1"

source:
  fn: expiration-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/expiration/expiration-0.1.tar.gz
  md5: 9df8291bf9cbc258a4149686f4cbc2b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expiration = expiration:main
    #
    # Would create an entry point called expiration that calls expiration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - expiration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ericvsmith/expiration
  license: Apache Software License
  summary: 'Manages complex expiration rules.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
