package:
  name: django-hitcount-headless
  version: "0.2"

source:
  fn: django-hitcount-headless-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hitcount-headless/django-hitcount-headless-0.2.tar.gz
  md5: 93c6c36f791642b314e739c97245e0d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hitcount-headless = django-hitcount-headless:main
    #
    # Would create an entry point called django-hitcount-headless that calls django-hitcount-headless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hitcount

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bashu/django-hitcount-headless
  license: BSD License
  summary: 'Django hit counter application that tracks the number of hits/views for chosen objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
