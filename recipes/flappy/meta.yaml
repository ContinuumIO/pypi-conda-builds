package:
  name: flappy
  version: "0.3.7"

source:
  fn: Flappy-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/F/Flappy/Flappy-0.3.7.tar.gz
  md5: 25a49450b05dd7ba61a6df72f61e4332
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flappy = flappy:main
    #
    # Would create an entry point called flappy that calls flappy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flappy
    - flappy.display
    - flappy.display3d
    - flappy.events
    - flappy.geom
    - flappy.text
    - flappy.ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pyronimous/flappy
  license: MIT License
  summary: 'Multimedia library with the Adobe Flash-like API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
