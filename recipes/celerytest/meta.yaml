package:
  name: celerytest
  version: "0.1.1"

source:
  fn: celerytest-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/celerytest/celerytest-0.1.1.tar.gz
  md5: 91ab4ebeb84adf0bbf9d0967c05872fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celerytest = celerytest:main
    #
    # Would create an entry point called celerytest that calls celerytest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery ==3.0.19

  run:
    - python
    - celery ==3.0.19

test:
  # Python imports
  imports:
    - celerytest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/RentMethod/celerytest
  license: BSD license, see LICENSE
  summary: 'Run a monitored Celery worker for integration tests that depend on Celery tasks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
