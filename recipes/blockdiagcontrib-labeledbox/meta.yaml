package:
  name: blockdiagcontrib-labeledbox
  version: "0.1.1"

source:
  fn: blockdiagcontrib-labeledbox-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-labeledbox/blockdiagcontrib-labeledbox-0.1.1.tar.gz
  md5: e4f05246431697dc6597171fde246dea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-labeledbox = blockdiagcontrib-labeledbox:main
    #
    # Would create an entry point called blockdiagcontrib-labeledbox that calls blockdiagcontrib-labeledbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.1.8
    - setuptools

  run:
    - python
    - setuptools
    - blockdiag >=1.1.8
    - setuptools

test:
  # Python imports
  imports:
    - blockdiagcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tk0miya.bitbucket.org/blockdiag/build/html/index.html
  license: Python Software Foundation License
  summary: 'noderenderer plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
