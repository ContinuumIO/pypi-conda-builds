package:
  name: flask-test
  version: "0.1.5"

source:
  fn: Flask-Test-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Test/Flask-Test-0.1.5.tar.gz
  md5: 813fdd1bff34da4459aa358267f92500
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-test = flask-test:main
    #
    # Would create an entry point called flask-test that calls flask-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7
    - sqlalchemy ==0.7.8

  run:
    - python
    - flask >=0.7
    - sqlalchemy ==0.7.8

test:
  # Python imports
  imports:
    - flask_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kvesteri/flask-test
  license: BSD License
  summary: 'Various unit testing helpers for Flask applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
