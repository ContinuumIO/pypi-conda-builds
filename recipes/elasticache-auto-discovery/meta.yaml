package:
  name: elasticache-auto-discovery
  version: "0.0.5"

source:
  fn: elasticache-auto-discovery-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/e/elasticache-auto-discovery/elasticache-auto-discovery-0.0.5.tar.gz
  md5: eb2de9abee7ad668fb6a4ff4838c825a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticache-auto-discovery = elasticache-auto-discovery:main
    #
    # Would create an entry point called elasticache-auto-discovery that calls elasticache-auto-discovery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - elasticache_auto_discovery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/studio-ousia/elasticache-auto-discovery
  license: Apache Software License
  summary: 'AWS ElastiCache Auto Discovery Client for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
