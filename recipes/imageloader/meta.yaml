package:
  name: imageloader
  version: "0.0.5"

source:
  fn: imageloader-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/i/imageloader/imageloader-0.0.5.tar.gz
  md5: f857d5571ae9a559363b83f7055f0a5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imageloader = imageloader:main
    #
    # Would create an entry point called imageloader that calls imageloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - imageloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saitoha/python-imageloader
  license: GNU General Public License (GPL)
  summary: 'An image loader library which provides a subset of PIL interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
