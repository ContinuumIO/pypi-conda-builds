package:
  name: doit-cmd
  version: "0.1.0"

source:
  fn: doit-cmd-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/doit-cmd/doit-cmd-0.1.0.tar.gz
  md5: a1b1d92fc4dfc6f80224c7ea5142eb67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doit-cmd = doit-cmd:main
    #
    # Would create an entry point called doit-cmd that calls doit-cmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - doit

  run:
    - python
    - doit

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pydoit/doit-cmd/
  license: MIT License
  summary: 'Helper to create doit tasks that execute a command'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
