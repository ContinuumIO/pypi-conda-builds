package:
  name: deform_ext_autocomplete
  version: "0.1"

source:
  fn: deform_ext_autocomplete-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/deform_ext_autocomplete/deform_ext_autocomplete-0.1.tar.gz
  md5: d9676e370c9646b64e7948915a467c22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deform_ext_autocomplete = deform_ext_autocomplete:main
    #
    # Would create an entry point called deform_ext_autocomplete that calls deform_ext_autocomplete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - deform

  run:
    - python
    - deform

test:
  # Python imports
  imports:
    - deform_ext_autocomplete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://readthedocs.org/projects/deform_ext_autocomplete/
  license: BSD License
  summary: 'This package provides a Deform autocomplete widget that stores a value that may be different from the one shown to the user.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
