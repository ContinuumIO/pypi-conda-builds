package:
  name: flask-sendmail
  version: "0.1"

source:
  fn: Flask-Sendmail-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Sendmail/Flask-Sendmail-0.1.tar.gz
  md5: 2aa8b155bbc4d11e39e78e898ed65177
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sendmail = flask-sendmail:main
    #
    # Would create an entry point called flask-sendmail that calls flask-sendmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_sendmail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ajford/flask-sendmail
  license: BSD License
  summary: 'Flask extension for sendmail'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
