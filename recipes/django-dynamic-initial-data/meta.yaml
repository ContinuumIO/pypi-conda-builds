package:
  name: django-dynamic-initial-data
  version: "0.4.1"

source:
  fn: django-dynamic-initial-data-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamic-initial-data/django-dynamic-initial-data-0.4.1.tar.gz
  md5: 45c3fe8c6b2e8cc096942513fec6a59e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-initial-data = django-dynamic-initial-data:main
    #
    # Would create an entry point called django-dynamic-initial-data that calls django-dynamic-initial-data.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-manager-utils >=0.8.2

  run:
    - python
    - django >=1.7
    - django-manager-utils >=0.8.2

test:
  # Python imports
  imports:
    - dynamic_initial_data
    - dynamic_initial_data.management
    - dynamic_initial_data.management.commands
    - dynamic_initial_data.migrations
    - dynamic_initial_data.tests
    - dynamic_initial_data.tests.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-dynamic-initial-data
  license: MIT License
  summary: 'Dynamic initial data fixtures for Django apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
