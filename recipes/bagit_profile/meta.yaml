package:
  name: bagit_profile
  version: "1.0.2"

source:
  fn: bagit_profile-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bagit_profile/bagit_profile-1.0.2.tar.gz
  md5: 195b36a3b9d7db9bf834369210f4ca05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bagit_profile = bagit_profile:main
    #
    # Would create an entry point called bagit_profile that calls bagit_profile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bagit
    - requests

  run:
    - python
    - bagit
    - requests

test:
  # Python imports
  imports:
    - bagit_profile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ruebot/bagit-profiles-validator
  license: CC0
  summary: '\n    This module can be used to validate BagitProfiles.\n    '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
