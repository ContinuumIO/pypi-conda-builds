package:
  name: bibliograph.core
  version: "1.0.3"

source:
  fn: bibliograph.core-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bibliograph.core/bibliograph.core-1.0.3.tar.gz
  md5: b515150447f97567db3622fd5337b846
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bibliograph.core = bibliograph.core:main
    #
    # Would create an entry point called bibliograph.core that calls bibliograph.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - bibliograph
    - bibliograph.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bibliograph.core
  license: Zope Public License
  summary: 'Core definitions of bibliograph packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
