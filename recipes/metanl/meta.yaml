package:
  name: metanl
  version: "1.0c"

source:
  fn: metanl-1.0c.tar.gz
  url: https://pypi.python.org/packages/source/m/metanl/metanl-1.0c.tar.gz
  md5: 5910af54936c18c47d11ad5e7c61783d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metanl = metanl:main
    #
    # Would create an entry point called metanl that calls metanl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nltk
    - ftfy >=3

  run:
    - python
    - nltk
    - ftfy >=3

test:
  # Python imports
  imports:
    - metanl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/commonsense/metanl/
  license: MIT License
  summary: 'Multilingual natural language tools, wrapping NLTK and other systems.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
