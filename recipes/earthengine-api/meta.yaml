package:
  name: earthengine-api
  version: "0.1.53"

source:
  fn: earthengine-api-0.1.53.tar.gz
  url: https://pypi.python.org/packages/source/e/earthengine-api/earthengine-api-0.1.53.tar.gz
  md5: 867e59bd860df8cbf5f3bb162f2e3a59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - earthengine-api = earthengine-api:main
    #
    # Would create an entry point called earthengine-api that calls earthengine-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client
    - pyopenssl >=0.11

  run:
    - python
    - google-api-python-client
    - pyopenssl >=0.11

test:
  # Python imports
  imports:
    - ee

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/earthengine-api/
  license: UNKNOWN
  summary: 'Earth Engine Python API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
