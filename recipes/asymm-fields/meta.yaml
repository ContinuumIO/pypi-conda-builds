package:
  name: asymm-fields
  version: "0.2.0-201405201336"

source:
  fn: asymm-fields-0.2.0-201405201336.tar.gz
  url: https://pypi.python.org/packages/source/a/asymm-fields/asymm-fields-0.2.0-201405201336.tar.gz
  md5: 42d57f79b97ac2991faa45d1732d8d48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asymm-fields = asymm-fields:main
    #
    # Would create an entry point called asymm-fields that calls asymm-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz

  run:
    - python
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz

test:
  # Python imports
  imports:
    - asymm_fields
    - asymm_fields.fields
    - asymm_fields.tests
    - asymm_fields.tests.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AsymmetricVentures/asym-fields
  license: GNU General Public License v2 (GPLv2)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
