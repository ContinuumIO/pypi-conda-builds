package:
  name: ponderosity
  version: "0.1"

source:
  fn: ponderosity-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/ponderosity/ponderosity-0.1.tar.gz
  md5: 087bcbb6a99453eb10e857354b3c435f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ponderosity = ponderosity:main
    #
    # Would create an entry point called ponderosity that calls ponderosity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ponderosity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Points system for nutrition and weight loss.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
