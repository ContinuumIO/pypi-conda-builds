package:
  name: ldapper
  version: "0.8.4"

source:
  fn: ldapper-0.8.4.tar.gz
  url: https://pypi.python.org/packages/source/l/ldapper/ldapper-0.8.4.tar.gz
  md5: e72e334f13741fadba8f5f11fe892ee3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldapper = ldapper:main
    #
    # Would create an entry point called ldapper that calls ldapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap ==2.4.10

  run:
    - python
    - python-ldap ==2.4.10

test:
  # Python imports
  imports:
    - ldapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nrocco.github.io/
  license: LdapPlugin
  summary: 'A python library that wraps the ldap module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
