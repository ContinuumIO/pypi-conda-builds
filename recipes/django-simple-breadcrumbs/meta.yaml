package:
  name: django-simple-breadcrumbs
  version: "0.2"

source:
  fn: django-simple-breadcrumbs-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-breadcrumbs/django-simple-breadcrumbs-0.2.tar.gz
  md5: 40f30e0cff64028ce6dab972136ed344
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-breadcrumbs = django-simple-breadcrumbs:main
    #
    # Would create an entry point called django-simple-breadcrumbs that calls django-simple-breadcrumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - breadcrumbs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/Mathiasdm/django-simple-breadcrumbs
  license: BSD License
  summary: 'A simple Django app to use breadcrumbs-based navigation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
