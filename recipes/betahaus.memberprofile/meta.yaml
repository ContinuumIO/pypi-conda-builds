package:
  name: betahaus.memberprofile
  version: "0.2rc1"

source:
  fn: betahaus.memberprofile-0.2rc1.zip
  url: https://pypi.python.org/packages/source/b/betahaus.memberprofile/betahaus.memberprofile-0.2rc1.zip
  md5: e875bc1f2060a113447819f98b9b9b27
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betahaus.memberprofile = betahaus.memberprofile:main
    #
    # Would create an entry point called betahaus.memberprofile that calls betahaus.memberprofile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.memberdatastorage
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.memberdatastorage
    - plone

test:
  # Python imports
  imports:
    - betahaus
    - betahaus.memberprofile
    - betahaus.memberprofile.browser
    - betahaus.memberprofile.content
    - betahaus.memberprofile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.betahaus.net
  license: GPL
  summary: 'Flexible member profiles for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
