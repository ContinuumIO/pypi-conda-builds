package:
  name: django-multiblogs
  version: "0.1.4"

source:
  fn: django-multiblogs-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multiblogs/django-multiblogs-0.1.4.tar.gz
  md5: 7f7091b051896d25b8837f56dda037b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multiblogs = django-multiblogs:main
    #
    # Would create an entry point called django-multiblogs that calls django-multiblogs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - multiblogs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-multiblogs
  license: BSD License
  summary: 'An application for managing multiple blogs, and even *gasp* blog "sets".'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
