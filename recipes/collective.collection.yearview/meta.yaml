package:
  name: collective.collection.yearview
  version: "1.0"

source:
  fn: collective.collection.yearview-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collection.yearview/collective.collection.yearview-1.0.tar.gz
  md5: dabf6a3452081d6035aa344d5d2788ac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collection.yearview = collective.collection.yearview:main
    #
    # Would create an entry point called collective.collection.yearview that calls collective.collection.yearview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.collection
    - collective.collection.yearview
    - collective.collection.yearview.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.twinapex.com
  license: GPL
  summary: 'Filter by year display for collection listings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
