package:
  name: monocle-reviews
  version: "0.3.0.0"

source:
  fn: monocle_reviews-0.3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle-reviews/monocle_reviews-0.3.0.0.tar.gz
  md5: a4e73a36749199f7c231af0f6e85252b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle-reviews = monocle-reviews:main
    #
    # Would create an entry point called monocle-reviews that calls monocle-reviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - easy_thumbnails

  run:
    - python
    - pillow
    - easy_thumbnails

test:
  # Python imports
  imports:
    - monocle_reviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.example.com/
  license: BSD License
  summary: 'Customers reviews app for monocle project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
