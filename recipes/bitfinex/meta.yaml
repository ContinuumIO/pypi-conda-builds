package:
  name: bitfinex
  version: "0.2.6"

source:
  fn: bitfinex-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/b/bitfinex/bitfinex-0.2.6.tar.gz
  md5: 32ce0d126bc8323f9d16dc04e27c1f38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitfinex = bitfinex:main
    #
    # Would create an entry point called bitfinex that calls bitfinex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - httpretty

  run:
    - python
    - requests
    - httpretty

test:
  # Python imports
  imports:
    - bitfinex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/scottjbarr/bitfinex
  license: MIT
  summary: 'Python client for the Bitfinex API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
