package:
  name: django-carton
  version: "1.2.0"

source:
  fn: django-carton-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-carton/django-carton-1.2.0.tar.gz
  md5: a4ed33ac41c8a3d50cae29bfb0977d73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-carton = django-carton:main
    #
    # Would create an entry point called django-carton that calls django-carton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - carton
    - carton.templatetags
    - carton.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lazybird/django-carton/
  license: Creative Commons Attribution 3.0 Unported
  summary: 'django-carton is a simple and lightweight application for shopping carts and wish lists.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
