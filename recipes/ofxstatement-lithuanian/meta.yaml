package:
  name: ofxstatement-lithuanian
  version: "1.0.2"

source:
  fn: ofxstatement-lithuanian-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/o/ofxstatement-lithuanian/ofxstatement-lithuanian-1.0.2.tar.gz
  md5: c99dd756a036a63d4ef6a06b5796c0ef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ofxstatement-lithuanian = ofxstatement-lithuanian:main
    #
    # Would create an entry point called ofxstatement-lithuanian that calls ofxstatement-lithuanian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ofxstatement

  run:
    - python
    - setuptools
    - ofxstatement

test:
  # Python imports
  imports:
    - ofxstatement
    - ofxstatement.plugins
    - ofxstatement.plugins.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kedder/ofxstatement-lithuanian
  license: GNU Affero General Public License v3
  summary: 'Statement parsers for banks, operatiing in Lithuania'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
