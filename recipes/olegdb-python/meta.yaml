package:
  name: olegdb-python
  version: "0.1.4"

source:
  fn: olegdb-python-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/o/olegdb-python/olegdb-python-0.1.4.tar.gz
  md5: 22d98d6b4ba347c0ead5104ba638abb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - olegdb-python = olegdb-python:main
    #
    # Would create an entry point called olegdb-python that calls olegdb-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - msgpack-python >=0.4.1

  run:
    - python
    - requests
    - msgpack-python >=0.4.1

test:
  # Python imports
  imports:
    - olegdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.kyoto.io/qpfiffer/olegdb-python
  license: BSD
  summary: 'Python wrappers for generic OlegDB methods.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
