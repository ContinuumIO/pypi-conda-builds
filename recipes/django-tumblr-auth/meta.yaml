package:
  name: django-tumblr-auth
  version: "0.0.1"

source:
  fn: django-tumblr-auth-0.0.1.tar.bz2
  url: https://pypi.python.org/packages/source/d/django-tumblr-auth/django-tumblr-auth-0.0.1.tar.bz2
  md5: e62bc04edf08e6587aa19657052a4ac4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tumblr-auth = django-tumblr-auth:main
    #
    # Would create an entry point called django-tumblr-auth that calls django-tumblr-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-social-auth >=0.3.3
    - oauth >=1.0.1

  run:
    - python
    - django-social-auth >=0.3.3
    - oauth >=1.0.1

test:
  # Python imports
  imports:
    - tumblr_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alageek/django-tumblr-auth
  license: BSD License
  summary: u'django-tumblr-auth is an extension to django-social-auth which adds a backend for Tumblr.com.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
