package:
  name: django-simple-templates
  version: "0.6.1"

source:
  fn: django-simple-templates-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-templates/django-simple-templates-0.6.1.tar.gz
  md5: 48b764b22a155c91b2cced8c3036ba91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-templates = django-simple-templates:main
    #
    # Would create an entry point called django-simple-templates that calls django-simple-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simple_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jaddison/django-simple-templates
  license: BSD License
  summary: 'Easy, designer-friendly templates and A/B testing friendly tools for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
