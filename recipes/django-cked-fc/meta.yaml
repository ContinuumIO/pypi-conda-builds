package:
  name: django-cked-fc
  version: "0.1.1"

source:
  fn: django-cked-fc-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-cked-fc/django-cked-fc-0.1.1.zip
  md5: 64164077ddb849fdbf96b5c83beac8fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cked-fc = django-cked-fc:main
    #
    # Would create an entry point called django-cked-fc that calls django-cked-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytils

  run:
    - python
    - pytils

test:
  # Python imports
  imports:
    - cked

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-cked
  license: BSD
  summary: 'CKEditor and elFinder integration for Django Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
