package:
  name: django-configuration
  version: "0.0.3"

source:
  fn: django-configuration-0.0.3.zip
  url: https://pypi.python.org/packages/source/d/django-configuration/django-configuration-0.0.3.zip
  md5: 12d4a13d6b3c6473cab17887e0c6ddef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-configuration = django-configuration:main
    #
    # Would create an entry point called django-configuration that calls django-configuration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-polymorphic

  run:
    - python
    - django
    - django-polymorphic

test:
  # Python imports
  imports:
    - configuration
    - configuration.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fah-designs/django-configuration
  license: MIT
  summary: 'Configuration admin/models for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
