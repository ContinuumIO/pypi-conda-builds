package:
  name: collective.localrolesdatatables
  version: "1.0.1"

source:
  fn: collective.localrolesdatatables-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.localrolesdatatables/collective.localrolesdatatables-1.0.1.zip
  md5: e8359c2c781f2af00fc9796cfa8faaf2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.localrolesdatatables = collective.localrolesdatatables:main
    #
    # Would create an entry point called collective.localrolesdatatables that calls collective.localrolesdatatables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.datatables

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.datatables

test:
  # Python imports
  imports:
    - collective
    - collective.localrolesdatatables
    - collective.localrolesdatatables.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.localrolesdatatables
  license: GPL
  summary: 'View / export localroles of all or parts of the site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
