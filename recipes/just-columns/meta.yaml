package:
  name: just-columns
  version: "0.0.1"

source:
  fn: just-columns-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/just-columns/just-columns-0.0.1.tar.gz
  md5: 0361ce917277524be161fb2fe2e844ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - just-columns = just-columns:main
    #
    # Would create an entry point called just-columns that calls just-columns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=2.2.0

  run:
    - python
    - tornado >=2.2.0

test:
  # Python imports
  imports:
    - just-columns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eeue56/just-columns
  license: jusText
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
