package:
  name: django-model-publisher
  version: "0.1.2"

source:
  fn: django-model-publisher-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-publisher/django-model-publisher-0.1.2.tar.gz
  md5: 1d46159f4353282ed41d2fd31b252ca2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-publisher = django-model-publisher:main
    #
    # Would create an entry point called django-model-publisher that calls django-model-publisher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - wheel ==0.23.0
    - django-model-utils ==2.0.3

  run:
    - python
    - django >=1.4.3
    - wheel ==0.23.0
    - django-model-utils ==2.0.3

test:
  # Python imports
  imports:
    - publisher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jp74/django-model-publisher
  license: BSD License
  summary: 'Handy mixin/abstract class for providing a "publisher workflow" to arbitrary Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
