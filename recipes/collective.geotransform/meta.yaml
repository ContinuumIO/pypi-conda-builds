package:
  name: collective.geotransform
  version: "1.0.1"

source:
  fn: collective.geotransform-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.geotransform/collective.geotransform-1.0.1.zip
  md5: 2e47f2168462e69e2ecb875cd68b16d5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geotransform = collective.geotransform:main
    #
    # Would create an entry point called collective.geotransform that calls collective.geotransform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.api
    - plone.transformchain
    - beautifulsoup4

  run:
    - python
    - setuptools
    - setuptools
    - plone.api
    - plone.transformchain
    - beautifulsoup4

test:
  # Python imports
  imports:
    - collective
    - collective.geotransform
    - collective.geotransform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.geotransform
  license: GPL
  summary: 'Gracefully email obfuscation for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
