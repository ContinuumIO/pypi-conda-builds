package:
  name: cubicweb-localperms
  version: "0.3.0"

source:
  fn: cubicweb-localperms-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-localperms/cubicweb-localperms-0.3.0.tar.gz
  md5: 8ef37be0af5dbef5d6df32291d0bd229
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-localperms = cubicweb-localperms:main
    #
    # Would create an entry point called cubicweb-localperms that calls cubicweb-localperms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.0

  run:
    - python
    - cubicweb >=3.16.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-localperms
  license: LGPL
  summary: 'allow definition of local permissions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
