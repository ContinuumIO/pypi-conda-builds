package:
  name: burlap
  version: "0.3.5"

source:
  fn: burlap-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/b/burlap/burlap-0.3.5.tar.gz
  md5: c1036832d22d435e46cee7e54c35a834
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - burlap = burlap:main
    #
    # Would create an entry point called burlap that calls burlap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.8.2
    - pyyaml >=3.11
    - feedparser >=5.1.3
    - pytz >=2014.4
    - python-dateutil >=2.2
    - lockfile >=0.9.1

  run:
    - python
    - fabric >=1.8.2
    - pyyaml >=3.11
    - feedparser >=5.1.3
    - pytz >=2014.4
    - python-dateutil >=2.2
    - lockfile >=0.9.1

test:
  # Python imports
  imports:
    - burlap
    - burlap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisspen/burlap
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Fabric commands for simplifying server deployments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
