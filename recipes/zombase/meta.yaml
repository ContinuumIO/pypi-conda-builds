package:
  name: zombase
  version: "0.4.8"

source:
  fn: zombase-0.4.8.tar.gz
  url: https://pypi.python.org/packages/source/z/zombase/zombase-0.4.8.tar.gz
  md5: fea5748456a38e1536fd8cbd0b1ada1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zombase = zombase:main
    #
    # Would create an entry point called zombase that calls zombase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9
    - voluptuous >=0.8
    - dogpile.cache >=0.5
    - six >=1.7

  run:
    - python
    - sqlalchemy >=0.9
    - voluptuous >=0.8
    - dogpile.cache >=0.5
    - six >=1.7

test:
  # Python imports
  imports:
    - zombase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'A toolset to build beautiful data layer APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
