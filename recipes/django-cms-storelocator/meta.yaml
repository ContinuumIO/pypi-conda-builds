package:
  name: django-cms-storelocator
  version: "1.10.0"

source:
  fn: django-cms-storelocator-1.10.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-storelocator/django-cms-storelocator-1.10.0.tar.gz
  md5: 74936980f9e9f8d15db1a8dcc6c23b61
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-storelocator = django-cms-storelocator:main
    #
    # Would create an entry point called django-cms-storelocator that calls django-cms-storelocator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-cms

  run:
    - python
    - setuptools
    - django
    - django-cms

test:
  # Python imports
  imports:
    - store_locator
    - store_locator.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/megamark16/django-cms-storelocator
  license: BSD License
  summary: 'A store locator extension for Django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
