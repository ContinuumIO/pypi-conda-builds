package:
  name: zodbcode
  version: "3.4.0"

source:
  fn: zodbcode-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zodbcode/zodbcode-3.4.0.tar.gz
  md5: 9b128f89aa2a2117fae4f74757eefeff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zodbcode = zodbcode:main
    #
    # Would create an entry point called zodbcode that calls zodbcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.interface

  run:
    - python
    - zodb3
    - zope.interface

test:
  # Python imports
  imports:
    - zodbcode
    - zodbcode.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zodbcode
  license: Zope Public License
  summary: 'Allows Python code to live in the ZODB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
