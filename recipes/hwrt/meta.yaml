package:
  name: hwrt
  version: "0.1.218"

source:
  fn: hwrt-0.1.218.tar.gz
  url: https://pypi.python.org/packages/source/h/hwrt/hwrt-0.1.218.tar.gz
  md5: 0f945b04681cada964de2f0840dccd7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hwrt = hwrt:main
    #
    # Would create an entry point called hwrt that calls hwrt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - theano
    - nose
    - natsort
    - pyyaml
    - matplotlib
    - nntoolkit
    - h5py
    - flask
    - flask-bootstrap
    - requests
    - six

  run:
    - python
    - argparse
    - theano
    - nose
    - natsort
    - pyyaml
    - matplotlib
    - nntoolkit
    - h5py
    - flask
    - flask-bootstrap
    - requests
    - six

test:
  # Python imports
  imports:
    - hwrt
    - hwrt.datasets
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MartinThoma/hwrt
  license: MIT License
  summary: 'Handwriting Recognition Tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
