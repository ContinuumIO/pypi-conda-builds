package:
  name: butler
  version: "0.3.0"

source:
  fn: butler-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/butler/butler-0.3.0.tar.gz
  md5: c951796a8285666aa09fe2e60f2056ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - butler = butler:main
    #
    # Would create an entry point called butler that calls butler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq ==2.2.0.1
    - gevent

  run:
    - python
    - pyzmq ==2.2.0.1
    - gevent

test:
  # Python imports
  imports:
    - butler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/140am/butler
  license: MIT License
  summary: '0MQ based Service Oriented RPC Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
