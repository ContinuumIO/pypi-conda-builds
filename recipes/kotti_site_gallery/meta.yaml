package:
  name: kotti_site_gallery
  version: "0.2.0"

source:
  fn: kotti_site_gallery-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_site_gallery/kotti_site_gallery-0.2.0.tar.gz
  md5: 5af6af81f9b186ea41670226f75e7e95
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_site_gallery = kotti_site_gallery:main
    #
    # Would create an entry point called kotti_site_gallery that calls kotti_site_gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.10b1

  run:
    - python
    - setuptools
    - kotti >=0.10b1

test:
  # Python imports
  imports:
    - kotti_site_gallery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/kotti_site_gallery
  license: BSD License
  summary: 'Site gallery for Kotti sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
