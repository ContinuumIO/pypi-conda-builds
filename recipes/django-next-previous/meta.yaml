package:
  name: django-next-previous
  version: "0.1"

source:
  fn: django-next-previous-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-next-previous/django-next-previous-0.1.tar.gz
  md5: ac6fb591bc7d157a1e860dcd77e5463f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-next-previous = django-next-previous:main
    #
    # Would create an entry point called django-next-previous that calls django-next-previous.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - next_previous

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.playfire.com/
  license: BSD
  summary: "Django model mixin for easy retrieval of the 'next' and 'previous' objects."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
