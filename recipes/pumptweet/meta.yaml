package:
  name: pumptweet
  version: "1.5.0"

source:
  fn: pumptweet-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pumptweet/pumptweet-1.5.0.tar.gz
  md5: 335ef367204c9a04102d2b72ad2944b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pumptweet = pumptweet:main
    #
    # Would create an entry point called pumptweet that calls pumptweet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pypump >=0.5
    - python-twitter >=2.0
    - beautifulsoup >=3.2.1

  run:
    - python
    - pypump >=0.5
    - python-twitter >=2.0
    - beautifulsoup >=3.2.1

test:
  # Python imports
  imports:
    - pumptweet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dper/pumptweet
  license: MIT License
  summary: 'Cross posts from Pump.io to Twitter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
