package:
  name: collective.folderorder
  version: "1.4.1"

source:
  fn: collective.folderorder-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.folderorder/collective.folderorder-1.4.1.tar.gz
  md5: 89035d16c9ec1f7bad65b0759ad955a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.folderorder = collective.folderorder:main
    #
    # Would create an entry point called collective.folderorder that calls collective.folderorder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - products.statusmessages
    - plone.folder
    - yafowil
    - yafowil.plone
    - yafowil.yaml
    - zope.component
    - zope.i18nmessageid
    - zope2

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - products.statusmessages
    - plone.folder
    - yafowil
    - yafowil.plone
    - yafowil.yaml
    - zope.component
    - zope.i18nmessageid
    - zope2

test:
  # Python imports
  imports:
    - collective
    - collective.folderorder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.folderorder
  license: GNU General Public License (GPL)
  summary: 'Set sort order of plone folders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
