package:
  name: django-bootstrap3-datetimepicker-timepicker
  version: "2.4"

source:
  fn: django-bootstrap3-datetimepicker-timepicker-2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap3-datetimepicker-timepicker/django-bootstrap3-datetimepicker-timepicker-2.4.tar.gz
  md5: cd74461cb9af6ac66bc28bf8f284957a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap3-datetimepicker-timepicker = django-bootstrap3-datetimepicker-timepicker:main
    #
    # Would create an entry point called django-bootstrap3-datetimepicker-timepicker that calls django-bootstrap3-datetimepicker-timepicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap3_datetime_time

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joshnroy/django-bootstrap3-datetimepicker
  license: Apache Software License
  summary: 'Bootstrap3 compatible datetimepicker and timepicker for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
