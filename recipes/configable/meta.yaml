package:
  name: configable
  version: "0.1.10"

source:
  fn: configable-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/c/configable/configable-0.1.10.tar.gz
  md5: b787360f788c75858b9f90ef978561e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configable = configable:main
    #
    # Would create an entry point called configable that calls configable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - configable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bauerca/configable
  license: MIT
  summary: u"Don't repeat yourself. Configure everything."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
