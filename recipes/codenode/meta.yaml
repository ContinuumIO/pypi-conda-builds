package:
  name: codenode
  version: "0.02"

source:
  fn: codenode-0.02.tar.gz
  url: https://pypi.python.org/packages/source/c/codenode/codenode-0.02.tar.gz
  md5: 6d77c985ab31908305fadee42ed6c47a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codenode = codenode:main
    #
    # Would create an entry point called codenode that calls codenode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=9.0.0
    - django >=1.2.1
    - whoosh >=0.3.9
    - simplejson
    - django-compress

  run:
    - python
    - twisted >=9.0.0
    - django >=1.2.1
    - whoosh >=0.3.9
    - simplejson
    - django-compress

test:
  # Python imports
  imports:
    - codenode
    - codenode.backend
    - codenode.engine
    - codenode.engine.sage
    - codenode.external
    - codenode.external.mmaplotlib
    - codenode.frontend
    - codenode.frontend.async
    - codenode.frontend.backend
    - codenode.frontend.backend.fixtures
    - codenode.frontend.bookshelf
    - codenode.frontend.notebook
    - codenode.frontend.registration
    - codenode.frontend.registration.management
    - codenode.frontend.registration.management.commands
    - codenode.frontend.search
    - codenode.frontend.usersettings
    - codenode.frontend.util
    - codenode.management
    - codenode.printing
    - codenode.twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://codenode.org
  license: BSD
  summary: 'Interactive Programming Notebook for the Web Browser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
