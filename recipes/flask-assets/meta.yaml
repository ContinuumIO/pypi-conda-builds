package:
  name: flask-assets
  version: "0.10"

source:
  fn: Flask-Assets-0.10.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Assets/Flask-Assets-0.10.tar.gz
  md5: 897ae9d0e818ea9f6468ffed46afc173
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-assets = flask-assets:main
    #
    # Would create an entry point called flask-assets that calls flask-assets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - webassets >=0.10

  run:
    - python
    - flask >=0.8
    - webassets >=0.10

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/miracle2k/flask-assets
  license: BSD License
  summary: 'Asset management for Flask, to compress and merge CSS and Javascript files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
