package:
  name: browserid
  version: "1.0"

source:
  fn: browserid-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/browserid/browserid-1.0.tar.gz
  md5: 7843944ca78f625c236d9116c916243a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - browserid = browserid:main
    #
    # Would create an entry point called browserid that calls browserid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - browserid
    - browserid.pages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/bidpom
  license: MPL 2.0
  summary: 'Mozilla BrowserID (Persona) Page Object Model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
