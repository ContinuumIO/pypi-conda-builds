package:
  name: dwight
  version: "0.8.0"

source:
  fn: dwight-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dwight/dwight-0.8.0.tar.gz
  md5: 7d07b2ff154dcb1ee815bdc29b8bf75f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dwight = dwight:main
    #
    # Would create an entry point called dwight that calls dwight.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - userconfig
    - argparse
    - argh
    - django >=1.3

  run:
    - python
    - userconfig
    - argparse
    - argh
    - django >=1.3

test:
  # Python imports
  imports:
    - dwight

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fish2000/dwight/
  license: GNU General Public License (GPL)
  summary: 'Run Django management commands against arbitrary settings files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
