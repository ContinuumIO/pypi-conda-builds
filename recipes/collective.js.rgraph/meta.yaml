package:
  name: collective.js.rgraph
  version: "1.0.1"

source:
  fn: collective.js.rgraph-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.rgraph/collective.js.rgraph-1.0.1.zip
  md5: 1d78cba495acd81130387c6869d76fad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.rgraph = collective.js.rgraph:main
    #
    # Would create an entry point called collective.js.rgraph that calls collective.js.rgraph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.rgraph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.js.rgraph
  license: GPL
  summary: 'RGraph javascript library for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
