package:
  name: python-rex
  version: "0.4"

source:
  fn: python-rex-0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/python-rex/python-rex-0.4.tar.gz
  md5: 381ad5596284a9b276a6f25f351d5f21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-rex = python-rex:main
    #
    # Would create an entry point called python-rex that calls python-rex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cypreess/python-rex
  license: MIT License
  summary: 'Python regular expressions for humans'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
