package:
  name: django-stage
  version: "0.0.1"

source:
  fn: django-stage-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stage/django-stage-0.0.1.tar.gz
  md5: ecc159468a1dde7b75facc33980cdb70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stage = django-stage:main
    #
    # Would create an entry point called django-stage that calls django-stage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_stage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kelonye/django-stage
  license: MIT License
  summary: 'Manage django stage settings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
