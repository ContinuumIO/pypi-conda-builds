package:
  name: genologics
  version: "0.2.1"

source:
  fn: genologics-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/genologics/genologics-0.2.1.tar.gz
  md5: 54cd63db31cf4e1b5f6b678195f7fd6e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genologics = genologics:main
    #
    # Would create an entry point called genologics that calls genologics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - setuptools
    - requests

test:
  # Python imports
  imports:
    - genologics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/scilifelab/genologics
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Python interface to the GenoLogics LIMS (Laboratory Information Management System) server via its REST API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
