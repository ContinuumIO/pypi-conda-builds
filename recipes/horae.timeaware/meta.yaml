package:
  name: horae.timeaware
  version: "1.0a1"

source:
  fn: horae.timeaware-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.timeaware/horae.timeaware-1.0a1.tar.gz
  md5: b111335a4175b8ac1a8e531ac62ccb2e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.timeaware = horae.timeaware:main
    #
    # Would create an entry point called horae.timeaware that calls horae.timeaware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - horae.core

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - horae.core

test:
  # Python imports
  imports:
    - horae
    - horae.timeaware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides basic datetime related tasks used by the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
