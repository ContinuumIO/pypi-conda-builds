package:
  name: datascience_tools
  version: "0.0.3"

source:
  fn: datascience_tools-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/datascience_tools/datascience_tools-0.0.3.tar.gz
  md5: 27db466df6433365b272c0a0915d9577
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datascience_tools = datascience_tools:main
    #
    # Would create an entry point called datascience_tools that calls datascience_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy ==1.9.1
    - scikit-learn ==0.15.2
    - scipy ==0.14.0

  run:
    - python
    - numpy ==1.9.1
    - scikit-learn ==0.15.2
    - scipy ==0.14.0

test:
  # Python imports
  imports:
    - datascience_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlshan/ml_workflow
  license: MIT
  summary: 'Tools for data science'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
