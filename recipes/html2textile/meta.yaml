package:
  name: html2textile
  version: "0.2"

source:
  fn: html2textile-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/html2textile/html2textile-0.2.tar.gz
  md5: 2d88289c421548d58f46224b763cc29f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html2textile = html2textile:main
    #
    # Would create an entry point called html2textile that calls html2textile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - html2textile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A WORKING converter for html -> textile, most of syntax and features is supported'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
