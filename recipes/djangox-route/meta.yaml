package:
  name: djangox-route
  version: "0.0.6"

source:
  fn: djangox-route-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/djangox-route/djangox-route-0.0.6.tar.gz
  md5: ff6d1e87dfa03072dabdcfa8bcf1ed1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangox-route = djangox-route:main
    #
    # Would create an entry point called djangox-route that calls djangox-route.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangox
    - djangox.route

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youngrok/djangox-route
  license: BSD License
  summary: 'Discover django views and register to urlconf by convention.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
