package:
  name: hyperion-dust
  version: "0.1.0"

source:
  fn: hyperion-dust-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hyperion-dust/hyperion-dust-0.1.0.tar.gz
  md5: 73fa7fcc4e1855c440b7a02b3a94ef4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyperion-dust = hyperion-dust:main
    #
    # Would create an entry point called hyperion-dust that calls hyperion-dust.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hyperion-rt.org
  license: BSD
  summary: 'Library of dust files for the Hyperion radiative transfer code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
