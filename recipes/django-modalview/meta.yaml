package:
  name: django-modalview
  version: "0.1.5"

source:
  fn: django-modalview-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modalview/django-modalview-0.1.5.tar.gz
  md5: d2ed61499803413b765ddd0210136cca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modalview = django-modalview:main
    #
    # Would create an entry point called django-modalview that calls django-modalview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_modalview
    - django_modalview.generic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/optiflows/django-modalview
  license: Apache Software License
  summary: 'Django app to add generic views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
