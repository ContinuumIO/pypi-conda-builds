package:
  name: plomino.leaflet
  version: "0.2dev"

source:
  fn: plomino.leaflet-0.2dev.tar.gz
  url: https://pypi.python.org/packages/source/p/plomino.leaflet/plomino.leaflet-0.2dev.tar.gz
  md5: 2716ecb3db19d5b63c81f54d8eb212d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plomino.leaflet = plomino.leaflet:main
    #
    # Would create an entry point called plomino.leaflet that calls plomino.leaflet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplomino
    - collective.js.leaflet

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplomino
    - collective.js.leaflet

test:
  # Python imports
  imports:
    - plomino
    - plomino.leaflet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plomino/plomino.leaflet
  license: GPL
  summary: 'Leaflet/Plomino integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
