package:
  name: logentries
  version: "0.7"

source:
  fn: Logentries-0.7.tar.gz
  url: https://pypi.python.org/packages/source/L/Logentries/Logentries-0.7.tar.gz
  md5: 6d8b7bf0ae89911787c811763ff2bc30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logentries = logentries:main
    #
    # Would create an entry point called logentries that calls logentries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - certifi

  run:
    - python
    - certifi

test:
  # Python imports
  imports:
    - logentries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Logentries/
  license: LICENSE.txt
  summary: 'Python Logger plugin to send logs to Logentries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
