package:
  name: quorabackup
  version: "0.1.8"

source:
  fn: quorabackup-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/q/quorabackup/quorabackup-0.1.8.tar.gz
  md5: 0158e8015aa6438c233c0b52e3fb526c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quorabackup = quorabackup:main
    #
    # Would create an entry point called quorabackup that calls quorabackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - beautifulsoup4 ==4.3.2
    - click ==2.4
    - feedparser ==5.1.3
    - pymongo ==2.7.2
    - quora ==0.1.6
    - requests ==2.3.0
    - wsgiref ==0.1.2

  run:
    - python
    - beautifulsoup4 ==4.3.2
    - click ==2.4
    - feedparser ==5.1.3
    - pymongo ==2.7.2
    - quora ==0.1.6
    - requests ==2.3.0
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - quorabackup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/quorabackup/
  license: LICENSE.txt
  summary: 'A module to back up data from Quora.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
