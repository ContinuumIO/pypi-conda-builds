package:
  name: biloba
  version: "1.2.2"

source:
  fn: biloba-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/biloba/biloba-1.2.2.tar.gz
  md5: 9d505ea2898d548dcf179059158737d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biloba = biloba:main
    #
    # Would create an entry point called biloba that calls biloba.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=1.0
    - logbook >=0.6.0

  run:
    - python
    - gevent >=1.0
    - logbook >=0.6.0

test:
  # Python imports
  imports:
    - biloba
    - biloba.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/njoyce/biloba
  license: UNKNOWN
  summary: 'Provides gevent primitives to orchestrate differentorthogonal servers and services together.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
