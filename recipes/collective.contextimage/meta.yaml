package:
  name: collective.contextimage
  version: "1.3.1"

source:
  fn: collective.contextimage-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contextimage/collective.contextimage-1.3.1.tar.gz
  md5: 1c49e74429c5a47d7283e375fcf58f97
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contextimage = collective.contextimage:main
    #
    # Would create an entry point called collective.contextimage that calls collective.contextimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.app.layout

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.app.layout

test:
  # Python imports
  imports:
    - collective
    - collective.contextimage
    - collective.contextimage.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contextimage
  license: GNU General Public License (GPL)
  summary: 'Context sensitive images and markup from extended plone content as viewlet, header, logo, footer, ...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
