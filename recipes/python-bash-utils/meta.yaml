package:
  name: python-bash-utils
  version: "0.1.0"

source:
  fn: Python-Bash-Utils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Python-Bash-Utils/Python-Bash-Utils-0.1.0.tar.gz
  md5: ed0276d8a4b6e9e3e1baa324d7fa9814
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-bash-utils = python-bash-utils:main
    #
    # Would create an entry point called python-bash-utils that calls python-bash-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bashutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tsantor/python-bash-utils
  license: MIT License
  summary: 'A package which allows making bash style command line scripts easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
