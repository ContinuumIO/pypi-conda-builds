package:
  name: django-ptree-mturk
  version: "1.0.1"

source:
  fn: django-ptree-mturk-1.0.1.zip
  url: https://pypi.python.org/packages/source/d/django-ptree-mturk/django-ptree-mturk-1.0.1.zip
  md5: 8cbfa65b89ba91efa38244b25ca5c3e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ptree-mturk = django-ptree-mturk:main
    #
    # Would create an entry point called django-ptree-mturk that calls django-ptree-mturk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - xmltodict

  run:
    - python
    - requests
    - xmltodict

test:
  # Python imports
  imports:
    - mturk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
