package:
  name: ductape
  version: "0.17.0"

source:
  fn: DuctApe-0.17.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DuctApe/DuctApe-0.17.0.tar.gz
  md5: 61fba36184318cb65444205548ce1cbd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ductape = ductape:main
    #
    # Would create an entry point called ductape that calls ductape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse >=1.1
    - biopython >=1.5
    - numpy
    - matplotlib >=1.1
    - multiprocessing
    - scipy
    - scikit-learn >=0.11
    - pyyaml
    - networkx

  run:
    - python
    - argparse >=1.1
    - biopython >=1.5
    - numpy
    - matplotlib >=1.1
    - multiprocessing
    - scipy
    - scikit-learn >=0.11
    - pyyaml
    - networkx

test:
  # Python imports
  imports:
    - ductape
    - ductape.common
    - ductape.genome
    - ductape.kegg
    - ductape.phenome
    - ductape.storage
    - ductape.storage.SQLite
    - ductape.storage.data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://combogenomics.github.com/DuctApe
  license: LICENSE.txt
  summary: 'Analyzing and linking genomics and phenomics experiments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
