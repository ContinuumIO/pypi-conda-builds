package:
  name: amqpqueue
  version: "0.4.2"

source:
  fn: AMQPQueue-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/A/AMQPQueue/AMQPQueue-0.4.2.tar.gz
  md5: 1756d3dec0c5104b7fc603cc4e1ee4fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amqpqueue = amqpqueue:main
    #
    # Would create an entry point called amqpqueue that calls amqpqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - amqplib
    - simplejson
    - httplib2

  run:
    - python
    - amqplib
    - simplejson
    - httplib2

test:
  # Python imports
  imports:
    - amqpqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'AMQPQueue - Python Queue interface for AMQP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
