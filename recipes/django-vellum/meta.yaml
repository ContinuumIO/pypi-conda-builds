package:
  name: django-vellum
  version: "1.4.2"

source:
  fn: django-vellum-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vellum/django-vellum-1.4.2.tar.gz
  md5: 081e518b31c26f64d2b34da42d9ab02b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vellum = django-vellum:main
    #
    # Would create an entry point called django-vellum that calls django-vellum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-inlineobjects
    - django-simplesearch
    - django-taggit
    - django-taggit-templatetags
    - django-markup
    - markdown

  run:
    - python
    - django-inlineobjects
    - django-simplesearch
    - django-taggit
    - django-taggit-templatetags
    - django-markup
    - markdown

test:
  # Python imports
  imports:
    - vellum
    - vellum.management
    - vellum.management.commands
    - vellum.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pigmonkey/django-vellum
  license: BSD License
  summary: 'A web log for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
