package:
  name: genapi
  version: "0.0.8"

source:
  fn: GenAPI-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/G/GenAPI/GenAPI-0.0.8.tar.gz
  md5: dbed68dc200198c8cf32a38abaf0fae6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genapi = genapi:main
    #
    # Would create an entry point called genapi that calls genapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-toolbelt >=0.3.0
    - slumber >=0.6.0
    - requests >=2.3.0
    - six

  run:
    - python
    - requests-toolbelt >=0.3.0
    - slumber >=0.6.0
    - requests >=2.3.0
    - six

test:
  # Python imports
  imports:
    - genapi
    - genapi.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/genialis/genesis-genapi/
  license: Proprietary software
  summary: 'A Python API for the Genesis platform.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
