package:
  name: authserverclient
  version: "0.0.3"

source:
  fn: AuthServerClient-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/A/AuthServerClient/AuthServerClient-0.0.3.tar.gz
  md5: 5e508f3effcb4552a24c6d693df9d530
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authserverclient = authserverclient:main
    #
    # Would create an entry point called authserverclient that calls authserverclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - auth_server_client
    - authserverclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AF83/auth_server_client_py
  license: GNU Affero General Public License v3
  summary: 'AuthServer client lib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
