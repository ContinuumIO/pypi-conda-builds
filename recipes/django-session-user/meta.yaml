package:
  name: django-session-user
  version: "0.1.1"

source:
  fn: django-session-user-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-session-user/django-session-user-0.1.1.tar.gz
  md5: 719e9a73586d1622189bd1857846f841
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-session-user = django-session-user:main
    #
    # Would create an entry point called django-session-user that calls django-session-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sessionuser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ericflo/django-session-user
  license: BSD License
  summary: "Stores the user's information in the session, reucing database queries by a lot."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
