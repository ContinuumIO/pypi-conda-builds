package:
  name: js.jquery_jqote2
  version: "0.9.7"

source:
  fn: js.jquery_jqote2-0.9.7.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_jqote2/js.jquery_jqote2-0.9.7.tar.gz
  md5: eb5a27e6f63b06d91680aebecd9718b0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_jqote2 = js.jquery_jqote2:main
    #
    # Would create an entry point called js.jquery_jqote2 that calls js.jquery_jqote2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_jqote2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jquery.jqote2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
