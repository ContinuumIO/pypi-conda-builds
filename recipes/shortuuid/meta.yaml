package:
  name: shortuuid
  version: "0.4.2"

source:
  fn: shortuuid-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/s/shortuuid/shortuuid-0.4.2.tar.gz
  md5: 142e3ae4e7cd32d41a71deb359db4cfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shortuuid = shortuuid:main
    #
    # Would create an entry point called shortuuid that calls shortuuid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - shortuuid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stochastic-technologies/shortuuid/
  license: BSD License
  summary: 'A generator library for concise, unambiguous and URL-safe UUIDs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
