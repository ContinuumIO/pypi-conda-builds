package:
  name: pyesprima
  version: "1.0.0"

source:
  fn: pyesprima-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyesprima/pyesprima-1.0.0.tar.gz
  md5: af6be6ca4bb22ecf80cd9f99475d14fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyesprima = pyesprima:main
    #
    # Would create an entry point called pyesprima that calls pyesprima.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyesprima

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/int3/pyesprima
  license: BSD License
  summary: 'Python port of Esprima, the Javascript parser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
