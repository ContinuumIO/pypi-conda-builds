package:
  name: openerp-web
  version: "7.0.406"

source:
  fn: openerp-web-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-web/openerp-web-7.0.406.tar.gz
  md5: eea3e5330f7b9833f18e7c602332171b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-web = openerp-web:main
    #
    # Would create an entry point called openerp-web that calls openerp-web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock

  run:
    - python
    - mock

test:
  # Python imports
  imports:
    - openerp.addons.web
    - openerp.addons.web.cli
    - openerp.addons.web.controllers
    - openerp.addons.web.tests
    - openerp.addons.web.tests.qunitsuite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Web'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
