package:
  name: leakdb
  version: "0.2"

source:
  fn: LeakDB-0.2.tar.gz
  url: https://pypi.python.org/packages/source/L/LeakDB/LeakDB-0.2.tar.gz
  md5: 7b5cff00669c84e2aa233150337a6125
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leakdb = leakdb:main
    #
    # Would create an entry point called leakdb that calls leakdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent

  run:
    - python
    - gevent

test:
  # Python imports
  imports:
    - leakdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Lujeni/LeakDB
  license: BSD License
  summary: 'LeakDB is a very simple and fast key value store for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
