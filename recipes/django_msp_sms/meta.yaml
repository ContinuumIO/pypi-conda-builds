package:
  name: django_msp_sms
  version: "0.0.3"

source:
  fn: django_msp_sms-0.0.3.zip
  url: https://pypi.python.org/packages/source/d/django_msp_sms/django_msp_sms-0.0.3.zip
  md5: 416e2f484ef99dafaf9e447149418f49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_msp_sms = django_msp_sms:main
    #
    # Would create an entry point called django_msp_sms that calls django_msp_sms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_microsip_base >=0.0.15
    - microsip_api >=1.4-beta.5

  run:
    - python
    - django_microsip_base >=0.0.15
    - microsip_api >=1.4-beta.5

test:
  # Python imports
  imports:
    - django_msp_sms
    - django_msp_sms.modulos
    - django_msp_sms.modulos.clientes
    - django_msp_sms.modulos.personalizados
    - django_msp_sms.modulos.preferencias
    - django_msp_sms.modulos.saldos_clientes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'django_msp_sms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
