package:
  name: django-header-auth
  version: "0.1.0"

source:
  fn: django-header-auth-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-header-auth/django-header-auth-0.1.0.tar.gz
  md5: af75aa645186e53d8f87532d89f0fe63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-header-auth = django-header-auth:main
    #
    # Would create an entry point called django-header-auth that calls django-header-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - header_auth
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/localmed/django-header-auth
  license: MIT License
  summary: 'Django middleware for authorizing requests based on headers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
