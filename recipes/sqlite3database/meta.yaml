package:
  name: sqlite3database
  version: "0.2.0"

source:
  fn: SQLite3Database-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLite3Database/SQLite3Database-0.2.0.tar.gz
  md5: 19dc703a97c1eb0a2413f272b98b5322
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlite3database = sqlite3database:main
    #
    # Would create an entry point called sqlite3database that calls sqlite3database.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - sqlite3database
    - sqlite3database.helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/sqlite3database/index.html
  license: GNU Affero General Public License v3
  summary: 'sqlite3 driver for the DatabasePipe package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
