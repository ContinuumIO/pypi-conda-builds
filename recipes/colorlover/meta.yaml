package:
  name: colorlover
  version: "0.2"

source:
  fn: colorlover-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/colorlover/colorlover-0.2.tar.gz
  md5: 524b38fa5653e51323ced5d6e8bcee8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorlover = colorlover:main
    #
    # Would create an entry point called colorlover that calls colorlover.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorlover

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jackparmer/colorlover
  license: UNKNOWN
  summary: 'Color scales for IPython notebook'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
