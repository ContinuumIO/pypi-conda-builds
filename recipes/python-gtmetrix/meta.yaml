package:
  name: python-gtmetrix
  version: "0.2.3"

source:
  fn: python-gtmetrix-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gtmetrix/python-gtmetrix-0.2.3.tar.gz
  md5: c538ba38f88ab21a88436d9102c72cb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gtmetrix = python-gtmetrix:main
    #
    # Would create an entry point called python-gtmetrix that calls python-gtmetrix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - gtmetrix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aisayko/python-gtmetrix
  license: MIT License
  summary: 'A Python client library for GTmetrix REST API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
