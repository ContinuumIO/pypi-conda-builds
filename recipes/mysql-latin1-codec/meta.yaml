package:
  name: mysql-latin1-codec
  version: "2.0"

source:
  fn: mysql-latin1-codec-2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mysql-latin1-codec/mysql-latin1-codec-2.0.tar.gz
  md5: ecb565946614a65938283db17449a773
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mysql-latin1-codec = mysql-latin1-codec:main
    #
    # Would create an entry point called mysql-latin1-codec that calls mysql-latin1-codec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wbolster/mysql-latin1-codec
  license: BSD License
  summary: "Python string codec for MySQL's latin1 encoding"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
