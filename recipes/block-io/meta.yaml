package:
  name: block-io
  version: "1.1.6"

source:
  fn: block-io-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/b/block-io/block-io-1.1.6.tar.gz
  md5: 4c3bfd958b38f4f36d81bcdec0039ff8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - block-io = block-io:main
    #
    # Would create an entry point called block-io that calls block-io.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pycrypto
    - ecdsa
    - six
    - base58

  run:
    - python
    - requests
    - pycrypto
    - ecdsa
    - six
    - base58

test:
  # Python imports
  imports:
    - block_io

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BlockIo/block_io-python
  license: MIT
  summary: 'The easiest way to integrate Bitcoin, Dogecoin and Litecoin in your applications. Sign up at Block.io for your API key.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
