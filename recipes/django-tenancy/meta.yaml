package:
  name: django-tenancy
  version: "0.1.2"

source:
  fn: django-tenancy-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tenancy/django-tenancy-0.1.2.tar.gz
  md5: b4bededabfe95d6d3f64500b10fb0fad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tenancy = django-tenancy:main
    #
    # Would create an entry point called django-tenancy that calls django-tenancy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - tenancy
    - tenancy.auth
    - tenancy.management
    - tenancy.management.commands
    - tenancy.mutant
    - tenancy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charettes/django-tenancy
  license: MIT License
  summary: u'Handle multi-tenancy in Django with no additional global state using schemas.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
