package:
  name: django-validated-file
  version: "2.0.1"

source:
  fn: django-validated-file-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-validated-file/django-validated-file-2.0.1.tar.gz
  md5: d40ee5b7d8a7eacd9d622f91683e9b1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-validated-file = django-validated-file:main
    #
    # Would create an entry point called django-validated-file that calls django-validated-file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - python-magic >=0.4.2

  run:
    - python
    - distribute
    - python-magic >=0.4.2

test:
  # Python imports
  imports:
    - testing
    - validatedfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleidos/django-validated-file
  license: BSD License
  summary: 'This Django app adds a new field type, ValidatedFileField, that add the capability of checking the document size and types the user may send.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
