package:
  name: goless
  version: "0.7.2"

source:
  fn: goless-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/g/goless/goless-0.7.2.tar.gz
  md5: b0766279728c59d9cf398dd9567a0ae4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goless = goless:main
    #
    # Would create an entry point called goless that calls goless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=1.0

  run:
    - python
    - gevent >=1.0

test:
  # Python imports
  imports:
    - goless

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rgalanakis/goless
  license: Apache Software License
  summary: 'Provides a Go-like concurrent programming style in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
