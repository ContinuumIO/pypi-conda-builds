package:
  name: pyvoro
  version: "1.3.2"

source:
  fn: pyvoro-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyvoro/pyvoro-1.3.2.tar.gz
  md5: 1d437c243c2d6ac9d43e1dd2a6c05946
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvoro = pyvoro:main
    #
    # Would create an entry point called pyvoro that calls pyvoro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyvoro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joe-jordan/pyvoro
  license: UNKNOWN
  summary: '2D and 3D Voronoi tessellations: a python entry point for the voro++ library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
