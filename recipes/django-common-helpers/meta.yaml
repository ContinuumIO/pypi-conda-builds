package:
  name: django-common-helpers
  version: "0.7.0"

source:
  fn: django-common-helpers-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-common-helpers/django-common-helpers-0.7.0.tar.gz
  md5: b317b9807bb0821810cb8f5856367254
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-common-helpers = django-common-helpers:main
    #
    # Would create an entry point called django-common-helpers that calls django-common-helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - django_common
    - django_common.management
    - django_common.management.commands
    - django_common.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tivix/django-common
  license: UNKNOWN
  summary: 'Common things every Django app needs!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
