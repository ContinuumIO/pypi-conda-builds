package:
  name: ogitm
  version: "0.1.0"

source:
  fn: ogitm-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/ogitm/ogitm-0.1.0.tar.gz
  md5: ea4681ac488e8e24a1ada2b50fbb0655
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ogitm = ogitm:main
    #
    # Would create an entry point called ogitm that calls ogitm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygit2 ==0.22.0
    - inflection ==0.3.0

  run:
    - python
    - pygit2 ==0.22.0
    - inflection ==0.3.0

test:
  # Python imports
  imports:
    - ogitm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MrJohz/ogitm
  license: MIT License
  summary: 'A OO-based Git Database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
