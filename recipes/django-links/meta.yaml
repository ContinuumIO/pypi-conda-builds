package:
  name: django-links
  version: "1.1"

source:
  fn: django-links-1.1.tar.gz
  url: http://www.doughellmann.com/downloads/django-links-1.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-links = django-links:main
    #
    # Would create an entry point called django-links that calls django-links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djangolinks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.doughellmann.com/projects/django-links/
  license: BSD License
  summary: 'Django links application for simple bookmark handling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
