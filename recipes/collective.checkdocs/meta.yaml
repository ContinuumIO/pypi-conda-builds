package:
  name: collective.checkdocs
  version: "0.2"

source:
  fn: collective.checkdocs-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.checkdocs/collective.checkdocs-0.2.zip
  md5: 472a6c7cafad4ee274c905ebd1603fb3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.checkdocs = collective.checkdocs:main
    #
    # Would create an entry point called collective.checkdocs that calls collective.checkdocs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils

  run:
    - python
    - setuptools
    - docutils

test:
  # Python imports
  imports:
    - collective
    - collective.checkdocs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.checkdocs
  license: GPL
  summary: "Distutils command to view and validate restructured text in package's long_description"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
