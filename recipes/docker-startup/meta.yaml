package:
  name: docker-startup
  version: "0.6"

source:
  fn: docker-startup-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-startup/docker-startup-0.6.tar.gz
  md5: 2252c1e5561527681c59242caf14aff4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-startup = docker-startup:main
    #
    # Would create an entry point called docker-startup that calls docker-startup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - docker_startup
    - docker_startup.base
    - docker_startup.django
    - docker_startup.java
    - docker_startup.php

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dpaw/docker-startup
  license: BSD License
  summary: 'Startup/Install scripts for docker-based VMs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
