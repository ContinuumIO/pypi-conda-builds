package:
  name: rosbag_pandas
  version: "0.3.7.2"

source:
  fn: rosbag_pandas-0.3.7.2.tar.gz
  url: https://pypi.python.org/packages/source/r/rosbag_pandas/rosbag_pandas-0.3.7.2.tar.gz
  md5: f6c6cd16570bafc8ff96025b2e76db40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rosbag_pandas = rosbag_pandas:main
    #
    # Would create an entry point called rosbag_pandas that calls rosbag_pandas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - rosbag_pandas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Create a Python pandas data frame from a ros bag file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
