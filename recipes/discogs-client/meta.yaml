package:
  name: discogs-client
  version: "2.1.0"

source:
  fn: discogs-client-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/discogs-client/discogs-client-2.1.0.tar.gz
  md5: 3acf8b5a77949634cd92839280181a8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - discogs-client = discogs-client:main
    #
    # Would create an entry point called discogs-client that calls discogs-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - six
    - oauthlib

  run:
    - python
    - requests
    - six
    - oauthlib

test:
  # Python imports
  imports:
    - discogs_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/discogs/discogs_client
  license: BSD License
  summary: 'Official Python API client for Discogs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
