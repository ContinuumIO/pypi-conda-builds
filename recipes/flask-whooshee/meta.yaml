package:
  name: flask-whooshee
  version: "0.0.6"

source:
  fn: flask-whooshee-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-whooshee/flask-whooshee-0.0.6.tar.gz
  md5: 950e6a0ceffb06570c80da546eada95e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-whooshee = flask-whooshee:main
    #
    # Would create an entry point called flask-whooshee that calls flask-whooshee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-sqlalchemy
    - whoosh

  run:
    - python
    - flask-sqlalchemy
    - whoosh

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bkabrda/flask-whooshee
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Flask - SQLAlchemy - Whoosh integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
