package:
  name: collective.gmaplocation
  version: "0.4"

source:
  fn: collective.gmaplocation-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.gmaplocation/collective.gmaplocation-0.4.tar.gz
  md5: dfc838946eb651a77a75c38cab7bcef1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.gmaplocation = collective.gmaplocation:main
    #
    # Would create an entry point called collective.gmaplocation that calls collective.gmaplocation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - yafowil.yaml >0.9
    - yafowil.plone
    - archetypes.schemaextender
    - bda.plone.ajax
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - yafowil.yaml >0.9
    - yafowil.plone
    - archetypes.schemaextender
    - bda.plone.ajax
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - collective
    - collective.gmaplocation
    - collective.gmaplocation.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Plone Add-On to manage and display locations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
