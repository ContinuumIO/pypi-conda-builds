package:
  name: django-safedelete
  version: "0.2.1"

source:
  fn: django-safedelete-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-safedelete/django-safedelete-0.2.1.tar.gz
  md5: 88feb47f10ba2056de918d14b35b326f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-safedelete = django-safedelete:main
    #
    # Would create an entry point called django-safedelete that calls django-safedelete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - safedelete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/makinacorpus/django-safedelete
  license: BSD License
  summary: 'Mask your objects instead of deleting them from your database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
