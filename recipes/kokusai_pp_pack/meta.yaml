package:
  name: kokusai_pp_pack
  version: "0.1.0"

source:
  fn: Kokusai_PP_pack-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/K/Kokusai_PP_pack/Kokusai_PP_pack-0.1.0.tar.gz
  md5: ad8c09be6181eb1f5d11ab047d6babd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kokusai_pp_pack = kokusai_pp_pack:main
    #
    # Would create an entry point called kokusai_pp_pack that calls kokusai_pp_pack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - kokusai_bpp
    - kokusai_pp_pack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Python package for kokusai post-processor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
