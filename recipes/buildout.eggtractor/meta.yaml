package:
  name: buildout.eggtractor
  version: "0.6"

source:
  fn: buildout.eggtractor-0.6.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.eggtractor/buildout.eggtractor-0.6.tar.gz
  md5: f137b4bc0218e90ab12f15f58accdf8e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.eggtractor = buildout.eggtractor:main
    #
    # Would create an entry point called buildout.eggtractor that calls buildout.eggtractor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - buildout
    - buildout.eggtractor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/buildout.eggtractor
  license: GPL
  summary: 'Auto generates zcml slugs, buildout:egg and buildout:develop entries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
