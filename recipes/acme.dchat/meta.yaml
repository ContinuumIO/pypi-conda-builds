package:
  name: acme.dchat
  version: "0.1"

source:
  fn: acme.dchat-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/acme.dchat/acme.dchat-0.1.tar.gz
  md5: 20865b1ffee439c663a84a1d97169fb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acme.dchat = acme.dchat:main
    #
    # Would create an entry point called acme.dchat that calls acme.dchat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - acme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jptomo/acme.dchat
  license: Apache Software License
  summary: 'This software aim to provide a chatting tool with the NTT docomo API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
