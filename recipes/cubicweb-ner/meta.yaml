package:
  name: cubicweb-ner
  version: "0.1.0"

source:
  fn: cubicweb-ner-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-ner/cubicweb-ner-0.1.0.tar.gz
  md5: a59e9e67ddb2f3409789d2ef8d648b48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-ner = cubicweb-ner:main
    #
    # Would create an entry point called cubicweb-ner that calls cubicweb-ner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.3
    - cubicweb-dataio

  run:
    - python
    - cubicweb >=3.16.3
    - cubicweb-dataio

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-ner
  license: LGPL
  summary: 'Cube for named entities source and recognition (NER).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
