package:
  name: diaspy
  version: "0.3.0"

source:
  fn: diaspy-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/diaspy/diaspy-0.3.0.tar.gz
  md5: bb6a9f91169571de9df0be3b93220110
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diaspy = diaspy:main
    #
    # Would create an entry point called diaspy that calls diaspy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - diaspy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Javafant/diaspora-api
  license: UNKNOWN
  summary: 'A Python API to the social network Diaspora*'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
