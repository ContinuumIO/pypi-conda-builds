package:
  name: slimes
  version: "0.0.2"

source:
  fn: SlimES-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SlimES/SlimES-0.0.2.tar.gz
  md5: 72ccbe2764ded84d2f5b48d48e45580e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slimes = slimes:main
    #
    # Would create an entry point called slimes that calls slimes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=0.8.2

  run:
    - python
    - requests >=0.8.2

test:
  # Python imports
  imports:
    - slimes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/SlimES/
  license: LICENSE.txt
  summary: 'Thin Python connector for Elasticsearch.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
