package:
  name: zope.app.zopeappgenerations
  version: "3.6.1"

source:
  fn: zope.app.zopeappgenerations-3.6.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.zopeappgenerations/zope.app.zopeappgenerations-3.6.1.tar.gz
  md5: 3c52b3253c540dcb15d005d9623a6f1f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.zopeappgenerations = zope.app.zopeappgenerations:main
    #
    # Would create an entry point called zope.app.zopeappgenerations that calls zope.app.zopeappgenerations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.annotation
    - zope.app.authentication
    - zope.app.component
    - zope.copypastemove
    - zope.dublincore
    - zope.generations

  run:
    - python
    - setuptools
    - zope.annotation
    - zope.app.authentication
    - zope.app.component
    - zope.copypastemove
    - zope.dublincore
    - zope.generations

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.zopeappgenerations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.zopeappgenerations
  license: Zope Public License
  summary: 'Zope Application ZODB Update Generations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
