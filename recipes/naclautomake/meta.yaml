package:
  name: naclautomake
  version: "0.0.1"

source:
  fn: naclautomake-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/naclautomake/naclautomake-0.0.1.tar.gz
  md5: f5efc8d04f00f4ff557f80aaa42519a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - naclautomake = naclautomake:main
    #
    # Would create an entry point called naclautomake that calls naclautomake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - naclautomake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://alexchi.me/
  license: Creative Commons Attribution 3.0 Unported License
  summary: 'Automake of Native Client from Google'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
