package:
  name: explorerhat
  version: "0.1.1"

source:
  fn: ExplorerHAT-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/E/ExplorerHAT/ExplorerHAT-0.1.1.tar.gz
  md5: 558d252d8d6cc1d95ee1514bbad20baf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - explorerhat = explorerhat:main
    #
    # Would create an entry point called explorerhat that calls explorerhat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - rpi.gpio >=0.5.10

  run:
    - python
    - rpi.gpio >=0.5.10

test:
  # Python imports
  imports:
    - explorerhat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://shop.pimoroni.com
  license: MIT License
  summary: 'A module to control the Explorer HAT Raspberry Pi Addon Board'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
