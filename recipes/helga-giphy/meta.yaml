package:
  name: helga-giphy
  version: "0.1.1"

source:
  fn: helga-giphy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-giphy/helga-giphy-0.1.1.tar.gz
  md5: d1261cd10916f5455355a01a55db22a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-giphy = helga-giphy:main
    #
    # Would create an entry point called helga-giphy that calls helga-giphy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - giphypop ==0.2

  run:
    - python
    - setuptools
    - giphypop ==0.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-giphy
  license: MIT License
  summary: 'A command plugin to search giphy for an anmiated gif.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
