package:
  name: django-saddle
  version: "0.3.beta2"

source:
  fn: django-saddle-0.3.beta2.zip
  url: https://pypi.python.org/packages/source/d/django-saddle/django-saddle-0.3.beta2.zip
  md5: f761b113c38afa6d24080f46b962e2ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-saddle = django-saddle:main
    #
    # Would create an entry point called django-saddle that calls django-saddle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - saddle
    - saddle.handlers
    - saddle.management
    - saddle.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/nl_0/django-saddle/
  license: BSD License
  summary: 'Settings on steroids and some shortcuts for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
