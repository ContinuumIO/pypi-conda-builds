package:
  name: goocalendar
  version: "0.2"

source:
  fn: GooCalendar-0.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GooCalendar/GooCalendar-0.2.tar.gz
  md5: 671804af9fd087b7f58086bb6083d7a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goocalendar = goocalendar:main
    #
    # Would create an entry point called goocalendar that calls goocalendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - goocalendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/goocalendar/
  license: GNU General Public License v2 (GPLv2)
  summary: 'A calendar widget for GTK using PyGoocanvas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
