package:
  name: django-pageguide
  version: "0.1"

source:
  fn: django-pageguide-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pageguide/django-pageguide-0.1.tar.gz
  md5: 48a4deeb8516f2a95e31c92a848a63c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pageguide = django-pageguide:main
    #
    # Would create an entry point called django-pageguide that calls django-pageguide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - south >=0.7.5

  run:
    - python
    - django >=1.3
    - south >=0.7.5

test:
  # Python imports
  imports:
    - pageguide
    - pageguide.migrations
    - pageguide.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/msaelices/django-pageguide/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'An Django integration with pageguide, an interactive, responsive, and smart guide for web page elements using jQuery and CSS3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
