package:
  name: collective.wowlichking
  version: "0.5"

source:
  fn: collective.wowlichking-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.wowlichking/collective.wowlichking-0.5.zip
  md5: 200ef05a6e8f5e316a860653f2f4aa5e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.wowlichking = collective.wowlichking:main
    #
    # Would create an entry point called collective.wowlichking that calls collective.wowlichking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.wowlichking
    - collective.wowlichking.browser
    - collective.wowlichking.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/collectivewowlichking/
  license: GPL
  summary: 'An installable WoW theme for Pone 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
