package:
  name: ipark.ereporter
  version: "0.3"

source:
  fn: ipark.ereporter-0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/ipark.ereporter/ipark.ereporter-0.3.tar.gz
  md5: b7aece2d4d99ca5d57ae24d89ff42059
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipark.ereporter = ipark.ereporter:main
    #
    # Would create an entry point called ipark.ereporter that calls ipark.ereporter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tipfy >=0.6,<0.7

  run:
    - python
    - tipfy >=0.6,<0.7

test:
  # Python imports
  imports:
    - ipark
    - ipark.ereporter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/iparkcode/
  license: BSD License
  summary: 'Tipfy extension to simplify projects beta test, it simply send all exceptions traceback via email to application administrators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
