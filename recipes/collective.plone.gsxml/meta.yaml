package:
  name: collective.plone.gsxml
  version: "0.4.7"

source:
  fn: collective.plone.gsxml-0.4.7.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.plone.gsxml/collective.plone.gsxml-0.4.7.tar.gz
  md5: 3ee79eff4b14b6e00a5cf718cf61cbb0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.plone.gsxml = collective.plone.gsxml:main
    #
    # Would create an entry point called collective.plone.gsxml that calls collective.plone.gsxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.plone
    - collective.plone.gsxml
    - collective.plone.gsxml.browser
    - collective.plone.gsxml.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/gsxml/releases/0.4.7
  license: GPL
  summary: 'A package for importing and exporting content from Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
