package:
  name: frasco-geoip
  version: "0.1"

source:
  fn: frasco-geoip-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-geoip/frasco-geoip-0.1.tar.gz
  md5: 40d867b7d3a7b8399380562de6370c29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-geoip = frasco-geoip:main
    #
    # Would create an entry point called frasco-geoip that calls frasco-geoip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - pygeoip >=0.3.1

  run:
    - python
    - frasco
    - pygeoip >=0.3.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-geoip
  license: MIT
  summary: 'pygeoip integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
