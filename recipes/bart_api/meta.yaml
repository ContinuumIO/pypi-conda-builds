package:
  name: bart_api
  version: "0.1-5"

source:
  fn: bart_api-0.1-5.tar.gz
  url: https://pypi.python.org/packages/source/b/bart_api/bart_api-0.1-5.tar.gz
  md5: bfffc693e17e97c7f7fe80314a5ff94a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bart_api = bart_api:main
    #
    # Would create an entry point called bart_api that calls bart_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bart_api
    - bart_api.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/projectdelphai/bart_api
  license: MIT
  summary: 'a python client that interacts with the bay area rapid transit api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
