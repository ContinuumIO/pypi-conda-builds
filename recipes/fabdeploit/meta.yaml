package:
  name: fabdeploit
  version: "0.12.2"

source:
  fn: fabdeploit-0.12.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fabdeploit/fabdeploit-0.12.2.tar.gz
  md5: 909641821e58fedf1d606c99b8ca7c9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fabdeploit = fabdeploit:main
    #
    # Would create an entry point called fabdeploit that calls fabdeploit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.4
    - gitpython >=0.3.0

  run:
    - python
    - fabric >=1.4
    - gitpython >=0.3.0

test:
  # Python imports
  imports:
    - fabdeploit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ddanier/fabdeploit
  license: BSD License
  summary: 'fabric utilities for git based deployments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
