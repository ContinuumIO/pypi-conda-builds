package:
  name: django-mediasync
  version: "2.2.0"

source:
  fn: django-mediasync-2.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mediasync/django-mediasync-2.2.0.tar.gz
  md5: df0c71b8c0d98e09a514fa3e578933c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mediasync = django-mediasync:main
    #
    # Would create an entry point called django-mediasync that calls django-mediasync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - mediasync
    - mediasync.backends
    - mediasync.management
    - mediasync.management.commands
    - mediasync.processors
    - mediasync.templatetags
    - mediasync.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/django-mediasync/
  license: BSD License
  summary: 'Django static media development and distribution tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
