package:
  name: collective.pygal.core
  version: "0.1"

source:
  fn: collective.pygal.core-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.pygal.core/collective.pygal.core-0.1.tar.gz
  md5: 9f91e2f61bf9195e064ff1d6f3b1806a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pygal.core = collective.pygal.core:main
    #
    # Would create an entry point called collective.pygal.core that calls collective.pygal.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pygal

  run:
    - python
    - setuptools
    - setuptools
    - pygal

test:
  # Python imports
  imports:
    - collective
    - collective.pygal
    - collective.pygal.core
    - collective.pygal.core.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.pygal.core
  license: GNU General Public License (GPL)
  summary: 'Pygal dynamic SVG charting library integration for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
