package:
  name: python-barbicanclient
  version: "3.1.1"

source:
  fn: python-barbicanclient-3.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-barbicanclient/python-barbicanclient-3.1.1.tar.gz
  md5: 3d84daa6f72fa5a903e9d3bb0ed786f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-barbicanclient = python-barbicanclient:main
    #
    # Would create an entry point called python-barbicanclient that calls python-barbicanclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbr

  run:
    - python
    - pbr

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Client Library for OpenStack Barbican Key Management API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
