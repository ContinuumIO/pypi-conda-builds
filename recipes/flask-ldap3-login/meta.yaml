package:
  name: flask-ldap3-login
  version: "0.9.7"

source:
  fn: flask-ldap3-login-0.9.7.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-ldap3-login/flask-ldap3-login-0.9.7.tar.gz
  md5: 5dd02faac183e0f96f12c330f3790caa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-ldap3-login = flask-ldap3-login:main
    #
    # Would create an entry point called flask-ldap3-login that calls flask-ldap3-login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ldap3
    - flask
    - flask-wtf
    - enum34

  run:
    - python
    - ldap3
    - flask
    - flask-wtf
    - enum34

test:
  # Python imports
  imports:
    - flask_ldap3_login

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nickw444/flask-ldap3-login
  license: UNKNOWN
  summary: 'LDAP Support for Flask in Python3/2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
