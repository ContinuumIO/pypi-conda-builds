package:
  name: django-discussion
  version: "0.6"

source:
  fn: django-discussion-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-discussion/django-discussion-0.6.tar.gz
  md5: 40c39d513d64614de3fbe706bd860019
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-discussion = django-discussion:main
    #
    # Would create an entry point called django-discussion that calls django-discussion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-orderable >=1.0.1
    - incuna-notification >=0.3.2

  run:
    - python
    - django-orderable >=1.0.1
    - incuna-notification >=0.3.2

test:
  # Python imports
  imports:
    - discussion
    - discussion.migrations
    - discussion.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://incuna.com/
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
