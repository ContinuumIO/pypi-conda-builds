package:
  name: jquery.jsonform
  version: "0.1.0c1"

source:
  fn: jquery.jsonform-0.1.0c1.tar.gz
  url: https://pypi.python.org/packages/source/j/jquery.jsonform/jquery.jsonform-0.1.0c1.tar.gz
  md5: 6d4062df16f4fcfa4b0b605c51e08949
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jquery.jsonform = jquery.jsonform:main
    #
    # Would create an entry point called jquery.jsonform that calls jquery.jsonform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zif.jsonserver
    - zope.component
    - zope.configuration
    - zope.interface
    - zope.publisher
    - zope.viewlet

  run:
    - python
    - setuptools
    - zif.jsonserver
    - zope.component
    - zope.configuration
    - zope.interface
    - zope.publisher
    - zope.viewlet

test:
  # Python imports
  imports:
    - jquery
    - jquery.jsonform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/jquery.jsonform
  license: Zope Public License
  summary: 'A package for JSON-enabled forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
