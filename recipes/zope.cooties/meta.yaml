package:
  name: zope.cooties
  version: "40.5"

source:
  fn: zope.cooties-40.5.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.cooties/zope.cooties-40.5.tar.gz
  md5: db4f28bdc51af56fdedf66bf135980de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.cooties = zope.cooties:main
    #
    # Would create an entry point called zope.cooties that calls zope.cooties.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.app.form
    - zc.security

  run:
    - python
    - zope.app.form
    - zc.security

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pylonsproject.org
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Give the haters more to hate.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
