package:
  name: luckystrike
  version: "0.11"

source:
  fn: luckystrike-0.11.tar.gz
  url: https://pypi.python.org/packages/source/l/luckystrike/luckystrike-0.11.tar.gz
  md5: 34ba4ad04991eea5df3903c304ca1492
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - luckystrike = luckystrike:main
    #
    # Would create an entry point called luckystrike that calls luckystrike.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pinder >=1.0
    - twisted >=13.2.0
    - pycrypto >=2.6.1
    - pyopenssl >=0.14
    - pyasn1 >=0.1.7

  run:
    - python
    - pinder >=1.0
    - twisted >=13.2.0
    - pycrypto >=2.6.1
    - pyopenssl >=0.14
    - pyasn1 >=0.1.7

test:
  # Python imports
  imports:
    - luckystrike

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mmichie/luckystrike
  license: MIT License
  summary: 'IRC to Campfire Bridge'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
