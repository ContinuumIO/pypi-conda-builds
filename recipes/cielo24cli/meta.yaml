package:
  name: cielo24cli
  version: "1.0.2"

source:
  fn: cielo24cli-1.0.2.zip
  url: https://pypi.python.org/packages/source/c/cielo24cli/cielo24cli-1.0.2.zip
  md5: 044d8ab1796aeaa79341588ebfd0e1f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cielo24cli = cielo24cli:main
    #
    # Would create an entry point called cielo24cli that calls cielo24cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - compago
    - cielo24

  run:
    - python
    - compago
    - cielo24

test:
  # Python imports
  imports:
    - cielo24cli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cielo24.com
  license: UNKNOWN
  summary: 'Commmand line for cielo24 package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
