package:
  name: omdox
  version: "0.6.1.alpha"

source:
  fn: omdox-0.6.1.alpha.tar.gz
  url: https://pypi.python.org/packages/source/o/omdox/omdox-0.6.1.alpha.tar.gz
  md5: 12a6bb70b121dbef67c63825f7447017
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omdox = omdox:main
    #
    # Would create an entry point called omdox that calls omdox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jinja2
    - pygments
    - pyyaml
    - walkdir
    - beautifulsoup4
    - markdown2

  run:
    - python
    - jinja2
    - pygments
    - pyyaml
    - walkdir
    - beautifulsoup4
    - markdown2

test:
  # Python imports
  imports:
    - omdox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://omdox.obscuremetaphor.co.uk
  license: BSD 3 Clause
  summary: 'A lightweight documentation builder.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
