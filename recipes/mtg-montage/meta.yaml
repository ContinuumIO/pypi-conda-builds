package:
  name: mtg-montage
  version: "0.1.2"

source:
  fn: mtg-montage-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mtg-montage/mtg-montage-0.1.2.tar.gz
  md5: 8584075c8726deb6b704db612ef54a92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mtg-montage = mtg-montage:main
    #
    # Would create an entry point called mtg-montage that calls mtg-montage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxmackie/mtg-montage
  license: LICENSE.txt
  summary: 'Creates proxy pages from Magic: The Gathering cards ready for printing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
