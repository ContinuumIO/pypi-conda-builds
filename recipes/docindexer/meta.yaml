package:
  name: docindexer
  version: "0.1.dev7"

source:
  fn: docindexer-0.1.dev7.tar.gz
  url: https://pypi.python.org/packages/source/d/docindexer/docindexer-0.1.dev7.tar.gz
  md5: 31d299e9334e4c3709fd7a492395e299
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docindexer = docindexer:main
    #
    # Would create an entry point called docindexer that calls docindexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - whoosh

  run:
    - python
    - whoosh

test:
  # Python imports
  imports:
    - docindexer
    - docindexer.tika

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/hjalves/docindexer
  license: GPL
  summary: 'Document indexer for multiple file formats.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
