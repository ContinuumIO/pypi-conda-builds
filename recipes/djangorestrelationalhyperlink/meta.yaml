package:
  name: djangorestrelationalhyperlink
  version: "1.2.1"

source:
  fn: djangorestrelationalhyperlink-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestrelationalhyperlink/djangorestrelationalhyperlink-1.2.1.tar.gz
  md5: a0a409cfa8758b5b3e37371c837eb35e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestrelationalhyperlink = djangorestrelationalhyperlink:main
    #
    # Would create an entry point called djangorestrelationalhyperlink that calls djangorestrelationalhyperlink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework

  run:
    - python
    - djangorestframework

test:
  # Python imports
  imports:
    - djangorestrelationalhyperlink
    - hyperlinked_relational_serializer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fredkingham/django_rest_model_hyperlink_serializers_project
  license: BSD License
  summary: 'A hyperlinked serialiser that can can be used to alter relationships via hyperlinks, but otherwise like a hyperlink model serializer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
