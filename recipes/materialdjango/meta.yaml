package:
  name: materialdjango
  version: "0.1a4"

source:
  fn: MaterialDjango-0.1a4.tar.gz
  url: https://pypi.python.org/packages/source/M/MaterialDjango/MaterialDjango-0.1a4.tar.gz
  md5: 3ea277621db92a608981da25db4d942d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - materialdjango = materialdjango:main
    #
    # Would create an entry point called materialdjango that calls materialdjango.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - vulcanize
    - bower.py

  run:
    - python
    - django
    - vulcanize
    - bower.py

test:
  # Python imports
  imports:
    - materialdjango
    - materialdjango.management
    - materialdjango.management.commands
    - materialdjango.migrations
    - materialdjango.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Colorless-Green-Ideas/MaterialDjango
  license: MIT License
  summary: 'A Django library bringing paper-ui and core-ui polymer elements to django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
