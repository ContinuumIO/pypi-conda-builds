package:
  name: couchutil
  version: "0.1.1dev"

source:
  fn: couchutil-0.1.1dev.tar.gz
  url: https://pypi.python.org/packages/source/c/couchutil/couchutil-0.1.1dev.tar.gz
  md5: 76a12e6ba92f59b41050634aa49f6b22
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchutil = couchutil:main
    #
    # Would create an entry point called couchutil that calls couchutil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - couchutil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ish.io
  license: UNKNOWN
  summary: 'utils for couchdb. changes handler and reservation handling ctx mgr.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
