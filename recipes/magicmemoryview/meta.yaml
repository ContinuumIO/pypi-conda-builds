package:
  name: magicmemoryview
  version: "0.1.5"

source:
  fn: magicmemoryview-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/m/magicmemoryview/magicmemoryview-0.1.5.tar.gz
  md5: c072df5c561228abd3eb39937e37f09d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magicmemoryview = magicmemoryview:main
    #
    # Would create an entry point called magicmemoryview that calls magicmemoryview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - magicmemoryview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cuspaceflight/magicmemoryview
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Magic memoryview() style casting for Cython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
