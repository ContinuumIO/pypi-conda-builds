package:
  name: django-lot
  version: "0.0.5"

source:
  fn: django-lot-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lot/django-lot-0.0.5.tar.gz
  md5: f128ec4bb3e0d34d7b3254755ddbbd70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lot = django-lot:main
    #
    # Would create an entry point called django-lot that calls django-lot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0
    - simplejson >=3.3.0

  run:
    - python
    - django >=1.3.0
    - simplejson >=3.3.0

test:
  # Python imports
  imports:
    - lot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jespino/django-lot
  license: UNKNOWN
  summary: 'Django Login over Token.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
