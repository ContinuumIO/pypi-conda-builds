package:
  name: cykooz.recipe.pastewsgi
  version: "0.5"

source:
  fn: cykooz.recipe.pastewsgi-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cykooz.recipe.pastewsgi/cykooz.recipe.pastewsgi-0.5.tar.gz
  md5: af305c650a18a1d020026da00c2ba5b0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cykooz.recipe.pastewsgi = cykooz.recipe.pastewsgi:main
    #
    # Would create an entry point called cykooz.recipe.pastewsgi that calls cykooz.recipe.pastewsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - cykooz
    - cykooz.recipe
    - cykooz.recipe.pastewsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cykooz/cykooz.recipe.pastewsgi
  license: GNU General Public License (GPL)
  summary: 'Buildout recipe to create paste.deploy entry points for mod_wsgi or uwsgi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
