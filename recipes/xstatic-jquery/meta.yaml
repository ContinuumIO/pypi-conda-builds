package:
  name: xstatic-jquery
  version: "1.10.2.1"

source:
  fn: XStatic-jQuery-1.10.2.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-jQuery/XStatic-jQuery-1.10.2.1.tar.gz
  md5: 7a29efeab6961ef00ea2272c923f4503
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-jquery = xstatic-jquery:main
    #
    # Would create an entry point called xstatic-jquery that calls xstatic-jquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.jquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jquery.com/
  license: (same as jQuery)
  summary: 'jQuery 1.10.2 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
