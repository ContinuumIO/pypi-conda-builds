package:
  name: django-simple-audit
  version: "0.1.21"

source:
  fn: django-simple-audit-0.1.21.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-audit/django-simple-audit-0.1.21.tar.gz
  md5: b31324cd450d0c04052236f1ad4cac8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-audit = django-simple-audit:main
    #
    # Would create an entry point called django-simple-audit that calls django-simple-audit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simple_audit
    - simple_audit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leandrosouza/django-simple-audit
  license: BSD License
  summary: 'Simple audit for model instances in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
