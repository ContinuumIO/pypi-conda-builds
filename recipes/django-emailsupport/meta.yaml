package:
  name: django-emailsupport
  version: "0.0.3"

source:
  fn: django-emailsupport-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailsupport/django-emailsupport-0.0.3.tar.gz
  md5: 2ebc1a6c72ed8b0b9be2345297c75b33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailsupport = django-emailsupport:main
    #
    # Would create an entry point called django-emailsupport that calls django-emailsupport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - imbox

  run:
    - python
    - django >=1.7
    - imbox

test:
  # Python imports
  imports:
    - django_emailsupport
    - django_emailsupport.management
    - django_emailsupport.management.commands
    - django_emailsupport.migrations
    - django_emailsupport.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rosti-cz/django-emailsupport
  license: MIT License
  summary: 'Simple email support over django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
