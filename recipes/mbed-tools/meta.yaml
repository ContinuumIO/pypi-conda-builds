package:
  name: mbed-tools
  version: "0.1.14"

source:
  fn: mbed-tools-0.1.14.tar.gz
  url: https://pypi.python.org/packages/source/m/mbed-tools/mbed-tools-0.1.14.tar.gz
  md5: b53b428e735eb9806d8e3490728361ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mbed-tools = mbed-tools:main
    #
    # Would create an entry point called mbed-tools that calls mbed-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - workspace_tools
    - workspace_tools.data
    - workspace_tools.dev
    - workspace_tools.export
    - workspace_tools.host_tests
    - workspace_tools.host_tests.example
    - workspace_tools.toolchains

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mbedmicro/mbed
  license: mb_nester
  summary: 'Build and test system for mbed'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
