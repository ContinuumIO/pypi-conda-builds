package:
  name: django-staticfiles-isotope
  version: "1.5.18"

source:
  fn: django-staticfiles-isotope-1.5.18.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-isotope/django-staticfiles-isotope-1.5.18.tar.gz
  md5: 4179a2a5293e4b4258e4528f4bdeb216
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-isotope = django-staticfiles-isotope:main
    #
    # Would create an entry point called django-staticfiles-isotope that calls django-staticfiles-isotope.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - isotope

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tswicegood/django-staticfiles-isotope/
  license: BSD License
  summary: 'Isotope meets Django staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
