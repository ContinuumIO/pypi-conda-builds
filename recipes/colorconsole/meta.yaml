package:
  name: colorconsole
  version: "0.7.2"

source:
  fn: colorconsole-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/c/colorconsole/colorconsole-0.7.2.tar.gz
  md5: 650e545de0a137d961cfdf337756fbc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorconsole = colorconsole:main
    #
    # Would create an entry point called colorconsole that calls colorconsole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorconsole

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lskbr/colorconsole
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Simple console routines to enable colors and cursor positioning.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
