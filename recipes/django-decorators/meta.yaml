package:
  name: django-decorators
  version: "0.1.1"

source:
  fn: django-decorators-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-decorators/django-decorators-0.1.1.tar.gz
  md5: e3df9dacff2ba9daaca19d02886a9264
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-decorators = django-decorators:main
    #
    # Would create an entry point called django-decorators that calls django-decorators.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.1.1

  run:
    - python
    - django >=1.1.1

test:
  # Python imports
  imports:
    - django_decorators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-decorators/
  license: LICENSE.md
  summary: 'A bunch of django extra decorators.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
