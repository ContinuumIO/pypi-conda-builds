package:
  name: backplane2-pyclient
  version: "0.2"

source:
  fn: backplane2-pyclient-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/backplane2-pyclient/backplane2-pyclient-0.2.tar.gz
  md5: 012ad5781fd9d1a0cb9dd8ff1a0351e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backplane2-pyclient = backplane2-pyclient:main
    #
    # Would create an entry point called backplane2-pyclient that calls backplane2-pyclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - backplane
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/janrain/backplane2-pyclient
  license: Apache License 2.0
  summary: 'Backplane 2 library for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
