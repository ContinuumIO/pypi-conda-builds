package:
  name: redshift-sqlalchemy
  version: "0.4.1"

source:
  fn: redshift-sqlalchemy-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redshift-sqlalchemy/redshift-sqlalchemy-0.4.1.tar.gz
  md5: 7cb817675b05984804f953a21559f966
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redshift-sqlalchemy = redshift-sqlalchemy:main
    #
    # Would create an entry point called redshift-sqlalchemy that calls redshift-sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2 >=2.5
    - sqlalchemy >=0.8.0

  run:
    - python
    - setuptools
    - psycopg2 >=2.5
    - sqlalchemy >=0.8.0

test:
  # Python imports
  imports:
    - redshift_sqlalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/binarydud/redshift_sqlalchemy
  license: MIT License
  summary: 'Amazon Redshift Dialect for sqlalchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
