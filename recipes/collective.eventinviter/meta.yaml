package:
  name: collective.eventinviter
  version: "0.1b1"

source:
  fn: collective.eventinviter-0.1b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.eventinviter/collective.eventinviter-0.1b1.tar.gz
  md5: 227043afa0e48b31ae04e43890ff4f4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.eventinviter = collective.eventinviter:main
    #
    # Would create an entry point called collective.eventinviter that calls collective.eventinviter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.eventinviter
    - collective.eventinviter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.eventinviter
  license: GPL
  summary: 'Event Inviter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
