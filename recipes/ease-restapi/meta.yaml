package:
  name: ease-restapi
  version: "1.3"

source:
  fn: ease-restapi-1.3.tar.gz
  url: https://pypi.python.org/packages/source/e/ease-restapi/ease-restapi-1.3.tar.gz
  md5: 4a8cc1a3d4e632180a4109e53b448702
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ease-restapi = ease-restapi:main
    #
    # Would create an entry point called ease-restapi that calls ease-restapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - requests

  run:
    - python
    - six
    - requests

test:
  # Python imports
  imports:
    - ease_restapi
    - ease_restapi.demo
    - ease_restapi.service
    - ease_restapi.simplify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kylinfish/ease-restapi
  license: MIT License
  summary: "ease-restapi is a Python package that use 'www.easemob.com' website REST API for you."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
