package:
  name: edna2
  version: "0.7.1"

source:
  fn: edna2-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/e/edna2/edna2-0.7.1.tar.gz
  md5: 0d6e6481cb6987eb9bfd7996f68011ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edna2 = edna2:main
    #
    # Would create an entry point called edna2 that calls edna2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - coverage
    - flask
    - beets
    - simplejson

  run:
    - python
    - nose
    - coverage
    - flask
    - beets
    - simplejson

test:
  # Python imports
  imports:
    - beetsplug
    - edna2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/aweidner/edna2
  license: LICENSE.txt
  summary: 'edna2 webserver for beets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
