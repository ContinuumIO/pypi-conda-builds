package:
  name: django-sabridge
  version: "0.0.1"

source:
  fn: django-sabridge-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sabridge/django-sabridge-0.0.1.tar.gz
  md5: ffd9301cf0a33f79638796ef1181fd2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sabridge = django-sabridge:main
    #
    # Would create an entry point called django-sabridge that calls django-sabridge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.6.0

  run:
    - python
    - sqlalchemy >=0.6.0

test:
  # Python imports
  imports:
    - sabridge
    - tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/johnpaulett/django-sabridge
  license: UNKNOWN
  summary: 'Provides SQLAlchemy access to Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
