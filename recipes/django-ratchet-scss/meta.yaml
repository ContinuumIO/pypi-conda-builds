package:
  name: django-ratchet-scss
  version: "2.0.2.1"

source:
  fn: django-ratchet-scss-2.0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ratchet-scss/django-ratchet-scss-2.0.2.1.tar.gz
  md5: ec8ef79dc7568a989860fc061804f07a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ratchet-scss = django-ratchet-scss:main
    #
    # Would create an entry point called django-ratchet-scss that calls django-ratchet-scss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-libsass
    - django-compressor

  run:
    - python
    - django-libsass
    - django-compressor

test:
  # Python imports
  imports:
    - ratchet_scss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A simple Django app to import ratchet-scss.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
