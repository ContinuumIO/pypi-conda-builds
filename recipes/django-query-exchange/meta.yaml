package:
  name: django-query-exchange
  version: "0.5"

source:
  fn: django-query-exchange-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-query-exchange/django-query-exchange-0.5.tar.gz
  md5: 1e690c9750081e3c76d9f72d554d31c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-query-exchange = django-query-exchange:main
    #
    # Would create an entry point called django-query-exchange that calls django-query-exchange.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - query_exchange
    - query_exchange.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/daevaorn/django-query-exchange/
  license: BSD License
  summary: 'Django application for handling GET query params for url creation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
