package:
  name: babelfish
  version: "0.5.4"

source:
  fn: babelfish-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/b/babelfish/babelfish-0.5.4.tar.gz
  md5: 49ef0bacc0886ffa2333bc85ca4b8799
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - babelfish = babelfish:main
    #
    # Would create an entry point called babelfish that calls babelfish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - babelfish
    - babelfish.converters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Diaoul/babelfish
  license: BSD License
  summary: 'A module to work with countries and languages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
