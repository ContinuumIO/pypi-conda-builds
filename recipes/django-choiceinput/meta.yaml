package:
  name: django-choiceinput
  version: "0.1.0"

source:
  fn: django-choiceinput-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-choiceinput/django-choiceinput-0.1.0.tar.gz
  md5: 5dccedf0a9dc3014508aa2035e713ae3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-choiceinput = django-choiceinput:main
    #
    # Would create an entry point called django-choiceinput that calls django-choiceinput.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0

  run:
    - python
    - django >=1.3.0

test:
  # Python imports
  imports:
    - choiceinput

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lqez/django-choiceinput
  license: MIT License
  summary: 'Display preset strings as select tag right beside TextInput widget in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
