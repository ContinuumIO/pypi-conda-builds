package:
  name: micropython-tty
  version: "1.0"

source:
  fn: micropython-tty-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-tty/micropython-tty-1.0.tar.gz
  md5: 5ed0f3afc1a55de6f96a5002f6de75e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-tty = micropython-tty:main
    #
    # Would create an entry point called micropython-tty that calls micropython-tty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: MIT
  summary: 'tty module for MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
