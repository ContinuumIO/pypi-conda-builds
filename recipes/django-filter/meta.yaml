package:
  name: django-filter
  version: "0.10.0"

source:
  fn: django-filter-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filter/django-filter-0.10.0.tar.gz
  md5: 8e29998b15eff06a6366aa8cc76c1c2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filter = django-filter:main
    #
    # Would create an entry point called django-filter that calls django-filter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_filters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alex/django-filter/tree/master
  license: BSD License
  summary: 'Django-filter is a reusable Django application for allowing users to filter querysets dynamically.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
