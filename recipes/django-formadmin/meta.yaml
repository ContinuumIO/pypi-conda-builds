package:
  name: django-formadmin
  version: "0.1"

source:
  fn: django-formadmin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formadmin/django-formadmin-0.1.tar.gz
  md5: a19e4df3a8622756184acc12e9caff64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formadmin = django-formadmin:main
    #
    # Would create an entry point called django-formadmin that calls django-formadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://appregsiter.readthedocs.org/
  license: MIT License
  summary: 'A Django app that provides the building blocks for an app registry system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
