package:
  name: coopr.neos
  version: "1.1.2"

source:
  fn: coopr.neos-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.neos/coopr.neos-1.1.2.tar.gz
  md5: 174e0b4739d2c3db8ef636ff05416fd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.neos = coopr.neos:main
    #
    # Would create an entry point called coopr.neos that calls coopr.neos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.neos
    - coopr.neos.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.neos
  license: BSD License
  summary: 'Coopr plugins that connect to NEOS solvers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
