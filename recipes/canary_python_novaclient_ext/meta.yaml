package:
  name: canary_python_novaclient_ext
  version: "1.0.106"

source:
  fn: canary_python_novaclient_ext-1.0.106.tar.gz
  url: https://pypi.python.org/packages/source/c/canary_python_novaclient_ext/canary_python_novaclient_ext-1.0.106.tar.gz
  md5: 3fd8f26517cb73c12d024cf92a958ec0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - canary_python_novaclient_ext = canary_python_novaclient_ext:main
    #
    # Would create an entry point called canary_python_novaclient_ext that calls canary_python_novaclient_ext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - canary_python_novaclient_ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gridcentric.com/
  license: UNKNOWN
  summary: 'Monitoring tools for Nova hosts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
