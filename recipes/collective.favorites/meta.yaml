package:
  name: collective.favorites
  version: "1.1.7"

source:
  fn: collective.favorites-1.1.7.zip
  url: https://pypi.python.org/packages/source/c/collective.favorites/collective.favorites-1.1.7.zip
  md5: 3f478cc1fb1c5cea9fb9ab847ce61ec1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.favorites = collective.favorites:main
    #
    # Would create an entry point called collective.favorites that calls collective.favorites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.favorites
    - collective.favorites.policies
    - collective.favorites.portlet
    - collective.favorites.tests
    - collective.favorites.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.favorites
  license: gpl
  summary: 'An extensible favorites system for Plone, with a portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
