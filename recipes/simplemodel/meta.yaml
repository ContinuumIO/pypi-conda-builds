package:
  name: simplemodel
  version: "0.1"

source:
  fn: simplemodel-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/simplemodel/simplemodel-0.1.tar.gz
  md5: bddd38ee178a78acc3bc973cb5703158
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplemodel = simplemodel:main
    #
    # Would create an entry point called simplemodel that calls simplemodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplemodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lann/simplemodel
  license: MIT
  summary: 'Simple example-based models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
