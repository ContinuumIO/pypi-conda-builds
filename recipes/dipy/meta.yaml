package:
  name: dipy
  version: "0.9.2"

source:
  fn: dipy-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dipy/dipy-0.9.2.tar.gz
  md5: e0cedd9db47a0c40706866b33a303dc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dipy = dipy:main
    #
    # Would create an entry point called dipy that calls dipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dipy
    - dipy.align
    - dipy.align.tests
    - dipy.boots
    - dipy.core
    - dipy.core.tests
    - dipy.data
    - dipy.denoise
    - dipy.denoise.tests
    - dipy.direction
    - dipy.direction.tests
    - dipy.external
    - dipy.external.tests
    - dipy.fixes
    - dipy.io
    - dipy.io.tests
    - dipy.reconst
    - dipy.reconst.benchmarks
    - dipy.reconst.tests
    - dipy.segment
    - dipy.segment.benchmarks
    - dipy.segment.tests
    - dipy.sims
    - dipy.sims.tests
    - dipy.testing
    - dipy.testing.tests
    - dipy.tests
    - dipy.tracking
    - dipy.tracking.benchmarks
    - dipy.tracking.local
    - dipy.tracking.local.tests
    - dipy.tracking.tests
    - dipy.utils
    - dipy.utils.tests
    - dipy.viz
    - dipy.viz.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dipy.org
  license: BSD License
  summary: 'Diffusion MRI utilities in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
