package:
  name: django-notify
  version: "1.1.2"

source:
  fn: django-notify-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-notify/django-notify-1.1.2.tar.gz
  md5: 5f6d9419bad678b8cd7d20bdd7cf5ce7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-notify = django-notify:main
    #
    # Would create an entry point called django-notify that calls django-notify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_notify
    - django_notify.storage
    - django_notify.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-notify/
  license: MIT License
  summary: 'A Django application which provides temporary notifications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
