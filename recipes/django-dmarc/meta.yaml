package:
  name: django-dmarc
  version: "0.1.10"

source:
  fn: django-dmarc-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dmarc/django-dmarc-0.1.10.tar.gz
  md5: 69487f227b8d137e909208bd2516433b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dmarc = django-dmarc:main
    #
    # Would create an entry point called django-dmarc that calls django-dmarc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dmarc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://p-o.co.uk/tech-articles/django-dmarc/
  license: BSD License
  summary: '\nMaking it easier to manage DMARC reports\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
