package:
  name: django-nullablecharfield
  version: "0.2.2"

source:
  fn: django-nullablecharfield-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nullablecharfield/django-nullablecharfield-0.2.2.tar.gz
  md5: ff8770ce31201651382564a6246ad428
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nullablecharfield = django-nullablecharfield:main
    #
    # Would create an entry point called django-nullablecharfield that calls django-nullablecharfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nullablecharfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alepane21/django-nullablecharfield
  license: BSD License
  summary: 'A simple Django app that allow the use of Nullable char field.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
