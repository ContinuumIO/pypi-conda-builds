package:
  name: mapentity
  version: "2.1.0"

source:
  fn: mapentity-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mapentity/mapentity-2.1.0.tar.gz
  md5: e3d2fee3a3d8a73789ad763984947d02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mapentity = mapentity:main
    #
    # Would create an entry point called mapentity that calls mapentity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - gdal >=1.9
    - gpxpy
    - beautifulsoup4
    - requests
    - django-modeltranslation
    - django-shapes
    - django-floppyforms
    - django-crispy-forms
    - django-compressor
    - django-filter
    - easy-thumbnails
    - django-tinymce
    - djangorestframework
    - djangorestframework-gis
    - django-appypod >=0.0.2
    - django-leaflet >=0.14
    - django-geojson >=2
    - paperclip
    - mock
    - factory_boy ==1.1.5

  run:
    - python
    - django >=1.4
    - gdal >=1.9
    - gpxpy
    - beautifulsoup4
    - requests
    - django-modeltranslation
    - django-shapes
    - django-floppyforms
    - django-crispy-forms
    - django-compressor
    - django-filter
    - easy-thumbnails
    - django-tinymce
    - djangorestframework
    - djangorestframework-gis
    - django-appypod >=0.0.2
    - django-leaflet >=0.14
    - django-geojson >=2
    - paperclip
    - mock
    - factory_boy ==1.1.5

test:
  # Python imports
  imports:
    - mapentity
    - mapentity.management
    - mapentity.management.commands
    - mapentity.serializers
    - mapentity.templatetags
    - mapentity.tests
    - mapentity.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/makinacorpus/django-mapentity
  license: BSD, see LICENSE file.
  summary: 'Generic CRUD with maps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
