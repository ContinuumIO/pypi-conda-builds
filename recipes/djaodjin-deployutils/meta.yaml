package:
  name: djaodjin-deployutils
  version: "0.2.2"

source:
  fn: djaodjin-deployutils-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djaodjin-deployutils/djaodjin-deployutils-0.2.2.tar.gz
  md5: 9efc4de12373711414330157de40eaa5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djaodjin-deployutils = djaodjin-deployutils:main
    #
    # Would create an entry point called djaodjin-deployutils that calls djaodjin-deployutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - deployutils
    - deployutils.backends
    - deployutils.management
    - deployutils.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djaodjin/djaodjin-deployutils/
  license: BSD
  summary: 'Management commands to deploy Django webapps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
