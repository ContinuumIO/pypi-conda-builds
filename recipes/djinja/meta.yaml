package:
  name: djinja
  version: "0.7"

source:
  fn: Djinja-0.7.tar.gz
  url: https://pypi.python.org/packages/source/D/Djinja/Djinja-0.7.tar.gz
  md5: f92a755157010b5862ba523f772cf888
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinja = djinja:main
    #
    # Would create an entry point called djinja that calls djinja.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - jinja2

  run:
    - python
    - django
    - jinja2

test:
  # Python imports
  imports:
    - djinja
    - djinja.contrib
    - djinja.contrib.admin
    - djinja.contrib.admin.templatetags
    - djinja.contrib.debug_toolbar
    - djinja.template
    - djinja.template.extended
    - djinja.utilities
    - djinja.utilities.templatetags
    - website_example
    - website_example.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/syrusakbary/djinja
  license: BSD License
  summary: 'A package that makes possible the integration of Jinja2 in Django, in a clean way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
