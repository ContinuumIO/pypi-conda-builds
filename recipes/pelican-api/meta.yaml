package:
  name: pelican-api
  version: "0.0.1b0"

source:
  fn: pelican-api-0.0.1b0.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-api/pelican-api-0.0.1b0.tar.gz
  md5: ef43e8b7a65f06eb24c24c82c89b2697
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-api = pelican-api:main
    #
    # Would create an entry point called pelican-api that calls pelican-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://okfn.org
  license: MIT License
  summary: u'A static API server running from Pelican.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
