package:
  name: django_testdriver
  version: "0.3"

source:
  fn: django_testdriver-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_testdriver/django_testdriver-0.3.tar.gz
  md5: bcde9ff66918335bcb612dfc4180e737
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_testdriver = django_testdriver:main
    #
    # Would create an entry point called django_testdriver that calls django_testdriver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - django_testdriver
    - django_testdriver.management
    - django_testdriver.management.commands
    - django_testdriver.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/spenoir/django_testdriver
  license: BSD License
  summary: 'A custom JsTestDriver spec runner etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
