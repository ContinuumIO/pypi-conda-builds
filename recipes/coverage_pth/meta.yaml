package:
  name: coverage_pth
  version: "0.0.1"

source:
  fn: coverage_pth-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coverage_pth/coverage_pth-0.0.1.tar.gz
  md5: 43d9107fa9663979bc0cf20b89bce63d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coverage_pth = coverage_pth:main
    #
    # Would create an entry point called coverage_pth that calls coverage_pth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coverage

  run:
    - python
    - coverage

test:
  # Python imports
  imports:
    - coverage_pth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Coverage PTH file to enable coverage at the virtualenv level'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
