package:
  name: flask-versioned
  version: "0.9.4-20101221"

source:
  fn: Flask-Versioned-0.9.4-20101221.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Versioned/Flask-Versioned-0.9.4-20101221.tar.gz
  md5: a2631a3acdfe5172149dfa5a62536233
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-versioned = flask-versioned:main
    #
    # Would create an entry point called flask-versioned that calls flask-versioned.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - setuptools
    - flask

test:
  # Python imports
  imports:
    - flaskext
    - flaskext.versioned

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pilt/flask-versioned
  license: BSD License
  summary: 'Add version info to file paths.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
