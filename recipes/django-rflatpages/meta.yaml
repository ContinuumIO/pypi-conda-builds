package:
  name: django-rflatpages
  version: "0.0.9"

source:
  fn: django-rflatpages-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rflatpages/django-rflatpages-0.0.9.tar.gz
  md5: f9ef312b38bef3957c3f7e51e513a516
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rflatpages = django-rflatpages:main
    #
    # Would create an entry point called django-rflatpages that calls django-rflatpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5
    - south ==0.7.5
    - django-compressor ==1.3
    - sorl-thumbnail ==11.12

  run:
    - python
    - django <1.5
    - south ==0.7.5
    - django-compressor ==1.3
    - sorl-thumbnail ==11.12

test:
  # Python imports
  imports:
    - rflatpages
    - rflatpages.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r0sk/django-rflatpages
  license: BSD License
  summary: 'Yet another Django Flatpages App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
