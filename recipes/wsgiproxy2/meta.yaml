package:
  name: wsgiproxy2
  version: "0.4.2"

source:
  fn: WSGIProxy2-0.4.2.zip
  url: https://pypi.python.org/packages/source/W/WSGIProxy2/WSGIProxy2-0.4.2.zip
  md5: 0f204d47f96b1229f94d26e3b67570dc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wsgiproxy2 = wsgiproxy2:main
    #
    # Would create an entry point called wsgiproxy2 that calls wsgiproxy2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob
    - six

  run:
    - python
    - setuptools
    - webob
    - six

test:
  # Python imports
  imports:
    - wsgiproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gawel/WSGIProxy2/
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
