package:
  name: django-extrasettings
  version: "0.1"

source:
  fn: django-extrasettings-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extrasettings/django-extrasettings-0.1.tar.gz
  md5: e260c01f44a54df12c050fed0b0bf81b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extrasettings = django-extrasettings:main
    #
    # Would create an entry point called django-extrasettings that calls django-extrasettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - extrasettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pistacchio/django-extrasettings
  license: MIT License
  summary: 'Admin-configurable Django settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
