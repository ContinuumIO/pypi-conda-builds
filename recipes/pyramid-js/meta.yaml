package:
  name: pyramid-js
  version: "0.1.3"

source:
  fn: pyramid-js-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid-js/pyramid-js-0.1.3.tar.gz
  md5: 2832aef41d8068416cd211f96703915a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid-js = pyramid-js:main
    #
    # Would create an entry point called pyramid-js that calls pyramid-js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - markupsafe

  run:
    - python
    - pyramid
    - markupsafe

test:
  # Python imports
  imports:
    - pyramid_js

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
