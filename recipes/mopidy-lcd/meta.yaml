package:
  name: mopidy-lcd
  version: "1.2.1"

source:
  fn: mopidy-lcd-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mopidy-lcd/mopidy-lcd-1.2.1.tar.gz
  md5: ce38995fc73076bb7fba268b7831188b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-lcd = mopidy-lcd:main
    #
    # Would create an entry point called mopidy-lcd that calls mopidy-lcd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mopidy-lcd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TerraBAS/mopidy-lcd
  license: Free For Home Use
  summary: "This is an MPC for Pi Music Box using  Adafruit's Character LCD with buttons"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
