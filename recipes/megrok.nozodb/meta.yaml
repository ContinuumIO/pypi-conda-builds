package:
  name: megrok.nozodb
  version: "0.1"

source:
  fn: megrok.nozodb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.nozodb/megrok.nozodb-0.1.tar.gz
  md5: 6b9e7f62b10c0d121ac6dcd8973494cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.nozodb = megrok.nozodb:main
    #
    # Would create an entry point called megrok.nozodb that calls megrok.nozodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok

  run:
    - python
    - setuptools
    - setuptools
    - grok

test:
  # Python imports
  imports:
    - megrok
    - megrok.nozodb
    - megrok.nozodb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi
  license: GPL
  summary: 'This package allows you to run grok without the zodb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
