package:
  name: django-ajax-upload-widget
  version: "0.5.5"

source:
  fn: django-ajax-upload-widget-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-upload-widget/django-ajax-upload-widget-0.5.5.tar.gz
  md5: b0efcb721b789a4744500ce32ab2f7a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-upload-widget = django-ajax-upload-widget:main
    #
    # Would create an entry point called django-ajax-upload-widget that calls django-ajax-upload-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajax_upload
    - ajax_upload.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zmathew/django-ajax-upload-widget
  license: BSD License
  summary: 'Provides AJAX file upload functionality for FileFields and ImageFields with a simple widget replacement in the form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
