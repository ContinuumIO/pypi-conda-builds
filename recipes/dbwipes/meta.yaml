package:
  name: dbwipes
  version: "0.0.25"

source:
  fn: dbwipes-0.0.25.tar.gz
  url: https://pypi.python.org/packages/source/d/dbwipes/dbwipes-0.0.25.tar.gz
  md5: 54e4e96da975df8a4e6c27382a6a61f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbwipes = dbwipes:main
    #
    # Would create an entry point called dbwipes that calls dbwipes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - psycopg2
    - sqlalchemy
    - flask-compress
    - scorpionsql
    - click

  run:
    - python
    - flask
    - psycopg2
    - sqlalchemy
    - flask-compress
    - scorpionsql
    - click

test:
  # Python imports
  imports:
    - dbwipes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sirrice/summary
  license: MIT
  summary: 'dbwipes a database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
