package:
  name: asm.workflow
  version: "0.1.8"

source:
  fn: asm.workflow-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/a/asm.workflow/asm.workflow-0.1.8.tar.gz
  md5: df5d8141a11078662f955d55335c035e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asm.workflow = asm.workflow:main
    #
    # Would create an entry point called asm.workflow that calls asm.workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grok
    - zope.deferredimport
    - asm.cms
    - zope.component
    - zope.event

  run:
    - python
    - setuptools
    - grok
    - zope.deferredimport
    - asm.cms
    - zope.component
    - zope.event

test:
  # Python imports
  imports:
    - asm
    - asm.workflow
    - asm.workflow.generations
    - asm.workflow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: proprietary
  summary: 'Workflow extension for the Assembly CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
