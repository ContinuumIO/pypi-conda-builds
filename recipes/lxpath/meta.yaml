package:
  name: lxpath
  version: "0.0.0"

source:
  fn: lxpath-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lxpath/lxpath-0.0.0.tar.gz
  md5: 15d22789c42e631cc5ea50da74ac154b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lxpath = lxpath:main
    #
    # Would create an entry point called lxpath that calls lxpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lxpath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xi/lxpath
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'utilities to work with common paths on linux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
