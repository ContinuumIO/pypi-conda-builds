package:
  name: collective.googledocsviewer
  version: "1.0b1"

source:
  fn: collective.googledocsviewer-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.googledocsviewer/collective.googledocsviewer-1.0b1.tar.gz
  md5: f948ddc3dbb026d593c6defa1d5517ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.googledocsviewer = collective.googledocsviewer:main
    #
    # Would create an entry point called collective.googledocsviewer that calls collective.googledocsviewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.configviews

  run:
    - python
    - setuptools
    - setuptools
    - collective.configviews

test:
  # Python imports
  imports:
    - collective
    - collective.googledocsviewer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Use google docs viewer to display embed version of your office files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
