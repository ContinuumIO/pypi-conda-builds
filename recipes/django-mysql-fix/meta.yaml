package:
  name: django-mysql-fix
  version: "0.1.6"

source:
  fn: django-mysql-fix-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mysql-fix/django-mysql-fix-0.1.6.tar.gz
  md5: 551ce41d3967baf33e612fd0ee9c2dad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mysql-fix = django-mysql-fix:main
    #
    # Would create an entry point called django-mysql-fix that calls django-mysql-fix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_mysql_fix
    - django_mysql_fix.backends
    - django_mysql_fix.backends.mysql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frol/django-mysql-fix
  license: UNKNOWN
  summary: 'This project contains optimizations (hacks) for MySQL for Django ORM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
