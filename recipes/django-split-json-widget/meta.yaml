package:
  name: django-split-json-widget
  version: "0.2"

source:
  fn: django-split-json-widget-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-split-json-widget/django-split-json-widget-0.2.tar.gz
  md5: d0f80383fbd0e8c8d8f50e9ad7721569
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-split-json-widget = django-split-json-widget:main
    #
    # Would create an entry point called django-split-json-widget that calls django-split-json-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - splitjson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abbasovalex/django-SplitJSONWidget-form
  license: MIT License
  summary: 'Provides a widget that renders JSON data as separate, editable inputs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
