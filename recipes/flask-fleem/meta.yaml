package:
  name: flask-fleem
  version: "0.0.5"

source:
  fn: Flask-Fleem-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Fleem/Flask-Fleem-0.0.5.tar.gz
  md5: b63f4b2a7258c421c85e66a08399bb32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-fleem = flask-fleem:main
    #
    # Would create an entry point called flask-fleem that calls flask-fleem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - flask-assets >=0.8
    - pyyaml >=3.10

  run:
    - python
    - flask >=0.9
    - flask-assets >=0.8
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - flask_fleem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thrisp/fleem
  license: MIT License
  summary: 'Theming for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
