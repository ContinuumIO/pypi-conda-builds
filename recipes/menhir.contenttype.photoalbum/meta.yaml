package:
  name: menhir.contenttype.photoalbum
  version: "0.4"

source:
  fn: menhir.contenttype.photoalbum-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/menhir.contenttype.photoalbum/menhir.contenttype.photoalbum-0.4.tar.gz
  md5: 9bad13dbd5f1000121da52c37747a61f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - menhir.contenttype.photoalbum = menhir.contenttype.photoalbum:main
    #
    # Would create an entry point called menhir.contenttype.photoalbum that calls menhir.contenttype.photoalbum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dolmen.app.content >=1.0b1
    - dolmen.app.layout
    - dolmen.app.security
    - dolmen.app.viewselector
    - dolmen.content >=0.7
    - dolmen.menu
    - fanstatic
    - grokcore.component
    - js.galleriffic >=2.0.1-1
    - megrok.chameleon
    - menhir.contenttype.image >=0.4
    - setuptools
    - zope.component
    - zope.container
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.intid

  run:
    - python
    - setuptools
    - dolmen.app.content >=1.0b1
    - dolmen.app.layout
    - dolmen.app.security
    - dolmen.app.viewselector
    - dolmen.content >=0.7
    - dolmen.menu
    - fanstatic
    - grokcore.component
    - js.galleriffic >=2.0.1-1
    - megrok.chameleon
    - menhir.contenttype.image >=0.4
    - setuptools
    - zope.component
    - zope.container
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.intid

test:
  # Python imports
  imports:
    - menhir
    - menhir.contenttype
    - menhir.contenttype.photoalbum
    - menhir.contenttype.photoalbum.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dolmen-project.org
  license: GNU General Public License (GPL)
  summary: 'Dolmen contenttype extension : photoalbum'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
