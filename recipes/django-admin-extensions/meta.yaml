package:
  name: django-admin-extensions
  version: "0.9.0"

source:
  fn: django-admin-extensions-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-extensions/django-admin-extensions-0.9.0.tar.gz
  md5: 54aa9768ecc689a167f06f9a2d316c98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-extensions = django-admin-extensions:main
    #
    # Would create an entry point called django-admin-extensions that calls django-admin-extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - adminextensions
    - adminextensions.templatetags
    - tests
    - tests.app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-admin-extensions
  license: UNKNOWN
  summary: 'Simple tools to extend the django admin site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
