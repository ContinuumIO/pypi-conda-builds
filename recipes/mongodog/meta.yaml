package:
  name: mongodog
  version: "0.1.1"

source:
  fn: mongodog-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mongodog/mongodog-0.1.1.tar.gz
  md5: a89567f6f6d5bda18e309f2604ee3bed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongodog = mongodog:main
    #
    # Would create an entry point called mongodog that calls mongodog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mongodog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Paulius-Maruska/mongodog
  license: Apache Software License
  summary: '\nMongoDog is a library designed to sniff out the things that your application is doing within\na mongo database. It plugs itself in between your app and pymongo and logs all of the commands\nthat your application sends to the server.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
