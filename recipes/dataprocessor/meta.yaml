package:
  name: dataprocessor
  version: "2.1.0"

source:
  fn: dataProcessor-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dataProcessor/dataProcessor-2.1.0.tar.gz
  md5: a78c08d803b19404f6ba512431e7e86e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataprocessor = dataprocessor:main
    #
    # Would create an entry point called dataprocessor that calls dataprocessor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dataProcessor
    - dataprocessor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://beef-wellington-inc.github.com
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A parser module that takes fractions and turns them into a percent, and puts them in a SQLite database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
