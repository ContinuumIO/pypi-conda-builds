package:
  name: cpu_affinity
  version: "0.1.0"

source:
  fn: cpu_affinity-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cpu_affinity/cpu_affinity-0.1.0.tar.gz
  md5: e737ae4799d3417f4b08b30f5cb37558
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cpu_affinity = cpu_affinity:main
    #
    # Would create an entry point called cpu_affinity that calls cpu_affinity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - affinity
    - cpu_affinity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/algodirect/affinity
  license: UNKNOWN
  summary: 'This is to support set/get CPU affinity in python2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
