package:
  name: simple_json
  version: "1.1"

source:
  fn: simple_json-1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/simple_json/simple_json-1.1.tar.gz
  md5: ec0a6732f4b172c4bb696d8fffbd6b65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple_json = simple_json:main
    #
    # Would create an entry point called simple_json that calls simple_json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - simple_json

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://undefined.org/python/#simple_json
  license: MIT License
  summary: 'Compatibility shim for simplejson'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
