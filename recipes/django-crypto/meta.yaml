package:
  name: django-crypto
  version: "0.2.0"

source:
  fn: django-crypto-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crypto/django-crypto-0.2.0.tar.gz
  md5: ae235dbd9aadcf3f450bcc2d67a3c1a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crypto = django-crypto:main
    #
    # Would create an entry point called django-crypto that calls django-crypto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_crypto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/votizen/django-crypto
  license: MIT License
  summary: 'Simple 2-way crypto functions for use with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
