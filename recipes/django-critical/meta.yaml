package:
  name: django-critical
  version: "0.1"

source:
  fn: django-critical-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-critical/django-critical-0.1.tar.gz
  md5: dcfe0392f4c1360bacb62c41fe2dd97d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-critical = django-critical:main
    #
    # Would create an entry point called django-critical that calls django-critical.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf >=0.4
    - cssmin >=0.2.0

  run:
    - python
    - django-appconf >=0.4
    - cssmin >=0.2.0

test:
  # Python imports
  imports:
    - critical
    - critical.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martinblech/django-critical
  license: MIT License
  summary: 'Inlines critical path CSS and defers loading full CSS asynchronously.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
