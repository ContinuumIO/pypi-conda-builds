package:
  name: linguistic-helper-functions
  version: "0.1.23"

source:
  fn: linguistic-helper-functions-0.1.23.tar.gz
  url: https://pypi.python.org/packages/source/l/linguistic-helper-functions/linguistic-helper-functions-0.1.23.tar.gz
  md5: fed0f00db1d507dec03abadbf990e342
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linguistic-helper-functions = linguistic-helper-functions:main
    #
    # Would create an entry point called linguistic-helper-functions that calls linguistic-helper-functions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - linghelper
    - linghelper.media

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/linguistic-helper-functions/
  license: LICENSE.txt
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
