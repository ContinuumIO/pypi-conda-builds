package:
  name: collective.collage.easyslider
  version: "1.0.4"

source:
  fn: collective.collage.easyslider-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collage.easyslider/collective.collage.easyslider-1.0.4.tar.gz
  md5: af8c65407c5f38ef342d16829ad7af0d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.easyslider = collective.collage.easyslider:main
    #
    # Would create an entry point called collective.collage.easyslider that calls collective.collage.easyslider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.collage
    - collective.easyslider

  run:
    - python
    - setuptools
    - setuptools
    - products.collage
    - collective.easyslider

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.easyslider
    - collective.collage.easyslider.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.collage.easyslider
  license: GPL
  summary: 'Collage view for content slide.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
