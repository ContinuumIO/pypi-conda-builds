package:
  name: logassert
  version: "1"

source:
  fn: logassert-1.tar.gz
  url: https://pypi.python.org/packages/source/l/logassert/logassert-1.tar.gz
  md5: 7935b23a0f4a65ea9816fe1b4f7c2750
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logassert = logassert:main
    #
    # Would create an entry point called logassert that calls logassert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - logassert

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/facundobatista/logassert
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Log Assertion.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
