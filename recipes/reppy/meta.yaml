package:
  name: reppy
  version: "0.3.0"

source:
  fn: reppy-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/r/reppy/reppy-0.3.0.tar.gz
  md5: e3f6f596e97e12f79115b2b2aa948b00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - reppy = reppy:main
    #
    # Would create an entry point called reppy that calls reppy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=1.5,!=2.0
    - url
    - requests

  run:
    - python
    - python-dateutil >=1.5,!=2.0
    - url
    - requests

test:
  # Python imports
  imports:
    - reppy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seomoz/reppy
  license: MIT License
  summary: 'Replacement robots.txt Parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
