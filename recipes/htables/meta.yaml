package:
  name: htables
  version: "0.5.1"

source:
  fn: htables-0.5.1.zip
  url: https://pypi.python.org/packages/source/h/htables/htables-0.5.1.zip
  md5: 05ec5f24fb38e68c94bc059e37001b8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htables = htables:main
    #
    # Would create an entry point called htables that calls htables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - htables

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/htables/
  license: BSD License
  summary: 'htables is a database library for storing mapping objects in a relational database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
