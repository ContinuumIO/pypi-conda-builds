package:
  name: django-moneybookers
  version: "0.2"

source:
  fn: django-moneybookers-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-moneybookers/django-moneybookers-0.2.tar.gz
  md5: f719a9116b780238119f1b70aff798d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-moneybookers = django-moneybookers:main
    #
    # Would create an entry point called django-moneybookers that calls django-moneybookers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - moneybookers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gotlium/django-moneybookers
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
