package:
  name: django-mixins
  version: "0.0.10"

source:
  fn: django-mixins-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mixins/django-mixins-0.0.10.tar.gz
  md5: a801415474e43462385a908a25e37898
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mixins = django-mixins:main
    #
    # Would create an entry point called django-mixins that calls django-mixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mixins
    - mixins.compat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/flashingpumpkin/django-mixins
  license: UNKNOWN
  summary: 'Useful mixins for Django class based views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
