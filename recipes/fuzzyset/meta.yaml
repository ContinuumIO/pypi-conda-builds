package:
  name: fuzzyset
  version: "0.0.9"

source:
  fn: fuzzyset-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/f/fuzzyset/fuzzyset-0.0.9.tar.gz
  md5: 22486108b767d5072ab006c2374fbad3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuzzyset = fuzzyset:main
    #
    # Would create an entry point called fuzzyset that calls fuzzyset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-levenshtein
    - texttable

  run:
    - python
    - python-levenshtein
    - texttable

test:
  # Python imports
  imports:
    - fuzzyset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/axiak/fuzzyset/
  license: BSD License
  summary: 'A simple python fuzzyset implementation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
