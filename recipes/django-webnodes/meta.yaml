package:
  name: django-webnodes
  version: "0.0.3"

source:
  fn: django-webnodes-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webnodes/django-webnodes-0.0.3.tar.gz
  md5: b8d2003d269a32e762f1dee5a0e60c9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webnodes = django-webnodes:main
    #
    # Would create an entry point called django-webnodes that calls django-webnodes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.7,<1.6

  run:
    - python
    - django >=1.3.7,<1.6

test:
  # Python imports
  imports:
    - webnodes
    - webnodes.ext
    - webnodes.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsk/django-webnodes
  license: MIT License
  summary: 'Re-usable bussiness components, modular units on a page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
