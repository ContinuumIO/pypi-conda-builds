package:
  name: zope.globalrequest
  version: "1.1"

source:
  fn: zope.globalrequest-1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.globalrequest/zope.globalrequest-1.1.tar.gz
  md5: e69b6a2cb8e31d1cb3fc483ca382947c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.globalrequest = zope.globalrequest:main
    #
    # Would create an entry point called zope.globalrequest that calls zope.globalrequest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.publisher
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.interface
    - zope.publisher
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.globalrequest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.globalrequest
  license: Zope Public License
  summary: 'Global way of retrieving the currently active request.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
