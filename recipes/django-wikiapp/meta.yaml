package:
  name: django-wikiapp
  version: "0.2.0"

source:
  fn: django-wikiapp-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wikiapp/django-wikiapp-0.2.0.tar.gz
  md5: 665e37c911e38689f538710841272bc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wikiapp = django-wikiapp:main
    #
    # Would create an entry point called django-wikiapp that calls django-wikiapp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - wiki
    - wiki.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/django-wikiapp/
  license: BSD
  summary: 'A simple pluggable wiki application for Django with revision and multiple markup support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
