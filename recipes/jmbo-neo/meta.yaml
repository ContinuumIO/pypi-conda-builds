package:
  name: jmbo-neo
  version: "0.4.5.1"

source:
  fn: jmbo-neo-0.4.5.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-neo/jmbo-neo-0.4.5.1.tar.gz
  md5: 14f2864c3ca4167c6661b215d053e89d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-neo = jmbo-neo:main
    #
    # Would create an entry point called jmbo-neo that calls jmbo-neo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmbo-foundry >=1.1.15,<1.3
    - django-ckeditor
    - requests
    - lxml
    - django >=1.4,<1.5
    - mock

  run:
    - python
    - jmbo-foundry >=1.1.15,<1.3
    - django-ckeditor
    - requests
    - lxml
    - django >=1.4,<1.5
    - mock

test:
  # Python imports
  imports:
    - neo
    - neo.management
    - neo.management.commands
    - neo.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-neo
  license: BSD License
  summary: 'Jmbo Neo Web Services integration app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
