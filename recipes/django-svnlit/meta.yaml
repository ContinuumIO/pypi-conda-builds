package:
  name: django-svnlit
  version: "0.1.1"

source:
  fn: django-svnlit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-svnlit/django-svnlit-0.1.1.tar.gz
  md5: b4bf2179fbbed2eaf5db79b5650722a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-svnlit = django-svnlit:main
    #
    # Would create an entry point called django-svnlit that calls django-svnlit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - svnlit
    - svnlit.management
    - svnlit.management.commands
    - svnlit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-svnlit/
  license: BSD License
  summary: 'Django-based subversion browser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
