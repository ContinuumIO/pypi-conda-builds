package:
  name: django-mothertongue
  version: "0.0.6"

source:
  fn: django-mothertongue-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mothertongue/django-mothertongue-0.0.6.tar.gz
  md5: f3921783e70f7dd1d37f646c003dbe8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mothertongue = django-mothertongue:main
    #
    # Would create an entry point called django-mothertongue that calls django-mothertongue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1.1
    - django-localeurl >=1.4

  run:
    - python
    - django >=1.1.1
    - django-localeurl >=1.4

test:
  # Python imports
  imports:
    - mothertongue
    - mothertongue.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robcharlwood/django-mothertongue
  license: MIT License
  summary: 'A light-weight translation application designed to simplify the process of creating multi-lingual websites and serving appropriate content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
