package:
  name: sake
  version: "0.9.0"

source:
  fn: sake-0.9.0.zip
  url: https://pypi.python.org/packages/source/s/sake/sake-0.9.0.zip
  md5: 65aed3509d8bbb74c4746aced316de30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sake = sake:main
    #
    # Would create an entry point called sake that calls sake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stacklesslib
    - paste
    - pyyaml

  run:
    - python
    - stacklesslib
    - paste
    - pyyaml

test:
  # Python imports
  imports:
    - sake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/sake/
  license: MIT License
  summary: 'Enables easier Stackless Python application development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
