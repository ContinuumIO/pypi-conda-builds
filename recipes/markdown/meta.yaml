package:
  name: markdown
  version: "2.6.2"

source:
  fn: Markdown-2.6.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Markdown/Markdown-2.6.2.tar.gz
  md5: 256d19afcc564dc4ce4c229bb762f7ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - markdown = markdown:main
    #
    # Would create an entry point called markdown that calls markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - markdown
    - markdown.extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pythonhosted.org/Markdown/
  license: BSD License
  summary: 'Python implementation of Markdown.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
