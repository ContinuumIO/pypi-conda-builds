package:
  name: hermes
  version: "0.3.0"

source:
  fn: hermes-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hermes/hermes-0.3.0.tar.gz
  md5: 823bf880f13081f32b632516acd9367a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hermes = hermes:main
    #
    # Would create an entry point called hermes that calls hermes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmpppy >=0.5.0rc1

  run:
    - python
    - xmpppy >=0.5.0rc1

test:
  # Python imports
  imports:
    - hermes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattlong/hermes
  license: ISC License (ISCL)
  summary: 'Hermes is an extensible XMPP-based chatroom server written in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
