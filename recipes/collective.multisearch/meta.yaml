package:
  name: collective.multisearch
  version: "1.0.3"

source:
  fn: collective.multisearch-1.0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.multisearch/collective.multisearch-1.0.3.zip
  md5: a21710cc931e02e498e1e8168a097012
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.multisearch = collective.multisearch:main
    #
    # Would create an entry point called collective.multisearch that calls collective.multisearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok
    - feedparser

  run:
    - python
    - setuptools
    - setuptools
    - five.grok
    - feedparser

test:
  # Python imports
  imports:
    - collective
    - collective.multisearch
    - collective.multisearch.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/collective.multisearch
  license: GPL
  summary: 'Portlet based display for the Plone search page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
