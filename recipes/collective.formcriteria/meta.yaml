package:
  name: collective.formcriteria
  version: "2.0"

source:
  fn: collective.formcriteria-2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.formcriteria/collective.formcriteria-2.0.tar.gz
  md5: fc4961fb66225a9588edcdbe62176c33
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.formcriteria = collective.formcriteria:main
    #
    # Would create an entry point called collective.formcriteria that calls collective.formcriteria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.browserlayer

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.browserlayer

test:
  # Python imports
  imports:
    - collective
    - collective.formcriteria
    - collective.formcriteria.columns
    - collective.formcriteria.criteria
    - collective.formcriteria.csv
    - collective.formcriteria.foldercontents
    - collective.formcriteria.form
    - collective.formcriteria.portlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.formcriteria
  license: GPL
  summary: 'Add forms for user enterable search criteria to collections.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
