package:
  name: glitter
  version: "0.1.7"

source:
  fn: glitter-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/g/glitter/glitter-0.1.7.tar.gz
  md5: e8fdbfac181fed4046ffdd075116b9d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glitter = glitter:main
    #
    # Would create an entry point called glitter that calls glitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - examples
    - glitter
    - glitter.arrays
    - glitter.contexts
    - glitter.convenience
    - glitter.framebuffers
    - glitter.misc
    - glitter.raw
    - glitter.shaders
    - glitter.textures
    - glitter.utils
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/glitter
  license: MIT License
  summary: 'Intuitive OpenGL wrappers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
