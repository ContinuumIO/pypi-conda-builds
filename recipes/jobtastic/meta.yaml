package:
  name: jobtastic
  version: "0.2.2"

source:
  fn: jobtastic-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jobtastic/jobtastic-0.2.2.tar.gz
  md5: 05ae6a7a1c70a3b4940b9f438fcce4da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobtastic = jobtastic:main
    #
    # Would create an entry point called jobtastic that calls jobtastic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=2.5
    - psutil

  run:
    - python
    - celery >=2.5
    - psutil

test:
  # Python imports
  imports:
    - jobtastic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://policystat.github.com/jobtastic
  license: BSD License
  summary: 'Make your user-facing Celery jobs totally awesomer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
