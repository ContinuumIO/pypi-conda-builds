package:
  name: li-pagador-paypal
  version: "2.0.8"

source:
  fn: li-pagador-paypal-2.0.8.tar.gz
  url: https://pypi.python.org/packages/source/l/li-pagador-paypal/li-pagador-paypal-2.0.8.tar.gz
  md5: 2f88f4fc19d6ad82a6d96c7de6ff0840
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - li-pagador-paypal = li-pagador-paypal:main
    #
    # Would create an entry point called li-pagador-paypal that calls li-pagador-paypal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - li-pagador

  run:
    - python
    - distribute
    - li-pagador

test:
  # Python imports
  imports:
    - pagador_paypal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maethorin/LI-Pagador-PayPal
  license: MIT License
  summary: 'Meio de pagamento usando o PayPal (https://www.paypal.com)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
