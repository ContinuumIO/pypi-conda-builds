package:
  name: django-flexisettings
  version: "0.2.5"

source:
  fn: django-flexisettings-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flexisettings/django-flexisettings-0.2.5.tar.gz
  md5: 72c08e5042b135098a517f3a49b97436
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flexisettings = django-flexisettings:main
    #
    # Would create an entry point called django-flexisettings that calls django-flexisettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - flexisettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ledapei/django-flexisettings
  license: BSD License
  summary: 'Django flexible settings with running environment support, separate security files and project layout detection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
