package:
  name: estnltk
  version: "1.1"

source:
  fn: estnltk-1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/estnltk/estnltk-1.1.tar.gz
  md5: 987f74cfc7505762b2b39f3846dc0640
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - estnltk = estnltk:main
    #
    # Would create an entry point called estnltk that calls estnltk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nltk >=3.0.0
    - python-crfsuite >=0.8.1
    - jsonpath-rw >=1.3.0
    - six >=1.7.3
    - tempdir >=0.6
    - xmltodict >=0.9.0
    - beautifulsoup4 >=4.3.2
    - pandas >=0.14.1
    - scikit-learn >=0.15.1
    - xlrd >=0.9.2
    - xlsxwriter >=0.5.7
    - numpy >=1.8.2
    - scipy >=0.11.0
    - pytz >=2014.4
    - python-dateutil >=2.2
    - nose >=1.3.3
    - pyparsing >=2.0.2
    - matplotlib >=1.2.1
    - xlwt-future >=0.7.5

  run:
    - python
    - nltk >=3.0.0
    - python-crfsuite >=0.8.1
    - jsonpath-rw >=1.3.0
    - six >=1.7.3
    - tempdir >=0.6
    - xmltodict >=0.9.0
    - beautifulsoup4 >=4.3.2
    - pandas >=0.14.1
    - scikit-learn >=0.15.1
    - xlrd >=0.9.2
    - xlsxwriter >=0.5.7
    - numpy >=1.8.2
    - scipy >=0.11.0
    - pytz >=2014.4
    - python-dateutil >=2.2
    - nose >=1.3.3
    - pyparsing >=2.0.2
    - matplotlib >=1.2.1
    - xlwt-future >=0.7.5

test:
  # Python imports
  imports:
    - estnltk
    - estnltk.estner
    - estnltk.examples
    - estnltk.mw_verbs
    - estnltk.pyvabamorf
    - estnltk.pyvabamorf.tests
    - estnltk.tests
    - estnltk.textclassifier
    - estnltk.textclassifier.tests
    - estnltk.wordnet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tpetmanson/estnltk
  license: GPL
  summary: 'Open source tools for Estonian natural language processing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
