package:
  name: argparse_tools
  version: "1.0.5"

source:
  fn: argparse_tools-1.0.5.zip
  url: https://pypi.python.org/packages/source/a/argparse_tools/argparse_tools-1.0.5.zip
  md5: 41fdb690bc3f55c3915d4c4197ab1110
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argparse_tools = argparse_tools:main
    #
    # Would create an entry point called argparse_tools that calls argparse_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse

  run:
    - python
    - argparse

test:
  # Python imports
  imports:
    - argparse_tools
    - argparse_tools.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adgaudio/argparse_tools
  license: UNKNOWN
  summary: 'Share a standardized set of argparse arguments within your codebase'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
