package:
  name: jsmod
  version: "0.0.2"

source:
  fn: jsmod-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsmod/jsmod-0.0.2.tar.gz
  md5: e94c01ad4c5194783179af94e41e9f03
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsmod = jsmod:main
    #
    # Would create an entry point called jsmod that calls jsmod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyv8 >=1.0-dev

  run:
    - python
    - pyv8 >=1.0-dev

test:
  # Python imports
  imports:
    - jsmod

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sciyoshi/jsmod/
  license: MIT License
  summary: 'Import JavaScript modules directly from Python code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
