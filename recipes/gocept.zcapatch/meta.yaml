package:
  name: gocept.zcapatch
  version: "1.0"

source:
  fn: gocept.zcapatch-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.zcapatch/gocept.zcapatch-1.0.tar.gz
  md5: 44e2b4759004d9cb933c86be2a762865
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.zcapatch = gocept.zcapatch:main
    #
    # Would create an entry point called gocept.zcapatch that calls gocept.zcapatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component

  run:
    - python
    - setuptools
    - zope.component

test:
  # Python imports
  imports:
    - gocept
    - gocept.zcapatch
    - gocept.zcapatch.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.gocept.com/hg/public/gocept.zcapatch
  license: ZPL
  summary: 'Test helpers to temporarily alter zope.component registrations\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
