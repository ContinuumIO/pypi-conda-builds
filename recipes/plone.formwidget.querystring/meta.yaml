package:
  name: plone.formwidget.querystring
  version: "1.1.4"

source:
  fn: plone.formwidget.querystring-1.1.4.zip
  url: https://pypi.python.org/packages/source/p/plone.formwidget.querystring/plone.formwidget.querystring-1.1.4.zip
  md5: 864da1b8ca55f3a005b21fa121bef16b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.formwidget.querystring = plone.formwidget.querystring:main
    #
    # Would create an entry point called plone.formwidget.querystring that calls plone.formwidget.querystring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.querystring

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.querystring

test:
  # Python imports
  imports:
    - plone
    - plone.formwidget
    - plone.formwidget.querystring

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.formwidget.querystring
  license: GPL
  summary: 'A widget for composing a Query string/search.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
