package:
  name: friendlydb
  version: "2.0.0"

source:
  fn: friendlydb-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/f/friendlydb/friendlydb-2.0.0.tar.gz
  md5: 5d83e363f4e1e8d3446e0afb9df3f926
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - friendlydb = friendlydb:main
    #
    # Would create an entry point called friendlydb that calls friendlydb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - redis >=2.72

  run:
    - python
    - redis >=2.72

test:
  # Python imports
  imports:
    - friendlydb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/toastdriven/friendlydb
  license: BSD License
  summary: 'A small & fast following/followers database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
