package:
  name: logilab-packaging
  version: "1.0.0"

source:
  fn: logilab-packaging-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logilab-packaging/logilab-packaging-1.0.0.tar.gz
  md5: 27e7de7faa136651b701ec3d3ab7b873
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logilab-packaging = logilab-packaging:main
    #
    # Would create an entry point called logilab-packaging that calls logilab-packaging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - logilab-common

  run:
    - python
    - logilab-common

test:
  # Python imports
  imports:
    - logilab.packaging
    - logilab.packaging.lgp
    - logilab.packaging.lib
    - logilab.packaging.test
    - logilab.packaging.test.data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.logilab.org/project/logilab-packaging
  license: GPL
  summary: 'tools used at Logilab to create debian packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
