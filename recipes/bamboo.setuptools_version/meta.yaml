package:
  name: bamboo.setuptools_version
  version: "0.1.0"

source:
  fn: bamboo.setuptools_version-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bamboo.setuptools_version/bamboo.setuptools_version-0.1.0.tar.gz
  md5: 9161e779e899062f94406c09225a3b1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bamboo.setuptools_version = bamboo.setuptools_version:main
    #
    # Would create an entry point called bamboo.setuptools_version that calls bamboo.setuptools_version.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bamboo
    - bamboo.setuptools_version

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Connexions/bamboo.setuptools_version
  license: LICENSE.txt
  summary: 'Tiny package to dop just one things -0 solve gettng a vfersion number for a package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
