package:
  name: cliff-tablib
  version: "1.1"

source:
  fn: cliff-tablib-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cliff-tablib/cliff-tablib-1.1.tar.gz
  md5: 5c0219ec93b760ec601d62e59b56f6a7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cliff-tablib = cliff-tablib:main
    #
    # Would create an entry point called cliff-tablib that calls cliff-tablib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cliff
    - tablib

  run:
    - python
    - setuptools
    - cliff
    - tablib

test:
  # Python imports
  imports:
    - clifftablib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dreamhost/cliff-tablib
  license: Apache Software License
  summary: 'tablib formatters for cliff'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
