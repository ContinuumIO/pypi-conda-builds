package:
  name: bigmax
  version: "3.5.1"

source:
  fn: bigmax-3.5.1.zip
  url: https://pypi.python.org/packages/source/b/bigmax/bigmax-3.5.1.zip
  md5: b0cb8f73247616c115a583725c88f35e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigmax = bigmax:main
    #
    # Would create an entry point called bigmax that calls bigmax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - pyramid_tm
    - pyramid_debugtoolbar
    - pyramid_beaker
    - pyramid_osiris
    - pymongo
    - rfc3339
    - requests
    - waitress
    - pygments
    - datetime

  run:
    - python
    - setuptools
    - pyramid
    - pyramid_tm
    - pyramid_debugtoolbar
    - pyramid_beaker
    - pyramid_osiris
    - pymongo
    - rfc3339
    - requests
    - waitress
    - pygments
    - datetime

test:
  # Python imports
  imports:
    - bigmax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/upcnet/bigmax
  license: UNKNOWN
  summary: 'Big MAX - MAX Web Administration Interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
