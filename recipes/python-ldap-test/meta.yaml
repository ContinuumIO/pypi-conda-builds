package:
  name: python-ldap-test
  version: "0.2.0"

source:
  fn: python-ldap-test-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-ldap-test/python-ldap-test-0.2.0.tar.gz
  md5: 1ede4fcc6a3360584fc143ddcc4f7412
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-ldap-test = python-ldap-test:main
    #
    # Would create an entry point called python-ldap-test that calls python-ldap-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - py4j >=0.8

  run:
    - python
    - py4j >=0.8

test:
  # Python imports
  imports:
    - ldap_test
    - ldap_test.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zoldar/python-ldap-test/
  license: LICENSE.txt
  summary: 'Tool for testing code speaking with LDAP server. Allows to easily configure and run an embedded, in-memory LDAP server. Uses UnboundID LDAP SDK through Py4J.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
