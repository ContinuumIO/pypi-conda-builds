package:
  name: django-timecode
  version: "0.1.4"

source:
  fn: django-timecode-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timecode/django-timecode-0.1.4.tar.gz
  md5: 3ade6ddc01235217f2c3ae6893014e0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timecode = django-timecode:main
    #
    # Would create an entry point called django-timecode that calls django-timecode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - timecode
    - timecode.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joerick/django-timecode/
  license: LICENSE.txt
  summary: 'Provides classes for working with timecodes (as used in the video industry).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
