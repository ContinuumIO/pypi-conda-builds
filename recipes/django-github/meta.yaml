package:
  name: django-github
  version: "0.1.2"

source:
  fn: django-github-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-github/django-github-0.1.2.tar.gz
  md5: 129614c3b3060a367d1cda081515ad76
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-github = django-github:main
    #
    # Would create an entry point called django-github that calls django-github.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - github
    - github.libs
    - github.management
    - github.management.commands
    - github.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coleifer/django-github/tree/master
  license: MIT License
  summary: "simple django integration with github's v2 api"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
