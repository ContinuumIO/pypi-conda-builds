package:
  name: django-gears
  version: "0.7.1"

source:
  fn: django-gears-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gears/django-gears-0.7.1.tar.gz
  md5: 2be7c04bf6f8d36c1bc7e6b1801a5318
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gears = django-gears:main
    #
    # Would create an entry point called django-gears that calls django-gears.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gears >=0.7

  run:
    - python
    - gears >=0.7

test:
  # Python imports
  imports:
    - django_gears
    - django_gears.management
    - django_gears.management.commands
    - django_gears.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gears/django-gears
  license: ISC License (ISCL)
  summary: 'Compiles and concatenates JavaScript and CSS assets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
