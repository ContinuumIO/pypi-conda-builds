package:
  name: infi.watchdog
  version: "0.6.3"

source:
  fn: infi.watchdog-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.watchdog/infi.watchdog-0.6.3.tar.gz
  md5: cfd0c2b4fdb855bda508db796acc2de7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.watchdog = infi.watchdog:main
    #
    # Would create an entry point called infi.watchdog that calls infi.watchdog.main()

    - watchmedo = watchdog.watchmedo:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.09
    - argh >=0.8.1
    - pathtools

  run:
    - python
    - pyyaml >=3.09
    - argh >=0.8.1
    - pathtools

test:
  # Python imports
  imports:
    - watchdog
    - watchdog.observers
    - watchdog.tricks
    - watchdog.utils

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - watchmedo --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gorakhargosh/watchdog
  license: Apache Software License
  summary: 'Filesystem events monitoring'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
