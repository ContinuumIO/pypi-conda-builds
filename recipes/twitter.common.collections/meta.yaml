package:
  name: twitter.common.collections
  version: "0.3.3"

source:
  fn: twitter.common.collections-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/t/twitter.common.collections/twitter.common.collections-0.3.3.tar.gz
  md5: 902ebdb7e3daa5d6a5c33f5cc6ea5f8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - twitter.common.collections = twitter.common.collections:main
    #
    # Would create an entry point called twitter.common.collections that calls twitter.common.collections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twitter.common.lang ==0.3.3

  run:
    - python
    - twitter.common.lang ==0.3.3

test:
  # Python imports
  imports:
    - twitter
    - twitter.common
    - twitter.common.collections

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twitter/commons
  license: Apache Software License
  summary: 'twitter.common collections library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
