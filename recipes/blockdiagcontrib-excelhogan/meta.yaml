package:
  name: blockdiagcontrib-excelhogan
  version: "0.1.0"

source:
  fn: blockdiagcontrib-excelhogan-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-excelhogan/blockdiagcontrib-excelhogan-0.1.0.tar.gz
  md5: 7bcb2618cd08ff32522cc9aab430dc96
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-excelhogan = blockdiagcontrib-excelhogan:main
    #
    # Would create an entry point called blockdiagcontrib-excelhogan that calls blockdiagcontrib-excelhogan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.2.0
    - setuptools
    - openpyxl

  run:
    - python
    - setuptools
    - blockdiag >=1.2.0
    - setuptools
    - openpyxl

test:
  # Python imports
  imports:
    - blockdiagcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tk0miya/blockdiagcontrib
  license: Python Software Foundation License
  summary: 'imagedrawer plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
