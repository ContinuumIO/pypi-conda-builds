package:
  name: toredis-fork
  version: "0.1.4"

source:
  fn: toredis-fork-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/t/toredis-fork/toredis-fork-0.1.4.tar.gz
  md5: e8dbb0d1492050a3da7267e2456182d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - toredis-fork = toredis-fork:main
    #
    # Would create an entry point called toredis-fork that calls toredis-fork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - hiredis

  run:
    - python
    - tornado
    - hiredis

test:
  # Python imports
  imports:
    - toredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mrjoes/toredis/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Really simple async Redis client for Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
