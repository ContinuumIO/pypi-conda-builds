package:
  name: admin-tools-zinnia
  version: "0.1"

source:
  fn: admin-tools-zinnia-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/admin-tools-zinnia/admin-tools-zinnia-0.1.tar.gz
  md5: a98e13eba3561fb5a4c90aef1208a1f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - admin-tools-zinnia = admin-tools-zinnia:main
    #
    # Would create an entry point called admin-tools-zinnia that calls admin-tools-zinnia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_tools_zinnia
    - admin_tools_zinnia.modules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Fantomas42/admin-tools-zinnia
  license: BSD License
  summary: 'Admin tools for django-blog-zinnia'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
