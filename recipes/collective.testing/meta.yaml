package:
  name: collective.testing
  version: "0.3"

source:
  fn: collective.testing-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.testing/collective.testing-0.3.tar.gz
  md5: e09ec6a8311477c7af8a8c8c69c80018
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.testing = collective.testing:main
    #
    # Would create an entry point called collective.testing that calls collective.testing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.testing/trunk#egg=collective.testing
  license: MIT
  summary: 'general package for testing and debugging aids for CMF, Plone, Zope2 and Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
