package:
  name: django-relationships
  version: "0.3.3"

source:
  fn: django-relationships-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-relationships/django-relationships-0.3.3.tar.gz
  md5: fa5d0299c2975bb22ec79c1cfbe3aa86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-relationships = django-relationships:main
    #
    # Would create an entry point called django-relationships that calls django-relationships.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - relationships
    - relationships.migrations
    - relationships.relationships_tests
    - relationships.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coleifer/django-relationships/tree/master
  license: MIT License
  summary: 'descriptive relationships between auth.User'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
