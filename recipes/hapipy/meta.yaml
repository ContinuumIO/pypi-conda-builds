package:
  name: hapipy
  version: "2.10.0"

source:
  fn: hapipy-2.10.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hapipy/hapipy-2.10.0.tar.gz
  md5: d10dcc9cb0e938131b3e995cbdf2e275
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hapipy = hapipy:main
    #
    # Would create an entry point called hapipy that calls hapipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose ==1.1.2
    - unittest2 ==0.5.1
    - simplejson >=2.1.2

  run:
    - python
    - nose ==1.1.2
    - unittest2 ==0.5.1
    - simplejson >=2.1.2

test:
  # Python imports
  imports:
    - hapi
    - hapi.mixins
    - hapipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HubSpot/hapipy
  license: LICENSE.txt
  summary: "A python wrapper around HubSpot's APIs"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
