package:
  name: monocle_yandexmap
  version: "0.0.4"

source:
  fn: monocle_yandexmap-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle_yandexmap/monocle_yandexmap-0.0.4.tar.gz
  md5: 992aa01d59218700f1b58b12989b59aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle_yandexmap = monocle_yandexmap:main
    #
    # Would create an entry point called monocle_yandexmap that calls monocle_yandexmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monocle_yandexmap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Yandex maps module for monocle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
