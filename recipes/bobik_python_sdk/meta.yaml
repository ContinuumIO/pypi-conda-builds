package:
  name: bobik_python_sdk
  version: "1.0"

source:
  fn: bobik_python_sdk-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bobik_python_sdk/bobik_python_sdk-1.0.tar.gz
  md5: 8b8a2f51892d53d245c1b26a47b6561f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bobik_python_sdk = bobik_python_sdk:main
    #
    # Would create an entry point called bobik_python_sdk that calls bobik_python_sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=0.12

  run:
    - python
    - requests >=0.12

test:
  # Python imports
  imports:
    - bobik
    - bobik_python_sdk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/emirkin/bobik_python_sdk
  license: UNKNOWN
  summary: 'Bobik Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
