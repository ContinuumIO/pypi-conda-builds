package:
  name: aiohttp-wsgi
  version: "0.1.2"

source:
  fn: aiohttp-wsgi-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aiohttp-wsgi/aiohttp-wsgi-0.1.2.tar.gz
  md5: 4f651f6b7cc0dc8e5c0aaaa4b361864b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiohttp-wsgi = aiohttp-wsgi:main
    #
    # Would create an entry point called aiohttp-wsgi that calls aiohttp-wsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aiohttp

  run:
    - python
    - aiohttp

test:
  # Python imports
  imports:
    - aiohttp_wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/etianen/aiohttp-wsgi
  license: BSD License
  summary: 'WSGI adapter for aiohttp.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
