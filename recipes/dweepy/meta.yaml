package:
  name: dweepy
  version: "0.2.0"

source:
  fn: dweepy-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dweepy/dweepy-0.2.0.tar.gz
  md5: 5ad51670567eec55f578fdc2583e93db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dweepy = dweepy:main
    #
    # Would create an entry point called dweepy that calls dweepy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2,<3

  run:
    - python
    - requests >=2,<3

test:
  # Python imports
  imports:
    - dweepy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paddycarey/dweepy
  license: MIT License
  summary: 'Dweepy is a Python client for dweet.io'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
