package:
  name: collective.js.twittertext
  version: "1.0"

source:
  fn: collective.js.twittertext-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.twittertext/collective.js.twittertext-1.0.tar.gz
  md5: 161498110c0030bd70fb852e7ce86f3e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.twittertext = collective.js.twittertext:main
    #
    # Would create an entry point called collective.js.twittertext that calls collective.js.twittertext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.twittertext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.twittertext
  license: GNU General Public License (GPL)
  summary: "Add twitter-text.js library to Plone's resource registry"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
