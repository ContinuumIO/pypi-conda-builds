package:
  name: discomll
  version: "0.1.3"

source:
  fn: discomll-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/discomll/discomll-0.1.3.tar.gz
  md5: b9d12bf3cacf91eaace60c078017cb06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - discomll = discomll:main
    #
    # Would create an entry point called discomll that calls discomll.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - discomll
    - discomll.classification
    - discomll.clustering
    - discomll.datasets
    - discomll.ensemble
    - discomll.ensemble.core
    - discomll.examples
    - discomll.examples.big_data
    - discomll.examples.classification
    - discomll.examples.clustering
    - discomll.examples.ensemble
    - discomll.examples.regression
    - discomll.regression
    - discomll.tests
    - discomll.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/romanorac/discomll
  license: Apache License, Version 2.0
  summary: 'Disco Machine Learning Library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
