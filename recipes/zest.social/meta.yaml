package:
  name: zest.social
  version: "1.3"

source:
  fn: zest.social-1.3.zip
  url: https://pypi.python.org/packages/source/z/zest.social/zest.social-1.3.zip
  md5: 329ae971445d01c0a5ca83a47e22b8e2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zest.social = zest.social:main
    #
    # Would create an entry point called zest.social that calls zest.social.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - zest
    - zest.social
    - zest.social.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/zest.social
  license: GPL
  summary: 'Zest Social Bookmarking'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
