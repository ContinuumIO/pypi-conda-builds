package:
  name: django_csp
  version: "2.0.3"

source:
  fn: django_csp-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_csp/django_csp-2.0.3.tar.gz
  md5: de8d9a8b7be3ebb80e0837a60cf11f8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_csp = django_csp:main
    #
    # Would create an entry point called django_csp that calls django_csp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - csp
    - csp.tests
    - django_csp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mozilla/django-csp
  license: BSD License
  summary: 'Django Content Security Policy support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
