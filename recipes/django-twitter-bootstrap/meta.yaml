package:
  name: django-twitter-bootstrap
  version: "3.3.0"

source:
  fn: django-twitter-bootstrap-3.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twitter-bootstrap/django-twitter-bootstrap-3.3.0.tar.gz
  md5: 74ae99cd6ad93e6a93003dc5ee5d789c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twitter-bootstrap = django-twitter-bootstrap:main
    #
    # Would create an entry point called django-twitter-bootstrap that calls django-twitter-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - twitter_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/estebistec/django-twitter-bootstrap
  license: MIT License
  summary: 'Provides a Django app whose static folder contains Bootstrap assets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
