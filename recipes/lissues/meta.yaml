package:
  name: lissues
  version: "0.1.0"

source:
  fn: lissues-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lissues/lissues-0.1.0.tar.gz
  md5: 7f5140f275771e86249366850c22c2ab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lissues = lissues:main
    #
    # Would create an entry point called lissues that calls lissues.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyactiveresource

  run:
    - python
    - setuptools
    - pyactiveresource

test:
  # Python imports
  imports:
    - lissues

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gcoop.coop
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A simple client to redmine issues tracker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
