package:
  name: lrdatatypes
  version: "0.1.1"

source:
  fn: lrdatatypes-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lrdatatypes/lrdatatypes-0.1.1.tar.gz
  md5: 299ef8e048c5ca364bc73cfea7658c34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lrdatatypes = lrdatatypes:main
    #
    # Would create an entry point called lrdatatypes that calls lrdatatypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - voluptuous ==0.8.6
    - pycountry ==1.8
    - uk-postcode-utils ==0.1.0
    - flask ==0.10.1
    - flask-wtf ==0.10.2
    - geojson ==1.0.9
    - simplejson ==3.6.4
    - python-dateutil ==2.2

  run:
    - python
    - voluptuous ==0.8.6
    - pycountry ==1.8
    - uk-postcode-utils ==0.1.0
    - flask ==0.10.1
    - flask-wtf ==0.10.2
    - geojson ==1.0.9
    - simplejson ==3.6.4
    - python-dateutil ==2.2

test:
  # Python imports
  imports:
    - datatypes
    - datatypes.validators
    - datatypes.voluptuous_helpers
    - lrdatatypes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/LandRegistry/audit-plugin
  license: MIT
  summary: 'Provides core Land Registry data types to dependent applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
