package:
  name: cs.portlet.aemet
  version: "1.3"

source:
  fn: cs.portlet.aemet-1.3.zip
  url: https://pypi.python.org/packages/source/c/cs.portlet.aemet/cs.portlet.aemet-1.3.zip
  md5: 6a893eb668b604889dcf761c4adfd8fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.portlet.aemet = cs.portlet.aemet:main
    #
    # Would create an entry point called cs.portlet.aemet that calls cs.portlet.aemet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - lxml

  run:
    - python
    - setuptools
    - setuptools
    - lxml

test:
  # Python imports
  imports:
    - cs
    - cs.portlet
    - cs.portlet.aemet
    - cs.portlet.aemet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codesyntax/cs.portlet.aemet
  license: GPL
  summary: 'A portlet to show the weather via AEMET'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
