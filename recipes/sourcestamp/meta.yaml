package:
  name: sourcestamp
  version: "0.1"

source:
  fn: sourcestamp-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sourcestamp/sourcestamp-0.1.tar.gz
  md5: 505bc518505e593582109abf21f8dc10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sourcestamp = sourcestamp:main
    #
    # Would create an entry point called sourcestamp that calls sourcestamp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sourcestamp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dnarvaez/sourcestamp
  license: Apache Software License
  summary: 'Compute timestamp for a source code tree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
