package:
  name: django-languages-plus
  version: "0.1.5"

source:
  fn: django-languages-plus-0.1.5.zip
  url: https://pypi.python.org/packages/source/d/django-languages-plus/django-languages-plus-0.1.5.zip
  md5: 04593f97b73ef5e04c902f88716002c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-languages-plus = django-languages-plus:main
    #
    # Would create an entry point called django-languages-plus that calls django-languages-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - languages_plus
    - languages_plus.migrations
    - languages_plus.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cordery/django-languages-plus
  license: LICENSE.txt
  summary: 'A django model & fixture containing common languages and culture codes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
