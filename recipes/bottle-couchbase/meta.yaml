package:
  name: bottle-couchbase
  version: "0.2.0"

source:
  fn: bottle-couchbase-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-couchbase/bottle-couchbase-0.2.0.tar.gz
  md5: e180cbed4ccca64ba4a2ae28c1de195f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-couchbase = bottle-couchbase:main
    #
    # Would create an entry point called bottle-couchbase that calls bottle-couchbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/42matters/bottle-couchbase
  license: MIT License
  summary: 'Couchbase integration for Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
