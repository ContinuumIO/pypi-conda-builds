package:
  name: aioirc
  version: "0.1"

source:
  fn: aioirc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aioirc/aioirc-0.1.tar.gz
  md5: 5015dc272cb6b79c02dd1518d50fe946
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aioirc = aioirc:main
    #
    # Would create an entry point called aioirc that calls aioirc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - asyncio

  run:
    - python
    - asyncio

test:
  # Python imports
  imports:
    - aioirc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/devunt/aioirc
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'AsyncIO IRC Library for >= Python 3.3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
