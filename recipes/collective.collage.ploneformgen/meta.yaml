package:
  name: collective.collage.ploneformgen
  version: "1.0a1"

source:
  fn: collective.collage.ploneformgen-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collage.ploneformgen/collective.collage.ploneformgen-1.0a1.tar.gz
  md5: bd1c97aaee1a7273524a1f5fcac1bdcf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.ploneformgen = collective.collage.ploneformgen:main
    #
    # Would create an entry point called collective.collage.ploneformgen that calls collective.collage.ploneformgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.collage
    - products.ploneformgen >=1.5b2

  run:
    - python
    - setuptools
    - products.collage
    - products.ploneformgen >=1.5b2

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.ploneformgen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plone.org/products/collective.collage.ploneformgen
  license: GNU General Public License (GPL)
  summary: 'Add-on that allows displaying PloneFormGen-forms inside a Collage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
