package:
  name: django-skd-tools
  version: "0.2.2"

source:
  fn: django-skd-tools-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-skd-tools/django-skd-tools-0.2.2.tar.gz
  md5: bf91a6882b8ce26e4567e3feb290d0e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-skd-tools = django-skd-tools:main
    #
    # Would create an entry point called django-skd-tools that calls django-skd-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - skd_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/steelkiwi/django-skd-tools
  license: MIT License
  summary: 'Steelkiwi Django Tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
