package:
  name: django-domains
  version: "0.5.1"

source:
  fn: django-domains-0.5.1.tar.bz2
  url: https://pypi.python.org/packages/source/d/django-domains/django-domains-0.5.1.tar.bz2
  md5: 8dd42988a0665900bf6e8ae56895f05e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-domains = django-domains:main
    #
    # Would create an entry point called django-domains that calls django-domains.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - domains
    - domains.hooks
    - domains.loaders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'An application that allows you to run many different sites on one Django instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
