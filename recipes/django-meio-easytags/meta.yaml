package:
  name: django-meio-easytags
  version: "0.7"

source:
  fn: django-meio-easytags-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-meio-easytags/django-meio-easytags-0.7.tar.gz
  md5: f904ab047c18384a6beee226ceb31b90
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-meio-easytags = django-meio-easytags:main
    #
    # Would create an entry point called django-meio-easytags that calls django-meio-easytags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - easytags
    - easytags.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.meiocodigo.com/projects/django-meio-easytags/
  license: BSD License
  summary: "An easy way to create custom template tags for Django's templating system."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
