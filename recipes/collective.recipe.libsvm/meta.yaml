package:
  name: collective.recipe.libsvm
  version: "0.3"

source:
  fn: collective.recipe.libsvm-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.libsvm/collective.recipe.libsvm-0.3.tar.gz
  md5: af544ebb3beaddc7cf4bdaef2a477034
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.libsvm = collective.recipe.libsvm:main
    #
    # Would create an entry point called collective.recipe.libsvm that calls collective.recipe.libsvm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.libsvm
    - collective.recipe.libsvm.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/buildout/collective.recipe.libsvm
  license: GNU General Public License (GPL)
  summary: 'Recipe to compile libsvm with python in a buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
