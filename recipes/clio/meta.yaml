package:
  name: clio
  version: "0.7"

source:
  fn: clio-0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/clio/clio-0.7.tar.gz
  md5: 6301c3c155c6b3afffa5fb6b0b9ebe5d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clio = clio:main
    #
    # Would create an entry point called clio that calls clio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.5.6,<0.6beta
    - mysql_python
    - zope.interface
    - zope.component

  run:
    - python
    - setuptools
    - sqlalchemy >=0.5.6,<0.6beta
    - mysql_python
    - zope.interface
    - zope.component

test:
  # Python imports
  imports:
    - clio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'A publication/history system for SQLAlchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
