package:
  name: corejet.testrunner
  version: "1.0.1"

source:
  fn: corejet.testrunner-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/corejet.testrunner/corejet.testrunner-1.0.1.zip
  md5: 588c5393fe7329580c3f9e06555948e8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corejet.testrunner = corejet.testrunner:main
    #
    # Would create an entry point called corejet.testrunner that calls corejet.testrunner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - lxml
    - zope.testrunner
    - corejet.core
    - corejet.visualization
    - zc.recipe.egg
    - zope.dottedname

  run:
    - python
    - setuptools
    - setuptools
    - lxml
    - zope.testrunner
    - corejet.core
    - corejet.visualization
    - zc.recipe.egg
    - zope.dottedname

test:
  # Python imports
  imports:
    - corejet
    - corejet.testrunner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://corejet.org
  license: ZPL 2.1
  summary: 'A test runner which can output an XML report compatible with JUnit and Hudson/Jenkins as well as XML and HTML reports compatible with the CoreJet BDD standard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
