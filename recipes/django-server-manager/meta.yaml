package:
  name: django-server-manager
  version: "0.1.0"

source:
  fn: django-server-manager-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-server-manager/django-server-manager-0.1.0.tar.gz
  md5: 3f315b43a6897cbcbd30b541826c6e52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-server-manager = django-server-manager:main
    #
    # Would create an entry point called django-server-manager that calls django-server-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - server_manager
    - server_manager.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-server-manager
  license: UNKNOWN
  summary: 'Django Server Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
