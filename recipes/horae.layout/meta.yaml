package:
  name: horae.layout
  version: "1.0a1"

source:
  fn: horae.layout-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.layout/horae.layout-1.0a1.tar.gz
  md5: 28e368ea6e587df65012e1804afd9f04
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.layout = horae.layout:main
    #
    # Would create an entry point called horae.layout that calls horae.layout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - fanstatic
    - zope.fanstatic
    - zc.relation
    - js.jquery
    - js.jqueryui
    - beautifulsoup
    - grokcore.chameleon
    - megrok.navigation
    - megrok.pagetemplate
    - megrok.form
    - horae.core
    - horae.datetime

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - fanstatic
    - zope.fanstatic
    - zc.relation
    - js.jquery
    - js.jqueryui
    - beautifulsoup
    - grokcore.chameleon
    - megrok.navigation
    - megrok.pagetemplate
    - megrok.form
    - horae.core
    - horae.datetime

test:
  # Python imports
  imports:
    - horae
    - horae.layout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides the global layout for the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
