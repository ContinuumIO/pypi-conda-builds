package:
  name: djanjinja
  version: "0.8"

source:
  fn: DjanJinja-0.8.tar.gz
  url: https://pypi.python.org/packages/source/D/DjanJinja/DjanJinja-0.8.tar.gz
  md5: d481d9b1b68a8c4adcdff858e2aff326
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djanjinja = djanjinja:main
    #
    # Would create an entry point called djanjinja that calls djanjinja.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djanjinja
    - djanjinja.bundles
    - djanjinja.extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/zacharyvoase/djanjinja/
  license: UNKNOWN
  summary: 'Easy Django and Jinja2 integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
