package:
  name: simpledbf
  version: "0.2.4"

source:
  fn: simpledbf-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/s/simpledbf/simpledbf-0.2.4.tar.gz
  md5: 003f972854f9c34306dc3aa98baeb275
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpledbf = simpledbf:main
    #
    # Would create an entry point called simpledbf that calls simpledbf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simpledbf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rnelsonchem/simpledbf
  license: BSD License
  summary: 'Convert DBF files to CSV, DataFrames, HDF5 tables, and SQL tables. Python3 compatible.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
