package:
  name: django-money
  version: "0.5.0"

source:
  fn: django-money-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-money/django-money-0.5.0.tar.gz
  md5: 2d60b6e9b55818516b3c425011b85f5b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-money = django-money:main
    #
    # Would create an entry point called django-money that calls django-money.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.8
    - py-moneyed >0.4
    - six

  run:
    - python
    - setuptools
    - django >=1.4,<1.8
    - py-moneyed >0.4
    - six

test:
  # Python imports
  imports:
    - djmoney
    - djmoney.forms
    - djmoney.models
    - djmoney.templatetags
    - djmoney.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jakewins/django-money
  license: BSD License
  summary: 'Adds support for using money and currency fields in django models and forms. Uses py-moneyed as the money implementation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
