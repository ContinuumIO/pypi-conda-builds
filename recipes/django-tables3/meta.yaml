package:
  name: django-tables3
  version: "0.14.1"

source:
  fn: django-tables3-0.14.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tables3/django-tables3-0.14.1.tar.gz
  md5: 898cc4fd5169f58a3654d09a4813532f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tables3 = django-tables3:main
    #
    # Would create an entry point called django-tables3 that calls django-tables3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - six

  run:
    - python
    - django >=1.2
    - six

test:
  # Python imports
  imports:
    - django_tables2
    - django_tables2.columns
    - django_tables2.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyayers/django-tables2/
  license: BSD License
  summary: 'Table/data-grid framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
