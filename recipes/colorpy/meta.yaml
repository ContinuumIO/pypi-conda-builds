package:
  name: colorpy
  version: "0.1.1"

source:
  fn: colorpy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colorpy/colorpy-0.1.1.tar.gz
  md5: f9718a9657fd11a1f277549d5f13497a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorpy = colorpy:main
    #
    # Would create an entry point called colorpy that calls colorpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://markkness.net/colorpy/
  license: GNU Lesser GPL Version 3
  summary: 'Color calculations with physical descriptions of light spectra'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
