package:
  name: globalregisterplugin
  version: "0.1.1"

source:
  fn: GlobalRegisterPlugin-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GlobalRegisterPlugin/GlobalRegisterPlugin-0.1.1.tar.gz
  md5: 2ed86089d6222a56acff846d72502e12
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - globalregisterplugin = globalregisterplugin:main
    #
    # Would create an entry point called globalregisterplugin that calls globalregisterplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - globalregister
    - globalregisterplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mitar.tnode.com/
  license: AGPLv3
  summary: 'Displays a notice to the user that accounts are shared globally on the server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
