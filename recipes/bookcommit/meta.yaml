package:
  name: bookcommit
  version: "1.0"

source:
  fn: bookcommit-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bookcommit/bookcommit-1.0.tar.gz
  md5: e194a511cd5f0fe996e5009ccfa3fa77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bookcommit = bookcommit:main
    #
    # Would create an entry point called bookcommit that calls bookcommit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bookcommit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/grimborg/bookcommit
  license: GPLv2
  summary: 'Mercurial extension to automatically prepend the current bookmark name to the commit message.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
