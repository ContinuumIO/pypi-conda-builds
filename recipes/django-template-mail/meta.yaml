package:
  name: django-template-mail
  version: "0.1"

source:
  fn: django-template-mail-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-mail/django-template-mail-0.1.tar.gz
  md5: fd79e25a16e0fd7e9cee2a7f835bb97a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-mail = django-template-mail:main
    #
    # Would create an entry point called django-template-mail that calls django-template-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - templatemail
    - templatemail.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/benoitbar/django-template-mail/
  license: MIT License
  summary: "A Django application to send email using django's templating system"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
