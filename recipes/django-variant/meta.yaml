package:
  name: django-variant
  version: "0.1.0"

source:
  fn: django-variant-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-variant/django-variant-0.1.0.tar.gz
  md5: 1edf5831b9c002bf5400fe6d05c17d6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-variant = django-variant:main
    #
    # Would create an entry point called django-variant that calls django-variant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - six

  run:
    - python
    - django >=1.5
    - six

test:
  # Python imports
  imports:
    - variant
    - variant.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jsatt/django-variant
  license: GNU General Public License v3 (GPLv3)
  summary: 'Django variant testing framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
