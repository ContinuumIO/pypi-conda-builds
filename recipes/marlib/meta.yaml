package:
  name: marlib
  version: "0.3"

source:
  fn: marlib-0.3.9.tar.gz
  url: https://pypi.python.org/packages/source/m/marlib/marlib-0.3.9.tar.gz
  md5: d1f78af16366cf8d3efb649abf0218b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marlib = marlib:main
    #
    # Would create an entry point called marlib that calls marlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - marlib
    - marlib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ejh333/marlib
  license: LGPL
  summary: 'Music Signal Processing and Analysis Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
