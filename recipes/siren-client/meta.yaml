package:
  name: siren-client
  version: "1.0.2"

source:
  fn: siren-client-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/siren-client/siren-client-1.0.2.tar.gz
  md5: 94a07afdfa7f036672b054ce4b8aba75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - siren-client = siren-client:main
    #
    # Would create an entry point called siren-client that calls siren-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - siren_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lonelyplanet/siren-client-python
  license: Apache Software License
  summary: 'Client for a Siren based Hypermedia API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
