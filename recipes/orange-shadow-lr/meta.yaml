package:
  name: orange-shadow-lr
  version: "1.0"

source:
  fn: Orange-Shadow-LR-1.0.tar.gz
  url: https://pypi.python.org/packages/source/O/Orange-Shadow-LR/Orange-Shadow-LR-1.0.tar.gz
  md5: df0f6fd347ef20d409e43738c4a96d5e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - orange-shadow-lr = orange-shadow-lr:main
    #
    # Would create an entry point called orange-shadow-lr that calls orange-shadow-lr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - orangecontrib
    - orangecontrib.shadow
    - orangecontrib.shadow.widgets
    - orangecontrib.shadow.widgets.user_defined

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://orange.biolab.si/
  license: GPLv3
  summary: 'Shadow, Ray-tracing simulation software, Customized utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
