package:
  name: django-hierarchy
  version: "0.2.3"

source:
  fn: django-hierarchy-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hierarchy/django-hierarchy-0.2.3.tar.gz
  md5: dfbb2e897fc20892414ee402093987b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hierarchy = django-hierarchy:main
    #
    # Would create an entry point called django-hierarchy that calls django-hierarchy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - consolelog

  run:
    - python
    - django
    - consolelog

test:
  # Python imports
  imports:
    - hierarchy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lars512/django-hierarchy/
  license: BSD
  summary: 'A library for storing trees in the Django ORM.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
