package:
  name: metoffer
  version: "1.3.2"

source:
  fn: MetOffer-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/M/MetOffer/MetOffer-1.3.2.tar.gz
  md5: 14923dd3a6d1bbd3310696e89fae5ba1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metoffer = metoffer:main
    #
    # Would create an entry point called metoffer that calls metoffer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - metoffer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/MetOffer/
  license: LICENSE.txt
  summary: 'Simple wrapper for the Met Office DataPoint API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
