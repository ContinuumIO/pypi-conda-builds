package:
  name: source-over-ssh
  version: "0.3"

source:
  fn: source-over-ssh-0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/source-over-ssh/source-over-ssh-0.3.tar.gz
  md5: ab230975648960344743c61f6ef3d076
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - source-over-ssh = source-over-ssh:main
    #
    # Would create an entry point called source-over-ssh that calls source-over-ssh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - twisted >=11.0.0
    - pycrypto >=3.2
    - pyasn1 >=0.0.13

  run:
    - python
    - twisted >=11.0.0
    - pycrypto >=3.2
    - pyasn1 >=0.0.13

test:
  # Python imports
  imports:
    - sos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spikeekips/source-over-ssh
  license: GNU General Public License (GPL)
  summary: 'source repository management server over ssh'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
