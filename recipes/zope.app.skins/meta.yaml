package:
  name: zope.app.skins
  version: "3.4.0"

source:
  fn: zope.app.skins-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.skins/zope.app.skins-3.4.0.tar.gz
  md5: a0bc210720ee50e40adb93e9c685e884
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.skins = zope.app.skins:main
    #
    # Would create an entry point called zope.app.skins that calls zope.app.skins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.deprecation

  run:
    - python
    - setuptools
    - zope.deprecation

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.skins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.skins
  license: Zope Public License
  summary: 'Zope 3 Skin Container Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
