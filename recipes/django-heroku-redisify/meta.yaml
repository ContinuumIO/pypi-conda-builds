package:
  name: django-heroku-redisify
  version: "0.2.1"

source:
  fn: django-heroku-redisify-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-heroku-redisify/django-heroku-redisify-0.2.1.tar.gz
  md5: 9ee0195604f2bc8b27a162c023d822c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-heroku-redisify = django-heroku-redisify:main
    #
    # Would create an entry point called django-heroku-redisify that calls django-heroku-redisify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-redis-cache

  run:
    - python
    - django-redis-cache

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dirn/django-heroku-redisify
  license: BSD License
  summary: 'Friendly Redis for Django on Heroku'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
