package:
  name: rosdep
  version: "0.11.2"

source:
  fn: rosdep-0.11.2.tar.gz
  url: https://pypi.python.org/packages/source/r/rosdep/rosdep-0.11.2.tar.gz
  md5: 5ee0f79c4bd8e0335464032ded7afd56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rosdep = rosdep:main
    #
    # Would create an entry point called rosdep that calls rosdep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - catkin_pkg
    - rospkg >=1.0.34
    - rosdistro >=0.4.0
    - pyyaml >=3.1

  run:
    - python
    - catkin_pkg
    - rospkg >=1.0.34
    - rosdistro >=0.4.0
    - pyyaml >=3.1

test:
  # Python imports
  imports:
    - rosdep
    - rosdep2
    - rosdep2.platforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.ros.org/rosdep
  license: BSD License
  summary: 'rosdep package manager abstrction tool for ROS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
