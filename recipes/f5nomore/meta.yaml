package:
  name: f5nomore
  version: "0.1.0"

source:
  fn: F5NoMore-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/F5NoMore/F5NoMore-0.1.0.tar.gz
  md5: ecfea85c7a18904e084dec9f63225a1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - f5nomore = f5nomore:main
    #
    # Would create an entry point called f5nomore that calls f5nomore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - watchdog >=0.7.1,<0.80

  run:
    - python
    - watchdog >=0.7.1,<0.80

test:
  # Python imports
  imports:
    - f5nomore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Autoreload webpage on file system changes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
