package:
  name: django-browser-info
  version: "1.0.1"

source:
  fn: django-browser-info-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-browser-info/django-browser-info-1.0.1.tar.gz
  md5: 52141d8eb3c49f3a923c9d16d96089a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-browser-info = django-browser-info:main
    #
    # Would create an entry point called django-browser-info that calls django-browser-info.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - browser_info

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisdrackett/django-browser-info
  license: BSD License
  summary: 'Django middleware and view decorator to add browser info to the request object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
