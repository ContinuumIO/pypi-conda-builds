package:
  name: ludolph
  version: "0.5.1"

source:
  fn: ludolph-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/l/ludolph/ludolph-0.5.1.tar.gz
  md5: 0f8df8fb4a958b938cd7d55b5265ebad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ludolph = ludolph:main
    #
    # Would create an entry point called ludolph that calls ludolph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zabbix-api-erigones
    - sleekxmpp >=1.1.11
    - bottle
    - dnspython

  run:
    - python
    - zabbix-api-erigones
    - sleekxmpp >=1.1.11
    - bottle
    - dnspython

test:
  # Python imports
  imports:
    - ludolph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erigones/Ludolph/
  license: BSD License
  summary: 'Monitoring Jabber Bot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
