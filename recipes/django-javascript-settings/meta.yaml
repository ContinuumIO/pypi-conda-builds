package:
  name: django-javascript-settings
  version: "1.1.2"

source:
  fn: django-javascript-settings-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-javascript-settings/django-javascript-settings-1.1.2.tar.gz
  md5: ddc6409a492fedfb0fa1521ca0624a73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-javascript-settings = django-javascript-settings:main
    #
    # Would create an entry point called django-javascript-settings that calls django-javascript-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - javascript_settings
    - javascript_settings.templatetags
    - javascript_settings.test_urls
    - javascript_settings.test_urls.test_empty
    - javascript_settings.test_urls.test_filled1
    - javascript_settings.test_urls.test_filled2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'django-javascript-settings is a Django application that provides a way of passing settings for Django applications to JavaScript.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
