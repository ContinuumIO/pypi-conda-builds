package:
  name: ieee754bin
  version: "0.1"

source:
  fn: ieee754bin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ieee754bin/ieee754bin-0.1.tar.gz
  md5: 1b127aeba94fd0e7534a04bf9a8b946f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ieee754bin = ieee754bin:main
    #
    # Would create an entry point called ieee754bin that calls ieee754bin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ieee754bin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tonquil.homeip.net/~dlenski/ieee754bin
  license: GPLv3
  summary: 'Python IEEE 754 binary library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
