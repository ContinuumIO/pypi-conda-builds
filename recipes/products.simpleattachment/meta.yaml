package:
  name: products.simpleattachment
  version: "4.3"

source:
  fn: Products.SimpleAttachment-4.3.zip
  url: https://pypi.python.org/packages/source/P/Products.SimpleAttachment/Products.SimpleAttachment-4.3.zip
  md5: e1ca9b47f5223aa1d8a2379aca178924
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.simpleattachment = products.simpleattachment:main
    #
    # Would create an entry point called products.simpleattachment that calls products.simpleattachment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - archetypes.schemaextender
    - plone.app.blob

  run:
    - python
    - setuptools
    - archetypes.schemaextender
    - plone.app.blob

test:
  # Python imports
  imports:
    - Products
    - Products.SimpleAttachment
    - Products.SimpleAttachment.content
    - Products.SimpleAttachment.migration
    - Products.SimpleAttachment.tests
    - Products.SimpleAttachment.widget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.SimpleAttachment
  license: GPL
  summary: 'Simple Attachments for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
