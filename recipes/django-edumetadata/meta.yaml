package:
  name: django-edumetadata
  version: "0.7"

source:
  fn: django-edumetadata-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-edumetadata/django-edumetadata-0.7.tar.gz
  md5: 6750cf0dbbfa1414efda3660ba8b793f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-edumetadata = django-edumetadata:main
    #
    # Would create an entry point called django-edumetadata that calls django-edumetadata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-categories >=1.0

  run:
    - python
    - django-categories >=1.0

test:
  # Python imports
  imports:
    - edumetadata
    - edumetadata.management
    - edumetadata.management.commands
    - edumetadata.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/organizations/Natgeoed/django-edumetadata
  license: Apache Software License
  summary: 'django-edumetadata'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
