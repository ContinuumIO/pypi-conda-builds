package:
  name: li-pagador-pagseguro
  version: "2.0.16"

source:
  fn: li-pagador-pagseguro-2.0.16.tar.gz
  url: https://pypi.python.org/packages/source/l/li-pagador-pagseguro/li-pagador-pagseguro-2.0.16.tar.gz
  md5: f6950f84c3e0abb2ef0fb4ab4d402241
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - li-pagador-pagseguro = li-pagador-pagseguro:main
    #
    # Would create an entry point called li-pagador-pagseguro that calls li-pagador-pagseguro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - li-pagador

  run:
    - python
    - distribute
    - li-pagador

test:
  # Python imports
  imports:
    - pagador_pagseguro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lojaintegrada/LI-Pagador-PagSeguro
  license: MIT License
  summary: 'Meio de pagamento usando o PagSeguro (https://pagseguro.uol.com.br)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
