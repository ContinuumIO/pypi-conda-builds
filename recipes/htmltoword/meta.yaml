package:
  name: htmltoword
  version: "0.4.9.6"

source:
  fn: HtmlToWord-0.4.9.6.zip
  url: https://pypi.python.org/packages/source/H/HtmlToWord/HtmlToWord-0.4.9.6.zip
  md5: b80604d9fd4ffb5b317049ed75fa846c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmltoword = htmltoword:main
    #
    # Would create an entry point called htmltoword that calls htmltoword.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - beautifulsoup >=3.2.1
    - pywin32

  run:
    - python
    - beautifulsoup >=3.2.1
    - pywin32

test:
  # Python imports
  imports:
    - HtmlToWord
    - HtmlToWord.elements
    - htmltoword

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orf/HtmlToWord
  license: UNKNOWN
  summary: 'Render HTML to a specific portion of a word document'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
