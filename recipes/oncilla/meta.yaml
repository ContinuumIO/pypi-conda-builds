package:
  name: oncilla
  version: "0.1.0"

source:
  fn: Oncilla-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/O/Oncilla/Oncilla-0.1.0.tar.gz
  md5: 8214bf83c2c7589b5b8f78658cfd875d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oncilla = oncilla:main
    #
    # Would create an entry point called oncilla that calls oncilla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - foundations >=2.1.0
    - sphinx >=1.1.3
    - sphinx-rtd-theme >=0.1.5

  run:
    - python
    - foundations >=2.1.0
    - sphinx >=1.1.3
    - sphinx-rtd-theme >=0.1.5

test:
  # Python imports
  imports:
    - oncilla
    - oncilla.globals
    - oncilla.libraries
    - oncilla.libraries.python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/KelSolaar/Oncilla
  license: GNU General Public License v3 (GPLv3)
  summary: u'Oncilla is the documentation building helper package of Oncilla, Color, Manager, Umbra, sIBL_GUI and sIBL_Reporter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
