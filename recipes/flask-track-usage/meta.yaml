package:
  name: flask-track-usage
  version: "1.0.1"

source:
  fn: Flask-Track-Usage-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Track-Usage/Flask-Track-Usage-1.0.1.tar.gz
  md5: 8f426c708a27fe79a862051abb1df681
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-track-usage = flask-track-usage:main
    #
    # Would create an entry point called flask-track-usage that calls flask-track-usage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_track_usage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ashcrow/flask-track-usage
  license: BSD License
  summary: 'Basic metrics tracking for the Flask framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
