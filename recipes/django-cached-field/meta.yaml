package:
  name: django-cached-field
  version: "1.2.11"

source:
  fn: django-cached-field-1.2.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cached-field/django-cached-field-1.2.11.tar.gz
  md5: 34500ac8a40148ea43f76bc2f6511c81
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cached-field = django-cached-field:main
    #
    # Would create an entry point called django-cached-field that calls django-cached-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - celery >=3.0
    - django-celery >=3.0

  run:
    - python
    - setuptools
    - django >=1.3.1
    - celery >=3.0
    - django-celery >=3.0

test:
  # Python imports
  imports:
    - django_cached_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aquameta/django-cached-field
  license: BSD
  summary: 'Celery-deferred, cached fields on Django ORM for expensive-to-calculate data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
