package:
  name: i-pylogger
  version: "0.2"

source:
  fn: i-pylogger-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/i-pylogger/i-pylogger-0.2.tar.gz
  md5: b7f7e91a31175cfdaf109a3dfef1cef9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - i-pylogger = i-pylogger:main
    #
    # Would create an entry point called i-pylogger that calls i-pylogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pylogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jizhouli/py-logger
  license: http://opensource.org/licenses/MIT
  summary: 'a easy-to-use logging wrapper for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
