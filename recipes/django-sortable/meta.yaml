package:
  name: django-sortable
  version: "0.3.1"

source:
  fn: django-sortable-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sortable/django-sortable-0.3.1.tar.gz
  md5: d84ef94b229b95365470f25a280abeb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sortable = django-sortable:main
    #
    # Would create an entry point called django-sortable that calls django-sortable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_sortable
    - django_sortable.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/drewyeaton/django-sortable
  license: GNU General Public License (GPL)
  summary: 'Flexible sorting for Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
