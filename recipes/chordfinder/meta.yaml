package:
  name: chordfinder
  version: "0.1"

source:
  fn: ChordFinder-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/ChordFinder/ChordFinder-0.1.tar.gz
  md5: 6691be307fccac1698214e23f1d8f84b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chordfinder = chordfinder:main
    #
    # Would create an entry point called chordfinder that calls chordfinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - wx >=2.6.4

  run:
    - python
    - wx >=2.6.4

test:
  # Python imports
  imports:
    - chordfinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ChordFinder/
  license: LICENSE.txt
  summary: 'A simple GUI chord finder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
