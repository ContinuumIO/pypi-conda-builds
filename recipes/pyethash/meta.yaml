package:
  name: pyethash
  version: "23.1"

source:
  fn: pyethash-23.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyethash/pyethash-23.1.tar.gz
  md5: bdb22f4a88fb0c0e8f5da5fad0c840cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyethash = pyethash:main
    #
    # Would create an entry point called pyethash that calls pyethash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyethash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ethereum/ethash
  license: GPL
  summary: 'Python wrappers for ethash, the ethereum proof of work hashing function'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
