package:
  name: js.bootstrap_datepicker
  version: "2013.03.12-1"

source:
  fn: js.bootstrap_datepicker-2013.03.12-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.bootstrap_datepicker/js.bootstrap_datepicker-2013.03.12-1.tar.gz
  md5: e7117465ebb42f11d90ca80391b0df8d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.bootstrap_datepicker = js.bootstrap_datepicker:main
    #
    # Would create an entry point called js.bootstrap_datepicker that calls js.bootstrap_datepicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.bootstrap

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.bootstrap

test:
  # Python imports
  imports:
    - js
    - js.bootstrap_datepicker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/s_nikolai/js.bootstrap_datepicker
  license: BSD
  summary: 'Fanstatic packaging of Datepicker for Bootstrap (bootstrap-datepicker.js)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
