package:
  name: ipdasite.policy
  version: "2.1.6"

source:
  fn: ipdasite.policy-2.1.6.zip
  url: http://oodt.jpl.nasa.gov/dist/ipdasite/ipdasite.policy-2.1.6.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipdasite.policy = ipdasite.policy:main
    #
    # Would create an entry point called ipdasite.policy that calls ipdasite.policy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - setuptools
    - plone
    - pillow
    - ipdasite.theme
    - ipdasite.services
    - ipdasite.views
    - ipdasite.projectmgmt
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - distribute
    - setuptools
    - plone
    - pillow
    - ipdasite.theme
    - ipdasite.services
    - ipdasite.views
    - ipdasite.projectmgmt
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - ipdasite
    - ipdasite.policy
    - ipdasite.policy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ipdasite.policy/
  license: Proprietary
  summary: 'International Planetary Data Alliance Site Policy and Component Orchestration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
