package:
  name: async_gui
  version: "0.1.1"

source:
  fn: async_gui-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/async_gui/async_gui-0.1.1.tar.gz
  md5: ffcdbda070243f3bf16183d9a92a65e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - async_gui = async_gui:main
    #
    # Would create an entry point called async_gui that calls async_gui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - futures

  run:
    - python
    - futures

test:
  # Python imports
  imports:
    - async_gui
    - async_gui.toolkits

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/reclosedev/async_gui
  license: BSD License
  summary: 'Easy threading and multiprocessing for GUI applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
