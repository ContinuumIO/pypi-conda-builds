package:
  name: cellnopt.core
  version: "1.0.0"

source:
  fn: cellnopt.core-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cellnopt.core/cellnopt.core-1.0.0.tar.gz
  md5: 71761d7d8acd258429987571f87a5308
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cellnopt.core = cellnopt.core:main
    #
    # Would create an entry point called cellnopt.core that calls cellnopt.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - easydev ==0.7.4
    - colormap ==0.9.4
    - numpy
    - networkx
    - pygraphviz
    - pandas
    - beautifulsoup4
    - cellnopt.data

  run:
    - python
    - easydev ==0.7.4
    - colormap ==0.9.4
    - numpy
    - networkx
    - pygraphviz
    - pandas
    - beautifulsoup4
    - cellnopt.data

test:
  # Python imports
  imports:
    - cellnopt
    - cellnopt.core
    - core
    - core.io

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ['http://pythonhosted.org/cellnopt.core/']
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Functions to manipulate networks and data related to signalling pathways.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
