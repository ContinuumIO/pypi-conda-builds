package:
  name: django-timezone-utils
  version: "0.9"

source:
  fn: django-timezone-utils-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timezone-utils/django-timezone-utils-0.9.tar.gz
  md5: f83ef1e1f1c9a90a28c753f69f90e540
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timezone-utils = django-timezone-utils:main
    #
    # Would create an entry point called django-timezone-utils that calls django-timezone-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz
    - django >=1.4,<1.9

  run:
    - python
    - pytz
    - django >=1.4,<1.9

test:
  # Python imports
  imports:
    - timezone_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/michaeljohnbarr/django-timezone-utils/
  license: MIT License
  summary: 'Time Zone Utilities for Django Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
