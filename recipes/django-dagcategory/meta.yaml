package:
  name: django-dagcategory
  version: "0.0.2"

source:
  fn: django-dagcategory-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dagcategory/django-dagcategory-0.0.2.tar.gz
  md5: fd7d651ac32a5cf2f13b3c50089ceb9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dagcategory = django-dagcategory:main
    #
    # Would create an entry point called django-dagcategory that calls django-dagcategory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dagcategory
    - dagcategory.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cuker/django-dagcategory
  license: New BSD License
  summary: 'A simple library for creating tree like categories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
