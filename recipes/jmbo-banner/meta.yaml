package:
  name: jmbo-banner
  version: "0.6"

source:
  fn: jmbo-banner-0.6.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-banner/jmbo-banner-0.6.tar.gz
  md5: 0539ac2fb2e96479d2fd8bee5ffc767f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-banner = jmbo-banner:main
    #
    # Would create an entry point called jmbo-banner that calls jmbo-banner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.7
    - jmbo >=1.1.1
    - django-dfp >=0.3.3

  run:
    - python
    - django >=1.4,<1.7
    - jmbo >=1.1.1
    - django-dfp >=0.3.3

test:
  # Python imports
  imports:
    - banner
    - banner.migrations
    - banner.templatetags
    - banner.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-banner
  license: BSD License
  summary: 'Jmbo banner app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
