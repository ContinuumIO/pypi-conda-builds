package:
  name: gaphas
  version: "0.7.2"

source:
  fn: gaphas-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gaphas/gaphas-0.7.2.tar.gz
  md5: 8a18fa17a7f4df29d9e7762eb430816e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaphas = gaphas:main
    #
    # Would create an entry point called gaphas that calls gaphas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator >=3.0.0
    - simplegeneric >=0.6

  run:
    - python
    - decorator >=3.0.0
    - simplegeneric >=0.6

test:
  # Python imports
  imports:
    - gaphas
    - gaphas.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gaphor.sourceforge.net
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Gaphas is a GTK+ based diagramming widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
