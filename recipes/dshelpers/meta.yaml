package:
  name: dshelpers
  version: "1.0.4"

source:
  fn: dshelpers-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dshelpers/dshelpers-1.0.4.tar.gz
  md5: 2325beec981ce8fb26fd9d85baa54d78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dshelpers = dshelpers:main
    #
    # Would create an entry point called dshelpers that calls dshelpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - requests_cache
    - mock
    - nose
    - scraperwiki

  run:
    - python
    - requests
    - requests_cache
    - mock
    - nose
    - scraperwiki

test:
  # Python imports
  imports:
    - dshelpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://scraperwiki.com
  license: BSD License
  summary: 'Provides some helpers functions used by the ScraperWiki Data Services team.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
