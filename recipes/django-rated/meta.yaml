package:
  name: django-rated
  version: "1.1.2"

source:
  fn: django-rated-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rated/django-rated-1.1.2.tar.gz
  md5: aefd64adfd6a7c78dfae72b3aabcf2fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rated = django-rated:main
    #
    # Would create an entry point called django-rated that calls django-rated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rated
    - rated.backend
    - rated.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/funkybob/django-rated
  license: BSD License
  summary: 'A rate limiting middleware for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
