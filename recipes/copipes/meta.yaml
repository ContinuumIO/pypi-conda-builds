package:
  name: copipes
  version: "0.1"

source:
  fn: CoPipes-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CoPipes/CoPipes-0.1.tar.gz
  md5: 61dec538b7ec6b7532582d5edc085327
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - copipes = copipes:main
    #
    # Would create an entry point called copipes that calls copipes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - copipes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kr41/copipes
  license: BSD License
  summary: 'A coroutine based utils for processing data via pipelines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
