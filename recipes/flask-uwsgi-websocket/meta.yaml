package:
  name: flask-uwsgi-websocket
  version: "0.4.1"

source:
  fn: Flask-uWSGI-WebSocket-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-uWSGI-WebSocket/Flask-uWSGI-WebSocket-0.4.1.tar.gz
  md5: 9feff1b03e9c02fef6f0cf52c654ac53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-uwsgi-websocket = flask-uwsgi-websocket:main
    #
    # Would create an entry point called flask-uwsgi-websocket that calls flask-uwsgi-websocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - uwsgi

  run:
    - python
    - flask
    - uwsgi

test:
  # Python imports
  imports:
    - flask_uwsgi_websocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zeekay/flask-uwsgi-websocket
  license: MIT License
  summary: 'High-performance WebSockets for your Flask apps powered by uWSGI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
