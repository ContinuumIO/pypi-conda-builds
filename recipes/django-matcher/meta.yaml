package:
  name: django-matcher
  version: "0.1"

source:
  fn: master.zip
  url: https://github.com/Raul-diffindo/Django-Matcher/archive/master.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-matcher = django-matcher:main
    #
    # Would create an entry point called django-matcher that calls django-matcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'Matching search between objects (model objects or object dictionaries).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
