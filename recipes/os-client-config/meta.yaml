package:
  name: os-client-config
  version: "1.3.0"

source:
  fn: os-client-config-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/o/os-client-config/os-client-config-1.3.0.tar.gz
  md5: 286b1057d2df432cf48f5b810a58f73b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - os-client-config = os-client-config:main
    #
    # Would create an entry point called os-client-config that calls os-client-config.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Client Configuation Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
