package:
  name: django-export
  version: "1.0.4"

source:
  fn: django-export-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-export/django-export-1.0.4.tar.gz
  md5: f57f4fd58701e303001c70426562a219
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-export = django-export:main
    #
    # Would create an entry point called django-export that calls django-export.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-object-tools >=1.0
    - pyyaml >=3.11

  run:
    - python
    - django-object-tools >=1.0
    - pyyaml >=3.11

test:
  # Python imports
  imports:
    - export
    - export.serializers
    - export.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-export
  license: BSD License
  summary: 'Django app allowing for filtered exporting of model data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
