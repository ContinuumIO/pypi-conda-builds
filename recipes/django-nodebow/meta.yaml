package:
  name: django-nodebow
  version: "0.0.1"

source:
  fn: django-nodebow-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nodebow/django-nodebow-0.0.1.tar.gz
  md5: c4086f3454fb17778d1d2e371d946c89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nodebow = django-nodebow:main
    #
    # Would create an entry point called django-nodebow that calls django-nodebow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyexecjs

  run:
    - python
    - pyexecjs

test:
  # Python imports
  imports:
    - nodebow
    - nodebow.management
    - nodebow.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jrief/django-nodebow
  license: MIT License
  summary: 'Manage dependencies of bower components and node packages for all installed Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
