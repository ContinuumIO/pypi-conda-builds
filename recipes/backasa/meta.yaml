package:
  name: backasa
  version: "0.9.5"

source:
  fn: backasa-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/b/backasa/backasa-0.9.5.tar.gz
  md5: 9a9e3c2484e084a166a1ae25d6676068
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backasa = backasa:main
    #
    # Would create an entry point called backasa that calls backasa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gdata >=2.0.17

  run:
    - python
    - gdata >=2.0.17

test:
  # Python imports
  imports:
    - backasa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jondavidjohn/backasa
  license: Apache Software License
  summary: 'A Picasa Web Albums backup utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
