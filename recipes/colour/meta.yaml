package:
  name: colour
  version: "0.1.1"

source:
  fn: colour-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colour/colour-0.1.1.tar.gz
  md5: 69c5fa7902b3a65f4e9a80a060ac5654
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colour = colour:main
    #
    # Would create an entry point called colour that calls colour.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - nose

test:
  # Python imports
  imports:
    - colour

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'converts and manipulates various color representation (HSL, RVB, web, X11, ...)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
