package:
  name: gavagai
  version: "0.1"

source:
  fn: Gavagai-0.1.tar.gz
  url: https://pypi.python.org/packages/source/G/Gavagai/Gavagai-0.1.tar.gz
  md5: cfef301e2523ae3765206d75750911cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gavagai = gavagai:main
    #
    # Would create an entry point called gavagai that calls gavagai.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.6.0,<3.0
    - urllib3
    - ndg-httpsclient

  run:
    - python
    - requests >=2.6.0,<3.0
    - urllib3
    - ndg-httpsclient

test:
  # Python imports
  imports:
    - gavagai

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dewe/gavagai-python
  license: The MIT License (MIT)
  summary: 'A Gavagai API helper library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
