package:
  name: bam2x
  version: "0.2.4"

source:
  fn: bam2x-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bam2x/bam2x-0.2.4.tar.gz
  md5: 69e14b95953e97f434ea43a99f3a383a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bam2x = bam2x:main
    #
    # Would create an entry point called bam2x that calls bam2x.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6.0
    - pysam >=0.7.0
    - twobitreader >=2.9
    - bx-python >=0.7.0
    - bitarray >=0.8.0

  run:
    - python
    - numpy >=1.6.0
    - pysam >=0.7.0
    - twobitreader >=2.9
    - bx-python >=0.7.0
    - bitarray >=0.8.0

test:
  # Python imports
  imports:
    - bam2x
    - bam2x.DBI
    - bam2x.MRun
    - bam2x.Run
    - bam2x.Run.Plot
    - bam2x.Tools
    - bam2x.Turing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nimezhu/bam2x
  license: GNU General Public License
  summary: 'bionformatics python lib for query bam files, this version set matplotlib as optional.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
