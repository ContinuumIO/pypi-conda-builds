package:
  name: js.angular_preventdefault
  version: "1.0.3-1"

source:
  fn: js.angular_preventdefault-1.0.3-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.angular_preventdefault/js.angular_preventdefault-1.0.3-1.tar.gz
  md5: c9e20da88baaedb7198628bf613b70e6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.angular_preventdefault = js.angular_preventdefault:main
    #
    # Would create an entry point called js.angular_preventdefault that calls js.angular_preventdefault.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.angular

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.angular

test:
  # Python imports
  imports:
    - js
    - js.angular_preventdefault

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of angular-prevent-default'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
