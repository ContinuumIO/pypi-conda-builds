package:
  name: django-simple-admin-export
  version: "0.0.1"

source:
  fn: django-simple-admin-export-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-admin-export/django-simple-admin-export-0.0.1.tar.gz
  md5: d53181f8afa6811d6af0df0c2b04aa2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-admin-export = django-simple-admin-export:main
    #
    # Would create an entry point called django-simple-admin-export that calls django-simple-admin-export.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unicodecsv ==0.9.4

  run:
    - python
    - unicodecsv ==0.9.4

test:
  # Python imports
  imports:
    - admin_export

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/albertoconnor/django-simple-admin-export
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
