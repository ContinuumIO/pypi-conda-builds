package:
  name: django-thinc-webservice
  version: "0.0.4"

source:
  fn: django-thinc-webservice-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-thinc-webservice/django-thinc-webservice-0.0.4.tar.gz
  md5: b8bae77f672a6e40c76486d03c127131
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-thinc-webservice = django-thinc-webservice:main
    #
    # Would create an entry point called django-thinc-webservice that calls django-thinc-webservice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework >=3.1.1
    - django >=1.8
    - pytz >=2014.10
    - django-grappelli >=2.6.4

  run:
    - python
    - djangorestframework >=3.1.1
    - django >=1.8
    - pytz >=2014.10
    - django-grappelli >=2.6.4

test:
  # Python imports
  imports:
    - webservice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/webair/thinc.django.webservice
  license: MIT License
  summary: 'Adds i18n and delta support for models to django-rest-framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
