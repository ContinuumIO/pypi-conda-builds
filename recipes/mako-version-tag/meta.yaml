package:
  name: mako-version-tag
  version: "1.1"

source:
  fn: mako-version-tag-1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mako-version-tag/mako-version-tag-1.1.tar.gz
  md5: d721b121ee42963a75920a6f793a36aa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mako-version-tag = mako-version-tag:main
    #
    # Would create an entry point called mako-version-tag that calls mako-version-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mako

  run:
    - python
    - setuptools
    - mako

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://alchemy.grimoire.ca/python/sites/mako-version-tag/
  license: MIT License
  summary: 'Displays pkg_resources distribution versions in Mako templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
