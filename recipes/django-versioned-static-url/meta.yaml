package:
  name: django-versioned-static-url
  version: "0.1.1"

source:
  fn: django-versioned-static-url-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versioned-static-url/django-versioned-static-url-0.1.1.tar.gz
  md5: 241bf03325ab2c7f594bae8cc0558114
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versioned-static-url = django-versioned-static-url:main
    #
    # Would create an entry point called django-versioned-static-url that calls django-versioned-static-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_versioned_static_url
    - django_versioned_static_url.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ubuntudesign/django-versioned-static-url
  license: UNKNOWN
  summary: 'A template tag for generating a URL for a static file, with a version string which will be effectively unique based on the contents of the file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
