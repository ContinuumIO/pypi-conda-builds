package:
  name: django-mail-templated
  version: "0.2.1"

source:
  fn: django-mail-templated-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mail-templated/django-mail-templated-0.2.1.tar.gz
  md5: 99c22f58155a1bedf088e797f9599ea3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mail-templated = django-mail-templated:main
    #
    # Would create an entry point called django-mail-templated that calls django-mail-templated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mail_templated

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/artemrizhov/django-mail-templated
  license: MIT License
  summary: 'Send emails with Django template system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
