package:
  name: flask-wtf-alchemy-utils
  version: "0.1.0"

source:
  fn: flask-wtf-alchemy-utils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-wtf-alchemy-utils/flask-wtf-alchemy-utils-0.1.0.tar.gz
  md5: 43d5cf2fe54b3fed62d2163d28e73f1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-wtf-alchemy-utils = flask-wtf-alchemy-utils:main
    #
    # Would create an entry point called flask-wtf-alchemy-utils that calls flask-wtf-alchemy-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-wtf
    - wtforms-alchemy

  run:
    - python
    - flask
    - flask-wtf
    - wtforms-alchemy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jaza/flask-wtf-alchemy-utils
  license: UNKNOWN
  summary: 'Form and field utility base classes for use with Flask, Flask-WTF, and wtforms-alchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
