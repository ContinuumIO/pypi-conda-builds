package:
  name: django-encrypted-settings
  version: "0.1.0"

source:
  fn: django-encrypted-settings-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-encrypted-settings/django-encrypted-settings-0.1.0.tar.gz
  md5: 952fe437827e2a8a9e8ea528b2dfa51a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-encrypted-settings = django-encrypted-settings:main
    #
    # Would create an entry point called django-encrypted-settings that calls django-encrypted-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-keyczar

  run:
    - python
    - python-keyczar

test:
  # Python imports
  imports:
    - encrypted_settings
    - encrypted_settings.management
    - encrypted_settings.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kami/django-encrypted-settings
  license: Apache Software License
  summary: 'Thin wrapper around Python keyczar bindings which allows you to manage and use encrypted settings in your Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
