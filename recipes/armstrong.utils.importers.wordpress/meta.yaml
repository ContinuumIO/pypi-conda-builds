package:
  name: armstrong.utils.importers.wordpress
  version: "0.1.1"

source:
  fn: armstrong.utils.importers.wordpress-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.utils.importers.wordpress/armstrong.utils.importers.wordpress-0.1.1.tar.gz
  md5: f5e4791259bacc22bbefd134a17cf331
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.utils.importers.wordpress = armstrong.utils.importers.wordpress:main
    #
    # Would create an entry point called armstrong.utils.importers.wordpress that calls armstrong.utils.importers.wordpress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml ==2.3

  run:
    - python
    - setuptools
    - lxml ==2.3

test:
  # Python imports
  imports:
    - armstrong.utils.importers.wordpress
    - armstrong.utils.importers.wordpress.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.utils.importers.wordpress/
  license: Apache Software License
  summary: 'Tools to import wordpress xml data into Armstrong'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
