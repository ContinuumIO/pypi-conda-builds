package:
  name: cssocialuser
  version: "0.36"

source:
  fn: cssocialuser-0.36.zip
  url: https://pypi.python.org/packages/source/c/cssocialuser/cssocialuser-0.36.zip
  md5: de65ef2d7e6ad43e5add2c89dcba4226
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cssocialuser = cssocialuser:main
    #
    # Would create an entry point called cssocialuser that calls cssocialuser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-social-auth
    - django-registration-redux
    - tweepy
    - facebook-sdk

  run:
    - python
    - setuptools
    - django-social-auth
    - django-registration-redux
    - tweepy
    - facebook-sdk

test:
  # Python imports
  imports:
    - cssocialuser
    - cssocialuser.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codesyntax/cssocialuser
  license: New BSD
  summary: 'CsSocialUser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
