package:
  name: pynamodb
  version: "1.3.7"

source:
  fn: pynamodb-1.3.7.tar.gz
  url: https://pypi.python.org/packages/source/p/pynamodb/pynamodb-1.3.7.tar.gz
  md5: 5f2d3d04525975bcd79961c6efe756bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynamodb = pynamodb:main
    #
    # Would create an entry point called pynamodb that calls pynamodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - delorean
    - six
    - botocore >=0.42.0
    - ordereddict

  run:
    - python
    - delorean
    - six
    - botocore >=0.42.0
    - ordereddict

test:
  # Python imports
  imports:
    - pynamodb
    - pynamodb.connection
    - pynamodb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jlafon.io/pynamodb.html
  license: MIT License
  summary: 'A Pythonic Interface to DynamoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
