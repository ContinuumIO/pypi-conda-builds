package:
  name: django-crequest
  version: "2014.9.19"

source:
  fn: django-crequest-2014.9.19.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crequest/django-crequest-2014.9.19.tar.gz
  md5: aae9ef4377d77daff6507791ac5bd8f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crequest = django-crequest:main
    #
    # Would create an entry point called django-crequest that calls django-crequest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - crequest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Alir3z4/django-crequest
  license: BSD License
  summary: 'Middleware to make current request always available.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
