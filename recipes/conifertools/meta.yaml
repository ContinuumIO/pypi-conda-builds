package:
  name: conifertools
  version: "0.1"

source:
  fn: conifertools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/conifertools/conifertools-0.1.tar.gz
  md5: aa1433decb764c88db404d121f3539e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - conifertools = conifertools:main
    #
    # Would create an entry point called conifertools that calls conifertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - conifertools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nkrumm/conifer-tools
  license: UNKNOWN
  summary: 'A collection of helpful tools for use with CoNIFER'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
