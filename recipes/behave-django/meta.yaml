package:
  name: behave-django
  version: "0.1.2"

source:
  fn: behave-django-0.1.2.zip
  url: https://pypi.python.org/packages/source/b/behave-django/behave-django-0.1.2.zip
  md5: c05e2cbdff79ca6931a616df85adba80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - behave-django = behave-django:main
    #
    # Would create an entry point called behave-django that calls behave-django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - behave
    - django >=1.7

  run:
    - python
    - behave
    - django >=1.7

test:
  # Python imports
  imports:
    - behave_django
    - behave_django.management
    - behave_django.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mixxorz/behave-django
  license: MIT License
  summary: 'Behave BDD integration for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
