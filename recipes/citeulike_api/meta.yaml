package:
  name: citeulike_api
  version: "0.1.3dev"

source:
  fn: citeulike_api-0.1.3dev.tar.gz
  url: https://pypi.python.org/packages/source/c/citeulike_api/citeulike_api-0.1.3dev.tar.gz
  md5: 90a7ddfd8c2b17c36224a3cc9ab4b9ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - citeulike_api = citeulike_api:main
    #
    # Would create an entry point called citeulike_api that calls citeulike_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mechanize
    - nose
    - openmeta

  run:
    - python
    - mechanize
    - nose
    - openmeta

test:
  # Python imports
  imports:
    - citeulike_api
    - citeulike_api.output

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/howthebodyworks/citeulike_api/src
  license: BSD License
  summary: 'a python api for http://citeulike.org/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
