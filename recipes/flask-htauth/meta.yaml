package:
  name: flask-htauth
  version: "0.1.2"

source:
  fn: Flask-HTAuth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-HTAuth/Flask-HTAuth-0.1.2.tar.gz
  md5: 0591ea5969674c493ce584b05798e748
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-htauth = flask-htauth:main
    #
    # Would create an entry point called flask-htauth that calls flask-htauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.6

  run:
    - python
    - flask >=0.6

test:
  # Python imports
  imports:
    - flask_htauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tomekwojcik.github.com/flask-htauth/
  license: MIT License
  summary: 'Easy to integrate basic HTTP authentication for Flask apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
