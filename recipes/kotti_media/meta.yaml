package:
  name: kotti_media
  version: "0.6"

source:
  fn: kotti_media-0.6.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_media/kotti_media-0.6.tar.gz
  md5: 73f23c30dc1d148ad71909483dd666b5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_media = kotti_media:main
    #
    # Would create an entry point called kotti_media that calls kotti_media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.10b1
    - js.jquery_form
    - js.mediaelement

  run:
    - python
    - setuptools
    - kotti >=0.10b1
    - js.jquery_form
    - js.mediaelement

test:
  # Python imports
  imports:
    - kotti_media
    - kotti_media.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/disko/kotti_media
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Add media content to your Kotti site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
