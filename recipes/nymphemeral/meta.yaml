package:
  name: nymphemeral
  version: "1.3.1"

source:
  fn: nymphemeral-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nymphemeral/nymphemeral-1.3.1.tar.gz
  md5: 5929f6759bbbfb206c929ee3a1d31966
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nymphemeral = nymphemeral:main
    #
    # Would create an entry point called nymphemeral that calls nymphemeral.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyaxo >=0.3.5
    - python-gnupg >=0.3.5
    - python-dateutil >=2.2

  run:
    - python
    - pyaxo >=0.3.5
    - python-gnupg >=0.3.5
    - python-dateutil >=2.2

test:
  # Python imports
  imports:
    - nymphemeral

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/felipedau/nymphemeral
  license: GNU General Public License (GPL)
  summary: 'An ephemeral nymserver GUI client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
