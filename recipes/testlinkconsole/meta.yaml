package:
  name: testlinkconsole
  version: "0.3"

source:
  fn: testlinkconsole-0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/testlinkconsole/testlinkconsole-0.3.tar.gz
  md5: 73a9d19eb22f415884f80c3733053925
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testlinkconsole = testlinkconsole:main
    #
    # Would create an entry point called testlinkconsole that calls testlinkconsole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - termcolor
    - progressbar2
    - yapsy
    - cmd2

  run:
    - python
    - termcolor
    - progressbar2
    - yapsy
    - cmd2

test:
  # Python imports
  imports:
    - testlinkconsole

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chbrun/testlinkconsole
  license: BSD License
  summary: 'Testlink Console '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
