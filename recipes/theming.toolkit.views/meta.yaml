package:
  name: theming.toolkit.views
  version: "0.3"

source:
  fn: theming.toolkit.views-0.3.zip
  url: https://pypi.python.org/packages/source/t/theming.toolkit.views/theming.toolkit.views-0.3.zip
  md5: 3d5a5a6532d8cec2cd99bd5066c0921d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - theming.toolkit.views = theming.toolkit.views:main
    #
    # Would create an entry point called theming.toolkit.views that calls theming.toolkit.views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - theming
    - theming.toolkit
    - theming.toolkit.views
    - theming.toolkit.views.browser
    - theming.toolkit.views.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/propertyshelf/theming.toolkit.views
  license: gpl
  summary: 'Custom views for Plone Add-Ons.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
