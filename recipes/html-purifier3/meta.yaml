package:
  name: html-purifier3
  version: "2.1.0"

source:
  fn: html-purifier3-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/html-purifier3/html-purifier3-2.1.0.tar.gz
  md5: 6f457dd38bb5d694dd08aab4571d3104
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html-purifier3 = html-purifier3:main
    #
    # Would create an entry point called html-purifier3 that calls html-purifier3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - purifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/meunierd/python-html-purifier
  license: GPL3
  summary: 'Cuts the tags and attributes from HTML that are not in the\n    whitelist. Their content is left.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
