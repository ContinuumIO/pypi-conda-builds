package:
  name: django-bootstrap-crud-templates
  version: "1.0.24"

source:
  fn: django-bootstrap-crud-templates-1.0.24.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-crud-templates/django-bootstrap-crud-templates-1.0.24.tar.gz
  md5: 165dbc71837d81cd3ccc84ec6c7b85de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-crud-templates = django-bootstrap-crud-templates:main
    #
    # Would create an entry point called django-bootstrap-crud-templates that calls django-bootstrap-crud-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bsct
    - bsct.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django-bootstrap-crud-templates
  license: GNU General Public License v3 (GPLv3)
  summary: 'A repository of Bootstrap CRUD templates for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
