package:
  name: mplstereonet
  version: "0.4.0"

source:
  fn: mplstereonet-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mplstereonet/mplstereonet-0.4.0.tar.gz
  md5: ec1ad8169c23749e953c87333121c83a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mplstereonet = mplstereonet:main
    #
    # Would create an entry point called mplstereonet that calls mplstereonet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib >=1.1
    - numpy >=1.1

  run:
    - python
    - matplotlib >=1.1
    - numpy >=1.1

test:
  # Python imports
  imports:
    - mplstereonet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joferkington/mplstereonet/
  license: MIT
  summary: 'Stereonets for matplotlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
