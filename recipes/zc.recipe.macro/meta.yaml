package:
  name: zc.recipe.macro
  version: "1.3.0"

source:
  fn: zc.recipe.macro-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.recipe.macro/zc.recipe.macro-1.3.0.tar.gz
  md5: 4b1c6705715d969a82898e2f0fc790d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.recipe.macro = zc.recipe.macro:main
    #
    # Would create an entry point called zc.recipe.macro that calls zc.recipe.macro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.testrunner
    - zope.testing
    - manuel

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.testrunner
    - zope.testing
    - manuel

test:
  # Python imports
  imports:
    - zc
    - zc.recipe
    - zc.recipe.macro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'Macro-recipe for buildout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
