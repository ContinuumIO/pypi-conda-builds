package:
  name: pu_in_core
  version: "1.0.4"

source:
  fn: pu_in_core-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pu_in_core/pu_in_core-1.0.4.tar.gz
  md5: 9e8c2d9e7cf6099e4c63c869e35c050b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pu_in_core = pu_in_core:main
    #
    # Would create an entry point called pu_in_core that calls pu_in_core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - pu_in_core
    - pu_in_core.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/pu.intranet.core
  license: beer-ware
  summary: 'PythonUnited Intranet Core'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
