package:
  name: mozprocess
  version: "0.22"

source:
  fn: mozprocess-0.22.tar.gz
  url: https://pypi.python.org/packages/source/m/mozprocess/mozprocess-0.22.tar.gz
  md5: 70e5053505d868cabcfa236c3b6193c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozprocess = mozprocess:main
    #
    # Would create an entry point called mozprocess that calls mozprocess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mozinfo

  run:
    - python
    - setuptools
    - mozinfo

test:
  # Python imports
  imports:
    - mozprocess

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://wiki.mozilla.org/Auto-tools/Projects/Mozbase
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Mozilla-authored process handling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
