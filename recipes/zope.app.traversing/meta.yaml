package:
  name: zope.app.traversing
  version: "3.4.0"

source:
  fn: zope.app.traversing-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.traversing/zope.app.traversing-3.4.0.tar.gz
  md5: b587bc9b795c129aebabba4a8bc5f845
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.traversing = zope.app.traversing:main
    #
    # Would create an entry point called zope.app.traversing that calls zope.app.traversing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.traversing
    - zope.app.traversing.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.traversing
  license: Zope Public License
  summary: 'Zope Application Traversal Support (Deprectated)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
