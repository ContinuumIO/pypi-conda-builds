package:
  name: djet
  version: "0.0.10"

source:
  fn: djet-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/djet/djet-0.0.10.tar.gz
  md5: 556900e3d58d7e7beb666e194e9923a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djet = djet:main
    #
    # Would create an entry point called djet that calls djet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - six >=1.4.1

  run:
    - python
    - django >=1.3
    - six >=1.4.1

test:
  # Python imports
  imports:
    - djet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sunscrapers/djet
  license: MIT License
  summary: 'Set of helpers for easy testing of Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
