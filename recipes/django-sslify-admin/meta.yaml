package:
  name: django-sslify-admin
  version: "0.5.0"

source:
  fn: django-sslify-admin-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sslify-admin/django-sslify-admin-0.5.0.tar.gz
  md5: 7ebb9adf01757ac6013d2027f77c43a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sslify-admin = django-sslify-admin:main
    #
    # Would create an entry point called django-sslify-admin that calls django-sslify-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - sslifyadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/return1/django-sslify-admin
  license: MIT License
  summary: 'Force SSL on your Django admin site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
