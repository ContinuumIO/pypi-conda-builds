package:
  name: mopidy-cd
  version: "0.1"

source:
  fn: Mopidy-Cd-0.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Cd/Mopidy-Cd-0.1.tar.gz
  md5: 5e6367ad3a9a768d10a512802fb0301d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-cd = mopidy-cd:main
    #
    # Would create an entry point called mopidy-cd that calls mopidy-cd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.14
    - pykka >=1.1
    - discid >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.14
    - pykka >=1.1
    - discid >=1.1

test:
  # Python imports
  imports:
    - mopidy_cd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asartori/mopidy-cd
  license: Apache Software License
  summary: u'Mopidy Extension to play audio CD'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
