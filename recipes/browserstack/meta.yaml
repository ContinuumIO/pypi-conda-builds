package:
  name: browserstack
  version: "0.1"

source:
  fn: browserstack-0.1.zip
  url: https://pypi.python.org/packages/source/b/browserstack/browserstack-0.1.zip
  md5: fb0a53c9017e879c750bf75aeefb59af
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - browserstack = browserstack:main
    #
    # Would create an entry point called browserstack that calls browserstack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - api
    - browserstack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tr4n2uil/python-browserstack
  license: MIT
  summary: 'Unofficial Python Library for Browserstack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
