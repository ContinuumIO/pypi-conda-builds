package:
  name: django-jquery-widgets
  version: "0.1.2"

source:
  fn: django-jquery-widgets-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery-widgets/django-jquery-widgets-0.1.2.tar.gz
  md5: 1cc90aa1feeb16cd4995383fb1613209
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery-widgets = django-jquery-widgets:main
    #
    # Would create an entry point called django-jquery-widgets that calls django-jquery-widgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.1

  run:
    - python
    - django >=1.1

test:
  # Python imports
  imports:
    - jquery_widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smyrman/django-jquery-widgets
  license: BSD
  summary: 'jQuery-powered widgets and fields for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
