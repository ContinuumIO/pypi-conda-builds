package:
  name: collective.local.sendto
  version: "1.6.1"

source:
  fn: collective.local.sendto-1.6.1.zip
  url: https://pypi.python.org/packages/source/c/collective.local.sendto/collective.local.sendto-1.6.1.zip
  md5: be6c723f959fb6c10b28166a3ac44619
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.local.sendto = collective.local.sendto:main
    #
    # Would create an entry point called collective.local.sendto that calls collective.local.sendto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.local.userlisting >=1.3
    - plone.api
    - plone.behavior

  run:
    - python
    - setuptools
    - setuptools
    - collective.local.userlisting >=1.3
    - plone.api
    - plone.behavior

test:
  # Python imports
  imports:
    - collective
    - collective.local
    - collective.local.sendto
    - collective.local.sendto.browser
    - collective.local.sendto.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: "Adds a 'Mailing' tab to selected content types that allows to select users that have a role on the content and send them a wysiwyg composed email. By Ecreall."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
