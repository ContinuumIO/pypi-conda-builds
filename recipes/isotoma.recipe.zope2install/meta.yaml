package:
  name: isotoma.recipe.zope2install
  version: "0.0"

source:
  fn: isotoma.recipe.zope2install-0.0.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.zope2install/isotoma.recipe.zope2install-0.0.zip
  md5: 44215fe933e06a4fd38ca7c5fea7ee4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.zope2install = isotoma.recipe.zope2install:main
    #
    # Would create an entry point called isotoma.recipe.zope2install that calls isotoma.recipe.zope2install.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.zope2install
    - isotoma.recipe.zope2install.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/isotoma/isotoma.recipe.zope2install
  license: Zope Public License
  summary: 'ZC Buildout recipe for installing Zope 2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
