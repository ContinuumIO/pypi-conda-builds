package:
  name: gcurl
  version: "1.0"

source:
  fn: Gcurl-1.0.tar.gz
  url: https://pypi.python.org/packages/source/G/Gcurl/Gcurl-1.0.tar.gz
  md5: e9a110b951f3efefafedbffe321fd683
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gcurl = gcurl:main
    #
    # Would create an entry point called gcurl that calls gcurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent

  run:
    - python
    - setuptools
    - gevent

test:
  # Python imports
  imports:
    - Gcurl
    - gcurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.piadu.com
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'The descusr web application development framework, a gevent curl project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
