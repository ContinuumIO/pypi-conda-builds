package:
  name: gc-facebook-sdk
  version: "1.0.0-alpha"

source:
  fn: gc-facebook-sdk-1.0.0-alpha.tar.gz
  url: https://pypi.python.org/packages/source/g/gc-facebook-sdk/gc-facebook-sdk-1.0.0-alpha.tar.gz
  md5: a0bea9492887fe408317104692f5042a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gc-facebook-sdk = gc-facebook-sdk:main
    #
    # Would create an entry point called gc-facebook-sdk that calls gc-facebook-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - facebook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pythonforfacebook/facebook-sdk
  license: Apache Software License
  summary: 'This client library is designed to support the Facebook Graph API and the official Facebook JavaScript SDK, which is the canonical way to implement Facebook authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
