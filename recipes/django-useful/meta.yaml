package:
  name: django-useful
  version: "0.1.2"

source:
  fn: django-useful-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-useful/django-useful-0.1.2.tar.gz
  md5: 49e220102d14710edf46b9c69c5c30a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-useful = django-useful:main
    #
    # Would create an entry point called django-useful that calls django-useful.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - useful
    - useful.context_processors
    - useful.helpers
    - useful.tasks
    - useful.tests
    - useful.tests.context_processors
    - useful.tests.helpers
    - useful.tests.tasks
    - useful.tests.views
    - useful.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yprez/django-useful
  license: ISC License (ISCL)
  summary: 'Reusable Django snippets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
