package:
  name: bw2speedups
  version: "2.0"

source:
  fn: bw2speedups-2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bw2speedups/bw2speedups-2.0.tar.gz
  md5: 7bd46b8aa4af951bcdd06fd08684bcc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bw2speedups = bw2speedups:main
    #
    # Would create an entry point called bw2speedups that calls bw2speedups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - bw2speedups

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: bw2temporalis
  summary: 'Cython functions to speed up Brightway2 LCA calculations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
