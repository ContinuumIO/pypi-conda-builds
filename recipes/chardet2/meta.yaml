package:
  name: chardet2
  version: "2.0.3"

source:
  fn: chardet2-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/chardet2/chardet2-2.0.3.tar.gz
  md5: 59d5afa66f5edcc6f092e22c305c5232
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chardet2 = chardet2:main
    #
    # Would create an entry point called chardet2 that calls chardet2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chardet
    - chardet2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/byroot/chardet
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Character encoding auto-detection in Python 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
