package:
  name: django-idmap
  version: "0.3.2"

source:
  fn: django-idmap-0.3.2.zip
  url: https://pypi.python.org/packages/source/d/django-idmap/django-idmap-0.3.2.zip
  md5: d48bfec8ff8c1b43f866c9e478225de9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-idmap = django-idmap:main
    #
    # Would create an entry point called django-idmap that calls django-idmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - idmap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tkhyn/django-idmap
  license: BSD License
  summary: 'An identity mapper for the Django ORM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
