package:
  name: django-throttleandcache
  version: "2.1.0"

source:
  fn: django-throttleandcache-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-throttleandcache/django-throttleandcache-2.1.0.tar.gz
  md5: d468f077b04a7181e0b1a0fb1e55a0ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-throttleandcache = django-throttleandcache:main
    #
    # Would create an entry point called django-throttleandcache that calls django-throttleandcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil <2.0a
    - django-appconf >=0.5

  run:
    - python
    - python-dateutil <2.0a
    - django-appconf >=0.5

test:
  # Python imports
  imports:
    - tests
    - throttleandcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/matthewwithanm/django-throttleandcache
  license: BSD License
  summary: 'A utility for caching/throttling function calls.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
