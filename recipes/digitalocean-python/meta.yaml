package:
  name: digitalocean-python
  version: "0.1.5"

source:
  fn: digitalocean-python-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/digitalocean-python/digitalocean-python-0.1.5.tar.gz
  md5: facef0a8f2101bb19a74343b66e96d1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digitalocean-python = digitalocean-python:main
    #
    # Would create an entry point called digitalocean-python that calls digitalocean-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - pyocean

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bsdnoobz/pyocean
  license: MIT
  summary: 'Python wrapper for the DigitalOcean API v2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
