package:
  name: collective.classification
  version: "0.1b2"

source:
  fn: collective.classification-0.1b2.zip
  url: https://pypi.python.org/packages/source/c/collective.classification/collective.classification-0.1b2.zip
  md5: fdcd6b1ba0ffbb37aff51135eb524c04
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.classification = collective.classification:main
    #
    # Would create an entry point called collective.classification that calls collective.classification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - nltk
    - numpy

  run:
    - python
    - setuptools
    - setuptools
    - nltk
    - numpy

test:
  # Python imports
  imports:
    - collective
    - collective.classification
    - collective.classification.adapters
    - collective.classification.browser
    - collective.classification.classifiers
    - collective.classification.data
    - collective.classification.taggers
    - collective.classification.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/ggozad/collective.classification
  license: GNU General Public License (GPL)
  summary: 'Content classification/clustering through language processing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
