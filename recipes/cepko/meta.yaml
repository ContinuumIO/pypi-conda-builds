package:
  name: cepko
  version: "1.0.1"

source:
  fn: cepko-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cepko/cepko-1.0.1.tar.gz
  md5: a19f4eefba6c1ac2b63a0783e89e8ce1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cepko = cepko:main
    #
    # Would create an entry point called cepko that calls cepko.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - cepko

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudsigma/cepko
  license: GNU General Public License v3 (GPLv3)
  summary: "Communication with CloudSigma's VMs through a virtual serial port"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
