package:
  name: django-delegate
  version: "0.3.0"

source:
  fn: django-delegate-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-delegate/django-delegate-0.3.0.tar.gz
  md5: e5a738a34179737c73f3295ac6866a56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-delegate = django-delegate:main
    #
    # Would create an entry point called django-delegate that calls django-delegate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - delegate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fish2000/django-delegate/
  license: BSD License
  summary: '\nAutomatic delegate methods for Django managers and querysets,\nwithout runtime dispatch penalties.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
