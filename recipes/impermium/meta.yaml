package:
  name: impermium
  version: "0.4.0"

source:
  fn: impermium-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/i/impermium/impermium-0.4.0.tar.gz
  md5: 98891b506fa0da8f8626db5d8d2b0b98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - impermium = impermium:main
    #
    # Would create an entry point called impermium that calls impermium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - impermium

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/disqus/impermium-python
  license: Apache License 2.0
  summary: 'Impermium API bindings for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
