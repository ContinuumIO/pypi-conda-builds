package:
  name: googlemaps
  version: "2.2"

source:
  fn: googlemaps-2.2.tar.gz
  url: https://pypi.python.org/packages/source/g/googlemaps/googlemaps-2.2.tar.gz
  md5: 49301eacec813da541b0c2eff6ef0223
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googlemaps = googlemaps:main
    #
    # Would create an entry point called googlemaps that calls googlemaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - googlemaps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/googlemaps/google-maps-services-python
  license: Apache Software License
  summary: 'Python client library for Google Maps API Web Services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
