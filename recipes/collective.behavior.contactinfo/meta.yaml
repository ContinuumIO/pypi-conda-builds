package:
  name: collective.behavior.contactinfo
  version: "0.9"

source:
  fn: collective.behavior.contactinfo-0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.behavior.contactinfo/collective.behavior.contactinfo-0.9.tar.gz
  md5: e7e3e7d330ef4ee70a65002b0d632022
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.behavior.contactinfo = collective.behavior.contactinfo:main
    #
    # Would create an entry point called collective.behavior.contactinfo that calls collective.behavior.contactinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity

test:
  # Python imports
  imports:
    - collective
    - collective.behavior
    - collective.behavior.contactinfo
    - collective.behavior.contactinfo.behavior
    - collective.behavior.contactinfo.docs
    - collective.behavior.contactinfo.interfaces
    - collective.behavior.contactinfo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'A set of dexterity behaviors providing common contact info fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
