package:
  name: medularis-django-utils
  version: "0.1.2"

source:
  fn: medularis-django-utils-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/medularis-django-utils/medularis-django-utils-0.1.2.tar.gz
  md5: 1246980d245e7520fd763366bfb11643
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medularis-django-utils = medularis-django-utils:main
    #
    # Would create an entry point called medularis-django-utils that calls medularis-django-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - med_djutils
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/medularis/medularis-django-utils
  license: BSD License
  summary: u"Medularis's open source utilities for use in Django projects"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
