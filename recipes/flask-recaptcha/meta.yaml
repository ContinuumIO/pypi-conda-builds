package:
  name: flask-recaptcha
  version: "0.3"

source:
  fn: Flask-ReCaptcha-0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-reCaptcha/Flask-ReCaptcha-0.3.tar.gz
  md5: 71b1341fb6c03a3aaa3ca2538a177887
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-recaptcha = flask-recaptcha:main
    #
    # Would create an entry point called flask-recaptcha that calls flask-recaptcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests

  run:
    - python
    - flask
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mardix/flask-recaptcha/
  license: BSD License
  summary: 'The new Google ReCaptcha implementation for Flask without Flask-WTF'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
