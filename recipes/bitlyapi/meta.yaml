package:
  name: bitlyapi
  version: "0.1.1"

source:
  fn: bitlyapi-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bitlyapi/bitlyapi-0.1.1.tar.gz
  md5: 93aac3707a3ca8a4fc17f9976ec87cde
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitlyapi = bitlyapi:main
    #
    # Would create an entry point called bitlyapi that calls bitlyapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - bitlyapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hellp/bitlyapi
  license: BSD License
  summary: 'A thin wrapper for the bit.ly REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
