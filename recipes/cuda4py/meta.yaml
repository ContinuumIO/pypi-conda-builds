package:
  name: cuda4py
  version: "1.1.1"

source:
  fn: cuda4py-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cuda4py/cuda4py-1.1.1.tar.gz
  md5: b935c0650b135452f0063f57c73a46d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cuda4py = cuda4py:main
    #
    # Would create an entry point called cuda4py that calls cuda4py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi

  run:
    - python
    - cffi

test:
  # Python imports
  imports:
    - cuda4py
    - cuda4py.blas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ajkxyz/cuda4py
  license: BSD License
  summary: 'CUDA cffi bindings and helper classes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
