package:
  name: django-fabdeploy-plus
  version: "0.4"

source:
  fn: django-fabdeploy-plus-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fabdeploy-plus/django-fabdeploy-plus-0.4.tar.gz
  md5: 76c76eb92334c5ebda69a5c7e3fe738c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fabdeploy-plus = django-fabdeploy-plus:main
    #
    # Would create an entry point called django-fabdeploy-plus that calls django-fabdeploy-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - fabric
    - jinja2

  run:
    - python
    - fabric
    - jinja2

test:
  # Python imports
  imports:
    - fabdeploy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vmihailenco/fabdeploy/
  license: MIT License
  summary: 'Fabric deployment for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
