package:
  name: django-private-media
  version: "0.1.3"

source:
  fn: django-private-media-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-private-media/django-private-media-0.1.3.tar.gz
  md5: 595bde11387105c669a77722b6e560ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-private-media = django-private-media:main
    #
    # Would create an entry point called django-private-media that calls django-private-media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - private_media

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RacingTadpole/django-private-media
  license: BSD License
  summary: "Private media for Django. Check the user's authorization before serving files at PRIVATE_MEDIA_URL, uploaded to PRIVATE_MEDIA_ROOT. Requires Django 1.5."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
