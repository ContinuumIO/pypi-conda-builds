package:
  name: flask-micropub
  version: "0.2.1"

source:
  fn: Flask-Micropub-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Micropub/Flask-Micropub-0.2.1.tar.gz
  md5: 9e704ae76ef3687c20fef15de9f65099
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-micropub = flask-micropub:main
    #
    # Would create an entry point called flask-micropub that calls flask-micropub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests
    - beautifulsoup4

  run:
    - python
    - flask
    - requests
    - beautifulsoup4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kylewm/flask-micropub/
  license: BSD License
  summary: 'Adds support for Micropub clients.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
