package:
  name: boto_utils
  version: "0.3"

source:
  fn: boto_utils-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/boto_utils/boto_utils-0.3.tar.gz
  md5: 3d293ce12463674e256e540c8525b7eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boto_utils = boto_utils:main
    #
    # Would create an entry point called boto_utils that calls boto_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.2.0
    - pytz

  run:
    - python
    - boto >=2.2.0
    - pytz

test:
  # Python imports
  imports:
    - boto_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/obeattie/boto_utils
  license: GNU General Public License (GPL)
  summary: 'Command-line tools based on Boto'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
