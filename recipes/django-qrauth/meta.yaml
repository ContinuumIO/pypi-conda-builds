package:
  name: django-qrauth
  version: "0.1.2"

source:
  fn: django-qrauth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qrauth/django-qrauth-0.1.2.tar.gz
  md5: dfe679dbd646d6e6ec62ba069f8bdee9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qrauth = django-qrauth:main
    #
    # Would create an entry point called django-qrauth that calls django-qrauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - qrcode
    - redis
    - pil

  run:
    - python
    - qrcode
    - redis
    - pil

test:
  # Python imports
  imports:
    - qrauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aruseni/django-qrauth
  license: BSD licence, see LICENCE.md
  summary: 'Nice QR codes that allow the users to instantly sign in to the website on their mobile devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
