package:
  name: python-tr
  version: "0.1.1"

source:
  fn: python-tr-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-tr/python-tr-0.1.1.tar.gz
  md5: 9a913829e799708ab3fe586395d13c6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-tr = python-tr:main
    #
    # Would create an entry point called python-tr that calls python-tr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ikegami-yukino/python-tr
  license: MIT License
  summary: 'A Pure-Python implementation of the tr algorithm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
