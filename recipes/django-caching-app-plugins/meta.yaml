package:
  name: django-caching-app-plugins
  version: "0.1.4"

source:
  fn: django-caching-app-plugins-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-caching-app-plugins/django-caching-app-plugins-0.1.4.tar.gz
  md5: 30edc9bc7d742c7b2bb9b140b35daef6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-caching-app-plugins = django-caching-app-plugins:main
    #
    # Would create an entry point called django-caching-app-plugins that calls django-caching-app-plugins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - app_plugins
    - app_plugins.management
    - app_plugins.management.commands
    - app_plugins.templatetags
    - app_plugins.test_app
    - app_plugins.test_app.site
    - app_plugins.test_app.someapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/bkroeze/django-caching-app-plugins
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
