package:
  name: blocks-and-bullets
  version: "0.1"

source:
  fn: Blocks and Bullets-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/Blocks-and-Bullets/Blocks and Bullets-0.1.tar.gz
  md5: 3c8c8827856e119b2f518ae1dd09d0af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blocks-and-bullets = blocks-and-bullets:main
    #
    # Would create an entry point called blocks-and-bullets that calls blocks-and-bullets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - BlocksandBullets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sirolligobbervii.zxq.net/Programming/BlocksandBullets/
  license: LICENSE.txt
  summary: 'A simple game of Blocks and Bullets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
