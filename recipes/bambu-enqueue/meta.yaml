package:
  name: bambu-enqueue
  version: "0.2"

source:
  fn: bambu-enqueue-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-enqueue/bambu-enqueue-0.2.tar.gz
  md5: 9336a798307e3545f1c57f513aba7c62
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-enqueue = bambu-enqueue:main
    #
    # Would create an entry point called bambu-enqueue that calls bambu-enqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bambu.enqueue
    - bambu.enqueue.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-enqueue
  license: UNKNOWN
  summary: 'Enqueue CSS and JavaScript (inspired by WordPress)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
