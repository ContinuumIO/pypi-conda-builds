package:
  name: django-scribbler
  version: "0.5.3"

source:
  fn: django-scribbler-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-scribbler/django-scribbler-0.5.3.tar.gz
  md5: f45d93b8fd0f1f03558d195a064de0a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-scribbler = django-scribbler:main
    #
    # Would create an entry point called django-scribbler that calls django-scribbler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - scribbler
    - scribbler.migrations
    - scribbler.templatetags
    - scribbler.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caktus/scribbler
  license: BSD License
  summary: 'django-scribbler is an application for managing snippets of text for a Django website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
