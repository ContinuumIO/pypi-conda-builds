package:
  name: openexr
  version: "1.2.0"

source:
  fn: OpenEXR-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/O/OpenEXR/OpenEXR-1.2.0.tar.gz
  md5: 10a4949d5b7d2372f7e2064f2c683264
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openexr = openexr:main
    #
    # Would create an entry point called openexr that calls openexr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - openexr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://excamera.com/articles/26/openexr.html
  license: UNKNOWN
  summary: "Python bindings for ILM's OpenEXR image file format"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
