package:
  name: celery-queued-once
  version: "0.2"

source:
  fn: celery-queued-once-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/celery-queued-once/celery-queued-once-0.2.tar.gz
  md5: a4a945afec43373a467079ee403a23b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celery-queued-once = celery-queued-once:main
    #
    # Would create an entry point called celery-queued-once that calls celery-queued-once.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery
    - django >=1.7

  run:
    - python
    - celery
    - django >=1.7

test:
  # Python imports
  imports:
    - queued_once

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Celery base task de-duplicating tasks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
