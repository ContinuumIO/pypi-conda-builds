package:
  name: django-configurable-inclusion-tag
  version: "0.1.1"

source:
  fn: django-configurable-inclusion-tag-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-configurable-inclusion-tag/django-configurable-inclusion-tag-0.1.1.tar.gz
  md5: f31021a33ca5ccb29feb67cc53fdb226
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-configurable-inclusion-tag = django-configurable-inclusion-tag:main
    #
    # Would create an entry point called django-configurable-inclusion-tag that calls django-configurable-inclusion-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/defcube/django_configurable_inclusion_tag/
  license: UNKNOWN
  summary: 'A smart template inclusion tag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
