package:
  name: collective.z3cform.addablechoice
  version: "0.6"

source:
  fn: collective.z3cform.addablechoice-0.6.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.addablechoice/collective.z3cform.addablechoice-0.6.zip
  md5: 91b01bb555433094a6896da4ae5552ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.addablechoice = collective.z3cform.addablechoice:main
    #
    # Would create an entry point called collective.z3cform.addablechoice that calls collective.z3cform.addablechoice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.addablechoice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.z3cform.addablechoice
  license: GPL
  summary: 'Adds a choice widget with text field to z3cform.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
