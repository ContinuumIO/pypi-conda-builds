package:
  name: ifttt
  version: "0.3.0"

source:
  fn: ifttt-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ifttt/ifttt-0.3.0.tar.gz
  md5: d248958995814e92c4ac4a0cf1fa439a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ifttt = ifttt:main
    #
    # Would create an entry point called ifttt that calls ifttt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz
    - gevent

  run:
    - python
    - pytz
    - gevent

test:
  # Python imports
  imports:
    - ifttt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/newslynx/ifttt
  license: MIT License
  summary: 'A Pythonic interface for building IFTTT plugins routed over email.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
