package:
  name: django-forms-smoq
  version: "0.1"

source:
  fn: django-forms-smoq-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-forms-smoq/django-forms-smoq-0.1.tar.gz
  md5: a78aee456df3f1dccbcf330e8e0c4d4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-forms-smoq = django-forms-smoq:main
    #
    # Would create an entry point called django-forms-smoq that calls django-forms-smoq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - forms_smoq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SmoQ/django-forms-smoq
  license: BSD License
  summary: 'Forms-SmoQ django is a simple application that contains useful forms for use in django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
