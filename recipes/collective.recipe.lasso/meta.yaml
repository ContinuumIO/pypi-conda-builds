package:
  name: collective.recipe.lasso
  version: "0.1"

source:
  fn: collective.recipe.lasso-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.lasso/collective.recipe.lasso-0.1.tar.gz
  md5: 6481d9d5b249b48f46fa637e85b7b563
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.lasso = collective.recipe.lasso:main
    #
    # Would create an entry point called collective.recipe.lasso that calls collective.recipe.lasso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.cmmi >=1.3.0
    - elementtree

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.cmmi >=1.3.0
    - elementtree

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.lasso

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://shane.willowrise.com
  license: ZPL
  summary: 'Buildout recipe to install Lasso (SSO library)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
