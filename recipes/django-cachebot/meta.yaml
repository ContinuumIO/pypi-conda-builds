package:
  name: django-cachebot
  version: "0.4.1"

source:
  fn: django-cachebot-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cachebot/django-cachebot-0.4.1.tar.gz
  md5: 317be7b6127b8f8e265efa261906a519
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cachebot = django-cachebot:main
    #
    # Would create an entry point called django-cachebot that calls django-cachebot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - cachebot
    - cachebot.backends
    - cachebot.management
    - cachebot.management.commands
    - cachebot.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dziegler/django-cachebot
  license: BSD License
  summary: 'Automated caching and invalidation for the Django ORM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
