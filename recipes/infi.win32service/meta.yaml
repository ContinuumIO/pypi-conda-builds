package:
  name: infi.win32service
  version: "0.1.6"

source:
  fn: infi.win32service-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.win32service/infi.win32service-0.1.6.tar.gz
  md5: 83a44eae03a746f14fde3c179bc9ed9b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.win32service = infi.win32service:main
    #
    # Would create an entry point called infi.win32service that calls infi.win32service.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.win32service

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.win32service
  license: Python Software Foundation License
  summary: 'Python bindings to Windows ServiceControlManager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
