package:
  name: js.flat_ui
  version: "1.1"

source:
  fn: js.flat_ui-1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.flat_ui/js.flat_ui-1.1.tar.gz
  md5: 42c7215538722a34f12e193d16fb1bd5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.flat_ui = js.flat_ui:main
    #
    # Would create an entry point called js.flat_ui that calls js.flat_ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.bootstrap

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.bootstrap

test:
  # Python imports
  imports:
    - js
    - js.flat_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: CC BY 3.0
  summary: 'Fanstatic packaging of Flat-UI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
