package:
  name: django-oneall
  version: "0.1.4"

source:
  fn: django-oneall-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oneall/django-oneall-0.1.4.tar.gz
  md5: b347440170deca73b04d586cc3f45d7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oneall = django-oneall:main
    #
    # Would create an entry point called django-oneall that calls django-oneall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyoneall ==0.1

  run:
    - python
    - pyoneall ==0.1

test:
  # Python imports
  imports:
    - django_oneall

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.leandigo.com/
  license: MIT License, see LICENSE file
  summary: 'Django Authentication support for OneAll. Provides unified authentication for 20+ social networks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
