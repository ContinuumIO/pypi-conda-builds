package:
  name: collectors
  version: "1.0"

source:
  fn: Collectors-1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/Collectors/Collectors-1.0.tar.gz
  md5: e78b210800b659116f05b043a6a60658
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collectors = collectors:main
    #
    # Would create an entry point called collectors that calls collectors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - collectors
    - collectors.storage
    - collectors.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stefan.sofa-rockers.org/Collectors/
  license: BSD License
  summary: 'Monitor your (SimPy) simulation models or other objects and collect data from them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
