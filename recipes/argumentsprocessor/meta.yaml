package:
  name: argumentsprocessor
  version: "1.1.6"

source:
  fn: argumentsprocessor-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/a/argumentsprocessor/argumentsprocessor-1.1.6.tar.gz
  md5: 6b1c483d3ca605bbc4cbe621042cb2f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argumentsprocessor = argumentsprocessor:main
    #
    # Would create an entry point called argumentsprocessor that calls argumentsprocessor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - argumentsprocessor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/excellentingenuity/argumentsprocessor
  license: BSD License
  summary: 'A Python module that process a dictionary of arguments against a supplied dictionary of expected arguments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
