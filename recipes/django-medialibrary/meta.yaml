package:
  name: django-medialibrary
  version: "1.4.9"

source:
  fn: django-medialibrary-1.4.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-medialibrary/django-medialibrary-1.4.9.tar.gz
  md5: 9fac4939236a324342b032daf1f9adac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-medialibrary = django-medialibrary:main
    #
    # Would create an entry point called django-medialibrary that calls django-medialibrary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.6
    - django-model-utils
    - django-jsonfield >=0.9.10
    - djangorestframework >=2.3.12

  run:
    - python
    - django >=1.4,<1.6
    - django-model-utils
    - django-jsonfield >=0.9.10
    - djangorestframework >=2.3.12

test:
  # Python imports
  imports:
    - medialibrary
    - medialibrary.migrations
    - medialibrary.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pulilab/django-medialibrary
  license: BSD 3-Clause License, see the LICENSE file for details
  summary: u'A pluggable django app for media management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
