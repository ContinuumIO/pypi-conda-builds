package:
  name: laptopcontrolpanel
  version: "0.1.0"

source:
  fn: LaptopControlPanel-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/L/LaptopControlPanel/LaptopControlPanel-0.1.0.tar.gz
  md5: 409313ce110875b9578ed73153c42866
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - laptopcontrolpanel = laptopcontrolpanel:main
    #
    # Would create an entry point called laptopcontrolpanel that calls laptopcontrolpanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - LaptopControlPanel
    - LaptopControlPanel.Acpi
    - LaptopControlPanel.Application
    - LaptopControlPanel.Config
    - LaptopControlPanel.DataBase
    - LaptopControlPanel.GUI
    - LaptopControlPanel.GUI.Pages
    - LaptopControlPanel.GUI.Pages.ui
    - LaptopControlPanel.GUI.Widgets
    - LaptopControlPanel.GUI.ui
    - LaptopControlPanel.Kernel
    - LaptopControlPanel.Logging
    - LaptopControlPanel.Monitoring
    - LaptopControlPanel.System
    - LaptopControlPanel.Tools
    - laptopcontrolpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FabriceSalvaire/LaptopControlPanel
  license: GNU General Public License (GPL)
  summary: 'A Control Panel for Lenovo Thinkpad Laptop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
