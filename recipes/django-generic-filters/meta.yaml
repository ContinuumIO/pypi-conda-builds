package:
  name: django-generic-filters
  version: "1.1"

source:
  fn: django-generic-filters-1.1.zip
  url: https://pypi.python.org/packages/source/d/django-generic-filters/django-generic-filters-1.1.zip
  md5: b4a99c833195de13210cf44103cb0973
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-filters = django-generic-filters:main
    #
    # Would create an entry point called django-generic-filters that calls django-generic-filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - bunch
    - django-templateaddons

  run:
    - python
    - setuptools
    - django
    - bunch
    - django-templateaddons

test:
  # Python imports
  imports:
    - django_genericfilters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/novapost/django-generic-filters
  license: BSD License
  summary: 'Easy filters for your Generic ListView with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
