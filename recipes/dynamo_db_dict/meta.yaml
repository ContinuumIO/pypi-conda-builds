package:
  name: dynamo_db_dict
  version: "0.2.4"

source:
  fn: dynamo_db_dict-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamo_db_dict/dynamo_db_dict-0.2.4.tar.gz
  md5: adf3e8c061c8abe2f6044af7a765fc57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamo_db_dict = dynamo_db_dict:main
    #
    # Would create an entry point called dynamo_db_dict that calls dynamo_db_dict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - adict
    - boto

  run:
    - python
    - adict
    - boto

test:
  # Python imports
  imports:
    - dynamo_db_dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/denis-ryzhkov/dynamo_db_dict
  license: MIT License
  summary: 'Simple Python interface to Amazon DynamoDB, adding some dict-like sugar to boto.dynamodb.layer2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
