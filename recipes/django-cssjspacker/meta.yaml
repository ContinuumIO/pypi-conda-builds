package:
  name: django-cssjspacker
  version: "0.2"

source:
  fn: django-cssjspacker-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cssjspacker/django-cssjspacker-0.2.tar.gz
  md5: a4550ae3ba9790cf3501e436676ae40a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cssjspacker = django-cssjspacker:main
    #
    # Would create an entry point called django-cssjspacker that calls django-cssjspacker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_cssjspacker
    - django_cssjspacker.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/n0s/django-cssjspacker/wiki/
  license: BSD
  summary: 'django application for js and css compression'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
