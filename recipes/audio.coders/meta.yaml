package:
  name: audio.coders
  version: "4.0.2"

source:
  fn: audio.coders-4.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/audio.coders/audio.coders-4.0.2.tar.gz
  md5: a38dc7e90d778e4ad662d5e3af0ca0a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audio.coders = audio.coders:main
    #
    # Would create an entry point called audio.coders that calls audio.coders.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bitstream

  run:
    - python
    - bitstream

test:
  # Python imports
  imports:
    - audio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Unary and Rice codecs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
