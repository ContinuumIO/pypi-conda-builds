package:
  name: ely.contentgenerator
  version: "0.1.dev-r149"

source:
  fn: ely.contentgenerator-0.1.dev-r149.tar.gz
  url: https://pypi.python.org/packages/source/e/ely.contentgenerator/ely.contentgenerator-0.1.dev-r149.tar.gz
  md5: e93f64145b11b7e26bc0b241486a513b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ely.contentgenerator = ely.contentgenerator:main
    #
    # Would create an entry point called ely.contentgenerator that calls ely.contentgenerator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ely.contentgenerator
    - elementtree

  run:
    - python
    - setuptools
    - ely.contentgenerator
    - elementtree

test:
  # Python imports
  imports:
    - ely
    - ely.contentgenerator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ely.googlecode.com/svn/ely.contentgenerator/trunk
  license: new BSD
  summary: 'Product for creating content in Zope'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
