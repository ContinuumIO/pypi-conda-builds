package:
  name: collective.cart.core
  version: "0.9.1"

source:
  fn: collective.cart.core-0.9.1.zip
  url: https://pypi.python.org/packages/source/c/collective.cart.core/collective.cart.core-0.9.1.zip
  md5: ceb5b170e4f67ee5fdb32227120b6cba
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cart.core = collective.cart.core:main
    #
    # Would create an entry point called collective.cart.core that calls collective.cart.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.base
    - collective.behavior.salable
    - plone.app.dexterity
    - setuptools

  run:
    - python
    - setuptools
    - collective.base
    - collective.behavior.salable
    - plone.app.dexterity
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.cart
    - collective.cart.core
    - collective.cart.core.adapter
    - collective.cart.core.browser
    - collective.cart.core.portlets
    - collective.cart.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.cart.core/
  license: BSD License
  summary: 'Yet another cart for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
