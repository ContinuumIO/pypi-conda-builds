package:
  name: django-denormalize
  version: "0.2.2"

source:
  fn: django-denormalize-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-denormalize/django-denormalize-0.2.2.tar.gz
  md5: 5647d6567fb0baf12c2137567f655c41
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-denormalize = django-denormalize:main
    #
    # Would create an entry point called django-denormalize that calls django-denormalize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - setuptools
    - django >=1.4

test:
  # Python imports
  imports:
    - denormalize
    - denormalize.backend
    - denormalize.integration
    - denormalize.management
    - denormalize.management.commands
    - denormalize.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/wojas/django-denormalize/
  license: MIT License
  summary: 'Converts Django ORM objects into data documents, and keeps them in sync'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
