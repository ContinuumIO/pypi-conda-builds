package:
  name: quizzer
  version: "0.4"

source:
  fn: quizzer-0.4.tar.gz
  url: https://pypi.python.org/packages/source/q/quizzer/quizzer-0.4.tar.gz
  md5: d9e52aeb8122ca3d6438844878242997
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quizzer = quizzer:main
    #
    # Would create an entry point called quizzer that calls quizzer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - quizzer
    - quizzer.ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.tremily.us/posts/quizzer/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Break learning up into small task-based tests for focused study.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
