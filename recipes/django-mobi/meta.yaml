package:
  name: django-mobi
  version: "0.1.7"

source:
  fn: django-mobi-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mobi/django-mobi-0.1.7.tar.gz
  md5: 1a0223bd5f0154983f077c37f0603838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobi = django-mobi:main
    #
    # Would create an entry point called django-mobi that calls django-mobi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mobi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kencochrane/django-mobi/
  license: BSD License
  summary: 'Django middleware and view decorator to detect phones and small-screen devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
