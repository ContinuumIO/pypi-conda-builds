package:
  name: cmsplugin-embedded-pages
  version: "0.0.4"

source:
  fn: cmsplugin-embedded-pages-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-embedded-pages/cmsplugin-embedded-pages-0.0.4.tar.gz
  md5: 3d64c5ba885967d05a5ea07a9a167b43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-embedded-pages = cmsplugin-embedded-pages:main
    #
    # Would create an entry point called cmsplugin-embedded-pages that calls cmsplugin-embedded-pages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms

  run:
    - python
    - django-cms

test:
  # Python imports
  imports:
    - cmsplugin_embeddedpages
    - cmsplugin_embeddedpages.lib
    - cmsplugin_embeddedpages.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/airtonix/cmsplugin-embedded-pages
  license: BSD License
  summary: 'DjangoCMS plugin that allows for embedding other pages in placeholders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
