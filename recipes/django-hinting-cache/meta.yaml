package:
  name: django-hinting-cache
  version: "0.2.1"

source:
  fn: django-hinting-cache-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hinting-cache/django-hinting-cache-0.2.1.tar.gz
  md5: 2fba5bc39e757e7755de3a5f95315c3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hinting-cache = django-hinting-cache:main
    #
    # Would create an entry point called django-hinting-cache that calls django-hinting-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_hinting_cache
    - django_hinting_cache.templatetags
    - django_hinting_cache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dmclain/django-hinting-cache
  license: MIT License
  summary: "Cache wrapper that turns many gets into fewer get_many's"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
