package:
  name: django-bylaws
  version: "1.0.2"

source:
  fn: django-bylaws-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bylaws/django-bylaws-1.0.2.tar.gz
  md5: 984ee4e8be179de2c6e37d9a02ddcdb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bylaws = django-bylaws:main
    #
    # Would create an entry point called django-bylaws that calls django-bylaws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-markup-mixin
    - django-extensions
    - simple_history

  run:
    - python
    - django-markup-mixin
    - django-extensions
    - simple_history

test:
  # Python imports
  imports:
    - bylaws

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-bylaws
  license: BSD License
  summary: 'Manages bylaws for a website built on Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
