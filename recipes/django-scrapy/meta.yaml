package:
  name: django-scrapy
  version: "0.1a1"

source:
  fn: django-scrapy-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-scrapy/django-scrapy-0.1a1.tar.gz
  md5: db672d1e917eecb0b0e4806872e6e792
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-scrapy = django-scrapy:main
    #
    # Would create an entry point called django-scrapy that calls django-scrapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - setuptools
    - django >=1.6

test:
  # Python imports
  imports:
    - djscrapy
    - djscrapy.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youngershen/django-scrapy
  license: MIT License
  summary: 'apis for you can use scrapy in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
