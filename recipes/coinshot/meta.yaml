package:
  name: coinshot
  version: "2.0.0"

source:
  fn: coinshot-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coinshot/coinshot-2.0.0.tar.gz
  md5: 9f4b6fc6cc2cfa11cdc1d802a8f49549
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinshot = coinshot:main
    #
    # Would create an entry point called coinshot that calls coinshot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=3.3.0

  run:
    - python
    - simplejson >=3.3.0

test:
  # Python imports
  imports:
    - coinshot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charlesthomas/coinshot
  license: MIT License
  summary: 'simple python module for pushover.net'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
