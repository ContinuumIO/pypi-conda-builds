package:
  name: flask-table
  version: "0.2.8"

source:
  fn: Flask-Table-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Table/Flask-Table-0.2.8.tar.gz
  md5: c85079b452be5233f0042beab0eb2eed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-table = flask-table:main
    #
    # Would create an entry point called flask-table that calls flask-table.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - babel

  run:
    - python
    - flask
    - babel

test:
  # Python imports
  imports:
    - flask_table

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plumdog/flask_table
  license: UNKNOWN
  summary: 'HTML tables for use with the Flask micro-framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
