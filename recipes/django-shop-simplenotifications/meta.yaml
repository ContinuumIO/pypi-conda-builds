package:
  name: django-shop-simplenotifications
  version: "0.2"

source:
  fn: django-shop-simplenotifications-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-simplenotifications/django-shop-simplenotifications-0.2.tar.gz
  md5: 996dac4abc23d39ed798d4b2442c3715
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-simplenotifications = django-shop-simplenotifications:main
    #
    # Would create an entry point called django-shop-simplenotifications that calls django-shop-simplenotifications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-shop

  run:
    - python
    - django-shop

test:
  # Python imports
  imports:
    - shop_simplenotifications
    - shop_simplenotifications.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-shop-simplenotifications
  license: BSD License
  summary: 'A simple reusable app for django-shop that sends emailnotifications on certain events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
