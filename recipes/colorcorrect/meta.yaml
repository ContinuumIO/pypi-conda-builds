package:
  name: colorcorrect
  version: "0.05"

source:
  fn: colorcorrect-0.05.tar.gz
  url: https://pypi.python.org/packages/source/c/colorcorrect/colorcorrect-0.05.tar.gz
  md5: f499146749836d1603a42ba5a20e7849
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorcorrect = colorcorrect:main
    #
    # Would create an entry point called colorcorrect that calls colorcorrect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - pil

  run:
    - python
    - numpy
    - pil

test:
  # Python imports
  imports:
    - colorcorrect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/aihara/colorcorrect
  license: MIT License
  summary: 'imprement some of color correction algorithms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
