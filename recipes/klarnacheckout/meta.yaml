package:
  name: klarnacheckout
  version: "1.0.2"

source:
  fn: KlarnaCheckout-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/K/KlarnaCheckout/KlarnaCheckout-1.0.2.tar.gz
  md5: 6d00aa02e2bf4a701f2c8269f1b7c894
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - klarnacheckout = klarnacheckout:main
    #
    # Would create an entry point called klarnacheckout that calls klarnacheckout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - klarnacheckout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://developers.klarna.com
  license: Apache 2
  summary: 'Klarna Checkout library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
