package:
  name: redomino.breadcrumbsbrain
  version: "0.4"

source:
  fn: redomino.breadcrumbsbrain-0.4.zip
  url: https://pypi.python.org/packages/source/r/redomino.breadcrumbsbrain/redomino.breadcrumbsbrain-0.4.zip
  md5: 6d8bb38c9deccbecc676378622062259
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.breadcrumbsbrain = redomino.breadcrumbsbrain:main
    #
    # Would create an entry point called redomino.breadcrumbsbrain that calls redomino.breadcrumbsbrain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redomino
    - redomino.breadcrumbsbrain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.breadcrumbsbrain
  license: GNU General Public License (GPL)
  summary: 'Breadcrumbs for brains'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
