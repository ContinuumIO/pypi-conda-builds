package:
  name: collective.portlet.categories
  version: "1.0"

source:
  fn: collective.portlet.categories-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.categories/collective.portlet.categories-1.0.zip
  md5: 61016b526101c6c54b33f721b0c9a3ba
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.categories = collective.portlet.categories:main
    #
    # Would create an entry point called collective.portlet.categories that calls collective.portlet.categories.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.categories

  run:
    - python
    - setuptools
    - setuptools
    - collective.categories

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.categories
    - collective.portlet.categories.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.portlet.categories
  license: GNU General Public License (GPL)
  summary: 'Categories portlet for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
