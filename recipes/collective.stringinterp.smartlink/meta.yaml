package:
  name: collective.stringinterp.smartlink
  version: "0.3"

source:
  fn: collective.stringinterp.smartlink-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.stringinterp.smartlink/collective.stringinterp.smartlink-0.3.zip
  md5: ccc53960276ac6166a852799a9ce08e2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.stringinterp.smartlink = collective.stringinterp.smartlink:main
    #
    # Would create an entry point called collective.stringinterp.smartlink that calls collective.stringinterp.smartlink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.stringinterp
    - redturtle.smartlink >=1.1.0
    - plone.app.discussion

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.stringinterp
    - redturtle.smartlink >=1.1.0
    - plone.app.discussion

test:
  # Python imports
  imports:
    - collective
    - collective.stringinterp
    - collective.stringinterp.smartlink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.stringinterp.smartlink
  license: GPL
  summary: 'Plone string interpolation override for URLs, with backend-frontend support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
