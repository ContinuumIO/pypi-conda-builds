package:
  name: winappdbg
  version: "1.5"

source:
  fn: winappdbg-1.5.zip
  url: https://pypi.python.org/packages/source/w/winappdbg/winappdbg-1.5.zip
  md5: 21ffe746b4efb87f39e6028d1c35956e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - winappdbg = winappdbg:main
    #
    # Would create an entry point called winappdbg that calls winappdbg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - winappdbg
    - winappdbg.plugins
    - winappdbg.win32

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://winappdbg.sourceforge.net/
  license: BSD License
  summary: 'Windows application debugging engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
