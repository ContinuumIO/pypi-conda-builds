package:
  name: basket-client
  version: "0.3.11"

source:
  fn: basket-client-0.3.11.tar.gz
  url: https://pypi.python.org/packages/source/b/basket-client/basket-client-0.3.11.tar.gz
  md5: 78f0692a8174bc2e0d0153628c2db991
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basket-client = basket-client:main
    #
    # Would create an entry point called basket-client that calls basket-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - basket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/basket-client
  license: BSD License
  summary: "A Python client for Mozilla's basket service."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
