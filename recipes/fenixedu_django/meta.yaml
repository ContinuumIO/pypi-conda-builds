package:
  name: fenixedu_django
  version: "1.0.0"

source:
  fn: fenixedu_django-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fenixedu_django/fenixedu_django-1.0.0.tar.gz
  md5: 6369f84e2a145830dff2e44e80613263
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fenixedu_django = fenixedu_django:main
    #
    # Would create an entry point called fenixedu_django that calls fenixedu_django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fenixedu.authentication
    - fenixedu_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/samfcmc/fenixedu_django
  license: UNKNOWN
  summary: 'FenixEdu Django utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
