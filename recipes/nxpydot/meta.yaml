package:
  name: nxpydot
  version: "0.1"

source:
  fn: nxpydot-0.1.zip
  url: https://pypi.python.org/packages/source/n/nxpydot/nxpydot-0.1.zip
  md5: c51a42e55ac2b4c460bb2ead8b9533ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nxpydot = nxpydot:main
    #
    # Would create an entry point called nxpydot that calls nxpydot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pydotplus

  run:
    - python
    - pydotplus

test:
  # Python imports
  imports:
    - nxpydot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pfmoore/nxpydot
  license: MIT License
  summary: 'Wrapper round pydotplus to make it work with NetworkX'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
