package:
  name: collective.dancingcustomtemplates
  version: "1.0.0"

source:
  fn: collective.dancingcustomtemplates-1.0.0.zip
  url: https://pypi.python.org/packages/source/c/collective.dancingcustomtemplates/collective.dancingcustomtemplates-1.0.0.zip
  md5: 06c75386e31afc1c8b2089c942af0942
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dancingcustomtemplates = collective.dancingcustomtemplates:main
    #
    # Would create an entry point called collective.dancingcustomtemplates that calls collective.dancingcustomtemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - collective.dancing

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - collective.dancing

test:
  # Python imports
  imports:
    - collective
    - collective.dancingcustomtemplates
    - collective.dancingcustomtemplates.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.dancingcustomtemplates
  license: gpl
  summary: 'A product that customize some templates for S&D'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
