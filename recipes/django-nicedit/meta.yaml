package:
  name: django-nicedit
  version: "0.23"

source:
  fn: django-nicedit-0.23.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nicedit/django-nicedit-0.23.tar.gz
  md5: 5acd8c663662e8544e9c5723863b996f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nicedit = django-nicedit:main
    #
    # Would create an entry point called django-nicedit that calls django-nicedit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - pillow

  run:
    - python
    - django
    - south
    - pillow

test:
  # Python imports
  imports:
    - nicedit
    - nicedit.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nanvel/django-nicedit
  license: The MIT License
  summary: 'Nicedit widget for django with image upload feature.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
