package:
  name: jsbridge
  version: "3.0.3"

source:
  fn: jsbridge-3.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jsbridge/jsbridge-3.0.3.tar.gz
  md5: f85a4b62c0c7bd85f489835846df095a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsbridge = jsbridge:main
    #
    # Would create an entry point called jsbridge that calls jsbridge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsbridge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/mozmill/tree/master/jsbridge
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Python to JavaScript bridge interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
