package:
  name: integer-tuple-generator
  version: "0.0.2"

source:
  fn: Integer-Tuple-Generator-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/I/Integer-Tuple-Generator/Integer-Tuple-Generator-0.0.2.tar.gz
  md5: d4f829c134dd5a08d44c358c00862582
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - integer-tuple-generator = integer-tuple-generator:main
    #
    # Would create an entry point called integer-tuple-generator that calls integer-tuple-generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plumdog/python-integer-tuple-generator/
  license: UNKNOWN
  summary: 'Generation of tuples of integers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
