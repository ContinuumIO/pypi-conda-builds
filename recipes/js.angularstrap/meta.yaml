package:
  name: js.angularstrap
  version: "2.1.6"

source:
  fn: js.angularstrap-2.1.6.zip
  url: https://pypi.python.org/packages/source/j/js.angularstrap/js.angularstrap-2.1.6.zip
  md5: 1ed08b319c71fe208dbc2923cd8964df
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.angularstrap = js.angularstrap:main
    #
    # Would create an entry point called js.angularstrap that calls js.angularstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.angular

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.angular

test:
  # Python imports
  imports:
    - js
    - js.angularstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of AngularStrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
