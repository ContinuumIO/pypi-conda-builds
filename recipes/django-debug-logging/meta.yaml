package:
  name: django-debug-logging
  version: "0.4"

source:
  fn: django-debug-logging-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-logging/django-debug-logging-0.4.tar.gz
  md5: 3b4de124a66a20dcfb55bf155a4ad963
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-logging = django-debug-logging:main
    #
    # Would create an entry point called django-debug-logging that calls django-debug-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-debug-toolbar
    - django-picklefield

  run:
    - python
    - django-debug-toolbar
    - django-picklefield

test:
  # Python imports
  imports:
    - debug_logging
    - debug_logging.management
    - debug_logging.management.commands
    - debug_logging.migrations
    - debug_logging.panels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lincolnloop.github.com/django-debug-logging/
  license: BSD License
  summary: 'A plugin for django_debug_toolbar that logs results to the database for aggregated review.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
