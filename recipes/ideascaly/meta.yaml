package:
  name: ideascaly
  version: "0.5"

source:
  fn: ideascaly-0.5.tar.gz
  url: https://pypi.python.org/packages/source/i/ideascaly/ideascaly-0.5.tar.gz
  md5: 8c2368b635222d3833078dbb5cfac81e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ideascaly = ideascaly:main
    #
    # Would create an entry point called ideascaly that calls ideascaly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - configparser ==3.5.0b2
    - python-dateutil ==2.4.2
    - requests ==2.6.0
    - six ==1.9.0

  run:
    - python
    - configparser ==3.5.0b2
    - python-dateutil ==2.4.2
    - requests ==2.6.0
    - six ==1.9.0

test:
  # Python imports
  imports:
    - examples
    - ideascaly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joausaga/ideascaly
  license: MIT License
  summary: 'IdeaScale RESTful API Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
