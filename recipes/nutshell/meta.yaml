package:
  name: nutshell
  version: "0.1.2"

source:
  fn: nutshell-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nutshell/nutshell-0.1.2.tar.gz
  md5: 540c939ea122f3c9bc39111d73cc6704
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nutshell = nutshell:main
    #
    # Would create an entry point called nutshell that calls nutshell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - nutshell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EmilStenstrom/python-nutshell
  license: UNKNOWN
  summary: 'A minimal python library to access Nutshell CRM:s JSON-RPC API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
