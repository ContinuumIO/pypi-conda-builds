package:
  name: zapian
  version: "0.4.0"

source:
  fn: zapian-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zapian/zapian-0.4.0.tar.gz
  md5: 643bd33c242027a578135c86a016d8a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zapian = zapian:main
    #
    # Would create an entry point called zapian that calls zapian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - setuptools
    - pyyaml

test:
  # Python imports
  imports:
    - zapian

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/everydo/zapian
  license: Private
  summary: 'indexer addons: sort, cjksplitter, and so on'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
