package:
  name: collective.datehacker
  version: "0.1.0a"

source:
  fn: collective.datehacker-0.1.0a.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.datehacker/collective.datehacker-0.1.0a.tar.gz
  md5: 542305d25ddb0a092edcd223aa1531f8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.datehacker = collective.datehacker:main
    #
    # Would create an entry point called collective.datehacker that calls collective.datehacker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.datehacker
    - collective.datehacker.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.datehacker
  license: GPL
  summary: 'Add to Plone a view for changing (cheating!) creation and last modification date'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
