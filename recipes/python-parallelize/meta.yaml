package:
  name: python-parallelize
  version: "1.0.0.0"

source:
  fn: python-parallelize-1.0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-parallelize/python-parallelize-1.0.0.0.tar.gz
  md5: d2bcb9ff8611233488a7e353243154c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-parallelize = python-parallelize:main
    #
    # Would create an entry point called python-parallelize that calls python-parallelize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/npryce/python-parallelize
  license: Apache Software License
  summary: 'Make the for loop run in parallel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
