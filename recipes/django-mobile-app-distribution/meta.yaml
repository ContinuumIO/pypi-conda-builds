package:
  name: django-mobile-app-distribution
  version: "0.3.5"

source:
  fn: django-mobile-app-distribution-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mobile-app-distribution/django-mobile-app-distribution-0.3.5.tar.gz
  md5: 2a3c25903b875de9eb231d83186f6c66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobile-app-distribution = django-mobile-app-distribution:main
    #
    # Would create an entry point called django-mobile-app-distribution that calls django-mobile-app-distribution.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south ==1.0.2

  run:
    - python
    - south ==1.0.2

test:
  # Python imports
  imports:
    - django_mobile_app_distribution
    - django_mobile_app_distribution.migrations
    - django_mobile_app_distribution.south_migrations
    - django_mobile_app_distribution.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Alp-Phone/django-mobile-app-distribution
  license: MIT License
  summary: 'A Django app that adds iOS and Android app upload functionality to the Django admin interface.  Provides a mobile optimized HTML fronted for clients to download Ad Hoc mobile applications using their iOS or Android devices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
