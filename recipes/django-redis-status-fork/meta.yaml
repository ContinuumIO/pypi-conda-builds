package:
  name: django-redis-status-fork
  version: "1.0.6.1"

source:
  fn: django-redis-status-fork-1.0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis-status-fork/django-redis-status-fork-1.0.6.1.tar.gz
  md5: ad90bb06354f1dfbed5a9571fb9df174
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis-status-fork = django-redis-status-fork:main
    #
    # Would create an entry point called django-redis-status-fork that calls django-redis-status-fork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_status
    - redis_status.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/erussell/django-redis-status
  license: BSD License
  summary: 'A django application that displays the load and some other statistics about your redis cache instances in the admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
