package:
  name: django-stored-queryset
  version: "0.0.2"

source:
  fn: django-stored-queryset-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stored-queryset/django-stored-queryset-0.0.2.tar.gz
  md5: 8edcaa52990239682fa773d9f39ca429
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stored-queryset = django-stored-queryset:main
    #
    # Would create an entry point called django-stored-queryset that calls django-stored-queryset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - stored
    - test
    - test.stored

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'pickleable Django QuerySet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
