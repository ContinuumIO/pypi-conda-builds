package:
  name: collective.portlet.fullview
  version: "1.0"

source:
  fn: collective.portlet.fullview-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.fullview/collective.portlet.fullview-1.0.tar.gz
  md5: 3bb569a9c4bff8fcca3bae0cd0de1d25
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.fullview = collective.portlet.fullview:main
    #
    # Would create an entry point called collective.portlet.fullview that calls collective.portlet.fullview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfplone

  run:
    - python
    - setuptools
    - products.cmfplone

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.fullview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.portlet.fullview
  license: GPL
  summary: 'Displays a content item as full view'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
