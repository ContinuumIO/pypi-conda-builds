package:
  name: flask-influxdb
  version: "0.1"

source:
  fn: Flask-InfluxDB-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-InfluxDB/Flask-InfluxDB-0.1.tar.gz
  md5: 4f9dc1f00d8ff655dc5a7660f45f65b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-influxdb = flask-influxdb:main
    #
    # Would create an entry point called flask-influxdb that calls flask-influxdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - influxdb

  run:
    - python
    - flask
    - influxdb

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ombitron/flask-influxdb
  license: BSD License
  summary: 'Flask bindings for the InfluxDB time series database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
