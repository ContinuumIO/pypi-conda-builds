package:
  name: goto-dir
  version: "0.2.0"

source:
  fn: goto-dir-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/goto-dir/goto-dir-0.2.0.tar.gz
  md5: 0f4e6aab19719471b96f399d4bb9cd29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goto-dir = goto-dir:main
    #
    # Would create an entry point called goto-dir that calls goto-dir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - goto
    - goto.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pauloborges/goto
  license: GNU General Public License v3 (GPLv3)
  summary: "easy'n'fast cd'ing."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
