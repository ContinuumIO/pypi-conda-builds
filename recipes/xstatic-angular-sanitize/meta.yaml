package:
  name: xstatic-angular-sanitize
  version: "1.2.1.1"

source:
  fn: XStatic-Angular-Sanitize-1.2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Angular-Sanitize/XStatic-Angular-Sanitize-1.2.1.1.tar.gz
  md5: 322929c54d7e28882095136049c540ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-angular-sanitize = xstatic-angular-sanitize:main
    #
    # Would create an entry point called xstatic-angular-sanitize that calls xstatic-angular-sanitize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.angular_sanitize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://angularjs.org
  license: (same as Angular-Sanitize)
  summary: 'Angular-Sanitize 1.2.1 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
