package:
  name: django-blogging
  version: "1.1"

source:
  fn: django-blogging-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-blogging/django-blogging-1.1.tar.gz
  md5: 56f8f09b086ff34921f69fd59339b82c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-blogging = django-blogging:main
    #
    # Would create an entry point called django-blogging that calls django-blogging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - blogging
    - blogging.templatetags
    - blogging.urls
    - tests
    - tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jibaku/blogging
  license: BSD License
  summary: 'Django blogging app. The goal is to keep it simple and configurable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
