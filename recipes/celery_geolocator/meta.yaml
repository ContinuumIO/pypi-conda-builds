package:
  name: celery_geolocator
  version: "0.0.11"

source:
  fn: celery_geolocator-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/c/celery_geolocator/celery_geolocator-0.0.11.tar.gz
  md5: 0b658623ec0ca04125c54322c92656fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celery_geolocator = celery_geolocator:main
    #
    # Would create an entry point called celery_geolocator that calls celery_geolocator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=3.1.11
    - geopy >=0.99

  run:
    - python
    - celery >=3.1.11
    - geopy >=0.99

test:
  # Python imports
  imports:
    - celery_geolocator
    - celery_geolocator.helpers
    - examples
    - examples.django_celery
    - examples.django_celery.demoapp
    - examples.django_celery.proj
    - examples.strait_celery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/brentpayne/celery-geolocator
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Celery Geolocator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
