package:
  name: sharescanner
  version: "0.1.0"

source:
  fn: Sharescanner-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/S/Sharescanner/Sharescanner-0.1.0.tar.gz
  md5: c61d1f7474cd9b8aa61343e28754af7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sharescanner = sharescanner:main
    #
    # Would create an entry point called sharescanner that calls sharescanner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - smbc >=1.0.6
    - gi >=0.0.0

  run:
    - python
    - smbc >=1.0.6
    - gi >=0.0.0

test:
  # Python imports
  imports:
    - sharescanner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/share-scanner/
  license: GNU General Public License (GPL)
  summary: 'A GTK tool for scanning network shares and downloading files from them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
