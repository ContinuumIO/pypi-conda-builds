package:
  name: isotopic-logging
  version: "1.0.0"

source:
  fn: isotopic-logging-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/isotopic-logging/isotopic-logging-1.0.0.tar.gz
  md5: 40ace9821c06617b31a61694b0eb4b00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotopic-logging = isotopic-logging:main
    #
    # Would create an entry point called isotopic-logging that calls isotopic-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - isotopic_logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oblalex/isotopic-logging
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Mark and trace events in your log alike isotopic labeling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
