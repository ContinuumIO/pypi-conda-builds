package:
  name: collective.recipe.logger
  version: "0.1.0b1"

source:
  fn: collective.recipe.logger-0.1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.logger/collective.recipe.logger-0.1.0b1.zip
  md5: b92ca9d29c6ef8c0d721ed3fc1b1b455
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.logger = collective.recipe.logger:main
    #
    # Would create an entry point called collective.recipe.logger that calls collective.recipe.logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.logger
    - collective.recipe.logger.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/potar/collective.recipe.logger
  license: GNU General Public License (GPL)
  summary: "This recipe logs an information into storage. It's a part of https://github.com/potar/dagger"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
