package:
  name: hummus
  version: "0.3.17"

source:
  fn: hummus-0.3.17.tar.gz
  url: https://pypi.python.org/packages/source/h/hummus/hummus-0.3.17.tar.gz
  md5: 53b55087f36c11b4dd5f24e473064192
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hummus = hummus:main
    #
    # Would create an entry point called hummus that calls hummus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - wand

  run:
    - python
    - six
    - wand

test:
  # Python imports
  imports:
    - hummus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/concordusapps/python-hummus
  license: MIT License
  summary: 'Pythonic API for hummus, the fast and free PDF reader / writer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
