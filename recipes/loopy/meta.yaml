package:
  name: loopy
  version: "0.1.0-3"

source:
  fn: Loopy-0.1.0-3.tar.gz
  url: https://pypi.python.org/packages/source/L/Loopy/Loopy-0.1.0-3.tar.gz
  md5: 74b3bad6905502e0445d3d49165a0d46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loopy = loopy:main
    #
    # Would create an entry point called loopy that calls loopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - multitask >=0.1.0

  run:
    - python
    - multitask >=0.1.0

test:
  # Python imports
  imports:
    - loopy
    - loopy.libevent
    - loopy.misc
    - loopy.py_multitask
    - loopy.stdsched

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Generic Event Loop Interface + Example Implementations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
