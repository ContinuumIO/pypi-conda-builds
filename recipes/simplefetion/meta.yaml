package:
  name: simplefetion
  version: "0.0.5"

source:
  fn: simplefetion-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/s/simplefetion/simplefetion-0.0.5.tar.gz
  md5: 1af65efc75d80ce53e84bd297c554ce0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplefetion = simplefetion:main
    #
    # Would create an entry point called simplefetion that calls simplefetion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

test:
  # Python imports
  imports:
    - simplefetion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://djangofetion.sinaapp.com/
  license: Apache Software License
  summary: 'Simple Fetion: send message / group messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
