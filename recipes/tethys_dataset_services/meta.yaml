package:
  name: tethys_dataset_services
  version: "1.0.3"

source:
  fn: tethys_dataset_services-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tethys_dataset_services/tethys_dataset_services-1.0.3.tar.gz
  md5: d50e683080c20f66aaa3dd161669dd40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tethys_dataset_services = tethys_dataset_services:main
    #
    # Would create an entry point called tethys_dataset_services that calls tethys_dataset_services.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - owslib
    - gsconfig
    - requests
    - requests_toolbelt

  run:
    - python
    - django
    - owslib
    - gsconfig
    - requests
    - requests_toolbelt

test:
  # Python imports
  imports:
    - tethys_dataset_services
    - tethys_dataset_services.base
    - tethys_dataset_services.engines
    - tethys_dataset_services.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'A generic Python interface for dataset services such as CKAN and HydroShare'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
