package:
  name: zdq_test
  version: "0.0.1"

source:
  fn: zdq_test-0.0.1.zip
  url: https://pypi.python.org/packages/source/z/zdq_test/zdq_test-0.0.1.zip
  md5: 72a8eda545f743ab8f60bc4b6b266476
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zdq_test = zdq_test:main
    #
    # Would create an entry point called zdq_test that calls zdq_test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zdq_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'a python json serialize/deserialize library which is easy to use'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
