package:
  name: udplog
  version: "0.1.1"

source:
  fn: udplog-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/u/udplog/udplog-0.1.1.tar.gz
  md5: 0e7d4b18fb21f5352b09faa4d451a087
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - udplog = udplog:main
    #
    # Would create an entry point called udplog that calls udplog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - twisted >=13.0.0
    - txamqp
    - txredis
    - thrift
    - scribe

  run:
    - python
    - simplejson
    - twisted >=13.0.0
    - txamqp
    - txredis
    - thrift
    - scribe

test:
  # Python imports
  imports:
    - twisted.plugins
    - udplog
    - udplog.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mochi/udplog
  license: MIT License
  summary: 'UDPLog is a system for emitting application log events via UDP and shipping them via RabbitMQ or Scribe for further processing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
