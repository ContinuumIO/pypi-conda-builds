package:
  name: django-glaze
  version: "0.1"

source:
  fn: django-glaze-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-glaze/django-glaze-0.1.tar.gz
  md5: e7454bfebeb1a97d1f18f08b67d29628
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-glaze = django-glaze:main
    #
    # Would create an entry point called django-glaze that calls django-glaze.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - utile >=0.3

  run:
    - python
    - utile >=0.3

test:
  # Python imports
  imports:
    - glaze
    - glaze.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marwano/django-glaze
  license: BSD License
  summary: 'Adding extra functionality to Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
