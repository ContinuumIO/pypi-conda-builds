package:
  name: django-shop-multiplecurrencies
  version: "1.0.0"

source:
  fn: django-shop-multiplecurrencies-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-multiplecurrencies/django-shop-multiplecurrencies-1.0.0.tar.gz
  md5: 9bcaaa7b802d3fb5ba525383b4316dc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-multiplecurrencies = django-shop-multiplecurrencies:main
    #
    # Would create an entry point called django-shop-multiplecurrencies that calls django-shop-multiplecurrencies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-shop >=0.0.9

  run:
    - python
    - django >=1.2
    - django-shop >=0.0.9

test:
  # Python imports
  imports:
    - shop_multiplecurrencies
    - shop_multiplecurrencies.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.django-shop.org/
  license: BSD License
  summary: 'Multiple currencies for Django Shop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
