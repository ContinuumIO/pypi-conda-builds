package:
  name: flask-and-redis
  version: "0.6"

source:
  fn: Flask-And-Redis-0.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-And-Redis/Flask-And-Redis-0.6.tar.gz
  md5: c5d6de1054b41c3907a8fe8c689ae9ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-and-redis = flask-and-redis:main
    #
    # Would create an entry point called flask-and-redis that calls flask-and-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - redis >=2.4.11

  run:
    - python
    - flask >=0.8
    - redis >=2.4.11

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/playpauseandstop/Flask-And-Redis
  license: BSD License
  summary: 'Simple as dead support of Redis database for Flask apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
