package:
  name: meerkat
  version: "0.2.3"

source:
  fn: meerkat-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/m/meerkat/meerkat-0.2.3.tar.gz
  md5: e6c910e656c66e4f77b2540f890d5d9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meerkat = meerkat:main
    #
    # Would create an entry point called meerkat that calls meerkat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabio
    - h5py
    - numpy

  run:
    - python
    - fabio
    - h5py
    - numpy

test:
  # Python imports
  imports:
    - meerkat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aglie/meerkat.git
  license: UNKNOWN
  summary: 'A program for reciprocal space reconstruction'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
