package:
  name: iterextra
  version: "0.1.1"

source:
  fn: iterextra-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iterextra/iterextra-0.1.1.tar.gz
  md5: a8865f817d6ca71e7098b7f7ade4a4d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iterextra = iterextra:main
    #
    # Would create an entry point called iterextra that calls iterextra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - nose

test:
  # Python imports
  imports:
    - iterextra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamkal/iterextra
  license: UNKNOWN
  summary: 'An extension to Python standard library itertools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
