package:
  name: inqbus.zopeftp
  version: "1.0.1"

source:
  fn: inqbus.zopeftp-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/inqbus.zopeftp/inqbus.zopeftp-1.0.1.tar.gz
  md5: c94254fbee62c17dcb302e1c2313be73
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inqbus.zopeftp = inqbus.zopeftp:main
    #
    # Would create an entry point called inqbus.zopeftp that calls inqbus.zopeftp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - inqbus
    - inqbus.zopeftp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License (GPL)
  summary: 'Add a config parameter to zope.conf which allow one to configure a start path for ftp users, like the path to a plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
