package:
  name: bugsnag
  version: "2.3.1"

source:
  fn: bugsnag-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bugsnag/bugsnag-2.3.1.tar.gz
  md5: a3f1d7d75aaebe349905c16ed203fe84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bugsnag = bugsnag:main
    #
    # Would create an entry point called bugsnag that calls bugsnag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob

  run:
    - python
    - webob

test:
  # Python imports
  imports:
    - bugsnag
    - bugsnag.celery
    - bugsnag.django
    - bugsnag.flask
    - bugsnag.tornado
    - bugsnag.wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bugsnag.com/
  license: MIT License
  summary: 'Automatic error monitoring for django, flask, etc. (https://bugsnag.com).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
