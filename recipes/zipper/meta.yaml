package:
  name: zipper
  version: "0.0.3"

source:
  fn: zipper-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zipper/zipper-0.0.3.tar.gz
  md5: f795adeed0fc6efb57343881e303aabd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zipper = zipper:main
    #
    # Would create an entry point called zipper that calls zipper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zipper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/trivio/zipper
  license: MIT License
  summary: 'Functional hierarchical zipper, with navigation, editing, and enumeration.  See Huet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
