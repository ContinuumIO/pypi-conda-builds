package:
  name: django-cms-themes
  version: "1.0.11"

source:
  fn: django-cms-themes-1.0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-themes/django-cms-themes-1.0.11.tar.gz
  md5: b21437b9d1c0a03e94fb24bcb3153efb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-themes = django-cms-themes:main
    #
    # Would create an entry point called django-cms-themes that calls django-cms-themes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-cms

  run:
    - python
    - setuptools
    - django
    - django-cms

test:
  # Python imports
  imports:
    - cms_themes
    - cms_themes.migrations
    - demo
    - demo.cms_themes
    - demo.cms_themes.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/megamark16/django-cms-themes
  license: BSD License
  summary: 'Load prepackaged themes (templates and accompanying media) into Django CMS projects through the admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
