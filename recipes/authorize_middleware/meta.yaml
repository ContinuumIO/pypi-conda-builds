package:
  name: authorize_middleware
  version: "0.1"

source:
  fn: authorize_middleware-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/authorize_middleware/authorize_middleware-0.1.tar.gz
  md5: 9b9877a7d6798912fa604858d33314af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authorize_middleware = authorize_middleware:main
    #
    # Would create an entry point called authorize_middleware that calls authorize_middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paste >=1.4
    - decorator >=2.1.0

  run:
    - python
    - paste >=1.4
    - decorator >=2.1.0

test:
  # Python imports
  imports:
    - authorize_middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.sandbox.lt/authorize-middleware
  license: MIT
  summary: 'An authorization middleware for WSGI applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
