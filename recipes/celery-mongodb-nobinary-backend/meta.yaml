package:
  name: celery-mongodb-nobinary-backend
  version: "0.0.10"

source:
  fn: celery-mongodb-nobinary-backend-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/c/celery-mongodb-nobinary-backend/celery-mongodb-nobinary-backend-0.0.10.tar.gz
  md5: aa079375bf877c5a449129ab5f103268
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celery-mongodb-nobinary-backend = celery-mongodb-nobinary-backend:main
    #
    # Would create an entry point called celery-mongodb-nobinary-backend that calls celery-mongodb-nobinary-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery

  run:
    - python
    - setuptools
    - celery

test:
  # Python imports
  imports:
    - zcelerybackend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License, Version 2.0
  summary: 'Celery backend for MongoDB that does not convert task results into a binary format before storing them in Mongo database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
