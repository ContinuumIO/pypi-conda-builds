package:
  name: zmqfirewall
  version: "0.1dev"

source:
  fn: zmqfirewall-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/z/zmqfirewall/zmqfirewall-0.1dev.tar.gz
  md5: ae15f07cdd28aab89e7bf1d28828cc0e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zmqfirewall = zmqfirewall:main
    #
    # Would create an entry point called zmqfirewall that calls zmqfirewall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - txzmq >=0.5.0
    - twisted >=10.0

  run:
    - python
    - setuptools
    - txzmq >=0.5.0
    - twisted >=10.0

test:
  # Python imports
  imports:
    - zmqfirewall
    - zmqfirewall.actions
    - zmqfirewall.core
    - zmqfirewall.filters
    - zmqfirewall.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gregjurman/zmqfirewall
  license: MIT
  summary: 'Object-based ZeroMQ message repeater/firewall'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
