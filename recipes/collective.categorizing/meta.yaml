package:
  name: collective.categorizing
  version: "0.2.3"

source:
  fn: collective.categorizing-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.categorizing/collective.categorizing-0.2.3.tar.gz
  md5: b6fff77c0007a5f02df6267617faf7f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.categorizing = collective.categorizing:main
    #
    # Would create an entry point called collective.categorizing that calls collective.categorizing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.categorizing
    - collective.categorizing.adapter
    - collective.categorizing.browser
    - collective.categorizing.content
    - collective.categorizing.interfaces
    - collective.categorizing.portlets
    - collective.categorizing.subscriber
    - collective.categorizing.tests
    - collective.categorizing.utility

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.categorizing
  license: GPL
  summary: 'collective.categorizing aims to add hierarchical categorizing to Plone with Category content type.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
