package:
  name: tinyrpc-amqp
  version: "0.1dev"

source:
  fn: tinyrpc-amqp-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/t/tinyrpc-amqp/tinyrpc-amqp-0.1dev.tar.gz
  md5: 81f8c1228b680cf831aa26ebb72dcc3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tinyrpc-amqp = tinyrpc-amqp:main
    #
    # Would create an entry point called tinyrpc-amqp that calls tinyrpc-amqp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tinyrpc
    - pika >=0.9.14

  run:
    - python
    - tinyrpc
    - pika >=0.9.14

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/whippiii/tinyrpc-amqp
  license: BSD
  summary: 'AMQP transport implementation for tinyrpc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
