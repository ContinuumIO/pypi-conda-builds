package:
  name: django-flag
  version: "0.1.1"

source:
  fn: django-flag-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flag/django-flag-0.1.1.tar.gz
  md5: 2774b9ae6c2940dc6030d1bbf16aa6dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flag = django-flag:main
    #
    # Would create an entry point called django-flag that calls django-flag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flag
    - flag.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-flag/
  license: MIT License
  summary: 'flagging of inapproriate/spam content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
