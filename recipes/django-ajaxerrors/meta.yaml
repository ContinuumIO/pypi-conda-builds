package:
  name: django-ajaxerrors
  version: "1.1"

source:
  fn: django-ajaxerrors-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajaxerrors/django-ajaxerrors-1.1.tar.gz
  md5: 6b6d184345c1accbf56b65e44c8130e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajaxerrors = django-ajaxerrors:main
    #
    # Would create an entry point called django-ajaxerrors that calls django-ajaxerrors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ajaxerrors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yaniv-aknin/django-ajaxerrors
  license: MIT
  summary: 'Django middleware for unhandled AJAX errors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
