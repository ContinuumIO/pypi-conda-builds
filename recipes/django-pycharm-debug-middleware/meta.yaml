package:
  name: django-pycharm-debug-middleware
  version: "1.0.2"

source:
  fn: django-pycharm-debug-middleware-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pycharm-debug-middleware/django-pycharm-debug-middleware-1.0.2.tar.gz
  md5: 512ed28aa46821539881d5ecb078955f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pycharm-debug-middleware = django-pycharm-debug-middleware:main
    #
    # Would create an entry point called django-pycharm-debug-middleware that calls django-pycharm-debug-middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - pycharm_debug_middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gabejackson/django-pycharm-debug-middleware
  license: BSD License
  summary: "A django middleware to debug exceptions in PyCharm's PyDev debugger"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
