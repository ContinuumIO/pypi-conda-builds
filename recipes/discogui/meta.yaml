package:
  name: discogui
  version: "0.0.2"

source:
  fn: discogui-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/discogui/discogui-0.0.2.tar.gz
  md5: 5288aa924f35624af69d27b793589cde
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - discogui = discogui:main
    #
    # Would create an entry point called discogui that calls discogui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - path.py
    - pymouse
    - pyscreenshot
    - easyprocess
    - pyvirtualdisplay

  run:
    - python
    - path.py
    - pymouse
    - pyscreenshot
    - easyprocess
    - pyvirtualdisplay

test:
  # Python imports
  imports:
    - discogui
    - discogui.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ponty/discogui
  license: BSD License
  summary: 'GUI discovery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
