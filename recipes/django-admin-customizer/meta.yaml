package:
  name: django-admin-customizer
  version: "0.2"

source:
  fn: django-admin-customizer-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-customizer/django-admin-customizer-0.2.tar.gz
  md5: 1bcc0bf7c7b0b2f4944f7ce479d87176
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-customizer = django-admin-customizer:main
    #
    # Would create an entry point called django-admin-customizer that calls django-admin-customizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - admin_customizer
    - admin_customizer.management
    - admin_customizer.management.commands
    - admin_customizer.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionelmc/django-admin-customizer
  license: BSD License
  summary: 'Django admin customizing interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
