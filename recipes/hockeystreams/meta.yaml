package:
  name: hockeystreams
  version: "0.2.4"

source:
  fn: hockeystreams-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hockeystreams/hockeystreams-0.2.4.tar.gz
  md5: 9d6bbd57bc3f1a81dff708c36e95966c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hockeystreams = hockeystreams:main
    #
    # Would create an entry point called hockeystreams that calls hockeystreams.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hockeystreams

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cemeyer2/HockeyStreamsAPI
  license: GNU General Public License (GPL)
  summary: 'Python wrapper for the hockeystreams.com REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
