package:
  name: fudge-je
  version: "1.0.3"

source:
  fn: fudge-je-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fudge-je/fudge-je-1.0.3.tar.gz
  md5: 91b08d1da2239915430d9d45f0923f66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fudge-je = fudge-je:main
    #
    # Would create an entry point called fudge-je that calls fudge-je.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fudge
    - fudge.tests
    - fudge.tests.support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://farmdev.com/projects/fudge/
  license: The MIT License
  summary: 'Replace real objects with fakes (mocks, stubs, etc) while testing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
