package:
  name: dyko
  version: "0.2"

source:
  fn: Dyko-0.2.tar.gz
  url: https://pypi.python.org/packages/source/D/Dyko/Dyko-0.2.tar.gz
  md5: 859f619331e486a33e0af0d82fb612a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dyko = dyko:main
    #
    # Would create an entry point called dyko that calls dyko.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dyko
    - kalamar
    - kalamar.access_point
    - kalamar.access_point.alchemy
    - kalamar.access_point.xml
    - kalamar.tests
    - kalamar.tests.access_point
    - kalamar.tests.combinations
    - kalamar.tests.combinations.access_point
    - kraken
    - kraken.template
    - kraken.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dyko.org/
  license: GNU General Public License (GPL)
  summary: 'A Lightweight Web Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
