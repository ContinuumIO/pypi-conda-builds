package:
  name: django-under-maintenance
  version: "0.1"

source:
  fn: django-under-maintenance-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-under-maintenance/django-under-maintenance-0.1.tar.gz
  md5: cf7b25a071f1cf6419edd09ca118a196
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-under-maintenance = django-under-maintenance:main
    #
    # Would create an entry point called django-under-maintenance that calls django-under-maintenance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_maintenance
    - django_maintenance.management
    - django_maintenance.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Setting you site Under Maintenance.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
