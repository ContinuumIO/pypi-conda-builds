package:
  name: m01.fs
  version: "1.0.0"

source:
  fn: m01.fs-1.0.0.zip
  url: https://pypi.python.org/packages/source/m/m01.fs/m01.fs-1.0.0.zip
  md5: 38029080e322c9d9a4a90b7ca7eac480
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m01.fs = m01.fs:main
    #
    # Would create an entry point called m01.fs that calls m01.fs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bson
    - m01.mongo
    - pymongo
    - z3c.form
    - zope.component
    - zope.contenttype
    - zope.datetime
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.site

  run:
    - python
    - setuptools
    - bson
    - m01.mongo
    - pymongo
    - z3c.form
    - zope.component
    - zope.contenttype
    - zope.datetime
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.site

test:
  # Python imports
  imports:
    - m01
    - m01.fs
    - m01.fs.testing
    - m01.fs.widget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/m01.fs
  license: Zope Public License
  summary: 'MongoDB file implementation wihtout gridfs for Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
