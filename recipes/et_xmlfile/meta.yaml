package:
  name: et_xmlfile
  version: "1.0.0"

source:
  fn: et_xmlfile-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/e/et_xmlfile/et_xmlfile-1.0.0.tar.gz
  md5: ef9922412f828b4c762be64e64ec5070
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - et_xmlfile = et_xmlfile:main
    #
    # Would create an entry point called et_xmlfile that calls et_xmlfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - et_xmlfile
    - et_xmlfile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/openpyxl/et_xmlfile
  license: MIT License
  summary: 'An implementation of lxml.xmlfile for the standard library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
