package:
  name: huawei_b593_status
  version: "0.0.1"

source:
  fn: huawei_b593_status-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/huawei_b593_status/huawei_b593_status-0.0.1.tar.gz
  md5: b1714ba501e1d1e2baf638e42812fcf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - huawei_b593_status = huawei_b593_status:main
    #
    # Would create an entry point called huawei_b593_status that calls huawei_b593_status.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmltodict
    - httplib2

  run:
    - python
    - xmltodict
    - httplib2

test:
  # Python imports
  imports:
    - huawei_b593_status

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ojarva/python-huawei-b593-status
  license: BSD License
  summary: 'Huawei B593 status fetcher'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
