package:
  name: django-fs-livesettings
  version: "0.1"

source:
  fn: django-fs-livesettings-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fs-livesettings/django-fs-livesettings-0.1.tar.gz
  md5: 4566eecf18b5be5f6bb30101c6413e88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fs-livesettings = django-fs-livesettings:main
    #
    # Would create an entry point called django-fs-livesettings that calls django-fs-livesettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-picklefield

  run:
    - python
    - django-picklefield

test:
  # Python imports
  imports:
    - livesettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fogstream/django-fs-livesettings
  license: The MIT License (MIT)
  summary: 'The Django-related reusable app provides the ability to store settings in a database and configure settings via an admin interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
