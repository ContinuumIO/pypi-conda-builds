package:
  name: js.placeholders
  version: "3.0.2"

source:
  fn: js.placeholders-3.0.2.zip
  url: https://pypi.python.org/packages/source/j/js.placeholders/js.placeholders-3.0.2.zip
  md5: af2c2f5c5274bcd8a27a0c42165bf809
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.placeholders = js.placeholders:main
    #
    # Would create an entry point called js.placeholders that calls js.placeholders.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.placeholders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of Placeholders.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
