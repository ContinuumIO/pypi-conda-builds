package:
  name: monkeytime
  version: "0.2.0"

source:
  fn: monkeytime-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/monkeytime/monkeytime-0.2.0.tar.gz
  md5: c56d8373806251de60a30973a0a67132
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monkeytime = monkeytime:main
    #
    # Would create an entry point called monkeytime that calls monkeytime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monkeytime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.bitbucket.org/johannestaas/monkeytime
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Patch datetime for functions and increased performance of strptime'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
