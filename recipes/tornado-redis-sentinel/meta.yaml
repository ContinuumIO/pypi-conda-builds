package:
  name: tornado-redis-sentinel
  version: "0.1.1"

source:
  fn: tornado-redis-sentinel-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-redis-sentinel/tornado-redis-sentinel-0.1.1.tar.gz
  md5: 4f1c49cb3ffffb597b5d0dfefeecf7ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-redis-sentinel = tornado-redis-sentinel:main
    #
    # Would create an entry point called tornado-redis-sentinel that calls tornado-redis-sentinel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - toredis
    - six

  run:
    - python
    - toredis
    - six

test:
  # Python imports
  imports:
    - tests
    - tornado_redis_sentinel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: 'Tornado redis library based in toredis that supports sentinel connections.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
