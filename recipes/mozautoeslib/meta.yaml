package:
  name: mozautoeslib
  version: "0.1.2"

source:
  fn: mozautoeslib-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mozautoeslib/mozautoeslib-0.1.2.tar.gz
  md5: 88ef49341aae21fef8b6ccb438844f45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozautoeslib = mozautoeslib:main
    #
    # Would create an entry point called mozautoeslib that calls mozautoeslib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyes ==0.16

  run:
    - python
    - pyes ==0.16

test:
  # Python imports
  imports:
    - mozautoeslib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.mozilla.org/automation/mozautoeslib
  license: MPL
  summary: 'Convenience lib for making certain kinds of ES queries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
