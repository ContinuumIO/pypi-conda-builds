package:
  name: django-popularity
  version: "0.2"

source:
  fn: django-popularity-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-popularity/django-popularity-0.2.tar.gz
  md5: db09cd79f11f2c5403a584dff98b1290
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-popularity = django-popularity:main
    #
    # Would create an entry point called django-popularity that calls django-popularity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - popularity
    - popularity.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dokterbob/django-popularity
  license: GNU Affero General Public License v3
  summary: 'A generic view- and popularity tracking pluggable for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
