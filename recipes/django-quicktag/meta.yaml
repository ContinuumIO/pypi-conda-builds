package:
  name: django-quicktag
  version: "0.6.1"

source:
  fn: django-quicktag-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quicktag/django-quicktag-0.6.1.tar.gz
  md5: 84dac18e64ad18bee56900bc33db4151
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quicktag = django-quicktag:main
    #
    # Would create an entry point called django-quicktag that calls django-quicktag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - quicktag

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ricobl/django-quicktag/
  license: UNKNOWN
  summary: 'Easily create templatetags that takes args and kwargs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
