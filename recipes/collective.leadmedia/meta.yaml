package:
  name: collective.leadmedia
  version: "0.1"

source:
  fn: collective.leadmedia-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.leadmedia/collective.leadmedia-0.1.tar.gz
  md5: 2845ae9ec692b56b99eb477d64640144
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.leadmedia = collective.leadmedia:main
    #
    # Would create an entry point called collective.leadmedia that calls collective.leadmedia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.folderishtypes ==2.0b2
    - collective.slickslideshow ==0.0.6
    - collective.foldercontentsportletmanager
    - collective.quickupload

  run:
    - python
    - setuptools
    - setuptools
    - collective.folderishtypes ==2.0b2
    - collective.slickslideshow ==0.0.6
    - collective.foldercontentsportletmanager
    - collective.quickupload

test:
  # Python imports
  imports:
    - collective
    - collective.leadmedia
    - collective.leadmedia.behaviors
    - collective.leadmedia.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.leadmedia
  license: GPL
  summary: 'Adds a slideshow to any dexterity folderish type.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
