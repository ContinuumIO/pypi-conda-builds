package:
  name: host_pool
  version: "0.2"

source:
  fn: host_pool-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/host_pool/host_pool-0.2.tar.gz
  md5: fe5c0ccb182cde7d42d3987c4e56e871
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - host_pool = host_pool:main
    #
    # Would create an entry point called host_pool that calls host_pool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - host_pool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jehiah/host_pool
  license: MIT License
  summary: 'A generic pool to track a set of remote hosts with the ability to mark hosts down on failures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
