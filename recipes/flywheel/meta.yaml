package:
  name: flywheel
  version: "0.4.1"

source:
  fn: flywheel-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flywheel/flywheel-0.4.1.tar.gz
  md5: fb714782af4aa2a0645e3efa7648c7b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flywheel = flywheel:main
    #
    # Would create an entry point called flywheel that calls flywheel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dynamo3 >=0.3.2
    - six

  run:
    - python
    - dynamo3 >=0.3.2
    - six

test:
  # Python imports
  imports:
    - flywheel
    - flywheel.fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://flywheel.readthedocs.org/
  license: MIT License
  summary: "SQLAlchemy-style ORM for Amazon's DynamoDB"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
