package:
  name: collective.delegatesiteadmin
  version: "0.1.2"

source:
  fn: collective.delegatesiteadmin-0.1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.delegatesiteadmin/collective.delegatesiteadmin-0.1.2.zip
  md5: e3940e31bd0a211a12791c05409ac498
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.delegatesiteadmin = collective.delegatesiteadmin:main
    #
    # Would create an entry point called collective.delegatesiteadmin that calls collective.delegatesiteadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.delegatesiteadmin
    - collective.delegatesiteadmin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.delegatesiteadmin
  license: gpl
  summary: 'Plone : Delegation of Site Administrator role through sharing tab'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
