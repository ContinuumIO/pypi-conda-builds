package:
  name: langner
  version: "0.2"

source:
  fn: langner-0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/langner/langner-0.2.tar.gz
  md5: eaac30477cb45b54da638771481f2838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - langner = langner:main
    #
    # Would create an entry point called langner that calls langner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ply

  run:
    - python
    - ply

test:
  # Python imports
  imports:
    - langner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/romankierzkowski/langner
  license: MIT License
  summary: 'Langner - language for expressing behaviour strategies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
