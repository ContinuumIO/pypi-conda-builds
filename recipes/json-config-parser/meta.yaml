package:
  name: json-config-parser
  version: "0.1.2"

source:
  fn: json-config-parser-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/json-config-parser/json-config-parser-0.1.2.tar.gz
  md5: dcf1a30b7041ffe800b3e983b6824fd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-config-parser = json-config-parser:main
    #
    # Would create an entry point called json-config-parser that calls json-config-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bwhmather/json-config-parser/
  license: Python Software Foundation License or BSD License
  summary: 'A straightforward and unambiguous config file parser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
