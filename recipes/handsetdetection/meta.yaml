package:
  name: handsetdetection
  version: "1.1"

source:
  fn: handsetdetection-1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/handsetdetection/handsetdetection-1.1.tar.gz
  md5: c1fde585473a29c341434018d483de61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - handsetdetection = handsetdetection:main
    #
    # Would create an entry point called handsetdetection that calls handsetdetection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - handsetdetection

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/handset-detection-python-api-kit/
  license: MIT License
  summary: 'Handset Detection Python API kit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
