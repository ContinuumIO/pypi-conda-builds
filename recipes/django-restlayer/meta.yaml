package:
  name: django-restlayer
  version: "0.9.0"

source:
  fn: django-restlayer-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restlayer/django-restlayer-0.9.0.tar.gz
  md5: 54111be62d9fdbf7d61daaf370e22736
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restlayer = django-restlayer:main
    #
    # Would create an entry point called django-restlayer that calls django-restlayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.5,<1.7
    - python-mimeparse >=0.1.4

  run:
    - python
    - django >=1.5.5,<1.7
    - python-mimeparse >=0.1.4

test:
  # Python imports
  imports:
    - restlayer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/olivier-m/django-restlayer
  license: MIT License
  summary: 'HTTP REST toolkit for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
