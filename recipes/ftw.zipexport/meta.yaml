package:
  name: ftw.zipexport
  version: "1.3.0"

source:
  fn: ftw.zipexport-1.3.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.zipexport/ftw.zipexport-1.3.0.zip
  md5: 32b0a6dae66633cf85c977bd6468d443
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.zipexport = ftw.zipexport:main
    #
    # Would create an entry point called ftw.zipexport that calls ftw.zipexport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfcore
    - setuptools
    - ftw.upgrade

  run:
    - python
    - setuptools
    - products.cmfcore
    - setuptools
    - ftw.upgrade

test:
  # Python imports
  imports:
    - ftw
    - ftw.zipexport
    - ftw.zipexport.browser
    - ftw.zipexport.representations
    - ftw.zipexport.tests
    - ftw.zipexport.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.zipexport
  license: GPL2
  summary: 'Zip export for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
