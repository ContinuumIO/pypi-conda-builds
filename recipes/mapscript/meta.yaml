package:
  name: mapscript
  version: "5.6.3.0"

source:
  fn: mapscript-5.6.3.0.zip
  url: https://pypi.python.org/packages/source/m/mapscript/mapscript-5.6.3.0.zip
  md5: 6764e8216204ba6c48bcafe2c5bbea6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mapscript = mapscript:main
    #
    # Would create an entry point called mapscript that calls mapscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6c9

  run:
    - python
    - setuptools >=0.6c9

test:
  # Python imports
  imports:
    - mapscript

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mapserver.org/
  license: UNKNOWN
  summary: 'Python interface to MapServer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
