package:
  name: billy-client
  version: "0.0.8"

source:
  fn: billy-client-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/b/billy-client/billy-client-0.0.8.tar.gz
  md5: 9008c1e9e7bcaeeb0e6dffededd8d1fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - billy-client = billy-client:main
    #
    # Would create an entry point called billy-client that calls billy-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - billy_client
    - billy_client.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/victorlin/billy-client
  license: UNKNOWN
  summary: 'Recurring payment system client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
