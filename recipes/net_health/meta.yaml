package:
  name: net_health
  version: "0.13"

source:
  fn: net_health-0.13.tar.gz
  url: https://pypi.python.org/packages/source/n/net_health/net_health-0.13.tar.gz
  md5: ebb2445009c2e0f4615ac75a165ec02b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - net_health = net_health:main
    #
    # Would create an entry point called net_health that calls net_health.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-neutronclient
    - python-novaclient

  run:
    - python
    - setuptools
    - python-neutronclient
    - python-novaclient

test:
  # Python imports
  imports:
    - net_health

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'check network connectivity'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
