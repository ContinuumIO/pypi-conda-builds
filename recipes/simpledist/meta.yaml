package:
  name: simpledist
  version: "0.1.12"

source:
  fn: simpledist-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/s/simpledist/simpledist-0.1.12.tar.gz
  md5: c0ecdf956847bb6cab2479f4d7cce4c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpledist = simpledist:main
    #
    # Would create an entry point called simpledist that calls simpledist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plotutils
    - pandas >=0.14

  run:
    - python
    - plotutils
    - pandas >=0.14

test:
  # Python imports
  imports:
    - simpledist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timothydmorton/simpledist
  license: UNKNOWN
  summary: 'Defines objects useful for describing simple probability distributions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
