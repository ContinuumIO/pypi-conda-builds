package:
  name: djbehave
  version: "0.0.1.0"

source:
  fn: djbehave-0.0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djbehave/djbehave-0.0.1.0.tar.gz
  md5: 73c47b166ced434d440e2d0bef00fdad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djbehave = djbehave:main
    #
    # Would create an entry point called djbehave that calls djbehave.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.1
    - subbehave ==0.0.1.0
    - behave >=1.2.3

  run:
    - python
    - django >=1.4.1
    - subbehave ==0.0.1.0
    - behave >=1.2.3

test:
  # Python imports
  imports:
    - djbehave
    - djbehave.management
    - djbehave.management.commands
    - djbehave.server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/popham/djbehave
  license: MIT License
  summary: "Integration of Behave into Django's command line interface."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
