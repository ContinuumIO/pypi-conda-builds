package:
  name: django-timezone-field
  version: "1.2"

source:
  fn: django-timezone-field-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timezone-field/django-timezone-field-1.2.tar.gz
  md5: 1b27c7bd9ab6b00477ce466d8b732f81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timezone-field = django-timezone-field:main
    #
    # Would create an entry point called django-timezone-field that calls django-timezone-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - pytz

  run:
    - python
    - django >=1.4.2
    - pytz

test:
  # Python imports
  imports:
    - timezone_field
    - timezone_field.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mfogel/django-timezone-field/
  license: BSD License
  summary: 'A Django app providing database and form fields for pytz timezone objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
