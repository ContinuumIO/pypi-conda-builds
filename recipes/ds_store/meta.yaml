package:
  name: ds_store
  version: "1.0.1"

source:
  fn: ds_store-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/ds_store/ds_store-1.0.1.tar.gz
  md5: 3ec322188a0304959e17cddf402ea8c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ds_store = ds_store:main
    #
    # Would create an entry point called ds_store that calls ds_store.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - biplist >=0.6
    - six >=1.4.1

  run:
    - python
    - biplist >=0.6
    - six >=1.4.1

test:
  # Python imports
  imports:
    - ds_store

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://alastairs-place.net/projects/ds_store
  license: MIT License
  summary: 'Manipulate Finder .DS_Store files from Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
