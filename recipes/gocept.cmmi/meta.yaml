package:
  name: gocept.cmmi
  version: "0.9.4"

source:
  fn: gocept.cmmi-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.cmmi/gocept.cmmi-0.9.4.tar.gz
  md5: b47f550d7077e6453e19be23f7120aa4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.cmmi = gocept.cmmi:main
    #
    # Would create an entry point called gocept.cmmi that calls gocept.cmmi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - gocept.download

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - gocept.download

test:
  # Python imports
  imports:
    - gocept
    - gocept.cmmi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.gocept.com/repos/gocept/buildout-recipes/gocept.cmmi
  license: ZPL 2.1
  summary: '**Deprecated** zc.buildout recipe for building a software package from source.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
