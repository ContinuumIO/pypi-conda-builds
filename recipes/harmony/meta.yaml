package:
  name: harmony
  version: "0.1.0"

source:
  fn: Harmony-0.1.0.zip
  url: https://pypi.python.org/packages/source/H/Harmony/Harmony-0.1.0.zip
  md5: e2fdeb13eeddb25eba2f7279a42366e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - harmony = harmony:main
    #
    # Would create an entry point called harmony that calls harmony.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema >=2.3.0,<3
    - jsonpointer >=1.3,<2
    - pyside >=1.2.2,<2
    - riffle >=0.1.0,<2

  run:
    - python
    - jsonschema >=2.3.0,<3
    - jsonpointer >=1.3,<2
    - pyside >=1.2.2,<2
    - riffle >=0.1.0,<2

test:
  # Python imports
  imports:
    - harmony
    - harmony.schema
    - harmony.ui
    - harmony.ui.model
    - harmony.ui.widget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4degrees/harmony
  license: Apache License (2.0)
  summary: 'Asset management schema framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
