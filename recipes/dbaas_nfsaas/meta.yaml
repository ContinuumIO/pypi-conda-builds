package:
  name: dbaas_nfsaas
  version: "0.5.1"

source:
  fn: dbaas_nfsaas-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dbaas_nfsaas/dbaas_nfsaas-0.5.1.tar.gz
  md5: 791fdf75d6fd633a45ae981164f1275b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbaas_nfsaas = dbaas_nfsaas:main
    #
    # Would create an entry point called dbaas_nfsaas that calls dbaas_nfsaas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dbaas_credentials

  run:
    - python
    - dbaas_credentials

test:
  # Python imports
  imports:
    - dbaas_nfsaas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/dbaas_nfsaas
  license: BSD License
  summary: 'NFSaaS integration for DBaaS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
