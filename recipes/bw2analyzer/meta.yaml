package:
  name: bw2analyzer
  version: "0.8"

source:
  fn: bw2analyzer-0.8.tar.gz
  url: https://pypi.python.org/packages/source/b/bw2analyzer/bw2analyzer-0.8.tar.gz
  md5: dd7b129fb4f930f9f81deb20db26bcb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bw2analyzer = bw2analyzer:main
    #
    # Would create an entry point called bw2analyzer that calls bw2analyzer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - brightway2
    - numpy
    - requests
    - scipy

  run:
    - python
    - brightway2
    - numpy
    - requests
    - scipy

test:
  # Python imports
  imports:
    - bw2analyzer
    - bw2analyzer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cmutel/brightway2-analyzer
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
