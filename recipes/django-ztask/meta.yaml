package:
  name: django-ztask
  version: "0.1.5"

source:
  fn: django-ztask-0.1.5.tgz
  url: https://pypi.python.org/packages/source/d/django-ztask/django-ztask-0.1.5.tgz
  md5: 66939ca9d4a6070821b6728155651d33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ztask = django-ztask:main
    #
    # Would create an entry point called django-ztask that calls django-ztask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq

  run:
    - python
    - pyzmq

test:
  # Python imports
  imports:
    - django_ztask
    - django_ztask.conf
    - django_ztask.management
    - django_ztask.management.commands
    - django_ztask.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dmgctrl/django-ztask
  license: django-zurb-foundation
  summary: 'Django ZTask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
