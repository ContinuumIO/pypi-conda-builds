package:
  name: pentaho-db-changer
  version: "0.0.3"

source:
  fn: pentaho-db-changer-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pentaho-db-changer/pentaho-db-changer-0.0.3.tar.gz
  md5: 88cac6ebaf94838de1446ca97f8067af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pentaho-db-changer = pentaho-db-changer:main
    #
    # Would create an entry point called pentaho-db-changer that calls pentaho-db-changer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlrd
    - xlwt
    - openpyxl
    - unicodecsv

  run:
    - python
    - xlrd
    - xlwt
    - openpyxl
    - unicodecsv

test:
  # Python imports
  imports:
    - set_db_pdi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kappius/pyheaderfile
  license: Apache
  summary: 'Change dynamically Pentaho database connections from kjb, ktr and xml files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
