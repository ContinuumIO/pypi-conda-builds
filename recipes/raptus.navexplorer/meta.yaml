package:
  name: raptus.navexplorer
  version: "1.7"

source:
  fn: raptus.navexplorer-1.7.zip
  url: https://pypi.python.org/packages/source/r/raptus.navexplorer/raptus.navexplorer-1.7.zip
  md5: cea1ad75808f14dc4f2e55973655bacb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.navexplorer = raptus.navexplorer:main
    #
    # Would create an entry point called raptus.navexplorer that calls raptus.navexplorer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ordereddict

  run:
    - python
    - setuptools
    - setuptools
    - ordereddict

test:
  # Python imports
  imports:
    - raptus
    - raptus.navexplorer
    - raptus.navexplorer.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Raptus/raptus.navexplorer
  license: GPL
  summary: 'Provide a navigation to explore and manage plone content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
