package:
  name: oauth1-provider
  version: "0.4.10"

source:
  fn: oauth1-provider-0.4.10.tar.gz
  url: https://pypi.python.org/packages/source/o/oauth1-provider/oauth1-provider-0.4.10.tar.gz
  md5: 41880bcb0fe1f5c274c0695c8f0d0cdc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oauth1-provider = oauth1-provider:main
    #
    # Would create an entry point called oauth1-provider that calls oauth1-provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - redis
    - hiredis
    - sqlalchemy

  run:
    - python
    - flask
    - redis
    - hiredis
    - sqlalchemy

test:
  # Python imports
  imports:
    - oauth1

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tistaharahap/oauth1-provider/
  license: MIT
  summary: 'OAuth 1.0a Provider for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
