package:
  name: flask-mysql
  version: "1.3"

source:
  fn: Flask-MySQL-1.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MySQL/Flask-MySQL-1.3.tar.gz
  md5: 138891c784728dec8bb0d9781b82da61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mysql = flask-mysql:main
    #
    # Would create an entry point called flask-mysql that calls flask-mysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - mysql-python

  run:
    - python
    - flask
    - mysql-python

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberdelia/flask-mysql/
  license: BSD License
  summary: 'Flask simple mysql client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
