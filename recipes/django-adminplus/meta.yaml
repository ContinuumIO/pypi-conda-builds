package:
  name: django-adminplus
  version: "0.3"

source:
  fn: django-adminplus-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adminplus/django-adminplus-0.3.tar.gz
  md5: 76b8375d513d8af62bd5403ac94438a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adminplus = django-adminplus:main
    #
    # Would create an entry point called django-adminplus that calls django-adminplus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adminplus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsocol/django-adminplus
  license: BSD License
  summary: 'Add new pages to the Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
