package:
  name: bitmazk-contact-form
  version: "0.3.1"

source:
  fn: bitmazk-contact-form-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bitmazk-contact-form/bitmazk-contact-form-0.3.1.tar.gz
  md5: e5b3fb0f3862201858b561dc44b27ab0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitmazk-contact-form = bitmazk-contact-form:main
    #
    # Would create an entry point called bitmazk-contact-form that calls bitmazk-contact-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-hvad >=0.3

  run:
    - python
    - django
    - django-hvad >=0.3

test:
  # Python imports
  imports:
    - contact_form
    - contact_form.migrations
    - contact_form.tests
    - contact_form.tests.integration_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/bitmazk-contact-form
  license: UNKNOWN
  summary: 'A reusable contact form app for Django. Can be used via CAPTCHA and/or via\nmodern user-friendly spam protection.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
