package:
  name: ofxhome
  version: "0.3.2"

source:
  fn: ofxhome-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/o/ofxhome/ofxhome-0.3.2.tar.gz
  md5: e6e1246f54865dfc19c5525bf2be6343
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ofxhome = ofxhome:main
    #
    # Would create an entry point called ofxhome that calls ofxhome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ofxhome
    - ofxhome.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/captin411/ofxhome
  license: MIT License
  summary: 'ofxhome.com financial institution lookup REST client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
