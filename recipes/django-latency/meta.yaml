package:
  name: django-latency
  version: "0.0.1.1"

source:
  fn: django-latency-0.0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-latency/django-latency-0.0.1.1.tar.gz
  md5: 1ef6a6cdb51f880a08c24ba087cc843f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-latency = django-latency:main
    #
    # Would create an entry point called django-latency that calls django-latency.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - latency

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-latency/
  license: LICENSE.txt
  summary: 'Destroy the performance of your site (for testing reasons!) with django-latency!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
