package:
  name: mongosync
  version: "0.0.0"

source:
  fn: mongosync-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mongosync/mongosync-0.0.0.tar.gz
  md5: b934c42a30df9c0e10be859528f10422
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongosync = mongosync:main
    #
    # Would create an entry point called mongosync that calls mongosync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mongosync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/csu/mongosync
  license: UNKNOWN
  summary: 'A Python library for syncing MongoDB with sets of data..'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
