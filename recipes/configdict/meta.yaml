package:
  name: configdict
  version: "20101116.1"

source:
  fn: configdict-20101116.1.tar.gz
  url: https://pypi.python.org/packages/source/c/configdict/configdict-20101116.1.tar.gz
  md5: 5b9a090f576ca6dc01c07d4dcc2f1e58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configdict = configdict:main
    #
    # Would create an entry point called configdict that calls configdict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - configdict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/larsks/configdict
  license: GNU General Public License (GPL)
  summary: 'Parse INI files into dictionaries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
