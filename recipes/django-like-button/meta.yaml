package:
  name: django-like-button
  version: "0.0.7"

source:
  fn: django-like-button-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-like-button/django-like-button-0.0.7.tar.gz
  md5: b3988d5cfc21d9b0687b9eab9412bb8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-like-button = django-like-button:main
    #
    # Would create an entry point called django-like-button that calls django-like-button.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - like_button
    - like_button.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johncosta/django-like-button
  license: UNKNOWN
  summary: 'Django App for adding a Facebook like button'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
