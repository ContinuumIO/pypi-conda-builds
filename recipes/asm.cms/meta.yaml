package:
  name: asm.cms
  version: "0.1.8"

source:
  fn: asm.cms-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/a/asm.cms/asm.cms-0.1.8.tar.gz
  md5: 59b79427f1c7b7c321ba357100e7b6d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asm.cms = asm.cms:main
    #
    # Would create an entry point called asm.cms that calls asm.cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grok
    - grokui.admin
    - zodb3
    - megrok.pagelet
    - zc.sourcefactory
    - zope.app.container
    - zope.app.form
    - zope.app.generations
    - zope.app.testing
    - zope.deferredimport
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.traversing
    - zope.app.undo
    - magic
    - lxml
    - barenecessities
    - hurry.zoperesource
    - hurry.tinymce
    - hurry.query
    - z3c.baseregistry

  run:
    - python
    - setuptools
    - grok
    - grokui.admin
    - zodb3
    - megrok.pagelet
    - zc.sourcefactory
    - zope.app.container
    - zope.app.form
    - zope.app.generations
    - zope.app.testing
    - zope.deferredimport
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.traversing
    - zope.app.undo
    - magic
    - lxml
    - barenecessities
    - hurry.zoperesource
    - hurry.tinymce
    - hurry.query
    - z3c.baseregistry

test:
  # Python imports
  imports:
    - asm
    - asm.cms
    - asm.cms.generations
    - asm.cms.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL 2.1
  summary: 'Assembly Website CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
