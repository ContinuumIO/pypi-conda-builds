package:
  name: collective.sharingroles
  version: "1.0b4"

source:
  fn: collective.sharingroles-1.0b4.zip
  url: https://pypi.python.org/packages/source/c/collective.sharingroles/collective.sharingroles-1.0b4.zip
  md5: 5f2317ae7b1cdd3c0da6d77989026d3d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sharingroles = collective.sharingroles:main
    #
    # Would create an entry point called collective.sharingroles that calls collective.sharingroles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - plone.app.workflow
    - products.genericsetup

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - plone.app.workflow
    - products.genericsetup

test:
  # Python imports
  imports:
    - collective
    - collective.sharingroles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.sharingroles
  license: GPL
  summary: "Makes it easier to manage the roles that show up on the 'sharing' page in Plone 3"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
