package:
  name: tornado-stripe
  version: "1.0.3"

source:
  fn: tornado-stripe-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-stripe/tornado-stripe-1.0.3.tar.gz
  md5: 9a4f9ee26fc122f3476b4121246aaa0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-stripe = tornado-stripe:main
    #
    # Would create an entry point called tornado-stripe that calls tornado-stripe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=2.4

  run:
    - python
    - tornado >=2.4

test:
  # Python imports
  imports:
    - tornado_stripe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/didip/tornado-stripe
  license: Apache Software License
  summary: 'Tornado client library for accessing Stripe API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
