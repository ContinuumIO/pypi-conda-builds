package:
  name: zourite
  version: "0.4.0"

source:
  fn: zourite-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zourite/zourite-0.4.0.tar.gz
  md5: e6bb186ff94f583b37f375cc4e624d37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zourite = zourite:main
    #
    # Would create an entry point called zourite that calls zourite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zourite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.zourite.bressure.net
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Zourite Professional Networking Application for N900'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
