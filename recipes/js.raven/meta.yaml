package:
  name: js.raven
  version: "1.1.1"

source:
  fn: js.raven-1.1.1.zip
  url: https://pypi.python.org/packages/source/j/js.raven/js.raven-1.1.1.zip
  md5: 17a20b1b046aa8add26c331755f068d4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.raven = js.raven:main
    #
    # Would create an entry point called js.raven that calls js.raven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.json2

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.json2

test:
  # Python imports
  imports:
    - js
    - js.raven

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of raven-js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
