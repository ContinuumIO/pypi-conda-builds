package:
  name: cgi.unescape
  version: "0.0.2"

source:
  fn: cgi.unescape-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cgi.unescape/cgi.unescape-0.0.2.tar.gz
  md5: 9efff7a6ab696c593045418583d037cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cgi.unescape = cgi.unescape:main
    #
    # Would create an entry point called cgi.unescape that calls cgi.unescape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cgi.unescape
  license: UNKNOWN
  summary: 'back replace html-safe sequences to special characters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
