package:
  name: banzaidb
  version: "0.3.0"

source:
  fn: BanzaiDB-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BanzaiDB/BanzaiDB-0.3.0.tar.gz
  md5: 2a11e16864331d769ad1c06dec0a358b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - banzaidb = banzaidb:main
    #
    # Would create an entry point called banzaidb that calls banzaidb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - reportlab
    - rethinkdb
    - biopython
    - fabric
    - tablib
    - argparse

  run:
    - python
    - reportlab
    - rethinkdb
    - biopython
    - fabric
    - tablib
    - argparse

test:
  # Python imports
  imports:
    - BanzaiDB
    - banzaidb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mscook/BanzaiDB
  license: ECL 2.0
  summary: 'Database for Banzai NGS pipeline tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
