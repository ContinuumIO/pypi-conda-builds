package:
  name: django-model-changes
  version: "0.15"

source:
  fn: django-model-changes-0.15.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-changes/django-model-changes-0.15.tar.gz
  md5: e40e34bffc1208014d0d757cc066e98e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-changes = django-model-changes:main
    #
    # Would create an entry point called django-model-changes that calls django-model-changes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_model_changes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kajic/django-model-changes
  license: MIT License
  summary: 'django-model-changes allows you to track model instance changes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
