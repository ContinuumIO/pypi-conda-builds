package:
  name: header-detail-footer
  version: "2.3"

source:
  fn: header-detail-footer-2.3.tar.gz
  url: https://pypi.python.org/packages/source/h/header-detail-footer/header-detail-footer-2.3.tar.gz
  md5: 5f565614eaed642897d47821b2a8eb50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - header-detail-footer = header-detail-footer:main
    #
    # Would create an entry point called header-detail-footer that calls header-detail-footer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ericvsmith/header-detail-footer
  license: Apache Software License
  summary: 'Parse input streams with headers and footers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
