package:
  name: pyvolution
  version: "1.1"

source:
  fn: Pyvolution-1.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Pyvolution/Pyvolution-1.1.tar.gz
  md5: 676288453260fa3ad380e5a58c920fb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvolution = pyvolution:main
    #
    # Would create an entry point called pyvolution that calls pyvolution.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - Genetic
    - pyvolution

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/inspectorG4dget/Genetic-Framework
  license: Apache License, Version 2.0
  summary: 'Evolutionary Algorithms Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
