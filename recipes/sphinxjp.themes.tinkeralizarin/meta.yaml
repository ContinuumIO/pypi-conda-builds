package:
  name: sphinxjp.themes.tinkeralizarin
  version: "0.1.0"

source:
  fn: sphinxjp.themes.tinkeralizarin-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.themes.tinkeralizarin/sphinxjp.themes.tinkeralizarin-0.1.0.tar.gz
  md5: 0f9e70ee85d9b62664ef5c77a2ca10ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.themes.tinkeralizarin = sphinxjp.themes.tinkeralizarin:main
    #
    # Would create an entry point called sphinxjp.themes.tinkeralizarin that calls sphinxjp.themes.tinkeralizarin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - docutils
    - tinkerer
    - sphinxjp.themecore
    - pygments-style-solarized

  run:
    - python
    - setuptools
    - setuptools
    - docutils
    - tinkerer
    - sphinxjp.themecore
    - pygments-style-solarized

test:
  # Python imports
  imports:
    - sphinxjp
    - sphinxjp.themes
    - sphinxjp.themes.tinkeralizarin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/naoiwata/sphinxjp.themes.tinkeralizarin
  license: MIT License
  summary: 'A tinkerer theme like some famous blogging tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
