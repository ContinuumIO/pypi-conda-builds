package:
  name: pyemail
  version: "0.0.1"

source:
  fn: PyEmail-0.0.1.zip
  url: https://pypi.python.org/packages/source/P/PyEmail/PyEmail-0.0.1.zip
  md5: b7a7bfe9aee1fc3a2f5a412c53b35201
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyemail = pyemail:main
    #
    # Would create an entry point called pyemail that calls pyemail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - pyemail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.yashsinha.com/PyEmail
  license: GNU GENERAL PUBLIC LICENSE Version 3
  summary: 'Python library to send emails using SMTPLIB library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
