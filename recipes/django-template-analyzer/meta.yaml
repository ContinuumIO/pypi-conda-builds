package:
  name: django-template-analyzer
  version: "1.5"

source:
  fn: django-template-analyzer-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-analyzer/django-template-analyzer-1.5.tar.gz
  md5: 675520aa799e0e33d8b18b745cdb756c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-analyzer = django-template-analyzer:main
    #
    # Would create an entry point called django-template-analyzer that calls django-template-analyzer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - template_analyzer
    - template_analyzer.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/django-template-analyzer
  license: BSD License
  summary: 'Django Template Analyzer - Extract template nodes from a Django template'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
