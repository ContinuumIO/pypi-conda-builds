package:
  name: django-group-user-mngt
  version: "1.0"

source:
  fn: django-group-user-mngt-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-group-user-mngt/django-group-user-mngt-1.0.tar.gz
  md5: fcbf49f25527ff7ff8372172291cf5cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-group-user-mngt = django-group-user-mngt:main
    #
    # Would create an entry point called django-group-user-mngt that calls django-group-user-mngt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - group_user_mngt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi//django-group-user-mngt
  license: UNKNOWN
  summary: 'Manage groups and user via jtable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
