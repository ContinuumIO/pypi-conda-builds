package:
  name: hoka.adapter.auth
  version: "2.13.19.3"

source:
  fn: hoka.adapter.auth-2.13.19.3.zip
  url: https://pypi.python.org/packages/source/h/hoka.adapter.auth/hoka.adapter.auth-2.13.19.3.zip
  md5: 8729bfe32bb3299bd34d6ceb2920a94a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hoka.adapter.auth = hoka.adapter.auth:main
    #
    # Would create an entry point called hoka.adapter.auth that calls hoka.adapter.auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2
    - z3c.autoinclude
    - hoka.patches.get_adapter
    - hoka.adapter.base

  run:
    - python
    - setuptools
    - setuptools
    - zope2
    - z3c.autoinclude
    - hoka.patches.get_adapter
    - hoka.adapter.base

test:
  # Python imports
  imports:
    - hoka
    - hoka.adapter
    - hoka.adapter.auth
    - hoka.adapter.auth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hoka/hoka.adapter.auth
  license: GNU General Public License (GPL)
  summary: 'Adapter to get the authenticated user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
