package:
  name: flask-espresso
  version: "0.2.0"

source:
  fn: Flask-Espresso-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Espresso/Flask-Espresso-0.2.0.tar.gz
  md5: 2dc1c3ad8155f07f9c41d5f826af53d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-espresso = flask-espresso:main
    #
    # Would create an entry point called flask-espresso that calls flask-espresso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pyexecjs
    - slimit

  run:
    - python
    - flask
    - pyexecjs
    - slimit

test:
  # Python imports
  imports:
    - flask_espresso

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/morgan-del/flask-espresso
  license: BSD License
  summary: 'Adds Coffescript support to Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
