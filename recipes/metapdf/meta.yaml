package:
  name: metapdf
  version: "0.3.2"

source:
  fn: metapdf-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/m/metapdf/metapdf-0.3.2.tar.gz
  md5: 93bdc85f339f8fc17fb6fa5e7b57f266
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metapdf = metapdf:main
    #
    # Would create an entry point called metapdf that calls metapdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pypdf

  run:
    - python
    - pypdf

test:
  # Python imports
  imports:
    - metapdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aanari/metapdf
  license: MIT License
  summary: 'A lightweight PDF library optimized for metadata extraction and insertion'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
