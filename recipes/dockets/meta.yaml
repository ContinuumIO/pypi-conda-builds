package:
  name: dockets
  version: "0.4.1"

source:
  fn: Dockets-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Dockets/Dockets-0.4.1.tar.gz
  md5: 3773bc866684c80eab8dc2ac56411e74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dockets = dockets:main
    #
    # Would create an entry point called dockets that calls dockets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - redis >=2.10
    - python-dateutil

  run:
    - python
    - simplejson
    - redis >=2.10
    - python-dateutil

test:
  # Python imports
  imports:
    - dockets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gamechanger/dockets
  license: BSD
  summary: 'SUPER simple Redis-backed queueing in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
