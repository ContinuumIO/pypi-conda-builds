package:
  name: medialog.foldersubskins
  version: "0.4"

source:
  fn: medialog.foldersubskins-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.foldersubskins/medialog.foldersubskins-0.4.tar.gz
  md5: 0294e03be99a3ab898c14ff5d3bff54f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.foldersubskins = medialog.foldersubskins:main
    #
    # Would create an entry point called medialog.foldersubskins that calls medialog.foldersubskins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield
    - products.plonesubskins
    - medialog.subskins
    - products.smartcolorwidget

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield
    - products.plonesubskins
    - medialog.subskins
    - products.smartcolorwidget

test:
  # Python imports
  imports:
    - medialog
    - medialog.foldersubskins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GPL
  summary: 'Makes it possible to have different layout on different parts of a plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
