package:
  name: gears-stylus
  version: "0.2.2"

source:
  fn: gears-stylus-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gears-stylus/gears-stylus-0.2.2.tar.gz
  md5: 06c3310024509088c2e3810b7e960f04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gears-stylus = gears-stylus:main
    #
    # Would create an entry point called gears-stylus that calls gears-stylus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gears_stylus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gears/gears-stylus
  license: ISC License (ISCL)
  summary: 'Stylus compiler for Gears'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
