package:
  name: products.presskit
  version: "0.3"

source:
  fn: Products.pressKit-0.3.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.pressKit/Products.pressKit-0.3.tar.gz
  md5: fdbea463700b9ec3c9344a9557cf9e02
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.presskit = products.presskit:main
    #
    # Would create an entry point called products.presskit that calls products.presskit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.pressKit
    - Products.pressKit.browser
    - Products.pressKit.content
    - Products.pressKit.interfaces
    - Products.pressKit.portlets
    - Products.pressKit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License (GPL)
  summary: 'Folderish type to hold press release files, downloadable zip bundle functionality'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
