package:
  name: django-fost-authn
  version: "0.3.8"

source:
  fn: django-fost-authn-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fost-authn/django-fost-authn-0.3.8.tar.gz
  md5: 4a1ad22ac0dc21f475f998f47269f2e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fost-authn = django-fost-authn:main
    #
    # Would create an entry point called django-fost-authn that calls django-fost-authn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fost_authn
    - fost_authn_debug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Felspar/django-fost-authn
  license: Boost Software License - Version 1.0 - August 17th, 2003
  summary: 'HTTP SHA1 HMAC authentication backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
