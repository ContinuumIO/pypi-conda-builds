package:
  name: django-wizard
  version: "0.3.0"

source:
  fn: django-wizard-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wizard/django-wizard-0.3.0.tar.gz
  md5: d28cc1c7e6b067e595752d2f53e916d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wizard = django-wizard:main
    #
    # Would create an entry point called django-wizard that calls django-wizard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django <1.5.0

  run:
    - python
    - django <1.5.0

test:
  # Python imports
  imports:
    - wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imtapps/django-wizard
  license: BSD License
  summary: 'A wizard that helps to control page flow.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
