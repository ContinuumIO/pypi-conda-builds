package:
  name: wadllib
  version: "1.3.2"

source:
  fn: wadllib-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/w/wadllib/wadllib-1.3.2.tar.gz
  md5: b78902ee7fda49ae50e5acc1bb0298f9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wadllib = wadllib:main
    #
    # Would create an entry point called wadllib that calls wadllib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lazr.uri

  run:
    - python
    - setuptools
    - lazr.uri

test:
  # Python imports
  imports:
    - wadllib
    - wadllib.tests
    - wadllib.tests.data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/wadllib
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Navigate HTTP resources using WADL files as guides.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
