package:
  name: lovely.buildouthttp
  version: "0.6.1"

source:
  fn: lovely.buildouthttp-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.buildouthttp/lovely.buildouthttp-0.6.1.tar.gz
  md5: f473f060aeb29ef5b025b9f4510fbec7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.buildouthttp = lovely.buildouthttp:main
    #
    # Would create an entry point called lovely.buildouthttp that calls lovely.buildouthttp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - six

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - six

test:
  # Python imports
  imports:
    - lovely
    - lovely.buildouthttp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lovelysystems/lovely.buildouthttp
  license: Zope Public License
  summary: 'Buildout extension to add http authentication for find-links and download recipies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
