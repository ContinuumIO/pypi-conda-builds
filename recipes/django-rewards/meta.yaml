package:
  name: django-rewards
  version: "0.1.7"

source:
  fn: django-rewards-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rewards/django-rewards-0.1.7.tar.gz
  md5: 209bf16605207c6d1a3bab2da2492e6b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rewards = django-rewards:main
    #
    # Would create an entry point called django-rewards that calls django-rewards.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-crispy-forms >=1.1.4
    - django-extensible-profiles >=1.2

  run:
    - python
    - django-crispy-forms >=1.1.4
    - django-extensible-profiles >=1.2

test:
  # Python imports
  imports:
    - rewards
    - rewards.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/incuna/django-rewards/
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
