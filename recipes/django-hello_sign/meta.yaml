package:
  name: django-hello_sign
  version: "0.2.1"

source:
  fn: django-hello_sign-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hello_sign/django-hello_sign-0.2.1.tar.gz
  md5: 13b41e3ea3aaae3488520ec431e9dc4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hello_sign = django-hello_sign:main
    #
    # Would create an entry point called django-hello_sign that calls django-hello_sign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hellosign

  run:
    - python
    - hellosign

test:
  # Python imports
  imports:
    - hello_sign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rosscdh/django-hello_sign
  license: MIT
  summary: 'Django app for integrating with HelloSign'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
