package:
  name: disktools
  version: "0.0.3"

source:
  fn: disktools-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/disktools/disktools-0.0.3.tar.gz
  md5: 2b61ae1c2be69c7e0d4d9d037323ff90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - disktools = disktools:main
    #
    # Would create an entry point called disktools that calls disktools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse >=1.1
    - bson >=0.3.3

  run:
    - python
    - argparse >=1.1
    - bson >=0.3.3

test:
  # Python imports
  imports:
    - disktools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ewiger/disktools
  license: MIT
  summary: 'Extensions to basic disk tools like du, df, etc, but written in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
