package:
  name: django-html-cleaner
  version: "0.2.5"

source:
  fn: django-html-cleaner-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-html-cleaner/django-html-cleaner-0.2.5.tar.gz
  md5: a6161fff4b365af99c06e58e7c8bcc99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-html-cleaner = django-html-cleaner:main
    #
    # Would create an entry point called django-html-cleaner that calls django-html-cleaner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.3.3
    - django

  run:
    - python
    - lxml >=3.3.3
    - django

test:
  # Python imports
  imports:
    - django_html_cleaner
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cndreisbach/django-html-cleaner
  license: Public Domain / CC0
  summary: 'Django text and character fields that clean HTML input.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
