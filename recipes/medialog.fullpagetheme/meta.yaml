package:
  name: medialog.fullpagetheme
  version: "0.2"

source:
  fn: medialog.fullpagetheme-0.2.zip
  url: https://pypi.python.org/packages/source/m/medialog.fullpagetheme/medialog.fullpagetheme-0.2.zip
  md5: c546d8735d4ddd9972aa635967199f04
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.fullpagetheme = medialog.fullpagetheme:main
    #
    # Would create an entry point called medialog.fullpagetheme that calls medialog.fullpagetheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

test:
  # Python imports
  imports:
    - medialog
    - medialog.fullpagetheme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenmn/medialog.fullpagetheme
  license: GPL
  summary: 'FullpageTheme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
