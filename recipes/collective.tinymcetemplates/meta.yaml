package:
  name: collective.tinymcetemplates
  version: "1.0b3"

source:
  fn: collective.tinymcetemplates-1.0b3.zip
  url: https://pypi.python.org/packages/source/c/collective.tinymcetemplates/collective.tinymcetemplates-1.0b3.zip
  md5: 4e54252b725ae855046f2475154815ff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.tinymcetemplates = collective.tinymcetemplates:main
    #
    # Would create an entry point called collective.tinymcetemplates that calls collective.tinymcetemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - products.tinymce

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - products.tinymce

test:
  # Python imports
  imports:
    - collective
    - collective.tinymcetemplates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.tinymcetemplates
  license: GPL
  summary: 'TinyMCE Plugin for templates and snippets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
