package:
  name: minitestlib
  version: "0.1.1"

source:
  fn: minitestlib-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/minitestlib/minitestlib-0.1.1.tar.gz
  md5: d1705262459ed5cfc6f85433a47aa503
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minitestlib = minitestlib:main
    #
    # Would create an entry point called minitestlib that calls minitestlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - minitestlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Library to handle TET journal files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
