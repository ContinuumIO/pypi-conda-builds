package:
  name: openerp-sale-order-dates
  version: "7.0.406"

source:
  fn: openerp-sale-order-dates-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-sale-order-dates/openerp-sale-order-dates-7.0.406.tar.gz
  md5: e2a89d8edbc294e84ed95ad7b2007838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-sale-order-dates = openerp-sale-order-dates:main
    #
    # Would create an entry point called openerp-sale-order-dates that calls openerp-sale-order-dates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-sale-stock

  run:
    - python
    - openerp-sale-stock

test:
  # Python imports
  imports:
    - openerp.addons.sale_order_dates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openerp.com
  license: GNU Affero General Public License v3
  summary: 'Dates on Sales Order'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
