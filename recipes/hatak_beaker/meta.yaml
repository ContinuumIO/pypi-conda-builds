package:
  name: hatak_beaker
  version: "0.1.2"

source:
  fn: Hatak_Beaker-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/H/Hatak_Beaker/Hatak_Beaker-0.1.2.tar.gz
  md5: 17f416d1d8eeeddd2458b49e6cfc7887
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hatak_beaker = hatak_beaker:main
    #
    # Would create an entry point called hatak_beaker that calls hatak_beaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hatak >=0.2.1
    - pyramid_beaker

  run:
    - python
    - hatak >=0.2.1
    - pyramid_beaker

test:
  # Python imports
  imports:
    - haplugin
    - hatak_beaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'Beaker plugin for Hatak.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
