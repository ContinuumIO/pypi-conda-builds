package:
  name: django-kvmodel
  version: "0.1.11"

source:
  fn: django-kvmodel-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kvmodel/django-kvmodel-0.1.11.tar.gz
  md5: 0d7a8413dcb2967b7238c20932a4ec25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kvmodel = django-kvmodel:main
    #
    # Would create an entry point called django-kvmodel that calls django-kvmodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.7

  run:
    - python
    - django >=1.2.7

test:
  # Python imports
  imports:
    - kvmodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amdorra/django-kvmodel
  license: MIT License
  summary: 'A package that helps creating django key-value models easily.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
