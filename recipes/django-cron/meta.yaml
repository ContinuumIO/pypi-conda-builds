package:
  name: django-cron
  version: "0.4.0"

source:
  fn: django-cron-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cron/django-cron-0.4.0.tar.gz
  md5: 38b2b83d38dbdca72d02064c1204947b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cron = django-cron:main
    #
    # Would create an entry point called django-cron that calls django-cron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.0
    - django-common-helpers >=0.6.4

  run:
    - python
    - django >=1.7.0
    - django-common-helpers >=0.6.4

test:
  # Python imports
  imports:
    - django_cron
    - django_cron.backends
    - django_cron.backends.lock
    - django_cron.management
    - django_cron.management.commands
    - django_cron.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tivix/django-cron
  license: UNKNOWN
  summary: 'Running python crons in a Django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
