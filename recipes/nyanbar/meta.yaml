package:
  name: nyanbar
  version: "0.3"

source:
  fn: nyanbar-0.3.tar.gz
  url: https://pypi.python.org/packages/source/n/nyanbar/nyanbar-0.3.tar.gz
  md5: 7a0ebe236553a04fdad27c3cfa704f70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nyanbar = nyanbar:main
    #
    # Would create an entry point called nyanbar that calls nyanbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nyanbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apgwoz/nyanbar
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'nyanbar is a progress bar for scripts that uses the Nyan cat instead of other stuff.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
