package:
  name: fwissr
  version: "1.0.5"

source:
  fn: fwissr-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fwissr/fwissr-1.0.5.tar.gz
  md5: c04ab251bb020f5e113ba580a5c8ffe1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fwissr = fwissr:main
    #
    # Would create an entry point called fwissr that calls fwissr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.5.2
    - pyyaml >=3.10

  run:
    - python
    - pymongo >=2.5.2
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - fwissr
    - fwissr.source

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fotonauts/fwissr-python
  license: MIT License
  summary: 'fwissr is a registry configuration tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
