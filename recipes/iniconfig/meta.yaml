package:
  name: iniconfig
  version: "0.2.dev0"

source:
  fn: iniconfig-0.2.dev0.tar.gz
  url: https://pypi.python.org/packages/source/i/iniconfig/iniconfig-0.2.dev0.tar.gz
  md5: b575d98a890d4896a089b09f660c462b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iniconfig = iniconfig:main
    #
    # Would create an entry point called iniconfig that calls iniconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iniconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/RonnyPfannschmidt/inipkg
  license: MIT License
  summary: 'iniconfig: brain-dead simple config-ini parsing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
