package:
  name: aead
  version: "0.2"

source:
  fn: aead-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aead/aead-0.2.tar.gz
  md5: 3482ad3dc5cf641ac985185dd3897c99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aead = aead:main
    #
    # Would create an entry point called aead that calls aead.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cryptography

  run:
    - python
    - cryptography

test:
  # Python imports
  imports:
    - aead

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Ayrx/python-aead
  license: Apache Software License or BSD License
  summary: 'An authenticated encryption implementation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
