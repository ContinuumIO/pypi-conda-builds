package:
  name: flask-silk
  version: "0.2"

source:
  fn: Flask-Silk-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Silk/Flask-Silk-0.2.tar.gz
  md5: aca545a94063dc4acd21779ea5dde330
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-silk = flask-silk:main
    #
    # Would create an entry point called flask-silk that calls flask-silk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_silk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sublee/flask-silk
  license: BSD License
  summary: 'Adds silk icons to your Flask application or blueprint, or extension.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
