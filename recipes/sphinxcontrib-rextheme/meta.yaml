package:
  name: sphinxcontrib-rextheme
  version: "0.1.2"

source:
  fn: sphinxcontrib-rextheme-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-rextheme/sphinxcontrib-rextheme-0.1.2.tar.gz
  md5: 9e5c968b1bd4adab71f286614c203912
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-rextheme = sphinxcontrib-rextheme:main
    #
    # Would create an entry point called sphinxcontrib-rextheme that calls sphinxcontrib-rextheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx

  run:
    - python
    - setuptools
    - sphinx

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/prometheus/sphinxcontrib-rextheme
  license: BSD License
  summary: 'Sphinx theme for RexDB Platform documentation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
