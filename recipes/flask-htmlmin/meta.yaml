package:
  name: flask-htmlmin
  version: "1.0"

source:
  fn: Flask-HTMLmin-1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-HTMLmin/Flask-HTMLmin-1.0.tar.gz
  md5: f27c80661db182b3caf1fc35c160eb26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-htmlmin = flask-htmlmin:main
    #
    # Would create an entry point called flask-htmlmin that calls flask-htmlmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - htmlmin

  run:
    - python
    - flask
    - htmlmin

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hamidfzm/Flask-HTMLmin
  license: BSD License
  summary: 'Minimize render templates html'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
