package:
  name: horae.reports
  version: "1.0a1"

source:
  fn: horae.reports-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.reports/horae.reports-1.0a1.tar.gz
  md5: c1a58fa145d41e58d5aa1f41b1023cfb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.reports = horae.reports:main
    #
    # Would create an entry point called horae.reports that calls horae.reports.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - horae.auth
    - horae.core
    - horae.layout
    - horae.lifecycle
    - horae.properties
    - horae.search
    - horae.ticketing

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - horae.auth
    - horae.core
    - horae.layout
    - horae.lifecycle
    - horae.properties
    - horae.search
    - horae.ticketing

test:
  # Python imports
  imports:
    - horae
    - horae.reports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides basic reporting functionality for horae'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
