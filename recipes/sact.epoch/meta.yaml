package:
  name: sact.epoch
  version: "1.3.0"

source:
  fn: sact.epoch-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sact.epoch/sact.epoch-1.3.0.tar.gz
  md5: 7fb7b51d28121877b7b9eb0f2aa6031b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sact.epoch = sact.epoch:main
    #
    # Would create an entry point called sact.epoch that calls sact.epoch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-dateutil
    - zope.interface
    - zope.component
    - pytz

  run:
    - python
    - setuptools
    - setuptools
    - python-dateutil
    - zope.interface
    - zope.component
    - pytz

test:
  # Python imports
  imports:
    - sact
    - sact.epoch
    - sact.epoch.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/securactive/sact.epoch
  license: BSD License
  summary: 'Time object subclassing datetime allowing diverting local clock mecanism'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
