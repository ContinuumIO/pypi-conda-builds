package:
  name: django-ajax-search
  version: "1.5.1"

source:
  fn: django-ajax-search-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-search/django-ajax-search-1.5.1.tar.gz
  md5: b0681cf4568f3fa6a2cdd69f653b2e86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-search = django-ajax-search:main
    #
    # Would create an entry point called django-ajax-search that calls django-ajax-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajax_search

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://prasenjitsingh.com/opensource/django-ajax-search/
  license: BSD License
  summary: 'A customizable AJAX-powered search for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
