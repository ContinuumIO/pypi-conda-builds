package:
  name: deiman
  version: "0.1.5"

source:
  fn: deiman-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/deiman/deiman-0.1.5.tar.gz
  md5: f736403475c2661be8583b3ca2b237a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deiman = deiman:main
    #
    # Would create an entry point called deiman that calls deiman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - deiman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://syslog.tv/deiman
  license: deimos
  summary: 'Deiman is a Python utility class for daemonizing a process. It provides start and stop methods, as well as a method for retrieving running status information. Linux/Unix-only.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
