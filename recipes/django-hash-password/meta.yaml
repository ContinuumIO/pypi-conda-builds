package:
  name: django-hash-password
  version: "0.0.1"

source:
  fn: django-hash-password-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hash-password/django-hash-password-0.0.1.tar.gz
  md5: 6289f9e4ec7670fd3a27bc28ecf13409
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hash-password = django-hash-password:main
    #
    # Would create an entry point called django-hash-password that calls django-hash-password.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_hash_password
    - django_hash_password.management
    - django_hash_password.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kelonye/django-hash-password
  license: MIT License
  summary: 'Generate hashed passwords.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
