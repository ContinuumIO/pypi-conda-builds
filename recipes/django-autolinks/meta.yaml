package:
  name: django-autolinks
  version: "0.1.0"

source:
  fn: django-autolinks-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-autolinks/django-autolinks-0.1.0.tar.gz
  md5: 47054df7d2fbcd846ad32612e33eba5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-autolinks = django-autolinks:main
    #
    # Would create an entry point called django-autolinks that calls django-autolinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_autolinks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-autolinks/
  license: BSD License
  summary: 'App for storing links and automatic link extraction from markdown texts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
