package:
  name: frasco-emails
  version: "0.2.2"

source:
  fn: frasco-emails-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-emails/frasco-emails-0.2.2.tar.gz
  md5: 58eda4608c49cecad5882096b00a123d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-emails = frasco-emails:main
    #
    # Would create an entry point called frasco-emails that calls frasco-emails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - flask-mail >=0.9.0
    - html2text >=2014.7.3
    - premailer >=2.5.0
    - markdown >=2.4.1

  run:
    - python
    - frasco
    - flask-mail >=0.9.0
    - html2text >=2014.7.3
    - premailer >=2.5.0
    - markdown >=2.4.1

test:
  # Python imports
  imports:
    - frasco_emails

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-emails
  license: MIT
  summary: 'Emails sending for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
