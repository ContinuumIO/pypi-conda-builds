package:
  name: django-braintree
  version: "1.3.4"

source:
  fn: django-braintree-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-braintree/django-braintree-1.3.4.tar.gz
  md5: e4abd3715216210e2932ba8c37651707
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-braintree = django-braintree:main
    #
    # Would create an entry point called django-braintree that calls django-braintree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - braintree >=2.8

  run:
    - python
    - braintree >=2.8

test:
  # Python imports
  imports:
    - django_braintree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danielgtaylor/braintree_django
  license: BSD License
  summary: 'Django app for interacting with the braintree API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
