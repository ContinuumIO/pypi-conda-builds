package:
  name: ciutils
  version: "0.0.6"

source:
  fn: ciutils-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/ciutils/ciutils-0.0.6.tar.gz
  md5: 71fb00b22b1ddc3912b6107b70989530
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ciutils = ciutils:main
    #
    # Would create an entry point called ciutils that calls ciutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unittest-xml-reporting

  run:
    - python
    - unittest-xml-reporting

test:
  # Python imports
  imports:
    - ciutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Helpers to use in CI environments (e.g. use xmlrunner with `test` command)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
