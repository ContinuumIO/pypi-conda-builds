package:
  name: django-api-playground
  version: "0.1.0"

source:
  fn: django-api-playground-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-api-playground/django-api-playground-0.1.0.tar.gz
  md5: 8096ed2c2fe78996e88ecc50413e5fa1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-api-playground = django-api-playground:main
    #
    # Would create an entry point called django-api-playground that calls django-api-playground.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - apiplayground
    - apiplayground.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fatiherikli/django-api-playground
  license: BSD License
  summary: 'API Playground for RESTful APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
