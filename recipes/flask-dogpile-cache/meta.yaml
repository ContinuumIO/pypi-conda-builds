package:
  name: flask-dogpile-cache
  version: "0.2"

source:
  fn: Flask-Dogpile-Cache-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Dogpile-Cache/Flask-Dogpile-Cache-0.2.tar.gz
  md5: ad64f3296c1f6e3fe6d50f5b14ebe782
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dogpile-cache = flask-dogpile-cache:main
    #
    # Would create an entry point called flask-dogpile-cache that calls flask-dogpile-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - dogpile.cache >=0.5.2
    - python-memcached

  run:
    - python
    - flask
    - dogpile.cache >=0.5.2
    - python-memcached

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ponomar/flask-dogpile-cache
  license: BSD License
  summary: 'Adds dogpile.cache support to your Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
