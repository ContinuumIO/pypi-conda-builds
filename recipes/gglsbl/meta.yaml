package:
  name: gglsbl
  version: "0.3"

source:
  fn: gglsbl-0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gglsbl/gglsbl-0.3.tar.gz
  md5: 02fb9b9f430a2a6a2243fd50b0a302bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gglsbl = gglsbl:main
    #
    # Would create an entry point called gglsbl that calls gglsbl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - pysqlite
    - protobuf

  run:
    - python
    - argparse
    - pysqlite
    - protobuf

test:
  # Python imports
  imports:
    - gglsbl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/afilipovich/gglsbl
  license: Apache2
  summary: 'Client library for Google Safe Browsing API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
