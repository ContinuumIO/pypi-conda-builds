package:
  name: dbbot
  version: "0.1"

source:
  fn: dbbot-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dbbot/dbbot-0.1.tar.gz
  md5: efab56aa40c8835ad233b9bdc728672a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbbot = dbbot:main
    #
    # Would create an entry point called dbbot that calls dbbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - robotframework

  run:
    - python
    - robotframework

test:
  # Python imports
  imports:
    - dbbot
    - dbbot.reader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robotframework/DbBot
  license: Apache Software License
  summary: 'A tool for serializing Robot Framework test run results into an sqlite3 database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
