package:
  name: django-matome
  version: "0.0.1"

source:
  fn: django-matome-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-matome/django-matome-0.0.1.tar.gz
  md5: 74a93c6c48fc6d748e1f33103c86b253
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-matome = django-matome:main
    #
    # Would create an entry point called django-matome that calls django-matome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - matome
    - matome.management
    - matome.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keitaoouchi/django-matome
  license: BSD License
  summary: "Django commands for reporting code statistics (Classes, KLOCs, etc) from your django project.Reporting Ruby on Rails' rake stats like stats."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
