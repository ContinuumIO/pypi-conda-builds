package:
  name: hadoop-manager
  version: "0.0.4"

source:
  fn: hadoop-manager-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hadoop-manager/hadoop-manager-0.0.4.tar.gz
  md5: 3d99afc8c7ace115c10bc4dae8af0322
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hadoop-manager = hadoop-manager:main
    #
    # Would create an entry point called hadoop-manager that calls hadoop-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hdpmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zemanta/hadoop-manager
  license: GPLv3
  summary: 'Python wrapper for hadoop.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
