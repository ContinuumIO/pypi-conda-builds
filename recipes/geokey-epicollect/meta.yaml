package:
  name: geokey-epicollect
  version: "0.2.0"

source:
  fn: geokey_epicollect-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/geokey-epicollect/geokey_epicollect-0.2.0.tar.gz
  md5: 8b26c53d1a706e0aca18a58a77b64153
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geokey-epicollect = geokey-epicollect:main
    #
    # Would create an entry point called geokey-epicollect that calls geokey-epicollect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml ==3.3.5

  run:
    - python
    - lxml ==3.3.5

test:
  # Python imports
  imports:
    - geokey_epicollect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ExCiteS/geokey-epicollect
  license: geokit
  summary: 'Enables GeoKey to be used with EpiCollect for data collection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
