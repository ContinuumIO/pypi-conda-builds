package:
  name: linaro-json
  version: "2.0.1"

source:
  fn: linaro-json-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/linaro-json/linaro-json-2.0.1.tar.gz
  md5: 87daaea56a15e5dc7be988a8d0dc7344
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linaro-json = linaro-json:main
    #
    # Would create an entry point called linaro-json that calls linaro-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=2.0.9

  run:
    - python
    - simplejson >=2.0.9

test:
  # Python imports
  imports:
    - linaro_json
    - linaro_json.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/linaro-python-json
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'JSON Schema Validator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
