package:
  name: django-historylinks
  version: "1.0.4"

source:
  fn: django-historylinks-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-historylinks/django-historylinks-1.0.4.tar.gz
  md5: 79b97f7937e8da42683042276caeb3d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-historylinks = django-historylinks:main
    #
    # Would create an entry point called django-historylinks that calls django-historylinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - historylinks
    - historylinks.management
    - historylinks.management.commands
    - historylinks.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/etianen/django-historylinks
  license: BSD License
  summary: 'Automatic SEO-friendly HTTP 301 redirects if the URL of a Django model changes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
