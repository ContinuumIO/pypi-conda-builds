package:
  name: ipython-cypher
  version: "0.2.2"

source:
  fn: ipython-cypher-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/i/ipython-cypher/ipython-cypher-0.2.2.tar.gz
  md5: c95d39fdffde8131dc6a3db649485e38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipython-cypher = ipython-cypher:main
    #
    # Would create an entry point called ipython-cypher that calls ipython-cypher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - neo4jrestclient >=2.1.0
    - prettytable
    - ipython >=1.0

  run:
    - python
    - neo4jrestclient >=2.1.0
    - prettytable
    - ipython >=1.0

test:
  # Python imports
  imports:
    - cypher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/versae/ipython-cypher
  license: GNU General Public License (GPL)
  summary: 'Neo4j Cypher cell and line magic for IPython, Pandas, NetworkX and matplotlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
