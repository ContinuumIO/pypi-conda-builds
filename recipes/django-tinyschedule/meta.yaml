package:
  name: django-tinyschedule
  version: "0.1.4"

source:
  fn: django-tinyschedule-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tinyschedule/django-tinyschedule-0.1.4.tar.gz
  md5: 829f68032547910ac8d4a64edc2dd4ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tinyschedule = django-tinyschedule:main
    #
    # Would create an entry point called django-tinyschedule that calls django-tinyschedule.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tinyschedule

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jgeskens/django-tinyschedule
  license: BSD License
  summary: 'A small package for managing schedules in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
