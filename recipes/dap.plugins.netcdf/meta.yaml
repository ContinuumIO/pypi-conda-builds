package:
  name: dap.plugins.netcdf
  version: "0.3.5"

source:
  fn: dap.plugins.netcdf-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.plugins.netcdf/dap.plugins.netcdf-0.3.5.tar.gz
  md5: 3673c4449e46e488bbc1837404b6c91e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.plugins.netcdf = dap.plugins.netcdf:main
    #
    # Would create an entry point called dap.plugins.netcdf that calls dap.plugins.netcdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dap[server] >=2.2.6
    - pupynere >=1.0.1
    - arrayterator >=1.0.0

  run:
    - python
    - setuptools
    - dap[server] >=2.2.6
    - pupynere >=1.0.1
    - arrayterator >=1.0.0

test:
  # Python imports
  imports:
    - dap
    - dap.plugins
    - dap.plugins.netcdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydap.org/plugins/netcdf.html
  license: MIT License
  summary: 'netCDF plugin for pydap server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
