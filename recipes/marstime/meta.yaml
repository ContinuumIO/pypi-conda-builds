package:
  name: marstime
  version: "0.4.6"

source:
  fn: marstime-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/m/marstime/marstime-0.4.6.tar.gz
  md5: c6e3509092db89d107870b0a6a816f00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marstime = marstime:main
    #
    # Would create an entry point called marstime that calls marstime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marstime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.foldmountain.com/marstime
  license: BSD License
  summary: 'marstime library, implements the Mars24 algorithm [Allison and McEwan, 2000]'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
