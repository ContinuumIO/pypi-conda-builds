package:
  name: lshift
  version: "0.1.1"

source:
  fn: lshift-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lshift/lshift-0.1.1.tar.gz
  md5: a21c31291b2bc974718723f8475e7bfa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lshift = lshift:main
    #
    # Would create an entry point called lshift that calls lshift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lshift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/v2e4lisp/lshift
  license: MIT License
  summary: 'left shift args to make a function a partial application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
