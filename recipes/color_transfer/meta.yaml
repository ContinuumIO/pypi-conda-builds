package:
  name: color_transfer
  version: "0.1"

source:
  fn: color_transfer-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/color_transfer/color_transfer-0.1.tar.gz
  md5: ae2072fea45b8f51c57ddc781c0708b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - color_transfer = color_transfer:main
    #
    # Would create an entry point called color_transfer that calls color_transfer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - color_transfer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jrosebr1/color_transfer
  license: MIT
  summary: 'Implements color transfer between two images using the Lab color space, similar to the Reinhard et al. paper, "Color Transfer between Images"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
