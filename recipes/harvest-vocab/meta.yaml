package:
  name: harvest-vocab
  version: "2.0.4"

source:
  fn: harvest-vocab-2.0.4.tar.gz
  url: https://pypi.python.org/packages/source/h/harvest-vocab/harvest-vocab-2.0.4.tar.gz
  md5: 736151f52fc6a963353efee418e6ee8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - harvest-vocab = harvest-vocab:main
    #
    # Would create an entry point called harvest-vocab that calls harvest-vocab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - serrano >=2.3.4,<2.4

  run:
    - python
    - serrano >=2.3.4,<2.4

test:
  # Python imports
  imports:
    - vocab

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cbmi/harvest-vocab/
  license: BSD License
  summary: 'A Harvest Stack app for modeling hierarchical data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
