package:
  name: url-obfuscate
  version: "0.1.3"

source:
  fn: url-obfuscate-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/u/url-obfuscate/url-obfuscate-0.1.3.tar.gz
  md5: e50a97386dd0627c268bf184025f26e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - url-obfuscate = url-obfuscate:main
    #
    # Would create an entry point called url-obfuscate that calls url-obfuscate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - pycrypto ==2.6.1

  run:
    - python
    - django >=1.6
    - pycrypto ==2.6.1

test:
  # Python imports
  imports:
    - tests
    - url_obfuscate
    - url_obfuscate.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lmanzurv/url_ofbuscate
  license: Academic Free License (AFL)
  summary: 'This is a Django application that provides methods to obfuscate/deobfuscate URLs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
