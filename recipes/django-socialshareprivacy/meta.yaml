package:
  name: django-socialshareprivacy
  version: "1.5.1"

source:
  fn: django-socialshareprivacy-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-socialshareprivacy/django-socialshareprivacy-1.5.1.tar.gz
  md5: f8c4141efd10303ce53f776dc6da5d45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-socialshareprivacy = django-socialshareprivacy:main
    #
    # Would create an entry point called django-socialshareprivacy that calls django-socialshareprivacy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - socialshareprivacy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adsworth/django-socialshareprivacy/
  license: BSD License
  summary: 'A Django application for the heise.de socialshareprivacy jQuery plugin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
