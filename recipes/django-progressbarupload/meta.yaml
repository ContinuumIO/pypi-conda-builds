package:
  name: django-progressbarupload
  version: "0.1.6"

source:
  fn: django-progressbarupload-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-progressbarupload/django-progressbarupload-0.1.6.tar.gz
  md5: 44ff997199268e23f28e4087d39cdc78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-progressbarupload = django-progressbarupload:main
    #
    # Would create an entry point called django-progressbarupload that calls django-progressbarupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - progressbarupload
    - progressbarupload.templatetags
    - progressbarupload.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ouhouhsami/django-progressbarupload
  license: BSD License
  summary: 'Progress bar upload for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
