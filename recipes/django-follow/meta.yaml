package:
  name: django-follow
  version: "0.6.1"

source:
  fn: django-follow-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-follow/django-follow-0.6.1.tar.gz
  md5: fb15bf1f9a834d432dfc480f57f54c5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-follow = django-follow:main
    #
    # Would create an entry point called django-follow that calls django-follow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - example.app
    - follow
    - follow.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caffeinehit/django-follow
  license: UNKNOWN
  summary: 'Application which enables following features for users. Can be used for contact books or whatnot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
