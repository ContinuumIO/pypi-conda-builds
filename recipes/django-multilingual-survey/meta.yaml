package:
  name: django-multilingual-survey
  version: "0.2"

source:
  fn: django-multilingual-survey-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multilingual-survey/django-multilingual-survey-0.2.tar.gz
  md5: b9e0f7eeeba9d462e3a1e10c5b26c788
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multilingual-survey = django-multilingual-survey:main
    #
    # Would create an entry point called django-multilingual-survey that calls django-multilingual-survey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-hvad >=0.4
    - django-generic-positions

  run:
    - python
    - django
    - django-hvad >=0.4
    - django-generic-positions

test:
  # Python imports
  imports:
    - multilingual_survey
    - multilingual_survey.migrations
    - multilingual_survey.tests
    - multilingual_survey.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-multilingual-survey
  license: The MIT License
  summary: 'Survey app that allows questions and answers to be created in several languages.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
