package:
  name: connector-events
  version: "0.1.0"

source:
  fn: connector-events-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/connector-events/connector-events-0.1.0.tar.gz
  md5: 5779298e0412b88f95c5a69aea659796
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - connector-events = connector-events:main
    #
    # Would create an entry point called connector-events that calls connector-events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown >=2.0.0
    - django-model-utils >=1.0.0
    - icalendar >=3.5
    - django-ical >=1.2

  run:
    - python
    - markdown >=2.0.0
    - django-model-utils >=1.0.0
    - icalendar >=3.5
    - django-ical >=1.2

test:
  # Python imports
  imports:
    - events

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rva-data/connector-events
  license: BSD License
  summary: "Events app primarily for the 'connector' events & community aggregator"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
