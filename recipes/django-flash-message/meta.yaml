package:
  name: django-flash-message
  version: "0.1a2"

source:
  fn: django-flash-message-0.1a2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flash-message/django-flash-message-0.1a2.tar.gz
  md5: 20e4aac2f9c27bede2b9790dc59fba6c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flash-message = django-flash-message:main
    #
    # Would create an entry point called django-flash-message that calls django-flash-message.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - setuptools
    - django >=1.6

test:
  # Python imports
  imports:
    - django_flash_message

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youngershen/django-flash-message
  license: MIT License
  summary: 'a simple message util for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
