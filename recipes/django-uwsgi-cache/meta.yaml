package:
  name: django-uwsgi-cache
  version: "1.0.0"

source:
  fn: django-uwsgi-cache-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uwsgi-cache/django-uwsgi-cache-1.0.0.tar.gz
  md5: 3c4d0e1c4499394a9ebebea80554562a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uwsgi-cache = django-uwsgi-cache:main
    #
    # Would create an entry point called django-uwsgi-cache that calls django-uwsgi-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - uwsgi

  run:
    - python
    - django
    - uwsgi

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionelmc/django-uwsgi-cache
  license: BSD License
  summary: 'uWSGI Django cache backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
