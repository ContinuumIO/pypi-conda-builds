package:
  name: password-management
  version: "1.0.0"

source:
  fn: password-management-1.0.0.zip
  url: https://pypi.python.org/packages/source/p/password-management/password-management-1.0.0.zip
  md5: 70a37b2521586afb20cbe4da6d70366b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - password-management = password-management:main
    #
    # Would create an entry point called password-management that calls password-management.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'A simple program that allows a user to manage passwords and contains a password generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
