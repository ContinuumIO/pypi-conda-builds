package:
  name: voyager-python
  version: "1.0"

source:
  fn: voyager-python-1.0.zip
  url: https://pypi.python.org/packages/source/v/voyager-python/voyager-python-1.0.zip
  md5: 070b4704f1e2d596b9ab7cc6ca440c51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - voyager-python = voyager-python:main
    #
    # Would create an entry point called voyager-python that calls voyager-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - voyager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datanitro/voyager-python
  license: MIT License
  summary: 'Python wrapper around DataNitro Voyager API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
