package:
  name: django-lutefisk
  version: "0.0.1"

source:
  fn: django-lutefisk-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lutefisk/django-lutefisk-0.0.1.tar.gz
  md5: 46298f0243c81f6b5738b1bafdcb89c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lutefisk = django-lutefisk:main
    #
    # Would create an entry point called django-lutefisk that calls django-lutefisk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lutefisk
    - lutefisk.management
    - lutefisk.management.commands
    - lutefisk.migrations
    - lutefisk.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rentalita.github.com/django-lutefisk
  license: UNKNOWN
  summary: 'Django Lutefisk is a Django user account management app with an API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
