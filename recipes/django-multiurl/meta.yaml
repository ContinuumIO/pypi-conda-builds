package:
  name: django-multiurl
  version: "1.0.1"

source:
  fn: django-multiurl-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multiurl/django-multiurl-1.0.1.tar.gz
  md5: 1daf58e31e2237b3fe5f86d5f5032d2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multiurl = django-multiurl:main
    #
    # Would create an entry point called django-multiurl that calls django-multiurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jacobian/django-multiurl
  license: BSD License
  summary: 'Allow multiple views to match the same URL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
