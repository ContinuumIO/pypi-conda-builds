package:
  name: publicsuffix
  version: "1.0.5"

source:
  fn: publicsuffix-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/publicsuffix/publicsuffix-1.0.5.tar.gz
  md5: 8f8c0b8749e8fb448661f21b65503a5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - publicsuffix = publicsuffix:main
    #
    # Would create an entry point called publicsuffix that calls publicsuffix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Get a public suffix for a domain name using the Public Suffix List.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
