package:
  name: django-funkymetrics
  version: "0.1.9"

source:
  fn: django-funkymetrics-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-funkymetrics/django-funkymetrics-0.1.9.tar.gz
  md5: 9044429597c2db1bd749e31e75b51290
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-funkymetrics = django-funkymetrics:main
    #
    # Would create an entry point called django-funkymetrics that calls django-funkymetrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - funkymetrics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/funkbit/django-funkymetrics
  license: BSD License
  summary: 'Super simple Django application for easily tracking events and submitting them asynchronously to KISSmetrics.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
