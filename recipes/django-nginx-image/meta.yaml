package:
  name: django-nginx-image
  version: "0.3.2"

source:
  fn: django-nginx-image-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nginx-image/django-nginx-image-0.3.2.tar.gz
  md5: f78252a011a677b94d6a061065e40d6e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nginx-image = django-nginx-image:main
    #
    # Would create an entry point called django-nginx-image that calls django-nginx-image.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - nginx_image
    - nginx_image.management
    - nginx_image.management.commands
    - nginx_image.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adw0rd/django-nginx-image
  license: BSD License
  summary: 'Resizing and cropping images via Nginx, and cache the result'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
