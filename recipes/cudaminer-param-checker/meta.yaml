package:
  name: cudaminer-param-checker
  version: "1.0"

source:
  fn: cudaminer-param-checker-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cudaminer-param-checker/cudaminer-param-checker-1.0.tar.gz
  md5: a61f07df5b15171ee1c6789083b58b11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cudaminer-param-checker = cudaminer-param-checker:main
    #
    # Would create an entry point called cudaminer-param-checker that calls cudaminer-param-checker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plac >=0.9.1
    - numpy
    - texttable
    - progressbar
    - python-essentials

  run:
    - python
    - plac >=0.9.1
    - numpy
    - texttable
    - progressbar
    - python-essentials

test:
  # Python imports
  imports:
    - .

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/krichter722/cudaminer-param-checker
  license: GPLv3
  summary: 'An application to check a large set of parameters which can be passed to `cudaminer` in order to figure out the optimal combination with console and application GUI frontend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
