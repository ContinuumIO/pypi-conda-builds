package:
  name: django-timepiece
  version: "0.9.4"

source:
  fn: django-timepiece-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timepiece/django-timepiece-0.9.4.tar.gz
  md5: f06701d39d05a20299656ef937658cc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timepiece = django-timepiece:main
    #
    # Would create an entry point called django-timepiece that calls django-timepiece.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - psycopg2 ==2.5
    - python-dateutil ==1.5
    - django-pagination ==1.0.7
    - django-selectable ==0.7.0
    - django-bootstrap-toolkit ==2.5.6
    - django-compressor ==1.2
    - pytz ==2012c

  run:
    - python
    - django >=1.4
    - psycopg2 ==2.5
    - python-dateutil ==1.5
    - django-pagination ==1.0.7
    - django-selectable ==0.7.0
    - django-bootstrap-toolkit ==2.5.6
    - django-compressor ==1.2
    - pytz ==2012c

test:
  # Python imports
  imports:
    - timepiece
    - timepiece.contracts
    - timepiece.contracts.migrations
    - timepiece.contracts.tests
    - timepiece.crm
    - timepiece.crm.migrations
    - timepiece.crm.tests
    - timepiece.entries
    - timepiece.entries.migrations
    - timepiece.entries.tests
    - timepiece.management
    - timepiece.management.commands
    - timepiece.migrations
    - timepiece.reports
    - timepiece.reports.migrations
    - timepiece.reports.tests
    - timepiece.templatetags
    - timepiece.tests
    - timepiece.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caktus/django-timepiece
  license: BSD License
  summary: "django-timepiece is a multi-user application for tracking people's time on projects."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
