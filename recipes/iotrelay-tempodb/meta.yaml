package:
  name: iotrelay-tempodb
  version: "1.0.0"

source:
  fn: iotrelay-tempodb-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/iotrelay-tempodb/iotrelay-tempodb-1.0.0.tar.gz
  md5: 15646bdf6efff49c4ac8fbef4668bfd0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iotrelay-tempodb = iotrelay-tempodb:main
    #
    # Would create an entry point called iotrelay-tempodb that calls iotrelay-tempodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iotrelay
    - tempodb

  run:
    - python
    - setuptools
    - iotrelay
    - tempodb

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eman/iotrelay-tempodb
  license: BSD License
  summary: 'TempoDB handler module for iotrelay'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
