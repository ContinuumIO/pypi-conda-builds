package:
  name: cyassimp
  version: "0.1.4"

source:
  fn: cyassimp-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cyassimp/cyassimp-0.1.4.tar.gz
  md5: ae4044ef5276dd95811c242bc5142464
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyassimp = cyassimp:main
    #
    # Would create an entry point called cyassimp that calls cyassimp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8.0

  run:
    - python
    - numpy >=1.8.0

test:
  # Python imports
  imports:
    - cyassimp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/menpo/cyassimp/
  license: BSD License
  summary: 'Fast Cython bindings for The Open Assimp Import Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
