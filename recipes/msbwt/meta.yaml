package:
  name: msbwt
  version: "0.2.5"

source:
  fn: msbwt-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/m/msbwt/msbwt-0.2.5.tar.gz
  md5: 2d1332d3eeed6724a5bf2e098834fd61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msbwt = msbwt:main
    #
    # Would create an entry point called msbwt that calls msbwt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysam
    - numpy

  run:
    - python
    - pysam
    - numpy

test:
  # Python imports
  imports:
    - MUS
    - MUSCython
    - msbwt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/msbwt
  license: MIT
  summary: 'Allows for merging and querying of multi-string BWTs for genomic strings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
