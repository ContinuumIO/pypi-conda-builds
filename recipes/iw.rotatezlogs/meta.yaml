package:
  name: iw.rotatezlogs
  version: "0.1.2"

source:
  fn: iw.rotatezlogs-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.rotatezlogs/iw.rotatezlogs-0.1.2.tar.gz
  md5: 7d6875ade5a65249f3a8239f2b697bdf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.rotatezlogs = iw.rotatezlogs:main
    #
    # Would create an entry point called iw.rotatezlogs that calls iw.rotatezlogs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.testing

  run:
    - python
    - setuptools
    - setuptools
    - zope.testing

test:
  # Python imports
  imports:
    - iw
    - iw.rotatezlogs
    - iw.rotatezlogs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://ingeniweb.svn.sourceforge.net/svnroot/ingeniweb/iw.rotatezlogs
  license: GPL
  summary: 'Rotates Zope logs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
