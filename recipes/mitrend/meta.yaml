package:
  name: mitrend
  version: "1.0.0"

source:
  fn: mitrend-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mitrend/mitrend-1.0.0.tar.gz
  md5: 54321b1fb7bf7b0d4a4a33cb4f2d98bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mitrend = mitrend:main
    #
    # Would create an entry point called mitrend that calls mitrend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - mitrend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bbertka/mitrend-python
  license: Apache Software License
  summary: 'A Python implementation of the MiTrend REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
