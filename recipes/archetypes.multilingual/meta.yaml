package:
  name: archetypes.multilingual
  version: "3.0"

source:
  fn: archetypes.multilingual-3.0.zip
  url: https://pypi.python.org/packages/source/a/archetypes.multilingual/archetypes.multilingual-3.0.zip
  md5: 74473be180082f7f0481eb183f64b40b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.multilingual = archetypes.multilingual:main
    #
    # Would create an entry point called archetypes.multilingual that calls archetypes.multilingual.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.atcontenttypes
    - plone.app.multilingual
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - products.atcontenttypes
    - plone.app.multilingual
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.multilingual
    - archetypes.multilingual.browser
    - archetypes.multilingual.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/archetypes.multilingual
  license: GPL
  summary: 'Multilingual support for archetypes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
