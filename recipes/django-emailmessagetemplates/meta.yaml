package:
  name: django-emailmessagetemplates
  version: "0.1.1"

source:
  fn: django-emailmessagetemplates-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailmessagetemplates/django-emailmessagetemplates-0.1.1.tar.gz
  md5: a67dbf1d4457b6ac77901c4b8b47a50a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailmessagetemplates = django-emailmessagetemplates:main
    #
    # Would create an entry point called django-emailmessagetemplates that calls django-emailmessagetemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf

  run:
    - python
    - django-appconf

test:
  # Python imports
  imports:
    - emailmessagetemplates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mcoconnor/django-emailmessagetemplates
  license: BSD License
  summary: 'A Django app that allows users to edit email content with an easy-to-integrate developer API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
