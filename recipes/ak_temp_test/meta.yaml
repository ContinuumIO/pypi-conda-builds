package:
  name: ak_temp_test
  version: "1.2.0"

source:
  fn: ak_temp_test-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/ak_temp_test/ak_temp_test-1.2.0.tar.gz
  md5: f03f3a0296ee0772256982dff5d258dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ak_temp_test = ak_temp_test:main
    #
    # Would create an entry point called ak_temp_test that calls ak_temp_test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ak_temp_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: areskris.blog.163.com
  license: UNKNOWN
  summary: 'This is temporary module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
