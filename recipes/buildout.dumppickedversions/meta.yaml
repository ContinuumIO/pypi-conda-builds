package:
  name: buildout.dumppickedversions
  version: "0.5"

source:
  fn: buildout.dumppickedversions-0.5.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.dumppickedversions/buildout.dumppickedversions-0.5.tar.gz
  md5: e81cffff329aaaaf8dd0d1c6bd63c8b0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.dumppickedversions = buildout.dumppickedversions:main
    #
    # Would create an entry point called buildout.dumppickedversions that calls buildout.dumppickedversions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - buildout
    - buildout.dumppickedversions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/buildout.dumppickedversions
  license: GPL
  summary: 'Dump buildout picked versions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
