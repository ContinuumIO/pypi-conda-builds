package:
  name: ecs.cart
  version: "0.4dev-r2383"

source:
  fn: ecs.cart-0.4dev-r2383.tar.gz
  url: https://pypi.python.org/packages/source/e/ecs.cart/ecs.cart-0.4dev-r2383.tar.gz
  md5: 7cb6d8ef1db6fd5b8f05423dbd03a986
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecs.cart = ecs.cart:main
    #
    # Would create an entry point called ecs.cart that calls ecs.cart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paste

  run:
    - python
    - setuptools
    - paste

test:
  # Python imports
  imports:
    - ecs
    - ecs.cart
    - ecs.cart.rules
    - ecs.cart.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://emencia.com
  license: GPLv3
  summary: 'Package from the ecs suite, that provide a cart object for ecommerce projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
