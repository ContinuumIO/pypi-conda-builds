package:
  name: dap.plugins.matlab
  version: "0.3"

source:
  fn: dap.plugins.matlab-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.plugins.matlab/dap.plugins.matlab-0.3.tar.gz
  md5: f3578da9c91a774d90d3f0617f162a38
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.plugins.matlab = dap.plugins.matlab:main
    #
    # Would create an entry point called dap.plugins.matlab that calls dap.plugins.matlab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dap[server] >=2.2.4
    - arrayterator >=0.2.4

  run:
    - python
    - setuptools
    - dap[server] >=2.2.4
    - arrayterator >=0.2.4

test:
  # Python imports
  imports:
    - dap
    - dap.plugins
    - dap.plugins.matlab

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydap.org/plugins/matlab.html
  license: MIT License
  summary: 'Matlab plugin for pydap server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
