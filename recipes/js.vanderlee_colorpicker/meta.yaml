package:
  name: js.vanderlee_colorpicker
  version: "1.0.9-1"

source:
  fn: js.vanderlee_colorpicker-1.0.9-1.zip
  url: https://pypi.python.org/packages/source/j/js.vanderlee_colorpicker/js.vanderlee_colorpicker-1.0.9-1.zip
  md5: 350df63b912e9bc0f0c0e3ff312fe7fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.vanderlee_colorpicker = js.vanderlee_colorpicker:main
    #
    # Would create an entry point called js.vanderlee_colorpicker that calls js.vanderlee_colorpicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery >=1.7.1
    - js.jqueryui >=1.8.0
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery >=1.7.1
    - js.jqueryui >=1.8.0
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.vanderlee_colorpicker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/js.vanderlee_colorpicker
  license: BSD
  summary: 'Fanstatic packaging of vanderlee/colorpicker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
