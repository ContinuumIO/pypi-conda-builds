package:
  name: django-datatap
  version: "0.0.1"

source:
  fn: django-datatap-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-datatap/django-datatap-0.0.1.tar.gz
  md5: 859f37aa84228c3e0789f9c8d33550b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-datatap = django-datatap:main
    #
    # Would create an entry point called django-datatap that calls django-datatap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datatap
    - datatap.datataps
    - datatap.management
    - datatap.management.commands
    - datatap.tests
    - datatap.tests.datataps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zbyte64/django-datatap
  license: New BSD License
  summary: 'django-datatap is a fixture system enabling applications to define their own loading and dumping process while supporting file asset storage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
