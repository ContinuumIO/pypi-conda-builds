package:
  name: flask-captain
  version: "0.1.1"

source:
  fn: Flask-Captain-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Captain/Flask-Captain-0.1.1.tar.gz
  md5: 88cb393a6a8fafd25f3d512971ae3c1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-captain = flask-captain:main
    #
    # Would create an entry point called flask-captain that calls flask-captain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/flask-captain
  license: MIT
  summary: 'Handle webhooks with Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
