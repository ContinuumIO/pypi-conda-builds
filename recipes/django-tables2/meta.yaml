package:
  name: django-tables2
  version: "0.16.0"

source:
  fn: django-tables2-0.16.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tables2/django-tables2-0.16.0.tar.gz
  md5: 68ecd101de75939d89bf8de14a546d10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tables2 = django-tables2:main
    #
    # Would create an entry point called django-tables2 that calls django-tables2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - six

  run:
    - python
    - django >=1.2
    - six

test:
  # Python imports
  imports:
    - django_tables2
    - django_tables2.columns
    - django_tables2.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyayers/django-tables2/
  license: BSD License
  summary: 'Table/data-grid framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
