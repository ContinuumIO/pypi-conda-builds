package:
  name: iterpipes3
  version: "0.4"

source:
  fn: iterpipes3-0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/iterpipes3/iterpipes3-0.4.tar.gz
  md5: 18bf1010ca215b2658cf0d41c5bd24c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iterpipes3 = iterpipes3:main
    #
    # Would create an entry point called iterpipes3 that calls iterpipes3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iterpipes3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kelleyk/iterpipes3
  license: MIT
  summary: 'A library for running shell pipelines using shell-like syntax'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
