package:
  name: genesis-pyapi
  version: "1.1.1"

source:
  fn: Genesis-PyAPI-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/G/Genesis-PyAPI/Genesis-PyAPI-1.1.1.tar.gz
  md5: ee532f45b33d618b226bf323ba5dc6a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genesis-pyapi = genesis-pyapi:main
    #
    # Would create an entry point called genesis-pyapi that calls genesis-pyapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx ==1.3.1
    - requests ==2.6.0
    - slumber ==0.6.2

  run:
    - python
    - sphinx ==1.3.1
    - requests ==2.6.0
    - slumber ==0.6.2

test:
  # Python imports
  imports:
    - genesis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/genialis/genesis-pyapi/
  license: BSD License
  summary: 'Python API for the Genesis platform.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
