package:
  name: committer
  version: "0.2.7"

source:
  fn: committer-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/c/committer/committer-0.2.7.tar.gz
  md5: 7cf92a0e9dfdb84f29401e64792c6c9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - committer = committer:main
    #
    # Would create an entry point called committer that calls committer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - committer
    - committer.vcsclients

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aelgru/committer
  license: Apache Software License
  summary: 'Unified command line interface for git, mercurial and subversion.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
