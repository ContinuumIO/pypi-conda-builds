package:
  name: gitflow
  version: "0.5.1"

source:
  fn: gitflow-0.5.1.tar.bz2
  url: https://pypi.python.org/packages/source/g/gitflow/gitflow-0.5.1.tar.bz2
  md5: 039b8afbaa1831df483662402710c844
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitflow = gitflow:main
    #
    # Would create an entry point called gitflow that calls gitflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython >=0.3.2c1
    - argparse

  run:
    - python
    - gitpython >=0.3.2c1
    - argparse

test:
  # Python imports
  imports:
    - gitflow
    - gitflow.bin
    - tests
    - tests.gitflow
    - tests.helpers
    - tests.usecases

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/htgoebel/gitflow/
  license: BSD License
  summary: "Git extensions to provide high-level repository operations for Vincent Driessen's branching model."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
