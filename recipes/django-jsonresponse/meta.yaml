package:
  name: django-jsonresponse
  version: "0.10.0"

source:
  fn: django-jsonresponse-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsonresponse/django-jsonresponse-0.10.0.tar.gz
  md5: 0bb97742177e6ef7a2d1e12999475bae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsonresponse = django-jsonresponse:main
    #
    # Would create an entry point called django-jsonresponse that calls django-jsonresponse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonresponse
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jjay/django-jsonresponse
  license: BSD License
  summary: 'Simple wrap django views to render json '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
