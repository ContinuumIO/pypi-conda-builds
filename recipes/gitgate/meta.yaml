package:
  name: gitgate
  version: "0.1.16"

source:
  fn: gitgate-0.1.16.tar.gz
  url: https://pypi.python.org/packages/source/g/gitgate/gitgate-0.1.16.tar.gz
  md5: 752f0702733b16736bb33f8a442f4c75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitgate = gitgate:main
    #
    # Would create an entry point called gitgate that calls gitgate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - peewee >=2.0
    - flask >=0.9
    - argparse >=1

  run:
    - python
    - peewee >=2.0
    - flask >=0.9
    - argparse >=1

test:
  # Python imports
  imports:
    - gitgate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitgate.nullism.com
  license: MIT
  summary: 'Dead simple gatekeeping code review for Git'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
