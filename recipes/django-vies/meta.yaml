package:
  name: django-vies
  version: "2.1.3"

source:
  fn: django-vies-2.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vies/django-vies-2.1.3.tar.gz
  md5: ad3a94689028fbdf31cc48adf139c37f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vies = django-vies:main
    #
    # Would create an entry point called django-vies that calls django-vies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds-jurko >=0.6
    - retrying >=1.1.0

  run:
    - python
    - suds-jurko >=0.6
    - retrying >=1.1.0

test:
  # Python imports
  imports:
    - vies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codingjoe/django-vies
  license: MIT License
  summary: 'European VIES VAT field for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
