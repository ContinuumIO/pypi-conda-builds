package:
  name: apiphant
  version: "0.2.5"

source:
  fn: apiphant-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/a/apiphant/apiphant-0.2.5.tar.gz
  md5: ddd20fda95bfe355583778f5da490e99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apiphant = apiphant:main
    #
    # Would create an entry point called apiphant that calls apiphant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - adict
    - gevent
    - requests >=1.1.0

  run:
    - python
    - adict
    - gevent
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - apiphant

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/denis-ryzhkov/apiphant
  license: MIT License
  summary: 'Simple Python Web API framework, based on Gevent, JSON, CRUD.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
