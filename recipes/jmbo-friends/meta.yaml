package:
  name: jmbo-friends
  version: "2.0.0a1"

source:
  fn: jmbo-friends-2.0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-friends/jmbo-friends-2.0.0a1.tar.gz
  md5: 249e9a1278944998cb096cb2027cf197
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-friends = jmbo-friends:main
    #
    # Would create an entry point called jmbo-friends that calls jmbo-friends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmbo-foundry

  run:
    - python
    - jmbo-foundry

test:
  # Python imports
  imports:
    - friends
    - friends.management
    - friends.management.commands
    - friends.migrations
    - friends.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-friends
  license: BSD License
  summary: 'Friends and badges for Jmbo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
