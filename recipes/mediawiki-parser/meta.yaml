package:
  name: mediawiki-parser
  version: "0.3.3"

source:
  fn: mediawiki-parser-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mediawiki-parser/mediawiki-parser-0.3.3.tar.gz
  md5: 10f89dd4883000e3e0ad961d4165a7df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mediawiki-parser = mediawiki-parser:main
    #
    # Would create an entry point called mediawiki-parser that calls mediawiki-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pijnu

  run:
    - python
    - pijnu

test:
  # Python imports
  imports:
    - mediawiki_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peter17/mediawiki-parser
  license: GNU General Public License v3 (GPLv3)
  summary: 'A parser for the MediaWiki syntax, based on Pijnu.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
