package:
  name: coopy
  version: "0.3.1"

source:
  fn: coopy-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coopy/coopy-0.3.1.tar.gz
  md5: 3ed2c5cfff3633766989ee1cc851cdc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopy = coopy:main
    #
    # Would create an entry point called coopy that calls coopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - coopy
    - coopy.network
    - tests
    - tests.functional
    - tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://coopy.readthedocs.org
  license: UNKNOWN
  summary: 'coopy - plain objects persistence/prevalence tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
