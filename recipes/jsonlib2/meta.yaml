package:
  name: jsonlib2
  version: "1.5.2"

source:
  fn: jsonlib2-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonlib2/jsonlib2-1.5.2.tar.gz
  md5: f650c6979c04ed128e76edaa9ba50323
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonlib2 = jsonlib2:main
    #
    # Would create an entry point called jsonlib2 that calls jsonlib2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonlib2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/jsonlib2/
  license: MIT License
  summary: 'JSON serializer/deserializer for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
