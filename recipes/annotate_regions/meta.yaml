package:
  name: annotate_regions
  version: "1.0"

source:
  fn: annotate_regions-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/annotate_regions/annotate_regions-1.0.tar.gz
  md5: 11e42aaa9dbb59c0748fa72c71b0ea21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - annotate_regions = annotate_regions:main
    #
    # Would create an entry point called annotate_regions that calls annotate_regions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - annotate_regions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'GUI for annotating regions in chromosomal copy number profiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
