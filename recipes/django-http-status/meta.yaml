package:
  name: django-http-status
  version: "0.2"

source:
  fn: django-http-status-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-http-status/django-http-status-0.2.tar.gz
  md5: 4e0104852064ee471f8d96cd37ec3843
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-http-status = django-http-status:main
    #
    # Would create an entry point called django-http-status that calls django-http-status.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - http_status

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/BerislavLopac/django-http-status
  license: FreeBSD
  summary: 'A library of useful HTTP response codes for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
