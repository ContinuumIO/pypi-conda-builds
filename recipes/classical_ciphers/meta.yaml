package:
  name: classical_ciphers
  version: "0.1.1"

source:
  fn: classical_ciphers-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/classical_ciphers/classical_ciphers-0.1.1.tar.gz
  md5: 1ffab5780d69eb174a8553a4e2b57ebe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - classical_ciphers = classical_ciphers:main
    #
    # Would create an entry point called classical_ciphers that calls classical_ciphers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - classical_ciphers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wwwy3y3/ciphers
  license: LICENSE.txt
  summary: 'Useful tools for classical ciphers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
