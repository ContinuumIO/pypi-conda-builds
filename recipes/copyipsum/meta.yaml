package:
  name: copyipsum
  version: "0.0.1"

source:
  fn: copyipsum-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/copyipsum/copyipsum-0.0.1.tar.gz
  md5: b716f201d0e6a3ff8ace15f1de4cc6b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - copyipsum = copyipsum:main
    #
    # Would create an entry point called copyipsum that calls copyipsum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - copyipsum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clioweb/copyipsum
  license: UNKNOWN
  summary: 'Grab text from Loripsum.net API."'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
