package:
  name: barrister
  version: "0.1.6"

source:
  fn: barrister-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/b/barrister/barrister-0.1.6.tar.gz
  md5: 2dc17c9650da993d1d27479edf4db0d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barrister = barrister:main
    #
    # Would create an entry point called barrister that calls barrister.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - markdown
    - plex

  run:
    - python
    - markdown
    - plex

test:
  # Python imports
  imports:
    - barrister

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coopernurse/barrister
  license: MIT License
  summary: 'Polyglot RPC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
