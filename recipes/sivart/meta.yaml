package:
  name: sivart
  version: "0.2.1"

source:
  fn: sivart-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sivart/sivart-0.2.1.tar.gz
  md5: 4f91f2bfb4e25927c155ba324d8f7369
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sivart = sivart:main
    #
    # Would create an entry point called sivart that calls sivart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-vagrant
    - argparse
    - pyyaml

  run:
    - python
    - python-vagrant
    - argparse
    - pyyaml

test:
  # Python imports
  imports:
    - sivart

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/serge-sans-paille/sivart
  license: BSD License
  summary: "\nA poor man's build farm\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
