package:
  name: bmv
  version: "0.2.1"

source:
  fn: BMV-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BMV/BMV-0.2.1.tar.gz
  md5: 5d7684e0b4e6b88e0e1a437e67e7f32b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bmv = bmv:main
    #
    # Would create an entry point called bmv that calls bmv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyserial
    - nose

  run:
    - python
    - pyserial
    - nose

test:
  # Python imports
  imports:
    - bmv
    - bmv.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/otternq/bmvmonitor
  license: LICENSE
  summary: 'A python script to record data from Victron BMV-600S and BMV-602S'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
