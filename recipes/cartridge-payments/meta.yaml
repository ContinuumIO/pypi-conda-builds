package:
  name: cartridge-payments
  version: "0.97.0"

source:
  fn: cartridge-payments-0.97.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cartridge-payments/cartridge-payments-0.97.0.tar.gz
  md5: 0af13899192dcf7c73d248a2d87ad8ac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cartridge-payments = cartridge-payments:main
    #
    # Would create an entry point called cartridge-payments that calls cartridge-payments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - payments
    - payments.multipayments
    - payments.multipayments.forms
    - payments.multipayments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/explodes/cartridge-payments
  license: Apache Software License
  summary: 'Cartridge multiple payment options module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
