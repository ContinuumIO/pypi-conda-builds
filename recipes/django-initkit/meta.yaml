package:
  name: django-initkit
  version: "0.0.1"

source:
  fn: django-initkit-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-initkit/django-initkit-0.0.1.tar.gz
  md5: b0ccea9016497b806166b965d9a8410b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-initkit = django-initkit:main
    #
    # Would create an entry point called django-initkit that calls django-initkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - initkit
    - initkit.init_app
    - initkit.init_app.management
    - initkit.init_app.management.commands
    - initkit.templates
    - initkit.templates.project_template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fitoria.net/
  license: BSD License
  summary: 'A personalized django startproject command'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
