package:
  name: maasclient
  version: "0.3"

source:
  fn: maasclient-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/maasclient/maasclient-0.3.tar.gz
  md5: f4441c970d763b99909439a762b936f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maasclient = maasclient:main
    #
    # Would create an entry point called maasclient that calls maasclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - maasclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Ubuntu-Solutions-Engineering/maasclient
  license: AGPLv3+
  summary: 'Python 3 bindings for MAAS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
