package:
  name: infi.recipe.buildout_logging
  version: "0.1.3"

source:
  fn: infi.recipe.buildout_logging-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.recipe.buildout_logging/infi.recipe.buildout_logging-0.1.3.tar.gz
  md5: b656dd780957769bacc3db93b21f2432
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.recipe.buildout_logging = infi.recipe.buildout_logging:main
    #
    # Would create an entry point called infi.recipe.buildout_logging that calls infi.recipe.buildout_logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - infi
    - infi.recipe
    - infi.recipe.buildout_logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.recipe.buildout_logging
  license: Python Software Foundation License
  summary: 'recipe enabling debug logging to files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
