package:
  name: bitbucket-distutils
  version: "0.1.2"

source:
  fn: bitbucket-distutils-0.1.2.tar.gz
  url: https://bitbucket.org/dahlia/bitbucket-distutils/downloads/bitbucket-distutils-0.1.2.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitbucket-distutils = bitbucket-distutils:main
    #
    # Would create an entry point called bitbucket-distutils that calls bitbucket-distutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==0.8.2

  run:
    - python
    - setuptools
    - requests ==0.8.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Public Domain
  summary: 'Distribute/setuptools/distutils command for Bitbucket. You can use Bitbucket downloads instead of PyPI downloads for release.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
