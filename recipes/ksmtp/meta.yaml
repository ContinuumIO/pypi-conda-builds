package:
  name: ksmtp
  version: "0.2.1"

source:
  fn: ksmtp-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/k/ksmtp/ksmtp-0.2.1.tar.gz
  md5: 70411c7d33657fa028b675786095735d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ksmtp = ksmtp:main
    #
    # Would create an entry point called ksmtp that calls ksmtp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ksmtp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ksmtp/
  license: LICENSE.txt
  summary: 'Simple Python SMTP relay replacement for sendmail with SSL authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
