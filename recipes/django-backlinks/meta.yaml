package:
  name: django-backlinks
  version: "0.1a1"

source:
  fn: django-backlinks-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-backlinks/django-backlinks-0.1a1.tar.gz
  md5: add16c9038bd216c491985e8edb76fd0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-backlinks = django-backlinks:main
    #
    # Would create an entry point called django-backlinks that calls django-backlinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - backlinks
    - backlinks.pingback
    - backlinks.pingback.templatetags
    - backlinks.templatetags
    - backlinks.tests
    - backlinks.trackback
    - backlinks.trackback.templatetags
    - backlinks.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jeffkistler/django-backlinks
  license: BSD License
  summary: 'A generic linkbacks app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
