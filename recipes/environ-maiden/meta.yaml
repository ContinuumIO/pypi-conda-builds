package:
  name: environ-maiden
  version: "0.1.3"

source:
  fn: environ-maiden-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/e/environ-maiden/environ-maiden-0.1.3.tar.gz
  md5: e5571319e534da225ca9cc24184c1970
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - environ-maiden = environ-maiden:main
    #
    # Would create an entry point called environ-maiden that calls environ-maiden.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seanbrant/environ-maiden
  license: BSD License
  summary: 'Simplifies converting ENV variables to config values.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
