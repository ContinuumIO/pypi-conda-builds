package:
  name: gingerit
  version: "0.5.4"

source:
  fn: gingerit-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/g/gingerit/gingerit-0.5.4.tar.gz
  md5: 627fd146bb49d29f0ec9bff460a7ef4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gingerit = gingerit:main
    #
    # Would create an entry point called gingerit that calls gingerit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.2.1

  run:
    - python
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - gingerit
    - gingerit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Azd325/gingerit
  license: BSD License
  summary: '\n    Correcting spelling and grammar mistakes based on the context of\n    complete sentences. Wrapper around the gingersoftware.com API\n    '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
