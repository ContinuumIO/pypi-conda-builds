package:
  name: django-oscar-testsupport
  version: "0.4.1"

source:
  fn: django-oscar-testsupport-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oscar-testsupport/django-oscar-testsupport-0.4.1.tar.gz
  md5: 3184f1486762514f80b0fb1cc866fd5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oscar-testsupport = django-oscar-testsupport:main
    #
    # Would create an entry point called django-oscar-testsupport that calls django-oscar-testsupport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-oscar >=0.4
    - webtest >=1.3,<2.1
    - django-webtest >=1.5.7,<1.6
    - purl >=0.4,<0.5
    - mock >=1.0.1,<1.1

  run:
    - python
    - django-oscar >=0.4
    - webtest >=1.3,<2.1
    - django-webtest >=1.5.7,<1.6
    - purl >=0.4,<0.5
    - mock >=1.0.1,<1.1

test:
  # Python imports
  imports:
    - oscar_testsupport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tangentlabs/django-oscar-testsupport
  license: django-oscar-unicredit
  summary: 'Testing utilities for Oscar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
