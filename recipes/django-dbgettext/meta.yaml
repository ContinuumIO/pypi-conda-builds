package:
  name: django-dbgettext
  version: "0.1.1"

source:
  fn: django-dbgettext-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbgettext/django-dbgettext-0.1.1.tar.gz
  md5: 99f37c2606609031679ec0c0b5d6fc7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbgettext = django-dbgettext:main
    #
    # Would create an entry point called django-dbgettext that calls django-dbgettext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dbgettext
    - dbgettext.lexicons
    - dbgettext.management
    - dbgettext.management.commands
    - dbgettext.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/drmeers/django-dbgettext/wiki
  license: UNKNOWN
  summary: 'Translate Django models by extracting data for gettext'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
