package:
  name: collective.piwik.flowplayer
  version: "0.4"

source:
  fn: collective.piwik.flowplayer-0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.piwik.flowplayer/collective.piwik.flowplayer-0.4.zip
  md5: 914e6642ddf709c6345b097de4bbba50
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.piwik.flowplayer = collective.piwik.flowplayer:main
    #
    # Would create an entry point called collective.piwik.flowplayer that calls collective.piwik.flowplayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.piwik.core
    - collective.flowplayer

  run:
    - python
    - setuptools
    - setuptools
    - collective.piwik.core
    - collective.flowplayer

test:
  # Python imports
  imports:
    - collective
    - collective.piwik
    - collective.piwik.flowplayer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.piwik.flowplayer
  license: GPL
  summary: 'Analytics support for flowplayer using piwik'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
