package:
  name: django-werkzeug-debugger-runserver
  version: "0.2"

source:
  fn: django-werkzeug-debugger-runserver-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-werkzeug-debugger-runserver/django-werkzeug-debugger-runserver-0.2.tar.gz
  md5: 5d9a94ec35407df03d23894c8a522a5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-werkzeug-debugger-runserver = django-werkzeug-debugger-runserver:main
    #
    # Would create an entry point called django-werkzeug-debugger-runserver that calls django-werkzeug-debugger-runserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug
    - six

  run:
    - python
    - werkzeug
    - six

test:
  # Python imports
  imports:
    - werkzeug_debugger_runserver
    - werkzeug_debugger_runserver.management
    - werkzeug_debugger_runserver.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/philippbosch/django-werkzeug-debugger-runserver
  license: BSD
  summary: "Replaces Django's runserver command with one that includes the Werkzeug debugger (shamelessly ripped out of django-extensions)"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
