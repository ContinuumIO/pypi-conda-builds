package:
  name: art3dutils
  version: "0.3.7"

source:
  fn: Art3dUtils-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/A/Art3dUtils/Art3dUtils-0.3.7.tar.gz
  md5: a7a0f29f02be84999590a409722af784
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - art3dutils = art3dutils:main
    #
    # Would create an entry point called art3dutils that calls art3dutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - mako

  run:
    - python
    - sqlalchemy
    - mako

test:
  # Python imports
  imports:
    - art3dutils
    - art3dutils.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Object models for storing and utilities for processing architecture data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
