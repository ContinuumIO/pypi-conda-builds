package:
  name: basin
  version: "0.1"

source:
  fn: basin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/basin/basin-0.1.tar.gz
  md5: 547d1798c1a50a976bf90faa5836666c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basin = basin:main
    #
    # Would create an entry point called basin that calls basin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - basin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/benhodgson/basin
  license: UNKNOWN
  summary: 'A Python module for generating string representations of integers and bytestrings with arbitrary base and alphabet.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
