package:
  name: updog
  version: "1.0"

source:
  fn: updog-1.0.tar.gz
  url: https://pypi.python.org/packages/source/u/updog/updog-1.0.tar.gz
  md5: 7780fa4edc42c8ac0d5271ce05475451
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - updog = updog:main
    #
    # Would create an entry point called updog that calls updog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pexpect >=3.1
    - shell >=1.0.1
    - nose

  run:
    - python
    - pexpect >=3.1
    - shell >=1.0.1
    - nose

test:
  # Python imports
  imports:
    - updog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/msheiny/updog
  license: MIT License
  summary: 'Network device configuration manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
