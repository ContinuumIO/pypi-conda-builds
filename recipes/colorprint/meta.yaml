package:
  name: colorprint
  version: "0.1"

source:
  fn: colorprint-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colorprint/colorprint-0.1.tar.gz
  md5: 1c0547948b914b54aefd5064c3d498e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorprint = colorprint:main
    #
    # Would create an entry point called colorprint that calls colorprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorprint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/don-ramon/colorprint
  license: BSD License
  summary: 'Python module to print in color using py3k-style print function'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
