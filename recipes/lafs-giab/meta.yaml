package:
  name: lafs-giab
  version: "0.1"

source:
  fn: lafs-giab-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lafs-giab/lafs-giab-0.1.tar.gz
  md5: 43a3de8e183d37fe7965de2271caffa3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lafs-giab = lafs-giab:main
    #
    # Would create an entry point called lafs-giab that calls lafs-giab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - allmydata-tahoe >=1.9.1

  run:
    - python
    - allmydata-tahoe >=1.9.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nejucomo/lafs-giab
  license: GPLv3
  summary: 'Configure/start/stop a self-contained Tahoe-LAFS grid.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
