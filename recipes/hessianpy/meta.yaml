package:
  name: hessianpy
  version: "1.0.4"

source:
  fn: HessianPy-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/H/HessianPy/HessianPy-1.0.4.tar.gz
  md5: 32867c7aaa92d18c693739a6dfa8a037
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hessianpy = hessianpy:main
    #
    # Would create an entry point called hessianpy that calls hessianpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hessian
    - hessian.test
    - hessianpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hessianpy.sourceforge.net
  license: Apache License 2.0
  summary: 'Implementation of Hessian RPC protocol'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
