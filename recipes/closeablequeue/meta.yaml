package:
  name: closeablequeue
  version: "0.9.1.1"

source:
  fn: CloseableQueue-0.9.1.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CloseableQueue/CloseableQueue-0.9.1.1.tar.gz
  md5: 52b7550f6b50322f8294d49a782cb5b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - closeablequeue = closeablequeue:main
    #
    # Would create an entry point called closeablequeue that calls closeablequeue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - CloseableQueue.test
    - closeablequeue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/intuited/CloseableQueue
  license: BSD License
  summary: ' These classes and functions provide the facility to close a queue and to use it as an iterator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
