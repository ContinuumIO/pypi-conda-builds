package:
  name: passerine
  version: "1.2.0"

source:
  fn: passerine-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/passerine/passerine-1.2.0.tar.gz
  md5: 29a278945130ae8a2f5162101903422b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - passerine = passerine:main
    #
    # Would create an entry point called passerine that calls passerine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - passerine
    - passerine.data
    - passerine.db
    - passerine.db.driver
    - passerine.db.metadata
    - passerine.decorator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shiroyuki/passerine
  license: MIT License
  summary: 'A generic object relational mapper (ORM) and data abstraction layer (DAL) primarily designed for NoSQL databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
