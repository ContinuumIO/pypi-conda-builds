package:
  name: passwordmetrics
  version: "1.0b1"

source:
  fn: passwordmetrics-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/p/passwordmetrics/passwordmetrics-1.0b1.tar.gz
  md5: cf9e5d91a6d6442494d0d8ad5f1b2efe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - passwordmetrics = passwordmetrics:main
    #
    # Would create an entry point called passwordmetrics that calls passwordmetrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - passwordmetrics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/regebro/passwordmetrics
  license: MIT License
  summary: 'Checking the entropy of a password. Useful in password checkers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
