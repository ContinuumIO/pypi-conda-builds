package:
  name: nylas
  version: "0.2.4"

source:
  fn: nylas-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/n/nylas/nylas-0.2.4.tar.gz
  md5: efdb47e6287e73621fe8855ef8b46207
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nylas = nylas:main
    #
    # Would create an entry point called nylas that calls nylas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0
    - six >=1.4.1
    - bumpversion >=0.5.0
    - pyopenssl
    - ndg-httpsclient
    - pyasn1

  run:
    - python
    - requests >=2.3.0
    - six >=1.4.1
    - bumpversion >=0.5.0
    - pyopenssl
    - ndg-httpsclient
    - pyasn1

test:
  # Python imports
  imports:
    - inbox
    - inbox.client
    - nylas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nylas/inbox-python
  license: MIT
  summary: 'Python bindings for Nylas, the next-generation email platform.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
