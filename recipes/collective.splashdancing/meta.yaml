package:
  name: collective.splashdancing
  version: "1.1a"

source:
  fn: collective.splashdancing-1.1a.zip
  url: https://pypi.python.org/packages/source/c/collective.splashdancing/collective.splashdancing-1.1a.zip
  md5: 09654fdb90556dd97167f52b6a454520
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.splashdancing = collective.splashdancing:main
    #
    # Would create an entry point called collective.splashdancing that calls collective.splashdancing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.dancing

  run:
    - python
    - setuptools
    - setuptools
    - collective.dancing

test:
  # Python imports
  imports:
    - collective
    - collective.splashdancing
    - collective.splashdancing.browser
    - collective.splashdancing.content
    - collective.splashdancing.portlets
    - collective.splashdancing.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://themes.alteroo.com
  license: GNU General Public License (GPL)
  summary: 'Adds pictures to newsitems in singing and dancing newsletters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
