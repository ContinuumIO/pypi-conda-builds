package:
  name: django-payfast
  version: "0.2.2"

source:
  fn: django-payfast-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-payfast/django-payfast-0.2.2.tar.gz
  md5: f9807fe48a224a2f8ca444a5f64f05c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-payfast = django-payfast:main
    #
    # Would create an entry point called django-payfast that calls django-payfast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - payfast
    - payfast.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-payfast/
  license: MIT License
  summary: 'A pluggable Django application for integrating payfast.co.za payment system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
