package:
  name: django-snippets
  version: "0.1.2"

source:
  fn: django-snippets-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-snippets/django-snippets-0.1.2.tar.gz
  md5: 51764811436897274906823ee73e0b5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-snippets = django-snippets:main
    #
    # Would create an entry point called django-snippets that calls django-snippets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - snippets
    - snippets.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/semente/django-snippets/
  license: BSD License
  summary: 'Provides a templatetag for Django acting as an {% include %}, but the template content comes from database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
