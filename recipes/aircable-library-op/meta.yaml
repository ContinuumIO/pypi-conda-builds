package:
  name: aircable-library-op
  version: "0.1.6"

source:
  fn: aircable-library-op-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/a/aircable-library-op/aircable-library-op-0.1.6.tar.gz
  md5: f50fa781a6203a5de3d36f006ec87251
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aircable-library-op = aircable-library-op:main
    #
    # Would create an entry point called aircable-library-op that calls aircable-library-op.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - net
    - net.aircable
    - net.aircable.openproximity
    - net.aircable.openproximity.signals
    - net.aircable.spp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OpenProximity/aircable-library
  license: MIT License
  summary: 'AIRcable libraries for BlueZ Development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
