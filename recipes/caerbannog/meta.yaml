package:
  name: caerbannog
  version: "0.1"

source:
  fn: caerbannog-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/caerbannog/caerbannog-0.1.tar.gz
  md5: 1e1e548b342667297a76465f647a8173
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caerbannog = caerbannog:main
    #
    # Would create an entry point called caerbannog that calls caerbannog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - caerbannog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ztane/caerbannog
  license: MIT
  summary: 'High-level interface to Vowpal Wabbit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
