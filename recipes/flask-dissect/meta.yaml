package:
  name: flask-dissect
  version: "1.0.10"

source:
  fn: Flask-Dissect-1.0.10.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Dissect/Flask-Dissect-1.0.10.tar.gz
  md5: ecf6c8d9d69700a09288cc31308b3568
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dissect = flask-dissect:main
    #
    # Would create an entry point called flask-dissect that calls flask-dissect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests
    - pycrypto

  run:
    - python
    - flask
    - requests
    - pycrypto

test:
  # Python imports
  imports:
    - flask_dissect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bit.epix.com.br/flask-dissect/
  license: BSD License
  summary: 'Dissect Distributed Session Control'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
