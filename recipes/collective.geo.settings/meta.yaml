package:
  name: collective.geo.settings
  version: "3.1"

source:
  fn: collective.geo.settings-3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.geo.settings/collective.geo.settings-3.1.tar.gz
  md5: f1f25bc14df6894b8351c7aa137f9179
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geo.settings = collective.geo.settings:main
    #
    # Would create an entry point called collective.geo.settings that calls collective.geo.settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - plone.app.registry
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - plone.app.registry
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.geo
    - collective.geo.settings
    - collective.geo.settings.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.geo.settings
  license: GPL
  summary: 'collective.geo settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
