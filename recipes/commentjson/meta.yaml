package:
  name: commentjson
  version: "0.4"

source:
  fn: commentjson-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/commentjson/commentjson-0.4.tar.gz
  md5: 7c3aad49716ec598200dc683d750d7bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commentjson = commentjson:main
    #
    # Would create an entry point called commentjson that calls commentjson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - commentjson
    - commentjson.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vaidik/commentjson
  license: UNKNOWN
  summary: 'Add Python and JavaScript style comments in your JSON files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
