package:
  name: easyshop.management
  version: "0.1a1"

source:
  fn: easyshop.management-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.management/easyshop.management-0.1a1.tar.gz
  md5: 2db897f8a0a9eaa59a384be6c34155ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.management = easyshop.management:main
    #
    # Would create an entry point called easyshop.management that calls easyshop.management.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.management
    - easyshop.management.adapters
    - easyshop.management.ajax
    - easyshop.management.browser
    - easyshop.management.scripts
    - easyshop.management.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.example
  license: GPL
  summary: 'Management screens for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
