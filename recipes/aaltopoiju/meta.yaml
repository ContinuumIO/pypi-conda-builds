package:
  name: aaltopoiju
  version: "0.0.3"

source:
  fn: aaltopoiju-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/aaltopoiju/aaltopoiju-0.0.3.tar.gz
  md5: a00f948e9666ff60d8ed5495a0d4de97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aaltopoiju = aaltopoiju:main
    #
    # Would create an entry point called aaltopoiju that calls aaltopoiju.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

test:
  # Python imports
  imports:
    - aaltopoiju

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ojarva/python-aaltopoiju
  license: BSD License
  summary: 'Screen scraper for aaltopoiju.fi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
