package:
  name: chandler-feedsplugin
  version: "0.8-r16144"

source:
  fn: Chandler-FeedsPlugin-0.8-r16144.tar.gz
  url: https://pypi.python.org/packages/source/C/Chandler-FeedsPlugin/Chandler-FeedsPlugin-0.8-r16144.tar.gz
  md5: 4e965b25de00f6bae04c8e952cd6f5fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chandler-feedsplugin = chandler-feedsplugin:main
    #
    # Would create an entry point called chandler-feedsplugin that calls chandler-feedsplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - feeds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Simple RSS/Atom feed support for Chandler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
