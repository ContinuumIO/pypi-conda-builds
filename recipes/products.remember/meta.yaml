package:
  name: products.remember
  version: "1.9.3"

source:
  fn: Products.remember-1.9.3.zip
  url: https://pypi.python.org/packages/source/P/Products.remember/Products.remember-1.9.3.zip
  md5: f246d3f5db249b87e81fbf3b1ea947b5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.remember = products.remember:main
    #
    # Would create an entry point called products.remember that calls products.remember.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.membrane >=2.0dev

  run:
    - python
    - setuptools
    - setuptools
    - products.membrane >=2.0dev

test:
  # Python imports
  imports:
    - Products
    - Products.remember
    - Products.remember.Extensions
    - Products.remember.browser
    - Products.remember.cmfmember
    - Products.remember.content
    - Products.remember.exportimport
    - Products.remember.pas
    - Products.remember.pas.browser
    - Products.remember.tests
    - Products.remember.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/remember
  license: GNU General Public License (GPL)
  summary: "A content-based implementation of Plone's default member infrastructure"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
