package:
  name: django-model-urls
  version: "0.4.0"

source:
  fn: django-model-urls-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-urls/django-model-urls-0.4.0.tar.gz
  md5: 2bc38aa56876a02c68d76cdb77e59d2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-urls = django-model-urls:main
    #
    # Would create an entry point called django-model-urls that calls django-model-urls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - model_urls
    - model_urls.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vinyll/django-model-urls
  license: MIT License
  summary: 'DRY way to manage url parameters associated with a model instance based on Jingo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
