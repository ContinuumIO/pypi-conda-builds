package:
  name: lck.common
  version: "0.4.5"

source:
  fn: lck.common-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/l/lck.common/lck.common-0.4.5.tar.gz
  md5: c385aae04db996bd3155ace3367e0ac5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lck.common = lck.common:main
    #
    # Would create an entry point called lck.common that calls lck.common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lockfile
    - null
    - pycrypto

  run:
    - python
    - lockfile
    - null
    - pycrypto

test:
  # Python imports
  imports:
    - lck
    - lck.cache
    - lck.collections
    - lck.concurrency
    - lck.crypto
    - lck.files
    - lck.git
    - lck.lang
    - lck.math
    - lck.xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/lck.common/
  license: MIT License
  summary: "A library of various simple common routines that keep being rewritten all over again in every project I'm working on."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
