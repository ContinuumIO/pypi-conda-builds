package:
  name: chiantipy
  version: "0.5.3"

source:
  fn: ChiantiPy-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/C/ChiantiPy/ChiantiPy-0.5.3.tar.gz
  md5: bae6810f860b0629b3c949b9f750c49f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chiantipy = chiantipy:main
    #
    # Would create an entry point called chiantipy that calls chiantipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chianti
    - chianti.core
    - chianti.gui_cl
    - chianti.gui_qt
    - chianti.gui_wx
    - chiantipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chiantipy.sourceforge.net
  license: GNU General Public License (GPL)
  summary: 'a Python interface to the CHIANTI atomic database for astrophysical spectroscopy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
