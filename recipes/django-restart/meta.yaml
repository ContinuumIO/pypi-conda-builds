package:
  name: django-restart
  version: "0.2.8.7"

source:
  fn: django-restart-0.2.8.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restart/django-restart-0.2.8.7.tar.gz
  md5: 294d767b3817e5eec61bf81fabd16e40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restart = django-restart:main
    #
    # Would create an entry point called django-restart that calls django-restart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-classy-tags >=0.5

  run:
    - python
    - django >=1.5
    - django-classy-tags >=0.5

test:
  # Python imports
  imports:
    - restart

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/lvlup/django-restart/overview
  license: BSD License
  summary: 'A simple Django app to let admins restart the application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
