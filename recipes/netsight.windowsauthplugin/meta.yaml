package:
  name: netsight.windowsauthplugin
  version: "2.3.1"

source:
  fn: netsight.windowsauthplugin-2.3.1.zip
  url: https://pypi.python.org/packages/source/n/netsight.windowsauthplugin/netsight.windowsauthplugin-2.3.1.zip
  md5: f17a1e5de9ac527a8223ae46ac11b298
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netsight.windowsauthplugin = netsight.windowsauthplugin:main
    #
    # Would create an entry point called netsight.windowsauthplugin that calls netsight.windowsauthplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - netsight
    - netsight.windowsauthplugin
    - netsight.windowsauthplugin.browser
    - netsight.windowsauthplugin.profiles_mixed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/netsight/netsight.windowsauthplugin
  license: GNU General Public License (GPL)
  summary: 'A Windows Integrated Authentication (SPNEGO/Kerberos) Plugin for PAS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
