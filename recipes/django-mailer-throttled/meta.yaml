package:
  name: django-mailer-throttled
  version: "0.1"

source:
  fn: django-mailer-throttled-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailer-throttled/django-mailer-throttled-0.1.tar.gz
  md5: 29f7edfac67ec38659235599dbccd8e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailer-throttled = django-mailer-throttled:main
    #
    # Would create an entry point called django-mailer-throttled that calls django-mailer-throttled.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - mailer_throttled
    - mailer_throttled.management
    - mailer_throttled.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-mailer-throttled
  license: The MIT License
  summary: 'Addon for django-mailer that allows to send throttled emails\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
