package:
  name: django-immutablemodel
  version: "0.3.4"

source:
  fn: django-immutablemodel-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-immutablemodel/django-immutablemodel-0.3.4.tar.gz
  md5: ad2558fa3aaa5d9517229bbefba5ebf6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-immutablemodel = django-immutablemodel:main
    #
    # Would create an entry point called django-immutablemodel that calls django-immutablemodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - immutablemodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/red56/django-immutablemodel
  license: BSD License
  summary: 'A base class for Django to allow immutable fields on Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
