package:
  name: marchanddesable
  version: "0.2"

source:
  fn: marchanddesable-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/marchanddesable/marchanddesable-0.2.tar.gz
  md5: 32984992b3ea58d33907aa1a25c647e1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marchanddesable = marchanddesable:main
    #
    # Would create an entry point called marchanddesable that calls marchanddesable.main()

    - marchand = marchanddesable:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marchanddesable

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - marchand --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madjar/marchanddesable
  license: ISC License (ISCL)
  summary: 'Turn off your server when you sleep'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
