package:
  name: django-cart-improved
  version: "1.0.1"

source:
  fn: django-cart-improved-1.0.1.zip
  url: https://pypi.python.org/packages/source/d/django-cart-improved/django-cart-improved-1.0.1.zip
  md5: 243e6883cc05e6cd531744f689a86a05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cart-improved = django-cart-improved:main
    #
    # Would create an entry point called django-cart-improved that calls django-cart-improved.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cart
    - cart.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/otaviosoares/django-cart
  license: BSD License
  summary: 'Django simple shopping cart, tests and south migrations included'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
