package:
  name: edwin
  version: "0.2.0"

source:
  fn: edwin-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/edwin/edwin-0.2.0.tar.gz
  md5: 5adf9ff5f13662d112cbb0c1591c4814
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edwin = edwin:main
    #
    # Would create an entry point called edwin that calls edwin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.4

  run:
    - python
    - numpy >=1.4

test:
  # Python imports
  imports:
    - edwin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/forieux/edwin/
  license: MIT License
  summary: 'Module related to Bayesian approach to inverse problems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
