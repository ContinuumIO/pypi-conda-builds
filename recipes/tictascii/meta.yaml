package:
  name: tictascii
  version: "0.0.3"

source:
  fn: tictascii-0.0.3.zip
  url: https://pypi.python.org/packages/source/t/tictascii/tictascii-0.0.3.zip
  md5: 61ed972e610f1757cdcd0c9d6081814b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tictascii = tictascii:main
    #
    # Would create an entry point called tictascii that calls tictascii.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tictascii
    - tictascii.ticlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/enginous/tictascii
  license: tidalstream-apiserver
  summary: 'Synergetically revolutionalizing mobile Tic Tac Toe in the cloud.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
