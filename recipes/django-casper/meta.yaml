package:
  name: django-casper
  version: "0.0.2"

source:
  fn: django-casper-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-casper/django-casper-0.0.2.tar.gz
  md5: ba2ab52f893c7f2d6a7566e69dcb31a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-casper = django-casper:main
    #
    # Would create an entry point called django-casper that calls django-casper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - casper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://goodcode.io/
  license: MIT License
  summary: 'CasperJS test integration for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
