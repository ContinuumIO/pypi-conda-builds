package:
  name: xcffib
  version: "0.3.2"

source:
  fn: xcffib-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/x/xcffib/xcffib-0.3.2.tar.gz
  md5: fa13f3fee67c83016a1242982a7c8bda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xcffib = xcffib:main
    #
    # Would create an entry point called xcffib that calls xcffib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - cffi >=1.1.0

  run:
    - python
    - six
    - cffi >=1.1.0

test:
  # Python imports
  imports:
    - xcffib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tych0/xcffib
  license: Apache Software License
  summary: 'A drop in replacement for xpyb, an XCB python binding'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
