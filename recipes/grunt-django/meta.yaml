package:
  name: grunt-django
  version: "0.2.4"

source:
  fn: grunt-django-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/g/grunt-django/grunt-django-0.2.4.tar.gz
  md5: 802d7fc0ba6ef73fdb0d2c46d162c44f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - grunt-django = grunt-django:main
    #
    # Would create an entry point called grunt-django that calls grunt-django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - grunt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Grunt initialization for your project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
