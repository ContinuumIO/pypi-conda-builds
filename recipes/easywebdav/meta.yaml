package:
  name: easywebdav
  version: "1.2.0"

source:
  fn: easywebdav-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/easywebdav/easywebdav-1.2.0.tar.gz
  md5: eeee0f3ea3ec9ca6711feb7c152669d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easywebdav = easywebdav:main
    #
    # Would create an entry point called easywebdav that calls easywebdav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - easywebdav

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/amnong/easywebdav
  license: ISC
  summary: 'A straight-forward WebDAV client, implemented using Requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
