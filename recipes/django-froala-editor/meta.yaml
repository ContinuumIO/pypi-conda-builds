package:
  name: django-froala-editor
  version: "1.2.6"

source:
  fn: django-froala-editor-1.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-froala-editor/django-froala-editor-1.2.6.tar.gz
  md5: 0b9c31197fc0e095cad661fdfb24c026
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-froala-editor = django-froala-editor:main
    #
    # Would create an entry point called django-froala-editor that calls django-froala-editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - froala_editor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/froala/django-froala-editor/
  license: BSD License
  summary: 'django-froala-editor package helps integrate Froala WYSIWYG editor with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
