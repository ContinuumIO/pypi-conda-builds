package:
  name: ubersmith
  version: "0.3.8"

source:
  fn: ubersmith-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/u/ubersmith/ubersmith-0.3.8.tar.gz
  md5: 1d521ddc450619c559763ad5e945744a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ubersmith = ubersmith:main
    #
    # Would create an entry point called ubersmith that calls ubersmith.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0
    - phpserialize >=1.3
    - six >=1.6.1

  run:
    - python
    - requests >=2.3.0
    - phpserialize >=1.3
    - six >=1.6.1

test:
  # Python imports
  imports:
    - ubersmith
    - ubersmith.calls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jasonkeene/python-ubersmith
  license: MIT License
  summary: 'Client library for the Ubersmith API 2.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
