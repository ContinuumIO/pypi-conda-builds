package:
  name: imprime_listas
  version: "1.1.1"

source:
  fn: imprime_listas-1.1.1.zip
  url: https://pypi.python.org/packages/source/i/imprime_listas/imprime_listas-1.1.1.zip
  md5: 6a2e9126894adc77f7d079ff30f1a145
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imprime_listas = imprime_listas:main
    #
    # Would create an entry point called imprime_listas that calls imprime_listas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - imprime_listas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://
  license: UNKNOWN
  summary: 'Uma simples aplicacao para impressao de listas e sub-listas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
