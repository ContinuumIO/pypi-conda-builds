package:
  name: django-mysql-pymysql
  version: "0.1"

source:
  fn: django-mysql-pymysql-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mysql-pymysql/django-mysql-pymysql-0.1.tar.gz
  md5: ef06c23bb0cdb6b4ee66b38a36020492
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mysql-pymysql = django-mysql-pymysql:main
    #
    # Would create an entry point called django-mysql-pymysql that calls django-mysql-pymysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mysql_pymysql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clelland/django-mysql-pymysql
  license: BSD License
  summary: 'Django MySQL backend for PyMySQL adapter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
