package:
  name: arrow-fatisar
  version: "0.5.2"

source:
  fn: arrow-fatisar-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/a/arrow-fatisar/arrow-fatisar-0.5.2.tar.gz
  md5: e5f0dd3bacea97eb0761677abd70cfd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arrow-fatisar = arrow-fatisar:main
    #
    # Would create an entry point called arrow-fatisar that calls arrow-fatisar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil

  run:
    - python
    - python-dateutil

test:
  # Python imports
  imports:
    - arrow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fatisar/arrow
  license: Apache 2.0
  summary: '("arrow" fork) Better dates and times for Python, with intervals'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
