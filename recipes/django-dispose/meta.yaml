package:
  name: django-dispose
  version: "1.1"

source:
  fn: django-dispose-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dispose/django-dispose-1.1.tar.gz
  md5: 9f0e4bf1543dfb2dde560be430856a6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dispose = django-dispose:main
    #
    # Would create an entry point called django-dispose that calls django-dispose.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_dispose
    - django_dispose.management
    - django_dispose.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/danhawkes/django-dispose
  license: MIT License
  summary: 'Disposes of media files that are no longer referenced in the database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
