package:
  name: django-freeperms
  version: "0.1"

source:
  fn: django-freeperms-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-freeperms/django-freeperms-0.1.tar.gz
  md5: 5a68c3c14a8c5f2eb3451f3f7c3d3d80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-freeperms = django-freeperms:main
    #
    # Would create an entry point called django-freeperms that calls django-freeperms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - freeperms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-freeperms
  license: BSD License
  summary: 'Django middleware for enabling anonymous permissions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
