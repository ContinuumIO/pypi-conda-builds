package:
  name: django-multiforloop-patched
  version: "0.2.0"

source:
  fn: django-multiforloop-patched-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multiforloop-patched/django-multiforloop-patched-0.2.0.tar.gz
  md5: cc0076e7e0e4cbb86e9f7acda0b6afa5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multiforloop-patched = django-multiforloop-patched:main
    #
    # Would create an entry point called django-multiforloop-patched that calls django-multiforloop-patched.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - multiforloop
    - multiforloop.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/gabrielgrant/django-multiforloop/
  license: LGPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
