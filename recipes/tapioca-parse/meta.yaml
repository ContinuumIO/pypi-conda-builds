package:
  name: tapioca-parse
  version: "0.1.0"

source:
  fn: tapioca-parse-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tapioca-parse/tapioca-parse-0.1.0.tar.gz
  md5: 859a26e03c8b51dc8b981b5c680714b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tapioca-parse = tapioca-parse:main
    #
    # Would create an entry point called tapioca-parse that calls tapioca-parse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tapioca-wrapper ==0.2.1

  run:
    - python
    - tapioca-wrapper ==0.2.1

test:
  # Python imports
  imports:
    - tapioca_parse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vintasoftware/tapioca-parse
  license: BSD License
  summary: 'Parse REST API wrapper using tapioca'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
