package:
  name: experimental.pietimemenu
  version: "0.1"

source:
  fn: experimental.pietimemenu-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/experimental.pietimemenu/experimental.pietimemenu-0.1.tar.gz
  md5: e9196b4ba1342ad83776e435b62833d5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.pietimemenu = experimental.pietimemenu:main
    #
    # Would create an entry point called experimental.pietimemenu that calls experimental.pietimemenu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - experimental
    - experimental.pietimemenu
    - experimental.pietimemenu.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/experimental.pietimemenu/
  license: GNU General Public License (GPL)
  summary: 'Experimental Radial (Pie) Menu for time input'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
