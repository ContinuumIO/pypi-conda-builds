package:
  name: editdistance
  version: "0.2"

source:
  fn: editdistance-0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/editdistance/editdistance-0.2.tar.gz
  md5: 5862b72e51acbabc5c9bc6afa5b7c896
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - editdistance = editdistance:main
    #
    # Would create an entry point called editdistance that calls editdistance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - editdistance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/aflc/editdistance
  license: MIT License
  summary: 'Fast implementation of the edit distance(Levenshtein distance)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
