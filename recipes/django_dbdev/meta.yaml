package:
  name: django_dbdev
  version: "0.9.4"

source:
  fn: django_dbdev-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django_dbdev/django_dbdev-0.9.4.tar.gz
  md5: 812236048c9269c2cec918db5ca8c539
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_dbdev = django_dbdev:main
    #
    # Would create an entry point called django_dbdev that calls django_dbdev.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - sh
    - future

  run:
    - python
    - django
    - sh
    - future

test:
  # Python imports
  imports:
    - django_dbdev
    - django_dbdev.backends
    - django_dbdev.management
    - django_dbdev.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenak/django_dbdev
  license: BSD
  summary: 'Makes it easy to create and manage databases during development.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
