package:
  name: cmsplugin-bootstrap-carousel
  version: "0.2.2"

source:
  fn: cmsplugin-bootstrap-carousel-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-bootstrap-carousel/cmsplugin-bootstrap-carousel-0.2.2.tar.gz
  md5: 8ca8a6ccaae87306454ff2d47d0005f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-bootstrap-carousel = cmsplugin-bootstrap-carousel:main
    #
    # Would create an entry point called cmsplugin-bootstrap-carousel that calls cmsplugin-bootstrap-carousel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.7,>=1.6
    - django-cms >=2.4
    - pillow

  run:
    - python
    - django <1.7,>=1.6
    - django-cms >=2.4
    - pillow

test:
  # Python imports
  imports:
    - cmsplugin_bootstrap_carousel
    - cmsplugin_bootstrap_carousel.migrations
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nimbis/cmsplugin-bootstrap-carousel/
  license: BSD License
  summary: 'Bootstrap carousel plugin for django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
