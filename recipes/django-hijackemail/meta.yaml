package:
  name: django-hijackemail
  version: "1.0.0"

source:
  fn: django-hijackemail-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hijackemail/django-hijackemail-1.0.0.tar.gz
  md5: 0cf62b91ee2547421e73f99d6345e7fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hijackemail = django-hijackemail:main
    #
    # Would create an entry point called django-hijackemail that calls django-hijackemail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hijackemail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/katylava/django-hijackemail
  license: MIT License
  summary: u'Reroute outgoing mail (for use in dev/testing/staging environments)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
