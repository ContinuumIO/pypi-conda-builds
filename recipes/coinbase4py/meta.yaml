package:
  name: coinbase4py
  version: "0.3.2"

source:
  fn: coinbase4py-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/coinbase4py/coinbase4py-0.3.2.tar.gz
  md5: e04094a9bd48a4fd8117a92dd1c600ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinbase4py = coinbase4py:main
    #
    # Would create an entry point called coinbase4py that calls coinbase4py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - coinbase4py
    - lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/claytantor/coinbase4py
  license: MIT License
  summary: 'Integration Library for the Coinbase API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
