package:
  name: lastsoul
  version: "1.0.1"

source:
  fn: lastSoul-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lastSoul/lastSoul-1.0.1.tar.gz
  md5: e8665127da5b888fc55e48f6ee093d64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lastsoul = lastsoul:main
    #
    # Would create an entry point called lastsoul that calls lastsoul.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lastsoul

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/lastmikoi/lastsoul
  license: GNU General Public License v3 (GPLv3)
  summary: "Python (not yet)full scale client for IONIS's students (EPITECH/EPITA/etc)"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
