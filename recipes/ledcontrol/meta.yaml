package:
  name: ledcontrol
  version: "0.2.0"

source:
  fn: ledcontrol-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ledcontrol/ledcontrol-0.2.0.tar.gz
  md5: f0e8536789b8676ed5ad511422cd1c97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ledcontrol = ledcontrol:main
    #
    # Would create an entry point called ledcontrol that calls ledcontrol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ledcontrol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nathan-osman/ledcontrol
  license: MIT License
  summary: 'Controls LED devices using the usbled Linux kernel module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
