package:
  name: collective.portlet.contribute
  version: "1.0b1"

source:
  fn: collective.portlet.contribute-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.contribute/collective.portlet.contribute-1.0b1.tar.gz
  md5: f791bb5da01f49dc61fc378252e649a7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.contribute = collective.portlet.contribute:main
    #
    # Would create an entry point called collective.portlet.contribute that calls collective.portlet.contribute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission
    - plone.app.portlets

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission
    - plone.app.portlets

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.contribute
    - collective.portlet.contribute.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.portlet.contribute
  license: GPL
  summary: 'A portlet to let people add content of a given type in a folder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
