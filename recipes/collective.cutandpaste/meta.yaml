package:
  name: collective.cutandpaste
  version: "1.1"

source:
  fn: collective.cutandpaste-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.cutandpaste/collective.cutandpaste-1.1.tar.gz
  md5: 79677f0f077edd5d869ff790e13d62b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cutandpaste = collective.cutandpaste:main
    #
    # Would create an entry point called collective.cutandpaste that calls collective.cutandpaste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.transmogrifier

  run:
    - python
    - setuptools
    - setuptools
    - collective.transmogrifier

test:
  # Python imports
  imports:
    - collective
    - collective.cutandpaste

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License (GPL)
  summary: 'for copy and paste items for plone with an csv file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
