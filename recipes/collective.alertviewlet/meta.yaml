package:
  name: collective.alertviewlet
  version: "0.2"

source:
  fn: collective.alertviewlet-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.alertviewlet/collective.alertviewlet-0.2.tar.gz
  md5: eea8f34563db9eb197b334f412f3deca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.alertviewlet = collective.alertviewlet:main
    #
    # Would create an entry point called collective.alertviewlet that calls collective.alertviewlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.alertviewlet
    - collective.alertviewlet.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/collective/collective.alertviewlet
  license: GPL
  summary: 'Allows site administrator to quickly put a global alert on the site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
