package:
  name: evidencetheory
  version: "0.0.1"

source:
  fn: evidencetheory-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/evidencetheory/evidencetheory-0.0.1.tar.gz
  md5: e0bec22a08979df4b7ead15bbd22504a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evidencetheory = evidencetheory:main
    #
    # Would create an entry point called evidencetheory that calls evidencetheory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - evidence
    - evidence.tests
    - evidence.tests.assets
    - evidence.theory
    - evidence.utils
    - evidencetheory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/d555/evidencetheory
  license: MIT
  summary: 'Evidence Theory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
