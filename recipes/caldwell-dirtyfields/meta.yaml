package:
  name: caldwell-dirtyfields
  version: "0.5b1"

source:
  fn: caldwell-dirtyfields-0.5b1.tar.gz
  url: https://pypi.python.org/packages/source/c/caldwell-dirtyfields/caldwell-dirtyfields-0.5b1.tar.gz
  md5: 784747af7552959c88045c1e88b46d91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caldwell-dirtyfields = caldwell-dirtyfields:main
    #
    # Would create an entry point called caldwell-dirtyfields that calls caldwell-dirtyfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dirtyfields
    - example_app
    - example_app.testing_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smn/django-dirtyfields
  license: BSD License
  summary: 'Tracking dirty fields on a Django model instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
