package:
  name: hashstatic
  version: "0.2"

source:
  fn: hashstatic-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hashstatic/hashstatic-0.2.tar.gz
  md5: ee430c30283a50b59fe1cb62772276a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hashstatic = hashstatic:main
    #
    # Would create an entry point called hashstatic that calls hashstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hashstatic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlio/hashstatic
  license: MIT
  summary: 'Simple checker for static content changes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
