package:
  name: hostout.supervisor
  version: "1.0a5"

source:
  fn: hostout.supervisor-1.0a5.tar.gz
  url: https://pypi.python.org/packages/source/h/hostout.supervisor/hostout.supervisor-1.0a5.tar.gz
  md5: 2fc457e7d7cf0aadcb88a0c8914c21f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hostout.supervisor = hostout.supervisor:main
    #
    # Would create an entry point called hostout.supervisor that calls hostout.supervisor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.recipe.egg
    - setuptools
    - collective.hostout >=1.0a5

  run:
    - python
    - setuptools
    - zc.recipe.egg
    - setuptools
    - collective.hostout >=1.0a5

test:
  # Python imports
  imports:
    - hostout
    - hostout.supervisor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/hostout.supervisor
  license: GPL
  summary: 'Plugin for collective.hostout that starts and stops supervisor\n    during deployment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
