package:
  name: awstestutils
  version: "0.1.5"

source:
  fn: awstestutils-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/a/awstestutils/awstestutils-0.1.5.tar.gz
  md5: f7b1ee420d3bdf47d5cac31fed62de0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - awstestutils = awstestutils:main
    #
    # Would create an entry point called awstestutils that calls awstestutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto3

  run:
    - python
    - boto3

test:
  # Python imports
  imports:
    - awstestutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/etoccalino/aws-test-utils
  license: BSD License
  summary: 'Artifacts to test dependencies with AWS using boto3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
