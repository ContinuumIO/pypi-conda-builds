package:
  name: kss.core
  version: "1.6.5"

source:
  fn: kss.core-1.6.5.zip
  url: https://pypi.python.org/packages/source/k/kss.core/kss.core-1.6.5.zip
  md5: 87e66e78c3bbd7af3ecce5b2fef935ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kss.core = kss.core:main
    #
    # Would create an entry point called kss.core that calls kss.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.browserpage
    - zope.browserresource
    - zope.component
    - zope.configuration
    - zope.contenttype
    - zope.datetime
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.pagetemplate
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.site
    - zope.testing
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.browserpage
    - zope.browserresource
    - zope.component
    - zope.configuration
    - zope.contenttype
    - zope.datetime
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.pagetemplate
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.site
    - zope.testing
    - zope.traversing

test:
  # Python imports
  imports:
    - kss
    - kss.core
    - kss.core.pluginregistry
    - kss.core.pluginregistry._concatresource
    - kss.core.pluginregistry._concatresource.compression
    - kss.core.pluginregistry._concatresource.compression.thirdparty
    - kss.core.pluginregistry._concatresource.test
    - kss.core.pluginregistry.browser
    - kss.core.pluginregistry.json
    - kss.core.plugins
    - kss.core.plugins.core
    - kss.core.plugins.core.demo
    - kss.core.plugins.core.demo.binderclasses
    - kss.core.plugins.core.demo.binderids
    - kss.core.plugins.core.demo.commandaction
    - kss.core.plugins.core.demo.coreplugin
    - kss.core.plugins.core.demo.coresyntax
    - kss.core.plugins.core.demo.parameterfunction
    - kss.core.plugins.core.demo.selectors
    - kss.core.plugins.draganddrop
    - kss.core.plugins.effects
    - kss.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kssproject.org
  license: GPL
  summary: 'KSS (Kinetic Style Sheets) core framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
