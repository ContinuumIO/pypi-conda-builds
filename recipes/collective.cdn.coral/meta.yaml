package:
  name: collective.cdn.coral
  version: "0.5"

source:
  fn: collective.cdn.coral-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.cdn.coral/collective.cdn.coral-0.5.zip
  md5: 3810788e77936729d989eabb6d9ab919
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cdn.coral = collective.cdn.coral:main
    #
    # Would create an entry point called collective.cdn.coral that calls collective.cdn.coral.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.cdn.core

  run:
    - python
    - setuptools
    - setuptools
    - collective.cdn.core

test:
  # Python imports
  imports:
    - collective
    - collective.cdn
    - collective.cdn.coral
    - collective.cdn.coral.docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Coral CDN support for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
