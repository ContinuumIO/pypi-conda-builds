package:
  name: diamond
  version: "4.0.41"

source:
  fn: diamond-4.0.41.tar.gz
  url: https://pypi.python.org/packages/source/d/diamond/diamond-4.0.41.tar.gz
  md5: 063ee62fc30a678260df582335a583c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diamond = diamond:main
    #
    # Would create an entry point called diamond that calls diamond.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - configobj
    - psutil

  run:
    - python
    - configobj
    - psutil

test:
  # Python imports
  imports:
    - diamond
    - diamond.handler
    - diamond.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/python-diamond/Diamond
  license: MIT License
  summary: 'Smart data producer for graphite graphing package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
