package:
  name: coop-tag
  version: "0.2.1"

source:
  fn: coop-tag-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coop-tag/coop-tag-0.2.1.tar.gz
  md5: f655d64d47eb9614b44ce1ab457f101f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coop-tag = coop-tag:main
    #
    # Would create an entry point called coop-tag that calls coop-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coop_tag
    - coop_tag.migrations
    - coop_tag.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quinode/coop-tag/
  license: BSD License
  summary: 'Repackaging django-taggit with add-ons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
