package:
  name: slc.aggregation
  version: "1.3"

source:
  fn: slc.aggregation-1.3.zip
  url: https://pypi.python.org/packages/source/s/slc.aggregation/slc.aggregation-1.3.zip
  md5: 35fd431b6b65b9e45e70f059afde34cd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.aggregation = slc.aggregation:main
    #
    # Would create an entry point called slc.aggregation that calls slc.aggregation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - p4a.common
    - p4a.z2utils
    - p4a.subtyper
    - archetypes.schemaextender
    - products.advancedquery

  run:
    - python
    - setuptools
    - setuptools
    - p4a.common
    - p4a.z2utils
    - p4a.subtyper
    - archetypes.schemaextender
    - products.advancedquery

test:
  # Python imports
  imports:
    - slc
    - slc.aggregation
    - slc.aggregation.browser
    - slc.aggregation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/slc.aggregation
  license: GPL
  summary: 'Provides an aggregation solution aimed at Plone sites with many different language versions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
