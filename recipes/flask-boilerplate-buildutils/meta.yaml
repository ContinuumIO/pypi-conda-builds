package:
  name: flask-boilerplate-buildutils
  version: "0.2.7"

source:
  fn: flask-boilerplate-buildutils-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-boilerplate-buildutils/flask-boilerplate-buildutils-0.2.7.tar.gz
  md5: e70e69a67646b51268993e5f6829a4a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-boilerplate-buildutils = flask-boilerplate-buildutils:main
    #
    # Would create an entry point called flask-boilerplate-buildutils that calls flask-boilerplate-buildutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - virtualenv

  run:
    - python
    - virtualenv

test:
  # Python imports
  imports:
    - flask_boilerplate_buildutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nickw444/flask-boilerplate-buildutils
  license: UNKNOWN
  summary: 'Built utils for the flask-boilerplate/flask-boilerplate-utils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
