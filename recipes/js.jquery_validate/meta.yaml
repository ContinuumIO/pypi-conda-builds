package:
  name: js.jquery_validate
  version: "1.11.0-1"

source:
  fn: js.jquery_validate-1.11.0-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_validate/js.jquery_validate-1.11.0-1.tar.gz
  md5: 83ff665d74988c0b3d80d2635e3d6252
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_validate = js.jquery_validate:main
    #
    # Would create an entry point called js.jquery_validate that calls js.jquery_validate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_validate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/s_nikolai/js.jquery_validate
  license: BSD
  summary: 'Fanstatic packaging of validate for jQuery (jquery.validate.js)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
