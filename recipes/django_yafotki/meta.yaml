package:
  name: django_yafotki
  version: "0.7.3"

source:
  fn: django_yafotki-0.7.3.zip
  url: https://pypi.python.org/packages/source/d/django_yafotki/django_yafotki-0.7.3.zip
  md5: 539568b88136602bcacdc657e744eecc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_yafotki = django_yafotki:main
    #
    # Would create an entry point called django_yafotki that calls django_yafotki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - feedparser
    - requests

  run:
    - python
    - feedparser
    - requests

test:
  # Python imports
  imports:
    - django_yafotki
    - yafotki
    - yafotki/client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glader/django_yafotki
  license: BSD License
  summary: 'Yandex.Fotki files backend for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
