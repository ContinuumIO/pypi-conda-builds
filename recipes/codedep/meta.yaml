package:
  name: codedep
  version: "0.3"

source:
  fn: codedep-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/codedep/codedep-0.3.tar.gz
  md5: 46c4b5b5622399ebfcacf2209fc085a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codedep = codedep:main
    #
    # Would create an entry point called codedep that calls codedep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - codedep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/MattShannon/codedep
  license: 3-clause BSD (see License file)
  summary: 'Semi-automated code-level dependency tracking for python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
