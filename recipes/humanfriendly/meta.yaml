package:
  name: humanfriendly
  version: "1.16"

source:
  fn: humanfriendly-1.16.tar.gz
  url: https://pypi.python.org/packages/source/h/humanfriendly/humanfriendly-1.16.tar.gz
  md5: bdbbc76ccb8c0244f3026e8e6d629b6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - humanfriendly = humanfriendly:main
    #
    # Would create an entry point called humanfriendly that calls humanfriendly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - humanfriendly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://humanfriendly.readthedocs.org
  license: UNKNOWN
  summary: 'Human friendly output for text interfaces using Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
