package:
  name: live_api
  version: "0.1.0"

source:
  fn: live_api-0.1.0.zip
  url: https://pypi.python.org/packages/source/l/live_api/live_api-0.1.0.zip
  md5: 34ded9a1e1cc4d0ee377d5e34db5a9f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - live_api = live_api:main
    #
    # Would create an entry point called live_api that calls live_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appier

  run:
    - python
    - appier

test:
  # Python imports
  imports:
    - live
    - live_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://live_api.hive.pt
  license: GNU General Public License (GPL)
  summary: 'Microsoft Live API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
