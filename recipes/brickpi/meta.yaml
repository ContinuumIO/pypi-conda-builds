package:
  name: brickpi
  version: "0.1.3"

source:
  fn: BrickPi-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/B/BrickPi/BrickPi-0.1.3.tar.gz
  md5: 12de15a2702344bab789620964e7d901
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brickpi = brickpi:main
    #
    # Would create an entry point called brickpi that calls brickpi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rpiscratchio >=0.1.6

  run:
    - python
    - rpiscratchio >=0.1.6

test:
  # Python imports
  imports:
    - BrickPi
    - brickpi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BrickPi/
  license: Creative Commons Attribution-ShareAlike 3.0
  summary: 'BrickPi interfaces for Python and Scratch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
