package:
  name: easyapi
  version: "0.1.5"

source:
  fn: EasyAPI-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/E/EasyAPI/EasyAPI-0.1.5.tar.gz
  md5: 036783bb6fb132e49f19cdb724bc9f50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyapi = easyapi:main
    #
    # Would create an entry point called easyapi that calls easyapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - easy_api
    - easy_api.test
    - easyapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/EasyAPI/
  license: LICENSE.txt
  summary: 'A generic RESTful API interface for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
