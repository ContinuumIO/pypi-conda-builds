package:
  name: i18nize_templates
  version: "0.1dev-20131011"

source:
  fn: i18nize_templates-0.1dev-20131011.tar.gz
  url: https://pypi.python.org/packages/source/i/i18nize_templates/i18nize_templates-0.1dev-20131011.tar.gz
  md5: 2c30fdcecc917763e8a7d1efd0eba40e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - i18nize_templates = i18nize_templates:main
    #
    # Would create an entry point called i18nize_templates that calls i18nize_templates.main()

    - i18nize-templates = i18nize_templates.__init__:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - i18nize_templates

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - i18nize-templates --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/csilvers/i18nize_templates
  license: MIT License
  summary: 'Adds i18n markup to jinja2 and handlebars templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
