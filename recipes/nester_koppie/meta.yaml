package:
  name: nester_koppie
  version: "1.3.1"

source:
  fn: nester_koppie-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nester_koppie/nester_koppie-1.3.1.tar.gz
  md5: 72a23b8c8061317a120efb70059f3d6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nester_koppie = nester_koppie:main
    #
    # Would create an entry point called nester_koppie that calls nester_koppie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nester_koppie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tenukidesign.com
  license: UNKNOWN
  summary: "Demo module with hat tip to O'Reilly Head First"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
