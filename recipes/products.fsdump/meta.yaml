package:
  name: products.fsdump
  version: "0.9.5"

source:
  fn: Products.FSDump-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.FSDump/Products.FSDump-0.9.5.tar.gz
  md5: 422e0c4e44a4afbc56200eda6d23765a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.fsdump = products.fsdump:main
    #
    # Would create an entry point called products.fsdump that calls products.fsdump.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.FSDump
    - Products.FSDump.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.FSDump
  license: Zope Public License
  summary: 'Dump through-the-web Zope objects to the filesystem'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
