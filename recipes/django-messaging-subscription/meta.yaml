package:
  name: django-messaging-subscription
  version: "0.7.0"

source:
  fn: django-messaging-subscription-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-messaging-subscription/django-messaging-subscription-0.7.0.tar.gz
  md5: 86b5439f09e5a49110619288cc834b88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-messaging-subscription = django-messaging-subscription:main
    #
    # Would create an entry point called django-messaging-subscription that calls django-messaging-subscription.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-tastypie
    - south
    - gunicorn ==19.0.0
    - django-celery ==3.1.10
    - go-http ==0.1.1

  run:
    - python
    - django
    - django-tastypie
    - south
    - gunicorn ==19.0.0
    - django-celery ==3.1.10
    - go-http ==0.1.1

test:
  # Python imports
  imports:
    - subscription
    - subscription.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/westerncapelabs/django-messaging-subscription
  license: BSD License
  summary: 'A RESTful API for managing messaging content, subscriptions and sending via Vumi-go'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
