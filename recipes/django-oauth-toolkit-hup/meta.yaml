package:
  name: django-oauth-toolkit-hup
  version: "0.7.2"

source:
  fn: django-oauth-toolkit-hup-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauth-toolkit-hup/django-oauth-toolkit-hup-0.7.2.tar.gz
  md5: aab718ebb26598d8f5be62956dddc7be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauth-toolkit-hup = django-oauth-toolkit-hup:main
    #
    # Would create an entry point called django-oauth-toolkit-hup that calls django-oauth-toolkit-hup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-braces >=1.2.2
    - six >=1.3.0
    - oauthlib >=0.6.1

  run:
    - python
    - django >=1.4
    - django-braces >=1.2.2
    - six >=1.3.0
    - oauthlib >=0.6.1

test:
  # Python imports
  imports:
    - oauth2_provider
    - oauth2_provider.ext
    - oauth2_provider.ext.rest_framework
    - oauth2_provider.migrations
    - oauth2_provider.tests
    - oauth2_provider.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/acenario/django-oauth2
  license: BSD License
  summary: 'OAuth2 goodies for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
