package:
  name: merlottemplates
  version: "0.2"

source:
  fn: MerlotTemplates-0.2.zip
  url: https://pypi.python.org/packages/source/M/MerlotTemplates/MerlotTemplates-0.2.zip
  md5: 46d77006cb994af6a235f414025e8cd7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - merlottemplates = merlottemplates:main
    #
    # Would create an entry point called merlottemplates that calls merlottemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - cheetah
    - pastescript

  run:
    - python
    - setuptools
    - setuptools
    - cheetah
    - pastescript

test:
  # Python imports
  imports:
    - merlot
    - merlot.templates
    - merlottemplates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/merlot/
  license: GPL2
  summary: 'Paster templates for Merlot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
