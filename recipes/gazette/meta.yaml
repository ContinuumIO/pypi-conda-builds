package:
  name: gazette
  version: "1.1.0"

source:
  fn: Gazette-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/G/Gazette/Gazette-1.1.0.tar.gz
  md5: c7c4df96445d3d85a71f21d102eda704
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gazette = gazette:main
    #
    # Would create an entry point called gazette that calls gazette.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle
    - jsonpickle

  run:
    - python
    - bottle
    - jsonpickle

test:
  # Python imports
  imports:
    - gazette
    - gazette.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://sf.net/p/gazette
  license: MIT
  summary: 'A flexible CMS library that can be added into any app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
