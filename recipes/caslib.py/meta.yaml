package:
  name: caslib.py
  version: "2.2.0"

source:
  fn: caslib.py-2.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/caslib.py/caslib.py-2.2.0.tar.gz
  md5: ee7ec8d61d142f1a96919039403d0f27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caslib.py = caslib.py:main
    #
    # Would create an entry point called caslib.py that calls caslib.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iPlantCollaborativeOpenSource/caslib.py
  license: caso
  summary: 'CAS Client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
