package:
  name: kuyruk-sentry
  version: "1.1.0"

source:
  fn: Kuyruk-Sentry-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/K/Kuyruk-Sentry/Kuyruk-Sentry-1.1.0.tar.gz
  md5: 5dfa0cbaf88e37734c42a1f030f5e7d7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kuyruk-sentry = kuyruk-sentry:main
    #
    # Would create an entry point called kuyruk-sentry that calls kuyruk-sentry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kuyruk >=3.0.0
    - raven >=5.1.1

  run:
    - python
    - setuptools
    - kuyruk >=3.0.0
    - raven >=5.1.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cenkalti/kuyruk-sentry
  license: UNKNOWN
  summary: 'Sends exceptions in Kuyruk workers to Sentry.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
