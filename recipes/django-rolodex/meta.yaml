package:
  name: django-rolodex
  version: "0.1.2.4"

source:
  fn: django-rolodex-0.1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rolodex/django-rolodex-0.1.2.4.tar.gz
  md5: 330ecd3d90825fc25b093c2e90887673
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rolodex = django-rolodex:main
    #
    # Would create an entry point called django-rolodex that calls django-rolodex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - djangorestframework >=2.4.4
    - dj-database-url >=0.3.0
    - networkx >=1.9.1

  run:
    - python
    - django >=1.7
    - djangorestframework >=2.4.4
    - dj-database-url >=0.3.0
    - networkx >=1.9.1

test:
  # Python imports
  imports:
    - rolodex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DallasMorningNews/django-rolodex/
  license: MIT License
  summary: 'A Django app for managing relationships between people and organizations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
