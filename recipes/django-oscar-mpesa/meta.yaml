package:
  name: django-oscar-mpesa
  version: "0.0.2"

source:
  fn: django-oscar-mpesa-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oscar-mpesa/django-oscar-mpesa-0.0.2.tar.gz
  md5: 07878d22fd0a315655e727f604b06cbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oscar-mpesa = django-oscar-mpesa:main
    #
    # Would create an entry point called django-oscar-mpesa that calls django-oscar-mpesa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.8
    - django-phonenumber-field ==0.6.0

  run:
    - python
    - django <1.8
    - django-phonenumber-field ==0.6.0

test:
  # Python imports
  imports:
    - mpesa
    - mpesa.dashboard
    - mpesa.migrations
    - mpesa.test_tools
    - mpesa.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/regulusweb/django-oscar-mpesa
  license: BSD License
  summary: "Oscar integration for M-Pesa's IPN service."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
