package:
  name: logsandra
  version: "0.1dev"

source:
  fn: logsandra-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/l/logsandra/logsandra-0.1dev.tar.gz
  md5: 6919c5125168a4eb7b410d6cdabce281
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logsandra = logsandra:main
    #
    # Would create an entry point called logsandra that calls logsandra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - logsandra
    - logsandra.config
    - logsandra.controllers
    - logsandra.lib
    - logsandra.model
    - logsandra.monitor
    - logsandra.monitor.parsers
    - logsandra.monitor.watchers
    - logsandra.tests
    - logsandra.tests.functional
    - logsandra.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thobbs/logsandra
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
