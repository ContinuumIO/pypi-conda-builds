package:
  name: locaweb_gateway
  version: "v0.1.1"

source:
  fn: locaweb_gateway-v0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/locaweb_gateway/locaweb_gateway-v0.1.1.tar.gz
  md5: 7c48938cbad057c75ef231807bf2d1d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - locaweb_gateway = locaweb_gateway:main
    #
    # Would create an entry point called locaweb_gateway that calls locaweb_gateway.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.8.8
    - simplejson

  run:
    - python
    - requests >=0.8.8
    - simplejson

test:
  # Python imports
  imports:
    - locaweb_gateway

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/locaweb/locaweb-gateway-python
  license: MIT license
  summary: 'Locaweb gateway bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
