package:
  name: django-statsy
  version: "0.1"

source:
  fn: django-statsy-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-statsy/django-statsy-0.1.tar.gz
  md5: d3a3035c64e9ef13ba9aa521d5b1a9e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-statsy = django-statsy:main
    #
    # Would create an entry point called django-statsy that calls django-statsy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.7
    - jsonfield >=1.0.0

  run:
    - python
    - django >=1.7
    - jsonfield >=1.0.0

test:
  # Python imports
  imports:
    - statsy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fata1ex/django-statsy
  license: MIT
  summary: 'Statistics for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
