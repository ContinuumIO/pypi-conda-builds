package:
  name: fist
  version: "0.1.5"

source:
  fn: fist-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fist/fist-0.1.5.tar.gz
  md5: 59bd24ed4889cc5613396d7fd56c689f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fist = fist:main
    #
    # Would create an entry point called fist that calls fist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mfiers.github.com/Moa/api/fist.html
  license: UNKNOWN
  summary: 'Fist / File sets\n\nHandle & map sets of files. Fist allows the definition of sets of\nfiles and deriving mapped sets. See\nhttp://mfiers.github.com/Moa/api/fist.html for more information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
