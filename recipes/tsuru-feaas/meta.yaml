package:
  name: tsuru-feaas
  version: "0.10.0"

source:
  fn: tsuru-feaas-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tsuru-feaas/tsuru-feaas-0.10.0.tar.gz
  md5: 3165932dd42f8cb6279557af43b4aeec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tsuru-feaas = tsuru-feaas:main
    #
    # Would create an entry point called tsuru-feaas that calls tsuru-feaas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.9
    - boto ==2.25.0
    - pymongo ==2.6.3
    - python-varnish ==0.2.1
    - httplib2 ==0.9

  run:
    - python
    - flask ==0.9
    - boto ==2.25.0
    - pymongo ==2.6.3
    - python-varnish ==0.2.1
    - httplib2 ==0.9

test:
  # Python imports
  imports:
    - feaas
    - feaas.managers
    - feaas.runners

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Frontend as-a-service API for Tsuru PaaS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
