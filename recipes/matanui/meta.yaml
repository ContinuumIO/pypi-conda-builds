package:
  name: matanui
  version: "0.1.0"

source:
  fn: matanui-0.1.0.tar.bz2
  url: https://pypi.python.org/packages/source/m/matanui/matanui-0.1.0.tar.bz2
  md5: 14c3de045465c1b4d6e6e563e6a20e21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - matanui = matanui:main
    #
    # Would create an entry point called matanui that calls matanui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - matanui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/matanui/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'MataNui Replicating Grid Data Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
