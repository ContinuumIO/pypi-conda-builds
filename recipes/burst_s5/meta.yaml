package:
  name: burst_s5
  version: "0.1"

source:
  fn: burst_s5-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/burst_s5/burst_s5-0.1.tar.gz
  md5: b01addca80d345c2e72154c6243c3a1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - burst_s5 = burst_s5:main
    #
    # Would create an entry point called burst_s5 that calls burst_s5.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - burst_s5

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/astraw/burst_s5
  license: MIT
  summary: 'standards-compliant HTML slideshows with HTML5 video and Inkscape'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
