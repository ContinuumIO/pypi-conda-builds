package:
  name: python-kadmin-local
  version: "0.1.1"

source:
  fn: python-kadmin-local-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-kadmin-local/python-kadmin-local-0.1.1.tar.gz
  md5: 5c04a8456bfd7206febe431c3a5bfa91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-kadmin-local = python-kadmin-local:main
    #
    # Would create an entry point called python-kadmin-local that calls python-kadmin-local.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/russjancewicz/python-kadmin
  license: MIT License
  summary: 'Python module for kerberos admin (kadm5) via root local interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
