package:
  name: django-classy-tags
  version: "0.6.1"

source:
  fn: django-classy-tags-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-classy-tags/django-classy-tags-0.6.1.tar.gz
  md5: eb686aa767ad8cf88c1fa0f400a42516
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-classy-tags = django-classy-tags:main
    #
    # Would create an entry point called django-classy-tags that calls django-classy-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >1.3

  run:
    - python
    - django >1.3

test:
  # Python imports
  imports:
    - classytags
    - classytags.test
    - classytags.test.project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-classy-tags
  license: BSD License
  summary: 'Class based template tags for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
