package:
  name: gomobile.imageinfo
  version: "0.9.3"

source:
  fn: gomobile.imageinfo-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gomobile.imageinfo/gomobile.imageinfo-0.9.3.tar.gz
  md5: b047f85c646c9e509570c17d026269fc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gomobile.imageinfo = gomobile.imageinfo:main
    #
    # Would create an entry point called gomobile.imageinfo that calls gomobile.imageinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - gomobile
    - gomobile.imageinfo
    - gomobile.imageinfo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://webandmobile.mfabrik.com
  license: GNU General Public License (GPL)
  summary: 'Extract and manipulate different Zope image objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
