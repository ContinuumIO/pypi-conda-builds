package:
  name: django-urlarchivefield
  version: "0.0.9"

source:
  fn: django-urlarchivefield-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urlarchivefield/django-urlarchivefield-0.0.9.tar.gz
  md5: e5fdc16b77ed911e1af467bdf4a87d23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urlarchivefield = django-urlarchivefield:main
    #
    # Would create an entry point called django-urlarchivefield that calls django-urlarchivefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - storytracker ==0.0.6
    - six >=1.7.2

  run:
    - python
    - storytracker ==0.0.6
    - six >=1.7.2

test:
  # Python imports
  imports:
    - urlarchivefield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pastpages/django-urlarchivefield
  license: MIT
  summary: 'A custom Django model field that automatically archives a URL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
