package:
  name: django-tour
  version: "0.6.3"

source:
  fn: django-tour-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tour/django-tour-0.6.3.tar.gz
  md5: 3cf92b90c26c413747c2562450080e1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tour = django-tour:main
    #
    # Would create an entry point called django-tour that calls django-tour.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - djangorestframework >=2.3.13,<3.0
    - django-manager-utils >=0.7.2
    - django_filter >=0.7

  run:
    - python
    - django >=1.6
    - djangorestframework >=2.3.13,<3.0
    - django-manager-utils >=0.7.2
    - django_filter >=0.7

test:
  # Python imports
  imports:
    - tour
    - tour.migrations
    - tour.south_migrations
    - tour.templatetags
    - tour.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-tour
  license: MIT License
  summary: 'Require the django user to complete a series of steps with custom logic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
