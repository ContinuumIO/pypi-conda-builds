package:
  name: djangochurch-data
  version: "0.1"

source:
  fn: djangochurch-data-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangochurch-data/djangochurch-data-0.1.tar.gz
  md5: b5bf3aa47dd3bc6abd82d2508ea1f026
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangochurch-data = djangochurch-data:main
    #
    # Would create an entry point called djangochurch-data that calls djangochurch-data.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangochurch_data
    - djangochurch_data.management
    - djangochurch_data.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djangochurch/djangochurch-data
  license: BSD License
  summary: 'Initial data for Django Church projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
