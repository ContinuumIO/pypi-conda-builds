package:
  name: collective.securitycleanup
  version: "0.3"

source:
  fn: collective.securitycleanup-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.securitycleanup/collective.securitycleanup-0.3.tar.gz
  md5: 3e264a1cd349a9ec19c95f30f280834b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.securitycleanup = collective.securitycleanup:main
    #
    # Would create an entry point called collective.securitycleanup that calls collective.securitycleanup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.genericsetup

  run:
    - python
    - setuptools
    - setuptools
    - products.genericsetup

test:
  # Python imports
  imports:
    - collective
    - collective.securitycleanup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.securitycleanup
  license: GPL
  summary: 'GenericSetup handlers to restore Zope security to defaults'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
