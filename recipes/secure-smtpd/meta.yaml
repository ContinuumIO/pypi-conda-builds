package:
  name: secure-smtpd
  version: "3.0.0"

source:
  fn: secure-smtpd-3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/secure-smtpd/secure-smtpd-3.0.0.tar.gz
  md5: 57dc3f6e25d3f7085090474bce325e65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secure-smtpd = secure-smtpd:main
    #
    # Would create an entry point called secure-smtpd that calls secure-smtpd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse

  run:
    - python
    - argparse

test:
  # Python imports
  imports:
    - secure_smtpd
    - secure_smtpd.config

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bcoe/secure-smtpd
  license: ISC License (ISCL)
  summary: "Adds support for SSL, AUTH, and other goodies, to Petri Lehtinen's SMTPD library."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
