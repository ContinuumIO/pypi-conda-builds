package:
  name: linkexchange.web.py
  version: "0.1"

source:
  fn: LinkExchange.web.py-0.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LinkExchange.web.py/LinkExchange.web.py-0.1.tar.gz
  md5: c8ef3947444fb6fc97bd6546d0e028b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkexchange.web.py = linkexchange.web.py:main
    #
    # Would create an entry point called linkexchange.web.py that calls linkexchange.web.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - linkexchange >=0.4dev
    - web.py

  run:
    - python
    - linkexchange >=0.4dev
    - web.py

test:
  # Python imports
  imports:
    - linkexchange
    - linkexchange.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://linkexchange.org.ua
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'web.py integration with LinkExchange library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
