package:
  name: django-ajax-messages
  version: "0.2.0"

source:
  fn: django-ajax-messages-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-messages/django-ajax-messages-0.2.0.tar.gz
  md5: c7256970c0d065b824423e197eec84ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-messages = django-ajax-messages:main
    #
    # Would create an entry point called django-ajax-messages that calls django-ajax-messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajaxmessages
    - ajaxmessages/migrations
    - ajaxmessages/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tomchristie/django-ajax-messages/
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
