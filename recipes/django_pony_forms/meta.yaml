package:
  name: django_pony_forms
  version: "0.3.8"

source:
  fn: django_pony_forms-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django_pony_forms/django_pony_forms-0.3.8.tar.gz
  md5: b52913f33afd1c03413cdecc686cb042
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_pony_forms = django_pony_forms:main
    #
    # Would create an entry point called django_pony_forms that calls django_pony_forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - django_pony_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mbraak/django_pony_forms
  license: Apache Software License
  summary: 'Django-pony-forms helps you to write better html for your Django forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
