package:
  name: tapioca-mandrill
  version: "0.2.1"

source:
  fn: tapioca-mandrill-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tapioca-mandrill/tapioca-mandrill-0.2.1.tar.gz
  md5: 4e071c29bb60045431f203828f3c61c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tapioca-mandrill = tapioca-mandrill:main
    #
    # Would create an entry point called tapioca-mandrill that calls tapioca-mandrill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tapioca-wrapper ==0.2.1

  run:
    - python
    - tapioca-wrapper ==0.2.1

test:
  # Python imports
  imports:
    - tapioca_mandrill

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vintasoftware/tapioca-mandrill
  license: BSD License
  summary: 'Mandrill API wrappper using Tapioca'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
