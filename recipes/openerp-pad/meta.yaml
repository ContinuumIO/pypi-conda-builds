package:
  name: openerp-pad
  version: "7.0.406"

source:
  fn: openerp-pad-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-pad/openerp-pad-7.0.406.tar.gz
  md5: 94aa8605e7a830a369c9e299730e0998
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-pad = openerp-pad:main
    #
    # Would create an entry point called openerp-pad that calls openerp-pad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-core

  run:
    - python
    - openerp-core

test:
  # Python imports
  imports:
    - openerp.addons.pad
    - openerp.addons.pad.py_etherpad

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://openerp.com
  license: GNU Affero General Public License v3
  summary: 'Collaborative Pads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
