package:
  name: django-calendar-sms
  version: "2.2"

source:
  fn: django-calendar-sms-2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-calendar-sms/django-calendar-sms-2.2.tar.gz
  md5: 199d1f0e5f8404b73059d5430a0bf8d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-calendar-sms = django-calendar-sms:main
    #
    # Would create an entry point called django-calendar-sms that calls django-calendar-sms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata

  run:
    - python
    - setuptools
    - gdata

test:
  # Python imports
  imports:
    - calendar_sms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gotlium/django-calendar-sms
  license: GNU General Public License (GPL)
  summary: 'This reusable Django app can help you to send sms via Google Calendar.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
