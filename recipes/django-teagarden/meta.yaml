package:
  name: django-teagarden
  version: "0.0.1"

source:
  fn: django-teagarden-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-teagarden/django-teagarden-0.0.1.tar.gz
  md5: 620bf13b1d3b69c5397ef40059134cc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-teagarden = django-teagarden:main
    #
    # Would create an entry point called django-teagarden that calls django-teagarden.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3

  run:
    - python
    - django ==1.3

test:
  # Python imports
  imports:
    - teagarden
    - teagarden.templatetags
    - teagarden.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-teagarden.googlecode.com
  license: GNU General Public License (GPL)
  summary: 'Database planning, visualization and code generation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
