package:
  name: zope2.zodbbrowser
  version: "0.2"

source:
  fn: zope2.zodbbrowser-0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zope2.zodbbrowser/zope2.zodbbrowser-0.2.tar.gz
  md5: 147a1645dc89af0cd84abbcdd04eee60
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope2.zodbbrowser = zope2.zodbbrowser:main
    #
    # Would create an entry point called zope2.zodbbrowser that calls zope2.zodbbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pygments

  run:
    - python
    - setuptools
    - setuptools
    - pygments

test:
  # Python imports
  imports:
    - zope2
    - zope2.zodbbrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/zodbbrowser
  license: GPL
  summary: 'A zodb browser for Zope2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
