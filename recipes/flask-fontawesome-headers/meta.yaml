package:
  name: flask-fontawesome-headers
  version: "0.1.0"

source:
  fn: Flask-FontAwesome-Headers-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-FontAwesome-Headers/Flask-FontAwesome-Headers-0.1.0.tar.gz
  md5: 04b91ea1db07c6c86337067758fca890
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-fontawesome-headers = flask-fontawesome-headers:main
    #
    # Would create an entry point called flask-fontawesome-headers that calls flask-fontawesome-headers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jaza/flask-fontawesome-headers
  license: UNKNOWN
  summary: "Sets CORS HTTP response headers for Font-Awesome files served by Flask's send_file()."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
