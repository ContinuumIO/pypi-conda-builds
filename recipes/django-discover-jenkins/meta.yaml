package:
  name: django-discover-jenkins
  version: "0.1.3"

source:
  fn: django-discover-jenkins-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-discover-jenkins/django-discover-jenkins-0.1.3.tar.gz
  md5: 6a6b9857cb930868d9f1e28c3d90fd34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-discover-jenkins = django-discover-jenkins:main
    #
    # Would create an entry point called django-discover-jenkins that calls django-discover-jenkins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - discover_jenkins
    - discover_jenkins.tasks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lincolnloop/django-discover-jenkins
  license: BSD License
  summary: 'A minimal fork of django-jenkins designed to work with the discover runner, made with simplicity in mind'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
