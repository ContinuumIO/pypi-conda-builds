package:
  name: zanpakuto
  version: "0.2"

source:
  fn: zanpakuto-0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zanpakuto/zanpakuto-0.2.tar.gz
  md5: 9cd43812465c6bc84bfe6156d596170f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zanpakuto = zanpakuto:main
    #
    # Would create an entry point called zanpakuto that calls zanpakuto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml ==3.2.3
    - six ==1.4.1

  run:
    - python
    - lxml ==3.2.3
    - six ==1.4.1

test:
  # Python imports
  imports:
    - zanpakuto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amferraz/zanpakuto
  license: UNKNOWN
  summary: 'A small utility to simplify HTML excerpts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
