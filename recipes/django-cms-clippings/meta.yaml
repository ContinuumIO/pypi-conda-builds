package:
  name: django-cms-clippings
  version: "0.1"

source:
  fn: django-cms-clippings-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-clippings/django-cms-clippings-0.1.tar.gz
  md5: a8219545e3edf527715d94163094ccc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-clippings = django-cms-clippings:main
    #
    # Would create an entry point called django-cms-clippings that calls django-cms-clippings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms

  run:
    - python
    - django-cms

test:
  # Python imports
  imports:
    - clippings
    - clippings.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-cms-clippings/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Include editable content in applications integrated with django-cms sites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
