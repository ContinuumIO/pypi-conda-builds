package:
  name: edw.recipe.responsecheck
  version: "1.0"

source:
  fn: edw.recipe.responsecheck-1.0.zip
  url: https://pypi.python.org/packages/source/e/edw.recipe.responsecheck/edw.recipe.responsecheck-1.0.zip
  md5: 3656561cc0ed6574c6acad711a404669
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edw.recipe.responsecheck = edw.recipe.responsecheck:main
    #
    # Would create an entry point called edw.recipe.responsecheck that calls edw.recipe.responsecheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - edw
    - edw.recipe
    - edw.recipe.responsecheck
    - edw.recipe.responsecheck.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eaudeweb/edw.recipe.responsecheck
  license: GNU General Public License (GPL)
  summary: 'A recipe that generates bash script for testing connectivity to application servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
