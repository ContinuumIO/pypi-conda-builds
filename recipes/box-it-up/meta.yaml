package:
  name: box-it-up
  version: "0.0.3"

source:
  fn: box-it-up-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/box-it-up/box-it-up-0.0.3.tar.gz
  md5: ca69c0e3de2d221ef4b03c6695902bd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - box-it-up = box-it-up:main
    #
    # Would create an entry point called box-it-up that calls box-it-up.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - box_it_up

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rpappalax/box-it-up
  license: UNKNOWN
  summary: 'Python class for formatting various kinds of table data into an ascii table.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
