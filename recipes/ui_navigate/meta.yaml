package:
  name: ui_navigate
  version: "1.0.2"

source:
  fn: ui_navigate-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ui_navigate/ui_navigate-1.0.2.tar.gz
  md5: cbcda4fa4b2d153107b87d73bd94eb2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ui_navigate = ui_navigate:main
    #
    # Would create an entry point called ui_navigate that calls ui_navigate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ui_navigate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RedHatQE/ui_navigate
  license: PSF
  summary: 'UI navigation library for automation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
