package:
  name: git-repo-hooks
  version: "1.01.004"

source:
  fn: git-repo-hooks-1.01.004.tar.gz
  url: https://pypi.python.org/packages/source/g/git-repo-hooks/git-repo-hooks-1.01.004.tar.gz
  md5: 0a3bc2376e0a5e6ab5a63f3dcd49eccd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-repo-hooks = git-repo-hooks:main
    #
    # Would create an entry point called git-repo-hooks that calls git-repo-hooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kjlib

  run:
    - python
    - kjlib

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/legion0/git-repo-hooks
  license: UNKNOWN
  summary: 'A tool to manage project, user, and global Git hooks for multiple git repositories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
