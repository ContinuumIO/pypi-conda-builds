package:
  name: helga-facts
  version: "0.1.0"

source:
  fn: helga-facts-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-facts/helga-facts-0.1.0.tar.gz
  md5: 041c1a59b4f49ef18f3651a4dee81835
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-facts = helga-facts:main
    #
    # Would create an entry point called helga-facts that calls helga-facts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - setuptools
    - pytz

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-facts
  license: MIT License
  summary: 'A helga plugin that can be used to store responses that can be returned from a question'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
