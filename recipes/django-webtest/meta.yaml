package:
  name: django-webtest
  version: "1.7.8"

source:
  fn: django-webtest-1.7.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webtest/django-webtest-1.7.8.tar.gz
  md5: 9eeb4896106736ec1b735e7073465a1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webtest = django-webtest:main
    #
    # Would create an entry point called django-webtest that calls django-webtest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webtest >=1.3.3

  run:
    - python
    - webtest >=1.3.3

test:
  # Python imports
  imports:
    - django_webtest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kmike/django-webtest/
  license: MIT License
  summary: " Instant integration of Ian Bicking's WebTest\n(http://webtest.pythonpaste.org/) with django's testing framework."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
