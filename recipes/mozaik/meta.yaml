package:
  name: mozaik
  version: "0.1.0"

source:
  fn: mozaik-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mozaik/mozaik-0.1.0.tar.gz
  md5: b68b8b4d8cdca7f87da215f244277dbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozaik = mozaik:main
    #
    # Would create an entry point called mozaik that calls mozaik.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mozaik
    - mozaik.analysis
    - mozaik.connectors
    - mozaik.experiments
    - mozaik.meta_workflow
    - mozaik.models
    - mozaik.models.vision
    - mozaik.sheets
    - mozaik.stimuli
    - mozaik.stimuli.vision
    - mozaik.storage
    - mozaik.tools
    - mozaik.visualization

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://neuralensemble.org/mozaik
  license: CeCILL http://www.cecill.info
  summary: 'Python package mozaik is an integrated workflow framework for large scale neural simulations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
