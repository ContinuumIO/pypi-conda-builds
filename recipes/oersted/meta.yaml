package:
  name: oersted
  version: "1.2.0"

source:
  fn: oersted-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oersted/oersted-1.2.0.tar.gz
  md5: 4f88e3b674471275dad92d235f86e6e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oersted = oersted:main
    #
    # Would create an entry point called oersted that calls oersted.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - oersted

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pcsol.be
  license: GNU General Public License (GPL)
  summary: 'Module to access OpenERP objects pythonically'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
