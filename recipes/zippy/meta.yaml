package:
  name: zippy
  version: "0.2"

source:
  fn: zippy-0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zippy/zippy-0.2.tar.gz
  md5: 1f51dd918b65f1c7f64aef0a6b475615
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zippy = zippy:main
    #
    # Would create an entry point called zippy that calls zippy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock
    - requests
    - simplejson

  run:
    - python
    - mock
    - requests
    - simplejson

test:
  # Python imports
  imports:
    - zippy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zachwill/zippy
  license: MIT License
  summary: 'A simple wrapper for the Ziptastic ZIPCode Search API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
