package:
  name: django-sse-wrapper
  version: "0.0.3"

source:
  fn: django-sse-wrapper-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sse-wrapper/django-sse-wrapper-0.0.3.tar.gz
  md5: cae572439aab9597ac5bd7f135736c35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sse-wrapper = django-sse-wrapper:main
    #
    # Would create an entry point called django-sse-wrapper that calls django-sse-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.5
    - sse >=1.2

  run:
    - python
    - django >=1.2.5
    - sse >=1.2

test:
  # Python imports
  imports:
    - sse_wrapper
    - sse_wrapper.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gfronza/django-sse-wrapper
  license: BSD
  summary: 'A simple wrapper of the sse python implementation for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
