package:
  name: django-images
  version: "0.4.2"

source:
  fn: django-images-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-images/django-images-0.4.2.tar.gz
  md5: f234c72fa14fa87e19825073b5b1b21c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-images = django-images:main
    #
    # Would create an entry point called django-images that calls django-images.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - pillow >=1.7.8

  run:
    - python
    - django >=1.3
    - pillow >=1.7.8

test:
  # Python imports
  imports:
    - django_images
    - django_images.migrations
    - django_images.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A database-driven thumbnailing solution for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
