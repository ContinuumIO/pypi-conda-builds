package:
  name: mongodbforms
  version: "0.3"

source:
  fn: mongodbforms-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mongodbforms/mongodbforms-0.3.tar.gz
  md5: e4d1ffb6d22b04efb1d4e2e405cddaa5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongodbforms = mongodbforms:main
    #
    # Would create an entry point called mongodbforms that calls mongodbforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - mongoengine >=0.8.3

  run:
    - python
    - setuptools
    - django >=1.4
    - mongoengine >=0.8.3

test:
  # Python imports
  imports:
    - mongodbforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.schafproductions.com/projects/django-mongodb-forms/
  license: BSD License
  summary: 'An implementation of django forms using mongoengine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
