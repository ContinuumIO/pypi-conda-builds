package:
  name: django-anonymizer
  version: "0.5"

source:
  fn: django-anonymizer-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-anonymizer/django-anonymizer-0.5.tar.gz
  md5: c009b71c634be4198930f47875584479
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-anonymizer = django-anonymizer:main
    #
    # Would create an entry point called django-anonymizer that calls django-anonymizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - faker >=0.0.4
    - django >=1.4

  run:
    - python
    - faker >=0.0.4
    - django >=1.4

test:
  # Python imports
  imports:
    - anonymizer
    - anonymizer.management
    - anonymizer.management.commands
    - anonymizer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/spookylukey/django-anonymizer/
  license: MIT License
  summary: 'App to anonymize data in Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
