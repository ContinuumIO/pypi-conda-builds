package:
  name: mr.awsome.openvz
  version: "1.0b2"

source:
  fn: mr.awsome.openvz-1.0b2.zip
  url: https://pypi.python.org/packages/source/m/mr.awsome.openvz/mr.awsome.openvz-1.0b2.zip
  md5: a42449b0524391571295ff51d2bfa6a2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.awsome.openvz = mr.awsome.openvz:main
    #
    # Would create an entry point called mr.awsome.openvz that calls mr.awsome.openvz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mr.awsome >=1.0rc2
    - lazy

  run:
    - python
    - setuptools
    - setuptools
    - mr.awsome >=1.0rc2
    - lazy

test:
  # Python imports
  imports:
    - mr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fschulze/mr.awsome.openvz
  license: UNKNOWN
  summary: 'A plugin for mr.awsome providing support for OpenVZ containers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
