package:
  name: fresco-genshi
  version: "0.3"

source:
  fn: fresco-genshi-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fresco-genshi/fresco-genshi-0.3.tar.gz
  md5: f70d54ef9667e2a9bb4c801287e25f00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fresco-genshi = fresco-genshi:main
    #
    # Would create an entry point called fresco-genshi that calls fresco-genshi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fresco >=0.3
    - genshi >=0.6

  run:
    - python
    - fresco >=0.3
    - genshi >=0.6

test:
  # Python imports
  imports:
    - fresco_genshi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Genshi integration for fresco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
