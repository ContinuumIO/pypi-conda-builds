package:
  name: collective.contextualcontactinfo
  version: "0.1.0"

source:
  fn: collective.contextualcontactinfo-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contextualcontactinfo/collective.contextualcontactinfo-0.1.0.tar.gz
  md5: eb91bba511276065128392febeebbfc1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contextualcontactinfo = collective.contextualcontactinfo:main
    #
    # Would create an entry point called collective.contextualcontactinfo that calls collective.contextualcontactinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contextualcontactinfo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.contextualcontactinfo
  license: GPL
  summary: 'Add the information about the context in contact-info message of your Plone Site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
