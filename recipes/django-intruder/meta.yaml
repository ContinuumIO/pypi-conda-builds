package:
  name: django-intruder
  version: "0.1.3"

source:
  fn: django-intruder-0.1.3.zip
  url: https://pypi.python.org/packages/source/d/django-intruder/django-intruder-0.1.3.zip
  md5: a8bbff857690e123386878c0b730560f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-intruder = django-intruder:main
    #
    # Would create an entry point called django-intruder that calls django-intruder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - intruder
    - intruder/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/paulocheque/django-intruder
  license: MIT
  summary: 'Django Intruder is a simple and unobtrusive application to intercept requests. It is useful to enable and disable features, for continuous deployment purpouses.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
