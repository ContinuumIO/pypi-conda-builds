package:
  name: redis-router
  version: "0.2"

source:
  fn: redis-router-0.2.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-router/redis-router-0.2.tar.gz
  md5: 95d88a935c7e7ab05f0411ac696589fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-router = redis-router:main
    #
    # Would create an entry point called redis-router that calls redis-router.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redis_router

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/emre/redis-router
  license: MIT
  summary: 'A redis sharding library/api for your sharding needs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
