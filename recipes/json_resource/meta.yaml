package:
  name: json_resource
  version: "0.2.11"

source:
  fn: json_resource-0.2.11.tar.gz
  url: https://pypi.python.org/packages/source/j/json_resource/json_resource-0.2.11.tar.gz
  md5: 1cfff90a7dac2d1dcb24734d625eaf72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_resource = json_resource:main
    #
    # Would create an entry point called json_resource that calls json_resource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - json_pointer
    - jsonschema
    - json_patch
    - pymongo

  run:
    - python
    - json_pointer
    - jsonschema
    - json_patch
    - pymongo

test:
  # Python imports
  imports:
    - json_resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
