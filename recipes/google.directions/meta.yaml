package:
  name: google.directions
  version: "0.3"

source:
  fn: google.directions-0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/google.directions/google.directions-0.3.tar.gz
  md5: 9ef59b0fa9cc3d145bbb9ba934db22c2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google.directions = google.directions:main
    #
    # Would create an entry point called google.directions that calls google.directions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - d9t.json

  run:
    - python
    - setuptools
    - setuptools
    - d9t.json

test:
  # Python imports
  imports:
    - google
    - google.directions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://d9t.de/os
  license: GPLv3
  summary: 'A python api to google directions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
