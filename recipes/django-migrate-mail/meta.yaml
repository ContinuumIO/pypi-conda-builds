package:
  name: django-migrate-mail
  version: "0.2.1"

source:
  fn: django-migrate-mail-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-migrate-mail/django-migrate-mail-0.2.1.tar.gz
  md5: 946f07a8c040bdab42ccd99f44ec2115
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-migrate-mail = django-migrate-mail:main
    #
    # Would create an entry point called django-migrate-mail that calls django-migrate-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - south >=0.7
    - pytils >=0.2

  run:
    - python
    - django >=1.2
    - south >=0.7
    - pytils >=0.2

test:
  # Python imports
  imports:
    - la_facebook
    - la_facebook.callbacks
    - la_facebook.templatetags
    - la_facebook.tests
    - la_facebook.utils
    - migrate_mail
    - migrate_mail.migrations
    - migrate_mail.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/msayapin/gmailcopy
  license: UNKNOWN
  summary: 'Copy Gmail messages to another Gmail account.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
