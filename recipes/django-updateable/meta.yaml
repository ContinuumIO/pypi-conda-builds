package:
  name: django-updateable
  version: "0.2.1"

source:
  fn: django-updateable-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-updateable/django-updateable-0.2.1.tar.gz
  md5: b37491b9ee120ade51af8314e7df35a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-updateable = django-updateable:main
    #
    # Would create an entry point called django-updateable that calls django-updateable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - updateable
    - updateable.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/baldurthoremilsson/django-updateable
  license: BSD License
  summary: 'Automatic updates for portions of templates with {% updateable %} tag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
