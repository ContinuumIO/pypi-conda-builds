package:
  name: httpie-oauth
  version: "1.0.2"

source:
  fn: httpie-oauth-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/httpie-oauth/httpie-oauth-1.0.2.tar.gz
  md5: 5644a00d4648a0d2b8af7c3fcbe4e36c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpie-oauth = httpie-oauth:main
    #
    # Would create an entry point called httpie-oauth that calls httpie-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httpie >=0.7.0
    - requests-oauthlib >=0.3.2

  run:
    - python
    - setuptools
    - httpie >=0.7.0
    - requests-oauthlib >=0.3.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jkbr/httpie-oauth
  license: BSD License
  summary: 'OAuth plugin for HTTPie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
