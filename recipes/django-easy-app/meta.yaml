package:
  name: django-easy-app
  version: "0.0.27"

source:
  fn: django-easy-app-0.0.27.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-app/django-easy-app-0.0.27.tar.gz
  md5: ba0fb92eb7799262671fe3535c7ba747
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-app = django-easy-app:main
    #
    # Would create an entry point called django-easy-app that calls django-easy-app.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_easy_app
    - django_easy_app.management
    - django_easy_app.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dwighthubbard/django-easy-app
  license: BSD License
  summary: 'Extend django-admin to create apps that are easier to set'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
