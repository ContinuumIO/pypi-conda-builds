package:
  name: leap.auth
  version: "0.1.2"

source:
  fn: leap.auth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/leap.auth/leap.auth-0.1.2.tar.gz
  md5: 677cb7263c8269677a54dcc65219ae23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leap.auth = leap.auth:main
    #
    # Would create an entry point called leap.auth that calls leap.auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - srp
    - requests

  run:
    - python
    - srp
    - requests

test:
  # Python imports
  imports:
    - leap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pixelated-project/leap_auth
  license: GNU Affero General Public License v3
  summary: 'Library to authenticate with a LEAP platform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
