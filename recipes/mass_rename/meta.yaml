package:
  name: mass_rename
  version: "0.0.1"

source:
  fn: mass_rename-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mass_rename/mass_rename-0.0.1.tar.gz
  md5: 180ea836ed61fd0bad71cb5ec7f52dbd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mass_rename = mass_rename:main
    #
    # Would create an entry point called mass_rename that calls mass_rename.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mass_rename

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlesso/mass_rename
  license: LICENSE.txt
  summary: 'Files group rename tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
