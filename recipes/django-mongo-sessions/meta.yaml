package:
  name: django-mongo-sessions
  version: "0.0.3"

source:
  fn: django-mongo-sessions-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mongo-sessions/django-mongo-sessions-0.0.3.tar.gz
  md5: 8616cf5ac78eb3e672e1c22945e26afe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mongo-sessions = django-mongo-sessions:main
    #
    # Would create an entry point called django-mongo-sessions that calls django-mongo-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pymongo >=2.4.2

  run:
    - python
    - django >=1.4
    - pymongo >=2.4.2

test:
  # Python imports
  imports:
    - mongo_sessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hellysmile/django-mongo-sessions
  license: Apache Software License
  summary: 'mongodb as Django sessions backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
