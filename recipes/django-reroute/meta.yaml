package:
  name: django-reroute
  version: "1.1.1"

source:
  fn: django-reroute-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reroute/django-reroute-1.1.1.tar.gz
  md5: d075fa9cf139dbc0067cee905ab5f6b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reroute = django-reroute:main
    #
    # Would create an entry point called django-reroute that calls django-reroute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - reroute

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dnerdy/django-reroute
  license: MIT License
  summary: 'A drop-in replacement for django.conf.urls.defaults which supports HTTP verb dispatch and view wrapping.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
