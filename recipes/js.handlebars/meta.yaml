package:
  name: js.handlebars
  version: "1.3.0"

source:
  fn: js.handlebars-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.handlebars/js.handlebars-1.3.0.tar.gz
  md5: f7a87404d43e349b60d3cee2fe70fa3e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.handlebars = js.handlebars:main
    #
    # Would create an entry point called js.handlebars that calls js.handlebars.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic

  run:
    - python
    - setuptools
    - fanstatic

test:
  # Python imports
  imports:
    - js
    - js.handlebars

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mgood/js.handlebars/
  license: BSD
  summary: 'Fanstatic package for Handlebars.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
