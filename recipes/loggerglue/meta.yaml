package:
  name: loggerglue
  version: "1.0"

source:
  fn: loggerglue-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/loggerglue/loggerglue-1.0.tar.gz
  md5: 47a970a343e158db62923d2b9029fa1c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loggerglue = loggerglue:main
    #
    # Would create an entry point called loggerglue that calls loggerglue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing

  run:
    - python
    - setuptools
    - pyparsing

test:
  # Python imports
  imports:
    - loggerglue
    - loggerglue.tests
    - loggerglue.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.evax.fr/
  license: MIT License
  summary: 'Syslog protocol (rfc5424 and rfc5425) utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
