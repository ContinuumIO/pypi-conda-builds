package:
  name: betterpath
  version: "0.2.2"

source:
  fn: betterpath-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/betterpath/betterpath-0.2.2.tar.gz
  md5: f8242ab4477ef923a4996716fffa467b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betterpath = betterpath:main
    #
    # Would create an entry point called betterpath that calls betterpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vcversioner
    - zope.interface >=4.0.0

  run:
    - python
    - vcversioner
    - zope.interface >=4.0.0

test:
  # Python imports
  imports:
    - betterpath
    - bp
    - bp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/MostAwesomeDude/betterpath
  license: MIT/X11
  summary: 'Path manipulation library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
