package:
  name: django-quickbooks-online
  version: "0.2.2"

source:
  fn: django-quickbooks-online-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quickbooks-online/django-quickbooks-online-0.2.2.tar.gz
  md5: b0a463533d3cf4c3746a14ab7cad5137
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quickbooks-online = django-quickbooks-online:main
    #
    # Would create an entry point called django-quickbooks-online that calls django-quickbooks-online.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - requests-oauthlib
    - python-keyczar ==0.71c
    - django-extensions

  run:
    - python
    - requests
    - requests-oauthlib
    - python-keyczar ==0.71c
    - django-extensions

test:
  # Python imports
  imports:
    - quickbooks
    - quickbooks.migrations
    - quickbooks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/grue/django-quickbooks-online
  license: MIT License
  summary: 'Django Quickbooks App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
