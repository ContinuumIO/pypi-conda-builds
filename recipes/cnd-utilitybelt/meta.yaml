package:
  name: cnd-utilitybelt
  version: "0.2.1"

source:
  fn: cnd-utilitybelt-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cnd-utilitybelt/cnd-utilitybelt-0.2.1.tar.gz
  md5: 5d2a95856d6b40f6e6f70683b495653c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cnd-utilitybelt = cnd-utilitybelt:main
    #
    # Would create an entry point called cnd-utilitybelt that calls cnd-utilitybelt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.6.0
    - pygeoip ==0.3.2
    - pytest >=2.6.0
    - pytest-cov >=1.8.1
    - coveralls >=0.5
    - pre-commit >=0.4.4
    - beautifulsoup4 >=4.3.2
    - netaddr >=0.7.14

  run:
    - python
    - requests >=2.6.0
    - pygeoip ==0.3.2
    - pytest >=2.6.0
    - pytest-cov >=1.8.1
    - coveralls >=0.5
    - pre-commit >=0.4.4
    - beautifulsoup4 >=4.3.2
    - netaddr >=0.7.14

test:
  # Python imports
  imports:
    - utilitybelt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yolothreat/utilitybelt
  license: MIT License
  summary: 'Utilities to make you a CND Batman'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
