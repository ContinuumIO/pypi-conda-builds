package:
  name: django-upyun-storage
  version: "0.0.1"

source:
  fn: django-upyun-storage-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-upyun-storage/django-upyun-storage-0.0.1.tar.gz
  md5: 810b877260c13f65233b7e6b5ddc8e5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-upyun-storage = django-upyun-storage:main
    #
    # Would create an entry point called django-upyun-storage that calls django-upyun-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - python-dateutil

  run:
    - python
    - requests
    - python-dateutil

test:
  # Python imports
  imports:
    - django_upyun

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/waylybaye/django-upyun
  license: MIT
  summary: 'django storage for upyun.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
