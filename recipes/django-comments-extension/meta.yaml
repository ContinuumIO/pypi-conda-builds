package:
  name: django-comments-extension
  version: "1.2.1"

source:
  fn: django-comments-extension-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-comments-extension/django-comments-extension-1.2.1.tar.gz
  md5: 98f72d393518f7155ecf1fa778f649f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-comments-extension = django-comments-extension:main
    #
    # Would create an entry point called django-comments-extension that calls django-comments-extension.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - comments_extension
    - comments_extension.templatetags
    - comments_extension.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rhblind/django-comments-extension
  license: Apache Software License
  summary: 'contrib.comments extensions. Currently only provides edit functionality.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
