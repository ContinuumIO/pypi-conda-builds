package:
  name: django-agregador
  version: "0.2.2"

source:
  fn: django-agregador-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-agregador/django-agregador-0.2.2.tar.gz
  md5: a0b0398e5732ae8cb065e4b9c6b57e20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-agregador = django-agregador:main
    #
    # Would create an entry point called django-agregador that calls django-agregador.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feedparser
    - dateutil

  run:
    - python
    - feedparser
    - dateutil

test:
  # Python imports
  imports:
    - agregador
    - agregador.management
    - agregador.management.commands
    - agregador.migrations
    - agregador.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/semente/django-agregador/
  license: BSD License
  summary: 'RSS/Atom feeds aggregator application for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
