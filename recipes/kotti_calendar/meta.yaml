package:
  name: kotti_calendar
  version: "0.8.2"

source:
  fn: kotti_calendar-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_calendar/kotti_calendar-0.8.2.tar.gz
  md5: d5b4bf97fd4723d06c96570b090aa3ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_calendar = kotti_calendar:main
    #
    # Would create an entry point called kotti_calendar that calls kotti_calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - js.fullcalendar >=2.2.5
    - kotti >=0.10b1
    - pytest-cov
    - pytest
    - webtest
    - wsgi_intercept
    - zope.testbrowser
    - webtest

  run:
    - python
    - setuptools
    - js.fullcalendar >=2.2.5
    - kotti >=0.10b1
    - pytest-cov
    - pytest
    - webtest
    - wsgi_intercept
    - zope.testbrowser
    - webtest

test:
  # Python imports
  imports:
    - kotti_calendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/kotti_calendar
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Add a calendar to your Kotti site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
