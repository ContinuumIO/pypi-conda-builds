package:
  name: gists
  version: "0.4.6"

source:
  fn: gists-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/g/gists/gists-0.4.6.tar.gz
  md5: 188034ffdc9ce3b7dcf89ebbb2d72071
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gists = gists:main
    #
    # Would create an entry point called gists that calls gists.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==0.14.0
    - clint ==0.3.1

  run:
    - python
    - requests ==0.14.0
    - clint ==0.3.1

test:
  # Python imports
  imports:
    - gists

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jdevesa.github.com/gists
  license: MIT License
  summary: 'CLI interface to manage Github gists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
