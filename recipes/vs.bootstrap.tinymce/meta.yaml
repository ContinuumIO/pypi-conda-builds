package:
  name: vs.bootstrap.tinymce
  version: "1.0.3"

source:
  fn: vs.bootstrap.tinymce-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/v/vs.bootstrap.tinymce/vs.bootstrap.tinymce-1.0.3.tar.gz
  md5: ea80a32e074b96d5236c2ff88f7c68fd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vs.bootstrap.tinymce = vs.bootstrap.tinymce:main
    #
    # Would create an entry point called vs.bootstrap.tinymce that calls vs.bootstrap.tinymce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - vs
    - vs.bootstrap
    - vs.bootstrap.tinymce
    - vs.bootstrap.tinymce.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/veit/vs.bootstrap.tinymce
  license: GPL version 2
  summary: 'Bootstrap css classes for TinyMCE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
