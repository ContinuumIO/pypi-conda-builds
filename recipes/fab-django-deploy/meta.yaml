package:
  name: fab-django-deploy
  version: "0.0.2"

source:
  fn: fab-django-deploy-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fab-django-deploy/fab-django-deploy-0.0.2.tar.gz
  md5: 03e47cf48bc13dc98a232f8d99e179a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fab-django-deploy = fab-django-deploy:main
    #
    # Would create an entry point called fab-django-deploy that calls fab-django-deploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.8

  run:
    - python
    - fabric >=1.8

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/fab-django-deploy/
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
