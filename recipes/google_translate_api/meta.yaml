package:
  name: google_translate_api
  version: "0.3"

source:
  fn: google_translate_api-0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/google_translate_api/google_translate_api-0.3.tar.gz
  md5: 08df21a63810e0af19f9f2ec9d3974e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google_translate_api = google_translate_api:main
    #
    # Would create an entry point called google_translate_api that calls google_translate_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - futures

  run:
    - python
    - requests
    - futures

test:
  # Python imports
  imports:
    - google_translate_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/haoxun/GoogleTranslateAPI
  license: MIT License
  summary: 'API of google translation service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
