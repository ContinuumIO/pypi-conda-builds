package:
  name: djangolightbox
  version: "0.1.2"

source:
  fn: DjangoLightbox-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoLightbox/DjangoLightbox-0.1.2.tar.gz
  md5: caae0537f9e7b6a53a588f658d5b083a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangolightbox = djangolightbox:main
    #
    # Would create an entry point called djangolightbox that calls djangolightbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - djangolightbox
    - lightbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/DjangoLightbox/
  license: LICENSE.txt
  summary: 'alternative for a full screen javascript lightbox for the django framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
