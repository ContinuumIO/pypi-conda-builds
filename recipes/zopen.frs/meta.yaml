package:
  name: zopen.frs
  version: "1.2.2"

source:
  fn: zopen.frs-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zopen.frs/zopen.frs-1.2.2.tar.gz
  md5: 6a8f8a82ffa94811899b7b6719de2099
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopen.frs = zopen.frs:main
    #
    # Would create an entry point called zopen.frs that calls zopen.frs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.datetime

  run:
    - python
    - setuptools
    - setuptools
    - zope.datetime

test:
  # Python imports
  imports:
    - zopen
    - zopen.frs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.zopen.cn/zopen.frs
  license: Private
  summary: 'zopen frs management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
