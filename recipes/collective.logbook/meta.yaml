package:
  name: collective.logbook
  version: "0.7"

source:
  fn: collective.logbook-0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.logbook/collective.logbook-0.7.tar.gz
  md5: 6157af212890ec878a620e1ba1d93853
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.logbook = collective.logbook:main
    #
    # Would create an entry point called collective.logbook that calls collective.logbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.logbook
    - collective.logbook.browser
    - collective.logbook.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.logbook
  license: GPL
  summary: 'Advanced Persistent Error Log'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
