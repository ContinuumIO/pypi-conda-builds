package:
  name: jsonext
  version: "0.4.1"

source:
  fn: jsonext-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonext/jsonext-0.4.1.tar.gz
  md5: 50c8dc87004355a8123c52aba2ee3914
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonext = jsonext:main
    #
    # Would create an entry point called jsonext that calls jsonext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - times

  run:
    - python
    - times

test:
  # Python imports
  imports:
    - jsonext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/jsonext
  license: MIT
  summary: 'Well-structured helpers to help serializing commonly encountered structures to JSON (like datetime, to_dict(),  etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
