package:
  name: arrayterator
  version: "1.0.1"

source:
  fn: arrayterator-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/arrayterator/arrayterator-1.0.1.tar.gz
  md5: 9cfa98d2d5429113d450ee47864121d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arrayterator = arrayterator:main
    #
    # Would create an entry point called arrayterator that calls arrayterator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - arrayterator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://taoetc.org/6
  license: MIT License
  summary: 'Buffered iterator for big arrays.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
