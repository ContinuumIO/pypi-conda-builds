package:
  name: localwiki-geocode-pagenames
  version: "0.1.1"

source:
  fn: localwiki-geocode-pagenames-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/localwiki-geocode-pagenames/localwiki-geocode-pagenames-0.1.1.tar.gz
  md5: 820fa83d5efc7b43e337c8f75246fb1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - localwiki-geocode-pagenames = localwiki-geocode-pagenames:main
    #
    # Would create an entry point called localwiki-geocode-pagenames that calls localwiki-geocode-pagenames.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - slumber
    - geopy

  run:
    - python
    - slumber
    - geopy

test:
  # Python imports
  imports:
    - geocode_pagenames

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/philipn/localwiki_geocode_pagenames
  license: UNKNOWN
  summary: 'A script to semi-automatically map pages in a LocalWiki instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
