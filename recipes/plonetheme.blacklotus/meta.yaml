package:
  name: plonetheme.blacklotus
  version: "1.0.0"

source:
  fn: plonetheme.blacklotus-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.blacklotus/plonetheme.blacklotus-1.0.0.tar.gz
  md5: 45b724b254319e1aa7817dae5f99b1e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.blacklotus = plonetheme.blacklotus:main
    #
    # Would create an entry point called plonetheme.blacklotus that calls plonetheme.blacklotus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plonetheme.sunburst

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plonetheme.sunburst

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.blacklotus
    - plonetheme.blacklotus.browser
    - plonetheme.blacklotus.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://seankelly.biz/products/plonetheme.blacklotus/
  license: GNU General Public License (GPL)
  summary: '"Black Lotus" theme for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
