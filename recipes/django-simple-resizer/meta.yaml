package:
  name: django-simple-resizer
  version: "0.1.4"

source:
  fn: django-simple-resizer-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-resizer/django-simple-resizer-0.1.4.tar.gz
  md5: ae74e9464196b9f4657c87ea10f82b0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-resizer = django-simple-resizer:main
    #
    # Would create an entry point called django-simple-resizer that calls django-simple-resizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wand ==0.4.0
    - pillow ==2.8.1

  run:
    - python
    - wand ==0.4.0
    - pillow ==2.8.1

test:
  # Python imports
  imports:
    - simple_resizer
    - simple_resizer.templatetags
    - simple_resizer.tests
    - simple_resizer.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zakdoek/django-simple-resizer.git
  license: BSD License
  summary: 'A simple image resizer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
