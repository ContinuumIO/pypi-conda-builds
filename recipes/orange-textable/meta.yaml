package:
  name: orange-textable
  version: "1.5.1"

source:
  fn: Orange-Textable-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/O/Orange-Textable/Orange-Textable-1.5.1.tar.gz
  md5: 95b9d0937472720448b3f75c23749d35
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - orange-textable = orange-textable:main
    #
    # Would create an entry point called orange-textable that calls orange-textable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - orange
    - setuptools

  run:
    - python
    - setuptools
    - orange
    - setuptools

test:
  # Python imports
  imports:
    - _textable
    - _textable.widgets
    - _textable.widgets.LTTL

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://langtech.ch/textable
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Orange Textable add-on for Orange data mining software package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
