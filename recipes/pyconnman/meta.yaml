package:
  name: pyconnman
  version: "0.1.0"

source:
  fn: pyconnman-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyconnman/pyconnman-0.1.0.tar.gz
  md5: b7fa82034b1c0e1fb1b518ffe3bb4fc0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyconnman = pyconnman:main
    #
    # Would create an entry point called pyconnman that calls pyconnman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - pyconnman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/liamw9534/pyconnman
  license: Apache Software License
  summary: u'Python-based Network Connectivity Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
