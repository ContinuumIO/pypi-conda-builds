package:
  name: jldcmds
  version: "0.3"

source:
  fn: jldcmds-0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jldcmds/jldcmds-0.3.tar.gz
  md5: a947ce31e6c8ca2937a20e0b0ed21a1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jldcmds = jldcmds:main
    #
    # Would create an entry point called jldcmds that calls jldcmds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - scapy

  run:
    - python
    - argparse
    - scapy

test:
  # Python imports
  imports:
    - jldcmds
    - jldcmds.common
    - jldcmds.network

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jldupont/jldcmds
  license: UNKNOWN
  summary: 'Collection of command line tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
