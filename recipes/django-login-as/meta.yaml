package:
  name: django-login-as
  version: "0.2"

source:
  fn: django-login-as-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-login-as/django-login-as-0.2.tar.gz
  md5: 3ab9b3a20beb05ce1503d0541946e203
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-login-as = django-login-as:main
    #
    # Would create an entry point called django-login-as that calls django-login-as.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - login_as

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-login-as
  license: UNKNOWN
  summary: 'Django Login As any user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
