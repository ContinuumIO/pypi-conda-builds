package:
  name: django-geohashing
  version: "0.2.1"

source:
  fn: django-geohashing-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-geohashing/django-geohashing-0.2.1.tar.gz
  md5: e371739680c0d2cd09cf167e9126e490
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geohashing = django-geohashing:main
    #
    # Would create an entry point called django-geohashing that calls django-geohashing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - requests
    - djangorestframework

  run:
    - python
    - django
    - requests
    - djangorestframework

test:
  # Python imports
  imports:
    - geohashing
    - geohashing.management
    - geohashing.management.commands
    - geohashing.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Django app for working with XKCD geohashing data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
