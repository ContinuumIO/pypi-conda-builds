package:
  name: zope.error
  version: "4.2.0"

source:
  fn: zope.error-4.2.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.error/zope.error-4.2.0.tar.gz
  md5: 019e43d4dcb7b022e4e7860435e6754a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.error = zope.error:main
    #
    # Would create an entry point called zope.error that calls zope.error.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.exceptions
    - zope.interface
    - zope.location
    - persistent
    - six

  run:
    - python
    - setuptools
    - zope.exceptions
    - zope.interface
    - zope.location
    - persistent
    - six

test:
  # Python imports
  imports:
    - zope
    - zope.error

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.error
  license: Zope Public License
  summary: 'An error reporting utility for Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
