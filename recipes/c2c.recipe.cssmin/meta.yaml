package:
  name: c2c.recipe.cssmin
  version: "0.6"

source:
  fn: c2c.recipe.cssmin-0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/c2c.recipe.cssmin/c2c.recipe.cssmin-0.6.tar.gz
  md5: 250aaf1e939af380138db66ad83e8fd9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2c.recipe.cssmin = c2c.recipe.cssmin:main
    #
    # Would create an entry point called c2c.recipe.cssmin that calls c2c.recipe.cssmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssmin

  run:
    - python
    - setuptools
    - cssmin

test:
  # Python imports
  imports:
    - c2c
    - c2c.recipe
    - c2c.recipe.cssmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/camptocamp/c2c.recipe.cssmin
  license: MIT License
  summary: 'A buildout recipe to merge and compress css files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
