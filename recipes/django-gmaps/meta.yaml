package:
  name: django-gmaps
  version: "0.0.4"

source:
  fn: django-gmaps-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gmaps/django-gmaps-0.0.4.tar.gz
  md5: 0f32f03829b4235473d1afd895bd9189
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gmaps = django-gmaps:main
    #
    # Would create an entry point called django-gmaps that calls django-gmaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gmaps
    - gmaps.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fcurella/django-google-maps
  license: MIT License
  summary: 'Template tags for creating Google Maps from GeoDjango fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
