package:
  name: tornado-encookie
  version: "0.0.1"

source:
  fn: tornado-encookie-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-encookie/tornado-encookie-0.0.1.tar.gz
  md5: 8fbbd9d5d3ac1e154f78dd90d7f93cec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-encookie = tornado-encookie:main
    #
    # Would create an entry point called tornado-encookie that calls tornado-encookie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - pycrypto

  run:
    - python
    - tornado
    - pycrypto

test:
  # Python imports
  imports:
    - tornadoencookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/whardier/tornado-encookie
  license: MIT License
  summary: 'Encrypted cookie support for the Tornado Web Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
