package:
  name: django-messages
  version: "0.5.1"

source:
  fn: django-messages-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-messages/django-messages-0.5.1.tar.gz
  md5: 77d6c271a959e9cdec948d092b0834dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-messages = django-messages:main
    #
    # Would create an entry point called django-messages that calls django-messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_messages
    - django_messages.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arneb/django-messages
  license: BSD License
  summary: 'User-to-user messaging system for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
