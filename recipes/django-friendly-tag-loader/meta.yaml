package:
  name: django-friendly-tag-loader
  version: "1.2"

source:
  fn: django-friendly-tag-loader-1.2.zip
  url: https://pypi.python.org/packages/source/d/django-friendly-tag-loader/django-friendly-tag-loader-1.2.zip
  md5: de694995d4b3a6c3f158cee3392c3838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-friendly-tag-loader = django-friendly-tag-loader:main
    #
    # Would create an entry point called django-friendly-tag-loader that calls django-friendly-tag-loader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - friendlytagloader
    - friendlytagloader.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jaap3/django-friendly-tag-loader/
  license: MIT License
  summary: 'Want to optionally use a template tag library? Use this!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
