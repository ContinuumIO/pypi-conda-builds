package:
  name: django-jsonify
  version: "0.3.0"

source:
  fn: django-jsonify-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsonify/django-jsonify-0.3.0.tar.gz
  md5: 0a24c603511ca8bb1200ef3515fe3688
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsonify = django-jsonify:main
    #
    # Would create an entry point called django-jsonify that calls django-jsonify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsonify
    - jsonify.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/marltu/django-jsonify/
  license: BSD License
  summary: 'Django additions for JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
