package:
  name: estimationtools
  version: "0.4.5"

source:
  fn: estimationtools-0.4.5.zip
  url: https://pypi.python.org/packages/source/E/EstimationTools/estimationtools-0.4.5.zip
  md5: 8f1ac21427466572364fd635513d4984
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - estimationtools = estimationtools:main
    #
    # Would create an entry point called estimationtools that calls estimationtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - estimationtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Trac plugin for visualizing and quick editing of effort estimations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
