package:
  name: micropython-tarfile
  version: "0.0.0"

source:
  fn: micropython-tarfile-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-tarfile/micropython-tarfile-0.0.0.tar.gz
  md5: 10065543e2d30083fbc0d3836880abb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-tarfile = micropython-tarfile:main
    #
    # Would create an entry point called micropython-tarfile that calls micropython-tarfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: MIT
  summary: 'Dummy tarfile module for MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
