package:
  name: plone.app.drafts
  version: "1.0a2"

source:
  fn: plone.app.drafts-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.drafts/plone.app.drafts-1.0a2.tar.gz
  md5: 568d38873698dbddbed25ab0ac2baa2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.drafts = plone.app.drafts:main
    #
    # Would create an entry point called plone.app.drafts that calls plone.app.drafts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - rwproperty
    - zodb3
    - zope.interface
    - zope.component
    - zope.schema
    - zope.annotation
    - plone.app.intid
    - zope2

  run:
    - python
    - setuptools
    - setuptools
    - rwproperty
    - zodb3
    - zope.interface
    - zope.component
    - zope.schema
    - zope.annotation
    - plone.app.intid
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.drafts
    - plone.app.drafts.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: GPL
  summary: 'Low-level container for draft content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
