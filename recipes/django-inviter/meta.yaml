package:
  name: django-inviter
  version: "0.2.4"

source:
  fn: django-inviter-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inviter/django-inviter-0.2.4.tar.gz
  md5: f8bb724f436c4021ac2798bb69008767
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inviter = django-inviter:main
    #
    # Would create an entry point called django-inviter that calls django-inviter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - shortuuid >=0.1

  run:
    - python
    - shortuuid >=0.1

test:
  # Python imports
  imports:
    - example
    - inviter
    - inviter.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Invite users to your Django apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
