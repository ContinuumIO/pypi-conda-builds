package:
  name: archetypes.referencebrowserwidget
  version: "2.5.3"

source:
  fn: archetypes.referencebrowserwidget-2.5.3.zip
  url: https://pypi.python.org/packages/source/a/archetypes.referencebrowserwidget/archetypes.referencebrowserwidget-2.5.3.zip
  md5: 8f0c55cdc67f9b7766c27ffebb067286
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.referencebrowserwidget = archetypes.referencebrowserwidget:main
    #
    # Would create an entry point called archetypes.referencebrowserwidget that calls archetypes.referencebrowserwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.formlib
    - plone.app.form
    - plone.app.jquerytools >=1.1b1

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.formlib
    - plone.app.form
    - plone.app.jquerytools >=1.1b1

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.referencebrowserwidget
    - archetypes.referencebrowserwidget.browser
    - archetypes.referencebrowserwidget.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/archetypes.referencebrowserwidget
  license: ZPL 2.1
  summary: 'A referencebrowser implementation for Archetypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
