package:
  name: pyruuvitracker
  version: "0.13"

source:
  fn: pyRuuviTracker-0.13.tar.gz
  url: https://pypi.python.org/packages/source/p/pyRuuviTracker/pyRuuviTracker-0.13.tar.gz
  md5: d414b0e1332be7d2244378f05aa303ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyruuvitracker = pyruuvitracker:main
    #
    # Would create an entry point called pyruuvitracker that calls pyruuvitracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3

  run:
    - python
    - requests >=1.2.3

test:
  # Python imports
  imports:
    - pyruuvitracker
    - rt_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kipe/pyRuuviTracker
  license: UNKNOWN
  summary: 'Client library for RuuviTracker in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
