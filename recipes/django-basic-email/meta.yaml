package:
  name: django-basic-email
  version: "0.0.2"

source:
  fn: django-basic-email-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-basic-email/django-basic-email-0.0.2.tar.gz
  md5: 24a5725ba8e41779fe3d58013f1b1f11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-basic-email = django-basic-email:main
    #
    # Would create an entry point called django-basic-email that calls django-basic-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - premailer >=2.8.1

  run:
    - python
    - django >=1.4.2
    - premailer >=2.8.1

test:
  # Python imports
  imports:
    - basic_email
    - example_project
    - example_project.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ArabellaTech/django-basic-email
  license: BSD License
  summary: 'Django Basic Email application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
