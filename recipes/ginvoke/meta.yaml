package:
  name: ginvoke
  version: "0.4.5"

source:
  fn: ginvoke-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/g/ginvoke/ginvoke-0.4.5.tar.gz
  md5: 684e495a6025f864d48a32fca3290147
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ginvoke = ginvoke:main
    #
    # Would create an entry point called ginvoke that calls ginvoke.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ginvoke
    - ginvoke.ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thomaslee/ginvoke
  license: GNU General Public License v3 (GPLv3)
  summary: 'A programmable QuickSilver/Gnome Do/Executor clone for GTK+'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
