package:
  name: llsd
  version: "unreleased.unofficialdev"

source:
  fn: llsd-unreleased.unofficialdev.tar.gz
  url: https://pypi.python.org/packages/source/l/llsd/llsd-unreleased.unofficialdev.tar.gz
  md5: 9bfa93b3af3c28153c85b7d65e6fcdf8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - llsd = llsd:main
    #
    # Would create an entry point called llsd that calls llsd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - llsd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.secondlife.com/wiki/LLSD
  license: MIT
  summary: 'LLSD parser and generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
