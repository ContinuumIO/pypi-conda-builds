package:
  name: asciitomathml
  version: ".89"

source:
  fn: asciitomathml-.89.tar.gz
  url: https://pypi.python.org/packages/source/a/asciitomathml/asciitomathml-.89.tar.gz
  md5: 2a2acacb0caa8fd4a1015f321221049d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asciitomathml = asciitomathml:main
    #
    # Would create an entry point called asciitomathml that calls asciitomathml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - asciitomathml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://sourceforge.net/projects/asciimathpython/
  license: BSD License
  summary: 'Module converts ASCII math to Mathml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
