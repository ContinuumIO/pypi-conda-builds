package:
  name: ed25519ll
  version: "0.6"

source:
  fn: ed25519ll-0.6.tar.gz
  url: https://pypi.python.org/packages/source/e/ed25519ll/ed25519ll-0.6.tar.gz
  md5: 35b3190ffefb631e7c5a45d96d768f80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ed25519ll = ed25519ll:main
    #
    # Would create an entry point called ed25519ll that calls ed25519ll.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ed25519ll

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

requires:
    - nose
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dholth/ed25519ll/
  license: MIT
  summary: 'A low-level ctypes wrapper for Ed25519 digital signatures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
