package:
  name: wtalchemy
  version: "0.1b3"

source:
  fn: WTAlchemy-0.1b3.tar.gz
  url: https://pypi.python.org/packages/source/W/WTAlchemy/WTAlchemy-0.1b3.tar.gz
  md5: 95ed598d577b10bc4c638927a3f083c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtalchemy = wtalchemy:main
    #
    # Would create an entry point called wtalchemy that calls wtalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms ==0.6.3
    - sqlalchemy

  run:
    - python
    - wtforms ==0.6.3
    - sqlalchemy

test:
  # Python imports
  imports:
    - wtalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jean-philippe/WTAlchemy
  license: MIT License
  summary: 'WTForms SQAlchemy extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
