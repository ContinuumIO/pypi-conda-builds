package:
  name: django-multifilefield
  version: "1.0"

source:
  fn: django-multifilefield-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multifilefield/django-multifilefield-1.0.tar.gz
  md5: 2e3b55679083f4143873c22b650b48ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multifilefield = django-multifilefield:main
    #
    # Would create an entry point called django-multifilefield that calls django-multifilefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - multifilefield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Cyberic/django-multifilefield
  license: UNKNOWN
  summary: 'Django model and form fields for storring html5 forms multiupload data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
