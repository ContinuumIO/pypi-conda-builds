package:
  name: lizard-structure
  version: "0.1"

source:
  fn: lizard-structure-0.1.zip
  url: https://pypi.python.org/packages/source/l/lizard-structure/lizard-structure-0.1.zip
  md5: 8c300fef9e1da399316bedeef9d3c13f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lizard-structure = lizard-structure:main
    #
    # Would create an entry point called lizard-structure that calls lizard-structure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-nose
    - djangorestframework >=2.0
    - south

  run:
    - python
    - django
    - django-nose
    - djangorestframework >=2.0
    - south

test:
  # Python imports
  imports:
    - lizard_structure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lizardsystem/lizard-structure
  license: GPL
  summary: 'Structure of Lizard, defined and documented in a REST interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
