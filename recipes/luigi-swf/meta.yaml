package:
  name: luigi-swf
  version: "0.12.3"

source:
  fn: luigi-swf-0.12.3.tar.gz
  url: https://pypi.python.org/packages/source/l/luigi-swf/luigi-swf-0.12.3.tar.gz
  md5: c8b09e69ac36794c8aca21b967c7d995
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - luigi-swf = luigi-swf:main
    #
    # Would create an entry point called luigi-swf that calls luigi-swf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.28.0
    - luigi >=1.0.16
    - pidfile >=0.1.0
    - python-daemon >=1.6.1
    - six >=1.6.1

  run:
    - python
    - boto >=2.28.0
    - luigi >=1.0.16
    - pidfile >=0.1.0
    - python-daemon >=1.6.1
    - six >=1.6.1

test:
  # Python imports
  imports:
    - luigi_swf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RUNDSP/luigi_swf
  license: Apache Software License
  summary: "Spotify's Luigi + Amazon's Simple Workflow Service integration"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
