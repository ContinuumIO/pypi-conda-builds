package:
  name: django-pytds
  version: "1.5"

source:
  fn: django-pytds-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pytds/django-pytds-1.5.tar.gz
  md5: 394e6d0d0a58bf31c5cfd203484f3dc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pytds = django-pytds:main
    #
    # Would create an entry point called django-pytds that calls django-pytds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-tds >=1.3.0

  run:
    - python
    - python-tds >=1.3.0

test:
  # Python imports
  imports:
    - django_pytds
    - django_pytds.models
    - django_pytds.sql_app
    - django_pytds.sql_app.management
    - django_pytds.sql_app.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/denisenkom/django-pytds
  license: MIT
  summary: 'Django backend database support for MS SQL 2005 and up.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
