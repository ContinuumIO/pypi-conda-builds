package:
  name: authlog
  version: "0.7"

source:
  fn: Authlog-0.7.tar.gz
  url: https://pypi.python.org/packages/source/A/Authlog/Authlog-0.7.tar.gz
  md5: a6a4be71631d835c85310361738c66cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authlog = authlog:main
    #
    # Would create an entry point called authlog that calls authlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - authlog
    - authlog.management
    - authlog.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.imagescape.com
  license: UNKNOWN
  summary: 'Log Django Authentications and Admin page visits.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
