package:
  name: circlator
  version: "0.2.0"

source:
  fn: circlator-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/circlator/circlator-0.2.0.tar.gz
  md5: a3ca78e8dc2900a9f67c29431454262f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circlator = circlator:main
    #
    # Would create an entry point called circlator that calls circlator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose >=1.3
    - openpyxl
    - pyfastaq >=3.3.0
    - pysam >=0.8.1
    - pymummer >=0.2.0
    - bio_assembly_refinement >=0.1.0

  run:
    - python
    - nose >=1.3
    - openpyxl
    - pyfastaq >=3.3.0
    - pysam >=0.8.1
    - pymummer >=0.2.0
    - bio_assembly_refinement >=0.1.0

test:
  # Python imports
  imports:
    - circlator
    - circlator.tasks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sanger-pathogens/circlator
  license: GNU General Public License v3 (GPLv3)
  summary: 'circlator: a tool to circularise bacterial genome assemblies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
