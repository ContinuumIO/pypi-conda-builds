package:
  name: products.userfield
  version: "3.0"

source:
  fn: Products.UserField-3.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.UserField/Products.UserField-3.0.tar.gz
  md5: 595f623fd002f2894df6868ba4267dc0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.userfield = products.userfield:main
    #
    # Would create an entry point called products.userfield that calls products.userfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.userandgroupselectionwidget >=3.0b

  run:
    - python
    - setuptools
    - products.userandgroupselectionwidget >=3.0b

test:
  # Python imports
  imports:
    - Products
    - Products.UserField

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/archetypes/MoreFieldsAndWidgets/UserField
  license: BSD License
  summary: 'Archetypes Field dealing with Plone Users and assign local roles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
