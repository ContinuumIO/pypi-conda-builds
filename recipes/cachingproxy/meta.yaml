package:
  name: cachingproxy
  version: "0.1"

source:
  fn: cachingproxy-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cachingproxy/cachingproxy-0.1.tar.gz
  md5: 1d80fc5c17c44252f65e368c62eec25e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cachingproxy = cachingproxy:main
    #
    # Would create an entry point called cachingproxy that calls cachingproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cachingproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zyga/cachingproxy
  license: UNKNOWN
  summary: 'CachingProxy magically records usage of arbitrary objects and allows you to replay the same behavior later (with serializable representation that can be stored on disk)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
