package:
  name: sphinx-readable-theme
  version: "1.3.0"

source:
  fn: sphinx-readable-theme-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-readable-theme/sphinx-readable-theme-1.3.0.tar.gz
  md5: 57e396552315d3c8034874c9eaef160f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-readable-theme = sphinx-readable-theme:main
    #
    # Would create an entry point called sphinx-readable-theme that calls sphinx-readable-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sphinx_readable_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ignacysokolowski/sphinx-readable-theme
  license: MIT License
  summary: 'Sphinx Readable Theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
