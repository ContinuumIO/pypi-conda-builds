package:
  name: gistore
  version: "0.3.2"

source:
  fn: gistore-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gistore/gistore-0.3.2.tar.gz
  md5: 7959918d3169617dac7569b86eb1f787
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gistore = gistore:main
    #
    # Would create an entry point called gistore that calls gistore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gistore
    - gistore.scm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://open.ossxp.com/
  license: GNU General Public License (GPL)
  summary: 'Backup system using DVCS backend, such as git'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
