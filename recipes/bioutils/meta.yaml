package:
  name: bioutils
  version: "0.0.2"

source:
  fn: bioutils-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bioutils/bioutils-0.0.2.tar.gz
  md5: 9df57aee74683f33242bdc009fb8287c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bioutils = bioutils:main
    #
    # Would create an entry point called bioutils that calls bioutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bioutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/uta/bioutils
  license: Apache Software License
  summary: 'miscellaneous simple bioinformatics utilities and lookup tables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
