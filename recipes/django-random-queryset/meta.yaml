package:
  name: django-random-queryset
  version: "0.0.1"

source:
  fn: django-random-queryset-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-random-queryset/django-random-queryset-0.0.1.tar.gz
  md5: 7f1ba88b6a3d7f30eeb4fd467cef07db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-random-queryset = django-random-queryset:main
    #
    # Would create an entry point called django-random-queryset that calls django-random-queryset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2 ==2.5.4
    - django >=1.5

  run:
    - python
    - psycopg2 ==2.5.4
    - django >=1.5

test:
  # Python imports
  imports:
    - django_random_queryset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rremizov/django-random-queryset
  license: MIT License
  summary: "The extension gives you ability to pull random records using Django's ORM."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
