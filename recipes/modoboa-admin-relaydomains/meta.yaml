package:
  name: modoboa-admin-relaydomains
  version: "1.0.1"

source:
  fn: modoboa-admin-relaydomains-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/modoboa-admin-relaydomains/modoboa-admin-relaydomains-1.0.1.tar.gz
  md5: 9b7ecd134502261c149f29fde873df07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modoboa-admin-relaydomains = modoboa-admin-relaydomains:main
    #
    # Would create an entry point called modoboa-admin-relaydomains that calls modoboa-admin-relaydomains.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - modoboa-admin

  run:
    - python
    - modoboa-admin

test:
  # Python imports
  imports:
    - modoboa_admin_relaydomains
    - modoboa_admin_relaydomains.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://modoboa.org/
  license: MIT License
  summary: 'Relay domains support for Modoboa'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
