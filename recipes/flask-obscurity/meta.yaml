package:
  name: flask-obscurity
  version: "0.4"

source:
  fn: Flask-Obscurity-0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Obscurity/Flask-Obscurity-0.4.tar.gz
  md5: 118d12c2f53160370efa453ad030f07b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-obscurity = flask-obscurity:main
    #
    # Would create an entry point called flask-obscurity that calls flask-obscurity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - six >=1.5.2

  run:
    - python
    - flask >=0.8
    - six >=1.5.2

test:
  # Python imports
  imports:
    - flask_obscurity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/flask-obscurity
  license: BSD License
  summary: 'Security-by-obscurity. Move along, nothing to see here.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
