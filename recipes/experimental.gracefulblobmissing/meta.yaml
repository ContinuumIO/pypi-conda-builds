package:
  name: experimental.gracefulblobmissing
  version: "0.4.0"

source:
  fn: experimental.gracefulblobmissing-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/e/experimental.gracefulblobmissing/experimental.gracefulblobmissing-0.4.0.tar.gz
  md5: 469db626fa4970a9bc5e98816feaaa9d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.gracefulblobmissing = experimental.gracefulblobmissing:main
    #
    # Would create an entry point called experimental.gracefulblobmissing that calls experimental.gracefulblobmissing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher >=1.0
    - plone.app.blob
    - sh

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher >=1.0
    - plone.app.blob
    - sh

test:
  # Python imports
  imports:
    - experimental
    - experimental.gracefulblobmissing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/experimental.gracefulblobmissing
  license: GPL
  summary: "Patch for Plone. Don't raise errors for file contents with missing BLOB file"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
