package:
  name: django-pushy
  version: "0.1.7"

source:
  fn: Django-Pushy-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Pushy/Django-Pushy-0.1.7.tar.gz
  md5: d0f26e1cdd5ca0e50e03be2e52cb0327
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pushy = django-pushy:main
    #
    # Would create an entry point called django-pushy that calls django-pushy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-gcm
    - django
    - django-celery
    - apns

  run:
    - python
    - python-gcm
    - django
    - django-celery
    - apns

test:
  # Python imports
  imports:
    - pushy
    - pushy/migrations
    - pushy/south_migrations
    - pushy/tasks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rakanalh/django-pushy
  license: LICENSE.txt
  summary: 'Handle push notifications at scale.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
