package:
  name: optypecheck
  version: "17"

source:
  fn: optypecheck-17.tar.gz
  url: https://pypi.python.org/packages/source/o/optypecheck/optypecheck-17.tar.gz
  md5: a093396aacbd4fac36771da5ed445fb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - optypecheck = optypecheck:main
    #
    # Would create an entry point called optypecheck that calls optypecheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - optypecheck
    - typecheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/carlopires/optypecheck
  license: MIT License
  summary: 'A non intrusive and optional *type checking for Python 3* using annotations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
