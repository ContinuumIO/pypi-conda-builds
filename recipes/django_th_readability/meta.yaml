package:
  name: django_th_readability
  version: "0.2.3"

source:
  fn: django_th_readability-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_th_readability/django_th_readability-0.2.3.tar.gz
  md5: f3eecf14e09969e5f6b6ee179e537254
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_th_readability = django_th_readability:main
    #
    # Would create an entry point called django_th_readability that calls django_th_readability.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_th >=0.10.1
    - readability-api ==0.2.4

  run:
    - python
    - django_th >=0.10.1
    - readability-api ==0.2.4

test:
  # Python imports
  imports:
    - django_th_readability
    - th_readability
    - th_readability.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/foxmask/django-th-readability
  license: BSD License
  summary: 'From Trigger Happy, this connector provides an access to your Readability account'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
