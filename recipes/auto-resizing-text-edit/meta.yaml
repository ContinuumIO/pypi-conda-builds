package:
  name: auto-resizing-text-edit
  version: "0.1.0"

source:
  fn: auto-resizing-text-edit-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/auto-resizing-text-edit/auto-resizing-text-edit-0.1.0.tar.gz
  md5: 7df271daee9553af8cdc5ac3b377b3d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auto-resizing-text-edit = auto-resizing-text-edit:main
    #
    # Would create an entry point called auto-resizing-text-edit that calls auto-resizing-text-edit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - auto_resizing_text_edit
    - auto_resizing_text_edit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cameel/auto-resizing-text-edit
  license: MIT License
  summary: 'A Qt widget based on QTextEdit, that changes its height automatically to accommodate the text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
