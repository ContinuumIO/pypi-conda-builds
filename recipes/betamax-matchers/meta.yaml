package:
  name: betamax-matchers
  version: "0.1.0"

source:
  fn: betamax-matchers-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/betamax-matchers/betamax-matchers-0.1.0.tar.gz
  md5: a6fbd689fd36d40fcba0d2c60e56f2f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betamax-matchers = betamax-matchers:main
    #
    # Would create an entry point called betamax-matchers that calls betamax-matchers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - betamax >=0.3.2

  run:
    - python
    - betamax >=0.3.2

test:
  # Python imports
  imports:
    - betamax_matchers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sigmavirus24/betamax_matchers
  license: betfair
  summary: 'A VCR imitation for python-requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
