package:
  name: simple_logging
  version: "0.1.0"

source:
  fn: simple_logging-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simple_logging/simple_logging-0.1.0.tar.gz
  md5: b7c81617cb30be697ad400842c51b093
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple_logging = simple_logging:main
    #
    # Would create an entry point called simple_logging that calls simple_logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simple_logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pnpnpn/simple-logging
  license: MIT License
  summary: 'Command-line execution with timeout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
