package:
  name: flask-fluiddb
  version: "0.1"

source:
  fn: Flask-FluidDB-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-FluidDB/Flask-FluidDB-0.1.tar.gz
  md5: 4151f6abe371bac124ebd53bb6d583ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-fluiddb = flask-fluiddb:main
    #
    # Would create an entry point called flask-fluiddb that calls flask-fluiddb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - fom
    - httplib2

  run:
    - python
    - flask
    - fom
    - httplib2

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/aafshar/flask-fluiddb-main
  license: MIT License
  summary: 'Fluiddb access for flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
