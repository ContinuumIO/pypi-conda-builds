package:
  name: git-lint
  version: "0.0.6.1"

source:
  fn: git-lint-0.0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/g/git-lint/git-lint-0.0.6.1.tar.gz
  md5: 6c33a46f602d22924b887d89d28e45d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-lint = git-lint:main
    #
    # Would create an entry point called git-lint that calls git-lint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - termcolor
    - docopt
    - html-linter
    - template-remover
    - docutils

  run:
    - python
    - pyyaml
    - termcolor
    - docopt
    - html-linter
    - template-remover
    - docutils

test:
  # Python imports
  imports:
    - gitlint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sk-/git-lint
  license: Apache Software License
  summary: 'Git Lint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
