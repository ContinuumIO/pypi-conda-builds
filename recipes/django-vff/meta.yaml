package:
  name: django-vff
  version: "0.2b3"

source:
  fn: django-vff-0.2b3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vff/django-vff-0.2b3.tar.gz
  md5: 9b49642953b0f059a29f054f024d5c9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vff = django-vff:main
    #
    # Would create an entry point called django-vff that calls django-vff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython ==0.3.6

  run:
    - python
    - gitpython ==0.3.6

test:
  # Python imports
  imports:
    - vff

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yaco-Sistemas/django-vff
  license: BSD License
  summary: 'Versioned file field for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
