package:
  name: django-s3backup
  version: "1.0.2"

source:
  fn: django-s3backup-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-s3backup/django-s3backup-1.0.2.tar.gz
  md5: dbba9ee8e6857f78c7258c63f5174eac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-s3backup = django-s3backup:main
    #
    # Would create an entry point called django-s3backup that calls django-s3backup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.1.1
    - path.py >=2.2.2

  run:
    - python
    - boto >=2.1.1
    - path.py >=2.2.2

test:
  # Python imports
  imports:
    - s3backup
    - s3backup.management
    - s3backup.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tino/django-s3backup/
  license: MIT License
  summary: 'A django app to backup database dumps to an S3 bucket'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
