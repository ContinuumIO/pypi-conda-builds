package:
  name: django-navigator
  version: "0.1"

source:
  fn: django-navigator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-navigator/django-navigator-0.1.tar.gz
  md5: 553f7d59007f520b06fcf13f30529b43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-navigator = django-navigator:main
    #
    # Would create an entry point called django-navigator that calls django-navigator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - navigator
    - navigator.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aruhlov/django-navigator/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django navigation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
