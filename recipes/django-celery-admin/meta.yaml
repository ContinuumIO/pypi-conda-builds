package:
  name: django-celery-admin
  version: "0.1"

source:
  fn: django-celery-admin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery-admin/django-celery-admin-0.1.tar.gz
  md5: d232fd407102ef7bc44fd199144e7dc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery-admin = django-celery-admin:main
    #
    # Would create an entry point called django-celery-admin that calls django-celery-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - celery_admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mattcaldwell/django-celery-admin-ext
  license: BSD License
  summary: 'A django application that lets you kick off periodic celery tasks from the admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
