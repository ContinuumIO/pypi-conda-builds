package:
  name: django-hstore
  version: "1.3.7"

source:
  fn: django-hstore-1.3.7-source.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hstore/django-hstore-1.3.7-source.tar.gz
  md5: 755a95c1327e97d847ca4f0e18ecf439
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hstore = django-hstore:main
    #
    # Would create an entry point called django-hstore that calls django-hstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_hstore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djangonauts/django-hstore
  license: BSD License
  summary: 'PostgreSQL HStore support for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
