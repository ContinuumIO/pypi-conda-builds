package:
  name: django-geoip-utils
  version: "0.1.1"

source:
  fn: django-geoip-utils-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-geoip-utils/django-geoip-utils-0.1.1.tar.gz
  md5: a4d9aa906eb41d395eef8e35ba32b45d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geoip-utils = django-geoip-utils:main
    #
    # Would create an entry point called django-geoip-utils that calls django-geoip-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - geoip_utils
    - geoip_utils.management
    - geoip_utils.management.commands
    - geoip_utils.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Gidsy/django-geoip-utils
  license: UNKNOWN
  summary: 'GeoIp data as an app to facilitate installing. Also features template filters, utility functions and a management command for updating the GeoIP data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
