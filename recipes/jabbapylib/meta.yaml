package:
  name: jabbapylib
  version: "0.3.8"

source:
  fn: jabbapylib-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/j/jabbapylib/jabbapylib-0.3.8.tar.gz
  md5: 611102a708da56aeba70ffec3b0bf02f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jabbapylib = jabbapylib:main
    #
    # Would create an entry point called jabbapylib that calls jabbapylib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html5lib
    - psutil
    - pycurl
    - pytest
    - praw
    - untangle

  run:
    - python
    - html5lib
    - psutil
    - pycurl
    - pytest
    - praw
    - untangle

test:
  # Python imports
  imports:
    - jabbapylib
    - jabbapylib.apps
    - jabbapylib.browser
    - jabbapylib.clipboard
    - jabbapylib.console
    - jabbapylib.dateandtime
    - jabbapylib.decorators
    - jabbapylib.dictionary
    - jabbapylib.distance
    - jabbapylib.filesystem
    - jabbapylib.hash
    - jabbapylib.imgur
    - jabbapylib.lib
    - jabbapylib.lib.pytesser
    - jabbapylib.mail
    - jabbapylib.math
    - jabbapylib.mouse
    - jabbapylib.multimedia
    - jabbapylib.network
    - jabbapylib.number
    - jabbapylib.ocr
    - jabbapylib.podium
    - jabbapylib.podium.gnome
    - jabbapylib.process
    - jabbapylib.reddit
    - jabbapylib.say
    - jabbapylib.serialization
    - jabbapylib.text
    - jabbapylib.torrent
    - jabbapylib.web
    - jabbapylib.web.download
    - jabbapylib.web.scraper
    - jabbapylib.window

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jabbalaci/jabbapylib
  license: GNU General Public License (GPL)
  summary: 'A lightweight, general-purpose Python library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
