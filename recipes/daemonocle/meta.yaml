package:
  name: daemonocle
  version: "0.8"

source:
  fn: daemonocle-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/daemonocle/daemonocle-0.8.tar.gz
  md5: b0912dec23b42adc5aa55f0cb410dde0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daemonocle = daemonocle:main
    #
    # Would create an entry point called daemonocle that calls daemonocle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - click ==2.5
    - psutil ==2.1.1

  run:
    - python
    - click ==2.5
    - psutil ==2.1.1

test:
  # Python imports
  imports:
    - daemonocle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jnrbsn/daemonocle
  license: MIT License
  summary: 'A Python library for creating super fancy Unix daemons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
