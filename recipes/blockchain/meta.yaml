package:
  name: blockchain
  version: "1.1.2"

source:
  fn: blockchain-1.1.2.zip
  url: https://pypi.python.org/packages/source/b/blockchain/blockchain-1.1.2.zip
  md5: 560daea559f952e3d6d4c92fcb36257d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockchain = blockchain:main
    #
    # Would create an entry point called blockchain that calls blockchain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - blockchain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blockchain/api-v1-client-python
  license: MIT License
  summary: 'Blockchain API library (v1)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
