package:
  name: django-thumborstorage
  version: "0.91.6"

source:
  fn: django-thumborstorage-0.91.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-thumborstorage/django-thumborstorage-0.91.6.tar.gz
  md5: 1a302da802a49efe13c7b61192656d1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-thumborstorage = django-thumborstorage:main
    #
    # Would create an entry point called django-thumborstorage that calls django-thumborstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - mock

  run:
    - python
    - requests
    - mock

test:
  # Python imports
  imports:
    - django_thumborstorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Starou/django-thumborstorage
  license: BSD License
  summary: 'Django custom storage for Thumbor backend.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
