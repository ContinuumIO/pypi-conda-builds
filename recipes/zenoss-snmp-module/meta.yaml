package:
  name: zenoss-snmp-module
  version: "1.0.0rc4"

source:
  fn: zenoss-snmp-module-1.0.0rc4.tar.gz
  url: https://pypi.python.org/packages/source/z/zenoss-snmp-module/zenoss-snmp-module-1.0.0rc4.tar.gz
  md5: 5b720667d7d0e0c601682390aa5eb22a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zenoss-snmp-module = zenoss-snmp-module:main
    #
    # Would create an entry point called zenoss-snmp-module that calls zenoss-snmp-module.main()

    - zenoss-snmp-module = zenoss_snmp_module:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - which
    - python-rrdtool
    - snmp-passpersist >=1.2.2

  run:
    - python
    - argparse
    - which
    - python-rrdtool
    - snmp-passpersist >=1.2.2

test:
  # Python imports
  # imports:

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - zenoss-snmp-module --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cluther/zenoss-snmp-module
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Net-SNMP pass_persist script for monitoring Zenoss.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
