package:
  name: py-configurator
  version: "0.0.3"

source:
  fn: py-configurator-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/py-configurator/py-configurator-0.0.3.tar.gz
  md5: af1f690d25d2ec42d578e008155e6dec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-configurator = py-configurator:main
    #
    # Would create an entry point called py-configurator that calls py-configurator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - py_configurator
    - py_configurator.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryankanno/py-configurator
  license: MIT License
  summary: 'Tiny configuration wrapper module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
