package:
  name: jondis
  version: "0.1"

source:
  fn: jondis-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jondis/jondis-0.1.tar.gz
  md5: 9c039ecd1dacbca92f5d62d0f8188643
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jondis = jondis:main
    #
    # Would create an entry point called jondis that calls jondis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - jondis
    - jondis.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/StartTheShift/jondis
  license: BSD License
  summary: 'Redis pool for HA redis clusters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
