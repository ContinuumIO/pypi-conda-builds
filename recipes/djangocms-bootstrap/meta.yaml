package:
  name: djangocms-bootstrap
  version: "0.2.1"

source:
  fn: djangocms-bootstrap-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-bootstrap/djangocms-bootstrap-0.2.1.tar.gz
  md5: 8c7b0940926c08b1e8be8f252e91374b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-bootstrap = djangocms-bootstrap:main
    #
    # Would create an entry point called djangocms-bootstrap that calls djangocms-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_bootstrap
    - cmsplugin_bootstrap.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jrief/djangocms-bootstrap
  license: MIT License
  summary: 'Collection of plugins for DjangoCMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
