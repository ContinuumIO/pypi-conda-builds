package:
  name: lowvoltage
  version: "0.7.1"

source:
  fn: LowVoltage-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LowVoltage/LowVoltage-0.7.1.tar.gz
  md5: 05a3a45584706036422ce6f4dd3300c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lowvoltage = lowvoltage:main
    #
    # Would create an entry point called lowvoltage that calls lowvoltage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2

  run:
    - python
    - requests >=2.2

test:
  # Python imports
  imports:
    - LowVoltage
    - LowVoltage.actions
    - LowVoltage.actions.tests
    - LowVoltage.actions.tests.integ
    - LowVoltage.compounds
    - LowVoltage.compounds.tests
    - LowVoltage.compounds.tests.integ
    - LowVoltage.connection
    - LowVoltage.connection.tests
    - LowVoltage.connection.tests.integ
    - LowVoltage.testing
    - LowVoltage.tests
    - LowVoltage.tests.integ
    - lowvoltage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/LowVoltage
  license: MIT License
  summary: 'Standalone DynamoDB client not hiding any feature'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
