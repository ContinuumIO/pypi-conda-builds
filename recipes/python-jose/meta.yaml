package:
  name: python-jose
  version: "0.2.0"

source:
  fn: python-jose-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-jose/python-jose-0.2.0.tar.gz
  md5: 9b847ae4b463148008cdf3aebb0c6d7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-jose = python-jose:main
    #
    # Would create an entry point called python-jose that calls python-jose.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto >=2.6.0,<2.7.0
    - six >=1.9.0,<1.10.0
    - ecdsa <1.0

  run:
    - python
    - pycrypto >=2.6.0,<2.7.0
    - six >=1.9.0,<1.10.0
    - ecdsa <1.0

test:
  # Python imports
  imports:
    - jose
    - jose/algorithms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mpdavis/python-jose
  license: MIT License
  summary: 'JOSE implementation in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
