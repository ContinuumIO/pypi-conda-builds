package:
  name: flask-geokit
  version: "0.1.7"

source:
  fn: flask-geokit-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-geokit/flask-geokit-0.1.7.tar.gz
  md5: d27ce291b13e8452e72e5df38145755a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-geokit = flask-geokit:main
    #
    # Would create an entry point called flask-geokit that calls flask-geokit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - furl
    - geohash
    - flask

  run:
    - python
    - furl
    - geohash
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bluemoon/flask-geokit
  license: BSD License
  summary: 'Geocoding toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
