package:
  name: django-ldap-groups-bsc
  version: "0.1.5"

source:
  fn: django-ldap-groups-bsc-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ldap-groups-bsc/django-ldap-groups-bsc-0.1.5.tar.gz
  md5: 429a4c14f3d38afe007f5b2bdc1d8417
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ldap-groups-bsc = django-ldap-groups-bsc:main
    #
    # Would create an entry point called django-ldap-groups-bsc that calls django-ldap-groups-bsc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ldap_groups
    - ldap_groups.accounts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-ldap-groups/
  license: BSD License
  summary: 'A Django app for authenticating and authorizing against LDAP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
