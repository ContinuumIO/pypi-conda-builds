package:
  name: chaoslib
  version: "0.0.1-Alpha"

source:
  fn: chaoslib-0.0.1-Alpha.tar.gz
  url: https://pypi.python.org/packages/source/c/chaoslib/chaoslib-0.0.1-Alpha.tar.gz
  md5: 8a08010dbb62e1f1cb499319fb26b48b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chaoslib = chaoslib:main
    #
    # Would create an entry point called chaoslib that calls chaoslib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - randomdotorg

  run:
    - python
    - randomdotorg

test:
  # Python imports
  imports:
    - chaoslib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dansun/chaoslib
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Sorting library for chaotic sorting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
