package:
  name: termenu
  version: "1.1.5"

source:
  fn: termenu-1.1.5.tar.gz
  url: https://pypi.python.org/packages/source/t/termenu/termenu-1.1.5.tar.gz
  md5: 137a807ea4d130b18f6a3b43edfc16e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - termenu = termenu:main
    #
    # Would create an entry point called termenu that calls termenu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - termenu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gooli/termenu
  license: GNU General Public License (GPL)
  summary: 'Interactive in-line menus for Unix-based terminals'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
