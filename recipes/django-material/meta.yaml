package:
  name: django-material
  version: "0.2.1"

source:
  fn: django-material-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-material/django-material-0.2.1.tar.gz
  md5: a849ffe966760dba38ce2ddf792b9883
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-material = django-material:main
    #
    # Would create an entry point called django-material that calls django-material.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - material
    - material.admin
    - material.admin.templatetags
    - material.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/viewflow/django-material
  license: BSD License
  summary: 'Material design for django forms and admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
