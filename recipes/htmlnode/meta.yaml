package:
  name: htmlnode
  version: "0.1.8"

source:
  fn: HtmlNode-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/H/HtmlNode/HtmlNode-0.1.8.tar.gz
  md5: 164144d0fbda040486fdb04789aa2af6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmlnode = htmlnode:main
    #
    # Would create an entry point called htmlnode that calls htmlnode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - html_node
    - htmlnode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hllau/html_node
  license: Apache Software License
  summary: 'A simple Python HTML generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
