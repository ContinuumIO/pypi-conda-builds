package:
  name: django_microsip_exportaexcel
  version: "0.0.1"

source:
  fn: django_microsip_exportaexcel-0.0.1.zip
  url: https://pypi.python.org/packages/source/d/django_microsip_exportaexcel/django_microsip_exportaexcel-0.0.1.zip
  md5: 914f8f1782ae5574cf19392653f468fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_microsip_exportaexcel = django_microsip_exportaexcel:main
    #
    # Would create an entry point called django_microsip_exportaexcel that calls django_microsip_exportaexcel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_microsip_base

  run:
    - python
    - django_microsip_base

test:
  # Python imports
  imports:
    - django_microsip_exportaexcel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'django_microsip_exportaexcel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
