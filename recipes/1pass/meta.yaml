package:
  name: 1pass
  version: "0.2.1"

source:
  fn: 1pass-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/1/1pass/1pass-0.2.1.tar.gz
  md5: cf47104095a3f3c8dc0242fe8be1f39c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - 1pass = 1pass:main
    #
    # Would create an entry point called 1pass that calls 1pass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m2crypto
    - fuzzywuzzy

  run:
    - python
    - m2crypto
    - fuzzywuzzy

test:
  # Python imports
  imports:
    - onepassword

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/georgebrock/1pass
  license: MIT
  summary: 'A Python library and command line interface for 1Password'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
