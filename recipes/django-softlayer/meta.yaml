package:
  name: django-softlayer
  version: "0.2"

source:
  fn: django-softlayer-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-softlayer/django-softlayer-0.2.tar.gz
  md5: c477f1c362a6a5c8542e55f4f33e15f7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-softlayer = django-softlayer:main
    #
    # Would create an entry point called django-softlayer that calls django-softlayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.1
    - python-cloudfiles ==1.7.10
    - django-cumulus ==1.0.5
    - softlayer-object-storage ==0.4.6

  run:
    - python
    - setuptools
    - django >=1.4.1
    - python-cloudfiles ==1.7.10
    - django-cumulus ==1.0.5
    - softlayer-object-storage ==0.4.6

test:
  # Python imports
  imports:
    - django_softlayer
    - django_softlayer.management
    - django_softlayer.management.commands
    - django_softlayer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Arpaso/django-softlayer
  license: UNKNOWN
  summary: 'Django storage for SoftLayer Cloud Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
