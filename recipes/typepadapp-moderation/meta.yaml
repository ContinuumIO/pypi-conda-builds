package:
  name: typepadapp-moderation
  version: "1.0.3"

source:
  fn: typepadapp-moderation-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/typepadapp-moderation/typepadapp-moderation-1.0.3.tar.gz
  md5: 01d37fa8a2608aca34d97d34b6a845cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typepadapp-moderation = typepadapp-moderation:main
    #
    # Would create an entry point called typepadapp-moderation that calls typepadapp-moderation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moderation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sixapart/typepadapp-moderation/
  license: UNKNOWN
  summary: 'Moderation panel for TypePad applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
