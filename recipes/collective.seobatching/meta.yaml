package:
  name: collective.seobatching
  version: "1.0"

source:
  fn: collective.seobatching-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.seobatching/collective.seobatching-1.0.tar.gz
  md5: e727a4b6285675fb599ccb35c464a61c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.seobatching = collective.seobatching:main
    #
    # Would create an entry point called collective.seobatching that calls collective.seobatching.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.seobatching
    - collective.seobatching.browser
    - collective.seobatching.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.seobatching
  license: GPL
  summary: 'This package replaces query parameters from batched listings thus making them more seo friendly.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
