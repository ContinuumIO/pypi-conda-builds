package:
  name: collective.googlemodifycontent
  version: "1.0.0"

source:
  fn: collective.googlemodifycontent-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.googlemodifycontent/collective.googlemodifycontent-1.0.0.tar.gz
  md5: 9298d2fc54ba97b24f14e83a5c41d1d0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.googlemodifycontent = collective.googlemodifycontent:main
    #
    # Would create an entry point called collective.googlemodifycontent that calls collective.googlemodifycontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.googleauthentication
    - collective.googlesystemstorage

  run:
    - python
    - setuptools
    - setuptools
    - collective.googleauthentication
    - collective.googlesystemstorage

test:
  # Python imports
  imports:
    - collective
    - collective.googlemodifycontent
    - collective.googlemodifycontent.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.googlemodifycontent
  license: GPL
  summary: 'GMC embeds the Google Docs application inside the Google Modify panel, allowing Plone users to edit their documents directly from the Plone application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
