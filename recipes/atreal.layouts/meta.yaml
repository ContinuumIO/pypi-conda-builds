package:
  name: atreal.layouts
  version: "0.6.2"

source:
  fn: atreal.layouts-0.6.2.zip
  url: https://pypi.python.org/packages/source/a/atreal.layouts/atreal.layouts-0.6.2.zip
  md5: cdc277c9f10a32bd33fa404cf51cb2c7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.layouts = atreal.layouts:main
    #
    # Would create an entry point called atreal.layouts that calls atreal.layouts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - atreal
    - atreal.layouts
    - atreal.layouts.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/atreal.layouts
  license: GPL
  summary: 'Various layouts for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
