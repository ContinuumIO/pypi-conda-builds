package:
  name: ansible-shell
  version: "0.0.2"

source:
  fn: ansible-shell-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/ansible-shell/ansible-shell-0.0.2.tar.gz
  md5: 1460aee9cbdf0dc204e8306c79fdab8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansible-shell = ansible-shell:main
    #
    # Would create an entry point called ansible-shell that calls ansible-shell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - ansible >=1.3

  run:
    - python
    - ansible >=1.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dominis/ansible-shell
  license: UNKNOWN
  summary: 'Interactive shell for ansible'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
