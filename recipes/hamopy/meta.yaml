package:
  name: hamopy
  version: "0.1.1"

source:
  fn: hamopy-0.1.1.zip
  url: https://pypi.python.org/packages/source/h/hamopy/hamopy-0.1.1.zip
  md5: 00567642602719866160044af3f06b4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hamopy = hamopy:main
    #
    # Would create an entry point called hamopy that calls hamopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hamopy
    - hamopy.benchmarks
    - hamopy.materials

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hamopy/
  license: LICENSE.txt
  summary: 'Heat, air and moisture transfer modelling in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
