package:
  name: flask-yamlconfig
  version: "0.0.3"

source:
  fn: Flask-YAMLConfig-0.0.3.zip
  url: https://pypi.python.org/packages/source/F/Flask-YAMLConfig/Flask-YAMLConfig-0.0.3.zip
  md5: e7c1e60682ce5d11f81d82e49d6dab56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-yamlconfig = flask-yamlconfig:main
    #
    # Would create an entry point called flask-yamlconfig that calls flask-yamlconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.6
    - pyyaml

  run:
    - python
    - flask >=0.6
    - pyyaml

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'YAML configurator for Flask app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
