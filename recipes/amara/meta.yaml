package:
  name: amara
  version: "2.0.0"

source:
  fn: Amara-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/A/Amara/Amara-2.0.0.tar.gz
  md5: c85d128755cc3cc3872e0b681675e066
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amara = amara:main
    #
    # Would create an entry point called amara that calls amara.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - amara
    - amara.bindery
    - amara.bindery.model
    - amara.dom
    - amara.lib
    - amara.pushtree
    - amara.test
    - amara.test.lib
    - amara.test.xpath
    - amara.test.xslt
    - amara.thirdparty
    - amara.thirdparty.html5lib
    - amara.thirdparty.html5lib.treebuilders
    - amara.thirdparty.httplib2
    - amara.writers
    - amara.xpath
    - amara.xpath.compiler
    - amara.xpath.expressions
    - amara.xpath.extensions
    - amara.xpath.functions
    - amara.xpath.locationpaths
    - amara.xpath.parser
    - amara.xslt
    - amara.xslt.expressions
    - amara.xslt.exslt
    - amara.xslt.extensions
    - amara.xslt.functions
    - amara.xslt.numbers
    - amara.xslt.reader
    - amara.xslt.tree
    - amara.xslt.xpatterns
    - amara.xupdate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.xml3k.org/Amara2
  license: Apache Software License
  summary: 'Library for XML processing in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
