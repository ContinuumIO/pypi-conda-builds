package:
  name: bulkwhois
  version: "0.2.1"

source:
  fn: BulkWhois-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BulkWhois/BulkWhois-0.2.1.tar.gz
  md5: c3c9a5eecac62f210f41675a83f11ed0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bulkwhois = bulkwhois:main
    #
    # Would create an entry point called bulkwhois that calls bulkwhois.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bulkwhois

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BulkWhois/
  license: LICENSE.txt
  summary: 'Interfaces for popular bulk WHOIS servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
