package:
  name: flipper
  version: "0.8.0"

source:
  fn: flipper-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flipper/flipper-0.8.0.tar.gz
  md5: 890193d4719a179b8bebcdce6297527d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flipper = flipper:main
    #
    # Would create an entry point called flipper that calls flipper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flipper
    - flipper.app
    - flipper.doc
    - flipper.example
    - flipper.kernel
    - flipper.load
    - flipper.profile
    - flipper.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/Mark_Bell/flipper
  license: UNKNOWN
  summary: 'For manipulating curves and measured laminations on surfaces and producing mapping tori.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
