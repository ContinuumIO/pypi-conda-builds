package:
  name: git-goggles
  version: "0.2.12"

source:
  fn: git-goggles-0.2.12.tar.gz
  url: https://pypi.python.org/packages/source/g/git-goggles/git-goggles-0.2.12.tar.gz
  md5: c7bead4e6b989ed1048e1d99bec8ffd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-goggles = git-goggles:main
    #
    # Would create an entry point called git-goggles that calls git-goggles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gitgoggles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nowells/git-goggles/
  license: MIT License
  summary: "A series of GIT utilities to streamline working with remote branches and reviewing code. You can think of git-goggles as 'git branch -a' on steroids. Just install and run 'git goggles'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
