package:
  name: django-multisafepay
  version: "0.1"

source:
  fn: django-multisafepay-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multisafepay/django-multisafepay-0.1.tar.gz
  md5: 27082a9d8e9bdf0b952e72658eed54c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multisafepay = django-multisafepay:main
    #
    # Would create an entry point called django-multisafepay that calls django-multisafepay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.1

  run:
    - python
    - requests >=1.2.1

test:
  # Python imports
  imports:
    - django_multisafepay
    - django_multisafepay.data
    - django_multisafepay.messages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/django-multisafepay
  license: Apache Software License
  summary: 'MultiSafepay Payments Gateway integration for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
