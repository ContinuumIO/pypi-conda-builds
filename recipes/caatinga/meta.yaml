package:
  name: caatinga
  version: "1.0.2"

source:
  fn: caatinga-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/caatinga/caatinga-1.0.2.tar.gz
  md5: 72ce531db22cd65dfc27c85ae545c64f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caatinga = caatinga:main
    #
    # Would create an entry point called caatinga that calls caatinga.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - caatinga
    - caatinga.caat
    - caatinga.core
    - caatinga.lscaat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/headmastersquall/caatinga
  license: GNU GENERAL PUBLIC LICENSE Version 3
  summary: 'A backup program that creates multiple snapshots of a file system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
