package:
  name: django-dajaxice
  version: "0.7"

source:
  fn: django-dajaxice-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dajaxice/django-dajaxice-0.7.tar.gz
  md5: 4561a11b406c39a1171e0ab3c9760a90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dajaxice = django-dajaxice:main
    #
    # Would create an entry point called django-dajaxice that calls django-dajaxice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - dajaxice
    - dajaxice.core
    - dajaxice.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dajaxproject.com
  license: BSD License
  summary: 'Agnostic and easy to use ajax library for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
