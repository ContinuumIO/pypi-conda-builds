package:
  name: django-ios-push
  version: "0.1.2"

source:
  fn: django-ios-push-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ios-push/django-ios-push-0.1.2.tar.gz
  md5: 65c01b5e34080c38c2a6d8830c962a7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ios-push = django-ios-push:main
    #
    # Would create an entry point called django-ios-push that calls django-ios-push.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iospush

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/appsome/django-ios-push
  license: Apache License 2.0
  summary: 'A Django Application for contacting the Apple Push Service and maintaining a list of iOS devices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
