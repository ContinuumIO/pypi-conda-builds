package:
  name: bottle-agamemnon
  version: "0.1.0"

source:
  fn: bottle-agamemnon-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-agamemnon/bottle-agamemnon-0.1.0.tar.gz
  md5: 9ce8cd0f45ee4f3e53a40744791e6513
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-agamemnon = bottle-agamemnon:main
    #
    # Would create an entry point called bottle-agamemnon that calls bottle-agamemnon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.9
    - agamemnon >=0.4

  run:
    - python
    - bottle >=0.9
    - agamemnon >=0.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/turtlebender/bottle-agamemnon/
  license: MIT License
  summary: 'Agamemnon integration for bottle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
