package:
  name: d2m.rendersource
  version: "0.4"

source:
  fn: d2m.rendersource-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/d2m.rendersource/d2m.rendersource-0.4.tar.gz
  md5: c27063d98fd30fa871472ace853630e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - d2m.rendersource = d2m.rendersource:main
    #
    # Would create an entry point called d2m.rendersource that calls d2m.rendersource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok

  run:
    - python
    - setuptools
    - setuptools
    - grok

test:
  # Python imports
  imports:
    - d2m
    - d2m.rendersource
    - d2m.rendersource.ftests
    - d2m.rendersource.ftests.directives

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL
  summary: 'Grok source renderers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
