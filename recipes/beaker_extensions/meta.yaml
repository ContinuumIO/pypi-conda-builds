package:
  name: beaker_extensions
  version: "0.1.1dev"

source:
  fn: beaker_extensions-0.1.1dev.tar.gz
  url: https://pypi.python.org/packages/source/b/beaker_extensions/beaker_extensions-0.1.1dev.tar.gz
  md5: 4fa71efd487079fb22e9179877302b47
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beaker_extensions = beaker_extensions:main
    #
    # Would create an entry point called beaker_extensions that calls beaker_extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - beaker_extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Beaker extensions for additional back-end stores.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
