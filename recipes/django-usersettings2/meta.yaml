package:
  name: django-usersettings2
  version: "0.1.3"

source:
  fn: django-usersettings2-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-usersettings2/django-usersettings2-0.1.3.tar.gz
  md5: b17740051c503da3d96eb53fbc129150
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-usersettings2 = django-usersettings2:main
    #
    # Would create an entry point called django-usersettings2 that calls django-usersettings2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.1

  run:
    - python
    - django >=1.5.1

test:
  # Python imports
  imports:
    - usersettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mishbahr/django-usersettings2
  license: BSD License
  summary: 'The missing extension to the Django \xe2\x80\x9csites\xe2\x80\x9d framework, use it to store additional information for your Django-powered sites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
