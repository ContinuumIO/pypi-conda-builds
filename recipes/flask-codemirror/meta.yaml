package:
  name: flask-codemirror
  version: "0.0.4"

source:
  fn: flask-codemirror-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-codemirror/flask-codemirror-0.0.4.tar.gz
  md5: 3dfb65a82a5550142cfb5417135bf1d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-codemirror = flask-codemirror:main
    #
    # Would create an entry point called flask-codemirror that calls flask-codemirror.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - wtforms
    - requests

  run:
    - python
    - flask
    - wtforms
    - requests

test:
  # Python imports
  imports:
    - flask_codemirror

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/j0ack/flask-codemirror
  license: GNU General Public License v3 (GPLv3)
  summary: 'Use CodeMirror Javascript library with Flask-WTF'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
