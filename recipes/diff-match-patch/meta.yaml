package:
  name: diff-match-patch
  version: "20121119"

source:
  fn: diff-match-patch-20121119.tar.gz
  url: https://pypi.python.org/packages/source/d/diff-match-patch/diff-match-patch-20121119.tar.gz
  md5: 08e02cad82dda942b09ee248772fe143
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diff-match-patch = diff-match-patch:main
    #
    # Would create an entry point called diff-match-patch that calls diff-match-patch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - diff_match_patch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/google-diff-match-patch/
  license: Apache Software License
  summary: 'The Diff Match and Patch libraries offer robust algorithms to perform the operations required for synchronizing plain text.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
