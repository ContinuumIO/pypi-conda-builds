package:
  name: tornadoist
  version: "0.5.2"

source:
  fn: tornadoist-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadoist/tornadoist-0.5.2.tar.gz
  md5: b6633e2945ba49c41274d25fd6d09a33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadoist = tornadoist:main
    #
    # Would create an entry point called tornadoist that calls tornadoist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tornadoist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eguven/tornadoist.git
  license: Apache Software License
  summary: 'mixins for tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
