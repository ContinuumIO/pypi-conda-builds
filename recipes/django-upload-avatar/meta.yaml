package:
  name: django-upload-avatar
  version: "0.1.0"

source:
  fn: django-upload-avatar-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-upload-avatar/django-upload-avatar-0.1.0.tar.gz
  md5: 5b5d021072dca7c5c23b6f117b37b5f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-upload-avatar = django-upload-avatar:main
    #
    # Would create an entry point called django-upload-avatar that calls django-upload-avatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - upload_avatar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yueyoum/django-upload-avatar
  license: BSD License
  summary: 'A Django APP for Upload avatars'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
