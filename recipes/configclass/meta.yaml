package:
  name: configclass
  version: "0.1.0"

source:
  fn: configclass-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/configclass/configclass-0.1.0.tar.gz
  md5: e49798a7ddba6f7c198639c0721082ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configclass = configclass:main
    #
    # Would create an entry point called configclass that calls configclass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mergedict >=0.2.0

  run:
    - python
    - mergedict >=0.2.0

test:
  # Python imports
  imports:
    - configclass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/schettino72/configclass/
  license: MIT License
  summary: 'A Python to class to hold configuration values.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
