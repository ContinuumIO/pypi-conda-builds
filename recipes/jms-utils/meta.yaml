package:
  name: jms-utils
  version: "0.6.2"

source:
  fn: JMS-Utils-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/J/JMS-Utils/JMS-Utils-0.6.2.tar.gz
  md5: 761310690387b02f552ea5de6b27f80b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jms-utils = jms-utils:main
    #
    # Would create an entry point called jms-utils that calls jms-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jms_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JohnyMoSwag/jms-utils
  license: MIT License
  summary: 'Various utility functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
