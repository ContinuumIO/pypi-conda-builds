package:
  name: booleannet
  version: "1.2.8"

source:
  fn: BooleanNet-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/B/BooleanNet/BooleanNet-1.2.8.tar.gz
  md5: 3b2852e6bdaf24c16df7c7604bc19529
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - booleannet = booleannet:main
    #
    # Would create an entry point called booleannet that calls booleannet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - networkx
    - numpy

  run:
    - python
    - networkx
    - numpy

test:
  # Python imports
  imports:
    - boolean2
    - boolean2.plde
    - boolean2.ply
    - booleannet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ialbert/booleannet
  license: UNKNOWN
  summary: 'Boolean Network Simulation Toolbox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
