package:
  name: inputgen
  version: "0.7.1"

source:
  fn: inputgen-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/i/inputgen/inputgen-0.7.1.tar.gz
  md5: 1d951440474056f0ce660cfa374a022a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inputgen = inputgen:main
    #
    # Would create an entry point called inputgen that calls inputgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - inputgen
    - inputgen.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gdub/python-inputgen
  license: MIT
  summary: 'Tool for automated input generation, useful for bounded exhaustive testing and the generation of complex data structures for test inputs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
