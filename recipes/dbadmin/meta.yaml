package:
  name: dbadmin
  version: "1.0"

source:
  fn: dbadmin-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dbadmin/dbadmin-1.0.tar.gz
  md5: 6110642c6939bbc06be097d7e030a186
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbadmin = dbadmin:main
    #
    # Would create an entry point called dbadmin that calls dbadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dbadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: git@github.com:anandjeyahar/python-DbAdmin.git
  license: GPL v2
  summary: 'just a python wrapper for doing most DB admin functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
