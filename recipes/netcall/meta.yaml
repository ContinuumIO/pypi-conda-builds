package:
  name: netcall
  version: "0.4.2"

source:
  fn: netcall-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/n/netcall/netcall-0.4.2.tar.gz
  md5: 07202f1d1445314a785995f550bcc2ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netcall = netcall:main
    #
    # Would create an entry point called netcall that calls netcall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq
    - pebble

  run:
    - python
    - pyzmq
    - pebble

test:
  # Python imports
  imports:
    - netcall
    - netcall.concurrency
    - netcall.green
    - netcall.sync
    - netcall.threading
    - netcall.tornado
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aglyzov/netcall
  license: Modified BSD
  summary: 'A simple Python RPC system (ZeroMQ + Threading/Tornado/Gevent/Eventlet/Greenhouse)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
