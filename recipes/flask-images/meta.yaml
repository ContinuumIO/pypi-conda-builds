package:
  name: flask-images
  version: "2.1.2"

source:
  fn: Flask-Images-2.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Images/Flask-Images-2.1.2.tar.gz
  md5: 11863f89deaf3b535f94e476d73a678e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-images = flask-images:main
    #
    # Would create an entry point called flask-images that calls flask-images.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - itsdangerous
    - pillowcase

  run:
    - python
    - flask >=0.9
    - itsdangerous
    - pillowcase

test:
  # Python imports
  imports:
    - flask_images

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mikeboers/Flask-Images
  license: BSD License
  summary: 'Dynamic image resizing for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
