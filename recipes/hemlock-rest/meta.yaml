package:
  name: hemlock-rest
  version: "0.1.6"

source:
  fn: hemlock-rest-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/h/hemlock-rest/hemlock-rest-0.1.6.tar.gz
  md5: 7fa39aa98cb9b7e636ea0530436a46ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hemlock-rest = hemlock-rest:main
    #
    # Would create an entry point called hemlock-rest that calls hemlock-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pexpect
    - web.py
    - hemlock

  run:
    - python
    - pexpect
    - web.py
    - hemlock

test:
  # Python imports
  imports:
    - hemlock_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lab41.github.io/Hemlock-REST
  license: LICENSE.txt
  summary: 'Hemlock is a way of providing a common data access layer. This is a RESTful server for Hemlock.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
