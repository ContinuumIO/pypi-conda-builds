package:
  name: antbkp
  version: "0.0.1"

source:
  fn: antbkp-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/antbkp/antbkp-0.0.1.tar.gz
  md5: a8e1bb3e4cd10d3cd91f2da87f0e91d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - antbkp = antbkp:main
    #
    # Would create an entry point called antbkp that calls antbkp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - antbkp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.zikzakmedia.com
  license: GNU Affero General Public License v3
  summary: 'Ant Backup. Simple Backup: Files, Rotation backups, Postgres, Mysql, OpenERP, Tryton,...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
