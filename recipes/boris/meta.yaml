package:
  name: boris
  version: "0.1"

source:
  fn: Boris-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/Boris/Boris-0.1.tar.gz
  md5: 705365834149e40227fa3d6cf15b92e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boris = boris:main
    #
    # Would create an entry point called boris that calls boris.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.0.1
    - postcodes >=0.1

  run:
    - python
    - lxml >=3.0.1
    - postcodes >=0.1

test:
  # Python imports
  imports:
    - boris

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/e-dard/boris
  license: WTFPL
  summary: 'Easily access and search Barclays bike availability in London, UK.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
