package:
  name: pygments_zenburn
  version: "0.9.0"

source:
  fn: pygments_zenburn-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pygments_zenburn/pygments_zenburn-0.9.0.tar.gz
  md5: 3589c0634a9e29755eb28c1ac20351b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygments_zenburn = pygments_zenburn:main
    #
    # Would create an entry point called pygments_zenburn that calls pygments_zenburn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - pygments_zenburn
    - zenburn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brolewis/pygments_zenburn
  license: BSD License
  summary: 'Syntax highlighting for Pygments based on the Zenburn theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
