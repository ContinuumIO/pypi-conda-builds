package:
  name: django_extjs4
  version: "1.2.0-extjs4.2.1"

source:
  fn: django_extjs4-1.2.0-extjs4.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_extjs4/django_extjs4-1.2.0-extjs4.2.1.tar.gz
  md5: b34d189ea094a06272147a92d519ca18
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_extjs4 = django_extjs4:main
    #
    # Would create an entry point called django_extjs4 that calls django_extjs4.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - django_extjs4
    - extjs4

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenak/django_extjs4
  license: GPL
  summary: 'Packages extjs4 as a Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
