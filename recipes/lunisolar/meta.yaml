package:
  name: lunisolar
  version: "0.1.3"

source:
  fn: lunisolar-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/l/lunisolar/lunisolar-0.1.3.tar.gz
  md5: 014c0726b492126b29fd215649179e31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lunisolar = lunisolar:main
    #
    # Would create an entry point called lunisolar that calls lunisolar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - mpmath

  run:
    - python
    - mpmath

test:
  # Python imports
  imports:
    - lunisolar
    - pycalcal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yen223/lunisolar
  license: MIT License
  summary: 'A library to handle the Chinese calendar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
