package:
  name: zope.locking
  version: "1.2.2"

source:
  fn: zope.locking-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.locking/zope.locking-1.2.2.tar.gz
  md5: b415fbadf21b3d151cd1848d9cdcace3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.locking = zope.locking:main
    #
    # Would create an entry point called zope.locking that calls zope.locking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - pytz
    - zc.i18n
    - zope.app.generations
    - zope.app.keyreference
    - zope.app.publisher
    - zope.app.testing
    - zope.component
    - zope.event
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.testing

  run:
    - python
    - setuptools
    - zodb3
    - pytz
    - zc.i18n
    - zope.app.generations
    - zope.app.keyreference
    - zope.app.publisher
    - zope.app.testing
    - zope.component
    - zope.event
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.testing

test:
  # Python imports
  imports:
    - zope
    - zope.locking
    - zope.locking.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Advisory exclusive locks, shared locks, and freezes (locked to no-one).\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
