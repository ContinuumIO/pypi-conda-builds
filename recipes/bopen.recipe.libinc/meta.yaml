package:
  name: bopen.recipe.libinc
  version: "0.3.0"

source:
  fn: bopen.recipe.libinc-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bopen.recipe.libinc/bopen.recipe.libinc-0.3.0.tar.gz
  md5: 68d1d8f856f061ec5935f0471d310826
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bopen.recipe.libinc = bopen.recipe.libinc:main
    #
    # Would create an entry point called bopen.recipe.libinc that calls bopen.recipe.libinc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - bopen
    - bopen.recipe
    - bopen.recipe.libinc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bopen.eu/open-source/bopen.recipe.libinc
  license: ZPL
  summary: 'zc.buildout recipe that parses compile time options from config scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
