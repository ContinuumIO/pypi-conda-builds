package:
  name: clip2zeus
  version: "1.5a"

source:
  fn: Clip2Zeus-1.5a.tar.gz
  url: https://pypi.python.org/packages/source/C/Clip2Zeus/Clip2Zeus-1.5a.tar.gz
  md5: 24c407d2e63a4aabf5d4944f93951b72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clip2zeus = clip2zeus:main
    #
    # Would create an entry point called clip2zeus that calls clip2zeus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - clip2zeus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codekoala.com/
  license: MIT License
  summary: 'Monitor your clipboard for URLs and automatically shorten them using 2Zeus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
