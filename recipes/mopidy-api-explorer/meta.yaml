package:
  name: mopidy-api-explorer
  version: "1.0.1"

source:
  fn: Mopidy-API-Explorer-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-API-Explorer/Mopidy-API-Explorer-1.0.1.tar.gz
  md5: 245a959dddfabccf796507d0ba2bf34a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-api-explorer = mopidy-api-explorer:main
    #
    # Would create an entry point called mopidy-api-explorer that calls mopidy-api-explorer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19

test:
  # Python imports
  imports:
    - mopidy_explorer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dz0ny/mopidy-api-explorer
  license: MIT License
  summary: u'Mopidy API Explorer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
