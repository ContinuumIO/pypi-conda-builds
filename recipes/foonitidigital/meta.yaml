package:
  name: foonitidigital
  version: "1"

source:
  fn: foonitidigital-1.tar.gz
  url: https://pypi.python.org/packages/source/f/foonitidigital/foonitidigital-1.tar.gz
  md5: b41adbb3680ace863b6bef83e9ebf2af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foonitidigital = foonitidigital:main
    #
    # Would create an entry point called foonitidigital that calls foonitidigital.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - foonitidigital
    - package

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: whatever
  license: GNU General Public License (GPL)
  summary: 'Testing the app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
