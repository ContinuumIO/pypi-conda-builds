package:
  name: backport_ipaddress
  version: "0.1"

source:
  fn: backport_ipaddress-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/backport_ipaddress/backport_ipaddress-0.1.tar.gz
  md5: 9c1f45f4361f71b124d7293a60006c05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backport_ipaddress = backport_ipaddress:main
    #
    # Would create an entry point called backport_ipaddress that calls backport_ipaddress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - backport_ipaddress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sk-/backport_ipaddress
  license: Python Software Foundation License
  summary: "Backport of Python 3's ipaddress module"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
