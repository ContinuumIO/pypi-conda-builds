package:
  name: hammr
  version: "0.2.5.2"

source:
  fn: hammr-0.2.5.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hammr/hammr-0.2.5.2.tar.gz
  md5: 6fa1b79490c66465f3575bfc04eeba9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hammr = hammr:main
    #
    # Would create an entry point called hammr that calls hammr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - uforge_python_sdk >=3.5.1,<3.6
    - httplib2 ==0.9
    - cmd2 ==0.6.7
    - texttable ==0.8.1
    - progressbar ==2.3
    - argparse
    - paramiko ==1.12
    - pyparsing ==2.0.2
    - hurry.filesize ==0.9
    - termcolor ==1.1.0
    - junit-xml ==1.3
    - xmlrunner ==1.7.7

  run:
    - python
    - uforge_python_sdk >=3.5.1,<3.6
    - httplib2 ==0.9
    - cmd2 ==0.6.7
    - texttable ==0.8.1
    - progressbar ==2.3
    - argparse
    - paramiko ==1.12
    - pyparsing ==2.0.2
    - hurry.filesize ==0.9
    - termcolor ==1.1.0
    - junit-xml ==1.3
    - xmlrunner ==1.7.7

test:
  # Python imports
  imports:
    - hammr
    - hammr.commands
    - hammr.commands.account
    - hammr.commands.bundle
    - hammr.commands.format
    - hammr.commands.image
    - hammr.commands.os
    - hammr.commands.quota
    - hammr.commands.scan
    - hammr.commands.template
    - hammr.commands.user
    - hammr.lib
    - hammr.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hammr.io
  license: Apache Software License
  summary: 'Command-line tool for building conistent and repeatable machine images for multiple cloud platforms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
