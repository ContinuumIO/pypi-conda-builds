package:
  name: isotoma.recipe.postdeploy
  version: "0.0.6"

source:
  fn: isotoma.recipe.postdeploy-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.postdeploy/isotoma.recipe.postdeploy-0.0.6.tar.gz
  md5: f616f48b9d89a6bf79045f5e48cb44d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.postdeploy = isotoma.recipe.postdeploy:main
    #
    # Would create an entry point called isotoma.recipe.postdeploy that calls isotoma.recipe.postdeploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - jinja2
    - missingbits

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - jinja2
    - missingbits

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.postdeploy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.postdeploy
  license: Apache Software License
  summary: 'Buildout recipes for postdeploy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
