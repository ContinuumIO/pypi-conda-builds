package:
  name: incf.dai
  version: "0.7"

source:
  fn: incf.dai-0.7.tar.gz
  url: https://pypi.python.org/packages/source/i/incf.dai/incf.dai-0.7.tar.gz
  md5: 767f78ecddd7a5e96e1d7c380f1881cd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incf.dai = incf.dai:main
    #
    # Would create an entry point called incf.dai that calls incf.dai.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - httplib2
    - odict

  run:
    - python
    - setuptools
    - setuptools
    - httplib2
    - odict

test:
  # Python imports
  imports:
    - incf
    - incf.dai

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://software.incf.org/software/incfdai
  license: GPL
  summary: 'Python API for the INCF Digital Atlasing infrastructure'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
