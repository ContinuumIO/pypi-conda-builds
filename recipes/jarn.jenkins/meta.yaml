package:
  name: jarn.jenkins
  version: "1.0b2"

source:
  fn: jarn.jenkins-1.0b2.zip
  url: https://pypi.python.org/packages/source/j/jarn.jenkins/jarn.jenkins-1.0b2.zip
  md5: 24d4b9cd54ac05a008870992f6228959
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jarn.jenkins = jarn.jenkins:main
    #
    # Would create an entry point called jarn.jenkins that calls jarn.jenkins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iw.recipe.template
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - iw.recipe.template
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - jarn
    - jarn.jenkins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/jarn.jenkins
  license: BSD License
  summary: 'Buildout recipe for installing a Jenkins instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
