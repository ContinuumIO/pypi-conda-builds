package:
  name: django-airbrake
  version: "1.1.0"

source:
  fn: django-airbrake-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-airbrake/django-airbrake-1.1.0.tar.gz
  md5: 8e2a07c2db135110a4a825034b94dbe1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-airbrake = django-airbrake:main
    #
    # Would create an entry point called django-airbrake that calls django-airbrake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - airbrake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Bouke/django-airbrake
  license: MIT License
  summary: 'Airbrake exception logger for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
