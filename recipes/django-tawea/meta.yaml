package:
  name: django-tawea
  version: "0.1"

source:
  fn: django-tawea-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tawea/django-tawea-0.1.tar.gz
  md5: 879f32237789ec3779e08ae1cd17385b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tawea = django-tawea:main
    #
    # Would create an entry point called django-tawea that calls django-tawea.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tawea
    - tawea.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/javimb/django-tawea
  license: BSD License
  summary: 'Add Facebook chat (Tawea) to a template with a simple tag.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
