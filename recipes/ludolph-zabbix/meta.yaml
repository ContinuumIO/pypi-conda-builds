package:
  name: ludolph-zabbix
  version: "1.0"

source:
  fn: ludolph-zabbix-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ludolph-zabbix/ludolph-zabbix-1.0.tar.gz
  md5: fb01b8853e42d2e12a14b4254db8ffdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ludolph-zabbix = ludolph-zabbix:main
    #
    # Would create an entry point called ludolph-zabbix that calls ludolph-zabbix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ludolph
    - zabbix-api-erigones

  run:
    - python
    - ludolph
    - zabbix-api-erigones

test:
  # Python imports
  imports:
    - ludolph_zabbix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erigones/ludolph-zabbix/
  license: MIT License
  summary: 'Ludolph: Zabbix API plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
