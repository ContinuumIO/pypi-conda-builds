package:
  name: django-db-readonly
  version: "0.3.3"

source:
  fn: django-db-readonly-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-readonly/django-db-readonly-0.3.3.tar.gz
  md5: f22d2f14be6fc173c18319b03e394eef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-readonly = django-db-readonly:main
    #
    # Would create an entry point called django-db-readonly that calls django-db-readonly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - readonly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/streeter/django-db-readonly
  license: MIT License
  summary: 'Add a global database read-only setting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
