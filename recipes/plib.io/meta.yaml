package:
  name: plib.io
  version: "0.9.6"

source:
  fn: plib.io-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/p/plib.io/plib.io-0.9.6.tar.gz
  md5: 8071721f0d560e296b2a88c09c4cce00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plib.io = plib.io:main
    #
    # Would create an entry point called plib.io that calls plib.io.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - plib
    - plib.io
    - plib.io.async
    - plib.io.base
    - plib.io.blocking
    - plib.io.classes
    - plib.io.comm
    - plib.io.data
    - plib.io.mixins
    - plib.io.serial
    - plib.io.socket
    - plib.test
    - plib.test.io

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plib.io
  license: GNU General Public License v2 (GPLv2)
  summary: 'Flexible handling of I/O channels.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
