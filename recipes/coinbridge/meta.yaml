package:
  name: coinbridge
  version: "0.1.3"

source:
  fn: coinbridge-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/coinbridge/coinbridge-0.1.3.tar.gz
  md5: c00b99fc86c2eece757a7f333524a13c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinbridge = coinbridge:main
    #
    # Would create an entry point called coinbridge that calls coinbridge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - psycopg2
    - bunch
    - python-jsonrpc

  run:
    - python
    - sqlalchemy
    - psycopg2
    - bunch
    - python-jsonrpc

test:
  # Python imports
  imports:
    - coinbridge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tensorjack/coinbridge
  license: MIT
  summary: 'Bitcoin/PostgreSQL bridge'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
