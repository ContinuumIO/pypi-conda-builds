package:
  name: django-gnupg-mails
  version: "0.1"

source:
  fn: django-gnupg-mails-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gnupg-mails/django-gnupg-mails-0.1.tar.gz
  md5: ce6feb1307aa5a5cd55cfd72feaa8596
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gnupg-mails = django-gnupg-mails:main
    #
    # Would create an entry point called django-gnupg-mails that calls django-gnupg-mails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-gnupg
    - sphinx-me

  run:
    - python
    - python-gnupg
    - sphinx-me

test:
  # Python imports
  imports:
    - gnupg_mails
    - gnupg_mails.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jandd/django-gnupg-mails
  license: UNKNOWN
  summary: 'A Django reusable app providing the ability to send PGP/MIME signed multipart emails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
