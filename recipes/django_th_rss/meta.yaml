package:
  name: django_th_rss
  version: "0.3.3"

source:
  fn: django_th_rss-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_th_rss/django_th_rss-0.3.3.tar.gz
  md5: 03546e26be81a58e5becb41e523e8ece
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_th_rss = django_th_rss:main
    #
    # Would create an entry point called django_th_rss that calls django_th_rss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_th >=0.10.1
    - feedparser ==5.1.3

  run:
    - python
    - django_th >=0.10.1
    - feedparser ==5.1.3

test:
  # Python imports
  imports:
    - django_th_rss
    - th_rss
    - th_rss.lib
    - th_rss.lib.conditionchecker
    - th_rss.lib.feedsservice
    - th_rss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/foxmask/django-th-rss
  license: BSD License
  summary: 'Django Trigger Happy : Service RSS to handle RSS/ATOM Feeds    and put those datas in the appropriate service of your choice'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
