package:
  name: hiero
  version: "0.3.4"

source:
  fn: hiero-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hiero/hiero-0.3.4.tar.gz
  md5: 5becd8ffd4d9015b0a98393d7dc74380
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hiero = hiero:main
    #
    # Would create an entry point called hiero that calls hiero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - pyramid_debugtoolbar
    - pyramid_tm
    - waitress
    - horus
    - celementtree
    - sqlalchemy
    - psycopg2
    - beautifulsoup
    - markdown
    - docutils
    - hem
    - horus

  run:
    - python
    - setuptools
    - pyramid
    - pyramid_debugtoolbar
    - pyramid_tm
    - waitress
    - horus
    - celementtree
    - sqlalchemy
    - psycopg2
    - beautifulsoup
    - markdown
    - docutils
    - hem
    - horus

test:
  # Python imports
  imports:
    - hiero
    - hiero.formatters
    - hiero.forms
    - hiero.models
    - hiero.schemas
    - hiero.tests
    - hiero.tests.blog
    - hiero.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'hiero'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
