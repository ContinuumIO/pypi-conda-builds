package:
  name: flask-mustachejs
  version: "0.6.0"

source:
  fn: Flask-MustacheJS-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MustacheJS/Flask-MustacheJS-0.6.0.tar.gz
  md5: 5e6856f27d0b3d67569b1b8f8604bc6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mustachejs = flask-mustachejs:main
    #
    # Would create an entry point called flask-mustachejs that calls flask-mustachejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pystache

  run:
    - python
    - flask
    - pystache

test:
  # Python imports
  imports:
    - flask_mustache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleywright/flask-mustachejs
  license: BSD License
  summary: 'Mustache integration in Flask, with Jinja and client-side libraries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
