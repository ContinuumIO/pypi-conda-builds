package:
  name: afapi
  version: "1.0.4"

source:
  fn: afapi-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/afapi/afapi-1.0.4.tar.gz
  md5: 63d689695cc696de4e9ee44af756a7c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - afapi = afapi:main
    #
    # Would create an entry point called afapi that calls afapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - afapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/appfirst/afapi
  license: Apache2
  summary: "A Python wrapper for interacting with AppFirst's APIs (v5)\n\nSee https://github.com/appfirst/afapi for more info."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
