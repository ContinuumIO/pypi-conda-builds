package:
  name: flask-noextref
  version: "0.1"

source:
  fn: Flask-NoExtRef-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-NoExtRef/Flask-NoExtRef-0.1.tar.gz
  md5: 4a3b669370cf16af5f189be93b8997af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-noextref = flask-noextref:main
    #
    # Would create an entry point called flask-noextref that calls flask-noextref.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ekalinin/flask-noextref
  license: BSD License
  summary: 'Support for hiding external URL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
