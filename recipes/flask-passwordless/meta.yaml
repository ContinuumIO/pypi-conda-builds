package:
  name: flask-passwordless
  version: "0.1.1"

source:
  fn: flask-passwordless-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-passwordless/flask-passwordless-0.1.1.tar.gz
  md5: a3b97e9c23457a0af15f524e19a9afaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-passwordless = flask-passwordless:main
    #
    # Would create an entry point called flask-passwordless that calls flask-passwordless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flask_passwordless

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevinjqiu/flask-passwordless
  license: BSD License
  summary: 'Flask extension for passwordless login'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
