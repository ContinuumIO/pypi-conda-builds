package:
  name: django-easybreadcrumbs
  version: "0.1"

source:
  fn: django-easybreadcrumbs-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-easybreadcrumbs/django-easybreadcrumbs-0.1.zip
  md5: 2b02aa92b7055343d5e5a09fa4f36216
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easybreadcrumbs = django-easybreadcrumbs:main
    #
    # Would create an entry point called django-easybreadcrumbs that calls django-easybreadcrumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - breadcrumbs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aruhlov/django-easybreadcrumbs/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django easy breadcrumbs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
