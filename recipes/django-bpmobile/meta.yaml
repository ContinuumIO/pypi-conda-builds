package:
  name: django-bpmobile
  version: "0.1"

source:
  fn: django-bpmobile-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bpmobile/django-bpmobile-0.1.tar.gz
  md5: 8c64252181ce37e7cbe6e64cac928a4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bpmobile = django-bpmobile:main
    #
    # Would create an entry point called django-bpmobile that calls django-bpmobile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - uamobile

  run:
    - python
    - uamobile

test:
  # Python imports
  imports:
    - bpmobile
    - bpmobile.e4u
    - bpmobile.management
    - bpmobile.management.commands
    - bpmobile.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tokibito/django-bpmobile/
  license: BSD License
  summary: 'A helper application for Japanese mobile phones.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
