package:
  name: django-cache-object
  version: "1.0.0"

source:
  fn: django-cache-object-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-object/django-cache-object-1.0.0.tar.gz
  md5: 343ce449a311ab22bc3d3f4200c76379
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-object = django-cache-object:main
    #
    # Would create an entry point called django-cache-object that calls django-cache-object.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cache_object

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yunmanger1/django-cache-object/
  license: WTFPL
  summary: 'This django module gives similar functionality like in django.db.models.loading'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
