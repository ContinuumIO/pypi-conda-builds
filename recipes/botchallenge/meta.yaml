package:
  name: botchallenge
  version: "1.2.0"

source:
  fn: botchallenge-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/botchallenge/botchallenge-1.2.0.tar.gz
  md5: 5d96ddd1ec54380553e2c4a8b6fa904e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - botchallenge = botchallenge:main
    #
    # Would create an entry point called botchallenge that calls botchallenge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - greenlet

  run:
    - python
    - greenlet

test:
  # Python imports
  imports:
    - botchallenge
    - botchallenge.api
    - google
    - google.protobuf
    - google.protobuf.compiler
    - google.protobuf.internal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/katharosada/botchallenge
  license: MIT License
  summary: 'A Python API for interacting with a Minecraft server plugin to control a bot.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
