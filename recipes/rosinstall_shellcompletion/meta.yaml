package:
  name: rosinstall_shellcompletion
  version: "0.0.2"

source:
  fn: rosinstall_shellcompletion-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/r/rosinstall_shellcompletion/rosinstall_shellcompletion-0.0.2.tar.gz
  md5: 4836d94190a8c26e05f2388456df4fc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rosinstall_shellcompletion = rosinstall_shellcompletion:main
    #
    # Would create an entry point called rosinstall_shellcompletion that calls rosinstall_shellcompletion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rosinstall

  run:
    - python
    - rosinstall

test:
  # Python imports
  imports:
    - rosinstall_shellcompletion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ros.org/wiki/rosinstall
  license: BSD License
  summary: 'Linux shell completion for rosinstall commands'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
