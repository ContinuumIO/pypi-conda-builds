package:
  name: brazilnum
  version: "0.7.1"

source:
  fn: brazilnum-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brazilnum/brazilnum-0.7.1.tar.gz
  md5: aab9928bb43c3e477b3c28299763a37f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brazilnum = brazilnum:main
    #
    # Would create an entry point called brazilnum that calls brazilnum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - brazilnum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/poliquin/brazilnum
  license: MIT License
  summary: 'Validators for Brazilian CNPJ, CEI, CPF, and PIS/PASEP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
