package:
  name: lwp
  version: "0.2-pre4"

source:
  fn: lwp-0.2-pre4.tar.gz
  url: https://pypi.python.org/packages/source/l/lwp/lwp-0.2-pre4.tar.gz
  md5: e7d79fcbbeb4ab8c3db055d74fdaf9b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lwp = lwp:main
    #
    # Would create an entry point called lwp that calls lwp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.9
    - arconfig
    - object_cacher

  run:
    - python
    - flask ==0.9
    - arconfig
    - object_cacher

test:
  # Python imports
  imports:
    - lwp
    - lwp.app
    - lwp.lxc
    - lwp.lxclite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'LXC Web Interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
