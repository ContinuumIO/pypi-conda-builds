package:
  name: exceptional-python
  version: "0.2.1"

source:
  fn: exceptional-python-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/e/exceptional-python/exceptional-python-0.2.1.tar.gz
  md5: f145e47222cb58f04b8b7fbfdb1f497b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exceptional-python = exceptional-python:main
    #
    # Would create an entry point called exceptional-python that calls exceptional-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - exceptional

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cenkalti/exceptional-python
  license: UNKNOWN
  summary: 'A python client for Exceptional (getexceptional.com), ported from pylons-exceptional'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
