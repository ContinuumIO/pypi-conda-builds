package:
  name: citibike
  version: "0.0.4"

source:
  fn: citibike-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/citibike/citibike-0.0.4.tar.gz
  md5: 059fe2c9a20dbb3e36187bed71b24e3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - citibike = citibike:main
    #
    # Would create an entry point called citibike that calls citibike.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bz2file
    - functional
    - geojson
    - python-gflags
    - requests
    - progressbar

  run:
    - python
    - bz2file
    - functional
    - geojson
    - python-gflags
    - requests
    - progressbar

test:
  # Python imports
  imports:
    - citibike

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamdeprince/python-citibike-data
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
