package:
  name: sqlalchemy_bigquery
  version: "0.0.5"

source:
  fn: sqlalchemy_bigquery-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy_bigquery/sqlalchemy_bigquery-0.0.5.tar.gz
  md5: df8c69965ea7acca8569179cf08f48eb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy_bigquery = sqlalchemy_bigquery:main
    #
    # Would create an entry point called sqlalchemy_bigquery that calls sqlalchemy_bigquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - sqlalchemy_bigquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'BigQuery for SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
