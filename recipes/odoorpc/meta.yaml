package:
  name: odoorpc
  version: "0.2.0"

source:
  fn: OdooRPC-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/O/OdooRPC/OdooRPC-0.2.0.tar.gz
  md5: eab1402d8fa4af7af0cf90dc979b53bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - odoorpc = odoorpc:main
    #
    # Would create an entry point called odoorpc that calls odoorpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - odoorpc
    - odoorpc.rpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/OdooRPC/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'OdooRPC is a Python module providing an easy way to pilot your Odoo servers through RPC.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
