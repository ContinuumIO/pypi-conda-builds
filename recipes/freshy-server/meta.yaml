package:
  name: freshy-server
  version: "0.2.3"

source:
  fn: freshy-server-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/f/freshy-server/freshy-server-0.2.3.tar.gz
  md5: ec6b439a78112c466f07aa5b56f621f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - freshy-server = freshy-server:main
    #
    # Would create an entry point called freshy-server that calls freshy-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - autobahn
    - watchdog

  run:
    - python
    - twisted
    - autobahn
    - watchdog

test:
  # Python imports
  imports:
    - freshy-server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lchi/freshy-server
  license: MIT License
  summary: 'Page refresh tool for web developers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
