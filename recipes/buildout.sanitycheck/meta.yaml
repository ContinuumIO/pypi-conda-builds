package:
  name: buildout.sanitycheck
  version: "1.0b1"

source:
  fn: buildout.sanitycheck-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.sanitycheck/buildout.sanitycheck-1.0b1.tar.gz
  md5: 96cfbfb25139aa2470e7c71e751acf10
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.sanitycheck = buildout.sanitycheck:main
    #
    # Would create an entry point called buildout.sanitycheck that calls buildout.sanitycheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - buildout
    - buildout.sanitycheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License v2 (GPLv2)
  summary: 'Sanity checks for buildout environment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
