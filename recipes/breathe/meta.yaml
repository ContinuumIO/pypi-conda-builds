package:
  name: breathe
  version: "4.0.0"

source:
  fn: breathe-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/breathe/breathe-4.0.0.tar.gz
  md5: 32316d5a890a3124ea3e8a9e0b2b3b97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - breathe = breathe:main
    #
    # Would create an entry point called breathe that calls breathe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.0.7
    - docutils >=0.5

  run:
    - python
    - sphinx >=1.0.7
    - docutils >=0.5

test:
  # Python imports
  imports:
    - breathe
    - breathe.directive
    - breathe.finder
    - breathe.finder.doxygen
    - breathe.parser
    - breathe.parser.doxygen
    - breathe.renderer
    - breathe.renderer.rst
    - breathe.renderer.rst.doxygen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/michaeljones/breathe
  license: BSD License
  summary: 'Sphinx Doxygen renderer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
