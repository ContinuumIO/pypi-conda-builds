package:
  name: azoufzouf
  version: "15.02.15"

source:
  fn: azoufzouf-15.02.15.tar.gz
  url: https://pypi.python.org/packages/source/a/azoufzouf/azoufzouf-15.02.15.tar.gz
  md5: 4db85551844ad0ee131597bfb34bf738
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - azoufzouf = azoufzouf:main
    #
    # Would create an entry point called azoufzouf that calls azoufzouf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - pygments
    - jinja2

  run:
    - python
    - docopt
    - pygments
    - jinja2

test:
  # Python imports
  imports:
    - azoufzouf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: frak it!
  summary: 'Slick markup language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
