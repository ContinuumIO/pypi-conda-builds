package:
  name: ardj
  version: "1.2.42"

source:
  fn: ardj-1.2.42.tar.gz
  url: https://pypi.python.org/packages/source/a/ardj/ardj-1.2.42.tar.gz
  md5: 0ac8ca80722b996e5390e4c887bedd60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ardj = ardj:main
    #
    # Would create an entry point called ardj that calls ardj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - mutagen
    - pydns
    - oauth2
    - web.py

  run:
    - python
    - pyyaml
    - mutagen
    - pydns
    - oauth2
    - web.py

test:
  # Python imports
  imports:
    - ardj
    - ardj.xmpp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://umonkey.net/ardj/
  license: GNU General Public License (GPL)
  summary: 'An artificial DJ for you internet radio.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
