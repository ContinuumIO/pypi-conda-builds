package:
  name: menhir.contenttype.image
  version: "0.4"

source:
  fn: menhir.contenttype.image-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/menhir.contenttype.image/menhir.contenttype.image-0.4.tar.gz
  md5: 0d9e5d718470c6db5f9569e0650ac457
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - menhir.contenttype.image = menhir.contenttype.image:main
    #
    # Would create an entry point called menhir.contenttype.image that calls menhir.contenttype.image.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dolmen.app.content >=1.0b1
    - dolmen.app.layout
    - dolmen.app.security
    - dolmen.blob
    - dolmen.content >=0.7
    - dolmen.file
    - dolmen.thumbnailer
    - dolmen.widget.image
    - fanstatic
    - grokcore.view
    - js.jquery_slimbox
    - setuptools
    - zope.i18nmessageid
    - zope.size

  run:
    - python
    - setuptools
    - dolmen.app.content >=1.0b1
    - dolmen.app.layout
    - dolmen.app.security
    - dolmen.blob
    - dolmen.content >=0.7
    - dolmen.file
    - dolmen.thumbnailer
    - dolmen.widget.image
    - fanstatic
    - grokcore.view
    - js.jquery_slimbox
    - setuptools
    - zope.i18nmessageid
    - zope.size

test:
  # Python imports
  imports:
    - menhir
    - menhir.contenttype
    - menhir.contenttype.image

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tracker.trollfot.org/
  license: GNU General Public License (GPL)
  summary: 'Dolmen content-type extension : image'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
