package:
  name: djangorequestlogger
  version: "0.2.0"

source:
  fn: DjangoRequestLogger-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoRequestLogger/DjangoRequestLogger-0.2.0.tar.gz
  md5: 555d14c0d6181a5041d8689828dafae8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorequestlogger = djangorequestlogger:main
    #
    # Would create an entry point called djangorequestlogger that calls djangorequestlogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_request_logger
    - django_request_logger.test
    - djangorequestlogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/DjangoRequestLogger/
  license: LICENSE.txt
  summary: 'Django app to add current username, client IP, etc. to logging output.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
