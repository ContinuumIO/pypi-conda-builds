package:
  name: flask-httpcaching
  version: "0.01"

source:
  fn: Flask-HttpCaching-0.01.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-HttpCaching/Flask-HttpCaching-0.01.tar.gz
  md5: f3c8f22e3f40b75afb63a4981a800f24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-httpcaching = flask-httpcaching:main
    #
    # Would create an entry point called flask-httpcaching that calls flask-httpcaching.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zakzou/flask_httpcaching
  license: MIT License
  summary: 'flask http caching'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
