package:
  name: flask-linktester
  version: "0.2.5"

source:
  fn: Flask-LinkTester-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-LinkTester/Flask-LinkTester-0.2.5.tar.gz
  md5: 684b6c9ba520c85a69487c943476d817
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-linktester = flask-linktester:main
    #
    # Would create an entry point called flask-linktester that calls flask-linktester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests

  run:
    - python
    - flask
    - requests

test:
  # Python imports
  imports:
    - flask_linktester

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sfermigier/flask-linktester
  license: BSD License
  summary: 'Link tester for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
