package:
  name: argumentparser
  version: "1.2.1"

source:
  fn: argumentparser-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/argumentparser/argumentparser-1.2.1.tar.gz
  md5: dab01673e973a3e6772d37f19436992e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argumentparser = argumentparser:main
    #
    # Would create an entry point called argumentparser that calls argumentparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ArgumentParser
    - argumentparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Python 2/3 compatible commandline argument parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
