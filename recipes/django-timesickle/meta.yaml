package:
  name: django-timesickle
  version: "0.2"

source:
  fn: django-timesickle-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timesickle/django-timesickle-0.2.tar.gz
  md5: 32b1f19e770c6970d9caae2901087600
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timesickle = django-timesickle:main
    #
    # Would create an entry point called django-timesickle that calls django-timesickle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - six

  run:
    - python
    - django
    - six

test:
  # Python imports
  imports:
    - timesickle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jleeothon/timesickle
  license: MIT License
  summary: 'Template name resolution for Django class-based views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
