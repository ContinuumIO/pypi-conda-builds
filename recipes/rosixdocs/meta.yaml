package:
  name: rosixdocs
  version: "0.1.1"

source:
  fn: rosixdocs-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/r/rosixdocs/rosixdocs-0.1.1.tar.gz
  md5: ab22510630b79fcb7cfc3ca54e384cdf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rosixdocs = rosixdocs:main
    #
    # Would create an entry point called rosixdocs that calls rosixdocs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.1

  run:
    - python
    - sphinx >=1.1

test:
  # Python imports
  imports:
    - rosixdocs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/djbaldey/rosixdocs
  license: MIT License
  summary: 'Theme for Sphinx from Rosix projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
