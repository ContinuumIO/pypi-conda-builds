package:
  name: django-fileflatpages
  version: "0.1"

source:
  fn: django-fileflatpages-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fileflatpages/django-fileflatpages-0.1.tar.gz
  md5: 8107c55d2ba574dfd2c248e802d62618
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fileflatpages = django-fileflatpages:main
    #
    # Would create an entry point called django-fileflatpages that calls django-fileflatpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - fileflatpages
    - fileflatpages.management
    - fileflatpages.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/keegan_csmith/django-fileflatpages
  license: BSD License
  summary: 'Django App for storing FlatPage fixtures per file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
