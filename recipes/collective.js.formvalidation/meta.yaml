package:
  name: collective.js.formvalidation
  version: "1.0b1"

source:
  fn: collective.js.formvalidation-1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.formvalidation/collective.js.formvalidation-1.0b1.zip
  md5: 05a9b1fb14b0fb93dcc3b5a6f6dad4e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.formvalidation = collective.js.formvalidation:main
    #
    # Would create an entry point called collective.js.formvalidation that calls collective.js.formvalidation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.formvalidation
    - collective.js.formvalidation.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.formvalidation
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
