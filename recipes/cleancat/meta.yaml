package:
  name: cleancat
  version: "0.4.1"

source:
  fn: cleancat-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cleancat/cleancat-0.4.1.tar.gz
  md5: ebfd0318db5ffe08a61c841658025ba3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cleancat = cleancat:main
    #
    # Would create an entry point called cleancat that calls cleancat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil

  run:
    - python
    - python-dateutil

test:
  # Python imports
  imports:
    - cleancat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/elasticsales/cleancat
  license: MIT License
  summary: 'Validation library for Python designed to be used with JSON REST frameworks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
