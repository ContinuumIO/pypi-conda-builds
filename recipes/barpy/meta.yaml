package:
  name: barpy
  version: "0.1.2"

source:
  fn: barpy-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/barpy/barpy-0.1.2.tar.gz
  md5: 74bf895f0c95c608c00482d9c4b49415
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barpy = barpy:main
    #
    # Would create an entry point called barpy that calls barpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil
    - qrcode

  run:
    - python
    - pil
    - qrcode

test:
  # Python imports
  imports:
    - barpy
    - barpy.barcode
    - barpy.outputter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kstep/barpy/
  license: BSD License
  summary: 'Ruby barby barcodes generating library port to Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
