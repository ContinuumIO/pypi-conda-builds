package:
  name: django-avocado
  version: "0.2.0"

source:
  fn: Django-Avocado-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Avocado/Django-Avocado-0.2.0.tar.gz
  md5: ecd0f083f2db98336a94df73eb8677f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-avocado = django-avocado:main
    #
    # Would create an entry point called django-avocado that calls django-avocado.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - dse

  run:
    - python
    - django
    - dse

test:
  # Python imports
  imports:
    - avocado

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/weholt/django-avocado
  license: GNU General Public License (GPL)
  summary: 'Delicious delayed and cached database-logging for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
