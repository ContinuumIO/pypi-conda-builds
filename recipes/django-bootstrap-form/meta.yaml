package:
  name: django-bootstrap-form
  version: "3.2"

source:
  fn: django-bootstrap-form-3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-form/django-bootstrap-form-3.2.tar.gz
  md5: 1e95b05a12362fe17e91b962c41d139e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-form = django-bootstrap-form:main
    #
    # Would create an entry point called django-bootstrap-form that calls django-bootstrap-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - bootstrapform
    - bootstrapform.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tzangms/django-bootstrap-form
  license: BSD
  summary: 'django-bootstrap-form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
