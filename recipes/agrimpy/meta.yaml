package:
  name: agrimpy
  version: "0.0.3"

source:
  fn: agrimpy-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/agrimpy/agrimpy-0.0.3.tar.gz
  md5: 964de24db5446e4e0826241a3fc35bb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agrimpy = agrimpy:main
    #
    # Would create an entry point called agrimpy that calls agrimpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse ==1.2.2
    - pyproj ==1.9.3

  run:
    - python
    - argparse ==1.2.2
    - pyproj ==1.9.3

test:
  # Python imports
  imports:
    - agrimpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/agrimpy/
  license: LICENSE.txt
  summary: 'Algunas operaciones con coordenadas geod\xc3\xa9sicas.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
