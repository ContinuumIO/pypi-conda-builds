package:
  name: vxaat
  version: "0.5.2"

source:
  fn: vxaat-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/v/vxaat/vxaat-0.5.2.tar.gz
  md5: af5dbfd7a33ce80c31672c970f217739
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vxaat = vxaat:main
    #
    # Would create an entry point called vxaat that calls vxaat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vumi
    - twisted >=13.1.0

  run:
    - python
    - vumi
    - twisted >=13.1.0

test:
  # Python imports
  imports:
    - vxaat
    - vxaat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/vumi-aat
  license: BSD License
  summary: 'An AAT USSD transport for Vumi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
