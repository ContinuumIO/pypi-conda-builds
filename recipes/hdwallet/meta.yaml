package:
  name: hdwallet
  version: "0.3"

source:
  fn: hdwallet-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hdwallet/hdwallet-0.3.tar.gz
  md5: 20aed57a0db7063d5298cb70af436ea3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hdwallet = hdwallet:main
    #
    # Would create an entry point called hdwallet that calls hdwallet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - ecdsa ==0.8

  run:
    - python
    - ecdsa ==0.8

test:
  # Python imports
  imports:
    - hdwallet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FelixWeis/python-hdwallet
  license: MIT
  summary: 'Secure, hierarchical Bitcoin wallet generation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
