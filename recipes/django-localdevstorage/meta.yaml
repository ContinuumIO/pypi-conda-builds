package:
  name: django-localdevstorage
  version: "0.5"

source:
  fn: django-localdevstorage-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localdevstorage/django-localdevstorage-0.5.tar.gz
  md5: 6efd773929303e60d6bfb8d548bbef98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localdevstorage = django-localdevstorage:main
    #
    # Would create an entry point called django-localdevstorage that calls django-localdevstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - requests >=1.0

  run:
    - python
    - django >=1.2
    - requests >=1.0

test:
  # Python imports
  imports:
    - localdevstorage
    - localdevstorage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piquadrat/django-localdevstorage
  license: BSD License
  summary: 'A Django storage backend for local development that downloads files from the live site on the fly.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
