package:
  name: django-mptt-urls
  version: "2.0.0a1"

source:
  fn: django-mptt-urls-2.0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mptt-urls/django-mptt-urls-2.0.0a1.tar.gz
  md5: 64c1522a70e4970a66e0a7a5b453ff34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mptt-urls = django-mptt-urls:main
    #
    # Would create an entry point called django-mptt-urls that calls django-mptt-urls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-mptt >=0.6.1

  run:
    - python
    - django >=1.7
    - django-mptt >=0.6.1

test:
  # Python imports
  imports:
    - mptt_urls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/c0ntribut0r/django-mptt-urls
  license: MIT License
  summary: 'Django app for creating hierarchical URLs associated with django-MPTT models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
