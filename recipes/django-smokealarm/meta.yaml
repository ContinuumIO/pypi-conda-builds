package:
  name: django-smokealarm
  version: "2013.05.13.0"

source:
  fn: django-smokealarm-2013.05.13.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smokealarm/django-smokealarm-2013.05.13.0.tar.gz
  md5: c3ef3f86d24ba5c070e520552c82167e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smokealarm = django-smokealarm:main
    #
    # Would create an entry point called django-smokealarm that calls django-smokealarm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - smokealarm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradbeattie/django-smokealarm
  license: BSD License
  summary: 'Simple JavaScript error reporting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
