package:
  name: httpie-msgpack
  version: "1.0.0"

source:
  fn: httpie-msgpack-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httpie-msgpack/httpie-msgpack-1.0.0.tar.gz
  md5: 0096f8641bcc90db3a7f6ff2f78ccf4a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpie-msgpack = httpie-msgpack:main
    #
    # Would create an entry point called httpie-msgpack that calls httpie-msgpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httpie >=0.7.0
    - msgpack-python

  run:
    - python
    - setuptools
    - httpie >=0.7.0
    - msgpack-python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rasky/httpie-msgpack
  license: BSD License
  summary: 'Msgpack plugin for HTTPie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
