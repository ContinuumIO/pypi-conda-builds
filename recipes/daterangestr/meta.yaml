package:
  name: daterangestr
  version: "0.0.3"

source:
  fn: daterangestr-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/daterangestr/daterangestr-0.0.3.tar.gz
  md5: 22a61e85902016b1d5b08caae1957050
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daterangestr = daterangestr:main
    #
    # Would create an entry point called daterangestr that calls daterangestr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - daterangestr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marians/py-daterangestr
  license: UNKNOWN
  summary: 'Utility to convert strings like "201301-201302" to start and end datetime tuples'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
