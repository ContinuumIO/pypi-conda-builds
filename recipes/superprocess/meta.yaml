package:
  name: superprocess
  version: "0.1.1"

source:
  fn: superprocess-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/superprocess/superprocess-0.1.1.tar.gz
  md5: 6faf3971b8de46f4e686291cb3b37a4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - superprocess = superprocess:main
    #
    # Would create an entry point called superprocess that calls superprocess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - superprocess

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sjdrake/superprocess
  license: Apache Software License
  summary: 'subprocess-like API for starting local and remote processes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
