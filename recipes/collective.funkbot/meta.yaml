package:
  name: collective.funkbot
  version: "1.0"

source:
  fn: collective.funkbot-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.funkbot/collective.funkbot-1.0.zip
  md5: 272b29540cc51a7ebe22c5372105047e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.funkbot = collective.funkbot:main
    #
    # Would create an entry point called collective.funkbot that calls collective.funkbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript

  run:
    - python
    - setuptools
    - setuptools
    - pastescript

test:
  # Python imports
  imports:
    - collective
    - collective.funkbot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.funkbot
  license: GPL
  summary: 'Using buildbot and funkload, allow you to have a feedback of your modifications of a python project with funkload differencial reports and buildbot waterfall'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
