package:
  name: jsobject
  version: "0.10.2"

source:
  fn: jsobject-0.10.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsobject/jsobject-0.10.2.tar.gz
  md5: 7d2cebdce1366d158951024e97c39d20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsobject = jsobject:main
    #
    # Would create an entry point called jsobject that calls jsobject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsobject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mavier.github.io/jsobject
  license: MIT License
  summary: 'Jsobject is simple implementation JavaScript-Style Objects                  in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
