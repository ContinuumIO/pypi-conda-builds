package:
  name: django-pygmy
  version: "0.1.5"

source:
  fn: django-pygmy-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pygmy/django-pygmy-0.1.5.tar.gz
  md5: 9f78844d44a8c355dd9ed8f9d83d162c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pygmy = django-pygmy:main
    #
    # Would create an entry point called django-pygmy that calls django-pygmy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments >=1.6

  run:
    - python
    - pygments >=1.6

test:
  # Python imports
  imports:
    - django_pygmy
    - django_pygmy.templatetags
    - django_pygmy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattack108/django-pygmy/
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
