package:
  name: pystrext
  version: "0.1.7"

source:
  fn: pystrext-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/p/pystrext/pystrext-0.1.7.tar.gz
  md5: 517183c99512ffbef58188ad837164eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystrext = pystrext:main
    #
    # Would create an entry point called pystrext that calls pystrext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx
    - sphinxcontrib-napoleon

  run:
    - python
    - sphinx
    - sphinxcontrib-napoleon

test:
  # Python imports
  imports:
    - pystrext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/pystrext
  license: LGPL 2.1
  summary: 'Python string extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
