package:
  name: hyperspace
  version: "0.1.0"

source:
  fn: hyperspace-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hyperspace/hyperspace-0.1.0.tar.gz
  md5: 2f8caa9de2bcf366232ba54216ce8fec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyperspace = hyperspace:main
    #
    # Would create an entry point called hyperspace that calls hyperspace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - rdflib
    - rdflib-jsonld
    - beautifulsoup4
    - html5lib

  run:
    - python
    - requests
    - rdflib
    - rdflib-jsonld
    - beautifulsoup4
    - html5lib

test:
  # Python imports
  imports:
    - hyperspace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rossfenning.co.uk/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'General-purpose REST/hypermedia client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
