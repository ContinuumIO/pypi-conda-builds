package:
  name: bayesian
  version: "0.3.1"

source:
  fn: Bayesian-0.3.1.zip
  url: https://pypi.python.org/packages/source/B/Bayesian/Bayesian-0.3.1.zip
  md5: d6ffb8409789dbac5cef54679d0bf205
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bayesian = bayesian:main
    #
    # Would create an entry point called bayesian that calls bayesian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bayesian

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bayesian/
  license: LICENSE.txt
  summary: 'Library and utility module for Bayesian reasoning'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
