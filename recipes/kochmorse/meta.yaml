package:
  name: kochmorse
  version: "2.0.0"

source:
  fn: KochMorse-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/K/KochMorse/KochMorse-2.0.0.tar.gz
  md5: 42b393dbc389d09c32d44de6c9a89596
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kochmorse = kochmorse:main
    #
    # Would create an entry point called kochmorse that calls kochmorse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - KochMorse
    - KochMorse.alsamorse
    - kochmorse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kochmorse.googlecode.com
  license: GNU General Public License (GPL)
  summary: ' Morse-tutor for Linux using the Koch-method. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
