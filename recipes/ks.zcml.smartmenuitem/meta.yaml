package:
  name: ks.zcml.smartmenuitem
  version: "1.0.2"

source:
  fn: ks.zcml.smartmenuitem-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/k/ks.zcml.smartmenuitem/ks.zcml.smartmenuitem-1.0.2.tar.gz
  md5: 508d671b07468b81bc07792515890c3a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ks.zcml.smartmenuitem = ks.zcml.smartmenuitem:main
    #
    # Would create an entry point called ks.zcml.smartmenuitem that calls ks.zcml.smartmenuitem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.cachedescriptors
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.traversing
    - zope.exceptions
    - zope.app.publisher
    - zope.configuration

  run:
    - python
    - setuptools
    - zope.cachedescriptors
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.traversing
    - zope.exceptions
    - zope.app.publisher
    - zope.configuration

test:
  # Python imports
  imports:
    - ks
    - ks.zcml
    - ks.zcml.smartmenuitem
    - ks.zcml.smartmenuitem.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.keysolutionscorp.com/downloads/public/pypi/ks.zcml.smartmenuitem
  license: Zope Public License
  summary: 'Create really "smart" menu items with ks.zcml.smartmenuitem for Hivurt'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
