package:
  name: election_parser
  version: "0.0.1"

source:
  fn: election_parser-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/election_parser/election_parser-0.0.1.tar.gz
  md5: ffad246126e91ff0eee7f42bd35d8eee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - election_parser = election_parser:main
    #
    # Would create an entry point called election_parser that calls election_parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko
    - python-gflags
    - pycurl
    - scpclient
    - unittest2

  run:
    - python
    - paramiko
    - python-gflags
    - pycurl
    - scpclient
    - unittest2

test:
  # Python imports
  imports:
    - election_parser
    - election_parser.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adamdeprince/election_parser
  license: GPLv2
  summary: 'Parse US real time and archived election data.  Really only supports CA real time rigth now'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
