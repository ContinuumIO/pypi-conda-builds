package:
  name: lizard-ui
  version: "5.1.1"

source:
  fn: lizard-ui-5.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lizard-ui/lizard-ui-5.1.1.tar.gz
  md5: 67985bd07cb0662c0fd421113526dfd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lizard-ui = lizard-ui:main
    #
    # Would create an entry point called lizard-ui that calls lizard-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup
    - django >=1.4,<1.7
    - django-nose
    - django-extensions
    - django_compressor >=1.1
    - docutils
    - lizard-security
    - lizard-map
    - raven
    - south
    - werkzeug
    - translations

  run:
    - python
    - beautifulsoup
    - django >=1.4,<1.7
    - django-nose
    - django-extensions
    - django_compressor >=1.1
    - docutils
    - lizard-security
    - lizard-map
    - raven
    - south
    - werkzeug
    - translations

test:
  # Python imports
  imports:
    - lizard_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nelen-schuurmans.nl/lizard/
  license: LGPL
  summary: 'Basic user interface for lizard websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
