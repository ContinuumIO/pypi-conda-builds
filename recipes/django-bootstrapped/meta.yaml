package:
  name: django-bootstrapped
  version: "0.1.1-dev"

source:
  fn: django-bootstrapped-0.1.1-dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrapped/django-bootstrapped-0.1.1-dev.tar.gz
  md5: a3a58b714aaf03dc6e8c6525c32a20bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrapped = django-bootstrapped:main
    #
    # Would create an entry point called django-bootstrapped that calls django-bootstrapped.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrapped
    - bootstrapped.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rbrady/django-bootstrapped
  license: BSD License
  summary: 'A reusable Django application to quickly integrate the Bootstrap toolkit from Twitter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
