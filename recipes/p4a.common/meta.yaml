package:
  name: p4a.common
  version: "1.1"

source:
  fn: p4a.common-1.1.zip
  url: https://pypi.python.org/packages/source/p/p4a.common/p4a.common-1.1.zip
  md5: d904862d11eb406f4c2fdba16c6fbfa9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.common = p4a.common:main
    #
    # Would create an entry point called p4a.common that calls p4a.common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-dateutil

  run:
    - python
    - setuptools
    - setuptools
    - python-dateutil

test:
  # Python imports
  imports:
    - p4a
    - p4a.common
    - p4a.common.descriptors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/p4a.common/
  license: GPL
  summary: 'Reusable code-bits for Zope 3 and Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
