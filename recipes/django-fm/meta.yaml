package:
  name: django-fm
  version: "0.1.9"

source:
  fn: django-fm-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fm/django-fm-0.1.9.tar.gz
  md5: 31ba2699ad3f4e6b7a6cd787e4b6b2e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fm = django-fm:main
    #
    # Would create an entry point called django-fm that calls django-fm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-crispy-forms >=1.4.0

  run:
    - python
    - django-crispy-forms >=1.4.0

test:
  # Python imports
  imports:
    - fm
    - fm/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FZambia/django-fm/
  license: MIT
  summary: 'Twitter Bootstrap 3 based modal AJAX form for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
