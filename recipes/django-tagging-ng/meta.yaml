package:
  name: django-tagging-ng
  version: "0.3.3"

source:
  fn: django-tagging-ng-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tagging-ng/django-tagging-ng-0.3.3.tar.gz
  md5: 3a848da89f52dc06ebf46a5bb4090605
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tagging-ng = django-tagging-ng:main
    #
    # Would create an entry point called django-tagging-ng that calls django-tagging-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tagging
    - tagging.templatetags
    - tagging.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-tagging-ng/
  license: BSD License
  summary: 'Enhanced tagging application for Django, based on django-tagging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
