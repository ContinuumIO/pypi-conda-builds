package:
  name: django-user-guide
  version: "0.8.3"

source:
  fn: django-user-guide-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-guide/django-user-guide-0.8.3.tar.gz
  md5: 0eafd61da11a0ebaa2872723ed9961ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-guide = django-user-guide:main
    #
    # Would create an entry point called django-user-guide that calls django-user-guide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - user_guide
    - user_guide.migrations
    - user_guide.south_migrations
    - user_guide.templatetags
    - user_guide.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-user-guide
  license: MIT License
  summary: 'Show configurable HTML guides to users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
