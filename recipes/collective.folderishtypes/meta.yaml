package:
  name: collective.folderishtypes
  version: "2.0.1"

source:
  fn: collective.folderishtypes-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.folderishtypes/collective.folderishtypes-2.0.1.tar.gz
  md5: 259e039f59689d6a8276e931b76da1fd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.folderishtypes = collective.folderishtypes:main
    #
    # Would create an entry point called collective.folderishtypes that calls collective.folderishtypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.schema
    - plone.app.imaging
    - zope.i18nmessageid

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.schema
    - plone.app.imaging
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - collective
    - collective.folderishtypes
    - collective.folderishtypes.at
    - collective.folderishtypes.at.browser
    - collective.folderishtypes.at.migration
    - collective.folderishtypes.browser
    - collective.folderishtypes.content
    - collective.folderishtypes.dx
    - collective.folderishtypes.dx.migration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.folderishtypes
  license: GPL
  summary: 'Folderish News Item, Document and Event as replacement for default types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
