package:
  name: livewires
  version: "2.1"

source:
  fn: LiveWires-2.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LiveWires/LiveWires-2.1.tar.gz
  md5: 738196e78637d5da16d2dc62996f45a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - livewires = livewires:main
    #
    # Would create an entry point called livewires that calls livewires.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygame

  run:
    - python
    - pygame

test:
  # Python imports
  imports:
    - livewires

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.livewires.org.uk/python/
  license: UNKNOWN
  summary: 'LiveWires package provides resources for people learning Python. It is intended for use with the LiveWires Python Course'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
