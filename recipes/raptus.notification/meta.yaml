package:
  name: raptus.notification
  version: "1.0a4"

source:
  fn: raptus.notification-1.0a4.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.notification/raptus.notification-1.0a4.tar.gz
  md5: 039cd50a976035589b396e6f4a09f5b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.notification = raptus.notification:main
    #
    # Would create an entry point called raptus.notification that calls raptus.notification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - raptus
    - raptus.notification
    - raptus.notification.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/raptus.notification
  license: GPL
  summary: 'Provides a generic way to send notifications to users in plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
