package:
  name: cloudplay
  version: "0.1"

source:
  fn: cloudplay-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudplay/cloudplay-0.1.tar.gz
  md5: f2e59a58162254e0267e87f1b5ce573f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudplay = cloudplay:main
    #
    # Would create an entry point called cloudplay that calls cloudplay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - lxml
    - requests

  run:
    - python
    - lxml
    - requests

test:
  # Python imports
  imports:
    - cloudplay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wcmaier/cloudplay
  license: ISC License (ISCL)
  summary: 'play the cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
