package:
  name: django-eloqua
  version: "0.4.1"

source:
  fn: django-eloqua-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-eloqua/django-eloqua-0.4.1.tar.gz
  md5: b781c3f74d5284531b77265fa17c7d9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-eloqua = django-eloqua:main
    #
    # Would create an entry point called django-eloqua that calls django-eloqua.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0
    - requests >=1.1.0

  run:
    - python
    - django >=1.3.0
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - eloqua
    - eloqua.management
    - eloqua.management.commands
    - eloqua.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svdgraaf/django-eloqua/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Python/Django interface for the Eloqua REST api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
