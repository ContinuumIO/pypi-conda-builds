package:
  name: live_stylus
  version: "0.2.7"

source:
  fn: live_stylus-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/l/live_stylus/live_stylus-0.2.7.tar.gz
  md5: ff7b55c8eaeacf6e26199756095d91e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - live_stylus = live_stylus:main
    #
    # Would create an entry point called live_stylus that calls live_stylus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stylus
    - watchdog

  run:
    - python
    - stylus
    - watchdog

test:
  # Python imports
  imports:
    - live_stylus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allenm/python-live-stylus
  license: MIT License
  summary: 'Convert stylus to css real time. Easily used by any web framwork.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
