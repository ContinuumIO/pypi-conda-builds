package:
  name: wtop
  version: "0.7.9"

source:
  fn: wtop-0.7.9.tar.gz
  url: https://pypi.python.org/packages/source/w/wtop/wtop-0.7.9.tar.gz
  md5: 1a77c8c8c28a58170273b07b3cdfd1e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtop = wtop:main
    #
    # Would create an entry point called wtop that calls wtop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-iqm

  run:
    - python
    - python-iqm

test:
  # Python imports
  imports:
    - wtop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ClockworkNet/wtop
  license: BSD License
  summary: "'top' for Apache and other web servers, plus powerful loggrepping"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
