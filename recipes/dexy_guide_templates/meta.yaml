package:
  name: dexy_guide_templates
  version: "0.0.5"

source:
  fn: dexy_guide_templates-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dexy_guide_templates/dexy_guide_templates-0.0.5.tar.gz
  md5: 1b9f40ef3f1bf2152268669670c042a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dexy_guide_templates = dexy_guide_templates:main
    #
    # Would create an entry point called dexy_guide_templates that calls dexy_guide_templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dexy >=0.9.9.4

  run:
    - python
    - dexy >=0.9.9.4

test:
  # Python imports
  imports:
    - dexy_guide_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dexy.it
  license: MIT License
  summary: 'Templates for the Dexy Guide, including Getting Started.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
