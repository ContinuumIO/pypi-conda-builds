package:
  name: beetle-sass
  version: "1.2.0"

source:
  fn: Beetle-Sass-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/B/Beetle-Sass/Beetle-Sass-1.2.0.tar.gz
  md5: 0f4564031574c6dd9ce2f44cb6aab49c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beetle-sass = beetle-sass:main
    #
    # Would create an entry point called beetle-sass that calls beetle-sass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - libsass

  run:
    - python
    - libsass

test:
  # Python imports
  imports:
    - beetle_sass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Tenzer/beetle-sass
  license: MIT License
  summary: 'Enables Beetle to handle Sass files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
