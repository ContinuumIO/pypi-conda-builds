package:
  name: django-historicalrecords
  version: "1.1"

source:
  fn: django-historicalrecords-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-historicalrecords/django-historicalrecords-1.1.tar.gz
  md5: 44372437f84db7538282574755f667f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-historicalrecords = django-historicalrecords:main
    #
    # Would create an entry point called django-historicalrecords that calls django-historicalrecords.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - history

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smn/django-historicalrecords
  license: BSD License
  summary: "Marty Alchin's HistoricalRecords from the ProDjango book."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
