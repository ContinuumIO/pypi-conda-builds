package:
  name: django-webdav-storage
  version: "0.4"

source:
  fn: django-webdav-storage-0.4.tar.bz2
  url: https://pypi.python.org/packages/source/d/django-webdav-storage/django-webdav-storage-0.4.tar.bz2
  md5: 22a5e3d3ccde7ebf39e9d89777e9d45e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webdav-storage = django-webdav-storage:main
    #
    # Would create an entry point called django-webdav-storage that calls django-webdav-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - django_webdav_storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marazmiki/django-webdav-storage
  license: UNKNOWN
  summary: u'This application allows you easily save media and static files into webdav storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
