package:
  name: appomatic_admin
  version: "0.0.30"

source:
  fn: appomatic_admin-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_admin/appomatic_admin-0.0.30.tar.gz
  md5: cccf62e37ea93db5658361a208759384
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_admin = appomatic_admin:main
    #
    # Would create an entry point called appomatic_admin that calls appomatic_admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appomaticcore ==0.0.30

  run:
    - python
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/redhog/appomatic
  license: GPL
  summary: 'Enables the django site admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
