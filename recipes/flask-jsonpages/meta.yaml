package:
  name: flask-jsonpages
  version: "0.2"

source:
  fn: Flask-JSONPages-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-JSONPages/Flask-JSONPages-0.2.tar.gz
  md5: 0c3942262e35f13704c3911748a8bcef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-jsonpages = flask-jsonpages:main
    #
    # Would create an entry point called flask-jsonpages that calls flask-jsonpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_jsonpages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/munhitsu/Flask-JSONPages
  license: BSD License
  summary: 'Provides static pages to a Flask application based on JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
