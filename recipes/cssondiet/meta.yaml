package:
  name: cssondiet
  version: "1.8.1"

source:
  fn: CSSOnDiet-1.8.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CSSOnDiet/CSSOnDiet-1.8.1.tar.gz
  md5: 20ff46d9ebf7eaa0400cc61abaaf49e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cssondiet = cssondiet:main
    #
    # Would create an entry point called cssondiet that calls cssondiet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - CSSOnDiet
    - cssondiet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cssondiet.com
  license: GNU General Public License v3 (GPLv3)
  summary: 'Easy and fast CSS preprocessor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
