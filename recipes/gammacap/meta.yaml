package:
  name: gammacap
  version: "0.9.32"

source:
  fn: gammacap-0.9.32.tar.gz
  url: https://pypi.python.org/packages/source/g/gammacap/gammacap-0.9.32.tar.gz
  md5: 841174224973cc6cac26434e7bd0285f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gammacap = gammacap:main
    #
    # Would create an entry point called gammacap that calls gammacap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scikit-learn
    - pyfits
    - matplotlib
    - scipy

  run:
    - python
    - numpy
    - scikit-learn
    - pyfits
    - matplotlib
    - scipy

test:
  # Python imports
  imports:
    - GammaCAP
    - GammaCAP.Stats
    - gammacap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://planck.ucsc.edu/gammacap
  license: UNKNOWN
  summary: 'Gamma Ray Cluster Analysis Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
