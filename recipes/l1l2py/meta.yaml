package:
  name: l1l2py
  version: "1.0.5"

source:
  fn: L1L2Py-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/L/L1L2Py/L1L2Py-1.0.5.tar.gz
  md5: 24f782cbbe306678dc684961d10bfa8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - l1l2py = l1l2py:main
    #
    # Would create an entry point called l1l2py that calls l1l2py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - l1l2py
    - l1l2py.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://slipguru.disi.unige.it/Software/L1L2Py
  license: GNU General Public License (GPL)
  summary: 'L1L2Py is a Python package to perform variable selection by means\nof l1l2 regularization with double optimization.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
