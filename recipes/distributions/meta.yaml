package:
  name: distributions
  version: "2.1.0"

source:
  fn: distributions-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/distributions/distributions-2.1.0.tar.gz
  md5: 114dcc2f46794e131bf53fef22193fda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distributions = distributions:main
    #
    # Would create an entry point called distributions that calls distributions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - distributions
    - distributions.dbg
    - distributions.dbg.models
    - distributions.hp
    - distributions.hp.models
    - distributions.io
    - distributions.lp
    - distributions.lp.models
    - distributions.tests
    - distributions.vendor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/forcedotcom/distributions
  license: Revised BSD
  summary: 'Primitives for Bayesian MCMC inference'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
