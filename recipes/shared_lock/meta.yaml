package:
  name: shared_lock
  version: "0.1.0"

source:
  fn: shared_lock-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/shared_lock/shared_lock-0.1.0.tar.gz
  md5: d90e53f718ebc1b31ea45744edcca948
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shared_lock = shared_lock:main
    #
    # Would create an entry point called shared_lock that calls shared_lock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oslo.utils >=1.4.0
    - six

  run:
    - python
    - oslo.utils >=1.4.0
    - six

test:
  # Python imports
  imports:
    - shared_lock
    - shared_lock.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/harlowja/SharedLock
  license: Apache Software License
  summary: 'A python package that provides shareable/exclusive (also known as reader/writer) locks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
