package:
  name: hatak_flashmsg
  version: "0.1.1"

source:
  fn: Hatak_Flashmsg-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hatak_Flashmsg/Hatak_Flashmsg-0.1.1.tar.gz
  md5: 19bdc81539de0800e0488096d21dd760
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hatak_flashmsg = hatak_flashmsg:main
    #
    # Would create an entry point called hatak_flashmsg that calls hatak_flashmsg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hatak >=0.2.1
    - hatak_jinja2
    - hatak_beaker

  run:
    - python
    - hatak >=0.2.1
    - hatak_jinja2
    - hatak_beaker

test:
  # Python imports
  imports:
    - haplugin
    - haplugin.flashmsg
    - haplugin.flashmsg.tests
    - hatak_flashmsg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'Flash Messages plugin for Hatak.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
