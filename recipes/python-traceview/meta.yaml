package:
  name: python-traceview
  version: "0.4.0"

source:
  fn: python-traceview-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-traceview/python-traceview-0.4.0.tar.gz
  md5: f8c9db011787dff1dc1f28e6febd61eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-traceview = python-traceview:main
    #
    # Would create an entry point called python-traceview that calls python-traceview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.2.1

  run:
    - python
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - traceview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danriti/python-traceview
  license: MIT License
  summary: 'TraceView API Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
