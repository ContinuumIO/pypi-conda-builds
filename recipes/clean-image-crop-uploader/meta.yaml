package:
  name: clean-image-crop-uploader
  version: "0.2.2"

source:
  fn: clean-image-crop-uploader-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/clean-image-crop-uploader/clean-image-crop-uploader-0.2.2.tar.gz
  md5: f1c7401b943fbae11bfa096f24c0f684
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clean-image-crop-uploader = clean-image-crop-uploader:main
    #
    # Would create an entry point called clean-image-crop-uploader that calls clean-image-crop-uploader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil ==1.1.7
    - django >=1.4.3
    - south >=0.7.6

  run:
    - python
    - pil ==1.1.7
    - django >=1.4.3
    - south >=0.7.6

test:
  # Python imports
  imports:
    - cicu
    - cicu.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Clean Image Crop Uploader (CICU) provides AJAX file upload and image CROP functionalities for ImageFields with a simple widget replacement in the form. It use Modal from twitter-bootstrap.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
