package:
  name: plone.fieldsets
  version: "2.0.2"

source:
  fn: plone.fieldsets-2.0.2.zip
  url: https://pypi.python.org/packages/source/p/plone.fieldsets/plone.fieldsets-2.0.2.zip
  md5: 4158c8a1f784fcb5cecbd63deda7222f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.fieldsets = plone.fieldsets:main
    #
    # Would create an entry point called plone.fieldsets that calls plone.fieldsets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.formlib
    - zope.component
    - zope.formlib
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - five.formlib
    - zope.component
    - zope.formlib
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - plone
    - plone.fieldsets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.fieldsets
  license: GNU General Public License (GPL)
  summary: 'An extension to zope.formlib, which allows to group fields into different fieldsets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
