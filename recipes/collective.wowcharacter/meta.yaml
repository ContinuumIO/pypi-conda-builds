package:
  name: collective.wowcharacter
  version: "0.5"

source:
  fn: collective.wowcharacter-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.wowcharacter/collective.wowcharacter-0.5.zip
  md5: d442f9e332719a33325dcf70775fff58
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.wowcharacter = collective.wowcharacter:main
    #
    # Would create an entry point called collective.wowcharacter that calls collective.wowcharacter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.wowcharacter
    - collective.wowcharacter.browser
    - collective.wowcharacter.content
    - collective.wowcharacter.interfaces
    - collective.wowcharacter.portlets
    - collective.wowcharacter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/collectivewowcharacter/
  license: GNU General Public License (GPL)
  summary: 'An installable content type for WoW characters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
