package:
  name: django-cumulus
  version: "1.0.19"

source:
  fn: django-cumulus-1.0.19.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cumulus/django-cumulus-1.0.19.tar.gz
  md5: 6216720655f37f762c3b37237c59f11c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cumulus = django-cumulus:main
    #
    # Would create an entry point called django-cumulus that calls django-cumulus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyrax >=1.9,<1.10

  run:
    - python
    - pyrax >=1.9,<1.10

test:
  # Python imports
  imports:
    - cumulus
    - cumulus.management
    - cumulus.management.commands
    - cumulus.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django-cumulus/django-cumulus/
  license: BSD License
  summary: 'An interface to python-swiftclient and rackspace cloudfiles API from Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
