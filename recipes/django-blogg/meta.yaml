package:
  name: django-blogg
  version: "0.2"

source:
  fn: django-blogg-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-blogg/django-blogg-0.2.tar.gz
  md5: 18ccaa1cd9fec88b7669bb0ba076ade7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-blogg = django-blogg:main
    #
    # Would create an entry point called django-blogg that calls django-blogg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-markdown >=0.8.4

  run:
    - python
    - django-markdown >=0.8.4

test:
  # Python imports
  imports:
    - blogg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ishahid/django-blogg
  license: MIT License
  summary: 'Blog app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
