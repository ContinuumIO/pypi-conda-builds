package:
  name: products.cmfsquidtool
  version: "1.5.1"

source:
  fn: Products.CMFSquidTool-1.5.1.zip
  url: https://pypi.python.org/packages/source/P/Products.CMFSquidTool/Products.CMFSquidTool-1.5.1.zip
  md5: 35586fcdfcdac7e75e250200ff275902
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.cmfsquidtool = products.cmfsquidtool:main
    #
    # Would create an entry point called products.cmfsquidtool that calls products.cmfsquidtool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.CMFSquidTool
    - Products.CMFSquidTool.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/Products.CMFSquidTool/
  license: ZPL
  summary: 'HTTP cache management for CMF sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
