package:
  name: openerp-mrp-jit
  version: "7.0.406"

source:
  fn: openerp-mrp-jit-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-mrp-jit/openerp-mrp-jit-7.0.406.tar.gz
  md5: fc10657118a06c04bdd597a45d9bfee6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-mrp-jit = openerp-mrp-jit:main
    #
    # Would create an entry point called openerp-mrp-jit that calls openerp-mrp-jit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-procurement

  run:
    - python
    - openerp-procurement

test:
  # Python imports
  imports:
    - openerp.addons.mrp_jit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Just In Time Scheduling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
