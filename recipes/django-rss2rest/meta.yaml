package:
  name: django-rss2rest
  version: "0.1"

source:
  fn: django-rss2rest-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rss2rest/django-rss2rest-0.1.tar.gz
  md5: 771c4591d8a31a5dfa16f09213588d53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rss2rest = django-rss2rest:main
    #
    # Would create an entry point called django-rss2rest that calls django-rss2rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.4
    - django-tastypie ==0.9.11
    - feedparser ==5.1.2
    - python-dateutil ==1.5

  run:
    - python
    - django ==1.4
    - django-tastypie ==0.9.11
    - feedparser ==5.1.2
    - python-dateutil ==1.5

test:
  # Python imports
  imports:
    - rss2rest
    - rss2rest.management
    - rss2rest.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gump/rss2rest
  license: BSD License
  summary: 'RSS scraper with immediate RESTful access to feed items'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
