package:
  name: frasco-assets
  version: "0.1"

source:
  fn: frasco-assets-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-assets/frasco-assets-0.1.tar.gz
  md5: 0fbbd6c64e5371c546028cdd84943eaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-assets = frasco-assets:main
    #
    # Would create an entry point called frasco-assets that calls frasco-assets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - easywebassets
    - flask-assets >=0.10
    - cssmin
    - jsmin

  run:
    - python
    - frasco
    - easywebassets
    - flask-assets >=0.10
    - cssmin
    - jsmin

test:
  # Python imports
  imports:
    - frasco_assets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-assets
  license: MIT
  summary: 'Assets management using easywebassets for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
