package:
  name: plone.app.deco
  version: "1.0"

source:
  fn: plone.app.deco-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.deco/plone.app.deco-1.0.tar.gz
  md5: 8afbf9d9b391ee747023bed16850ac15
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.deco = plone.app.deco:main
    #
    # Would create an entry point called plone.app.deco that calls plone.app.deco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.blocks
    - plone.app.tiles
    - plone.app.registry
    - plone.app.texttile
    - plone.app.imagetile
    - plone.app.layoutpage
    - plone.app.toolbar
    - plone.app.jquery

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.blocks
    - plone.app.tiles
    - plone.app.registry
    - plone.app.texttile
    - plone.app.imagetile
    - plone.app.layoutpage
    - plone.app.toolbar
    - plone.app.jquery

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.deco
    - plone.app.deco.tests
    - plone.app.deco.tests.selenium

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.app.deco
  license: GPL
  summary: 'Package for Deco UI functionality'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
