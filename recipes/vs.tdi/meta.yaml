package:
  name: vs.tdi
  version: "0.3.2"

source:
  fn: vs.tdi-0.3.2.zip
  url: https://pypi.python.org/packages/source/v/vs.tdi/vs.tdi-0.3.2.zip
  md5: 9ce52227c1a805d6a81fa6f43a177d97
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vs.tdi = vs.tdi:main
    #
    # Would create an entry point called vs.tdi that calls vs.tdi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - lxml

  run:
    - python
    - setuptools
    - setuptools
    - lxml

test:
  # Python imports
  imports:
    - vs
    - vs.tdi
    - vs.tdi.browser
    - vs.tdi.content
    - vs.tdi.extenders
    - vs.tdi.interfaces
    - vs.tdi.portlets
    - vs.tdi.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/vs.tdi
  license: GNU General Public License (GPL)
  summary: 'vs.tdi (tabbed document interface)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
