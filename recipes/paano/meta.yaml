package:
  name: paano
  version: "0.2.4"

source:
  fn: paano-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/p/paano/paano-0.2.4.tar.gz
  md5: 8c4ef17d4ea85528a316e8c1c55e0870
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - paano = paano:main
    #
    # Would create an entry point called paano that calls paano.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask <0.11
    - flask-babel ==0.8
    - flask-sqlalchemy ==0.16
    - flask-wtf ==0.8.2
    - flask-googlelogin >=0.3,<0.4
    - misaka ==1.0.2

  run:
    - python
    - flask <0.11
    - flask-babel ==0.8
    - flask-sqlalchemy ==0.16
    - flask-wtf ==0.8.2
    - flask-googlelogin >=0.3,<0.4
    - misaka ==1.0.2

test:
  # Python imports
  imports:
    - paano

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/marksteve/paano
  license: MIT License
  summary: 'Dead simple FAQ/HowTo CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
