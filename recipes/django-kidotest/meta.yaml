package:
  name: django-kidotest
  version: "1.1"

source:
  fn: django-kidotest-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kidotest/django-kidotest-1.1.tar.gz
  md5: 5a4659c8c479a3224f34f68bb72aa984
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kidotest = django-kidotest:main
    #
    # Would create an entry point called django-kidotest that calls django-kidotest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock

  run:
    - python
    - mock

test:
  # Python imports
  imports:
    - kidotest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kidosoft.pl
  license: BSD License
  summary: 'Code snippets which help writing automated tests for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
