package:
  name: django-slugfield
  version: "0.9"

source:
  fn: django-slugfield-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-slugfield/django-slugfield-0.9.tar.gz
  md5: 748f1322dfa48bae2ee6774dea232139
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-slugfield = django-slugfield:main
    #
    # Would create an entry point called django-slugfield that calls django-slugfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - slugfield
    - test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dimka665/django-slugfield
  license: MIT License
  summary: 'Django manual slug field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
