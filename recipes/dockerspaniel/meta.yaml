package:
  name: dockerspaniel
  version: "0.1.3"

source:
  fn: dockerspaniel-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dockerspaniel/dockerspaniel-0.1.3.tar.gz
  md5: 99517446466b09554f8f753c45d2f4d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dockerspaniel = dockerspaniel:main
    #
    # Would create an entry point called dockerspaniel that calls dockerspaniel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - nose >=1.3.0
    - six >=1.6.1

  run:
    - python
    - nose >=1.3.0
    - six >=1.6.1

test:
  # Python imports
  imports:
    - dockerspaniel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jdolitsky/dockerspaniel.py
  license: UNKNOWN
  summary: 'Create Dockerfiles from JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
