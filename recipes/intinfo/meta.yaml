package:
  name: intinfo
  version: "0.1"

source:
  fn: intinfo-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/intinfo/intinfo-0.1.tar.gz
  md5: 6db849a9a80466a61a6434132567b017
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intinfo = intinfo:main
    #
    # Would create an entry point called intinfo that calls intinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - intinfo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mastahyeti/intinfo
  license: GPL v3
  summary: 'Its hard to get network interface information in python in a portable manner. This should help, at least in *nix environments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
