package:
  name: howabout
  version: "1.1"

source:
  fn: howabout-1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/howabout/howabout-1.1.tar.gz
  md5: 1b32efaad5f706cb4c3db43934267429
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - howabout = howabout:main
    #
    # Would create an entry point called howabout that calls howabout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.8.0

  run:
    - python
    - six ==1.8.0

test:
  # Python imports
  imports:
    - howabout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clibc/howabout
  license: MIT License
  summary: 'A library to suggest similar strings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
