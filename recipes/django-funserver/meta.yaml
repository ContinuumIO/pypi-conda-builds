package:
  name: django-funserver
  version: "1.0"

source:
  fn: django-funserver-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-funserver/django-funserver-1.0.tar.gz
  md5: d21198e210ba7c9d7e38f9b6632ad258
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-funserver = django-funserver:main
    #
    # Would create an entry point called django-funserver that calls django-funserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - funserver
    - funserver.management
    - funserver.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ubernostrum/django-funserver/
  license: BSD License
  summary: 'FUNserver for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
