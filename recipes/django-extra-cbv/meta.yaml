package:
  name: django-extra-cbv
  version: "0.1.5"

source:
  fn: django-extra-cbv-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extra-cbv/django-extra-cbv-0.1.5.tar.gz
  md5: e37ca4baf8a28f81e5aa98708532f4f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extra-cbv = django-extra-cbv:main
    #
    # Would create an entry point called django-extra-cbv that calls django-extra-cbv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - extra_cbv
    - extra_cbv.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alekam/django-extra-cbv
  license: BSD License
  summary: 'Extra class based views for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
