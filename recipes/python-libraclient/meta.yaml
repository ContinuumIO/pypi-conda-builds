package:
  name: python-libraclient
  version: "2.0.0"

source:
  fn: python-libraclient-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libraclient/python-libraclient-2.0.0.tar.gz
  md5: 5e425a67e0fc3f27fea1da7cc8c4438f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libraclient = python-libraclient:main
    #
    # Would create an entry point called python-libraclient that calls python-libraclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Client library for Libra LBaaS tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
