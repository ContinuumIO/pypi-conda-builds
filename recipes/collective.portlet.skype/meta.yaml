package:
  name: collective.portlet.skype
  version: "1.0-beta1"

source:
  fn: collective.portlet.skype-1.0-beta1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.skype/collective.portlet.skype-1.0-beta1.tar.gz
  md5: 4181ff32df98053c8a2d5e76afbd11fd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.skype = collective.portlet.skype:main
    #
    # Would create an entry point called collective.portlet.skype that calls collective.portlet.skype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.skype

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.portlet.skype
  license: GPL
  summary: 'Skype portlet for Plone 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
