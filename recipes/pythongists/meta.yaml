package:
  name: pythongists
  version: "1.1.3"

source:
  fn: PythonGists-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/P/PythonGists/PythonGists-1.1.3.tar.gz
  md5: cfd0a8f61f35a2b753e70d50154235f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythongists = pythongists:main
    #
    # Would create an entry point called pythongists that calls pythongists.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - PythonGists
    - pythongists

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/geekpradd/PythonGists
  license: MIT License
  summary: 'A Python Module to create and access GitHub Gists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
