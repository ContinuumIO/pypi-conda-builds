package:
  name: ip2country
  version: "0.1.1"

source:
  fn: ip2country-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ip2country/ip2country-0.1.1.tar.gz
  md5: db36d515061fd01bcfce6ee2b3ed5e1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ip2country = ip2country:main
    #
    # Would create an entry point called ip2country that calls ip2country.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ip2country

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yosida95/ip2country
  license: MIT License
  summary: 'Lookup country code (iso 3166) by IP address.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
