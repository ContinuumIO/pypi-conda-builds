package:
  name: exon-python-libs
  version: "0.0.4"

source:
  fn: exon-python-libs-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/e/exon-python-libs/exon-python-libs-0.0.4.tar.gz
  md5: 9c659464d90d298f5df374940b15eee0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exon-python-libs = exon-python-libs:main
    #
    # Would create an entry point called exon-python-libs that calls exon-python-libs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - exon
    - exon.utils
    - exon.utils.arch
    - exon.utils.command

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License (2.0)
  summary: 'Exon Python Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
