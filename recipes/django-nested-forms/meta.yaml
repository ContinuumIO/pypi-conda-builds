package:
  name: django-nested-forms
  version: "0.1.1"

source:
  fn: django-nested-forms-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nested-forms/django-nested-forms-0.1.1.tar.gz
  md5: bae127822bd33f5306ab9f7faabb2bcf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nested-forms = django-nested-forms:main
    #
    # Would create an entry point called django-nested-forms that calls django-nested-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nested_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dszczyt/django-nested-forms
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
