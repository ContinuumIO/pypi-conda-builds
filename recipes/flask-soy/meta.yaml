package:
  name: flask-soy
  version: "0.3"

source:
  fn: Flask-Soy-0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Soy/Flask-Soy-0.3.tar.gz
  md5: 5e17e4e7d297a39588d8c2c79bf97961
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-soy = flask-soy:main
    #
    # Would create an entry point called flask-soy that calls flask-soy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - soy

  run:
    - python
    - flask
    - soy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/lalinsky/flask-soy
  license: Apache Software License
  summary: 'Provides support for Closure Templates (Soy) in Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
