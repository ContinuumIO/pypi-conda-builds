package:
  name: django-fbrt
  version: "0.1.1"

source:
  fn: django-fbrt-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fbrt/django-fbrt-0.1.1.tar.gz
  md5: 6afd461018fcae7601f765e3d8a75e6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fbrt = django-fbrt:main
    #
    # Would create an entry point called django-fbrt that calls django-fbrt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - fandjango ==4.2
    - python-dateutil ==2.2
    - jsonfield ==0.9.22
    - django-gevent-websocket ==0.1.0

  run:
    - python
    - fandjango ==4.2
    - python-dateutil ==2.2
    - jsonfield ==0.9.22
    - django-gevent-websocket ==0.1.0

test:
  # Python imports
  imports:
    - fbrt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/luismasuelli/facebook-realtime
  license: GPL
  summary: 'This package uses websockets and facebook API to let you create real-time application (e.g. a facebook-authenticated game). It is based on Fandjango and gevent/websockets using django-gevent-websocket. Readme is still pending. Sorry.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
