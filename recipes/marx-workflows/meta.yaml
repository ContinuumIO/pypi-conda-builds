package:
  name: marx-workflows
  version: "0.0.8"

source:
  fn: marx-workflows-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/m/marx-workflows/marx-workflows-0.0.8.tar.gz
  md5: 468244653062674dce09dce5e22812fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marx-workflows = marx-workflows:main
    #
    # Would create an entry point called marx-workflows that calls marx-workflows.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marx
    - marx.workflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ninowalker/marx
  license: BSD License
  summary: 'marx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
