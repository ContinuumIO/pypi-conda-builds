package:
  name: ftw.builder
  version: "1.6.0"

source:
  fn: ftw.builder-1.6.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.builder/ftw.builder-1.6.0.zip
  md5: 48cb528a53c51ce6878bcfda5bf215a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.builder = ftw.builder:main
    #
    # Would create an entry point called ftw.builder that calls ftw.builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - products.cmfplone
    - lxml
    - path.py
    - setuptools
    - zope.component
    - zope.container

  run:
    - python
    - products.cmfplone
    - lxml
    - path.py
    - setuptools
    - zope.component
    - zope.container

test:
  # Python imports
  imports:
    - ftw
    - ftw.builder
    - ftw.builder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.builder
  license: GPL2
  summary: 'Builder pattern for creating Plone objects in tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
