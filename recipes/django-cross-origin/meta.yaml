package:
  name: django-cross-origin
  version: "0.9.0"

source:
  fn: django-cross-origin-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cross-origin/django-cross-origin-0.9.0.tar.gz
  md5: 989605a1313bc501b20057007f3bd3cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cross-origin = django-cross-origin:main
    #
    # Would create an entry point called django-cross-origin that calls django-cross-origin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cross_origin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mohawkhq/django-cross-origin
  license: BSD License
  summary: 'A Django app enabling cross-origin resource sharing in views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
