package:
  name: collective.overridemailrecipients
  version: "1.0.2"

source:
  fn: collective.overridemailrecipients-1.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.overridemailrecipients/collective.overridemailrecipients-1.0.2.zip
  md5: e57300238816dbe3a64ff6f80005bf05
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.overridemailrecipients = collective.overridemailrecipients:main
    #
    # Would create an entry point called collective.overridemailrecipients that calls collective.overridemailrecipients.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - collective
    - collective.overridemailrecipients
    - collective.overridemailrecipients.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License (GPL)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
