package:
  name: django-sphinx
  version: "2.2.4"

source:
  fn: django-sphinx-2.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sphinx/django-sphinx-2.2.4.tar.gz
  md5: 90a5a28abeb88a7883b644c0cdf49eb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sphinx = django-sphinx:main
    #
    # Would create an entry point called django-sphinx that calls django-sphinx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - djangosphinx
    - djangosphinx.apis
    - djangosphinx.apis.api263
    - djangosphinx.apis.api275
    - djangosphinx.apis.api278
    - djangosphinx.management
    - djangosphinx.management.commands
    - djangosphinx.utils
    - sphinxtest
    - sphinxtest.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcramer/django-sphinx
  license: UNKNOWN
  summary: 'An integration layer bringing Django and Sphinx Search together.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
