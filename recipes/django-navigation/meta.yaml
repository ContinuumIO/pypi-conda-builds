package:
  name: django-navigation
  version: "0.8.1"

source:
  fn: django-navigation-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-navigation/django-navigation-0.8.1.tar.gz
  md5: c4faba568405124f9c2af0dc5d8d5cc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-navigation = django-navigation:main
    #
    # Would create an entry point called django-navigation that calls django-navigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - navigation
    - navigation.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/neithere/django-navigation/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Extensible breadcrumbs navigation for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
