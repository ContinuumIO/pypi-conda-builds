package:
  name: b2g-commands
  version: "0.11"

source:
  fn: b2g-commands-0.11.tar.gz
  url: https://pypi.python.org/packages/source/b/b2g-commands/b2g-commands-0.11.tar.gz
  md5: 738eb2bc2b3b73b0e23b99e176e60d86
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - b2g-commands = b2g-commands:main
    #
    # Would create an entry point called b2g-commands that calls b2g-commands.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - b2gcommands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ahal/b2g-commands
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'A set of mach commands to make working with the B2G repo easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
