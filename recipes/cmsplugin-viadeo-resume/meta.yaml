package:
  name: cmsplugin-viadeo-resume
  version: "0.4"

source:
  fn: cmsplugin-viadeo-resume-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-viadeo-resume/cmsplugin-viadeo-resume-0.4.tar.gz
  md5: 69ad37be0e8d4a5933f22264b6968024
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-viadeo-resume = cmsplugin-viadeo-resume:main
    #
    # Would create an entry point called cmsplugin-viadeo-resume that calls cmsplugin-viadeo-resume.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.2
    - oauth2
    - simplejson

  run:
    - python
    - django-cms >=2.2
    - oauth2
    - simplejson

test:
  # Python imports
  imports:
    - cmsplugin_viadeo_resume

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tonioo/cmsplugin-viadeo-resume
  license: BSD License
  summary: 'Viadeo resume plugin for django-cms 2.2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
