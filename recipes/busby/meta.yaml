package:
  name: busby
  version: "1.0.0"

source:
  fn: busby-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/busby/busby-1.0.0.tar.gz
  md5: c6a4d1acd9ef83dc66e2b82ba813f631
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - busby = busby:main
    #
    # Would create an entry point called busby that calls busby.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - websocket-client ==0.16.0
    - docopt ==0.6.1

  run:
    - python
    - websocket-client ==0.16.0
    - docopt ==0.6.1

test:
  # Python imports
  imports:
    - busby

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yhat/busby
  license: BSD
  summary: 'Parse a csv file and send through a websocket.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
