package:
  name: flask-zen
  version: "0.2"

source:
  fn: Flask-Zen-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Zen/Flask-Zen-0.2.tar.gz
  md5: 1daf3a576cad14da8e0e638c82675f80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-zen = flask-zen:main
    #
    # Would create an entry point called flask-zen that calls flask-zen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-script
    - pyzen

  run:
    - python
    - flask-script
    - pyzen

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coderanger/flask-zen
  license: BSD License
  summary: 'Flask-Script commands to integrate with PyZen.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
