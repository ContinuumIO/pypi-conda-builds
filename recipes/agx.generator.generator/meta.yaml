package:
  name: agx.generator.generator
  version: "1.0a1"

source:
  fn: agx.generator.generator-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/a/agx.generator.generator/agx.generator.generator-1.0a1.tar.gz
  md5: 95d95e7191f47c59b966cf1f7b0fc7c5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agx.generator.generator = agx.generator.generator:main
    #
    # Would create an entry point called agx.generator.generator that calls agx.generator.generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - agx
    - agx.generator
    - agx.generator.generator
    - agx.generator.generator.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bluedynamics/agx.generator.generator
  license: GNU General Public License (GPL)
  summary: 'AGX generator for generators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
