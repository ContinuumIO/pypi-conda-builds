package:
  name: geobazadjango
  version: "1.0"

source:
  fn: geobazadjango-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/geobazadjango/geobazadjango-1.0.tar.gz
  md5: c1a8e13ca85bf07751b373eb8d0b1a6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobazadjango = geobazadjango:main
    #
    # Would create an entry point called geobazadjango that calls geobazadjango.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - geobaza

  run:
    - python
    - geobaza

test:
  # Python imports
  imports:
    - geobazadjango

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://geobaza.ru/
  license: BSD License
  summary: 'GeobazaDjango provides functionality for geolocation by IP-address for Django framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
