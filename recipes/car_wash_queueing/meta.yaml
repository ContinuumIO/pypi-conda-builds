package:
  name: car_wash_queueing
  version: "1.0.1"

source:
  fn: car_wash_queueing-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/car_wash_queueing/car_wash_queueing-1.0.1.tar.gz
  md5: b1d1156635100eb1b4936c138b642ec7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - car_wash_queueing = car_wash_queueing:main
    #
    # Would create an entry point called car_wash_queueing that calls car_wash_queueing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - car_wash_queueing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DayleChettiar
  license: UNKNOWN
  summary: 'module to model failures of several cars arriving into a group of car wash shops'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
