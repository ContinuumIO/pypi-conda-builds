package:
  name: figplotter
  version: "0.0.2"

source:
  fn: figplotter-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/figplotter/figplotter-0.0.2.tar.gz
  md5: c6c10bfc5c4126396354dd80726ed808
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - figplotter = figplotter:main
    #
    # Would create an entry point called figplotter that calls figplotter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib

  run:
    - python
    - matplotlib

test:
  # Python imports
  imports:
    - figplotter
    - figplotter.plot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: git+https://github.com/javier-cabezas/figplotter.git
  license: MIT License
  summary: 'A figure plotter using matplotlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
