package:
  name: django-easytz
  version: "0.3"

source:
  fn: django-easytz-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easytz/django-easytz-0.3.tar.gz
  md5: 7d7338f3414b9386bb4f81b115c10e7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easytz = django-easytz:main
    #
    # Would create an entry point called django-easytz that calls django-easytz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - easytz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jamesmfriedman/django-easytz/
  license: Apache
  summary: 'Timezone localization without any thinking or doing whatsoever.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
