package:
  name: django-painless-seo
  version: "0.0.4"

source:
  fn: django-painless-seo-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-painless-seo/django-painless-seo-0.0.4.tar.gz
  md5: 954f632bb85a1272b81301e32f8c39de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-painless-seo = django-painless-seo:main
    #
    # Would create an entry point called django-painless-seo that calls django-painless-seo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - painlessseo
    - painlessseo.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Glamping-Hub/django-painless-seo
  license: BSD License
  summary: 'Painless SEO app for Django framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
