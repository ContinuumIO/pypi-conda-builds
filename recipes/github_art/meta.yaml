package:
  name: github_art
  version: "0.3.1"

source:
  fn: github_art-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/g/github_art/github_art-0.3.1.tar.gz
  md5: 55aef2a199f901b94772bf4d42f07749
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github_art = github_art:main
    #
    # Would create an entry point called github_art that calls github_art.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - github_art
    - github_art.dictionaries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ufocoder/py-GithubArt
  license: MIT
  summary: 'Github Contributions table Writer. Just for fun'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
