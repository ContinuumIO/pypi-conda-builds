package:
  name: django-kvstore
  version: "1.0"

source:
  fn: django-kvstore-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kvstore/django-kvstore-1.0.tar.gz
  md5: 5052904876fcc79a480bbd5fe07ce0ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kvstore = django-kvstore:main
    #
    # Would create an entry point called django-kvstore that calls django-kvstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1.1

  run:
    - python
    - django >=1.1.1

test:
  # Python imports
  imports:
    - django_kvstore
    - django_kvstore.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sixapart/django-kvstore
  license: BSD License
  summary: 'An extensible key-value store backend for Django applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
