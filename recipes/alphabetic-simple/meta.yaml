package:
  name: alphabetic-simple
  version: "0.2"

source:
  fn: alphabetic-simple-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/alphabetic-simple/alphabetic-simple-0.2.tar.gz
  md5: 964512d5318d3e1ccad009bcecc5fc44
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alphabetic-simple = alphabetic-simple:main
    #
    # Would create an entry point called alphabetic-simple that calls alphabetic-simple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-native-tags

  run:
    - python
    - setuptools
    - django
    - django-native-tags

test:
  # Python imports
  imports:
    - alphabetic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Arpaso/alphabetic-simple
  license: UNKNOWN
  summary: 'Alphabetic template tag to filter django queryset'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
