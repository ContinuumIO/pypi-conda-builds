package:
  name: archetypes.gridfield
  version: "0.1a1"

source:
  fn: archetypes.gridfield-0.1a1.zip
  url: https://pypi.python.org/packages/source/a/archetypes.gridfield/archetypes.gridfield-0.1a1.zip
  md5: 9f7ac43ca58606a9c1bc886c974c3ee0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.gridfield = archetypes.gridfield:main
    #
    # Would create an entry point called archetypes.gridfield that calls archetypes.gridfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.z3cform >=0.5.10
    - plone.app.z3cform
    - plone.app.jquerytools

  run:
    - python
    - setuptools
    - setuptools
    - plone.z3cform >=0.5.10
    - plone.app.z3cform
    - plone.app.jquerytools

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.gridfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/archetypes.gridfield
  license: GPL
  summary: 'Datagrid for Archetypes content, rows based on z3c.form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
