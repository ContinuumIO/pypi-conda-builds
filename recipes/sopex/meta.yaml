package:
  name: sopex
  version: "0.1"

source:
  fn: sopex-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sopex/sopex-0.1.tar.gz
  md5: 5078129f196ebe8159b3cf7cc951dc3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sopex = sopex:main
    #
    # Would create an entry point called sopex that calls sopex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - jpype ==0.5.4.2
    - argparse
    - pyparsing ==1.5

  run:
    - python
    - simplejson
    - jpype ==0.5.4.2
    - argparse
    - pyparsing ==1.5

test:
  # Python imports
  imports:
    - sopex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codemaniac/sopex
  license: LICENSE
  summary: 'Library and CLI to extract the subject, predicate and object for a given english sentence'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
