package:
  name: brightcontent
  version: "0.1"

source:
  fn: BrightContent-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BrightContent/BrightContent-0.1.tar.gz
  md5: b34171a0120807d978d9a00ba4e072ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brightcontent = brightcontent:main
    #
    # Would create an entry point called brightcontent that calls brightcontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paste
    - pastescript
    - amara
    - beaker

  run:
    - python
    - setuptools
    - paste
    - pastescript
    - amara
    - beaker

test:
  # Python imports
  imports:
    - brightcontent
    - brightcontent.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://brightcontent.net
  license: Academic Free License (AFL)
  summary: 'Bright Content is Python Weblog software built from reusable components.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
