package:
  name: boundary_annotations
  version: "1.0.1"

source:
  fn: boundary_annotations-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/boundary_annotations/boundary_annotations-1.0.1.tar.gz
  md5: ed9968c32f8c9621a17d3f25b283c60c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boundary_annotations = boundary_annotations:main
    #
    # Would create an entry point called boundary_annotations that calls boundary_annotations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boundary_annotations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ampledata/boundary-annotations-python
  license: Apache License 2.0
  summary: 'Boundary Annotations API Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
