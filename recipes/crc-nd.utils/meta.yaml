package:
  name: crc-nd.utils
  version: "0.2.0"

source:
  fn: crc-nd.utils-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/crc-nd.utils/crc-nd.utils-0.2.0.tar.gz
  md5: e41a48fa85b0a13bf8284fe4cf5ee837
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crc-nd.utils = crc-nd.utils:main
    #
    # Would create an entry point called crc-nd.utils that calls crc-nd.utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - path.py ==5.3

  run:
    - python
    - path.py ==5.3

test:
  # Python imports
  imports:
    - crc_nd
    - crc_nd.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/crc-nd/py-utils
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Utility code for Python projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
