package:
  name: django-imagefit
  version: "0.4"

source:
  fn: django-imagefit-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-imagefit/django-imagefit-0.4.tar.gz
  md5: b01263734acf0ed456bd7c7f730a6325
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-imagefit = django-imagefit:main
    #
    # Would create an entry point called django-imagefit that calls django-imagefit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - imagefit
    - imagefit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vinyll/django-imagefit
  license: BSD License
  summary: 'Render an optimized version of your original image on display. Ability to resize and crop.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
