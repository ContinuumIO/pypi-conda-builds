package:
  name: bitstring
  version: "3.1.3"

source:
  fn: bitstring-3.1.3.zip
  url: https://pypi.python.org/packages/source/b/bitstring/bitstring-3.1.3.zip
  md5: 1db5d54ee269f6b54d50c3eb257eea4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitstring = bitstring:main
    #
    # Would create an entry point called bitstring that calls bitstring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bitstring

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://python-bitstring.googlecode.com
  license: MIT License
  summary: 'Simple construction, analysis and modification of binary data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
