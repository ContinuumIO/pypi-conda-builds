package:
  name: ddah-promises
  version: "0.0.1"

source:
  fn: ddah-promises-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/ddah-promises/ddah-promises-0.0.1.tar.gz
  md5: 18eac1ec1221c0587affd8bedbc0e85c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ddah-promises = ddah-promises:main
    #
    # Would create an entry point called ddah-promises that calls ddah-promises.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-popolo

  run:
    - python
    - django-popolo

test:
  # Python imports
  imports:
    - promises

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ciudadanointeligente/ddah-promises
  license: GNU Affero General Public License v3
  summary: 'Promises app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
