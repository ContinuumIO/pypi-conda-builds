package:
  name: django-ad-code
  version: "0.5.0"

source:
  fn: django-ad-code-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ad-code/django-ad-code-0.5.0.tar.gz
  md5: 6a003421d50ef2464a76eadba8797ed5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ad-code = django-ad-code:main
    #
    # Would create an entry point called django-ad-code that calls django-ad-code.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adcode
    - adcode.migrations
    - adcode.south_migrations
    - adcode.templatetags
    - adcode.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mlavin/django-ad-code
  license: BSD License
  summary: u'django-ad-code is a reusable application for managing and rendering ad tags from ad networks or exchanges such Adsense, DoubleClick or OpenX.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
