package:
  name: flask-redisconfig
  version: "0.3.0"

source:
  fn: Flask-RedisConfig-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-RedisConfig/Flask-RedisConfig-0.3.0.tar.gz
  md5: 1f82bd4f7d6b15c1342920e81e874cdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-redisconfig = flask-redisconfig:main
    #
    # Would create an entry point called flask-redisconfig that calls flask-redisconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - durabledict
    - redis

  run:
    - python
    - durabledict
    - redis

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/flask-redisconfig
  license: MIT
  summary: 'Redis-backed config for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
