package:
  name: django-concurrency
  version: "0.9"

source:
  fn: django-concurrency-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-concurrency/django-concurrency-0.9.tar.gz
  md5: bd9068a605ef16edd202b3414fefad6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-concurrency = django-concurrency:main
    #
    # Would create an entry point called django-concurrency that calls django-concurrency.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - concurrency
    - concurrency.db
    - concurrency.db.backends
    - concurrency.db.backends.mysql
    - concurrency.db.backends.postgresql_psycopg2
    - concurrency.db.backends.sqlite3
    - concurrency.management
    - concurrency.management.commands
    - concurrency.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saxix/django-concurrency
  license: BSD License
  summary: 'Optimistic lock implementation for Django. Prevents users from doing concurrent editing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
