package:
  name: lineage.registry
  version: "1.2"

source:
  fn: lineage.registry-1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lineage.registry/lineage.registry-1.2.tar.gz
  md5: cc2741b6df13fb770c52f299029ce141
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lineage.registry = lineage.registry:main
    #
    # Would create an entry point called lineage.registry that calls lineage.registry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.lineage

  run:
    - python
    - setuptools
    - setuptools
    - collective.lineage

test:
  # Python imports
  imports:
    - lineage
    - lineage.registry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/lineage.registry
  license: GNU General Public License (GPL)
  summary: u'Lineage AddOn: plone.app.registry for subsites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
