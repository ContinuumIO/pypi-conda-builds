package:
  name: clize
  version: "3.0b1"

source:
  fn: clize-3.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/clize/clize-3.0b1.tar.gz
  md5: 75f0f94a84e2cd883b28581f9d1f9e16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clize = clize:main
    #
    # Would create an entry point called clize that calls clize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - sigtools >=0.1b2

  run:
    - python
    - six
    - sigtools >=0.1b2

test:
  # Python imports
  imports:
    - clize
    - clize.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/epsy/clize
  license: MIT License
  summary: 'Command-line argument parsing for Python, without the effort'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
