package:
  name: dateandtime
  version: "0.0.5"

source:
  fn: dateandtime-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dateandtime/dateandtime-0.0.5.tar.gz
  md5: 7157aa0d9d388bc63022d19bdf64bbe3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dateandtime = dateandtime:main
    #
    # Would create an entry point called dateandtime that calls dateandtime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ddate >=0.0.4

  run:
    - python
    - ddate >=0.0.4

test:
  # Python imports
  imports:
    - dateandtime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/a-tal/dateandtime
  license: BSD License
  summary: 'A silly little text calendar application for your terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
