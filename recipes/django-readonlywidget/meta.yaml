package:
  name: django-readonlywidget
  version: "0.2"

source:
  fn: django-readonlywidget-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-readonlywidget/django-readonlywidget-0.2.tar.gz
  md5: 8a0f3996536ba86b5b644d587fe04b8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-readonlywidget = django-readonlywidget:main
    #
    # Would create an entry point called django-readonlywidget that calls django-readonlywidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_readonlywidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/stephrdev/django-readonlywidget/src/
  license: BSD License
  summary: 'A ReadOnly Widget for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
