package:
  name: brodul.recipe.template
  version: "1.2.1"

source:
  fn: brodul.recipe.template-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brodul.recipe.template/brodul.recipe.template-1.2.1.tar.gz
  md5: ae5c3b0dcbbf610b83f95b5d0dd48237
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brodul.recipe.template = brodul.recipe.template:main
    #
    # Would create an entry point called brodul.recipe.template that calls brodul.recipe.template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - jinja2
    - zope.dottedname

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - jinja2
    - zope.dottedname

test:
  # Python imports
  imports:
    - brodul
    - brodul.recipe
    - brodul.recipe.template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brodul/amplecode.recipe.template
  license: BSD License
  summary: 'Buildout recipe for making files out of Jinja2 templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
