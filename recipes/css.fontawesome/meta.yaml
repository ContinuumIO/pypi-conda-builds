package:
  name: css.fontawesome
  version: "3.2.1"

source:
  fn: css.fontawesome-3.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/css.fontawesome/css.fontawesome-3.2.1.tar.gz
  md5: 0d066ddf27b31f05cbc8720a1c58540a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - css.fontawesome = css.fontawesome:main
    #
    # Would create an entry point called css.fontawesome that calls css.fontawesome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.bootstrap

  run:
    - python
    - setuptools
    - fanstatic
    - js.bootstrap

test:
  # Python imports
  imports:
    - css
    - css.fontawesome

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jkoelker/css.fontawesome
  license: CC BY 3.0
  summary: 'fanstatic font awesome'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
