package:
  name: json-sempai
  version: "0.4.0"

source:
  fn: json-sempai-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/j/json-sempai/json-sempai-0.4.0.tar.gz
  md5: 77b03027634ed962b9c0bd6317fc689b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-sempai = json-sempai:main
    #
    # Would create an entry point called json-sempai that calls json-sempai.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonsempai
    - jsonsempai.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kragniz/json-sempai
  license: MIT License
  summary: "Use JSON files as if they're python modules"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
