package:
  name: mongomonkey
  version: "0.1"

source:
  fn: mongomonkey-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mongomonkey/mongomonkey-0.1.tar.gz
  md5: e8b5b5dd82d610d7b278feb2b40e7c28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongomonkey = mongomonkey:main
    #
    # Would create an entry point called mongomonkey that calls mongomonkey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - mongomonkey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/xonatius/mongo-monkey
  license: BSD
  summary: 'A MongoDB object-document mapping layer for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
