package:
  name: django-codesnips
  version: "0.1.0"

source:
  fn: django-codesnips-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-codesnips/django-codesnips-0.1.0.tar.gz
  md5: e5cd36166fe04ed41b67a4ac40570bf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-codesnips = django-codesnips:main
    #
    # Would create an entry point called django-codesnips that calls django-codesnips.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - codesnips
    - codesnips.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PacketPerception/django-codesnips
  license: MIT License
  summary: 'Django code snippets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
