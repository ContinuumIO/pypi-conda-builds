package:
  name: django-separated
  version: "1.0.1"

source:
  fn: django-separated-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-separated/django-separated-1.0.1.tar.gz
  md5: d3cb77f422f3382c18cea82c46d099c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-separated = django-separated:main
    #
    # Would create an entry point called django-separated that calls django-separated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - unicodecsv

  run:
    - python
    - django >=1.3
    - unicodecsv

test:
  # Python imports
  imports:
    - separated

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fusionbox/django-separated
  license: BSD License
  summary: 'Class-based view and mixins for handling CSV with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
