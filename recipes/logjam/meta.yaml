package:
  name: logjam
  version: "0.0.4-beta"

source:
  fn: logjam-0.0.4-beta.tar.gz
  url: https://pypi.python.org/packages/source/l/logjam/logjam-0.0.4-beta.tar.gz
  md5: ec8fb410fa5a7c9e41c14743c7dd90ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logjam = logjam:main
    #
    # Would create an entry point called logjam that calls logjam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.2.2

  run:
    - python
    - boto >=2.2.2

test:
  # Python imports
  imports:
    - logjam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hblanks/logjam
  license: MIT License
  summary: 'Small tools for archiving ISO8601 logfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
