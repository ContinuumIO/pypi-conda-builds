package:
  name: kypy
  version: "0.1.8"

source:
  fn: kypy-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/k/kypy/kypy-0.1.8.tar.gz
  md5: 8d6c64111a3991d2df44f52674184cd3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kypy = kypy:main
    #
    # Would create an entry point called kypy that calls kypy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib >=1.4.2
    - numpy >=1.9.1

  run:
    - python
    - matplotlib >=1.4.2
    - numpy >=1.9.1

test:
  # Python imports
  imports:
    - kypy
    - kypy.mathematics
    - kypy.science

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kylecorry31/KyPy
  license: UNKNOWN
  summary: 'Math and Science Functions from Kyle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
