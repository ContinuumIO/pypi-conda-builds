package:
  name: zettwerk.mobile
  version: "0.2.1"

source:
  fn: zettwerk.mobile-0.2.1.zip
  url: https://pypi.python.org/packages/source/z/zettwerk.mobile/zettwerk.mobile-0.2.1.zip
  md5: 9de813b5b6c9907f77a71a9e445074da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zettwerk.mobile = zettwerk.mobile:main
    #
    # Would create an entry point called zettwerk.mobile that calls zettwerk.mobile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zettwerk.mobiletheming >=0.2

  run:
    - python
    - setuptools
    - setuptools
    - zettwerk.mobiletheming >=0.2

test:
  # Python imports
  imports:
    - zettwerk
    - zettwerk.mobile
    - zettwerk.mobile.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.zettwerk.com
  license: gpl
  summary: 'jquery.mobile based themes for plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
