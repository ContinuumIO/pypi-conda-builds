package:
  name: django-autoslug
  version: "1.7.2"

source:
  fn: django-autoslug-1.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-autoslug/django-autoslug-1.7.2.tar.gz
  md5: 2aeec8bd3d2c443c0dea6e123dcfd686
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-autoslug = django-autoslug:main
    #
    # Would create an entry point called django-autoslug that calls django-autoslug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - autoslug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/neithere/django-autoslug/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'An automated slug field for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
