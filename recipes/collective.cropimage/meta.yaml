package:
  name: collective.cropimage
  version: "1.7"

source:
  fn: collective.cropimage-1.7.zip
  url: https://pypi.python.org/packages/source/c/collective.cropimage/collective.cropimage-1.7.zip
  md5: 7051a0fe04966f00095adf31763e5c58
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cropimage = collective.cropimage:main
    #
    # Would create an entry point called collective.cropimage that calls collective.cropimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfplone
    - setuptools

  run:
    - python
    - setuptools
    - products.cmfplone
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.cropimage
    - collective.cropimage.browser
    - collective.cropimage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.cropimage/
  license: BSD License
  summary: 'Store cropped image dimension for farther usage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
