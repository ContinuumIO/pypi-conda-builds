package:
  name: django-dict-response
  version: "0.0.8"

source:
  fn: django-dict-response-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dict-response/django-dict-response-0.0.8.tar.gz
  md5: 8fa048cbfd2d9711f526aa999de105ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dict-response = django-dict-response:main
    #
    # Would create an entry point called django-dict-response that calls django-dict-response.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - django_dict_response

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juknsh/django-dict-response
  license: MIT License
  summary: 'Django Dict Response'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
