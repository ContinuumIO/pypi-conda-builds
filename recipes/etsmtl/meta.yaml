package:
  name: etsmtl
  version: "0.1.2"

source:
  fn: etsmtl-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/etsmtl/etsmtl-0.1.2.tar.gz
  md5: 8f2fbf188127057be40de38b2a5e8d4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etsmtl = etsmtl:main
    #
    # Would create an entry point called etsmtl that calls etsmtl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.8.8
    - simplejson

  run:
    - python
    - requests >=0.8.8
    - simplejson

test:
  # Python imports
  imports:
    - etsmtl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'ETSMTL python bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
