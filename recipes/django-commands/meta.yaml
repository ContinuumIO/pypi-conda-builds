package:
  name: django-commands
  version: "0.6"

source:
  fn: django-commands-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-commands/django-commands-0.6.tar.gz
  md5: 8d20d19a439bfa7f1ec54deb9d914ada
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-commands = django-commands:main
    #
    # Would create an entry point called django-commands that calls django-commands.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RutledgePaulV/django-commands
  license: BSD License
  summary: 'A django app that provides a plugin type model for integrating the front and backend.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
