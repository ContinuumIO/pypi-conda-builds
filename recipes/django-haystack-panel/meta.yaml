package:
  name: django-haystack-panel
  version: "0.2.1"

source:
  fn: django-haystack-panel-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-haystack-panel/django-haystack-panel-0.2.1.tar.gz
  md5: e5286ae6ee521a9a5388c35838ef3db0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-haystack-panel = django-haystack-panel:main
    #
    # Would create an entry point called django-haystack-panel that calls django-haystack-panel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-debug-toolbar >=1.0

  run:
    - python
    - django-debug-toolbar >=1.0

test:
  # Python imports
  imports:
    - haystack_panel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/streeter/django-haystack-panel
  license: MIT License
  summary: 'A Django Debug Toolbar panel for Haystack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
