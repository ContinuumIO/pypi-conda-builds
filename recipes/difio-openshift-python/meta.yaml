package:
  name: difio-openshift-python
  version: "2.1.2"

source:
  fn: difio-openshift-python-2.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/difio-openshift-python/difio-openshift-python-2.1.2.tar.gz
  md5: 87004963610c55f28607cef89841c8b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - difio-openshift-python = difio-openshift-python:main
    #
    # Would create an entry point called difio-openshift-python that calls difio-openshift-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - common-python-difio

  run:
    - python
    - common-python-difio

test:
  # Python imports
  imports:
    - difio_openshift_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/difio/difio-openshift-python
  license: MIT License
  summary: 'Difio registration agent for OpenShift / Python applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
