package:
  name: pystockfish
  version: "1.0.0"

source:
  fn: pystockfish-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pystockfish/pystockfish-1.0.0.tar.gz
  md5: 55c21a75384753631b6133177ff7167f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystockfish = pystockfish:main
    #
    # Would create an entry point called pystockfish that calls pystockfish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pystockfish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/iamjarret/pystockfish/
  license: GNU General Public License (GPL)
  summary: 'Wraps the open-source Stockfish chess engine for easy integration into python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
