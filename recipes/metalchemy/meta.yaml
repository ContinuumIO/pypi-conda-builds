package:
  name: metalchemy
  version: "1.0.0"

source:
  fn: metalchemy-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/metalchemy/metalchemy-1.0.0.tar.gz
  md5: 992ac30f7763674620b06d36d53f7655
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metalchemy = metalchemy:main
    #
    # Would create an entry point called metalchemy that calls metalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy

  run:
    - python
    - sqlalchemy

test:
  # Python imports
  imports:
    - metalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paylogic/metalchemy
  license: MIT
  summary: 'SQLAlchemy hierarchical key/value helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
