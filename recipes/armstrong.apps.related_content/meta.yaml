package:
  name: armstrong.apps.related_content
  version: "2.0.4"

source:
  fn: armstrong.apps.related_content-2.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.apps.related_content/armstrong.apps.related_content-2.0.4.tar.gz
  md5: 1df1aa9be680c52ab90fd4fb96136682
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.apps.related_content = armstrong.apps.related_content:main
    #
    # Would create an entry point called armstrong.apps.related_content that calls armstrong.apps.related_content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3,<=1.4.5
    - armstrong.hatband >=1.2.4,<2.0
    - armstrong.core.arm_wells >=1.4.1,<2.0

  run:
    - python
    - django >=1.3,<=1.4.5
    - armstrong.hatband >=1.2.4,<2.0
    - armstrong.core.arm_wells >=1.4.1,<2.0

test:
  # Python imports
  imports:
    - armstrong.apps.related_content
    - armstrong.apps.related_content.migrations
    - armstrong.apps.related_content.templatetags
    - armstrong.apps.related_content.tests
    - armstrong.apps.related_content.tests.related_content_support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.apps.related_content/
  license: Apache Software License
  summary: 'Provides mechanism for relating content to other models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
