package:
  name: django-nadb
  version: "0.1a1"

source:
  fn: django-nadb-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nadb/django-nadb-0.1a1.tar.gz
  md5: e174de1c8bc31b12f8310d28cb47d1d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nadb = django-nadb:main
    #
    # Would create an entry point called django-nadb that calls django-nadb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-markup

  run:
    - python
    - django-markup

test:
  # Python imports
  imports:
    - nadb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/earonne/django-nadb/
  license: BSD License
  summary: 'Not Another Django Blog app!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
