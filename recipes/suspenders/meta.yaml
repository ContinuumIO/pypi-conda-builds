package:
  name: suspenders
  version: "0.2.5"

source:
  fn: suspenders-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/s/suspenders/suspenders-0.2.5.tar.gz
  md5: 315b855929b92edf4197ff5320fb9ee7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - suspenders = suspenders:main
    #
    # Would create an entry point called suspenders that calls suspenders.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysam
    - matplotlib

  run:
    - python
    - pysam
    - matplotlib

test:
  # Python imports
  imports:
    - MergeImprove
    - suspenders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/suspenders
  license: MIT
  summary: 'Allows the merging of alignments that have been annotated using pylapels into a single alignment that picks the highest quality alignment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
