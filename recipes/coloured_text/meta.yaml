package:
  name: coloured_text
  version: "2.0"

source:
  fn: Coloured_Text-2.0.tar.gz
  url: https://pypi.python.org/packages/source/C/Coloured_Text/Coloured_Text-2.0.tar.gz
  md5: f2e572c911af63d6eaa87240843f04b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coloured_text = coloured_text:main
    #
    # Would create an entry point called coloured_text that calls coloured_text.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - coloured_text

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/icedvariables/coloured_text
  license: GPL
  summary: 'Print coloured text in a python script'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
