package:
  name: nva.stormcontainer
  version: "0.2"

source:
  fn: nva.stormcontainer-0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nva.stormcontainer/nva.stormcontainer-0.2.tar.gz
  md5: 45baf74b90532e96f23256f255583def
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nva.stormcontainer = nva.stormcontainer:main
    #
    # Would create an entry point called nva.stormcontainer that calls nva.stormcontainer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - storm
    - zodb3
    - zope.component
    - zope.interface
    - zope.schema
    - zope.app.testing
    - zope.app.component
    - zope.app.keyreference
    - zope.app.container
    - zope.app.pagetemplate
    - pysqlite

  run:
    - python
    - setuptools
    - setuptools
    - storm
    - zodb3
    - zope.component
    - zope.interface
    - zope.schema
    - zope.app.testing
    - zope.app.component
    - zope.app.keyreference
    - zope.app.container
    - zope.app.pagetemplate
    - pysqlite

test:
  # Python imports
  imports:
    - nva
    - nva.stormcontainer
    - nva.stormcontainer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.novareto.de
  license: GPL
  summary: 'A Zope Storm Container'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
