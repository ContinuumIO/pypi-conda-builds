package:
  name: messytables-jts
  version: "0.3"

source:
  fn: messytables-jts-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/messytables-jts/messytables-jts-0.3.tar.gz
  md5: f420f55f3f8dc3071be00cb37805be8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - messytables-jts = messytables-jts:main
    #
    # Would create an entry point called messytables-jts that calls messytables-jts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - messytables
    - json-table-schema

  run:
    - python
    - messytables
    - json-table-schema

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Apache Licence v2.0
  summary: 'Python library for mapping Messytable cell types to strings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
