package:
  name: django-spectacles
  version: "0.0.8"

source:
  fn: django-spectacles-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spectacles/django-spectacles-0.0.8.tar.gz
  md5: 833b85ce80a26b29231185f1ebad66e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spectacles = django-spectacles:main
    #
    # Would create an entry point called django-spectacles that calls django-spectacles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - splinter
    - selenium
    - requests
    - pyyaml
    - nose
    - mock

  run:
    - python
    - splinter
    - selenium
    - requests
    - pyyaml
    - nose
    - mock

test:
  # Python imports
  imports:
    - spectacles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toast38coza/django-spectacles
  license: MIT licence, see LICENCE
  summary: "Tools for BDD built on top of django's  unit testing framework"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
