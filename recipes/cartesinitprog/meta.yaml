package:
  name: cartesinitprog
  version: "0.2.2"

source:
  fn: CartesInitProg-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/C/CartesInitProg/CartesInitProg-0.2.2.tar.gz
  md5: bcd93f20f2d7714d22d25636146bd639
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cartesinitprog = cartesinitprog:main
    #
    # Would create an entry point called cartesinitprog that calls cartesinitprog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing >=1.5.6

  run:
    - python
    - pyparsing >=1.5.6

test:
  # Python imports
  imports:
    - Cartes
    - cartesinitprog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fil.univ-lille1.fr/
  license: UNKNOWN
  summary: "Package Cartes pour le module d'InitProg."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
