package:
  name: appeus.theme
  version: "1.3.1"

source:
  fn: appeus.theme-1.3.1.zip
  url: https://pypi.python.org/packages/source/a/appeus.theme/appeus.theme-1.3.1.zip
  md5: d5d5da5b395910de39d74161777d3cdc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appeus.theme = appeus.theme:main
    #
    # Would create an entry point called appeus.theme that calls appeus.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plonetheme.bootstrap
    - z3c.jbot
    - five.grok
    - plone.app.contenttypes
    - cs.folderishpage
    - appeus.content
    - eea.tags
    - eea.facetednavigation
    - cs.editabletagline
    - cs.editablefooter
    - products.ploneformgen
    - plone.formwidget.recaptcha
    - collective.recaptcha

  run:
    - python
    - setuptools
    - setuptools
    - plonetheme.bootstrap
    - z3c.jbot
    - five.grok
    - plone.app.contenttypes
    - cs.folderishpage
    - appeus.content
    - eea.tags
    - eea.facetednavigation
    - cs.editabletagline
    - cs.editablefooter
    - products.ploneformgen
    - plone.formwidget.recaptcha
    - collective.recaptcha

test:
  # Python imports
  imports:
    - appeus
    - appeus.theme
    - appeus.theme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/codesyntax/appeus.theme/
  license: GPL
  summary: 'Installable theme: appeus.theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
