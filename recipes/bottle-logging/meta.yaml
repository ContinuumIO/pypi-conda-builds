package:
  name: bottle-logging
  version: "1.1.3"

source:
  fn: bottle-logging-1.1.3.zip
  url: https://pypi.python.org/packages/source/b/bottle-logging/bottle-logging-1.1.3.zip
  md5: ef5dac14311e36288b9ef2d3103e1a69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-logging = bottle-logging:main
    #
    # Would create an entry point called bottle-logging that calls bottle-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle

  run:
    - python
    - bottle

test:
  # Python imports
  imports:
    - bottle_logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: #
  license: MIT License
  summary: 'A simple bottle plugin for logging application events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
