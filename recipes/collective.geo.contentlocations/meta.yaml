package:
  name: collective.geo.contentlocations
  version: "3.1"

source:
  fn: collective.geo.contentlocations-3.1.zip
  url: https://pypi.python.org/packages/source/c/collective.geo.contentlocations/collective.geo.contentlocations-3.1.zip
  md5: fdb19609cc00c36ce0435af5686d38b1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geo.contentlocations = collective.geo.contentlocations:main
    #
    # Would create an entry point called collective.geo.contentlocations that calls collective.geo.contentlocations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.archetypes
    - pygeoif >0.2
    - collective.geo.mapwidget >=2.1
    - collective.z3cform.mapwidget >=2.0
    - collective.geo.geographer >=2.0

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.archetypes
    - pygeoif >0.2
    - collective.geo.mapwidget >=2.1
    - collective.z3cform.mapwidget >=2.0
    - collective.geo.geographer >=2.0

test:
  # Python imports
  imports:
    - collective
    - collective.geo
    - collective.geo.contentlocations
    - collective.geo.contentlocations.browser
    - collective.geo.contentlocations.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.geo.contentlocations
  license: GPL
  summary: 'geo reference for plone contents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
