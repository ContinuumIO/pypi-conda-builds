package:
  name: topp.recipes.cfgtemplate
  version: "0.1.0dev-r19469"

source:
  fn: topp.recipes.cfgtemplate-0.1.0dev-r19469.tar.gz
  url: https://pypi.python.org/packages/source/t/topp.recipes.cfgtemplate/topp.recipes.cfgtemplate-0.1.0dev-r19469.tar.gz
  md5: d40949aca6d47eee0f757580be4baf08
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - topp.recipes.cfgtemplate = topp.recipes.cfgtemplate:main
    #
    # Would create an entry point called topp.recipes.cfgtemplate that calls topp.recipes.cfgtemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - topp
    - topp.recipes
    - topp.recipes.cfgtemplate
    - topp.recipes.cfgtemplate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://topp.openplans.org/
  license: GPLv3
  summary: 'Provides template substitution for other recipe configurations in the same buildout.cfg file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
