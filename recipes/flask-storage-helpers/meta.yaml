package:
  name: flask-storage-helpers
  version: "0.1.5"

source:
  fn: Flask-Storage-Helpers-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Storage-Helpers/Flask-Storage-Helpers-0.1.5.tar.gz
  md5: f1b95301f72f0dc4510508bd883fd65c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-storage-helpers = flask-storage-helpers:main
    #
    # Would create an entry point called flask-storage-helpers that calls flask-storage-helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7
    - boto >=2.5.2
    - python-cloudfiles >=1.7.10

  run:
    - python
    - flask >=0.7
    - boto >=2.5.2
    - python-cloudfiles >=1.7.10

test:
  # Python imports
  imports:
    - flask_storage_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/achauve/flask-storage-helpers
  license: BSD License
  summary: 'Various file storage backends for Flask apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
