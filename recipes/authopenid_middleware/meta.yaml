package:
  name: authopenid_middleware
  version: "0.2.2"

source:
  fn: authopenid_middleware-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/a/authopenid_middleware/authopenid_middleware-0.2.2.tar.gz
  md5: d00a265c9a5d31c18496a9e0a337165f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authopenid_middleware = authopenid_middleware:main
    #
    # Would create an entry point called authopenid_middleware that calls authopenid_middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - paste >=1.4
    - decorator >=2.1.0
    - python-openid >=2.1.0
    - beaker >=0.7.3

  run:
    - python
    - distribute
    - paste >=1.4
    - decorator >=2.1.0
    - python-openid >=2.1.0
    - beaker >=0.7.3

test:
  # Python imports
  imports:
    - authopenid_middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.sandbox.lt/auth/wiki/AuthOpenIdMiddleware
  license: MIT
  summary: 'OpenID authentication middleware for WSGI applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
