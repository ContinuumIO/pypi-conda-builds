package:
  name: djangobars
  version: "0.2.1"

source:
  fn: djangobars-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangobars/djangobars-0.2.1.tar.gz
  md5: 7156dc16bf9b01e2be2f1af62b14b007
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangobars = djangobars:main
    #
    # Would create an entry point called djangobars that calls djangobars.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymeta3
    - pybars3

  run:
    - python
    - pymeta3
    - pybars3

test:
  # Python imports
  imports:
    - djangobars
    - djangobars.template
    - djangobars.template.loaders
    - djangobars.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjumbewu/djangobars/
  license: BSD License
  summary: 'An extension to allow Django to use Handlebars templates through the pybars port of Handlebars.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
