package:
  name: tornado-geopy
  version: "0.1.0"

source:
  fn: tornado-geopy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-geopy/tornado-geopy-0.1.0.tar.gz
  md5: a762271bb4e4eb2ea56fd0c9a87ddb37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-geopy = tornado-geopy:main
    #
    # Would create an entry point called tornado-geopy that calls tornado-geopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.1.0,<3.2.0
    - geopy

  run:
    - python
    - tornado >=3.1.0,<3.2.0
    - geopy

test:
  # Python imports
  imports:
    - tests
    - tests.geocoders
    - tornado_geopy
    - tornado_geopy.geocoders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/heynemann/tornado-geopy/
  license: MIT License
  summary: 'tornado-geopy is an asynchronous version of the awesome geopy library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
