package:
  name: dataone.generic_member_node
  version: "1.2.2"

source:
  fn: dataone.generic_member_node-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.generic_member_node/dataone.generic_member_node-1.2.2.tar.gz
  md5: 7492ef7c412c9af3921364da8e7578e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.generic_member_node = dataone.generic_member_node:main
    #
    # Would create an entry point called dataone.generic_member_node that calls dataone.generic_member_node.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dataone.libclient ==1.2.6
    - dataone.certificate_extensions ==1.1.3
    - django ==1.6.1
    - pyxb ==1.2.3
    - iso8601 ==0.1.4
    - psycopg2 ==2.5.2
    - dataone.cli >=1.0.0

  run:
    - python
    - dataone.libclient ==1.2.6
    - dataone.certificate_extensions ==1.1.3
    - django ==1.6.1
    - pyxb ==1.2.3
    - iso8601 ==0.1.4
    - psycopg2 ==2.5.2
    - dataone.cli >=1.0.0

test:
  # Python imports
  imports:
    - deployment
    - service
    - service.mn
    - service.mn.management
    - service.mn.management.commands
    - service.mn.middleware
    - service.mn.templates
    - service.mn.views
    - service.stores
    - service.stores.object
    - service.stores.sysmeta
    - tests
    - tests.unittest2
    - tests.unittest2.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'DataONE Generic Member Node (GMN)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
