package:
  name: django-popup-forms
  version: "1.0.3"

source:
  fn: django-popup-forms-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-popup-forms/django-popup-forms-1.0.3.tar.gz
  md5: 0fda72931cb2ea17a88d0b17e505c020
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-popup-forms = django-popup-forms:main
    #
    # Would create an entry point called django-popup-forms that calls django-popup-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - popup_forms
    - popup_forms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joinourtalents/django-popup-forms
  license: BSD License
  summary: 'Django pop-up forms framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
