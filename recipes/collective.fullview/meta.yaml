package:
  name: collective.fullview
  version: "1.3"

source:
  fn: collective.fullview-1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.fullview/collective.fullview-1.3.tar.gz
  md5: 6ce2f36d8c72a0aa7ddf522281aa8069
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.fullview = collective.fullview:main
    #
    # Would create an entry point called collective.fullview that calls collective.fullview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - zope2
    - zope.publisher

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - zope2
    - zope.publisher

test:
  # Python imports
  imports:
    - collective
    - collective.fullview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.fullview
  license: GPL
  summary: 'Alternative full view for Plone with support for BrowserViews.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
