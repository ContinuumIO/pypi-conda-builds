package:
  name: ujson-bedframe
  version: "1.33.0"

source:
  fn: ujson-bedframe-1.33.0.tar.gz
  url: https://pypi.python.org/packages/source/u/ujson-bedframe/ujson-bedframe-1.33.0.tar.gz
  md5: 7981af0112e1500bab57686cf27cdef2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ujson-bedframe = ujson-bedframe:main
    #
    # Would create an entry point called ujson-bedframe that calls ujson-bedframe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.esn.me
  license: BSD License
  summary: 'Ultra fast JSON encoder and decoder for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
