package:
  name: flask-makestatic
  version: "0.3.0"

source:
  fn: Flask-MakeStatic-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MakeStatic/Flask-MakeStatic-0.3.0.tar.gz
  md5: 6ace8393fee1bab02d878f2ebe6af3c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-makestatic = flask-makestatic:main
    #
    # Would create an entry point called flask-makestatic that calls flask-makestatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10

  run:
    - python
    - flask >=0.10

test:
  # Python imports
  imports:
    - flask_makestatic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DasIch/Flask-MakeStatic
  license: BSD License
  summary: 'Make for your flask app assets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
