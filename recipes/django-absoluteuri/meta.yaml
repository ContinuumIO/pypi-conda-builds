package:
  name: django-absoluteuri
  version: "1.1.0"

source:
  fn: django-absoluteuri-1.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-absoluteuri/django-absoluteuri-1.1.0.zip
  md5: b6a5369336a62666af0f4b9179f25503
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-absoluteuri = django-absoluteuri:main
    #
    # Would create an entry point called django-absoluteuri that calls django-absoluteuri.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - absoluteuri
    - absoluteuri.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fusionbox/django-absoluteuri
  license: Apache Software License
  summary: 'Absolute URI functions and template tags for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
