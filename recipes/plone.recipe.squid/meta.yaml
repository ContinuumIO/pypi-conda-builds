package:
  name: plone.recipe.squid
  version: "1.0b1"

source:
  fn: plone.recipe.squid-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.squid/plone.recipe.squid-1.0b1.tar.gz
  md5: 7b13efe89c3e8f113e649ed22972f371
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.squid = plone.recipe.squid:main
    #
    # Would create an entry point called plone.recipe.squid that calls plone.recipe.squid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.squid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Buildout recipe to install squid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
