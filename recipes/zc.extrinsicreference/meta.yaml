package:
  name: zc.extrinsicreference
  version: "0.3.0"

source:
  fn: zc.extrinsicreference-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.extrinsicreference/zc.extrinsicreference-0.3.0.tar.gz
  md5: 04f411a2246e82621f6f655eb3a3994b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.extrinsicreference = zc.extrinsicreference:main
    #
    # Would create an entry point called zc.extrinsicreference that calls zc.extrinsicreference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3
    - setuptools
    - zc.shortcut
    - zope.app.keyreference
    - zope.component
    - zope.interface
    - zope.testing

  run:
    - python
    - zodb3
    - setuptools
    - zc.shortcut
    - zope.app.keyreference
    - zope.component
    - zope.interface
    - zope.testing

test:
  # Python imports
  imports:
    - zc
    - zc.extrinsicreference

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.extrinsicreference
  license: Zope Public License
  summary: 'Extrinsic reference registries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
