package:
  name: collective.jqganalytics
  version: "1.1.1"

source:
  fn: collective.jqganalytics-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.jqganalytics/collective.jqganalytics-1.1.1.tar.gz
  md5: 8095456d68a2ac63a39161ced999ec16
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jqganalytics = collective.jqganalytics:main
    #
    # Would create an entry point called collective.jqganalytics that calls collective.jqganalytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.jqganalytics
    - collective.jqganalytics.browser
    - collective.jqganalytics.form

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.jqganalytics/
  license: ZPL
  summary: 'A jQuery based load and set of Google Analytics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
