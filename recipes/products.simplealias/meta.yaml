package:
  name: products.simplealias
  version: "2.1.1"

source:
  fn: Products.SimpleAlias-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SimpleAlias/Products.SimpleAlias-2.1.1.tar.gz
  md5: a440d0c36e04c2f644295df32f523dc0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.simplealias = products.simplealias:main
    #
    # Would create an entry point called products.simplealias that calls products.simplealias.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.SimpleAlias
    - Products.SimpleAlias.content
    - Products.SimpleAlias.exportimport
    - Products.SimpleAlias.interfaces
    - Products.SimpleAlias.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/simplealias
  license: GPL
  summary: "SimpleAlias is a product that let's you create aliases or shortcuts to content somewhere in your Plone."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
