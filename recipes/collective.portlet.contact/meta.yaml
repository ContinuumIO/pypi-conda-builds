package:
  name: collective.portlet.contact
  version: "1.1.2"

source:
  fn: collective.portlet.contact-1.1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.contact/collective.portlet.contact-1.1.2.zip
  md5: c33b1e68c024bcbf34f4a2b531f7be1c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.contact = collective.portlet.contact:main
    #
    # Would create an entry point called collective.portlet.contact that calls collective.portlet.contact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.contact
    - collective.portlet.contact.browser
    - collective.portlet.contact.dummy
    - collective.portlet.contact.ldap
    - collective.portlet.contact.portlet
    - collective.portlet.contact.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.makina-corpus.com
  license: GNU General Public License (GPL)
  summary: 'Display LDAP contacts in Plone portlets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
