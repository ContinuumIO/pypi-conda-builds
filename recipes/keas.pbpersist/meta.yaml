package:
  name: keas.pbpersist
  version: "0.1"

source:
  fn: keas.pbpersist-0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/keas.pbpersist/keas.pbpersist-0.1.tar.gz
  md5: 149258f233a9a4c9568366895ba1df2c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - keas.pbpersist = keas.pbpersist:main
    #
    # Would create an entry point called keas.pbpersist that calls keas.pbpersist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - keas.pbstate
    - zodb3

  run:
    - python
    - setuptools
    - keas.pbstate
    - zodb3

test:
  # Python imports
  imports:
    - keas
    - keas.pbpersist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'ZODB Persistence in a Google Protocol Buffer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
