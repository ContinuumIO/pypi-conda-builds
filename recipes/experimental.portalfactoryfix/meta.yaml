package:
  name: experimental.portalfactoryfix
  version: "0.4.2"

source:
  fn: experimental.portalfactoryfix-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/e/experimental.portalfactoryfix/experimental.portalfactoryfix-0.4.2.tar.gz
  md5: 270d17ae3b4955a7357bae68ddf81020
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.portalfactoryfix = experimental.portalfactoryfix:main
    #
    # Would create an entry point called experimental.portalfactoryfix that calls experimental.portalfactoryfix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - experimental
    - experimental.portalfactoryfix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.plone.org/collective/browser/experimental.portalfactoryfix
  license: GPL
  summary: 'Plone fix for portal factory not to cause zodb writes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
