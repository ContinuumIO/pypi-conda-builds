package:
  name: django-oauth2-provider
  version: "0.2.6.1"

source:
  fn: django-oauth2-provider-0.2.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauth2-provider/django-oauth2-provider-0.2.6.1.tar.gz
  md5: 30337e9e2f254f6f99c81216c5466deb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauth2-provider = django-oauth2-provider:main
    #
    # Would create an entry point called django-oauth2-provider that calls django-oauth2-provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - shortuuid >=0.3

  run:
    - python
    - shortuuid >=0.3

test:
  # Python imports
  imports:
    - provider
    - provider.compat
    - provider.oauth2
    - provider.oauth2.migrations
    - provider.templatetags
    - provider.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caffeinehit/django-oauth2-provider
  license: MIT License
  summary: 'Provide OAuth2 access to your app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
