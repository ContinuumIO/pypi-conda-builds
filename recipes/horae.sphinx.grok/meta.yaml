package:
  name: horae.sphinx.grok
  version: "1.0a1"

source:
  fn: horae.sphinx.grok-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.sphinx.grok/horae.sphinx.grok-1.0a1.tar.gz
  md5: 03bd953bc1efac9ea1468d8cb8162806
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.sphinx.grok = horae.sphinx.grok:main
    #
    # Would create an entry point called horae.sphinx.grok that calls horae.sphinx.grok.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - horae
    - horae.sphinx
    - horae.sphinx.grok

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides grok specific autodoc directives for sphinx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
