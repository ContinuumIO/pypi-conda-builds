package:
  name: sveeaccounts
  version: "0.4"

source:
  fn: sveeaccounts-0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/sveeaccounts/sveeaccounts-0.4.tar.gz
  md5: e6d90563d7e02ee24ef05737314db203
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sveeaccounts = sveeaccounts:main
    #
    # Would create an entry point called sveeaccounts that calls sveeaccounts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - autobreadcrumbs
    - django-registration >=1.0
    - django-simple-captcha >=0.4.1
    - django-braces >=1.0.0

  run:
    - python
    - autobreadcrumbs
    - django-registration >=1.0
    - django-simple-captcha >=0.4.1
    - django-braces >=1.0.0

test:
  # Python imports
  imports:
    - sveeaccounts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/sveeaccounts
  license: MIT License
  summary: '\nDjango application to embed all the registration stuff with captcha\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
