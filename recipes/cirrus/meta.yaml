package:
  name: cirrus
  version: "0.10"

source:
  fn: Cirrus-0.10.tar.gz
  url: https://pypi.python.org/packages/source/C/Cirrus/Cirrus-0.10.tar.gz
  md5: 3e42ef2030a85cafa89607eda9bdd6ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cirrus = cirrus:main
    #
    # Would create an entry point called cirrus that calls cirrus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.0
    - pyyaml >=3.09
    - dnspython

  run:
    - python
    - boto >=2.0
    - pyyaml >=3.09
    - dnspython

test:
  # Python imports
  imports:
    - cirrus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/cirrus
  license: BSD License
  summary: 'A set of tools for configuring Amazon route53'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
