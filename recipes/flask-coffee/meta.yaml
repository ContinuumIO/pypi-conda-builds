package:
  name: flask-coffee
  version: "0.3"

source:
  fn: Flask-Coffee-0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Coffee/Flask-Coffee-0.3.tar.gz
  md5: 7921c7ebdc8458b87c070c2d8e0ac101
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-coffee = flask-coffee:main
    #
    # Would create an entry point called flask-coffee that calls flask-coffee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://terse-words.blogspot.com/2011/06/flask-coffee-fill-your-flask-with.html
  license: BSD License
  summary: 'Fill your flask with coffee.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
