package:
  name: improviser
  version: "0.8.6.3"

source:
  fn: improviser-0.8.6.3.tar.gz
  url: https://pypi.python.org/packages/source/i/improviser/improviser-0.8.6.3.tar.gz
  md5: 8cbeccc96bba17c9cd05f094d32106f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - improviser = improviser:main
    #
    # Would create an entry point called improviser that calls improviser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - improviser
    - improviser.Blocks
    - improviser.Movements
    - improviser.Musicians
    - improviser.Musicians.Accompaniment
    - improviser.Musicians.Bassists
    - improviser.Musicians.Drummers
    - improviser.Musicians.Guitarists
    - improviser.Musicians.Pianists
    - improviser.Musicians.Soloists
    - improviser.Progressions
    - improviser.Visualizations
    - improviser.qtGUI
    - improviser.qtGUI.UI

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://improviser.onderstekop.nl/
  license: GNU General Public License (GPL)
  summary: 'Automatic music generation software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
