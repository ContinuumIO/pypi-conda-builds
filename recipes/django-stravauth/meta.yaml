package:
  name: django-stravauth
  version: "0.2"

source:
  fn: django-stravauth-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stravauth/django-stravauth-0.2.tar.gz
  md5: 9385c310840625895f472308486a5df3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stravauth = django-stravauth:main
    #
    # Would create an entry point called django-stravauth that calls django-stravauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stravauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.example.com/
  license: BSD License
  summary: 'Django accounts Strava authentication package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
