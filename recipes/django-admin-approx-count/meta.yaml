package:
  name: django-admin-approx-count
  version: "0.1.3"

source:
  fn: django-admin-approx-count-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-approx-count/django-admin-approx-count-0.1.3.tar.gz
  md5: bd4e6b9ec0fe8d3449ee1c5ef0cf8961
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-approx-count = django-admin-approx-count:main
    #
    # Would create an entry point called django-admin-approx-count that calls django-admin-approx-count.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - approx_count

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lqez/django-admin-approx-count
  license: MIT License
  summary: 'A dirty mixin to rescue from dumb count(*) query of Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
