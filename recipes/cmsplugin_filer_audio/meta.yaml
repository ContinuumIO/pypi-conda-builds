package:
  name: cmsplugin_filer_audio
  version: "0.2"

source:
  fn: cmsplugin_filer_audio-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_filer_audio/cmsplugin_filer_audio-0.2.tar.gz
  md5: 97ef6a1818da2bc61d73ef060b64b92e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_filer_audio = cmsplugin_filer_audio:main
    #
    # Would create an entry point called cmsplugin_filer_audio that calls cmsplugin_filer_audio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - z3c.setuptools_mercurial
    - django-cms
    - django-filer

  run:
    - python
    - setuptools
    - distribute
    - z3c.setuptools_mercurial
    - django-cms
    - django-filer

test:
  # Python imports
  imports:
    - cmsplugin_filer_audio
    - cmsplugin_filer_audio.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lagunak.gisa-elkartea.org/projects/cmsplugin_filer_audio
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'MP3 player for Django CMS with flowplayer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
