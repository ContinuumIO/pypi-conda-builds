package:
  name: django-dynamic-content
  version: "0.2"

source:
  fn: django-dynamic-content-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamic-content/django-dynamic-content-0.2.tar.gz
  md5: bc3fa9750f8b96db97bf30880355b1be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-content = django-dynamic-content:main
    #
    # Would create an entry point called django-dynamic-content that calls django-dynamic-content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-hvad
    - django-ckeditor

  run:
    - python
    - django
    - django-hvad
    - django-ckeditor

test:
  # Python imports
  imports:
    - dynamic_content
    - dynamic_content.migrations
    - dynamic_content.south_migrations
    - dynamic_content.templatetags
    - dynamic_content.tests
    - dynamic_content.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-dynamic-content
  license: The MIT License
  summary: 'A reusable Django app that allows to place dynamic content into templates.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
