package:
  name: ispdb
  version: "0.3"

source:
  fn: ispdb-0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/ispdb/ispdb-0.3.tar.gz
  md5: f4d53b76bec22bc6649082eb86e125a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ispdb = ispdb:main
    #
    # Would create an entry point called ispdb that calls ispdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml ==3.1.0
    - requests ==1.1.0
    - tox ==1.4.3

  run:
    - python
    - lxml ==3.1.0
    - requests ==1.1.0
    - tox ==1.4.3

test:
  # Python imports
  imports:
    - ispdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Interface to Mozilla ISP database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
