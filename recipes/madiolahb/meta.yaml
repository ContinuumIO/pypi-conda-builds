package:
  name: madiolahb
  version: "11.12.07"

source:
  fn: madiolahb-11.12.07.tar.gz
  url: https://pypi.python.org/packages/source/m/madiolahb/madiolahb-11.12.07.tar.gz
  md5: c47b2bc1ba07919d6def194992279bad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - madiolahb = madiolahb:main
    #
    # Would create an entry point called madiolahb that calls madiolahb.main()

    - madiolahb = madiolahb.__main__:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse

  run:
    - python
    - argparse

test:
  # Python imports
  imports:
    - madiolahb

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - madiolahb --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://madiolahb.code.worldmaker.net
  license: Microsoft Reciprocal License (Ms-RL)
  summary: 'madiolahb -- Bahloidam laboratory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
