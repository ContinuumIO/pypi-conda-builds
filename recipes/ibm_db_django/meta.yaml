package:
  name: ibm_db_django
  version: "1.0.7"

source:
  fn: ibm_db_django-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/i/ibm_db_django/ibm_db_django-1.0.7.tar.gz
  md5: d68fef27ec301311dae226c71e3693ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ibm_db_django = ibm_db_django:main
    #
    # Would create an entry point called ibm_db_django that calls ibm_db_django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ibm_db >=1.0.3
    - django >=1.0.3

  run:
    - python
    - setuptools
    - ibm_db >=1.0.3
    - django >=1.0.3

test:
  # Python imports
  imports:
    - ibm_db_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ibm_db_django/
  license: Apache Software License
  summary: 'DB2 support for Django framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
