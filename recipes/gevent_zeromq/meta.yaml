package:
  name: gevent_zeromq
  version: "0.2.5"

source:
  fn: gevent_zeromq-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gevent_zeromq/gevent_zeromq-0.2.5.tar.gz
  md5: a6a58817df97c7385f5547b8636693a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gevent_zeromq = gevent_zeromq:main
    #
    # Would create an entry point called gevent_zeromq that calls gevent_zeromq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyzmq ==2.2.0
    - gevent

  run:
    - python
    - pyzmq ==2.2.0
    - gevent

test:
  # Python imports
  imports:
    - gevent_zeromq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/traviscline/gevent-zeromq
  license: New BSD
  summary: 'gevent compatibility layer for pyzmq'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
