package:
  name: m5
  version: "0.3.2"

source:
  fn: M5-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/M/M5/M5-0.3.2.tar.gz
  md5: 467d7845fd3d1018e2d95b2308235e4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m5 = m5:main
    #
    # Would create an entry point called m5 that calls m5.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - slimit >=0.5.3
    - bottle >=0.9.5
    - cherrypy
    - urllib2-file
    - markdown

  run:
    - python
    - slimit >=0.5.3
    - bottle >=0.9.5
    - cherrypy
    - urllib2-file
    - markdown

test:
  # Python imports
  imports:
    - m5

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://m5apps.org/
  license: LICENSE.txt
  summary: 'HTML5 mobile app toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
