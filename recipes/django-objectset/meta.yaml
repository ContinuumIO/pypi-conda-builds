package:
  name: django-objectset
  version: "0.2.4"

source:
  fn: django-objectset-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-objectset/django-objectset-0.2.4.tar.gz
  md5: 20e8f392785c904217c9b62af94200c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-objectset = django-objectset:main
    #
    # Would create an entry point called django-objectset that calls django-objectset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - objectset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cbmi.github.io/django-objectset/
  license: BSD License
  summary: 'ObjectSet abstract class for set-like models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
