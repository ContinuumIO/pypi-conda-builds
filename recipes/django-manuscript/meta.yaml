package:
  name: django-manuscript
  version: "0.3.1"

source:
  fn: django-manuscript-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-manuscript/django-manuscript-0.3.1.tar.gz
  md5: 115c05a24effa188911c78173b0301b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-manuscript = django-manuscript:main
    #
    # Would create an entry point called django-manuscript that calls django-manuscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - manuscript

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://christopheradams.info
  license: MIT License
  summary: 'Django application for managing rare book scanning/OCR projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
