package:
  name: codepackager
  version: "1.02"

source:
  fn: codepackager-1.02.tar.gz
  url: https://pypi.python.org/packages/source/c/codepackager/codepackager-1.02.tar.gz
  md5: 975d2659ad35296fda6a332f2d4496d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codepackager = codepackager:main
    #
    # Would create an entry point called codepackager that calls codepackager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyyaml >=3.09

  run:
    - python
    - pyyaml >=3.09

test:
  # Python imports
  imports:
    - codepackager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/codepackager
  license: MIT License
  summary: 'Utility for packing code to mobile storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
