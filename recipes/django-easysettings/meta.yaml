package:
  name: django-easysettings
  version: "1.0.1"

source:
  fn: django-easysettings-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easysettings/django-easysettings-1.0.1.tar.gz
  md5: dbe9a0b3086df9b932396bcbb47fdbb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easysettings = django-easysettings:main
    #
    # Would create an entry point called django-easysettings that calls django-easysettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/SmileyChris/django-easysettings
  license: BSD License
  summary: 'Easy app-specific settings for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
