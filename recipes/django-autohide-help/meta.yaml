package:
  name: django-autohide-help
  version: "0.0.1"

source:
  fn: django-autohide-help-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-autohide-help/django-autohide-help-0.0.1.tar.gz
  md5: c6fde0fca6c825707d545e7452945a7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-autohide-help = django-autohide-help:main
    #
    # Would create an entry point called django-autohide-help that calls django-autohide-help.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - autohide_help

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/monwara/django-autohide-help
  license: BSD License
  summary: 'jQuery plugin that auto-hides form field help blocks as pluggable Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
