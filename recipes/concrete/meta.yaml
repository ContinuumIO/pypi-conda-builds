package:
  name: concrete
  version: "4.4.3"

source:
  fn: concrete-4.4.3.tar.gz
  url: https://pypi.python.org/packages/source/c/concrete/concrete-4.4.3.tar.gz
  md5: 8f1f5fa14503c56f045b282b7c50ba78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - concrete = concrete:main
    #
    # Would create an entry point called concrete that calls concrete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - networkx
    - testfixtures
    - thrift >=0.9.2

  run:
    - python
    - networkx
    - testfixtures
    - thrift >=0.9.2

test:
  # Python imports
  imports:
    - concrete
    - concrete.audio
    - concrete.clustering
    - concrete.communication
    - concrete.communication_service
    - concrete.discourse
    - concrete.email
    - concrete.entities
    - concrete.language
    - concrete.linking
    - concrete.metadata
    - concrete.nitf
    - concrete.situations
    - concrete.spans
    - concrete.structure
    - concrete.twitter
    - concrete.util
    - concrete.uuid
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hltcoe/concrete-python
  license: BSD
  summary: 'Python modules and scripts for working with Concrete'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
