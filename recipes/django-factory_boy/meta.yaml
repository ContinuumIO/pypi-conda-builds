package:
  name: django-factory_boy
  version: "0.1.6"

source:
  fn: django-factory_boy-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-factory_boy/django-factory_boy-0.1.6.tar.gz
  md5: 50a5af3561a595c62e65a1bdbfa4e2d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-factory_boy = django-factory_boy:main
    #
    # Would create an entry point called django-factory_boy that calls django-factory_boy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - factory_boy >=1.0.4

  run:
    - python
    - factory_boy >=1.0.4

test:
  # Python imports
  imports:
    - django_factory_boy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/votizen/django-factory_boy
  license: UNKNOWN
  summary: 'Uses factory_boy to supply test data factory classes for all stock Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
