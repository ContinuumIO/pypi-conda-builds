package:
  name: cachefly
  version: "1.0.0"

source:
  fn: cachefly-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cachefly/cachefly-1.0.0.tar.gz
  md5: 1537ee5fdae738483223b097f7572d20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cachefly = cachefly:main
    #
    # Would create an entry point called cachefly that calls cachefly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0,<2.0.0

  run:
    - python
    - requests >=1.0.0,<2.0.0

test:
  # Python imports
  imports:
    - cachefly
    - django_cachefly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bruun.co/
  license: Apache Software License
  summary: 'Convenient CacheFly CDN management for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
