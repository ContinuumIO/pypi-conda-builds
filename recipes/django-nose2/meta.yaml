package:
  name: django-nose2
  version: "0.1.1"

source:
  fn: django-nose2-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nose2/django-nose2-0.1.1.tar.gz
  md5: 9c0a3af77a3d3cf54e276a6453d40ce7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nose2 = django-nose2:main
    #
    # Would create an entry point called django-nose2 that calls django-nose2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2,
    - nose2 >=0.3

  run:
    - python
    - django >=1.2,
    - nose2 >=0.3

test:
  # Python imports
  imports:
    - djnose2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpellerin/django-nose2
  license: UNKNOWN
  summary: 'Test runner for django that runs tests with nose2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
