package:
  name: django-standalone
  version: "0.4"

source:
  fn: django-standalone-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-standalone/django-standalone-0.4.tar.gz
  md5: d11295dc5e58721eba75fda8597d453b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-standalone = django-standalone:main
    #
    # Would create an entry point called django-standalone that calls django-standalone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - standalone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/rfc1437/django-standalone/
  license: BSD License
  summary: 'use the Django ORM with standalone scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
