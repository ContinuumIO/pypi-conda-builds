package:
  name: django-mapistration
  version: "0.5"

source:
  fn: django-mapistration-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mapistration/django-mapistration-0.5.tar.gz
  md5: 7807159d0864cd272ad77d5486dc2039
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mapistration = django-mapistration:main
    #
    # Would create an entry point called django-mapistration that calls django-mapistration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - mapistration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamkhush/django-mapistration
  license: BSD License
  summary: 'Map integration with registration form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
