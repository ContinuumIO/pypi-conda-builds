package:
  name: mplstyler
  version: "0.1"

source:
  fn: mplstyler-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mplstyler/mplstyler-0.1.tar.gz
  md5: e1fc1e97b9cc0140e3818752cc5cc414
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mplstyler = mplstyler:main
    #
    # Would create an entry point called mplstyler that calls mplstyler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib >=1.0.0

  run:
    - python
    - matplotlib >=1.0.0

test:
  # Python imports
  imports:
    - mplstyler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mfitzp/mplstyler
  license: GNU General Public License v3 (GPLv3)
  summary: 'An API for assigning consistent marker styles to plots. Assign colours, markers, line-styles to labels and re-use on subsequent plots.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
