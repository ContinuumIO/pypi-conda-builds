package:
  name: maried
  version: "0.1.0a2"

source:
  fn: maried-0.1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/m/maried/maried-0.1.0a2.tar.gz
  md5: f81563a537ecf87b17fe2de4dcc7fd5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maried = maried:main
    #
    # Would create an entry point called maried that calls maried.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.3
    - mirte >=0.1.0a2
    - sarah >=0.1.0a2
    - joyce >=0.1.0a2

  run:
    - python
    - docutils >=0.3
    - mirte >=0.1.0a2
    - sarah >=0.1.0a2
    - joyce >=0.1.0a2

test:
  # Python imports
  imports:
    - maried

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bwesterb/maried/
  license: UNKNOWN
  summary: 'MarieD music daemon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
