package:
  name: buck.pprint
  version: "0.0"

source:
  fn: buck.pprint-0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/buck.pprint/buck.pprint-0.0.tar.gz
  md5: 0380a98b0348510c2fde4db696f27c2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buck.pprint = buck.pprint:main
    #
    # Would create an entry point called buck.pprint that calls buck.pprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - buck.pprint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bukzor/buck.pprint
  license: MIT License
  summary: "A fork of the cpython's pprint which gives standard indentation."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
