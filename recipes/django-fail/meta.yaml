package:
  name: django-fail
  version: "0.1.0"

source:
  fn: django-fail-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fail/django-fail-0.1.0.tar.gz
  md5: fc4841bf3dc552298b1ecc0093256b3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fail = django-fail:main
    #
    # Would create an entry point called django-fail that calls django-fail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsocol/django-fail
  license: BSD License
  summary: 'Make your app fail!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
