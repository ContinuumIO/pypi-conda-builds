package:
  name: clplot
  version: "0.2.1a0"

source:
  fn: clplot-0.2.1a0.zip
  url: https://pypi.python.org/packages/source/c/clplot/clplot-0.2.1a0.zip
  md5: 49b1662e91a7dacef998ef901becfc80
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clplot = clplot:main
    #
    # Would create an entry point called clplot that calls clplot.main()

    - clplot = clplot.main:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clplot

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - clplot --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/breisfeld/clplot
  license: MIT License
  summary: 'clplot is a command line utility to create plots and pages of plots from csv-like files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
