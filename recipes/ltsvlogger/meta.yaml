package:
  name: ltsvlogger
  version: "0.9.0"

source:
  fn: ltsvlogger-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ltsvlogger/ltsvlogger-0.9.0.tar.gz
  md5: 6a3dbdae299749b15ec859aaf8398a56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ltsvlogger = ltsvlogger:main
    #
    # Would create an entry point called ltsvlogger that calls ltsvlogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ltsvlogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/shimizukawa/ltsvlogger
  license: Apache Software License
  summary: 'labeled TSV logger.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
