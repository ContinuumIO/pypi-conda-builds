package:
  name: collective.vaporisation
  version: "1.3.7"

source:
  fn: collective.vaporisation-1.3.7.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.vaporisation/collective.vaporisation-1.3.7.tar.gz
  md5: 650c8dd09d975dfbc76f033a5378aadf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.vaporisation = collective.vaporisation:main
    #
    # Would create an entry point called collective.vaporisation that calls collective.vaporisation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

test:
  # Python imports
  imports:
    - collective
    - collective.vaporisation
    - collective.vaporisation.browser
    - collective.vaporisation.portlets
    - collective.vaporisation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.vaporisation
  license: GNU General Public License (GPL)
  summary: 'Plone portlet for vaporisation tagcloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
