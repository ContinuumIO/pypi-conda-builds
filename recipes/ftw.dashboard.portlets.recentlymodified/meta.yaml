package:
  name: ftw.dashboard.portlets.recentlymodified
  version: "1.2.8"

source:
  fn: ftw.dashboard.portlets.recentlymodified-1.2.8.zip
  url: https://pypi.python.org/packages/source/f/ftw.dashboard.portlets.recentlymodified/ftw.dashboard.portlets.recentlymodified-1.2.8.zip
  md5: bb5b6d0e3bd2790bf6f24aa42d7c0a51
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.dashboard.portlets.recentlymodified = ftw.dashboard.portlets.recentlymodified:main
    #
    # Would create an entry point called ftw.dashboard.portlets.recentlymodified that calls ftw.dashboard.portlets.recentlymodified.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade
    - collective.js.jqsmarttruncation

  run:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade
    - collective.js.jqsmarttruncation

test:
  # Python imports
  imports:
    - ftw
    - ftw.dashboard
    - ftw.dashboard.portlets
    - ftw.dashboard.portlets.recentlymodified
    - ftw.dashboard.portlets.recentlymodified.browser
    - ftw.dashboard.portlets.recentlymodified.tests
    - ftw.dashboard.portlets.recentlymodified.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.dashboard.portlets.recentlymodified
  license: GPL2
  summary: 'Recently modified portlet for the dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
