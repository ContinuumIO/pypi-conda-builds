package:
  name: go_http
  version: "0.2.4"

source:
  fn: go_http-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/g/go_http/go_http-0.2.4.tar.gz
  md5: 05fdb041f0c4da08cef6a46822c0018b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - go_http = go_http:main
    #
    # Would create an entry point called go_http that calls go_http.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2

  run:
    - python
    - requests >=2

test:
  # Python imports
  imports:
    - go_http
    - go_http.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/go-http-api
  license: BSD License
  summary: "A client library for Vumi Go's HTTP API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
