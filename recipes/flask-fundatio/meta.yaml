package:
  name: flask-fundatio
  version: "0.1"

source:
  fn: Flask-Fundatio-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Fundatio/Flask-Fundatio-0.1.tar.gz
  md5: db344c7e8738c9fff0b78a2716da8ef2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-fundatio = flask-fundatio:main
    #
    # Would create an entry point called flask-fundatio that calls flask-fundatio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9

  run:
    - python
    - flask >=0.9

test:
  # Python imports
  imports:
    - flask_fundatio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wbolster/flask-fundatio
  license: BSD
  summary: 'Flask extension to integrate the Foundation front-end framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
