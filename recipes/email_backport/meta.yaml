package:
  name: email_backport
  version: "4.0.3b3"

source:
  fn: email_backport-4.0.3b3.tar.gz
  url: https://pypi.python.org/packages/source/e/email_backport/email_backport-4.0.3b3.tar.gz
  md5: a3a977ee73220b1589308ba95d595e47
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - email_backport = email_backport:main
    #
    # Would create an entry point called email_backport that calls email_backport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - email_backport
    - email_backport.mime
    - email_backport.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: Python License (CNRI Python License) or Python Software Foundation License
  summary: 'A backport of the newest Python 2.x email module to >= Python 2.3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
