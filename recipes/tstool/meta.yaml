package:
  name: tstool
  version: "1.1"

source:
  fn: tstool-1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tstool/tstool-1.1.tar.gz
  md5: 6a6412fafef05dd9738801acacae365e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tstool = tstool:main
    #
    # Would create an entry point called tstool that calls tstool.main()

    - tstool = tstool.tstool:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sanction

  run:
    - python
    - sanction

test:
  # Python imports
  imports:
    - tstool

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - tstool --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ziqijin.com/brightidea
  license: UNKNOWN
  summary: 'Brightidea TS Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
