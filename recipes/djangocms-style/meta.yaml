package:
  name: djangocms-style
  version: "1.5"

source:
  fn: djangocms-style-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-style/djangocms-style-1.5.tar.gz
  md5: e15bed74cbb888599c97ce0aaf76eb4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-style = djangocms-style:main
    #
    # Would create an entry point called djangocms-style that calls djangocms-style.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_style
    - djangocms_style.migrations
    - djangocms_style.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-style
  license: BSD License
  summary: 'Style Plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
