package:
  name: django-wordpress-rss
  version: "0.1.0"

source:
  fn: django-wordpress-rss-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wordpress-rss/django-wordpress-rss-0.1.0.tar.gz
  md5: f6403827d5fc5c218584b8198004cbfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wordpress-rss = django-wordpress-rss:main
    #
    # Would create an entry point called django-wordpress-rss that calls django-wordpress-rss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3
    - feedparser ==5.1.2

  run:
    - python
    - django >=1.3
    - feedparser ==5.1.2

test:
  # Python imports
  imports:
    - wordpress_rss
    - wordpress_rss.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.kevinschaul.com
  license: MIT License
  summary: 'A Django template tag for integrating Wordpress articles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
