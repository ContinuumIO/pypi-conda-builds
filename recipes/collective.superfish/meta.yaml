package:
  name: collective.superfish
  version: "1.0"

source:
  fn: collective.superfish-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.superfish/collective.superfish-1.0.zip
  md5: beb743e5987764ec9f91e9b5f69535da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.superfish = collective.superfish:main
    #
    # Would create an entry point called collective.superfish that calls collective.superfish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.superfish
    - collective.superfish.browser
    - collective.superfish.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.superfish
  license: GPL
  summary: 'A suckerfish/superfish integration into plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
