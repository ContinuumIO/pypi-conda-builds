package:
  name: infi.sysv_service
  version: "0.1.11"

source:
  fn: infi.sysv_service-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.sysv_service/infi.sysv_service-0.1.11.tar.gz
  md5: 25ab102f4cd65fa09fcb6050713f2896
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.sysv_service = infi.sysv_service:main
    #
    # Would create an entry point called infi.sysv_service that calls infi.sysv_service.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - infi.exceptools
    - infi.pyutils
    - setuptools

  run:
    - python
    - infi.exceptools
    - infi.pyutils
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.sysv_service

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.sysv_service
  license: Python Software Foundation License
  summary: 'cross-distribution module for managing Linux services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
