package:
  name: metaq
  version: "0.1"

source:
  fn: metaq-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/metaq/metaq-0.1.tar.gz
  md5: 075b0bbc0580e934b075183eeba34bb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metaq = metaq:main
    #
    # Would create an entry point called metaq that calls metaq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zkpython >=0.4

  run:
    - python
    - zkpython >=0.4

test:
  # Python imports
  imports:
    - metaq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/killme2008/Metamorphosis/tree/master/contrib/python/meta-python
  license: Apache Software License
  summary: 'Metaq client for python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
