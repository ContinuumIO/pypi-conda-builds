package:
  name: django-scspostgis
  version: "1.0"

source:
  fn: django-scspostgis-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-scspostgis/django-scspostgis-1.0.tar.gz
  md5: ee4ca70c7837ea08ba1945f854e74878
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-scspostgis = django-scspostgis:main
    #
    # Would create an entry point called django-scspostgis that calls django-scspostgis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - scspostgis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A PostGIS db backend that works with standard_conforming_strings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
