package:
  name: django-helpdesk
  version: "0.1.16"

source:
  fn: django-helpdesk-0.1.16.tar.gz
  url: https://pypi.python.org/packages/source/d/django-helpdesk/django-helpdesk-0.1.16.tar.gz
  md5: fa543cda4123812ba779a1497017d8bc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-helpdesk = django-helpdesk:main
    #
    # Would create an entry point called django-helpdesk that calls django-helpdesk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >1.4
    - django-bootstrap-form >=3.1,<4
    - email-reply-parser
    - django-markdown-deux
    - simplejson
    - setuptools
    - pytz

  run:
    - python
    - django >1.4
    - django-bootstrap-form >=3.1,<4
    - email-reply-parser
    - django-markdown-deux
    - simplejson
    - setuptools
    - pytz

test:
  # Python imports
  imports:
    - helpdesk
    - helpdesk.management
    - helpdesk.management.commands
    - helpdesk.migrations
    - helpdesk.south_migrations
    - helpdesk.templatetags
    - helpdesk.tests
    - helpdesk.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rossp/django-helpdesk
  license: BSD License
  summary: 'Django-powered ticket tracker for your helpdesk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
