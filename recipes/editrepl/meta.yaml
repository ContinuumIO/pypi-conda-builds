package:
  name: editrepl
  version: "2013.06.20"

source:
  fn: EditREPL-2013.06.20.tar.gz
  url: https://pypi.python.org/packages/source/E/EditREPL/EditREPL-2013.06.20.tar.gz
  md5: 24a79c6d615093ead2617407bf6a5528
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - editrepl = editrepl:main
    #
    # Would create an entry point called editrepl that calls editrepl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - editrepl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/philipbjorge/EditREPL
  license: LICENSE.txt
  summary: 'Use your CLI text editor from within the Python REPL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
