package:
  name: django-content-edit
  version: "1.5"

source:
  fn: django-content-edit-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-content-edit/django-content-edit-1.5.tar.gz
  md5: 04df9bd0c416d6985794bcb5ccae5896
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-content-edit = django-content-edit:main
    #
    # Would create an entry point called django-content-edit that calls django-content-edit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-ckeditor-updated
    - django-reversion

  run:
    - python
    - django
    - django-ckeditor-updated
    - django-reversion

test:
  # Python imports
  imports:
    - content_edit
    - content_edit.migrations
    - content_edit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/burke-software/django-content-edit
  license: BSD License
  summary: 'A very simple way to let users edit content on the front end of a website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
