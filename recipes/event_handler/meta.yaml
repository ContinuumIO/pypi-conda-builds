package:
  name: event_handler
  version: "0.1.0"

source:
  fn: event_handler-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/event_handler/event_handler-0.1.0.tar.gz
  md5: bee307e34c754249c662870485ae190a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - event_handler = event_handler:main
    #
    # Would create an entry point called event_handler that calls event_handler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - event_handler
    - event_handler.events
    - tests
    - tests.events

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dlitvakb
  license: GPL v3
  summary: 'A simple event handler and dispatcher'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
