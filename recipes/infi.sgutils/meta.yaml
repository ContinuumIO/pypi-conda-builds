package:
  name: infi.sgutils
  version: "0.1.10"

source:
  fn: infi.sgutils-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.sgutils/infi.sgutils-0.1.10.tar.gz
  md5: c4dcb67e091d1b2c7e691629ca4cff91
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.sgutils = infi.sgutils:main
    #
    # Would create an entry point called infi.sgutils that calls infi.sgutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - infi.dtypes.hctl
    - infi.instruct
    - setuptools

  run:
    - python
    - infi.dtypes.hctl
    - infi.instruct
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.sgutils
    - infi.sgutils.ioctl
    - infi.sgutils.ioctl.opcodes
    - infi.sgutils.ioctl.structures
    - infi.sgutils.ioctl.tests
    - infi.sgutils.sg_map
    - infi.sgutils.sg_map.tests
    - infi.sgutils.sg_reset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.sgutils
  license: Python Software Foundation License
  summary: 'an attempt to be a pure-Python implementation of sg3-utils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
