package:
  name: caliendo
  version: "2.1.10"

source:
  fn: caliendo-2.1.10.tar.gz
  url: https://pypi.python.org/packages/source/c/caliendo/caliendo-2.1.10.tar.gz
  md5: c95a24db810e291a9666512d25d22fab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caliendo = caliendo:main
    #
    # Would create an entry point called caliendo that calls caliendo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock ==1.0.0
    - dill ==0.2b1

  run:
    - python
    - mock ==1.0.0
    - dill ==0.2b1

test:
  # Python imports
  imports:
    - caliendo
    - caliendo.db
    - test
    - test.api
    - test.api.services
    - test.nested

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/buzzfeed/caliendo
  license: UNKNOWN
  summary: 'Makes mocking services for tests simpler by caching calls to services and loading those responses as fixtures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
