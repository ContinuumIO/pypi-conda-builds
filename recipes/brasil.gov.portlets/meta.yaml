package:
  name: brasil.gov.portlets
  version: "1.0b1"

source:
  fn: brasil.gov.portlets-1.0b1.zip
  url: https://pypi.python.org/packages/source/b/brasil.gov.portlets/brasil.gov.portlets-1.0b1.zip
  md5: 8f53f58f36903b1b192c2a3dae9263fe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brasil.gov.portlets = brasil.gov.portlets:main
    #
    # Would create an entry point called brasil.gov.portlets that calls brasil.gov.portlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.pt
    - plone.api
    - plone.app.upgrade
    - products.cmfplone >=4.3
    - products.genericsetup
    - sc.embedder
    - setuptools
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - five.pt
    - plone.api
    - plone.app.upgrade
    - products.cmfplone >=4.3
    - products.genericsetup
    - sc.embedder
    - setuptools
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - brasil
    - brasil.gov
    - brasil.gov.portlets
    - brasil.gov.portlets.portlets
    - brasil.gov.portlets.tests
    - brasil.gov.portlets.upgrades
    - brasil.gov.portlets.upgrades.v1000

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plonegovbr/brasil.gov.portlets
  license: GNU General Public License v2 (GPLv2)
  summary: 'Portlets para Portal Padr\xc3\xa3o'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
