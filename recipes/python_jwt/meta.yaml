package:
  name: python_jwt
  version: "1.0.0"

source:
  fn: python_jwt-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python_jwt/python_jwt-1.0.0.tar.gz
  md5: 12a7f8b44a7345f7db6abb348f7d348a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python_jwt = python_jwt:main
    #
    # Would create an entry point called python_jwt that calls python_jwt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - python_jwt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Module for generating and verifying JSON Web Tokens'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
