package:
  name: cloud-resolve-conflicts
  version: "0.0.8"

source:
  fn: cloud-resolve-conflicts-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/cloud-resolve-conflicts/cloud-resolve-conflicts-0.0.8.tar.gz
  md5: cd5f23644c5f417f09f014c22137d31a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloud-resolve-conflicts = cloud-resolve-conflicts:main
    #
    # Would create an entry point called cloud-resolve-conflicts that calls cloud-resolve-conflicts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - send2trash
    - pathlib
    - flufl.enum

  run:
    - python
    - send2trash
    - pathlib
    - flufl.enum

test:
  # Python imports
  imports:
    - cloud_resolve_conflicts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/rominf/cloud-resolve-conflicts
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'ownCloud and Seafile conflict resolver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
