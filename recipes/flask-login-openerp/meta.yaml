package:
  name: flask-login-openerp
  version: "0.2.0"

source:
  fn: flask-login-openerp-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-login-openerp/flask-login-openerp-0.2.0.tar.gz
  md5: 186f1b0bd21b85272b291714987037a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-login-openerp = flask-login-openerp:main
    #
    # Would create an entry point called flask-login-openerp that calls flask-login-openerp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-login
    - flask-openerp
    - flask-wtf

  run:
    - python
    - flask
    - flask-login
    - flask-openerp
    - flask-wtf

test:
  # Python imports
  imports:
    - flask_login_openerp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gisce/flask-login-openerp
  license: MIT
  summary: 'OpenERP Login for Flask using Flask-Login'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
