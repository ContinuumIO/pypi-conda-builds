package:
  name: django_openid_provider
  version: "0.6"

source:
  fn: django_openid_provider-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django_openid_provider/django_openid_provider-0.6.tar.gz
  md5: 81e1f8b4b2d9d403bec94d9cd378083e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_openid_provider = django_openid_provider:main
    #
    # Would create an entry point called django_openid_provider that calls django_openid_provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_openid_provider
    - openid_provider
    - openid_provider.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-openid-provider.readthedocs.org/
  license: Apache Software License
  summary: 'An OpenID provider for your django.contrib.auth accounts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
