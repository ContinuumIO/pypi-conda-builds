package:
  name: django-cratis-admin-suit
  version: "0.1.0"

source:
  fn: django-cratis-admin-suit-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cratis-admin-suit/django-cratis-admin-suit-0.1.0.tar.gz
  md5: 85bea834517a0aa458c3efcf8b0a9a53
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cratis-admin-suit = django-cratis-admin-suit:main
    #
    # Would create an entry point called django-cratis-admin-suit that calls django-cratis-admin-suit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-cratis
    - setuptools
    - django-suit

  run:
    - python
    - django-cratis
    - setuptools
    - django-suit

test:
  # Python imports
  imports:
    - cratis_admin_suit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Integrates django-suit theme to django-admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
