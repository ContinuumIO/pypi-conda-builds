package:
  name: django-currencies
  version: "0.3.3"

source:
  fn: django-currencies-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-currencies/django-currencies-0.3.3.tar.gz
  md5: 75a24cf2f7d722dc2b406318a4e6c26f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-currencies = django-currencies:main
    #
    # Would create an entry point called django-currencies that calls django-currencies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - currencies
    - currencies.management
    - currencies.management.commands
    - currencies.templatetags
    - currencies.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/panosl/django-currencies
  license: BSD License
  summary: 'Adds support for multiple currencies as a Django application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
