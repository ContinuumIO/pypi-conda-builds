package:
  name: django-html5accordion
  version: "0.1.8"

source:
  fn: django-html5accordion-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-html5accordion/django-html5accordion-0.1.8.tar.gz
  md5: c43cbe160d769a904b379684de476b5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-html5accordion = django-html5accordion:main
    #
    # Would create an entry point called django-html5accordion that calls django-html5accordion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - html5accordion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jgerigmeyer/django-html5accordion/
  license: BSD License
  summary: 'JS support to expand/collapse content, like HTML5 details/summary elements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
