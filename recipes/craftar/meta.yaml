package:
  name: craftar
  version: "1.3.2"

source:
  fn: craftar-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/craftar/craftar-1.3.2.tar.gz
  md5: 9b080c65784419b13cfaf513625c27e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - craftar = craftar:main
    #
    # Would create an entry point called craftar that calls craftar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests ==2.4.1
    - pillow ==2.5.3

  run:
    - python
    - requests ==2.4.1
    - pillow ==2.5.3

test:
  # Python imports
  imports:
    - craftar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://catchoom.com
  license: MIT License
  summary: 'CraftAR Python Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
