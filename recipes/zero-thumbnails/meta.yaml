package:
  name: zero-thumbnails
  version: "0.1.2"

source:
  fn: Zero-Thumbnails-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/Z/Zero-Thumbnails/Zero-Thumbnails-0.1.2.tar.gz
  md5: 93fb7c067e163972e216190eeb2256fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zero-thumbnails = zero-thumbnails:main
    #
    # Would create an entry point called zero-thumbnails that calls zero-thumbnails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - south >=0.7.3
    - zero-common
    - pil >=1.1.7

  run:
    - python
    - django >=1.3.1
    - south >=0.7.3
    - zero-common
    - pil >=1.1.7

test:
  # Python imports
  imports:
    - thumbnails
    - thumbnails.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mandlaweb/Zero-Thumbnails
  license: apache license v2.0
  summary: 'App to add and manage thumbnails into a model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
