package:
  name: boltzmannizer
  version: "0.1"

source:
  fn: Boltzmannizer-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/Boltzmannizer/Boltzmannizer-0.1.tar.gz
  md5: ed17e7ce2b36053e744456329313a976
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boltzmannizer = boltzmannizer:main
    #
    # Would create an entry point called boltzmannizer that calls boltzmannizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - matplotlib
    - numpy
    - wxpython

  run:
    - python
    - argparse
    - matplotlib
    - numpy
    - wxpython

test:
  # Python imports
  imports:
    - boltzmannizer
    - boltzmannizer.gui
    - boltzmannizer.science
    - boltzmannizer.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/0/Boltzmannizer
  license: MIT License
  summary: 'Visualization tool for discrete Boltzmann distributions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
