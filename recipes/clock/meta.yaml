package:
  name: clock
  version: "0.1"

source:
  fn: clock-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clock/clock-0.1.tar.gz
  md5: 1507698baf6620ef39bed1886a103451
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clock = clock:main
    #
    # Would create an entry point called clock that calls clock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz >=0a
    - tzlocal >=1.1.1

  run:
    - python
    - pytz >=0a
    - tzlocal >=1.1.1

test:
  # Python imports
  imports:
    - clock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fengsp/clock
  license: BSD License
  summary: 'A minimalist datetime library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
