package:
  name: youkudownloader
  version: "0.1.5"

source:
  fn: youkudownloader-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/y/youkudownloader/youkudownloader-0.1.5.tar.gz
  md5: 69d8806e18b3b2d58e0f5eccedabdb0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - youkudownloader = youkudownloader:main
    #
    # Would create an entry point called youkudownloader that calls youkudownloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - youkudownloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cnblogs.com/feisky/
  license: MIT
  summary: 'Python module and script for downloading video source files from the major video sites in China.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
