package:
  name: djangocms-page-sitemap
  version: "0.1.0"

source:
  fn: djangocms-page-sitemap-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-page-sitemap/djangocms-page-sitemap-0.1.0.tar.gz
  md5: 3b83650b09c331d007a7212742a36583
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-page-sitemap = djangocms-page-sitemap:main
    #
    # Would create an entry point called djangocms-page-sitemap that calls djangocms-page-sitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >3

  run:
    - python
    - django-cms >3

test:
  # Python imports
  imports:
    - djangocms_page_sitemap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/djangocms-page-sitemap
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'django CMS page extension to handle sitemap customization'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
