package:
  name: django-webid-auth
  version: "0.2"

source:
  fn: django-webid-auth-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webid-auth/django-webid-auth-0.2.tar.gz
  md5: 5df3e2294d2624fedce4669e6304a064
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webid-auth = django-webid-auth:main
    #
    # Would create an entry point called django-webid-auth that calls django-webid-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-webid-provider >=0.2

  run:
    - python
    - django-webid-provider >=0.2

test:
  # Python imports
  imports:
    - django_webid
    - django_webid.auth
    - django_webid.auth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'A django app to enable WebID authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
