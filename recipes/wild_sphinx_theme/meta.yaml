package:
  name: wild_sphinx_theme
  version: "1.0.0"

source:
  fn: wild_sphinx_theme-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/w/wild_sphinx_theme/wild_sphinx_theme-1.0.0.tar.gz
  md5: 8789617bd4e5cc43bea3b8a166abadb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wild_sphinx_theme = wild_sphinx_theme:main
    #
    # Would create an entry point called wild_sphinx_theme that calls wild_sphinx_theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - wild_sphinx_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rhardouin/wild-sphinx-theme
  license: BSD License
  summary: 'Like Nature sphinx theme but allows pygments style to be dark'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
