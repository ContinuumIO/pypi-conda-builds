package:
  name: leaf-blade
  version: "0.2.0"

source:
  fn: leaf-blade-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/l/leaf-blade/leaf-blade-0.2.0.tar.gz
  md5: ccc78abf272cc75b709e85df3b201977
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leaf-blade = leaf-blade:main
    #
    # Would create an entry point called leaf-blade that calls leaf-blade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - leafblade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charmander/leaf-blade
  license: UNKNOWN
  summary: 'A template engine for HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
