package:
  name: cellardoor
  version: "0.3.4"

source:
  fn: cellardoor-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cellardoor/cellardoor-0.3.4.tar.gz
  md5: 2713abefed911423bbcf03a3c4d13625
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cellardoor = cellardoor:main
    #
    # Would create an entry point called cellardoor that calls cellardoor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo
    - timelib
    - msgpack-python

  run:
    - python
    - pymongo
    - timelib
    - msgpack-python

test:
  # Python imports
  imports:
    - cellardoor
    - cellardoor.api
    - cellardoor.authentication
    - cellardoor.model
    - cellardoor.serializers
    - cellardoor.spec
    - cellardoor.storage
    - cellardoor.views
    - cellardoor.wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cooper-software/cellardoor
  license: MIT License
  summary: "Create CRUD APIs how you've always wanted."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
