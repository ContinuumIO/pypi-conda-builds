package:
  name: collective.contentgenerator
  version: "0.2"

source:
  fn: collective.contentgenerator-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentgenerator/collective.contentgenerator-0.2.tar.gz
  md5: ef347cb428d12227a02bda0f30e79928
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentgenerator = collective.contentgenerator:main
    #
    # Would create an entry point called collective.contentgenerator that calls collective.contentgenerator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentgenerator
    - collective.contentgenerator.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.contentgenerator
  license: BSD License
  summary: 'Generates populated plone sites from specifed content profiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
