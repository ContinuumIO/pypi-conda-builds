package:
  name: bundleman
  version: "1.3.0"

source:
  fn: bundleman-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bundleman/bundleman-1.3.0.tar.gz
  md5: aae26b6a23bb5e448f73a310020d6fc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bundleman = bundleman:main
    #
    # Would create an entry point called bundleman that calls bundleman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bundleman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://public.dev.nuxeo.com/~ben/bundleman/
  license: GNU General Public License (GPL)
  summary: 'Manage svn bundle releasing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
