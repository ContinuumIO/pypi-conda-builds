package:
  name: flatxml
  version: "0.12"

source:
  fn: flatxml-0.12.tar.gz
  url: https://pypi.python.org/packages/source/f/flatxml/flatxml-0.12.tar.gz
  md5: 2750d52406d53a9e79ec86e4eddfe0a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flatxml = flatxml:main
    #
    # Would create an entry point called flatxml that calls flatxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmltodict

  run:
    - python
    - xmltodict

test:
  # Python imports
  imports:
    - flatxml
    - flatxml.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AbleEng/flatxml
  license: flauxtext
  summary: 'Transform XML blob, file, or OrderedDict to a flat CSV or dictionary.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
