package:
  name: django-smileys-plus
  version: "0.2.0-beta"

source:
  fn: django-smileys-plus-0.2.0-beta.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smileys-plus/django-smileys-plus-0.2.0-beta.tar.gz
  md5: e5094ab413e12587dc6578643d0d6bca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smileys-plus = django-smileys-plus:main
    #
    # Would create an entry point called django-smileys-plus that calls django-smileys-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - easy_thumbnails >=1.1

  run:
    - python
    - easy_thumbnails >=1.1

test:
  # Python imports
  imports:
    - smileys
    - smileys.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/artscoop/django-smileys/
  license: BSD License
  summary: 'Easily add, use and manage smileys on your Django-powered site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
