package:
  name: flickrapi
  version: "2.0"

source:
  fn: flickrapi-2.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flickrapi/flickrapi-2.0.tar.gz
  md5: ef90438f2c67afb5800466211dbc6da7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flickrapi = flickrapi:main
    #
    # Would create an entry point called flickrapi that calls flickrapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.3
    - six >=1.8.0
    - requests_oauthlib >=0.4.2
    - requests_toolbelt >=0.3.1

  run:
    - python
    - requests >=2.4.3
    - six >=1.8.0
    - requests_oauthlib >=0.4.2
    - requests_toolbelt >=0.3.1

test:
  # Python imports
  imports:
    - flickrapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stuvel.eu/projects/flickrapi
  license: Python License (CNRI Python License)
  summary: 'The official Python interface to the Flickr API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
