package:
  name: horae.usersandgroups
  version: "1.0a1"

source:
  fn: horae.usersandgroups-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.usersandgroups/horae.usersandgroups-1.0a1.tar.gz
  md5: 8e0703117056182e36550cbf67c47205
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.usersandgroups = horae.usersandgroups:main
    #
    # Would create an entry point called horae.usersandgroups that calls horae.usersandgroups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - horae.auth
    - horae.core
    - horae.layout
    - horae.search

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - horae.auth
    - horae.core
    - horae.layout
    - horae.search

test:
  # Python imports
  imports:
    - horae
    - horae.usersandgroups

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides a persistent implementation of users and groups for the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
