package:
  name: django-email-notification
  version: "1.3"

source:
  fn: django-email-notification-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-notification/django-email-notification-1.3.tar.gz
  md5: ea00812f3fc3556e46b96f8af1d8a42e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-notification = django-email-notification:main
    #
    # Would create an entry point called django-email-notification that calls django-email-notification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - notification
    - notification.management
    - notification.management.commands
    - test_project
    - test_project.app
    - test_project.notification
    - test_project.notification.management
    - test_project.notification.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/rmoch/django-email-notification
  license: MIT License
  summary: "django-email-notification allows backoffice users to send short email notifications to django registered users or users you only know the email about what is new or changed from django's admin or a dedicated view. And then track their clicks."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
