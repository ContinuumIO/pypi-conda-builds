package:
  name: django-blackhole
  version: "0.1.7"

source:
  fn: django-blackhole-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-blackhole/django-blackhole-0.1.7.tar.gz
  md5: f2181d2eaad844d8365ccecd0d20a1bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-blackhole = django-blackhole:main
    #
    # Would create an entry point called django-blackhole that calls django-blackhole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - blackhole

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ikame/django-blackhole
  license: MIT
  summary: "Django application that let's you work in your templates apart from having or not the\ncorresponding views created.\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
