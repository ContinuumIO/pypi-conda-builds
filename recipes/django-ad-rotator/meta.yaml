package:
  name: django-ad-rotator
  version: "0.1"

source:
  fn: django-ad-rotator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ad-rotator/django-ad-rotator-0.1.tar.gz
  md5: 9e269723f7eb43d5b44b26dbe9ba1d42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ad-rotator = django-ad-rotator:main
    #
    # Would create an entry point called django-ad-rotator that calls django-ad-rotator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - ad_rotator
    - ad_rotator.migrations
    - ad_rotator.templatetags
    - ad_rotator.tests
    - ad_rotator.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-ad-rotator
  license: The MIT License
  summary: 'A reusable Django app for managing and displaying banner ads.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
