package:
  name: django-organice-theme-rssk
  version: "0.2"

source:
  fn: django-organice-theme-rssk-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-organice-theme-rssk/django-organice-theme-rssk-0.2.tar.gz
  md5: 895968a1ce10d1dcc40c1c1af85bc64b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-organice-theme-rssk = django-organice-theme-rssk:main
    #
    # Would create an entry point called django-organice-theme-rssk that calls django-organice-theme-rssk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-organice-theme

  run:
    - python
    - django-organice-theme

test:
  # Python imports
  imports:
    - organice_theme_rssk
    - organice_theme_rssk.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://organice.io/themes/
  license: Apache Software License
  summary: 'A responsive theme with a classic look for django-organice.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
