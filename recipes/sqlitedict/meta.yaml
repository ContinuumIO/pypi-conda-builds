package:
  name: sqlitedict
  version: "1.3.0"

source:
  fn: sqlitedict-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlitedict/sqlitedict-1.3.0.tar.gz
  md5: 8b52931987006ff02f3b4f74b776407a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlitedict = sqlitedict:main
    #
    # Would create an entry point called sqlitedict that calls sqlitedict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlitedict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piskvorky/sqlitedict
  license: Apache Software License
  summary: 'Persistent dict in Python, backed up by sqlite3 and pickle, multithread-safe.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
