package:
  name: bitis
  version: "0.12.3"

source:
  fn: bitis-0.12.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bitis/bitis-0.12.3.tar.gz
  md5: 896d1813f7f3feedd5fee13976ff91f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitis = bitis:main
    #
    # Would create an entry point called bitis that calls bitis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bitis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitis.inrim.it
  license: GNU General Public License (GPL)
  summary: 'Bitis is a processing library for binary timed signals'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
