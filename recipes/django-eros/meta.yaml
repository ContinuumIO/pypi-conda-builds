package:
  name: django-eros
  version: "0.2.1"

source:
  fn: django-eros-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-eros/django-eros-0.2.1.tar.gz
  md5: d5d2860430cfaa9088cb6576e202c056
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-eros = django-eros:main
    #
    # Would create an entry point called django-eros that calls django-eros.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eros
    - eros.management
    - eros.management.commands
    - eros.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-eros
  license: MIT License
  summary: 'Standalone Django application to like any content types you want'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
