package:
  name: frogstarb
  version: "0.1.0"

source:
  fn: FrogstarB-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/FrogstarB/FrogstarB-0.1.0.tar.gz
  md5: febe54953c3785f6db3f63deaa6d4f88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frogstarb = frogstarb:main
    #
    # Would create an entry point called frogstarb that calls frogstarb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - frogstarb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jweslley/frogstarb
  license: MIT License
  summary: 'Simple tool to post to Blogger.com from the command line.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
