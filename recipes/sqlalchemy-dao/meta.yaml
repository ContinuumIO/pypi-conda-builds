package:
  name: sqlalchemy-dao
  version: "1.1.2"

source:
  fn: SQLAlchemy-Dao-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Dao/SQLAlchemy-Dao-1.1.2.tar.gz
  md5: 4c5560f1630dc0f92872a3636ed8f3cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-dao = sqlalchemy-dao:main
    #
    # Would create an entry point called sqlalchemy-dao that calls sqlalchemy-dao.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - decorated
    - fixtures2
    - inflection
    - sqlalchemy

  run:
    - python
    - decorated
    - fixtures2
    - inflection
    - sqlalchemy

test:
  # Python imports
  imports:
    - sqlalchemy_dao

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CooledCoffee/sqlalchemy-dao/
  license: UNKNOWN
  summary: 'Simple wrapper for sqlalchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
