package:
  name: frog-croak
  version: "1.1"

source:
  fn: frog-croak-1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frog-croak/frog-croak-1.1.tar.gz
  md5: b062b900b0e1f518f70ee2538a38cc1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frog-croak = frog-croak:main
    #
    # Would create an entry point called frog-croak that calls frog-croak.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - frog_croak
    - frog_croak.speedtest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlos-jenkins/frog-croak/
  license: Apache Software License
  summary: 'Internet connection speed and monitor tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
