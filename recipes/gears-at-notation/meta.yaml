package:
  name: gears-at-notation
  version: "0.2"

source:
  fn: gears-at-notation-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gears-at-notation/gears-at-notation-0.2.tar.gz
  md5: 59cd74d9d0e9ea118533796fd3f5bb75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gears-at-notation = gears-at-notation:main
    #
    # Would create an entry point called gears-at-notation that calls gears-at-notation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gears_at_notation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jjay/gears-at-notation
  license: ISC License (ISCL)
  summary: 'Alternative dependencies parser for gears'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
