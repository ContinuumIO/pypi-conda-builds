package:
  name: tahoe-whoosh
  version: "0.3.1"

source:
  fn: Tahoe-whoosh-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/T/Tahoe-whoosh/Tahoe-whoosh-0.3.1.tar.gz
  md5: 8c39e89a2351900def272179044f2b84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tahoe-whoosh = tahoe-whoosh:main
    #
    # Would create an entry point called tahoe-whoosh that calls tahoe-whoosh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git.leloop.org/okhin/tahoe-whoosh/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Using and storing whoosh index in Tahoe LAFS grid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
