package:
  name: mapkit
  version: "1.1.0"

source:
  fn: mapkit-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mapkit/mapkit-1.1.0.tar.gz
  md5: 005d686893c6e66fa038ffa96cc69297
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mapkit = mapkit:main
    #
    # Would create an entry point called mapkit that calls mapkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7
    - requests

  run:
    - python
    - sqlalchemy >=0.7
    - requests

test:
  # Python imports
  imports:
    - mapkit
    - mapkit.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/swainn/mapkit
  license: BSD 2-Clause License
  summary: 'Mapping tools for PostGIS-enabled PostgreSQL databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
