package:
  name: leap.soledad.server
  version: "0.6.3"

source:
  fn: leap.soledad.server-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/l/leap.soledad.server/leap.soledad.server-0.6.3.tar.gz
  md5: 9515f473fbe548e67374555305b35445
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leap.soledad.server = leap.soledad.server:main
    #
    # Would create an entry point called leap.soledad.server that calls leap.soledad.server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - configparser
    - couchdb
    - simplejson
    - u1db
    - routes
    - pyopenssl <0.14
    - twisted >=12.0.0
    - leap.soledad.common >=0.3.0
    - oauth

  run:
    - python
    - configparser
    - couchdb
    - simplejson
    - u1db
    - routes
    - pyopenssl <0.14
    - twisted >=12.0.0
    - leap.soledad.common >=0.3.0
    - oauth

test:
  # Python imports
  imports:
    - leap
    - leap.soledad
    - leap.soledad.server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://leap.se/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Synchronization of locally encrypted data among devices (server components)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
