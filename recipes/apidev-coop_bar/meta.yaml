package:
  name: apidev-coop_bar
  version: "1.2.0"

source:
  fn: apidev-coop_bar-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/apidev-coop_bar/apidev-coop_bar-1.2.0.tar.gz
  md5: cc8cf0693a6e2e664d65d9256b75f964
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apidev-coop_bar = apidev-coop_bar:main
    #
    # Would create an entry point called apidev-coop_bar that calls apidev-coop_bar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coop_bar
    - coop_bar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ljean/coop-bar/
  license: BSD License
  summary: 'Pluggable admin bar system , works well with coop_cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
