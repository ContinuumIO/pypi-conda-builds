package:
  name: js.jquery_cookie
  version: "1.0"

source:
  fn: js.jquery_cookie-1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_cookie/js.jquery_cookie-1.0.tar.gz
  md5: 1bb438de7aacbf37327c4eeb6cd53432
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_cookie = js.jquery_cookie:main
    #
    # Would create an entry point called js.jquery_cookie that calls js.jquery_cookie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_cookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Fanstatic packaging of jQuery Cookie'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
