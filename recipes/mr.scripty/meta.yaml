package:
  name: mr.scripty
  version: "1.0b3"

source:
  fn: mr.scripty-1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/m/mr.scripty/mr.scripty-1.0b3.tar.gz
  md5: 86d08d7de0f01bb8142500e768ec70b3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.scripty = mr.scripty:main
    #
    # Would create an entry point called mr.scripty that calls mr.scripty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - mr
    - mr.scripty
    - mr.scripty.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/mr.scripty
  license: Zope Public License
  summary: 'Use python to write configuration in zc.buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
