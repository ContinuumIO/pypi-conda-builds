package:
  name: commonware
  version: "0.4.3"

source:
  fn: commonware-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/c/commonware/commonware-0.4.3.tar.gz
  md5: 16490768147a35cfcd0240f6ef4bec10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commonware = commonware:main
    #
    # Would create an entry point called commonware that calls commonware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - commonware
    - commonware.exceptions
    - commonware.log
    - commonware.request
    - commonware.response
    - commonware.response.cookies
    - commonware.session

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsocol/commonware
  license: BSD License
  summary: 'A place to keep stuff we want to share, like middleware.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
