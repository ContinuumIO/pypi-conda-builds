package:
  name: apidev-djaloha
  version: "1.0.4"

source:
  fn: apidev-djaloha-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/apidev-djaloha/apidev-djaloha-1.0.4.tar.gz
  md5: e917a7fd9ff05c44bcaeab1545ddbfd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apidev-djaloha = apidev-djaloha:main
    #
    # Would create an entry point called apidev-djaloha that calls apidev-djaloha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-floppyforms ==1.3.0

  run:
    - python
    - django-floppyforms ==1.3.0

test:
  # Python imports
  imports:
    - djaloha
    - djaloha.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ljean/djaloha/
  license: BSD
  summary: 'Django integration for aloha HTML5 editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
