package:
  name: caches
  version: "0.2.18"

source:
  fn: caches-0.2.18.tar.gz
  url: https://pypi.python.org/packages/source/c/caches/caches-0.2.18.tar.gz
  md5: 3733cc2dbde21c54631ee0f3b45748ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caches = caches:main
    #
    # Would create an entry point called caches that calls caches.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dsnparse
    - redis
    - redis-collections
    - decorators

  run:
    - python
    - dsnparse
    - redis
    - redis-collections
    - decorators

test:
  # Python imports
  imports:
    - caches

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/firstopinion/caches
  license: MIT License
  summary: 'Python caching backed by Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
