package:
  name: django-file-form
  version: "0.1.6"

source:
  fn: django-file-form-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-file-form/django-file-form-0.1.6.tar.gz
  md5: fbadee3ecf6049abf9b4a302c15395aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-file-form = django-file-form:main
    #
    # Would create an entry point called django-file-form that calls django-file-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - path.py

  run:
    - python
    - six
    - path.py

test:
  # Python imports
  imports:
    - django_file_form
    - django_file_form.ajaxuploader
    - django_file_form.ajaxuploader.backends
    - django_file_form.management
    - django_file_form.management.commands
    - django_file_form.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Django-file-form helps you to write forms with a pretty ajax upload'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
