package:
  name: django-tailordev-contact
  version: "0.5.0"

source:
  fn: django-tailordev-contact-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tailordev-contact/django-tailordev-contact-0.5.0.tar.gz
  md5: 30005585af7ad3936768107e83354489
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tailordev-contact = django-tailordev-contact:main
    #
    # Would create an entry point called django-tailordev-contact that calls django-tailordev-contact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - td_contact

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tailordev/django-tailordev-contact
  license: MIT
  summary: u'Django TailorDev Contact  A simple contact form for your django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
