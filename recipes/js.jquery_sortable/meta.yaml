package:
  name: js.jquery_sortable
  version: "0.9.12"

source:
  fn: js.jquery_sortable-0.9.12.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_sortable/js.jquery_sortable-0.9.12.tar.gz
  md5: 48f5aa6d7a40e807d1189b9912f7502e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_sortable = js.jquery_sortable:main
    #
    # Would create an entry point called js.jquery_sortable that calls js.jquery_sortable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_sortable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/disko/js.jquery_sortable
  license: BSD
  summary: 'Fanstatic packaging of jQuery Sortable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
