package:
  name: django-quickedit
  version: "0.1.1"

source:
  fn: django-quickedit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quickedit/django-quickedit-0.1.1.tar.gz
  md5: a5b0443e5f2b7fa84b27e17073038e0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quickedit = django-quickedit:main
    #
    # Would create an entry point called django-quickedit that calls django-quickedit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - quickedit
    - quickedit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/washingtontimes/django-quickedit
  license: Apache Software License
  summary: 'Quick editing of fields in the django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
