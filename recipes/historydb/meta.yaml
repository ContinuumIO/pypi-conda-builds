package:
  name: historydb
  version: "0.4.1"

source:
  fn: historydb-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/h/historydb/historydb-0.4.1.tar.gz
  md5: b4275fbc9779cb3d5eec9d0d282096c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - historydb = historydb:main
    #
    # Would create an entry point called historydb that calls historydb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - historydb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/asdtech/historydb-python
  license: BSD License
  summary: 'HistoryDB implemented in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
