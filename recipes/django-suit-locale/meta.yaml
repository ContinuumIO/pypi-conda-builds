package:
  name: django-suit-locale
  version: "1.0.9"

source:
  fn: django-suit-locale-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-suit-locale/django-suit-locale-1.0.9.tar.gz
  md5: c46297f6553eae84d017df4bfe61e6a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-suit-locale = django-suit-locale:main
    #
    # Would create an entry point called django-suit-locale that calls django-suit-locale.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - suitlocale

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbub/django-suit-locale
  license: BSD License
  summary: 'Django Suit localization.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
