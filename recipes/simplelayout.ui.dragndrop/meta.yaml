package:
  name: simplelayout.ui.dragndrop
  version: "3.1.0"

source:
  fn: simplelayout.ui.dragndrop-3.1.0.zip
  url: https://pypi.python.org/packages/source/s/simplelayout.ui.dragndrop/simplelayout.ui.dragndrop-3.1.0.zip
  md5: dd8cdbd3e8eb998f1504d7a76fab8c2b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplelayout.ui.dragndrop = simplelayout.ui.dragndrop:main
    #
    # Would create an entry point called simplelayout.ui.dragndrop that calls simplelayout.ui.dragndrop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - simplelayout
    - simplelayout.ui
    - simplelayout.ui.dragndrop
    - simplelayout.ui.dragndrop.Extensions
    - simplelayout.ui.dragndrop.browser
    - simplelayout.ui.dragndrop.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/simplelayout.ui.dragndrop
  license: GPL2
  summary: 'simplelayout drag and drop support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
