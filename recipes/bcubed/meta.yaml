package:
  name: bcubed
  version: "1.1"

source:
  fn: bcubed-1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bcubed/bcubed-1.1.tar.gz
  md5: a7063acdbeff88084051b03fc415687c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bcubed = bcubed:main
    #
    # Would create an entry point called bcubed that calls bcubed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - bcubed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hhromic/python-bcubed
  license: MIT
  summary: 'Simple extended BCubed implementation in Python for clustering evaluation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
