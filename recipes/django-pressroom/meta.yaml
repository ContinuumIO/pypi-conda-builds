package:
  name: django-pressroom
  version: "0.6"

source:
  fn: django-pressroom-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pressroom/django-pressroom-0.6.tar.gz
  md5: 2571b153fe892d82b5f39cd72164d705
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pressroom = django-pressroom:main
    #
    # Would create an entry point called django-pressroom that calls django-pressroom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-photologue

  run:
    - python
    - setuptools
    - django-photologue

test:
  # Python imports
  imports:
    - pressroom
    - pressroom.migrations
    - pressroom.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-pressroom/
  license: BSD License
  summary: 'A pressroom application for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
