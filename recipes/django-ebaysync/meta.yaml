package:
  name: django-ebaysync
  version: "0.2.5"

source:
  fn: django-ebaysync-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ebaysync/django-ebaysync-0.2.5.tar.gz
  md5: 3cc7ee8e29861737a9fc87d81855e75f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ebaysync = django-ebaysync:main
    #
    # Would create an entry point called django-ebaysync that calls django-ebaysync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - ebaysuds >=0.4.5

  run:
    - python
    - ebaysuds >=0.4.5

test:
  # Python imports
  imports:
    - ebaysync
    - ebaysync.management
    - ebaysync.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/anentropic/django-ebaysync
  license: LGPL v3
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
