package:
  name: flask-flatpagescut
  version: "0.5.1"

source:
  fn: Flask-FlatPagesCut-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-FlatPagesCut/Flask-FlatPagesCut-0.5.1.tar.gz
  md5: 4d497304d07a4709cc9bda7adab4a5ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-flatpagescut = flask-flatpagescut:main
    #
    # Would create an entry point called flask-flatpagescut that calls flask-flatpagescut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pyyaml
    - markdown

  run:
    - python
    - flask
    - pyyaml
    - markdown

test:
  # Python imports
  imports:
    - flask_flatpagescut

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/billyfbrain/Flask-FlatPagesCut
  license: BSD License
  summary: 'Flask-FlatPagesCut is fork Flask-FlatPages (Provides flat static pages to a Flask application)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
