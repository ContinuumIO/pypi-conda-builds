package:
  name: sterch.scrapingtools
  version: "0.7.4"

source:
  fn: sterch.scrapingtools-0.7.4.tar.gz
  url: https://pypi.python.org/packages/source/s/sterch.scrapingtools/sterch.scrapingtools-0.7.4.tar.gz
  md5: e70f166c5cb4651222830899e9b2e95d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sterch.scrapingtools = sterch.scrapingtools:main
    #
    # Would create an entry point called sterch.scrapingtools that calls sterch.scrapingtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zope.component
    - zope.schema
    - setuptools

  run:
    - python
    - zope.component
    - zope.schema
    - setuptools

test:
  # Python imports
  imports:
    - sterch
    - sterch.scrapingtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sterch.net
  license: ZPL
  summary: 'Library for building scrapers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
