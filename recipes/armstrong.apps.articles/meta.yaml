package:
  name: armstrong.apps.articles
  version: "1.1.2"

source:
  fn: armstrong.apps.articles-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.apps.articles/armstrong.apps.articles-1.1.2.tar.gz
  md5: f2c47874ecf0790c626a8691516a2e17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.apps.articles = armstrong.apps.articles:main
    #
    # Would create an entry point called armstrong.apps.articles that calls armstrong.apps.articles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3,<=1.4.3
    - armstrong.apps.content >=1.0.2,<2.0
    - armstrong.apps.related_content >=2.0.1,<3.0
    - armstrong.core.arm_content >=1.3.2,<2.0
    - armstrong.hatband >=1.2.4,<2.0
    - django-reversion ==1.4

  run:
    - python
    - django >=1.3,<=1.4.3
    - armstrong.apps.content >=1.0.2,<2.0
    - armstrong.apps.related_content >=2.0.1,<3.0
    - armstrong.core.arm_content >=1.3.2,<2.0
    - armstrong.hatband >=1.2.4,<2.0
    - django-reversion ==1.4

test:
  # Python imports
  imports:
    - armstrong.apps.articles
    - armstrong.apps.articles.migrations
    - armstrong.apps.articles.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.apps.articles/
  license: Apache Software License
  summary: 'Provides a concrete implementation of ContentBase'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
