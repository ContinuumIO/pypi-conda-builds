package:
  name: sloth-ci.validators.github
  version: "1.0.6"

source:
  fn: sloth-ci.validators.github-1.0.6.zip
  url: https://pypi.python.org/packages/source/s/sloth-ci.validators.github/sloth-ci.validators.github-1.0.6.zip
  md5: 07041d6a682fab0322ec85d5b8630d8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sloth-ci.validators.github = sloth-ci.validators.github:main
    #
    # Would create an entry point called sloth-ci.validators.github that calls sloth-ci.validators.github.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sloth-ci >=1.1.9

  run:
    - python
    - sloth-ci >=1.1.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/moigagoo/sloth-ci-validators
  license: MIT
  summary: 'GitHub validator for Sloth CI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
