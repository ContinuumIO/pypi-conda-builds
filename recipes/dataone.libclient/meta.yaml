package:
  name: dataone.libclient
  version: "1.2.8"

source:
  fn: dataone.libclient-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.libclient/dataone.libclient-1.2.8.tar.gz
  md5: 8fa324c6531b89826816f30b566f6555
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.libclient = dataone.libclient:main
    #
    # Would create an entry point called dataone.libclient that calls dataone.libclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dataone.common ==1.1.6
    - rdflib ==4.0.1
    - google.foresite-toolkit ==1.3
    - python-dateutil ==2.1

  run:
    - python
    - dataone.common ==1.1.6
    - rdflib ==4.0.1
    - google.foresite-toolkit ==1.3
    - python-dateutil ==2.1

test:
  # Python imports
  imports:
    - d1_client
    - examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'A DataONE client library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
