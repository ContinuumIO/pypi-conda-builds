package:
  name: hexfile
  version: "0.1.1"

source:
  fn: hexfile-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hexfile/hexfile-0.1.1.tar.gz
  md5: 5d7c30475959d8dee2b454f06db21506
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hexfile = hexfile:main
    #
    # Would create an entry point called hexfile that calls hexfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hexfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/ryansturmer/hexfile
  license: UNKNOWN
  summary: 'Library for loading and manipulating hex files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
