package:
  name: carinata
  version: "0.12.1"

source:
  fn: carinata-0.12.1.tar.gz
  url: https://pypi.python.org/packages/source/c/carinata/carinata-0.12.1.tar.gz
  md5: 5ce82bfabc9fb5938d54a07942bcb8c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - carinata = carinata:main
    #
    # Would create an entry point called carinata that calls carinata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - carinata
    - carinata.management
    - carinata/management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/scottmcginness/carinata
  license: GNU General Public License v3 (GPLv3)
  summary: 'A rough-scaled python spec generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
