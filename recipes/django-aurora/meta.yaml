package:
  name: django-aurora
  version: "0.9.0"

source:
  fn: django-aurora-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aurora/django-aurora-0.9.0.tar.gz
  md5: bd5e4bad7b81f2fa7156efd49b7c5470
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aurora = django-aurora:main
    #
    # Would create an entry point called django-aurora that calls django-aurora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aurora

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aurora.io
  license: BSD
  summary: 'Django decorators and some other utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
