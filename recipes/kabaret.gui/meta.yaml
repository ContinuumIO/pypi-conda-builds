package:
  name: kabaret.gui
  version: "0.1.0b3"

source:
  fn: kabaret.gui-0.1.0b3.zip
  url: https://pypi.python.org/packages/source/k/kabaret.gui/kabaret.gui-0.1.0b3.zip
  md5: 070736e72901bddd99eb9d0b5e3aaf6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kabaret.gui = kabaret.gui:main
    #
    # Would create an entry point called kabaret.gui that calls kabaret.gui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kabaret.core

  run:
    - python
    - kabaret.core

test:
  # Python imports
  imports:
    - kabaret
    - kabaret.gui
    - kabaret.gui.icons
    - kabaret.gui.models
    - kabaret.gui.standalone
    - kabaret.gui.styles
    - kabaret.gui.styles.dark
    - kabaret.gui.styles.neonative
    - kabaret.gui.user_gui
    - kabaret.gui.widgets
    - kabaret.gui.widgets.value_editor
    - kabaret.gui.widgets.value_editor.editors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/kabaret
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Qt based GUI package for the kabaret framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
