package:
  name: autodocs
  version: "0.1.3"

source:
  fn: autodocs-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/autodocs/autodocs-0.1.3.tar.gz
  md5: e9f0e0e01726d8273fc5f78380ba777d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autodocs = autodocs:main
    #
    # Would create an entry point called autodocs that calls autodocs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - autodocs
    - autodocs.management
    - autodocs.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/thinkingpotato/autodocs
  license: BSD License
  summary: 'Management command for documenting and validating data models in Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
