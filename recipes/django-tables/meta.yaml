package:
  name: django-tables
  version: "0.2"

source:
  fn: django-tables-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tables/django-tables-0.2.tar.gz
  md5: f1d27872f34bc353be3e83ad57a30ead
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tables = django-tables:main
    #
    # Would create an entry point called django-tables that calls django-tables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_tables
    - django_tables.app
    - django_tables.app.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/django-tables
  license: BSD License
  summary: 'Render QuerySets as tabular data in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
