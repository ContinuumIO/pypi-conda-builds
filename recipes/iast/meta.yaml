package:
  name: iast
  version: "0.2.1"

source:
  fn: iAST-0.2.1.zip
  url: https://pypi.python.org/packages/source/i/iAST/iAST-0.2.1.zip
  md5: db75714233f980c6026226d5b0e05a8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iast = iast:main
    #
    # Would create an entry point called iast that calls iast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplestruct >=0.2.1

  run:
    - python
    - simplestruct >=0.2.1

test:
  # Python imports
  imports:
    - iast
    - iast.asdl
    - iast.python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brandjon/iast
  license: MIT License
  summary: 'A library for defining and manipulating ASTs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
