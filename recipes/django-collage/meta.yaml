package:
  name: django-collage
  version: "1.0.1"

source:
  fn: django-collage-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-collage/django-collage-1.0.1.tar.gz
  md5: bb12386e95fc7f2e9ee3754bdfcd2603
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-collage = django-collage:main
    #
    # Would create an entry point called django-collage that calls django-collage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - sorl-thumbnail
    - pil

  run:
    - python
    - setuptools
    - django
    - sorl-thumbnail
    - pil

test:
  # Python imports
  imports:
    - collage
    - collage.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gotlium/django-collage
  license: GNU General Public License (GPL)
  summary: 'django collages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
