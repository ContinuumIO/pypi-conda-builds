package:
  name: archetypes.linguakeywordwidget
  version: "1.1"

source:
  fn: archetypes.linguakeywordwidget-1.1.zip
  url: https://pypi.python.org/packages/source/a/archetypes.linguakeywordwidget/archetypes.linguakeywordwidget-1.1.zip
  md5: e6a1fc81ad066cacb4d9b7b1d8f4c3e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.linguakeywordwidget = archetypes.linguakeywordwidget:main
    #
    # Would create an entry point called archetypes.linguakeywordwidget that calls archetypes.linguakeywordwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.archetypes

  run:
    - python
    - setuptools
    - setuptools
    - products.archetypes

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.linguakeywordwidget
    - archetypes.linguakeywordwidget.tests
    - archetypes.linguakeywordwidget.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/archetypes.linguakeywordwidget
  license: ZPL 2.1
  summary: 'Archetypes keyword widget with multilanguage support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
