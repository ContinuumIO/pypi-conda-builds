package:
  name: django-vmail
  version: "0.2.3"

source:
  fn: django-vmail-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vmail/django-vmail-0.2.3.tar.gz
  md5: f4b67a7f63f84713bcb7c9b8e667e30b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vmail = django-vmail:main
    #
    # Would create an entry point called django-vmail that calls django-vmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5.0

  run:
    - python
    - django >=1.5.0

test:
  # Python imports
  imports:
    - vmail
    - vmail.management
    - vmail.management.commands
    - vmail.migrations
    - vmail.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lgunsch/django-vmail/
  license: MIT License
  summary: 'Virtual mail administration django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
