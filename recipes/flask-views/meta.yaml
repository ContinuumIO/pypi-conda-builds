package:
  name: flask-views
  version: "0.2.1"

source:
  fn: Flask-Views-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Views/Flask-Views-0.2.1.tar.gz
  md5: 89fd9c04d6721e8b04c42b999170e74a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-views = flask-views:main
    #
    # Would create an entry point called flask-views that calls flask-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_views
    - flask_views.db
    - flask_views.db.mongoengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brocaar/flask-views
  license: BSD License
  summary: 'Class based views for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
