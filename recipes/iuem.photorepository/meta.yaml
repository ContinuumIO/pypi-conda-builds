package:
  name: iuem.photorepository
  version: "1.0b4"

source:
  fn: iuem.photorepository-1.0b4.zip
  url: https://pypi.python.org/packages/source/i/iuem.photorepository/iuem.photorepository-1.0b4.zip
  md5: 97cd9628b09012df6727eec28742461c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iuem.photorepository = iuem.photorepository:main
    #
    # Would create an entry point called iuem.photorepository that calls iuem.photorepository.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform
    - plone.directives.form
    - collective.quickupload
    - pillow
    - plone.app.theming
    - plone.api
    - archetypes.schemaextender
    - plone.app.registry
    - products.atvocabularymanager ==1.6.2
    - eea.facetednavigation ==5.0
    - products.addremovewidget
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform
    - plone.directives.form
    - collective.quickupload
    - pillow
    - plone.app.theming
    - plone.api
    - archetypes.schemaextender
    - plone.app.registry
    - products.atvocabularymanager ==1.6.2
    - eea.facetednavigation ==5.0
    - products.addremovewidget
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - iuem
    - iuem.photorepository
    - iuem.photorepository.browser
    - iuem.photorepository.tests
    - iuem.photorepository.upgrades
    - iuem.photorepository.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erichardy/iuem.photorepository
  license: GPL
  summary: 'Addon for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
