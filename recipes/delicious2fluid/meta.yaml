package:
  name: delicious2fluid
  version: "0.1.2"

source:
  fn: delicious2fluid-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/delicious2fluid/delicious2fluid-0.1.2.tar.gz
  md5: 5f424ae58954bdacd48a5f01e4a929d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - delicious2fluid = delicious2fluid:main
    #
    # Would create an entry point called delicious2fluid that calls delicious2fluid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - delicious2fluid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fluidinfo.com
  license: MIT License
  summary: 'Imports delicious bookmarks to FluidDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
