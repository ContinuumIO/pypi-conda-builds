package:
  name: humboldt.cmfbibliographyat
  version: "0.2.0"

source:
  fn: humboldt.cmfbibliographyat-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/humboldt.cmfbibliographyat/humboldt.cmfbibliographyat-0.2.0.tar.gz
  md5: 8b18869b5b0c968fe9c498534c5b9559
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - humboldt.cmfbibliographyat = humboldt.cmfbibliographyat:main
    #
    # Would create an entry point called humboldt.cmfbibliographyat that calls humboldt.cmfbibliographyat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - collective.monkeypatcherpanel
    - products.basesyndication
    - products.fatsyndication

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - collective.monkeypatcherpanel
    - products.basesyndication
    - products.fatsyndication

test:
  # Python imports
  imports:
    - humboldt
    - humboldt.cmfbibliographyat
    - humboldt.cmfbibliographyat.adapters
    - humboldt.cmfbibliographyat.browser
    - humboldt.cmfbibliographyat.content
    - humboldt.cmfbibliographyat.interfaces
    - humboldt.cmfbibliographyat.portlets
    - humboldt.cmfbibliographyat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/humboldt.cmfbibliographyat
  license: GNU General Public License (GPL)
  summary: 'Humboldt University CMFBibliographyAT extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
