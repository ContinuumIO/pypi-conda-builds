package:
  name: django-gubbins
  version: "1.1.2"

source:
  fn: django-gubbins-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gubbins/django-gubbins-1.1.2.tar.gz
  md5: 2cacd3a211ae9b9bd55a29c126a4cb61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gubbins = django-gubbins:main
    #
    # Would create an entry point called django-gubbins that calls django-gubbins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3,<1.6

  run:
    - python
    - django >=1.3,<1.6

test:
  # Python imports
  imports:
    - gubbins
    - gubbins.db
    - gubbins.files

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlio/django-gubbins
  license: BSD
  summary: 'A collection of useful snippets for enhancing or replacing functionality within Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
