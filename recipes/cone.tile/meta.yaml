package:
  name: cone.tile
  version: "0.9.2"

source:
  fn: cone.tile-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cone.tile/cone.tile-0.9.2.tar.gz
  md5: 3964ba0e86ed9a0c4c979052867b8108
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cone.tile = cone.tile:main
    #
    # Would create an entry point called cone.tile that calls cone.tile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.exceptions
    - pyramid

  run:
    - python
    - setuptools
    - zope.component
    - zope.exceptions
    - pyramid

test:
  # Python imports
  imports:
    - cone
    - cone.tile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bluedynamics/cone.tile
  license: Simplified BSD
  summary: 'Provide parts of a web application as tiles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
