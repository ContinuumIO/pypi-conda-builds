package:
  name: collective.steps
  version: "0.3"

source:
  fn: collective.steps-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.steps/collective.steps-0.3.zip
  md5: df7fd4d212effba1249e25fd8a9f91fc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.steps = collective.steps:main
    #
    # Would create an entry point called collective.steps that calls collective.steps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.steps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.steps
  license: GPL
  summary: 'Steps to manage a plone site with Stepper by Makina Corpus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
