package:
  name: simpleldap
  version: "0.8"

source:
  fn: simpleldap-0.8.tar.gz
  url: https://pypi.python.org/packages/source/s/simpleldap/simpleldap-0.8.tar.gz
  md5: f8a95b24895596338032ca4b4450f1de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleldap = simpleldap:main
    #
    # Would create an entry point called simpleldap that calls simpleldap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap

  run:
    - python
    - python-ldap

test:
  # Python imports
  imports:
    - simpleldap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gdub/python-simpleldap
  license: MIT
  summary: 'A module that makes simple LDAP usage simple.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
