package:
  name: django-mptt-admin
  version: "0.2.1"

source:
  fn: django-mptt-admin-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mptt-admin/django-mptt-admin-0.2.1.tar.gz
  md5: 5ca5e87f1e6997a04f782e58c1500dd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mptt-admin = django-mptt-admin:main
    #
    # Would create an entry point called django-mptt-admin that calls django-mptt-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mptt
    - six

  run:
    - python
    - django-mptt
    - six

test:
  # Python imports
  imports:
    - django_mptt_admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mbraak/django-mptt-admin
  license: Apache Software License
  summary: 'Django-mptt-admin provides a nice Django Admin interface for Mptt models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
