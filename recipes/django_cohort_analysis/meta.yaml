package:
  name: django_cohort_analysis
  version: "0.0.2b"

source:
  fn: django_cohort_analysis-0.0.2b.tar.gz
  url: https://pypi.python.org/packages/source/d/django_cohort_analysis/django_cohort_analysis-0.0.2b.tar.gz
  md5: f72350fda42f97e6456eb19d8ccf7683
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_cohort_analysis = django_cohort_analysis:main
    #
    # Would create an entry point called django_cohort_analysis that calls django_cohort_analysis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.0

  run:
    - python
    - django >=1.6.0

test:
  # Python imports
  imports:
    - django_cohort_analysis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jturner/django_cohort_analysis
  license: BSD License
  summary: 'A small app to perform cohort analysis in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
