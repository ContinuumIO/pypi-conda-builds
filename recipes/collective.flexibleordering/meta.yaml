package:
  name: collective.flexibleordering
  version: "1.0beta2"

source:
  fn: collective.flexibleordering-1.0beta2.zip
  url: https://pypi.python.org/packages/source/c/collective.flexibleordering/collective.flexibleordering-1.0beta2.zip
  md5: e193a490a82bcd009ba3d30a3454c5a7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.flexibleordering = collective.flexibleordering:main
    #
    # Would create an entry point called collective.flexibleordering that calls collective.flexibleordering.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - blist

  run:
    - python
    - setuptools
    - setuptools
    - blist

test:
  # Python imports
  imports:
    - collective
    - collective.flexibleordering

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jazkarta/collective.flexibleordering
  license: GPL
  summary: 'A custom folder ordering implementation, which stores order persistently, but sorts content based on a rule.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
