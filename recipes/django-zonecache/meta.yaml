package:
  name: django-zonecache
  version: "0.2.2"

source:
  fn: django-zonecache-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zonecache/django-zonecache-0.2.2.tar.gz
  md5: f261294741d5a961ab553394a9aaaba6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zonecache = django-zonecache:main
    #
    # Would create an entry point called django-zonecache that calls django-zonecache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_zonecache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ailabs/django-zone-cache/
  license: MIT License
  summary: 'Small library to add support for cache zones to Django cache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
