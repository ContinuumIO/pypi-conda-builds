package:
  name: django-authopenid
  version: "1.0.1"

source:
  fn: django-authopenid-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-authopenid/django-authopenid-1.0.1.tar.gz
  md5: 93d44b4ce40de55bed36c9ed292adb49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-authopenid = django-authopenid:main
    #
    # Would create an entry point called django-authopenid that calls django-authopenid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-openid >=2.2.1
    - django-registration

  run:
    - python
    - python-openid >=2.2.1
    - django-registration

test:
  # Python imports
  imports:
    - django_authopenid
    - django_authopenid.management
    - django_authopenid.management.commands
    - django_authopenid.tests
    - django_authopenid.utils
    - example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.e-engura.org/django-authopenid/
  license: BSD License or Apache Software License
  summary: 'Openid authentification application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
