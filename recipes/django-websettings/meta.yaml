package:
  name: django-websettings
  version: "1.0b1"

source:
  fn: django-websettings-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-websettings/django-websettings-1.0b1.tar.gz
  md5: 3c53b3b0e525a4b71109f39547335893
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-websettings = django-websettings:main
    #
    # Would create an entry point called django-websettings that calls django-websettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.6

  run:
    - python
    - django >=1.4,<1.6

test:
  # Python imports
  imports:
    - websettings
    - websettings.backends
    - websettings.management
    - websettings.management.commands
    - websettings.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hirokiky/django-websettings
  license: MIT License
  summary: "Django application to provide a web interface to set a yet another django's settings"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
