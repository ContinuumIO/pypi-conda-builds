package:
  name: django_rediscache
  version: "1.0.5"

source:
  fn: django_rediscache-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django_rediscache/django_rediscache-1.0.5.tar.gz
  md5: 3c49b397bde148003a4c2fa32e7e3d21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_rediscache = django_rediscache:main
    #
    # Would create an entry point called django_rediscache that calls django_rediscache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.4.13

  run:
    - python
    - redis >=2.4.13

test:
  # Python imports
  imports:
    - django_rediscache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Django ORM cached tools in Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
