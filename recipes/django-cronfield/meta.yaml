package:
  name: django-cronfield
  version: "0.2"

source:
  fn: django-cronfield-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cronfield/django-cronfield-0.2.tar.gz
  md5: 85d9e66d5becf568c0d9fd5786534488
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cronfield = django-cronfield:main
    #
    # Would create an entry point called django-cronfield that calls django-cronfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cronfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/knaperek/django-cronfield
  license: BSD License
  summary: 'Simple CRON field with nice javascript-enabled User Interface and Cron format validation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
