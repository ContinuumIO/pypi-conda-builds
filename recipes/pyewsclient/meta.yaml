package:
  name: pyewsclient
  version: "1.2"

source:
  fn: PyEwsClient-1.2.tar.gz
  url: https://pypi.python.org/packages/source/P/PyEwsClient/PyEwsClient-1.2.tar.gz
  md5: 2c143d2db1beb7de5669aa1b2fc60d29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyewsclient = pyewsclient:main
    #
    # Would create an entry point called pyewsclient that calls pyewsclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - requests

  run:
    - python
    - lxml
    - requests

test:
  # Python imports
  imports:
    - pyewsclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/greenpau/PyEwsClient
  license: GNU General Public License v3 (GPLv3) or GNU General Public License v3 or later (GPLv3+)
  summary: 'Microsoft Office 365 EWS (Exchange Web Services) Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
