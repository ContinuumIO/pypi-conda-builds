package:
  name: collective.behavior.localdiazo
  version: "1.0b3"

source:
  fn: collective.behavior.localdiazo-1.0b3.zip
  url: https://pypi.python.org/packages/source/c/collective.behavior.localdiazo/collective.behavior.localdiazo-1.0b3.zip
  md5: d3d76b0d2dd4475af3f94b1caf126e4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.behavior.localdiazo = collective.behavior.localdiazo:main
    #
    # Would create an entry point called collective.behavior.localdiazo that calls collective.behavior.localdiazo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.behavior.localregistry
    - plone.app.registry
    - plone.app.theming >=1.1
    - plone.behavior
    - plone.directives.form
    - plone.registry
    - products.cmfcore
    - products.cmfplone >=4.2
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - collective.behavior.localregistry
    - plone.app.registry
    - plone.app.theming >=1.1
    - plone.behavior
    - plone.directives.form
    - plone.registry
    - products.cmfcore
    - products.cmfplone >=4.2
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.behavior
    - collective.behavior.localdiazo
    - collective.behavior.localdiazo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.behavior.localdiazo
  license: GNU General Public License v2 (GPLv2)
  summary: 'Dexterity behavior to enable a local Diazo theme.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
