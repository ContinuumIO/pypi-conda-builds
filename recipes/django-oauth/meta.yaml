package:
  name: django-oauth
  version: "1.1"

source:
  fn: django-oauth-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauth/django-oauth-1.1.tar.gz
  md5: 494439157bd9eee503c21abc6520d01c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauth = django-oauth:main
    #
    # Would create an entry point called django-oauth that calls django-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - examples
    - examples.provider
    - oauth
    - oauth_provider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.welldev.org/django-oauth/
  license: BSD License
  summary: 'Support of OAuth in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
