package:
  name: products.smartprintng
  version: "1.2.13"

source:
  fn: Products.SmartPrintNG-1.2.13.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SmartPrintNG/Products.SmartPrintNG-1.2.13.tar.gz
  md5: d6a7afa6aaf2e28abd08e32a8bfed8e4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.smartprintng = products.smartprintng:main
    #
    # Would create an entry point called products.smartprintng that calls products.smartprintng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zopyx.convert >=1.1.7

  run:
    - python
    - setuptools
    - zopyx.convert >=1.1.7

test:
  # Python imports
  imports:
    - Products
    - Products.SmartPrintNG

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://opensource.zopyx.com/projects/SmartPrintNG
  license: LGPL 3
  summary: 'Produce & Publish for Plone - Conversion of Plone content to PDF, RTS, ODT, DOCX and WML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
