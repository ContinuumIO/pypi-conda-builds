package:
  name: django-deserializer
  version: "0.11"

source:
  fn: django-deserializer-0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-deserializer/django-deserializer-0.11.tar.gz
  md5: 0c92e821b13d0f98af860927f48e82c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-deserializer = django-deserializer:main
    #
    # Would create an entry point called django-deserializer that calls django-deserializer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - deserializer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stargazer/django-deserializer/
  license: WTFPL
  summary: "A Django Mixin capable of deserializing a request's body into python data structures."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
