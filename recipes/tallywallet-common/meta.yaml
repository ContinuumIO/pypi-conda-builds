package:
  name: tallywallet-common
  version: "0.009"

source:
  fn: tallywallet-common-0.009.tar.gz
  url: https://pypi.python.org/packages/source/t/tallywallet-common/tallywallet-common-0.009.tar.gz
  md5: 84e79af685352e899181db3b2d5ba0dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tallywallet-common = tallywallet-common:main
    #
    # Would create an entry point called tallywallet-common that calls tallywallet-common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tallywallet.common
    - tallywallet.common.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.assembla.com/spaces/tallywallet/messages
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'A currency trading ledger in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
