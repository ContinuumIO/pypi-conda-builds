package:
  name: monupco-dotcloud-python
  version: "2.4.1"

source:
  fn: monupco-dotcloud-python-2.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/monupco-dotcloud-python/monupco-dotcloud-python-2.4.1.tar.gz
  md5: 93d9b4b6a1cf45312691b6066de6ba43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monupco-dotcloud-python = monupco-dotcloud-python:main
    #
    # Would create an entry point called monupco-dotcloud-python that calls monupco-dotcloud-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - monupco_dcp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/monupco/monupco-dotcloud-python
  license: MIT License
  summary: 'monupco.com registartion agent for dotCloud / Python applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
