package:
  name: collective.amberjack.plonetour
  version: "1.1"

source:
  fn: collective.amberjack.plonetour-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.amberjack.plonetour/collective.amberjack.plonetour-1.1.tar.gz
  md5: 602037409e5f69a8087ab763c7f63d0f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.amberjack.plonetour = collective.amberjack.plonetour:main
    #
    # Would create an entry point called collective.amberjack.plonetour that calls collective.amberjack.plonetour.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.amberjack.core

  run:
    - python
    - setuptools
    - setuptools
    - collective.amberjack.core

test:
  # Python imports
  imports:
    - collective
    - collective.amberjack
    - collective.amberjack.plonetour

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.amberjack.plonetour
  license: GNU General Public License (GPL)
  summary: 'Plone first install tour'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
