package:
  name: dsfinterp
  version: "0.1"

source:
  fn: dsfinterp-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dsfinterp/dsfinterp-0.1.tar.gz
  md5: 8425aff8a75cc68e559e2c01b19f55a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dsfinterp = dsfinterp:main
    #
    # Would create an entry point called dsfinterp that calls dsfinterp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dsfinterp
    - dsfinterp/test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/camm-sns/dsfinterp
  license: MIT License
  summary: 'Cubic Spline Interpolation of Dynamics Structure Factors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
