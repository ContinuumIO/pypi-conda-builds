package:
  name: elementtreewriter
  version: "1.1"

source:
  fn: elementtreewriter-1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/elementtreewriter/elementtreewriter-1.1.tar.gz
  md5: 3efd2e82e73f076f6ca553166b569e04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elementtreewriter = elementtreewriter:main
    #
    # Would create an entry point called elementtreewriter that calls elementtreewriter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - elementtree >=1.2.6

  run:
    - python
    - elementtree >=1.2.6

test:
  # Python imports
  imports:
    - elementtreewriter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: D-FSL - German Free Software License
  summary: 'XML writer for elementtree with sane namespace support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
