package:
  name: ftw.quota
  version: "1.0"

source:
  fn: ftw.quota-1.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.quota/ftw.quota-1.0.zip
  md5: f8baf6ba2f6289ae7be80199c2a86066
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.quota = ftw.quota:main
    #
    # Would create an entry point called ftw.quota that calls ftw.quota.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - ftw
    - ftw.quota
    - ftw.quota.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.quota/
  license: GNU General Public License (GPL)
  summary: 'Quota support for archetypes containers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
