package:
  name: easyshop.groups
  version: "0.1a1"

source:
  fn: easyshop.groups-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.groups/easyshop.groups-0.1a1.tar.gz
  md5: 1e1dca9a0f3ef2f008ec5d24dfa511d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.groups = easyshop.groups:main
    #
    # Would create an entry point called easyshop.groups that calls easyshop.groups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.groups
    - easyshop.groups.adapters
    - easyshop.groups.browser
    - easyshop.groups.content
    - easyshop.groups.profiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'Groups for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
