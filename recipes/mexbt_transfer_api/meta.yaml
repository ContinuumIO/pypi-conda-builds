package:
  name: mexbt_transfer_api
  version: "0.3"

source:
  fn: mexbt_transfer_api-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mexbt_transfer_api/mexbt_transfer_api-0.3.tar.gz
  md5: a103cb361ce7d0b4b7739e05732447df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mexbt_transfer_api = mexbt_transfer_api:main
    #
    # Would create an entry point called mexbt_transfer_api that calls mexbt_transfer_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - mexbt_transfer_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/meXBT/transfer-api-python
  license: MIT
  summary: 'A lightweight python client for the meXBT Transfer API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
