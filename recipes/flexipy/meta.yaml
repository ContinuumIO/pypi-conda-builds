package:
  name: flexipy
  version: "0.4.2"

source:
  fn: flexipy-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flexipy/flexipy-0.4.2.tar.gz
  md5: d4c266e630e5d7be26018da2d93fa011
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flexipy = flexipy:main
    #
    # Would create an entry point called flexipy that calls flexipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - paver ==1.0.5
    - requests

  run:
    - python
    - setuptools >=0.6b1
    - paver ==1.0.5
    - requests

test:
  # Python imports
  imports:
    - flexipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JakubJecminek/flexipy
  license: BSD
  summary: 'A library for communication with accounting system Flexibee.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
