package:
  name: zettwerk.mailtemplates
  version: "0.2.2"

source:
  fn: zettwerk.mailtemplates-0.2.2.zip
  url: https://pypi.python.org/packages/source/z/zettwerk.mailtemplates/zettwerk.mailtemplates-0.2.2.zip
  md5: cc2069f6f951aeedfdb428bac6237f68
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zettwerk.mailtemplates = zettwerk.mailtemplates:main
    #
    # Would create an entry point called zettwerk.mailtemplates that calls zettwerk.mailtemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

test:
  # Python imports
  imports:
    - zettwerk
    - zettwerk.mailtemplates
    - zettwerk.mailtemplates.browser
    - zettwerk.mailtemplates.content
    - zettwerk.mailtemplates.filters
    - zettwerk.mailtemplates.interfaces
    - zettwerk.mailtemplates.portlets
    - zettwerk.mailtemplates.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zettwerk.com
  license: GNU General Public License (GPL)
  summary: 'Create and send mail templates in plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
