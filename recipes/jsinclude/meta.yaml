package:
  name: jsinclude
  version: "1.2.4"

source:
  fn: jsinclude-1.2.4.zip
  url: https://pypi.python.org/packages/source/j/jsinclude/jsinclude-1.2.4.zip
  md5: 3424ab727426f9f3cbbd6082f630760b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsinclude = jsinclude:main
    #
    # Would create an entry point called jsinclude that calls jsinclude.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rjsmin

  run:
    - python
    - rjsmin

test:
  # Python imports
  imports:
    - jsinclude
    - jsinclude.templatetags
    - jsinclude.templatetags.pkg
    - jsinclude.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cobbdb/jsinclude
  license: MIT License
  summary: 'A Django 1.3+ tag to keep JavaScript out of your templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
