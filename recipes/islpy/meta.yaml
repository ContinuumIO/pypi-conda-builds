package:
  name: islpy
  version: "2015.1.1"

source:
  fn: islpy-2015.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/islpy/islpy-2015.1.1.tar.gz
  md5: f0cd59fad1a06f0e94ec0a2b073b063f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - islpy = islpy:main
    #
    # Would create an entry point called islpy that calls islpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest >=2
    - six

  run:
    - python
    - pytest >=2
    - six

test:
  # Python imports
  imports:
    - islpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://documen.tician.de/islpy
  license: MIT License
  summary: 'Wrapper around isl, an integer set library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
