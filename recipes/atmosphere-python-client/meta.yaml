package:
  name: atmosphere-python-client
  version: "1.0.0"

source:
  fn: atmosphere-python-client-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/atmosphere-python-client/atmosphere-python-client-1.0.0.tar.gz
  md5: 014ca10a1afc0ee35415689026d47bae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atmosphere-python-client = atmosphere-python-client:main
    #
    # Would create an entry point called atmosphere-python-client that calls atmosphere-python-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - atmosphere
    - atmosphere.appliance
    - atmosphere.machine
    - atmosphere.mapping
    - atmosphere.property

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dice-cyfronet/atmosphere-python-client
  license: UNKNOWN
  summary: 'Atmosphere REST API client written in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
