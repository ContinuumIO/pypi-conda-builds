package:
  name: ua-parser
  version: "0.3.6"

source:
  fn: ua-parser-0.3.6.tar.gz
  url: https://pypi.python.org/packages/source/u/ua-parser/ua-parser-0.3.6.tar.gz
  md5: 9c108bcb65d358ea3091d25457ad735a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ua-parser = ua-parser:main
    #
    # Would create an entry point called ua-parser that calls ua-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ua_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tobie/ua-parser
  license: Apache Software License
  summary: "Python port of Browserscope's user agent parser"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
