package:
  name: django-templatetag-handlebars
  version: "1.2.0"

source:
  fn: django-templatetag-handlebars-1.2.0.zip
  url: https://pypi.python.org/packages/source/d/django-templatetag-handlebars/django-templatetag-handlebars-1.2.0.zip
  md5: 2eb9932a484cfc33d6ca9ad6c0ed8e89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templatetag-handlebars = django-templatetag-handlebars:main
    #
    # Would create an entry point called django-templatetag-handlebars that calls django-templatetag-handlebars.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - templatetag_handlebars
    - templatetag_handlebars.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/makinacorpus/django-templatetag-handlebars
  license: LPGL, see LICENSE file.
  summary: 'Easily embed Handlebars.js templates in your django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
