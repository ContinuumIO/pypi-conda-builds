package:
  name: aversion
  version: "0.1.0"

source:
  fn: aversion-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aversion/aversion-0.1.0.tar.gz
  md5: 4d0805b1c918d2d9a0491c2fc3f3e56b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aversion = aversion:main
    #
    # Would create an entry point called aversion that calls aversion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob >=1.1

  run:
    - python
    - setuptools
    - webob >=1.1

test:
  # Python imports
  imports:
    - aversion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klmitch/aversion
  license: Apache Software License
  summary: 'AVersion WSGI Version Selection Application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
