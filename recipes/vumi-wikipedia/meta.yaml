package:
  name: vumi-wikipedia
  version: "0.2.0"

source:
  fn: vumi-wikipedia-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/v/vumi-wikipedia/vumi-wikipedia-0.2.0.tar.gz
  md5: 17d3ebeb2ee6a46d07ea4fbe5d3cc94d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vumi-wikipedia = vumi-wikipedia:main
    #
    # Would create an entry point called vumi-wikipedia that calls vumi-wikipedia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vumi >=0.5
    - unidecode

  run:
    - python
    - vumi >=0.5
    - unidecode

test:
  # Python imports
  imports:
    - vumi_wikipedia
    - vumi_wikipedia.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/vumi-wikipedia
  license: BSD License
  summary: 'Vumi Wikipedia App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
