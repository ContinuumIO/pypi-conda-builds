package:
  name: slapos.recipe.template
  version: "2.6"

source:
  fn: slapos.recipe.template-2.6.tar.gz
  url: https://pypi.python.org/packages/source/s/slapos.recipe.template/slapos.recipe.template-2.6.tar.gz
  md5: 10d09291db282026ee7f2a7b55a0c946
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slapos.recipe.template = slapos.recipe.template:main
    #
    # Would create an entry point called slapos.recipe.template that calls slapos.recipe.template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - jinja2 >=2.7

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - jinja2 >=2.7

test:
  # Python imports
  imports:
    - slapos
    - slapos.recipe
    - slapos.recipe.template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3
  summary: 'Templating recipe with remote resource support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
