package:
  name: hl.plone.boardnotifications
  version: "1.0"

source:
  fn: hl.plone.boardnotifications-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hl.plone.boardnotifications/hl.plone.boardnotifications-1.0.tar.gz
  md5: 60f22247e277ba00b9c55244233f1194
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hl.plone.boardnotifications = hl.plone.boardnotifications:main
    #
    # Would create an entry point called hl.plone.boardnotifications that calls hl.plone.boardnotifications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneboard >=3.0
    - products.cmfplone >=4.1

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneboard >=3.0
    - products.cmfplone >=4.1

test:
  # Python imports
  imports:
    - hl
    - hl.plone
    - hl.plone.boardnotifications
    - hl.plone.boardnotifications.browser
    - hl.plone.boardnotifications.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Haufe-Lexware/hl.plone.boardnotifications
  license: GPL
  summary: 'Notify users when Ploneboard threads or comments are modified'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
