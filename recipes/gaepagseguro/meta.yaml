package:
  name: gaepagseguro
  version: "0.3"

source:
  fn: gaepagseguro-0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gaepagseguro/gaepagseguro-0.3.tar.gz
  md5: 1fd89c5698713b775fb930cd56611967
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaepagseguro = gaepagseguro:main
    #
    # Would create an entry point called gaepagseguro that calls gaepagseguro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gaebusiness >=4.5.2
    - gaegraph >=3.8
    - gaeforms >=0.14
    - mock ==1.0.1
    - gaepermission >=0.12
    - xmltodict ==0.9.0

  run:
    - python
    - gaebusiness >=4.5.2
    - gaegraph >=3.8
    - gaeforms >=0.14
    - mock ==1.0.1
    - gaepermission >=0.12
    - xmltodict ==0.9.0

test:
  # Python imports
  imports:
    - gaepagseguro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renzon/gaepagseguro
  license: BSD License
  summary: 'A client library for Google App Engine integration with pagseguro.com.br'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
