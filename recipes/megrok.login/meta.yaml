package:
  name: megrok.login
  version: "0.4"

source:
  fn: megrok.login-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.login/megrok.login-0.4.tar.gz
  md5: 836c38b042f97c0a27c3e5ef046b5208
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.login = megrok.login:main
    #
    # Would create an entry point called megrok.login that calls megrok.login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grok
    - martian
    - zope.authentication
    - zope.container
    - zope.component
    - zope.interface
    - zope.pluggableauth
    - zope.securitypolicy

  run:
    - python
    - setuptools
    - grok
    - martian
    - zope.authentication
    - zope.container
    - zope.component
    - zope.interface
    - zope.pluggableauth
    - zope.securitypolicy

test:
  # Python imports
  imports:
    - megrok
    - megrok.login
    - megrok.login.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/megrok.login
  license: Zope Public License
  summary: 'Providing login screens for your Grok apps made easy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
