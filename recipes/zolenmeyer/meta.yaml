package:
  name: zolenmeyer
  version: "0.1"

source:
  fn: Zolenmeyer-0.1.zip
  url: https://pypi.python.org/packages/source/Z/Zolenmeyer/Zolenmeyer-0.1.zip
  md5: 74c72059c4c67b0306ea1309c8163c65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zolenmeyer = zolenmeyer:main
    #
    # Would create an entry point called zolenmeyer that calls zolenmeyer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zolenmeyer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zorceta/Zolenmeyer
  license: BSD
  summary: 'Stupid personally customized Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
