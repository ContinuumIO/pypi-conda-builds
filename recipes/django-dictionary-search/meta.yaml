package:
  name: django-dictionary-search
  version: "0.1"

source:
  fn: django-dictionary-search-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dictionary-search/django-dictionary-search-0.1.tar.gz
  md5: 148311451a9f6be7a4226258efff87ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dictionary-search = django-dictionary-search:main
    #
    # Would create an entry point called django-dictionary-search that calls django-dictionary-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dictionary_search

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/magenta-aps/
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'A simple, yet full-featured Django dictionary API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
