package:
  name: djangorestframework-filters
  version: "0.2.2"

source:
  fn: djangorestframework-filters-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-filters/djangorestframework-filters-0.2.2.tar.gz
  md5: b9e29fe824230d1e5cd7b564f14b209c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-filters = djangorestframework-filters:main
    #
    # Would create an entry point called djangorestframework-filters that calls djangorestframework-filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework
    - django-filter

  run:
    - python
    - djangorestframework
    - django-filter

test:
  # Python imports
  imports:
    - rest_framework_filters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/philipn/django-rest-framework-filters
  license: BSD License
  summary: 'Better filtering for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
