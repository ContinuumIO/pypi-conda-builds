package:
  name: django-theherk-download
  version: "1.2"

source:
  fn: django-theherk-download-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-theherk-download/django-theherk-download-1.2.tar.gz
  md5: 6332c1f75a1c75afa3e69beda1f2bb45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-theherk-download = django-theherk-download:main
    #
    # Would create an entry point called django-theherk-download that calls django-theherk-download.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - download
    - download.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theherk/django-theherk-download
  license: see file LICENSE
  summary: 'Django CMS plugin to post a download link'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
