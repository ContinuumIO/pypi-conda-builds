package:
  name: zope.modulealias
  version: "3.4.0"

source:
  fn: zope.modulealias-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.modulealias/zope.modulealias-3.4.0.tar.gz
  md5: 77f4603524b578a5c6b4b4fdde58a484
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.modulealias = zope.modulealias:main
    #
    # Would create an entry point called zope.modulealias that calls zope.modulealias.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.configuration

  run:
    - python
    - setuptools
    - zope.interface
    - zope.configuration

test:
  # Python imports
  imports:
    - zope
    - zope.modulealias
    - zope.modulealias.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.modulealias
  license: Zope Public License
  summary: 'Zope modulealias'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
