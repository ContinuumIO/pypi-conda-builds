package:
  name: cmsplugin-newsplus
  version: "0.1.9"

source:
  fn: cmsplugin-newsplus-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-newsplus/cmsplugin-newsplus-0.1.9.tar.gz
  md5: 53a3976fffceb645d5d21cb745d7019e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-newsplus = cmsplugin-newsplus:main
    #
    # Would create an entry point called cmsplugin-newsplus that calls cmsplugin-newsplus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.8
    - django-cms >=2.4
    - djangocms-text-ckeditor >=2.0
    - pillow

  run:
    - python
    - django <1.8
    - django-cms >=2.4
    - djangocms-text-ckeditor >=2.0
    - pillow

test:
  # Python imports
  imports:
    - cmsplugin_newsplus
    - cmsplugin_newsplus.migrations
    - cmsplugin_newsplus.south_migrations
    - cmsplugin_newsplus.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nimbis/cmsplugin-newsplus/
  license: BSD License
  summary: 'Simple news plugin for django-cms 3.x'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
