package:
  name: collective.searchform
  version: "1.0"

source:
  fn: collective.searchform-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.searchform/collective.searchform-1.0.zip
  md5: 90bbb5eec4f8ce818a42f93985556b98
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.searchform = collective.searchform:main
    #
    # Would create an entry point called collective.searchform that calls collective.searchform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.searchform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.searchform
  license: gpl
  summary: 'An extended search form for Plone >= 4.2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
