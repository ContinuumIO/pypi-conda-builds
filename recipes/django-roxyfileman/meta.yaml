package:
  name: django-roxyfileman
  version: "1.4.2.3"

source:
  fn: django-roxyfileman-1.4.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-roxyfileman/django-roxyfileman-1.4.2.3.tar.gz
  md5: ded4e28f1256ed37bad119866390a8cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-roxyfileman = django-roxyfileman:main
    #
    # Would create an entry point called django-roxyfileman that calls django-roxyfileman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - roxyfileman
    - roxyfileman.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zenwalker/django-roxyfileman
  license: GPLv3
  summary: 'Integrate Django with Roxy Fileman'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
