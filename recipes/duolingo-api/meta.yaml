package:
  name: duolingo-api
  version: "0.2"

source:
  fn: duolingo-api-0.2.zip
  url: https://pypi.python.org/packages/source/d/duolingo-api/duolingo-api-0.2.zip
  md5: 4ac22e3870651d948cf97805305bb349
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - duolingo-api = duolingo-api:main
    #
    # Would create an entry point called duolingo-api that calls duolingo-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug

  run:
    - python
    - werkzeug

test:
  # Python imports
  imports:
    - duolingo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/KartikTalwar/duolingo
  license: Apache Software License
  summary: 'Unofficial API for duolingo.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
