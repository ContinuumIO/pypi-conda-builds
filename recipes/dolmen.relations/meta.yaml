package:
  name: dolmen.relations
  version: "0.5"

source:
  fn: dolmen.relations-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dolmen.relations/dolmen.relations-0.5.tar.gz
  md5: 60ef4dd740b7d1bf2c74894eb3891eea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolmen.relations = dolmen.relations:main
    #
    # Would create an entry point called dolmen.relations that calls dolmen.relations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - grokcore.component
    - setuptools
    - zc.relation >=1.0
    - zope.component
    - zope.container
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.intid
    - zope.lifecycleevent
    - zope.schema

  run:
    - python
    - grokcore.component
    - setuptools
    - zc.relation >=1.0
    - zope.component
    - zope.container
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.intid
    - zope.lifecycleevent
    - zope.schema

test:
  # Python imports
  imports:
    - dolmen
    - dolmen.relations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitweb.dolmen-project.org
  license: GNU General Public License (GPL)
  summary: 'Dolmen relations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
