package:
  name: django-rest-test-data
  version: "0.2.0"

source:
  fn: django-rest-test-data-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-test-data/django-rest-test-data-0.2.0.tar.gz
  md5: 12ef67332271f96b6d5b11435490a5e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-test-data = django-rest-test-data:main
    #
    # Would create an entry point called django-rest-test-data that calls django-rest-test-data.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - model_mommy
    - six

  run:
    - python
    - django
    - model_mommy
    - six

test:
  # Python imports
  imports:
    - rest_test_data
    - rest_test_data/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hogarthww/django-rest-test-data
  license: GPLv2
  summary: 'Django app that exposes an API that lets you put the database in any (legal) desired state.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
