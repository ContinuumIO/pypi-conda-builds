package:
  name: django-le-twitter
  version: "0.2"

source:
  fn: django-le-twitter-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-le-twitter/django-le-twitter-0.2.tar.gz
  md5: 97bbfcf2c5ef45e897b943bbfbb609a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-le-twitter = django-le-twitter:main
    #
    # Would create an entry point called django-le-twitter that calls django-le-twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - tweepy >=1.5

  run:
    - python
    - django >=1.3
    - tweepy >=1.5

test:
  # Python imports
  imports:
    - le_twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brutasse/django-le-twitter
  license: BSD License
  summary: 'Twitter authentication that sucks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
