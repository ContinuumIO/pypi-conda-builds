package:
  name: super-resin-component
  version: "0.0.2"

source:
  fn: super-resin-component-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/super-resin-component/super-resin-component-0.0.2.tar.gz
  md5: 52cdaa4e944e754dcdef5852c6ffde3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - super-resin-component = super-resin-component:main
    #
    # Would create an entry point called super-resin-component that calls super-resin-component.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - components
    - components.accounts
    - components.address
    - components.business_units
    - components.calling_numbers
    - components.commons
    - components.controls
    - components.departments
    - components.electronic_mails
    - components.organizations
    - components.persons
    - components.positions
    - components.profiles
    - components.profiles_address
    - components.profiles_careers
    - components.profiles_certificates
    - components.profiles_educations
    - components.profiles_emails
    - components.profiles_phones
    - components.profiles_salaries
    - components.profiles_test_scores
    - components.sections
    - components.teams

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Super Resin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
