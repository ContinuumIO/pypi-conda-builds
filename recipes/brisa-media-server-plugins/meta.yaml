package:
  name: brisa-media-server-plugins
  version: "0.1.1"

source:
  fn: brisa-media-server-plugins-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brisa-media-server-plugins/brisa-media-server-plugins-0.1.1.tar.gz
  md5: 50856d1be9609250d6f5a9bc4f116143
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brisa-media-server-plugins = brisa-media-server-plugins:main
    #
    # Would create an entry point called brisa-media-server-plugins that calls brisa-media-server-plugins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - brisa_media_server/plugins
    - brisa_media_server/plugins/flickr
    - brisa_media_server/plugins/shoutcast
    - brisa_media_server/plugins/youtube

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://garage.maemo.org/projects/brisa/
  license: MIT License
  summary: 'BRisa Media Server plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
