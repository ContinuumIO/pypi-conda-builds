package:
  name: onegov.core
  version: "0.1.0"

source:
  fn: onegov.core-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/onegov.core/onegov.core-0.1.0.tar.gz
  md5: 3dbf844f988b529bf109a1db651b9508
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onegov.core = onegov.core:main
    #
    # Would create an entry point called onegov.core that calls onegov.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9
    - zope.sqlalchemy
    - pylru
    - onegov.server
    - cached_property
    - pyreact
    - passlib
    - psycopg2
    - python-magic
    - pylibmc
    - fs
    - py-bcrypt
    - chameleon
    - more.webassets
    - morepath
    - more.transaction
    - delorean
    - unidecode
    - translationstring
    - wtforms
    - polib
    - more.itsdangerous
    - dogpile.cache

  run:
    - python
    - sqlalchemy >=0.9
    - zope.sqlalchemy
    - pylru
    - onegov.server
    - cached_property
    - pyreact
    - passlib
    - psycopg2
    - python-magic
    - pylibmc
    - fs
    - py-bcrypt
    - chameleon
    - more.webassets
    - morepath
    - more.transaction
    - delorean
    - unidecode
    - translationstring
    - wtforms
    - polib
    - more.itsdangerous
    - dogpile.cache

test:
  # Python imports
  imports:
    - onegov
    - onegov.core
    - onegov.core.crypto
    - onegov.core.orm
    - onegov.core.orm.mixins
    - onegov.core.orm.types
    - onegov.core.security

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seantis/onegov.core
  license: GNU General Public License v2 (GPLv2)
  summary: 'Contains code shared by all OneGov applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
