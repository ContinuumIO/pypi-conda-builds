package:
  name: sphinx-nameko-theme
  version: "0.0.3"

source:
  fn: sphinx-nameko-theme-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-nameko-theme/sphinx-nameko-theme-0.0.3.tar.gz
  md5: 985c93fb810f947c982fbf103b44070d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-nameko-theme = sphinx-nameko-theme:main
    #
    # Would create an entry point called sphinx-nameko-theme that calls sphinx-nameko-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx

  run:
    - python
    - setuptools
    - sphinx

test:
  # Python imports
  imports:
    - sphinx_nameko_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/onefinestay/sphinx-nameko-theme
  license: MIT License
  summary: 'Sphinx Nameko Theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
