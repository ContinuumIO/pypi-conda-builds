package:
  name: collective.diffbot
  version: "1.0"

source:
  fn: collective.diffbot-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.diffbot/collective.diffbot-1.0.zip
  md5: 8373a9ffd4166daccbaf43d5ffc0b848
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.diffbot = collective.diffbot:main
    #
    # Would create an entry point called collective.diffbot that calls collective.diffbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.diffbot
    - collective.diffbot.browser
    - collective.diffbot.browser.app
    - collective.diffbot.cache
    - collective.diffbot.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone/org/products/collective.diffbot
  license: GNU General Public License (GPL)
  summary: 'difbot.com wrapper for Plone usage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
