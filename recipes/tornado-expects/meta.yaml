package:
  name: tornado-expects
  version: "0.1.0"

source:
  fn: tornado-expects-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-expects/tornado-expects-0.1.0.tar.gz
  md5: 5f977816e27fabc95fee661c43052dcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-expects = tornado-expects:main
    #
    # Would create an entry point called tornado-expects that calls tornado-expects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - expects >=0.4.0

  run:
    - python
    - tornado
    - expects >=0.4.0

test:
  # Python imports
  imports:
    - tornado_expects

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaimegildesagredo/tornado-expects
  license: Apache Software License
  summary: 'Expects matchers for Tornado request and response objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
