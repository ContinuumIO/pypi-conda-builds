package:
  name: infi.memuse
  version: "0.0.11"

source:
  fn: infi.memuse-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.memuse/infi.memuse-0.0.11.tar.gz
  md5: 98780c11bdcd0af78b38165f1d1822f5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.memuse = infi.memuse:main
    #
    # Would create an entry point called infi.memuse that calls infi.memuse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - infi.pyutils
    - psutil >=1.2
    - setuptools

  run:
    - python
    - infi.pyutils
    - psutil >=1.2
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.memuse
    - infi.memuse.nose

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.memuse
  license: Python Software Foundation License
  summary: 'Nose plugin for testing for memory leaks during running tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
