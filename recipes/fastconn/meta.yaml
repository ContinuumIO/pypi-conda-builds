package:
  name: fastconn
  version: "1.0"

source:
  fn: fastconn-1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fastConn/fastconn-1.0.tar.gz
  md5: 21931f4d47c727fbf7d0f41476ea13cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fastconn = fastconn:main
    #
    # Would create an entry point called fastconn that calls fastconn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - termcolor ==1.1.0

  run:
    - python
    - termcolor ==1.1.0

test:
  # Python imports
  imports:
    - fastconn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A simple prgramme for short  communication such as share links or small files within LAN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
