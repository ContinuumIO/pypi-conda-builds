package:
  name: flask-bootstrap
  version: "3.3.2.1"

source:
  fn: Flask-Bootstrap-3.3.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Bootstrap/Flask-Bootstrap-3.3.2.1.tar.gz
  md5: 1ba62fb21946586385a70590294919b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bootstrap = flask-bootstrap:main
    #
    # Would create an entry point called flask-bootstrap that calls flask-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/flask-bootstrap
  license: BSD License
  summary: 'An extension that includes Bootstrap in your project, without any boilerplate code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
