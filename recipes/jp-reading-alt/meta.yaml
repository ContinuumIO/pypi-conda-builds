package:
  name: jp-reading-alt
  version: "0.1.1"

source:
  fn: jp-reading-alt-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jp-reading-alt/jp-reading-alt-0.1.1.tar.gz
  md5: 90299bbd43533bf5d18127ca9f0b8c2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jp-reading-alt = jp-reading-alt:main
    #
    # Would create an entry point called jp-reading-alt that calls jp-reading-alt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cjktools
    - cjktools_data
    - simplestats
    - django
    - django-hierarchy
    - south

  run:
    - python
    - cjktools
    - cjktools_data
    - simplestats
    - django
    - django-hierarchy
    - south

test:
  # Python imports
  imports:
    - jp_reading_alt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lars512/jp-reading-alt/
  license: BSD
  summary: 'Probabilistic reading error models for Japanese kanji.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
