package:
  name: django-maploom
  version: "1.1.1"

source:
  fn: django-maploom-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-maploom/django-maploom-1.1.1.tar.gz
  md5: 299af74e998125e847a563d6020ab5ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-maploom = django-maploom:main
    #
    # Would create an entry point called django-maploom that calls django-maploom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - maploom
    - maploom.geonode
    - maploom.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ROGUE-JCTD/django-maploom
  license: See LICENSE file.
  summary: 'Use MapLoom in your django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
