package:
  name: generators-to-coroutines
  version: "0.2.0"

source:
  fn: generators-to-coroutines-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/generators-to-coroutines/generators-to-coroutines-0.2.0.tar.gz
  md5: 06a19121a29201acdaccbe43850fa6ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - generators-to-coroutines = generators-to-coroutines:main
    #
    # Would create an entry point called generators-to-coroutines that calls generators-to-coroutines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - generators_to_coroutines

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kholdstare/generators-to-coroutines
  license: BSD License
  summary: 'Decorator for converting pull-based generators into push-based coroutines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
