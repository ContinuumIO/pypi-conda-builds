package:
  name: django-nested-formsets
  version: "0.4.4"

source:
  fn: django-nested-formsets-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nested-formsets/django-nested-formsets-0.4.4.tar.gz
  md5: e0319bff5758ebe72e700fda257cd511
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nested-formsets = django-nested-formsets:main
    #
    # Would create an entry point called django-nested-formsets that calls django-nested-formsets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - ordereddict ==1.1

  run:
    - python
    - django >=1.4
    - ordereddict ==1.1

test:
  # Python imports
  imports:
    - nestedformsets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ionata/django-nested-formsets
  license: UNKNOWN
  summary: 'Simple nested formsets in a ModelForm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
