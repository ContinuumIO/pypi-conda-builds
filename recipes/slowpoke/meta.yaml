package:
  name: slowpoke
  version: "1.0.1"

source:
  fn: slowpoke-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/slowpoke/slowpoke-1.0.1.tar.gz
  md5: 8f59d7c04cf7316ea9c46183684d589c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slowpoke = slowpoke:main
    #
    # Would create an entry point called slowpoke that calls slowpoke.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - slowpoke

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamfast/django-slowpoke
  license: BSD License
  summary: 'A utility to track test execution over time.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
