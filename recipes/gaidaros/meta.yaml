package:
  name: gaidaros
  version: "0.3.13"

source:
  fn: gaidaros-0.3.13.tar.gz
  url: https://pypi.python.org/packages/source/g/gaidaros/gaidaros-0.3.13.tar.gz
  md5: c835f16aad3adee27820053e061bfc06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaidaros = gaidaros:main
    #
    # Would create an entry point called gaidaros that calls gaidaros.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gaidaros

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rowanthorpe/gaidaros
  license: MIT License
  summary: u'Async server micro-framework for control freaks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
