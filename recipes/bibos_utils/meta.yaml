package:
  name: bibos_utils
  version: "0.0.2.5"

source:
  fn: bibos_utils-0.0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bibos_utils/bibos_utils-0.0.2.5.tar.gz
  md5: d3618a3f5d954144258ff2ba45f8772a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bibos_utils = bibos_utils:main
    #
    # Would create an entry point called bibos_utils that calls bibos_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - bibos_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/magenta-aps/
  license: GPLv3
  summary: 'Utilities for the BibOS system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
