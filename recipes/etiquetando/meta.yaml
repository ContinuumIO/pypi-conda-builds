package:
  name: etiquetando
  version: "0.1"

source:
  fn: etiquetando-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/etiquetando/etiquetando-0.1.tar.gz
  md5: de6d22d0419bb354be9b6541dc46ca91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etiquetando = etiquetando:main
    #
    # Would create an entry point called etiquetando that calls etiquetando.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stemming >=1.0
    - repoze.lru >=0.6

  run:
    - python
    - stemming >=1.0
    - repoze.lru >=0.6

test:
  # Python imports
  imports:
    - etiquetando

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/TracyWebTech/etiquetando
  license: BSD License
  summary: 'Term extractor (tags) for Brazilian Portuguese (pt-br)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
