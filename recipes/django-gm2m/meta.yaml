package:
  name: django-gm2m
  version: "0.2.7"

source:
  fn: django-gm2m-0.2.7.zip
  url: https://pypi.python.org/packages/source/d/django-gm2m/django-gm2m-0.2.7.zip
  md5: 6786b5e145e4683f6357f8d395d08f70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gm2m = django-gm2m:main
    #
    # Would create an entry point called django-gm2m that calls django-gm2m.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - gm2m

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tkhyn/django-gm2m
  license: MIT License
  summary: 'Django generic many-to-many field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
