package:
  name: django-hierarchical
  version: "0.1.6"

source:
  fn: django-hierarchical-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hierarchical/django-hierarchical-0.1.6.tar.gz
  md5: c9572928dd17b2c35781f877e7fbbd2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hierarchical = django-hierarchical:main
    #
    # Would create an entry point called django-hierarchical that calls django-hierarchical.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_hierarchy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://moxypark.co.uk/
  license: UNKNOWN
  summary: 'Hierarchical models and form fields for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
