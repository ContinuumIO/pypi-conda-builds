package:
  name: django-trello-webhooks
  version: "0.3"

source:
  fn: django-trello-webhooks-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-trello-webhooks/django-trello-webhooks-0.3.tar.gz
  md5: 271949163c1d1dab1d12b11548f915c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-trello-webhooks = django-trello-webhooks:main
    #
    # Would create an entry point called django-trello-webhooks that calls django-trello-webhooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.1

  run:
    - python
    - django >=1.7.1

test:
  # Python imports
  imports:
    - trello_webhooks
    - trello_webhooks.management
    - trello_webhooks.management.commands
    - trello_webhooks.migrations
    - trello_webhooks.templatetags
    - trello_webhooks.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yunojuno/django-trello-webhooks
  license: MIT License
  summary: 'Django Trello Webhooks - Trello callback integration for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
