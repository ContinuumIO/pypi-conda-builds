package:
  name: cmsmenu-redirect
  version: "0.1"

source:
  fn: cmsmenu-redirect-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsmenu-redirect/cmsmenu-redirect-0.1.tar.gz
  md5: b52a412fd34f349a996576ca0f722dc8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsmenu-redirect = cmsmenu-redirect:main
    #
    # Would create an entry point called cmsmenu-redirect that calls cmsmenu-redirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.1.0

  run:
    - python
    - setuptools
    - django-cms >=2.1.0

test:
  # Python imports
  imports:
    - cmsmenu_redirect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benliles/cmsmenu-redirect
  license: UNKNOWN
  summary: 'Django CMS Menu Plugin for Django Contrib Redirects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
