package:
  name: giphy-ipython-magic
  version: "0.0.2"

source:
  fn: giphy-ipython-magic-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/giphy-ipython-magic/giphy-ipython-magic-0.0.2.tar.gz
  md5: 92170c319012a579c52e983b8d4adfda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - giphy-ipython-magic = giphy-ipython-magic:main
    #
    # Would create an entry point called giphy-ipython-magic that calls giphy-ipython-magic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython >=2.0
    - requests

  run:
    - python
    - ipython >=2.0
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AustinRochford/giphy-ipython-magic
  license: MIT
  summary: 'giphy line magic for ipython 2 and 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
