package:
  name: buildout.dumprequirements
  version: "0.1.1"

source:
  fn: buildout.dumprequirements-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.dumprequirements/buildout.dumprequirements-0.1.1.tar.gz
  md5: b4133d469743e8280a761d616c146983
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.dumprequirements = buildout.dumprequirements:main
    #
    # Would create an entry point called buildout.dumprequirements that calls buildout.dumprequirements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - buildout
    - buildout.dumprequirements

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stefanfoulis/buildout.dumprequirements
  license: GPL
  summary: 'Dump buildout picked versions as a pip compatible requirements.txt file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
