package:
  name: django-cms-ajax-text-plugin
  version: "2.1.0"

source:
  fn: django-cms-ajax-text-plugin-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-ajax-text-plugin/django-cms-ajax-text-plugin-2.1.0.tar.gz
  md5: 9e904565887b4cec92cc0829262c53f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-ajax-text-plugin = django-cms-ajax-text-plugin:main
    #
    # Would create an entry point called django-cms-ajax-text-plugin that calls django-cms-ajax-text-plugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms <3
    - djangocms-text-ckeditor

  run:
    - python
    - django-cms <3
    - djangocms-text-ckeditor

test:
  # Python imports
  imports:
    - cms_ajax_text_plugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD license
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
