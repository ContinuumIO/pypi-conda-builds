package:
  name: feed2mb
  version: "0.9.1"

source:
  fn: feed2mb-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/f/feed2mb/feed2mb-0.9.1.tar.gz
  md5: 4836f1dc1002a83cf5baf7937ca60e69
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feed2mb = feed2mb:main
    #
    # Would create an entry point called feed2mb that calls feed2mb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - docs
    - feed2mb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/waltercruz/feed2mb/
  license: AGPL
  summary: 'Publish your feed items to a microblog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
