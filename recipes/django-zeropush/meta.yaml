package:
  name: django-zeropush
  version: "0.2.3"

source:
  fn: django-zeropush-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zeropush/django-zeropush-0.2.3.tar.gz
  md5: 55da18e5483fe26a4f822b944b34b296
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zeropush = django-zeropush:main
    #
    # Would create an entry point called django-zeropush that calls django-zeropush.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - zeropush

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hakanw/django-zeropush
  license: BSD licence, see LICENCE.txt
  summary: 'ZeroPush iOS push notifications support for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
