package:
  name: clamd
  version: "1.0.2"

source:
  fn: clamd-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/clamd/clamd-1.0.2.tar.gz
  md5: 637f1d44a60a511544b6bd0bcb817f8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clamd = clamd:main
    #
    # Would create an entry point called clamd that calls clamd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clamd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/graingert/python-clamd
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Clamd is a python interface to Clamd (Clamav daemon).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
