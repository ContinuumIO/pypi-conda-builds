package:
  name: django-static-compiler
  version: "0.3.3"

source:
  fn: django-static-compiler-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-static-compiler/django-static-compiler-0.3.3.tar.gz
  md5: 019f9aa814fa96cbb298686ce6abdeab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-static-compiler = django-static-compiler:main
    #
    # Would create an entry point called django-static-compiler that calls django-static-compiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - static_compiler
    - static_compiler.management
    - static_compiler.management.commands
    - static_compiler.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcramer/django-static-compiler
  license: Apache License 2.0
  summary: 'A static file compiler for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
