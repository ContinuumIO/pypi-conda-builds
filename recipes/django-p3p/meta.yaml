package:
  name: django-p3p
  version: "1.0.1"

source:
  fn: django-p3p-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-p3p/django-p3p-1.0.1.tar.gz
  md5: 198c19805df93590840edf785296c276
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-p3p = django-p3p:main
    #
    # Would create an entry point called django-p3p that calls django-p3p.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - p3p

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jjanssen/django-p3p
  license: Apache Software License
  summary: 'Django P3P makes it easier to set P3P HTTP headers to prevent session loss.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
