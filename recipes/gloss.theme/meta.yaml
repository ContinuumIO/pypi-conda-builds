package:
  name: gloss.theme
  version: "0.5.4"

source:
  fn: gloss.theme-0.5.4.zip
  url: https://pypi.python.org/packages/source/g/gloss.theme/gloss.theme-0.5.4.zip
  md5: 3062dbbf595012466b644f4d6248a5e6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gloss.theme = gloss.theme:main
    #
    # Would create an entry point called gloss.theme that calls gloss.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

test:
  # Python imports
  imports:
    - gloss
    - gloss.theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glossproject/gloss.theme
  license: GPL
  summary: 'gloss.theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
