package:
  name: flask-github
  version: "0.1.3"

source:
  fn: Flask-Github-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Github/Flask-Github-0.1.3.tar.gz
  md5: 267104fa7518f0081b9c9ee900cccfaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-github = flask-github:main
    #
    # Would create an entry point called flask-github that calls flask-github.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - oauth2

  run:
    - python
    - flask
    - oauth2

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jarodl/flask-github
  license: MIT
  summary: 'Adds support for authorizing users with Github to Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
