package:
  name: dj-dynamic-forms
  version: "0.4.dev20140206215248"

source:
  fn: dj-dynamic-forms-0.4.dev20140206215248.tar.gz
  url: https://pypi.python.org/packages/source/d/dj-dynamic-forms/dj-dynamic-forms-0.4.dev20140206215248.tar.gz
  md5: d1c7a3ea38777cbe927456098b810e51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj-dynamic-forms = dj-dynamic-forms:main
    #
    # Would create an entry point called dj-dynamic-forms that calls dj-dynamic-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-appconf >=0.6
    - six

  run:
    - python
    - django >=1.5
    - django-appconf >=0.6
    - six

test:
  # Python imports
  imports:
    - dynamic_forms
    - dynamic_forms.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chhantyal/dj-dynamic-forms
  license: BSD License
  summary: 'Dynamic forms for Django - data storage in Postgres Hstore. It is a reusable Django application to create and configure forms through the admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
