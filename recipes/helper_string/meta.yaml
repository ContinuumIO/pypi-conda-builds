package:
  name: helper_string
  version: "0.3"

source:
  fn: helper_string-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/helper_string/helper_string-0.3.tar.gz
  md5: eb5526fbc9c638fa439f5fb1fe201113
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helper_string = helper_string:main
    #
    # Would create an entry point called helper_string that calls helper_string.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - helper_string

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shuge/helper_string
  license: MIT License
  summary: 'String Helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
