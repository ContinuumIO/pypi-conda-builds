package:
  name: cubicweb-container
  version: "2.7.0"

source:
  fn: cubicweb-container-2.7.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-container/cubicweb-container-2.7.0.tar.gz
  md5: 2194a769e48dacbc70846978d9e1c1cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-container = cubicweb-container:main
    #
    # Would create an entry point called cubicweb-container that calls cubicweb-container.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.17
    - cubicweb-fastimport >=0.2.1
    - yams >=0.38.1

  run:
    - python
    - cubicweb >=3.17.17
    - cubicweb-fastimport >=0.2.1
    - yams >=0.38.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-container
  license: LGPL
  summary: 'provides "generic container" services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
