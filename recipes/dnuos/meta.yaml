package:
  name: dnuos
  version: "1.0.11"

source:
  fn: dnuos-1.0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/dnuos/dnuos-1.0.11.tar.gz
  md5: 5f6f6f1914ee4b45ecea456b56196d40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dnuos = dnuos:main
    #
    # Would create an entry point called dnuos that calls dnuos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dnuos
    - dnuos.cache
    - dnuos.id3
    - dnuos.output

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitheap.org/dnuos/
  license: GNU General Public License (GPL)
  summary: 'A tool for creating lists of music collections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
