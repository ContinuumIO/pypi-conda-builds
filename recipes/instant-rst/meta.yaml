package:
  name: instant-rst
  version: "0.9.1.0"

source:
  fn: instant-rst-0.9.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/instant-rst/instant-rst-0.9.1.0.tar.gz
  md5: 262efd57561e2416d93a0cd9f2dea513
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instant-rst = instant-rst:main
    #
    # Would create an entry point called instant-rst that calls instant-rst.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - docutils
    - pygments
    - flask-socketio

  run:
    - python
    - flask
    - docutils
    - pygments
    - flask-socketio

test:
  # Python imports
  imports:
    - instant_rst

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rykka/instant-rst.py
  license: MIT License
  summary: 'Preview Rst Document Instantly '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
