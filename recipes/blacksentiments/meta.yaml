package:
  name: blacksentiments
  version: "0.1"

source:
  fn: BlackSentiments-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BlackSentiments/BlackSentiments-0.1.tar.gz
  md5: 35347c7d460ef6f7b0d3b2dab36e8a68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blacksentiments = blacksentiments:main
    #
    # Would create an entry point called blacksentiments that calls blacksentiments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - blacksentiments

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BlackSentiments/
  license: LICENSE.txt
  summary: "Fast sentiment analysis for Tolkien's Black Speech of Mordor."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
