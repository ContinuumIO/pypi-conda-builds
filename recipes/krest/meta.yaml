package:
  name: krest
  version: "1.2.1"

source:
  fn: krest-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/k/krest/krest-1.2.1.tar.gz
  md5: 85dd0192e115acf3e43ca741bd50cdd4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - krest = krest:main
    #
    # Would create an entry point called krest that calls krest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.0

  run:
    - python
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - krest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kaminario/krest
  license: Apache Software License
  summary: 'The Kaminario REST (krest) is a client library that provides ORM like interface for working with Kaminario K2 REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
