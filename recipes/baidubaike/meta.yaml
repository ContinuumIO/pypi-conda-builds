package:
  name: baidubaike
  version: "1.3.2"

source:
  fn: baidubaike-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/b/baidubaike/baidubaike-1.3.2.tar.gz
  md5: 734bfe46dc00fc0d6999d676f21b290d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baidubaike = baidubaike:main
    #
    # Would create an entry point called baidubaike that calls baidubaike.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - baidubaike

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yakiang/baidubaike
  license: MIT License
  summary: 'A wrapper of Baidu Baike'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
