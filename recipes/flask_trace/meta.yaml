package:
  name: flask_trace
  version: "0.0.1"

source:
  fn: flask_trace-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_trace/flask_trace-0.0.1.tar.gz
  md5: beec4a0bc6950d63612b2b418420b552
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_trace = flask_trace:main
    #
    # Would create an entry point called flask_trace that calls flask_trace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flask_trace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/canassa/flask_trace
  license: MIT License
  summary: 'Log trace decorator function for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
