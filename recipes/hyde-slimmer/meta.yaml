package:
  name: hyde-slimmer
  version: "0.0.1"

source:
  fn: hyde-slimmer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hyde-slimmer/hyde-slimmer-0.0.1.tar.gz
  md5: 62c29cab6256c814058efb02d2628d2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyde-slimmer = hyde-slimmer:main
    #
    # Would create an entry point called hyde-slimmer that calls hyde-slimmer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - slimmer

  run:
    - python
    - setuptools
    - slimmer

test:
  # Python imports
  imports:
    - hyde_slimmer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstufft/hyde-slimmer
  license: BSD
  summary: 'Html compression as post processor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
