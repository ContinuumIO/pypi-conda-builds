package:
  name: django_lookup_dict
  version: "0.1"

source:
  fn: django_lookup_dict-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_lookup_dict/django_lookup_dict-0.1.tar.gz
  md5: ec43ec8aeefded3b8d3ffc5dad725a98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_lookup_dict = django_lookup_dict:main
    #
    # Would create an entry point called django_lookup_dict that calls django_lookup_dict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_lookup_dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hendawy/django_lookup_dict
  license: UNKNOWN
  summary: 'Django Lookup Dict is a django app that enables you use a django model the Python dict way'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
