package:
  name: iw.recipe.fss
  version: "0.2.1"

source:
  fn: iw.recipe.fss-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.recipe.fss/iw.recipe.fss-0.2.1.tar.gz
  md5: b1bc8831b8edaa26e28ad8ac4265b6ba
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.recipe.fss = iw.recipe.fss:main
    #
    # Would create an entry point called iw.recipe.fss that calls iw.recipe.fss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zc.buildout

test:
  # Python imports
  imports:
    - iw
    - iw.recipe
    - iw.recipe.fss
    - iw.recipe.fss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ingeniweb.svn.sourceforge.net/viewvc/ingeniweb/iw.recipe.fss
  license: GPL
  summary: 'Recipe to configure File System Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
