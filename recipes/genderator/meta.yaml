package:
  name: genderator
  version: "0.2.7.8"

source:
  fn: genderator-0.2.7.8.tar.gz
  url: https://pypi.python.org/packages/source/g/genderator/genderator-0.2.7.8.tar.gz
  md5: cce8a6e9d5483f0a6b0cc612f93566f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genderator = genderator:main
    #
    # Would create an entry point called genderator that calls genderator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - normalizr >=0.1.4

  run:
    - python
    - normalizr >=0.1.4

test:
  # Python imports
  imports:
    - genderator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidmogar/genderator
  license: MIT License
  summary: 'Python library to guess gender given a spanish full name'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
