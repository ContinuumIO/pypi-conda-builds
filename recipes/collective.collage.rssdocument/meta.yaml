package:
  name: collective.collage.rssdocument
  version: "0.2"

source:
  fn: collective.collage.rssdocument-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collage.rssdocument/collective.collage.rssdocument-0.2.tar.gz
  md5: 227c6c8ee919978c1302bda0cbaa15bd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.rssdocument = collective.collage.rssdocument:main
    #
    # Would create an entry point called collective.collage.rssdocument that calls collective.collage.rssdocument.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.collage
    - iservices.rssdocument

  run:
    - python
    - setuptools
    - setuptools
    - products.collage
    - iservices.rssdocument

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.rssdocument

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.collage.rssdocument
  license: LGPL
  summary: 'Add-on that allows displaying RSSDocuments inside a Collage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
