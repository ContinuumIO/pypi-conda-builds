package:
  name: eea.frame
  version: "0.4.3"

source:
  fn: eea.frame-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/e/eea.frame/eea.frame-0.4.3.tar.gz
  md5: 70fa788b3b9525963c9f3f0331bc0138
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eea.frame = eea.frame:main
    #
    # Would create an entry point called eea.frame that calls eea.frame.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - requests >=2.2

  run:
    - python
    - django >=1.6
    - requests >=2.2

test:
  # Python imports
  imports:
    - frame

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'EEA Frame'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
