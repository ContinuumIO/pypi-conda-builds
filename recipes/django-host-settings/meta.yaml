package:
  name: django-host-settings
  version: "0.3"

source:
  fn: django-host-settings-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-host-settings/django-host-settings-0.3.tar.gz
  md5: cb124f1698093d50ba9de825101b1ea8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-host-settings = django-host-settings:main
    #
    # Would create an entry point called django-host-settings that calls django-host-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_host_settings
    - django_host_settings.management
    - django_host_settings.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sandersnewmedia/django-host-settings
  license: UNKNOWN
  summary: 'A small Django app for managing local settings files for multiple hosts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
