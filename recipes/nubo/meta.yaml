package:
  name: nubo
  version: "0.7"

source:
  fn: nubo-0.7.tar.gz
  url: https://pypi.python.org/packages/source/n/nubo/nubo-0.7.tar.gz
  md5: 209f13913e5f6374ee23902e27801a5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nubo = nubo:main
    #
    # Would create an entry point called nubo that calls nubo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - apache-libcloud
    - paramiko
    - texttable

  run:
    - python
    - setuptools
    - apache-libcloud
    - paramiko
    - texttable

test:
  # Python imports
  imports:
    - nubo
    - nubo.clouds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/nubo
  license: BSD License
  summary: 'Virtual Machine deployments on multiple cloud providers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
