package:
  name: django-fabtastic
  version: "1.1"

source:
  fn: django-fabtastic-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fabtastic/django-fabtastic-1.1.tar.gz
  md5: 7e46bdb0fd88c6f0af8ad2fa605a5b4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fabtastic = django-fabtastic:main
    #
    # Would create an entry point called django-fabtastic that calls django-fabtastic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fabtastic
    - fabtastic.db
    - fabtastic.fabric
    - fabtastic.fabric.commands
    - fabtastic.management
    - fabtastic.management.commands
    - fabtastic.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/duointeractive/django-fabtastic
  license: BSD License
  summary: 'A Django app that uses Django manage.py extension commands with Fabric for deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
