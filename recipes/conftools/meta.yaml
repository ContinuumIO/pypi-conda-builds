package:
  name: conftools
  version: "1.5"

source:
  fn: ConfTools-1.5.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfTools/ConfTools-1.5.tar.gz
  md5: 2a56e58cd715ba35efd925238758ab7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - conftools = conftools:main
    #
    # Would create an entry point called conftools that calls conftools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - pycontracts >=1.2,<2

  run:
    - python
    - pyyaml
    - pycontracts >=1.2,<2

test:
  # Python imports
  imports:
    - conf_tools
    - conf_tools.unittests
    - conf_tools.unittests.groups
    - conf_tools.unittests.instantiate_tests
    - conf_tools.unittests.templating
    - conf_tools.utils
    - conftools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/AndreaCensi/conf_tools/
  license: LGPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
