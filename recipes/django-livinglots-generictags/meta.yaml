package:
  name: django-livinglots-generictags
  version: "1.0"

source:
  fn: django-livinglots-generictags-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livinglots-generictags/django-livinglots-generictags-1.0.tar.gz
  md5: b3b63e9b610f8f4895ed24b04033f1a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livinglots-generictags = django-livinglots-generictags:main
    #
    # Would create an entry point called django-livinglots-generictags that calls django-livinglots-generictags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - django-classy-tags ==0.4

  run:
    - python
    - django >=1.3.1
    - django-classy-tags ==0.4

test:
  # Python imports
  imports:
    - livinglots_generictags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/596acres/django-livinglots-generictags/
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'A set of helpers for creating Django template tags for models with generic relations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
