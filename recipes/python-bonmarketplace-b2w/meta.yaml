package:
  name: python-bonmarketplace-b2w
  version: "1.0.1b"

source:
  fn: python-bonmarketplace-b2w-1.0.1b.tar.gz
  url: https://pypi.python.org/packages/source/p/python-bonmarketplace-b2w/python-bonmarketplace-b2w-1.0.1b.tar.gz
  md5: 580d77176b9da2e78419c74ca9dc2bf7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-bonmarketplace-b2w = python-bonmarketplace-b2w:main
    #
    # Would create an entry point called python-bonmarketplace-b2w that calls python-bonmarketplace-b2w.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - b2w

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gabrielgaliaso/python-bonmarketplace-b2w
  license: MIT
  summary: u'Cliente Python para integra\xe7\xe3o com Bon Marketplace B2W (Shoptime, Lojas Americanas, Submarino e Soubarato)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
