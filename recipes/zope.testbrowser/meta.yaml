package:
  name: zope.testbrowser
  version: "4.0.4"

source:
  fn: zope.testbrowser-4.0.4.zip
  url: https://pypi.python.org/packages/source/z/zope.testbrowser/zope.testbrowser-4.0.4.zip
  md5: 6d840bf64003a5106b16321634c6ae34
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.testbrowser = zope.testbrowser:main
    #
    # Would create an entry point called zope.testbrowser that calls zope.testbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - mechanize >=0.2.0
    - setuptools
    - zope.interface
    - zope.schema
    - pytz >dev

  run:
    - python
    - mechanize >=0.2.0
    - setuptools
    - zope.interface
    - zope.schema
    - pytz >dev

test:
  # Python imports
  imports:
    - zope
    - zope.testbrowser
    - zope.testbrowser.ftests
    - zope.testbrowser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.testbrowser
  license: Zope Public License
  summary: 'Programmable browser for functional black-box tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
