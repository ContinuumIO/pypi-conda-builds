package:
  name: invoicegenerator
  version: "0.5.2"

source:
  fn: InvoiceGenerator-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/I/InvoiceGenerator/InvoiceGenerator-0.5.2.tar.gz
  md5: 3c23fc7c4a66393c7de7442b7b4f0121
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - invoicegenerator = invoicegenerator:main
    #
    # Would create an entry point called invoicegenerator that calls invoicegenerator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - reportlab
    - pillow
    - qrplatba >=0.3.3

  run:
    - python
    - reportlab
    - pillow
    - qrplatba >=0.3.3

test:
  # Python imports
  imports:
    - InvoiceGenerator
    - invoicegenerator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/creckx/InvoiceGenerator
  license: BSD License
  summary: 'Library to generate PDF invoice.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
