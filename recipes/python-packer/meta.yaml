package:
  name: python-packer
  version: "0.0.1"

source:
  fn: python-packer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-packer/python-packer-0.0.1.tar.gz
  md5: 63eaa7cb2090ae61a5fb2f22d459df7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-packer = python-packer:main
    #
    # Would create an entry point called python-packer that calls python-packer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sh ==1.11

  run:
    - python
    - sh ==1.11

test:
  # Python imports
  imports:
    - packer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nir0s/python-packer
  license: Apache Software License
  summary: "A Python interface for Hashicorp's Packer"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
