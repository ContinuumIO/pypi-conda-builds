package:
  name: kaichu
  version: "0.0.1"

source:
  fn: kaichu-0.0.1.zip
  url: https://pypi.python.org/packages/source/k/kaichu/kaichu-0.0.1.zip
  md5: 2f8cf3abea9c9c6556e582779e7bdecc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kaichu = kaichu:main
    #
    # Would create an entry point called kaichu that calls kaichu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jira-python
    - nose-sneeze

  run:
    - python
    - setuptools
    - jira-python
    - nose-sneeze

test:
  # Python imports
  imports:
    - kaichu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/NYTimes/kaichu
  license: Apache2.0
  summary: 'A Sneeze plugin for JIRA integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
