package:
  name: django-tribe-client
  version: "0.0.1"

source:
  fn: django-tribe-client-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tribe-client/django-tribe-client-0.0.1.tar.gz
  md5: bb2b575619d3adb35898e5327f540ff6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tribe-client = django-tribe-client:main
    #
    # Would create an entry point called django-tribe-client that calls django-tribe-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.1.0

  run:
    - python
    - requests ==2.1.0

test:
  # Python imports
  imports:
    - tribe-client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/greenelab/tribe-client
  license: BSD License
  summary: 'Reusable Django app to connect servers with the Tribe web service at Dartmouth College'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
