package:
  name: bettercache
  version: "0.6.1"

source:
  fn: bettercache-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bettercache/bettercache-0.6.1.tar.gz
  md5: b8a84def6b27d196112263eb4aa21a3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bettercache = bettercache:main
    #
    # Would create an entry point called bettercache that calls bettercache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bettercache
    - bettercache.templatetags
    - bettercache.tests
    - bettercachedemo
    - docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ironfroggy/django-better-cache
  license: MIT
  summary: 'A suite of better cache tools for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
