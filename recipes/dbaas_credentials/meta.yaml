package:
  name: dbaas_credentials
  version: "1.0.4"

source:
  fn: dbaas_credentials-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dbaas_credentials/dbaas_credentials-1.0.4.tar.gz
  md5: 130fd95b56705789229726d47b82cf10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbaas_credentials = dbaas_credentials:main
    #
    # Would create an entry point called dbaas_credentials that calls dbaas_credentials.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbaas_credentials

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/dbaas_credentials
  license: BSD License
  summary: 'A credential manager for DBaaS integrations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
