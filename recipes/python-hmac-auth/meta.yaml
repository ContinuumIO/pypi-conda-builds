package:
  name: python-hmac-auth
  version: "0.4"

source:
  fn: python-hmac-auth-0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hmac-auth/python-hmac-auth-0.4.tar.gz
  md5: 130f08ccdbb1715127f2cc4c6de4375d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hmac-auth = python-hmac-auth:main
    #
    # Would create an entry point called python-hmac-auth that calls python-hmac-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - python_hmac_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bazaarvoice/python-hmac-auth
  license: Apache Software License
  summary: 'Python client for jersey-hmac-auth (https://github.com/bazaarvoice/jersey-hmac-auth)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
