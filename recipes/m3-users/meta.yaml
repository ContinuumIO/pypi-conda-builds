package:
  name: m3-users
  version: "3.0.2"

source:
  fn: m3-users-3.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-users/m3-users-3.0.2.tar.gz
  md5: a70e7fef9f0f5a92fd769832040539b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-users = m3-users:main
    #
    # Would create an entry point called m3-users that calls m3-users.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - m3_users
    - m3_users.migrations
    - tests
    - tests.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barsgroup/m3_users
  license: MIT License
  summary: u'\u041f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u0438 \u0438 \u0440\u043e\u043b\u0438'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
