package:
  name: app-monitor
  version: "0.1.3"

source:
  fn: app-monitor-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/app-monitor/app-monitor-0.1.3.tar.gz
  md5: 3c34ccd20ac5385779e12233491d6441
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - app-monitor = app-monitor:main
    #
    # Would create an entry point called app-monitor that calls app-monitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy ==0.7.6
    - clint ==0.3.1
    - plac ==0.9.1

  run:
    - python
    - sqlalchemy ==0.7.6
    - clint ==0.3.1
    - plac ==0.9.1

test:
  # Python imports
  imports:
    - monitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Check script scheduler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
