package:
  name: flask-jwt
  version: "0.2.0"

source:
  fn: Flask-JWT-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-JWT/Flask-JWT-0.2.0.tar.gz
  md5: e2ac18c765f538c0340119355652a0c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-jwt = flask-jwt:main
    #
    # Would create an entry point called flask-jwt that calls flask-jwt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - itsdangerous >=0.24

  run:
    - python
    - flask >=0.9
    - itsdangerous >=0.24

test:
  # Python imports
  imports:
    - flask_jwt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattupstate/flask-jwt
  license: MIT License
  summary: 'JWT token authentication for Flask apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
