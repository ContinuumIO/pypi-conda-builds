package:
  name: dict_compare
  version: "1.0.2"

source:
  fn: dict_compare-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dict_compare/dict_compare-1.0.2.tar.gz
  md5: 841298a1c6e44c0b3e28ee6449d86a53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dict_compare = dict_compare:main
    #
    # Would create an entry point called dict_compare that calls dict_compare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dict_compare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://protocultura.cl/hg/dict_compare/
  license: BSD License
  summary: 'A dictionary comparer with decent difference reporting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
