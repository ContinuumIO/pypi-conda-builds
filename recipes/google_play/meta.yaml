package:
  name: google_play
  version: "0.2.1"

source:
  fn: google_play-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/google_play/google_play-0.2.1.tar.gz
  md5: 56b29496ed60ed932ca0f8df33ff1b8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google_play = google_play:main
    #
    # Would create an entry point called google_play that calls google_play.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.3.2
    - lxml ==3.3.1
    - requests ==2.2.1

  run:
    - python
    - beautifulsoup4 ==4.3.2
    - lxml ==3.3.1
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - google_play

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/h2rd/google_play
  license: MIT
  summary: 'Google Play app fetcher'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
