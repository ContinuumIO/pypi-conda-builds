package:
  name: collective.uuid
  version: "1.0rc1"

source:
  fn: collective.uuid-1.0rc1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.uuid/collective.uuid-1.0rc1.tar.gz
  md5: d321cf1e82fc2da7cd403a0515990ac2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.uuid = collective.uuid:main
    #
    # Would create an entry point called collective.uuid that calls collective.uuid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.baseid
    - uuid

  run:
    - python
    - setuptools
    - setuptools
    - collective.baseid
    - uuid

test:
  # Python imports
  imports:
    - collective
    - collective.uuid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openplans.org/projects/vice/
  license: ZPL,GPL
  summary: 'RFC 4122 universally unique identifier utility.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
