package:
  name: topp.recipes.opvarnish
  version: "0.1.0dev-r19468"

source:
  fn: topp.recipes.opvarnish-0.1.0dev-r19468.tar.gz
  url: https://pypi.python.org/packages/source/t/topp.recipes.opvarnish/topp.recipes.opvarnish-0.1.0dev-r19468.tar.gz
  md5: 8a6e08ecbecbf6f32166baab4d4e8126
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - topp.recipes.opvarnish = topp.recipes.opvarnish:main
    #
    # Would create an entry point called topp.recipes.opvarnish that calls topp.recipes.opvarnish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - topp
    - topp.recipes
    - topp.recipes.opvarnish
    - topp.recipes.opvarnish.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://topp.openplans.org/
  license: GPLv3
  summary: 'OpenPlans varnish caching server configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
