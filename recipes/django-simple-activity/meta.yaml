package:
  name: django-simple-activity
  version: "0.1.1"

source:
  fn: django-simple-activity-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-simple-activity/django-simple-activity-0.1.1.zip
  md5: a87f0f4462ab66d5ba99a162385fb4ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-activity = django-simple-activity:main
    #
    # Would create an entry point called django-simple-activity that calls django-simple-activity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0,<1.6.0
    - django-model-utils >=2.2

  run:
    - python
    - django >=1.5.0,<1.6.0
    - django-model-utils >=2.2

test:
  # Python imports
  imports:
    - activity
    - activity.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/richardasaurus/django-simple-activity
  license: Apache 2.0
  summary: 'Simple, generic, activity streams from the actions on your site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
