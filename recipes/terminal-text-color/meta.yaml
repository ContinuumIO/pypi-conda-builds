package:
  name: terminal-text-color
  version: "0.3"

source:
  fn: Terminal-Text-Color-0.3.tar.gz
  url: https://pypi.python.org/packages/source/T/Terminal-Text-Color/Terminal-Text-Color-0.3.tar.gz
  md5: 941633bfe9bdbea09ed6c3a69dba2b02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - terminal-text-color = terminal-text-color:main
    #
    # Would create an entry point called terminal-text-color that calls terminal-text-color.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - terminal_text_color

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ElMijo/terminal-text-color-py
  license: MIT License
  summary: 'Simple herramienta para darle color al texto en consola'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
