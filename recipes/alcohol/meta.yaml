package:
  name: alcohol
  version: "0.4"

source:
  fn: alcohol-0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/alcohol/alcohol-0.4.tar.gz
  md5: 468345150da877a2728b5a8204ec00a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alcohol = alcohol:main
    #
    # Would create an entry point called alcohol that calls alcohol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blinker
    - passlib >=1.6
    - six
    - itsdangerous >=0.14

  run:
    - python
    - blinker
    - passlib >=1.6
    - six
    - itsdangerous >=0.14

test:
  # Python imports
  imports:
    - alcohol
    - alcohol.mixins
    - alcohol.rbac

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/alcohol
  license: MIT
  summary: 'Handles user authentication, in a way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
