package:
  name: wtfmongoengine
  version: "0.1.2"

source:
  fn: WTFMongoengine-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/W/WTFMongoengine/WTFMongoengine-0.1.2.tar.gz
  md5: 678af591d972bf83da0d0d66dc5d25f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtfmongoengine = wtfmongoengine:main
    #
    # Would create an entry point called wtfmongoengine that calls wtfmongoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms

  run:
    - python
    - wtforms

test:
  # Python imports
  imports:
    - wtfmongoengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brocaar/wtfmongoengine
  license: BSD License
  summary: 'WTForms for Mongoengine documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
