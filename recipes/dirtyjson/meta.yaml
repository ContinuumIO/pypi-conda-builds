package:
  name: dirtyjson
  version: "1.0.3"

source:
  fn: dirtyjson-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dirtyjson/dirtyjson-1.0.3.tar.gz
  md5: 50e98bd2a71669480122a54cdad1d3fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dirtyjson = dirtyjson:main
    #
    # Would create an entry point called dirtyjson that calls dirtyjson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dirtyjson
    - dirtyjson.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codecobblers/dirtyjson
  license: MIT License or Academic Free License (AFL)
  summary: 'JSON decoder for Python that can extract data from the muck'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
