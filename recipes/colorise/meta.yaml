package:
  name: colorise
  version: "0.1.4"

source:
  fn: colorise-0.1.4.zip
  url: https://pypi.python.org/packages/source/c/colorise/colorise-0.1.4.zip
  md5: 980e159b22492c4f3743caca3663b141
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorise = colorise:main
    #
    # Would create an entry point called colorise that calls colorise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorise
    - colorise.nix
    - colorise.win

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MisanthropicBit/colorise
  license: MIT License
  summary: 'Easily print colored text to the console'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
