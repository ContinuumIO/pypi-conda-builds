package:
  name: browser_cookie
  version: "0.4"

source:
  fn: browser-cookie-0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/browser_cookie/browser-cookie-0.4.tar.gz
  md5: 4a8d10bb84c1caada61abfa9b6bc25b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - browser_cookie = browser_cookie:main
    #
    # Would create an entry point called browser_cookie that calls browser_cookie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pycrypto
    - keyring

  run:
    - python
    - pycrypto
    - keyring

test:
  # Python imports
  imports:
    - browser_cookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/richardpenman/browser_cookie
  license: lgpl
  summary: 'Loads cookies from your browser into a cookiejar object so can download with urllib and other libraries the same content you see in the web browser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
