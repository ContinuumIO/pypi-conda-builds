package:
  name: smartdrive
  version: "1.0"

source:
  fn: SmartDrive-1.1.tar.gz
  url: https://pypi.python.org/packages/source/S/SmartDrive/SmartDrive-1.1.tar.gz
  md5: e3033f75df01ce6dbc13f9ab3ad04683
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smartdrive = smartdrive:main
    #
    # Would create an entry point called smartdrive that calls smartdrive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - openelectrons_i2c

  run:
    - python
    - openelectrons_i2c

test:
  # Python imports
  imports:
    - smartdrive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openelectrons.com/index.php?module=documents&JAS_DocumentManager_op=viewDocument&JAS_Document_id=8
  license: smartencoding
  summary: 'OpenElectrons SmartDrive library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
