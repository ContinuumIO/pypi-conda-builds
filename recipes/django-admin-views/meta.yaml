package:
  name: django-admin-views
  version: "0.2.0"

source:
  fn: django-admin-views-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-views/django-admin-views-0.2.0.tar.gz
  md5: 6be2513b644ed82d012d1babdede9004
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-views = django-admin-views:main
    #
    # Would create an entry point called django-admin-views that calls django-admin-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - admin_views
    - admin_views.management
    - admin_views.management.commands
    - admin_views.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frankwiles/django-admin-views
  license: BSD License
  summary: 'django-admin-views is a simple way to add custom admin views and direct URLs to the Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
