package:
  name: chandler-gdataplugin
  version: "0.7"

source:
  fn: Chandler-gdataPlugin-0.7.tar.gz
  url: https://pypi.python.org/packages/source/C/Chandler-gdataPlugin/Chandler-gdataPlugin-0.7.tar.gz
  md5: 6143d8a4a137af44c7c164abb2772e1c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chandler-gdataplugin = chandler-gdataplugin:main
    #
    # Would create an entry point called chandler-gdataplugin that calls chandler-gdataplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata.py >=1.0.7

  run:
    - python
    - setuptools
    - gdata.py >=1.0.7

test:
  # Python imports
  imports:
    - gdatasync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Google Data synchronization support for Chandler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
