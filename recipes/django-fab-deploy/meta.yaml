package:
  name: django-fab-deploy
  version: "0.7.5"

source:
  fn: django-fab-deploy-0.7.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fab-deploy/django-fab-deploy-0.7.5.tar.gz
  md5: 1768f6b2676056cc810850f5b873214a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fab-deploy = django-fab-deploy:main
    #
    # Would create an entry point called django-fab-deploy that calls django-fab-deploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fab_deploy
    - fab_deploy.vcs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kmike/django-fab-deploy/
  license: MIT License
  summary: ' Django deployment tool '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
