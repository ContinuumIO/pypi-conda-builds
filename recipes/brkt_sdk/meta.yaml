package:
  name: brkt_sdk
  version: "0.1"

source:
  fn: brkt_sdk-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brkt_sdk/brkt_sdk-0.1.tar.gz
  md5: c50753ebfa2750e61b9415e23af5044a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brkt_sdk = brkt_sdk:main
    #
    # Would create an entry point called brkt_sdk that calls brkt_sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - oauthlib

  run:
    - python
    - requests
    - oauthlib

test:
  # Python imports
  imports:
    - brkt_requests
    - brkt_sdk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache
  summary: 'Python SDK to the Bracket API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
