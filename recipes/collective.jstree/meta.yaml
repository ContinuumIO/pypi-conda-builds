package:
  name: collective.jstree
  version: "0.9.7"

source:
  fn: collective.jstree-0.9.7.zip
  url: https://pypi.python.org/packages/source/c/collective.jstree/collective.jstree-0.9.7.zip
  md5: 315ecf19082397deb47cfee4d153f464
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jstree = collective.jstree:main
    #
    # Would create an entry point called collective.jstree that calls collective.jstree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.jstree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.jstree
  license: GPL
  summary: 'jsTree as a zope resource'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
