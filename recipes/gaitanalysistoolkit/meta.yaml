package:
  name: gaitanalysistoolkit
  version: "0.1.2"

source:
  fn: GaitAnalysisToolKit-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GaitAnalysisToolKit/GaitAnalysisToolKit-0.1.2.tar.gz
  md5: 16a30b1d2ba9a3ed0a425c95ada6ad15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaitanalysistoolkit = gaitanalysistoolkit:main
    #
    # Would create an entry point called gaitanalysistoolkit that calls gaitanalysistoolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6.1
    - scipy >=0.9.0
    - matplotlib >=1.1.0
    - tables >=2.3.1
    - pandas >=0.12.0
    - pyyaml >=3.10
    - dynamicisttoolkit >=0.3.5
    - oct2py >=1.2.0

  run:
    - python
    - numpy >=1.6.1
    - scipy >=0.9.0
    - matplotlib >=1.1.0
    - tables >=2.3.1
    - pandas >=0.12.0
    - pyyaml >=3.10
    - dynamicisttoolkit >=0.3.5
    - oct2py >=1.2.0

test:
  # Python imports
  imports:
    - gaitanalysis
    - gaitanalysis.tests
    - gaitanalysistoolkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/csu-hmc/GaitAnalysisToolKit
  license: LICENSE.txt
  summary: 'Various tools for gait analysis used at the Cleveland State\nUniversity Human Motion and Control Lab.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
