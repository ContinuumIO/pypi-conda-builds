package:
  name: redis_simple_queue
  version: "1.0"

source:
  fn: redis_simple_queue-1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_simple_queue/redis_simple_queue-1.0.tar.gz
  md5: 86f42a18e7963008b936218c87ab06c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_simple_queue = redis_simple_queue:main
    #
    # Would create an entry point called redis_simple_queue that calls redis_simple_queue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_simple_queue
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.amix.dk/
  license: BSD License
  summary: 'Python queue implemented on top of Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
