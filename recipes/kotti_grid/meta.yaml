package:
  name: kotti_grid
  version: "0.1a2"

source:
  fn: kotti_grid-0.1a2.zip
  url: https://pypi.python.org/packages/source/k/kotti_grid/kotti_grid-0.1a2.zip
  md5: c09d2076e549ff93347eb95d9be0c30a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_grid = kotti_grid:main
    #
    # Would create an entry point called kotti_grid that calls kotti_grid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti
    - kotti_settings >=0.1b4
    - js.jquery_colorpicker
    - js.gridster

  run:
    - python
    - setuptools
    - kotti
    - kotti_settings >=0.1b4
    - js.jquery_colorpicker
    - js.gridster

test:
  # Python imports
  imports:
    - kotti_grid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/j23d/kotti_grid
  license: BSD License
  summary: 'grid widget for Kotti'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
