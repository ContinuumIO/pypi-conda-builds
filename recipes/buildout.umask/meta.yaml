package:
  name: buildout.umask
  version: "0.5.2"

source:
  fn: buildout.umask-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.umask/buildout.umask-0.5.2.tar.gz
  md5: ce6e3abe04a4f112a7e393194571e377
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.umask = buildout.umask:main
    #
    # Would create an entry point called buildout.umask that calls buildout.umask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - buildout
    - buildout.umask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/buildout.umask
  license: GPL
  summary: 'Use a custom umask when running buildout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
