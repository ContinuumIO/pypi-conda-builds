package:
  name: ascension_kit
  version: "0.0.10"

source:
  fn: ascension_kit-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/a/ascension_kit/ascension_kit-0.0.10.tar.gz
  md5: 9acbe8769a0ffdc7637fa47a6ab709bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ascension_kit = ascension_kit:main
    #
    # Would create an entry point called ascension_kit that calls ascension_kit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.4
    - beautifulsoup4 >=4.3.2
    - prettytable ==0.7.2

  run:
    - python
    - requests >=1.0.4
    - beautifulsoup4 >=4.3.2
    - prettytable ==0.7.2

test:
  # Python imports
  imports:
    - ascension_kit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/ascension_kit
  license: BSD License
  summary: 'A utility to report ascension information from NAO.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
