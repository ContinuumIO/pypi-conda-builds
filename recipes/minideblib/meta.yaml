package:
  name: minideblib
  version: "0.6.21.29"

source:
  fn: minideblib-0.6.21.29.tar.gz
  url: https://pypi.python.org/packages/source/m/minideblib/minideblib-0.6.21.29.tar.gz
  md5: 13b8798a9824c7376506eb3378172a0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minideblib = minideblib:main
    #
    # Would create an entry point called minideblib that calls minideblib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - minideblib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bifh.org/wiki/python-minideblib
  license: GNU General Public License (GPL)
  summary: 'Access to deb files and repositories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
