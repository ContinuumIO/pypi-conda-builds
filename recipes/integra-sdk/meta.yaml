package:
  name: integra-sdk
  version: "0.1.1"

source:
  fn: integra-sdk-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/integra-sdk/integra-sdk-0.1.1.tar.gz
  md5: 1ca1e17e33bb9ae7f4e0a0633eda5284
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - integra-sdk = integra-sdk:main
    #
    # Would create an entry point called integra-sdk that calls integra-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests ==2.5.1
    - lxml ==3.4.2

  run:
    - python
    - requests ==2.5.1
    - lxml ==3.4.2

test:
  # Python imports
  imports:
    - integra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/integra-sdk/
  license: LICENSE.txt
  summary: 'Integra SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
