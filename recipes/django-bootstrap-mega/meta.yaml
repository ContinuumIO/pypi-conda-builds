package:
  name: django-bootstrap-mega
  version: "0.1.3"

source:
  fn: django-bootstrap-mega-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-mega/django-bootstrap-mega-0.1.3.tar.gz
  md5: cc73a284ed2b34fed5bfbf322eba6d0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-mega = django-bootstrap-mega:main
    #
    # Would create an entry point called django-bootstrap-mega that calls django-bootstrap-mega.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chrisspen/django-bootstrap
  license: BSD License
  summary: 'Twitter Bootstrap ready Django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
