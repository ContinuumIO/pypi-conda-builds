package:
  name: filedes
  version: "0.1.1"

source:
  fn: 0.1.1.tar.gz
  url: https://github.com/fmoo/python-filedes/archive/0.1.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - filedes = filedes:main
    #
    # Would create an entry point called filedes that calls filedes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unittest2

  run:
    - python
    - unittest2

test:
  # Python imports
  imports:
    - filedes
    - filedes.socket
    - filedes.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fmoo/python-filedes
  license: ISC License (ISCL)
  summary: 'Work with file descriptors in a more human way'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
