package:
  name: git-deb
  version: "0.3.0"

source:
  fn: git-deb-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/g/git-deb/git-deb-0.3.0.tar.gz
  md5: 76e80ea162f5c33d4fa7e8b5f8ee8730
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-deb = git-deb:main
    #
    # Would create an entry point called git-deb that calls git-deb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - chardet
    - isodate
    - python-debian >=0.1.21-nmu1
    - requests

  run:
    - python
    - chardet
    - isodate
    - python-debian >=0.1.21-nmu1
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/g2p/git-deb
  license: GNU GPL
  summary: 'Bring any Debian package to Git'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
