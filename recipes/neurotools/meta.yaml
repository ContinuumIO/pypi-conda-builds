package:
  name: neurotools
  version: "0.2.0"

source:
  fn: NeuroTools-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/N/NeuroTools/NeuroTools-0.2.0.tar.gz
  md5: 63a6184142b67100ff20196249a69f8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - neurotools = neurotools:main
    #
    # Would create an entry point called neurotools that calls neurotools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - NeuroTools
    - NeuroTools.datastore
    - NeuroTools.optimize
    - NeuroTools.parameters
    - NeuroTools.signals
    - NeuroTools.spike2
    - NeuroTools.spike2.sonpy
    - NeuroTools.tisean
    - NeuroTools.utilities
    - neurotools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://neuralensemble.org/NeuroTools
  license: GNU General Public License (GPL)
  summary: 'NeuroTools is a collection of tools to support all tasks associated with the analysis of neural activity - from neurophysiology to neural simulations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
