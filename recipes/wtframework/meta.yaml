package:
  name: wtframework
  version: "0.4.11"

source:
  fn: wtframework-0.4.11.tar.gz
  url: https://pypi.python.org/packages/source/w/wtframework/wtframework-0.4.11.tar.gz
  md5: 14840cdb20a49693a2b6a888bc435d45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtframework = wtframework:main
    #
    # Would create an entry point called wtframework that calls wtframework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - nose2 >=0.4.7
    - unittest2 >=0.5.1
    - ddt >=0.2.0
    - pyyaml >=3.10
    - selenium >=2.37.2
    - six >=1.5.2

  run:
    - python
    - nose2 >=0.4.7
    - unittest2 >=0.5.1
    - ddt >=0.2.0
    - pyyaml >=3.10
    - selenium >=2.37.2
    - six >=1.5.2

test:
  # Python imports
  imports:
    - wtframework
    - wtframework.wtf
    - wtframework.wtf._devtools_
    - wtframework.wtf._devtools_.filetemplates
    - wtframework.wtf.data
    - wtframework.wtf.testobjects
    - wtframework.wtf.utils
    - wtframework.wtf.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wiredrive/wtframework
  license: LICENSE.txt
  summary: 'WTF - Web Test Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
