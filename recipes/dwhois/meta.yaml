package:
  name: dwhois
  version: "0.11"

source:
  fn: dwhois-0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/dwhois/dwhois-0.11.tar.gz
  md5: f2cb9950c54e9e57ca7f8b068265eaf4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dwhois = dwhois:main
    #
    # Would create an entry point called dwhois that calls dwhois.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1
    - pymongo >=2.6.3
    - chardet >=2.0.1

  run:
    - python
    - requests >=2.2.1
    - pymongo >=2.6.3
    - chardet >=2.0.1

test:
  # Python imports
  imports:
    - dwhois
    - dwhois.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hstern/dwhois
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Distributed WHOIS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
