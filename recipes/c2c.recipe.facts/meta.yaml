package:
  name: c2c.recipe.facts
  version: "1.2"

source:
  fn: c2c.recipe.facts-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/c2c.recipe.facts/c2c.recipe.facts-1.2.tar.gz
  md5: a08a2e4b4796a1f67f4ed36b27c2f47c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2c.recipe.facts = c2c.recipe.facts:main
    #
    # Would create an entry point called c2c.recipe.facts that calls c2c.recipe.facts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - pyyaml

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - pyyaml

test:
  # Python imports
  imports:
    - c2c_recipe_facts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sbrunner/c2c.recipe.facts
  license: BSD License
  summary: 'Collect the puppet facter facts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
