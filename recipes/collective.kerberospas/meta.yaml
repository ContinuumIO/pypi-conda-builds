package:
  name: collective.kerberospas
  version: "1.0"

source:
  fn: collective.KerberosPAS-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.KerberosPAS/collective.KerberosPAS-1.0.tar.gz
  md5: fb5b30974ee20cb2828680b1a724e9a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.kerberospas = collective.kerberospas:main
    #
    # Would create an entry point called collective.kerberospas that calls collective.kerberospas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - kerberos

  run:
    - python
    - setuptools
    - setuptools
    - kerberos

test:
  # Python imports
  imports:
    - collective
    - collective.KerberosPAS

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.KerberosPAS
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Kerberos PAS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
