package:
  name: xstatic-anywikidraw
  version: "0.14.2"

source:
  fn: XStatic-AnyWikiDraw-0.14.2.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-AnyWikiDraw/XStatic-AnyWikiDraw-0.14.2.tar.gz
  md5: 1f7ef23cc09777a6a2c56d622b8bfbd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-anywikidraw = xstatic-anywikidraw:main
    #
    # Would create an entry point called xstatic-anywikidraw that calls xstatic-anywikidraw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.anywikidraw

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/anywikidraw
  license: (same as AnyWikiDraw)
  summary: 'AnyWikiDraw 0.14 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
