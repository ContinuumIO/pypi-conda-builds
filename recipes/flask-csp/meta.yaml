package:
  name: flask-csp
  version: "0.9"

source:
  fn: flask-csp-0.9.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-csp/flask-csp-0.9.tar.gz
  md5: 03857e621db9a150065c3476893bc0f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-csp = flask-csp:main
    #
    # Would create an entry point called flask-csp that calls flask-csp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flask_csp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twaldear/flask-csp
  license: MIT License
  summary: 'Flask Content Security Policy header support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
