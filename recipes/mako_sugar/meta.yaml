package:
  name: mako_sugar
  version: "0.1.0"

source:
  fn: mako_sugar-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mako_sugar/mako_sugar-0.1.0.tar.gz
  md5: 6d3421f19eb36c43f457bed29f77bd34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mako_sugar = mako_sugar:main
    #
    # Would create an entry point called mako_sugar that calls mako_sugar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mako_sugar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marcpare/mako-sugar
  license: MIT
  summary: 'Syntax sugar for Python Mako templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
