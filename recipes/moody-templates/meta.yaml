package:
  name: moody-templates
  version: "0.9.1"

source:
  fn: moody-templates-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/m/moody-templates/moody-templates-0.9.1.tar.gz
  md5: 2a3c4d2fe012534208638186ba5d153a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moody-templates = moody-templates:main
    #
    # Would create an entry point called moody-templates that calls moody-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moody

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/etianen/moody-templates
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
