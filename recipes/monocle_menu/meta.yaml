package:
  name: monocle_menu
  version: "0.1.0.5"

source:
  fn: monocle_menu-0.1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle_menu/monocle_menu-0.1.0.5.tar.gz
  md5: 6c4b8a5b102a87124309aaaed8186f60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle_menu = monocle_menu:main
    #
    # Would create an entry point called monocle_menu that calls monocle_menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monocle_menu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Menu app for django-monocle project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
