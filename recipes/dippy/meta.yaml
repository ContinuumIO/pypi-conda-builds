package:
  name: dippy
  version: "0.1.0"

source:
  fn: dippy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/D/Dippy/dippy-0.1.0.tar.gz
  md5: 97b18ce24e38ec4997a422644a0e0b14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dippy = dippy:main
    #
    # Would create an entry point called dippy that calls dippy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dippy
    - dippy.dependency
    - dippy.env
    - dippy.example
    - dippy.example.api
    - dippy.example.download
    - dippy.example.json
    - dippy.example.render
    - dippy.example.search
    - dippy.library
    - dippy.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dippy/
  license: LICENSE.txt
  summary: 'Dependency Inversion Principle in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
