package:
  name: beetle-preview
  version: "0.1.0"

source:
  fn: Beetle-Preview-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/B/Beetle-Preview/Beetle-Preview-0.1.0.tar.gz
  md5: 70a051f0e4eb39de92e41acb504cf995
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beetle-preview = beetle-preview:main
    #
    # Would create an entry point called beetle-preview that calls beetle-preview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - watchdog

  run:
    - python
    - watchdog

test:
  # Python imports
  imports:
    - beetle_preview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cknv/beetle-preview
  license: MIT License
  summary: 'Local test server plugin for Beetle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
