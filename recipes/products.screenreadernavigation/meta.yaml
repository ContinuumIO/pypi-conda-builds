package:
  name: products.screenreadernavigation
  version: "1.0.2"

source:
  fn: Products.ScreenReaderNavigation-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.ScreenReaderNavigation/Products.ScreenReaderNavigation-1.0.2.tar.gz
  md5: afc1f5801959f979fc3cfe3b7df6dd60
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.screenreadernavigation = products.screenreadernavigation:main
    #
    # Would create an entry point called products.screenreadernavigation that calls products.screenreadernavigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.ScreenReaderNavigation
    - Products.ScreenReaderNavigation.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: GNU General Public License v2 (GPLv2)
  summary: 'Adds header-2 HTML to navigation portlet for accessibility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
