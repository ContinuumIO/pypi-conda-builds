package:
  name: tornado_pyvows
  version: "0.6.0"

source:
  fn: tornado_pyvows-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_pyvows/tornado_pyvows-0.6.0.tar.gz
  md5: b5664ad6771ab49d9bbe3547ae97b622
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_pyvows = tornado_pyvows:main
    #
    # Would create an entry point called tornado_pyvows that calls tornado_pyvows.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyvows
    - tornado
    - pycurl
    - urllib3

  run:
    - python
    - pyvows
    - tornado
    - pycurl
    - urllib3

test:
  # Python imports
  imports:
    - tornado_pyvows

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rafaelcaricio/tornado_pyvows
  license: MIT License
  summary: 'tornado_pyvows are pyvows extensions to tornado web framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
