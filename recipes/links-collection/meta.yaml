package:
  name: links-collection
  version: "0.1.9"

source:
  fn: links-collection-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/l/links-collection/links-collection-0.1.9.tar.gz
  md5: fd93c9c1259f7d97f0d1b81250204f6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - links-collection = links-collection:main
    #
    # Would create an entry point called links-collection that calls links-collection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - sorl-thumbnail
    - uuid
    - django-widget-tweaks
    - south
    - python-dateutil

  run:
    - python
    - django
    - sorl-thumbnail
    - uuid
    - django-widget-tweaks
    - south
    - python-dateutil

test:
  # Python imports
  imports:
    - collection
    - collection.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linkscreative.co.uk/
  license: LICENSE.txt
  summary: 'Collection for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
