package:
  name: banzai-ngs
  version: "0.0.5"

source:
  fn: Banzai-NGS-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/B/Banzai-NGS/Banzai-NGS-0.0.5.tar.gz
  md5: 7b4f151b6e728e32fbc761ef9d9ef6b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - banzai-ngs = banzai-ngs:main
    #
    # Would create an entry point called banzai-ngs that calls banzai-ngs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - Banzai

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mscook/Banzai-MicrobialGenomics-Pipeline
  license: GPLv2
  summary: 'Banzai is a Microbial Genomics Next Generation Sequencing (NGS) Pipeline Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
