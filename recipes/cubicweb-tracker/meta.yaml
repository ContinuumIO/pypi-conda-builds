package:
  name: cubicweb-tracker
  version: "1.16.3"

source:
  fn: cubicweb-tracker-1.16.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-tracker/cubicweb-tracker-1.16.3.tar.gz
  md5: 15c14bdeb4c611dc3f454076562b66ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-tracker = cubicweb-tracker:main
    #
    # Would create an entry point called cubicweb-tracker that calls cubicweb-tracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.4
    - cubicweb-localperms
    - cubicweb-activitystream
    - cubicweb-iprogress

  run:
    - python
    - cubicweb >=3.19.4
    - cubicweb-localperms
    - cubicweb-activitystream
    - cubicweb-iprogress

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-tracker
  license: LGPL
  summary: 'basic tracker with project, version, ticket for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
