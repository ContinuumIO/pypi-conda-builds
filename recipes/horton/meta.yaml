package:
  name: horton
  version: "0.1.1"

source:
  fn: Horton-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Horton/Horton-0.1.1.tar.gz
  md5: aec8fb346aab3db9b315c6f1a65d3ee4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horton = horton:main
    #
    # Would create an entry point called horton that calls horton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx_bootstrap_theme

  run:
    - python
    - sphinx_bootstrap_theme

test:
  # Python imports
  imports:
    - horton
    - horton.render

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: '\nA library of grids and other fine amusements. Contains a Grid and\nGrid-like data structures and optional modules for rendering them with\npygame, creating cellular-automata simulations, games, and such\nthings.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
