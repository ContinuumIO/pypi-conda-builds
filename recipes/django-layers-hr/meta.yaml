package:
  name: django-layers-hr
  version: "0.4"

source:
  fn: django-layers-hr-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-layers-hr/django-layers-hr-0.4.tar.gz
  md5: 1156d01ff50439c1de411d84522a3f28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-layers-hr = django-layers-hr:main
    #
    # Would create an entry point called django-layers-hr that calls django-layers-hr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.7

  run:
    - python
    - django <1.7

test:
  # Python imports
  imports:
    - layers
    - layers.loaders
    - layers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hedleyroos/django-layers
  license: BSD License
  summary: 'Serve different templates and static files for eg. mobi and web. Layers can be stacked to enable resource re-use.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
