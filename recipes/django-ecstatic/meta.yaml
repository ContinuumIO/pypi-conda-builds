package:
  name: django-ecstatic
  version: "0.2.1"

source:
  fn: django-ecstatic-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ecstatic/django-ecstatic-0.2.1.tar.gz
  md5: 00520f8d2fd0ee12a7822171980ed632
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ecstatic = django-ecstatic:main
    #
    # Would create an entry point called django-ecstatic that calls django-ecstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-appconf >=0.5

  run:
    - python
    - django >=1.4
    - django-appconf >=0.5

test:
  # Python imports
  imports:
    - ecstatic
    - ecstatic.management
    - ecstatic.management.commands
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hzdg/django-ecstatic
  license: BSD License
  summary: 'An expansion pack for django.contrib.staticfiles!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
