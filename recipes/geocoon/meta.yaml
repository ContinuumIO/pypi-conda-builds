package:
  name: geocoon
  version: "0.2.0"

source:
  fn: geocoon-0.2.0.tar.bz2
  url: https://pypi.python.org/packages/source/g/geocoon/geocoon-0.2.0.tar.bz2
  md5: 0894211bd34dcfe96c0eaf726781a6dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geocoon = geocoon:main
    #
    # Would create an entry point called geocoon that calls geocoon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - shapely >=1.3
    - pandas >=0.14.0

  run:
    - python
    - shapely >=1.3
    - pandas >=0.14.0

test:
  # Python imports
  imports:
    - geocoon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wrobell.it-zone.org/geocoon/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'GIS data analysis library based on Pandas and Shapely'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
