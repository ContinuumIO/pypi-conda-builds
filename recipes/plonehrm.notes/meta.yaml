package:
  name: plonehrm.notes
  version: "1.1"

source:
  fn: plonehrm.notes-1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plonehrm.notes/plonehrm.notes-1.1.tar.gz
  md5: f37a77a208f9c11f97e7d25d17dfc385
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonehrm.notes = plonehrm.notes:main
    #
    # Would create an entry point called plonehrm.notes that calls plonehrm.notes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

test:
  # Python imports
  imports:
    - plonehrm
    - plonehrm.notes
    - plonehrm.notes.browser
    - plonehrm.notes.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/plonehrm.notes
  license: GPL
  summary: 'A plonehrm extension module to add notes to an employee.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
