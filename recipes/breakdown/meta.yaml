package:
  name: breakdown
  version: "1.0.9"

source:
  fn: breakdown-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/b/breakdown/breakdown-1.0.9.tar.gz
  md5: 27a2d04fb5de2203834f32555efc24b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - breakdown = breakdown:main
    #
    # Would create an entry point called breakdown that calls breakdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.6
    - clevercss

  run:
    - python
    - jinja2 >=2.6
    - clevercss

test:
  # Python imports
  imports:
    - breakdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Lightweight jinja2 template prototyping server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
