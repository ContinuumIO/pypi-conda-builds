package:
  name: django-resumable
  version: "0.1"

source:
  fn: django-resumable-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-resumable/django-resumable-0.1.tar.gz
  md5: d4bafff1adfc6743eac2dcd4071fa9ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-resumable = django-resumable:main
    #
    # Would create an entry point called django-resumable that calls django-resumable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - python-magic

  run:
    - python
    - django >=1.4
    - python-magic

test:
  # Python imports
  imports:
    - resumable
    - resumable.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeanphix/django-resumable
  license: MIT License
  summary: 'Django resumable uploads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
