package:
  name: django-rbac
  version: "0.9"

source:
  fn: django-rbac-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rbac/django-rbac-0.9.tar.gz
  md5: 5ac52ce2c1a2998c0e8fc823a6b4cd54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rbac = django-rbac:main
    #
    # Would create an entry point called django-rbac that calls django-rbac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - example.myapp
    - rbac
    - rbac.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nomadblue.com/projects/django-rbac/
  license: BSD License
  summary: 'Role-based Access Control (RBAC) implementation for management of permissions in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
