package:
  name: iservices.rssdocument
  version: "0.5.0"

source:
  fn: iservices.rssdocument-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/i/iservices.rssdocument/iservices.rssdocument-0.5.0.tar.gz
  md5: 8e5d96d0bb8df314f8bbc8e3c6e378df
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iservices.rssdocument = iservices.rssdocument:main
    #
    # Would create an entry point called iservices.rssdocument that calls iservices.rssdocument.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - iservices
    - iservices.rssdocument
    - iservices.rssdocument.browser
    - iservices.rssdocument.content
    - iservices.rssdocument.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/iservices.rssdocument
  license: LGPL
  summary: 'A dynamic RSS content-type for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
