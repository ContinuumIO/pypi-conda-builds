package:
  name: django-blarg
  version: "0.1.4"

source:
  fn: django-blarg-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-blarg/django-blarg-0.1.4.tar.gz
  md5: 002a901e356b3bce4070ae1ce8e44c9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-blarg = django-blarg:main
    #
    # Would create an entry point called django-blarg that calls django-blarg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pydanny/django-blarg
  license: BSD License
  summary: 'Django 404 and 500 pages the blarg way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
