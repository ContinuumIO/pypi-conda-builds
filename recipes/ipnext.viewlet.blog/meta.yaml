package:
  name: ipnext.viewlet.blog
  version: "0.3.4"

source:
  fn: ipnext.viewlet.blog-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/i/ipnext.viewlet.blog/ipnext.viewlet.blog-0.3.4.tar.gz
  md5: e8e4ead97237ac3e5560571cab1ad80b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipnext.viewlet.blog = ipnext.viewlet.blog:main
    #
    # Would create an entry point called ipnext.viewlet.blog that calls ipnext.viewlet.blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - ipnext
    - ipnext.viewlet
    - ipnext.viewlet.blog
    - ipnext.viewlet.blog.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Display a relevant post for this page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
