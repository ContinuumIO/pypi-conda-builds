package:
  name: collective.js.pikachoose
  version: "1.1"

source:
  fn: collective.js.pikachoose-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.pikachoose/collective.js.pikachoose-1.1.zip
  md5: c24f3b14ae80072538f64ccdcdce3311
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.pikachoose = collective.js.pikachoose:main
    #
    # Would create an entry point called collective.js.pikachoose that calls collective.js.pikachoose.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.jcarousel

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.jcarousel

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.pikachoose
    - collective.js.pikachoose.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.pikachoose
  license: GNU General Public License (GPL)
  summary: 'JQuery pickachoose plugin as browser resource'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
