package:
  name: discsim
  version: "1.0.0"

source:
  fn: discsim-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/discsim/discsim-1.0.0.tar.gz
  md5: 7caace9ce911612d0f854b36d3269010
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - discsim = discsim:main
    #
    # Would create an entry point called discsim that calls discsim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - discsim

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/discsim
  license: GNU General Public License v3 (GPLv3)
  summary: 'Efficient coalescent simulation in continuous space'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
