package:
  name: django-vagrantize
  version: "0.1.0"

source:
  fn: django-vagrantize-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vagrantize/django-vagrantize-0.1.0.tar.gz
  md5: 6296cb0598ab1485b91146456d48d09b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vagrantize = django-vagrantize:main
    #
    # Would create an entry point called django-vagrantize that calls django-vagrantize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - vagrantize
    - vagrantize.management
    - vagrantize.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Run Django dev server from VM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
