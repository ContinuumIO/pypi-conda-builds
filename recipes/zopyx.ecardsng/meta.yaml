package:
  name: zopyx.ecardsng
  version: "0.3.3"

source:
  fn: zopyx.ecardsng-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zopyx.ecardsng/zopyx.ecardsng-0.3.3.tar.gz
  md5: b2879a902364bea817572627172ee42b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.ecardsng = zopyx.ecardsng:main
    #
    # Would create an entry point called zopyx.ecardsng that calls zopyx.ecardsng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - uuid

  run:
    - python
    - setuptools
    - setuptools
    - uuid

test:
  # Python imports
  imports:
    - zopyx
    - zopyx.ecardsng
    - zopyx.ecardsng.browser
    - zopyx.ecardsng.content
    - zopyx.ecardsng.portlets
    - zopyx.ecardsng.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/zopyx.ecardsng/trunk
  license: Zope Public License
  summary: 'An ECard implementation for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
