package:
  name: js.typeahead
  version: "0.9.2"

source:
  fn: js.typeahead-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/j/js.typeahead/js.typeahead-0.9.2.tar.gz
  md5: f1b23c65d48facdaa4e7b3ed8e15bb6a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.typeahead = js.typeahead:main
    #
    # Would create an entry point called js.typeahead that calls js.typeahead.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic >=0.16
    - js.jquery >=1.9

  run:
    - python
    - setuptools
    - fanstatic >=0.16
    - js.jquery >=1.9

test:
  # Python imports
  imports:
    - js
    - js.typeahead

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.merchise.org
  license: GPL
  summary: u"Fanstatic Package of Twitter's Typeadhead.js"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
