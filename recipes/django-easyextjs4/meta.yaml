package:
  name: django-easyextjs4
  version: "1.1"

source:
  fn: django-easyextjs4-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easyextjs4/django-easyextjs4-1.1.tar.gz
  md5: 506f419c140c7fe9ee00bc2eed8a6b99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easyextjs4 = django-easyextjs4:main
    #
    # Would create an entry point called django-easyextjs4 that calls django-easyextjs4.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - EasyExtJS4

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TofPlay/django-easyextjs4
  license: UNKNOWN
  summary: 'Django extension for ExtJS 4 and Sencha Touch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
