package:
  name: gittle
  version: "0.5.0"

source:
  fn: gittle-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gittle/gittle-0.5.0.tar.gz
  md5: 35c4d3f608c81b6f7cc91b8bfc1221ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gittle = gittle:main
    #
    # Would create an entry point called gittle that calls gittle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko ==1.10.0
    - pycrypto ==2.6
    - dulwich ==0.9.7
    - funky ==0.0.2

  run:
    - python
    - paramiko ==1.10.0
    - pycrypto ==2.6
    - dulwich ==0.9.7
    - funky ==0.0.2

test:
  # Python imports
  imports:
    - gittle
    - gittle.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FriendCode/gittle
  license: MIT
  summary: 'A high level pure python git implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
