package:
  name: googlesitemap.common
  version: "1.3"

source:
  fn: googlesitemap.common-1.3.zip
  url: https://pypi.python.org/packages/source/g/googlesitemap.common/googlesitemap.common-1.3.zip
  md5: 423ca297679a088405902efd4d30c614
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googlesitemap.common = googlesitemap.common:main
    #
    # Would create an entry point called googlesitemap.common that calls googlesitemap.common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - googlesitemap
    - googlesitemap.common
    - googlesitemap.common.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/googlesitemap.common
  license: GPL
  summary: 'Google Sitemap common support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
