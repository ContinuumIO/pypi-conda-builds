package:
  name: gdscad
  version: "0.4.5"

source:
  fn: gdsCAD-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gdsCAD/gdsCAD-0.4.5.tar.gz
  md5: 9e9abbc577997270b4000704ea61727a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gdscad = gdscad:main
    #
    # Would create an entry point called gdscad that calls gdscad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gdsCAD
    - gdscad

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hohlraum/gdsCAD
  license: GNU General Public License v3 (GPLv3)
  summary: 'A simple Python package for creating or reading GDSII layout files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
