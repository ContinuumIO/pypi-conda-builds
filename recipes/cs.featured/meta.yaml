package:
  name: cs.featured
  version: "1.1.1"

source:
  fn: cs.featured-1.1.1.zip
  url: https://pypi.python.org/packages/source/c/cs.featured/cs.featured-1.1.1.zip
  md5: 691dcbac740c0525739c34b00db90dcf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.featured = cs.featured:main
    #
    # Would create an entry point called cs.featured that calls cs.featured.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - cs
    - cs.featured
    - cs.featured.content
    - cs.featured.portlets
    - cs.featured.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codesyntax/cs.featured
  license: GPL
  summary: 'Content-type to reference external things: title+image+url'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
