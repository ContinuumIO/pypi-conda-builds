package:
  name: django-licenses
  version: "0.2.5"

source:
  fn: django-licenses-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-licenses/django-licenses-0.2.5.tar.gz
  md5: f402b53e9fe21de393acb273d429bc4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-licenses = django-licenses:main
    #
    # Would create an entry point called django-licenses that calls django-licenses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - licenses
    - licenses.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jezdez/django-licenses/
  license: BSD License
  summary: 'A Django application that provides the ability to publish content under different licenses.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
