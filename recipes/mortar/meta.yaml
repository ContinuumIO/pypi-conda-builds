package:
  name: mortar
  version: "0.1.2"

source:
  fn: mortar-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mortar/mortar-0.1.2.tar.gz
  md5: 517d7c7014d2b581f321d500eed4305e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mortar = mortar:main
    #
    # Would create an entry point called mortar that calls mortar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - lxml
    - python-dateutil

  run:
    - python
    - zope.interface
    - zope.component
    - lxml
    - python-dateutil

test:
  # Python imports
  imports:
    - mortar
    - mortar.storage
    - mortar.storage.tests
    - mortar.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.mortar.org
  license: MIT License
  summary: 'An implementation independent content environment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
