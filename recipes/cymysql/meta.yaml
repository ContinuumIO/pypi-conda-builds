package:
  name: cymysql
  version: "0.8.1"

source:
  fn: cymysql-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cymysql/cymysql-0.8.1.tar.gz
  md5: 3cd6b7ea61471a5ac22bacd843aaa8be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cymysql = cymysql:main
    #
    # Would create an entry point called cymysql that calls cymysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cymysql
    - cymysql.constants
    - cymysql.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nakagami/CyMySQL/
  license: MIT License
  summary: 'Python MySQL Driver using Cython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
