package:
  name: mojangauth
  version: "0.0.4"

source:
  fn: MojangAuth-0.0.4.zip
  url: https://pypi.python.org/packages/source/M/MojangAuth/MojangAuth-0.0.4.zip
  md5: db568fc9651b47906ca92fcc20b6c8be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mojangauth = mojangauth:main
    #
    # Would create an entry point called mojangauth that calls mojangauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mojang
    - mojang.auth
    - mojangauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gdude2002/MojangAuth
  license: Artistic License 2.0
  summary: 'Provides an API wrapper for the Mojang authentication API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
