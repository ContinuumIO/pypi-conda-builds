package:
  name: django-github-log
  version: "0.1.9"

source:
  fn: django-github-log-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-github-log/django-github-log-0.1.9.tar.gz
  md5: 0076ca9b6b6d3f4481d05a6c1283fd54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-github-log = django-github-log:main
    #
    # Would create an entry point called django-github-log that calls django-github-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - celery
    - pygithub3

  run:
    - python
    - django
    - celery
    - pygithub3

test:
  # Python imports
  imports:
    - github_log

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Django log handler to create issue on github automatically'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
