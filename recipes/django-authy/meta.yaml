package:
  name: django-authy
  version: "0.1.0"

source:
  fn: django-authy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-authy/django-authy-0.1.0.tar.gz
  md5: 04b8b7931b21c318ed079a498bcc7a60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-authy = django-authy:main
    #
    # Would create an entry point called django-authy that calls django-authy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - authy
    - django-phonenumber-field

  run:
    - python
    - authy
    - django-phonenumber-field

test:
  # Python imports
  imports:
    - dj_authy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rosscdh/django-authy
  license: MIT
  summary: 'A Django app for integrating with authy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
