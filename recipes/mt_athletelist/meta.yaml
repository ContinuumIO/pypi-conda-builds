package:
  name: mt_athletelist
  version: "1.1.0"

source:
  fn: mt_AthleteList-1.1.0.zip
  url: https://pypi.python.org/packages/source/m/mt_AthleteList/mt_AthleteList-1.1.0.zip
  md5: 72a843a4c10d1c32fc5a342c8167aa9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mt_athletelist = mt_athletelist:main
    #
    # Would create an entry point called mt_athletelist that calls mt_athletelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mt_athletelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.163.com
  license: UNKNOWN
  summary: 'Athlete class for mt'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
