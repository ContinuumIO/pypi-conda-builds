package:
  name: zope.outputchecker
  version: "1.0.0"

source:
  fn: zope.outputchecker-1.0.0.zip
  url: https://pypi.python.org/packages/source/z/zope.outputchecker/zope.outputchecker-1.0.0.zip
  md5: 271c31c4de2d040e3dd9e0536bad52fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.outputchecker = zope.outputchecker:main
    #
    # Would create an entry point called zope.outputchecker that calls zope.outputchecker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - lxml
    - setuptools
    - zope.testing

  run:
    - python
    - lxml
    - setuptools
    - zope.testing

test:
  # Python imports
  imports:
    - zope
    - zope.outputchecker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.outputchecker
  license: Zope Public License
  summary: 'A set of output checkers for doctests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
