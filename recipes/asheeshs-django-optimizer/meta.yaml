package:
  name: asheeshs-django-optimizer
  version: "2.0"

source:
  fn: asheeshs-django-optimizer-2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/asheeshs-django-optimizer/asheeshs-django-optimizer-2.0.tar.gz
  md5: 0e9320cdeda44142b2730486fdc2aaff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asheeshs-django-optimizer = asheeshs-django-optimizer:main
    #
    # Would create an entry point called asheeshs-django-optimizer that calls asheeshs-django-optimizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asheeshs_django_optimizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'A pretend Django optimizer for use in a web security tutorial'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
