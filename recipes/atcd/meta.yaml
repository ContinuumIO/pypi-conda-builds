package:
  name: atcd
  version: "0.1.1"

source:
  fn: atcd-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/atcd/atcd-0.1.1.tar.gz
  md5: 9761a63b59e30b228befcaca8f779fc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atcd = atcd:main
    #
    # Would create an entry point called atcd that calls atcd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyroute2 ==0.3.3
    - pyotp ==1.4.1
    - sparts ==0.7.1
    - atc_thrift

  run:
    - python
    - pyroute2 ==0.3.3
    - pyotp ==1.4.1
    - sparts ==0.7.1
    - atc_thrift

test:
  # Python imports
  imports:
    - atcd
    - atcd.backends
    - atcd.scripts
    - atcd.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/facebook/augmented-traffic-control
  license: atcrashes
  summary: 'ATC Daemon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
