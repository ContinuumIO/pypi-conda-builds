package:
  name: cs.plt.contentnegotiator
  version: "1.3"

source:
  fn: cs.plt.contentnegotiator-1.3.zip
  url: https://pypi.python.org/packages/source/c/cs.plt.contentnegotiator/cs.plt.contentnegotiator-1.3.zip
  md5: 2645fbfb2f918bb849bcc1e25ce8b982
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.plt.contentnegotiator = cs.plt.contentnegotiator:main
    #
    # Would create an entry point called cs.plt.contentnegotiator that calls cs.plt.contentnegotiator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - cs
    - cs.plt
    - cs.plt.contentnegotiator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.app.example
  license: GPL
  summary: "A patch to get PloneLanguageTool's contentnegotiator work in virtual hosting environment"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
