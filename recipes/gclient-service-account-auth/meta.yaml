package:
  name: gclient-service-account-auth
  version: "0.2.1"

source:
  fn: gclient-service-account-auth-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gclient-service-account-auth/gclient-service-account-auth-0.2.1.tar.gz
  md5: 899d59c350582e3cf53dbea5463912e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gclient-service-account-auth = gclient-service-account-auth:main
    #
    # Would create an entry point called gclient-service-account-auth that calls gclient-service-account-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.9
    - google-api-python-client >=1.2
    - pyopenssl >=0.14

  run:
    - python
    - httplib2 >=0.9
    - google-api-python-client >=1.2
    - pyopenssl >=0.14

test:
  # Python imports
  imports:
    - service_account_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ambitioninc/gclient-service-account-auth
  license: MIT License
  summary: "Easily create an authorized service-object for interacting with google's client APIs, server to server."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
