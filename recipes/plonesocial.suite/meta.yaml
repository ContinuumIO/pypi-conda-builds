package:
  name: plonesocial.suite
  version: "0.5.3"

source:
  fn: plonesocial.suite-0.5.3.zip
  url: https://pypi.python.org/packages/source/p/plonesocial.suite/plonesocial.suite-0.5.3.zip
  md5: 965fa37125bd74f588f598f959dc34b5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonesocial.suite = plonesocial.suite:main
    #
    # Would create an entry point called plonesocial.suite that calls plonesocial.suite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - plonesocial.microblog
    - plonesocial.activitystream
    - plonesocial.network
    - plonesocial.theme
    - loremipsum
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - plonesocial.microblog
    - plonesocial.activitystream
    - plonesocial.network
    - plonesocial.theme
    - loremipsum
    - plone.api

test:
  # Python imports
  imports:
    - plonesocial
    - plonesocial.suite
    - plonesocial.suite.Extensions
    - plonesocial.suite.browser
    - plonesocial.suite.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cosent/plonesocial.suite
  license: gpl
  summary: 'Pre-integrated social business suite for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
