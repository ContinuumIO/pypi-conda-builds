package:
  name: datastore.objects
  version: "0.2.3"

source:
  fn: datastore.objects-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/datastore.objects/datastore.objects-0.2.3.tar.gz
  md5: bac25932a1e6ce3069a7add2e29b6c35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datastore.objects = datastore.objects:main
    #
    # Would create an entry point called datastore.objects that calls datastore.objects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datastore >=0.3.4

  run:
    - python
    - datastore >=0.3.4

test:
  # Python imports
  imports:
    - datastore
    - datastore.core
    - datastore.core.test
    - datastore.core.util
    - datastore.objects
    - datastore.objects.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datastore/datastore.objects
  license: MIT License
  summary: 'object mapper for datastore'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
