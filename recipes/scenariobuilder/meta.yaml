package:
  name: scenariobuilder
  version: "0.1.16"

source:
  fn: scenariobuilder-0.1.16.tar.gz
  url: https://pypi.python.org/packages/source/s/scenariobuilder/scenariobuilder-0.1.16.tar.gz
  md5: 562f18c0831a98ed726eb3e3cc354561
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - scenariobuilder = scenariobuilder:main
    #
    # Would create an entry point called scenariobuilder that calls scenariobuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-novaclient
    - python-neutronclient
    - pyyaml
    - jinja2

  run:
    - python
    - python-novaclient
    - python-neutronclient
    - pyyaml
    - jinja2

test:
  # Python imports
  imports:
    - scenariobuilder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CiscoSystems/scenariobuilder
  license: Apache
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
