package:
  name: django-rms
  version: "0.0.0"

source:
  fn: django-rms-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rms/django-rms-0.0.0.tar.gz
  md5: b4d9b1a5ff2393240d70ba0f826dca1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rms = django-rms:main
    #
    # Would create an entry point called django-rms that calls django-rms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - rms
    - rms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/psaavedra/django-rms
  license: MIT License
  summary: 'Django Rule Management System app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
