package:
  name: msdas
  version: "0.0.1"

source:
  fn: msdas-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/msdas/msdas-0.0.1.tar.gz
  md5: 43dfca8368e35c7e1ac21b3693d8d0b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msdas = msdas:main
    #
    # Would create an entry point called msdas that calls msdas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - easydev >=0.6.12
    - numpy
    - matplotlib
    - scikit-learn
    - bioservices >=1.2.3
    - pandas >=0.13
    - cellnopt.core >=0.5.2

  run:
    - python
    - easydev >=0.6.12
    - numpy
    - matplotlib
    - scikit-learn
    - bioservices >=1.2.3
    - pandas >=0.13
    - cellnopt.core >=0.5.2

test:
  # Python imports
  imports:
    - msdas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ['http://pythonhosted.org/msdas/']
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Mass Spectrometry analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
