package:
  name: gxml
  version: "0.5"

source:
  fn: gxml-0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gxml/gxml-0.5.tar.gz
  md5: f18d9aeeefad1638993db847ceeb0be9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gxml = gxml:main
    #
    # Would create an entry point called gxml that calls gxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.xkenneth.com/gxml
  license: UNKNOWN
  summary: 'A Good XML Abstraction. Provides a common interface to popular XML libaries. Currently supports elementree and minidom. Tutorials are available at www.xkenneth.com/gxml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
