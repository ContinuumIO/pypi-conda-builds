package:
  name: ipuz
  version: "0.1.1"

source:
  fn: ipuz-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ipuz/ipuz-0.1.1.tar.gz
  md5: 5bca8f12d6247a6a30df076d99c5f7a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipuz = ipuz:main
    #
    # Would create an entry point called ipuz that calls ipuz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.6.1

  run:
    - python
    - six ==1.6.1

test:
  # Python imports
  imports:
    - ipuz
    - ipuz.puzzlekinds
    - ipuz.structures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/svisser/ipuz
  license: MIT License
  summary: 'Python library for reading and writing ipuz puzzle files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
