package:
  name: django-representations
  version: "0.1"

source:
  fn: django-representations-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-representations/django-representations-0.1.tar.gz
  md5: 0796cabbf65126c57c80ef0c06290209
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-representations = django-representations:main
    #
    # Would create an entry point called django-representations that calls django-representations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - representations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-representations/
  license: UNKNOWN
  summary: 'Django templatetag for representing models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
