package:
  name: django-gevent-websocket
  version: "0.1.0"

source:
  fn: django-gevent-websocket-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gevent-websocket/django-gevent-websocket-0.1.0.tar.gz
  md5: 0fa4d80a42606742982f88fdc5b449f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gevent-websocket = django-gevent-websocket:main
    #
    # Would create an entry point called django-gevent-websocket that calls django-gevent-websocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent
    - gevent-websocket
    - django

  run:
    - python
    - gevent
    - gevent-websocket
    - django

test:
  # Python imports
  imports:
    - django_websocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-gevent-websocket/
  license: BSD License
  summary: 'Easy WebSocket server for django using gevent-websocket'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
