package:
  name: python-yubikey
  version: "0.1.0"

source:
  fn: python-yubikey-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-yubikey/python-yubikey-0.1.0.tar.gz
  md5: ad3874bfa9c9bae0e02c76a9676e94e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-yubikey = python-yubikey:main
    #
    # Would create an entry point called python-yubikey that calls python-yubikey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.2.3

  run:
    - python
    - requests ==1.2.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fmartingr/python-yubikey
  license: GPLv2
  summary: 'Simple Yubico API Wrappers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
