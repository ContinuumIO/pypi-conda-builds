package:
  name: sortedcollection
  version: "0.1.11"

source:
  fn: sortedcollection-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/s/sortedcollection/sortedcollection-0.1.11.tar.gz
  md5: 6061d40bacde6d364f439245df49d9cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sortedcollection = sortedcollection:main
    #
    # Would create an entry point called sortedcollection that calls sortedcollection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest

  run:
    - python
    - pytest

test:
  # Python imports
  imports:
    - sortedcollection

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: 'SortedCollection class that abstracts bisect extended from http://code.activestate.com/recipes/577197-sortedcollection/.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
