package:
  name: logstash-index-cleaner
  version: "0.2.4"

source:
  fn: logstash-index-cleaner-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/l/logstash-index-cleaner/logstash-index-cleaner-0.2.4.tar.gz
  md5: 857351ee7400cdd914076c39b90c7aa9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logstash-index-cleaner = logstash-index-cleaner:main
    #
    # Would create an entry point called logstash-index-cleaner that calls logstash-index-cleaner.main()

    - logstash-index-cleaner = logstash_index_cleaner:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyes >=0.19.1
    - argparse

  run:
    - python
    - pyes >=0.19.1
    - argparse

test:
  # Python imports
  # imports:

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - logstash-index-cleaner --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yipit/logstash-index-cleaner
  license: UNKNOWN
  summary: 'Delete old logstash indices from Elasticsearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
