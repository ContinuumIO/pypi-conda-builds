package:
  name: onetimepass
  version: "0.3.0"

source:
  fn: onetimepass-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/o/onetimepass/onetimepass-0.3.0.tar.gz
  md5: 43253199a45bf2fcd1feb14813850964
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onetimepass = onetimepass:main
    #
    # Would create an entry point called onetimepass that calls onetimepass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - onetimepass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tadeck/onetimepass/
  license: MIT License
  summary: 'Module for generating and validating HOTP and TOTP tokens'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
