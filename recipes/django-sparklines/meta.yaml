package:
  name: django-sparklines
  version: "0.1"

source:
  fn: django-sparklines-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sparklines/django-sparklines-0.1.tar.gz
  md5: 8fe8f9c41e060e0ec2e923e0c0601602
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sparklines = django-sparklines:main
    #
    # Would create an entry point called django-sparklines that calls django-sparklines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sparklines
    - sparklines.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Simple tag abstraction for jquery sparklines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
