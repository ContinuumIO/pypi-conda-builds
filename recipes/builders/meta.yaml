package:
  name: builders
  version: "1.2.8"

source:
  fn: builders-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/b/builders/builders-1.2.8.tar.gz
  md5: b0d5fcc85bcc14c5e2b16adb7c27a5ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - builders = builders:main
    #
    # Would create an entry point called builders that calls builders.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - builders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yandex-qatools/builders
  license: Apache Software License
  summary: 'Lightweight test data generation framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
