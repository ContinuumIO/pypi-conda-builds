package:
  name: uconf
  version: "0.3.8"

source:
  fn: uconf-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/u/uconf/uconf-0.3.8.tar.gz
  md5: 60906e6571f5ce82f4d6fd9411c047fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uconf = uconf:main
    #
    # Would create an entry point called uconf that calls uconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fslib >=0.2.0
    - tdparser
    - confutils >=0.3.6

  run:
    - python
    - fslib >=0.2.0
    - tdparser
    - confutils >=0.3.6

test:
  # Python imports
  imports:
    - uconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rbarrois/uconf/
  license: BSD License
  summary: 'UConf, a smart tool for managing config files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
