package:
  name: bundle-celery
  version: "2.5"

source:
  fn: bundle-celery-2.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bundle-celery/bundle-celery-2.5.tar.gz
  md5: 5df4ecbcd208155d84d05d855bb2552a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bundle-celery = bundle-celery:main
    #
    # Would create an entry point called bundle-celery that calls bundle-celery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=2.5,<3.0
    - django-celery >=2.5,<3.0
    - flask-celery >=2.5,<3.0
    - django
    - setproctitle
    - celerymon
    - cyme
    - kombu-sqlalchemy
    - django-kombu

  run:
    - python
    - celery >=2.5,<3.0
    - django-celery >=2.5,<3.0
    - flask-celery >=2.5,<3.0
    - django
    - setproctitle
    - celerymon
    - cyme
    - kombu-sqlalchemy
    - django-kombu

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://celeryproject.org
  license: BSD
  summary: 'Bundle that installs Celery related modules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
