package:
  name: django-data-schema
  version: "0.8.1"

source:
  fn: django-data-schema-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-data-schema/django-data-schema-0.8.1.tar.gz
  md5: 78679473909b722022b8db88fe3ddda0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-data-schema = django-data-schema:main
    #
    # Would create an entry point called django-data-schema that calls django-data-schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-manager-utils >=0.8.2
    - fleming >=0.4
    - python-dateutil >=2.2

  run:
    - python
    - django >=1.7
    - django-manager-utils >=0.8.2
    - fleming >=0.4
    - python-dateutil >=2.2

test:
  # Python imports
  imports:
    - data_schema
    - data_schema.migrations
    - data_schema.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-data-schema
  license: BSD License
  summary: 'Schemas over dictionaries and models in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
