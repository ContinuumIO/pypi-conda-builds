package:
  name: geminimotordrive
  version: "0.1.1"

source:
  fn: GeminiMotorDrive-0.1.1.zip
  url: https://pypi.python.org/packages/source/G/GeminiMotorDrive/GeminiMotorDrive-0.1.1.zip
  md5: 7e249172d50a21b4d89462f53d17a1bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geminimotordrive = geminimotordrive:main
    #
    # Would create an entry point called geminimotordrive that calls geminimotordrive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - geminimotordrive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frejanordsiek/GeminiMotorDrive
  license: BSD License
  summary: 'Utilities to control Parker Hannifin Gemini stepper/servo motor drives.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
