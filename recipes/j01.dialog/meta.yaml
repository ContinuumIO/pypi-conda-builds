package:
  name: j01.dialog
  version: "2.0.0"

source:
  fn: j01.dialog-2.0.0.zip
  url: https://pypi.python.org/packages/source/j/j01.dialog/j01.dialog-2.0.0.zip
  md5: 6fdfc13c2c47869a66ae63af4ebb6608
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.dialog = j01.dialog:main
    #
    # Would create an entry point called j01.dialog that calls j01.dialog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - j01.jsonrpc
    - z3c.jsonrpc
    - z3c.template
    - zope.interface
    - zope.publisher

  run:
    - python
    - setuptools
    - j01.jsonrpc
    - z3c.jsonrpc
    - z3c.template
    - zope.interface
    - zope.publisher

test:
  # Python imports
  imports:
    - j01
    - j01.dialog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.dialog
  license: Zope Public License
  summary: 'Dialog box based on JQuery for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
