package:
  name: jackfrost
  version: "0.5.4"

source:
  fn: jackfrost-0.5.4.zip
  url: https://pypi.python.org/packages/source/j/jackfrost/jackfrost-0.5.4.zip
  md5: a91edfa5a6ec232e4ec5f7ed622c5809
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jackfrost = jackfrost:main
    #
    # Would create an entry point called jackfrost that calls jackfrost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jackfrost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Autocomplete feature for django. Allows you to serve a channel as a datasource to select an option from a large amounth without need to hit the whole DB set.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
