package:
  name: django-crispy-bootstrap
  version: "0.1.1.1"

source:
  fn: django-crispy-bootstrap-0.1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crispy-bootstrap/django-crispy-bootstrap-0.1.1.1.tar.gz
  md5: 22df52acf5dc80ecc47e4788f95bb96f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crispy-bootstrap = django-crispy-bootstrap:main
    #
    # Would create an entry point called django-crispy-bootstrap that calls django-crispy-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - crispy_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dimka665/django-crispy-bootstrap
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
