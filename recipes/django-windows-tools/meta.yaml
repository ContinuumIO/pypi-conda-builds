package:
  name: django-windows-tools
  version: "0.1.1"

source:
  fn: django-windows-tools-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-windows-tools/django-windows-tools-0.1.1.zip
  md5: 687c448ac1016df1ba5712a915fec1b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-windows-tools = django-windows-tools:main
    #
    # Would create an entry point called django-windows-tools that calls django-windows-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_windows_tools
    - django_windows_tools.management
    - django_windows_tools.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/antoinemartin/django-windows-tools
  license: BSD License
  summary: 'Helper Management commands to host Django applications on Windows Servers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
