package:
  name: collective.interfaces
  version: "1.1.2"

source:
  fn: collective.interfaces-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.interfaces/collective.interfaces-1.1.2.tar.gz
  md5: b720160ad2368817f2e3ba6b4675c7a7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.interfaces = collective.interfaces:main
    #
    # Would create an entry point called collective.interfaces that calls collective.interfaces.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.interfaces
  license: GPL
  summary: "Adds interfaces tab to Plone's content where Manager's can manage marker interfaces."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
