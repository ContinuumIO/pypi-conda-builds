package:
  name: python-libmemcached
  version: "0.40dev"

source:
  fn: python-libmemcached-0.40dev.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libmemcached/python-libmemcached-0.40dev.tar.gz
  md5: e07c6927ca3312ab0e2e7ee21e90edc4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libmemcached = python-libmemcached:main
    #
    # Would create an entry point called python-libmemcached that calls python-libmemcached.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'python memcached client wrapped on libmemcached'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
