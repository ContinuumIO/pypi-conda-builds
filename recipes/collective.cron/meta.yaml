package:
  name: collective.cron
  version: "2.7"

source:
  fn: collective.cron-2.7.zip
  url: https://pypi.python.org/packages/source/c/collective.cron/collective.cron-2.7.zip
  md5: 8b43dfe3fe4585e51baf66c74bf9c3bf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cron = collective.cron:main
    #
    # Would create an entry point called collective.cron that calls collective.cron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.autopermission
    - simplejson
    - croniter
    - plone.app.async >1.3
    - pytz
    - ordereddict
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - collective.autopermission
    - simplejson
    - croniter
    - plone.app.async >1.3
    - pytz
    - ordereddict
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - collective
    - collective.cron
    - collective.cron.Extensions
    - collective.cron.adapters
    - collective.cron.browser
    - collective.cron.tests
    - collective.cron.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.cron
  license: GPL
  summary: 'Product that enable cron like jobs for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
