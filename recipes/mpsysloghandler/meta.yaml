package:
  name: mpsysloghandler
  version: "0.1.2"

source:
  fn: mpSyslogHandler-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mpSyslogHandler/mpSyslogHandler-0.1.2.tar.gz
  md5: a9e15e00018f2b1bc8859bee624a2aa4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpsysloghandler = mpsysloghandler:main
    #
    # Would create an entry point called mpsysloghandler that calls mpsysloghandler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mpSyslogHandler
    - mpsysloghandler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kopertop/MultiProcSysLogHandler
  license: MIT License
  summary: 'MultiProcess-safe SysLog Handler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
