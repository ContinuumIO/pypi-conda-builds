package:
  name: supervisor-wildcards
  version: "0.1.3"

source:
  fn: supervisor-wildcards-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/s/supervisor-wildcards/supervisor-wildcards-0.1.3.tar.gz
  md5: e9b293072fe69a9a977857fe0a5960e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supervisor-wildcards = supervisor-wildcards:main
    #
    # Would create an entry point called supervisor-wildcards that calls supervisor-wildcards.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - supervisorwildcards

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aleszoulek/supervisor-wildcards
  license: BSD License
  summary: 'Implemenents start/stop/restart commands with both parallel and wildcard support for Supervisor.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
