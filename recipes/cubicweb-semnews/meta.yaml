package:
  name: cubicweb-semnews
  version: "0.2.0"

source:
  fn: cubicweb-semnews-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-semnews/cubicweb-semnews-0.2.0.tar.gz
  md5: da4030b45f98087cd411f2de564862fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-semnews = cubicweb-semnews:main
    #
    # Would create an entry point called cubicweb-semnews that calls cubicweb-semnews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-orbui
    - cubicweb >=3.16.0
    - numpy
    - python-feedparser
    - cubicweb-card
    - nerdy
    - cubicweb-leaflet
    - python-sparqlwrapper

  run:
    - python
    - cubicweb-orbui
    - cubicweb >=3.16.0
    - numpy
    - python-feedparser
    - cubicweb-card
    - nerdy
    - cubicweb-leaflet
    - python-sparqlwrapper

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-semnews
  license: LGPL
  summary: 'Cube for news storage and analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
