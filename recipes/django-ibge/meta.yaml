package:
  name: django-ibge
  version: "0.2"

source:
  fn: django-ibge-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ibge/django-ibge-0.2.tar.gz
  md5: 8d910771d9d2ece41b5f125b0076f65c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ibge = django-ibge:main
    #
    # Would create an entry point called django-ibge that calls django-ibge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ibge
    - ibge.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/semente/django-ibge/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Django application to provide information from Brazilian Institute of Geography and Statistics (IBGE).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
