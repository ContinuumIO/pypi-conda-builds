package:
  name: collective.tinymceportlets
  version: "1.0a2"

source:
  fn: collective.tinymceportlets-1.0a2.zip
  url: https://pypi.python.org/packages/source/c/collective.tinymceportlets/collective.tinymceportlets-1.0a2.zip
  md5: e4e5d54e7e3c1fe07aa9a0923d0a7bbd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.tinymceportlets = collective.tinymceportlets:main
    #
    # Would create an entry point called collective.tinymceportlets that calls collective.tinymceportlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.transformchain
    - plone.formwidget.contenttree
    - plone.app.z3cform
    - lxml
    - repoze.xmliter

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.transformchain
    - plone.formwidget.contenttree
    - plone.app.z3cform
    - lxml
    - repoze.xmliter

test:
  # Python imports
  imports:
    - collective
    - collective.tinymceportlets
    - collective.tinymceportlets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'And portlets into content with tiny mce'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
