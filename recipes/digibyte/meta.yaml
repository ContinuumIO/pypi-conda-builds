package:
  name: digibyte
  version: "1.1.25"

source:
  fn: digibyte-1.1.25.tar.gz
  url: https://pypi.python.org/packages/source/d/digibyte/digibyte-1.1.25.tar.gz
  md5: aace373908f146b39791579bc1a5f6bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digibyte = digibyte:main
    #
    # Would create an entry point called digibyte that calls digibyte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.8.0

  run:
    - python
    - six ==1.8.0

test:
  # Python imports
  imports:
    - bitcoin
    - digibyte

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Digitools/pydigibytetools
  license: UNKNOWN
  summary: 'Python Digibyte Tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
