package:
  name: django-loaderio
  version: "0.3.0"

source:
  fn: django-loaderio-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-loaderio/django-loaderio-0.3.0.tar.gz
  md5: 2fdfca144a0875dfb6e88d6e6bac8846
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-loaderio = django-loaderio:main
    #
    # Would create an entry point called django-loaderio that calls django-loaderio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - loaderio
    - loaderio.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sendgridlabs/loaderio-django
  license: MIT License
  summary: 'An app for adding loader.io validation tokens'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
