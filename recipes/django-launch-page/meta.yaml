package:
  name: django-launch-page
  version: "0.1.1"

source:
  fn: django-launch-page-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-launch-page/django-launch-page-0.1.1.tar.gz
  md5: 0c5719d11df6181470aaa85db6858fe5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-launch-page = django-launch-page:main
    #
    # Would create an entry point called django-launch-page that calls django-launch-page.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - south >=0.7.5

  run:
    - python
    - django >=1.5
    - south >=0.7.5

test:
  # Python imports
  imports:
    - launch_page

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/RyanBalfanz/django-launch-page
  license: UNKNOWN
  summary: '\ndjango-launch-page\n\nA launch page for a Django project to collect e-mail addresses and more.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
