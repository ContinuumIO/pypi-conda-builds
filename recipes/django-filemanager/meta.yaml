package:
  name: django-filemanager
  version: "0.0.5"

source:
  fn: django-filemanager-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filemanager/django-filemanager-0.0.5.tar.gz
  md5: 15c86a03ce37870b36756bc8cbc13b74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filemanager = django-filemanager:main
    #
    # Would create an entry point called django-filemanager that calls django-filemanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - filemanager
    - filemanager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/byteweaver/django-filemanager
  license: BSD License
  summary: 'Template for reusable django applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
