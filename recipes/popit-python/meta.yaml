package:
  name: popit-python
  version: "0.1.11"

source:
  fn: PopIt-Python-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/P/PopIt-Python/PopIt-Python-0.1.11.tar.gz
  md5: 5874d8850b9811010ada2cf74a9830c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - popit-python = popit-python:main
    #
    # Would create an entry point called popit-python that calls popit-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.6.0
    - slumber

  run:
    - python
    - requests ==2.6.0
    - slumber

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mysociety/popit-python
  license: UNKNOWN
  summary: 'Python bindings to connect to the PopIt API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
