package:
  name: verboselib
  version: "0.2.0"

source:
  fn: verboselib-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/v/verboselib/verboselib-0.2.0.tar.gz
  md5: d24361d1414ff00eafdbcc38b1b0384f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - verboselib = verboselib:main
    #
    # Would create an entry point called verboselib that calls verboselib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stringlike ==0.3.3

  run:
    - python
    - stringlike ==0.3.3

test:
  # Python imports
  imports:
    - verboselib
    - verboselib.management
    - verboselib.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oblalex/verboselib
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'A little L10N framework for libraries and applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
