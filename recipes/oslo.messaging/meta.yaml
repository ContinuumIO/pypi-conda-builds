package:
  name: oslo.messaging
  version: "1.8.2"

source:
  fn: oslo.messaging-1.8.2.tar.gz
  url: https://pypi.python.org/packages/source/o/oslo.messaging/oslo.messaging-1.8.2.tar.gz
  md5: 9ea5aafc2e9857e7ea9241e0782651dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oslo.messaging = oslo.messaging:main
    #
    # Would create an entry point called oslo.messaging that calls oslo.messaging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbr

  run:
    - python
    - pbr

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Oslo Messaging API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
