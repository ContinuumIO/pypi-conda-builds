package:
  name: typeschema
  version: "0.0.2"

source:
  fn: typeschema-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/typeschema/typeschema-0.0.2.tar.gz
  md5: 85ba8a720a15245ad9e2efb4cf16569a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typeschema = typeschema:main
    #
    # Would create an entry point called typeschema that calls typeschema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema ==2.4.0
    - incf.countryutils ==1.0

  run:
    - python
    - jsonschema ==2.4.0
    - incf.countryutils ==1.0

test:
  # Python imports
  imports:
    - typeschema
    - typeschema.properties
    - typeschema.types

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tyba/typeschema
  license: MIT License
  summary: 'A simpler type validator based on jsonschema.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
