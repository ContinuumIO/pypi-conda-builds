package:
  name: beanstream
  version: "1.0.0rc1"

source:
  fn: beanstream-1.0.0rc1.zip
  url: https://pypi.python.org/packages/source/b/beanstream/beanstream-1.0.0rc1.zip
  md5: 20bfdafa3691292d481bcac6f9366c80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beanstream = beanstream:main
    #
    # Would create an entry point called beanstream that calls beanstream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - beanstream

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Beanstream-DRWP/beanstream-python
  license: Apache2
  summary: 'Beanstream SDK library for processing credit card payments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
