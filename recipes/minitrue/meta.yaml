package:
  name: minitrue
  version: "20130126"

source:
  fn: minitrue-20130126.tar.gz
  url: https://pypi.python.org/packages/source/m/minitrue/minitrue-20130126.tar.gz
  md5: 1be057f12ade73b64e2a0c16a1625384
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minitrue = minitrue:main
    #
    # Would create an entry point called minitrue that calls minitrue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted

  run:
    - python
    - twisted

test:
  # Python imports
  imports:
    - minitrue
    - minitrue.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lvh/minitrue
  license: ISC License (ISCL)
  summary: 'Proxy designed to lie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
