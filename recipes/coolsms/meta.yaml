package:
  name: coolsms
  version: "0.1"

source:
  fn: coolsms-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coolsms/coolsms-0.1.tar.gz
  md5: a433e06bd8575ac7dd1465c24574bd32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coolsms = coolsms:main
    #
    # Would create an entry point called coolsms that calls coolsms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.3.0

  run:
    - python
    - requests ==2.3.0

test:
  # Python imports
  imports:
    - coolsms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Simple python library for korean SMS service provider CoolSMS(coolsms.co.kr)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
