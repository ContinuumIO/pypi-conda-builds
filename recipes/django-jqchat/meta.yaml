package:
  name: django-jqchat
  version: "0.3"

source:
  fn: django-jqchat-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jqchat/django-jqchat-0.3.tar.gz
  md5: 83c343a6615e248f978199643bd15bca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jqchat = django-jqchat:main
    #
    # Would create an entry point called django-jqchat that calls django-jqchat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0
    - django-timezones
    - pytz
    - south

  run:
    - python
    - django >=1.0
    - django-timezones
    - pytz
    - south

test:
  # Python imports
  imports:
    - jqchat
    - jqchat.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tomscytale/django-jqchat
  license: UNKNOWN
  summary: 'An extensible instant messenger thingy for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
