package:
  name: mredoc
  version: "0.1"

source:
  fn: mredoc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mredoc/mredoc-0.1.tar.gz
  md5: df93fc1af9ed6b074fc01c06bf87a2f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mredoc = mredoc:main
    #
    # Would create an entry point called mredoc that calls mredoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib
    - mredoc
    - pygments

  run:
    - python
    - matplotlib
    - mredoc
    - pygments

test:
  # Python imports
  imports:
    - mredoc
    - mredoc.util
    - mredoc.visitors
    - mredoc.writers
    - mredoc.writers.html
    - mredoc.writers.latex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikehulluk/mredoc
  license: BSD License
  summary: 'Tools for generation hierachical documents in HTML and PDF, primarily designed for building reports of computational models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
