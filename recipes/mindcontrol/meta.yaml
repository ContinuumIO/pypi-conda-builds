package:
  name: mindcontrol
  version: "0.54dev"

source:
  fn: mindcontrol-0.54dev.tar.gz
  url: https://pypi.python.org/packages/source/m/mindcontrol/mindcontrol-0.54dev.tar.gz
  md5: 8da13d987b525977b2f812cf19a29673
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mindcontrol = mindcontrol:main
    #
    # Would create an entry point called mindcontrol that calls mindcontrol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - examples
    - mindcontrol
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/mindcontrol
  license: GPLv3
  summary: 'Python wrappers for the NeuroSky Mindwave Headsets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
