package:
  name: hebel
  version: "0.02.1"

source:
  fn: Hebel-0.02.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hebel/Hebel-0.02.1.tar.gz
  md5: 75ae526b858adfa9d5ad78a4ed0aeb21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hebel = hebel:main
    #
    # Would create an entry point called hebel that calls hebel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycuda
    - numpy
    - pyyaml
    - skdata

  run:
    - python
    - pycuda
    - numpy
    - pyyaml
    - skdata

test:
  # Python imports
  imports:
    - hebel
    - hebel.layers
    - hebel.models
    - hebel.pycuda_ops
    - hebel.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hannes-brt/hebel
  license: GNU General Public License v2 (GPLv2)
  summary: 'GPU-Accelerated Deep Learning Library in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
