package:
  name: ipngnc
  version: "0.1"

source:
  fn: ipngnc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ipngnc/ipngnc-0.1.tar.gz
  md5: ca727aa8cc7e7071a41bd3a664a3440a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipngnc = ipngnc:main
    #
    # Would create an entry point called ipngnc that calls ipngnc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ipngnc
    - ipngnc.devices
    - ipngnc.operations
    - ipngnc.transport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DavidJohnGee/ipngnc
  license: Apache License 2.0
  summary: 'Python library for NETCONF clients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
