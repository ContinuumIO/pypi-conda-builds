package:
  name: marcx
  version: "0.1.17"

source:
  fn: marcx-0.1.17.tar.gz
  url: https://pypi.python.org/packages/source/m/marcx/marcx-0.1.17.tar.gz
  md5: d011691d71478d7bbc5b0c8884136808
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marcx = marcx:main
    #
    # Would create an entry point called marcx that calls marcx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymarc >=2.0
    - jsonpath-rw ==1.3.0
    - ply ==3.4

  run:
    - python
    - pymarc >=2.0
    - jsonpath-rw ==1.3.0
    - ply ==3.4

test:
  # Python imports
  imports:
    - marcx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/miku/marcx
  license: MIT License
  summary: 'MARC record manipulation library based on pymarc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
