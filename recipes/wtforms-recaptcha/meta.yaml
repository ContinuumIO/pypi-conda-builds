package:
  name: wtforms-recaptcha
  version: "0.3.2"

source:
  fn: wtforms-recaptcha-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/w/wtforms-recaptcha/wtforms-recaptcha-0.3.2.tar.gz
  md5: c3d7d1d02d0ecdb50d653c4e46ba75b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-recaptcha = wtforms-recaptcha:main
    #
    # Would create an entry point called wtforms-recaptcha that calls wtforms-recaptcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms >=0.6.1

  run:
    - python
    - wtforms >=0.6.1

test:
  # Python imports
  imports:
    - wtfrecaptcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/excieve/wtforms-recaptcha
  license: BSD License
  summary: 'Custom WTForms field that handles reCaptcha display and validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
