package:
  name: django-qoptions
  version: "0.1.4"

source:
  fn: django-qoptions-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qoptions/django-qoptions-0.1.4.tar.gz
  md5: 07b887b316df1939888a1d23c06fe517
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qoptions = django-qoptions:main
    #
    # Would create an entry point called django-qoptions that calls django-qoptions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - options
    - options.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Brick85/options/
  license: BSD License
  summary: 'Options, Labels and standalone Texts for django admin. Administrator emails, phones, contact data, etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
