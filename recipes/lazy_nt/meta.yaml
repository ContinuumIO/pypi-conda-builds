package:
  name: lazy_nt
  version: "0.1.0"

source:
  fn: LaZy_NT-0.1.0.zip
  url: https://pypi.python.org/packages/source/L/LaZy_NT/LaZy_NT-0.1.0.zip
  md5: 4d7e7788434c18c98428a39f29046b23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazy_nt = lazy_nt:main
    #
    # Would create an entry point called lazy_nt that calls lazy_nt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pypdf >=1.13
    - pillow >=2.4.0
    - openxmllib >=1.0.7
    - hachoir-core >=1.3.3
    - hachoir-metadata >=1.3.3
    - hachoir-parser >=1.3.4

  run:
    - python
    - pypdf >=1.13
    - pillow >=2.4.0
    - openxmllib >=1.0.7
    - hachoir-core >=1.3.3
    - hachoir-metadata >=1.3.3
    - hachoir-parser >=1.3.4

test:
  # Python imports
  imports:
    - LaZy_NT
    - lazy_nt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: none
  license: LICENSE.txt
  summary: 'File carving for compressed NTFS volumes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
