package:
  name: django-mongodb-cache
  version: "0.1.0"

source:
  fn: django-mongodb-cache-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mongodb-cache/django-mongodb-cache-0.1.0.tar.gz
  md5: f42ab3b39b5f0d9066952bdda9d7a0c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mongodb-cache = django-mongodb-cache:main
    #
    # Would create an entry point called django-mongodb-cache that calls django-mongodb-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mongodb_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/django-mongodb-cache/
  license: BSD License
  summary: 'Mongodb cache backend for Django framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
