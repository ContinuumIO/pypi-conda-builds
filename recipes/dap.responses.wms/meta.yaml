package:
  name: dap.responses.wms
  version: "0.3.5"

source:
  fn: dap.responses.wms-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.responses.wms/dap.responses.wms-0.3.5.tar.gz
  md5: 6eb7e6e401422a60e804e7412f793354
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.responses.wms = dap.responses.wms:main
    #
    # Would create an entry point called dap.responses.wms that calls dap.responses.wms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - pil
    - dap[server] >=2.2.5.1
    - paste

  run:
    - python
    - setuptools
    - numpy
    - pil
    - dap[server] >=2.2.5.1
    - paste

test:
  # Python imports
  imports:
    - dap
    - dap.responses
    - dap.responses.wms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydap.org/responses/wms
  license: MIT License
  summary: 'WMS response for pydap server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
