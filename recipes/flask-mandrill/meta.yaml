package:
  name: flask-mandrill
  version: "0.2"

source:
  fn: Flask-Mandrill-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Mandrill/Flask-Mandrill-0.2.tar.gz
  md5: e6870f0db8a77289199876b520aaa1a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mandrill = flask-mandrill:main
    #
    # Would create an entry point called flask-mandrill that calls flask-mandrill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests

  run:
    - python
    - flask
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/volker48/flask-mandrill
  license: MIT License
  summary: 'Adds Mandrill support to Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
