package:
  name: django-stagingcontext
  version: "0.1.0"

source:
  fn: django-stagingcontext-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stagingcontext/django-stagingcontext-0.1.0.tar.gz
  md5: 42b37aabb3ff3f28d28fc99da9aaf888
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stagingcontext = django-stagingcontext:main
    #
    # Would create an entry point called django-stagingcontext that calls django-stagingcontext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stagingcontext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'staging environment context processor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
