package:
  name: amifs_core
  version: "1.0.0-beta1"

source:
  fn: amifs_core-1.0.0-beta1.tar.gz
  url: https://pypi.python.org/packages/source/a/amifs_core/amifs_core-1.0.0-beta1.tar.gz
  md5: 7458eb606b8046ea22ba57bd84fc1a46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amifs_core = amifs_core:main
    #
    # Would create an entry point called amifs_core that calls amifs_core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyami_core

  run:
    - python
    - pyami_core

test:
  # Python imports
  imports:
    - amifs
    - amifs_core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ami.in2p3.fr/
  license: CeCILL-C
  summary: 'ATLAS Metadata Interface File System (AMIFS)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
