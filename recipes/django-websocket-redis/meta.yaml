package:
  name: django-websocket-redis
  version: "0.4.4"

source:
  fn: django-websocket-redis-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-websocket-redis/django-websocket-redis-0.4.4.tar.gz
  md5: 2b4770da614c2db32c4b218b397ba770
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-websocket-redis = django-websocket-redis:main
    #
    # Would create an entry point called django-websocket-redis that calls django-websocket-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - gevent
    - greenlet
    - six

  run:
    - python
    - setuptools
    - redis
    - gevent
    - greenlet
    - six

test:
  # Python imports
  imports:
    - ws4redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jrief/django-websocket-redis
  license: MIT License
  summary: 'Websocket support for Django using Redis as datastore'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
