package:
  name: halp
  version: "1.0.0"

source:
  fn: halp-1.0.0.zip
  url: https://pypi.python.org/packages/source/h/halp/halp-1.0.0.zip
  md5: 90404c61b4b59ed13af72f69fa2fdf56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - halp = halp:main
    #
    # Would create an entry point called halp that calls halp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - halp
    - halp.algorithms
    - halp.utilities
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tmmurali.github.io/halp/
  license: GNU General Public License (GPL)
  summary: 'Hypergraph Algorithms Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
