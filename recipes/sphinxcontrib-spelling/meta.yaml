package:
  name: sphinxcontrib-spelling
  version: "2.1.1"

source:
  fn: sphinxcontrib-spelling-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-spelling/sphinxcontrib-spelling-2.1.1.tar.gz
  md5: c6758dd166486dc609a7aacb8c97cabf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-spelling = sphinxcontrib-spelling:main
    #
    # Would create an entry point called sphinxcontrib-spelling that calls sphinxcontrib-spelling.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Sphinx "spelling" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
