package:
  name: django-oscar-approval
  version: "0.1"

source:
  fn: django-oscar-approval-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oscar-approval/django-oscar-approval-0.1.tar.gz
  md5: 0ea1c5a07c654b7cb2031cf2812dd1bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oscar-approval = django-oscar-approval:main
    #
    # Would create an entry point called django-oscar-approval that calls django-oscar-approval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-oscar >=0.4,<0.6

  run:
    - python
    - django-oscar >=0.4,<0.6

test:
  # Python imports
  imports:
    - oscar_approval
    - oscar_approval.apps
    - oscar_approval.apps.approval
    - oscar_approval.apps.catalogue
    - oscar_approval.apps.customer
    - oscar_approval.apps.dashboard
    - oscar_approval.apps.order

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tangentlabs/django-oscar-approval
  license: BSD License
  summary: 'Order approval module for django-oscar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
