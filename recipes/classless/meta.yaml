package:
  name: classless
  version: "0.0.2"

source:
  fn: Classless-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/C/Classless/Classless-0.0.2.tar.gz
  md5: d604f2708358028b3c2668541e27543f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - classless = classless:main
    #
    # Would create an entry point called classless that calls classless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - classless

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djyale/classless
  license: BSD License
  summary: 'Easily curry multiple functions to a given list of arguments without using classes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
