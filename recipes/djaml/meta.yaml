package:
  name: djaml
  version: "1.1"

source:
  fn: djaml-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djaml/djaml-1.1.tar.gz
  md5: fea630a2b46c3eeb240aa3d4e64a6973
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djaml = djaml:main
    #
    # Would create an entry point called djaml that calls djaml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hamlpy
    - pygments

  run:
    - python
    - hamlpy
    - pygments

test:
  # Python imports
  imports:
    - djaml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chartjes/djaml/
  license: MIT License
  summary: 'A Django template loader for loading and converting HamlPy markup to HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
