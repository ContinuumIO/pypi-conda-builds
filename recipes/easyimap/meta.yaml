package:
  name: easyimap
  version: "0.6.1"

source:
  fn: easyimap-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyimap/easyimap-0.6.1.tar.gz
  md5: 970c565174eff595f93e4777aa01674d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyimap = easyimap:main
    #
    # Would create an entry point called easyimap that calls easyimap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chardet >=2.3.0

  run:
    - python
    - chardet >=2.3.0

test:
  # Python imports
  imports:
    - easyimap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keitaoouchi/easyimap
  license: BSD License
  summary: 'Simple imap wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
