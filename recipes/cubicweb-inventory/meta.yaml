package:
  name: cubicweb-inventory
  version: "0.2.0"

source:
  fn: cubicweb-inventory-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-inventory/cubicweb-inventory-0.2.0.tar.gz
  md5: 135108b1b0543e480d96daf021403351
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-inventory = cubicweb-inventory:main
    #
    # Would create an entry point called cubicweb-inventory that calls cubicweb-inventory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-comment
    - cubicweb-tag
    - cubicweb-link
    - cubicweb >=3.12.5
    - cubicweb-zone
    - cubicweb-company
    - cubicweb-folder

  run:
    - python
    - cubicweb-comment
    - cubicweb-tag
    - cubicweb-link
    - cubicweb >=3.12.5
    - cubicweb-zone
    - cubicweb-company
    - cubicweb-folder

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-inventory
  license: LGPL
  summary: 'Hardware inventory application based on the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
