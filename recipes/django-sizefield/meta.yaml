package:
  name: django-sizefield
  version: "0.7"

source:
  fn: django-sizefield-0.7.zip
  url: https://pypi.python.org/packages/source/d/django-sizefield/django-sizefield-0.7.zip
  md5: b7e8a80b846f87cef47b771e12028297
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sizefield = django-sizefield:main
    #
    # Would create an entry point called django-sizefield that calls django-sizefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - sizefield
    - sizefield.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leplatrem/django-sizefield
  license: LPGL, see LICENSE file.
  summary: 'A model field to store a file size, whose edition and display shows units.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
