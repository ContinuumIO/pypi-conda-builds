package:
  name: django-feedmapper
  version: "1.0.2"

source:
  fn: django-feedmapper-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-feedmapper/django-feedmapper-1.0.2.tar.gz
  md5: 8b4d738ddd63b05ae543ed83105090b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-feedmapper = django-feedmapper:main
    #
    # Would create an entry point called django-feedmapper that calls django-feedmapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml ==2.3

  run:
    - python
    - lxml ==2.3

test:
  # Python imports
  imports:
    - example
    - example.myapp
    - feedmapper
    - feedmapper.management
    - feedmapper.management.commands
    - feedmapper.migrations
    - feedmapper.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/natgeo/django-feedmapper
  license: BSD License
  summary: 'A utility for mapping feeds to Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
