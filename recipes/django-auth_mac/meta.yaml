package:
  name: django-auth_mac
  version: "0.1.2"

source:
  fn: django-auth_mac-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth_mac/django-auth_mac-0.1.2.tar.gz
  md5: 2bb2e4c553f604706540609b7c793ba1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth_mac = django-auth_mac:main
    #
    # Would create an entry point called django-auth_mac that calls django-auth_mac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - auth_mac
    - auth_mac.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ndevenish/auth_mac
  license: MIT License
  summary: 'Basic Django implementation of the draft RFC ietf-oauth-v2-http-mac-01'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
