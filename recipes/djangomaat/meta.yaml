package:
  name: djangomaat
  version: "1.4"

source:
  fn: djangomaat-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/djangomaat/djangomaat-1.4.tar.gz
  md5: 2c32fd46d4cbb61b43f083e0ecbd7aed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangomaat = djangomaat:main
    #
    # Would create an entry point called djangomaat that calls djangomaat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangomaat
    - djangomaat.management
    - djangomaat.management.commands
    - djangomaat.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GermanoGuerrini/django-maat
  license: MIT License
  summary: 'Fast MySQL ordering'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
