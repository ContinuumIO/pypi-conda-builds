package:
  name: collective.collage.blogging
  version: "1.0.1"

source:
  fn: collective.collage.blogging-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.collage.blogging/collective.collage.blogging-1.0.1.zip
  md5: c3e1273f0728b85e7d4f58ed11eb10c1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.blogging = collective.collage.blogging:main
    #
    # Would create an entry point called collective.collage.blogging that calls collective.collage.blogging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - products.collage

  run:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - products.collage

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.blogging
    - collective.collage.blogging.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.collage.blogging
  license: GPL
  summary: 'A collective.blogging integration for Products.Collage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
