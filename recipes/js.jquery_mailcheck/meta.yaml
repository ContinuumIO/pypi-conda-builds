package:
  name: js.jquery_mailcheck
  version: "1.0.1"

source:
  fn: js.jquery_mailcheck-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_mailcheck/js.jquery_mailcheck-1.0.1.tar.gz
  md5: 32684ec4102fef88a4b20b792782bcff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_mailcheck = js.jquery_mailcheck:main
    #
    # Would create an entry point called js.jquery_mailcheck that calls js.jquery_mailcheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_mailcheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jQuery mailcheck'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
