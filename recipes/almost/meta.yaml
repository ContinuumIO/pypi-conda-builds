package:
  name: almost
  version: "0.1.5"

source:
  fn: almost-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/a/almost/almost-0.1.5.tar.gz
  md5: 2b0c0f576341622d7dd18d32a6c1d402
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - almost = almost:main
    #
    # Would create an entry point called almost that calls almost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - almost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sublee/almost
  license: BSD License
  summary: 'A helper to compare two numbers generously'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
