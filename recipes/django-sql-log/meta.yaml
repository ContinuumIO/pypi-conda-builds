package:
  name: django-sql-log
  version: "1.1.0"

source:
  fn: django-sql-log-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sql-log/django-sql-log-1.1.0.tar.gz
  md5: c9497c65e5ff509d652b22ce975715f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sql-log = django-sql-log:main
    #
    # Would create an entry point called django-sql-log that calls django-sql-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.7

  run:
    - python
    - django <1.7

test:
  # Python imports
  imports:
    - django_sql_log

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/novapost/django-sql-log
  license: MIT License
  summary: u'Write Start/Stop events in your SQL logs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
