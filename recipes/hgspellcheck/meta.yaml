package:
  name: hgspellcheck
  version: "1.0rc2"

source:
  fn: hgspellcheck-1.0rc2.tar.gz
  url: https://pypi.python.org/packages/source/h/hgspellcheck/hgspellcheck-1.0rc2.tar.gz
  md5: dffc6589dadfb7e0374f72940e197857
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgspellcheck = hgspellcheck:main
    #
    # Would create an entry point called hgspellcheck that calls hgspellcheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyenchant >=1.5.3

  run:
    - python
    - pyenchant >=1.5.3

test:
  # Python imports
  imports:
    - hgspellcheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ilowe/hgspellcheck
  license: MIT
  summary: 'Hook and extension to spellcheck Mercurial commit messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
