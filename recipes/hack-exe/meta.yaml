package:
  name: hack-exe
  version: "0.2"

source:
  fn: hack-exe-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hack-exe/hack-exe-0.2.tar.gz
  md5: 4da344e62f9977283643120af41012f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hack-exe = hack-exe:main
    #
    # Would create an entry point called hack-exe that calls hack-exe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hack_exe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gardaud/hack-exe
  license: MIT License
  summary: 'An animated terminal application that pretends to be hacking a website, just like in the movies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
