package:
  name: sphinxcontrib-webmocks
  version: "0.1.0"

source:
  fn: sphinxcontrib-webmocks-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-webmocks/sphinxcontrib-webmocks-0.1.0.tar.gz
  md5: 8fc3b4a64e6a6a859a67beae93b16924
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-webmocks = sphinxcontrib-webmocks:main
    #
    # Would create an entry point called sphinxcontrib-webmocks that calls sphinxcontrib-webmocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6

  run:
    - python
    - sphinx >=0.6

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tk0miya/sphinxcontrib-webmocks
  license: BSD License
  summary: 'Sphinx "webmocks" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
