package:
  name: esclient
  version: "0.5.8"

source:
  fn: ESClient-0.5.8.tar.gz
  url: https://pypi.python.org/packages/source/E/ESClient/ESClient-0.5.8.tar.gz
  md5: 0f8d7dfc1eebf9360c5739a8f84a7cde
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - esclient = esclient:main
    #
    # Would create an entry point called esclient that calls esclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - esclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eriky/ESClient
  license: BSD License
  summary: 'A lightweight Python client for ElasticSearch, including a dump and import tool for indexes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
