package:
  name: flask-openerp
  version: "0.3.1"

source:
  fn: Flask-OpenERP-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-OpenERP/Flask-OpenERP-0.3.1.tar.gz
  md5: c66ea173b9c89fb91379e1844f151b8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-openerp = flask-openerp:main
    #
    # Would create an entry point called flask-openerp that calls flask-openerp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matrixise/flask-openerp/
  license: BSD License
  summary: 'OpenERP Connector for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
