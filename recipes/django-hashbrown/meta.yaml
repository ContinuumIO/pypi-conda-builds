package:
  name: django-hashbrown
  version: "0.6.1"

source:
  fn: django-hashbrown-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hashbrown/django-hashbrown-0.6.1.tar.gz
  md5: 03052609dd6aecc46193fdb678b48e01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hashbrown = django-hashbrown:main
    #
    # Would create an entry point called django-hashbrown that calls django-hashbrown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hashbrown
    - hashbrown.management
    - hashbrown.management.commands
    - hashbrown.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/potatolondon/django-hashbrown
  license: BSD License
  summary: 'Yet another dead simple feature switching library for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
