package:
  name: mprpc
  version: "0.1.4"

source:
  fn: mprpc-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mprpc/mprpc-0.1.4.tar.gz
  md5: f00a906398fc24058889ae957c982d97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mprpc = mprpc:main
    #
    # Would create an entry point called mprpc that calls mprpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gsocketpool
    - gevent
    - msgpack-python

  run:
    - python
    - gsocketpool
    - gevent
    - msgpack-python

test:
  # Python imports
  imports:
    - mprpc
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/studio-ousia/mprpc
  license: Apache Software License
  summary: 'A fast MessagePack RPC library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
