package:
  name: pelican-advthumbnailer
  version: "0.4.0"

source:
  fn: pelican-advthumbnailer-0.4.0.zip
  url: https://pypi.python.org/packages/source/p/pelican-advthumbnailer/pelican-advthumbnailer-0.4.0.zip
  md5: 79d6aafe4a7fe0018521fbc8ac6c162d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-advthumbnailer = pelican-advthumbnailer:main
    #
    # Would create an entry point called pelican-advthumbnailer that calls pelican-advthumbnailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican >=3.4.0
    - beautifulsoup4 >=4.3.2
    - pillow >=2.5.3

  run:
    - python
    - pelican >=3.4.0
    - beautifulsoup4 >=4.3.2
    - pillow >=2.5.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AlexJF/pelican-advthumbnailer
  license: Apache
  summary: 'A thumbnail generator for Pelican that operates by looking at the filename of missing files to determine thumb format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
