package:
  name: django-sitesutils
  version: "0.1.2"

source:
  fn: django-sitesutils-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sitesutils/django-sitesutils-0.1.2.tar.gz
  md5: 3f15238b80164c6b7a38305c5f4e36d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sitesutils = django-sitesutils:main
    #
    # Would create an entry point called django-sitesutils that calls django-sitesutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sitesutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trilan/django-sitesutils
  license: ISC License (ISCL)
  summary: "Utils for Django's sites framework"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
