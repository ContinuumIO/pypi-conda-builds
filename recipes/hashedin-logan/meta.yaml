package:
  name: hashedin-logan
  version: "0.6"

source:
  fn: hashedin-logan-0.6.tar.gz
  url: https://pypi.python.org/packages/source/h/hashedin-logan/hashedin-logan-0.6.tar.gz
  md5: 472a19d1a4a71be16f29dffb73031325
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hashedin-logan = hashedin-logan:main
    #
    # Would create an entry point called hashedin-logan that calls hashedin-logan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - certifi
    - requests

  run:
    - python
    - certifi
    - requests

test:
  # Python imports
  imports:
    - logan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Logan/
  license: LICENSE
  summary: 'Python Logger plugin to send logs to Logan'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
