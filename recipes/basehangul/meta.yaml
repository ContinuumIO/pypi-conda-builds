package:
  name: basehangul
  version: "1.1"

source:
  fn: basehangul-1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/basehangul/basehangul-1.1.tar.gz
  md5: 623a02d44fa8a8c036581a71a7b32983
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basehangul = basehangul:main
    #
    # Would create an entry point called basehangul that calls basehangul.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - basehangul

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ssut/basehangul
  license: UNKNOWN
  summary: 'Human-readable binary encoding, BaseHangul for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
