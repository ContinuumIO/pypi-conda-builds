package:
  name: win_unc
  version: "0.6.1"

source:
  fn: win_unc-0.6.1.zip
  url: https://pypi.python.org/packages/source/w/win_unc/win_unc-0.6.1.zip
  md5: 0917665c1c4f08d527f9df2b420e69b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - win_unc = win_unc:main
    #
    # Would create an entry point called win_unc that calls win_unc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - win_unc
    - win_unc.internal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CovenantEyes/py_win_unc
  license: MIT License
  summary: 'UNC network drive handling and mounting for Windows'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
