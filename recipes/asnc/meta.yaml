package:
  name: asnc
  version: "1.0.4b"

source:
  fn: ASNC-1.0.4b.tar.gz
  url: https://pypi.python.org/packages/source/A/ASNC/ASNC-1.0.4b.tar.gz
  md5: 3e79b063b559525044ec5cdd49b81df9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asnc = asnc:main
    #
    # Would create an entry point called asnc that calls asnc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - asnc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/0rax/ASNC
  license: LICENSE.txt
  summary: 'ASNC (Asynchronous NetSoul Client) is small Netsoul client compatible with Python 2 and 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
