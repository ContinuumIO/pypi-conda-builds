package:
  name: crdown
  version: "0.6.9"

source:
  fn: crdown-0.6.9.tar.gz
  url: https://pypi.python.org/packages/source/c/crdown/crdown-0.6.9.tar.gz
  md5: 7dce1565f80a56ecc49a0725b0d9f434
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crdown = crdown:main
    #
    # Would create an entry point called crdown that calls crdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appdirs
    - beautifulsoup4
    - pycrypto
    - lxml

  run:
    - python
    - appdirs
    - beautifulsoup4
    - pycrypto
    - lxml

test:
  # Python imports
  imports:
    - crdown
    - crunchy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/m45t3r/crdown
  license: Creative Commons Attribution-ShareAlike 3.0 Unported
  summary: 'Crunchyroll video downloader.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
