package:
  name: typographie
  version: "0.0.2"

source:
  fn: typographie-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/typographie/typographie-0.0.2.tar.gz
  md5: e8b4abad915e02a669af7398a8374b9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typographie = typographie:main
    #
    # Would create an entry point called typographie that calls typographie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - typographie
    - typographie.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oxys-net/django-typographie
  license: UNKNOWN
  summary: 'Apply french typografy rules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
