package:
  name: ip2location
  version: "7.0.0"

source:
  fn: ip2location-python-7.0.0.tar.gz
  url: https://pypi.python.org/packages/source/I/IP2Location/ip2location-python-7.0.0.tar.gz
  md5: 053e30837b0ec6ddb0d587e99a42b2f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ip2location = ip2location:main
    #
    # Would create an entry point called ip2location that calls ip2location.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ip2location

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ip2location.com
  license: LGPL
  summary: 'Python API for IP2Location database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
