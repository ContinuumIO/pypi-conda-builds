package:
  name: mopidy-mopify
  version: "1.4.2"

source:
  fn: Mopidy-Mopify-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Mopify/Mopidy-Mopify-1.4.2.tar.gz
  md5: d8a4395d46bffe3fd698d6345dc53a6b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-mopify = mopidy-mopify:main
    #
    # Would create an entry point called mopidy-mopify that calls mopidy-mopify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19
    - configobj

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19
    - configobj

test:
  # Python imports
  imports:
    - mopidy_mopify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dirkgroenen/Mopify
  license: Apache Software License
  summary: u'A Mopidy Web client based on the (old) Spotify interface. Improved to work with spotify as main library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
