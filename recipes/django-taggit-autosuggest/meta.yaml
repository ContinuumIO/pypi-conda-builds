package:
  name: django-taggit-autosuggest
  version: "0.2.7"

source:
  fn: django-taggit-autosuggest-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-taggit-autosuggest/django-taggit-autosuggest-0.2.7.tar.gz
  md5: 8b4d86be8f2239caf8e98cda4077ef36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-taggit-autosuggest = django-taggit-autosuggest:main
    #
    # Would create an entry point called django-taggit-autosuggest that calls django-taggit-autosuggest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-taggit

  run:
    - python
    - django-taggit

test:
  # Python imports
  imports:
    - taggit_autosuggest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fabian/django-taggit-autosuggest
  license: MIT License
  summary: 'Autosuggestions for django-taggit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
