package:
  name: censored_regression
  version: "0.0.1"

source:
  fn: censored_regression-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/censored_regression/censored_regression-0.0.1.tar.gz
  md5: 1aa2059efd34a1646e39304df056ad8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - censored_regression = censored_regression:main
    #
    # Would create an entry point called censored_regression that calls censored_regression.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - censored_regression
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/censored_regression
  license: Apache Software License
  summary: 'Linear regression where some of the labels are lower bounds'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
