package:
  name: mr.inquisition
  version: "0.1.1"

source:
  fn: mr.inquisition-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mr.inquisition/mr.inquisition-0.1.1.tar.gz
  md5: 0d2107a2bbd3c68004cf1319129b37a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.inquisition = mr.inquisition:main
    #
    # Would create an entry point called mr.inquisition that calls mr.inquisition.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - mr
    - mr.inquisition

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/markvl/mr.inquisition
  license: GPL
  summary: 'A package to help with exploring a Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
