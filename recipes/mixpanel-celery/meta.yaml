package:
  name: mixpanel-celery
  version: "0.8.0"

source:
  fn: mixpanel-celery-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mixpanel-celery/mixpanel-celery-0.8.0.tar.gz
  md5: e968494802c93396165ee3c3fdd8534b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixpanel-celery = mixpanel-celery:main
    #
    # Would create an entry point called mixpanel-celery that calls mixpanel-celery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=2.5

  run:
    - python
    - celery >=2.5

test:
  # Python imports
  imports:
    - mixpanel
    - mixpanel.conf
    - testproj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://winhamwr.github.io/mixpanel-celery/
  license: BSD License
  summary: 'Asynchronous event tracking for Mixpanel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
