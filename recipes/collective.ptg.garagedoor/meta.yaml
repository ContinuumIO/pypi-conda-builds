package:
  name: collective.ptg.garagedoor
  version: "0.1"

source:
  fn: collective.ptg.garagedoor-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.garagedoor/collective.ptg.garagedoor-0.1.zip
  md5: f6fd791c9a472f49d00bd1a5260741f2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.garagedoor = collective.ptg.garagedoor:main
    #
    # Would create an entry point called collective.ptg.garagedoor that calls collective.ptg.garagedoor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.garagedoor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.garagedoor
  license: GPL
  summary: 'garagedoor for plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
