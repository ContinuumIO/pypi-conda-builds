package:
  name: armstrong.utils.celery
  version: "0.1.4"

source:
  fn: armstrong.utils.celery-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.utils.celery/armstrong.utils.celery-0.1.4.tar.gz
  md5: 0ec08a2055c07869f653a5dbfcfbcee8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.utils.celery = armstrong.utils.celery:main
    #
    # Would create an entry point called armstrong.utils.celery that calls armstrong.utils.celery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.3.1
    - django-celery ==2.3.3

  run:
    - python
    - django ==1.3.1
    - django-celery ==2.3.3

test:
  # Python imports
  imports:
    - armstrong.utils.celery
    - armstrong.utils.celery.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.utils.celery/
  license: Apache Software License
  summary: 'Provides some utility functions for dealing with Celery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
