package:
  name: envious
  version: "0.1.1"

source:
  fn: envious-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/envious/envious-0.1.1.tar.gz
  md5: 720a38a6ec30b585b1d13327c2605987
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - envious = envious:main
    #
    # Would create an entry point called envious that calls envious.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - envious

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BendingSpoons/envious
  license: MIT
  summary: 'Easy injection of environment variables from .env files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
