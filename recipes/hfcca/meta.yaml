package:
  name: hfcca
  version: "1.8.11"

source:
  fn: hfcca-1.8.11.tar.gz
  url: https://pypi.python.org/packages/source/h/hfcca/hfcca-1.8.11.tar.gz
  md5: b2b7f2027fe010743b845b2fd93ec6c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hfcca = hfcca:main
    #
    # Would create an entry point called hfcca that calls hfcca.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hfcca
    - lizard_ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.lizard.ws
  license: MIT
  summary: ' A code analyzer without caring the C/C++ header files.\nIt works with Java, C/C++, JavaScript, Python, Objective C. Metrics includes cyclomatic complexity number etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
