package:
  name: sphinxcontrib-taglist
  version: "0.2"

source:
  fn: sphinxcontrib-taglist-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-taglist/sphinxcontrib-taglist-0.2.tar.gz
  md5: 6c68a5976ae60ee9749f1d9596879354
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-taglist = sphinxcontrib-taglist:main
    #
    # Would create an entry point called sphinxcontrib-taglist that calls sphinxcontrib-taglist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.9

  run:
    - python
    - sphinx >=0.9

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/spinus/sphinxcontrib-taglist
  license: BSD License
  summary: 'Sphinx "taglist" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
