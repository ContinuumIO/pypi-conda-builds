package:
  name: htmlgrab
  version: "0.0.3-alpha"

source:
  fn: htmlgrab-0.0.3-alpha.tar.gz
  url: https://pypi.python.org/packages/source/h/htmlgrab/htmlgrab-0.0.3-alpha.tar.gz
  md5: d4d534f0c3f01fd77dcf7aa1ad66e714
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmlgrab = htmlgrab:main
    #
    # Would create an entry point called htmlgrab that calls htmlgrab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - htmlgrab
    - htmlgrab.bin
    - htmlgrab.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iepsen/python-htmlgrab
  license: MIT
  summary: 'A simple command line script to generate static web pages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
