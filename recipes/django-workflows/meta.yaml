package:
  name: django-workflows
  version: "1.1.1"

source:
  fn: django-workflows-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-workflows/django-workflows-1.1.1.tar.gz
  md5: 6bdc11fb4e932eaedf2b01ea459a5a9f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-workflows = django-workflows:main
    #
    # Would create an entry point called django-workflows that calls django-workflows.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-permissions ==1.1

  run:
    - python
    - setuptools
    - django-permissions ==1.1

test:
  # Python imports
  imports:
    - workflows

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.iqpp.de
  license: BSD License
  summary: 'Generic workflow engine for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
