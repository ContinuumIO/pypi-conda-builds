package:
  name: django-uuslug
  version: "1.0.5"

source:
  fn: django-uuslug-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uuslug/django-uuslug-1.0.5.tar.gz
  md5: ff6d0016f701da9491928e81377b654f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uuslug = django-uuslug:main
    #
    # Would create an entry point called django-uuslug that calls django-uuslug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-slugify >=0.1.0

  run:
    - python
    - python-slugify >=0.1.0

test:
  # Python imports
  imports:
    - uuslug
    - uuslug/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un33k/django-uuslug
  license: BSD License
  summary: 'A Django slugify application that guarantees uniqueness and handles unicode.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
