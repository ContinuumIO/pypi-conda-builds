package:
  name: django-secure-auth
  version: "1.2.5"

source:
  fn: django-secure-auth-1.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-secure-auth/django-secure-auth-1.2.5.tar.gz
  md5: 206fb4b169193a4812b90d634c2e811a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-secure-auth = django-secure-auth:main
    #
    # Would create an entry point called django-secure-auth that calls django-secure-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyotp ==1.3.1
    - twilio >=3.6.4
    - httpagentparser >=1.5.0
    - django-tables2 >=0.14.0
    - django-filter >=0.7
    - phonenumbers >=6.1.0
    - django-simple-captcha >=0.4.2
    - django-ipware >=0.0.8
    - slowaes ==0.1a1
    - ipaddress >=1.0.6
    - django-phonenumber-field >=0.5

  run:
    - python
    - pyotp ==1.3.1
    - twilio >=3.6.4
    - httpagentparser >=1.5.0
    - django-tables2 >=0.14.0
    - django-filter >=0.7
    - phonenumbers >=6.1.0
    - django-simple-captcha >=0.4.2
    - django-ipware >=0.0.8
    - slowaes ==0.1a1
    - ipaddress >=1.0.6
    - django-phonenumber-field >=0.5

test:
  # Python imports
  imports:
    - secureauth
    - secureauth.migrations
    - secureauth.registration
    - secureauth.sms_backends
    - secureauth.templatetags
    - secureauth.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gotlium/django-secure-auth
  license: GNU General Public License v3 (GPLv3)
  summary: 'Secure authentication by TOTP, SMS, Codes & Question'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
