package:
  name: dict.sorted
  version: "1.0.0"

source:
  fn: dict.sorted-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dict.sorted/dict.sorted-1.0.0.tar.gz
  md5: 6bdc743c886bb8ede9cae94fcc986405
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dict.sorted = dict.sorted:main
    #
    # Would create an entry point called dict.sorted that calls dict.sorted.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.3.0

  run:
    - python
    - six >=1.3.0

test:
  # Python imports
  imports:
    - sdict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lukesneeringer/dict-sorted
  license: BSD License
  summary: 'Dictionaries sorted by key or by comparison function. For Python 2 or Python 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
