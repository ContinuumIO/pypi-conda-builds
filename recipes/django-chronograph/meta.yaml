package:
  name: django-chronograph
  version: "0.3.1"

source:
  fn: django-chronograph-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chronograph/django-chronograph-0.3.1.tar.gz
  md5: 187194c7a83eb1f5f7309335ea218edd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chronograph = django-chronograph:main
    #
    # Would create an entry point called django-chronograph that calls django-chronograph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0
    - python-dateutil <=1.5

  run:
    - python
    - django >=1.0
    - python-dateutil <=1.5

test:
  # Python imports
  imports:
    - chronograph
    - chronograph.compatibility
    - chronograph.management
    - chronograph.management.commands
    - chronograph.templatetags
    - chronograph.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/wnielson/django-chronograph
  license: BSD License
  summary: 'Django chronograph application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
