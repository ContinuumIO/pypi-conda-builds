package:
  name: django-inviteme
  version: "1.0a2"

source:
  fn: django-inviteme-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inviteme/django-inviteme-1.0a2.tar.gz
  md5: 63cea6a11760839319fcb5c74376c053
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inviteme = django-inviteme:main
    #
    # Would create an entry point called django-inviteme that calls django-inviteme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demo
    - inviteme
    - inviteme.templatetags
    - inviteme.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danirus/django-inviteme/
  license: MIT License
  summary: 'Django invite-me form App with email verification'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
