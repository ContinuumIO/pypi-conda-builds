package:
  name: httpforge
  version: "11.02.01"

source:
  fn: httpforge-11.02.01.tar.gz
  url: https://pypi.python.org/packages/source/h/httpforge/httpforge-11.02.01.tar.gz
  md5: 6bbed7aca982cb872d129ad606e9f005
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpforge = httpforge:main
    #
    # Would create an entry point called httpforge that calls httpforge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - httpforge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.aspyct.org/
  license: MIT License
  summary: 'Command line tools to manipulate HTTP requests and responses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
