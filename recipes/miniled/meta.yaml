package:
  name: miniled
  version: "0-1"

source:
  fn: MiniLED-0-1.tar.gz
  url: https://pypi.python.org/packages/source/M/MiniLED/MiniLED-0-1.tar.gz
  md5: edc3b1c600c597d3825a83fa86d5bf81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - miniled = miniled:main
    #
    # Would create an entry point called miniled that calls miniled.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - MiniLED
    - miniled

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robertf224/MiniLED/
  license: UNKNOWN
  summary: 'A python API to display text/images to small LED badges and signs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
