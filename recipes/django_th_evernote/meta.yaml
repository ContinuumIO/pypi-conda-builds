package:
  name: django_th_evernote
  version: "0.3.3"

source:
  fn: django_th_evernote-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_th_evernote/django_th_evernote-0.3.3.tar.gz
  md5: 2f020260bf075770be0b49378f5d9ed0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_th_evernote = django_th_evernote:main
    #
    # Would create an entry point called django_th_evernote that calls django_th_evernote.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_th >=0.10.1
    - pytidylib6 ==0.2.2
    - arrow ==0.4.2

  run:
    - python
    - django_th >=0.10.1
    - pytidylib6 ==0.2.2
    - arrow ==0.4.2

test:
  # Python imports
  imports:
    - django_th_evernote
    - th_evernote
    - th_evernote.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/foxmask/django-th-evernote
  license: BSD License
  summary: 'From Trigger Happy, this connector provides an access to your Evernote account'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
