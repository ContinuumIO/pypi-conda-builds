package:
  name: couchbase
  version: "2.0.2"

source:
  fn: couchbase-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/couchbase/couchbase-2.0.2.tar.gz
  md5: 7f7bffd4881a82ba18e5aea66ca093df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchbase = couchbase:main
    #
    # Would create an entry point called couchbase that calls couchbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - couchbase
    - couchbase.async
    - couchbase.iops
    - couchbase.tests
    - couchbase.tests.cases
    - couchbase.views
    - gcouchbase
    - txcouchbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - nose
    - testresources >=0.2.7
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/couchbase/couchbase-python-client
  license: Apache Software License
  summary: 'Python Client for Couchbase'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
