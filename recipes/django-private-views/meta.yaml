package:
  name: django-private-views
  version: "1.1"

source:
  fn: django-private-views-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-private-views/django-private-views-1.1.tar.gz
  md5: 1e744bc8e48ea08bda4214aaf4f26a69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-private-views = django-private-views:main
    #
    # Would create an entry point called django-private-views that calls django-private-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - privateviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dabapps/django-private-views
  license: BSD
  summary: 'Site-wide login protection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
