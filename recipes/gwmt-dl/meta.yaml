package:
  name: gwmt-dl
  version: "0.1.0"

source:
  fn: gwmt-dl-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gwmt-dl/gwmt-dl-0.1.0.tar.gz
  md5: 8f420ce715e9d55565ff6e34672a1298
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gwmt-dl = gwmt-dl:main
    #
    # Would create an entry point called gwmt-dl that calls gwmt-dl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gdata >=2.0.18

  run:
    - python
    - gdata >=2.0.18

test:
  # Python imports
  imports:
    - gwmt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Tatsh/gwmt-dl
  license: LICENSE.txt
  summary: 'Downloads Google WebMaster search queries and top pages data as CSV data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
