package:
  name: mprofi_api_client
  version: "0.1.0"

source:
  fn: mprofi_api_client-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mprofi_api_client/mprofi_api_client-0.1.0.tar.gz
  md5: 833ad47fcc819b99facc01ccd0d871a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mprofi_api_client = mprofi_api_client:main
    #
    # Would create an entry point called mprofi_api_client that calls mprofi_api_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mprofi_api_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/materna/mprofi_api_client_python
  license: Apache Software License
  summary: 'Python Client library for mProfi REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
