package:
  name: garcon
  version: "0.0.3"

source:
  fn: Garcon-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/G/Garcon/Garcon-0.0.3.tar.gz
  md5: 728b20d70c567119f111fee993a224be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - garcon = garcon:main
    #
    # Would create an entry point called garcon that calls garcon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - garcon
    - tests
    - tests.fixtures
    - tests.fixtures.flows

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xethorn/garcon/
  license: MIT
  summary: 'Lightweight library for AWS SWF.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
