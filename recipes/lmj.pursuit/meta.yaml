package:
  name: lmj.pursuit
  version: "0.3.1"

source:
  fn: lmj.pursuit-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lmj.pursuit/lmj.pursuit-0.3.1.tar.gz
  md5: cdd41066a1a0708bf2294eefc5badb7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lmj.pursuit = lmj.pursuit:main
    #
    # Would create an entry point called lmj.pursuit that calls lmj.pursuit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - lmj
    - lmj.pursuit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lmjohns3/py-pursuit
  license: MIT License
  summary: 'A library of matching pursuit implementations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
