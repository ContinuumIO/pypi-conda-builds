package:
  name: cubicweb-mediaplayer
  version: "0.1.4"

source:
  fn: cubicweb-mediaplayer-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-mediaplayer/cubicweb-mediaplayer-0.1.4.tar.gz
  md5: 1b0df9e296713f13bf9dada4e4a9f5d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-mediaplayer = cubicweb-mediaplayer:main
    #
    # Would create an entry point called cubicweb-mediaplayer that calls cubicweb-mediaplayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.12
    - python-id3
    - cubicweb-file >=1.15.0

  run:
    - python
    - cubicweb >=3.17.12
    - python-id3
    - cubicweb-file >=1.15.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-mediaplayer
  license: LGPL
  summary: 'provide a schema and API for sound and media files with a html5 player'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
