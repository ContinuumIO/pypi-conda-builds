package:
  name: bitty
  version: "0.4.1"

source:
  fn: bitty-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bitty/bitty-0.4.1.tar.gz
  md5: 5854c71ebae5c12411f3064d196e9f8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitty = bitty:main
    #
    # Would create an entry point called bitty that calls bitty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bitty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/toastdriven/bitty
  license: BSD License
  summary: 'A tiny database layer. Serious Python Programmers(tm) with Enterprise Requirements need not apply.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
