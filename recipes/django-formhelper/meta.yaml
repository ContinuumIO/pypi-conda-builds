package:
  name: django-formhelper
  version: "0.2"

source:
  fn: django-formhelper-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formhelper/django-formhelper-0.2.tar.gz
  md5: 219be58a7c4c0e043e08f7c102650102
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formhelper = django-formhelper:main
    #
    # Would create an entry point called django-formhelper that calls django-formhelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formhelper
    - formhelper.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bendavis78
  license: UNKNOWN
  summary: 'Django FormHelper is a collection of templates and templatetags to ease the pain in building out web forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
