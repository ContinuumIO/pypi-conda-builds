package:
  name: instacache
  version: "0.2.3"

source:
  fn: instacache-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/i/instacache/instacache-0.2.3.tar.gz
  md5: 294200961519042089993279192ea9e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instacache = instacache:main
    #
    # Would create an entry point called instacache that calls instacache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2 >=1.5.211

  run:
    - python
    - oauth2 >=1.5.211

test:
  # Python imports
  imports:
    - instacache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thefil/instacache
  license: LICENSE.txt
  summary: 'Simple program to back up Instapaper favorites in plaintext and html form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
