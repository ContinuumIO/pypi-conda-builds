package:
  name: learn-tibetan
  version: "0.0.1.dev1"

source:
  fn: Learn-Tibetan-0.0.1.dev1.tar.gz
  url: https://pypi.python.org/packages/source/L/Learn-Tibetan/Learn-Tibetan-0.0.1.dev1.tar.gz
  md5: 7d29b10e9d40703707dbd18e154a7905
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - learn-tibetan = learn-tibetan:main
    #
    # Would create an entry point called learn-tibetan that calls learn-tibetan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygame

  run:
    - python
    - pygame

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://budismoclasico.wordpress.com/tibetano/
  license: GPL v3
  summary: 'Quiz for learn tibetan'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
