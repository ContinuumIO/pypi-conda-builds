package:
  name: imhdsk
  version: "0.0.4"

source:
  fn: imhdsk-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/imhdsk/imhdsk-0.0.4.tar.gz
  md5: 3a9082d67774aaa1e091ace9d11be01a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imhdsk = imhdsk:main
    #
    # Would create an entry point called imhdsk that calls imhdsk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - lxml

  run:
    - python
    - requests
    - lxml

test:
  # Python imports
  imports:
    - imhdsk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mrshu/python-imhdsk-api
  license: imhotep
  summary: 'An unofficial API for imhd.sk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
