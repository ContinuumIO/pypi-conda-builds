package:
  name: ircutils
  version: "0.1.4"

source:
  fn: ircutils-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/i/ircutils/ircutils-0.1.4.tar.gz
  md5: 521c7be8d53d2e255832fc7e616858cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ircutils = ircutils:main
    #
    # Would create an entry point called ircutils that calls ircutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ircutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.guardedcode.com/projects/ircutils/
  license: MIT License
  summary: 'IRC framework and utilities. Great for bot creation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
