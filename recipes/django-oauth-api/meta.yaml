package:
  name: django-oauth-api
  version: "0.4.0.post1"

source:
  fn: django-oauth-api-0.4.0.post1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauth-api/django-oauth-api-0.4.0.post1.tar.gz
  md5: 3e575cd1e856683e79a86ddff0b72266
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauth-api = django-oauth-api:main
    #
    # Would create an entry point called django-oauth-api that calls django-oauth-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - oauthlib ==0.6.0

  run:
    - python
    - django >=1.7
    - oauthlib ==0.6.0

test:
  # Python imports
  imports:
    - oauth_api
    - oauth_api.migrations
    - oauth_api.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eofs/django-oauth-api
  license: BSD License
  summary: 'OAuth API for Django using Django Rest Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
