package:
  name: mr.cython
  version: "1.0"

source:
  fn: mr.cython-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mr.cython/mr.cython-1.0.tar.gz
  md5: 960dd5ff816ec0dd5b6299558bf75ac9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.cython = mr.cython:main
    #
    # Would create an entry point called mr.cython that calls mr.cython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - mr
    - mr.cython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sendgridlabs/mr.cython
  license: BSD
  summary: 'A zc.buildout extension to befriend mr.developer and Cython.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
