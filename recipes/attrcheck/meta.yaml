package:
  name: attrcheck
  version: "0.1.2"

source:
  fn: attrcheck-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/attrcheck/attrcheck-0.1.2.tar.gz
  md5: 26b660ce2971ea1f3aee8a438e02e2a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - attrcheck = attrcheck:main
    #
    # Would create an entry point called attrcheck that calls attrcheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - attrcheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jnamika/attrcheck
  license: ISC License (ISCL)
  summary: 'A simple attribution checker implemented as a decorator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
