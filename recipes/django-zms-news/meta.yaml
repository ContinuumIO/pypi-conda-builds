package:
  name: django-zms-news
  version: "0.1"

source:
  fn: django-zms-news-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zms-news/django-zms-news-0.1.tar.gz
  md5: a36bfe7ec8b8989bab3be90ce5d34123
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zms-news = django-zms-news:main
    #
    # Would create an entry point called django-zms-news that calls django-zms-news.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-ckeditor
    - easy_thumbnails

  run:
    - python
    - django-ckeditor
    - easy_thumbnails

test:
  # Python imports
  imports:
    - news

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hisie/django_simple_news
  license: Apache Software License
  summary: 'Django news with CKEditor integrated.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
