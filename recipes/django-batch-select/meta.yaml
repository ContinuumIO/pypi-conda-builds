package:
  name: django-batch-select
  version: "0.2.4"

source:
  fn: django-batch-select-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-batch-select/django-batch-select-0.2.4.tar.gz
  md5: fe4214f01d9dd334742430b74b5f9593
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-batch-select = django-batch-select:main
    #
    # Would create an entry point called django-batch-select that calls django-batch-select.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - batch_select

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lilspikey/django-batch-select/
  license: BSD License
  summary: 'batch select many-to-many and one-to-many fields (to help avoid n+1 query problem)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
