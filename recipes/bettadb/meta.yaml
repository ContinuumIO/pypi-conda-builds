package:
  name: bettadb
  version: "0.1.3"

source:
  fn: bettadb-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bettadb/bettadb-0.1.3.tar.gz
  md5: 4c91ad85f67e611b2259b7440c5ec1f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bettadb = bettadb:main
    #
    # Would create an entry point called bettadb that calls bettadb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eventedpy

  run:
    - python
    - eventedpy

test:
  # Python imports
  imports:
    - bettadb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ideaxod/bettaDB.git
  license: UNKNOWN
  summary: 'Simple-to-use embedded database, modelled on MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
