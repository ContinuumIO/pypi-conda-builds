package:
  name: flask-alchemyview
  version: "0.1.4"

source:
  fn: Flask-AlchemyView-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-AlchemyView/Flask-AlchemyView-0.1.4.tar.gz
  md5: 153e0d83699377de8f6c468d6158cd43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-alchemyview = flask-alchemyview:main
    #
    # Would create an entry point called flask-alchemyview that calls flask-alchemyview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.8.0
    - flask-classy
    - colander
    - dictalchemy

  run:
    - python
    - sqlalchemy >=0.8.0
    - flask-classy
    - colander
    - dictalchemy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danielholmstrom/flask-alchemyview/
  license: MIT License
  summary: 'Simple ModelView for auto-generating Flask Views based on SQLAlchemy models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
