package:
  name: django-profiling
  version: "0.1"

source:
  fn: django-profiling-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-profiling/django-profiling-0.1.tar.gz
  md5: fe5e50090b1800312785afdfb1208144
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-profiling = django-profiling:main
    #
    # Would create an entry point called django-profiling that calls django-profiling.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - setuptools
    - django >=1.5

test:
  # Python imports
  imports:
    - dprofiling
    - dprofiling.backends
    - dprofiling.tests
    - dprofiling.tests.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TAMUArch/django-profiling
  license: Apache Software License
  summary: 'Django Profiling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
