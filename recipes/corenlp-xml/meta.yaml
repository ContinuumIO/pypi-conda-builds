package:
  name: corenlp-xml
  version: "1.0.2"

source:
  fn: corenlp-xml-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/corenlp-xml/corenlp-xml-1.0.2.tar.gz
  md5: 38dff8270010d5716222ab8472286888
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corenlp-xml = corenlp-xml:main
    #
    # Would create an entry point called corenlp-xml that calls corenlp-xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.10
    - bidict >=0.1.1
    - lxml >=3.2.4
    - nltk >=2.0.4
    - wsgiref >=0.1.2

  run:
    - python
    - pyyaml >=3.10
    - bidict >=0.1.1
    - lxml >=3.2.4
    - nltk >=2.0.4
    - wsgiref >=0.1.2

test:
  # Python imports
  imports:
    - corenlp_xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/relwell/corenlp-xml-lib
  license: Other
  summary: 'Library for interacting with the XML output of the Stanford CoreNLP pipeline.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
