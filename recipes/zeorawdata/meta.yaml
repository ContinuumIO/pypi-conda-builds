package:
  name: zeorawdata
  version: "2.0"

source:
  fn: ZeoRawData-2.0.tar.gz
  url: https://pypi.python.org/packages/source/Z/ZeoRawData/ZeoRawData-2.0.tar.gz
  md5: 4cca04fa25d7030ae2d7f04aee02a7bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeorawdata = zeorawdata:main
    #
    # Would create an entry point called zeorawdata that calls zeorawdata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - ZeoRawData
    - zeorawdata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sleepstreamonline.com/rdl/
  license: UNKNOWN
  summary: 'Zeo Raw Data Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
