package:
  name: django-admin-permissions
  version: "0.2.1"

source:
  fn: django-admin-permissions-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-permissions/django-admin-permissions-0.2.1.tar.gz
  md5: 81b5f78da4b580c2cba545516a76ea3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-permissions = django-admin-permissions:main
    #
    # Would create an entry point called django-admin-permissions that calls django-admin-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - admin_permissions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silentsokolov/django-admin-permissions
  license: MIT License
  summary: 'Very simple extension that adds a permissions check on the field in admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
