package:
  name: django-parsley
  version: "0.5"

source:
  fn: django-parsley-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-parsley/django-parsley-0.5.tar.gz
  md5: cd8fb14a8e62be6ecf696fe5d5c59d2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-parsley = django-parsley:main
    #
    # Would create an entry point called django-parsley that calls django-parsley.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3,<=1.7

  run:
    - python
    - django >=1.3,<=1.7

test:
  # Python imports
  imports:
    - parsley
    - parsley.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/agiliq/django-parsley
  license: BSD License
  summary: 'Django app for client-side form validation. Wrapper for parsley.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
