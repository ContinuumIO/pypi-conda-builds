package:
  name: pynakotheka
  version: "1.1.0"

source:
  fn: pynakotheka-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pynakotheka/pynakotheka-1.1.0.tar.gz
  md5: 82b44c59ecf40ee3cb3ecd64016579b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynakotheka = pynakotheka:main
    #
    # Would create an entry point called pynakotheka that calls pynakotheka.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pynakotheka

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://inigo.katxi.org/devel/pynakotheka
  license: GNU General Public License (GPL)
  summary: 'pynakotheka is a simple python script which generates static HTML photo albums.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
