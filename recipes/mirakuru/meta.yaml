package:
  name: mirakuru
  version: "0.5.0"

source:
  fn: mirakuru-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mirakuru/mirakuru-0.5.0.tar.gz
  md5: 2e8b96b5079617906210a3fe11ea7661
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mirakuru = mirakuru:main
    #
    # Would create an entry point called mirakuru that calls mirakuru.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mirakuru
    - tests
    - tests.executors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ClearcodeHQ/mirakuru
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Process executor for tests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
