package:
  name: experimental.atrefspeedup
  version: "1.0a6"

source:
  fn: experimental.atrefspeedup-1.0a6.zip
  url: https://pypi.python.org/packages/source/e/experimental.atrefspeedup/experimental.atrefspeedup-1.0a6.zip
  md5: d517bc5a25a3108f649d8f61b77c9a2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.atrefspeedup = experimental.atrefspeedup:main
    #
    # Would create an entry point called experimental.atrefspeedup that calls experimental.atrefspeedup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.testcaselayer
    - products.archetypes
    - products.cmfcore
    - products.plonetestcase
    - zope2

  run:
    - python
    - setuptools
    - setuptools
    - collective.testcaselayer
    - products.archetypes
    - products.cmfcore
    - products.plonetestcase
    - zope2

test:
  # Python imports
  imports:
    - experimental
    - experimental.atrefspeedup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jarn.com/
  license: GPL version 2
  summary: 'Speedup of the Archetypes reference engine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
