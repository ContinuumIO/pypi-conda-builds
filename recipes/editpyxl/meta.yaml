package:
  name: editpyxl
  version: "0.1.0"

source:
  fn: editpyxl-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/editpyxl/editpyxl-0.1.0.tar.gz
  md5: 87fb59c42759498bce4748a7455d3cb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - editpyxl = editpyxl:main
    #
    # Would create an entry point called editpyxl that calls editpyxl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - editpyxl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://editpyxl.readthedocs.org
  license: MIT License
  summary: 'A Python library to edit Excel 2007 xlsx/xlsm files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
