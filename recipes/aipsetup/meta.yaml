package:
  name: aipsetup
  version: "3.0.124"

source:
  fn: aipsetup-3.0.124.tar.gz
  url: https://pypi.python.org/packages/source/a/aipsetup/aipsetup-3.0.124.tar.gz
  md5: 7da7726ef2ec8facc4c9944797184c80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aipsetup = aipsetup:main
    #
    # Would create an entry point called aipsetup that calls aipsetup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - org_wayround_utils
    - certdata
    - sqlalchemy
    - bottle
    - mako

  run:
    - python
    - org_wayround_utils
    - certdata
    - sqlalchemy
    - bottle
    - mako

test:
  # Python imports
  imports:
    - aipsetup
    - org.wayround.aipsetup
    - org.wayround.aipsetup.buildtools
    - org.wayround.aipsetup.gui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AnimusPEXUS/org_wayround_aipsetup
  license: UNKNOWN
  summary: 'software tools for building and maintaining own gnu+linux distro'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
