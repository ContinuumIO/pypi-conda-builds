package:
  name: django-singleactiveobject
  version: "0.9.1"

source:
  fn: django-singleactiveobject-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-singleactiveobject/django-singleactiveobject-0.9.1.tar.gz
  md5: 01db39fecd9e4f333a9c9639c4b7fd8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-singleactiveobject = django-singleactiveobject:main
    #
    # Would create an entry point called django-singleactiveobject that calls django-singleactiveobject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - singleactiveobject
    - singleactiveobject.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/byteweaver/django-singleactiveobject
  license: django-singlerecord
  summary: 'Model mixin that ensures there is only one active model instance in your database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
