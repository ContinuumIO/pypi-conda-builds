package:
  name: django-js-error-hook
  version: "0.3"

source:
  fn: django-js-error-hook-0.3.zip
  url: https://pypi.python.org/packages/source/d/django-js-error-hook/django-js-error-hook-0.3.zip
  md5: 588fe397c2359437284ccb972f148165
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-js-error-hook = django-js-error-hook:main
    #
    # Would create an entry point called django-js-error-hook that calls django-js-error-hook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_js_error_hook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jojax/django-js-error-hook
  license: BSD License
  summary: 'Generic handler for hooking client side javascript error.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
