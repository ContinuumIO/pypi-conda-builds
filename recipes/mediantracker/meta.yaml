package:
  name: mediantracker
  version: "1.0"

source:
  fn: mediantracker-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mediantracker/mediantracker-1.0.tar.gz
  md5: 77666357b7340811a8b4d995c9ce1135
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mediantracker = mediantracker:main
    #
    # Would create an entry point called mediantracker that calls mediantracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mediantracker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/mediantracker/
  license: MIT License
  summary: 'Tracks the overall median of a stream of values "on-line" in reasonably efficient fashion.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
