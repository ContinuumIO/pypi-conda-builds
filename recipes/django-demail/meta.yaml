package:
  name: django-demail
  version: "0.3"

source:
  fn: django-demail-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-demail/django-demail-0.3.tar.gz
  md5: 6d76286b9744bf79805a4f98db64e55f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-demail = django-demail:main
    #
    # Would create an entry point called django-demail that calls django-demail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - demail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goodtune/django-demail
  license: UNKNOWN
  summary: 'Delegating email backend.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
