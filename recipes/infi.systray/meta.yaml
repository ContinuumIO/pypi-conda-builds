package:
  name: infi.systray
  version: "0.1.7"

source:
  fn: infi.systray-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.systray/infi.systray-0.1.7.tar.gz
  md5: 43564e3544aa23d85b292501dbfec470
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.systray = infi.systray:main
    #
    # Would create an entry point called infi.systray that calls infi.systray.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.systray

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.systray
  license: Python Software Foundation License
  summary: 'Windows system tray icon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
