package:
  name: django-configglue
  version: "0.7.1"

source:
  fn: django-configglue-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-configglue/django-configglue-0.7.1.tar.gz
  md5: 2c30e984ae05888d9f28ff818b6a7559
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-configglue = django-configglue:main
    #
    # Would create an entry point called django-configglue that calls django-configglue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.7
    - configglue >=1.0

  run:
    - python
    - django >=1.3.7
    - configglue >=1.0

test:
  # Python imports
  imports:
    - django_configglue
    - django_configglue.management
    - django_configglue.management.commands
    - django_configglue.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-configglue/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django commands for managing configglue generated settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
