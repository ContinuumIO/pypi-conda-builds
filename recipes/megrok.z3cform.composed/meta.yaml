package:
  name: megrok.z3cform.composed
  version: "0.1"

source:
  fn: megrok.z3cform.composed-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.z3cform.composed/megrok.z3cform.composed-0.1.tar.gz
  md5: 270d9cee67b4e09b88375c5d7f778230
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.z3cform.composed = megrok.z3cform.composed:main
    #
    # Would create an entry point called megrok.z3cform.composed that calls megrok.z3cform.composed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.form
    - megrok.z3cform.base
    - grokcore.view
    - grokcore.viewlet
    - grokcore.component
    - zope.component

  run:
    - python
    - setuptools
    - z3c.form
    - megrok.z3cform.base
    - grokcore.view
    - grokcore.viewlet
    - grokcore.component
    - zope.component

test:
  # Python imports
  imports:
    - megrok
    - megrok.z3cform
    - megrok.z3cform.composed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Composed forms for Grok, using z3c.form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
