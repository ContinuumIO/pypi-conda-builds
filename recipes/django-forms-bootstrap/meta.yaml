package:
  name: django-forms-bootstrap
  version: "3.0.1"

source:
  fn: django-forms-bootstrap-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-forms-bootstrap/django-forms-bootstrap-3.0.1.tar.gz
  md5: 9f0fab1a22dafd7faf11f4cdd18e471a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-forms-bootstrap = django-forms-bootstrap:main
    #
    # Would create an entry point called django-forms-bootstrap that calls django-forms-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_forms_bootstrap
    - django_forms_bootstrap.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pinax/django-forms-bootstrap
  license: MIT License
  summary: 'a simple form filter for using Django forms with Bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
