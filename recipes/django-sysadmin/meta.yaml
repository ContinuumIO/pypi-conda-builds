package:
  name: django-sysadmin
  version: "0.3"

source:
  fn: django-sysadmin-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sysadmin/django-sysadmin-0.3.tar.gz
  md5: a037c3ad9901bafd84e032174a829e1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sysadmin = django-sysadmin:main
    #
    # Would create an entry point called django-sysadmin that calls django-sysadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.1
    - wsgiref ==0.1.2

  run:
    - python
    - django >=1.4.1
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - django_sysadmin
    - django_sysadmin.postfix
    - django_sysadmin.proftpd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bmaeser/django-sysadmin
  license: MIT License
  summary: 'Django models to make a sysadmins life easier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
