package:
  name: django-noaa
  version: "0.2.1"

source:
  fn: django-noaa-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-noaa/django-noaa-0.2.1.tar.gz
  md5: f05d5004b5307f67f7b4597736c5b8d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-noaa = django-noaa:main
    #
    # Would create an entry point called django-noaa that calls django-noaa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0
    - fixed2csv >=0.2.0
    - django-admin-steroids >=0.2.0

  run:
    - python
    - django >=1.4.0
    - fixed2csv >=0.2.0
    - django-admin-steroids >=0.2.0

test:
  # Python imports
  imports:
    - django_noaa
    - django_noaa.management
    - django_noaa.management.commands
    - django_noaa.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisspen/django-noaa
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Django app for downloading and storing historical weather data from NOAA.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
