package:
  name: halcon
  version: "0.0.1"

source:
  fn: halcon-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/halcon/halcon-0.0.1.tar.gz
  md5: d360b148b694c072f907e3029e27cd49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - halcon = halcon:main
    #
    # Would create an entry point called halcon that calls halcon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy

  run:
    - python
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - halcon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/icaoberg/falcon
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python implementation of FALCON: Feedback Adaptive Loop for Content-Based Retrieval'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
