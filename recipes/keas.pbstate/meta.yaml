package:
  name: keas.pbstate
  version: "0.1.1"

source:
  fn: keas.pbstate-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/keas.pbstate/keas.pbstate-0.1.1.tar.gz
  md5: a354b21cd1f51bab6f3ac73dc40d8b94
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - keas.pbstate = keas.pbstate:main
    #
    # Would create an entry point called keas.pbstate that calls keas.pbstate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - protobuf

  run:
    - python
    - setuptools
    - protobuf

test:
  # Python imports
  imports:
    - keas
    - keas.pbstate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: "An object database foundation based on Google's Protocol Buffers"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
