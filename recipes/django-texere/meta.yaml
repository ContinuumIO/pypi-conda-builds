package:
  name: django-texere
  version: "0.0.1c"

source:
  fn: django-texere-0.0.1c.tar.gz
  url: https://pypi.python.org/packages/source/d/django-texere/django-texere-0.0.1c.tar.gz
  md5: 19e965fe2fdd08a99821cb91f906c85a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-texere = django-texere:main
    #
    # Would create an entry point called django-texere that calls django-texere.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5
    - texere

  run:
    - python
    - django >=1.5
    - texere

test:
  # Python imports
  imports:
    - django_texere
    - django_texere.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paetzke/django-texere
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
