package:
  name: chipster-job-manager
  version: "3.1.8"

source:
  fn: chipster-job-manager-3.1.8.tar.gz
  url: https://pypi.python.org/packages/source/c/chipster-job-manager/chipster-job-manager-3.1.8.tar.gz
  md5: ccd1073460268b14b00749c063b4c6df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chipster-job-manager = chipster-job-manager:main
    #
    # Would create an entry point called chipster-job-manager that calls chipster-job-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - stompest
    - pyyaml
    - stompest.async
    - twisted
    - sqlalchemy

  run:
    - python
    - flask
    - stompest
    - pyyaml
    - stompest.async
    - twisted
    - sqlalchemy

test:
  # Python imports
  imports:
    - jobmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Manages long running jobs on Chipster platform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
