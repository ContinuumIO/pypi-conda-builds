package:
  name: links-luxuryadmin-follit
  version: "0.1.11"

source:
  fn: links-luxuryadmin-follit-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/l/links-luxuryadmin-follit/links-luxuryadmin-follit-0.1.11.tar.gz
  md5: 0aa2ad9ca049eadb2945730e65d033e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - links-luxuryadmin-follit = links-luxuryadmin-follit:main
    #
    # Would create an entry point called links-luxuryadmin-follit that calls links-luxuryadmin-follit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - luxuryadmin
    - luxuryadmin.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linkscreative.co.uk/
  license: LICENSE.txt
  summary: 'Luxury Admin for Django (follit fork)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
