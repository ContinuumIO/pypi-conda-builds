package:
  name: hfchlevimodel
  version: "1.0.1"

source:
  fn: HfChLeviModel-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/H/HfChLeviModel/HfChLeviModel-1.0.1.tar.gz
  md5: d1fdbe028ae78c537bcf7b54fb2953dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hfchlevimodel = hfchlevimodel:main
    #
    # Would create an entry point called hfchlevimodel that calls hfchlevimodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hfchlevimodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.headfirstlabs
  license: UNKNOWN
  summary: 'the first Python webapp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
