package:
  name: products.zope2makesite
  version: "0.1"

source:
  fn: Products.zope2makesite-0.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.zope2makesite/Products.zope2makesite-0.1.tar.gz
  md5: c5da8d125be96b4b5ed7c1aaf91ec973
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.zope2makesite = products.zope2makesite:main
    #
    # Would create an entry point called products.zope2makesite that calls products.zope2makesite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.localsitemanager

  run:
    - python
    - setuptools
    - setuptools
    - five.localsitemanager

test:
  # Python imports
  imports:
    - Products
    - Products.zope2makesite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1 (http://www.zope.org/Resources/License/ZPL-2.1)
  summary: 'Makes the zope2-root a site (providing a componentregistry).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
