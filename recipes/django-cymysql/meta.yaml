package:
  name: django-cymysql
  version: "0.3.1"

source:
  fn: django-cymysql-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cymysql/django-cymysql-0.3.1.tar.gz
  md5: 6dd6f6144ed3019d4a0ff9cba259c195
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cymysql = django-cymysql:main
    #
    # Would create an entry point called django-cymysql that calls django-cymysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mysql_cymysql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nakagami/django-cymysql/
  license: BSD License
  summary: '\nDjango database backend for cymysql\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
