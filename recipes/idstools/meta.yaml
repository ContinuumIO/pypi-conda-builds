package:
  name: idstools
  version: "0.4.4"

source:
  fn: idstools-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/i/idstools/idstools-0.4.4.tar.gz
  md5: 94e218fe9fca9e785e4d682194bdc457
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - idstools = idstools:main
    #
    # Would create an entry point called idstools that calls idstools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - idstools
    - idstools.compat
    - idstools.compat.argparse
    - idstools.scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jasonish/py-idstools
  license: BSD License
  summary: 'IDS Utility Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
