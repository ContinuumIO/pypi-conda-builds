package:
  name: infrae.testbrowser
  version: "2.0.2"

source:
  fn: infrae.testbrowser-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/infrae.testbrowser/infrae.testbrowser-2.0.2.tar.gz
  md5: fb792a176e95b888cb3dbe72f9daca94
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infrae.testbrowser = infrae.testbrowser:main
    #
    # Would create an entry point called infrae.testbrowser that calls infrae.testbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cssselect
    - lxml
    - setuptools
    - zope.interface

  run:
    - python
    - cssselect
    - lxml
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - infrae
    - infrae.testbrowser
    - infrae.testbrowser.selenium
    - infrae.testbrowser.selenium.tests
    - infrae.testbrowser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://infrae.com/products/silva
  license: BSD License
  summary: 'Sane functionnal test browser for WSGI applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
