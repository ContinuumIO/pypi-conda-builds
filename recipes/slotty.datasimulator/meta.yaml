package:
  name: slotty.datasimulator
  version: "0.1dev"

source:
  fn: slotty.datasimulator-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/s/slotty.datasimulator/slotty.datasimulator-0.1dev.tar.gz
  md5: b0d43d5f9b10e6a9fe1134877bc07db3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slotty.datasimulator = slotty.datasimulator:main
    #
    # Would create an entry point called slotty.datasimulator that calls slotty.datasimulator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gunicorn
    - flask
    - wsaccel
    - setuptools
    - gevent_socketio
    - pyzmq

  run:
    - python
    - setuptools
    - gunicorn
    - flask
    - wsaccel
    - setuptools
    - gevent_socketio
    - pyzmq

test:
  # Python imports
  imports:
    - slotty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Carrera Racing Unit data simulator for slotty'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
