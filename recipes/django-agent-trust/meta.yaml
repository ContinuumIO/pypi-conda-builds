package:
  name: django-agent-trust
  version: "0.1.9"

source:
  fn: django-agent-trust-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-agent-trust/django-agent-trust-0.1.9.tar.gz
  md5: bf8edfd586abb6abba2f3769c4741b77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-agent-trust = django-agent-trust:main
    #
    # Would create an entry point called django-agent-trust that calls django-agent-trust.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_agent_trust
    - django_agent_trust.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/psagers/django-agent-trust
  license: BSD License
  summary: 'A framework for managing agent trust, such as public vs. private computers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
