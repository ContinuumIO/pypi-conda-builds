package:
  name: gooeypip
  version: "0.5"

source:
  fn: gooeypip-0.5.zip
  url: https://pypi.python.org/packages/source/g/gooeypip/gooeypip-0.5.zip
  md5: 3c9b064e2dc2a711c8b13b4a8d44e0db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gooeypip = gooeypip:main
    #
    # Would create an entry point called gooeypip that calls gooeypip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gooeypip
    - gooeypip.grammar
    - gooeypip.interpreter
    - gooeypip.pypeg2
    - gooeypip.pypeg2.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/harrise/gooey-pip
  license: UNKNOWN
  summary: 'Gooey Language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
