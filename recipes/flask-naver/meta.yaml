package:
  name: flask-naver
  version: "1.2"

source:
  fn: Flask-naver-1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-naver/Flask-naver-1.2.tar.gz
  md5: 63db96da05ca612686b7839d19b43730
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-naver = flask-naver:main
    #
    # Would create an entry point called flask-naver that calls flask-naver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - xml2dict

  run:
    - python
    - flask
    - xml2dict

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seyriz/flask-naver
  license: BSD License
  summary: 'Oauth2 wraper for Naver login'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
