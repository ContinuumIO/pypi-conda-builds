package:
  name: normdb
  version: "0.0.2"

source:
  fn: normdb-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/normdb/normdb-0.0.2.tar.gz
  md5: 90a0c217fd5485c13dae14924d59e2ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - normdb = normdb:main
    #
    # Would create an entry point called normdb that calls normdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - interfaces

  run:
    - python
    - interfaces

test:
  # Python imports
  imports:
    - norm
    - norm.backends
    - normdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joshmarshall/norm
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Database-agnostic model and storage library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
