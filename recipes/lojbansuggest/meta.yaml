package:
  name: lojbansuggest
  version: "0.2.2"

source:
  fn: lojbansuggest-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lojbansuggest/lojbansuggest-0.2.2.tar.gz
  md5: 2e8c4ea7a15d7135867cb6bd978c10dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lojbansuggest = lojbansuggest:main
    #
    # Would create an entry point called lojbansuggest that calls lojbansuggest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lojbantools

  run:
    - python
    - lojbantools

test:
  # Python imports
  imports:
    - lojbansuggest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Find common beginner mistakes in text, or extract some semantic information.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
