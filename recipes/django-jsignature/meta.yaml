package:
  name: django-jsignature
  version: "0.8"

source:
  fn: django-jsignature-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsignature/django-jsignature-0.8.tar.gz
  md5: 718e8ca123c8ca6f1ef9c9a4f7f514b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsignature = django-jsignature:main
    #
    # Would create an entry point called django-jsignature that calls django-jsignature.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - jsignature
    - jsignature.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fle/django-jsignature
  license: LPGL, see LICENSE file.
  summary: 'Use jSignature jQuery plugin in your django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
