package:
  name: seasondog
  version: "1.0.6"

source:
  fn: seasondog-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/seasondog/seasondog-1.0.6.tar.gz
  md5: e09a2eff4762e3377f8e5a82d6a96377
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - seasondog = seasondog:main
    #
    # Would create an entry point called seasondog that calls seasondog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - seasondog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/shadowprince/seasondog
  license: UNKNOWN
  summary: 'Tool for tracking your progress in watching series and playing you the next episode'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
