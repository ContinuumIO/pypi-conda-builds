package:
  name: mopidy-wampfrontend
  version: "0.1.0"

source:
  fn: Mopidy-WAMPFrontend-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-WAMPFrontend/Mopidy-WAMPFrontend-0.1.0.tar.gz
  md5: bedd351bb8b22287742b162a12220ab2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-wampfrontend = mopidy-wampfrontend:main
    #
    # Would create an entry point called mopidy-wampfrontend that calls mopidy-wampfrontend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - autobahn >=0.10.3

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - autobahn >=0.10.3

test:
  # Python imports
  imports:
    - mopidy_wampfrontend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nethack42/mopidy-wampfrontend
  license: Apache Software License
  summary: u'Mopidy extension providing a WAMP frontend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
