package:
  name: helga-newrelic
  version: "0.1.0"

source:
  fn: helga-newrelic-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-newrelic/helga-newrelic-0.1.0.tar.gz
  md5: 6d956deafd6c608a3f7fcda53f5bd0ef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-newrelic = helga-newrelic:main
    #
    # Would create an entry point called helga-newrelic that calls helga-newrelic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.2.1

  run:
    - python
    - setuptools
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - helga_newrelic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga
  license: MIT License
  summary: 'IRC bot using twisted'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
