package:
  name: kamikaze
  version: "0.1.1"

source:
  fn: kamikaze-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kamikaze/kamikaze-0.1.1.tar.gz
  md5: d4df725386c17ab431ec8a4b45dc5932
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kamikaze = kamikaze:main
    #
    # Would create an entry point called kamikaze that calls kamikaze.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argh ==0.26.1
    - asyncio-redis ==0.13.4
    - redis ==2.10.3
    - tabulate ==0.7.3
    - wheel ==0.24.0

  run:
    - python
    - argh ==0.26.1
    - asyncio-redis ==0.13.4
    - redis ==2.10.3
    - tabulate ==0.7.3
    - wheel ==0.24.0

test:
  # Python imports
  imports:
    - kamikaze

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brendanmaguire/kamikaze
  license: BSD License
  summary: 'A service for placing prioritised packages with expiry times on a queue and having a consumer notified of the packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
