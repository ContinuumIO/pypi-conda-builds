package:
  name: hgrecipe
  version: "0.9"

source:
  fn: hgrecipe-0.9.tar.gz
  url: https://pypi.python.org/packages/source/h/hgrecipe/hgrecipe-0.9.tar.gz
  md5: d39390002b07d188e0b2eb6f03ef00a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgrecipe = hgrecipe:main
    #
    # Would create an entry point called hgrecipe that calls hgrecipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial
    - mock ==0.6.0

  run:
    - python
    - setuptools
    - mercurial
    - mock ==0.6.0

test:
  # Python imports
  imports:
    - hgrecipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Buildout recipe for cloning sources from a mercurial repository.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
