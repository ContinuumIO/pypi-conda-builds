package:
  name: django-generate-scaffold
  version: "0.0.5a0"

source:
  fn: django-generate-scaffold-0.0.5a0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generate-scaffold/django-generate-scaffold-0.0.5a0.tar.gz
  md5: a4369af8cdfc4ef3dff18a3ee8b3e48a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generate-scaffold = django-generate-scaffold:main
    #
    # Would create an entry point called django-generate-scaffold that calls django-generate-scaffold.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - generate_scaffold
    - generate_scaffold.generators
    - generate_scaffold.management
    - generate_scaffold.management.commands
    - generate_scaffold.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/modocache/django-generate-scaffold
  license: MIT License
  summary: 'Generate a Django model, views, URLconf, and templates on the command line in seconds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
