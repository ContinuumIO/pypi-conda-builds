package:
  name: flask-olinauth
  version: "1.0.1"

source:
  fn: Flask-OlinAuth-1.0.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-OlinAuth/Flask-OlinAuth-1.0.1.zip
  md5: 580c3ecfe5dc99bf620c7bd65aa8b24c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-olinauth = flask-olinauth:main
    #
    # Would create an entry point called flask-olinauth that calls flask-olinauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - requests >=1.1.0

  run:
    - python
    - flask >=0.8
    - requests >=1.1.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wcdolphin/flask-olinauth
  license: BSD License
  summary: "A simple Flask extension implementing Olin's authentication"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
