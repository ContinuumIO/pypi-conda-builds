package:
  name: pythonlangutil
  version: "0.1"

source:
  fn: pythonlangutil-0.1.zip
  url: https://pypi.python.org/packages/source/p/pythonlangutil/pythonlangutil-0.1.zip
  md5: b5d443a261044d771239cd05ae334cbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythonlangutil = pythonlangutil:main
    #
    # Would create an entry point called pythonlangutil that calls pythonlangutil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pythonlangutil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ehsan-keshavarzian/pythonlangutil
  license: MIT License
  summary: 'Utilities for Python programmers with background in other languages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
