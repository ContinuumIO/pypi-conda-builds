package:
  name: django_formrenderer
  version: "2.0"

source:
  fn: django_formrenderer-2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_formrenderer/django_formrenderer-2.0.tar.gz
  md5: 2ee3f2c9f085519065b55b785310ce68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_formrenderer = django_formrenderer:main
    #
    # Would create an entry point called django_formrenderer that calls django_formrenderer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_formrenderer
    - formrenderer
    - formrenderer.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/humphrey/django_formrenderer
  license: UNKNOWN
  summary: 'A reusable django app for customising form rendering from your template.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
