package:
  name: mallet
  version: "0.1"

source:
  fn: Mallet-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/M/Mallet/Mallet-0.1.5.tar.gz
  md5: 4099225be4a7d84357dda669aeb3b7a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mallet = mallet:main
    #
    # Would create an entry point called mallet that calls mallet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mallet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tobias.mcnulty/mallet
  license: BSD
  summary: 'This package provides scripts for building and maintaining Debian-based virtual servers for web application hosting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
