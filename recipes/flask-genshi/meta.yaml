package:
  name: flask-genshi
  version: "0.5.1"

source:
  fn: Flask-Genshi-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Genshi/Flask-Genshi-0.5.1.tar.gz
  md5: 700a77251ddf036a965578897cd24864
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-genshi = flask-genshi:main
    #
    # Would create an entry point called flask-genshi that calls flask-genshi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - genshi >=0.5

  run:
    - python
    - flask
    - genshi >=0.5

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/Flask-Genshi
  license: BSD License
  summary: 'An extension to Flask for easy Genshi templating.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
