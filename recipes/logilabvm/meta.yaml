package:
  name: logilabvm
  version: "0.1"

source:
  fn: logilabvm-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/logilabvm/logilabvm-0.1.tar.gz
  md5: 7632a5b565d8fc703243b73cd99d7c8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logilabvm = logilabvm:main
    #
    # Would create an entry point called logilabvm that calls logilabvm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logilabvm
    - logilabvm.hmi
    - logilabvm.lib
    - logilabvm.lib.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.logilab.org/project/logilabvm
  license: GPL
  summary: 'useful miscellaneous modules used by Logilab projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
