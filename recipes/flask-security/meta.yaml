package:
  name: flask-security
  version: "1.7.4"

source:
  fn: Flask-Security-1.7.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Security/Flask-Security-1.7.4.tar.gz
  md5: af2a12ab15e5d360fbb242a0305d6642
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-security = flask-security:main
    #
    # Would create an entry point called flask-security that calls flask-security.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - flask-login >=0.1.3
    - flask-mail >=0.7.3
    - flask-principal >=0.3.3
    - flask-wtf >=0.8
    - itsdangerous >=0.17
    - passlib >=1.6.1

  run:
    - python
    - flask >=0.9
    - flask-login >=0.1.3
    - flask-mail >=0.7.3
    - flask-principal >=0.3.3
    - flask-wtf >=0.8
    - itsdangerous >=0.17
    - passlib >=1.6.1

test:
  # Python imports
  imports:
    - flask_security

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattupstate/flask-security
  license: MIT License
  summary: 'Simple security for Flask apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
