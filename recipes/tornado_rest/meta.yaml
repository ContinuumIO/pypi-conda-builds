package:
  name: tornado_rest
  version: "1.0"

source:
  fn: tornado_rest-1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_rest/tornado_rest-1.0.tar.gz
  md5: d220c506d59af3134cbe25d3bd9d82b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_rest = tornado_rest:main
    #
    # Would create an entry point called tornado_rest that calls tornado_rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tornado_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hobarrera/tornado_rest
  license: ISC
  summary: 'Utilitary classes used for tornado-based REST proyects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
