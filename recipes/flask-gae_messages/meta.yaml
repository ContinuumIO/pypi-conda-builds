package:
  name: flask-gae_messages
  version: "1.0.1"

source:
  fn: flask-gae_messages-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-gae_messages/flask-gae_messages-1.0.1.tar.gz
  md5: 80a32073dabb1e08c0e797c9aa752276
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-gae_messages = flask-gae_messages:main
    #
    # Would create an entry point called flask-gae_messages that calls flask-gae_messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - blinker

  run:
    - python
    - flask
    - blinker

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gregorynicholas/flask-gae_messages
  license: MIT License
  summary: 'Flask extension for working with messages using the mail & xmpp apis on App Engine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
