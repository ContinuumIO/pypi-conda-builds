package:
  name: feincms-articles
  version: "1.1.2"

source:
  fn: feincms-articles-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/feincms-articles/feincms-articles-1.1.2.tar.gz
  md5: 9793e618970eb5b5f4a60ab56b200249
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-articles = feincms-articles:main
    #
    # Would create an entry point called feincms-articles that calls feincms-articles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feincms >=1.7
    - django-mptt
    - django-pagination

  run:
    - python
    - feincms >=1.7
    - django-mptt
    - django-pagination

test:
  # Python imports
  imports:
    - articles
    - articles.extensions
    - articles.modules
    - articles.modules.category
    - articles.modules.category.extensions
    - articles.modules.category.templatetags
    - articles.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/feincms-articles/
  license: UNKNOWN
  summary: 'Provides Articles using (FeinCMS content) with categories and tags.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
