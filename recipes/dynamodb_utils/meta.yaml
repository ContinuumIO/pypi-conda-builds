package:
  name: dynamodb_utils
  version: "0.4.2"

source:
  fn: dynamodb_utils-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamodb_utils/dynamodb_utils-0.4.2.tar.gz
  md5: d7a483a550203997cba89612032b7467
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamodb_utils = dynamodb_utils:main
    #
    # Would create an entry point called dynamodb_utils that calls dynamodb_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pynamodb

  run:
    - python
    - pynamodb

test:
  # Python imports
  imports:
    - dynamodb_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamchainz/dynamodb_utils
  license: GPLv3
  summary: 'A toolchain for AWS DynamoDB to make common operations easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
