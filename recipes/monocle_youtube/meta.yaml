package:
  name: monocle_youtube
  version: "0.1.0"

source:
  fn: monocle_youtube-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle_youtube/monocle_youtube-0.1.0.tar.gz
  md5: d5c6c7680d0546c0899e86fb4a95674b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle_youtube = monocle_youtube:main
    #
    # Would create an entry point called monocle_youtube that calls monocle_youtube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monocle_youtube

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'App that plays video from youtube channel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
