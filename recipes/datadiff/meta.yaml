package:
  name: datadiff
  version: "1.1.6"

source:
  fn: datadiff-1.1.6.zip
  url: https://pypi.python.org/packages/source/d/datadiff/datadiff-1.1.6.zip
  md5: c34a690db75eead148aa5fa89e575d1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datadiff = datadiff:main
    #
    # Would create an entry point called datadiff that calls datadiff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datadiff

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/datadiff/
  license: Apache Software License
  summary: 'DataDiff is a library to provide human-readable diffs of python data structures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
