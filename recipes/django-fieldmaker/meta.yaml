package:
  name: django-fieldmaker
  version: "0.0.4"

source:
  fn: django-fieldmaker-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fieldmaker/django-fieldmaker-0.0.4.tar.gz
  md5: 89a09d616a6d01e1fb07815d3ec9f848
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fieldmaker = django-fieldmaker:main
    #
    # Would create an entry point called django-fieldmaker that calls django-fieldmaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fieldmaker
    - fieldmaker.admin
    - fieldmaker.contrib
    - fieldmaker.contrib.oembed
    - fieldmaker.contrib.recaptcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cuker/
  license: New BSD License
  summary: 'Dynamic form management in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
