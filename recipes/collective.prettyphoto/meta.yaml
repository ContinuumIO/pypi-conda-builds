package:
  name: collective.prettyphoto
  version: "0.5"

source:
  fn: collective.prettyphoto-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.prettyphoto/collective.prettyphoto-0.5.zip
  md5: 5d57d20d8ae98c8c59eef375bbf7191f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.prettyphoto = collective.prettyphoto:main
    #
    # Would create an entry point called collective.prettyphoto that calls collective.prettyphoto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.prettyphoto
    - collective.prettyphoto.Extensions
    - collective.prettyphoto.browser
    - collective.prettyphoto.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.prettyphoto
  license: GPL
  summary: 'prettyPhoto integration for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
