package:
  name: django-password-reset
  version: "0.7"

source:
  fn: django-password-reset-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-password-reset/django-password-reset-0.7.tar.gz
  md5: 7ff5bfc2ae927ce8f3fe106cd019f7d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-password-reset = django-password-reset:main
    #
    # Would create an entry point called django-password-reset that calls django-password-reset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - password_reset
    - password_reset.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brutasse/django-password-reset
  license: BSD License
  summary: 'Class-based views for password reset.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
