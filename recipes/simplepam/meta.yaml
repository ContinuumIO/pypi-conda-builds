package:
  name: simplepam
  version: "0.1.5"

source:
  fn: simplepam-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/s/simplepam/simplepam-0.1.5.tar.gz
  md5: 5454db514625bbfa3faed2277754c92a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplepam = simplepam:main
    #
    # Would create an entry point called simplepam that calls simplepam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simplepam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leonnnn/python3-simplepam
  license: MIT
  summary: 'An interface to the Pluggable Authentication Modules (PAM) library on linux, written in pure python (using ctypes)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
