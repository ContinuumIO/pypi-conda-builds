package:
  name: bambu-ajax
  version: "0.1"

source:
  fn: bambu-ajax-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-ajax/bambu-ajax-0.1.tar.gz
  md5: 301a86f592396932aed815aa260cd2be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-ajax = bambu-ajax:main
    #
    # Would create an entry point called bambu-ajax that calls bambu-ajax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bambu.ajax
    - bambu.ajax.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-ajax
  license: UNKNOWN
  summary: 'AJAX utility functions for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
