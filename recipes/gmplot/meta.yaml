package:
  name: gmplot
  version: "1.0.5"

source:
  fn: gmplot-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gmplot/gmplot-1.0.5.tar.gz
  md5: 44847db0f0fe467f8c49c09e4813a1b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gmplot = gmplot:main
    #
    # Would create an entry point called gmplot that calls gmplot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - gmplot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vgm64/gmplot
  license: MIT
  summary: 'Provide a matplotlib like interface to plotting data with Google Maps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
