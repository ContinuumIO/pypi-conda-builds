package:
  name: dojango
  version: "0.5.8"

source:
  fn: dojango-0.5.8.tar.gz
  url: https://pypi.python.org/packages/source/d/dojango/dojango-0.5.8.tar.gz
  md5: e198742b36f5c3b841ab04fb9ef3d32f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dojango = dojango:main
    #
    # Would create an entry point called dojango that calls dojango.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dojango
    - dojango.appengine
    - dojango.conf
    - dojango.contrib
    - dojango.contrib.auth
    - dojango.data
    - dojango.data.modelstore
    - dojango.data.piston
    - dojango.forms
    - dojango.management
    - dojango.management.commands
    - dojango.templatetags
    - dojango.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dojango/
  license: License :: OSI Approved :: BSD License
  summary: 'Reusable django application that helps you to use the client-side framework dojo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
