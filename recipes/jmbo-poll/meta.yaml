package:
  name: jmbo-poll
  version: "2.0.0a1"

source:
  fn: jmbo-poll-2.0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-poll/jmbo-poll-2.0.0a1.tar.gz
  md5: 3e1083df3b94ce8f2360a44d672e8347
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-poll = jmbo-poll:main
    #
    # Would create an entry point called jmbo-poll that calls jmbo-poll.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmbo ==2.0.0a2

  run:
    - python
    - jmbo ==2.0.0a2

test:
  # Python imports
  imports:
    - poll
    - poll.migrations
    - poll.templatetags
    - poll.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-poll
  license: BSD License
  summary: 'Polling app for Jmbo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
