package:
  name: kagiso_flake8
  version: "0.1"

source:
  fn: kagiso_flake8-0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kagiso_flake8/kagiso_flake8-0.1.tar.gz
  md5: eb779a08b081b9519560be86d3607ac7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kagiso_flake8 = kagiso_flake8:main
    #
    # Would create an entry point called kagiso_flake8 that calls kagiso_flake8.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8
    - pep8-naming
    - flake8-import-order
    - flake8-quotes

  run:
    - python
    - flake8
    - pep8-naming
    - flake8-import-order
    - flake8-quotes

test:
  # Python imports
  imports:
    - kagiso_flake8

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
