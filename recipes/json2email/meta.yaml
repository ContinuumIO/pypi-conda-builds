package:
  name: json2email
  version: "0.0.6"

source:
  fn: json2email-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/j/json2email/json2email-0.0.6.tar.gz
  md5: 961b92b48fd55011260d7343124b4a52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json2email = json2email:main
    #
    # Would create an entry point called json2email that calls json2email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - json2email

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sanger-pathogens/json2email
  license: GNU General Public License v3 (GPLv3)
  summary: 'Takes a jinja2 template and some json and sends an email'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
