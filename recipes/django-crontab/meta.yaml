package:
  name: django-crontab
  version: "0.6.0"

source:
  fn: django-crontab-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crontab/django-crontab-0.6.0.tar.gz
  md5: 49cbed75c9f20ab32fa785d13cf93323
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crontab = django-crontab:main
    #
    # Would create an entry point called django-crontab that calls django-crontab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_crontab
    - django_crontab.management
    - django_crontab.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kraiz/django-crontab
  license: MIT License
  summary: 'dead simple crontab powered job scheduling for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
