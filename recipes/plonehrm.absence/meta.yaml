package:
  name: plonehrm.absence
  version: "1.5"

source:
  fn: plonehrm.absence-1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/plonehrm.absence/plonehrm.absence-1.5.tar.gz
  md5: af4da6595c9587bb4e16460e2ca43ccf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonehrm.absence = plonehrm.absence:main
    #
    # Would create an entry point called plonehrm.absence that calls plonehrm.absence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.plonehrm >=2.7

  run:
    - python
    - setuptools
    - setuptools
    - products.plonehrm >=2.7

test:
  # Python imports
  imports:
    - plonehrm
    - plonehrm.absence
    - plonehrm.absence.browser
    - plonehrm.absence.content
    - plonehrm.absence.notifications
    - plonehrm.absence.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Register employee absence'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
