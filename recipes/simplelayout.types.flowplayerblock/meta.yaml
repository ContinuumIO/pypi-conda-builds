package:
  name: simplelayout.types.flowplayerblock
  version: "1.1.0"

source:
  fn: simplelayout.types.flowplayerblock-1.1.0.zip
  url: https://pypi.python.org/packages/source/s/simplelayout.types.flowplayerblock/simplelayout.types.flowplayerblock-1.1.0.zip
  md5: 9c47c62f468fdd24d1892dc418d29510
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplelayout.types.flowplayerblock = simplelayout.types.flowplayerblock:main
    #
    # Would create an entry point called simplelayout.types.flowplayerblock that calls simplelayout.types.flowplayerblock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplelayout.base
    - collective.flowplayer
    - ftw.upgrade

  run:
    - python
    - setuptools
    - setuptools
    - simplelayout.base
    - collective.flowplayer
    - ftw.upgrade

test:
  # Python imports
  imports:
    - simplelayout
    - simplelayout.types
    - simplelayout.types.flowplayerblock
    - simplelayout.types.flowplayerblock.tests
    - simplelayout.types.flowplayerblock.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/simplelayout.types.flowplayerblock
  license: GPL2
  summary: 'Flow-player support for simplelayout file blocks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
