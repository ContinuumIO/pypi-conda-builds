package:
  name: django-bfm
  version: "0.6"

source:
  fn: django-bfm-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bfm/django-bfm-0.6.tar.gz
  md5: 6dc03d12045843ac02f370ea94840d93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bfm = django-bfm:main
    #
    # Would create an entry point called django-bfm that calls django-bfm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_bfm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simukis/django-bfm
  license: Apache Software License
  summary: 'Basic Django File Manager with multifile uploads, live file status, and file deleting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
