package:
  name: django-document-library
  version: "1.5.1"

source:
  fn: django-document-library-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-document-library/django-document-library-1.5.1.tar.gz
  md5: f1bef97083987a115df8a9df507b47f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-document-library = django-document-library:main
    #
    # Would create an entry point called django-document-library that calls django-document-library.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-libs
    - python-dateutil

  run:
    - python
    - django
    - django-libs
    - python-dateutil

test:
  # Python imports
  imports:
    - document_library
    - document_library.migrations
    - document_library.templatetags
    - document_library.tests
    - document_library.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-document-library
  license: The MIT License
  summary: 'Django app to manage multilingual documents based on django-filer.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
