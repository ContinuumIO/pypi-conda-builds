package:
  name: django-cms-contactform
  version: "0.2"

source:
  fn: django-cms-contactform-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-contactform/django-cms-contactform-0.2.tar.gz
  md5: ca411fe6e6ad676ebe3aa0e304e31927
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-contactform = django-cms-contactform:main
    #
    # Would create an entry point called django-cms-contactform that calls django-cms-contactform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.6

  run:
    - python
    - django >=1.4,<1.6

test:
  # Python imports
  imports:
    - cmsplugin_contactform
    - cmsplugin_contactform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nagyv/django-cms-contactform
  license: MIT License
  summary: u'A simple contact us plugin for django-cms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
