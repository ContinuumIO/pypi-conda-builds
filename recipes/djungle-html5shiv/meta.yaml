package:
  name: djungle-html5shiv
  version: "0.0.0"

source:
  fn: djungle-html5shiv-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djungle-html5shiv/djungle-html5shiv-0.0.0.tar.gz
  md5: a67236f71d6030b2e3d0484d47b1dcc0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djungle-html5shiv = djungle-html5shiv:main
    #
    # Would create an entry point called djungle-html5shiv that calls djungle-html5shiv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - djungle
    - djungle.html5shiv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djungle/djungle-html5shiv
  license: GNU General Public License (GPL)
  summary: 'html5shiv as Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
