package:
  name: bandwidthtracker
  version: "1.0.1"

source:
  fn: bandwidthTracker-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bandwidthTracker/bandwidthTracker-1.0.1.tar.gz
  md5: d29381d824097af2eab9adea321dd709
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bandwidthtracker = bandwidthtracker:main
    #
    # Would create an entry point called bandwidthtracker that calls bandwidthtracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bandwidthtracker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.cardiffgiant.org
  license: UNKNOWN
  summary: 'A class with methods to interface with a rrd file containing data on bandwidth usage via /proc/net/dev.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
