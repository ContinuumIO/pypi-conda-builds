package:
  name: pubnub
  version: "3.7.1"

source:
  fn: pubnub-3.7.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pubnub/pubnub-3.7.1.tar.gz
  md5: 3036427f9a8d7d1903f8f54ffd28f2e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pubnub = pubnub:main
    #
    # Would create an entry point called pubnub that calls pubnub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto >=2.6.1
    - requests >=2.4.0

  run:
    - python
    - pycrypto >=2.6.1
    - requests >=2.4.0

test:
  # Python imports
  imports:
    - pubnub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pubnub.com
  license: MIT License
  summary: 'PubNub Real-time push service in the cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
