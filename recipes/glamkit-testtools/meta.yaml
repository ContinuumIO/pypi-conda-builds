package:
  name: glamkit-testtools
  version: "0.1"

source:
  fn: glamkit-testtools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/glamkit-testtools/glamkit-testtools-0.1.tar.gz
  md5: 52a1d47c29af9fa079dc9be6f8cfebd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glamkit-testtools = glamkit-testtools:main
    #
    # Would create an entry point called glamkit-testtools that calls glamkit-testtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - testtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Improvements for the Django testing framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
