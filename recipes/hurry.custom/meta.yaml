package:
  name: hurry.custom
  version: "0.6.2"

source:
  fn: hurry.custom-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.custom/hurry.custom-0.6.2.tar.gz
  md5: 625b638fa2f6b6dc3b04819885c7fa3e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.custom = hurry.custom:main
    #
    # Would create an entry point called hurry.custom that calls hurry.custom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.hookable
    - jsontemplate
    - zope.configuration

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.hookable
    - jsontemplate
    - zope.configuration

test:
  # Python imports
  imports:
    - hurry
    - hurry.custom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL 2.1
  summary: 'A framework for allowing customizing templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
