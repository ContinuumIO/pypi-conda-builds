package:
  name: zope.principalregistry
  version: "4.0.0"

source:
  fn: zope.principalregistry-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.principalregistry/zope.principalregistry-4.0.0.tar.gz
  md5: c4de04558e567dde268095f15a940638
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.principalregistry = zope.principalregistry:main
    #
    # Would create an entry point called zope.principalregistry that calls zope.principalregistry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.authentication
    - zope.component
    - zope.interface
    - zope.password
    - zope.security

  run:
    - python
    - setuptools
    - zope.authentication
    - zope.component
    - zope.interface
    - zope.password
    - zope.security

test:
  # Python imports
  imports:
    - zope
    - zope.principalregistry
    - zope.principalregistry.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.principalregistry
  license: Zope Public License
  summary: 'Global principal registry component for Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
