package:
  name: gae-pesapal
  version: "0.0.2"

source:
  fn: gae-pesapal-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gae-pesapal/gae-pesapal-0.0.2.tar.gz
  md5: 5f82d3debe9823a5ac6e1584a8d66eb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gae-pesapal = gae-pesapal:main
    #
    # Would create an entry point called gae-pesapal that calls gae-pesapal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gae_pesapal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kelonye/gae-pesapal
  license: MIT
  summary: 'Pesapal NDB Model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
