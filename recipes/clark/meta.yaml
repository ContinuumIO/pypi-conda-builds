package:
  name: clark
  version: "0.1.0"

source:
  fn: clark-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/clark/clark-0.1.0.tar.gz
  md5: 558770431faed802e9f1c521935873ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clark = clark:main
    #
    # Would create an entry point called clark that calls clark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - wraptor
    - argparse

  run:
    - python
    - simplejson
    - wraptor
    - argparse

test:
  # Python imports
  imports:
    - clark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/carlsverre/clark
  license: MIT License
  summary: 'Utility library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
