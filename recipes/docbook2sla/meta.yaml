package:
  name: docbook2sla
  version: "0.0.16"

source:
  fn: docbook2sla-0.0.16.tar.gz
  url: https://pypi.python.org/packages/source/d/docbook2sla/docbook2sla-0.0.16.tar.gz
  md5: 11f814a7a370bd80e8c3ba5c1d5454cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docbook2sla = docbook2sla:main
    #
    # Would create an entry point called docbook2sla that calls docbook2sla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - zope.interface

  run:
    - python
    - setuptools
    - lxml
    - zope.interface

test:
  # Python imports
  imports:
    - docbook2sla
    - docbook2sla.tests
    - docbook2sla.tests.data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://scribus.zmag.de
  license: GNU General Public License (GPL)
  summary: 'A Python interface to convert DocBook to Scribus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
