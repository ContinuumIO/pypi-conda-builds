package:
  name: pyramid_duh
  version: "0.1.2"

source:
  fn: pyramid_duh-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_duh/pyramid_duh-0.1.2.tar.gz
  md5: fcadce0426a8da8b5d1cf07e3357f1f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_duh = pyramid_duh:main
    #
    # Would create an entry point called pyramid_duh that calls pyramid_duh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - six

  run:
    - python
    - pyramid
    - six

test:
  # Python imports
  imports:
    - pyramid_duh

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyramid-duh.readthedocs.org/
  license: MIT License
  summary: 'Useful utilities for every pyramid app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
