package:
  name: products.scrawl
  version: "2.0.2"

source:
  fn: Products.Scrawl-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.Scrawl/Products.Scrawl-2.0.2.tar.gz
  md5: c04c0487ad0360f6124e821cf4cd5e88
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.scrawl = products.scrawl:main
    #
    # Would create an entry point called products.scrawl that calls products.scrawl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.Scrawl
    - Products.Scrawl.Extensions
    - Products.Scrawl.browser
    - Products.Scrawl.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/scrawl
  license: GNU General Public License (GPL)
  summary: 'Scrawl is a dirt-simple blog product for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
