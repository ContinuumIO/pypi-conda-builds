package:
  name: etk.docking
  version: "0.2"

source:
  fn: etk.docking-0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/etk.docking/etk.docking-0.2.tar.gz
  md5: ec6249aee6cd210bbdffd7bbe839e0a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etk.docking = etk.docking:main
    #
    # Would create an entry point called etk.docking that calls etk.docking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplegeneric >=0.6

  run:
    - python
    - setuptools
    - simplegeneric >=0.6

test:
  # Python imports
  imports:
    - etk
    - etk.docking

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dieterv/etk.docking/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'PyGTK Docking Widgets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
