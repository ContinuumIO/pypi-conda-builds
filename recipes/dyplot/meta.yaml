package:
  name: dyplot
  version: "0.7.006"

source:
  fn: dyplot-0.7.006.zip
  url: https://pypi.python.org/packages/source/d/dyplot/dyplot-0.7.006.zip
  md5: 7649ab0adb215baae2f6a2cb655e3e42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dyplot = dyplot:main
    #
    # Would create an entry point called dyplot that calls dyplot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas >=0.7.3

  run:
    - python
    - pandas >=0.7.3

test:
  # Python imports
  imports:
    - dyplot
    - dyplot.c3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/dyplot/
  license: LICENSE.txt
  summary: 'matplotlib-like plot functions for dygraphs.js and c3.js.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
