package:
  name: django-dynamicpages
  version: "0.1"

source:
  fn: django-dynamicpages-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamicpages/django-dynamicpages-0.1.tar.gz
  md5: 4fc463d450e457fe423a6da2ddd7f224
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamicpages = django-dynamicpages:main
    #
    # Would create an entry point called django-dynamicpages that calls django-dynamicpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dynamic_pages
    - dynamic_pages.context_processors
    - dynamic_pages.dynamic
    - dynamic_pages.form
    - dynamic_pages.middleware
    - dynamic_pages.templatetags
    - dynamic_pages.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matllubos/django-dynamicpages
  license: GNU General Public License (GPL)
  summary: 'Application which allows dynamically change urls.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
