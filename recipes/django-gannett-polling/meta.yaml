package:
  name: django-gannett-polling
  version: "1.1.46"

source:
  fn: django-gannett-polling-1.1.46.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gannett-polling/django-gannett-polling-1.1.46.tar.gz
  md5: cb16d0413bf05b05f2d2f79d585ca8ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gannett-polling = django-gannett-polling:main
    #
    # Would create an entry point called django-gannett-polling that calls django-gannett-polling.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - crowdsourcing
    - crowdsourcing.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: 'Django app for collecting and displaying surveys.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
