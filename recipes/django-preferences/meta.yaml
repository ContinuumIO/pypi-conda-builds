package:
  name: django-preferences
  version: "0.1"

source:
  fn: django-preferences-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-preferences/django-preferences-0.1.tar.gz
  md5: 48b5eda0e8019bfbd3e5a621040232a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-preferences = django-preferences:main
    #
    # Would create an entry point called django-preferences that calls django-preferences.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - preferences
    - preferences.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-preferences
  license: BSD License
  summary: 'Django app allowing users to set app specific preferences through the admin interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
