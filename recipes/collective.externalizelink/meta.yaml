package:
  name: collective.externalizelink
  version: "0.1.0"

source:
  fn: collective.externalizelink-0.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.externalizelink/collective.externalizelink-0.1.0.zip
  md5: 46b359a199f918076c93bfedc2db0e57
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.externalizelink = collective.externalizelink:main
    #
    # Would create an entry point called collective.externalizelink that calls collective.externalizelink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.registry
    - collective.jsconfiguration
    - collective.regjsonify

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.registry
    - collective.jsconfiguration
    - collective.regjsonify

test:
  # Python imports
  imports:
    - collective
    - collective.externalizelink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keul/collective.externalizelink
  license: GNU General Public License (GPL)
  summary: 'Controls when Plone must open links in external page (in an accessible way)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
