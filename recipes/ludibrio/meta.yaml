package:
  name: ludibrio
  version: "3.1.0"

source:
  fn: ludibrio-3.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ludibrio/ludibrio-3.1.0.tar.gz
  md5: d83f752b7cad8baa9c6b6aa92605cab3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ludibrio = ludibrio:main
    #
    # Would create an entry point called ludibrio that calls ludibrio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ludibrio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nsigustavo.blogspot.com
  license: GPL
  summary: 'Platform for test doubles in Python (mocks, stubs, and dummies)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
