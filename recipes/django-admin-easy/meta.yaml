package:
  name: django-admin-easy
  version: "0.2.2.1"

source:
  fn: django-admin-easy-0.2.2.1.zip
  url: https://pypi.python.org/packages/source/d/django-admin-easy/django-admin-easy-0.2.2.1.zip
  md5: 4c502cec0bc3fae15532a5b40e75bea8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-easy = django-admin-easy:main
    #
    # Would create an entry point called django-admin-easy that calls django-admin-easy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - easy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ebertti/django-admin-easy/
  license: MIT License
  summary: 'Collection of admin fields and decorators to help to create computed or custom fields more friendly and easy way'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
