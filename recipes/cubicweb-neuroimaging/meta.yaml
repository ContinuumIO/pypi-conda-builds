package:
  name: cubicweb-neuroimaging
  version: "0.7.0"

source:
  fn: cubicweb-neuroimaging-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-neuroimaging/cubicweb-neuroimaging-0.7.0.tar.gz
  md5: fada32c5d5221bfb35c36b67b852108d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-neuroimaging = cubicweb-neuroimaging:main
    #
    # Would create an entry point called cubicweb-neuroimaging that calls cubicweb-neuroimaging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18.0

  run:
    - python
    - cubicweb >=3.18.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-neuroimaging
  license: LGPL
  summary: 'Cube for neuroimaging data. Is part of Brainomics Project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
