package:
  name: dagpype
  version: "0.1.5.1"

source:
  fn: DAGPype-0.1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DAGPype/DAGPype-0.1.5.1.tar.gz
  md5: 9c819b82ab98bf1e3b3bdd4d6f56b4b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dagpype = dagpype:main
    #
    # Would create an entry point called dagpype that calls dagpype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dagpype
    - dagpype.np
    - dagpype.plot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/DAGPype
  license: BSD License
  summary: 'Low-footprint flexible data-processing and data-preparation pipelines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
