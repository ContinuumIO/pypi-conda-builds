package:
  name: django-startappextracontext
  version: "0.1"

source:
  fn: django-startappextracontext-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-startappextracontext/django-startappextracontext-0.1.tar.gz
  md5: 5aac6e3a1cb19e1a308a3acf5ca31c52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-startappextracontext = django-startappextracontext:main
    #
    # Would create an entry point called django-startappextracontext that calls django-startappextracontext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - startappextracontext
    - startappextracontext.management
    - startappextracontext.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/saevarom/startappextracontext
  license: MIT License
  summary: 'A small django app that provides an enhanced startapp management command. \n              An extra parameter called extra_context is supplied to allow for more flexible \n              app templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
