package:
  name: kiste
  version: "0.0.3.14"

source:
  fn: kiste-0.0.3.14.tar.gz
  url: https://pypi.python.org/packages/source/k/kiste/kiste-0.0.3.14.tar.gz
  md5: 5651abf6fddd852a15e31d0e2c99b466
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kiste = kiste:main
    #
    # Would create an entry point called kiste that calls kiste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kiste

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/igraltist/kiste
  license: GNU General Public License v3 (GPLv3)
  summary: 'Helper scripts for my linux system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
