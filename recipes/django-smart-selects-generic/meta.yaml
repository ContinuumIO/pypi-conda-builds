package:
  name: django-smart-selects-generic
  version: "0.1.0"

source:
  fn: django-smart-selects-generic-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-smart-selects-generic/django-smart-selects-generic-0.1.0.zip
  md5: 086f82527c40f0c1742a1f4c2154437e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smart-selects-generic = django-smart-selects-generic:main
    #
    # Would create an entry point called django-smart-selects-generic that calls django-smart-selects-generic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-smart-selects >=1.0.5,<2.0

  run:
    - python
    - django-smart-selects >=1.0.5,<2.0

test:
  # Python imports
  imports:
    - smart_generic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/suvitorg/django-smart-selects-generic
  license: UNKNOWN
  summary: 'Django application to handle chained generic model fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
