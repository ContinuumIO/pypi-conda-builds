package:
  name: django-bft
  version: "0.2.0"

source:
  fn: django-bft-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bft/django-bft-0.2.0.tar.gz
  md5: a6a40fe7ceb7531fb4f3418930f65dfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bft = django-bft:main
    #
    # Would create an entry point called django-bft that calls django-bft.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bft
    - bft.management
    - bft.management.commands
    - bft.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/django-bft/dango-bft
  license: GNU General Public License (GPL)
  summary: 'Big File Transfer Application written in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
