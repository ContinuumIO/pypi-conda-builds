package:
  name: django-digest
  version: "1.13"

source:
  fn: django-digest-1.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-digest/django-digest-1.13.tar.gz
  md5: ecc57d543f601dab89c4f4431fe76ccf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-digest = django-digest:main
    #
    # Would create an entry point called django-digest that calls django-digest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_digest
    - django_digest.backend
    - django_digest.migrations
    - django_digest.test
    - django_digest.test.methods

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/akoha/django-digest/
  license: BSD License
  summary: 'An implementation of HTTP Digest Authentication for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
