package:
  name: hostb_client
  version: "6"

source:
  fn: hostb_client-6.tar.gz
  url: https://pypi.python.org/packages/source/h/hostb_client/hostb_client-6.tar.gz
  md5: ce0e35c91e536a93e74ada50c9a3da81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hostb_client = hostb_client:main
    #
    # Would create an entry point called hostb_client that calls hostb_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hostb_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://r-w-x.org/?p=hostb_client.git;a=summary
  license: GNU General Public License (GPL)
  summary: 'hostb_client is a command line client for hostb.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
