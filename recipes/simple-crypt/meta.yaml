package:
  name: simple-crypt
  version: "4.0.0"

source:
  fn: simple-crypt-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simple-crypt/simple-crypt-4.0.0.tar.gz
  md5: 2ea3dd3e68d59f0ec5edaa454cd122e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple-crypt = simple-crypt:main
    #
    # Would create an entry point called simple-crypt that calls simple-crypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pycrypto

  run:
    - python
    - pycrypto

test:
  # Python imports
  imports:
    - simplecrypt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andrewcooke/simple-crypt
  license: UNKNOWN
  summary: 'Simple, secure encryption and decryption for Python 2.7 and 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
