package:
  name: setuptools_hg
  version: "0.4"

source:
  fn: setuptools_hg-0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools_hg/setuptools_hg-0.4.tar.gz
  md5: 1435518e523f2d2296294c9abc37a7c7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools_hg = setuptools_hg:main
    #
    # Would create an entry point called setuptools_hg that calls setuptools_hg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - setuptools_hg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jezdez/setuptools_hg/
  license: GNU General Public License (GPL)
  summary: 'Setuptools/distribute plugin for finding files under Mercurial version control.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
