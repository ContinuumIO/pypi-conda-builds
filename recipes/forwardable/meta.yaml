package:
  name: forwardable
  version: "0.3.1"

source:
  fn: forwardable-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/f/forwardable/forwardable-0.3.1.tar.gz
  md5: 9245b87ba3b01fe26202d26c81188a8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - forwardable = forwardable:main
    #
    # Would create an entry point called forwardable that calls forwardable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - forwardable
    - forwardable.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/5long/forwardable
  license: MIT License
  summary: "Forwardable as in Ruby's stdlib"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
