package:
  name: mother-webapps-framework
  version: "0.1.0"

source:
  fn: mother-webapps-framework-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mother-webapps-framework/mother-webapps-framework-0.1.0.tar.gz
  md5: ccc6cc256f2baff79a253d71a7fc8d5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mother-webapps-framework = mother-webapps-framework:main
    #
    # Would create an entry point called mother-webapps-framework that calls mother-webapps-framework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mother

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://devedge.bour.cc/wiki/Mother/
  license: GNU Affero General Public License v3
  summary: 'Mother - Web Applications Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
