package:
  name: jinja2-alchemy-starter
  version: "0.3.1"

source:
  fn: jinja2_alchemy_starter-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja2-alchemy-starter/jinja2_alchemy_starter-0.3.1.tar.gz
  md5: f01eccdeab695e03aae54679f448ea88
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja2-alchemy-starter = jinja2-alchemy-starter:main
    #
    # Would create an entry point called jinja2-alchemy-starter that calls jinja2-alchemy-starter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid >=1.4

  run:
    - python
    - setuptools
    - pyramid >=1.4

test:
  # Python imports
  imports:
    - jinja2_alchemy_starter
    - jinja2_alchemy_starter.scaffolds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Pyramid Scaffold for getting started with SQLAlchemy ORM and Jinja2 Templating Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
