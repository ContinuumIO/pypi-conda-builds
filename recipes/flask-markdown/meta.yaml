package:
  name: flask-markdown
  version: "0.3"

source:
  fn: Flask-Markdown-0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Markdown/Flask-Markdown-0.3.tar.gz
  md5: 37bf5cf3171adb8ba8d2235cb4ef049f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-markdown = flask-markdown:main
    #
    # Would create an entry point called flask-markdown that calls flask-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown

  run:
    - python
    - markdown

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcolish/flask-markdown
  license: BSD License
  summary: 'Small extension to make using markdown easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
