package:
  name: fuggit
  version: "0.0.3"

source:
  fn: fuggit-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fuggit/fuggit-0.0.3.tar.gz
  md5: bdda5c6472129e22714b474f7bb95840
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuggit = fuggit:main
    #
    # Would create an entry point called fuggit that calls fuggit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fuggit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: "For managing that one annoying remote file you can't put under version control"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
