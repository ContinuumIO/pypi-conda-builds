package:
  name: genderizer
  version: "0.1.2.3"

source:
  fn: genderizer-0.1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/g/genderizer/genderizer-0.1.2.3.tar.gz
  md5: 9a7f1c6ca76990c24caf9dbd04e5faf1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genderizer = genderizer:main
    #
    # Would create an entry point called genderizer that calls genderizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - genderizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/muatik/genderizer
  license: MIT
  summary: 'Genderizer tries to infer gender information looking at first name and/or making text analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
