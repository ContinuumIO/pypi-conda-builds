package:
  name: docopt-unicode
  version: "0.6.1"

source:
  fn: docopt-unicode-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/docopt-unicode/docopt-unicode-0.6.1.tar.gz
  md5: de1d3a7726d421654e1a85c37da3be1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docopt-unicode = docopt-unicode:main
    #
    # Would create an entry point called docopt-unicode that calls docopt-unicode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://docopt.org
  license: MIT License
  summary: 'Pythonic argument parser, that will make you smile'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
