package:
  name: bold_retriever
  version: "1.0.0"

source:
  fn: bold_retriever-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bold_retriever/bold_retriever-1.0.0.tar.gz
  md5: 8bed9f1bd826f533f39fa958e0b64765
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bold_retriever = bold_retriever:main
    #
    # Would create an entry point called bold_retriever that calls bold_retriever.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bold_retriever

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlosp420/bold_retriever
  license: GNU General Public License v3 (GPLv3)
  summary: 'It queries the BOLD database to get identification of taxa based on COI sequences'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
