package:
  name: shake-images
  version: "0.4"

source:
  fn: Shake-Images-0.4.tar.gz
  url: https://pypi.python.org/packages/source/S/Shake-Images/Shake-Images-0.4.tar.gz
  md5: f773ed638f46ab12e1cbd7b2b5e30cb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shake-images = shake-images:main
    #
    # Would create an entry point called shake-images that calls shake-images.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - shake
    - pil

  run:
    - python
    - shake
    - pil

test:
  # Python imports
  imports:
    - shake_images

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lucuma/Shake-Mail
  license: MIT License
  summary: 'Utilities to simplify processing images and making thumbnails in your Shake app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
