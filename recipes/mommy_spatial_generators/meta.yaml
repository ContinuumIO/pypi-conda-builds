package:
  name: mommy_spatial_generators
  version: "0.1.2"

source:
  fn: mommy_spatial_generators-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mommy_spatial_generators/mommy_spatial_generators-0.1.2.tar.gz
  md5: 0ff285624b6f73ea93058eae234f0809
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mommy_spatial_generators = mommy_spatial_generators:main
    #
    # Would create an entry point called mommy_spatial_generators that calls mommy_spatial_generators.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - model_mommy

  run:
    - python
    - model_mommy

test:
  # Python imports
  imports:
    - mommy_spatial_generators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sigmageosistemas.com.br
  license: Apache 2.0
  summary: 'Spatial generators for model mommy. This enables you to generate dummy values for GeoDjango fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
