package:
  name: plonetheme.classic
  version: "1.3.3"

source:
  fn: plonetheme.classic-1.3.3.zip
  url: https://pypi.python.org/packages/source/p/plonetheme.classic/plonetheme.classic-1.3.3.zip
  md5: 5bcf588a8998f4a1c5aecb99c3d9ecf4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.classic = plonetheme.classic:main
    #
    # Would create an entry point called plonetheme.classic that calls plonetheme.classic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.classic
    - plonetheme.classic.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plonetheme.classic
  license: GNU General Public License (GPL)
  summary: 'The classic Plone 3 default theme.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
