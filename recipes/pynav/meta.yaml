package:
  name: pynav
  version: "0.7"

source:
  fn: pynav-0.7.tar.gz
  url: https://pypi.python.org/packages/source/p/pynav/pynav-0.7.tar.gz
  md5: c501a4a5f76ad3991d172e3265e5ad32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynav = pynav:main
    #
    # Would create an entry point called pynav that calls pynav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pynav

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/sloft/pynav/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python programmatic web browser to fetch data and test web sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
