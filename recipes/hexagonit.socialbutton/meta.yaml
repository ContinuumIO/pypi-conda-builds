package:
  name: hexagonit.socialbutton
  version: "0.11"

source:
  fn: hexagonit.socialbutton-0.11.zip
  url: https://pypi.python.org/packages/source/h/hexagonit.socialbutton/hexagonit.socialbutton-0.11.zip
  md5: c371c29955eeaba894d603ecfa749f4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hexagonit.socialbutton = hexagonit.socialbutton:main
    #
    # Would create an entry point called hexagonit.socialbutton that calls hexagonit.socialbutton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.stringinterp >=1.0.11
    - setuptools

  run:
    - python
    - setuptools
    - plone.stringinterp >=1.0.11
    - setuptools

test:
  # Python imports
  imports:
    - hexagonit
    - hexagonit.socialbutton
    - hexagonit.socialbutton.adapter
    - hexagonit.socialbutton.browser
    - hexagonit.socialbutton.tests
    - hexagonit.socialbutton.tests.old

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hexagonit.fi
  license: BSD License
  summary: 'Adds viewlets for embedding codes such as social buttons for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
