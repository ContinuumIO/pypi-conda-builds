package:
  name: djangocms-widgets
  version: "0.2.0"

source:
  fn: djangocms-widgets-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-widgets/djangocms-widgets-0.2.0.tar.gz
  md5: 143af725fa08ca323982091a9e14ebfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-widgets = djangocms-widgets:main
    #
    # Would create an entry point called djangocms-widgets that calls djangocms-widgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_widgets
    - djangocms_widgets.migrations
    - djangocms_widgets_socialshareprivacy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Widget plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
