package:
  name: lazy_python
  version: "0.1.10"

source:
  fn: lazy_python-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/l/lazy_python/lazy_python-0.1.10.tar.gz
  md5: 1651564558f546abef0c697e24ecd480
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazy_python = lazy_python:main
    #
    # Would create an entry point called lazy_python that calls lazy_python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - codetransformer ==0.2.0

  run:
    - python
    - codetransformer ==0.2.0

test:
  # Python imports
  imports:
    - lazy
    - lazy_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/llllllllll/lazy_python
  license: GNU General Public License v2 (GPLv2)
  summary: 'Lazy evaluation for python 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
