package:
  name: tango-photos
  version: "0.3.1"

source:
  fn: tango-photos-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tango-photos/tango-photos-0.3.1.tar.gz
  md5: 5c0dd2f204cff91237c91b56f5668b71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tango-photos = tango-photos:main
    #
    # Would create an entry point called tango-photos that calls tango-photos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tango-shared-core >=0.6

  run:
    - python
    - tango-shared-core >=0.6

test:
  # Python imports
  imports:
    - photos
    - photos.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tBaxter/tango-photos
  license: LICENSE.txt
  summary: 'Reusable Django photo galleries app. Can be used with or without Tango.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
