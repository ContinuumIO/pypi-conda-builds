package:
  name: django-isitup
  version: "0.1.4"

source:
  fn: django-isitup-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-isitup/django-isitup-0.1.4.tar.gz
  md5: fdaaa48fab80182a45f41cf5e0e2d1d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-isitup = django-isitup:main
    #
    # Would create an entry point called django-isitup that calls django-isitup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-extensions

  run:
    - python
    - django-extensions

test:
  # Python imports
  imports:
    - isitup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-isitup
  license: BSD License
  summary: 'A simple reusable application for managing a small orgs governance in a Django application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
