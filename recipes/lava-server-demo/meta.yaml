package:
  name: lava-server-demo
  version: "1.0"

source:
  fn: lava-server-demo-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-server-demo/lava-server-demo-1.0.tar.gz
  md5: bc27dc051628b6ce2cddf952bc06ca27
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-server-demo = lava-server-demo:main
    #
    # Would create an entry point called lava-server-demo that calls lava-server-demo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lava-server >=0.2

  run:
    - python
    - setuptools
    - lava-server >=0.2

test:
  # Python imports
  imports:
    - demo_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: AGPL
  summary: 'Demo application for LAVA Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
