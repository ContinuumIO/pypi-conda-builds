package:
  name: django-developer-toolkit
  version: "0.3.0"

source:
  fn: django-developer-toolkit-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-developer-toolkit/django-developer-toolkit-0.3.0.tar.gz
  md5: 5ca01e4efc81285098c49e5c70cc909f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-developer-toolkit = django-developer-toolkit:main
    #
    # Would create an entry point called django-developer-toolkit that calls django-developer-toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - ordereddict

  run:
    - python
    - django
    - ordereddict

test:
  # Python imports
  imports:
    - developer_toolkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-developer-toolkit
  license: BSD License
  summary: 'Django utility app for Django developers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
