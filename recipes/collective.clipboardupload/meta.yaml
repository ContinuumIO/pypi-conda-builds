package:
  name: collective.clipboardupload
  version: "1.0"

source:
  fn: collective.clipboardupload-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.clipboardupload/collective.clipboardupload-1.0.zip
  md5: a8bd7cb8583eb7c3bbfa23d2cd739e1f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.clipboardupload = collective.clipboardupload:main
    #
    # Would create an entry point called collective.clipboardupload that calls collective.clipboardupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup4

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup4

test:
  # Python imports
  imports:
    - collective
    - collective.clipboardupload
    - collective.clipboardupload.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quintagroup/collective.clipboardupload
  license: GNU General Public License (GPL)
  summary: 'Allows uploading images into Plone site by pasting them into TinyMCE WYSIWYG editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
