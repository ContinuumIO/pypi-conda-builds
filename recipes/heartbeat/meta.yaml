package:
  name: heartbeat
  version: "0.1.0"

source:
  fn: HeartBeat-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/H/HeartBeat/HeartBeat-0.1.0.tar.gz
  md5: 5d48515e7ddd24d8101f96cd4001e39c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heartbeat = heartbeat:main
    #
    # Would create an entry point called heartbeat that calls heartbeat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hbeat
    - hbeat.logging
    - heartbeat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://git.lxsameer.com/?p=heartbeat.git;a=summary
  license: GNU General Public License (GPL)
  summary: 'Yet Another HeartBeat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
