package:
  name: abstract.jwrotator
  version: "0.3"

source:
  fn: abstract.jwrotator-0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/abstract.jwrotator/abstract.jwrotator-0.3.tar.gz
  md5: f8e908d02f0f44a2d591342d8a32383a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abstract.jwrotator = abstract.jwrotator:main
    #
    # Would create an entry point called abstract.jwrotator that calls abstract.jwrotator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - abstract
    - abstract.jwrotator
    - abstract.jwrotator.browser
    - abstract.jwrotator.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.abstract.it/svn
  license: GPL
  summary: 'Abstract JWRotator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
