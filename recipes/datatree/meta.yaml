package:
  name: datatree
  version: "0.1.8.1"

source:
  fn: datatree-0.1.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/datatree/datatree-0.1.8.1.tar.gz
  md5: 71171dbf4b9ef7d07bc01b4a962af3a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datatree = datatree:main
    #
    # Would create an entry point called datatree that calls datatree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datatree
    - datatree.render
    - datatree.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bigjason/datatree
  license: Apache Software License
  summary: 'DSL for creating structured documents in python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
