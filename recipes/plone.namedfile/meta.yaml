package:
  name: plone.namedfile
  version: "2.0.8"

source:
  fn: plone.namedfile-2.0.8.zip
  url: https://pypi.python.org/packages/source/p/plone.namedfile/plone.namedfile-2.0.8.zip
  md5: da475b5b67d4b2a2d21313b703b20360
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.namedfile = plone.namedfile:main
    #
    # Would create an entry point called plone.namedfile that calls plone.namedfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.browserpage
    - zope.component
    - zope.security
    - zope.traversing
    - plone.rfc822 >=1.0b2

  run:
    - python
    - setuptools
    - zope.browserpage
    - zope.component
    - zope.security
    - zope.traversing
    - plone.rfc822 >=1.0b2

test:
  # Python imports
  imports:
    - plone
    - plone.namedfile
    - plone.namedfile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.namedfile
  license: BSD License
  summary: 'File types and fields for images, files and blob files with filenames'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
