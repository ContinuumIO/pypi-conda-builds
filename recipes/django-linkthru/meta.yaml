package:
  name: django-linkthru
  version: "0.2.2"

source:
  fn: django-linkthru-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-linkthru/django-linkthru-0.2.2.tar.gz
  md5: 5c897e787aa759b509eaafdba6fb023e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-linkthru = django-linkthru:main
    #
    # Would create an entry point called django-linkthru that calls django-linkthru.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-extensions

  run:
    - python
    - django-extensions

test:
  # Python imports
  imports:
    - linkthru

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-linkthru
  license: BSD License
  summary: 'A riff on adzone, but without the advert cruft. Just text/images linking to arbitrary urls.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
