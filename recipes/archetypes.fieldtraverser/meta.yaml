package:
  name: archetypes.fieldtraverser
  version: "0.4.4"

source:
  fn: archetypes.fieldtraverser-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/a/archetypes.fieldtraverser/archetypes.fieldtraverser-0.4.4.tar.gz
  md5: 84ca777961d505841eb9544600799843
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.fieldtraverser = archetypes.fieldtraverser:main
    #
    # Would create an entry point called archetypes.fieldtraverser that calls archetypes.fieldtraverser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.archetypes

  run:
    - python
    - setuptools
    - setuptools
    - products.archetypes

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.fieldtraverser
    - archetypes.fieldtraverser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/archetypes/archetypes.fieldtraverser
  license: GNU General Public License (GPL)
  summary: 'Archetypes fieldtraverser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
