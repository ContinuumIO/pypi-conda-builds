package:
  name: tornadopack
  version: "0.1.1"

source:
  fn: TornadoPack-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/T/TornadoPack/TornadoPack-0.1.1.tar.gz
  md5: c8acf7cb61b6d069bea21f63ba0b48be
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadopack = tornadopack:main
    #
    # Would create an entry point called tornadopack that calls tornadopack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=1.0,<=1.1.99
    - commandtool >=0.4.0,<=0.4.99
    - barenecessities >=0.2.5,<=0.2.99

  run:
    - python
    - setuptools
    - tornado >=1.0,<=1.1.99
    - commandtool >=0.4.0,<=0.4.99
    - barenecessities >=0.2.5,<=0.2.99

test:
  # Python imports
  imports:
    - tornadopack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/tornadopack/index.html
  license: GNU Affero General Public License v3
  summary: 'Serve a PipeStack application using Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
