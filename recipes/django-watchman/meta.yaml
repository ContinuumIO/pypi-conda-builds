package:
  name: django-watchman
  version: "0.5.0"

source:
  fn: django-watchman-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-watchman/django-watchman-0.5.0.tar.gz
  md5: 347c969d0e34eab2d468a001793fa904
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-watchman = django-watchman:main
    #
    # Would create an entry point called django-watchman that calls django-watchman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-jsonview ==0.4.3

  run:
    - python
    - django
    - django-jsonview ==0.4.3

test:
  # Python imports
  imports:
    - watchman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mwarkentin/django-watchman
  license: BSD License
  summary: 'django-watchman exposes a status endpoint for your backing services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
