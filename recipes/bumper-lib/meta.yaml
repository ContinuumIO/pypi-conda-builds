package:
  name: bumper-lib
  version: "0.2.12"

source:
  fn: bumper-lib-0.2.12.tar.gz
  url: https://pypi.python.org/packages/source/b/bumper-lib/bumper-lib-0.2.12.tar.gz
  md5: 9b94bc70ea646b0740ee45d902dd7911
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bumper-lib = bumper-lib:main
    #
    # Would create an entry point called bumper-lib that calls bumper-lib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - brownie
    - requests
    - simplejson

  run:
    - python
    - brownie
    - requests
    - simplejson

test:
  # Python imports
  imports:
    - bumper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxzheng/bumper-lib
  license: MIT License
  summary: 'A library to bump / pin your dependency requirements.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
