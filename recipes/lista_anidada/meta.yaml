package:
  name: lista_anidada
  version: "1.1.0"

source:
  fn: Lista_Anidada-1.1.0.zip
  url: https://pypi.python.org/packages/source/L/Lista_Anidada/Lista_Anidada-1.1.0.zip
  md5: d40ddfe1ed0ec608c945d88d5d41d6ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lista_anidada = lista_anidada:main
    #
    # Would create an entry point called lista_anidada that calls lista_anidada.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lista_anidada

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.headfirstlabs.com
  license: UNKNOWN
  summary: 'Un simple print() de listas anidadas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
