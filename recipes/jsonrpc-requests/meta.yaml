package:
  name: jsonrpc-requests
  version: "0.1"

source:
  fn: jsonrpc-requests-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonrpc-requests/jsonrpc-requests-0.1.tar.gz
  md5: f29fe7691689469311c26779b3762eb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonrpc-requests = jsonrpc-requests:main
    #
    # Would create an entry point called jsonrpc-requests that calls jsonrpc-requests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - jsonrpc_requests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gciotta/jsonrpc-requests
  license: BSD License
  summary: 'A JSON-RPC client library, backed by requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
