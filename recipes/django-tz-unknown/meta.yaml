package:
  name: django-tz-unknown
  version: "0.5.dev1"

source:
  fn: django-tz-unknown-0.5.dev1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tz-unknown/django-tz-unknown-0.5.dev1.tar.gz
  md5: da6290db1d8a6db12d58767d3e941491
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tz-unknown = django-tz-unknown:main
    #
    # Would create an entry point called django-tz-unknown that calls django-tz-unknown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - django_tz
    - django_tz.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Django timezones localization app based on global cache (similar to django.utils.translation)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
