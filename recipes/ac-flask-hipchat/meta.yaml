package:
  name: ac-flask-hipchat
  version: "0.1.1"

source:
  fn: AC-Flask-HipChat-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/A/AC-Flask-HipChat/AC-Flask-HipChat-0.1.1.tar.gz
  md5: 034e8cb7e0bd198168f6e0225fed5906
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ac-flask-hipchat = ac-flask-hipchat:main
    #
    # Would create an entry point called ac-flask-hipchat that calls ac-flask-hipchat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pymongo
    - redis
    - requests
    - pyjwt

  run:
    - python
    - flask
    - pymongo
    - redis
    - requests
    - pyjwt

test:
  # Python imports
  imports:
    - ac_flask
    - ac_flask.hipchat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mrdon/ac-flask-hipchat
  license: BSD License
  summary: 'Atlassian Connect library based on Flask for HipChat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
