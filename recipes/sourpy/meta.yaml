package:
  name: sourpy
  version: "0.2.1"

source:
  fn: sourpy-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sourpy/sourpy-0.2.1.tar.gz
  md5: 95d7af062f136e7c4b737da3e5cfa903
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sourpy = sourpy:main
    #
    # Would create an entry point called sourpy that calls sourpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.3.2

  run:
    - python
    - beautifulsoup4 >=4.3.2

test:
  # Python imports
  imports:
    - sourpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/canpolat/sourpy
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Ek\xc5\x9fi S\xc3\xb6zl\xc3\xbck scraper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
