package:
  name: edxml
  version: "2.1.0"

source:
  fn: edxml-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/edxml/edxml-2.1.0.tar.gz
  md5: d3e1beb2b4119ec9100972162131182a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edxml = edxml:main
    #
    # Would create an entry point called edxml that calls edxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.4
    - elementtree

  run:
    - python
    - lxml >=3.4
    - elementtree

test:
  # Python imports
  imports:
    - edxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dtakken/edxml-sdk
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'The EDXML Software Developers Kit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
