package:
  name: cuisine_sweet
  version: "0.1.17"

source:
  fn: cuisine_sweet-0.1.17.tar.gz
  url: https://pypi.python.org/packages/source/c/cuisine_sweet/cuisine_sweet-0.1.17.tar.gz
  md5: 450e6829dae1fdb94e956ec791b8b44c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cuisine_sweet = cuisine_sweet:main
    #
    # Would create an entry point called cuisine_sweet that calls cuisine_sweet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric
    - pyyaml
    - cuisine ==0.3.2
    - distribute
    - docutils
    - decorator
    - pexpect

  run:
    - python
    - fabric
    - pyyaml
    - cuisine ==0.3.2
    - distribute
    - docutils
    - decorator
    - pexpect

test:
  # Python imports
  imports:
    - cuisine_sweet
    - cuisine_sweet.ensure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dexterbt1/cuisine_sweet
  license: Revised BSD License
  summary: 'Sugar-coated declarative deployment recipes built on top of Fabric and Cuisine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
