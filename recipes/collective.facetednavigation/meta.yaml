package:
  name: collective.facetednavigation
  version: "0.7.2"

source:
  fn: collective.facetednavigation-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.facetednavigation/collective.facetednavigation-0.7.2.tar.gz
  md5: a2c1ef79d9f70c3064cae244253a212b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.facetednavigation = collective.facetednavigation:main
    #
    # Would create an entry point called collective.facetednavigation that calls collective.facetednavigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - collective
    - collective.facetednavigation
    - collective.facetednavigation.browser
    - collective.facetednavigation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/faceted-navigation
  license: GNU General Public License (GPL)
  summary: 'Collective Faceted Navigation provides an user interface which lets an user browse items of a Plone site by selecting amongst pre-defined criteria.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
