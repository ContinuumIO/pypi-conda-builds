package:
  name: django-admin-csv
  version: "0.2"

source:
  fn: django-admin-csv-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-csv/django-admin-csv-0.2.tar.gz
  md5: 73f98e74ad31f2eadd2a8162d4357981
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-csv = django-admin-csv:main
    #
    # Would create an entry point called django-admin-csv that calls django-admin-csv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_csv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bendavis78/django-admin-csv
  license: MIT
  summary: 'Adds a "download csv" option to your ModelAdmin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
