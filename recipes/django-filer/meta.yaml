package:
  name: django-filer
  version: "0.9.9"

source:
  fn: django-filer-0.9.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filer/django-filer-0.9.9.tar.gz
  md5: 5f754b1c8a70356e7aac55c107e43230
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filer = django-filer:main
    #
    # Would create an entry point called django-filer that calls django-filer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - easy-thumbnails >=1.0
    - django-mptt >=0.6,<0.6.2
    - django_polymorphic >=0.2
    - unidecode >=0.04

  run:
    - python
    - django >=1.4
    - easy-thumbnails >=1.0
    - django-mptt >=0.6,<0.6.2
    - django_polymorphic >=0.2
    - unidecode >=0.04

test:
  # Python imports
  imports:
    - filer
    - filer.admin
    - filer.admin.patched
    - filer.fields
    - filer.management
    - filer.management.commands
    - filer.migrations
    - filer.migrations_django
    - filer.models
    - filer.server
    - filer.server.backends
    - filer.templatetags
    - filer.test_utils
    - filer.test_utils.custom_image
    - filer.test_utils.custom_image.migrations
    - filer.test_utils.custom_image.south_migrations
    - filer.tests
    - filer.tests.utils
    - filer.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stefanfoulis/django-filer
  license: BSD License
  summary: 'A file management application for django that makes handling of files and images a breeze.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
