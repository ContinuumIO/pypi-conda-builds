package:
  name: tornado-menumaker
  version: "0.1.2"

source:
  fn: Tornado-Menumaker-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/T/Tornado-Menumaker/Tornado-Menumaker-0.1.2.tar.gz
  md5: 495ba62cef3180f7d5b4d4affd0fa79d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-menumaker = tornado-menumaker:main
    #
    # Would create an entry point called tornado-menumaker that calls tornado-menumaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - tornado_menumaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MartinMartimeo/tornado-menumaker
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'simple tornado menus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
