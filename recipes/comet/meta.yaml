package:
  name: comet
  version: "1.2.2"

source:
  fn: Comet-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/C/Comet/Comet-1.2.2.tar.gz
  md5: 49b27ff2a879c048f5f9610ec35ea5db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - comet = comet:main
    #
    # Would create an entry point called comet that calls comet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - twisted >=11.1.0
    - lxml >=2.3
    - zope.interface >=3.6
    - ipaddr

  run:
    - python
    - twisted >=11.1.0
    - lxml >=2.3
    - zope.interface >=3.6
    - ipaddr

test:
  # Python imports
  imports:
    - comet
    - comet.handler
    - comet.handler.test
    - comet.plugins
    - comet.plugins.test
    - comet.service
    - comet.service.test
    - comet.tcp
    - comet.tcp.test
    - comet.test
    - comet.utility
    - comet.utility.test
    - comet.validator
    - comet.validator.test
    - twisted

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://comet.transientskp.org/
  license: UNKNOWN
  summary: 'VOEvent Broker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
