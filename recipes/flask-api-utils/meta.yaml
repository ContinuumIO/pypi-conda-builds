package:
  name: flask-api-utils
  version: "1.0.0"

source:
  fn: Flask-API-Utils-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-API-Utils/Flask-API-Utils-1.0.0.tar.gz
  md5: e80e1cc7f4be4975f7132a1663140cb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-api-utils = flask-api-utils:main
    #
    # Would create an entry point called flask-api-utils that calls flask-api-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask >=0.10

  run:
    - python
    - flask >=0.10

test:
  # Python imports
  imports:
    - api_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marselester/flask-api-utils
  license: UNKNOWN
  summary: 'Flask-API-Utils helps you to create APIs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
