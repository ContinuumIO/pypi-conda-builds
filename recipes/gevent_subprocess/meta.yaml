package:
  name: gevent_subprocess
  version: "0.1.1"

source:
  fn: gevent_subprocess-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gevent_subprocess/gevent_subprocess-0.1.1.tar.gz
  md5: 256c22ec2b5ec89748839e99ebd1303b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gevent_subprocess = gevent_subprocess:main
    #
    # Would create an entry point called gevent_subprocess that calls gevent_subprocess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent

  run:
    - python
    - gevent

test:
  # Python imports
  imports:
    - gevent_subprocess

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bombela/gevent_subprocess
  license: MIT
  summary: 'gevent adapter for the standard python subprocess'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
