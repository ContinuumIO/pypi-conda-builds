package:
  name: chinup
  version: "0.3.2"

source:
  fn: chinup-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/chinup/chinup-0.3.2.tar.gz
  md5: 2afef58873e02c412ca2b916f42a767c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chinup = chinup:main
    #
    # Would create an entry point called chinup that calls chinup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urlobject >=2.4.0
    - requests

  run:
    - python
    - urlobject >=2.4.0
    - requests

test:
  # Python imports
  imports:
    - chinup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pagepart/chinup
  license: MIT
  summary: 'Python Facebook Graph API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
