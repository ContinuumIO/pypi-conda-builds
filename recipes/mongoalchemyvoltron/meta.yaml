package:
  name: mongoalchemyvoltron
  version: "0.12.1"

source:
  fn: MongoAlchemyVoltron-0.12.1.tar.gz
  url: https://pypi.python.org/packages/source/M/MongoAlchemyVoltron/MongoAlchemyVoltron-0.12.1.tar.gz
  md5: 01bb82cb0f76e2b48ad9df1888c5ca31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoalchemyvoltron = mongoalchemyvoltron:main
    #
    # Would create an entry point called mongoalchemyvoltron that calls mongoalchemyvoltron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - mongoalchemy
    - mongoalchemyvoltron

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shakefu/MongoAlchemy
  license: MIT License
  summary: 'Document-Object Mapper/Toolkit for MongoDB - Voltron Fork'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
