package:
  name: esdrt.theme
  version: "1.15.5"

source:
  fn: esdrt.theme-1.15.5.zip
  url: https://pypi.python.org/packages/source/e/esdrt.theme/esdrt.theme-1.15.5.zip
  md5: ca4ceb149485465257635e871da57d2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - esdrt.theme = esdrt.theme:main
    #
    # Would create an entry point called esdrt.theme that calls esdrt.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot
    - five.grok
    - eea.icons
    - esdrt.content
    - plone.app.ldap
    - python-ldap
    - products.ldapmultiplugins
    - products.ldapuserfolder
    - products.collage
    - eea.controlpanel

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot
    - five.grok
    - eea.icons
    - esdrt.content
    - plone.app.ldap
    - python-ldap
    - products.ldapmultiplugins
    - products.ldapuserfolder
    - products.collage
    - eea.controlpanel

test:
  # Python imports
  imports:
    - esdrt
    - esdrt.theme
    - esdrt.theme.browser
    - esdrt.theme.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eea/esdrt.theme/
  license: GPL
  summary: 'Installable theme: esdrt.theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
