package:
  name: django-admin-filtrate
  version: "0.1.0"

source:
  fn: django-admin-filtrate-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-filtrate/django-admin-filtrate-0.1.0.tar.gz
  md5: 46c1ad134c29fb49796aac865cb332b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-filtrate = django-admin-filtrate:main
    #
    # Would create an entry point called django-admin-filtrate that calls django-admin-filtrate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - filtrate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/runekaagaard/django-admin-filtrate
  license: MIT
  summary: 'This Django app makes it easier to create custom filters in the change list of Django Admin and supplies a TreeFilter and a DateRangeFilter too.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
