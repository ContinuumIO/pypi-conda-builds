package:
  name: exec_cmd
  version: "0.1.0"

source:
  fn: exec_cmd-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/exec_cmd/exec_cmd-0.1.0.tar.gz
  md5: ee76fdc79221b78b05bcaf97968d386c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exec_cmd = exec_cmd:main
    #
    # Would create an entry point called exec_cmd that calls exec_cmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - exec_cmd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pnpnpn/py-exec-cmd
  license: MIT License
  summary: 'Command-line execution with timeout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
