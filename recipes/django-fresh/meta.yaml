package:
  name: django-fresh
  version: "1.0.5"

source:
  fn: django-fresh-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fresh/django-fresh-1.0.5.tar.gz
  md5: 7e471ab8b84cb6421862bc036254d699
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fresh = django-fresh:main
    #
    # Would create an entry point called django-fresh that calls django-fresh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - watchdog
    - beautifulsoup4

  run:
    - python
    - django
    - watchdog
    - beautifulsoup4

test:
  # Python imports
  imports:
    - fresh

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/overshard/django-fresh
  license: BSD License
  summary: 'Auto-refresh your browser when files in your project change.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
