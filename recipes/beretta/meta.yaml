package:
  name: beretta
  version: "0.3.0"

source:
  fn: beretta-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/beretta/beretta-0.3.0.tar.gz
  md5: 92bc78b65de5f66b8f599e8f72d7eb59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beretta = beretta:main
    #
    # Would create an entry point called beretta that calls beretta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - termformat >=0.1.6

  run:
    - python
    - termformat >=0.1.6

test:
  # Python imports
  imports:
    - beretta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tyrannosaurus/beretta
  license: MIT
  summary: 'BERT (de)serializer for your Pythons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
