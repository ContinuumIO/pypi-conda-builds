package:
  name: csfd-parser
  version: "1.0.5"

source:
  fn: csfd-parser-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/csfd-parser/csfd-parser-1.0.5.tar.gz
  md5: a40d9d44fc6d805e9fbb0892b97c3f89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csfd-parser = csfd-parser:main
    #
    # Would create an entry point called csfd-parser that calls csfd-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jirutka/CSFD-parser
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Parser for movie pages and search on CSFD.cz'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
