package:
  name: django-static
  version: "1.5.5"

source:
  fn: django-static-1.5.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-static/django-static-1.5.5.tar.gz
  md5: 2db80302ace2ea3bbec30c111a68d3ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-static = django-static:main
    #
    # Would create an entry point called django-static that calls django-static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_static
    - django_static.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/peterbe/django-static
  license: BSD
  summary: 'Template tags for better serving static files from templates in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
