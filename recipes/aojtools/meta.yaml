package:
  name: aojtools
  version: "0.2.4"

source:
  fn: aojtools-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/a/aojtools/aojtools-0.2.4.tar.gz
  md5: 222cdd6d35dde9e754f768ecefff4db7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aojtools = aojtools:main
    #
    # Would create an entry point called aojtools that calls aojtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aojtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yatt/aojtools
  license: MIT
  summary: 'api client library and scripts for aizu online judge system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
