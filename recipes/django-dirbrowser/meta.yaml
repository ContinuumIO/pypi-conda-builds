package:
  name: django-dirbrowser
  version: "0.0.3"

source:
  fn: django-dirbrowser-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dirbrowser/django-dirbrowser-0.0.3.tar.gz
  md5: 64a5ceaef33a187ca0b8cfa0b0c49942
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dirbrowser = django-dirbrowser:main
    #
    # Would create an entry point called django-dirbrowser that calls django-dirbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dirbrowser
    - dirbrowser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ferranp/django-dirbrowser
  license: GNU General Public License (GPL)
  summary: 'Django app For browsing a directory tree.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
