package:
  name: httptestserver
  version: "0.3.0"

source:
  fn: httptestserver-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httptestserver/httptestserver-0.3.0.tar.gz
  md5: 3047cd076282184dc8177fabd452d70a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httptestserver = httptestserver:main
    #
    # Would create an entry point called httptestserver that calls httptestserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - httptestserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'HTTP(s) and SMTP testing server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
