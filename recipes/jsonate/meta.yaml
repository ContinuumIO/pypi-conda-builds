package:
  name: jsonate
  version: "0.3.2"

source:
  fn: jsonate-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonate/jsonate-0.3.2.tar.gz
  md5: 8d4278730cdc22fdaff9f9a9147d51c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonate = jsonate:main
    #
    # Would create an entry point called jsonate that calls jsonate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - jsonate
    - jsonate.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://jsonate.com
  license: MIT License
  summary: 'Django library that can make ANYTHING into json'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
