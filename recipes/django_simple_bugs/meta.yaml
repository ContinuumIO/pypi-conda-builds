package:
  name: django_simple_bugs
  version: "0.1.4"

source:
  fn: django_simple_bugs-0.1.4.zip
  url: https://pypi.python.org/packages/source/d/django_simple_bugs/django_simple_bugs-0.1.4.zip
  md5: 450696b85e2f7b233469dab9dd07097d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_simple_bugs = django_simple_bugs:main
    #
    # Would create an entry point called django_simple_bugs that calls django_simple_bugs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.1
    - south >=0.8.4
    - django_haystack >=2.1.0
    - django_crispy_forms >=1.4.0
    - django_wysiwyg >=0.5.1
    - django_simple_history >=1.3.0
    - djangorestframework >=2.3.12

  run:
    - python
    - django >=1.5.1
    - south >=0.8.4
    - django_haystack >=2.1.0
    - django_crispy_forms >=1.4.0
    - django_wysiwyg >=0.5.1
    - django_simple_history >=1.3.0
    - djangorestframework >=2.3.12

test:
  # Python imports
  imports:
    - django_simple_bugs
    - simple_bugs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thethc3/django_simple_bugs
  license: BSD License
  summary: 'Bug tracking and project management app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
