package:
  name: jobtastic-mixins
  version: "0.1"

source:
  fn: jobtastic-mixins-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jobtastic-mixins/jobtastic-mixins-0.1.tar.gz
  md5: 45c76cd8f2bd3d3670143f460beb797a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobtastic-mixins = jobtastic-mixins:main
    #
    # Would create an entry point called jobtastic-mixins that calls jobtastic-mixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jobtastic >=0.2.2

  run:
    - python
    - jobtastic >=0.2.2

test:
  # Python imports
  imports:
    - jobtasticmixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abbasovalex/JobtasticMixins
  license: MIT License
  summary: 'Provides ability calculate average time of tasks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
