package:
  name: cellnopt.wrapper
  version: "1.0.5"

source:
  fn: cellnopt.wrapper-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cellnopt.wrapper/cellnopt.wrapper-1.0.5.tar.gz
  md5: 2828b8498acd4a49e7ec2f9fe19aa551
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cellnopt.wrapper = cellnopt.wrapper:main
    #
    # Would create an entry point called cellnopt.wrapper that calls cellnopt.wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rpy2
    - rtools >=0.2.11
    - easydev >=0.5.6
    - cellnopt.data >=0.7.8

  run:
    - python
    - rpy2
    - rtools >=0.2.11
    - easydev >=0.5.6
    - cellnopt.data >=0.7.8

test:
  # Python imports
  imports:
    - cellnopt
    - cellnopt.wrapper
    - wrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ['http://www.ebi.ac.uk/~cokelaer/cellnopt/wrapper']
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'CellNOpt R packages wrapper and utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
