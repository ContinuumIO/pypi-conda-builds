package:
  name: collective.powertoken.view
  version: "0.1.0"

source:
  fn: collective.powertoken.view-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.powertoken.view/collective.powertoken.view-0.1.0.tar.gz
  md5: 926b484cd3049d38ab20dbcb6f10ea2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.powertoken.view = collective.powertoken.view:main
    #
    # Would create an entry point called collective.powertoken.view that calls collective.powertoken.view.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.powertoken.core

  run:
    - python
    - setuptools
    - setuptools
    - collective.powertoken.core

test:
  # Python imports
  imports:
    - collective
    - collective.powertoken
    - collective.powertoken.view
    - collective.powertoken.view.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/collective.powertoken.core
  license: GPL
  summary: 'View and content access add-ons for collective.powertoken support for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
