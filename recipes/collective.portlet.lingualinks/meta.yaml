package:
  name: collective.portlet.lingualinks
  version: "1.1"

source:
  fn: collective.portlet.lingualinks-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.lingualinks/collective.portlet.lingualinks-1.1.zip
  md5: e25fb81b8e48d8d74e5b3dbdd4a6dab3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.lingualinks = collective.portlet.lingualinks:main
    #
    # Would create an entry point called collective.portlet.lingualinks that calls collective.portlet.lingualinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - products.linguaplone

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - products.linguaplone

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.lingualinks
    - collective.portlet.lingualinks.tests
    - collective.portlet.lingualinks.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective/collective.portlet.lingualinks
  license: GPL
  summary: 'A portlet to provide links to translated content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
