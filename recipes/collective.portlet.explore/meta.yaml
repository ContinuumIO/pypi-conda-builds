package:
  name: collective.portlet.explore
  version: "1.2"

source:
  fn: collective.portlet.explore-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.explore/collective.portlet.explore-1.2.zip
  md5: f0b524e55f471180f323536cf265dbf7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.explore = collective.portlet.explore:main
    #
    # Would create an entry point called collective.portlet.explore that calls collective.portlet.explore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.layout
    - plone.app.portlets
    - plone.portlets

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.layout
    - plone.app.portlets
    - plone.portlets

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.explore
    - collective.portlet.explore.browser
    - collective.portlet.explore.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jarn.com
  license: GPL
  summary: 'A navigation portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
