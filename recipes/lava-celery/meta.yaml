package:
  name: lava-celery
  version: "0.4.1"

source:
  fn: lava-celery-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-celery/lava-celery-0.4.1.tar.gz
  md5: 3df589ccf56fc088305e6787be07f9f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-celery = lava-celery:main
    #
    # Would create an entry point called lava-celery that calls lava-celery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - versiontools >=1.8
    - celery >=2.0

  run:
    - python
    - setuptools
    - versiontools >=1.8
    - celery >=2.0

test:
  # Python imports
  imports:
    - lava
    - lava.celery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3
  summary: 'Celery integration for LAVA Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
