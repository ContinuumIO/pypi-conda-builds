package:
  name: aws.minisite
  version: "1.0.2"

source:
  fn: aws.minisite-1.0.2.zip
  url: https://pypi.python.org/packages/source/a/aws.minisite/aws.minisite-1.0.2.zip
  md5: 57e6b9af23bee9db3ad2b5df4cefcf3f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aws.minisite = aws.minisite:main
    #
    # Would create an entry point called aws.minisite that calls aws.minisite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.phantasy >=1.0
    - products.fckeditor >=2.6.5.2
    - collective.uploadify >=0.9

  run:
    - python
    - setuptools
    - setuptools
    - collective.phantasy >=1.0
    - products.fckeditor >=2.6.5.2
    - collective.uploadify >=0.9

test:
  # Python imports
  imports:
    - aws
    - aws.minisite
    - aws.minisite.browser
    - aws.minisite.content
    - aws.minisite.extendcontents
    - aws.minisite.interfaces
    - aws.minisite.portlets
    - aws.minisite.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://ingeniweb.svn.sourceforge.net/svnroot/ingeniweb/aws.minisite/trunk
  license: GNU General Public License (GPL)
  summary: 'Minimal sites for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
