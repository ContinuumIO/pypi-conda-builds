package:
  name: django-ignoretests2
  version: "0.4.1"

source:
  fn: django-ignoretests2-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ignoretests2/django-ignoretests2-0.4.1.tar.gz
  md5: 9ed87de84f9881973da65e90e8bdb901
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ignoretests2 = django-ignoretests2:main
    #
    # Would create an entry point called django-ignoretests2 that calls django-ignoretests2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ignoretests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberj/django-ignoretests
  license: MIT License
  summary: 'Ignore tests of given django apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
