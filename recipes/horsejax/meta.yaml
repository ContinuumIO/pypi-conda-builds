package:
  name: horsejax
  version: "0.1.2"

source:
  fn: horsejax-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/horsejax/horsejax-0.1.2.tar.gz
  md5: 3aab91f8e2c5f601156d7de3ee7ab854
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horsejax = horsejax:main
    #
    # Would create an entry point called horsejax that calls horsejax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - horsephrase
    - haddock

  run:
    - python
    - horsephrase
    - haddock

test:
  # Python imports
  imports:
    - horsejax
    - twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hawkowl/horsejax/
  license: MIT
  summary: 'Secure password generator via not so secure JSON.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
