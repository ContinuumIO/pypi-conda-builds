package:
  name: django-uturn
  version: "0.3.1"

source:
  fn: django-uturn-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uturn/django-uturn-0.3.1.tar.gz
  md5: bd3e3a73de34117f7f6649c01f5c5dfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uturn = django-uturn:main
    #
    # Would create an entry point called django-uturn that calls django-uturn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - uturn
    - uturn.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/roam/django-uturn
  license: BSD License
  summary: 'Overriding redirects in Django, to return where you came from'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
