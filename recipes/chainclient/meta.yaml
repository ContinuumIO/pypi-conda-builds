package:
  name: chainclient
  version: "0.4.0"

source:
  fn: chainclient-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chainclient/chainclient-0.4.0.tar.gz
  md5: aee81027caa47227f645564b3ab56604
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chainclient = chainclient:main
    #
    # Would create an entry point called chainclient that calls chainclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - chainclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ssfrr/chainclient.py
  license: MIT
  summary: 'A Python client for the Chain API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
