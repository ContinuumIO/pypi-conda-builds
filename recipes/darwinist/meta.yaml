package:
  name: darwinist
  version: "4.1.1"

source:
  fn: darwinist-4.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/darwinist/darwinist-4.1.1.tar.gz
  md5: 327c5696dd5b7ff7330f45986fed9dfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - darwinist = darwinist:main
    #
    # Would create an entry point called darwinist that calls darwinist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - systematic >=4.0.2
    - appscript
    - pyfsevents

  run:
    - python
    - systematic >=4.0.2
    - appscript
    - pyfsevents

test:
  # Python imports
  imports:
    - darwinist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hile/darwinist
  license: PSF
  summary: 'Sysadmin utility modules and scripts for OS/X'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
