package:
  name: missandei
  version: "0.2.0"

source:
  fn: missandei-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/missandei/missandei-0.2.0.tar.gz
  md5: 271af2488fdcd4dce0ac1008486ad612
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - missandei = missandei:main
    #
    # Would create an entry point called missandei that calls missandei.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - missandei

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gamechanger/missandei
  license: MIT
  summary: 'A dict to dict translation framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
