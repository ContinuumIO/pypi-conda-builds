package:
  name: igor2txt
  version: "0.1"

source:
  fn: igor2txt-0.1.zip
  url: https://pypi.python.org/packages/source/i/igor2txt/igor2txt-0.1.zip
  md5: 08463597fbd39492ddcfda95da9aad29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - igor2txt = igor2txt:main
    #
    # Would create an entry point called igor2txt that calls igor2txt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - igor

  run:
    - python
    - numpy
    - igor

test:
  # Python imports
  imports:
    - igor2txt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/igor2txt/
  license: MIT License
  summary: 'Extract ASCII waves from Igor *.pxp, *.pxt & *.ibw files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
