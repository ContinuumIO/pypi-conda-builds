package:
  name: helga-prod-fixer
  version: "0.3.0"

source:
  fn: helga-prod-fixer-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-prod-fixer/helga-prod-fixer-0.3.0.tar.gz
  md5: 9d70c9dd01a537693f4d4f654aa1eb0b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-prod-fixer = helga-prod-fixer:main
    #
    # Would create an entry point called helga-prod-fixer that calls helga-prod-fixer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga
  license: MIT License
  summary: 'IRC bot using twisted'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
