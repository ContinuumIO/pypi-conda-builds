package:
  name: apiforms
  version: "1.0a1"

source:
  fn: apiforms-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/a/apiforms/apiforms-1.0a1.tar.gz
  md5: a8e592c1a0d85b1e1621d9f492111530
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apiforms = apiforms:main
    #
    # Would create an entry point called apiforms that calls apiforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - apiforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/derekkwok/apiforms
  license: BSD License
  summary: 'A library for validating and sanitizing http request data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
