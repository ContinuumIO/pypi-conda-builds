package:
  name: html5tidy
  version: "1.0"

source:
  fn: html5tidy-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/html5tidy/html5tidy-1.0.tar.gz
  md5: 8ab0b67bf9b5942b9689a6e972b1d564
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html5tidy = html5tidy:main
    #
    # Would create an entry point called html5tidy that calls html5tidy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html5lib
    - lxml

  run:
    - python
    - html5lib
    - lxml

test:
  # Python imports
  imports:
    - html5tidy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://automatist.org/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Simple wrapper around html5lib & lxml.etree to "tidy" html in the wild to well-formed xml/html'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
