package:
  name: django-typed-models
  version: "0.4.0"

source:
  fn: django-typed-models-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-typed-models/django-typed-models-0.4.0.tar.gz
  md5: 47ad7f8eaf39f4551d2a35864bb7b445
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-typed-models = django-typed-models:main
    #
    # Would create an entry point called django-typed-models that calls django-typed-models.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - typedmodels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/craigds/django-typed-models
  license: BSD License
  summary: 'Sane single table model inheritance for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
