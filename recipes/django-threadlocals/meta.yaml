package:
  name: django-threadlocals
  version: "0.7.2"

source:
  fn: django-threadlocals-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-threadlocals/django-threadlocals-0.7.2.tar.gz
  md5: fcb8b3831f3ec6265851a93329dfa447
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-threadlocals = django-threadlocals:main
    #
    # Would create an entry point called django-threadlocals that calls django-threadlocals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - threadlocals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nebstrebor/django-threadlocals
  license: BSD License
  summary: 'Contains utils for storing and retreiving values from threadlocals, and middleware for placing the current Django request in threadlocal storage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
