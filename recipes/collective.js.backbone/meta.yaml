package:
  name: collective.js.backbone
  version: "1.0.0.2"

source:
  fn: collective.js.backbone-1.0.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.js.backbone/collective.js.backbone-1.0.0.2.zip
  md5: 34798670a2328da677ca26d09e9f8c88
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.backbone = collective.js.backbone:main
    #
    # Would create an entry point called collective.js.backbone that calls collective.js.backbone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.underscore

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.underscore

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.backbone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.backbone
  license: MIT License
  summary: 'backbone.js for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
