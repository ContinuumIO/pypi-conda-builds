package:
  name: plone.app.intid
  version: "1.0.4"

source:
  fn: plone.app.intid-1.0.4.zip
  url: https://pypi.python.org/packages/source/p/plone.app.intid/plone.app.intid-1.0.4.zip
  md5: 85a887f112e414d374369c81e8e825c1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.intid = plone.app.intid:main
    #
    # Would create an entry point called plone.app.intid that calls plone.app.intid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.intid
    - zope.lifecycleevent
    - five.intid >=1.0
    - products.cmfcore

  run:
    - python
    - setuptools
    - setuptools
    - zope.intid
    - zope.lifecycleevent
    - five.intid >=1.0
    - products.cmfcore

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.intid
    - plone.app.intid.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.app.intid
  license: GPL
  summary: 'Installation and migration support for five.intid within Plone/CMF'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
