package:
  name: django-natural-key-cache
  version: "0.1"

source:
  fn: django-natural-key-cache-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-natural-key-cache/django-natural-key-cache-0.1.tar.gz
  md5: deb7ccb18ec5086389dfa5432adb9dea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-natural-key-cache = django-natural-key-cache:main
    #
    # Would create an entry point called django-natural-key-cache that calls django-natural-key-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - natural_key_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shadowfax-chc/django-natural-key-cache
  license: ISC License
  summary: 'Cache manager for caching django models by natural keys'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
