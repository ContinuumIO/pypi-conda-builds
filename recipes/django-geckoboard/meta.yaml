package:
  name: django-geckoboard
  version: "1.2.8"

source:
  fn: django-geckoboard-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-geckoboard/django-geckoboard-1.2.8.tar.gz
  md5: 60248f0deb27362431fc90e330ccb730
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geckoboard = django-geckoboard:main
    #
    # Would create an entry point called django-geckoboard that calls django-geckoboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_geckoboard
    - django_geckoboard.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jcassee/django-geckoboard
  license: MIT License
  summary: 'Geckoboard custom widgets for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
