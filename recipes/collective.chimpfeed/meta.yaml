package:
  name: collective.chimpfeed
  version: "2.0"

source:
  fn: collective.chimpfeed-2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.chimpfeed/collective.chimpfeed-2.0.tar.gz
  md5: 391ceb26c16ee1e385e1ce1dcc1982e6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.chimpfeed = collective.chimpfeed:main
    #
    # Would create an entry point called collective.chimpfeed that calls collective.chimpfeed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.indexer
    - plone.z3cform
    - greatape
    - simplejson
    - products.advancedquery
    - beautifulsoup
    - zope.app.component

  run:
    - python
    - setuptools
    - setuptools
    - plone.indexer
    - plone.z3cform
    - greatape
    - simplejson
    - products.advancedquery
    - beautifulsoup
    - zope.app.component

test:
  # Python imports
  imports:
    - collective
    - collective.chimpfeed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'MailChimp-integration for Plone!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
