package:
  name: silva.core.editor
  version: "3.0.3"

source:
  fn: silva.core.editor-3.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.core.editor/silva.core.editor-3.0.3.tar.gz
  md5: 257731ca0c44382410db6f356fcf8e22
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.core.editor = silva.core.editor:main
    #
    # Would create an entry point called silva.core.editor that calls silva.core.editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - infrae.rest
    - js.ckeditor
    - lxml
    - setuptools
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.references
    - silva.core.messages
    - silva.core.services
    - silva.core.views
    - silva.core.xml
    - silva.translations
    - silva.ui
    - tinycss
    - zeam.form.silva
    - zeam.jsontemplate
    - zope.annotation
    - zope.component
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.traversing

  run:
    - python
    - setuptools
    - five.grok
    - infrae.rest
    - js.ckeditor
    - lxml
    - setuptools
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.references
    - silva.core.messages
    - silva.core.services
    - silva.core.views
    - silva.core.xml
    - silva.translations
    - silva.ui
    - tinycss
    - zeam.form.silva
    - zeam.jsontemplate
    - zope.annotation
    - zope.component
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.traversing

test:
  # Python imports
  imports:
    - silva
    - silva.core
    - silva.core.editor
    - silva.core.editor.tests
    - silva.core.editor.transform
    - silva.core.editor.transform.editor
    - silva.core.editor.transform.silvaxml
    - silva.core.editor.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.core.editor
  license: BSD License
  summary: 'Support for the WYSIWYG editor CKEditor in the Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
