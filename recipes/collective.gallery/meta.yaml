package:
  name: collective.gallery
  version: "2.0.1"

source:
  fn: collective.gallery-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.gallery/collective.gallery-2.0.1.tar.gz
  md5: 3f79f4114ae564329ffec2a99c06281d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.gallery = collective.gallery:main
    #
    # Would create an entry point called collective.gallery that calls collective.gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - gdata
    - flickrapi

  run:
    - python
    - setuptools
    - setuptools
    - gdata
    - flickrapi

test:
  # Python imports
  imports:
    - collective
    - collective.gallery
    - collective.gallery.browser
    - collective.gallery.link
    - collective.gallery.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.gallery
  license: GPL
  summary: 'Base gallery for plone with external services support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
