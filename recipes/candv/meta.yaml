package:
  name: candv
  version: "1.3.0"

source:
  fn: candv-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/candv/candv-1.3.0.tar.gz
  md5: 1d52899968e169aecbee2fb249023b81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - candv = candv:main
    #
    # Would create an entry point called candv that calls candv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.6.1

  run:
    - python
    - six >=1.6.1

test:
  # Python imports
  imports:
    - candv
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oblalex/candv
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Constants and Values: create grouped non-standard named constants, add values, verbose names, help texts or anything you like to them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
