package:
  name: blazeutils
  version: "0.5.0"

source:
  fn: BlazeUtils-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BlazeUtils/BlazeUtils-0.5.0.tar.gz
  md5: bc1d90392c7a014904be5c4b0f858756
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blazeutils = blazeutils:main
    #
    # Would create an entry point called blazeutils that calls blazeutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - wrapt

  run:
    - python
    - six
    - wrapt

test:
  # Python imports
  imports:
    - blazeutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/blazelibs/blazeutils/
  license: BSD License
  summary: 'A collection of python utility functions and classes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
