package:
  name: cubicweb-rememberme
  version: "0.3.0"

source:
  fn: cubicweb-rememberme-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-rememberme/cubicweb-rememberme-0.3.0.tar.gz
  md5: ede34bd9800cced5c2951d28ca72aab1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-rememberme = cubicweb-rememberme:main
    #
    # Would create an entry point called cubicweb-rememberme that calls cubicweb-rememberme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0

  run:
    - python
    - cubicweb >=3.19.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-rememberme
  license: LGPL
  summary: 'implementation of the Remember Me feature for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
