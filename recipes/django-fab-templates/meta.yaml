package:
  name: django-fab-templates
  version: "0.1.2"

source:
  fn: django-fab-templates-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fab-templates/django-fab-templates-0.1.2.tar.gz
  md5: 5e9f9a76a24f025bff0a3b688da7d6a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fab-templates = django-fab-templates:main
    #
    # Would create an entry point called django-fab-templates that calls django-fab-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript >=1.3
    - cheetah
    - fabric >=0.9.5
    - jinja2

  run:
    - python
    - setuptools
    - setuptools
    - pastescript >=1.3
    - cheetah
    - fabric >=0.9.5
    - jinja2

test:
  # Python imports
  imports:
    - django_fab_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dreamiurg/django-fab-templates
  license: MIT License
  summary: 'Django project templates that support fabric-style deployment (vagrant+ubuntu, webfaction)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
