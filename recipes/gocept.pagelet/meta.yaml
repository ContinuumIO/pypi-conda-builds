package:
  name: gocept.pagelet
  version: "0.4"

source:
  fn: gocept.pagelet-0.4.zip
  url: https://pypi.python.org/packages/source/g/gocept.pagelet/gocept.pagelet-0.4.zip
  md5: aebebc4e00e551cba8413122bc5c915b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.pagelet = gocept.pagelet:main
    #
    # Would create an entry point called gocept.pagelet that calls gocept.pagelet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.publisher
    - zope.viewlet
    - z3c.template
    - z3c.pagelet
    - zope.browsermenu
    - zope.browserpage

  run:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.publisher
    - zope.viewlet
    - z3c.template
    - z3c.pagelet
    - zope.browsermenu
    - zope.browserpage

test:
  # Python imports
  imports:
    - gocept
    - gocept.pagelet
    - gocept.pagelet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.pagelet
  license: Zope Public License
  summary: 'Easier z3c.pagelet handling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
