package:
  name: flask-menumanager
  version: "1.0a2"

source:
  fn: flask-MenuManager-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-MenuManager/flask-MenuManager-1.0a2.tar.gz
  md5: 8c1508c6e07355ac57ecea6ce9590517
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-menumanager = flask-menumanager:main
    #
    # Would create an entry point called flask-menumanager that calls flask-menumanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_MenuManager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.apache.org/repos/asf/labs/panopticon/
  license: BSD License
  summary: 'An easy way to build and manage menus in Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
