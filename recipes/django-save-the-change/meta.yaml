package:
  name: django-save-the-change
  version: "1.1.0"

source:
  fn: django-save-the-change-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-save-the-change/django-save-the-change-1.1.0.tar.gz
  md5: 0a83e80c4bec178bd3c74424aff785d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-save-the-change = django-save-the-change:main
    #
    # Would create an entry point called django-save-the-change that calls django-save-the-change.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0

  run:
    - python
    - django >=1.5.0

test:
  # Python imports
  imports:
    - save_the_change

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/karanlyons/django-save-the-change
  license: Apache Software License
  summary: 'Automatically save only changed model data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
