package:
  name: bookmark_merger
  version: "0.2.2.1"

source:
  fn: bookmark_merger-0.2.2.1.zip
  url: https://pypi.python.org/packages/source/b/bookmark_merger/bookmark_merger-0.2.2.1.zip
  md5: cd20272b08d7f74cc70ed4c80b2aa342
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bookmark_merger = bookmark_merger:main
    #
    # Would create an entry point called bookmark_merger that calls bookmark_merger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing

  run:
    - python
    - pyparsing

test:
  # Python imports
  imports:
    - bookmark_merger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://sourceforge.net/projects/bookmark-merger/
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'code for merging multiple firefox bookmark.html files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
