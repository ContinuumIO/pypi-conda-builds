package:
  name: isotoma.recipe.plonetools
  version: "0.0.17"

source:
  fn: isotoma.recipe.plonetools-0.0.17.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.plonetools/isotoma.recipe.plonetools-0.0.17.zip
  md5: 1d63b5d24eb8fdd9d84af8c7b64a8c09
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.plonetools = isotoma.recipe.plonetools:main
    #
    # Would create an entry point called isotoma.recipe.plonetools that calls isotoma.recipe.plonetools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.plonetools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.plonetools
  license: Zope Public License
  summary: 'Buildout recipes for setting up a plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
