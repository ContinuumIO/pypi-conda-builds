package:
  name: collective.multimodeview
  version: "0.2"

source:
  fn: collective.multimodeview-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.multimodeview/collective.multimodeview-0.2.zip
  md5: f9ff5d04c696c597df6fc072cfcc6b0f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.multimodeview = collective.multimodeview:main
    #
    # Would create an entry point called collective.multimodeview that calls collective.multimodeview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.multimodeview
    - collective.multimodeview.browser
    - collective.multimodeview.samples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/collective.multimodeview
  license: GPL
  summary: 'Simple package to manage views with multiple modes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
