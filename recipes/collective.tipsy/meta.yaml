package:
  name: collective.tipsy
  version: "1.0.0a"

source:
  fn: collective.tipsy-1.0.0a.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.tipsy/collective.tipsy-1.0.0a.tar.gz
  md5: 76a7c6a2e508fcba018c9beb6219085c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.tipsy = collective.tipsy:main
    #
    # Would create an entry point called collective.tipsy that calls collective.tipsy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.tipsy
    - collective.tipsy.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.tipsy/
  license: GPL
  summary: 'Tipsy for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
