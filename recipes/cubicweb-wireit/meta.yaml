package:
  name: cubicweb-wireit
  version: "0.4.2"

source:
  fn: cubicweb-wireit-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-wireit/cubicweb-wireit-0.4.2.tar.gz
  md5: d3d8704cdb06a63a094f62ce4dd1925c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-wireit = cubicweb-wireit:main
    #
    # Would create an entry point called cubicweb-wireit that calls cubicweb-wireit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.0
    - cubicweb-file

  run:
    - python
    - cubicweb >=3.16.0
    - cubicweb-file

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-wireit
  license: LGPL
  summary: 'WireIt workflow store for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
