package:
  name: pops
  version: "0.2.0"

source:
  fn: pops-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pops/pops-0.2.0.tar.gz
  md5: 8dabf0a0d6a5751fb39d5eda73693c6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pops = pops:main
    #
    # Would create an entry point called pops that calls pops.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-admin-tools >=0.4.1
    - beautifulsoup >=3.2.0
    - django-appconf >=0.4.1
    - django_compressor >=1.3

  run:
    - python
    - django-admin-tools >=0.4.1
    - beautifulsoup >=3.2.0
    - django-appconf >=0.4.1
    - django_compressor >=1.3

test:
  # Python imports
  imports:
    - pops
    - pops.menu
    - pops.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tswicegood/pops
  license: Apache Software License
  summary: "Bootstrap styling for Django's admin"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
