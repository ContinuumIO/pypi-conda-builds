package:
  name: django-secretballot
  version: "0.3.0"

source:
  fn: django-secretballot-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-secretballot/django-secretballot-0.3.0.tar.gz
  md5: 3437f633e8564f544c3e4f19fed647c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-secretballot = django-secretballot:main
    #
    # Would create an entry point called django-secretballot that calls django-secretballot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south

  run:
    - python
    - django
    - south

test:
  # Python imports
  imports:
    - secretballot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/django-secretballot/
  license: BSD License
  summary: 'Django anonymous voting application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
