package:
  name: jmbo-paste
  version: "0.1.7"

source:
  fn: jmbo-paste-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-paste/jmbo-paste-0.1.7.tar.gz
  md5: 946a62c71522e0b41edc1555b9f48d9d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-paste = jmbo-paste:main
    #
    # Would create an entry point called jmbo-paste that calls jmbo-paste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cheetah >=2.4.2.1
    - pastescript
    - setuptools

  run:
    - python
    - setuptools
    - cheetah >=2.4.2.1
    - pastescript
    - setuptools

test:
  # Python imports
  imports:
    - jmbo_paste
    - jmbo_paste.config

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/praekelt/jmbo-paste
  license: BSD License
  summary: 'Python Paste templates creating Jmbo Buildout environments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
