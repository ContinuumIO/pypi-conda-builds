package:
  name: ldap-notify
  version: "0.9.6"

source:
  fn: ldap-notify-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/l/ldap-notify/ldap-notify-0.9.6.tar.gz
  md5: 1e994c5023ebabe66b0ace8ac73a8cfa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldap-notify = ldap-notify:main
    #
    # Would create an entry point called ldap-notify that calls ldap-notify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap

  run:
    - python
    - python-ldap

test:
  # Python imports
  imports:
    - ldap_notify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sttts/ldap_notify
  license: GNU General Public License (GPL)
  summary: 'LDAP Login/Password Email Notification Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
