package:
  name: foundry
  version: "0.0.3"

source:
  fn: foundry-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/foundry/foundry-0.0.3.tar.gz
  md5: 3e54a16f7f523bc25384e8d5c59de51c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foundry = foundry:main
    #
    # Would create an entry point called foundry that calls foundry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.0,<4.a0

  run:
    - python
    - pyyaml >=3.0,<4.a0

test:
  # Python imports
  imports:
    - foundry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eriktaubeneck/foundry
  license: MIT License
  summary: 'Casting objects from seed data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
