package:
  name: cloudeebus
  version: "0.6.0"

source:
  fn: cloudeebus-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudeebus/cloudeebus-0.6.0.tar.gz
  md5: d431ff9f9150cf7ba0f6f89980789a32
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudeebus = cloudeebus:main
    #
    # Would create an entry point called cloudeebus that calls cloudeebus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - autobahn ==0.5.8

  run:
    - python
    - setuptools
    - autobahn ==0.5.8

test:
  # Python imports
  imports:
    - cloudeebus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/01org/cloudeebus/wiki
  license: Apache Software License
  summary: 'Javascript-DBus bridge'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
