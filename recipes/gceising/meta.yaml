package:
  name: gceising
  version: "1.0"

source:
  fn: gceising-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gceising/gceising-1.0.tar.gz
  md5: d06d61ba0c9305731a3d6982bd3796fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gceising = gceising:main
    #
    # Would create an entry point called gceising that calls gceising.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gceising

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/gce-ising/
  license: GNU General Public License (GPL)
  summary: 'Monte Carlo simulator of Generalized Canonical Ensemble of Ising Model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
