package:
  name: python-briefdruckzentrum
  version: "0.1.2"

source:
  fn: python-briefdruckzentrum-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-briefdruckzentrum/python-briefdruckzentrum-0.1.2.tar.gz
  md5: b9c2c2701d7a8c2e2a33caacf705a5f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-briefdruckzentrum = python-briefdruckzentrum:main
    #
    # Would create an entry point called python-briefdruckzentrum that calls python-briefdruckzentrum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3
    - xmltodict >=0.9
    - money >=1.2

  run:
    - python
    - requests >=2.3
    - xmltodict >=0.9
    - money >=1.2

test:
  # Python imports
  imports:
    - briefdruckzentrum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Thermondo/python-briefdruckzentrum
  license: MIT License
  summary: 'a lightweight python wrapper for briefdruckzentrum.de API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
