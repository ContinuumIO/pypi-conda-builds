package:
  name: django-secdownload-storage
  version: "0.1.1"

source:
  fn: django-secdownload-storage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-secdownload-storage/django-secdownload-storage-0.1.1.tar.gz
  md5: 5aca50600dd235ae80e07afaac67980c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-secdownload-storage = django-secdownload-storage:main
    #
    # Would create an entry point called django-secdownload-storage that calls django-secdownload-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - secdownload_storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ionelmc/django-secdownload-storage
  license: BSD License
  summary: "Django storage backend that can be used to serve files via lighttpd's mod_secdownload module. This \nstorage backend is an extension to the reqular FileSystemStorage that will generate proper signed \ndownload urls."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
