package:
  name: lisa-plugin-wifiledlamps
  version: "1.2.2"

source:
  fn: lisa-plugin-Wifiledlamps-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lisa-plugin-Wifiledlamps/lisa-plugin-Wifiledlamps-1.2.2.tar.gz
  md5: 883397cc1883f598d7472794bcf8c48d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lisa-plugin-wifiledlamps = lisa-plugin-wifiledlamps:main
    #
    # Would create an entry point called lisa-plugin-wifiledlamps that calls lisa-plugin-wifiledlamps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.2,<1.7
    - django-tastypie >=0.11.0
    - wifileds

  run:
    - python
    - django >=1.6.2,<1.7
    - django-tastypie >=0.11.0
    - wifileds

test:
  # Python imports
  imports:
    - lisa.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.lisa-project.net
  license: MIT License
  summary: 'LISA home automation system - Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
