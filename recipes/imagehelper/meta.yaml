package:
  name: imagehelper
  version: "0.3.0rc9"

source:
  fn: imagehelper-0.3.0rc9.tar.gz
  url: https://pypi.python.org/packages/source/i/imagehelper/imagehelper-0.3.0rc9.tar.gz
  md5: f85cdb4e9aa892a0d17b14688498b565
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imagehelper = imagehelper:main
    #
    # Would create an entry point called imagehelper that calls imagehelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - envoy

  run:
    - python
    - envoy

test:
  # Python imports
  imports:
    - imagehelper
    - imagehelper.saver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jvanasco/imagehelper
  license: BSD License
  summary: 'simple utilites for image resizing and uploading and stuff like that'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
