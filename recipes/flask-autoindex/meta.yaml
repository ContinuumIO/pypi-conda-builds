package:
  name: flask-autoindex
  version: "0.5"

source:
  fn: Flask-AutoIndex-0.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-AutoIndex/Flask-AutoIndex-0.5.tar.gz
  md5: 24984602365704737468bb4d2586a739
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-autoindex = flask-autoindex:main
    #
    # Would create an entry point called flask-autoindex that calls flask-autoindex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - flask-silk >=0.2

  run:
    - python
    - flask >=0.8
    - flask-silk >=0.2

test:
  # Python imports
  imports:
    - flask_autoindex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/Flask-AutoIndex
  license: BSD License
  summary: 'The mod_autoindex for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
