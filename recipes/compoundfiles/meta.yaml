package:
  name: compoundfiles
  version: "0.3"

source:
  fn: compoundfiles-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/compoundfiles/compoundfiles-0.3.tar.gz
  md5: f816df9d4777001c56770ce38fa76682
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compoundfiles = compoundfiles:main
    #
    # Would create an entry point called compoundfiles that calls compoundfiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - compoundfiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://compound-files.readthedocs.org/
  license: MIT License
  summary: u'Library for parsing and reading OLE Compound Documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
