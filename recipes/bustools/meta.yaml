package:
  name: bustools
  version: "0.1.dev0"

source:
  fn: bustools-0.1.dev0.tar.gz
  url: https://pypi.python.org/packages/source/b/bustools/bustools-0.1.dev0.tar.gz
  md5: 5fc9bae467872d9fb388bf55e0640452
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bustools = bustools:main
    #
    # Would create an entry point called bustools that calls bustools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bustools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FlyingCampDesign/bustools
  license: BSD
  summary: 'Python tools/wrappers for electrical busses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
