package:
  name: jhashcode
  version: "0.03"

source:
  fn: jhashcode-0.03.tar.gz
  url: https://pypi.python.org/packages/source/j/jhashcode/jhashcode-0.03.tar.gz
  md5: a0dce2e1d12d15803b6679faf567f52b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jhashcode = jhashcode:main
    #
    # Would create an entry point called jhashcode that calls jhashcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jhashcode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neuront/pyjhashcode
  license: MIT
  summary: 'Python implemented Java `hashCode` methods.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
