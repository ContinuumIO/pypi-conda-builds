package:
  name: plone.app.contentlisting
  version: "1.1.3"

source:
  fn: plone.app.contentlisting-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.contentlisting/plone.app.contentlisting-1.1.3.tar.gz
  md5: bae5850eb98168e256bbddd17b10358b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.contentlisting = plone.app.contentlisting:main
    #
    # Would create an entry point called plone.app.contentlisting that calls plone.app.contentlisting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.uuid

  run:
    - python
    - setuptools
    - setuptools
    - plone.uuid

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.contentlisting
    - plone.app.contentlisting.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.contentlisting
  license: GPL version 2
  summary: 'Listing of content for the Plone CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
