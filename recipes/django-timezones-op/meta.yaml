package:
  name: django-timezones-op
  version: "0.2.dev2"

source:
  fn: django-timezones-op-0.2.dev2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timezones-op/django-timezones-op-0.2.dev2.tar.gz
  md5: ad877b180b13759bc252dc2c0579a90e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timezones-op = django-timezones-op:main
    #
    # Would create an entry point called django-timezones-op that calls django-timezones-op.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - timezones
    - timezones.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/OpenProximity/django-timezones/
  license: BSD License
  summary: 'A Django reusable app to deal with timezone localization for users modified for OP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
