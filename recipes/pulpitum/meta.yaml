package:
  name: pulpitum
  version: "0.2.2"

source:
  fn: pulpitum-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pulpitum/pulpitum-0.2.2.tar.gz
  md5: 1e863a1eb52978e8c21dbfa5322e51d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pulpitum = pulpitum:main
    #
    # Would create an entry point called pulpitum that calls pulpitum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pkgtools

  run:
    - python
    - pkgtools

test:
  # Python imports
  imports:
    - pulpitum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/requirements/pulpitum
  license: GNU General Public License v3 (GPLv3)
  summary: 'PyPI client for Humans\xe2\x84\xa2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
