package:
  name: minibooks
  version: "0.2.0"

source:
  fn: minibooks-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/minibooks/minibooks-0.2.0.tar.gz
  md5: f21ed2428976f51fa36ba2fea0231a28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minibooks = minibooks:main
    #
    # Would create an entry point called minibooks that calls minibooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - minibooks
    - minibooks.call_log
    - minibooks.ledger
    - minibooks.ledger.management
    - minibooks.ledger.management.commands
    - minibooks.ledger.templatetags
    - minibooks.trac_tickets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/caktus/minibooks
  license: BSD License
  summary: 'minibooks is a simple CRM, bookkeeping and double entry accounting package for the Django web framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
