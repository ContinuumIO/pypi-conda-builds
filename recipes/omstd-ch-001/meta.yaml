package:
  name: omstd-ch-001
  version: "0.1.1"

source:
  fn: OMSTD-ch-001-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/O/OMSTD-ch-001/OMSTD-ch-001-0.1.1.tar.gz
  md5: 77bc139bcb636250610d10ac82587f02
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omstd-ch-001 = omstd-ch-001:main
    #
    # Would create an entry point called omstd-ch-001 that calls omstd-ch-001.main()

    - omstd-ch-001 = omstd_ch_001.ch_001:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - omstd_ch_001
    - omstd_ch_001.lib

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - omstd-ch-001 --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cr0hn/OMSTD/tree/master/examples/cracking/ch-001
  license: BSD License
  summary: 'OMSTD CH-001 study case: cracking MD5 password using online resolver services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
