package:
  name: django-sendsms
  version: "0.2.2"

source:
  fn: django-sendsms-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sendsms/django-sendsms-0.2.2.tar.gz
  md5: 6a7eb207863ef41e2ca8c7cd5df543ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sendsms = django-sendsms:main
    #
    # Would create an entry point called django-sendsms that calls django-sendsms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sendsms
    - sendsms.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stefanfoulis/django-sendsms
  license: BSD License
  summary: 'A simple API to send SMS messages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
