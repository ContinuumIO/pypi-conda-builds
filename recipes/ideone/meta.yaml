package:
  name: ideone
  version: "0.2.0"

source:
  fn: ideone-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ideone/ideone-0.2.0.tar.gz
  md5: caf352b7bd4398b0c27949123868150c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ideone = ideone:main
    #
    # Would create an entry point called ideone that calls ideone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds

  run:
    - python
    - suds

test:
  # Python imports
  imports:
    - ideone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jschaf/ideone-api/
  license: BSD License
  summary: 'A Python binding to the Ideone (Online Compiler) API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
