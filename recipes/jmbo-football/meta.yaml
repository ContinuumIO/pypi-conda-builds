package:
  name: jmbo-football
  version: "0.3.3"

source:
  fn: jmbo-football-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-football/jmbo-football-0.3.3.tar.gz
  md5: 9bf2eea1effc440ea6c24be88ee22e25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-football = jmbo-football:main
    #
    # Would create an entry point called jmbo-football that calls jmbo-football.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - django-football365
    - jmbo-foundry >=0.5.1

  run:
    - python
    - lxml
    - django-football365
    - jmbo-foundry >=0.5.1

test:
  # Python imports
  imports:
    - football
    - football.management
    - football.management.commands
    - football.migrations
    - football.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-football
  license: BSD License
  summary: 'Football app for Jmbo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
