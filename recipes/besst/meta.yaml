package:
  name: besst
  version: "1.3.7"

source:
  fn: BESST-1.3.7.tar.gz
  url: https://pypi.python.org/packages/source/B/BESST/BESST-1.3.7.tar.gz
  md5: f032ee870c37a05b5f107d260e7fa71d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - besst = besst:main
    #
    # Would create an entry point called besst that calls besst.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysam >=0.7
    - networkx >=1.9
    - mathstats >=0.1.5
    - scipy >=0.9

  run:
    - python
    - pysam >=0.7
    - networkx >=1.9
    - mathstats >=0.1.5
    - scipy >=0.9

test:
  # Python imports
  imports:
    - BESST
    - BESST.diploid
    - BESST.statistics
    - besst

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ksahlin/BESST
  license: GPLv3
  summary: 'Scaffolder for genomic assemblies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
