package:
  name: django-url-tracker
  version: "0.1.3"

source:
  fn: django-url-tracker-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-tracker/django-url-tracker-0.1.3.tar.gz
  md5: 792d04f0514fd493e8e07684ef63f4e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-tracker = django-url-tracker:main
    #
    # Would create an entry point called django-url-tracker that calls django-url-tracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - south >=0.7.3

  run:
    - python
    - django >=1.3.1
    - south >=0.7.3

test:
  # Python imports
  imports:
    - url_tracker
    - url_tracker.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tangentlabs/django-url-tracker
  license: Apache Software License or BSD License
  summary: 'A little app that trackes URL changes in a database table to provide HTTP 301 & 410 on request.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
