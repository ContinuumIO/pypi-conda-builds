package:
  name: datachef
  version: "0.3.2"

source:
  fn: datachef-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/datachef/datachef-0.3.2.tar.gz
  md5: ee9cef53f7692b849dabda7ffc2dd074
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datachef = datachef:main
    #
    # Would create an entry point called datachef that calls datachef.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - datachef
    - datachef.exhibit
    - datachef.geo
    - datachef.ids
    - datachef.square
    - datachef.thirdparty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://uche.ogbuji.net
  license: UNKNOWN
  summary: 'Data chef recipes, a grab bag for dealing with all sorts of data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
