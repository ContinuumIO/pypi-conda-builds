package:
  name: dolmen.widget.tinymce
  version: "1.0b3"

source:
  fn: dolmen.widget.tinymce-1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/d/dolmen.widget.tinymce/dolmen.widget.tinymce-1.0b3.tar.gz
  md5: 80ee3648977d80959bf391826eb1e98c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolmen.widget.tinymce = dolmen.widget.tinymce:main
    #
    # Would create an entry point called dolmen.widget.tinymce that calls dolmen.widget.tinymce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - grokcore.view
    - js.jquery
    - js.tinymce
    - setuptools
    - zeam.form.base >=1.0
    - zeam.form.ztk >=1.0
    - zope.interface

  run:
    - python
    - setuptools
    - fanstatic
    - grokcore.view
    - js.jquery
    - js.tinymce
    - setuptools
    - zeam.form.base >=1.0
    - zeam.form.ztk >=1.0
    - zope.interface

test:
  # Python imports
  imports:
    - dolmen
    - dolmen.widget
    - dolmen.widget.tinymce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitweb.dolmen-project.org/
  license: GNU General Public License (GPL)
  summary: 'A rich text widget using TinyMCE for Dolmen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
