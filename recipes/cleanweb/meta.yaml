package:
  name: cleanweb
  version: "2.0"

source:
  fn: cleanweb-2.0.zip
  url: https://pypi.python.org/packages/source/c/cleanweb/cleanweb-2.0.zip
  md5: ed31bfa1fc1f4f95a632c89aa2b0d937
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cleanweb = cleanweb:main
    #
    # Would create an entry point called cleanweb that calls cleanweb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0

  run:
    - python
    - requests >=1.0.0

test:
  # Python imports
  imports:
    - cleanweb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coagulant/cleanweb
  license: MIT License
  summary: 'Python wrapper for cleanweb API of Yandex to fight spam.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
