package:
  name: dbaas_cloudstack
  version: "0.7.1"

source:
  fn: dbaas_cloudstack-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dbaas_cloudstack/dbaas_cloudstack-0.7.1.tar.gz
  md5: 54a6c3e3b2cb394c1021a247d8d1f025
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbaas_cloudstack = dbaas_cloudstack:main
    #
    # Would create an entry point called dbaas_cloudstack that calls dbaas_cloudstack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dbaas_credentials
    - dbaas_flipper

  run:
    - python
    - dbaas_credentials
    - dbaas_flipper

test:
  # Python imports
  imports:
    - dbaas_cloudstack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/dbaas-cloudstack
  license: BSD License
  summary: 'A cloudstack adapter to DBaaS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
