package:
  name: django-html5-colorfield
  version: "1.0"

source:
  fn: django-html5-colorfield-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-html5-colorfield/django-html5-colorfield-1.0.zip
  md5: 94bb093f098459f31c3dd6618bb3a42c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-html5-colorfield = django-html5-colorfield:main
    #
    # Would create an entry point called django-html5-colorfield that calls django-html5-colorfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - colorfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/knyghty/django-html5-colorfield
  license: MIT License
  summary: 'Provides an HTML5 color django model field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
