package:
  name: django-better500s
  version: "0.2.1"

source:
  fn: django-better500s-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-better500s/django-better500s-0.2.1.tar.gz
  md5: 986886b84d5d14ad5ba6523a67d5d31e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-better500s = django-better500s:main
    #
    # Would create an entry point called django-better500s that calls django-better500s.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - better500s

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skoczen/django-better500s
  license: MIT License
  summary: 'Library for Django that provides better error logging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
