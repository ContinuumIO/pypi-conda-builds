package:
  name: django-coreutils
  version: "0.1.10"

source:
  fn: django-coreutils-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-coreutils/django-coreutils-0.1.10.tar.gz
  md5: 8b95604105034fc44232ef1eb4924fb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-coreutils = django-coreutils:main
    #
    # Would create an entry point called django-coreutils that calls django-coreutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.5.1

  run:
    - python
    - django ==1.5.1

test:
  # Python imports
  imports:
    - coreutils
    - coreutils.templatetags
    - coreutils.tests
    - coreutils.tests.templatetags
    - coreutils.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Django core components and utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
