package:
  name: django-videoembed
  version: "0.3.1-1"

source:
  fn: django-videoembed-0.3.1-1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-videoembed/django-videoembed-0.3.1-1.tar.gz
  md5: bb836147b786f859b157c7d008b9d708
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-videoembed = django-videoembed:main
    #
    # Would create an entry point called django-videoembed that calls django-videoembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - videoembed
    - videoembed.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marcinn/django-videoembed
  license: MIT License
  summary: 'Easy embedding videos with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
