package:
  name: alm.solrindex
  version: "1.1.1"

source:
  fn: alm.solrindex-1.1.1.zip
  url: https://pypi.python.org/packages/source/a/alm.solrindex/alm.solrindex-1.1.1.zip
  md5: d96fbfd3144eb6124ebd165982f8fa05
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alm.solrindex = alm.solrindex:main
    #
    # Would create an entry point called alm.solrindex that calls alm.solrindex.main()

    - waituri=alm.solrindex.scripts.waituri:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - alm
    - alm.solrindex
    - alm.solrindex.scripts
    - alm.solrindex.tests

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - waituri --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'A ZCatalog multi-index that uses Solr\n\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
