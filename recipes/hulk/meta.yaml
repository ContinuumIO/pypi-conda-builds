package:
  name: hulk
  version: "0.2.2"

source:
  fn: hulk-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hulk/hulk-0.2.2.tar.gz
  md5: 826c1eb41bed22f3abd6c640590084bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hulk = hulk:main
    #
    # Would create an entry point called hulk that calls hulk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests
    - docopt

  run:
    - python
    - flask
    - requests
    - docopt

test:
  # Python imports
  imports:
    - hulk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Strathcom/hulk
  license: MIT License
  summary: 'Big dumb proxy server for testing service-heavy applications based on requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
