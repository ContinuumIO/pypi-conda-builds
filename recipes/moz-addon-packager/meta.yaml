package:
  name: moz-addon-packager
  version: "1.0.1"

source:
  fn: moz-addon-packager-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/moz-addon-packager/moz-addon-packager-1.0.1.tar.gz
  md5: 4cc2879969f839388c6f56f786fa01e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moz-addon-packager = moz-addon-packager:main
    #
    # Would create an entry point called moz-addon-packager that calls moz-addon-packager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - packager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mattbasta/addon-packager
  license: BSD License
  summary: 'Creates addons for Mozilla products.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
