package:
  name: django-rest-resources
  version: "0.2dev"

source:
  fn: django-rest-resources-0.2dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-resources/django-rest-resources-0.2dev.tar.gz
  md5: 5f562fd22d61986cfde1a287cc37c341
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-resources = django-rest-resources:main
    #
    # Would create an entry point called django-rest-resources that calls django-rest-resources.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisguidry/django-rest-resources
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
