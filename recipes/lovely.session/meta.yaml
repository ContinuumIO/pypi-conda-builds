package:
  name: lovely.session
  version: "0.3.0"

source:
  fn: lovely.session-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.session/lovely.session-0.3.0.tar.gz
  md5: 90a1cad7bb294e134f5244788d9c78da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.session = lovely.session:main
    #
    # Would create an entry point called lovely.session that calls lovely.session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lovely.memcached
    - zope.container
    - zope.session
    - zope.schema
    - zope.interface

  run:
    - python
    - setuptools
    - lovely.memcached
    - zope.container
    - zope.session
    - zope.schema
    - zope.interface

test:
  # Python imports
  imports:
    - lovely
    - lovely.session

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/lovely.session
  license: Zope Public License
  summary: 'memcache-based session storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
