package:
  name: django-pagseguro
  version: "1.4.2"

source:
  fn: django-pagseguro-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pagseguro/django-pagseguro-1.4.2.tar.gz
  md5: 5835eff46240e589d154ac36b304dd2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pagseguro = django-pagseguro:main
    #
    # Would create an entry point called django-pagseguro that calls django-pagseguro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1

  run:
    - python
    - django >=1.1

test:
  # Python imports
  imports:
    - django_pagseguro
    - django_pagseguro.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fabiocerqueira/django-pagseguro
  license: UNKNOWN
  summary: 'A pluggable Django application for integrating PagSeguro payment system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
