package:
  name: django-usernotes
  version: "0.1.1dev"

source:
  fn: django-usernotes-0.1.1dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-usernotes/django-usernotes-0.1.1dev.tar.gz
  md5: 3bedfb367d566c33fa7bdf80855eaa4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-usernotes = django-usernotes:main
    #
    # Would create an entry point called django-usernotes that calls django-usernotes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - usernotes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
