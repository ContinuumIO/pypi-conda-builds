package:
  name: django-improved-inlines
  version: "0.2.2"

source:
  fn: django-improved-inlines-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-improved-inlines/django-improved-inlines-0.2.2.tar.gz
  md5: 58e3eadc60ce7b7cd723a6761b44123e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-improved-inlines = django-improved-inlines:main
    #
    # Would create an entry point called django-improved-inlines that calls django-improved-inlines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup >=3.2.0

  run:
    - python
    - beautifulsoup >=3.2.0

test:
  # Python imports
  imports:
    - improved_inlines
    - improved_inlines.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/issackelly/django-improved-inlines
  license: BSD License
  summary: 'Inline object rendering for django, based on django-basic-apps + filters + templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
