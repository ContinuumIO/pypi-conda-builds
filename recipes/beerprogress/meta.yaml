package:
  name: beerprogress
  version: "0.0.2"

source:
  fn: beerprogress-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/beerprogress/beerprogress-0.0.2.tar.gz
  md5: 04748aad1518fb639eb3ad36a3640bab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beerprogress = beerprogress:main
    #
    # Would create an entry point called beerprogress that calls beerprogress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psutil ==2.1.1

  run:
    - python
    - psutil ==2.1.1

test:
  # Python imports
  imports:
    - beerprogress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fcvarela/beerprogress
  license: UNKNOWN
  summary: 'Command line progress indicator library for Python3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
