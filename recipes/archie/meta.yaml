package:
  name: archie
  version: "0.0.3"

source:
  fn: archie-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/archie/archie-0.0.3.tar.gz
  md5: ec3567c4e3008a34485f1762bb959443
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archie = archie:main
    #
    # Would create an entry point called archie that calls archie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - docopt

  run:
    - python
    - docopt

test:
  # Python imports
  imports:
    - archie
    - archie.handlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/fudanchii/archie
  license: MIT License
  summary: 'Symlink management for configuration (rc) file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
