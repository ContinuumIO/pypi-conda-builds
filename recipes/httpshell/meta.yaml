package:
  name: httpshell
  version: "0.8.0"

source:
  fn: httpshell-0.8.0.tar.gz
  url: http://github.com/downloads/chrislongo/HttpShell/httpshell-0.8.0.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpshell = httpshell:main
    #
    # Would create an entry point called httpshell that calls httpshell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments >=1.1.1
    - httplib2 >=0.7.0
    - oauth2 >=1.5

  run:
    - python
    - pygments >=1.1.1
    - httplib2 >=0.7.0
    - oauth2 >=1.5

test:
  # Python imports
  imports:
    - httpshell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrislongo/HttpShell/
  license: MIT License
  summary: 'An interactive shell for issuing HTTP commands to a web server or REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
