package:
  name: jinjatools
  version: "0.1.4"

source:
  fn: jinjatools-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/j/jinjatools/jinjatools-0.1.4.tar.gz
  md5: cb7417dfd9687125101ad28901640620
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinjatools = jinjatools:main
    #
    # Would create an entry point called jinjatools that calls jinjatools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jinjatools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Various tools for Jinja2, including new filters and tests based on moretools, a JinjaLoader class for Django, and a simple JinjaBuilder class for SCons.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
