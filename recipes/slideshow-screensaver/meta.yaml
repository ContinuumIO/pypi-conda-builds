package:
  name: slideshow-screensaver
  version: "0.3.1"

source:
  fn: slideshow-screensaver-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/s/slideshow-screensaver/slideshow-screensaver-0.3.1.tar.gz
  md5: c1de935d2455f418a994a609010b6b80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slideshow-screensaver = slideshow-screensaver:main
    #
    # Would create an entry point called slideshow-screensaver that calls slideshow-screensaver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - pygame

  run:
    - python
    - distribute
    - pygame

test:
  # Python imports
  imports:
    - slidesaver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jamesadney/slideshow-screensaver
  license: GPL3
  summary: 'Slideshow screensaver for Gnome based on Pygame'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
