package:
  name: cwr-api
  version: "0.0.13"

source:
  fn: CWR-API-0.0.13.zip
  url: https://pypi.python.org/packages/source/C/CWR-API/CWR-API-0.0.13.zip
  md5: 44f6f2695f2e2ba4910ddaa32043dc7a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cwr-api = cwr-api:main
    #
    # Would create an entry point called cwr-api that calls cwr-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - chardet
    - pyparsing
    - pyyaml
    - setuptools

  run:
    - python
    - chardet
    - pyparsing
    - pyyaml
    - setuptools

test:
  # Python imports
  imports:
    - cwr
    - cwr.constraints
    - cwr.grammar
    - cwr.grammar.factory
    - cwr.grammar.field
    - cwr.parser
    - cwr.utils
    - data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/weso/CWR-DataApi
  license: MIT License
  summary: 'API library for the CWR standard format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
