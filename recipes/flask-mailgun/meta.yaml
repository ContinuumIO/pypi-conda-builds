package:
  name: flask-mailgun
  version: "0.4"

source:
  fn: Flask-Mailgun-0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Mailgun/Flask-Mailgun-0.4.tar.gz
  md5: cb1786b5f8542293b06d937ba0f4a077
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mailgun = flask-mailgun:main
    #
    # Would create an entry point called flask-mailgun that calls flask-mailgun.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests

  run:
    - python
    - flask
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sleekslush/flask-mailgun
  license: BSD License
  summary: 'Adds Mailgun support to Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
