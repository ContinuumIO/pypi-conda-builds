package:
  name: django-suit-ckeditor
  version: "0.0.2"

source:
  fn: django-suit-ckeditor-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-suit-ckeditor/django-suit-ckeditor-0.0.2.tar.gz
  md5: 8b278cd62ed2457a9b202d0850849280
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-suit-ckeditor = django-suit-ckeditor:main
    #
    # Would create an entry point called django-suit-ckeditor that calls django-suit-ckeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - suit_ckeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/darklow/django-suit-ckeditor
  license: UNKNOWN
  summary: 'CKEditor (WYSIWYG editor) integration app for Django admin. http://ckeditor.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
