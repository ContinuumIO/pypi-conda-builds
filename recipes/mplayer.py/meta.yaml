package:
  name: mplayer.py
  version: "0.7.0"

source:
  fn: mplayer.py-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mplayer.py/mplayer.py-0.7.0.tar.gz
  md5: 8440abf66111b3c5437b32e896b0c44e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mplayer.py = mplayer.py:main
    #
    # Would create an entry point called mplayer.py that calls mplayer.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mplayer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-mplayer/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Lightweight and dynamic MPlayer wrapper with a Pythonic API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
