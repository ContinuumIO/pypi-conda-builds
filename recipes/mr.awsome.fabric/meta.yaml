package:
  name: mr.awsome.fabric
  version: "1.0b3"

source:
  fn: mr.awsome.fabric-1.0b3.zip
  url: https://pypi.python.org/packages/source/m/mr.awsome.fabric/mr.awsome.fabric-1.0b3.zip
  md5: 0a58a9dcd34775f69ea0e8fe5dbff2f2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.awsome.fabric = mr.awsome.fabric:main
    #
    # Would create an entry point called mr.awsome.fabric that calls mr.awsome.fabric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mr.awsome >=1.0rc2
    - fabric >=1.3.0,!=1.8.3

  run:
    - python
    - setuptools
    - setuptools
    - mr.awsome >=1.0rc2
    - fabric >=1.3.0,!=1.8.3

test:
  # Python imports
  imports:
    - mr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fschulze/mr.awsome.fabric
  license: UNKNOWN
  summary: 'A plugin for mr.awsome providing integration with Fabric.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
