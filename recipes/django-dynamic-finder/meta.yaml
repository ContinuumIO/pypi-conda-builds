package:
  name: django-dynamic-finder
  version: "0.2"

source:
  fn: django-dynamic-finder-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamic-finder/django-dynamic-finder-0.2.tar.gz
  md5: 0cfa5d68ff755b501b502e9298c86d7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-finder = django-dynamic-finder:main
    #
    # Would create an entry point called django-dynamic-finder that calls django-dynamic-finder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_dynamic_finder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/luanfonceca/django-dynamic-finder
  license: MIT License
  summary: 'A Django version of the Rails Dynamic Finder, using Django Managers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
