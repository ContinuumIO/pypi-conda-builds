package:
  name: aigo
  version: "0.1.0"

source:
  fn: AIGO-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/A/AIGO/AIGO-0.1.0.tar.gz
  md5: a85939b0f89c4f312e6ff7a751d3990d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aigo = aigo:main
    #
    # Would create an entry point called aigo that calls aigo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - AIGO
    - AIGO.go
    - AIGO.gui
    - AIGO.utils
    - aigo
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/AIGO/
  license: LICENSE.txt
  summary: 'Analysis and Inter-comparison of Gene Ontology functional annotations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
