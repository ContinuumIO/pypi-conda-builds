package:
  name: gathercontent
  version: "0.2"

source:
  fn: gathercontent-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gathercontent/gathercontent-0.2.tar.gz
  md5: 7a13fef369011af7a932af4602f3b197
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gathercontent = gathercontent:main
    #
    # Would create an entry point called gathercontent that calls gathercontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.5.1

  run:
    - python
    - requests ==2.5.1

test:
  # Python imports
  imports:
    - gathercontent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/wearefarm/gathercontent
  license: MIT License
  summary: 'A very simple API wrapper for Gather Content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
