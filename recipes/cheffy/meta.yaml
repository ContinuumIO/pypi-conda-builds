package:
  name: cheffy
  version: "0.3.8"

source:
  fn: cheffy-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/c/cheffy/cheffy-0.3.8.tar.gz
  md5: 3f134ba48ba843799d3713066f299e61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cheffy = cheffy:main
    #
    # Would create an entry point called cheffy that calls cheffy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m2crypto >=0.21
    - requests >=1.2.3

  run:
    - python
    - m2crypto >=0.21
    - requests >=1.2.3

test:
  # Python imports
  imports:
    - cheffy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/cheffy
  license: LICENSE
  summary: 'A simple requests based wrapper for a Chef server api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
