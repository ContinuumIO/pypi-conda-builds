package:
  name: metalocgramn
  version: "0.99"

source:
  fn: MetaLocGramN-0.99.tar.gz
  url: https://pypi.python.org/packages/source/M/MetaLocGramN/MetaLocGramN-0.99.tar.gz
  md5: 77128861a2060429c7593423b522de0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metalocgramn = metalocgramn:main
    #
    # Would create an entry point called metalocgramn that calls metalocgramn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds >=0.04

  run:
    - python
    - suds >=0.04

test:
  # Python imports
  imports:
    - metalocgramn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iimcb.genesilico.pl/MetaLocGramN/home
  license: GPLv3
  summary: 'MetaLocGramN: a method for subcellular localization prediction of Gram-negative proteins.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
