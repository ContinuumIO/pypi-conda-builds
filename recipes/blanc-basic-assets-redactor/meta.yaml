package:
  name: blanc-basic-assets-redactor
  version: "0.1"

source:
  fn: blanc-basic-assets-redactor-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/blanc-basic-assets-redactor/blanc-basic-assets-redactor-0.1.tar.gz
  md5: fc7d113b876cd558c79842c309b42512
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blanc-basic-assets-redactor = blanc-basic-assets-redactor:main
    #
    # Would create an entry point called blanc-basic-assets-redactor that calls blanc-basic-assets-redactor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - blanc_basic_assets_redactor
    - blanc_basic_assets_redactor.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.blanctools.com/
  license: BSD License
  summary: 'Blanc Basic Assets RedactorJS Upload for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
