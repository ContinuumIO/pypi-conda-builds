package:
  name: django_internal_urls
  version: "0.1.0-2"

source:
  fn: django_internal_urls-0.1.0-2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_internal_urls/django_internal_urls-0.1.0-2.tar.gz
  md5: 033ce60497f2d93b113b2540df5c0627
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_internal_urls = django_internal_urls:main
    #
    # Would create an entry point called django_internal_urls that calls django_internal_urls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_internal_urls
    - django_internal_urls.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ddanier/django_internal_urls
  license: BSD License
  summary: 'Add modular url callbacks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
