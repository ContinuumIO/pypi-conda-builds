package:
  name: slbackup
  version: "2.0"

source:
  fn: slbackup-2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/slbackup/slbackup-2.0.tar.gz
  md5: 24d9dbbf9f84137bf99674f60a1197e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slbackup = slbackup:main
    #
    # Would create an entry point called slbackup that calls slbackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - softlayer-object-storage >=0.4.6

  run:
    - python
    - softlayer-object-storage >=0.4.6

test:
  # Python imports
  imports:
    - slbackup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/softlayer/softlayer-object-storage-backup
  license: MIT License
  summary: 'Backup utility for SWIFT object storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
