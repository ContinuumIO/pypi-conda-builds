package:
  name: django_branding
  version: "0.0.6"

source:
  fn: django_branding-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django_branding/django_branding-0.0.6.tar.gz
  md5: 4ff9864b9070d419c24790d65b8553ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_branding = django_branding:main
    #
    # Would create an entry point called django_branding that calls django_branding.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - branding
    - branding.tests
    - django_branding

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Lispython/django-branding
  license: UNKNOWN
  summary: 'Add branding headers to you response'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
