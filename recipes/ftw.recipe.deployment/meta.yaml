package:
  name: ftw.recipe.deployment
  version: "1.1.1"

source:
  fn: ftw.recipe.deployment-1.1.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.recipe.deployment/ftw.recipe.deployment-1.1.1.zip
  md5: cc4aba7966142d7cdde97ffc0cc8db3f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.recipe.deployment = ftw.recipe.deployment:main
    #
    # Would create an entry point called ftw.recipe.deployment that calls ftw.recipe.deployment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - ftw
    - ftw.recipe
    - ftw.recipe.deployment

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.recipe.deployment
  license: Zope Public License
  summary: 'A zc.buildout recipe for Plone deployments which configures various unix system services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
