package:
  name: django-cache-magic
  version: "0.1.0"

source:
  fn: django-cache-magic-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-magic/django-cache-magic-0.1.0.tar.gz
  md5: 3259854a653c1c411803c3e5a9b9c9c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-magic = django-cache-magic:main
    #
    # Would create an entry point called django-cache-magic that calls django-cache-magic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - cachemagic
    - test_project
    - test_project.sample_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ntucker/django-cache-magic
  license: BSD License
  summary: u'Django Cache Magic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
