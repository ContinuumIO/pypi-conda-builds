package:
  name: brokerest
  version: "0.1.14"

source:
  fn: brokerest-0.1.14.tar.gz
  url: https://pypi.python.org/packages/source/b/brokerest/brokerest-0.1.14.tar.gz
  md5: 1959befa43bbc16c44c0a15a305d8ed0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brokerest = brokerest:main
    #
    # Would create an entry point called brokerest that calls brokerest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3

  run:
    - python
    - requests >=1.2.3

test:
  # Python imports
  imports:
    - brokerest
    - brokerest/resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/satrails/brokerest
  license: MIT
  summary: 'Python REST API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
