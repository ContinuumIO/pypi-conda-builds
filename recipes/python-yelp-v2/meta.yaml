package:
  name: python-yelp-v2
  version: "0.5.2"

source:
  fn: python-yelp-v2-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-yelp-v2/python-yelp-v2-0.5.2.tar.gz
  md5: b567a39961aa17c6f54f1fc84f7e180c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-yelp-v2 = python-yelp-v2:main
    #
    # Would create an entry point called python-yelp-v2 that calls python-yelp-v2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2

  run:
    - python
    - setuptools
    - oauth2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mathisonian/python-yelp-v2
  license: Apache License 2.0
  summary: 'A Python wrapper around the Yelp API v2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
