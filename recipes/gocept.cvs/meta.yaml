package:
  name: gocept.cvs
  version: "0.1.10"

source:
  fn: gocept.cvs-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.cvs/gocept.cvs-0.1.10.tar.gz
  md5: 6f121966bfa88a0634cebdb9676cbe08
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.cvs = gocept.cvs:main
    #
    # Would create an entry point called gocept.cvs that calls gocept.cvs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.cvs
    - gocept.cvs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.gocept.com/repos/gocept/gocept.cvs
  license: ZPL 2.1
  summary: 'zc.buildout recipe for checking out cvs modules.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
