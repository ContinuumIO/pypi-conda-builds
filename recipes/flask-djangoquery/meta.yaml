package:
  name: flask-djangoquery
  version: "0.2.4"

source:
  fn: Flask-DjangoQuery-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-DjangoQuery/Flask-DjangoQuery-0.2.4.tar.gz
  md5: d9daa9ee219db3980645d6fa2014830f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-djangoquery = flask-djangoquery:main
    #
    # Would create an entry point called flask-djangoquery that calls flask-djangoquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - sqlalchemy
    - flask-sqlalchemy

  run:
    - python
    - flask
    - sqlalchemy
    - flask-sqlalchemy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/messense/Flask-DjangoQuery
  license: MIT License
  summary: 'Django like query for Flask-SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
