package:
  name: bottle-pycassa
  version: "0.1"

source:
  fn: bottle-pycassa-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-pycassa/bottle-pycassa-0.1.tar.gz
  md5: e8d7030329f17b6a56a73e595384b8f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-pycassa = bottle-pycassa:main
    #
    # Would create an entry point called bottle-pycassa that calls bottle-pycassa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/dmcnelis/bottle-pycassa
  license: MIT License
  summary: 'Pycassa integration for Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
