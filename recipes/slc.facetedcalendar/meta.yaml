package:
  name: slc.facetedcalendar
  version: "0.3"

source:
  fn: slc.facetedcalendar-0.3.zip
  url: https://pypi.python.org/packages/source/s/slc.facetedcalendar/slc.facetedcalendar-0.3.zip
  md5: 7fa2b510b4ecfac2aab76c8f93156a45
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.facetedcalendar = slc.facetedcalendar:main
    #
    # Would create an entry point called slc.facetedcalendar that calls slc.facetedcalendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - collective.solr
    - collective.js.fullcalendar
    - solgema.fullcalendar

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - collective.solr
    - collective.js.fullcalendar
    - solgema.fullcalendar

test:
  # Python imports
  imports:
    - slc
    - slc.facetedcalendar
    - slc.facetedcalendar.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
