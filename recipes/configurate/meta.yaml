package:
  name: configurate
  version: "0.4.0"

source:
  fn: configurate-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/configurate/configurate-0.4.0.tar.gz
  md5: e661f7e58d9fc8177b967c2251297116
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configurate = configurate:main
    #
    # Would create an entry point called configurate that calls configurate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - configurate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikenerone/configurate
  license: Apache Software License
  summary: "A simple, but smart, dict-like configuration object that's not opinionated about the file format you prefer."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
