package:
  name: astoptimizer
  version: "0.6"

source:
  fn: astoptimizer-0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/astoptimizer/astoptimizer-0.6.tar.gz
  md5: 87849d9030802d6e345f8ec18174d92d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astoptimizer = astoptimizer:main
    #
    # Would create an entry point called astoptimizer that calls astoptimizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - astoptimizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/haypo/astoptimizer
  license: BSD License
  summary: 'Experimental AST optimizer for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
