package:
  name: django-spillway
  version: "0.3.1"

source:
  fn: django-spillway-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spillway/django-spillway-0.3.1.tar.gz
  md5: 4b193f13a04b8f28d1eca3518a626a14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spillway = django-spillway:main
    #
    # Would create an entry point called django-spillway that calls django-spillway.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - djangorestframework <3.1.0
    - greenwich >=0.3

  run:
    - python
    - django
    - djangorestframework <3.1.0
    - greenwich >=0.3

test:
  # Python imports
  imports:
    - spillway
    - spillway.forms
    - spillway.renderers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bkg/django-spillway
  license: BSD License
  summary: 'Geodata extensions for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
