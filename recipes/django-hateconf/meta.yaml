package:
  name: django-hateconf
  version: "0.2.3"

source:
  fn: django-hateconf-0.2.3.tar.gz
  url: https://bitbucket.org/leliel12/django-hateconf/downloads/django-hateconf-0.2.3.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hateconf = django-hateconf:main
    #
    # Would create an entry point called django-hateconf that calls django-hateconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - django
    - configobj

  run:
    - python
    - pyyaml
    - django
    - configobj

test:
  # Python imports
  imports:
    - django_hateconf
    - django_hateconf.management
    - django_hateconf.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/leliel12/django-hateconf
  license: GPL3
  summary: 'Application for distribuited configurations in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
