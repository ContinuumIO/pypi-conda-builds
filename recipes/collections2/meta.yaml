package:
  name: collections2
  version: "0.3.0"

source:
  fn: collections2-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collections2/collections2-0.3.0.tar.gz
  md5: 0c2446b52d2896a2ecf12e7af571fdce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collections2 = collections2:main
    #
    # Would create an entry point called collections2 that calls collections2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - collections2
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/therealfakemoot/BetterOrderedDict/
  license: UNKNOWN
  summary: "A set of improved data types inspired by the standard library's collections module."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
