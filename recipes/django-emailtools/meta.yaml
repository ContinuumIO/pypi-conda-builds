package:
  name: django-emailtools
  version: "0.2.2"

source:
  fn: django-emailtools-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailtools/django-emailtools-0.2.2.tar.gz
  md5: 48c50f63cf0adefe076e905b4bfe34ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailtools = django-emailtools:main
    #
    # Would create an entry point called django-emailtools that calls django-emailtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - markdown

  run:
    - python
    - django >=1.3
    - markdown

test:
  # Python imports
  imports:
    - emailtools
    - emailtools.cbe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-emailtools.readthedocs.org/en/latest/
  license: UNKNOWN
  summary: '\nApp for Django featuring class based email sending\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
