package:
  name: anillo
  version: "0.0.1"

source:
  fn: anillo-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/anillo/anillo-0.0.1.tar.gz
  md5: 5b966172c584e7ff7fe4ceff87df2c14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anillo = anillo:main
    #
    # Would create an entry point called anillo that calls anillo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug

  run:
    - python
    - werkzeug

test:
  # Python imports
  imports:
    - anillo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jespino/anillo
  license: BSD License
  summary: 'Ring/compojure like nanoframework build on top of werkzoug'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
