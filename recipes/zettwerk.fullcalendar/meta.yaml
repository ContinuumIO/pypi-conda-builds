package:
  name: zettwerk.fullcalendar
  version: "0.3.1"

source:
  fn: zettwerk.fullcalendar-0.3.1.zip
  url: https://pypi.python.org/packages/source/z/zettwerk.fullcalendar/zettwerk.fullcalendar-0.3.1.zip
  md5: 94cf0eb9aa8bad853abb02a217375f88
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zettwerk.fullcalendar = zettwerk.fullcalendar:main
    #
    # Would create an entry point called zettwerk.fullcalendar that calls zettwerk.fullcalendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - zettwerk
    - zettwerk.fullcalendar
    - zettwerk.fullcalendar.browser
    - zettwerk.fullcalendar.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/zettwerk.fullcalendar
  license: GPL
  summary: 'Adding jquery.fullcalendar to plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
