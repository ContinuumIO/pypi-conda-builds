package:
  name: django-bits
  version: "0.3.1"

source:
  fn: django-bits-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bits/django-bits-0.3.1.tar.gz
  md5: 88e55396d0fcd7d35921bb166dfe66ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bits = django-bits:main
    #
    # Would create an entry point called django-bits that calls django-bits.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bits
    - bits.management
    - bits.management.commands
    - bits.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/scdoshi/django-bits/
  license: BSD License
  summary: 'Basic utilities and helpers for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
