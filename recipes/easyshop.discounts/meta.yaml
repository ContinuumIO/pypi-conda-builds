package:
  name: easyshop.discounts
  version: "0.1a1"

source:
  fn: easyshop.discounts-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.discounts/easyshop.discounts-0.1a1.tar.gz
  md5: 58388f9d7699620c16bf7d7e60dc8d46
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.discounts = easyshop.discounts:main
    #
    # Would create an entry point called easyshop.discounts that calls easyshop.discounts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.discounts
    - easyshop.discounts.adapters
    - easyshop.discounts.browser
    - easyshop.discounts.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'Discount management for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
