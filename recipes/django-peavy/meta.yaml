package:
  name: django-peavy
  version: "0.9.0"

source:
  fn: django-peavy-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-peavy/django-peavy-0.9.0.tar.gz
  md5: 8c78e56dbcb3d5395feec59081638198
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-peavy = django-peavy:main
    #
    # Would create an entry point called django-peavy that calls django-peavy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - south >=0.7.6

  run:
    - python
    - django >=1.3
    - south >=0.7.6

test:
  # Python imports
  imports:
    - peavy
    - peavy.migrations
    - peavy.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fairview/django-peavy
  license: MIT License
  summary: '\ndjango-peavy makes it easy to collect and monitor Django application logging.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
