package:
  name: gears-babel
  version: "0.1"

source:
  fn: gears-babel-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gears-babel/gears-babel-0.1.tar.gz
  md5: 29ff0cb8c29f0e4d8ed5ae2cc12d97e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gears-babel = gears-babel:main
    #
    # Would create an entry point called gears-babel that calls gears-babel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gears >=0.7.2

  run:
    - python
    - gears >=0.7.2

test:
  # Python imports
  imports:
    - gears_babel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mandx/gears-babel
  license: ISC
  summary: 'Gears compiler for Babel (previously 6to5) http://babeljs.io/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
