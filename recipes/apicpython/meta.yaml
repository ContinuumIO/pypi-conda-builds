package:
  name: apicpython
  version: "0.3.1"

source:
  fn: apicPython-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/apicPython/apicPython-0.3.1.tar.gz
  md5: 2fc4f5c02adf0bebc9bf2fa31e341877
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apicpython = apicpython:main
    #
    # Would create an entry point called apicpython that calls apicpython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - apicPython
    - apicpython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datacenter/ACI/tree/master/configuration-python
  license: Cisco
  summary: 'This are codes for creating MO in APIC through Python API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
