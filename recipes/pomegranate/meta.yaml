package:
  name: pomegranate
  version: "0.2.2"

source:
  fn: pomegranate-0.2.2.zip
  url: https://pypi.python.org/packages/source/p/pomegranate/pomegranate-0.2.2.zip
  md5: b7be4fc042ea495748d317d983debd0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pomegranate = pomegranate:main
    #
    # Would create an entry point called pomegranate that calls pomegranate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cython >=0.20.1
    - numpy >=1.8.0
    - scipy >=0.13.3
    - networkx >=1.8.1
    - matplotlib >=1.3.1

  run:
    - python
    - cython >=0.20.1
    - numpy >=1.8.0
    - scipy >=0.13.3
    - networkx >=1.8.1
    - matplotlib >=1.3.1

test:
  # Python imports
  imports:
    - pomegranate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/pomegranate/
  license: LICENSE.txt
  summary: 'Pomegranate is a graphical models library for Python, implemented in Cython for speed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
