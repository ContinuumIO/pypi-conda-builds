package:
  name: lucidity
  version: "1.3.1"

source:
  fn: Lucidity-1.3.1.zip
  url: https://pypi.python.org/packages/source/L/Lucidity/Lucidity-1.3.1.zip
  md5: 2add6ad60d6f2f33917b1dd8d988df05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lucidity = lucidity:main
    #
    # Would create an entry point called lucidity that calls lucidity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bunch >=1.0.1

  run:
    - python
    - bunch >=1.0.1

test:
  # Python imports
  imports:
    - lucidity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4degrees/lucidity
  license: Apache License (2.0)
  summary: 'Filesystem templating and management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
