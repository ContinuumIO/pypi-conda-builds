package:
  name: collective.portlet.wordpress
  version: "1.0"

source:
  fn: collective.portlet.wordpress-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.wordpress/collective.portlet.wordpress-1.0.tar.gz
  md5: 236f9837532af26a8a4ef540862d9c4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.wordpress = collective.portlet.wordpress:main
    #
    # Would create an entry point called collective.portlet.wordpress that calls collective.portlet.wordpress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - elementtree

  run:
    - python
    - setuptools
    - setuptools
    - elementtree

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.wordpress
    - collective.portlet.wordpress.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.portlet.wordpress
  license: GPL
  summary: 'This package provides portlet that lists blog posts from your wordpress blog.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
