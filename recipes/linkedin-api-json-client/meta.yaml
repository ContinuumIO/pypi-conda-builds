package:
  name: linkedin-api-json-client
  version: "0.2.6"

source:
  fn: linkedin-api-json-client-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/l/linkedin-api-json-client/linkedin-api-json-client-0.2.6.tar.gz
  md5: b0f03da7ba2124a6e430d255346f4acb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkedin-api-json-client = linkedin-api-json-client:main
    #
    # Would create an entry point called linkedin-api-json-client that calls linkedin-api-json-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - mock
    - oauth2
    - simplejson

  run:
    - python
    - mock
    - oauth2
    - simplejson

test:
  # Python imports
  imports:
    - linkedin_json_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattsnider/LinkedIn-API-JSON-Client
  license: MIT License
  summary: 'Python API for interacting with LinkedIn API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
