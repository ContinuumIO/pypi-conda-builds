package:
  name: nuxeo-automation
  version: "0.1"

source:
  fn: nuxeo-automation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nuxeo-automation/nuxeo-automation-0.1.tar.gz
  md5: 652354e9a6335bc24c6dea91d8a3b7be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nuxeo-automation = nuxeo-automation:main
    #
    # Would create an entry point called nuxeo-automation that calls nuxeo-automation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/sfermigier/nuxeo-automation-clients/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A small Python client for the Nuxeo Document Automation JSON-RPC API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
