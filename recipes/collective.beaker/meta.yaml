package:
  name: collective.beaker
  version: "1.0b3"

source:
  fn: collective.beaker-1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.beaker/collective.beaker-1.0b3.tar.gz
  md5: ba0134e1552a4d5a265879c9141795da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.beaker = collective.beaker:main
    #
    # Would create an entry point called collective.beaker that calls collective.beaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beaker
    - zope.interface
    - zope.component
    - zope2
    - zope.publisher

  run:
    - python
    - setuptools
    - setuptools
    - beaker
    - zope.interface
    - zope.component
    - zope2
    - zope.publisher

test:
  # Python imports
  imports:
    - collective
    - collective.beaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.beaker
  license: BSD
  summary: 'Beaker integration for Zope and Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
