package:
  name: django-mpowering
  version: "0.0.2"

source:
  fn: django-mpowering-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mpowering/django-mpowering-0.0.2.tar.gz
  md5: 410ca0332177ffa19c03e78896fbbe61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mpowering = django-mpowering:main
    #
    # Would create an entry point called django-mpowering that calls django-mpowering.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-tastypie >=0.11.0
    - django-tablib >=0.9.11
    - django-crispy-forms >=1.4.0
    - django-tinymce
    - django-wysiwyg

  run:
    - python
    - django >=1.7
    - django-tastypie >=0.11.0
    - django-tablib >=0.9.11
    - django-crispy-forms >=1.4.0
    - django-tinymce
    - django-wysiwyg

test:
  # Python imports
  imports:
    - mpowering
    - mpowering.fixtures
    - mpowering.migrations
    - mpowering.profile
    - mpowering.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://oppia-mobile.org/
  license: GNU General Public License v3 (GPLv3)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
