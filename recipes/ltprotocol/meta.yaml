package:
  name: ltprotocol
  version: "0.2.1"

source:
  fn: ltprotocol-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/l/ltprotocol/ltprotocol-0.2.1.tar.gz
  md5: 1d24cca100e1bc20690db95dbeea7553
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ltprotocol = ltprotocol:main
    #
    # Would create an entry point called ltprotocol that calls ltprotocol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted

  run:
    - python
    - setuptools
    - twisted

test:
  # Python imports
  imports:
    - ltprotocol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dound.com/projects/python/ltprotocol/
  license: GNU General Public License (GPL)
  summary: 'Twisted-based client and server for protocols which begin with a length and type field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
