package:
  name: django_chartit
  version: "0.1"

source:
  fn: django_chartit-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_chartit/django_chartit-0.1.tar.gz
  md5: bd9123eb77b3f43f61142a6bf6bde534
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_chartit = django_chartit:main
    #
    # Would create an entry point called django_chartit that calls django_chartit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chartit
    - chartit.highcharts
    - chartit.templatetags
    - django_chartit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pgollakota/django-chartit
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
