package:
  name: django-docs
  version: "0.1.2"

source:
  fn: django-docs-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-docs/django-docs-0.1.2.tar.gz
  md5: a2de61496883cf478576ba13527bc767
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-docs = django-docs:main
    #
    # Would create an entry point called django-docs that calls django-docs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - docs
    - docs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/littlepea/django-docs
  license: BSD
  summary: u'Allows to serve Sphinx generated docs from django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
