package:
  name: lprettytable
  version: "0.5"

source:
  fn: lprettytable-0.5.tar.gz
  url: https://pypi.python.org/packages/source/l/lprettytable/lprettytable-0.5.tar.gz
  md5: 42a42402cbefe545c0a925936dbe174b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lprettytable = lprettytable:main
    #
    # Would create an entry point called lprettytable that calls lprettytable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lprettytable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudnull/lprettytable
  license: http://www.luke.maurits.id.au/software/bsdlicense.txt
  summary: 'A simple Python library for easily displaying tabular data in a visually appealing ASCII table format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
