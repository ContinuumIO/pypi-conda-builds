package:
  name: textclean
  version: "0.4"

source:
  fn: textclean-0.4.zip
  url: https://pypi.python.org/packages/source/t/textclean/textclean-0.4.zip
  md5: 6dbe71f613bc49c9fe969b8e7b191e22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - textclean = textclean:main
    #
    # Would create an entry point called textclean that calls textclean.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - textclean

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Handle the very famous decoding - encoding errors in Python !'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
