package:
  name: collective.inplacetopicview
  version: "1.0"

source:
  fn: collective.inplacetopicview-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.inplacetopicview/collective.inplacetopicview-1.0.zip
  md5: 10c6b462a1f4a04c2215d79335432edb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.inplacetopicview = collective.inplacetopicview:main
    #
    # Would create an entry point called collective.inplacetopicview that calls collective.inplacetopicview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

test:
  # Python imports
  imports:
    - collective
    - collective.inplacetopicview
    - collective.inplacetopicview.browser
    - collective.inplacetopicview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.inplacetopicview
  license: GPL
  summary: 'Lets view the content of an item listed by a topic within the topic context'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
