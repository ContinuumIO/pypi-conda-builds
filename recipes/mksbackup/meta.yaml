package:
  name: mksbackup
  version: "1.0.4"

source:
  fn: mksbackup-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mksbackup/mksbackup-1.0.4.tar.gz
  md5: 00726b1009f8dee1312f81ea27f4876e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mksbackup = mksbackup:main
    #
    # Would create an entry point called mksbackup that calls mksbackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mksbackup
    - mksbackuplib
    - mksbackuplib.pywbem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.magikmon.com/mksbackup
  license: GNU General Public License (GPL)
  summary: 'Front-end for popular Windows, Linux and VMware backup tools. Sending a mail report at end of backup.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
