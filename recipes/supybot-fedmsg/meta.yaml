package:
  name: supybot-fedmsg
  version: "0.1.0"

source:
  fn: supybot-fedmsg-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/supybot-fedmsg/supybot-fedmsg-0.1.0.tar.gz
  md5: 474f99ae2b3a597b50c6f7f3902c7f95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supybot-fedmsg = supybot-fedmsg:main
    #
    # Would create an entry point called supybot-fedmsg that calls supybot-fedmsg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - supybot-fedmsg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/supybot-fedmsg/
  license: LGPLv2+
  summary: 'Supybot plugin for emitting events to the Fedora message bus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
