package:
  name: fixtures2
  version: "0.1.3"

source:
  fn: fixtures2-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fixtures2/fixtures2-0.1.3.tar.gz
  md5: 55d151a1a7e2c2841e759f081d6e83ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fixtures2 = fixtures2:main
    #
    # Would create an entry point called fixtures2 that calls fixtures2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - fixtures

  run:
    - python
    - fixtures

test:
  # Python imports
  imports:
    - fixtures2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CooledCoffee/fixtures2/
  license: UNKNOWN
  summary: 'Fixtures2 is an extension of the fixtures test framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
