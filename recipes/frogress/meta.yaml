package:
  name: frogress
  version: "0.9.1"

source:
  fn: frogress-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frogress/frogress-0.9.1.tar.gz
  md5: 63df68b172eeae8cb4166cd938a6b0de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frogress = frogress:main
    #
    # Would create an entry point called frogress that calls frogress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - frogress
    - frogress.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lukaszb/frogress
  license: MIT License
  summary: '\nfrogress is simple progress tool\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
