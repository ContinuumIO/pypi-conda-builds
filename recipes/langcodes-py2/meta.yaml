package:
  name: langcodes-py2
  version: "1.1.0"

source:
  fn: langcodes-py2-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/langcodes-py2/langcodes-py2-1.1.0.tar.gz
  md5: cf0cd6287255afdd966480ed1c23857c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - langcodes-py2 = langcodes-py2:main
    #
    # Would create an entry point called langcodes-py2 that calls langcodes-py2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - langcodes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/LuminosoInsight/langcodes
  license: MIT License
  summary: 'Labels and compares human languages in a standardized way -- Python 2 backport'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
