package:
  name: django-indb-logger
  version: "0.1"

source:
  fn: django-indb-logger-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-indb-logger/django-indb-logger-0.1.tar.gz
  md5: c387215627690d0592dcc8ff5761dc06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-indb-logger = django-indb-logger:main
    #
    # Would create an entry point called django-indb-logger that calls django-indb-logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - indb_logger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/a1fred/django-indb-logger
  license: MIT
  summary: 'Django app with in-database log handler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
