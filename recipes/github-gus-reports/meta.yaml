package:
  name: github-gus-reports
  version: "0.1.8"

source:
  fn: github-gus-reports-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/g/github-gus-reports/github-gus-reports-0.1.8.tar.gz
  md5: ca8b3fec0f32a53ade128e4ecd9faf2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github-gus-reports = github-gus-reports:main
    #
    # Would create an entry point called github-gus-reports that calls github-gus-reports.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gus_client >=0.4.6

  run:
    - python
    - gus_client >=0.4.6

test:
  # Python imports
  imports:
    - ws
    - ws.commits
    - ws.releases

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Keep it real
  summary: 'Cross reference github commits with GUS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
