package:
  name: collective.error.detector
  version: "0.1.0b1"

source:
  fn: collective.error.detector-0.1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.error.detector/collective.error.detector-0.1.0b1.zip
  md5: df436252350283f230545f29a699641d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.error.detector = collective.error.detector:main
    #
    # Would create an entry point called collective.error.detector that calls collective.error.detector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.error
    - collective.error.detector
    - collective.error.detector.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/potar/collective.error.detector
  license: gpl
  summary: "The package looks for requests which cause an error. It's a part of https://github.com/potar/dagger"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
