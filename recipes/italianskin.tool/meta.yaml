package:
  name: italianskin.tool
  version: "3.1.7-r1"

source:
  fn: italianskin.tool-3.1.7-r1.tar.gz
  url: https://pypi.python.org/packages/source/i/italianskin.tool/italianskin.tool-3.1.7-r1.tar.gz
  md5: ec12902e73c65af841dac802e69462f2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - italianskin.tool = italianskin.tool:main
    #
    # Would create an entry point called italianskin.tool that calls italianskin.tool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - italianskin
    - italianskin.tool
    - italianskin.tool.browser
    - italianskin.tool.content
    - italianskin.tool.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.redomino.com/it/labs/progetti/ItalianSkin
  license: GPL
  summary: 'italianskin tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
