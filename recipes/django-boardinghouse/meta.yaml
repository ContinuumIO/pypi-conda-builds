package:
  name: django-boardinghouse
  version: "0.3"

source:
  fn: django-boardinghouse-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-boardinghouse/django-boardinghouse-0.3.tar.gz
  md5: 37fc9a669179c26b28b8a07d876b7e12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-boardinghouse = django-boardinghouse:main
    #
    # Would create an entry point called django-boardinghouse that calls django-boardinghouse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - boardinghouse
    - boardinghouse.backends
    - boardinghouse.backends.postgres
    - boardinghouse.contrib
    - boardinghouse.contrib.demo
    - boardinghouse.contrib.invite
    - boardinghouse.contrib.template
    - boardinghouse.contrib.template.migrations
    - boardinghouse.management
    - boardinghouse.management.commands
    - boardinghouse.migrations
    - boardinghouse.templatetags
    - docs
    - tests
    - tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.schinckel.net/django-boardinghouse
  license: BSD License
  summary: 'Postgres schema support in django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
