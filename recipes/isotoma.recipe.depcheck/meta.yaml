package:
  name: isotoma.recipe.depcheck
  version: "0.0.9"

source:
  fn: isotoma.recipe.depcheck-0.0.9.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.depcheck/isotoma.recipe.depcheck-0.0.9.zip
  md5: 22ea5fe8566f47756b7cddaa55d9dc40
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.depcheck = isotoma.recipe.depcheck:main
    #
    # Would create an entry point called isotoma.recipe.depcheck that calls isotoma.recipe.depcheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.depcheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.depcheck
  license: Apache Software License
  summary: 'Buildout recipe to check various dependencies are in place.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
