package:
  name: beautifulhue
  version: "0.1.2"

source:
  fn: BeautifulHue-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/B/BeautifulHue/BeautifulHue-0.1.2.tar.gz
  md5: 3c423d2c006a16233784f37c7fb457b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beautifulhue = beautifulhue:main
    #
    # Would create an entry point called beautifulhue that calls beautifulhue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - beautifulhue
    - beautifulhue.api
    - beautifulhue.libs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allanbunch/beautifulhue
  license: MIT License
  summary: 'A Python module for the Philips Hue Lighting System API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
