package:
  name: j01.wizard
  version: "1.0.0"

source:
  fn: j01.wizard-1.0.0.zip
  url: https://pypi.python.org/packages/source/j/j01.wizard/j01.wizard-1.0.0.zip
  md5: 0a599f4c4d16b3d9454a4b880f418bb1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.wizard = j01.wizard:main
    #
    # Would create an entry point called j01.wizard that calls j01.wizard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - j01.dialog
    - j01.jsonrpc
    - z3c.jsonrpc
    - z3c.template
    - zope.interface
    - zope.publisher

  run:
    - python
    - setuptools
    - j01.dialog
    - j01.jsonrpc
    - z3c.jsonrpc
    - z3c.template
    - zope.interface
    - zope.publisher

test:
  # Python imports
  imports:
    - j01
    - j01.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.wizard
  license: Zope Public License
  summary: 'Form wizard using dialog box based on JQuery for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
