package:
  name: brining
  version: "0.2.5"

source:
  fn: brining-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/b/brining/brining-0.2.5.tar.gz
  md5: 3447158651a0bb9157b221ec070e99fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brining = brining:main
    #
    # Would create an entry point called brining that calls brining.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - ordereddict

  run:
    - python
    - simplejson
    - ordereddict

test:
  # Python imports
  imports:
    - brining
    - brining.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SmithSamuelM/brine
  license: MIT
  summary: 'Python object to/from JSON serialization/deserialization module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
