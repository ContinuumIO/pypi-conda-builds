package:
  name: gdrsync
  version: "1.0.1"

source:
  fn: gdrsync-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gdrsync/gdrsync-1.0.1.tar.gz
  md5: cd7b098601d01c84f4d877994b9c5845
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gdrsync = gdrsync:main
    #
    # Would create an entry point called gdrsync that calls gdrsync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client

  run:
    - python
    - google-api-python-client

test:
  # Python imports
  imports:
    - gdrsync
    - gdrsynclib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/farbeiza/gdrsync.py
  license: LICENSE.txt
  summary: 'Rsync with google drive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
