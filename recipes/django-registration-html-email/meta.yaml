package:
  name: django-registration-html-email
  version: "0.8.1b0"

source:
  fn: django-registration-html-email-0.8.1b0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-html-email/django-registration-html-email-0.8.1b0.tar.gz
  md5: 17c547f3344384521cfd7af89e141362
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-html-email = django-registration-html-email:main
    #
    # Would create an entry point called django-registration-html-email that calls django-registration-html-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - registration_html_email
    - registration_html_email.backends
    - registration_html_email.backends.default
    - registration_html_email.backends.simple
    - registration_html_email.management
    - registration_html_email.management.commands
    - registration_html_email.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/hisie/django-registration-html-mail
  license: BSD License
  summary: 'An extensible user-registration application for Django, with html emails (django-registration simple fork)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
