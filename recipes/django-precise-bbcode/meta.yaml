package:
  name: django-precise-bbcode
  version: "1.0.9"

source:
  fn: django-precise-bbcode-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-precise-bbcode/django-precise-bbcode-1.0.9.tar.gz
  md5: 015ecc929c609771b7e485bd8001a390
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-precise-bbcode = django-precise-bbcode:main
    #
    # Would create an entry point called django-precise-bbcode that calls django-precise-bbcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pillow >=2.2.1

  run:
    - python
    - django >=1.4
    - pillow >=2.2.1

test:
  # Python imports
  imports:
    - precise_bbcode
    - precise_bbcode.bbcode
    - precise_bbcode.bbcode.defaults
    - precise_bbcode.conf
    - precise_bbcode.core
    - precise_bbcode.migrations
    - precise_bbcode.south_migrations
    - precise_bbcode.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ellmetha/django-precise-bbcode
  license: BSD License
  summary: 'A django BBCode integration..'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
