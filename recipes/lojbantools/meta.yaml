package:
  name: lojbantools
  version: "0.2.3"

source:
  fn: lojbantools-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/l/lojbantools/lojbantools-0.2.3.tar.gz
  md5: 5a7bc21e1515445d9de513651e9aac05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lojbantools = lojbantools:main
    #
    # Would create an entry point called lojbantools that calls lojbantools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lojbantools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'This package offers access to a bunch of lojban tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
