package:
  name: django-jasmine
  version: "0.4.1"

source:
  fn: django-jasmine-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jasmine/django-jasmine-0.4.1.tar.gz
  md5: 41c90e71d5975178e93ba6a0a0f38762
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jasmine = django-jasmine:main
    #
    # Would create an entry point called django-jasmine that calls django-jasmine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_jasmine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Fandekasp/django-jasmine
  license: BSD License
  summary: 'Jasmine Javascript testing integration for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
