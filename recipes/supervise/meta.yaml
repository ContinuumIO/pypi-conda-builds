package:
  name: supervise
  version: "1.1.1"

source:
  fn: supervise-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/supervise/supervise-1.1.1.tar.gz
  md5: 0bbff85fa5897dddfdfbf0cbc7aa3eab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supervise = supervise:main
    #
    # Would create an entry point called supervise that calls supervise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - supervise

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fmoo/python-supervise
  license: ISC License (ISCL)
  summary: 'Tools for communicating with runit / daemontools supervisors.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
