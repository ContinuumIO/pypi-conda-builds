package:
  name: bud.nospam
  version: "1.0.1"

source:
  fn: bud.nospam-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bud.nospam/bud.nospam-1.0.1.tar.gz
  md5: 25a05c9110d1f42e661589428c26c060
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bud.nospam = bud.nospam:main
    #
    # Would create an entry point called bud.nospam that calls bud.nospam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - bud
    - bud.nospam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'ROT 13 encrypt text to prevent spam'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
