package:
  name: plone.pony
  version: "1.2"

source:
  fn: plone.pony-1.2.zip
  url: https://pypi.python.org/packages/source/p/plone.pony/plone.pony-1.2.zip
  md5: d4698109cad5d21190c3193e7233a19e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.pony = plone.pony:main
    #
    # Would create an entry point called plone.pony that calls plone.pony.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.pony

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.pony
  license: GNU General Public License (GPL)
  summary: 'A pony for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
