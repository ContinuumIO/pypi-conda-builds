package:
  name: slitheringfm
  version: "0.2"

source:
  fn: SlitheringFM-0.2.zip
  url: https://pypi.python.org/packages/source/S/SlitheringFM/SlitheringFM-0.2.zip
  md5: ab74f25eea2b122fb3091233ac175dcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slitheringfm = slitheringfm:main
    #
    # Would create an entry point called slitheringfm that calls slitheringfm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - slitheringfm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/malcolm2608/lastfmapi
  license: http://www.opensource.org/licenses/mit-license.php
  summary: 'A fork of a think wrapper for accessing the LastFM API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
