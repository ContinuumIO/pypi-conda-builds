package:
  name: nimfa
  version: "1.1"

source:
  fn: nimfa-1.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nimfa/nimfa-1.1.tar.gz
  md5: b1eff9ded1d312db6e6d963569c6f450
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nimfa = nimfa:main
    #
    # Would create an entry point called nimfa that calls nimfa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nimfa
    - nimfa.datasets
    - nimfa.examples
    - nimfa.methods
    - nimfa.methods.factorization
    - nimfa.methods.seeding
    - nimfa.models
    - nimfa.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nimfa.biolab.si
  license: GNU General Public License (GPL)
  summary: 'A Python Library for Nonnegative Matrix Factorization Techniques'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
