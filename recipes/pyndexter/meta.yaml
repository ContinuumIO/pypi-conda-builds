package:
  name: pyndexter
  version: "0.2"

source:
  fn: pyndexter-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyndexter/pyndexter-0.2.tar.gz
  md5: 5c0240a7dc1ca9d105c338f27cfa116d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyndexter = pyndexter:main
    #
    # Would create an entry point called pyndexter that calls pyndexter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyndexter
    - pyndexter.indexers
    - pyndexter.sources
    - pyndexter.stemmers
    - pyndexter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://swapoff.org/pyndexter
  license: BSD License
  summary: 'An abstraction layer for full-text indexing engines.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
