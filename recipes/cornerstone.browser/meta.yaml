package:
  name: cornerstone.browser
  version: "1.3.4"

source:
  fn: cornerstone.browser-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cornerstone.browser/cornerstone.browser-1.3.4.tar.gz
  md5: 94ee25c509fe4c86d646cf2b297c764b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cornerstone.browser = cornerstone.browser:main
    #
    # Would create an entry point called cornerstone.browser that calls cornerstone.browser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - repoze.formapi
    - products.cmfcore

  run:
    - python
    - setuptools
    - setuptools
    - repoze.formapi
    - products.cmfcore

test:
  # Python imports
  imports:
    - cornerstone
    - cornerstone.browser
    - cornerstone.browser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/cornerstone.browser
  license: GNU General Public License (GPL)
  summary: 'Common browser utils for ZOPE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
