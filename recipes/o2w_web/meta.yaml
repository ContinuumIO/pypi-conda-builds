package:
  name: o2w_web
  version: "0.0.5"

source:
  fn: o2w_web-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/o/o2w_web/o2w_web-0.0.5.tar.gz
  md5: 7f4055b2d2a5b174b4860822c1a4202d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - o2w_web = o2w_web:main
    #
    # Would create an entry point called o2w_web that calls o2w_web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-annoying
    - django-pipeline
    - cssmin
    - slimit
    - django-modeltranslation
    - easy-thumbnails
    - o2w-image-diet
    - django-extensions
    - django-grappelli
    - django-rosetta
    - django-filebrowser
    - grappelli-modeltranslation
    - django-pure-pagination
    - raven
    - o2w-cache-invalidator
    - o2w-templates
    - o2w-core
    - o2w-settings

  run:
    - python
    - django
    - django-annoying
    - django-pipeline
    - cssmin
    - slimit
    - django-modeltranslation
    - easy-thumbnails
    - o2w-image-diet
    - django-extensions
    - django-grappelli
    - django-rosetta
    - django-filebrowser
    - grappelli-modeltranslation
    - django-pure-pagination
    - raven
    - o2w-cache-invalidator
    - o2w-templates
    - o2w-core
    - o2w-settings

test:
  # Python imports
  imports:
    - o2w_web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.o2w.es/
  license: LICENSE.txt
  summary: u'Base package for web creation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
