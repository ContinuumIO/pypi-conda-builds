package:
  name: gfsdownload
  version: "0.0.2"

source:
  fn: GFSDownload-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GFSDownload/GFSDownload-0.0.2.tar.gz
  md5: c43b37854014807edb56f7cef99dcc25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gfsdownload = gfsdownload:main
    #
    # Would create an entry point called gfsdownload that calls gfsdownload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdal
    - ogr
    - ecmwfapi

  run:
    - python
    - gdal
    - ogr
    - ecmwfapi

test:
  # Python imports
  imports:
    - gfsdownload
    - python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yoannMoreau/GFSDownload
  license: CCO
  summary: 'A utility to download GFS meteo data '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
