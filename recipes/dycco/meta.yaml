package:
  name: dycco
  version: "1.0.1"

source:
  fn: dycco-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dycco/dycco-1.0.1.tar.gz
  md5: 20438480ae7e1c18a2ff1bd6c023e93b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dycco = dycco:main
    #
    # Would create an entry point called dycco that calls dycco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - markdown ==2.4
    - pygments ==1.6
    - pystache ==0.5.3

  run:
    - python
    - markdown ==2.4
    - pygments ==1.6
    - pystache ==0.5.3

test:
  # Python imports
  imports:
    - dycco

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mccutchen/dycco
  license: MIT License
  summary: 'Literate-programming-style documentation generator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
