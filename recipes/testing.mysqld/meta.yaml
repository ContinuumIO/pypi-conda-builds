package:
  name: testing.mysqld
  version: "1.2.8"

source:
  fn: testing.mysqld-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/t/testing.mysqld/testing.mysqld-1.2.8.tar.gz
  md5: 7192bc075dd2fdb287e0125912c9df1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testing.mysqld = testing.mysqld:main
    #
    # Would create an entry point called testing.mysqld that calls testing.mysqld.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymysql

  run:
    - python
    - pymysql

test:
  # Python imports
  imports:
    - testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tk0miya/testing.mysqld
  license: Apache Software License
  summary: 'automatically setups a mysqld instance in a temporary directory, and destroys it after testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
