package:
  name: ar-virtualenv-api
  version: "2.0.0"

source:
  fn: ar-virtualenv-api-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/ar-virtualenv-api/ar-virtualenv-api-2.0.0.tar.gz
  md5: 62e29d7776dd3c31e626143b2638d3a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ar-virtualenv-api = ar-virtualenv-api:main
    #
    # Would create an entry point called ar-virtualenv-api that calls ar-virtualenv-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - virtualenvapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arteria/ar-virtualenv-api
  license: BSD
  summary: 'An API for virtualenv/pip'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
