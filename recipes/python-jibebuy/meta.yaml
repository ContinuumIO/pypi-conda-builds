package:
  name: python-jibebuy
  version: "0.9.0"

source:
  fn: python-jibebuy-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-jibebuy/python-jibebuy-0.9.0.tar.gz
  md5: 3584a5744f90ca705f5374995d4c1130
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-jibebuy = python-jibebuy:main
    #
    # Would create an entry point called python-jibebuy that calls python-jibebuy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - setuptools
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pjtpj/python-jibebuy
  license: Apache Software License
  summary: 'A python wrapper for the Jibebuy REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
