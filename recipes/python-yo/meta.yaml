package:
  name: python-yo
  version: "0.1.0"

source:
  fn: python-yo-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-yo/python-yo-0.1.0.tar.gz
  md5: be26d92f565371cb1dd08776cf3b58a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-yo = python-yo:main
    #
    # Would create an entry point called python-yo that calls python-yo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - yo
    - yo.http_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mcos/yo-py
  license: BSD License
  summary: 'A Python client for the Yo platform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
