package:
  name: flask-wdb
  version: "0.0.2"

source:
  fn: Flask-Wdb-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Wdb/Flask-Wdb-0.0.2.tar.gz
  md5: 950c16fa1e3e0d16fb3bbb923fb70037
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-wdb = flask-wdb:main
    #
    # Would create an entry point called flask-wdb that calls flask-wdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - wdb

  run:
    - python
    - flask
    - wdb

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/techniq/flask-wdb/
  license: BSD License
  summary: 'Integrate Wdb instead of Werkzeug debugger for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
