package:
  name: airypi
  version: "0.0.1"

source:
  fn: airypi-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/airypi/airypi-0.0.1.tar.gz
  md5: 0d9211da07162bd1a7c95741d8740a00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - airypi = airypi:main
    #
    # Would create an entry point called airypi that calls airypi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-oauthlib
    - flask-socketio
    - jinja2
    - markupsafe
    - werkzeug
    - gevent
    - gevent-socketio
    - gevent-websocket
    - greenlet
    - itsdangerous
    - oauthlib
    - redis
    - requests
    - wsgiref

  run:
    - python
    - flask
    - flask-oauthlib
    - flask-socketio
    - jinja2
    - markupsafe
    - werkzeug
    - gevent
    - gevent-socketio
    - gevent-websocket
    - greenlet
    - itsdangerous
    - oauthlib
    - redis
    - requests
    - wsgiref

test:
  # Python imports
  imports:
    - airypi
    - airypi.device

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.airypi.com/docs/
  license: Apache Software License
  summary: 'The server libraries for airypi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
