package:
  name: collective.transform.docbook
  version: "0.1"

source:
  fn: collective.transform.docbook-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.transform.docbook/collective.transform.docbook-0.1.tar.gz
  md5: af1f17febfe6fc799f89bcb16d80ab84
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.transform.docbook = collective.transform.docbook:main
    #
    # Would create an entry point called collective.transform.docbook that calls collective.transform.docbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - html2docbook

  run:
    - python
    - setuptools
    - setuptools
    - html2docbook

test:
  # Python imports
  imports:
    - collective
    - collective.transform
    - collective.transform.docbook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.transform.docbook
  license: GPL
  summary: 'DocBook text transform for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
