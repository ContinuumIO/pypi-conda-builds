package:
  name: hoka.patches.get_browser
  version: "2.13.19.2"

source:
  fn: hoka.patches.get_browser-2.13.19.2.zip
  url: https://pypi.python.org/packages/source/h/hoka.patches.get_browser/hoka.patches.get_browser-2.13.19.2.zip
  md5: 2210584f4a13f4d8ee6aafbfa328cbae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hoka.patches.get_browser = hoka.patches.get_browser:main
    #
    # Would create an entry point called hoka.patches.get_browser that calls hoka.patches.get_browser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - zope2
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - hoka
    - hoka.patches
    - hoka.patches.get_browser
    - hoka.patches.get_browser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hoka/hoka.patches.get_browser
  license: GNU General Public License (GPL)
  summary: 'A patch to query an browser on every context'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
