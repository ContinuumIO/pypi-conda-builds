package:
  name: django_minify
  version: "0.1"

source:
  fn: django-minify-1ab0dda.tar.gz
  url: https://pypi.python.org/packages/source/d/django_minify/django-minify-1ab0dda.tar.gz
  md5: 65412916a74d0ec01da0241300399753
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_minify = django_minify:main
    #
    # Would create an entry point called django_minify that calls django_minify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_minify
    - django_minify.management
    - django_minify.management.commands
    - django_minify.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/raliste/django-minify
  license: BSD License
  summary: 'A Django app that will contact and minify JS and CSS.Based on Jingo Minify by James Socol.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
