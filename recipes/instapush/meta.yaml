package:
  name: instapush
  version: "0.1.1"

source:
  fn: instapush-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/instapush/instapush-0.1.1.tar.gz
  md5: dcacd991fd88939413d55c2ca9aedf05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instapush = instapush:main
    #
    # Would create an entry point called instapush that calls instapush.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - instapush

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamwen829/instapush-py
  license: MIT License
  summary: 'a python wrapper for instapush'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
