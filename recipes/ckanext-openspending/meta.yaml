package:
  name: ckanext-openspending
  version: "0.1.0"

source:
  fn: ckanext-openspending-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-openspending/ckanext-openspending-0.1.0.tar.gz
  md5: 660d80157c4b95e0ae96bf86003caded
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-openspending = ckanext-openspending:main
    #
    # Would create an entry point called ckanext-openspending that calls ckanext-openspending.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ckanext-budgets
    - requests

  run:
    - python
    - setuptools
    - ckanext-budgets
    - requests

test:
  # Python imports
  imports:
    - ckanext
    - ckanext.openspending

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tryggvib/ckanext-openspending
  license: GNU Affero General Public License v3
  summary: 'Automatically model and submit budgets to the OpenSpending'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
