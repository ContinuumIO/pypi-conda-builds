package:
  name: cgrspy
  version: "1.1.1rc1"

source:
  fn: cgrspy-1.1.1rc1.tar.gz
  url: https://pypi.python.org/packages/source/c/cgrspy/cgrspy-1.1.1rc1.tar.gz
  md5: 0e5ba9548c9b2cfee2311a3c0087fce2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cgrspy = cgrspy:main
    #
    # Would create an entry point called cgrspy that calls cgrspy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cgrspy
    - cgrspy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cellml-api.sf.net/
  license: GPL/LGPL/MPL
  summary: 'Python interface to the CellML Generics and Reflection Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
