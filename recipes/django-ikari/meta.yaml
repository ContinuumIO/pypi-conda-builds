package:
  name: django-ikari
  version: "0.0.5"

source:
  fn: django-ikari-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ikari/django-ikari-0.0.5.tar.gz
  md5: ee76199b14a508aab53ac12a1e2aecfd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ikari = django-ikari:main
    #
    # Would create an entry point called django-ikari that calls django-ikari.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf ==0.4.1

  run:
    - python
    - django-appconf ==0.4.1

test:
  # Python imports
  imports:
    - ikari

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Django middleware to allow user configurable domain anchoring to admin configured urlconfs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
