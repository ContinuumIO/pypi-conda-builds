package:
  name: msrflux
  version: "1.3"

source:
  fn: msrflux-1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/msrflux/msrflux-1.3.tar.gz
  md5: 78ad5c38f91d70d6f3a09caf44d0b76b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msrflux = msrflux:main
    #
    # Would create an entry point called msrflux that calls msrflux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.6.0

  run:
    - python
    - numpy >=1.6.0

test:
  # Python imports
  imports:
    - msrflux

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/msrflux/
  license: LICENSE.txt
  summary: 'Computes the neutrino flux from a muon decay ring while averaging over the detector and accelerator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
