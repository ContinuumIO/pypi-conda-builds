package:
  name: pyledsign
  version: "2.0.2"

source:
  fn: pyLEDSign-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyLEDSign/pyLEDSign-2.0.2.tar.gz
  md5: 8ca8b4fdb0865cc43d96fa8e554c944c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyledsign = pyledsign:main
    #
    # Would create an entry point called pyledsign that calls pyledsign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bbcode

  run:
    - python
    - bbcode

test:
  # Python imports
  imports:
    - ledsign
    - ledsign.am03127
    - pyledsign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Mezgrman/pyLEDSign
  license: AGPLv3
  summary: 'A LED message board library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
