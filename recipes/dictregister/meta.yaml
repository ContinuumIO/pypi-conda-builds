package:
  name: dictregister
  version: "1.0.0"

source:
  fn: dictregister-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dictregister/dictregister-1.0.0.tar.gz
  md5: 2d32489fc48a70fe7fff1a77da5b595b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictregister = dictregister:main
    #
    # Would create an entry point called dictregister that calls dictregister.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dictregister

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lgiordani/dictregister
  license: BSD License
  summary: 'A searchable list of dictionaries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
