package:
  name: django-currency_rates
  version: "0.4"

source:
  fn: django-currency_rates-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-currency_rates/django-currency_rates-0.4.tar.gz
  md5: 05eab036675edb88a23a31b15d21fe97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-currency_rates = django-currency_rates:main
    #
    # Would create an entry point called django-currency_rates that calls django-currency_rates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - currency_rates
    - currency_rates.management
    - currency_rates.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ferranp/django-currency-rates
  license: GNU General Public License (GPL)
  summary: 'Currencies & echange rates for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
