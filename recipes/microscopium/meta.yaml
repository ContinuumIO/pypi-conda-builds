package:
  name: microscopium
  version: "0.1.dev0"

source:
  fn: microscopium-0.1.dev0.tar.gz
  url: https://pypi.python.org/packages/source/m/microscopium/microscopium-0.1.dev0.tar.gz
  md5: 0af440545e74967ad945b3cb53aa5485
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - microscopium = microscopium:main
    #
    # Would create an entry point called microscopium that calls microscopium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - microscopium
    - microscopium.screens

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/microscopium/microscopium
  license: BSD 3-clause
  summary: 'Clustering of High Content Screen Images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
