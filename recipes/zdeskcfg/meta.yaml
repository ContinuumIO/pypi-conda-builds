package:
  name: zdeskcfg
  version: "1.0.7"

source:
  fn: zdeskcfg-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/z/zdeskcfg/zdeskcfg-1.0.7.tar.gz
  md5: e31c3af2cca1a6d77cd36f8638f877f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zdeskcfg = zdeskcfg:main
    #
    # Would create an entry point called zdeskcfg that calls zdeskcfg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plac_ini

  run:
    - python
    - plac_ini

test:
  # Python imports
  imports:
    - zdeskcfg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fprimex/zdeskcfg
  license: BSD License
  summary: 'Easy configuration of zendesk/zdesk scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
