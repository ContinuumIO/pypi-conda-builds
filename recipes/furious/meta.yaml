package:
  name: furious
  version: "1.3.0"

source:
  fn: furious-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/f/furious/furious-1.3.0.tar.gz
  md5: 9cff0824644bb54a56a8d5e9d9b1d6c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - furious = furious:main
    #
    # Would create an entry point called furious that calls furious.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - furious
    - furious.context
    - furious.extras
    - furious.extras.appengine
    - furious.handlers
    - furious.test_stubs
    - furious.test_stubs.appengine
    - furious.tests
    - furious.tests.context
    - furious.tests.dummy_module
    - furious.tests.extras
    - furious.tests.extras.appengine
    - furious.tests.handlers
    - furious.tests.test_stubs
    - furious.tests.test_stubs.appengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/WebFilings/furious
  license: Apache Software License
  summary: 'Furious is a lightweight library that wraps Google App Enginetaskqueues to make building dynamic workflows easy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
