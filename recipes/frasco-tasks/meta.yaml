package:
  name: frasco-tasks
  version: "0.1.1"

source:
  fn: frasco-tasks-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-tasks/frasco-tasks-0.1.1.tar.gz
  md5: 6a372298c8e527fefdb3e670fce2659a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-tasks = frasco-tasks:main
    #
    # Would create an entry point called frasco-tasks that calls frasco-tasks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - celery-with-redis >=3.0

  run:
    - python
    - frasco
    - celery-with-redis >=3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-tasks
  license: MIT
  summary: 'Celery integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
