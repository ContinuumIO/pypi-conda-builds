package:
  name: django-lightapi
  version: "0.109"

source:
  fn: django-lightapi-0.109.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lightapi/django-lightapi-0.109.tar.gz
  md5: ac83f06712d995729385a0fcdb255df4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lightapi = django-lightapi:main
    #
    # Would create an entry point called django-lightapi that calls django-lightapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - pymongo

  run:
    - python
    - python-dateutil
    - pymongo

test:
  # Python imports
  imports:
    - lightapi
    - lightapi.cache
    - lightapi.crypto
    - lightapi.django
    - lightapi.django.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ulfur/django-lightapi
  license: UNKNOWN
  summary: 'django-lightapi is a super light weight api framework for exposing restful django webservices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
