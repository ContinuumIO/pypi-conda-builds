package:
  name: django-auction
  version: "0.2.1"

source:
  fn: django-auction-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auction/django-auction-0.2.1.tar.gz
  md5: 5c3a0d4e877644b7ac8bd9cda34b514a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auction = django-auction:main
    #
    # Would create an entry point called django-auction that calls django-auction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.6
    - south ==0.7.6
    - django-polymorphic ==0.2
    - wsgiref ==0.1.2
    - pytz

  run:
    - python
    - django >=1.4,<1.6
    - south ==0.7.6
    - django-polymorphic ==0.2
    - wsgiref ==0.1.2
    - pytz

test:
  # Python imports
  imports:
    - auction
    - auction.migrations
    - auction.models
    - auction.tests
    - auction.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/littlepea/django-auction/
  license: BSD License
  summary: 'Based on django-shop, Django Auction aims to allow easy development of auction apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
