package:
  name: berry_module
  version: "1.3.0"

source:
  fn: berry_module-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/berry_module/berry_module-1.3.0.tar.gz
  md5: ead60da6b0c6f2e653f86be1c48b63a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - berry_module = berry_module:main
    #
    # Would create an entry point called berry_module that calls berry_module.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - berry_module

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home:  
  license: UNKNOWN
  summary: 'Module to print data items within a nested list'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
