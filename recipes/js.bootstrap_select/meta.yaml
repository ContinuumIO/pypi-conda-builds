package:
  name: js.bootstrap_select
  version: "1.5.2"

source:
  fn: js.bootstrap_select-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/j/js.bootstrap_select/js.bootstrap_select-1.5.2.tar.gz
  md5: 44abc6c8c750ac17ed850eafc56c9fd6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.bootstrap_select = js.bootstrap_select:main
    #
    # Would create an entry point called js.bootstrap_select that calls js.bootstrap_select.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.bootstrap
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.bootstrap
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.bootstrap_select

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tmassman/js.bootstrap_select
  license: BSD
  summary: 'Fanstatic packaging of bootstrap-select.js.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
