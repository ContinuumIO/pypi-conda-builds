package:
  name: ckanext-storage
  version: "0.6"

source:
  fn: ckanext-storage-0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-storage/ckanext-storage-0.6.tar.gz
  md5: e8449ad731fd63cc79e291938b8ff963
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-storage = ckanext-storage:main
    #
    # Would create an entry point called ckanext-storage that calls ckanext-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.0b1
    - ofs >=0.4.1

  run:
    - python
    - setuptools
    - boto >=2.0b1
    - ofs >=0.4.1

test:
  # Python imports
  imports:
    - ckanext
    - ckanext.storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://okfn.org/
  license: AGPL
  summary: 'CKAN API Extension for OFS Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
