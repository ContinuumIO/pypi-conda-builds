package:
  name: django-memoize
  version: "1.1.2"

source:
  fn: django-memoize-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-memoize/django-memoize-1.1.2.tar.gz
  md5: 385f6b46121b9e57e08a4f050924c721
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-memoize = django-memoize:main
    #
    # Would create an entry point called django-memoize that calls django-memoize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - memoize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tvavrys/django-memoize
  license: BSD License
  summary: 'An implementation of memoization technique for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
