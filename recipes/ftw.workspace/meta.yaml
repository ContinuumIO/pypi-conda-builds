package:
  name: ftw.workspace
  version: "3.0.1"

source:
  fn: ftw.workspace-3.0.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.workspace/ftw.workspace-3.0.1.zip
  md5: 94f42fce92206e1671456120efdb232d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.workspace = ftw.workspace:main
    #
    # Would create an entry point called ftw.workspace that calls ftw.workspace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.relationfield
    - collective.js.jqsmarttruncation
    - ftw.activity
    - ftw.calendar
    - ftw.colorbox
    - ftw.tabbedview
    - ftw.upgrade
    - plone.formwidget.contenttree
    - plone.namedfile
    - plone.principalsource
    - setuptools

  run:
    - python
    - setuptools
    - z3c.relationfield
    - collective.js.jqsmarttruncation
    - ftw.activity
    - ftw.calendar
    - ftw.colorbox
    - ftw.tabbedview
    - ftw.upgrade
    - plone.formwidget.contenttree
    - plone.namedfile
    - plone.principalsource
    - setuptools

test:
  # Python imports
  imports:
    - ftw
    - ftw.workspace
    - ftw.workspace.browser
    - ftw.workspace.browser.previews
    - ftw.workspace.content
    - ftw.workspace.latex
    - ftw.workspace.tests
    - ftw.workspace.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.workspace
  license: GPL2
  summary: 'A project folder for plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
