package:
  name: django-multilingual-ng
  version: "0.1.21"

source:
  fn: django-multilingual-ng-0.1.21.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multilingual-ng/django-multilingual-ng-0.1.21.tar.gz
  md5: cf47de990eccefc5d5aa78083baf3f1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multilingual-ng = django-multilingual-ng:main
    #
    # Would create an entry point called django-multilingual-ng that calls django-multilingual-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - multilingual
    - multilingual.flatpages
    - multilingual.management
    - multilingual.management.commands
    - multilingual.templatetags
    - tests
    - tests.testproject
    - tests.testproject.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-multilingual-ng
  license: UNKNOWN
  summary: 'Multilingual extension for Django - NG'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
