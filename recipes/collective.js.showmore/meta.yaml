package:
  name: collective.js.showmore
  version: "1.0a4"

source:
  fn: collective.js.showmore-1.0a4.zip
  url: https://pypi.python.org/packages/source/c/collective.js.showmore/collective.js.showmore-1.0a4.zip
  md5: c484b869593b4d2482d65ffcbcf99fb0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.showmore = collective.js.showmore:main
    #
    # Would create an entry point called collective.js.showmore that calls collective.js.showmore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.showmore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.js.showmore/
  license: GNU General Public License (GPL)
  summary: 'JS add-on to show/hide parts of a page.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
