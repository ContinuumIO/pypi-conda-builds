package:
  name: django-memcachedkeys
  version: "0.1.0"

source:
  fn: django-memcachedkeys-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-memcachedkeys/django-memcachedkeys-0.1.0.tar.gz
  md5: 4de52e2dc00b3aa948a5030986496c88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-memcachedkeys = django-memcachedkeys:main
    #
    # Would create an entry point called django-memcachedkeys that calls django-memcachedkeys.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - memcachedkeys
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hzdg/django-memcachedkeys
  license: BSD License
  summary: 'Sanitize your keys, please.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
