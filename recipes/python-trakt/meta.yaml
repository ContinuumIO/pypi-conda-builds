package:
  name: python-trakt
  version: "1.0"

source:
  fn: python-trakt-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-trakt/python-trakt-1.0.tar.gz
  md5: 35e07017574e8d3283e54e9fa0dfed42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-trakt = python-trakt:main
    #
    # Would create an entry point called python-trakt that calls python-trakt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - trakt
    - trakt.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/z4r/python-trakt
  license: Apache Software License
  summary: 'Python interface to trakt.tv API\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
