package:
  name: django-geoportail
  version: "0.4.6"

source:
  fn: django-geoportail-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-geoportail/django-geoportail-0.4.6.tar.gz
  md5: 64925b759a9d9b1a29e8e17848b73900
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geoportail = django-geoportail:main
    #
    # Would create an entry point called django-geoportail that calls django-geoportail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - examples
    - examples.geoportal
    - examples.geoportal.forms
    - examples.geoportal.templatetags
    - geoportal
    - geoportal.forms
    - geoportal.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/bruno/django-geoportail
  license: BSD License
  summary: 'Add maps and photos from the French National Geographic Institute to GeoDjango'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
