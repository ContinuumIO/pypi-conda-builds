package:
  name: django-pipeline-rapydscript
  version: "1.0.0"

source:
  fn: django-pipeline-rapydscript-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pipeline-rapydscript/django-pipeline-rapydscript-1.0.0.tar.gz
  md5: a818b5ad03cb436f52b3065bdbe993f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pipeline-rapydscript = django-pipeline-rapydscript:main
    #
    # Would create an entry point called django-pipeline-rapydscript that calls django-pipeline-rapydscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pipeline_rapydscript

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Use rapydScript with django-pipeline'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
