package:
  name: asteval
  version: "0.9.3"

source:
  fn: asteval-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/a/asteval/asteval-0.9.3.tar.gz
  md5: c8b234b65c2965af48b17f7fbf814902
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asteval = asteval:main
    #
    # Would create an entry point called asteval that calls asteval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asteval

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/newville/asteval
  license: BSD
  summary: 'Safe, minimalistic evaluator of python expression using ast module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
