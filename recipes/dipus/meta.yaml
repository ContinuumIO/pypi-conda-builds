package:
  name: dipus
  version: "0.0.5"

source:
  fn: dipus-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dipus/dipus-0.0.5.tar.gz
  md5: 8600525ae46023397699ce7407ab3d17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dipus = dipus:main
    #
    # Would create an entry point called dipus that calls dipus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle
    - sphinx
    - whoosh
    - docutils
    - simplejson

  run:
    - python
    - bottle
    - sphinx
    - whoosh
    - docutils
    - simplejson

test:
  # Python imports
  imports:
    - dipus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r_rudi/dipus
  license: BSD License
  summary: 'Dipus is a simple full-text search server using Whoosh for Sphinx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
