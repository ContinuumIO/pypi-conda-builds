package:
  name: autonetkit_vis
  version: "0.1.13"

source:
  fn: autonetkit_vis-0.1.13.tar.gz
  url: https://pypi.python.org/packages/source/a/autonetkit_vis/autonetkit_vis-0.1.13.tar.gz
  md5: 215aa7454b1b951482b16c615691a4af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autonetkit_vis = autonetkit_vis:main
    #
    # Would create an entry point called autonetkit_vis that calls autonetkit_vis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - autonetkit_vis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.autonetkit.org
  license: UNKNOWN
  summary: 'Visualisation for AutoNetkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
