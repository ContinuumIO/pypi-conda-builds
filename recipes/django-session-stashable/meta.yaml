package:
  name: django-session-stashable
  version: "1.0"

source:
  fn: django-session-stashable-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-session-stashable/django-session-stashable-1.0.tar.gz
  md5: dd11413d7d79ad53cae88a870692a192
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-session-stashable = django-session-stashable:main
    #
    # Would create an entry point called django-session-stashable that calls django-session-stashable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_session_stashable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaylett/django_session_stashable
  license: UNKNOWN
  summary: 'Django model mixin that makes it easy to manage a list of objects associated with the current session.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
