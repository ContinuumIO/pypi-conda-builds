package:
  name: kryptonium
  version: "0.1.4"

source:
  fn: kryptonium-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/k/kryptonium/kryptonium-0.1.4.tar.gz
  md5: cbf40bc8c1170ffe942e50166105e737
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kryptonium = kryptonium:main
    #
    # Would create an entry point called kryptonium that calls kryptonium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - selenium

  run:
    - python
    - selenium

test:
  # Python imports
  imports:
    - kryptonium

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danggrianto/kryptonium
  license: GNU GPL v2.0
  summary: 'Automation Framework using Selenium'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
