package:
  name: django-email-as-username
  version: "1.6.7"

source:
  fn: django-email-as-username-1.6.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-as-username/django-email-as-username-1.6.7.tar.gz
  md5: 9f186c7913422e00710a0e25592d67f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-as-username = django-email-as-username:main
    #
    # Would create an entry point called django-email-as-username that calls django-email-as-username.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - emailusernames
    - emailusernames/management
    - emailusernames/management/commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dabapps/django-email-as-username
  license: BSD
  summary: 'User authentication with email addresses instead of usernames.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
