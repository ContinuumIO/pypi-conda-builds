package:
  name: plonetheme.delicious2
  version: "0.2"

source:
  fn: plonetheme.delicious2-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.delicious2/plonetheme.delicious2-0.2.tar.gz
  md5: e2fed6a8d91b4fb4a103744750d9315b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.delicious2 = plonetheme.delicious2:main
    #
    # Would create an entry point called plonetheme.delicious2 that calls plonetheme.delicious2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.delicious2
    - plonetheme.delicious2.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/plonetheme.delicious2
  license: GPL
  summary: 'An installable theme for Plone 3.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
