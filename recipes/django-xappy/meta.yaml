package:
  name: django-xappy
  version: "0.1"

source:
  fn: django-xappy-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-xappy/django-xappy-0.1.zip
  md5: a14b1b29a6034bd814aa677dd445187e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xappy = django-xappy:main
    #
    # Would create an entry point called django-xappy that calls django-xappy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_xappy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/django-xappy
  license: BSD License
  summary: 'Bridges the Xappy/Xapian search engine library with the Django web framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
