package:
  name: django_util_js
  version: "0.0.15"

source:
  fn: django_util_js-0.0.15.tar.gz
  url: https://pypi.python.org/packages/source/d/django_util_js/django_util_js-0.0.15.tar.gz
  md5: bd08de4d512523fced57967ec135e002
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_util_js = django_util_js:main
    #
    # Would create an entry point called django_util_js that calls django_util_js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_util_js

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/django_util_js
  license: BSD
  summary: "django's util in javascript. such as url_for etc."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
