package:
  name: django-materialized-views
  version: "0.3.3"

source:
  fn: django-materialized-views-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-materialized-views/django-materialized-views-0.3.3.tar.gz
  md5: 9714a684d8103029b60cc336a08b54e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-materialized-views = django-materialized-views:main
    #
    # Would create an entry point called django-materialized-views that calls django-materialized-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_materialized_views
    - django_materialized_views.management
    - django_materialized_views.management.commands
    - django_materialized_views.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisspen/django-materialized-views
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
