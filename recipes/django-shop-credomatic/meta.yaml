package:
  name: django-shop-credomatic
  version: "0.0.1"

source:
  fn: django-shop-credomatic-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-credomatic/django-shop-credomatic-0.0.1.tar.gz
  md5: 8ac0988d2a5b48577e0956011410f9f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-credomatic = django-shop-credomatic:main
    #
    # Would create an entry point called django-shop-credomatic that calls django-shop-credomatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-crispy-forms

  run:
    - python
    - django >=1.3
    - django-crispy-forms

test:
  # Python imports
  imports:
    - shop_bac

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Credomatic payment backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
