package:
  name: django-select-multiple-field
  version: "0.4.0"

source:
  fn: django-select-multiple-field-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-select-multiple-field/django-select-multiple-field-0.4.0.tar.gz
  md5: faad2467dc040e67149ba227385dd626
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-select-multiple-field = django-select-multiple-field:main
    #
    # Would create an entry point called django-select-multiple-field that calls django-select-multiple-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - select_multiple_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kelvinwong-ca/django-select-multiple-field
  license: BSD License
  summary: 'Select multiple choices in a single Django model field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
