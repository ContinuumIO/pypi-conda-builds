package:
  name: hyou
  version: "1.2"

source:
  fn: hyou-1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hyou/hyou-1.2.tar.gz
  md5: cca51e57dfa36259876e9dbc18ee341b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyou = hyou:main
    #
    # Would create an entry point called hyou that calls hyou.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata
    - oauth2client
    - python-gflags

  run:
    - python
    - gdata
    - oauth2client
    - python-gflags

test:
  # Python imports
  imports:
    - hyou

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/google/hyou/
  license: Apache Software License
  summary: 'Pythonic Interface to manipulate Google Spreadsheet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
