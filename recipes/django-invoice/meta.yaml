package:
  name: django-invoice
  version: "0.3.2"

source:
  fn: django-invoice-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-invoice/django-invoice-0.3.2.tar.gz
  md5: a4c52ce7ec56f8e7210f6927b6696baf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-invoice = django-invoice:main
    #
    # Would create an entry point called django-invoice that calls django-invoice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-addresses
    - django-extensions
    - reportlab

  run:
    - python
    - django-addresses
    - django-extensions
    - reportlab

test:
  # Python imports
  imports:
    - invoice
    - invoice/conf
    - invoice/management
    - invoice/management/commands
    - invoice/migrations
    - invoice/utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/simonluijk/django-invoice
  license: BSD License
  summary: 'Django invoicing app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
