package:
  name: bracket_expansion
  version: "0.1.0"

source:
  fn: bracket_expansion-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bracket_expansion/bracket_expansion-0.1.0.tar.gz
  md5: 8374aef0d25950d7fff9bd68d0b8d9cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bracket_expansion = bracket_expansion:main
    #
    # Would create an entry point called bracket_expansion that calls bracket_expansion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bracket_expansion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeremyschulman/bracket_expansion
  license: GNU General Public License v3 (GPLv3)
  summary: 'string generator for bracket patterns'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
