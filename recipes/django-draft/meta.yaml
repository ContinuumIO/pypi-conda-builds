package:
  name: django-draft
  version: "0.2.2"

source:
  fn: django-draft-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-draft/django-draft-0.2.2.tar.gz
  md5: c4b50b8d564182693b5f4415ba511805
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-draft = django-draft:main
    #
    # Would create an entry point called django-draft that calls django-draft.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - draft
    - draft.migrations
    - draft.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/platypus-creation/django-draft
  license: UNKNOWN
  summary: '\n    Drafts for django admin\n    '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
