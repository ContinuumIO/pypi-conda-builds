package:
  name: django_structurer
  version: "0.3.1"

source:
  fn: django_structurer-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_structurer/django_structurer-0.3.1.tar.gz
  md5: dcf5aaf7eaceedc1d3ad22b0f3aae54c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_structurer = django_structurer:main
    #
    # Would create an entry point called django_structurer that calls django_structurer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyyaml ==3.09

  run:
    - python
    - pyyaml ==3.09

test:
  # Python imports
  imports:
    - django_structurer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arruda/django-structurer
  license: MIT License
  summary: 'This app allow you to create your own custom django project structure, and then use it as you please.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
