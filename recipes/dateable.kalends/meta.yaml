package:
  name: dateable.kalends
  version: "1.0"

source:
  fn: dateable.kalends-1.0.zip
  url: https://pypi.python.org/packages/source/d/dateable.kalends/dateable.kalends-1.0.zip
  md5: 2d142df14884a1f471126b5dcad9128c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dateable.kalends = dateable.kalends:main
    #
    # Would create an entry point called dateable.kalends that calls dateable.kalends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - dateable
    - dateable.kalends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/dateable.kalends
  license: GNU General Public License (GPL)
  summary: 'Dateable calendaring API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
