package:
  name: xstatic-jquery.tablesorter
  version: "2.14.5.1"

source:
  fn: XStatic-JQuery.TableSorter-2.14.5.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-JQuery.TableSorter/XStatic-JQuery.TableSorter-2.14.5.1.tar.gz
  md5: fc05a6731b6ac3f6489b893f96a5d29a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-jquery.tablesorter = xstatic-jquery.tablesorter:main
    #
    # Would create an entry point called xstatic-jquery.tablesorter that calls xstatic-jquery.tablesorter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.jquery_tablesorter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Mottie/tablesorter
  license: (same as JQuery.TableSorter)
  summary: 'JQuery.TableSorter 2.14.5 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
