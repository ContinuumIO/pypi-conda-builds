package:
  name: buildout.eggscleaner
  version: "0.1.7"

source:
  fn: buildout.eggscleaner-0.1.7.zip
  url: https://pypi.python.org/packages/source/b/buildout.eggscleaner/buildout.eggscleaner-0.1.7.zip
  md5: 09faef8c498742bdeacac9d83f887e75
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.eggscleaner = buildout.eggscleaner:main
    #
    # Would create an entry point called buildout.eggscleaner that calls buildout.eggscleaner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - buildout
    - buildout.eggscleaner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thepjot/buildout.eggscleaner
  license: Zope Public License
  summary: 'A buildout extension to move non-used eggs to a specified directory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
