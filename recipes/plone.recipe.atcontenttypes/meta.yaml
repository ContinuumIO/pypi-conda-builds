package:
  name: plone.recipe.atcontenttypes
  version: "0.2.1"

source:
  fn: plone.recipe.atcontenttypes-0.2.1.zip
  url: https://pypi.python.org/packages/source/p/plone.recipe.atcontenttypes/plone.recipe.atcontenttypes-0.2.1.zip
  md5: 5ae053efcf09d2877b80515869b9b974
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.atcontenttypes = plone.recipe.atcontenttypes:main
    #
    # Would create an entry point called plone.recipe.atcontenttypes that calls plone.recipe.atcontenttypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.atcontenttypes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.plone.org/collective/browser/buildout/plone.recipe.atcontenttypes
  license: Zope Public License
  summary: 'ZC Buildout recipe to generate an ATContentTypes configuration file in the etc/ folder of a zope instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
