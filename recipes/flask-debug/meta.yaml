package:
  name: flask-debug
  version: "0.4.1"

source:
  fn: Flask-Debug-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Debug/Flask-Debug-0.4.1.tar.gz
  md5: 09aedf17179e0125751810c1aee5485d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-debug = flask-debug:main
    #
    # Would create an entry point called flask-debug that calls flask-debug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - inflection

  run:
    - python
    - flask
    - inflection

test:
  # Python imports
  imports:
    - flask_debug
    - flask_debug_plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/flask-debug
  license: MIT
  summary: 'Shows reflection/configuration to aid the development of Flask applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
