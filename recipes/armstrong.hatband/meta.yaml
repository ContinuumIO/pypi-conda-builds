package:
  name: armstrong.hatband
  version: "1.4.0"

source:
  fn: armstrong.hatband-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.hatband/armstrong.hatband-1.4.0.tar.gz
  md5: ef47b8b28ee6ec8596818b06ce301673
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.hatband = armstrong.hatband:main
    #
    # Would create an entry point called armstrong.hatband that calls armstrong.hatband.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - armstrong.utils.backends >=1.0.0,<2.0
    - armstrong.core.arm_layout >=1.1.1,<2.0

  run:
    - python
    - armstrong.utils.backends >=1.0.0,<2.0
    - armstrong.core.arm_layout >=1.1.1,<2.0

test:
  # Python imports
  imports:
    - armstrong.hatband
    - armstrong.hatband.templatetags
    - armstrong.hatband.tests
    - armstrong.hatband.tests.hatband_support
    - armstrong.hatband.tests.widgets
    - armstrong.hatband.views
    - armstrong.hatband.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.hatband/
  license: Apache Software License
  summary: 'The administrative interface for Armstrong'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
