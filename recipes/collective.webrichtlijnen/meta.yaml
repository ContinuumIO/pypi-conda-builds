package:
  name: collective.webrichtlijnen
  version: "1.1.2"

source:
  fn: collective.webrichtlijnen-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.webrichtlijnen/collective.webrichtlijnen-1.1.2.tar.gz
  md5: 0371a0d32f0b6a27d34423443657c3dc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.webrichtlijnen = collective.webrichtlijnen:main
    #
    # Would create an entry point called collective.webrichtlijnen that calls collective.webrichtlijnen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone >4.0a

  run:
    - python
    - setuptools
    - setuptools
    - plone >4.0a

test:
  # Python imports
  imports:
    - collective
    - collective.webrichtlijnen
    - collective.webrichtlijnen.browser
    - collective.webrichtlijnen.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.webrichtlijnen/
  license: GPL
  summary: 'A theme product for the Dutch web guidelines.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
