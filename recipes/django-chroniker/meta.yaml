package:
  name: django-chroniker
  version: "0.8.1"

source:
  fn: django-chroniker-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chroniker/django-chroniker-0.8.1.tar.gz
  md5: 3240a96c9c8e41c9873a10954bb025c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chroniker = django-chroniker:main
    #
    # Would create an entry point called django-chroniker that calls django-chroniker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0
    - python-dateutil >=2.2
    - psutil >=2.1.1
    - six >=1.7.2

  run:
    - python
    - django >=1.4.0
    - python-dateutil >=2.2
    - psutil >=2.1.1
    - six >=1.7.2

test:
  # Python imports
  imports:
    - chroniker
    - chroniker.management
    - chroniker.management.commands
    - chroniker.migrations
    - chroniker.templatetags
    - chroniker.tests
    - chroniker.tests.management
    - chroniker.tests.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisspen/django-chroniker
  license: BSD License
  summary: "Easily control cron jobs via Django's admin."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
