package:
  name: metriqueu
  version: "0.2.7-1a"

source:
  fn: metriqueu-0.2.7-1a.tar.gz
  url: https://pypi.python.org/packages/source/m/metriqueu/metriqueu-0.2.7-1a.tar.gz
  md5: d54c6ec2b032c3880d4c932a04a16826
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metriqueu = metriqueu:main
    #
    # Would create an entry point called metriqueu that calls metriqueu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator ==3.4.0
    - futures ==2.1.6
    - pql ==0.4.2
    - psutil ==1.2.1
    - python_dateutil ==2.2.0
    - pytz
    - simplejson ==3.3.2

  run:
    - python
    - decorator ==3.4.0
    - futures ==2.1.6
    - pql ==0.4.2
    - psutil ==1.2.1
    - python_dateutil ==2.2.0
    - pytz
    - simplejson ==3.3.2

test:
  # Python imports
  imports:
    - metriqueu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drpoovilleorg/metrique
  license: GPLv3
  summary: 'Metrique - Shared Utility Libraries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
