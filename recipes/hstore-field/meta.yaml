package:
  name: hstore-field
  version: "1.0.3"

source:
  fn: hstore-field-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hstore-field/hstore-field-1.0.3.tar.gz
  md5: 4f50abc43e11ade0607c69f9028f6cfd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hstore-field = hstore-field:main
    #
    # Would create an entry point called hstore-field that calls hstore-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hstore_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/anantasty/hstore-field
  license: BSD
  summary: "Support for PostgreSQL's hstore for Django."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
