package:
  name: httreplay
  version: "0.2.0"

source:
  fn: httreplay-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httreplay/httreplay-0.2.0.tar.gz
  md5: 9a0baa6a510905acf95e1aca6db6a6df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httreplay = httreplay:main
    #
    # Would create an entry point called httreplay that calls httreplay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - six ==1.5.2

  run:
    - python
    - six ==1.5.2

test:
  # Python imports
  imports:
    - httreplay
    - httreplay.stubs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/davepeck/httreplay
  license: MIT License
  summary: 'A HTTP replay library for testing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
