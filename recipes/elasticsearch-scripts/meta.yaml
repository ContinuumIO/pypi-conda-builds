package:
  name: elasticsearch-scripts
  version: "1.0.2"

source:
  fn: elasticsearch-scripts-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/elasticsearch-scripts/elasticsearch-scripts-1.0.2.tar.gz
  md5: 6bf8a585d6e1fb3f5bdfc87aa5cb8740
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticsearch-scripts = elasticsearch-scripts:main
    #
    # Would create an entry point called elasticsearch-scripts that calls elasticsearch-scripts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - esadmin

  run:
    - python
    - esadmin

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/anchor/elasticsearch-scripts
  license: MIT
  summary: 'Set of scripts for administering Elasticsearch clusters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
