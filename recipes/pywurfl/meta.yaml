package:
  name: pywurfl
  version: "7.2.1"

source:
  fn: pywurfl-7.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pywurfl/pywurfl-7.2.1.tar.gz
  md5: 9c487f2548e90f7ba24125c81ed55538
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pywurfl = pywurfl:main
    #
    # Would create an entry point called pywurfl that calls pywurfl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-levenshtein

  run:
    - python
    - python-levenshtein

test:
  # Python imports
  imports:
    - pywurfl
    - pywurfl.algorithms
    - pywurfl.algorithms.wurfl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://celljam.net/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'pywurfl - Python tools for processing and querying the Wireless Universal Resource File (WURFL)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
