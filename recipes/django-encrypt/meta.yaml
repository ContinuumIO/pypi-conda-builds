package:
  name: django-encrypt
  version: "0.1"

source:
  fn: django-encrypt-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-encrypt/django-encrypt-0.1.tar.gz
  md5: 042507d90c633edda226023bd0433557
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-encrypt = django-encrypt:main
    #
    # Would create an entry point called django-encrypt that calls django-encrypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - encrypt
    - encrypt.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'A quick way to encrypt python objects and store'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
