package:
  name: aprslib
  version: "0.6.37"

source:
  fn: aprslib-0.6.37.tar.gz
  url: https://pypi.python.org/packages/source/a/aprslib/aprslib-0.6.37.tar.gz
  md5: 14295c2917c47219f7e5e1214aeecb43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aprslib = aprslib:main
    #
    # Would create an entry point called aprslib that calls aprslib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aprslib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rossengeorgiev/aprs-python
  license: GNU General Public License v2 (GPLv2)
  summary: 'Module for accessing APRS-IS and parsing APRS packets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
