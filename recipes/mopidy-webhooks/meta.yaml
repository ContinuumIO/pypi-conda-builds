package:
  name: mopidy-webhooks
  version: "0.2.0"

source:
  fn: Mopidy-Webhooks-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Webhooks/Mopidy-Webhooks-0.2.0.tar.gz
  md5: 33b8c1c8af0b0ddf655856e7f2064eb8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-webhooks = mopidy-webhooks:main
    #
    # Would create an entry point called mopidy-webhooks that calls mopidy-webhooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - requests

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - requests

test:
  # Python imports
  imports:
    - mopidy_webhooks
    - mopidy_webhooks.reporters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paddycarey/mopidy-webhooks
  license: Apache Software License
  summary: u'Mopidy extension that sends webhook notifications to a remote server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
