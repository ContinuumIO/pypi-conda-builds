package:
  name: vdt.versionplugin.gitchain
  version: "0.0.4"

source:
  fn: vdt.versionplugin.gitchain-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/v/vdt.versionplugin.gitchain/vdt.versionplugin.gitchain-0.0.4.tar.gz
  md5: f8cf6837bed14839240ddf38cb5ac965
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vdt.versionplugin.gitchain = vdt.versionplugin.gitchain:main
    #
    # Would create an entry point called vdt.versionplugin.gitchain that calls vdt.versionplugin.gitchain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vdt.version
    - vdt.versionplugin.default

  run:
    - python
    - setuptools
    - vdt.version
    - vdt.versionplugin.default

test:
  # Python imports
  imports:
    - vdt
    - vdt.versionplugin
    - vdt.versionplugin.gitchain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Instead of building packages as a freezed state, propagate tags between git repositories.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
