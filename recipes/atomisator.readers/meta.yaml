package:
  name: atomisator.readers
  version: "0.1.3"

source:
  fn: atomisator.readers-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/atomisator.readers/atomisator.readers-0.1.3.tar.gz
  md5: 7b7ecf66bb62492e0778d890d46cc18d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomisator.readers = atomisator.readers:main
    #
    # Would create an entry point called atomisator.readers that calls atomisator.readers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - atomisator.parser

  run:
    - python
    - setuptools
    - setuptools
    - atomisator.parser

test:
  # Python imports
  imports:
    - atomisator
    - atomisator.readers
    - atomisator.readers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atomisator.ziade.org
  license: GPL
  summary: 'Expert Python Programming - Readers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
