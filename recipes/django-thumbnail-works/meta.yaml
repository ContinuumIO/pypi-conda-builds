package:
  name: django-thumbnail-works
  version: "0.2.3"

source:
  fn: django-thumbnail-works-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-thumbnail-works/django-thumbnail-works-0.2.3.tar.gz
  md5: 4211e4f7d68aa921b4b00874de290c33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-thumbnail-works = django-thumbnail-works:main
    #
    # Would create an entry point called django-thumbnail-works that calls django-thumbnail-works.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - thumbnail_works

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-thumbnail-works
  license: Apache Software License
  summary: 'django-thumbnail-works adds thumbnail support to the default ImageField.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
