package:
  name: collective.contentrules.template
  version: "1.0"

source:
  fn: collective.contentrules.template-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentrules.template/collective.contentrules.template-1.0.tar.gz
  md5: 9f0d1f643c37e794d12bcc8d86e04b72
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.template = collective.contentrules.template:main
    #
    # Would create an entry point called collective.contentrules.template that calls collective.contentrules.template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.contentrules
    - plone.contentrules

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.contentrules
    - plone.contentrules

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contentrules.template
  license: GPL
  summary: 'A Plone content rule for creating content based on templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
