package:
  name: collective.linguasitemap
  version: "1.0"

source:
  fn: collective.linguasitemap-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.linguasitemap/collective.linguasitemap-1.0.zip
  md5: f17b888d83b1dcc7d20c95fba5c1afd9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.linguasitemap = collective.linguasitemap:main
    #
    # Would create an entry point called collective.linguasitemap that calls collective.linguasitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.linguaplone

  run:
    - python
    - setuptools
    - setuptools
    - products.linguaplone

test:
  # Python imports
  imports:
    - collective
    - collective.linguasitemap
    - collective.linguasitemap.browser
    - collective.linguasitemap.tests
    - collective.linguasitemap.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.linguasitemap
  license: GPL
  summary: 'Sitemap for MultiLanguage Plone site using LinguaPlone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
