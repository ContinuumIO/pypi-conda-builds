package:
  name: django-treeform
  version: "0.1"

source:
  fn: django-treeform-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-treeform/django-treeform-0.1.tar.gz
  md5: 43456bdcb885a5ec6bd2b34b9ec09774
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-treeform = django-treeform:main
    #
    # Would create an entry point called django-treeform that calls django-treeform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - django_treeform
    - django_treeform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/podhmo/djang-treeform
  license: UNKNOWN
  summary: '-'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
