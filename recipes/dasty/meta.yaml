package:
  name: dasty
  version: "0.1"

source:
  fn: dasty-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dasty/dasty-0.1.tar.gz
  md5: 828eb1e032f059f43bb85bc15ca68d8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dasty = dasty:main
    #
    # Would create an entry point called dasty that calls dasty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dasty
    - dasty.core
    - dasty.material
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.irit.fr/NatSim
  license: http://www.cecill.info/licences/Licence_CeCILL-C_V1-en.html
  summary: 'Dasty: data structure for multi-models and multi-resolutions 3D scenes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
