package:
  name: common-mimetypes
  version: "0.1rc1"

source:
  fn: common-mimetypes-0.1rc1.zip
  url: https://pypi.python.org/packages/source/c/common-mimetypes/common-mimetypes-0.1rc1.zip
  md5: d0dcb4b3765837754e9be28f1bb0d3a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - common-mimetypes = common-mimetypes:main
    #
    # Would create an entry point called common-mimetypes that calls common-mimetypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iki/common-mimetypes
  license: BSD License
  summary: 'Common MIME types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
