package:
  name: collective.ads
  version: "0.1"

source:
  fn: collective.ads-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ads/collective.ads-0.1.tar.gz
  md5: 26c2aa570477fddd36d09af94e4033c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ads = collective.ads:main
    #
    # Would create an entry point called collective.ads that calls collective.ads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.ads
    - collective.ads.admin
    - collective.ads.browser
    - collective.ads.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.example
  license: GPL
  summary: 'Plone Ads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
