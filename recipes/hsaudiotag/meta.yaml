package:
  name: hsaudiotag
  version: "1.1.1"

source:
  fn: hsaudiotag-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hsaudiotag/hsaudiotag-1.1.1.tar.gz
  md5: 751a554bd22c8c588bf25dd7ef00b9ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hsaudiotag = hsaudiotag:main
    #
    # Would create an entry point called hsaudiotag that calls hsaudiotag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hsaudiotag

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.hardcoded.net/hsaudiotag/
  license: BSD License
  summary: 'Read metdata (tags) of mp3, mp4, wma, ogg, flac and aiff files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
