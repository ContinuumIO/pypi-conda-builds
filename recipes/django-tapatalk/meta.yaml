package:
  name: django-tapatalk
  version: "0.5.4"

source:
  fn: django-tapatalk-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tapatalk/django-tapatalk-0.5.4.tar.gz
  md5: 89b1a481c29ddaeace26a8a4e361d4f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tapatalk = django-tapatalk:main
    #
    # Would create an entry point called django-tapatalk that calls django-tapatalk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tapatalk
    - tapatalk.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svdgraaf/django-tapatalk/
  license: GNU General Public License v3 (GPLv3)
  summary: 'DjangoBB Tapatalk implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
