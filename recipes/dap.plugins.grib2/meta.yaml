package:
  name: dap.plugins.grib2
  version: "0.0dev-r37"

source:
  fn: dap.plugins.grib2-0.0dev-r37.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.plugins.grib2/dap.plugins.grib2-0.0dev-r37.tar.gz
  md5: d22a2cf12dc332aa0e61f3beb9edcbd1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.plugins.grib2 = dap.plugins.grib2:main
    #
    # Would create an entry point called dap.plugins.grib2 that calls dap.plugins.grib2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dap[server] >=2.2.4

  run:
    - python
    - setuptools
    - dap[server] >=2.2.4

test:
  # Python imports
  imports:
    - dap
    - dap.plugins
    - dap.plugins.grib2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'A plugin for pydap server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
