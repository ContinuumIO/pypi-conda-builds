package:
  name: django-stashboard
  version: "0.1dev.20141117.1520"

source:
  fn: django-stashboard-0.1dev.20141117.1520.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stashboard/django-stashboard-0.1dev.20141117.1520.tar.gz
  md5: 17038d6ca7f53a66f8895a4efac69804
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stashboard = django-stashboard:main
    #
    # Would create an entry point called django-stashboard that calls django-stashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7,<1.8
    - django-uuidfield ==0.5.0
    - djangorestframework >=2.4,<2.5
    - pytz

  run:
    - python
    - django >=1.7,<1.8
    - django-uuidfield ==0.5.0
    - djangorestframework >=2.4,<2.5
    - pytz

test:
  # Python imports
  imports:
    - stashboard
    - stashboard.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goodtune/django-stashboard
  license: UNKNOWN
  summary: 'Fork of Stashboard (GAE) modified for pure-django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
