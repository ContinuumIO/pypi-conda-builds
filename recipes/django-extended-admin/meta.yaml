package:
  name: django-extended-admin
  version: "1.0.6"

source:
  fn: django-extended-admin-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extended-admin/django-extended-admin-1.0.6.tar.gz
  md5: 428f54a260fd3b7edb31de903491b1f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extended-admin = django-extended-admin:main
    #
    # Would create an entry point called django-extended-admin that calls django-extended-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5.1
    - django-apptemplates

  run:
    - python
    - django >=1.5.1
    - django-apptemplates

test:
  # Python imports
  imports:
    - admin_extended

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/masterzeb/django-extended-admin/
  license: BSD
  summary: 'Additional features for django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
