package:
  name: asynchttp
  version: "0.0.4"

source:
  fn: asynchttp-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/asynchttp/asynchttp-0.0.4.tar.gz
  md5: a5ffa9536948d02eb8e560e2115b76b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asynchttp = asynchttp:main
    #
    # Would create an entry point called asynchttp that calls asynchttp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.5.0

  run:
    - python
    - httplib2 >=0.5.0

test:
  # Python imports
  imports:
    - asynchttp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ross/python-asynchttp
  license: UNKNOWN
  summary: 'A simple httplib2 based asynchronous HTTP library for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
