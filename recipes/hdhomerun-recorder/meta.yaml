package:
  name: hdhomerun-recorder
  version: "0.1.0"

source:
  fn: hdhomerun-recorder-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hdhomerun-recorder/hdhomerun-recorder-0.1.0.tar.gz
  md5: 213fbef3468b74ab7eb3b9c9c716ffae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hdhomerun-recorder = hdhomerun-recorder:main
    #
    # Would create an entry point called hdhomerun-recorder that calls hdhomerun-recorder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hdhomerun_recorder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hdhomerun-recorder/
  license: UNKNOWN
  summary: 'hdhomerun network tuner recorder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
