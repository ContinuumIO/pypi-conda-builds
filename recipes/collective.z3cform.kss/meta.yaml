package:
  name: collective.z3cform.kss
  version: "0.1b1dev"

source:
  fn: collective.z3cform.kss-0.1b1dev.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.z3cform.kss/collective.z3cform.kss-0.1b1dev.tar.gz
  md5: 2be6c48483f9297b888bf960f2c5ec4d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.kss = collective.z3cform.kss:main
    #
    # Would create an entry point called collective.z3cform.kss that calls collective.z3cform.kss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - kss.core
    - plone.z3cform
    - plone.app.form

  run:
    - python
    - setuptools
    - setuptools
    - kss.core
    - plone.z3cform
    - plone.app.form

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.kss
    - collective.z3cform.kss.kss
    - collective.z3cform.kss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.z3cform.kss/trunk/
  license: GPL
  summary: 'enable inline validation with z3c form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
