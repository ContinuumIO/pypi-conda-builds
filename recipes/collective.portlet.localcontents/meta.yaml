package:
  name: collective.portlet.localcontents
  version: "0.3.0"

source:
  fn: collective.portlet.localcontents-0.3.0.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.localcontents/collective.portlet.localcontents-0.3.0.zip
  md5: 9fcbcd737c3ab3d2d979ddbc8be270da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.localcontents = collective.portlet.localcontents:main
    #
    # Would create an entry point called collective.portlet.localcontents that calls collective.portlet.localcontents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.localcontents
    - collective.portlet.localcontents.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.portlet.localcontents
  license: GPL
  summary: 'A local navigation for Plone, that shows only contents in current folder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
