package:
  name: django-auto-urls
  version: "0.1"

source:
  fn: django-auto-urls-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auto-urls/django-auto-urls-0.1.tar.gz
  md5: 5d74d2601d3dc5c976f2b1a45c25ed91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auto-urls = django-auto-urls:main
    #
    # Would create an entry point called django-auto-urls that calls django-auto-urls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - auto_urls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-auto-urls/
  license: MIT License
  summary: "Django app that try to load template by it's file name passed in url."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
