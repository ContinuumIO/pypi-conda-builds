package:
  name: aldryn-bootstrap3
  version: "0.4.5"

source:
  fn: aldryn-bootstrap3-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-bootstrap3/aldryn-bootstrap3-0.4.5.tar.gz
  md5: b395fa12429fb6af3bdbbdd5f738fc11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-bootstrap3 = aldryn-bootstrap3:main
    #
    # Would create an entry point called aldryn-bootstrap3 that calls aldryn-bootstrap3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-durationfield >=0.5.1

  run:
    - python
    - django-durationfield >=0.5.1

test:
  # Python imports
  imports:
    - aldryn_bootstrap3
    - aldryn_bootstrap3.migrations
    - aldryn_bootstrap3.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-bootstrap3
  license: BSD License
  summary: 'cms plugins and helpers for bootstrap3 based sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
