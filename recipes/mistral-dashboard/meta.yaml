package:
  name: mistral-dashboard
  version: "2015.1.0"

source:
  fn: mistral-dashboard-2015.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mistral-dashboard/mistral-dashboard-2015.1.0.tar.gz
  md5: 1e0eb5f2cd745b9c60e38edf06d7029c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mistral-dashboard = mistral-dashboard:main
    #
    # Would create an entry point called mistral-dashboard that calls mistral-dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License, Version 2.0
  summary: 'Mistral dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
