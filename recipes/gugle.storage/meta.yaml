package:
  name: gugle.storage
  version: "1.0dev-r627"

source:
  fn: gugle.storage-1.0dev-r627.tar.gz
  url: https://pypi.python.org/packages/source/g/gugle.storage/gugle.storage-1.0dev-r627.tar.gz
  md5: 13435803cc0d41099f3553fac86a3ca3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gugle.storage = gugle.storage:main
    #
    # Would create an entry point called gugle.storage that calls gugle.storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zodb3 >=3.8

  run:
    - python
    - setuptools
    - setuptools
    - zodb3 >=3.8

test:
  # Python imports
  imports:
    - gugle
    - gugle.storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://manuelonsoftware.wordpress.com/
  license: GNU General Public License (GPL)
  summary: 'Storages for Gugle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
