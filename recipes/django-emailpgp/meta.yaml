package:
  name: django-emailpgp
  version: "1.0"

source:
  fn: django-emailpgp-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailpgp/django-emailpgp-1.0.tar.gz
  md5: 042fb1f8178f9bfd821a29c1033a8d0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailpgp = django-emailpgp:main
    #
    # Would create an entry point called django-emailpgp that calls django-emailpgp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - DjangoEmailPGP

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.abnorm.org/projects/django-emailpgp/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Django module to support PGP encrypted and signed e-mails with attachments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
