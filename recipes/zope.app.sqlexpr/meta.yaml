package:
  name: zope.app.sqlexpr
  version: "0.1"

source:
  fn: zope.app.sqlexpr-0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.sqlexpr/zope.app.sqlexpr-0.1.tar.gz
  md5: cc0861b2073c25e79b00cc13c27f77b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.sqlexpr = zope.app.sqlexpr:main
    #
    # Would create an entry point called zope.app.sqlexpr that calls zope.app.sqlexpr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.tales
    - zope.exceptions
    - zope.rdb

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.tales
    - zope.exceptions
    - zope.rdb

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.sqlexpr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.sqlexpr
  license: Zope Public License
  summary: 'allow quick SQL queries in TALES expressions and Zope Page Templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
