package:
  name: clever-alexis
  version: "0.1.1.dev5"

source:
  fn: clever-alexis-0.1.1.dev5.tar.gz
  url: https://pypi.python.org/packages/source/c/clever-alexis/clever-alexis-0.1.1.dev5.tar.gz
  md5: 4fd4eea97994657c8247863dae26b520
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clever-alexis = clever-alexis:main
    #
    # Would create an entry point called clever-alexis that calls clever-alexis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.4.0

  run:
    - python
    - fabric >=1.4.0

test:
  # Python imports
  imports:
    - alexis
    - alexis.libs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/KulaPard/alexis
  license: BSD License
  summary: 'Clever redhead girl that builds and packs Python project with Virtualenv into rpm, deb, etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
