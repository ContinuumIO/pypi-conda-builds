package:
  name: collective.googleauthentication
  version: "1.0.0"

source:
  fn: collective.googleauthentication-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.googleauthentication/collective.googleauthentication-1.0.0.tar.gz
  md5: b0f94c5dff052157226d2012f148a579
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.googleauthentication = collective.googleauthentication:main
    #
    # Would create an entry point called collective.googleauthentication that calls collective.googleauthentication.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.googleauthentication
    - collective.googleauthentication.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.googleauthentication/
  license: GPL
  summary: 'GA takes care of the user authentication upon Google Docs service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
