package:
  name: django-urls-sugar
  version: "0.1.4"

source:
  fn: django-urls-sugar-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urls-sugar/django-urls-sugar-0.1.4.tar.gz
  md5: f8a7f96d9833b42c681198111be4d2a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urls-sugar = django-urls-sugar:main
    #
    # Would create an entry point called django-urls-sugar that calls django-urls-sugar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - urls_sugar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ahref/django-urls-sugar
  license: BSD License
  summary: 'A library for making complex urls in Django easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
