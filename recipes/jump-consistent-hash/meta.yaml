package:
  name: jump-consistent-hash
  version: "2.0.1"

source:
  fn: jump_consistent_hash-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jump-consistent-hash/jump_consistent_hash-2.0.1.tar.gz
  md5: aca7551af09f10d48ecee14e1558cd65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jump-consistent-hash = jump-consistent-hash:main
    #
    # Would create an entry point called jump-consistent-hash that calls jump-consistent-hash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jump
    - jump.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renstrom/python-jump-consistent-hash
  license: MIT License
  summary: 'Implementation of the Jump Consistent Hash algorithm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
