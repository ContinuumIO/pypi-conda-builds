package:
  name: collective.twitterportlet
  version: "0.10"

source:
  fn: collective.twitterportlet-0.10.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.twitterportlet/collective.twitterportlet-0.10.tar.gz
  md5: fe75c97c68558b3db16e87ea591423dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.twitterportlet = collective.twitterportlet:main
    #
    # Would create an entry point called collective.twitterportlet that calls collective.twitterportlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-twitter
    - simplejson

  run:
    - python
    - setuptools
    - setuptools
    - python-twitter
    - simplejson

test:
  # Python imports
  imports:
    - collective
    - collective.twitterportlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sharkbyte.co.uk
  license: GPL
  summary: 'A Twitter portlet for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
