package:
  name: shanbay
  version: "0.3.3"

source:
  fn: shanbay-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/s/shanbay/shanbay-0.3.3.tar.gz
  md5: ff2c0bc2002b34b7b9707328b2f3bbb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shanbay = shanbay:main
    #
    # Would create an entry point called shanbay that calls shanbay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.1
    - beautifulsoup4
    - html5lib
    - requests-oauthlib

  run:
    - python
    - requests >=2.0.1
    - beautifulsoup4
    - html5lib
    - requests-oauthlib

test:
  # Python imports
  imports:
    - shanbay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozillazg/python-shanbay
  license: MIT License
  summary: 'Python wrapper for shanbay.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
