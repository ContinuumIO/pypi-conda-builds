package:
  name: isotoma.recipe.ploneprefetch
  version: "0.0.8"

source:
  fn: isotoma.recipe.ploneprefetch-0.0.8.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.ploneprefetch/isotoma.recipe.ploneprefetch-0.0.8.zip
  md5: 96753eeb1ae17bb88a0833362c047a00
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.ploneprefetch = isotoma.recipe.ploneprefetch:main
    #
    # Would create an entry point called isotoma.recipe.ploneprefetch that calls isotoma.recipe.ploneprefetch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.ploneprefetch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/isotoma/isotoma.recipe.ploneprefetch
  license: ZPL 2.1
  summary: 'ZC Buildout recipe for installing Plone quickly'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
