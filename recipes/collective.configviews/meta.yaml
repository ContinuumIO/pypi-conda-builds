package:
  name: collective.configviews
  version: "3.0.2"

source:
  fn: collective.configviews-3.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.configviews/collective.configviews-3.0.2.zip
  md5: d05fabbf7d9b4c22c7b229c1645d4982
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.configviews = collective.configviews:main
    #
    # Would create an entry point called collective.configviews that calls collective.configviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.autoform
    - plone.app.registry
    - products.cmfdynamicviewfti >=4.0.4

  run:
    - python
    - setuptools
    - setuptools
    - plone.autoform
    - plone.app.registry
    - products.cmfdynamicviewfti >=4.0.4

test:
  # Python imports
  imports:
    - collective
    - collective.configviews
    - collective.configviews.browser
    - collective.configviews.tests
    - collective.configviews.tests.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.configviews
  license: GNU General Public License (GPL)
  summary: 'Manage configuration of browser views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
