package:
  name: collective.jqhistory
  version: "1.0.1"

source:
  fn: collective.jqhistory-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.jqhistory/collective.jqhistory-1.0.1.tar.gz
  md5: 3a941254d2e8f650ce2fcbc2824caa86
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jqhistory = collective.jqhistory:main
    #
    # Would create an entry point called collective.jqhistory that calls collective.jqhistory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.jqhistory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.jqhistory/
  license: GNU Affero General Public License v3Programming Language :: Python
  summary: 'Packages up the jquery-history plugin for plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
