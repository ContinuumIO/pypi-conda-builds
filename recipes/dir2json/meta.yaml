package:
  name: dir2json
  version: "0.1.0"

source:
  fn: dir2json-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dir2json/dir2json-0.1.0.tar.gz
  md5: be954c53b4bc9f6241b437b71f0601ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dir2json = dir2json:main
    #
    # Would create an entry point called dir2json that calls dir2json.main()

    - dir2json = dir2json:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dir2json

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - dir2json --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'creates a json tree from a directory structure'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
