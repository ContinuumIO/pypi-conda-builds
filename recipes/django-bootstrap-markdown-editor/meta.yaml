package:
  name: django-bootstrap-markdown-editor
  version: "1.1"

source:
  fn: django-bootstrap-markdown-editor-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-markdown-editor/django-bootstrap-markdown-editor-1.1.tar.gz
  md5: 365331b858fc528a27b95db90a957bb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-markdown-editor = django-bootstrap-markdown-editor:main
    #
    # Would create an entry point called django-bootstrap-markdown-editor that calls django-bootstrap-markdown-editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - bootstrap_markdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MSA-Argentina/django-bootstrap-markdown
  license: BSD License
  summary: 'An useful editor wrapped up in bootstrap for your forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
