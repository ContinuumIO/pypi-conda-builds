package:
  name: collective.portlet.workflowsteps
  version: "1.0b3"

source:
  fn: collective.portlet.workflowsteps-1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.workflowsteps/collective.portlet.workflowsteps-1.0b3.tar.gz
  md5: e148f72b57a30a6db8e6e939d160bf2a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.workflowsteps = collective.portlet.workflowsteps:main
    #
    # Would create an entry point called collective.portlet.workflowsteps that calls collective.portlet.workflowsteps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - plone.portlets
    - zope.app.publisher
    - zope.schema
    - zope.formlib
    - plone.app.form
    - products.cmfcore

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - plone.portlets
    - zope.app.publisher
    - zope.schema
    - zope.formlib
    - plone.app.form
    - products.cmfcore

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.workflowsteps
    - collective.portlet.workflowsteps.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi//collective.portlet.workflowsteps
  license: GPL
  summary: "A Plone portlet for showing 'next steps' in a workflow. May include rich text tied to the workflow state and transitions with descriptions."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
