package:
  name: agoraplex.themes.sphinx
  version: "0.1.3"

source:
  fn: agoraplex.themes.sphinx-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/agoraplex.themes.sphinx/agoraplex.themes.sphinx-0.1.3.tar.gz
  md5: 4b85a0382e51b80b706962977249503a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agoraplex.themes.sphinx = agoraplex.themes.sphinx:main
    #
    # Would create an entry point called agoraplex.themes.sphinx that calls agoraplex.themes.sphinx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - sphinx >=1.1

  run:
    - python
    - distribute
    - sphinx >=1.1

test:
  # Python imports
  imports:
    - agoraplex
    - agoraplex.themes
    - agoraplex.themes.sphinx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agoraplex/themes
  license: See LICENSE.rst
  summary: 'A Sphinx theme for Agoraplex projects, based on the Pylons Sphinx theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
