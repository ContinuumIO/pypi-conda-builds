package:
  name: hermes-chat
  version: "0.2.2"

source:
  fn: hermes-chat-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hermes-chat/hermes-chat-0.2.2.tar.gz
  md5: 7a3c6ac22073298a479c17f201ddfe7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hermes-chat = hermes-chat:main
    #
    # Would create an entry point called hermes-chat that calls hermes-chat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmpppy >=0.5.0rc1

  run:
    - python
    - xmpppy >=0.5.0rc1

test:
  # Python imports
  imports:
    - hermes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattlong/hermes
  license: BSD
  summary: 'A simple python-based group chat server built on XMPP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
