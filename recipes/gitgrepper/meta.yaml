package:
  name: gitgrepper
  version: "0.1.0"

source:
  fn: gitgrepper-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gitgrepper/gitgrepper-0.1.0.tar.gz
  md5: 3e390d4d1c738f9f72b2cf20a982978b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitgrepper = gitgrepper:main
    #
    # Would create an entry point called gitgrepper that calls gitgrepper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gitgrepper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zsero/gitgrepper
  license: MIT
  summary: 'A git batch processing and grepping tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
