package:
  name: flask-dashboard
  version: "0.0.6a"

source:
  fn: Flask-Dashboard-0.0.6a.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Dashboard/Flask-Dashboard-0.0.6a.tar.gz
  md5: 7aa6674647b047150f01363d7a0cfed3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dashboard = flask-dashboard:main
    #
    # Would create an entry point called flask-dashboard that calls flask-dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-admin ==1.0.8

  run:
    - python
    - flask-admin ==1.0.8

test:
  # Python imports
  imports:
    - flask_dashboard
    - flask_dashboard.contrib
    - flask_dashboard.contrib.sqla
    - flask_dashboard.form
    - flask_dashboard.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ISC License (ISCL)
  summary: 'Yet another admin interface for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
