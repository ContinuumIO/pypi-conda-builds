package:
  name: hatenagraph
  version: "0.1"

source:
  fn: hatenagraph-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hatenagraph/hatenagraph-0.1.tar.gz
  md5: 7410d5ad45f6bc34119ee93426accd5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hatenagraph = hatenagraph:main
    #
    # Would create an entry point called hatenagraph that calls hatenagraph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - hatenagraph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A python wrapper around the Hatena Graph API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
