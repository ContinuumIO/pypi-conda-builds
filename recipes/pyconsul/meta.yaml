package:
  name: pyconsul
  version: "0.0.3"

source:
  fn: pyconsul-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pyconsul/pyconsul-0.0.3.tar.gz
  md5: d4ed44468035ec0efa6a5b3c85963695
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyconsul = pyconsul:main
    #
    # Would create an entry point called pyconsul that calls pyconsul.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0

  run:
    - python
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - pyconsul

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hackliff/pyconsul
  license: MIT License
  summary: 'Python client for Consul (http://www.consul.io)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
