package:
  name: collective.geo.json
  version: "0.2"

source:
  fn: collective.geo.json-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.geo.json/collective.geo.json-0.2.zip
  md5: d9b6815a9cf8a7ba8fe45e3fa0fc070f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geo.json = collective.geo.json:main
    #
    # Would create an entry point called collective.geo.json that calls collective.geo.json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.geo.geographer
    - collective.geo.settings
    - collective.geo.contentlocations
    - geojson
    - pygeoif
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - collective.geo.geographer
    - collective.geo.settings
    - collective.geo.contentlocations
    - geojson
    - pygeoif
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.geo
    - collective.geo.json
    - collective.geo.json.browser
    - collective.geo.json.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.geo.json
  license: GPL
  summary: 'Collective Geo GeoJson output'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
