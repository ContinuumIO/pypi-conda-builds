package:
  name: django-save-form
  version: "0.1.3"

source:
  fn: django-save-form-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-save-form/django-save-form-0.1.3.tar.gz
  md5: 087beda42730737fb8e9513008e3f6a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-save-form = django-save-form:main
    #
    # Would create an entry point called django-save-form that calls django-save-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/defcube/django-save-form/
  license: UNKNOWN
  summary: 'Encapsulate the common workflow of saving a form if submitted, displaying it if not.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
