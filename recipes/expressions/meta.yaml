package:
  name: expressions
  version: "0.2.2"

source:
  fn: expressions-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/e/expressions/expressions-0.2.2.tar.gz
  md5: 3d872bcdafcd737851e4a286fbe4cd65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expressions = expressions:main
    #
    # Would create an entry point called expressions that calls expressions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - expressions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://databrewery.org
  license: MIT License
  summary: 'Extensible arithmetic expression parser and compiler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
