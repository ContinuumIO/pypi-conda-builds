package:
  name: botalert
  version: "0.5a"

source:
  fn: botalert-0.5a.zip
  url: https://pypi.python.org/packages/source/b/botalert/botalert-0.5a.zip
  md5: ae4f2296608d709c7e405d11235aecd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - botalert = botalert:main
    #
    # Would create an entry point called botalert that calls botalert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - botalert

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License version 2.0
  summary: 'parse logged dropped outgoing packets in order to detect bot infected machines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
