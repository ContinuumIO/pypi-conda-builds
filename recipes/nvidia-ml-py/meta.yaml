package:
  name: nvidia-ml-py
  version: "7.346.0"

source:
  fn: nvidia-ml-py-7.346.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nvidia-ml-py/nvidia-ml-py-7.346.0.tar.gz
  md5: 41716a93b8cff713e948e1dcb685a1e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nvidia-ml-py = nvidia-ml-py:main
    #
    # Would create an entry point called nvidia-ml-py that calls nvidia-ml-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nvidia.com/
  license: BSD License
  summary: 'Python Bindings for the NVIDIA Management Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
