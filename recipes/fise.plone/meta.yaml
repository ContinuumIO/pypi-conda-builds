package:
  name: fise.plone
  version: "0.3"

source:
  fn: fise.plone-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fise.plone/fise.plone-0.3.tar.gz
  md5: 7197e1f10ddc8bebb99e296da66c3c75
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fise.plone = fise.plone:main
    #
    # Would create an entry point called fise.plone that calls fise.plone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - fise.client
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - fise.client
    - plone

test:
  # Python imports
  imports:
    - fise
    - fise.plone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Stanbol FISE Semantic Engine: RESTful Plone Integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
