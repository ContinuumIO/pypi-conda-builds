package:
  name: collective.themecustomizer
  version: "1.0b1"

source:
  fn: collective.themecustomizer-1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.themecustomizer/collective.themecustomizer-1.0b1.zip
  md5: b9249b0cb1d2987d0b6db910bba30147
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.themecustomizer = collective.themecustomizer:main
    #
    # Would create an entry point called collective.themecustomizer that calls collective.themecustomizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.api
    - plone.app.controlpanel
    - plone.app.layout
    - plone.app.registry
    - plone.i18n
    - products.cmfplone >=4.2
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - plone.api
    - plone.app.controlpanel
    - plone.app.layout
    - plone.app.registry
    - plone.i18n
    - products.cmfplone >=4.2
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.themecustomizer
    - collective.themecustomizer.Extensions
    - collective.themecustomizer.browser
    - collective.themecustomizer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.themecustomizer
  license: GNU General Public License v2 (GPLv2)
  summary: 'Theme customizer for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
