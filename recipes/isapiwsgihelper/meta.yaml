package:
  name: isapiwsgihelper
  version: "0.1.3"

source:
  fn: ISAPIWSGIHelper-0.1.3.zip
  url: https://pypi.python.org/packages/source/I/ISAPIWSGIHelper/ISAPIWSGIHelper-0.1.3.zip
  md5: 2df53166dbd4602ad914f2a2a3ffedd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isapiwsgihelper = isapiwsgihelper:main
    #
    # Would create an entry point called isapiwsgihelper that calls isapiwsgihelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - isapi_wsgi

  run:
    - python
    - isapi_wsgi

test:
  # Python imports
  imports:
    - isapiwsgihelper
    - iwhelper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/rsyring/isapi-wsgi-helper/
  license: BSD License
  summary: 'utilities to help bootstrap deployment of WSGI applications using isapi-wsgi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
