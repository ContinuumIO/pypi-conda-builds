package:
  name: flufl.password
  version: "1.3"

source:
  fn: flufl.password-1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flufl.password/flufl.password-1.3.tar.gz
  md5: d0132fddbb0e7d7bf0cf05cdb7294d6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flufl.password = flufl.password:main
    #
    # Would create an entry point called flufl.password that calls flufl.password.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flufl
    - flufl.password
    - flufl.password.docs
    - flufl.password.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/flufl.password
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Password hashing and verification.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
