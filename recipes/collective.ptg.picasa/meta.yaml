package:
  name: collective.ptg.picasa
  version: "0.1.1"

source:
  fn: collective.ptg.picasa-0.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.picasa/collective.ptg.picasa-0.1.1.zip
  md5: 415be75b18882d04751edc67d55e6220
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.picasa = collective.ptg.picasa:main
    #
    # Would create an entry point called collective.ptg.picasa that calls collective.ptg.picasa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - gdata
    - plone.app.z3cform
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - gdata
    - plone.app.z3cform
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.picasa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plone.org/products/plone-true-gallery
  license: GPL
  summary: 'Add on collective.plonetruegallery to aggregate from picasa'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
