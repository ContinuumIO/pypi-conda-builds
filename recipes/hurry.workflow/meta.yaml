package:
  name: hurry.workflow
  version: "0.13.1"

source:
  fn: hurry.workflow-0.13.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.workflow/hurry.workflow-0.13.1.tar.gz
  md5: 6320059d03b50d7df8597ee3c41c9c63
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.workflow = hurry.workflow:main
    #
    # Would create an entry point called hurry.workflow that calls hurry.workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zodb3
    - zope.event
    - zope.security
    - zope.annotation
    - zope.lifecycleevent

  run:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zodb3
    - zope.event
    - zope.security
    - zope.annotation
    - zope.lifecycleevent

test:
  # Python imports
  imports:
    - hurry
    - hurry.workflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'hurry.workflow is a simple workflow system. It can be used to\nimplement stateful multi-version workflows for Zope Toolkit applications.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
