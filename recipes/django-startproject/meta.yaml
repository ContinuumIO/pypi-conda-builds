package:
  name: django-startproject
  version: "1.0a"

source:
  fn: django-startproject-1.0a.tar.gz
  url: https://pypi.python.org/packages/source/d/django-startproject/django-startproject-1.0a.tar.gz
  md5: 5ec4213cae623178ac57c227459301a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-startproject = django-startproject:main
    #
    # Would create an entry point called django-startproject that calls django-startproject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_startproject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Create a Django project layout based on Lincoln Loop best practices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
