package:
  name: sphinxcontrib-visio
  version: "1.1.0"

source:
  fn: sphinxcontrib-visio-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-visio/sphinxcontrib-visio-1.1.0.tar.gz
  md5: c2de05acf97449716ed39180ffcd268d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-visio = sphinxcontrib-visio:main
    #
    # Would create an entry point called sphinxcontrib-visio that calls sphinxcontrib-visio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.0.0
    - visio2img >=1.2.0

  run:
    - python
    - sphinx >=1.0.0
    - visio2img >=1.2.0

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/visio2img/sphinxcontrib-visio
  license: Apache Software License
  summary: 'Sphinx "visio" extension; embed MS-Visio file (.vsd, .vsdx)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
