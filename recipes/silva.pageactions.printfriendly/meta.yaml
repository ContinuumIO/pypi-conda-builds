package:
  name: silva.pageactions.printfriendly
  version: "1.0.2"

source:
  fn: silva.pageactions.printfriendly-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.pageactions.printfriendly/silva.pageactions.printfriendly-1.0.2.tar.gz
  md5: 56c19e55d1c305b7c80fbde44d638fb1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.pageactions.printfriendly = silva.pageactions.printfriendly:main
    #
    # Would create an entry point called silva.pageactions.printfriendly that calls silva.pageactions.printfriendly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - silva.core.views >2.0.999,<2.1.999
    - silva.pageactions.base
    - five.grok
    - megrok.chameleon

  run:
    - python
    - setuptools
    - silva.core.views >2.0.999,<2.1.999
    - silva.pageactions.base
    - five.grok
    - megrok.chameleon

test:
  # Python imports
  imports:
    - silva
    - silva.pageactions
    - silva.pageactions.printfriendly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.pageactions.printfriendly
  license: BSD
  summary: 'Display a simple printable version of the content in Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
