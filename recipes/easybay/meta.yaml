package:
  name: easybay
  version: "0.1dev"

source:
  fn: easyBay-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/e/easyBay/easyBay-0.1dev.tar.gz
  md5: fa0ea6b6a35f54f15f0766d99f4b6cb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easybay = easybay:main
    #
    # Would create an entry point called easybay that calls easybay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - easyBay
    - easybay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://exogen.case.edu/easyBay/
  license: PSF
  summary: 'A simple way to talk to eBay (manage listings, gather data) with Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
