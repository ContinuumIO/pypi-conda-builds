package:
  name: collective.onlogin
  version: "1.0"

source:
  fn: collective.onlogin-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.onlogin/collective.onlogin-1.0.tar.gz
  md5: ac4f425fdc2cfd7c80eefdacec58766e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.onlogin = collective.onlogin:main
    #
    # Would create an entry point called collective.onlogin that calls collective.onlogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.onlogin
    - collective.onlogin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martinschoel/collective.onlogin
  license: GPL
  summary: 'This package allows the configuration of HTTP redirects after user logged in into Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
