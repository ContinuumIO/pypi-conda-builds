package:
  name: assembla
  version: "2.8.0"

source:
  fn: assembla-2.8.0.tar.gz
  url: https://pypi.python.org/packages/source/a/assembla/assembla-2.8.0.tar.gz
  md5: 8f6c1c46bf764714f1d224868017810a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - assembla = assembla:main
    #
    # Would create an entry point called assembla that calls assembla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - assembla
    - assembla.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/markfinger/assembla/
  license: MIT License
  summary: 'Python wrapper for the Assembla API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
