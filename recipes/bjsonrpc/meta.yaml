package:
  name: bjsonrpc
  version: "0.2.1"

source:
  fn: bjsonrpc-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bjsonrpc/bjsonrpc-0.2.1.tar.gz
  md5: 6ca5c37dbe01de5172f86e5dc3c3754c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bjsonrpc = bjsonrpc:main
    #
    # Would create an entry point called bjsonrpc that calls bjsonrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bjsonrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bjsonrpc.wordpress.com/
  license: BSD License
  summary: 'Asynchronous Bidirectional JSON-RPC protocol implementation over TCP/IP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
