package:
  name: django-br-addresses
  version: "0.1.1"

source:
  fn: django-br-addresses-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-br-addresses/django-br-addresses-0.1.1.tar.gz
  md5: 11142c6461622adf00cb83a52e87b3cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-br-addresses = django-br-addresses:main
    #
    # Would create an entry point called django-br-addresses that calls django-br-addresses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-uuslug
    - django-localflavor

  run:
    - python
    - django-uuslug
    - django-localflavor

test:
  # Python imports
  imports:
    - br_addresses
    - br_addresses.migrations
    - br_addresses.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A simple Django app to provide a CRUD of cities and address.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
