package:
  name: ejtp
  version: "0.9.7p1"

source:
  fn: ejtp-0.9.7p1.tar.gz
  url: https://pypi.python.org/packages/source/e/ejtp/ejtp-0.9.7p1.tar.gz
  md5: a02281fa307d6b29934873cc63eb4f65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ejtp = ejtp:main
    #
    # Would create an entry point called ejtp that calls ejtp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto
    - persei
    - requests
    - streql

  run:
    - python
    - pycrypto
    - persei
    - requests
    - streql

test:
  # Python imports
  imports:
    - ejtp
    - ejtp.applications
    - ejtp.applications.ejforward
    - ejtp.crypto
    - ejtp.frame
    - ejtp.identity
    - ejtp.jacks
    - ejtp.tests
    - ejtp.util
    - ejtp.vendor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/campadrenalin/EJTP-lib-python/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Encrypted JSON Transport Protocol library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
