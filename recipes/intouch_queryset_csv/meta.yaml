package:
  name: intouch_queryset_csv
  version: "0.1.1"

source:
  fn: intouch_queryset_csv-0.1.1.zip
  url: https://pypi.python.org/packages/source/i/intouch_queryset_csv/intouch_queryset_csv-0.1.1.zip
  md5: cd3c0171176162f0fe4b1266abb54b6b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intouch_queryset_csv = intouch_queryset_csv:main
    #
    # Would create an entry point called intouch_queryset_csv that calls intouch_queryset_csv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - intouch
    - intouch.queryset_csv
    - intouch_queryset_csv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/intouchltd/queryset-csv
  license: MIT License
  summary: 'Module to export django querysets as csv files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
