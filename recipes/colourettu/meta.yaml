package:
  name: colourettu
  version: "1.0.0"

source:
  fn: colourettu-1.0.0.zip
  url: https://pypi.python.org/packages/source/c/colourettu/colourettu-1.0.0.zip
  md5: c45aef121376a8f657c1ca2326d50082
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colourettu = colourettu:main
    #
    # Would create an entry point called colourettu that calls colourettu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colourettu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/minchinweb/colourettu
  license: MIT License
  summary: 'Colour related functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
