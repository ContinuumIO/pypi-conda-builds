package:
  name: hoedown
  version: "0.2"

source:
  fn: hoedown-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hoedown/hoedown-0.2.tar.gz
  md5: 76f56e222a52e0b575bba5274cd6bddf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hoedown = hoedown:main
    #
    # Would create an entry point called hoedown that calls hoedown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hoedown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hhatto/python-hoedown
  license: MIT License
  summary: 'The Python binding for Hoedown, a markdown parsing library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
