package:
  name: collective.js.nomensamediaplayer
  version: "1.0"

source:
  fn: collective.js.nomensamediaplayer-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.js.nomensamediaplayer/collective.js.nomensamediaplayer-1.0.zip
  md5: 9bd2628df2f581aa74f5e8620b229d1b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.nomensamediaplayer = collective.js.nomensamediaplayer:main
    #
    # Would create an entry point called collective.js.nomensamediaplayer that calls collective.js.nomensamediaplayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.swfobject
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - collective.swfobject
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.nomensamediaplayer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.nomensamediaplayer
  license: GPL
  summary: "Nomensa's Accessible Media Player registration in Plone resources registries"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
