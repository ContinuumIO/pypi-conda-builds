package:
  name: flask-sockets
  version: "0.1"

source:
  fn: Flask-Sockets-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Sockets/Flask-Sockets-0.1.tar.gz
  md5: 97045adc408b87ff9ad51cf5fdeb6724
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sockets = flask-sockets:main
    #
    # Would create an entry point called flask-sockets that calls flask-sockets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - gevent
    - gevent-websocket

  run:
    - python
    - flask
    - gevent
    - gevent-websocket

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennethreitz/flask-sockets
  license: BSD License
  summary: 'Elegant WebSockets for your Flask apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
