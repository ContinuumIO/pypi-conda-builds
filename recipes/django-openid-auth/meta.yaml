package:
  name: django-openid-auth
  version: "0.5"

source:
  fn: django-openid-auth-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-openid-auth/django-openid-auth-0.5.tar.gz
  md5: 75277ad166be391fbc4ca87666bf64d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-openid-auth = django-openid-auth:main
    #
    # Would create an entry point called django-openid-auth that calls django-openid-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_openid_auth
    - django_openid_auth.management
    - django_openid_auth.management.commands
    - django_openid_auth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-openid-auth
  license: BSD License
  summary: 'OpenID integration for django.contrib.auth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
