package:
  name: cherrypy-wsgiserver
  version: "0.1dev"

source:
  fn: cherrypy-wsgiserver-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/c/cherrypy-wsgiserver/cherrypy-wsgiserver-0.1dev.tar.gz
  md5: 27833fe08733941269d22fee07c36f5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherrypy-wsgiserver = cherrypy-wsgiserver:main
    #
    # Would create an entry point called cherrypy-wsgiserver that calls cherrypy-wsgiserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - wsgiserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/od-eon/cherrypy-wsgiserver
  license: LICENSE
  summary: 'Wsgiserver configured for cherrypy and django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
