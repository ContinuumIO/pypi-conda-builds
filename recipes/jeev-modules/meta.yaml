package:
  name: jeev-modules
  version: "0.0.2"

source:
  fn: jeev-modules-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jeev-modules/jeev-modules-0.0.2.tar.gz
  md5: 160f3ef21059b7f317cbe73fdc8760de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jeev-modules = jeev-modules:main
    #
    # Would create an entry point called jeev-modules that calls jeev-modules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jeev

  run:
    - python
    - jeev

test:
  # Python imports
  imports:
    - jeev_modules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jhgg/jeev-modules
  license: MIT License or MIT License
  summary: 'Modules for Jeev'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
