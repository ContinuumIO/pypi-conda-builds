package:
  name: asynx
  version: "0.4dev4"

source:
  fn: asynx-0.4dev4.tar.gz
  url: https://pypi.python.org/packages/source/a/asynx/asynx-0.4dev4.tar.gz
  md5: f17f8d5707ac4326d2da48397d9c93e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asynx = asynx:main
    #
    # Would create an entry point called asynx that calls asynx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=2.2
    - anyjson >=0.3.3
    - requests >=2.3.0

  run:
    - python
    - python-dateutil >=2.2
    - anyjson >=0.3.3
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - asynx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guokr/asynx
  license: MIT License
  summary: 'Python SDK for an open source, distributed, and web / HTTP oriented taskqueue & scheduler service inspired by Google App Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
