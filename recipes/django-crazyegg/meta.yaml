package:
  name: django-crazyegg
  version: "2.1.1"

source:
  fn: django-crazyegg-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crazyegg/django-crazyegg-2.1.1.tar.gz
  md5: f16f507b03b57669e43ab35d977c7cf2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crazyegg = django-crazyegg:main
    #
    # Would create an entry point called django-crazyegg that calls django-crazyegg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_crazyegg
    - django_crazyegg.templatetags
    - django_crazyegg.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jcassee/django-crazyegg
  license: MIT License
  summary: 'Crazy Egg heatmaps for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
