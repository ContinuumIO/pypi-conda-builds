package:
  name: django-lithium-api
  version: "0.1.5"

source:
  fn: django-lithium-api-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lithium-api/django-lithium-api-0.1.5.tar.gz
  md5: 7607c60e2f0a2e04b70ecbe6aff1c0ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lithium-api = django-lithium-api:main
    #
    # Would create an entry point called django-lithium-api that calls django-lithium-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - requests
    - python-dateutil ==1.5
    - lxml

  run:
    - python
    - django >=1.3
    - requests
    - python-dateutil ==1.5
    - lxml

test:
  # Python imports
  imports:
    - django_lithium_api
    - django_lithium_api.management
    - django_lithium_api.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piquadrat/django-lithium-api
  license: BSD License
  summary: 'A (very) limited implementation of the Lithium REST API for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
