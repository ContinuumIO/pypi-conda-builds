package:
  name: blockdiagcontrib-eps
  version: "0.1.1"

source:
  fn: blockdiagcontrib-eps-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-eps/blockdiagcontrib-eps-0.1.1.tar.gz
  md5: f520d55e835098ad12b9c12f04f4dadf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-eps = blockdiagcontrib-eps:main
    #
    # Would create an entry point called blockdiagcontrib-eps that calls blockdiagcontrib-eps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.4.0
    - setuptools

  run:
    - python
    - setuptools
    - blockdiag >=1.4.0
    - setuptools

test:
  # Python imports
  imports:
    - blockdiagcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tk0miya/blockdiagcontrib
  license: Python Software Foundation License
  summary: 'EPS imagedrawer plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
