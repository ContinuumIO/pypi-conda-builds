package:
  name: csp_eventlet
  version: "0.7.0"

source:
  fn: csp_eventlet-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/c/csp_eventlet/csp_eventlet-0.7.0.tar.gz
  md5: 2b4f362126a54ad662e07b3ccd2badb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csp_eventlet = csp_eventlet:main
    #
    # Would create an entry point called csp_eventlet that calls csp_eventlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eventlet

  run:
    - python
    - eventlet

test:
  # Python imports
  imports:
    - csp_eventlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mcarter/csp_eventlet
  license: MIT License
  summary: 'An implemention of the Comet Session protocol specification for eventlet: http://orbited.org/blog/files/cps.html'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
