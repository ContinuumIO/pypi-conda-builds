package:
  name: jptokenpreprocessing
  version: "0.1.5a"

source:
  fn: JpTokenPreprocessing-0.1.5a.tar.gz
  url: https://pypi.python.org/packages/source/J/JpTokenPreprocessing/JpTokenPreprocessing-0.1.5a.tar.gz
  md5: 27f5c7fa149cc6ade68d612d65a9a88b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jptokenpreprocessing = jptokenpreprocessing:main
    #
    # Would create an entry point called jptokenpreprocessing that calls jptokenpreprocessing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jp_token_preprocessing
    - jptokenpreprocessing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kesin11/JpTokenPreprocessing
  license: MIT License
  summary: 'JpTokenPreprocessing is Python library for token preprocessing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
