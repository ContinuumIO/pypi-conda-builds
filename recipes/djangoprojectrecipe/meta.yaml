package:
  name: djangoprojectrecipe
  version: "1.1.2"

source:
  fn: djangoprojectrecipe-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangoprojectrecipe/djangoprojectrecipe-1.1.2.tar.gz
  md5: e6d1365bce6b313d4bb03568d0aeb396
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoprojectrecipe = djangoprojectrecipe:main
    #
    # Would create an entry point called djangoprojectrecipe that calls djangoprojectrecipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - djangoprojectrecipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stefanfoulis/djangoprojectrecipe
  license: BSD License
  summary: 'simple buildout recipe for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
