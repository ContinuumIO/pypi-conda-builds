package:
  name: json262
  version: "0.2.0"

source:
  fn: json262-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/j/json262/json262-0.2.0.tar.gz
  md5: 0977a0085c961a822b3d58889cb04e1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json262 = json262:main
    #
    # Would create an entry point called json262 that calls json262.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - json262

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/audreyr/json262
  license: BSD License
  summary: 'JSON encoder fully compliant with ECMA-262 specification.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
