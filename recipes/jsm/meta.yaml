package:
  name: jsm
  version: "0.18"

source:
  fn: jsm-0.18.tar.gz
  url: https://pypi.python.org/packages/source/j/jsm/jsm-0.18.tar.gz
  md5: e7fda9181856d616695aab4db521d631
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsm = jsm:main
    #
    # Would create an entry point called jsm that calls jsm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - html5lib
    - requests
    - six

  run:
    - python
    - beautifulsoup4
    - html5lib
    - requests
    - six

test:
  # Python imports
  imports:
    - jsm
    - jsm.sbi
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/utahta/jsm
  license: MIT License
  summary: 'Get the japanese stock market data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
