package:
  name: django-tmdb
  version: "0.1"

source:
  fn: django-tmdb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tmdb/django-tmdb-0.1.tar.gz
  md5: 95e35e2ac4b97727ff56042c60162110
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tmdb = django-tmdb:main
    #
    # Would create an entry point called django-tmdb that calls django-tmdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tmdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Merenon/django-tmdb
  license: BEER-WARE License
  summary: 'A small TMDB (themoviedatabase) API v3 wrapper for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
