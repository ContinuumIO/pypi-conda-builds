package:
  name: cherrypy-cgiserver
  version: "0.3.5"

source:
  fn: cherrypy-cgiserver-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cherrypy-cgiserver/cherrypy-cgiserver-0.3.5.tar.gz
  md5: 18883d3ffc8ad6bd46522dd0521de43a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherrypy-cgiserver = cherrypy-cgiserver:main
    #
    # Would create an entry point called cherrypy-cgiserver that calls cherrypy-cgiserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cpcgiserver
    - cpcgiserver.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gerold-penz.github.com/cherrypy-cgiserver/
  license: GNU Library or Lesser General Public License (LGPL) or MIT License
  summary: 'Python CGI Server - Perfect To Deliver PHP Files Within A Python CherryPy Application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
