package:
  name: flask-nap
  version: "0.0.1"

source:
  fn: flask-nap-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-nap/flask-nap-0.0.1.tar.gz
  md5: 76c3410306cb429d6b27accb765b610a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-nap = flask-nap:main
    #
    # Would create an entry point called flask-nap that calls flask-nap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9.7

  run:
    - python
    - sqlalchemy >=0.9.7

test:
  # Python imports
  imports:
    - nap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aergener/nap
  license: MIT
  summary: 'Flask REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
