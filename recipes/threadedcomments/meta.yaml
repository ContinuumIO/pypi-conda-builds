package:
  name: threadedcomments
  version: "0.5"

source:
  fn: threadedcomments-0.5.tar.gz
  url: https://pypi.python.org/packages/source/t/threadedcomments/threadedcomments-0.5.tar.gz
  md5: 559fb5bec19a3b181f9f919a0320e975
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - threadedcomments = threadedcomments:main
    #
    # Would create an entry point called threadedcomments that calls threadedcomments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - threadedcomments
    - threadedcomments.management
    - threadedcomments.management.commands
    - threadedcomments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-threadedcomments/
  license: BSD License
  summary: 'A simple yet flexible threaded commenting system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
