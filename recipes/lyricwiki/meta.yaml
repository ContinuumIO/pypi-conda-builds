package:
  name: lyricwiki
  version: "0.1.35"

source:
  fn: lyricwiki-0.1.35.tar.gz
  url: https://pypi.python.org/packages/source/l/lyricwiki/lyricwiki-0.1.35.tar.gz
  md5: 8729749836e74b125c91e3e8645b0b7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lyricwiki = lyricwiki:main
    #
    # Would create an entry point called lyricwiki that calls lyricwiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lyricwiki

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: gpl3
  summary: 'python-lyricwiki provides simple access to lyrics from http://lyrics.wikia.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
