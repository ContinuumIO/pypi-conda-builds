package:
  name: omdb
  version: "0.4.0"

source:
  fn: omdb-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/o/omdb/omdb-0.4.0.tar.gz
  md5: e9ba4c9682acda66319a287cc311d8e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omdb = omdb:main
    #
    # Would create an entry point called omdb that calls omdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.1

  run:
    - python
    - requests >=2.0.1

test:
  # Python imports
  imports:
    - omdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dgilland/omdb.py
  license: MIT License
  summary: 'Python wrapper for OMDb API: http://www.omdbapi.com/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
