package:
  name: django-contentadmin
  version: "0.2.0"

source:
  fn: django-contentadmin-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-contentadmin/django-contentadmin-0.2.0.tar.gz
  md5: 15ea12a79d4e83b9f3f8b6b9aff6769b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contentadmin = django-contentadmin:main
    #
    # Would create an entry point called django-contentadmin that calls django-contentadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-form-utils

  run:
    - python
    - django-form-utils

test:
  # Python imports
  imports:
    - contentadmin
    - contentadmin.migrations
    - contentadmin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/surfeurx/django-contentadmin
  license: UNKNOWN
  summary: 'Templatetags that let you modify text and image directly in the admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
