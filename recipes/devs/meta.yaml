package:
  name: devs
  version: "0.1.8"

source:
  fn: devs-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/devs/devs-0.1.8.tar.gz
  md5: d21eca85cd66e2687a65f675927ec8e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - devs = devs:main
    #
    # Would create an entry point called devs that calls devs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - future

  run:
    - python
    - future

test:
  # Python imports
  imports:
    - devs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/andsor/pydevs
  license: Apache Software License
  summary: 'A Python wrapper of adevs, a C++ library implementing the Discrete Event System Specification (DEVS)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
