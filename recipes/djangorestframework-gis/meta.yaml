package:
  name: djangorestframework-gis
  version: "0.8.2"

source:
  fn: djangorestframework-gis-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-gis/djangorestframework-gis-0.8.2.tar.gz
  md5: 02d482df3354bb76619235451c27e1c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-gis = djangorestframework-gis:main
    #
    # Would create an entry point called djangorestframework-gis that calls djangorestframework-gis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework >=3.0.4,<=3.1.1

  run:
    - python
    - djangorestframework >=3.0.4,<=3.1.1

test:
  # Python imports
  imports:
    - rest_framework_gis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djangonauts/django-rest-framework-gis
  license: BSD License
  summary: 'Geographic add-ons for Django Rest Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
