package:
  name: django-routers
  version: "0.2"

source:
  fn: django-routers-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-routers/django-routers-0.2.tar.gz
  md5: 7dd0d9fa274f827773b7dcd217fd6d83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-routers = django-routers:main
    #
    # Would create an entry point called django-routers that calls django-routers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - routers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/avelino/django-routers/
  license: BSD License
  summary: u'Django multiple databases, auto routers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
