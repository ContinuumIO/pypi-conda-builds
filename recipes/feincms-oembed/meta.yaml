package:
  name: feincms-oembed
  version: "1.3.1"

source:
  fn: feincms-oembed-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/f/feincms-oembed/feincms-oembed-1.3.1.tar.gz
  md5: 03820e3714fa847e5e0f1ab04abc4a55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-oembed = feincms-oembed:main
    #
    # Would create an entry point called feincms-oembed that calls feincms-oembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - feincms_oembed
    - feincms_oembed.migrations
    - feincms_oembed.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/feincms/feincms-oembed/
  license: BSD License
  summary: 'OEmbed anything.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
