package:
  name: collective.emaillogin
  version: "1.3"

source:
  fn: collective.emaillogin-1.3.zip
  url: https://pypi.python.org/packages/source/c/collective.emaillogin/collective.emaillogin-1.3.zip
  md5: 673db85436fce4556c9a87679f71c963
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.emaillogin = collective.emaillogin:main
    #
    # Would create an entry point called collective.emaillogin that calls collective.emaillogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.emaillogin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.emaillogin
  license: GPL
  summary: 'Allow logins with email address rather than login name.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
