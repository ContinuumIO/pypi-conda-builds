package:
  name: sorl-defined-thumbnail
  version: "0.0.1"

source:
  fn: master.zip
  url: https://github.com/jjdelc/sorl-defined-thumbnails/archive/master.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sorl-defined-thumbnail = sorl-defined-thumbnail:main
    #
    # Would create an entry point called sorl-defined-thumbnail that calls sorl-defined-thumbnail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - defined_thumbnails
    - defined_thumbnails.management
    - defined_thumbnails.management.commands
    - defined_thumbnails.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jjdelc/sorl-defined-thumbnails
  license: BSD License
  summary: 'A sorl-thumbnail extension to restrict the thumbnailgeneration to a pre defined set of values'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
