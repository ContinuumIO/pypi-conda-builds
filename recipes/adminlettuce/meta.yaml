package:
  name: adminlettuce
  version: "0.0.6"

source:
  fn: adminlettuce-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/adminlettuce/adminlettuce-0.0.6.tar.gz
  md5: cce40fb18e9c0d5b190d942488309b6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adminlettuce = adminlettuce:main
    #
    # Would create an entry point called adminlettuce that calls adminlettuce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - adminlettuce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ff0000.com/
  license: MIT License
  summary: 'Displays lettuce features as documentation in Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
