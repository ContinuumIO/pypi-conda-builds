package:
  name: bungee
  version: "0.1"

source:
  fn: bungee-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bungee/bungee-0.1.tar.gz
  md5: a979fd5236e4a70af5bc10daa2c1bae4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bungee = bungee:main
    #
    # Would create an entry point called bungee that calls bungee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyelasticsearch >=0.5

  run:
    - python
    - pyelasticsearch >=0.5

test:
  # Python imports
  imports:
    - bungee
    - bungee.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wan/bungee
  license: BSD License
  summary: 'Python models and query expressions for ElasticSearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
