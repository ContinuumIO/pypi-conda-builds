package:
  name: json_schema_generator
  version: "0.3"

source:
  fn: json_schema_generator-0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/json_schema_generator/json_schema_generator-0.3.tar.gz
  md5: 8a22268db6dfe68223479c886c4e0845
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_schema_generator = json_schema_generator:main
    #
    # Would create an entry point called json_schema_generator that calls json_schema_generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema ==0.8.0

  run:
    - python
    - jsonschema ==0.8.0

test:
  # Python imports
  imports:
    - json_schema_generator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/json_schema_generator/
  license: GNU General Public License v3 (GPLv3)
  summary: 'A simple json schema generator based on json resource with auto validation tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
