package:
  name: django-forms-ajaxified
  version: "0.1.3"

source:
  fn: django-forms-ajaxified-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-forms-ajaxified/django-forms-ajaxified-0.1.3.tar.gz
  md5: 047aaf27a248a6104ba52e7c90f03188
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-forms-ajaxified = django-forms-ajaxified:main
    #
    # Would create an entry point called django-forms-ajaxified that calls django-forms-ajaxified.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-libs

  run:
    - python
    - django
    - django-libs

test:
  # Python imports
  imports:
    - forms_ajaxified
    - forms_ajaxified.templatetags
    - forms_ajaxified.tests
    - forms_ajaxified.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-forms-ajaxified
  license: The MIT License
  summary: 'A reusable Django app that allows to load and submit forms via AJAX.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
