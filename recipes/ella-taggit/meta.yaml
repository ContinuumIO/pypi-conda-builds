package:
  name: ella-taggit
  version: "1.0.8"

source:
  fn: ella-taggit-1.0.8.tar.gz
  url: https://pypi.python.org/packages/source/e/ella-taggit/ella-taggit-1.0.8.tar.gz
  md5: 353e9c7654bf94892382e2b08c86b3ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ella-taggit = ella-taggit:main
    #
    # Would create an entry point called ella-taggit that calls ella-taggit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - ella >=3.0.0

  run:
    - python
    - setuptools >=0.6b1
    - ella >=3.0.0

test:
  # Python imports
  imports:
    - ella_taggit
    - ella_taggit.management
    - ella_taggit.management.commands
    - ella_taggit.migrations
    - ella_taggit.templatetags
    - test_ella_taggit.settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ella/ella-taggit
  license: BSD License
  summary: 'django-taggit wrapper for Ella CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
