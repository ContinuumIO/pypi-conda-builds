package:
  name: cleanup_zope_persistent_registry
  version: "0.1.1"

source:
  fn: cleanup_zope_persistent_registry-0.1.1.zip
  url: https://pypi.python.org/packages/source/c/cleanup_zope_persistent_registry/cleanup_zope_persistent_registry-0.1.1.zip
  md5: 8dacdeef5514576c397be4043fcd6757
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cleanup_zope_persistent_registry = cleanup_zope_persistent_registry:main
    #
    # Would create an entry point called cleanup_zope_persistent_registry that calls cleanup_zope_persistent_registry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cleanup_zope_persistent_registry
    - cleanup_zope_persistent_registry.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Cleanup of keys leftover in zope component registries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
