package:
  name: django-exportdata
  version: "0.2"

source:
  fn: django-exportdata-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-exportdata/django-exportdata-0.2.tar.gz
  md5: 972bda0921eb99f1db307c44bc6db775
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-exportdata = django-exportdata:main
    #
    # Would create an entry point called django-exportdata that calls django-exportdata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - exportdata
    - exportdata.management
    - exportdata.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saippuakauppias/django-exportdata
  license: ISC License (ISCL)
  summary: 'Export model data (with selected fields) to csv file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
