package:
  name: django-sproutcore
  version: "0.7.3"

source:
  fn: django-sproutcore-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sproutcore/django-sproutcore-0.7.3.tar.gz
  md5: 3ddc4e3f8eadde0383afe36afc1a74fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sproutcore = django-sproutcore:main
    #
    # Would create an entry point called django-sproutcore that calls django-sproutcore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocore
    - polls
    - sproutcore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/erichocean/django-sproutcore/tree/master
  license: BSD License
  summary: 'A package to help automate creation of sproutcore apps for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
