package:
  name: mechanicalsoup
  version: "0.3.1"

source:
  fn: MechanicalSoup-0.3.1.zip
  url: https://pypi.python.org/packages/source/M/MechanicalSoup/MechanicalSoup-0.3.1.zip
  md5: b60b0f0f1bc5eb3652ea5f0c6b7d24c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mechanicalsoup = mechanicalsoup:main
    #
    # Would create an entry point called mechanicalsoup that calls mechanicalsoup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0
    - beautifulsoup4
    - six >=1.4

  run:
    - python
    - requests >=2.0
    - beautifulsoup4
    - six >=1.4

test:
  # Python imports
  imports:
    - mechanicalsoup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hickford/MechanicalSoup
  license: MIT License
  summary: 'A Python library for automating interaction with websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
