package:
  name: collective.contentrules.talesaction
  version: "1.0"

source:
  fn: collective.contentrules.talesaction-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.contentrules.talesaction/collective.contentrules.talesaction-1.0.zip
  md5: ac7dd38ea302635420e92de69d38a2a6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.talesaction = collective.contentrules.talesaction:main
    #
    # Would create an entry point called collective.contentrules.talesaction that calls collective.contentrules.talesaction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.contentrules

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.contentrules

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.talesaction

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'An add-on to add TALES expression actions in your content rules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
