package:
  name: eke.review
  version: "1.0.4"

source:
  fn: eke.review-1.0.4.zip
  url: https://pypi.python.org/packages/source/e/eke.review/eke.review-1.0.4.zip
  md5: 4945c48344988791b641b70bea5bc48e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eke.review = eke.review:main
    #
    # Would create an entry point called eke.review that calls eke.review.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - products.cmfplone
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - products.cmfplone
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - eke
    - eke.review
    - eke.review.content
    - eke.review.tests
    - eke.review.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EDRN/eke.review
  license: Apache Software License
  summary: 'Review and Quality Control for the EDRN Knowledge Environment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
