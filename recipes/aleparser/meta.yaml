package:
  name: aleparser
  version: "0.1.1"

source:
  fn: aleparser-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aleparser/aleparser-0.1.1.tar.gz
  md5: 00d204e988584317fe82a10db7e4d535
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aleparser = aleparser:main
    #
    # Would create an entry point called aleparser that calls aleparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aleparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kloptops/ALEParser
  license: Public Domain
  summary: 'A simple python ALE (AvidLogExchange) parser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
