package:
  name: django-widgeter
  version: "0.1"

source:
  fn: django-widgeter-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-widgeter/django-widgeter-0.1.tar.gz
  md5: d549f52f302c7a76325836a445e393c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-widgeter = django-widgeter:main
    #
    # Would create an entry point called django-widgeter that calls django-widgeter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - widgeter
    - widgeter.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dlapiduz/django-widgeter
  license: Public Domain
  summary: u'Widgeter: A simple engine for backend widgets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
