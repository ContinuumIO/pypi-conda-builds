package:
  name: djinn_workflow
  version: "1.0.0"

source:
  fn: djinn_workflow-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djinn_workflow/djinn_workflow-1.0.0.tar.gz
  md5: 055fd1353e804672f273c3754f89469d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinn_workflow = djinn_workflow:main
    #
    # Would create an entry point called djinn_workflow that calls djinn_workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - djinn_auth

  run:
    - python
    - setuptools
    - django
    - djinn_auth

test:
  # Python imports
  imports:
    - djinn_workflow
    - djinn_workflow.models
    - djinn_workflow.templatetags
    - djinn_workflow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/djinn_workflow
  license: beer-ware
  summary: 'Djinn Intranet workflow module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
