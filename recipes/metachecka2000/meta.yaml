package:
  name: metachecka2000
  version: "0.1.3"

source:
  fn: MetaChecka2000-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/M/MetaChecka2000/MetaChecka2000-0.1.3.tar.gz
  md5: 59de3b74b932553a71eadc7a57a5d498
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metachecka2000 = metachecka2000:main
    #
    # Would create an entry point called metachecka2000 that calls metachecka2000.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - simplehmmer >=0.2.3
    - biopython >=1.58

  run:
    - python
    - simplehmmer >=0.2.3
    - biopython >=1.58

test:
  # Python imports
  imports:
    - metachecka2000
    - metachecka2000.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/MetaChecka2000/
  license: GPL3
  summary: 'MetaChecka2000'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
