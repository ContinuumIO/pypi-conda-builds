package:
  name: django-bagou
  version: "0.0.1"

source:
  fn: django-bagou-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bagou/django-bagou-0.0.1.tar.gz
  md5: 7523e3dbde01248dda7eaa37c72530a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bagou = django-bagou:main
    #
    # Would create an entry point called django-bagou that calls django-bagou.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado ==3.1.1
    - pika ==0.9.13

  run:
    - python
    - tornado ==3.1.1
    - pika ==0.9.13

test:
  # Python imports
  imports:
    - bagou

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/socketubs/bagou
  license: MIT License
  summary: 'Django Websocket for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
