package:
  name: sheepdog
  version: "0.2.3"

source:
  fn: Sheepdog-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/S/Sheepdog/Sheepdog-0.2.3.tar.gz
  md5: 42daee709bd300586d5fdc5ffc40e868
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sheepdog = sheepdog:main
    #
    # Would create an entry point called sheepdog that calls sheepdog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - paramiko ==1.13.0

  run:
    - python
    - flask >=0.10.1
    - paramiko ==1.13.0

test:
  # Python imports
  imports:
    - sheepdog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sheepdog.readthedocs.org
  license: MIT License
  summary: 'Shepherd GridEngine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
