package:
  name: django-smartfields
  version: "1.0.6"

source:
  fn: django-smartfields-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smartfields/django-smartfields-1.0.6.tar.gz
  md5: 41de50b10744a90d7dcf467d2dc9cab1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smartfields = django-smartfields:main
    #
    # Would create an entry point called django-smartfields that calls django-smartfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.0

  run:
    - python
    - django >=1.7.0

test:
  # Python imports
  imports:
    - smartfields
    - smartfields.crispy
    - smartfields.processors
    - tests
    - tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lehins/django-smartfields
  license: MIT License
  summary: 'Django Model Fields that are smart.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
