package:
  name: assertlib
  version: "0.2"

source:
  fn: assertlib-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/assertlib/assertlib-0.2.tar.gz
  md5: d329553248c18c4962e18c82dd211de6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - assertlib = assertlib:main
    #
    # Would create an entry point called assertlib that calls assertlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - assertlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.antlong.com/
  license: UNKNOWN
  summary: 'Assertlib is a library of assertion methods.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
