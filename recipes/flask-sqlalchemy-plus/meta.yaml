package:
  name: flask-sqlalchemy-plus
  version: "0.1.2"

source:
  fn: flask-sqlalchemy-plus-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-sqlalchemy-plus/flask-sqlalchemy-plus-0.1.2.tar.gz
  md5: 302d0cdfc4a10c01619c6079910154e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sqlalchemy-plus = flask-sqlalchemy-plus:main
    #
    # Would create an entry point called flask-sqlalchemy-plus that calls flask-sqlalchemy-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - toolspy >=0.1.
    - flask >=0.10.1
    - sqlalchemy >=0.9.8
    - flask-sqlalchemy >=2.0

  run:
    - python
    - toolspy >=0.1.
    - flask >=0.10.1
    - sqlalchemy >=0.9.8
    - flask-sqlalchemy >=2.0

test:
  # Python imports
  imports:
    - flask_sqlalchemy_plus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
