package:
  name: ffvideo
  version: "0.0.13"

source:
  fn: FFVideo-0.0.13.tar.gz
  url: https://pypi.python.org/packages/source/F/FFVideo/FFVideo-0.0.13.tar.gz
  md5: a28ff6e211fbb114c60b51090c6d7d1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ffvideo = ffvideo:main
    #
    # Would create an entry point called ffvideo that calls ffvideo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ffvideo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/zakhar/ffvideo/
  license: UNKNOWN
  summary: 'FFVideo is a python extension makes possible to access to decoded frames at two format: PIL.Image or numpy.ndarray.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
