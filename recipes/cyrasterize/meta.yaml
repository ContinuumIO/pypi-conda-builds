package:
  name: cyrasterize
  version: "0.1.5"

source:
  fn: cyrasterize-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cyrasterize/cyrasterize-0.1.5.tar.gz
  md5: 33dcc76dbeb639112c7936711122ec2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyrasterize = cyrasterize:main
    #
    # Would create an entry point called cyrasterize that calls cyrasterize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8.0

  run:
    - python
    - numpy >=1.8.0

test:
  # Python imports
  imports:
    - cyrasterize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/menpo/cyrasterize/
  license: BSD License
  summary: 'Simple fast OpenGL offscreen rasterizing in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
