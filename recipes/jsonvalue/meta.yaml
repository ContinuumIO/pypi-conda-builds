package:
  name: jsonvalue
  version: "0.1"

source:
  fn: jsonvalue-0.1.zip
  url: https://pypi.python.org/packages/source/j/jsonvalue/jsonvalue-0.1.zip
  md5: c1af3cba50a9582242fb152b1d81da7a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonvalue = jsonvalue:main
    #
    # Would create an entry point called jsonvalue that calls jsonvalue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyld >=0.6.1
    - isodate >=0.5.0

  run:
    - python
    - setuptools
    - pyld >=0.6.1
    - isodate >=0.5.0

test:
  # Python imports
  imports:
    - jsonvalue
    - jsonvalue.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Rich values for JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
