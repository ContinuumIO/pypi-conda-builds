package:
  name: clicheck
  version: "0.1.0"

source:
  fn: clicheck-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/clicheck/clicheck-0.1.0.tar.gz
  md5: 16e3d63e376cfd33280c1cdacd67ca41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clicheck = clicheck:main
    #
    # Would create an entry point called clicheck that calls clicheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyyaml ==3.11
    - argparse ==1.2.1
    - wsgiref ==0.1.2

  run:
    - python
    - pyyaml ==3.11
    - argparse ==1.2.1
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - clicheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sebbrochet/clicheck
  license: MIT License
  summary: 'CLI acceptance testing based on output comparison'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
