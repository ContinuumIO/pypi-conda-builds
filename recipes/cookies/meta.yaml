package:
  name: cookies
  version: "2.2.1"

source:
  fn: cookies-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cookies/cookies-2.2.1.tar.gz
  md5: 6f4c53aba3ed03e4e7b50812c2c2579a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cookies = cookies:main
    #
    # Would create an entry point called cookies that calls cookies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sashahart/cookies
  license: MIT License
  summary: 'Friendlier RFC 6265-compliant cookie parser/renderer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
