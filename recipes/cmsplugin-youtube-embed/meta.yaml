package:
  name: cmsplugin-youtube-embed
  version: "0.1"

source:
  fn: cmsplugin-youtube-embed-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-youtube-embed/cmsplugin-youtube-embed-0.1.tar.gz
  md5: e62463a6a9ed74338adf67d968860199
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-youtube-embed = cmsplugin-youtube-embed:main
    #
    # Would create an entry point called cmsplugin-youtube-embed that calls cmsplugin-youtube-embed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_youtube

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A simple Django app to embed youtube videos into the cms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
