package:
  name: django_hostproof_auth
  version: "0.1.0"

source:
  fn: django_hostproof_auth-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_hostproof_auth/django_hostproof_auth-0.1.0.tar.gz
  md5: 9d6c2f9776d1e0a27a841d392d22f37d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_hostproof_auth = django_hostproof_auth:main
    #
    # Would create an entry point called django_hostproof_auth that calls django_hostproof_auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rsa >=3.1.2

  run:
    - python
    - rsa >=3.1.2

test:
  # Python imports
  imports:
    - django_hostproof_auth
    - hostproof_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpintado/django-hostproof-auth
  license: MIT
  summary: 'Secure Host-Proof authentication backend for Django-powered sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
