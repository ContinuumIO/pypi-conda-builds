package:
  name: django-token-asena
  version: "0.2"

source:
  fn: django-token-asena-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-token-asena/django-token-asena-0.2.tar.gz
  md5: ee3d043f4012cde8605b27b74c857665
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-token-asena = django-token-asena:main
    #
    # Would create an entry point called django-token-asena that calls django-token-asena.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - asena

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.gitorious.org/token-asena
  license: GPLv3
  summary: 'Generate anonymous-use tokens and protect views from unauthorized access.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
