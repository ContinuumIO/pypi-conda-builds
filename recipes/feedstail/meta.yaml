package:
  name: feedstail
  version: "0.5.1"

source:
  fn: feedstail-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/f/feedstail/feedstail-0.5.1.tar.gz
  md5: 4986dadde2edf8c6241c551b32b0c603
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feedstail = feedstail:main
    #
    # Would create an entry point called feedstail that calls feedstail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - feedparser

  run:
    - python
    - argparse
    - feedparser

test:
  # Python imports
  imports:
    - feedstail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Psycojoker/feedstail
  license: GNU General Public License (GPL)
  summary: 'A tail-f-like utility for feeds'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
