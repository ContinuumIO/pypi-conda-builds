package:
  name: django-smart-slug
  version: "0.1.0"

source:
  fn: django-smart-slug-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smart-slug/django-smart-slug-0.1.0.tar.gz
  md5: 8aa1bf6ef7177941fcf6bac6c255b69e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smart-slug = django-smart-slug:main
    #
    # Would create an entry point called django-smart-slug that calls django-smart-slug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smart_slug
    - smart_slug.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coleifer/django-smart-slug/tree/master
  license: MIT License
  summary: 'a smart slug field for your models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
