package:
  name: hgcanttype
  version: "1.0.8"

source:
  fn: hgcanttype-1.0.8.tar.gz
  url: https://pypi.python.org/packages/source/h/hgcanttype/hgcanttype-1.0.8.tar.gz
  md5: 251ee571c7d0dd5c2ff77c24d5108f61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgcanttype = hgcanttype:main
    #
    # Would create an entry point called hgcanttype that calls hgcanttype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial

  run:
    - python
    - mercurial

test:
  # Python imports
  imports:
    - hgcanttype

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/getoffmalawn/hgcanttype
  license: GPLv2+
  summary: 'Typo handler for Mercurial commands'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
