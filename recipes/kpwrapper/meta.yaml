package:
  name: kpwrapper
  version: "1.0.4"

source:
  fn: kpwrapper-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/k/kpwrapper/kpwrapper-1.0.4.tar.gz
  md5: 0addcce4576155e4fb13284fd2903994
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kpwrapper = kpwrapper:main
    #
    # Would create an entry point called kpwrapper that calls kpwrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - smart-m3-pythonkp >=0.9.0

  run:
    - python
    - setuptools
    - smart-m3-pythonkp >=0.9.0

test:
  # Python imports
  imports:
    - kpwrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://asibsync.sourceforge.net
  license: BSD
  summary: 'Smart-M3 KP wrapper library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
