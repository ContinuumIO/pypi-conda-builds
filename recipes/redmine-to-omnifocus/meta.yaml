package:
  name: redmine-to-omnifocus
  version: "1.0"

source:
  fn: redmine-to-omnifocus-1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redmine-to-omnifocus/redmine-to-omnifocus-1.0.tar.gz
  md5: 8259d806bdb7661d609a57d447937350
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redmine-to-omnifocus = redmine-to-omnifocus:main
    #
    # Would create an entry point called redmine-to-omnifocus that calls redmine-to-omnifocus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup >=3.0
    - appscript >=0.20
    - gntp

  run:
    - python
    - beautifulsoup >=3.0
    - appscript >=0.20
    - gntp

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guyboltonking/redmine-to-omnifocus
  license: MIT License
  summary: 'Import Redmine issues into OmniFocus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
