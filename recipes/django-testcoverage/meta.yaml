package:
  name: django-testcoverage
  version: "0.1.3"

source:
  fn: django-testcoverage-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-testcoverage/django-testcoverage-0.1.3.tar.gz
  md5: d797514a4a744ad39f3f01e1bcac55ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-testcoverage = django-testcoverage:main
    #
    # Would create an entry point called django-testcoverage that calls django-testcoverage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.2
    - coverage >=3.4

  run:
    - python
    - django >=1.2
    - coverage >=3.4

test:
  # Python imports
  imports:
    - testcoverage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikoskay/django-testcoverage
  license: BSD License
  summary: 'Code coverage reports for Django unit testing framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
