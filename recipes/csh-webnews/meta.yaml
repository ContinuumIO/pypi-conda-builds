package:
  name: csh-webnews
  version: "0.1.2"

source:
  fn: csh-webnews-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/csh-webnews/csh-webnews-0.1.2.tar.gz
  md5: 254b22bf1c9ec294d8ec68c7b85758eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csh-webnews = csh-webnews:main
    #
    # Would create an entry point called csh-webnews that calls csh-webnews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/gambogi/pywebnews
  license: MIT
  summary: 'A CSH webnews API wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
