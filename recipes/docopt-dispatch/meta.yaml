package:
  name: docopt-dispatch
  version: "0.0.2"

source:
  fn: docopt-dispatch-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/docopt-dispatch/docopt-dispatch-0.0.2.tar.gz
  md5: 109aad28343e1454d54acd2b509adfa3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docopt-dispatch = docopt-dispatch:main
    #
    # Would create an entry point called docopt-dispatch that calls docopt-dispatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt

  run:
    - python
    - docopt

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/halst/docopt-dispatch
  license: MIT License
  summary: 'Dispatch from command-line arguments to functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
