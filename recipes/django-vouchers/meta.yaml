package:
  name: django-vouchers
  version: "0.6"

source:
  fn: django-vouchers-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vouchers/django-vouchers-0.6.tar.gz
  md5: a5a06d566b8ddddcf5dc1ef2f3183c41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vouchers = django-vouchers:main
    #
    # Would create an entry point called django-vouchers that calls django-vouchers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - voucher
    - voucher.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yeago/django-vouchers
  license: UNKNOWN
  summary: 'Vouchers in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
