package:
  name: ropevim
  version: "0.5.0"

source:
  fn: ropevim-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/r/ropevim/ropevim-0.5.0.tar.gz
  md5: 1631979305b772f36eaf543e8b4e50f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ropevim = ropevim:main
    #
    # Would create an entry point called ropevim that calls ropevim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rope >=0.9.3
    - ropemode

  run:
    - python
    - rope >=0.9.3
    - ropemode

test:
  # Python imports
  imports:
    - ropevim

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rope.sf.net/ropevim.html
  license: GNU General Public License (GPL)
  summary: 'A vim plugin for using rope python refactoring library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
