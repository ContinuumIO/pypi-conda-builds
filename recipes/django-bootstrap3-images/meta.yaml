package:
  name: django-bootstrap3-images
  version: "1.0-rc1"

source:
  fn: django-bootstrap3-images-1.0-rc1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap3-images/django-bootstrap3-images-1.0-rc1.tar.gz
  md5: 4faf9d36f6b901ba689d80eed75b1899
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap3-images = django-bootstrap3-images:main
    #
    # Would create an entry point called django-bootstrap3-images that calls django-bootstrap3-images.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - bootstrap3_images

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dploeger/django-bootstrap3-images
  license: BSD License
  summary: 'Django model field and widget for storing, retrieving and displaying bootstrap3 glyphicon-images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
