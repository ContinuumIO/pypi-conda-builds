package:
  name: redis-faina
  version: "0.1.0"

source:
  fn: redis-faina-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-faina/redis-faina-0.1.0.tar.gz
  md5: 4b20d5775c0e311979b598bb23178b9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-faina = redis-faina:main
    #
    # Would create an entry point called redis-faina that calls redis-faina.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - redis_faina

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Instagram/redis-faina
  license: LICENSE
  summary: 'Query analyzer that parses Redis MONITOR command output'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
