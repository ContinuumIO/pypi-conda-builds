package:
  name: lp-show-my-bugs
  version: "0.0.4"

source:
  fn: lp-show-my-bugs-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/l/lp-show-my-bugs/lp-show-my-bugs-0.0.4.tar.gz
  md5: a07f9113685fecd9636ad1ac56c4563c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lp-show-my-bugs = lp-show-my-bugs:main
    #
    # Would create an entry point called lp-show-my-bugs that calls lp-show-my-bugs.main()

    - lp-show-my-bugs = lp_show_my_bugs.cli:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - launchpadlib
    - tabulate

  run:
    - python
    - launchpadlib
    - tabulate

test:
  # Python imports
  imports:
    - lp_show_my_bugs

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - lp-show-my-bugs --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
