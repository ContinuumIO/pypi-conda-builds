package:
  name: axmlparserpy
  version: "0.01"

source:
  fn: AxmlParserPY-0.01.tar.gz
  url: https://pypi.python.org/packages/source/A/AxmlParserPY/AxmlParserPY-0.01.tar.gz
  md5: 9cb49fbe11107500e7bc46a0f41e1bbd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - axmlparserpy = axmlparserpy:main
    #
    # Would create an entry point called axmlparserpy that calls axmlparserpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - axmlparserpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kzjeef/AxmlParserPY.git
  license: UNKNOWN
  summary: "Python support for extract information from android 'apk' package"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
