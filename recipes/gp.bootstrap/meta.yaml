package:
  name: gp.bootstrap
  version: "0.3"

source:
  fn: gp.bootstrap-0.3.zip
  url: https://pypi.python.org/packages/source/g/gp.bootstrap/gp.bootstrap-0.3.zip
  md5: a1cb8fb5d34353ef832ba3bb54cc8f2f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gp.bootstrap = gp.bootstrap:main
    #
    # Would create an entry point called gp.bootstrap that calls gp.bootstrap.main()

    - bootstrap=gpbootstrap:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - gpbootstrap

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - bootstrap --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Small script to deal with zc.buildout bootstrapping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
