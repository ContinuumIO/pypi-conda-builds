package:
  name: flask-stormpath
  version: "0.4.0"

source:
  fn: Flask-Stormpath-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Stormpath/Flask-Stormpath-0.4.0.tar.gz
  md5: ba164f8981a94b02e182a31b3e17cfa7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-stormpath = flask-stormpath:main
    #
    # Would create an entry point called flask-stormpath that calls flask-stormpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9.0
    - flask-login ==0.2.9
    - flask-wtf >=0.9.5
    - facebook-sdk ==0.4.0
    - oauth2client ==1.2
    - stormpath ==1.3.5

  run:
    - python
    - flask >=0.9.0
    - flask-login ==0.2.9
    - flask-wtf >=0.9.5
    - facebook-sdk ==0.4.0
    - oauth2client ==1.2
    - stormpath ==1.3.5

test:
  # Python imports
  imports:
    - flask_stormpath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stormpath/stormpath-flask
  license: Apache Software License
  summary: 'Simple and secure user authentication for Flask via Stormpath.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
