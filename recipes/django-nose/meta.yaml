package:
  name: django-nose
  version: "1.4"

source:
  fn: django-nose-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nose/django-nose-1.4.tar.gz
  md5: 2713d95286ea49860458a312d2efe653
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nose = django-nose:main
    #
    # Would create an entry point called django-nose that calls django-nose.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose >=1.2.1
    - django >=1.4

  run:
    - python
    - nose >=1.2.1
    - django >=1.4

test:
  # Python imports
  imports:
    - django_nose
    - django_nose.management
    - django_nose.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/django-nose/django-nose
  license: BSD License
  summary: 'Makes your Django tests simple and snappy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
