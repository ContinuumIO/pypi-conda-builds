package:
  name: upfront.foldercontents
  version: "1.0"

source:
  fn: upfront.foldercontents-1.0.tar.gz
  url: https://pypi.python.org/packages/source/u/upfront.foldercontents/upfront.foldercontents-1.0.tar.gz
  md5: 32c4f5f47228bb3c95fe1f562b597765
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - upfront.foldercontents = upfront.foldercontents:main
    #
    # Would create an entry point called upfront.foldercontents that calls upfront.foldercontents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.content

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.content

test:
  # Python imports
  imports:
    - upfront
    - upfront.foldercontents
    - upfront.foldercontents.docs
    - upfront.foldercontents.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/upfront.foldercontents
  license: GPL
  summary: 'A folder contents implementation for Plone that does not depend on the portal_catalog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
