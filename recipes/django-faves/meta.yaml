package:
  name: django-faves
  version: "0.6.2"

source:
  fn: django-faves-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-faves/django-faves-0.6.2.tar.gz
  md5: 53d158a7260323eb53555cdef56d22c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-faves = django-faves:main
    #
    # Would create an entry point called django-faves that calls django-faves.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - faves
    - faves.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-faves/
  license: BSD License
  summary: 'Generic favorites pluggable django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
