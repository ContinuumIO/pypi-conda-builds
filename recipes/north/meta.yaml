package:
  name: north
  version: "0.1.7"

source:
  fn: North-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/N/North/North-0.1.7.tar.gz
  md5: f026fe8f59c79ad65f4abd3685b0ee8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - north = north:main
    #
    # Would create an entry point called north that calls north.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangosite
    - babel

  run:
    - python
    - djangosite
    - babel

test:
  # Python imports
  imports:
    - north
    - north.dbutils
    - north.demo
    - north.management
    - north.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://north.lino-framework.org
  license: Free BSD
  summary: 'Another way to migrate Django databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
