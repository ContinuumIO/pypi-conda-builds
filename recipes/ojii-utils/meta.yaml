package:
  name: ojii-utils
  version: "0.1.0"

source:
  fn: ojii-utils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/ojii-utils/ojii-utils-0.1.0.tar.gz
  md5: acafe5f7403549a396a22272bbe4bd4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ojii-utils = ojii-utils:main
    #
    # Would create an entry point called ojii-utils that calls ojii-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ojii
    - ojii.templatetags
    - ojii.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/ojii-utils
  license: UNKNOWN
  summary: 'Some utilities I wrote'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
