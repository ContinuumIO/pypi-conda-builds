package:
  name: django-filtered-mail
  version: "0.2"

source:
  fn: django-filtered-mail-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filtered-mail/django-filtered-mail-0.2.tar.gz
  md5: 46b4d9a272d44763ab97a7192135283e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filtered-mail = django-filtered-mail:main
    #
    # Would create an entry point called django-filtered-mail that calls django-filtered-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - django_filtered_mail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rudolphfroger/django-filtered-mail
  license: MIT License
  summary: 'Django Email Backend which only sends to whitelisted recipients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
