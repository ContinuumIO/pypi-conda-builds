package:
  name: hatenadiary
  version: "0.3"

source:
  fn: hatenadiary-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hatenadiary/hatenadiary-0.3.tar.gz
  md5: 7d57cfb569e63d6a7f20d80d46111ce2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hatenadiary = hatenadiary:main
    #
    # Would create an entry point called hatenadiary that calls hatenadiary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hatenadiary

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A python wrapper around the Hatena Diary AtomPub'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
