package:
  name: genomediff
  version: "0.2.0"

source:
  fn: genomediff-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/genomediff/genomediff-0.2.0.tar.gz
  md5: 259db6d20228e5456f63ad26e2bcc8e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genomediff = genomediff:main
    #
    # Would create an entry point called genomediff that calls genomediff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - genomediff

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/biosustain/genomediff-python.git
  license: MIT License
  summary: 'GenomeDiff (*.gd) file reader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
