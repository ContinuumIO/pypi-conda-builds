package:
  name: js.jquery_fullcalendar
  version: "1.5.2"

source:
  fn: js.jquery_fullcalendar-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_fullcalendar/js.jquery_fullcalendar-1.5.2.tar.gz
  md5: e8290b91e090d6101301ffb1a59f41dc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_fullcalendar = js.jquery_fullcalendar:main
    #
    # Would create an entry point called js.jquery_fullcalendar that calls js.jquery_fullcalendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_fullcalendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jQuery FullCalendar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
