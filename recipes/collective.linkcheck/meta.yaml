package:
  name: collective.linkcheck
  version: "1.2"

source:
  fn: collective.linkcheck-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.linkcheck/collective.linkcheck-1.2.tar.gz
  md5: 0133b461843cb65a9527ba0dae56bcb6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.linkcheck = collective.linkcheck:main
    #
    # Would create an entry point called collective.linkcheck that calls collective.linkcheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.queue >=1.3
    - requests
    - plone.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - zc.queue >=1.3
    - requests
    - plone.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.linkcheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv2+
  summary: 'Add-on for Plone that provides link validity checking and reporting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
