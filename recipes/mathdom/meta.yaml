package:
  name: mathdom
  version: "0.8"

source:
  fn: mathdom-0.8.tar.gz
  url: https://pypi.python.org/packages/source/m/mathdom/mathdom-0.8.tar.gz
  md5: 9cc9f378198b44a1bb1c9069167f1ded
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mathdom = mathdom:main
    #
    # Would create an entry point called mathdom that calls mathdom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mathdom
    - mathml
    - mathml.pmathml
    - mathml.schema
    - mathml.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mathdom.sourceforge.net/
  license: MIT License
  summary: 'MathDOM - Content MathML in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
