package:
  name: django-adminaudit
  version: "0.3.3"

source:
  fn: django-adminaudit-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adminaudit/django-adminaudit-0.3.3.tar.gz
  md5: 72fc580352c2140427bf4f71d2455e86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adminaudit = django-adminaudit:main
    #
    # Would create an entry point called django-adminaudit that calls django-adminaudit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - adminaudit
    - adminaudit.management
    - adminaudit.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-adminaudit
  license: GNU Library or Lesser General Public License (LGPL)
  summary: "Extends Django's admin logging capabilities"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
