package:
  name: python-itunes
  version: "1.0"

source:
  fn: python-itunes.tar.gz
  url: https://pypi.python.org/packages/source/p/python-itunes/python-itunes.tar.gz
  md5: 6690e8a261d3486b90763e3281842c1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-itunes = python-itunes:main
    #
    # Would create an entry point called python-itunes that calls python-itunes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - itunes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ocelma/python-itunes
  license: http://www.gnu.org/copyleft/gpl.html
  summary: 'A simple python wrapper to access iTunes Store API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
