package:
  name: harvest_api_client
  version: "1.1.3"

source:
  fn: harvest_api_client-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/harvest_api_client/harvest_api_client-1.1.3.tar.gz
  md5: 269fdea4d0d556e34fccb1f806552185
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - harvest_api_client = harvest_api_client:main
    #
    # Would create an entry point called harvest_api_client that calls harvest_api_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - harvest_api_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GildedHonour/harvest-api-client
  license: MIT License
  summary: 'A client for the Harvest API (getharvest.com)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
