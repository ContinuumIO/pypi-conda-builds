package:
  name: django_openkm
  version: "0.5.1-pre-alpha"

source:
  fn: django_openkm-0.5.1-pre-alpha.tar.gz
  url: https://pypi.python.org/packages/source/d/django_openkm/django_openkm-0.5.1-pre-alpha.tar.gz
  md5: 7f7bf26736963db96d9a139ff1e84b6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_openkm = django_openkm:main
    #
    # Would create an entry point called django_openkm that calls django_openkm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds >=0.4

  run:
    - python
    - suds >=0.4

test:
  # Python imports
  imports:
    - django_openkm
    - openkm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tangentlabs/django-openkm
  license: BSD License
  summary: 'A Python client library and functionality for the OpenKM document management system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
