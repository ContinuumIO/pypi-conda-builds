package:
  name: cement
  version: "2.6.0"

source:
  fn: cement-2.6.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cement/cement-2.6.0.tar.gz
  md5: 3f5be05103f200c7aba3b23bf5e4da73
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cement = cement:main
    #
    # Would create an entry point called cement that calls cement.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - cement
    - cement.core
    - cement.ext
    - cement.utils
    - examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://builtoncement.org
  license: BSD License
  summary: 'CLI Application Framework for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
