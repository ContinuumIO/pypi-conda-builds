package:
  name: mqlalchemy
  version: "0.1.3"

source:
  fn: MQLAlchemy-0.1.3.zip
  url: https://pypi.python.org/packages/source/M/MQLAlchemy/MQLAlchemy-0.1.3.zip
  md5: 89293054f9c6fe5dbb2897d0bc3f6bf6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mqlalchemy = mqlalchemy:main
    #
    # Would create an entry point called mqlalchemy that calls mqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9

  run:
    - python
    - sqlalchemy >=0.9

test:
  # Python imports
  imports:
    - mqlalchemy
    - mqlalchemy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/repole/mqlalchemy
  license: MIT License
  summary: 'Query SQLAlchemy models with MongoDB syntax.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
