package:
  name: pythonic-porin
  version: "0.2.1"

source:
  fn: pythonic-porin-0.2.1.zip
  url: https://pypi.python.org/packages/source/p/pythonic-porin/pythonic-porin-0.2.1.zip
  md5: ada1622e489362bb5c8638984d08fc73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythonic-porin = pythonic-porin:main
    #
    # Would create an entry point called pythonic-porin that calls pythonic-porin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cython >=0.20.1
    - numpy >=1.8.0
    - matplotlib >=1.3.1

  run:
    - python
    - cython >=0.20.1
    - numpy >=1.8.0
    - matplotlib >=1.3.1

test:
  # Python imports
  imports:
    - PyPore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/pythonic-porin/
  license: LICENSE.txt
  summary: 'Nanopore Data Analysis package. Provides tools for reading data,        performing event detection, segmentation, visualization, and analysis using        hidden Markov models, and other tools. Designed for the UCSC Nanopore Group.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
