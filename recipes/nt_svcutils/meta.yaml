package:
  name: nt_svcutils
  version: "2.13.0"

source:
  fn: nt_svcutils-2.13.0.zip
  url: https://pypi.python.org/packages/source/n/nt_svcutils/nt_svcutils-2.13.0.zip
  md5: 5d5f2c527d323e4e82cd6c7c113207ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nt_svcutils = nt_svcutils:main
    #
    # Would create an entry point called nt_svcutils that calls nt_svcutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nt_svcutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/nt_svcutils
  license: ZPL 2.1
  summary: 'nt_svcutils for Zope2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
