package:
  name: django-theherk-external-urls
  version: "1.4"

source:
  fn: django-theherk-external-urls-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-theherk-external-urls/django-theherk-external-urls-1.4.tar.gz
  md5: 539c885170ca0c8dcc44001405ad4e34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-theherk-external-urls = django-theherk-external-urls:main
    #
    # Would create an entry point called django-theherk-external-urls that calls django-theherk-external-urls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - menu_external_urls
    - menu_external_urls.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theherk/django-theherk-external-urls
  license: see file LICENSE
  summary: 'Django CMS plugin to allowing using external urls in the menu system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
