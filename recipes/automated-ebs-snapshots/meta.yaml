package:
  name: automated-ebs-snapshots
  version: "0.4.1a1"

source:
  fn: automated-ebs-snapshots-0.4.1a1.tar.gz
  url: https://pypi.python.org/packages/source/a/automated-ebs-snapshots/automated-ebs-snapshots-0.4.1a1.tar.gz
  md5: 862e4115b6ba369ee15a08c9cc1bb3e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - automated-ebs-snapshots = automated-ebs-snapshots:main
    #
    # Would create an entry point called automated-ebs-snapshots that calls automated-ebs-snapshots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.29.1

  run:
    - python
    - boto >=2.29.1

test:
  # Python imports
  imports:
    - automated_ebs_snapshots

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skymill/automated-ebs-snapshots
  license: Apache Software License
  summary: 'Automatic backup management of AWS EC2 EBS volumes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
