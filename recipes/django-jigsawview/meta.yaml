package:
  name: django-jigsawview
  version: "0.1.0"

source:
  fn: django-jigsawview-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jigsawview/django-jigsawview-0.1.0.tar.gz
  md5: 7d835df6193784cf57beb61c4f55f0bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jigsawview = django-jigsawview:main
    #
    # Would create an entry point called django-jigsawview that calls django-jigsawview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - django >=1.4
    - django-filter

  run:
    - python
    - six
    - django >=1.4
    - django-filter

test:
  # Python imports
  imports:
    - jigsawview
    - jigsawview.pieces
    - jigsawview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/linovia/django-jigsawview
  license: BSD
  summary: 'An extended alternative to Django generic class based views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
