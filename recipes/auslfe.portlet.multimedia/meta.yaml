package:
  name: auslfe.portlet.multimedia
  version: "1.0.1"

source:
  fn: auslfe.portlet.multimedia-1.0.1.zip
  url: https://pypi.python.org/packages/source/a/auslfe.portlet.multimedia/auslfe.portlet.multimedia-1.0.1.zip
  md5: ace7207482f0bd45243a8df5cc970cca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auslfe.portlet.multimedia = auslfe.portlet.multimedia:main
    #
    # Would create an entry point called auslfe.portlet.multimedia that calls auslfe.portlet.multimedia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - collective.js.imagesloaded

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - collective.js.imagesloaded

test:
  # Python imports
  imports:
    - auslfe
    - auslfe.portlet
    - auslfe.portlet.multimedia
    - auslfe.portlet.multimedia.browser
    - auslfe.portlet.multimedia.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/auslfe.portlet.multimedia
  license: GPL
  summary: 'A simple Plone multimedia Portlet with an eye to accessibility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
