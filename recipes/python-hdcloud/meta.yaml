package:
  name: python-hdcloud
  version: "1.0.1"

source:
  fn: python-hdcloud-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hdcloud/python-hdcloud-1.0.1.tar.gz
  md5: 6f3edf8fc8ca5d519da48ce42c988cdd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hdcloud = python-hdcloud:main
    #
    # Would create an entry point called python-hdcloud that calls python-hdcloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - hdcloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/python-hdcloud
  license: BSD License
  summary: 'Client library for the HD Cloud API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
