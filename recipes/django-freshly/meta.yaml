package:
  name: django-freshly
  version: "0.1.1"

source:
  fn: django-freshly-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-freshly/django-freshly-0.1.1.tar.gz
  md5: babecb89cb9b46978f88bd510b971854
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-freshly = django-freshly:main
    #
    # Would create an entry point called django-freshly that calls django-freshly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - freshly
    - freshly/middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un33k/django-freshly
  license: BSD License
  summary: 'A Django application that enables your server to remotely trigger assets reload by the client(s)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
