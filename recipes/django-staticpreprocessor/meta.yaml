package:
  name: django-staticpreprocessor
  version: "0.3.0"

source:
  fn: django-staticpreprocessor-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticpreprocessor/django-staticpreprocessor-0.3.0.tar.gz
  md5: 36cadf37d9fc73767a9dad6a3477c023
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticpreprocessor = django-staticpreprocessor:main
    #
    # Would create an entry point called django-staticpreprocessor that calls django-staticpreprocessor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - django-appconf

  run:
    - python
    - django >=1.4.3
    - django-appconf

test:
  # Python imports
  imports:
    - staticpreprocessor
    - staticpreprocessor.contrib
    - staticpreprocessor.contrib.processors
    - staticpreprocessor.management
    - staticpreprocessor.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lpomfrey/django-staticpreprocessor
  license: BSD License
  summary: 'Pre-process static files in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
