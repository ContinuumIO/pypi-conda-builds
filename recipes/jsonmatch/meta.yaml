package:
  name: jsonmatch
  version: "0.1"

source:
  fn: jsonmatch-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonmatch/jsonmatch-0.1.tar.gz
  md5: bd6369f464b87497c9a8b68fae15692a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonmatch = jsonmatch:main
    #
    # Would create an entry point called jsonmatch that calls jsonmatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsonmatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/percolate/jsonmatch
  license: BSD License
  summary: 'A flexible framework for testing JSON dicts against schemas.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
