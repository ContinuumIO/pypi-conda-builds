package:
  name: celery_once
  version: "0.1.2"

source:
  fn: celery_once-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/celery_once/celery_once-0.1.2.tar.gz
  md5: f2596048a7d40ffe8c2215af39489666
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celery_once = celery_once:main
    #
    # Would create an entry point called celery_once that calls celery_once.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery
    - redis

  run:
    - python
    - celery
    - redis

test:
  # Python imports
  imports:
    - celery_once
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trackmaven/celery-once
  license: BSD License
  summary: 'Allows you to prevent multiple execution and queuing of celery tasks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
