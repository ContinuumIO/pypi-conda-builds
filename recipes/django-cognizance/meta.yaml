package:
  name: django-cognizance
  version: "0.0a1"

source:
  fn: django-cognizance-0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cognizance/django-cognizance-0.0a1.tar.gz
  md5: 66f8348b3faba18413deb000986eeb6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cognizance = django-cognizance:main
    #
    # Would create an entry point called django-cognizance that calls django-cognizance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-guardian

  run:
    - python
    - django >=1.4
    - django-guardian

test:
  # Python imports
  imports:
    - cognizance
    - cognizance.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bmelton/django-cognizance/
  license: Creative Commons Non-Commercial, 3.0
  summary: "Cognizance isn't that great yet.  Don't use it."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
