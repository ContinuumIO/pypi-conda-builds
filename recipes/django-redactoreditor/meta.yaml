package:
  name: django-redactoreditor
  version: "1.2.10"

source:
  fn: django-redactoreditor-1.2.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redactoreditor/django-redactoreditor-1.2.10.tar.gz
  md5: 2bab8d61b26ede6b908a27614f3160f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redactoreditor = django-redactoreditor:main
    #
    # Would create an entry point called django-redactoreditor that calls django-redactoreditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1

  run:
    - python
    - django >=1.3.1

test:
  # Python imports
  imports:
    - redactor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mazelife/django-redactoreditor
  license: CC licence, see LICENSE.txt
  summary: 'Integrates the Redactor Javascript WYSIWYG editor with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
