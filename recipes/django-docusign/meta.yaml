package:
  name: django-docusign
  version: "0.11"

source:
  fn: django-docusign-0.11.zip
  url: https://pypi.python.org/packages/source/d/django-docusign/django-docusign-0.11.zip
  md5: bb06090553d6246141af4934fe0ac0c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-docusign = django-docusign:main
    #
    # Would create an entry point called django-docusign that calls django-docusign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django <1.7
    - django-anysign >=0.3
    - pydocusign >=0.13.1
    - setuptools
    - mock

  run:
    - python
    - django <1.7
    - django-anysign >=0.3
    - pydocusign >=0.13.1
    - setuptools
    - mock

test:
  # Python imports
  imports:
    - django_docusign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-docusign.readthedocs.org/
  license: BSD License
  summary: 'Django application for DocuSign signature SAAS platform.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
