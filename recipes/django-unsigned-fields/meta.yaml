package:
  name: django-unsigned-fields
  version: "1.3.0"

source:
  fn: django-unsigned-fields-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-unsigned-fields/django-unsigned-fields-1.3.0.tar.gz
  md5: 68869196908112b5b960898de3c055b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-unsigned-fields = django-unsigned-fields:main
    #
    # Would create an entry point called django-unsigned-fields that calls django-unsigned-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_unsigned_fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simpleenergy/django-unsigned-fields
  license: BSD License
  summary: 'Django fields for using unsigned integers for relationships'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
