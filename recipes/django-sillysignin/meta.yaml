package:
  name: django-sillysignin
  version: "0.0.6"

source:
  fn: django-sillysignin-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sillysignin/django-sillysignin-0.0.6.tar.gz
  md5: fb9904c401249b64ca783f2c8277eaf6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sillysignin = django-sillysignin:main
    #
    # Would create an entry point called django-sillysignin that calls django-sillysignin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-class-based-auth-views

  run:
    - python
    - django-class-based-auth-views

test:
  # Python imports
  imports:
    - sillysignin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rosskarchner/django-sillysignin
  license: Same license as Python
  summary: u"A username-only authentication app.  Also, unknown users get created automatically.  You probably don't want this in production."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
