package:
  name: pexpect-u
  version: "2.5.1"

source:
  fn: pexpect-u-2.5.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pexpect-u/pexpect-u-2.5.1.tar.gz
  md5: 7c916a9f42d636ec6aec39f7cdd96eb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pexpect-u = pexpect-u:main
    #
    # Would create an entry point called pexpect-u that calls pexpect-u.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pexpect
    - pexpect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pexpect.sourceforge.net/
  license: MIT License
  summary: 'Pexpect is a pure Python Expect. It allows easy control of other applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
