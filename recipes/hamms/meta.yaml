package:
  name: hamms
  version: "1.3"

source:
  fn: hamms-1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hamms/hamms-1.3.tar.gz
  md5: 1764c0fa7cd796da6ea5804441a98fb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hamms = hamms:main
    #
    # Would create an entry point called hamms that calls hamms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - httpbin
    - twisted

  run:
    - python
    - flask
    - httpbin
    - twisted

test:
  # Python imports
  imports:
    - hamms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevinburke/hamms
  license: UNKNOWN
  summary: 'Malformed servers to test your HTTP client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
