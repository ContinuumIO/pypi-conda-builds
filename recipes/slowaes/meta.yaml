package:
  name: slowaes
  version: "0.1a1"

source:
  fn: slowaes-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/s/slowaes/slowaes-0.1a1.tar.gz
  md5: eafee95a788a795403e972a35e80ce4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slowaes = slowaes:main
    #
    # Would create an entry point called slowaes that calls slowaes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - slowaes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/slowaes/
  license: Apache License, Version 2.0
  summary: 'implementation of AES in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
