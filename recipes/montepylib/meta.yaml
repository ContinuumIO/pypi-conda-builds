package:
  name: montepylib
  version: "0.1.0"

source:
  fn: montepylib-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/montepylib/montepylib-0.1.0.tar.gz
  md5: 2d976bd88fd110ab7666a0e267a53d7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - montepylib = montepylib:main
    #
    # Would create an entry point called montepylib that calls montepylib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas >=0.13.1

  run:
    - python
    - pandas >=0.13.1

test:
  # Python imports
  imports:
    - montepylib
    - montepylib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/artstr/montepylib
  license: BSD LICENSE
  summary: 'Tools and examples to analyse high-impact business decisions, and dodge many of the problems of using spreadsheets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
