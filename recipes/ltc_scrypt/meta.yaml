package:
  name: ltc_scrypt
  version: "1.0"

source:
  fn: ltc_scrypt-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ltc_scrypt/ltc_scrypt-1.0.tar.gz
  md5: 7d019c3c98f16eb466a272e518ffb014
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ltc_scrypt = ltc_scrypt:main
    #
    # Would create an entry point called ltc_scrypt that calls ltc_scrypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ltc_scrypt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Bindings for scrypt proof of work used by Litecoin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
