package:
  name: collective.behavior.vat
  version: "0.5"

source:
  fn: collective.behavior.vat-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.behavior.vat/collective.behavior.vat-0.5.zip
  md5: 92fa1b39f9ac51a1e5c478f975d5f04d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.behavior.vat = collective.behavior.vat:main
    #
    # Would create an entry point called collective.behavior.vat that calls collective.behavior.vat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.behavior
    - setuptools

  run:
    - python
    - setuptools
    - plone.behavior
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.behavior
    - collective.behavior.vat
    - collective.behavior.vat.Extensions
    - collective.behavior.vat.adapter
    - collective.behavior.vat.tests
    - collective.behavior.vat.tests.dexterity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.behavior.vat/
  license: BSD License
  summary: 'Adds VAT field to dexterity content type.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
