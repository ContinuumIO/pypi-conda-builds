package:
  name: binterlude
  version: "1.0b1"

source:
  fn: binterlude-1.0b1.zip
  url: https://pypi.python.org/packages/source/b/binterlude/binterlude-1.0b1.zip
  md5: 2615890149c3b21502561141dbed765e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - binterlude = binterlude:main
    #
    # Would create an entry point called binterlude that calls binterlude.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - binterlude

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tlyng/binterlude/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Provides an interactive shell aka console inside your doctest case.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
