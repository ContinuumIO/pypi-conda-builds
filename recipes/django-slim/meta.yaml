package:
  name: django-slim
  version: "0.7.5"

source:
  fn: django-slim-0.7.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-slim/django-slim-0.7.5.tar.gz
  md5: de0700a6ea742354b5d7d8e2294d6520
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-slim = django-slim:main
    #
    # Would create an entry point called django-slim that calls django-slim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.4.1
    - django-localeurl ==2.0.1

  run:
    - python
    - six >=1.4.1
    - django-localeurl ==2.0.1

test:
  # Python imports
  imports:
    - slim
    - slim.models
    - slim.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/barseghyanartur/django-slim
  license: GNU General Public License v2 (GPLv2) or GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Simple implementation of multi-lingual models for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
