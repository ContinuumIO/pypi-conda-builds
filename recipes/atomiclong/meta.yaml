package:
  name: atomiclong
  version: "0.1.1"

source:
  fn: atomiclong-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/atomiclong/atomiclong-0.1.1.tar.gz
  md5: 1cb5dac8191060069f34a13cb868ed4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomiclong = atomiclong:main
    #
    # Would create an entry point called atomiclong that calls atomiclong.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi

  run:
    - python
    - cffi

test:
  # Python imports
  imports:
    - atomiclong

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dreid/atomiclong
  license: MIT
  summary: 'An AtomicLong type using CFFI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
