package:
  name: bagit
  version: "1.5.2"

source:
  fn: bagit-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bagit/bagit-1.5.2.tar.gz
  md5: b5c5ab98eff0ebeeca2abec87424a38f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bagit = bagit:main
    #
    # Would create an entry point called bagit that calls bagit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bagit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://libraryofcongress.github.io/bagit-python/
  license: UNKNOWN
  summary: '\nThis package can be used to create BagIt style packages of\ndigital content for safe transmission and digital preservation.\nSee: http://en.wikipedia.org/wiki/BagIt for more details.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
