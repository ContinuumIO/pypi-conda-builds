package:
  name: silva.pas.base
  version: "3.0.4"

source:
  fn: silva.pas.base-3.0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.pas.base/silva.pas.base-3.0.4.tar.gz
  md5: 85dc3aa7f8662178510efd3378470718
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.pas.base = silva.pas.base:main
    #
    # Would create an entry point called silva.pas.base that calls silva.pas.base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.genericsetup >1.5.0
    - products.pluggableauthservice >=1.7.0
    - products.silva
    - five.grok
    - infrae.wsgi
    - grokcore.chameleon
    - setuptools
    - silva.core.cache
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.layout
    - silva.core.services
    - silva.core.smi
    - silva.core.views
    - silva.translations
    - silva.ui
    - zeam.form.silva
    - zope.cachedescriptors
    - zope.component
    - zope.datetime
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.session

  run:
    - python
    - setuptools
    - products.genericsetup >1.5.0
    - products.pluggableauthservice >=1.7.0
    - products.silva
    - five.grok
    - infrae.wsgi
    - grokcore.chameleon
    - setuptools
    - silva.core.cache
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.layout
    - silva.core.services
    - silva.core.smi
    - silva.core.views
    - silva.translations
    - silva.ui
    - zeam.form.silva
    - zope.cachedescriptors
    - zope.component
    - zope.datetime
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.session

test:
  # Python imports
  imports:
    - silva
    - silva.pas
    - silva.pas.base
    - silva.pas.base.plugins
    - silva.pas.base.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.pas.base
  license: BSD License
  summary: 'Authentication support for Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
