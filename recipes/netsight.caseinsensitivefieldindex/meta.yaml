package:
  name: netsight.caseinsensitivefieldindex
  version: "0.2dev-r3746"

source:
  fn: netsight.caseinsensitivefieldindex-0.2dev-r3746.tar.gz
  url: https://pypi.python.org/packages/source/n/netsight.caseinsensitivefieldindex/netsight.caseinsensitivefieldindex-0.2dev-r3746.tar.gz
  md5: 8d7dd12077c5190ea3e1f793efa953d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netsight.caseinsensitivefieldindex = netsight.caseinsensitivefieldindex:main
    #
    # Would create an entry point called netsight.caseinsensitivefieldindex that calls netsight.caseinsensitivefieldindex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - netsight
    - netsight.caseinsensitivefieldindex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.netsight.co.uk/
  license: BSD
  summary: "A case insensitive version of Zope's FieldIndex"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
