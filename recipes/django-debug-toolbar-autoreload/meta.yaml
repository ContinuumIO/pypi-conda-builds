package:
  name: django-debug-toolbar-autoreload
  version: "0.2.0"

source:
  fn: django-debug-toolbar-autoreload-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-toolbar-autoreload/django-debug-toolbar-autoreload-0.2.0.tar.gz
  md5: b0eae67dc4e1faf53443ed4bb340be0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-toolbar-autoreload = django-debug-toolbar-autoreload:main
    #
    # Would create an entry point called django-debug-toolbar-autoreload that calls django-debug-toolbar-autoreload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - debug_toolbar_autoreload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gregmuellegger/django-debug-toolbar-autoreload
  license: BSD License
  summary: 'Automatically reloads your browser when a template, css or javascript file was modified.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
