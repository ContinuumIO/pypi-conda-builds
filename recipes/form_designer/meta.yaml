package:
  name: form_designer
  version: "0.6.1"

source:
  fn: form_designer-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/f/form_designer/form_designer-0.6.1.tar.gz
  md5: 0d7327495da769ddac8986ad56452fec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - form_designer = form_designer:main
    #
    # Would create an entry point called form_designer that calls form_designer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - form_designer
    - form_designer.migrations
    - form_designer.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/feincms/form_designer/
  license: BSD License
  summary: 'Form Designer - a simple form designer for FeinCMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
