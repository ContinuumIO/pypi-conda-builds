package:
  name: medialog.tinymceplugins.helpmenu
  version: "0.2.8"

source:
  fn: medialog.tinymceplugins.helpmenu-0.2.8.zip
  url: https://pypi.python.org/packages/source/m/medialog.tinymceplugins.helpmenu/medialog.tinymceplugins.helpmenu-0.2.8.zip
  md5: b0718a41b794816f7b5ddec0bd47e4fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.tinymceplugins.helpmenu = medialog.tinymceplugins.helpmenu:main
    #
    # Would create an entry point called medialog.tinymceplugins.helpmenu that calls medialog.tinymceplugins.helpmenu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.tinymce

  run:
    - python
    - setuptools
    - setuptools
    - products.tinymce

test:
  # Python imports
  imports:
    - medialog
    - medialog.tinymceplugins
    - medialog.tinymceplugins.helpmenu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/medialog.tinymceplugins.helpmenu/
  license: GPL
  summary: 'Add on that displays help (how-tos) inside TinyMCE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
