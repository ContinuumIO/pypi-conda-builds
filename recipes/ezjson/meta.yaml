package:
  name: ezjson
  version: "1.0"

source:
  fn: ezjson-1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/ezjson/ezjson-1.0.tar.gz
  md5: 17abfc6ef07425f7c336f8721fdd1823
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezjson = ezjson:main
    #
    # Would create an entry point called ezjson that calls ezjson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ezjson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sacanix/ezjson
  license: BSD License
  summary: 'Ezjson (means easy json) is a simple python module to encode and decode json.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
