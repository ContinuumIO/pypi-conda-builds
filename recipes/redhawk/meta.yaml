package:
  name: redhawk
  version: "1.2.1"

source:
  fn: redhawk-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redhawk/redhawk-1.2.1.tar.gz
  md5: a39ed46b0f531b53b44f343e53832452
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redhawk = redhawk:main
    #
    # Would create an entry point called redhawk that calls redhawk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycparser >=2.02

  run:
    - python
    - pycparser >=2.02

test:
  # Python imports
  imports:
    - redhawk
    - redhawk.c
    - redhawk.c.utils
    - redhawk.common
    - redhawk.common.readers
    - redhawk.common.writers
    - redhawk.examples
    - redhawk.python
    - redhawk.python.utils
    - redhawk.scripts
    - redhawk.test
    - redhawk.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/redhawk/
  license: BSD License
  summary: 'An AST based navigation system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
