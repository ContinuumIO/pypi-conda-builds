package:
  name: collective.easyslider
  version: "1.4.1"

source:
  fn: collective.easyslider-1.4.1.zip
  url: https://pypi.python.org/packages/source/c/collective.easyslider/collective.easyslider-1.4.1.zip
  md5: 7b827c29390e6fa8edcfec27911a5f99
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.easyslider = collective.easyslider:main
    #
    # Would create an entry point called collective.easyslider that calls collective.easyslider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.easyslider
    - collective.easyslider.browser
    - collective.easyslider.portlets
    - collective.easyslider.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.easyslider/
  license: GPL
  summary: 'The product will allow you to apply an easyslider to any page with the ability to create each slide using a WYSIWYG editor. It also provides a slider view for Folders and Collections.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
