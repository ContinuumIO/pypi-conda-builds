package:
  name: django-cms-facetsearch
  version: "0.9.2"

source:
  fn: django-cms-facetsearch-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-facetsearch/django-cms-facetsearch-0.9.2.tar.gz
  md5: b0bf881a68c3d579a2998fe67072ca34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-facetsearch = django-cms-facetsearch:main
    #
    # Would create an entry point called django-cms-facetsearch that calls django-cms-facetsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cms_facetsearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fivethreeo/django-cms-facetsearch/
  license: UNKNOWN
  summary: 'Searching using language facets for django-cms pages and plugins.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
