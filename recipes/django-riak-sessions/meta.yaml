package:
  name: django-riak-sessions
  version: "0.0.4"

source:
  fn: django-riak-sessions-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-riak-sessions/django-riak-sessions-0.0.4.tar.gz
  md5: 185ac0be94abe69407fcc0505e2b863a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-riak-sessions = django-riak-sessions:main
    #
    # Would create an entry point called django-riak-sessions that calls django-riak-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - riak

  run:
    - python
    - riak

test:
  # Python imports
  imports:
    - riak_sessions
    - riak_sessions.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/flashingpumpkin/django-riak-sessions
  license: UNKNOWN
  summary: 'Riak session backend for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
