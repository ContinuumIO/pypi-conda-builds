package:
  name: humbledb
  version: "5.5.1"

source:
  fn: humbledb-5.5.1.tar.gz
  url: https://pypi.python.org/packages/source/h/humbledb/humbledb-5.5.1.tar.gz
  md5: d1ec21e43abe2a8815742d0e6cb1f9f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - humbledb = humbledb:main
    #
    # Would create an entry point called humbledb that calls humbledb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.0.1
    - pyconfig
    - pytool >=3.0.1

  run:
    - python
    - pymongo >=2.0.1
    - pyconfig
    - pytool >=3.0.1

test:
  # Python imports
  imports:
    - humbledb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'HumbleDB - MongoDB Object-Document Mapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
