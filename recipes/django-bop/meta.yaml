package:
  name: django-bop
  version: "0.3"

source:
  fn: django-bop-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bop/django-bop-0.3.tar.gz
  md5: 831a66d40fe4c8af1d43260193f12a37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bop = django-bop:main
    #
    # Would create an entry point called django-bop that calls django-bop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bop
    - bop.migrations
    - bop.templatetags
    - bop.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pterk/django-bop
  license: BSD
  summary: 'Basic Object-level Permissions in django (1.2+)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
