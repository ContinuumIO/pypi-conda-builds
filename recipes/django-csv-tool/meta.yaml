package:
  name: django-csv-tool
  version: "0.3.7"

source:
  fn: django-csv-tool-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-csv-tool/django-csv-tool-0.3.7.tar.gz
  md5: 8d04e7a85f2f401c97a19528bb4176a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-csv-tool = django-csv-tool:main
    #
    # Would create an entry point called django-csv-tool that calls django-csv-tool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - csv_tool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/freakypie/django-csv-tool
  license: UNKNOWN
  summary: 'Tool to help create CSV imports faster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
