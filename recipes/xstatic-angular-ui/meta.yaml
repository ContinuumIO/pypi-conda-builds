package:
  name: xstatic-angular-ui
  version: "0.4.0.1"

source:
  fn: XStatic-angular-ui-0.4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-angular-ui/XStatic-angular-ui-0.4.0.1.tar.gz
  md5: f522ce697b18b2968a321484f966c7ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-angular-ui = xstatic-angular-ui:main
    #
    # Would create an entry point called xstatic-angular-ui that calls xstatic-angular-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.angular_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/angular-ui/angular-ui
  license: (same as angular-ui)
  summary: 'angular-ui 0.4.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
