package:
  name: lisa-plugin-freebox
  version: "0.7.9.1"

source:
  fn: lisa-plugin-Freebox-0.7.9.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lisa-plugin-Freebox/lisa-plugin-Freebox-0.7.9.1.tar.gz
  md5: 50656f4f3c243d4c3001a34ca0cbf10d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lisa-plugin-freebox = lisa-plugin-freebox:main
    #
    # Would create an entry point called lisa-plugin-freebox that calls lisa-plugin-freebox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.2
    - django-tastypie >=0.11.0

  run:
    - python
    - django >=1.6.2
    - django-tastypie >=0.11.0

test:
  # Python imports
  imports:
    - lisa.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jfcjfc/LISA-PLUGINS-Freebox
  license: MIT License
  summary: 'LISA home automation system - Plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
