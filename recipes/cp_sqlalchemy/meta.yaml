package:
  name: cp_sqlalchemy
  version: "0.5.1"

source:
  fn: cp_sqlalchemy-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cp_sqlalchemy/cp_sqlalchemy-0.5.1.tar.gz
  md5: bc1c1ac037164c73a51c146c5348762a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cp_sqlalchemy = cp_sqlalchemy:main
    #
    # Would create an entry point called cp_sqlalchemy that calls cp_sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy
    - sqlalchemy

  run:
    - python
    - cherrypy
    - sqlalchemy

test:
  # Python imports
  imports:
    - cp_sqlalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionrock/cp_sqlalchemy
  license: BSD License
  summary: 'Use SQLAlchemy with CherryPy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
