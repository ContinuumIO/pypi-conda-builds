package:
  name: frescoext-genshi
  version: "0.2"

source:
  fn: frescoext-genshi-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/frescoext-genshi/frescoext-genshi-0.2.tar.gz
  md5: 57fa86267307a1ce614652a37c91db11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frescoext-genshi = frescoext-genshi:main
    #
    # Would create an entry point called frescoext-genshi that calls frescoext-genshi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fresco >=0.2.1
    - genshi >=0.6

  run:
    - python
    - fresco >=0.2.1
    - genshi >=0.6

test:
  # Python imports
  imports:
    - frescoext
    - frescoext.genshi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Genshi integration for fresco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
