package:
  name: cstruct
  version: "1.0"

source:
  fn: cstruct-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cstruct/cstruct-1.0.tar.gz
  md5: b12c1f3718875cafd52bc9222bd21dd5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cstruct = cstruct:main
    #
    # Would create an entry point called cstruct that calls cstruct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - cstruct
    - cstruct.examples
    - cstruct.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/andreax79/python-cstruct
  license: MIT
  summary: 'C-style structs for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
