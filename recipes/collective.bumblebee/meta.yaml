package:
  name: collective.bumblebee
  version: "1.0a3"

source:
  fn: collective.bumblebee-1.0a3.zip
  url: https://pypi.python.org/packages/source/c/collective.bumblebee/collective.bumblebee-1.0a3.zip
  md5: 9a6eefbf8725b390b0a6881ce17455f7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.bumblebee = collective.bumblebee:main
    #
    # Would create an entry point called collective.bumblebee that calls collective.bumblebee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bumblebee >=1.0a2
    - plone.transformchain
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - bumblebee >=1.0a2
    - plone.transformchain
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.bumblebee

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.bumblebee
  license: GPL
  summary: 'Integration of Bumblebee into plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
