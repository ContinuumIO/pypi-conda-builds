package:
  name: django-misaka
  version: "0.2.0"

source:
  fn: django-misaka-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-misaka/django-misaka-0.2.0.tar.gz
  md5: ff3aea3dcda6b991229f3d3a2dc3fc43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-misaka = django-misaka:main
    #
    # Would create an entry point called django-misaka that calls django-misaka.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - houdini.py
    - misaka
    - pygments

  run:
    - python
    - houdini.py
    - misaka
    - pygments

test:
  # Python imports
  imports:
    - django_misaka

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chiehtu/django-misaka/
  license: MIT License
  summary: 'A Django template tag for rendering Markdown (by Misaka Markdown parser).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
