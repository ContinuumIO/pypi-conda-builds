package:
  name: gocept.jsform
  version: "2.1.0"

source:
  fn: gocept.jsform-2.1.0.zip
  url: https://pypi.python.org/packages/source/g/gocept.jsform/gocept.jsform-2.1.0.zip
  md5: 93d1ff39e5b84a5d6aba062b68e56af8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.jsform = gocept.jsform:main
    #
    # Would create an entry point called gocept.jsform that calls gocept.jsform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic >=1.0a2
    - js.classy
    - js.jquery
    - js.handlebars
    - js.knockout >=3.1.0
    - cssmin
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic >=1.0a2
    - js.classy
    - js.jquery
    - js.handlebars
    - js.knockout >=3.1.0
    - cssmin
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.jsform
    - gocept.jsform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/gocept.jsform/
  license: Zope Public License
  summary: 'Next generation forms in javascript'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
