package:
  name: json-schema-validator
  version: "2.3.1"

source:
  fn: json-schema-validator-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/j/json-schema-validator/json-schema-validator-2.3.1.tar.gz
  md5: 6a49df75ff648b5944e270828a7b90d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-schema-validator = json-schema-validator:main
    #
    # Would create an entry point called json-schema-validator that calls json-schema-validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=2.0.9

  run:
    - python
    - simplejson >=2.0.9

test:
  # Python imports
  imports:
    - json_schema_validator
    - json_schema_validator.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zyga/json-schema-validator
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'JSON Schema Validator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
