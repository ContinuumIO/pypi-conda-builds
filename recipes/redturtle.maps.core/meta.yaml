package:
  name: redturtle.maps.core
  version: "0.2.1"

source:
  fn: redturtle.maps.core-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.maps.core/redturtle.maps.core-0.2.1.tar.gz
  md5: 600d8ebe352cff8059f66ff908f42ee7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.maps.core = redturtle.maps.core:main
    #
    # Would create an entry point called redturtle.maps.core that calls redturtle.maps.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.maps

  run:
    - python
    - setuptools
    - setuptools
    - products.maps

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.maps
    - redturtle.maps.core
    - redturtle.maps.core.browser
    - redturtle.maps.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle-maps.core
  license: GPL
  summary: "Every plone contents can be drawn on a Maps'map"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
