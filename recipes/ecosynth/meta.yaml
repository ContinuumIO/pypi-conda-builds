package:
  name: ecosynth
  version: "0.2.4"

source:
  fn: ecosynth-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/e/ecosynth/ecosynth-0.2.4.tar.gz
  md5: 358e920ba4d453af37278055db6c9812
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecosynth = ecosynth:main
    #
    # Would create an entry point called ecosynth that calls ecosynth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - matplotlib
    - utm
    - bottle
    - pillow
    - pysftp

  run:
    - python
    - numpy
    - scipy
    - matplotlib
    - utm
    - bottle
    - pillow
    - pysftp

test:
  # Python imports
  imports:
    - ecosynth
    - ecosynth.acquire
    - ecosynth.datasets
    - ecosynth.generate
    - ecosynth.generate.tests
    - ecosynth.gui
    - ecosynth.gui.static
    - ecosynth.gui.static.css
    - ecosynth.gui.static.fonts
    - ecosynth.gui.static.html
    - ecosynth.gui.static.js
    - ecosynth.postprocess
    - ecosynth.postprocess.tests
    - ecosynth.share

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ecosynth.org
  license: Creative Commons Attribution 4.0 International
  summary: 'Ecosynth Processing Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
