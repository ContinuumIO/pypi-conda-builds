package:
  name: kaeru
  version: "0.0.1"

source:
  fn: kaeru-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kaeru/kaeru-0.0.1.tar.gz
  md5: 682160c5ec3a364425151a492557dae0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kaeru = kaeru:main
    #
    # Would create an entry point called kaeru that calls kaeru.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - korg >=0.0.4
    - load >=0.0.3
    - plot >=0.0.3
    - rctl >=0.1.2

  run:
    - python
    - korg >=0.0.4
    - load >=0.0.3
    - plot >=0.0.3
    - rctl >=0.1.2

test:
  # Python imports
  imports:
    - kaeru

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaeru-repo/kaeru
  license: MIT License
  summary: 'Sample configuration used for performance testing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
