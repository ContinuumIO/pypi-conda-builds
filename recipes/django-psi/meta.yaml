package:
  name: django-psi
  version: "0.2.1"

source:
  fn: django-psi-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-psi/django-psi-0.2.1.tar.gz
  md5: 1ed4b03ffbc54ca06f09f6f2f21d0bdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-psi = django-psi:main
    #
    # Would create an entry point called django-psi that calls django-psi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - google-api-python-client ==1.2

  run:
    - python
    - django >=1.4
    - google-api-python-client ==1.2

test:
  # Python imports
  imports:
    - psi
    - psi.management
    - psi.management.commands
    - psi.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/montylounge/django-psi/
  license: MIT License
  summary: 'Google Pagespeed Insights for your Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
