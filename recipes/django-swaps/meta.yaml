package:
  name: django-swaps
  version: "0.5.3"

source:
  fn: django-swaps-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-swaps/django-swaps-0.5.3.tar.gz
  md5: e34909b8caba9908d10900c936d43cda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-swaps = django-swaps:main
    #
    # Would create an entry point called django-swaps that calls django-swaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - swaps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-swaps.googlecode.com/
  license: MIT
  summary: 'Django-swaps is a social application that allows users of a Django site to swap anything for anything with each other.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
