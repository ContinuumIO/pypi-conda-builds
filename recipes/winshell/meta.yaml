package:
  name: winshell
  version: "0.6"

source:
  fn: winshell-0.6.zip
  url: https://pypi.python.org/packages/source/w/winshell/winshell-0.6.zip
  md5: 378164955ff8511eaebc64fda8a04d77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - winshell = winshell:main
    #
    # Would create an entry point called winshell that calls winshell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - winshell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tjguk/winshell
  license: http://www.opensource.org/licenses/mit-license.php
  summary: 'Windows shell functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
