package:
  name: confduino
  version: "0.2.1"

source:
  fn: confduino-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/confduino/confduino-0.2.1.tar.gz
  md5: ca1f6ff02c20d9ad639db5a6ebed3972
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confduino = confduino:main
    #
    # Would create an entry point called confduino that calls confduino.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - entrypoint2
    - path.py
    - bunch
    - pyunpack
    - prettyprint

  run:
    - python
    - entrypoint2
    - path.py
    - bunch
    - pyunpack
    - prettyprint

test:
  # Python imports
  imports:
    - confduino
    - confduino.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ponty/confduino
  license: BSD License
  summary: 'Arduino software configurator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
