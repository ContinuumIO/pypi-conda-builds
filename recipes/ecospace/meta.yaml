package:
  name: ecospace
  version: "0.34dev"

source:
  fn: ecospace-0.34dev.tar.gz
  url: https://pypi.python.org/packages/source/e/ecospace/ecospace-0.34dev.tar.gz
  md5: 8a01c937d5dadf99c03e7c7f9ec69a80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecospace = ecospace:main
    #
    # Would create an entry point called ecospace that calls ecospace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ecospace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'ecospace\n========\n\nA library for develop applications / extensions based on "ecospace" engine\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
