package:
  name: cmsplugin_eviscape
  version: "0.9.3"

source:
  fn: cmsplugin_eviscape-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_eviscape/cmsplugin_eviscape-0.9.3.tar.gz
  md5: bd11902d87c845151ae68762b606fd8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_eviscape = cmsplugin_eviscape:main
    #
    # Would create an entry point called cmsplugin_eviscape that calls cmsplugin_eviscape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-cms >=2.2

  run:
    - python
    - django >=1.3
    - django-cms >=2.2

test:
  # Python imports
  imports:
    - cmsplugin_eviscape
    - cmsplugin_eviscape.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TESOBE/cmsplugin_eviscape
  license: BSD License
  summary: 'An Eviscape extension for Django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
