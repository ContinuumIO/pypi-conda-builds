package:
  name: plone.recipe.apache
  version: "0.3.2"

source:
  fn: plone.recipe.apache-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.apache/plone.recipe.apache-0.3.2.tar.gz
  md5: 58d03ef9b5078b60b0402387ab467895
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.apache = plone.recipe.apache:main
    #
    # Would create an entry point called plone.recipe.apache that calls plone.recipe.apache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.cmmi
    - cheetah >1.0,<=2.2.1

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.cmmi
    - cheetah >1.0,<=2.2.1

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.apache
    - plone.recipe.apache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/buildout/plone.recipe.apache
  license: Zope Public License
  summary: 'An zc buildout for build and configure apache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
