package:
  name: django-weeny
  version: "1.0.4"

source:
  fn: django-weeny-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-weeny/django-weeny-1.0.4.tar.gz
  md5: 2011282ae589ef690b7e37ed78326b74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-weeny = django-weeny:main
    #
    # Would create an entry point called django-weeny that calls django-weeny.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0
    - pyyaml
    - south
    - ua-parser
    - user-agents

  run:
    - python
    - django >=1.5.0
    - pyyaml
    - south
    - ua-parser
    - user-agents

test:
  # Python imports
  imports:
    - weeny
    - weeny.forms
    - weeny.migrations
    - weeny.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mootacom/django-weeny
  license: Apache Software License
  summary: 'Small and simple URL shortener app for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
