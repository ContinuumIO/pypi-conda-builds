package:
  name: zopeundo
  version: "4.0"

source:
  fn: ZopeUndo-4.0.zip
  url: https://pypi.python.org/packages/source/Z/ZopeUndo/ZopeUndo-4.0.zip
  md5: 7527b694f90c765d3e4ba1108f84e2c5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopeundo = zopeundo:main
    #
    # Would create an entry point called zopeundo that calls zopeundo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ZopeUndo
    - ZopeUndo.tests
    - zopeundo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ZopeUndo
  license: Zope Public License
  summary: 'ZODB undo support for Zope2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
