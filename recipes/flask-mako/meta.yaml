package:
  name: flask-mako
  version: "0.3"

source:
  fn: Flask-Mako-0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Mako/Flask-Mako-0.3.tar.gz
  md5: 28e789cc4dc2afd7e548d58d62d96767
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mako = flask-mako:main
    #
    # Would create an entry point called flask-mako that calls flask-mako.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - mako

  run:
    - python
    - flask
    - mako

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benselme/flask-mako
  license: BSD License
  summary: 'Mako templating support for Flask applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
