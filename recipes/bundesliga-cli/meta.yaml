package:
  name: bundesliga-cli
  version: "0.1.2"

source:
  fn: bundesliga-cli-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bundesliga-cli/bundesliga-cli-0.1.2.tar.gz
  md5: 08fc070b8aefdf477d8d17354bb9efed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bundesliga-cli = bundesliga-cli:main
    #
    # Would create an entry point called bundesliga-cli that calls bundesliga-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - click >=3.3.0
    - prettytable >=0.7.2

  run:
    - python
    - click >=3.3.0
    - prettytable >=0.7.2

test:
  # Python imports
  imports:
    - bundesliga

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hypebeast/bundesliga-cli
  license: bundle
  summary: 'Bundesliga results and stats for hackers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
