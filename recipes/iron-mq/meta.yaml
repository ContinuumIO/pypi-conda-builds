package:
  name: iron-mq
  version: "0.5"

source:
  fn: iron-mq-0.5.tar.gz
  url: https://pypi.python.org/packages/source/i/iron-mq/iron-mq-0.5.tar.gz
  md5: 1c9bd34061a5ec06a120fc6a36856f78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iron-mq = iron-mq:main
    #
    # Would create an entry point called iron-mq that calls iron-mq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iron_core

  run:
    - python
    - iron_core

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iron-io/iron_mq_python
  license: BSD License
  summary: 'Client library for IronMQ, a message queue in the cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
