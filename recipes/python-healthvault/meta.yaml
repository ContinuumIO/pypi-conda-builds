package:
  name: python-healthvault
  version: "0.1.5"

source:
  fn: python-healthvault-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/python-healthvault/python-healthvault-0.1.5.tar.gz
  md5: e99bff4c177cd82cc5b39b5192fde2f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-healthvault = python-healthvault:main
    #
    # Would create an entry point called python-healthvault that calls python-healthvault.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto >=2.6
    - sphinx

  run:
    - python
    - pycrypto >=2.6
    - sphinx

test:
  # Python imports
  imports:
    - healthvaultlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orcasgit/python-healthvault
  license: MIT License
  summary: 'Python library to access Microsoft Healthvault'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
