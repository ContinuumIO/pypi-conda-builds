package:
  name: collective.simpleeditbutton
  version: "1.0"

source:
  fn: collective.simpleeditbutton-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.simpleeditbutton/collective.simpleeditbutton-1.0.tar.gz
  md5: 9fc1f81046f0dbe6a51a154cb52413b8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.simpleeditbutton = collective.simpleeditbutton:main
    #
    # Would create an entry point called collective.simpleeditbutton that calls collective.simpleeditbutton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

test:
  # Python imports
  imports:
    - collective
    - collective.simpleeditbutton
    - collective.simpleeditbutton.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.simpleeditbutton
  license: GNU General Public License (GPL)
  summary: 'Adjusts the default permissions so that users with the Member role only see an edit button for items they are allowed to modify, rather than the full set of tabs and actions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
