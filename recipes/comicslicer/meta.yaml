package:
  name: comicslicer
  version: "0.0.1"

source:
  fn: comicslicer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/comicslicer/comicslicer-0.0.1.tar.gz
  md5: b9a472c8c88ba0502f5cccc6152ccd4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - comicslicer = comicslicer:main
    #
    # Would create an entry point called comicslicer that calls comicslicer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - comicslicer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arnehilmann/comicslicer
  license: GNU General Public License (GPL)
  summary: 'slices a comic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
