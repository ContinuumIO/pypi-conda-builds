package:
  name: django-sitemapper
  version: "1.0.0"

source:
  fn: django-sitemapper-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sitemapper/django-sitemapper-1.0.0.tar.gz
  md5: be2cf7eb8dfb21b7041d938b8fca0dd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sitemapper = django-sitemapper:main
    #
    # Would create an entry point called django-sitemapper that calls django-sitemapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - sitemapper
    - sitemapper.migrations
    - sitemapper.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mhurt/django-sitemapper/
  license: MIT License
  summary: 'A small Django app to manage sitemap.xml overrides on a per-object basis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
