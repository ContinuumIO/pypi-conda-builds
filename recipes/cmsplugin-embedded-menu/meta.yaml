package:
  name: cmsplugin-embedded-menu
  version: "0.0.7"

source:
  fn: cmsplugin-embedded-menu-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-embedded-menu/cmsplugin-embedded-menu-0.0.7.tar.gz
  md5: c593923aecebdd6f9b3f6682491e19fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-embedded-menu = cmsplugin-embedded-menu:main
    #
    # Would create an entry point called cmsplugin-embedded-menu that calls cmsplugin-embedded-menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms

  run:
    - python
    - django-cms

test:
  # Python imports
  imports:
    - cmsplugin_embeddedmenu
    - cmsplugin_embeddedmenu.lib
    - cmsplugin_embeddedmenu.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/airtonix/cmsplugin-embedded-menu
  license: BSD License
  summary: 'DjangoCMS plugin for embedding menus in placeholders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
