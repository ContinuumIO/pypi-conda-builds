package:
  name: ecreall.trashcan
  version: "1.6"

source:
  fn: ecreall.trashcan-1.6.zip
  url: https://pypi.python.org/packages/source/e/ecreall.trashcan/ecreall.trashcan-1.6.zip
  md5: 34d317b75e94f9fc6699b3902d99c99d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecreall.trashcan = ecreall.trashcan:main
    #
    # Would create an entry point called ecreall.trashcan that calls ecreall.trashcan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - plone >=3.3

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - plone >=3.3

test:
  # Python imports
  imports:
    - ecreall
    - ecreall.trashcan
    - ecreall.trashcan.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ecreall.trashcan
  license: GNU General Public License (GPL)
  summary: 'Trashcan for Plone. By Ecreall.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
