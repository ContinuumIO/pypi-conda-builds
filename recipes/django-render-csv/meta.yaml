package:
  name: django-render-csv
  version: "1.1.1"

source:
  fn: django-render-csv-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-render-csv/django-render-csv-1.1.1.tar.gz
  md5: 2e00f05e460627c6b835426a15630a1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-render-csv = django-render-csv:main
    #
    # Would create an entry point called django-render-csv that calls django-render-csv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/limijiaoyin/django-render-csv
  license: UNKNOWN
  summary: 'utilities to render csv in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
