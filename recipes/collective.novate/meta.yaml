package:
  name: collective.novate
  version: "1.0b4"

source:
  fn: collective.novate-1.0b4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.novate/collective.novate-1.0b4.tar.gz
  md5: 166210ffc238017575490c40760a2c18
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.novate = collective.novate:main
    #
    # Would create an entry point called collective.novate that calls collective.novate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.workflow
    - five.grok
    - plone.formwidget.autocomplete
    - plone.directives.form
    - plone.principalsource

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.workflow
    - five.grok
    - plone.formwidget.autocomplete
    - plone.directives.form
    - plone.principalsource

test:
  # Python imports
  imports:
    - collective
    - collective.novate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.novate
  license: GPL
  summary: 'Adds an option too reassign ownership on the sharing page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
