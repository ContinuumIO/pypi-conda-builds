package:
  name: collective.viewlet.navbar
  version: "0.1"

source:
  fn: collective.viewlet.navbar-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.viewlet.navbar/collective.viewlet.navbar-0.1.tar.gz
  md5: 8c19027805880901a4dd106de8958251
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.viewlet.navbar = collective.viewlet.navbar:main
    #
    # Would create an entry point called collective.viewlet.navbar that calls collective.viewlet.navbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.viewlet
    - collective.viewlet.navbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: GPL
  summary: 'Horizontal navigation in a viewlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
