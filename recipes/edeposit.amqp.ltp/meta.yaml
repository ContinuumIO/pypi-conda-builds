package:
  name: edeposit.amqp.ltp
  version: "0.9.2"

source:
  fn: edeposit.amqp.ltp-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/e/edeposit.amqp.ltp/edeposit.amqp.ltp-0.9.2.tar.gz
  md5: bce8b6517827dedefff45ed7a17ee4be
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edeposit.amqp.ltp = edeposit.amqp.ltp:main
    #
    # Would create an entry point called edeposit.amqp.ltp that calls edeposit.amqp.ltp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - pydhtmlparser
    - xmltodict
    - edeposit.amqp.aleph >=1.6.3

  run:
    - python
    - setuptools
    - lxml
    - pydhtmlparser
    - xmltodict
    - edeposit.amqp.aleph >=1.6.3

test:
  # Python imports
  imports:
    - edeposit
    - edeposit.amqp
    - edeposit.amqp.ltp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edeposit/edeposit.amqp.ltp/
  license: GNU General Public License v2 or later (GPLv2+)
  summary: "E-Deposit's AMQP binding to Long Time Preservation system."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
