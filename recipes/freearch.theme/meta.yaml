package:
  name: freearch.theme
  version: "1.0.2"

source:
  fn: freearch.theme-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/freearch.theme/freearch.theme-1.0.2.tar.gz
  md5: 3038cdbec87d7f7583ae2e0dafaf1055
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - freearch.theme = freearch.theme:main
    #
    # Would create an entry point called freearch.theme that calls freearch.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - freearch
    - freearch.theme
    - freearch.theme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: freearch
  license: GPL
  summary: 'Free Arch Theme for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
