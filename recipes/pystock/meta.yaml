package:
  name: pystock
  version: "0.0.8"

source:
  fn: pyStock-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/p/pyStock/pyStock-0.0.8.tar.gz
  md5: 446499a8b3d7ddaaee303ec13e4157cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystock = pystock:main
    #
    # Would create an entry point called pystock that calls pystock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - alembic
    - psycopg2

  run:
    - python
    - sqlalchemy
    - alembic
    - psycopg2

test:
  # Python imports
  imports:
    - pyStock
    - pyStock.scripts
    - pystock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.lazzaroleonardo.com.ar
  license: UNKNOWN
  summary: 'A stock market model for persistence using SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
