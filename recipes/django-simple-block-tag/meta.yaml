package:
  name: django-simple-block-tag
  version: "0.1.3"

source:
  fn: django-simple-block-tag-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-block-tag/django-simple-block-tag-0.1.3.tar.gz
  md5: d5ed57e16ce835756524f84f1897844e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-block-tag = django-simple-block-tag:main
    #
    # Would create an entry point called django-simple-block-tag that calls django-simple-block-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.0

  run:
    - python
    - django >=1.4.0

test:
  # Python imports
  imports:
    - simpleblocktag

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piha/django-simple-block-tag/
  license: LICENSE.txt
  summary: 'Block tag decorator for Django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
