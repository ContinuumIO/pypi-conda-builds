package:
  name: dogslow
  version: "0.9.7"

source:
  fn: dogslow-0.9.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dogslow/dogslow-0.9.7.tar.gz
  md5: 846c6bd82e5d8ac4876230e1cd8af14d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dogslow = dogslow:main
    #
    # Would create an entry point called dogslow that calls dogslow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dogslow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/evzijst/dogslow
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A Django middleware that logs tracebacks of slow requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
