package:
  name: bambu-attachments
  version: "2.0.1"

source:
  fn: bambu-attachments-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-attachments/bambu-attachments-2.0.1.tar.gz
  md5: 2464ef15929b423f69e83625ebe0217e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-attachments = bambu-attachments:main
    #
    # Would create an entry point called bambu-attachments that calls bambu-attachments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bambu_attachments
    - bambu_attachments.migrations
    - bambu_attachments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-attachments
  license: UNKNOWN
  summary: 'A setup for handling generic model attachments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
