package:
  name: django-standard-form
  version: "1.1.1"

source:
  fn: django-standard-form-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-standard-form/django-standard-form-1.1.1.tar.gz
  md5: e95f6e064080b4aed96f40e2ac871ea1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-standard-form = django-standard-form:main
    #
    # Would create an entry point called django-standard-form that calls django-standard-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-classy-tags >=0.3.3

  run:
    - python
    - django >=1.3
    - django-classy-tags >=0.3.3

test:
  # Python imports
  imports:
    - standard_form
    - standard_form.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alesdotio/django-standard-form
  license: BSD License
  summary: 'Quick and simple django templatetags for displaying forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
