package:
  name: hexahexacontadecimal
  version: "1.0"

source:
  fn: hexahexacontadecimal-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hexahexacontadecimal/hexahexacontadecimal-1.0.tar.gz
  md5: 5ba8eb3c27ce579a4eb621893d56412a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hexahexacontadecimal = hexahexacontadecimal:main
    #
    # Would create an entry point called hexahexacontadecimal that calls hexahexacontadecimal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hexahexacontadecimal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aljungberg/hexahexacontadecimal
  license: BSD License
  summary: 'Encode and decode hexahexacontadecimal numbers, a compact number representation for URLs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
