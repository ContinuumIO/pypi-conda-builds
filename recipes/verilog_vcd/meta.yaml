package:
  name: verilog_vcd
  version: "1.05"

source:
  fn: Verilog_VCD-1.05.tar.gz
  url: https://pypi.python.org/packages/source/V/Verilog_VCD/Verilog_VCD-1.05.tar.gz
  md5: b5ed595bae948529560fb18266200aa1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - verilog_vcd = verilog_vcd:main
    #
    # Would create an entry point called verilog_vcd that calls verilog_vcd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - Verilog_VCD
    - verilog_vcd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Verilog_VCD/
  license: UNKNOWN
  summary: 'Module to Parse VCD (Value-Change-Dump) files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
