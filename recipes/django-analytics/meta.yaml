package:
  name: django-analytics
  version: "0.0.1"

source:
  fn: django-analytics-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-analytics/django-analytics-0.0.1.tar.gz
  md5: 8f4654b7add1f480e34b164ac15afa25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-analytics = django-analytics:main
    #
    # Would create an entry point called django-analytics that calls django-analytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_geckoboard >=1.1.0

  run:
    - python
    - django_geckoboard >=1.1.0

test:
  # Python imports
  imports:
    - analytics
    - analytics.management
    - analytics.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/praekelt/django-analytics
  license: BSD License
  summary: 'Django app facilitating tracking of arbitrary simple metrics.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
