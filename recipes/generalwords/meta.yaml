package:
  name: generalwords
  version: "1.0"

source:
  fn: generalwords-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/generalwords/generalwords-1.0.tar.gz
  md5: f15b70c250a92dec407d8e86a1f75dde
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - generalwords = generalwords:main
    #
    # Would create an entry point called generalwords that calls generalwords.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - generalwords

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/petrilli/generalwords
  license: BSD License
  summary: 'A somewhat curated collection of words to use in nonce generation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
