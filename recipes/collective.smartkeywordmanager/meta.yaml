package:
  name: collective.smartkeywordmanager
  version: "0.6"

source:
  fn: collective.smartkeywordmanager-0.6.zip
  url: https://pypi.python.org/packages/source/c/collective.smartkeywordmanager/collective.smartkeywordmanager-0.6.zip
  md5: d3350d3574ab2f07fdfb46de9d415475
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.smartkeywordmanager = collective.smartkeywordmanager:main
    #
    # Would create an entry point called collective.smartkeywordmanager that calls collective.smartkeywordmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.smartkeywordmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Used to apply tags to objects in a collection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
