package:
  name: punchcard.py
  version: "0.2"

source:
  fn: punchcard.py-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/punchcard.py/punchcard.py-0.2.tar.gz
  md5: 342cdfcbef3046010df9d0649052eece
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - punchcard.py = punchcard.py:main
    #
    # Would create an entry point called punchcard.py that calls punchcard.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pygooglechart

  run:
    - python
    - pygooglechart

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aaronjorbin/punchcard.py
  license: UNKNOWN
  summary: 'Command line utility to create a punchcard graph'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
