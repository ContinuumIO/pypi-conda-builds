package:
  name: hgcampfire
  version: "0.3.2"

source:
  fn: hgcampfire-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hgcampfire/hgcampfire-0.3.2.tar.gz
  md5: c80a156bc9bda3c5aa93eacd2473ba70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgcampfire = hgcampfire:main
    #
    # Would create an entry point called hgcampfire that calls hgcampfire.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hgcampfire

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/markdrago/hgcampfire/
  license: GNU General Public License (GPL)
  summary: 'Mercurial hook to report incoming changesets to Campfire chatroom'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
