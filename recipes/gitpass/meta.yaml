package:
  name: gitpass
  version: "0.1.4"

source:
  fn: gitpass-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/g/gitpass/gitpass-0.1.4.tar.gz
  md5: 9a8d430f107e4ae72e217d9e281ee7ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitpass = gitpass:main
    #
    # Would create an entry point called gitpass that calls gitpass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gitpass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dasmith/gitpass
  license: UNKNOWN
  summary: "Gitpass is an extension of the Python standard library's getpass, designed for keeping passwords out of your git repository."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
