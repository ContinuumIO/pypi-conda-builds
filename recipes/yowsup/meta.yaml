package:
  name: yowsup
  version: "0.2"

source:
  fn: Yowsup-0.2.tar.gz
  url: https://pypi.python.org/packages/source/Y/Yowsup/Yowsup-0.2.tar.gz
  md5: 9b44e65b943d2fe03f2e12b1a6bb5daf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - yowsup = yowsup:main
    #
    # Would create an entry point called yowsup that calls yowsup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - Yowsup
    - Yowsup.Auth
    - Yowsup.Auth.mechanisms
    - Yowsup.Common
    - Yowsup.Common.Http
    - Yowsup.ConnectionIO
    - Yowsup.Contacts
    - Yowsup.Interfaces
    - Yowsup.Interfaces.DBus
    - Yowsup.Interfaces.Lib
    - Yowsup.Media
    - Yowsup.Registration
    - Yowsup.Registration.v1
    - Yowsup.Registration.v2
    - yowsup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jperelli/yowsup
  license: UNKNOWN
  summary: 'A library to use whatsapp from python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
