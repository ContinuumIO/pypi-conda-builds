package:
  name: macroecotools
  version: "0.3"

source:
  fn: macroecotools-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/macroecotools/macroecotools-0.3.tar.gz
  md5: b573225be78303e5bc80b599183d7ca4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - macroecotools = macroecotools:main
    #
    # Would create an entry point called macroecotools that calls macroecotools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6
    - scipy >=0.12
    - pandas >=0.13

  run:
    - python
    - numpy >=1.6
    - scipy >=0.12
    - pandas >=0.13

test:
  # Python imports
  imports:
    - macroeco_distributions
    - macroecotools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/weecology/macroecotools
  license: MIT
  summary: 'Tools for conducting macroecological analyses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
