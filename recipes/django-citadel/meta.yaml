package:
  name: django-citadel
  version: "0.1.3"

source:
  fn: django-citadel-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-citadel/django-citadel-0.1.3.tar.gz
  md5: 35b40291dbe68e3f49332e50cf419c46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-citadel = django-citadel:main
    #
    # Would create an entry point called django-citadel that calls django-citadel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto

  run:
    - python
    - pycrypto

test:
  # Python imports
  imports:
    - citadel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mypetyak/django-citadel
  license: UNKNOWN
  summary: 'A Django app that provides an encrypted Model and ModelField'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
