package:
  name: bufrpy
  version: "0.2.1"

source:
  fn: bufrpy-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bufrpy/bufrpy-0.2.1.tar.gz
  md5: cd375aac68368ba83b7048c94f2136d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bufrpy = bufrpy:main
    #
    # Would create an entry point called bufrpy that calls bufrpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bitstring >=3.1.2

  run:
    - python
    - bitstring >=3.1.2

test:
  # Python imports
  imports:
    - bufrpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tazle/bufrpy
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Pyre-Python BUFR decoder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
