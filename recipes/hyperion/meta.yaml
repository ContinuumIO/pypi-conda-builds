package:
  name: hyperion
  version: "0.9.6"

source:
  fn: Hyperion-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/H/Hyperion/Hyperion-0.9.6.tar.gz
  md5: 0c2a682e525f98be3ed3789f120a81ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyperion = hyperion:main
    #
    # Would create an entry point called hyperion that calls hyperion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hyperion
    - hyperion.conf
    - hyperion.conf.tests
    - hyperion.densities
    - hyperion.densities.tests
    - hyperion.dust
    - hyperion.dust.tests
    - hyperion.grid
    - hyperion.grid.tests
    - hyperion.importers
    - hyperion.importers.tests
    - hyperion.model
    - hyperion.model.tests
    - hyperion.sources
    - hyperion.sources.tests
    - hyperion.sphinx
    - hyperion.sphinx.ext
    - hyperion.testing
    - hyperion.util
    - hyperion.util.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hyperion-rt.org
  license: BSD
  summary: 'Monte-Carlo Radiative Transfer Code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
