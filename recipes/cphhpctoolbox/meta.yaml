package:
  name: cphhpctoolbox
  version: "0.0.2"

source:
  fn: cphhpctoolbox-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cphhpctoolbox/cphhpctoolbox-0.0.2.tar.gz
  md5: 868ef361bfe669e60cf6a0506db70475
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cphhpctoolbox = cphhpctoolbox:main
    #
    # Would create an entry point called cphhpctoolbox that calls cphhpctoolbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - pyopencl

  run:
    - python
    - numpy
    - pyopencl

test:
  # Python imports
  imports:
    - cphhpc
    - cphhpc/device
    - cphhpc/device/gpu
    - cphhpc/image
    - cphhpc/image/filter
    - cphhpc/signal
    - cphhpc/signal/convolution
    - cphhpctoolbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/cphhpc/
  license: GNU General Public License (GPL)
  summary: 'CPHHPC Toolbox is a set of high performance Python extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
