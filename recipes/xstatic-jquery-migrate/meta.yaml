package:
  name: xstatic-jquery-migrate
  version: "1.2.1.1"

source:
  fn: XStatic-JQuery-Migrate-1.2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-JQuery-Migrate/XStatic-JQuery-Migrate-1.2.1.1.tar.gz
  md5: 2dd39f9d6351aeaf129b33d4134ac6a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-jquery-migrate = xstatic-jquery-migrate:main
    #
    # Would create an entry point called xstatic-jquery-migrate that calls xstatic-jquery-migrate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.jquery_migrate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jquery/jquery-migrate
  license: (same as JQuery-Migrate)
  summary: 'JQuery-Migrate 1.2.1 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
