package:
  name: django-bigbrother
  version: "0.4.2"

source:
  fn: django-bigbrother-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bigbrother/django-bigbrother-0.4.2.tar.gz
  md5: 297b7366834dc911121a28822f7611b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bigbrother = django-bigbrother:main
    #
    # Would create an entry point called django-bigbrother that calls django-bigbrother.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - python-dateutil >=1.4,<2.0
    - django-qsstats-magic

  run:
    - python
    - django >=1.6
    - python-dateutil >=1.4,<2.0
    - django-qsstats-magic

test:
  # Python imports
  imports:
    - bigbrother
    - bigbrother.management
    - bigbrother.management.commands
    - bigbrother.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/anderspetersson/django-bigbrother
  license: UNKNOWN
  summary: 'Modular Dashboard for Django Projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
