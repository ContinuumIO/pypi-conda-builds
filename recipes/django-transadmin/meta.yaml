package:
  name: django-transadmin
  version: "0.2.8"

source:
  fn: django-transadmin-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-transadmin/django-transadmin-0.2.8.tar.gz
  md5: 7130331388e675318d7edcaf01851c40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-transadmin = django-transadmin:main
    #
    # Would create an entry point called django-transadmin that calls django-transadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - transadmin
    - transadmin.management
    - transadmin.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vinyll/django-transadmin
  license: BSD License
  summary: 'Translate texts from the admin interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
