package:
  name: db-obcuscate-id
  version: "0.1"

source:
  fn: db-obcuscate-id-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/db-obcuscate-id/db-obcuscate-id-0.1.tar.gz
  md5: 5c623e8438d93b0243f548acf33c8b3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - db-obcuscate-id = db-obcuscate-id:main
    #
    # Would create an entry point called db-obcuscate-id that calls db-obcuscate-id.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - db_obfuscate
    - db_obfuscate.management
    - db_obfuscate.management.commands
    - db_obfuscate.tests
    - db_obfuscate.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thisiscam/django-db_obfuscate-id
  license: The MIT License
  summary: 'A reusable Django app that psuedo encrypts your id from a database level.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
