package:
  name: django-sphinx-db
  version: "0.1-3"

source:
  fn: django-sphinx-db-0.1-3.tar.gz
  url: https://github.com/downloads/smartfile/django-sphinx-db/django-sphinx-db-0.1-3.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sphinx-db = django-sphinx-db:main
    #
    # Would create an entry point called django-sphinx-db that calls django-sphinx-db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_sphinx_db
    - django_sphinx_db.backend
    - django_sphinx_db.backend.sphinx
    - django_sphinx_db.management
    - django_sphinx_db.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smartfile/django-sphinx-db/
  license: MIT
  summary: 'Django database backend for SphinxQL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
