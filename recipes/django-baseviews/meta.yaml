package:
  name: django-baseviews
  version: "0.5"

source:
  fn: django-baseviews-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-baseviews/django-baseviews-0.5.tar.gz
  md5: 1f66fb237c6391e67a4aa19ede000275
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-baseviews = django-baseviews:main
    #
    # Would create an entry point called django-baseviews that calls django-baseviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - baseviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bkonkle/django-baseviews
  license: License :: OSI Approved :: BSD License
  summary: 'A small collection of Django view classes to build upon.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
