package:
  name: datacheck
  version: "0.0.2"

source:
  fn: datacheck-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/datacheck/datacheck-0.0.2.tar.gz
  md5: 546c2c34167b2239bc7270047c1edbc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datacheck = datacheck:main
    #
    # Would create an entry point called datacheck that calls datacheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - future ==0.14.3

  run:
    - python
    - future ==0.14.3

test:
  # Python imports
  imports:
    - datacheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/csdev/datacheck
  license: MIT License
  summary: 'data validation library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
