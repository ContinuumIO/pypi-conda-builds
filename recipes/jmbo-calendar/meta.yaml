package:
  name: jmbo-calendar
  version: "2.0.0a1"

source:
  fn: jmbo-calendar-2.0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-calendar/jmbo-calendar-2.0.0a1.tar.gz
  md5: 5b2b64885fcc6b40c94ecc4a9a5b391a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-calendar = jmbo-calendar:main
    #
    # Would create an entry point called jmbo-calendar that calls jmbo-calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-ckeditor >=4.0.2
    - jmbo ==2.0.0a2
    - django-atlas >=0.1

  run:
    - python
    - django-ckeditor >=4.0.2
    - jmbo ==2.0.0a2
    - django-atlas >=0.1

test:
  # Python imports
  imports:
    - cal
    - jmbo_calendar
    - jmbo_calendar.migrations
    - jmbo_calendar.templatetags
    - jmbo_calendar.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-calendar
  license: BSD License
  summary: 'Jmbo calendar app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
