package:
  name: collective.package
  version: "0.3.1"

source:
  fn: collective.package-0.3.1.zip
  url: https://pypi.python.org/packages/source/c/collective.package/collective.package-0.3.1.zip
  md5: 8dc9e7ccc30624da0440f3be852f079e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.package = collective.package:main
    #
    # Would create an entry point called collective.package that calls collective.package.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.package
    - collective.package.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://collective.github.com/collective.package/
  license: ZPL
  summary: 'Sample package to demonstrate development of         add-ons for the Plone CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
