package:
  name: simpleinput
  version: "0.1"

source:
  fn: simpleinput-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/simpleinput/simpleinput-0.1.tar.gz
  md5: 4093cfd308a868bd4c892392ee269a2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleinput = simpleinput:main
    #
    # Would create an entry point called simpleinput that calls simpleinput.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simpleinput

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: code.google.com/p/py-simpleinput
  license: GNU General Public License (GPL)
  summary: 'Request inpunts of specific types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
