package:
  name: products.plonesubskins
  version: "4.7.5.1"

source:
  fn: Products.PloneSubSkins-4.7.5.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PloneSubSkins/Products.PloneSubSkins-4.7.5.1.tar.gz
  md5: d329a0ddb46fcf6eb72a9cd7021d7d8d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.plonesubskins = products.plonesubskins:main
    #
    # Would create an entry point called products.plonesubskins that calls products.plonesubskins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - medialog.subskins

  run:
    - python
    - setuptools
    - setuptools
    - medialog.subskins

test:
  # Python imports
  imports:
    - Products
    - Products.PloneSubSkins
    - Products.PloneSubSkins.browser
    - Products.PloneSubSkins.tests
    - Products.PloneSubSkins.tests.testSkinsPackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/subskins
  license: ZPL
  summary: 'Manage Plone skins CSS, logo, and base colors dynamically from the Plone control panel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
