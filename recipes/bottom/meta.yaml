package:
  name: bottom
  version: "0.9.13"

source:
  fn: bottom-0.9.13.tar.gz
  url: https://pypi.python.org/packages/source/b/bottom/bottom-0.9.13.tar.gz
  md5: 8080e980a05e717a2cfd2e4fe69f8b9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottom = bottom:main
    #
    # Would create an entry point called bottom that calls bottom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplex

  run:
    - python
    - simplex

test:
  # Python imports
  imports:
    - bottom
    - bottom.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/numberoverzero/bottom
  license: MIT License
  summary: 'asyncio-based rfc2812-compliant IRC Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
