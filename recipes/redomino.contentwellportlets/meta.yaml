package:
  name: redomino.contentwellportlets
  version: "4.1.2"

source:
  fn: redomino.contentwellportlets-4.1.2.tar.gz
  url: https://pypi.python.org/packages/source/r/redomino.contentwellportlets/redomino.contentwellportlets-4.1.2.tar.gz
  md5: a9b3e8ea549c40bf7158d82ebcec163c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.contentwellportlets = redomino.contentwellportlets:main
    #
    # Would create an entry point called redomino.contentwellportlets that calls redomino.contentwellportlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone >=4.0b1

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone >=4.0b1

test:
  # Python imports
  imports:
    - redomino
    - redomino.contentwellportlets
    - redomino.contentwellportlets.browser
    - redomino.contentwellportlets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.contentwellportlets
  license: GPL
  summary: 'A Plone product that enables you to add portlets to the central column in a page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
