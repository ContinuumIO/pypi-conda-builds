package:
  name: mathgraph
  version: "0.0.5"

source:
  fn: mathgraph-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mathgraph/mathgraph-0.0.5.tar.gz
  md5: 5aa90fc9fa70b6b68afde3f586d6fbb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mathgraph = mathgraph:main
    #
    # Would create an entry point called mathgraph that calls mathgraph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - networkx
    - mathchem

  run:
    - python
    - numpy
    - networkx
    - mathchem

test:
  # Python imports
  imports:
    - mathgraph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/mathgraph
  license: MIT License
  summary: 'Mathgraph is an open source Python package for calculating topological indices and energy of graphs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
