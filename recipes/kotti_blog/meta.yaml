package:
  name: kotti_blog
  version: "0.4"

source:
  fn: kotti_blog-0.4.zip
  url: https://pypi.python.org/packages/source/k/kotti_blog/kotti_blog-0.4.zip
  md5: fa75e2206537dda77ea1ad9dcb6654c6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_blog = kotti_blog:main
    #
    # Would create an entry point called kotti_blog that calls kotti_blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti
    - kotti_settings >=0.1
    - js.jquery_infinite_ajax_scroll
    - python-dateutil

  run:
    - python
    - setuptools
    - kotti
    - kotti_settings >=0.1
    - js.jquery_infinite_ajax_scroll
    - python-dateutil

test:
  # Python imports
  imports:
    - kotti_blog
    - kotti_blog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/kotti_blog
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Add a simple blog to your Kotti site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
