package:
  name: django-request-mock
  version: "0.1.4"

source:
  fn: django-request-mock-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-request-mock/django-request-mock-0.1.4.tar.gz
  md5: b6de95431a907d44d5f2e562ccf8a9fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-request-mock = django-request-mock:main
    #
    # Would create an entry point called django-request-mock that calls django-request-mock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - request_mock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/valeriansaliou/django-request-mock
  license: MIT - http://opensource.org/licenses/mit-license.php
  summary: 'Create a Django request object that mocks a real one.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
