package:
  name: cherrypy-jsonrpcserver
  version: "0.3.1"

source:
  fn: cherrypy-jsonrpcserver-0.3.1.tar.gz
  url: https://github.com/gerold-penz/cherrypy-jsonrpcserver/raw/master/dist/cherrypy-jsonrpcserver-0.3.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherrypy-jsonrpcserver = cherrypy-jsonrpcserver:main
    #
    # Would create an entry point called cherrypy-jsonrpcserver that calls cherrypy-jsonrpcserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cpjsonrpcserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gerold-penz/cherrypy-jsonrpcserver
  license: GNU Library or Lesser General Public License (LGPL) or MIT License
  summary: 'Python CherryPy JSON-RPC-Server Tool - Einfach verwendbarer JSONRPC-Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
