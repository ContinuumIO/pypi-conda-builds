package:
  name: django-flatblocks
  version: "0.9.1"

source:
  fn: django-flatblocks-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flatblocks/django-flatblocks-0.9.1.tar.gz
  md5: 7ca3a796e5cb93f6db6e581ecf5f5fe0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flatblocks = django-flatblocks:main
    #
    # Would create an entry point called django-flatblocks that calls django-flatblocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flatblocks
    - flatblocks.management
    - flatblocks.management.commands
    - flatblocks.migrations
    - flatblocks.south_migrations
    - flatblocks.templatetags
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/funkybob/django-flatblocks/
  license: BSD License
  summary: 'django-flatblocks acts like django.contrib.flatpages but for parts of a page; like an editable help box you want show alongside the main content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
