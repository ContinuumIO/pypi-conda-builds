package:
  name: django-depiction
  version: "0.1.2"

source:
  fn: django-depiction-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-depiction/django-depiction-0.1.2.tar.gz
  md5: 816758358af10f9969562b86697c06e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-depiction = django-depiction:main
    #
    # Would create an entry point called django-depiction that calls django-depiction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyprof2calltree ==1.1.0

  run:
    - python
    - pyprof2calltree ==1.1.0

test:
  # Python imports
  imports:
    - depiction
    - depiction.tests
    - depiction.tests.fixtures
    - depiction.tests.fixtures.djangoproject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/robmadole/django-depiction
  license: BSD License
  summary: 'Tooling for profiling Django applications, inspired by David Cramer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
