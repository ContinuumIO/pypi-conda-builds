package:
  name: django-html-field
  version: "0.1.3"

source:
  fn: django-html-field-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-html-field/django-html-field-0.1.3.tar.gz
  md5: 863b6dcecf97c669454ae80b42663334
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-html-field = django-html-field:main
    #
    # Would create an entry point called django-html-field that calls django-html-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - html_field
    - html_field.db
    - html_field.db.models
    - html_field.forms
    - html_field.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/gabrielgrant/django-html-field/
  license: LGPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
