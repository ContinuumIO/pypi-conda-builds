package:
  name: doac
  version: "0.2.0"

source:
  fn: doac-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/doac/doac-0.2.0.tar.gz
  md5: 67812f293b290260456ee9b86d5925bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doac = doac:main
    #
    # Would create an entry point called doac that calls doac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - simplejson >=1.5

  run:
    - python
    - django >=1.3
    - simplejson >=1.5

test:
  # Python imports
  imports:
    - doac
    - doac.contrib
    - doac.contrib.rest_framework
    - doac.exceptions
    - doac.handlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevin-brown/django-oauth2
  license: MIT License
  summary: 'An OAuth2 Consumer application for your Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
