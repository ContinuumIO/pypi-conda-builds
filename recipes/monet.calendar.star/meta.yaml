package:
  name: monet.calendar.star
  version: "2.0.0"

source:
  fn: monet.calendar.star-2.0.0.zip
  url: https://pypi.python.org/packages/source/m/monet.calendar.star/monet.calendar.star-2.0.0.zip
  md5: 4339162ea0b867246f56dbcfaac74742
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monet.calendar.star = monet.calendar.star:main
    #
    # Would create an entry point called monet.calendar.star that calls monet.calendar.star.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone >4.0b1
    - monet.calendar.event >=0.5.0
    - monet.calendar.extensions >=0.10.0
    - monet.calendar.portlet >=0.4.0
    - monet.calendar.criteria

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone >4.0b1
    - monet.calendar.event >=0.5.0
    - monet.calendar.extensions >=0.10.0
    - monet.calendar.portlet >=0.4.0
    - monet.calendar.criteria

test:
  # Python imports
  imports:
    - monet
    - monet.calendar
    - monet.calendar.star

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/monet.calendar.star
  license: GPL
  summary: 'A complete and modular site calendar application for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
