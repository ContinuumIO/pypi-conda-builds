package:
  name: aiodns
  version: "0.3.2"

source:
  fn: aiodns-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aiodns/aiodns-0.3.2.tar.gz
  md5: 07d22af802c0b1174010cd213763272d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiodns = aiodns:main
    #
    # Would create an entry point called aiodns that calls aiodns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycares
    - trollius

  run:
    - python
    - pycares
    - trollius

test:
  # Python imports
  imports:
    - aiodns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/saghul/aiodns
  license: MIT License
  summary: 'Simple DNS resolver for asyncio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
