package:
  name: django-datapurge
  version: "0.0.1"

source:
  fn: django-datapurge-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-datapurge/django-datapurge-0.0.1.tar.gz
  md5: 42362ba573f5ed3528a82fb439a7ea99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-datapurge = django-datapurge:main
    #
    # Would create an entry point called django-datapurge that calls django-datapurge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datapurge
    - datapurge.management
    - datapurge.management.commands
    - datapurge.purge
    - datapurge.runtests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A simple Django app to easily handle cleanup of old data (sessions, nonces, etc.)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
