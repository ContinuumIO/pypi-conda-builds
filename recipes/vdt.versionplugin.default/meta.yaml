package:
  name: vdt.versionplugin.default
  version: "0.0.2"

source:
  fn: vdt.versionplugin.default-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/v/vdt.versionplugin.default/vdt.versionplugin.default-0.0.2.tar.gz
  md5: 2dddbb86ace1ecdda93e5bc3a7c47b2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vdt.versionplugin.default = vdt.versionplugin.default:main
    #
    # Would create an entry point called vdt.versionplugin.default that calls vdt.versionplugin.default.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vdt.version

  run:
    - python
    - setuptools
    - vdt.version

test:
  # Python imports
  imports:
    - vdt
    - vdt.versionplugin
    - vdt.versionplugin.default

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Default Version Increment Plugin for GIT'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
