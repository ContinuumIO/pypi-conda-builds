package:
  name: sqlalchemy-wrapper
  version: "1.4.0"

source:
  fn: SQLAlchemy-Wrapper-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Wrapper/SQLAlchemy-Wrapper-1.4.0.tar.gz
  md5: 1d783f1b8539270754a061c37a1452db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-wrapper = sqlalchemy-wrapper:main
    #
    # Would create an entry point called sqlalchemy-wrapper that calls sqlalchemy-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - inflection >=0.2.0
    - sqlalchemy >=0.8

  run:
    - python
    - inflection >=0.2.0
    - sqlalchemy >=0.8

test:
  # Python imports
  imports:
    - sqlalchemy_wrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lucuma/sqlalchemy-wrapper
  license: BSD License
  summary: 'A framework-independent wrapper for SQLAlchemy that makes it really easy to use'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
