package:
  name: django-tastypie-swagger-python3
  version: "0.1.3"

source:
  fn: django-tastypie-swagger-python3-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tastypie-swagger-python3/django-tastypie-swagger-python3-0.1.3.tar.gz
  md5: 685d4af5d37451903b440924dc089019
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tastypie-swagger-python3 = django-tastypie-swagger-python3:main
    #
    # Would create an entry point called django-tastypie-swagger-python3 that calls django-tastypie-swagger-python3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tastypie_swagger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/williamfligor/django-tastypie-swagger
  license: BSD
  summary: 'An adapter to use swagger-ui with django-tastypie'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
