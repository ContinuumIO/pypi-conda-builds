package:
  name: python-twitter
  version: "2.2"

source:
  fn: python-twitter-2.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-twitter/python-twitter-2.2.tar.gz
  md5: eab697b3a9bc6bf3432d09a489f4ed8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-twitter = python-twitter:main
    #
    # Would create an entry point called python-twitter that calls python-twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - requests-oauthlib

  run:
    - python
    - requests
    - requests-oauthlib

test:
  # Python imports
  imports:
    - twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bear/python-twitter
  license: Apache Software License
  summary: 'A Python wrapper around the Twitter API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
