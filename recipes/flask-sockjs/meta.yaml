package:
  name: flask-sockjs
  version: "0.1.0"

source:
  fn: Flask-SockJS-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SockJS/Flask-SockJS-0.1.0.tar.gz
  md5: 63df2717bc0661102d2c31c45f1871f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sockjs = flask-sockjs:main
    #
    # Would create an entry point called flask-sockjs that calls flask-sockjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - twisted
    - flask-twisted
    - txsockjs
    - observable

  run:
    - python
    - flask
    - twisted
    - flask-twisted
    - txsockjs
    - observable

test:
  # Python imports
  imports:
    - flask_sockjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cravler/flask-sockjs/
  license: MIT License
  summary: 'Simple integration of Flask and SockJS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
