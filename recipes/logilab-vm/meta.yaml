package:
  name: logilab-vm
  version: "0.2.0"

source:
  fn: logilab-vm-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logilab-vm/logilab-vm-0.2.0.tar.gz
  md5: 3d8dc434dba82e2903482c0552df0d3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logilab-vm = logilab-vm:main
    #
    # Would create an entry point called logilab-vm that calls logilab-vm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logilab-vm
    - logilab-vm.hmi
    - logilab-vm.lib
    - logilab-vm.lib.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.logilab.org/project/logilab-vm
  license: GPL
  summary: 'useful miscellaneous modules used by Logilab projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
