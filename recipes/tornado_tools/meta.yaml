package:
  name: tornado_tools
  version: "0.3.0"

source:
  fn: tornado_tools-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_tools/tornado_tools-0.3.0.tar.gz
  md5: ca63dab670706ba31882f63f55318825
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_tools = tornado_tools:main
    #
    # Would create an entry point called tornado_tools that calls tornado_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tornado_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sitnin
  license: Apache Software License
  summary: 'Classes and functions for extending Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
