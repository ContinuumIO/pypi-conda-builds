package:
  name: icnews.acquire
  version: "0.9-dev-r371"

source:
  fn: icnews.acquire-0.9-dev-r371.tar.gz
  url: https://pypi.python.org/packages/source/i/icnews.acquire/icnews.acquire-0.9-dev-r371.tar.gz
  md5: d0e3bc571ac442118fa618c0bca57d0d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icnews.acquire = icnews.acquire:main
    #
    # Would create an entry point called icnews.acquire that calls icnews.acquire.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - icnews
    - icnews.acquire
    - icnews.acquire.Extensions
    - icnews.acquire.browser
    - icnews.acquire.content
    - icnews.acquire.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.platecom.com/productos/icnews.acquire/
  license: GNU General Public License (GPL)
  summary: 'icNews.Acquire'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
