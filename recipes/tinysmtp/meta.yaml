package:
  name: tinysmtp
  version: "0.1.2"

source:
  fn: tinysmtp-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tinysmtp/tinysmtp-0.1.2.tar.gz
  md5: 7938899776a871baac8d2c5a620fcc95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tinysmtp = tinysmtp:main
    #
    # Would create an entry point called tinysmtp that calls tinysmtp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - envparse

  run:
    - python
    - envparse

test:
  # Python imports
  imports:
    - tinysmtp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rconradharris/tinysmtp
  license: BSD License
  summary: 'Basically Flask-Mail without the Flask part'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
