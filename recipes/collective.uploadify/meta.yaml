package:
  name: collective.uploadify
  version: "1.2"

source:
  fn: collective.uploadify-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.uploadify/collective.uploadify-1.2.tar.gz
  md5: 0599cf230e07a982074c541a5561ca46
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.uploadify = collective.uploadify:main
    #
    # Would create an entry point called collective.uploadify that calls collective.uploadify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.uploadify
    - collective.uploadify.browser
    - collective.uploadify.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.uploadify
  license: GPL
  summary: 'Multi File Upload for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
