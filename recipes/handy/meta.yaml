package:
  name: handy
  version: "0.4"

source:
  fn: handy-0.4.tar.gz
  url: https://pypi.python.org/packages/source/h/handy/handy-0.4.tar.gz
  md5: 7164c4105ed45ed9e3db09526432f1ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - handy = handy:main
    #
    # Would create an entry point called handy that calls handy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - pytz >=2012j

  run:
    - python
    - django >=1.2
    - pytz >=2012j

test:
  # Python imports
  imports:
    - handy
    - handy.forms
    - handy.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Suor/handy
  license: BSD License
  summary: 'A collection of tools to make your django life easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
