package:
  name: django-ses
  version: "0.7.0"

source:
  fn: django-ses-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ses/django-ses-0.7.0.tar.gz
  md5: da730a7571b7a94bbd69ccd70b7e8bed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ses = django-ses:main
    #
    # Would create an entry point called django-ses that calls django-ses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.1.0

  run:
    - python
    - boto >=2.1.0

test:
  # Python imports
  imports:
    - django_ses
    - django_ses.management
    - django_ses.management.commands
    - django_ses.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hmarr/django-ses/
  license: MIT License
  summary: "A Django email backend for Amazon's Simple Email Service"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
