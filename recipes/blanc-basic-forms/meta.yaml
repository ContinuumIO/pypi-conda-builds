package:
  name: blanc-basic-forms
  version: "0.2"

source:
  fn: blanc-basic-forms-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/blanc-basic-forms/blanc-basic-forms-0.2.tar.gz
  md5: 0f19c6ba5afab8c35cff2fab3a1c07c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blanc-basic-forms = blanc-basic-forms:main
    #
    # Would create an entry point called blanc-basic-forms that calls blanc-basic-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - blanc_basic_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.blanctools.com/
  license: BSD License
  summary: 'Blanc Basic Forms for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
