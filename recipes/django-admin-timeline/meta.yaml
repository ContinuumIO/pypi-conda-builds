package:
  name: django-admin-timeline
  version: "1.5.1"

source:
  fn: django-admin-timeline-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-timeline/django-admin-timeline-1.5.1.tar.gz
  md5: a450cdc0951f5738809e1180c1af1d48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-timeline = django-admin-timeline:main
    #
    # Would create an entry point called django-admin-timeline that calls django-admin-timeline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-nine >=0.1.1

  run:
    - python
    - django >=1.4
    - django-nine >=0.1.1

test:
  # Python imports
  imports:
    - admin_timeline
    - admin_timeline.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/barseghyanartur/django-admin-timeline
  license: GNU General Public License v2 (GPLv2) or GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Facebook-like timeline for Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
