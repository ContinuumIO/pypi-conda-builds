package:
  name: jsonklog
  version: "0.15.0"

source:
  fn: jsonklog-0.15.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonklog/jsonklog-0.15.0.tar.gz
  md5: 9a9632a1d897ebab52327cf47bd3cba6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonklog = jsonklog:main
    #
    # Would create an entry point called jsonklog that calls jsonklog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - anyjson
    - pymongo
    - requests

  run:
    - python
    - setuptools
    - anyjson
    - pymongo
    - requests

test:
  # Python imports
  imports:
    - jsonklog
    - jsonklog.handlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/neogenix/jsonklog
  license: Apache Software License
  summary: 'JSON Logging Library with Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
