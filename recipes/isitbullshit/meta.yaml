package:
  name: isitbullshit
  version: "0.2.1"

source:
  fn: isitbullshit-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/isitbullshit/isitbullshit-0.2.1.tar.gz
  md5: 46516f5e3393d990bb0486f93fdb0746
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isitbullshit = isitbullshit:main
    #
    # Would create an entry point called isitbullshit that calls isitbullshit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - isitbullshit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/9seconds/isitbullshit/
  license: MIT License
  summary: 'Small library for verifying parsed JSONs if they are bullshit or not'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
