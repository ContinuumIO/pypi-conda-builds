package:
  name: cubicweb-skillmat
  version: "0.9.0"

source:
  fn: cubicweb-skillmat-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-skillmat/cubicweb-skillmat-0.9.0.tar.gz
  md5: 6a64f8d07ca281301309a6944723bc5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-skillmat = cubicweb-skillmat:main
    #
    # Would create an entry point called cubicweb-skillmat that calls cubicweb-skillmat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.10.0
    - cubicweb-comment
    - cubicweb-folder

  run:
    - python
    - cubicweb >=3.10.0
    - cubicweb-comment
    - cubicweb-folder

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-skillmat
  license: LGPL
  summary: 'skill matrix component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
