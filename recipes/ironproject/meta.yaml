package:
  name: ironproject
  version: "0.1"

source:
  fn: ironproject-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ironproject/ironproject-0.1.tar.gz
  md5: 95206b7ed7b481b40b6314b0a8902639
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ironproject = ironproject:main
    #
    # Would create an entry point called ironproject that calls ironproject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ironproject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python-ironproject.org
  license: GNU General Public License v3 (GPLv3)
  summary: 'Interaccion dinamica con la computadora mediante gestos y voz'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
