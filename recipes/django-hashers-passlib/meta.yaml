package:
  name: django-hashers-passlib
  version: "0.2"

source:
  fn: django-hashers-passlib-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hashers-passlib/django-hashers-passlib-0.2.tar.gz
  md5: 3b83e71f6823ad9d158fdac2e01bc253
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hashers-passlib = django-hashers-passlib:main
    #
    # Would create an entry point called django-hashers-passlib that calls django-hashers-passlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - passlib >=1.6.2
    - django >=1.5

  run:
    - python
    - passlib >=1.6.2
    - django >=1.5

test:
  # Python imports
  imports:
    - hashers_passlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mathiasertl/django-hashers-passlib
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Django hashers using passlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
