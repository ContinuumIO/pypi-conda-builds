package:
  name: coopr.gdp
  version: "1.2"

source:
  fn: coopr.gdp-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.gdp/coopr.gdp-1.2.tar.gz
  md5: f1cfa88cf9bec612215ff6f88f11042d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.gdp = coopr.gdp:main
    #
    # Would create an entry point called coopr.gdp that calls coopr.gdp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.gdp
    - coopr.gdp.plugins
    - coopr.gdp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.gdp
  license: BSD License
  summary: 'Generalized Disjunctive Programming (GDP) extensions to the Pyomo environment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
