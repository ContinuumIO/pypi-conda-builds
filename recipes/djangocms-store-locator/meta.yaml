package:
  name: djangocms-store-locator
  version: "0.1.3"

source:
  fn: djangocms-store-locator-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-store-locator/djangocms-store-locator-0.1.3.tar.gz
  md5: fc80cd5ea02c57f1558064107ac4f997
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-store-locator = djangocms-store-locator:main
    #
    # Would create an entry point called djangocms-store-locator that calls djangocms-store-locator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-filer >=0.8
    - django-sekizai >=0.6.1

  run:
    - python
    - django-filer >=0.8
    - django-sekizai >=0.6.1

test:
  # Python imports
  imports:
    - djangocms_store_locator
    - djangocms_store_locator.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.django-cms.org/
  license: BSD License
  summary: 'A simple store locator django CMS plugin that uses Google Maps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
