package:
  name: torasync
  version: "0.0.0.1"

source:
  fn: torasync-0.0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/torasync/torasync-0.0.0.1.tar.gz
  md5: d72878c0c37f23b7f7d9bb4147027aa5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torasync = torasync:main
    #
    # Would create an entry point called torasync that calls torasync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=2.4.1

  run:
    - python
    - tornado >=2.4.1

test:
  # Python imports
  imports:
    - torasync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ipconfiger/torasync
  license: LGPL
  summary: 'Run task asynchronously in other processes in easy way'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
