package:
  name: jobmanager-common
  version: "0.2.2"

source:
  fn: jobmanager-common-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jobmanager-common/jobmanager-common-0.2.2.tar.gz
  md5: f0115f7c06e362a19099998aa54bfebe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobmanager-common = jobmanager-common:main
    #
    # Would create an entry point called jobmanager-common that calls jobmanager-common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tbx
    - six >=1.8.0
    - pymongo <3.0
    - mongoengine
    - blinker

  run:
    - python
    - tbx
    - six >=1.8.0
    - pymongo <3.0
    - mongoengine
    - blinker

test:
  # Python imports
  imports:
    - jobmanager
    - jobmanager.common

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ronhanson/python-jobmanager-common
  license: MIT License
  summary: 'Job Manager Common Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
