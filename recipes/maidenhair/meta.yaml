package:
  name: maidenhair
  version: "0.3.5"

source:
  fn: maidenhair-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/m/maidenhair/maidenhair-0.3.5.tar.gz
  md5: 9e76f783999cc1ed8b3fba60a4015959
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maidenhair = maidenhair:main
    #
    # Would create an entry point called maidenhair that calls maidenhair.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - natsort
    - bunch
    - app_version
    - ordereddict

  run:
    - python
    - setuptools
    - natsort
    - bunch
    - app_version
    - ordereddict

test:
  # Python imports
  imports:
    - maidenhair
    - maidenhair.classification
    - maidenhair.filters
    - maidenhair.loaders
    - maidenhair.parsers
    - maidenhair.statistics
    - maidenhair.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/maidenhair
  license: MIT License
  summary: 'Convert raw text data files into a single excel file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
