package:
  name: eagle-maemo
  version: "0.7"

source:
  fn: eagle-maemo-0.7.tar.gz
  url: https://pypi.python.org/packages/source/e/eagle-maemo/eagle-maemo-0.7.tar.gz
  md5: 9b54db5db124163e0575bdcc83d02d3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eagle-maemo = eagle-maemo:main
    #
    # Would create an entry point called eagle-maemo that calls eagle-maemo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gustavobarbieri.com.br/eagle/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Eagle is an abstraction layer atop Graphical Toolkits focused on making simple applications easy to build while powerful in features.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
