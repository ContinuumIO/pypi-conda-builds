package:
  name: headintheclouds
  version: "0.5.2"

source:
  fn: headintheclouds-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/h/headintheclouds/headintheclouds-0.5.2.tar.gz
  md5: 94ef96c0e3157c7f3805080418d49c05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - headintheclouds = headintheclouds:main
    #
    # Would create an entry point called headintheclouds that calls headintheclouds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.6.1
    - pyyaml ==3.10
    - boto ==2.9.8
    - dop ==0.1.6
    - python-dateutil ==2.1
    - simplejson
    - envtpl ==0.3.2

  run:
    - python
    - fabric >=1.6.1
    - pyyaml ==3.10
    - boto ==2.9.8
    - dop ==0.1.6
    - python-dateutil ==2.1
    - simplejson
    - envtpl ==0.3.2

test:
  # Python imports
  imports:
    - headintheclouds
    - headintheclouds.dependencies
    - headintheclouds.dependencies.PyDbLite
    - headintheclouds.ensemble

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andreasjansson/head-in-the-clouds
  license: GNU GPLv3
  summary: 'Provider-agnostic cloud provisioning and Docker orchestration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
