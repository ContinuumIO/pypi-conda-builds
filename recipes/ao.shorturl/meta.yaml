package:
  name: ao.shorturl
  version: "1.1.7"

source:
  fn: ao.shorturl-1.1.7.tar.gz
  url: https://pypi.python.org/packages/source/a/ao.shorturl/ao.shorturl-1.1.7.tar.gz
  md5: a806320bf91c1ddd6118a59138adc384
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ao.shorturl = ao.shorturl:main
    #
    # Would create an entry point called ao.shorturl that calls ao.shorturl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ao
    - ao.shorturl
    - ao.shorturl.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aatiis/ao.shorturl
  license: GNU General Public License (GPL)
  summary: 'Reusable url shortener and lookup library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
