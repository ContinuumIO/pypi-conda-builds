package:
  name: seantis.web
  version: "1.2"

source:
  fn: seantis.web-1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/seantis.web/seantis.web-1.2.tar.gz
  md5: 95f0fa84665b5354c4653ebd18ea95ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - seantis.web = seantis.web:main
    #
    # Would create an entry point called seantis.web that calls seantis.web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming
    - plone.app.themingplugins
    - products.contentwellportlets
    - collective.portlet.embed

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming
    - plone.app.themingplugins
    - products.contentwellportlets
    - collective.portlet.embed

test:
  # Python imports
  imports:
    - seantis
    - seantis.web
    - seantis.web.portlets
    - seantis.web.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/seantis/seantis.web
  license: gpl
  summary: 'The seantis website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
