package:
  name: zope.app.annotation
  version: "3.4.0"

source:
  fn: zope.app.annotation-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.annotation/zope.app.annotation-3.4.0.tar.gz
  md5: 4c0379de373c6d4f18ab1d7166596a50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.annotation = zope.app.annotation:main
    #
    # Would create an entry point called zope.app.annotation that calls zope.app.annotation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.annotation
    - zope.interface
    - zope.deprecation

  run:
    - python
    - zope.annotation
    - zope.interface
    - zope.deprecation

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.annotation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.annotation
  license: Zope Public License
  summary: 'Zope Annotations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
