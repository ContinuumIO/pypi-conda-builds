package:
  name: guesslanguage
  version: "0.2.1"

source:
  fn: guesslanguage-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/guesslanguage/guesslanguage-0.2.1.tar.gz
  md5: 301a979aa47e9dbe82bb0a354c8bafbe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guesslanguage = guesslanguage:main
    #
    # Would create an entry point called guesslanguage that calls guesslanguage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - silpa_common

  run:
    - python
    - setuptools
    - silpa_common

test:
  # Python imports
  imports:
    - guesslanguage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://silpa.org.in/Guess_Language
  license: LGPL-3.0
  summary: 'Guess primary language of given text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
