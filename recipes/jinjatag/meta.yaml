package:
  name: jinjatag
  version: "0.1.6"

source:
  fn: jinjatag-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/j/jinjatag/jinjatag-0.1.6.tar.gz
  md5: 78dbb90dbbf0c7fb87bf26e7171822f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinjatag = jinjatag:main
    #
    # Would create an entry point called jinjatag that calls jinjatag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.5

  run:
    - python
    - jinja2 >=2.5

test:
  # Python imports
  imports:
    - jinjatag

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mankyd/jinjatag
  license: GNU General Public License (GPL)
  summary: 'A library to make Jinja2 Extensions Easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
