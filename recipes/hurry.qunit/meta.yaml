package:
  name: hurry.qunit
  version: "0.5"

source:
  fn: hurry.qunit-0.5.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.qunit/hurry.qunit-0.5.tar.gz
  md5: c3a8779204985a03cb89f66b892bc64e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.qunit = hurry.qunit:main
    #
    # Would create an entry point called hurry.qunit that calls hurry.qunit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10

  run:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10

test:
  # Python imports
  imports:
    - hurry
    - hurry.qunit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hurry.qunit
  license: MIT / GPL
  summary: 'Hurry resource for JQuery Qunit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
