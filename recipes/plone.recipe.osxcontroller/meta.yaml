package:
  name: plone.recipe.osxcontroller
  version: "0.4"

source:
  fn: plone.recipe.osxcontroller-0.4.zip
  url: https://pypi.python.org/packages/source/p/plone.recipe.osxcontroller/plone.recipe.osxcontroller-0.4.zip
  md5: e4f17cb38bde71c7c3463c3167bff458
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.osxcontroller = plone.recipe.osxcontroller:main
    #
    # Would create an entry point called plone.recipe.osxcontroller that calls plone.recipe.osxcontroller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.osxcontroller
    - plone.recipe.osxcontroller.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/
  license: Zope Public License
  summary: 'Installs a wxPython-based Plone controller for OS X'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
