package:
  name: gcpuploader
  version: "2015.2.28"

source:
  fn: GcpUploader-2015.2.28.tar.gz
  url: https://pypi.python.org/packages/source/G/GcpUploader/GcpUploader-2015.2.28.tar.gz
  md5: 001d8ec399a317be1a92d43b5dbb27d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gcpuploader = gcpuploader:main
    #
    # Would create an entry point called gcpuploader that calls gcpuploader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - logging
    - simplejson
    - requests
    - argparse
    - configparser

  run:
    - python
    - logging
    - simplejson
    - requests
    - argparse
    - configparser

test:
  # Python imports
  imports:
    - gcpuploader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/gcpuploader/
  license: GNU General Public License (GPL)
  summary: 'A tool to upload FIT, GPX, and TCX files to the Garmin Connect web site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
