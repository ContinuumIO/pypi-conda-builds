package:
  name: lz4-cffi
  version: "1.0.5"

source:
  fn: lz4-cffi-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/l/lz4-cffi/lz4-cffi-1.0.5.tar.gz
  md5: b7349c2840f5725c9586bafee752db84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lz4-cffi = lz4-cffi:main
    #
    # Would create an entry point called lz4-cffi that calls lz4-cffi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi >=0.8

  run:
    - python
    - cffi >=0.8

test:
  # Python imports
  imports:
    - lz4

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/GregBowyer/lz4-cffi/
  license: BSD License
  summary: 'A port of the lz4tools package from to CFFI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
