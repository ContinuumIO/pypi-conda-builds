package:
  name: gdm2setup
  version: "0.5.3-karmic"

source:
  fn: gdm2setup-0.5.3-karmic.tar.gz
  url: https://pypi.python.org/packages/source/g/gdm2setup/gdm2setup-0.5.3-karmic.tar.gz
  md5: 8df2527f5e85a09a98bea31c774b0e34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gdm2setup = gdm2setup:main
    #
    # Would create an entry point called gdm2setup that calls gdm2setup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gdm2
    - gdm2setup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/gdm2setup/
  license: GNU General Public License (GPL)
  summary: 'GDM2 Setup utility and libraries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
