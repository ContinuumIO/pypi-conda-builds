package:
  name: iw.recipe.cmd
  version: "0.3"

source:
  fn: iw.recipe.cmd-0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.recipe.cmd/iw.recipe.cmd-0.3.tar.gz
  md5: c15c0b9cd9486531cf6d1a3d521cb3a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.recipe.cmd = iw.recipe.cmd:main
    #
    # Would create an entry point called iw.recipe.cmd that calls iw.recipe.cmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - iw
    - iw.recipe
    - iw.recipe.cmd
    - iw.recipe.cmd.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/iw-recipes
  license: GNU General Public License (GPL)
  summary: 'ZC Buildout recipe to execute a commande line'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
