package:
  name: gathernews
  version: "0.2.1"

source:
  fn: GatherNews-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GatherNews/GatherNews-0.2.1.tar.gz
  md5: 089efce1657d3d99f54f0f2c1ff86e3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gathernews = gathernews:main
    #
    # Would create an entry point called gathernews that calls gathernews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse >=1.2.1
    - feedparser >=5.1.3
    - nose >=1.3.0
    - simpleflake >=0.1.5
    - wsgiref >=0.1.2

  run:
    - python
    - argparse >=1.2.1
    - feedparser >=5.1.3
    - nose >=1.3.0
    - simpleflake >=0.1.5
    - wsgiref >=0.1.2

test:
  # Python imports
  imports:
    - gathernews
    - gathernews.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GatherNews/
  license: doc/LICENSE.txt
  summary: 'Gathers unstructured News data into a SQLite3 db'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
