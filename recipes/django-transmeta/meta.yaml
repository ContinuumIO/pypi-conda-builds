package:
  name: django-transmeta
  version: "0.7.3"

source:
  fn: django-transmeta-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-transmeta/django-transmeta-0.7.3.tar.gz
  md5: 874f7815923acf1ff42f428ff68812fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-transmeta = django-transmeta:main
    #
    # Would create an entry point called django-transmeta that calls django-transmeta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - transmeta
    - transmeta.management
    - transmeta.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yaco-Sistemas/django-transmeta
  license: GNU Library or Lesser General Public License (LGPL)
  summary: "Transmeta is an application for translatable content in Django's models."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
