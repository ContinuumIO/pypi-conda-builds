package:
  name: django-render-url
  version: "0.9.9.4"

source:
  fn: django-render-url-0.9.9.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-render-url/django-render-url-0.9.9.4.tar.gz
  md5: 18d89d5415a735a39aa068427d3de434
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-render-url = django-render-url:main
    #
    # Would create an entry point called django-render-url that calls django-render-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_render
    - django_render.annotations
    - django_render.middleware
    - django_render.url_patterns_maker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wangtai/django_render
  license: Apache2
  summary: 'a very light django plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
