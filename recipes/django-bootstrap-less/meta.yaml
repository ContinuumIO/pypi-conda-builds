package:
  name: django-bootstrap-less
  version: "2.1.1"

source:
  fn: django-bootstrap-less-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-less/django-bootstrap-less-2.1.1.tar.gz
  md5: 6147da56ad8c9982047571860e1b1bc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-less = django-bootstrap-less:main
    #
    # Would create an entry point called django-bootstrap-less that calls django-bootstrap-less.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/graingert/django-static-libs/tree/master/bootstrap
  license: UNKNOWN
  summary: 'Bootstrap packaged in a handy django app to speed up new applications and reduce duplicaiton.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
