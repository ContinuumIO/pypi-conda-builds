package:
  name: bottlecbv
  version: "0.2"

source:
  fn: BottleCBV-0.2.tar.gz
  url: https://pypi.python.org/packages/source/B/BottleCBV/BottleCBV-0.2.tar.gz
  md5: 587608a5e394801911f913bc0ef1c8aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottlecbv = bottlecbv:main
    #
    # Would create an entry point called bottlecbv that calls bottlecbv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle ==0.12.7

  run:
    - python
    - bottle ==0.12.7

test:
  # Python imports
  imports:
    - bottlecbv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/techchunks/bottleCBV
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Class based views for Bottle apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
