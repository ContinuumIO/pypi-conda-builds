package:
  name: collective.subscribemember
  version: "1.11"

source:
  fn: collective.subscribemember-1.11.zip
  url: https://pypi.python.org/packages/source/c/collective.subscribemember/collective.subscribemember-1.11.zip
  md5: 09c2d0bb3aa4b4789b150ebd9dd93338
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.subscribemember = collective.subscribemember:main
    #
    # Would create an entry point called collective.subscribemember that calls collective.subscribemember.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.remember
    - testfixtures
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - products.remember
    - testfixtures
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - collective
    - collective.subscribemember
    - collective.subscribemember.browser
    - collective.subscribemember.content
    - collective.subscribemember.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.emergetec.com
  license: GNU General Public License (GPL)
  summary: 'A tool to allow users to subscribe to a Plone site and pay their membership fees.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
