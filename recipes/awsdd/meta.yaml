package:
  name: awsdd
  version: "0.2.3"

source:
  fn: awsdd-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/a/awsdd/awsdd-0.2.3.tar.gz
  md5: d7aa9d97827e7222669a403052dd9311
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - awsdd = awsdd:main
    #
    # Would create an entry point called awsdd that calls awsdd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto ==2.29.1
    - requests ==2.3.0
    - wsgiref ==0.1.2

  run:
    - python
    - boto ==2.29.1
    - requests ==2.3.0
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - add
    - add.config
    - awsdd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dsoprea/AwsDynDns
  license: GPL 2
  summary: 'An AWS (Route53)-based dynamic DNS client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
