package:
  name: django-datetime-widget
  version: "0.9.3"

source:
  fn: django-datetime-widget-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-datetime-widget/django-datetime-widget-0.9.3.tar.gz
  md5: 72ec2dd3c2d2f9e324bae6052e38cd7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-datetime-widget = django-datetime-widget:main
    #
    # Would create an entry point called django-datetime-widget that calls django-datetime-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pytz

  run:
    - python
    - django
    - pytz

test:
  # Python imports
  imports:
    - datetimewidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Django-datetime-widget is a simple and clean widget for DateField, Timefiled and DateTimeField  in Django framework. It is based on Bootstrap datetime picker, supports both Bootstrap 3 and Bootstrap 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
