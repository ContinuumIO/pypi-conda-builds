package:
  name: pythonkc-meetups
  version: "0.1.0"

source:
  fn: pythonkc-meetups-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pythonkc-meetups/pythonkc-meetups-0.1.0.tar.gz
  md5: 826ee96296ede7ad2083e5a21213eb48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythonkc-meetups = pythonkc-meetups:main
    #
    # Would create an entry point called pythonkc-meetups that calls pythonkc-meetups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - requests ==0.7.5
    - mimeparse ==0.1.3
    - python-dateutil ==1.5

  run:
    - python
    - distribute
    - requests ==0.7.5
    - mimeparse ==0.1.3
    - python-dateutil ==1.5

test:
  # Python imports
  imports:
    - pythonkc_meetups

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pythonkc/pythonkc-meetups
  license: MIT License
  summary: 'Provides PythonKC Meetup.com events.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
