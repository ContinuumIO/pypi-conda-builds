package:
  name: intervaltree_bio
  version: "1.0.1"

source:
  fn: intervaltree_bio-1.0.1.zip
  url: https://pypi.python.org/packages/source/i/intervaltree_bio/intervaltree_bio-1.0.1.zip
  md5: aed813f69920e8f53fa8ce678256e226
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intervaltree_bio = intervaltree_bio:main
    #
    # Would create an entry point called intervaltree_bio that calls intervaltree_bio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - intervaltree

  run:
    - python
    - intervaltree

test:
  # Python imports
  imports:
    - intervaltree_bio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/konstantint/intervaltree-bio
  license: MIT License
  summary: 'Interval tree convenience classes for genomic data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
