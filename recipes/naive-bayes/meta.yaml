package:
  name: naive-bayes
  version: "0.1.1"

source:
  fn: naive-bayes-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/n/naive-bayes/naive-bayes-0.1.1.tar.gz
  md5: 42a1cabe2c316e79847a7460b10cdaab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - naive-bayes = naive-bayes:main
    #
    # Would create an entry point called naive-bayes that calls naive-bayes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scikit-learn >=0.15.2
    - numpy >=1.9.0

  run:
    - python
    - scikit-learn >=0.15.2
    - numpy >=1.9.0

test:
  # Python imports
  imports:
    - naivebayes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Naive Bayes Text Classification'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
