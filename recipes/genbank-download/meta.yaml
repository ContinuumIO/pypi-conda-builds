package:
  name: genbank-download
  version: "0.5"

source:
  fn: genbank-download-0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/genbank-download/genbank-download-0.5.tar.gz
  md5: 6fc2917348796831a7e0c155bcbd0496
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genbank-download = genbank-download:main
    #
    # Would create an entry point called genbank-download that calls genbank-download.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://simon.net.nz/code/genbank-download/
  license: BSD License
  summary: 'a small script to download nucleotide sequences from genbank using an accession number.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
