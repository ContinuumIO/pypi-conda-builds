package:
  name: pelican_slug
  version: "0.1"

source:
  fn: pelican_slug-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican_slug/pelican_slug-0.1.tar.gz
  md5: 2d21ed95938309eafef7a617ff2167ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican_slug = pelican_slug:main
    #
    # Would create an entry point called pelican_slug that calls pelican_slug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican

  run:
    - python
    - pelican

test:
  # Python imports
  imports:
    - pelican_slug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wutali/pelican_slug
  license: UNKNOWN
  summary: 'Pelican plugin to make a multi-byte slug the alphabetical one.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
