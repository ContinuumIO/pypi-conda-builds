package:
  name: httpie-http2
  version: "0.0.1"

source:
  fn: httpie-http2-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/httpie-http2/httpie-http2-0.0.1.tar.gz
  md5: 8bf399925298b60be39ba7556e5a37e0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpie-http2 = httpie-http2:main
    #
    # Would create an entry point called httpie-http2 that calls httpie-http2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httpie >0.9.0
    - hyper

  run:
    - python
    - setuptools
    - httpie >0.9.0
    - hyper

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jakubroztocil/httpie-http2
  license: BSD License
  summary: 'HTTP/2 plugin for HTTPie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
