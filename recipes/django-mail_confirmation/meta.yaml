package:
  name: django-mail_confirmation
  version: "1.2.6"

source:
  fn: django-mail_confirmation-1.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mail_confirmation/django-mail_confirmation-1.2.6.tar.gz
  md5: 4d75a70025136c3f736cc27108e7f08f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mail_confirmation = django-mail_confirmation:main
    #
    # Would create an entry point called django-mail_confirmation that calls django-mail_confirmation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mail_confirmation
    - mail_confirmation.migrations
    - mail_confirmation.south_migrations
    - mail_confirmation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://v.licheni.net/drc/django-mail_confirmation.git
  license: GNU General Public License v3 (GPLv3)
  summary: 'A general django user mail confirmation app usable with multiple models at the same time.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
