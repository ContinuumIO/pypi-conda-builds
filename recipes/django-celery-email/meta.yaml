package:
  name: django-celery-email
  version: "1.1.1"

source:
  fn: django-celery-email-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery-email/django-celery-email-1.1.1.tar.gz
  md5: 62ec76a8a164a1299289f463fce0f465
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery-email = django-celery-email:main
    #
    # Would create an entry point called django-celery-email that calls django-celery-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - celery >=2.3.0
    - django-appconf

  run:
    - python
    - django
    - celery >=2.3.0
    - django-appconf

test:
  # Python imports
  imports:
    - djcelery_email

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pmclanahan/django-celery-email
  license: BSD License
  summary: 'Django email backend for celery.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
