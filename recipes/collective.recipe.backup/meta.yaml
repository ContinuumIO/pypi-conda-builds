package:
  name: collective.recipe.backup
  version: "2.20"

source:
  fn: collective.recipe.backup-2.20.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.backup/collective.recipe.backup-2.20.zip
  md5: 0041655bfce723802744870cfdbf7c54
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.backup = collective.recipe.backup:main
    #
    # Would create an entry point called collective.recipe.backup that calls collective.recipe.backup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.backup
    - collective.recipe.backup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.recipe.backup
  license: GNU General Public License (GPL)
  summary: 'bin/backup script: sensible defaults around bin/repozo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
