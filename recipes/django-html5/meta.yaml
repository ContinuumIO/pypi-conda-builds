package:
  name: django-html5
  version: "1.0.0"

source:
  fn: django-html5-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-html5/django-html5-1.0.0.tar.gz
  md5: 137baa327bd0e4b31708d58df64eae7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-html5 = django-html5:main
    #
    # Would create an entry point called django-html5 that calls django-html5.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - html5
    - html5.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dyve/django-html5
  license: Apache Software License
  summary: 'HTML5 support for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
