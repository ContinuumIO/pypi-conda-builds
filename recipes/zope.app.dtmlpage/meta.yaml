package:
  name: zope.app.dtmlpage
  version: "3.5.0"

source:
  fn: zope.app.dtmlpage-3.5.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.dtmlpage/zope.app.dtmlpage-3.5.0.tar.gz
  md5: 33c3fa52453911963e62add10638689f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.dtmlpage = zope.app.dtmlpage:main
    #
    # Would create an entry point called zope.app.dtmlpage that calls zope.app.dtmlpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.app.publication
    - zope.app.preview
    - zope.app.testing
    - zope.container
    - zope.documenttemplate
    - zope.filerepresentation
    - zope.interface
    - zope.schema
    - zope.security
    - zope.traversing

  run:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.app.publication
    - zope.app.preview
    - zope.app.testing
    - zope.container
    - zope.documenttemplate
    - zope.filerepresentation
    - zope.interface
    - zope.schema
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.dtmlpage
    - zope.app.dtmlpage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.dtmlpage
  license: Zope Public License
  summary: 'DTML Page -- A Zope 3 Content Component'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
