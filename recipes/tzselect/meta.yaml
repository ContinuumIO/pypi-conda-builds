package:
  name: tzselect
  version: "0.1"

source:
  fn: tzselect-0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tzselect/tzselect-0.1.tar.gz
  md5: 0d76eb5c24dfb9dcf7523d086012c1f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tzselect = tzselect:main
    #
    # Would create an entry point called tzselect that calls tzselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tzselect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/davidreynolds/tzselect
  license: MIT License
  summary: 'tzselect provides a simple solution for creating a timezone select field.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
