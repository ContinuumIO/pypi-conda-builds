package:
  name: dict2colander
  version: "0.2"

source:
  fn: dict2colander-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dict2colander/dict2colander-0.2.tar.gz
  md5: 1ae1bfcc42d706932e40992b9292a4de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dict2colander = dict2colander:main
    #
    # Would create an entry point called dict2colander that calls dict2colander.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dict2colander

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/pfacka/dict2colander
  license: MIT License
  summary: 'Dictionary to Colander schema conversion library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
