package:
  name: bson_lazy
  version: "0.2.3"

source:
  fn: bson_lazy-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bson_lazy/bson_lazy-0.2.3.tar.gz
  md5: a584b7d5e87f4ab890e0ec8b284d2df6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bson_lazy = bson_lazy:main
    #
    # Would create an entry point called bson_lazy that calls bson_lazy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.5

  run:
    - python
    - pymongo >=2.5

test:
  # Python imports
  imports:
    - bson_lazy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ShinNoNoir/bson_lazy
  license: LICENSE.txt
  summary: 'A lazy BSON file loader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
