package:
  name: megrok.rendersource
  version: "0.5.1"

source:
  fn: megrok.rendersource-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.rendersource/megrok.rendersource-0.5.1.tar.gz
  md5: dcd1af1c36d99bd8ca05405988b01feb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.rendersource = megrok.rendersource:main
    #
    # Would create an entry point called megrok.rendersource that calls megrok.rendersource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grokcore.view

  run:
    - python
    - setuptools
    - setuptools
    - grokcore.view

test:
  # Python imports
  imports:
    - megrok
    - megrok.rendersource
    - megrok.rendersource.ftests
    - megrok.rendersource.ftests.directives

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL
  summary: 'Grok source renderers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
