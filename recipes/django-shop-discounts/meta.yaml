package:
  name: django-shop-discounts
  version: "0.0.7"

source:
  fn: django-shop-discounts-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-discounts/django-shop-discounts-0.0.7.tar.gz
  md5: ccccf3669dc367b1cb17cb086a4cddbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-discounts = django-shop-discounts:main
    #
    # Would create an entry point called django-shop-discounts that calls django-shop-discounts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-shop >=0.2.0

  run:
    - python
    - django-shop >=0.2.0

test:
  # Python imports
  imports:
    - discount
    - discount.default_discounts
    - discount.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bmihelac/django-shop-discounts
  license: BSD License
  summary: 'configurable and extendible discount app for Django-shop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
