package:
  name: django-sem
  version: "0.2.2"

source:
  fn: django-sem-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sem/django-sem-0.2.2.tar.gz
  md5: 04e73f61f85d2e21d22ad8b1f42b163e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sem = django-sem:main
    #
    # Would create an entry point called django-sem that calls django-sem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1.1
    - m2crypto >=0.21.1

  run:
    - python
    - django >=1.1.1
    - m2crypto >=0.21.1

test:
  # Python imports
  imports:
    - SEM

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/SignedEmailMessage/
  license: LICENSE.txt
  summary: 'Useful tool for sending signed emails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
