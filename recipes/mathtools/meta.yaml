package:
  name: mathtools
  version: "1.2"

source:
  fn: mathtools-1.2.zip
  url: https://pypi.python.org/packages/source/m/mathtools/mathtools-1.2.zip
  md5: f8ef053e5e397a8901ffb86efde889db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mathtools = mathtools:main
    #
    # Would create an entry point called mathtools that calls mathtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mathtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Simple math formulas and calculations,\nsuch as area of a circle, feet to meters, speed = distance/time and many more and even more on the way.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
