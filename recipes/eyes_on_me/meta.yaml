package:
  name: eyes_on_me
  version: "0.5"

source:
  fn: eyes_on_me-0.5.tar.gz
  url: https://pypi.python.org/packages/source/e/eyes_on_me/eyes_on_me-0.5.tar.gz
  md5: 4cad9a385f6976ac8dd371974faefda9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eyes_on_me = eyes_on_me:main
    #
    # Would create an entry point called eyes_on_me that calls eyes_on_me.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - astral
    - pil
    - daemonize

  run:
    - python
    - astral
    - pil
    - daemonize

test:
  # Python imports
  imports:
    - eyes_on_me

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dehun/eyes_on_me/
  license: BSD
  summary: 'eyes_on_me is simple utility which allows you to control your screen backlight and temperature according to lighting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
