package:
  name: django-filetransfers
  version: "0.1.0"

source:
  fn: django-filetransfers-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filetransfers/django-filetransfers-0.1.0.tar.gz
  md5: f2a9741e73d4fb832652686bef0fca6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filetransfers = django-filetransfers:main
    #
    # Would create an entry point called django-filetransfers that calls django-filetransfers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - filetransfers
    - filetransfers.backends
    - filetransfers.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.allbuttonspressed.com/projects/django-filetransfers
  license: UNKNOWN
  summary: 'Helper for writing reusable Django apps that handle uploads and downloads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
