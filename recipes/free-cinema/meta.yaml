package:
  name: free-cinema
  version: "0.3.0"

source:
  fn: Free-Cinema-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Free-Cinema/Free-Cinema-0.3.0.tar.gz
  md5: 2164bb23782a422015fc89cdaec75c8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - free-cinema = free-cinema:main
    #
    # Would create an entry point called free-cinema that calls free-cinema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wxpython
    - beautifulsoup4

  run:
    - python
    - wxpython
    - beautifulsoup4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kaveensblog.wordpress.com
  license: GNU General Public License v3 (GPLv3)
  summary: 'Search, Download movies via Torrent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
