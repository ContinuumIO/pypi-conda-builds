package:
  name: django-google-urchin
  version: "0.1a1"

source:
  fn: django-google-urchin-0.1a1.zip
  url: https://pypi.python.org/packages/source/d/django-google-urchin/django-google-urchin-0.1a1.zip
  md5: bd2a9327333fc30e16c955c3c187e527
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-urchin = django-google-urchin:main
    #
    # Would create an entry point called django-google-urchin that calls django-google-urchin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google_analytics
    - google_analytics.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-google-analytics
  license: BSD License
  summary: 'Provides a Django template tag to insert the Google Analytics tracking code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
