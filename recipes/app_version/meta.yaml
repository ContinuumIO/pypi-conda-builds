package:
  name: app_version
  version: "0.2.1"

source:
  fn: app_version-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/app_version/app_version-0.2.1.tar.gz
  md5: fc09b5d2a8d7f35636dc0adfe0617b5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - app_version = app_version:main
    #
    # Would create an entry point called app_version that calls app_version.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tolerance

  run:
    - python
    - tolerance

test:
  # Python imports
  imports:
    - app_version

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/app_version
  license: MIT License
  summary: 'A tiny utility to get application version from pkg_resouces'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
