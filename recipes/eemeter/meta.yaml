package:
  name: eemeter
  version: "0.1.0"

source:
  fn: eemeter-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/eemeter/eemeter-0.1.0.tar.gz
  md5: 23709c4a7753bfaf45c5b34c4f6cc54f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eemeter = eemeter:main
    #
    # Would create an entry point called eemeter that calls eemeter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pint >=0.6
    - pyyaml >=3.11
    - scipy >=0.15
    - numpy >=1.9
    - requests >=2.5

  run:
    - python
    - pint >=0.6
    - pyyaml >=3.11
    - scipy >=0.15
    - numpy >=1.9
    - requests >=2.5

test:
  # Python imports
  imports:
    - eemeter
    - eemeter.config
    - eemeter.meter
    - eemeter.models
    - eemeter.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/impactlab/eemeter/
  license: MIT License
  summary: 'Open Energy Efficiency Meter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
