package:
  name: django-themes
  version: "0.1"

source:
  fn: django-themes-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-themes/django-themes-0.1.tar.gz
  md5: 64cbcb962fd15299f7292401ec455729
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-themes = django-themes:main
    #
    # Would create an entry point called django-themes that calls django-themes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - themes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/LimpidTech/django-themes
  license: MIT License
  summary: 'A powerful application that can be used to develop themes to completely reskin your django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
