package:
  name: armstrong.apps.crm
  version: "0.1.0"

source:
  fn: armstrong.apps.crm-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.apps.crm/armstrong.apps.crm-0.1.0.tar.gz
  md5: ee583ead79cde88c98b1b96d397a882b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.apps.crm = armstrong.apps.crm:main
    #
    # Would create an entry point called armstrong.apps.crm that calls armstrong.apps.crm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - armstrong.utils.backends

  run:
    - python
    - armstrong.utils.backends

test:
  # Python imports
  imports:
    - armstrong.apps.crm
    - armstrong.apps.crm.tests
    - armstrong.apps.crm.tests.crm_support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.apps.crm/
  license: Apache Software License
  summary: 'Mechanism for hooking up a CRM to Armstrong'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
