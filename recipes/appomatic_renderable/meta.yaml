package:
  name: appomatic_renderable
  version: "0.0.5"

source:
  fn: appomatic_renderable-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_renderable/appomatic_renderable-0.0.5.tar.gz
  md5: 967b8990f2bbc1c1c4b7dc5303205cbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_renderable = appomatic_renderable:main
    #
    # Would create an entry point called appomatic_renderable that calls appomatic_renderable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appomaticcore
    - appomatic_autocomplete
    - appomatic_ckeditor
    - django-mptt

  run:
    - python
    - appomaticcore
    - appomatic_autocomplete
    - appomatic_ckeditor
    - django-mptt

test:
  # Python imports
  imports:
    - appomatic_renderable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/redhog/appomatic_renderable
  license: GPL
  summary: "Renderable is a mini cms framework to make django models easily 'renderable' using templates."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
