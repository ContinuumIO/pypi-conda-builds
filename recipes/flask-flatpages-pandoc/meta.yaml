package:
  name: flask-flatpages-pandoc
  version: "0.2"

source:
  fn: Flask-FlatPages-Pandoc-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-FlatPages-Pandoc/Flask-FlatPages-Pandoc-0.2.tar.gz
  md5: 49e71611eee09b33e4820aca9a22094e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-flatpages-pandoc = flask-flatpages-pandoc:main
    #
    # Would create an entry point called flask-flatpages-pandoc that calls flask-flatpages-pandoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-flatpages

  run:
    - python
    - flask
    - flask-flatpages

test:
  # Python imports
  imports:
    - flask_flatpages_pandoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fhirschmann/Flask-FlatPages-Pandoc
  license: MIT License
  summary: 'Pandoc rendering for Flask-FlatPages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
