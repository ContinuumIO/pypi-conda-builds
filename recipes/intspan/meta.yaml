package:
  name: intspan
  version: "1.0.2"

source:
  fn: intspan-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/intspan/intspan-1.0.2.tar.gz
  md5: 91497b653c738b8702ba6a0de30b7690
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intspan = intspan:main
    #
    # Would create an entry point called intspan that calls intspan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - intspan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jeunice/intspan
  license: BSD License
  summary: "Sets of integers like 1,3-7,33. Inspired by Perl's Set::IntSpan"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
