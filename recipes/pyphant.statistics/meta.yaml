package:
  name: pyphant.statistics
  version: "1.0b3"

source:
  fn: pyphant.statistics-1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/p/pyphant.statistics/pyphant.statistics-1.0b3.tar.gz
  md5: 957e0054541c2b7a008d4c97cadb207b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyphant.statistics = pyphant.statistics:main
    #
    # Would create an entry point called pyphant.statistics that calls pyphant.statistics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyphant >=1.0b3
    - numpy
    - scipy

  run:
    - python
    - setuptools
    - pyphant >=1.0b3
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - Statistics
    - Statistics.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: '\nPyphant Statistics toolbox\nThis is the Statistics toolbox, that provides workers\nfor the Pyphant framework. In order to use it you must have\nthe Pyphant framework installed first. Visit\nhttp://pyphant.sourceforge.net for more information.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
