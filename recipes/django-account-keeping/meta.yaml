package:
  name: django-account-keeping
  version: "0.2.1"

source:
  fn: django-account-keeping-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-account-keeping/django-account-keeping-0.2.1.tar.gz
  md5: 332119254abd394dd778043bc0efdf02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-account-keeping = django-account-keeping:main
    #
    # Would create an entry point called django-account-keeping that calls django-account-keeping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-libs >=1.61.1
    - python-dateutil
    - django-currency-history

  run:
    - python
    - django
    - django-libs >=1.61.1
    - python-dateutil
    - django-currency-history

test:
  # Python imports
  imports:
    - account_keeping
    - account_keeping.management
    - account_keeping.management.commands
    - account_keeping.migrations
    - account_keeping.templatetags
    - account_keeping.tests
    - account_keeping.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-account-keeping
  license: The MIT License
  summary: 'A reusable Django app for keeping track of transactions in your bank accounts.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
