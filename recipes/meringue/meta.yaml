package:
  name: meringue
  version: "0.3.1.a.4.dev.6"

source:
  fn: Meringue-0.3.1.a.4.dev.6.tar.gz
  url: https://pypi.python.org/packages/source/M/Meringue/Meringue-0.3.1.a.4.dev.6.tar.gz
  md5: 49452d569d623d3b858c0f896885c388
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meringue = meringue:main
    #
    # Would create an entry point called meringue that calls meringue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0
    - pillow >=2.0.0

  run:
    - python
    - django >=1.5.0
    - pillow >=2.0.0

test:
  # Python imports
  imports:
    - meringue
    - meringue.templatetags
    - meringue.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dd/Meringue
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Weboven django base package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
