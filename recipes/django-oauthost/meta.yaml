package:
  name: django-oauthost
  version: "0.3.0"

source:
  fn: django-oauthost-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauthost/django-oauthost-0.3.0.tar.gz
  md5: c315818759bd0dd42ad6b296b489a3f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauthost = django-oauthost:main
    #
    # Would create an entry point called django-oauthost that calls django-oauthost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oauthost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/idlesign/django-oauthost
  license: BSD License
  summary: 'This reusable Django application introduces OAuth2 server functionality.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
