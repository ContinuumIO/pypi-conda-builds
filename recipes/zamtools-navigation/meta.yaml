package:
  name: zamtools-navigation
  version: "1.0"

source:
  fn: zamtools-navigation-1.0.zip
  url: https://pypi.python.org/packages/source/z/zamtools-navigation/zamtools-navigation-1.0.zip
  md5: 6f8ba4d2739e7a0e1063147ee698c29f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zamtools-navigation = zamtools-navigation:main
    #
    # Would create an entry point called zamtools-navigation that calls zamtools-navigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - navigation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/zamtools-navigation/
  license: BSD License
  summary: 'Django application for displaying and tracking site navigation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
