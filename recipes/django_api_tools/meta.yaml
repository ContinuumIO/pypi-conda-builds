package:
  name: django_api_tools
  version: "0.1.1"

source:
  fn: django_api_tools-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_api_tools/django_api_tools-0.1.1.tar.gz
  md5: 0a80d33bc22996f61c2c8d7de191704b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_api_tools = django_api_tools:main
    #
    # Would create an entry point called django_api_tools that calls django_api_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_api_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/szpytfire/django-api-tools
  license: UNKNOWN
  summary: 'Django API add-on is a mini-framework which allows developers to run RESTful APIs alongside websites using Forms/Templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
