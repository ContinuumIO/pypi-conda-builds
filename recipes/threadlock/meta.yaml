package:
  name: threadlock
  version: "2.13.0"

source:
  fn: ThreadLock-2.13.0.zip
  url: https://pypi.python.org/packages/source/T/ThreadLock/ThreadLock-2.13.0.zip
  md5: 3e646d4583bf29c66d44c8d256281ca1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - threadlock = threadlock:main
    #
    # Would create an entry point called threadlock that calls threadlock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - extensionclass

  run:
    - python
    - extensionclass

test:
  # Python imports
  imports:
    - ThreadLock
    - threadlock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ThreadLock
  license: ZPL 2.1
  summary: 'Special ThreadLock objects used in Zope2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
