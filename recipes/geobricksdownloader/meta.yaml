package:
  name: geobricksdownloader
  version: "0.1.2"

source:
  fn: GeobricksDownloader-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksDownloader/GeobricksDownloader-0.1.2.tar.gz
  md5: 6ccccac793beaac7a51734228407cc41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobricksdownloader = geobricksdownloader:main
    #
    # Would create an entry point called geobricksdownloader that calls geobricksdownloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - geobricksmodis

  run:
    - python
    - geobricksmodis

test:
  # Python imports
  imports:
    - geobricks_downloader
    - geobricks_downloader.download
    - geobricks_downloader.utils
    - geobricksdownloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksDownloader/
  license: LICENSE.txt
  summary: 'Core functionalities for geospatial data download.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
