package:
  name: django-email-manager
  version: "0.2.1"

source:
  fn: django-email-manager-0.2.1.zip
  url: https://pypi.python.org/packages/source/d/django-email-manager/django-email-manager-0.2.1.zip
  md5: e6cf42cadcd2b205b1ece99821e1621e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-manager = django-email-manager:main
    #
    # Would create an entry point called django-email-manager that calls django-email-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_manager
    - email_manager.management
    - email_manager.management.commands
    - email_manager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paulocheque/django-email-manager
  license: MIT
  summary: 'A simple application to store a summary of system e-mails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
