package:
  name: hpelm
  version: "0.6.6"

source:
  fn: hpelm-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/h/hpelm/hpelm-0.6.6.tar.gz
  md5: df91f4ac0bb249d0879916c45ccc6a8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hpelm = hpelm:main
    #
    # Would create an entry point called hpelm that calls hpelm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - numexpr
    - scipy >=0.12
    - tables
    - cython

  run:
    - python
    - numpy
    - numexpr
    - scipy >=0.12
    - tables
    - cython

test:
  # Python imports
  imports:
    - hpelm
    - hpelm.acc
    - hpelm.modules
    - hpelm.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.researchgate.net/profile/Anton_Akusok
  license: BSD License
  summary: 'High-Performance implementation of an                   Extreme Learning Machine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
