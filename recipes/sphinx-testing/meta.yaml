package:
  name: sphinx-testing
  version: "0.7.0"

source:
  fn: sphinx-testing-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-testing/sphinx-testing-0.7.0.tar.gz
  md5: f398ca347354892c7dcd8c30b0bc4e41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-testing = sphinx-testing:main
    #
    # Would create an entry point called sphinx-testing that calls sphinx-testing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx
    - six

  run:
    - python
    - sphinx
    - six

test:
  # Python imports
  imports:
    - sphinx_testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sphinx-doc/sphinx-testing
  license: BSD License
  summary: 'testing utility classes and functions for Sphinx extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
