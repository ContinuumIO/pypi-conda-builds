package:
  name: api-proxy
  version: "0.1.3"

source:
  fn: api-proxy-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/api-proxy/api-proxy-0.1.3.tar.gz
  md5: 931957ad4b184bfaa43ab8643f44cd41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - api-proxy = api-proxy:main
    #
    # Would create an entry point called api-proxy that calls api-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - api_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ccortezia/api-proxy
  license: UNKNOWN
  summary: "Thin layer to access resources living behind api's"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
