package:
  name: gpalign
  version: "0.2.1"

source:
  fn: gpalign-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gpalign/gpalign-0.2.1.tar.gz
  md5: 2f6de7a251ce302e0efab600f55cbf8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gpalign = gpalign:main
    #
    # Would create an entry point called gpalign that calls gpalign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cjktools
    - consolelog >=0.2.4
    - simplestats

  run:
    - python
    - cjktools
    - consolelog >=0.2.4
    - simplestats

test:
  # Python imports
  imports:
    - gpalign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lars512/gpalign-py/
  license: BSD
  summary: 'Unsupervised grapheme-phoneme aligner for Japanese'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
