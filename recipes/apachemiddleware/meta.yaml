package:
  name: apachemiddleware
  version: "0.1.1"

source:
  fn: apachemiddleware-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/apachemiddleware/apachemiddleware-0.1.1.tar.gz
  md5: 6e2acd1e19db1e5b8c6f6f88c37ce652
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apachemiddleware = apachemiddleware:main
    #
    # Would create an entry point called apachemiddleware that calls apachemiddleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - apachemiddleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/apachemiddleware/index.html
  license: MIT
  summary: 'Useful Python middleware for use with mod_wsgi deployments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
