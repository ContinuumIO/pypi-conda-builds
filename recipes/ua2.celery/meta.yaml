package:
  name: ua2.celery
  version: "0.0.2"

source:
  fn: ua2.celery-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ua2.celery/ua2.celery-0.0.2.tar.gz
  md5: 07b476cd4b8cdf8a1a0a6139daffb82b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ua2.celery = ua2.celery:main
    #
    # Would create an entry point called ua2.celery that calls ua2.celery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ua2
    - ua2.celery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ua2web/ua2.celery
  license: BSD License
  summary: 'Extended handlers and helpers worh with celery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
