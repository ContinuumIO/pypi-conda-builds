package:
  name: wildcard.tinymceplugins.youtube
  version: "1.0"

source:
  fn: wildcard.tinymceplugins.youtube-1.0.zip
  url: https://pypi.python.org/packages/source/w/wildcard.tinymceplugins.youtube/wildcard.tinymceplugins.youtube-1.0.zip
  md5: 5f070541240aac38400511e1683dbf9a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wildcard.tinymceplugins.youtube = wildcard.tinymceplugins.youtube:main
    #
    # Would create an entry point called wildcard.tinymceplugins.youtube that calls wildcard.tinymceplugins.youtube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.tinymce

  run:
    - python
    - setuptools
    - setuptools
    - products.tinymce

test:
  # Python imports
  imports:
    - wildcard
    - wildcard.tinymceplugins
    - wildcard.tinymceplugins.youtube

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/wildcard.tinymceplugins.youtube
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
