package:
  name: boaconstructor
  version: "0.3.0"

source:
  fn: boaconstructor-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/boaconstructor/boaconstructor-0.3.0.tar.gz
  md5: 01ff6dce9832058426490da25390441f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boaconstructor = boaconstructor:main
    #
    # Would create an entry point called boaconstructor that calls boaconstructor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boaconstructor
    - boaconstructor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License v2.0
  summary: 'The boacontructor is a templating library for *data*, i.e. templating for dictionaries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
