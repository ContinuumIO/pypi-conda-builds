package:
  name: batou_scm
  version: "0.1.2"

source:
  fn: batou_scm-0.1.2.zip
  url: https://pypi.python.org/packages/source/b/batou_scm/batou_scm-0.1.2.zip
  md5: 767ed793d3f047190ac07b096ab2595d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - batou_scm = batou_scm:main
    #
    # Would create an entry point called batou_scm that calls batou_scm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - batou >=1.0b26.dev0

  run:
    - python
    - batou >=1.0b26.dev0

test:
  # Python imports
  imports:
    - batou_scm
    - batou_scm.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/batou_scm/
  license: Zope Public License
  summary: 'Batou components for managing source code in a development deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
