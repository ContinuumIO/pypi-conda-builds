package:
  name: diablo3api
  version: "0.1.0"

source:
  fn: diablo3api-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/diablo3api/diablo3api-0.1.0.tar.gz
  md5: 349b1a10ceee351973b72fb9d4cb10d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diablo3api = diablo3api:main
    #
    # Would create an entry point called diablo3api that calls diablo3api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - diablo3api
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/menglewis/diablo3api
  license: BSD License
  summary: 'A Python API Wrapper for the Diablo 3 API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
