package:
  name: infi.nose_plugins
  version: "0.0.4"

source:
  fn: infi.nose_plugins-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.nose_plugins/infi.nose_plugins-0.0.4.tar.gz
  md5: a9630211968c26b2cb63515897a10e87
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.nose_plugins = infi.nose_plugins:main
    #
    # Would create an entry point called infi.nose_plugins that calls infi.nose_plugins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - setuptools

  run:
    - python
    - setuptools
    - nose
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.nose_plugins
    - infi.nose_plugins.html

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://git.infinidat.com/host-opensource/infi.nose_plugins
  license: Python Software Foundation License
  summary: 'nose plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
