package:
  name: django-sifac
  version: "0.3.0"

source:
  fn: django-sifac-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sifac/django-sifac-0.3.0.tar.gz
  md5: e463e200e2b49ffcbe41f1e1c4ff823e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sifac = django-sifac:main
    #
    # Would create an entry point called django-sifac that calls django-sifac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - saprfc

  run:
    - python
    - saprfc

test:
  # Python imports
  imports:
    - sifac
    - sifac.sap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unistra/django-sifac/
  license: CeCILL-B
  summary: 'A SAP API for SIFAC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
