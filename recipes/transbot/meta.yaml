package:
  name: transbot
  version: "1.0.2"

source:
  fn: transbot-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/transbot/transbot-1.0.2.tar.gz
  md5: bacee66a358079f57492b4dd4597ffcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - transbot = transbot:main
    #
    # Would create an entry point called transbot that calls transbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - transbot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fedorahosted.org/transbot
  license: ['gpl-3.0']
  summary: 'Inter-channel IRC automatic translation bot using Google Translate API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
