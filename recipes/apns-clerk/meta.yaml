package:
  name: apns-clerk
  version: "0.1.2"

source:
  fn: apns-clerk-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/apns-clerk/apns-clerk-0.1.2.tar.gz
  md5: 895fa3733d82e8f94dfb22ff7930dbc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apns-clerk = apns-clerk:main
    #
    # Would create an entry point called apns-clerk that calls apns-clerk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopenssl
    - six

  run:
    - python
    - pyopenssl
    - six

test:
  # Python imports
  imports:
    - apns_clerk
    - apns_clerk.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/aleksihoffman/apns-clerk
  license: Apache Software License
  summary: 'Python client for Apple Push Notification service (APNs)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
