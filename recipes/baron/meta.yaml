package:
  name: baron
  version: "0.6.1"

source:
  fn: baron-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/b/baron/baron-0.6.1.tar.gz
  md5: aabd5b963d3fcee2531e98b68f5f9b81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baron = baron:main
    #
    # Would create an entry point called baron that calls baron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rply

  run:
    - python
    - rply

test:
  # Python imports
  imports:
    - baron

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Psycojoker/baron
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Full Syntax Tree for python to make writing refactoring code a realist task'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
