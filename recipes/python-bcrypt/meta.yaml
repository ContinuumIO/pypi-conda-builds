package:
  name: python-bcrypt
  version: "0.3.1"

source:
  fn: python-bcrypt-0.3.1.zip
  url: https://pypi.python.org/packages/source/p/python-bcrypt/python-bcrypt-0.3.1.zip
  md5: b73269b7bc85e5b5381965abdc43ff66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-bcrypt = python-bcrypt:main
    #
    # Would create an entry point called python-bcrypt that calls python-bcrypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bcrypt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DisruptiveLabs/python-bcrypt
  license: BSD
  summary: 'Blowfish password hashing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
