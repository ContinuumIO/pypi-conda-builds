package:
  name: metasyntactic
  version: "0.99"

source:
  fn: metasyntactic-0.99.tar.gz
  url: https://pypi.python.org/packages/source/m/metasyntactic/metasyntactic-0.99.tar.gz
  md5: d19938622ed55471ce7e8f0e935c43b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metasyntactic = metasyntactic:main
    #
    # Would create an entry point called metasyntactic that calls metasyntactic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - metasyntactic
    - metasyntactic.themes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ask/metasyntactic/
  license: Artistic License
  summary: 'metasyntactic - Themed metasyntactic variables names'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
