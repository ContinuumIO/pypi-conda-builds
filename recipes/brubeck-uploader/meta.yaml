package:
  name: brubeck-uploader
  version: "0.1.14"

source:
  fn: brubeck-uploader-0.1.14.tar.gz
  url: https://pypi.python.org/packages/source/b/brubeck-uploader/brubeck-uploader-0.1.14.tar.gz
  md5: 6bc8a5729d94f948f2e152c4b3d6e3cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brubeck-uploader = brubeck-uploader:main
    #
    # Would create an entry point called brubeck-uploader that calls brubeck-uploader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - brubeck-service >=0.1.4

  run:
    - python
    - brubeck-service >=0.1.4

test:
  # Python imports
  imports:
    - brubeckuploader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sethmurphy/BrubeckUploader
  license: UNKNOWN
  summary: 'Brubeck module for uploading files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
