package:
  name: beast2bpp
  version: "0.13"

source:
  fn: beast2bpp-0.13.tar.gz
  url: https://pypi.python.org/packages/source/b/beast2bpp/beast2bpp-0.13.tar.gz
  md5: 19caeeb44a2416b8896f0f022cc67063
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beast2bpp = beast2bpp:main
    #
    # Would create an entry point called beast2bpp that calls beast2bpp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - beast2bpp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brannala/Beast2BPP.git/package/beast2bpp
  license: MIT License
  summary: 'Script that converts starBeast xml format input files to BPP format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
