package:
  name: chartkick
  version: "0.4.2"

source:
  fn: chartkick-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/c/chartkick/chartkick-0.4.2.tar.gz
  md5: 965cfadba4d9e9f740a389f89607d8b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chartkick = chartkick:main
    #
    # Would create an entry point called chartkick that calls chartkick.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chartkick
    - chartkick.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mher/chartkick.py
  license: MIT License
  summary: 'Create beautiful Javascript charts with minimal code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
