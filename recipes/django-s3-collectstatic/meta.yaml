package:
  name: django-s3-collectstatic
  version: "1.0"

source:
  fn: django-s3-collectstatic-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-s3-collectstatic/django-s3-collectstatic-1.0.tar.gz
  md5: fefbaecc449c73120f3c3dd7189307dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-s3-collectstatic = django-s3-collectstatic:main
    #
    # Would create an entry point called django-s3-collectstatic that calls django-s3-collectstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_s3_collectstatic
    - django_s3_collectstatic.management
    - django_s3_collectstatic.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agoodid/django-s3-collectstatic
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
