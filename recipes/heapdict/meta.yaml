package:
  name: heapdict
  version: "1.0.0"

source:
  fn: HeapDict-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/H/HeapDict/HeapDict-1.0.0.tar.gz
  md5: 47320e136156593c84072d09e770800f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heapdict = heapdict:main
    #
    # Would create an entry point called heapdict that calls heapdict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - heapdict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stutzbachenterprises.com/
  license: BSD License
  summary: 'a heap with decrease-key and increase-key operations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
