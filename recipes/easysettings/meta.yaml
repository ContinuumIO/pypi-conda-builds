package:
  name: easysettings
  version: "1.9.3-5"

source:
  fn: EasySettings-1.9.3-5.tar.gz
  url: https://pypi.python.org/packages/source/E/EasySettings/EasySettings-1.9.3-5.tar.gz
  md5: 2b9d71b0d03b0f37a85c7afdeb9206aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easysettings = easysettings:main
    #
    # Would create an entry point called easysettings that calls easysettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - easysettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/EasySettings/
  license: LICENSE.txt
  summary: "Easily save & retrieve your application's settings.\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
