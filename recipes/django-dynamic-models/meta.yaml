package:
  name: django-dynamic-models
  version: "0.0.5"

source:
  fn: django-dynamic-models-0.0.5.zip
  url: https://pypi.python.org/packages/source/d/django-dynamic-models/django-dynamic-models-0.0.5.zip
  md5: a68aedd1273ef52a94bab16c2149faa7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-models = django-dynamic-models:main
    #
    # Would create an entry point called django-dynamic-models that calls django-dynamic-models.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_dynamic_models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/django-dynamic-models
  license: BSD License
  summary: 'A Django app that enable change model fields if other other apps models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
