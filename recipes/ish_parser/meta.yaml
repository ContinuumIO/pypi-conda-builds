package:
  name: ish_parser
  version: "0.0.4"

source:
  fn: ish_parser-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/ish_parser/ish_parser-0.0.4.tar.gz
  md5: 7b17b694685d424cd07f1a308dfe2a3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ish_parser = ish_parser:main
    #
    # Would create an entry point called ish_parser that calls ish_parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ish_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/haydenth/ish_parser
  license: UNKNOWN
  summary: 'Parser for NOAA ISH (integrated surface hourly) reports'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
