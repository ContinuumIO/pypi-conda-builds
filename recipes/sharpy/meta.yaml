package:
  name: sharpy
  version: "0.8"

source:
  fn: Sharpy-0.8.tar.gz
  url: https://pypi.python.org/packages/source/S/Sharpy/Sharpy-0.8.tar.gz
  md5: 404ae35e13aaf60e2357f0fee34a229d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sharpy = sharpy:main
    #
    # Would create an entry point called sharpy that calls sharpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - httplib2
    - elementtree
    - python-dateutil <2.0

  run:
    - python
    - httplib2
    - elementtree
    - python-dateutil <2.0

test:
  # Python imports
  imports:
    - sharpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Saaspire/sharpy
  license: BSD License
  summary: 'Python client for the Cheddar Getter API (http://cheddargetter.com).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
