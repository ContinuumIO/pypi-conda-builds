package:
  name: cubicweb-varnish
  version: "0.3.0"

source:
  fn: cubicweb-varnish-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-varnish/cubicweb-varnish-0.3.0.tar.gz
  md5: dc429a1fc178192eaeb67b201df1862c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-varnish = cubicweb-varnish:main
    #
    # Would create an entry point called cubicweb-varnish that calls cubicweb-varnish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.12.5

  run:
    - python
    - cubicweb >=3.12.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-varnish
  license: LGPL
  summary: 'cubicweb varnish helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
