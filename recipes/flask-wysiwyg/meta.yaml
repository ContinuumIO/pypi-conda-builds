package:
  name: flask-wysiwyg
  version: "0.1"

source:
  fn: flask-wysiwyg-0.1.zip
  url: https://pypi.python.org/packages/source/f/flask-wysiwyg/flask-wysiwyg-0.1.zip
  md5: bbc98c3cb81f49cf6d04ae66bf2dc833
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-wysiwyg = flask-wysiwyg:main
    #
    # Would create an entry point called flask-wysiwyg that calls flask-wysiwyg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-wtf ==0.9.4

  run:
    - python
    - flask-wtf ==0.9.4

test:
  # Python imports
  imports:
    - flask_wysiwyg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nbtcnet/flask-wysiwyg/
  license: BSD
  summary: u"django-secure-input provides custom form fields to render wysiwyg editor instead of regular textareas. It takes care of cleaning html for you too. With its super secure defaults you do not want to modified it's whitelisting rules."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
