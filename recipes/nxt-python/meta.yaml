package:
  name: nxt-python
  version: "2.2.2"

source:
  fn: nxt-python-2.2.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nxt-python/nxt-python-2.2.2.tar.gz
  md5: a2407611e2b9475a9686e7981e970f0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nxt-python = nxt-python:main
    #
    # Would create an entry point called nxt-python that calls nxt-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nxt
    - nxt.sensor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/nxt-python/
  license: Gnu GPL v3
  summary: 'LEGO Mindstorms NXT Control Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
