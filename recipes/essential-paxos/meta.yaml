package:
  name: essential-paxos
  version: "2.0"

source:
  fn: essential-paxos-2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/essential-paxos/essential-paxos-2.0.tar.gz
  md5: 58f7184e529245cf7820cdfd355f18d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - essential-paxos = essential-paxos:main
    #
    # Would create an entry point called essential-paxos that calls essential-paxos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - paxos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cocagne/paxos
  license: MIT License
  summary: 'Paxos algorithm implementation suitable for practical and educational use'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
