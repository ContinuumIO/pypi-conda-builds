package:
  name: django-idshost
  version: "0.1.0"

source:
  fn: django-idshost-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-idshost/django-idshost-0.1.0.tar.gz
  md5: 6ba28c9112ed2c38a7de72cd42de65e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-idshost = django-idshost:main
    #
    # Would create an entry point called django-idshost that calls django-idshost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - spyne >=2.11,<3.0
    - suds >=0.4,<0.5
    - lxml >=3

  run:
    - python
    - django >=1.5
    - spyne >=2.11,<3.0
    - suds >=0.4,<0.5
    - lxml >=3

test:
  # Python imports
  imports:
    - django_idshost
    - django_idshost.auth
    - django_idshost.certificate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/optiflows/django-idshost
  license: Apache Software License
  summary: 'Django app to interface with the ids infrastructure.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
