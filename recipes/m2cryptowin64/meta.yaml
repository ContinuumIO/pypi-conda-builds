package:
  name: m2cryptowin64
  version: "0.21.1-3"

source:
  fn: M2CryptoWin64-0.21.1-3.tar.gz
  url: https://pypi.python.org/packages/source/M/M2CryptoWin64/M2CryptoWin64-0.21.1-3.tar.gz
  md5: b9cf1238a56b38195aca61b80bb305b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m2cryptowin64 = m2cryptowin64:main
    #
    # Would create an entry point called m2cryptowin64 that calls m2cryptowin64.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - M2Crypto
    - M2Crypto.PGP
    - M2Crypto.SSL
    - m2cryptowin64

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dsoprea/M2CryptoWin64
  license: UNKNOWN
  summary: 'M2Crypto for Windows (64-bit)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
