package:
  name: cmigemo
  version: "0.1.5"

source:
  fn: cmigemo-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cmigemo/cmigemo-0.1.5.tar.gz
  md5: 027b300b68f9467d2d0aa30e2d5087f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmigemo = cmigemo:main
    #
    # Would create an entry point called cmigemo that calls cmigemo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.7.3

  run:
    - python
    - six >=1.7.3

test:
  # Python imports
  imports:
    - cmigemo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mooz/python-cmigemo
  license: MIT
  summary: 'A pure python binding for C/Migemo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
