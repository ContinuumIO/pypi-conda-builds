package:
  name: codeblock
  version: "0.1"

source:
  fn: codeblock-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/codeblock/codeblock-0.1.tar.gz
  md5: 950a91cafaab2bc3a6570d56ec9d4821
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codeblock = codeblock:main
    #
    # Would create an entry point called codeblock that calls codeblock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - silvercity

  run:
    - python
    - setuptools
    - silvercity

test:
  # Python imports
  imports:
    - codeblock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aspn.activestate.com/ASPN/Cookbook/Python/Recipe/252170
  license: Python Cookbook code is freely available for use and review.
  summary: 'Syntax-highlighted code blocks for docutils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
