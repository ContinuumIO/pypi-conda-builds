package:
  name: konval
  version: "0.3dev"

source:
  fn: konval-0.3dev.tar.gz
  url: https://pypi.python.org/packages/source/k/konval/konval-0.3dev.tar.gz
  md5: e663ffe655b9128497e5c0ebe1374b00
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - konval = konval:main
    #
    # Would create an entry point called konval that calls konval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - konval

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.agapow.net/software/py-konval
  license: MIT License
  summary: 'Routines for data validation & conversion'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
