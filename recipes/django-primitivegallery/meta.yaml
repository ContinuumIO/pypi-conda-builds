package:
  name: django-primitivegallery
  version: "0.0.9"

source:
  fn: django-primitivegallery-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-primitivegallery/django-primitivegallery-0.0.9.tar.gz
  md5: cf04fcbab35a0506867738fae322876c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-primitivegallery = django-primitivegallery:main
    #
    # Would create an entry point called django-primitivegallery that calls django-primitivegallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0

  run:
    - python
    - django >=1.5.0

test:
  # Python imports
  imports:
    - primitivegallery
    - primitivegallery/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ludw/django-primitivegallery
  license: BSD License
  summary: 'A simple image gallery.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
