package:
  name: dblplib
  version: "0.1.1"

source:
  fn: dblplib-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dblplib/dblplib-0.1.1.tar.gz
  md5: 9d8f55fb85a16b0c74e2d22efeb28a51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dblplib = dblplib:main
    #
    # Would create an entry point called dblplib that calls dblplib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sqlalchemy >=0.9.3
    - whoosh >=2.6.0

  run:
    - python
    - sqlalchemy >=0.9.3
    - whoosh >=2.6.0

test:
  # Python imports
  imports:
    - dblplib
    - dblplib.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/dblp-parser/
  license: LICENSE.txt
  summary: 'A Python lib to parse the DBLP Computer Science Bibliography. It provides a class model which is used to extract publication entries from the XML file provided by the DBLP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
