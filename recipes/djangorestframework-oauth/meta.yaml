package:
  name: djangorestframework-oauth
  version: "1.0.1"

source:
  fn: djangorestframework-oauth-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-oauth/djangorestframework-oauth-1.0.1.tar.gz
  md5: 6c5fe2108dbaf6dedcb80d786045c99b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-oauth = djangorestframework-oauth:main
    #
    # Would create an entry point called djangorestframework-oauth that calls djangorestframework-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-oauth-plus >=2.2.1
    - oauth2 >=1.5.211
    - django-oauth2-provider >=0.2.4

  run:
    - python
    - django-oauth-plus >=2.2.1
    - oauth2 >=1.5.211
    - django-oauth2-provider >=0.2.4

test:
  # Python imports
  imports:
    - rest_framework_oauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpadilla/django-rest-framework-oauth
  license: BSD License
  summary: 'OAuth support for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
