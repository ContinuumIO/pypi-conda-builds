package:
  name: django-staticfiles-jquery
  version: "1.7.2"

source:
  fn: django-staticfiles-jquery-1.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-jquery/django-staticfiles-jquery-1.7.2.tar.gz
  md5: 2dac5d25566ef641d60f71040e9e2ed0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-jquery = django-staticfiles-jquery:main
    #
    # Would create an entry point called django-staticfiles-jquery that calls django-staticfiles-jquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tswicegood/django-staticfiles-jquery/
  license: BSD License
  summary: 'jQuery meets Django staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
