package:
  name: django_db_sampler
  version: "0.21"

source:
  fn: django_db_sampler-0.21.tar.gz
  url: https://pypi.python.org/packages/source/d/django_db_sampler/django_db_sampler-0.21.tar.gz
  md5: 90b98cabb889931a7027169b9461621a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_db_sampler = django_db_sampler:main
    #
    # Would create an entry point called django_db_sampler that calls django_db_sampler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_db_sampler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JivanAmara/django_db_sampler
  license: UNKNOWN
  summary: u'Utility to easily extract specific models from a database with their dependencies.  Especially useful for creating fixtures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
