package:
  name: collective.listjs
  version: "0.1.2"

source:
  fn: collective.listjs-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.listjs/collective.listjs-0.1.2.tar.gz
  md5: 2016bf3c9f939ad52dc48f68d112cab3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.listjs = collective.listjs:main
    #
    # Would create an entry point called collective.listjs that calls collective.listjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.listjs
    - collective.listjs.browser
    - collective.listjs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Plone integration for Listjs (http://www.listjs.com)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
