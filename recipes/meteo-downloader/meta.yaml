package:
  name: meteo-downloader
  version: "1.0"

source:
  fn: meteo-downloader-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/meteo-downloader/meteo-downloader-1.0.tar.gz
  md5: b49a79bf9e739c9ef7490ac728aa76f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meteo-downloader = meteo-downloader:main
    #
    # Would create an entry point called meteo-downloader that calls meteo-downloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ios
    - ncfile
    - parse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: None
  license: UNKNOWN
  summary: 'MeteoFile downloader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
