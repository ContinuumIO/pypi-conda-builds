package:
  name: polysquare-setuptools-lint
  version: "0.0.5"

source:
  fn: polysquare-setuptools-lint-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/polysquare-setuptools-lint/polysquare-setuptools-lint-0.0.5.tar.gz
  md5: 8fed416f9f341406d50db0340796056b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - polysquare-setuptools-lint = polysquare-setuptools-lint:main
    #
    # Would create an entry point called polysquare-setuptools-lint that calls polysquare-setuptools-lint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - parmap
    - pep8
    - pylint
    - pylint-common
    - dodgy
    - frosted
    - mccabe
    - pep257
    - pyflakes
    - pyroma
    - vulture
    - prospector >=0.10.1
    - flake8 ==2.3.0
    - flake8-blind-except
    - flake8-docstrings
    - flake8-double-quotes
    - flake8-import-order
    - flake8-todo
    - six
    - pychecker

  run:
    - python
    - setuptools
    - setuptools
    - parmap
    - pep8
    - pylint
    - pylint-common
    - dodgy
    - frosted
    - mccabe
    - pep257
    - pyflakes
    - pyroma
    - vulture
    - prospector >=0.10.1
    - flake8 ==2.3.0
    - flake8-blind-except
    - flake8-docstrings
    - flake8-double-quotes
    - flake8-import-order
    - flake8-todo
    - six
    - pychecker

test:
  # Python imports
  imports:
    - polysquare_setuptools_lint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/polysquare/polysquare-setuptools-lint
  license: MIT License
  summary: "Provides a 'polysquarelint' command for setuptools"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
