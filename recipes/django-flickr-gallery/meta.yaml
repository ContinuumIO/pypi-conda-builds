package:
  name: django-flickr-gallery
  version: "0.1"

source:
  fn: django-flickr-gallery-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flickr-gallery/django-flickr-gallery-0.1.tar.gz
  md5: 96f3d9aa27280be1ff923677f2cb5e1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flickr-gallery = django-flickr-gallery:main
    #
    # Would create an entry point called django-flickr-gallery that calls django-flickr-gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - flickrapi

  run:
    - python
    - django >=1.4
    - flickrapi

test:
  # Python imports
  imports:
    - django_flickr_gallery
    - django_flickr_gallery.migrations
    - django_flickr_gallery.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/arkanister/django-flickr-gallery
  license: BSD License
  summary: 'Django Gallery with flickr integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
