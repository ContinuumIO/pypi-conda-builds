package:
  name: aufmachen
  version: "0.2.1"

source:
  fn: aufmachen-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aufmachen/aufmachen-0.2.1.tar.gz
  md5: 961659dcdd284f1eca38911e6d8f04f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aufmachen = aufmachen:main
    #
    # Would create an entry point called aufmachen that calls aufmachen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aufmachen
    - aufmachen.websites

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fdb/aufmachen
  license: BSD License
  summary: "Turns a website's HTML into nice, clean objects."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
