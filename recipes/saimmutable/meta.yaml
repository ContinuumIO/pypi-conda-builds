package:
  name: saimmutable
  version: "0.2"

source:
  fn: saimmutable-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/saimmutable/saimmutable-0.2.tar.gz
  md5: b7ddb22897f24257aff26e71b96ef70f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - saimmutable = saimmutable:main
    #
    # Would create an entry point called saimmutable that calls saimmutable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7.5

  run:
    - python
    - sqlalchemy >=0.7.5

test:
  # Python imports
  imports:
    - saimmutable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'SA instrumentation machinery for immutable data models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
