package:
  name: fshttpstream
  version: "0.3.4"

source:
  fn: fshttpstream-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/f/fshttpstream/fshttpstream-0.3.4.tar.gz
  md5: 6a5b35c0b331214ad76c951a1775cb55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fshttpstream = fshttpstream:main
    #
    # Would create an entry point called fshttpstream that calls fshttpstream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - telephonie
    - gevent-websocket

  run:
    - python
    - telephonie
    - gevent-websocket

test:
  # Python imports
  imports:
    - fshttpstream

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tamiel/fshttpstream
  license: Mozilla Public License 1.1 (MPL 1.1)
  summary: 'Websocket proxy server to send freeswitch events to websocket client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
