package:
  name: sorl-watermark
  version: "1.0.0"

source:
  fn: sorl-watermark-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sorl-watermark/sorl-watermark-1.0.0.tar.gz
  md5: 2d13fe4e9c3390e92dc56e3c3b4536ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sorl-watermark = sorl-watermark:main
    #
    # Would create an entry point called sorl-watermark that calls sorl-watermark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sorl-thumbnail

  run:
    - python
    - sorl-thumbnail

test:
  # Python imports
  imports:
    - sorl_watermarker
    - sorl_watermarker.conf
    - sorl_watermarker.engines
    - tests
    - tests.engines

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/originell/sorl-watermark
  license: BSD License
  summary: 'Image based watermarks for sorl-thumbnail'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
