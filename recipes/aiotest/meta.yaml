package:
  name: aiotest
  version: "0.2"

source:
  fn: aiotest-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aiotest/aiotest-0.2.tar.gz
  md5: 0700577f5b44e8ab25508d62bc4407b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiotest = aiotest:main
    #
    # Would create an entry point called aiotest that calls aiotest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aiotest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/aiotest
  license: Apache Software License
  summary: 'test suite to validate an implementation of the asyncio API, the PEP 3156'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
