package:
  name: thinkify
  version: "0.2.8"

source:
  fn: thinkify-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/t/thinkify/thinkify-0.2.8.tar.gz
  md5: c690c09380c31e59906ca63dc66bba8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thinkify = thinkify:main
    #
    # Would create an entry point called thinkify that calls thinkify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyserial >=2.6

  run:
    - python
    - pyserial >=2.6

test:
  # Python imports
  imports:
    - thinkify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cooncesean/thinkify
  license: LICENSE.txt
  summary: 'A Python wrapper for the Thinkify Reader Protocol.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
