package:
  name: collective.teaser
  version: "2.3"

source:
  fn: collective.teaser-2.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.teaser/collective.teaser-2.3.tar.gz
  md5: adf544fa243fad7fc956100d21fa3ec3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.teaser = collective.teaser:main
    #
    # Would create an entry point called collective.teaser that calls collective.teaser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.formwidget.contenttree
    - archetypes.referencebrowserwidget
    - plone.app.portlets
    - node

  run:
    - python
    - setuptools
    - setuptools
    - plone.formwidget.contenttree
    - archetypes.referencebrowserwidget
    - plone.app.portlets
    - node

test:
  # Python imports
  imports:
    - collective
    - collective.teaser
    - collective.teaser.browser
    - collective.teaser.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.teaser
  license: GPL
  summary: 'Teaser/Banner content type for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
