package:
  name: bda.calendar.base
  version: "1.2.2"

source:
  fn: bda.calendar.base-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.calendar.base/bda.calendar.base-1.2.2.tar.gz
  md5: 3b2c1d639b6bac4824b2ae5b2cb82e8f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.calendar.base = bda.calendar.base:main
    #
    # Would create an entry point called bda.calendar.base that calls bda.calendar.base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pytz
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - pytz
    - zope.interface

test:
  # Python imports
  imports:
    - bda
    - bda.calendar
    - bda.calendar.base

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/bda.calendar.base
  license: GNU General Public License (GPL)
  summary: 'Base common calendaring features: Convinience or not coverd yet.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
