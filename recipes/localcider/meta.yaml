package:
  name: localcider
  version: "0.1.7"

source:
  fn: localcider-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/l/localcider/localcider-0.1.7.tar.gz
  md5: 4586ad56a449393249d401b7502d2643
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - localcider = localcider:main
    #
    # Would create an entry point called localcider that calls localcider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - matplotlib

  run:
    - python
    - numpy
    - matplotlib

test:
  # Python imports
  imports:
    - localcider
    - localcider.backend
    - localcider.backend.data
    - localcider.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pappulab.github.io/localCIDER/
  license: LICENSE.txt
  summary: 'Tools for calculating sequence properties of disordered proteins [from the Pappu Lab in at Washington University in St. Louis]'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
