package:
  name: django-templatecomponents
  version: "0.03"

source:
  fn: django-templatecomponents-0.03.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templatecomponents/django-templatecomponents-0.03.tar.gz
  md5: 114f98b2909633ad624294c99e079a99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templatecomponents = django-templatecomponents:main
    #
    # Would create an entry point called django-templatecomponents that calls django-templatecomponents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - management
    - management.commands
    - templatecomponents
    - templatecomponents.management
    - templatecomponents.management.commands
    - templatecomponents.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://j03.de/projects/django-templatecomponents/
  license: Beerware
  summary: 'django-templatecomponents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
