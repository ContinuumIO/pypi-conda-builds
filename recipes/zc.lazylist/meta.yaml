package:
  name: zc.lazylist
  version: "0.1.1"

source:
  fn: zc.lazylist-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.lazylist/zc.lazylist-0.1.1.tar.gz
  md5: b1c6848882f01a166297346c5da4c783
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.lazylist = zc.lazylist:main
    #
    # Would create an entry point called zc.lazylist that calls zc.lazylist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zope.testing
    - setuptools

  run:
    - python
    - zope.testing
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.lazylist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A buffer enabled iterable wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
