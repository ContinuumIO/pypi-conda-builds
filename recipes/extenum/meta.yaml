package:
  name: extenum
  version: "0.8.0"

source:
  fn: extenum-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/e/extenum/extenum-0.8.0.tar.gz
  md5: bd2e56d98d0e46f44701efd04bdda6f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extenum = extenum:main
    #
    # Would create an entry point called extenum that calls extenum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - enum34

  run:
    - python
    - enum34

test:
  # Python imports
  imports:
    - extenum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/t2y/extenum
  license: Apache Software License
  summary: 'Extended Enum classes for the Python 3 enum module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
