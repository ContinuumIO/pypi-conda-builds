package:
  name: aws-hostname
  version: "0.0.2"

source:
  fn: aws-hostname-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aws-hostname/aws-hostname-0.0.2.tar.gz
  md5: 5659bb5ca601ca915cb95301d72900e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aws-hostname = aws-hostname:main
    #
    # Would create an entry point called aws-hostname that calls aws-hostname.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.15.0

  run:
    - python
    - boto >=2.15.0

test:
  # Python imports
  imports:
    - aws_hostname

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/josegonzalez/python-aws-hostname
  license: MIT License
  summary: 'Outputs a valid hostname for a given AWS instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
