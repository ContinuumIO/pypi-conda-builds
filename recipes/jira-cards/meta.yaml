package:
  name: jira-cards
  version: "0.2.0"

source:
  fn: jira-cards-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jira-cards/jira-cards-0.2.0.tar.gz
  md5: 6f57d62c252699058861c55a5e2e6b2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jira-cards = jira-cards:main
    #
    # Would create an entry point called jira-cards that calls jira-cards.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cement >=2.2.2
    - requests >=2.3.0
    - jinja2 >=2.7.2

  run:
    - python
    - cement >=2.2.2
    - requests >=2.3.0
    - jinja2 >=2.7.2

test:
  # Python imports
  imports:
    - jiracards

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hypebeast/jiraCards
  license: jiracli
  summary: 'JiraCards prints agile cards for your physical board from Jira. The issues are read from a Jira Agile Board or individual issues can be provided to create single cards.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
