package:
  name: products.quota
  version: "1.0b3"

source:
  fn: Products.Quota-1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.Quota/Products.Quota-1.0b3.tar.gz
  md5: 7b70919e19256547937f72b5d262a8b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.quota = products.quota:main
    #
    # Would create an entry point called products.quota that calls products.quota.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.Quota
    - Products.Quota.browser
    - Products.Quota.content
    - Products.Quota.dexterity
    - Products.Quota.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/quota
  license: GPL
  summary: 'Adds a folder type with a size limit to your Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
