package:
  name: miniconf
  version: "1.2.0"

source:
  fn: miniconf-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/miniconf/miniconf-1.2.0.tar.gz
  md5: 0ae38af15f98706d7490b828e31c6936
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - miniconf = miniconf:main
    #
    # Would create an entry point called miniconf that calls miniconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - miniconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Python Software Foundation License
  summary: 'Plain text Python built-in objects persistence'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
