package:
  name: django-url-history
  version: "1.0.1"

source:
  fn: django-url-history-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-history/django-url-history-1.0.1.tar.gz
  md5: 492be442da3ef0e14521cd23985b3b2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-history = django-url-history:main
    #
    # Would create an entry point called django-url-history that calls django-url-history.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >1.4,<1.7

  run:
    - python
    - django >1.4,<1.7

test:
  # Python imports
  imports:
    - url_history

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brightinteractive/django-url-history/
  license: BSD
  summary: 'Store the last few URLs visited in the session to aid debugging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
