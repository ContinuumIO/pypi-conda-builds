package:
  name: djangorestframework-digestauth
  version: "1.1.0"

source:
  fn: djangorestframework-digestauth-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-digestauth/djangorestframework-digestauth-1.1.0.tar.gz
  md5: fbef9f53f4b519c58c3e7c28a7c0526c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-digestauth = djangorestframework-digestauth:main
    #
    # Would create an entry point called djangorestframework-digestauth that calls djangorestframework-digestauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - djangorestframework >=2.1.15
    - six ==1.5

  run:
    - python
    - django >=1.4.3
    - djangorestframework >=2.1.15
    - six ==1.5

test:
  # Python imports
  imports:
    - rest_framework_digestauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/juanriaza/django-rest-framework-digestauth
  license: BSD License
  summary: 'Digest HTTP auth support for Django REST framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
