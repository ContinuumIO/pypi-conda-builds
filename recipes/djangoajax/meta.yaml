package:
  name: djangoajax
  version: "2.2.15"

source:
  fn: djangoajax-2.2.15.tar.gz
  url: https://pypi.python.org/packages/source/d/djangoajax/djangoajax-2.2.15.tar.gz
  md5: 4f88d1c1a7abd34ba97da20015cf3ed8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoajax = djangoajax:main
    #
    # Would create an entry point called djangoajax that calls djangoajax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - setuptools
    - django >=1.3

test:
  # Python imports
  imports:
    - django_ajax
    - djangoajax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yceruto/django-ajax
  license: MIT License
  summary: 'Powerful and easy AJAX framework for django applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
