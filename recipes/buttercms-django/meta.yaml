package:
  name: buttercms-django
  version: "0.2.5"

source:
  fn: buttercms-django-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/b/buttercms-django/buttercms-django-0.2.5.tar.gz
  md5: 8809b6062b7642ea777137e5a68e4637
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buttercms-django = buttercms-django:main
    #
    # Would create an entry point called buttercms-django that calls buttercms-django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - buttercms-django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.buttercms.com
  license: MIT License
  summary: 'Company blogs as a service. Built for developers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
