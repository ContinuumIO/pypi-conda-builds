package:
  name: holmesalf
  version: "0.1.3"

source:
  fn: holmesalf-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/holmesalf/holmesalf-0.1.3.tar.gz
  md5: b91c2e5651c9ffd313370b53bb0725be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - holmesalf = holmesalf:main
    #
    # Would create an entry point called holmesalf that calls holmesalf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - alf >=0.4.1
    - tornado-alf >=0.4.1

  run:
    - python
    - alf >=0.4.1
    - tornado-alf >=0.4.1

test:
  # Python imports
  imports:
    - holmesalf
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/holmes-app/holmes-alf
  license: MIT License
  summary: 'holmes-alf is a wrapper for OAuth 2 synchronous (based on alf) and asynchronous (based on the tornado-alf) clients that can bu used in holmes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
