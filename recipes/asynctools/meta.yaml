package:
  name: asynctools
  version: "0.1.2-2"

source:
  fn: asynctools-0.1.2-2.tar.gz
  url: https://pypi.python.org/packages/source/a/asynctools/asynctools-0.1.2-2.tar.gz
  md5: 1187bbd6b16e0d8d5a0f4144e840b42b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asynctools = asynctools:main
    #
    # Would create an entry point called asynctools that calls asynctools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asynctools
    - asynctools.threading

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kolypto/py-asynctools
  license: BSD
  summary: ' Async tools for Python '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
