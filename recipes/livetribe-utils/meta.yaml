package:
  name: livetribe-utils
  version: "1.0a4"

source:
  fn: livetribe-utils-1.0a4.tar.gz
  url: https://pypi.python.org/packages/source/l/livetribe-utils/livetribe-utils-1.0a4.tar.gz
  md5: d33ff13e84bab1bc0c5358d5efb526cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - livetribe-utils = livetribe-utils:main
    #
    # Would create an entry point called livetribe-utils that calls livetribe-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - livetribe
    - livetribe.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/livetribe/livetribe-utils/
  license: Apache Software License
  summary: 'Shared utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
