package:
  name: messytables
  version: "0.14.5"

source:
  fn: messytables-0.14.5.tar.gz
  url: https://pypi.python.org/packages/source/m/messytables/messytables-0.14.5.tar.gz
  md5: 21dd89cfdf7e4ff8c4dfeb7937a6fde3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - messytables = messytables:main
    #
    # Would create an entry point called messytables that calls messytables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlrd >=0.8.0
    - python-magic ==0.4.6
    - chardet ==2.1.1
    - python-dateutil >=1.5.0,<2.0.0
    - json-table-schema
    - lxml >=3.2
    - requests
    - html5lib

  run:
    - python
    - setuptools
    - xlrd >=0.8.0
    - python-magic ==0.4.6
    - chardet ==2.1.1
    - python-dateutil >=1.5.0,<2.0.0
    - json-table-schema
    - lxml >=3.2
    - requests
    - html5lib

test:
  # Python imports
  imports:
    - messytables

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://okfn.org
  license: MIT License
  summary: 'Parse messy tabular data in various formats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
