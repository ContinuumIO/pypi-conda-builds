package:
  name: django-rulez
  version: "1.0.2"

source:
  fn: django-rulez-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rulez/django-rulez-1.0.2.tar.gz
  md5: 4168d0c61632d289cf912ac3bd7ccbe3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rulez = django-rulez:main
    #
    # Would create an entry point called django-rulez that calls django-rulez.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rulez
    - rulez.rolez
    - rulez.templatetags
    - rulez.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chrisglass/django-rulez
  license: BSD License
  summary: 'A lean and mean object-level rules system for the Django framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
