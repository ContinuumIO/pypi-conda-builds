package:
  name: django-bankdownloads
  version: "0.1.0"

source:
  fn: django-bankdownloads-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bankdownloads/django-bankdownloads-0.1.0.tar.gz
  md5: 4da61df978968311b53bde0aab53d7c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bankdownloads = django-bankdownloads:main
    #
    # Would create an entry point called django-bankdownloads that calls django-bankdownloads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bankdownloads
    - bankdownloads.management
    - bankdownloads.management.commands
    - bankdownloads.models
    - bankdownloads.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://celeryproject.org
  license: BSD License
  summary: 'Django-app for managing and processing bank transaction downloads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
