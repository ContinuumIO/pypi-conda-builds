package:
  name: svccache
  version: "0.1"

source:
  fn: svccache-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/svccache/svccache-0.1.tar.gz
  md5: 96fd0e3111ab69eda1ee2fe37d3c940c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - svccache = svccache:main
    #
    # Would create an entry point called svccache that calls svccache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - memcache
    - requests

  run:
    - python
    - memcache
    - requests

test:
  # Python imports
  imports:
    - svccache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/langloisjp/pysvccache
  license: MIT
  summary: 'Python caching utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
