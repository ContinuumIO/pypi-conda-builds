package:
  name: flask-elasticsearch
  version: "0.2"

source:
  fn: Flask-Elasticsearch-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Elasticsearch/Flask-Elasticsearch-0.2.tar.gz
  md5: cb8d5c271d4862da368c297d99dbd51f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-elasticsearch = flask-elasticsearch:main
    #
    # Would create an entry point called flask-elasticsearch that calls flask-elasticsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chiangf/Flask-Elasticsearch
  license: MIT License
  summary: 'Flask extension for Elasticsearch integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
