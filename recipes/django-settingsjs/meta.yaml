package:
  name: django-settingsjs
  version: "0.1.5"

source:
  fn: django-settingsjs-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-settingsjs/django-settingsjs-0.1.5.tar.gz
  md5: 12c49f1eb3c63dfc40dfd04541d3d575
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-settingsjs = django-settingsjs:main
    #
    # Would create an entry point called django-settingsjs that calls django-settingsjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - settingsjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/incuna/django-settingsjs/
  license: UNKNOWN
  summary: 'Configurable Javascript settings in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
