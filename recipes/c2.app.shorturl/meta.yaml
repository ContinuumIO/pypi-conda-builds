package:
  name: c2.app.shorturl
  version: "1.0b1"

source:
  fn: c2.app.shorturl-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/c2.app.shorturl/c2.app.shorturl-1.0b1.tar.gz
  md5: 1360cb4f6431d42f0be633ec86bd7699
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2.app.shorturl = c2.app.shorturl:main
    #
    # Would create an entry point called c2.app.shorturl that calls c2.app.shorturl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - c2
    - c2.app
    - c2.app.shorturl
    - c2.app.shorturl.browser
    - c2.app.shorturl.content
    - c2.app.shorturl.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/cmscom/c2.app.shorturl
  license: GPL
  summary: 'Create a Short URL for Plone content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
