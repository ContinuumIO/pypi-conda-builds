package:
  name: morf
  version: "0.2.5"

source:
  fn: morf-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/m/morf/morf-0.2.5.tar.gz
  md5: 3fd13d3a0382c801a98d057258ffbc68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - morf = morf:main
    #
    # Would create an entry point called morf that calls morf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - markupsafe
    - jinja2
    - ordereddict

  run:
    - python
    - python-dateutil
    - markupsafe
    - jinja2
    - ordereddict

test:
  # Python imports
  imports:
    - morf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ollycope.com/software/morf/
  license: Apache Software License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
