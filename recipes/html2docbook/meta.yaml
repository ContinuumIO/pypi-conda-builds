package:
  name: html2docbook
  version: "0.1"

source:
  fn: html2docbook-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/html2docbook/html2docbook-0.1.tar.gz
  md5: d4af6a8e28c7250f646bfd407dab1f24
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html2docbook = html2docbook:main
    #
    # Would create an entry point called html2docbook that calls html2docbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - beautifulsoup
    - lxml

  run:
    - python
    - setuptools
    - nose
    - beautifulsoup
    - lxml

test:
  # Python imports
  imports:
    - html2docbook
    - html2docbook.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'HTML to DocBook transformation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
