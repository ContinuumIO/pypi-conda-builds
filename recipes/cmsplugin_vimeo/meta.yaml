package:
  name: cmsplugin_vimeo
  version: "0.1"

source:
  fn: cmsplugin_vimeo-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_vimeo/cmsplugin_vimeo-0.1.tar.gz
  md5: 536a71beed081685a8762d579e8e2a22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_vimeo = cmsplugin_vimeo:main
    #
    # Would create an entry point called cmsplugin_vimeo that calls cmsplugin_vimeo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - django-cms >=2.3

  run:
    - python
    - django >=1.4
    - django-cms >=2.3

test:
  # Python imports
  imports:
    - cmsplugin_vimeo
    - cmsplugin_vimeo.migrations
    - cmsplugin_vimeo.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Immediatic/cmsplugin_vimeo
  license: BSD License
  summary: 'Plugin for Django CMS that makes easy to embed Vimeo movies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
