package:
  name: collective.explicitacquisition
  version: "1.1"

source:
  fn: collective.explicitacquisition-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.explicitacquisition/collective.explicitacquisition-1.1.tar.gz
  md5: d74b2fc46a37c972ab309489efc60626
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.explicitacquisition = collective.explicitacquisition:main
    #
    # Would create an entry point called collective.explicitacquisition that calls collective.explicitacquisition.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.cmfplone
    - zope2
    - zexceptions
    - zope.component
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.cmfplone
    - zope2
    - zexceptions
    - zope.component
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.explicitacquisition
    - collective.explicitacquisition.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.explicitacquisition
  license: GPL
  summary: 'Disallow access to acquired content outside the current path.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
