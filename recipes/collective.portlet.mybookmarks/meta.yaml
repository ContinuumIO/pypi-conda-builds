package:
  name: collective.portlet.mybookmarks
  version: "1.3.4"

source:
  fn: collective.portlet.mybookmarks-1.3.4.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.mybookmarks/collective.portlet.mybookmarks-1.3.4.zip
  md5: 9baace8173a80e143a9fde2acb4e7581
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.mybookmarks = collective.portlet.mybookmarks:main
    #
    # Would create an entry point called collective.portlet.mybookmarks that calls collective.portlet.mybookmarks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.mybookmarks
    - collective.portlet.mybookmarks.browser
    - collective.portlet.mybookmarks.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.portlet.mybookmarks
  license: GPL
  summary: 'A portlet that allows to store some internal and external bookmarks for users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
