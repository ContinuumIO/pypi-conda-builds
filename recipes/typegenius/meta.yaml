package:
  name: typegenius
  version: "0.5"

source:
  fn: typegenius-0.5.tar.gz
  url: https://pypi.python.org/packages/source/t/typegenius/typegenius-0.5.tar.gz
  md5: 3a07c6099e6a288482523abd33a7b56f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typegenius = typegenius:main
    #
    # Would create an entry point called typegenius that calls typegenius.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - typegenius

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stphivos/type-genius
  license: UNKNOWN
  summary: 'A library that can recognize and parse variables to the appropriate built-in type'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
