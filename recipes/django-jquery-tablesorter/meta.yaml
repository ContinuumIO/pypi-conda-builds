package:
  name: django-jquery-tablesorter
  version: "2.12.0"

source:
  fn: django-jquery-tablesorter-2.12.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery-tablesorter/django-jquery-tablesorter-2.12.0.tar.gz
  md5: 650b17646740a03bc32d7d2be7787691
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery-tablesorter = django-jquery-tablesorter:main
    #
    # Would create an entry point called django-jquery-tablesorter that calls django-jquery-tablesorter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jquery >=1.2.6

  run:
    - python
    - django-jquery >=1.2.6

test:
  # Python imports
  imports:
    - jquery_tablesorter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikebryant/django-jquery-tablesorter
  license: GPL
  summary: 'Django package for the tablesorter plugin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
