package:
  name: django_tlsauth
  version: "0.1.2"

source:
  fn: django_tlsauth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_tlsauth/django_tlsauth-0.1.2.tar.gz
  md5: 52479ca0621588b05b632b72aeb5d72a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_tlsauth = django_tlsauth:main
    #
    # Would create an entry point called django_tlsauth that calls django_tlsauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tlsauth
    - django

  run:
    - python
    - tlsauth
    - django

test:
  # Python imports
  imports:
    - demo
    - django_tlsauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django_tlsauth
  license: BSD License
  summary: 'Django app implementing TLS Authentication - simple client certificate CA inclusive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
