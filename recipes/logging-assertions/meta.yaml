package:
  name: logging-assertions
  version: "0.0.1"

source:
  fn: logging-assertions-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/logging-assertions/logging-assertions-0.0.1.tar.gz
  md5: e46ff0dc6ff56791de384b8f88c57b4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logging-assertions = logging-assertions:main
    #
    # Would create an entry point called logging-assertions that calls logging-assertions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wolever/logging-assertions
  license: BSD License
  summary: 'Easily make assertions about logged messages in tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
