package:
  name: flask-outdated-browser
  version: "0.1"

source:
  fn: Flask-outdated-browser-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-outdated-browser/Flask-outdated-browser-0.1.tar.gz
  md5: 7ec6fd7d2af1ef8f145948847805b5e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-outdated-browser = flask-outdated-browser:main
    #
    # Would create an entry point called flask-outdated-browser that calls flask-outdated-browser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_outdated_browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ondoheer/flask_outdated_browser
  license: MIT License
  summary: 'Easily add outdated-browser project to your Flask project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
