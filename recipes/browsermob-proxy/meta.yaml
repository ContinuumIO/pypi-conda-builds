package:
  name: browsermob-proxy
  version: "0.6.0"

source:
  fn: browsermob-proxy-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/b/browsermob-proxy/browsermob-proxy-0.6.0.tar.gz
  md5: 1c64df4541fcc22747af3dca8aff0636
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - browsermob-proxy = browsermob-proxy:main
    #
    # Would create an entry point called browsermob-proxy that calls browsermob-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1.0

  run:
    - python
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - browsermobproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://oss.theautomatedtester.co.uk/browsermob-proxy-py
  license: Apache Software License
  summary: 'A library for interacting with the Browsermob Proxy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
