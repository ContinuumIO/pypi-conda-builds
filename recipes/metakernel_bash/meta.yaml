package:
  name: metakernel_bash
  version: "0.7.0"

source:
  fn: metakernel_bash-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/m/metakernel_bash/metakernel_bash-0.7.0.tar.gz
  md5: b261ef275abefff4bda6149e6b4a59ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metakernel_bash = metakernel_bash:main
    #
    # Would create an entry point called metakernel_bash that calls metakernel_bash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - metakernel

  run:
    - python
    - metakernel

test:
  # Python imports
  imports:
    - metakernel_bash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/calysto/metakernel/tree/master/metakernel_bash
  license: BSD License
  summary: 'A Bash kernel for Jupyter/IPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
