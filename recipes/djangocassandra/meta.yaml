package:
  name: djangocassandra
  version: "0.1.1"

source:
  fn: djangocassandra-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocassandra/djangocassandra-0.1.1.tar.gz
  md5: 94622f5822dcc048b136be1a3081610b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocassandra = djangocassandra:main
    #
    # Would create an entry point called djangocassandra that calls djangocassandra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.7,<1.8
    - cassandra-driver >=2.1.4
    - blist

  run:
    - python
    - django >=1.7,<1.8
    - cassandra-driver >=2.1.4
    - blist

test:
  # Python imports
  imports:
    - djangocassandra
    - djangocassandra.db
    - djangocassandra.db.backends
    - djangocassandra.db.backends.cassandra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Knotis/djangocassandra
  license: BSD License
  summary: 'Cassandra support for the Django web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
