package:
  name: itolapi
  version: "1.2.0"

source:
  fn: itolapi-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/itolapi/itolapi-1.2.0.tar.gz
  md5: c1be68d890946776e75c004b9385e62d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - itolapi = itolapi:main
    #
    # Would create an entry point called itolapi that calls itolapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0,<3.0

  run:
    - python
    - requests >=2.0,<3.0

test:
  # Python imports
  imports:
    - itolapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/albertyw/itolapi
  license: MIT License
  summary: 'API for interacting with itol.embl.de'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
