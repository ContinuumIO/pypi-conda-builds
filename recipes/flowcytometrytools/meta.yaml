package:
  name: flowcytometrytools
  version: "0.4.3"

source:
  fn: FlowCytometryTools-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/F/FlowCytometryTools/FlowCytometryTools-0.4.3.tar.gz
  md5: ac2d34cc852e06e683b23bcc7f47faba
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flowcytometrytools = flowcytometrytools:main
    #
    # Would create an entry point called flowcytometrytools that calls flowcytometrytools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator
    - goreutilities ==0.5.0

  run:
    - python
    - setuptools
    - decorator
    - goreutilities ==0.5.0

test:
  # Python imports
  imports:
    - FlowCytometryTools
    - FlowCytometryTools.GUI
    - FlowCytometryTools.IO
    - FlowCytometryTools.core
    - FlowCytometryTools.tests
    - flowcytometrytools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gorelab.bitbucket.org/flowcytometrytools
  license: MIT
  summary: 'A python package for performing flow cytometry analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
