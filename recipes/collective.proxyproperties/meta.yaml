package:
  name: collective.proxyproperties
  version: "0.3"

source:
  fn: collective.proxyproperties-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.proxyproperties/collective.proxyproperties-0.3.zip
  md5: 542b8e06b2ba71a94dd5c05b6c8619c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.proxyproperties = collective.proxyproperties:main
    #
    # Would create an entry point called collective.proxyproperties that calls collective.proxyproperties.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

test:
  # Python imports
  imports:
    - collective
    - collective.proxyproperties
    - collective.proxyproperties.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Local utility to set portal properties for a sub site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
