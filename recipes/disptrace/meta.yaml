package:
  name: disptrace
  version: "0.4"

source:
  fn: disptrace-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/disptrace/disptrace-0.4.tar.gz
  md5: b93459c4f89bd01ac7428ef0fa2e2fc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - disptrace = disptrace:main
    #
    # Would create an entry point called disptrace that calls disptrace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - disptrace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/atsuoishimoto/disptrace
  license: MIT License
  summary: 'Generate Python trace log in HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
