package:
  name: djangorestframework-apidoc
  version: "0.0.1"

source:
  fn: djangorestframework-apidoc-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-apidoc/djangorestframework-apidoc-0.0.1.tar.gz
  md5: e01bfcd7c0dfbc35200016bc39bc94e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-apidoc = djangorestframework-apidoc:main
    #
    # Would create an entry point called djangorestframework-apidoc that calls djangorestframework-apidoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-markup
    - djangorestframework

  run:
    - python
    - django
    - django-markup
    - djangorestframework

test:
  # Python imports
  imports:
    - rest_framework_apidoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleidos/djangorestframework-apidoc
  license: BSD License
  summary: 'Modular documentation generator for rest framwork apis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
