package:
  name: plone.batching
  version: "1.0.4"

source:
  fn: plone.batching-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.batching/plone.batching-1.0.4.tar.gz
  md5: 562c51961bd59503d1a89dd306db235f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.batching = plone.batching:main
    #
    # Would create an entry point called plone.batching that calls plone.batching.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.batching

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.batching
  license: GPL
  summary: 'Batching facilities used in Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
