package:
  name: django-genericadmin
  version: "0.6.1"

source:
  fn: django-genericadmin-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-genericadmin/django-genericadmin-0.6.1.tar.gz
  md5: 4fb8646891b6ade064ecb8f4a2231e8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-genericadmin = django-genericadmin:main
    #
    # Would create an entry point called django-genericadmin that calls django-genericadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - genericadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jschrewe/django-genericadmin
  license: BSD License
  summary: "Adds support for generic relations within Django's admin interface."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
