package:
  name: django-sts
  version: "0.7.3"

source:
  fn: django-sts-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sts/django-sts-0.7.3.tar.gz
  md5: 3692660aa691155a68c6fce5744285e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sts = django-sts:main
    #
    # Would create an entry point called django-sts that calls django-sts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - sts
    - sts.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cbmi/django-sts/
  license: BSD License
  summary: 'State Transition System for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
