package:
  name: django-payonline
  version: "0.1.1"

source:
  fn: django-payonline-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-payonline/django-payonline-0.1.1.tar.gz
  md5: af925b68b985144c87e90879e4364888
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-payonline = django-payonline:main
    #
    # Would create an entry point called django-payonline that calls django-payonline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - payonline
    - payonline.fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ISC License (ISCL)
  summary: 'A Django application for integration with PayOnline System'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
