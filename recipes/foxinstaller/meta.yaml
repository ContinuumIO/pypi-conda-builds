package:
  name: foxinstaller
  version: "0.2.015"

source:
  fn: foxInstaller-0.2.015.zip
  url: https://pypi.python.org/packages/source/f/foxInstaller/foxInstaller-0.2.015.zip
  md5: e5fa0b279242e2b43d58c9f23462e97f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foxinstaller = foxinstaller:main
    #
    # Would create an entry point called foxinstaller that calls foxinstaller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - foxInstaller
    - foxinstaller

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://th3f0x.com
  license: BSD License
  summary: 'A simple installation script for my own django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
