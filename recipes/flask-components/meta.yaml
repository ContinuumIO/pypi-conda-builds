package:
  name: flask-components
  version: "0.1.1"

source:
  fn: Flask-Components-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Components/Flask-Components-0.1.1.tar.gz
  md5: 0154bb7697bbe2a03f820fc4bf1a7450
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-components = flask-components:main
    #
    # Would create an entry point called flask-components that calls flask-components.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9.0

  run:
    - python
    - flask >=0.9.0

test:
  # Python imports
  imports:
    - flask_components

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/concordusapps/flask-components
  license: MIT License
  summary: 'A simple flask extension to discover files in a declared array of components.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
