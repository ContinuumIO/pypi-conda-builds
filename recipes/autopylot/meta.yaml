package:
  name: autopylot
  version: "1.9.6"

source:
  fn: autopylot-1.9.6.tar.gz
  url: https://pypi.python.org/packages/source/a/autopylot/autopylot-1.9.6.tar.gz
  md5: c9e71c7607c1cdcbe2a3dc1491dc140e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autopylot = autopylot:main
    #
    # Would create an entry point called autopylot that calls autopylot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.5
    - coverage >=3.6
    - mock >=1.0.1
    - nose >=1.3.0
    - requests
    - wsgiref ==0.1.2

  run:
    - python
    - django >=1.4.5
    - coverage >=3.6
    - mock >=1.0.1
    - nose >=1.3.0
    - requests
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - autopylot
    - autopylot.django
    - autopylot.django.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattcaldwell/autopYlot
  license: UNKNOWN
  summary: 'Put your Python on autopylot.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
