package:
  name: python-novaclient
  version: "2.25.0"

source:
  fn: python-novaclient-2.25.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-novaclient/python-novaclient-2.25.0.tar.gz
  md5: a02254b37c1bca0581689d20f9f6bf4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-novaclient = python-novaclient:main
    #
    # Would create an entry point called python-novaclient that calls python-novaclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbr

  run:
    - python
    - pbr

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Client library for OpenStack Compute API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
