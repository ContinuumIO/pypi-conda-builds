package:
  name: megrok.z3cform.ui
  version: "0.5"

source:
  fn: megrok.z3cform.ui-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.z3cform.ui/megrok.z3cform.ui-0.5.tar.gz
  md5: 3a7e62770dce28d18ef5f5c9d8aba59b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.z3cform.ui = megrok.z3cform.ui:main
    #
    # Would create an entry point called megrok.z3cform.ui that calls megrok.z3cform.ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.formui

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.formui

test:
  # Python imports
  imports:
    - megrok
    - megrok.z3cform
    - megrok.z3cform.ui
    - megrok.z3cform.ui.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Installation Helper for z3c.formui'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
