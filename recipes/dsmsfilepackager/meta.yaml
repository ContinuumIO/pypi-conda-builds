package:
  name: dsmsfilepackager
  version: "0.1.10"

source:
  fn: dsmsfilepackager-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/dsmsfilepackager/dsmsfilepackager-0.1.10.tar.gz
  md5: 08662a788c83d33c49eb3eccf06f0568
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dsmsfilepackager = dsmsfilepackager:main
    #
    # Would create an entry point called dsmsfilepackager that calls dsmsfilepackager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sftpsyncer ==0.1.8
    - python-magic

  run:
    - python
    - sftpsyncer ==0.1.8
    - python-magic

test:
  # Python imports
  imports:
    - dsmsfilepackager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dsms/dsmsfilepackager/
  license: LICENSE.txt
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
