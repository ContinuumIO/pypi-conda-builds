package:
  name: redis-collections
  version: "0.1.7"

source:
  fn: redis-collections-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-collections/redis-collections-0.1.7.tar.gz
  md5: 67aa817d9a2f1f63b3b3251062762e7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-collections = redis-collections:main
    #
    # Would create an entry point called redis-collections that calls redis-collections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.7.2

  run:
    - python
    - redis >=2.7.2

test:
  # Python imports
  imports:
    - redis_collections

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/honzajavorek/redis-collections
  license: ISC License (ISCL)
  summary: 'Set of basic Python collections backed by Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
