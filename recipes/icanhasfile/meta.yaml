package:
  name: icanhasfile
  version: "0.1.0.dev"

source:
  fn: icanhasfile-0.1.0.dev.tar.gz
  url: https://pypi.python.org/packages/source/i/icanhasfile/icanhasfile-0.1.0.dev.tar.gz
  md5: 0617dcf081008ac34ddfd9b0a79ab65e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icanhasfile = icanhasfile:main
    #
    # Would create an entry point called icanhasfile that calls icanhasfile.main()

    - icanhasfile = icanhasfile:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt

  run:
    - python
    - docopt

test:
  # Python imports
  imports:
    - icanhasfile

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - icanhasfile --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.artloder.com/code/icanhasfile/
  license: MIT License
  summary: 'COMPUTR, U HAS FILE. I CAN HAS IT?'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
