package:
  name: m2cryptowin32
  version: "0.21.1-3"

source:
  fn: M2CryptoWin32-0.21.1-3.tar.gz
  url: https://pypi.python.org/packages/source/M/M2CryptoWin32/M2CryptoWin32-0.21.1-3.tar.gz
  md5: caad41e96f07601aa5fcb729c2260d2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m2cryptowin32 = m2cryptowin32:main
    #
    # Would create an entry point called m2cryptowin32 that calls m2cryptowin32.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - M2Crypto
    - M2Crypto.PGP
    - M2Crypto.SSL
    - m2cryptowin32

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dsoprea/M2CryptoWin32
  license: UNKNOWN
  summary: 'M2Crypto for Windows (32-bit)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
