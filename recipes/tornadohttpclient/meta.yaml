package:
  name: tornadohttpclient
  version: "0.3.2"

source:
  fn: tornadohttpclient-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadohttpclient/tornadohttpclient-0.3.2.tar.gz
  md5: 74b94a7db18a6e9206e9cd59a9a26325
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadohttpclient = tornadohttpclient:main
    #
    # Would create an entry point called tornadohttpclient that calls tornadohttpclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - pycurl

  run:
    - python
    - tornado
    - pycurl

test:
  # Python imports
  imports:
    - tornadohttpclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linuxzen.com
  license: Apache Software License
  summary: 'Asynchronous http client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
