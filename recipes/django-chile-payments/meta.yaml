package:
  name: django-chile-payments
  version: "0.2.0"

source:
  fn: django-chile-payments-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chile-payments/django-chile-payments-0.2.0.tar.gz
  md5: 54aaf1f3d3a771a1f8f351059d9bb0b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chile-payments = django-chile-payments:main
    #
    # Would create an entry point called django-chile-payments that calls django-chile-payments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto
    - requests
    - rsa

  run:
    - python
    - pycrypto
    - requests
    - rsa

test:
  # Python imports
  imports:
    - getpaid
    - getpaid.backends
    - getpaid.backends.dummy
    - getpaid.backends.webpay
    - getpaid.backends.webpaypure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Nomadblue/django-chile-payments
  license: MIT License
  summary: 'Chilean payment brokers for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
