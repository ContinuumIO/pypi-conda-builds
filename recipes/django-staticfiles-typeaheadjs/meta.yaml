package:
  name: django-staticfiles-typeaheadjs
  version: "0.9.1"

source:
  fn: django-staticfiles-typeaheadjs-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-typeaheadjs/django-staticfiles-typeaheadjs-0.9.1.tar.gz
  md5: 3bd9881d8a5b8542a0d8925388d7bc3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-typeaheadjs = django-staticfiles-typeaheadjs:main
    #
    # Would create an entry point called django-staticfiles-typeaheadjs that calls django-staticfiles-typeaheadjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - typeaheadjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tswicegood/django-staticfiles-typeaheadjs/
  license: BSD License
  summary: 'typeahead.js meets Django staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
