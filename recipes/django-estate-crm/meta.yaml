package:
  name: django-estate-crm
  version: "0.1.0"

source:
  fn: django-estate-crm-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-estate-crm/django-estate-crm-0.1.0.tar.gz
  md5: 0c5941bad5aa30937958e0f59b358285
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-estate-crm = django-estate-crm:main
    #
    # Would create an entry point called django-estate-crm that calls django-estate-crm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-estate >=0.1.0
    - django-crm >=0.1.0

  run:
    - python
    - django-estate >=0.1.0
    - django-crm >=0.1.0

test:
  # Python imports
  imports:
    - realestate_crm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.de-nieuwbouwmakelaar.nl
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
