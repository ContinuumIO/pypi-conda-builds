package:
  name: jetway
  version: "0.0.512"

source:
  fn: jetway-0.0.512.tar.gz
  url: https://pypi.python.org/packages/source/j/jetway/jetway-0.0.512.tar.gz
  md5: 753bb38b2d47c96381f44cb3f16e05ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jetway = jetway:main
    #
    # Would create an entry point called jetway that calls jetway.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coverage ==3.7.1
    - google-api-python-client ==1.3.1
    - httplib2 ==0.9
    - keyring >=2.1
    - nose ==1.3.1
    - oauth2client ==1.4.6
    - progressbar2 ==2.6.2
    - rednose ==0.4.1
    - requests

  run:
    - python
    - coverage ==3.7.1
    - google-api-python-client ==1.3.1
    - httplib2 ==0.9
    - keyring >=2.1
    - nose ==1.3.1
    - oauth2client ==1.4.6
    - progressbar2 ==2.6.2
    - rednose ==0.4.1
    - requests

test:
  # Python imports
  imports:
    - jetway

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/grow/jetway-client
  license: MIT License
  summary: 'Client library for the Jetway static site staging service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
