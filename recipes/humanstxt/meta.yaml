package:
  name: humanstxt
  version: "0.1"

source:
  fn: humanstxt-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/humanstxt/humanstxt-0.1.tar.gz
  md5: 727aab6151e0993e20150fa85eac230d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - humanstxt = humanstxt:main
    #
    # Would create an entry point called humanstxt that calls humanstxt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - humanstxt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/whtsky/humanstxt
  license: MIT License
  summary: 'A python library for dealing with humans.txt'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
