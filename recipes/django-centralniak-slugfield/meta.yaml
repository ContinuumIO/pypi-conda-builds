package:
  name: django-centralniak-slugfield
  version: "0.1.5"

source:
  fn: django-centralniak-slugfield-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-centralniak-slugfield/django-centralniak-slugfield-0.1.5.tar.gz
  md5: ef9be012704e91ce111c70f9c38e05d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-centralniak-slugfield = django-centralniak-slugfield:main
    #
    # Would create an entry point called django-centralniak-slugfield that calls django-centralniak-slugfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_centralniak_slugfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/centralniak
  license: BSD License
  summary: 'Provides more handy support for slugs in Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
