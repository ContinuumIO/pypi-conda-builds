package:
  name: jenkins-job-builder-naginator
  version: "0.0.1"

source:
  fn: jenkins-job-builder-naginator-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jenkins-job-builder-naginator/jenkins-job-builder-naginator-0.0.1.tar.gz
  md5: c5d7f4687b9399c2f8df9514a641371b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jenkins-job-builder-naginator = jenkins-job-builder-naginator:main
    #
    # Would create an entry point called jenkins-job-builder-naginator that calls jenkins-job-builder-naginator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License, Version 2.0
  summary: 'Automatically reschedule a build after a build failure.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
