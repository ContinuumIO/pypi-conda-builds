package:
  name: django-hotrunner
  version: "0.2.2"

source:
  fn: django-hotrunner-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hotrunner/django-hotrunner-0.2.2.tar.gz
  md5: 11ffe116014b94eaa54bfad577c8c4bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hotrunner = django-hotrunner:main
    #
    # Would create an entry point called django-hotrunner that calls django-hotrunner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/metametrics/django-hotrunner
  license: BSD License
  summary: 'HotRunner is a Django test runner with useful features, like excluding apps and displaying running times of individual tests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
