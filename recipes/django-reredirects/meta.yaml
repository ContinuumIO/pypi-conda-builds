package:
  name: django-reredirects
  version: "0.1.2"

source:
  fn: django-reredirects-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reredirects/django-reredirects-0.1.2.tar.gz
  md5: 54a889cdc3a945447bc2ec92f323d036
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reredirects = django-reredirects:main
    #
    # Would create an entry point called django-reredirects that calls django-reredirects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - reredirects

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmoswalt/django-reredirects
  license: BSD License
  summary: 'Django Redirects, but with regex and without the sites framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
