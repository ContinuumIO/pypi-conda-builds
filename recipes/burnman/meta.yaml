package:
  name: burnman
  version: "0.7"

source:
  fn: burnman-0.7.tar.gz
  url: https://pypi.python.org/packages/source/b/burnman/burnman-0.7.tar.gz
  md5: 741cdb60d27df0f7c851a7489992b469
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - burnman = burnman:main
    #
    # Would create an entry point called burnman that calls burnman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - matplotlib
    - scipy

  run:
    - python
    - numpy
    - matplotlib
    - scipy

test:
  # Python imports
  imports:
    - burnman
    - burnman.minerals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://burnman.org
  license: GPL
  summary: 'a lower mantle toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
