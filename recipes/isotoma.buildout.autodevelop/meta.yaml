package:
  name: isotoma.buildout.autodevelop
  version: "0.0.19"

source:
  fn: isotoma.buildout.autodevelop-0.0.19.zip
  url: https://pypi.python.org/packages/source/i/isotoma.buildout.autodevelop/isotoma.buildout.autodevelop-0.0.19.zip
  md5: 616e79f97bfc667987afe7f5254fc291
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.buildout.autodevelop = isotoma.buildout.autodevelop:main
    #
    # Would create an entry point called isotoma.buildout.autodevelop that calls isotoma.buildout.autodevelop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.buildout
    - isotoma.buildout.autodevelop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.buildout.autodevelop
  license: Apache Software License
  summary: 'Buildout extension to automatically develop eggs found in specified directories.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
