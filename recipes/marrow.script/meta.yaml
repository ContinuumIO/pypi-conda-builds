package:
  name: marrow.script
  version: "2.0.0b2"

source:
  fn: marrow.script-2.0.0b2.tar.gz
  url: https://pypi.python.org/packages/source/m/marrow.script/marrow.script-2.0.0b2.tar.gz
  md5: de44a334963974c298d1ae0193df6616
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marrow.script = marrow.script:main
    #
    # Would create an entry point called marrow.script that calls marrow.script.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - marrow.util

  run:
    - python
    - marrow.util

test:
  # Python imports
  imports:
    - marrow
    - marrow.script

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marrow/marrow.script
  license: MIT License
  summary: 'Turn any callable into a powerful command line script through arglist introspection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
