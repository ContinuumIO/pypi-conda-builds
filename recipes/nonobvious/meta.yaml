package:
  name: nonobvious
  version: "0.1"

source:
  fn: nonobvious-0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nonobvious/nonobvious-0.1.tar.gz
  md5: f9e6c2ff94f340514165227d319a9a2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nonobvious = nonobvious:main
    #
    # Would create an entry point called nonobvious that calls nonobvious.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - concon ==2.0
    - valideer ==0.3

  run:
    - python
    - concon ==2.0
    - valideer ==0.3

test:
  # Python imports
  imports:
    - nonobvious

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eykd/nonobvious
  license: BSD License
  summary: 'A simple, but non-obvious approach to setting boundaries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
