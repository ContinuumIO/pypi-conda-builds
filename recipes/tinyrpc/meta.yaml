package:
  name: tinyrpc
  version: "0.5"

source:
  fn: tinyrpc-0.5.tar.gz
  url: https://pypi.python.org/packages/source/t/tinyrpc/tinyrpc-0.5.tar.gz
  md5: 4bb9ed559bcccc4bf91a7843da781d28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tinyrpc = tinyrpc:main
    #
    # Would create an entry point called tinyrpc that calls tinyrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tinyrpc
    - tinyrpc.dispatch
    - tinyrpc.protocols
    - tinyrpc.server
    - tinyrpc.transports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/tinyrpc
  license: MIT
  summary: 'A small, modular, transport and protocol neutral RPC library that, among other things, supports JSON-RPC and zmq.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
