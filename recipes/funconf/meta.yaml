package:
  name: funconf
  version: "0.3.0"

source:
  fn: funconf-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/f/funconf/funconf-0.3.0.tar.gz
  md5: 6f6d124f40105afeee29be4386e37172
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - funconf = funconf:main
    #
    # Would create an entry point called funconf that calls funconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - funcsigs

  run:
    - python
    - pyyaml
    - funcsigs

test:
  # Python imports
  imports:
    - funconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjdorma/funconf
  license: Apache Software License
  summary: 'Function Configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
