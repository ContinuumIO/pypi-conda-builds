package:
  name: elasticpy
  version: "0.12"

source:
  fn: elasticpy-0.12.tar.gz
  url: https://pypi.python.org/packages/source/e/elasticpy/elasticpy-0.12.tar.gz
  md5: ca39f9a132c4fc5f257e5eb66b951a5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticpy = elasticpy:main
    #
    # Would create an entry point called elasticpy that calls elasticpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.12.1
    - simplejson >=2.1.6

  run:
    - python
    - requests >=0.12.1
    - simplejson >=2.1.6

test:
  # Python imports
  imports:
    - elasticpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ooici/elasticpy/
  license: Apache Software License
  summary: 'Python Wrapper for elasticsearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
