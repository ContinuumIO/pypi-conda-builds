package:
  name: enslyon.existda
  version: "1.6"

source:
  fn: enslyon.existda-1.6.zip
  url: https://pypi.python.org/packages/source/e/enslyon.existda/enslyon.existda-1.6.zip
  md5: e0854ba88ee8cd3cce9685cd4ead3a3e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - enslyon.existda = enslyon.existda:main
    #
    # Would create an entry point called enslyon.existda that calls enslyon.existda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - enslyon
    - enslyon.existda

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/pratic/enslyon.existda
  license: GPL
  summary: 'Connector to eXist XML Database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
