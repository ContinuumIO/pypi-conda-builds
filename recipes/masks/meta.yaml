package:
  name: masks
  version: "0.1.3"

source:
  fn: masks-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/masks/masks-0.1.3.tar.gz
  md5: ddb8fcf5a764594ea7cf0776840979e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - masks = masks:main
    #
    # Would create an entry point called masks that calls masks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pandas >=0.11.0
    - numpy >=1.7.1

  run:
    - python
    - pandas >=0.11.0
    - numpy >=1.7.1

test:
  # Python imports
  imports:
    - masks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/masks/
  license: LICENSE.txt
  summary: 'Syntactic sugar for data filtering with pandas.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
