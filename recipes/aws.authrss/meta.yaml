package:
  name: aws.authrss
  version: "1.0.0b2"

source:
  fn: aws.authrss-1.0.0b2.tar.gz
  url: https://pypi.python.org/packages/source/a/aws.authrss/aws.authrss-1.0.0b2.tar.gz
  md5: 8e330f4fe58ba154025c5c7ba06fcb53
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aws.authrss = aws.authrss:main
    #
    # Would create an entry point called aws.authrss that calls aws.authrss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.uuid
    - plone.app.uuid

  run:
    - python
    - setuptools
    - setuptools
    - plone.uuid
    - plone.app.uuid

test:
  # Python imports
  imports:
    - aws
    - aws.authrss
    - aws.authrss.browser
    - aws.authrss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/aws.authrss
  license: GNU General Public License (GPL)
  summary: 'Private Plone RSS feeds through a user private token'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
