package:
  name: cubicweb-nazcaui
  version: "0.3.1"

source:
  fn: cubicweb-nazcaui-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-nazcaui/cubicweb-nazcaui-0.3.1.tar.gz
  md5: 7069a666573a6bfa9e08dae062b55843
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-nazcaui = cubicweb-nazcaui:main
    #
    # Would create an entry point called cubicweb-nazcaui that calls cubicweb-nazcaui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nazca >=0.6.1
    - cubicweb-card
    - cubicweb-squareui >=0.3.2
    - cubicweb >=3.18.0
    - cubicweb-bootstrap >=0.5.2
    - cubicweb-file

  run:
    - python
    - nazca >=0.6.1
    - cubicweb-card
    - cubicweb-squareui >=0.3.2
    - cubicweb >=3.18.0
    - cubicweb-bootstrap >=0.5.2
    - cubicweb-file

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-nazcaui
  license: LGPL
  summary: 'A cube for a nazca usage example'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
