package:
  name: zopyx_gridfs
  version: "0.3.0"

source:
  fn: zopyx_gridfs-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zopyx_gridfs/zopyx_gridfs-0.3.0.tar.gz
  md5: c8da5745b7764c1e95034d920358f6f6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx_gridfs = zopyx_gridfs:main
    #
    # Would create an entry point called zopyx_gridfs that calls zopyx_gridfs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - weberror
    - pymongo

  run:
    - python
    - setuptools
    - pyramid
    - weberror
    - pymongo

test:
  # Python imports
  imports:
    - zopyx_gridfs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zopyx_gridfs
  license: UNKNOWN
  summary: 'A MongoDB-to-Web gateway based on the Pyramid web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
