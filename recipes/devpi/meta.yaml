package:
  name: devpi
  version: "2.1.0"

source:
  fn: devpi-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/devpi/devpi-2.1.0.tar.gz
  md5: 8d249364e614cf91ea502eae1a32bf27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - devpi = devpi:main
    #
    # Would create an entry point called devpi that calls devpi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - devpi-server >=2.1.0,<2.2dev
    - devpi-client >=2.0.3,<2.1dev
    - devpi-web >=2.2.0,<2.3dev

  run:
    - python
    - devpi-server >=2.1.0,<2.2dev
    - devpi-client >=2.0.3,<2.1dev
    - devpi-web >=2.2.0,<2.3dev

test:
  # Python imports
  imports:
    - devpi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://doc.devpi.net
  license: MIT License
  summary: 'devpi: github-style pypi index server and packaging meta tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
