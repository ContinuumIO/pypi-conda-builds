package:
  name: django-viewflow
  version: "0.8.0"

source:
  fn: django-viewflow-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewflow/django-viewflow-0.8.0.tar.gz
  md5: ceefcc0423407972ba141999d7bccca3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewflow = django-viewflow:main
    #
    # Would create an entry point called django-viewflow that calls django-viewflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - singledispatch >=3.0
    - django-fsm >=2.0
    - mock
    - django-webtest
    - webtest

  run:
    - python
    - django >=1.6
    - singledispatch >=3.0
    - django-fsm >=2.0
    - mock
    - django-webtest
    - webtest

test:
  # Python imports
  imports:
    - viewflow
    - viewflow.contrib
    - viewflow.flow
    - viewflow.management
    - viewflow.migrations
    - viewflow.south_migrations
    - viewflow.templatetags
    - viewflow.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/viewflow/viewflow
  license: BSD License
  summary: 'Reusable workflow library for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
