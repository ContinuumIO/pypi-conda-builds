package:
  name: dsmsmessages
  version: "0.1.54"

source:
  fn: dsmsmessages-0.1.54.tar.gz
  url: https://pypi.python.org/packages/source/d/dsmsmessages/dsmsmessages-0.1.54.tar.gz
  md5: 775ff8bb0e95320fff04e88a5f311018
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dsmsmessages = dsmsmessages:main
    #
    # Would create an entry point called dsmsmessages that calls dsmsmessages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jsonschema ==2.3.0
    - iso8601 ==0.1.10

  run:
    - python
    - jsonschema ==2.3.0
    - iso8601 ==0.1.10

test:
  # Python imports
  imports:
    - dsmsmessages
    - dsmsmessages.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dsms/dsmsmessages/
  license: LICENSE.txt
  summary: 'Messaging services for DSMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
