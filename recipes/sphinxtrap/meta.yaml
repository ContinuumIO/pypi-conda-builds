package:
  name: sphinxtrap
  version: "0.2.3"

source:
  fn: sphinxtrap-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxtrap/sphinxtrap-0.2.3.tar.gz
  md5: 5febe5ad586fa1d801628c8c52e1a846
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxtrap = sphinxtrap:main
    #
    # Would create an entry point called sphinxtrap that calls sphinxtrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.1

  run:
    - python
    - sphinx >=1.1

test:
  # Python imports
  imports:
    - sphinxtrap
    - sphinxtrap.ext
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jfardello/Sphinxtrap
  license: MIT License
  summary: 'Yet another bootstrap sphinx theme.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
