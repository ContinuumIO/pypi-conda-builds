package:
  name: sqlalchemy-defaults
  version: "0.4.4"

source:
  fn: SQLAlchemy-Defaults-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Defaults/SQLAlchemy-Defaults-0.4.4.tar.gz
  md5: 58b9fe940dba7f6b2211f59912cbfa0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-defaults = sqlalchemy-defaults:main
    #
    # Would create an entry point called sqlalchemy-defaults that calls sqlalchemy-defaults.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - sqlalchemy >=0.7.8

  run:
    - python
    - six
    - sqlalchemy >=0.7.8

test:
  # Python imports
  imports:
    - sqlalchemy_defaults

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/sqlalchemy-defaults
  license: BSD License
  summary: 'Smart SQLAlchemy defaults for lazy guys, like me.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
