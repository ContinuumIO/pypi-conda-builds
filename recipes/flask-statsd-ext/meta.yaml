package:
  name: flask-statsd-ext
  version: "0.0.2a1"

source:
  fn: Flask-Statsd-Ext-0.0.2a1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Statsd-Ext/Flask-Statsd-Ext-0.0.2a1.tar.gz
  md5: 3c8ce21a1a5c50fba4a76cb0694f0957
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-statsd-ext = flask-statsd-ext:main
    #
    # Would create an entry point called flask-statsd-ext that calls flask-statsd-ext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - statsd

  run:
    - python
    - flask
    - statsd

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nilas.com
  license: UNKNOWN
  summary: 'Statsd Extension for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
