package:
  name: django-vintage
  version: "0.3"

source:
  fn: django-vintage-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vintage/django-vintage-0.3.tar.gz
  md5: 27947499b0e10869baa9524115772d80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vintage = django-vintage:main
    #
    # Would create an entry point called django-vintage that calls django-vintage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-formfield >=0.1

  run:
    - python
    - django-formfield >=0.1

test:
  # Python imports
  imports:
    - example
    - example.simpleapp
    - vintage
    - vintage.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://callowayproject.com/projects/
  license: Apache Software License
  summary: 'An archiver of web pages. Like a specialized django-flatpages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
