package:
  name: atreal.filecart
  version: "1.1rc1"

source:
  fn: atreal.filecart-1.1rc1.zip
  url: https://pypi.python.org/packages/source/a/atreal.filecart/atreal.filecart-1.1rc1.zip
  md5: 61a2044bfc0fe98a0509ae5a7ef31d8a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.filecart = atreal.filecart:main
    #
    # Would create an entry point called atreal.filecart that calls atreal.filecart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.intid
    - atreal.override.albumview
    - plone.app.kss

  run:
    - python
    - setuptools
    - setuptools
    - five.intid
    - atreal.override.albumview
    - plone.app.kss

test:
  # Python imports
  imports:
    - atreal
    - atreal.filecart
    - atreal.filecart.browser
    - atreal.filecart.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.atreal.net/
  license: GPL
  summary: 'A cart for Plone to add file and download them in an archive.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
