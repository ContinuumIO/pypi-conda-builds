package:
  name: dcallme
  version: "0.0.3a"

source:
  fn: dcallme-0.0.3a.tar.gz
  url: https://pypi.python.org/packages/source/d/dcallme/dcallme-0.0.3a.tar.gz
  md5: 30cd90cbb2722864087d4036f5562e97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dcallme = dcallme:main
    #
    # Would create an entry point called dcallme that calls dcallme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.2.0

  run:
    - python
    - requests ==1.2.0

test:
  # Python imports
  imports:
    - dcallme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/swight/dcallme
  license: BSD License
  summary: 'dCall.me IP-PBX Cloud Service client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
