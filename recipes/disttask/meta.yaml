package:
  name: disttask
  version: "1.1"

source:
  fn: disttask-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/disttask/disttask-1.1.tar.gz
  md5: 2a5792d234b0a484cc2306cb9d80b925
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - disttask = disttask:main
    #
    # Would create an entry point called disttask that calls disttask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - disttask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v3 (GPLv3)
  summary: 'Utility which provides the ability to distribute a task across a fixed number of processes, for better utilization of multiprocessing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
