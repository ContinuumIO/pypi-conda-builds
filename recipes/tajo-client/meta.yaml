package:
  name: tajo-client
  version: "0.0.6"

source:
  fn: tajo-client-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/t/tajo-client/tajo-client-0.0.6.tar.gz
  md5: ba72252f4663e854e4bc6f5d0dc69535
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tajo-client = tajo-client:main
    #
    # Would create an entry point called tajo-client that calls tajo-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - protobuf ==2.5.0

  run:
    - python
    - protobuf ==2.5.0

test:
  # Python imports
  imports:
    - tajo
    - tajo.proto
    - tajo.rpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/charsyam/python-tajo-client/
  license: Apache Software License
  summary: 'a Python implementation of Tajo Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
