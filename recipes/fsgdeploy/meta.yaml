package:
  name: fsgdeploy
  version: "0.2.26"

source:
  fn: FSGDeploy-0.2.26.zip
  url: https://pypi.python.org/packages/source/F/FSGDeploy/FSGDeploy-0.2.26.zip
  md5: 2d5cae2088a2212e6939c89e638c44f0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fsgdeploy = fsgdeploy:main
    #
    # Would create an entry point called fsgdeploy that calls fsgdeploy.main()

    - deploy = FSGDeploy.deploy:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - FSGDeploy
    - fsgdeploy

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - deploy --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/FSGDeploy/
  license: LICENSE.txt
  summary: 'Universal website deploy script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
