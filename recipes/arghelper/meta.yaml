package:
  name: arghelper
  version: "0.2.2"

source:
  fn: arghelper-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/a/arghelper/arghelper-0.2.2.tar.gz
  md5: 74cd4acf196af35332a3a9fd43df7c31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arghelper = arghelper:main
    #
    # Would create an entry point called arghelper that calls arghelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - arghelper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/questrail/arghelper
  license: MIT License
  summary: 'Python arparse helper module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
