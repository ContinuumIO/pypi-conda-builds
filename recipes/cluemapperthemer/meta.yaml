package:
  name: cluemapperthemer
  version: "0.6.3"

source:
  fn: ClueMapperThemer-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/C/ClueMapperThemer/ClueMapperThemer-0.6.3.tar.gz
  md5: 2add1f470a984e12f586dcca8d4ac83c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cluemapperthemer = cluemapperthemer:main
    #
    # Would create an entry point called cluemapperthemer that calls cluemapperthemer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - deliverance >=0.2
    - lxml >=2.0.7,<=2.0.9999
    - paste >=1.7,<=1.7.9999

  run:
    - python
    - setuptools
    - deliverance >=0.2
    - lxml >=2.0.7,<=2.0.9999
    - paste >=1.7,<=1.7.9999

test:
  # Python imports
  imports:
    - clue
    - clue.themer
    - cluemapperthemer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.serverzen.com/pm/p/cluemapper
  license: BSD
  summary: 'Theming plugin for ClueMapper+trac.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
