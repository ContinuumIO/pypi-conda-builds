package:
  name: python-bitcoinlib
  version: "0.4.0"

source:
  fn: python-bitcoinlib-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-bitcoinlib/python-bitcoinlib-0.4.0.tar.gz
  md5: 1e5b2700cf5a473f3a917b9dd48915b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-bitcoinlib = python-bitcoinlib:main
    #
    # Would create an entry point called python-bitcoinlib that calls python-bitcoinlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bitcoin
    - bitcoin.core
    - bitcoin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/petertodd/python-bitcoinlib
  license: UNKNOWN
  summary: 'The Swiss Army Knife of the Bitcoin protocol.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
