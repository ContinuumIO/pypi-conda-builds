package:
  name: microurl
  version: "0.0.6"

source:
  fn: microurl-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/m/microurl/microurl-0.0.6.tar.gz
  md5: 4669b29e48a2d13ca915f79f1b1fa019
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - microurl = microurl:main
    #
    # Would create an entry point called microurl that calls microurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - microurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/micropyramid/microurl
  license: GPL3
  summary: 'microurl is a python module to create minfied urls'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
