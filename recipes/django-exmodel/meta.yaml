package:
  name: django-exmodel
  version: "0.0.2"

source:
  fn: django-exmodel-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-exmodel/django-exmodel-0.0.2.tar.gz
  md5: 38050ebbdc87aad918c5144b2841c36c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-exmodel = django-exmodel:main
    #
    # Would create an entry point called django-exmodel that calls django-exmodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - six >=1.9.0

  run:
    - python
    - django >=1.5
    - six >=1.9.0

test:
  # Python imports
  imports:
    - exmodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sorl/django-exmodel
  license: ISC License (ISCL)
  summary: 'Makes your Django models extendable.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
