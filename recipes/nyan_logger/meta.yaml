package:
  name: nyan_logger
  version: "0.2"

source:
  fn: nyan_logger-0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nyan_logger/nyan_logger-0.2.tar.gz
  md5: 5a9fc3cae96b085478093c8d4d11fc59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nyan_logger = nyan_logger:main
    #
    # Would create an entry point called nyan_logger that calls nyan_logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nyan_logger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tomhennigan/nyan_logger
  license: FU
  summary: 'A nyan cat log formatter for the python logging module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
