package:
  name: django-proxy
  version: "1.0.2"

source:
  fn: django-proxy-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-proxy/django-proxy-1.0.2.tar.gz
  md5: c57f51e5d43cfa8621c3907662e8ae5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-proxy = django-proxy:main
    #
    # Would create an entry point called django-proxy that calls django-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjumbewu/django-proxy
  license: BSD License
  summary: 'A simple HTTP proxy service as a Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
