package:
  name: ymci-ext-oauth
  version: "1.5.0"

source:
  fn: ymci-ext-oauth-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/y/ymci-ext-oauth/ymci-ext-oauth-1.5.0.tar.gz
  md5: a0849cb0d4ce4a1ced44446739b2ea68
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ymci-ext-oauth = ymci-ext-oauth:main
    #
    # Would create an entry point called ymci-ext-oauth that calls ymci-ext-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ymci_ext_oauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'OAuth plugin for ymci'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
