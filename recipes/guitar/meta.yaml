package:
  name: guitar
  version: "0.1.1"

source:
  fn: guitar-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/guitar/guitar-0.1.1.tar.gz
  md5: 273006dd57bbfcec8d24d0fd3b1cbad5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guitar = guitar:main
    #
    # Would create an entry point called guitar that calls guitar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pip >=1.3.1
    - clint >=0.3.1
    - docopt >=0.6.1

  run:
    - python
    - pip >=1.3.1
    - clint >=0.3.1
    - docopt >=0.6.1

test:
  # Python imports
  imports:
    - guitar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://guitar.djangostars.com/
  license: MIT License
  summary: 'django package manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
