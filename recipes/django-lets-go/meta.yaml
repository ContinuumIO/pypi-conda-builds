package:
  name: django-lets-go
  version: "2.9.3"

source:
  fn: django-lets-go-2.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lets-go/django-lets-go-2.9.3.tar.gz
  md5: b8f710d1cfaebefb6a5632c264f6f75b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lets-go = django-lets-go:main
    #
    # Would create an entry point called django-lets-go that calls django-lets-go.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_lets_go
    - django_lets_go.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/areski/django-lets-go
  license: MIT License
  summary: 'Django helpers, goodies, mix of snippets, etc...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
