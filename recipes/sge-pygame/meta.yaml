package:
  name: sge-pygame
  version: "0.18.1"

source:
  fn: sge-pygame-0.18.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sge-pygame/sge-pygame-0.18.1.tar.gz
  md5: 00e92ae38296f0850a9c0ac748dd2389
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sge-pygame = sge-pygame:main
    #
    # Would create an entry point called sge-pygame that calls sge-pygame.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stellarengine.nongnu.org
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'A 2-D game engine for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
