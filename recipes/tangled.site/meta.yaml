package:
  name: tangled.site
  version: "0.1a2"

source:
  fn: tangled.site-0.1a2.tar.gz
  url: https://pypi.python.org/packages/source/t/tangled.site/tangled.site-0.1a2.tar.gz
  md5: e66a55e8914df8225333c82887183e4f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tangled.site = tangled.site:main
    #
    # Would create an entry point called tangled.site that calls tangled.site.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tangled.mako >=0.1a2
    - tangled.sqlalchemy >=0.1a2
    - tangled.web >=0.1a5
    - alembic >=0.6.2
    - bcrypt >=1.0.2
    - markdown >=2.3.1
    - sqlalchemy >=0.9.2

  run:
    - python
    - setuptools
    - tangled.mako >=0.1a2
    - tangled.sqlalchemy >=0.1a2
    - tangled.web >=0.1a5
    - alembic >=0.6.2
    - bcrypt >=1.0.2
    - markdown >=2.3.1
    - sqlalchemy >=0.9.2

test:
  # Python imports
  imports:
    - tangled
    - tangled.site
    - tangled.site.model
    - tangled.site.resources
    - tangled.site.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tangledframework.org/
  license: MIT License
  summary: 'Simple site/blog/cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
