package:
  name: django-akamai-storage
  version: "20141026.1"

source:
  fn: django-akamai-storage-20141026.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-akamai-storage/django-akamai-storage-20141026.1.tar.gz
  md5: 3e07c015bee309a4e67a1cfeeed61685
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-akamai-storage = django-akamai-storage:main
    #
    # Would create an entry point called django-akamai-storage that calls django-akamai-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-polymorphic >=0.5.6

  run:
    - python
    - django-polymorphic >=0.5.6

test:
  # Python imports
  imports:
    - akamai_storage
    - akamai_storage.db
    - akamai_storage.forms
    - akamai_storage.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/g3rd/django-akamai-storage
  license: MIT License
  summary: 'Django storage engine that connects to Akamai NetStorage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
