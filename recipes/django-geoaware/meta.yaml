package:
  name: django-geoaware
  version: "0.5.0"

source:
  fn: django-geoaware-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-geoaware/django-geoaware-0.5.0.tar.gz
  md5: 84829284f6598e82141cb49be1702882
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geoaware = django-geoaware:main
    #
    # Would create an entry point called django-geoaware that calls django-geoaware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - geoaware
    - geoaware/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un33k/django-geoaware
  license: BSD License
  summary: 'A Django application that can include GeoIP info in session and/or context.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
