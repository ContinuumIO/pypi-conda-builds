package:
  name: collective.castle
  version: "1.2"

source:
  fn: collective.castle-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.castle/collective.castle-1.2.tar.gz
  md5: e151846707a881ae34a5e141673642cc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.castle = collective.castle:main
    #
    # Would create an entry point called collective.castle that calls collective.castle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - rwproperty
    - products.cas4pas

  run:
    - python
    - setuptools
    - setuptools
    - rwproperty
    - products.cas4pas

test:
  # Python imports
  imports:
    - collective
    - collective.castle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.castle
  license: GPL
  summary: 'Plone UI for CAS login.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
