package:
  name: collective.customoverrides
  version: "1.0.1"

source:
  fn: collective.customoverrides-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.customoverrides/collective.customoverrides-1.0.1.zip
  md5: 44fb98062d5c755f8443ff4b83f7bf45
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.customoverrides = collective.customoverrides:main
    #
    # Would create an entry point called collective.customoverrides that calls collective.customoverrides.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.customoverrides
    - collective.customoverrides.browser
    - collective.customoverrides.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.customoverrides
  license: gpl
  summary: 'Custom CSS and Javascript in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
