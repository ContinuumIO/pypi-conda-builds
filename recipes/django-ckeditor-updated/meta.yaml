package:
  name: django-ckeditor-updated
  version: "4.4.4"

source:
  fn: django-ckeditor-updated-4.4.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ckeditor-updated/django-ckeditor-updated-4.4.4.tar.gz
  md5: 48a496c85b0d0647824232b19cf619a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ckeditor-updated = django-ckeditor-updated:main
    #
    # Would create an entry point called django-ckeditor-updated that calls django-ckeditor-updated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - ckeditor
    - ckeditor.image
    - ckeditor.management
    - ckeditor.management.commands
    - ckeditor_demo
    - ckeditor_demo.demo_application

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/riklaunim/django-ckeditor
  license: BSD License
  summary: 'Django admin CKEditor integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
