package:
  name: askbot-fedmsg
  version: "0.1.1"

source:
  fn: askbot-fedmsg-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/askbot-fedmsg/askbot-fedmsg-0.1.1.tar.gz
  md5: f930ed511a23494b6a562489d1c32f38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - askbot-fedmsg = askbot-fedmsg:main
    #
    # Would create an entry point called askbot-fedmsg that calls askbot-fedmsg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fedmsg
    - askbot

  run:
    - python
    - fedmsg
    - askbot

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fedora-infra/askbot-fedmsg/
  license: LGPLv2+
  summary: 'Askbot plugin for emitting events to the Fedora message bus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
