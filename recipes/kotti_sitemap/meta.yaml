package:
  name: kotti_sitemap
  version: "0.1.1"

source:
  fn: kotti_sitemap-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_sitemap/kotti_sitemap-0.1.1.tar.gz
  md5: 6ba844928ab16e14eef968c0a96ba7b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_sitemap = kotti_sitemap:main
    #
    # Would create an entry point called kotti_sitemap that calls kotti_sitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti

  run:
    - python
    - kotti

test:
  # Python imports
  imports:
    - kotti_sitemap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/koansys/kotti_sitemap
  license: http://koansys.com/license.txt
  summary: 'Kotti Sitemap - an addon to easily create a sitemap for your Kotti project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
