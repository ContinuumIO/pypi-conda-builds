package:
  name: chube
  version: "0.1.21"

source:
  fn: chube-0.1.21.tar.gz
  url: https://pypi.python.org/packages/source/c/chube/chube-0.1.21.tar.gz
  md5: 5ac3ae09f18db61f553a0f68c4d6592d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chube = chube:main
    #
    # Would create an entry point called chube that calls chube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - linode-python >=1.0
    - pyyaml >=3.10
    - pycurl >=7.10

  run:
    - python
    - linode-python >=1.0
    - pyyaml >=3.10
    - pycurl >=7.10

test:
  # Python imports
  imports:
    - chube

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/chube/
  license: LICENSE.txt
  summary: 'Object-oriented bindings for the Linode API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
