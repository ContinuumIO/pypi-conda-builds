package:
  name: kloudless
  version: "0.1.0"

source:
  fn: kloudless-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kloudless/kloudless-0.1.0.tar.gz
  md5: c37fa1f804df04cff8e11b2056add6e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kloudless = kloudless:main
    #
    # Would create an entry point called kloudless that calls kloudless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0
    - python-dateutil

  run:
    - python
    - requests >=1.0
    - python-dateutil

test:
  # Python imports
  imports:
    - kloudless

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://developers.kloudless.com/
  license: MIT License
  summary: 'Python library for the Kloudless API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
