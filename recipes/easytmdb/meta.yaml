package:
  name: easytmdb
  version: "0.2.0"

source:
  fn: easytmdb-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/easytmdb/easytmdb-0.2.0.tar.gz
  md5: e5f34d748173d8e452c1ca739dbee5f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easytmdb = easytmdb:main
    #
    # Would create an entry point called easytmdb that calls easytmdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.11.1

  run:
    - python
    - requests >=0.11.1

test:
  # Python imports
  imports:
    - easytmdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benregn/easytmdb
  license: UNKNOWN
  summary: 'A Python wrapper for The Movie Database API v3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
