package:
  name: blobuploader
  version: "1.2.4"

source:
  fn: blobuploader-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/b/blobuploader/blobuploader-1.2.4.tar.gz
  md5: 33bce245e5c8c00649eecdb2177b50f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blobuploader = blobuploader:main
    #
    # Would create an entry point called blobuploader that calls blobuploader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests ==1.2.3.
    - docopt ==0.6.1

  run:
    - python
    - requests ==1.2.3.
    - docopt ==0.6.1

test:
  # Python imports
  imports:
    - blobuploader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/build-blobuploader
  license: MPL
  summary: 'Command-line client for uploading blobs to the Mozilla [blobber] server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
