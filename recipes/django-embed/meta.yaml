package:
  name: django-embed
  version: "0.2.1"

source:
  fn: django-embed-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-embed/django-embed-0.2.1.tar.gz
  md5: cc352e3cb2cbfd350de8b40acf0a864c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-embed = django-embed:main
    #
    # Would create an entry point called django-embed that calls django-embed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==0.14.2
    - requests-oauth ==0.4.1

  run:
    - python
    - requests ==0.14.2
    - requests-oauth ==0.4.1

test:
  # Python imports
  imports:
    - embed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mejorando.la/
  license: MIT License
  summary: 'A simple Django app to generate embed code from youtube, twitter and slideshare.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
