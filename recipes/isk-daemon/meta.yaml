package:
  name: isk-daemon
  version: "0.9.5"

source:
  fn: isk-daemon-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/i/isk-daemon/isk-daemon-0.9.5.tar.gz
  md5: 011865480e138d6f1448ef957f86c122
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isk-daemon = isk-daemon:main
    #
    # Would create an entry point called isk-daemon that calls isk-daemon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=8
    - simplejson
    - fpconst
    - soappy

  run:
    - python
    - twisted >=8
    - simplejson
    - fpconst
    - soappy

test:
  # Python imports
  imports:
    - core
    - imgSeekLib
    - plugins
    - ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://server.imgseek.net/
  license: GPLv2
  summary: 'Server and library for adding content-based (visual) image searching to any image related website or software.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
