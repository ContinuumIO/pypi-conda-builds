package:
  name: bda.basen
  version: "1.0"

source:
  fn: bda.basen-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.basen/bda.basen-1.0.tar.gz
  md5: b79be9e8be880d3d32734a1a9945d438
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.basen = bda.basen:main
    #
    # Would create an entry point called bda.basen that calls bda.basen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - bda
    - bda.basen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.bluedynamics.net/svn/internal/mdb
  license: GNU General Public Licence
  summary: 'bda.basen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
