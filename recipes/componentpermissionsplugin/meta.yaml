package:
  name: componentpermissionsplugin
  version: "0.1.6"

source:
  fn: ComponentPermissionsPlugin-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/C/ComponentPermissionsPlugin/ComponentPermissionsPlugin-0.1.6.tar.gz
  md5: 898eb54cb07e5323e70c4d056c3dd84c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - componentpermissionsplugin = componentpermissionsplugin:main
    #
    # Would create an entry point called componentpermissionsplugin that calls componentpermissionsplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - componentpermissions
    - componentpermissionsplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mitar.tnode.com/
  license: AGPLv3
  summary: 'Provides permissions based on ticket components for Trac.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
