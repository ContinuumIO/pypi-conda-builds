package:
  name: ajenti.plugin.services
  version: "0.17"

source:
  fn: ajenti.plugin.services-0.17.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.services/ajenti.plugin.services-0.17.tar.gz
  md5: 96defb33a528b70f41fe49f8a0c40055
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.services = ajenti.plugin.services:main
    #
    # Would create an entry point called ajenti.plugin.services that calls ajenti.plugin.services.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - upstart-new
    - aj
    - ajenti.plugin.core
    - ajenti.plugin.dashboard

  run:
    - python
    - upstart-new
    - aj
    - ajenti.plugin.core
    - ajenti.plugin.dashboard

test:
  # Python imports
  imports:
    - ajenti_plugin_services
    - ajenti_plugin_services.managers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
