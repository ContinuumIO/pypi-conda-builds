package:
  name: cotidia-admin-tools
  version: "0.4.1"

source:
  fn: cotidia-admin-tools-0.4.1.tar.gz
  url: https://bitbucket.org/guillaumepiot/cotidia-admin-tools/downloads/cotidia-admin-tools-0.4.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cotidia-admin-tools = cotidia-admin-tools:main
    #
    # Would create an entry point called cotidia-admin-tools that calls cotidia-admin-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.4.1

  run:
    - python
    - django ==1.4.1

test:
  # Python imports
  imports:
    - admin_tools
    - admin_tools.dashboard
    - admin_tools.dashboard.management
    - admin_tools.dashboard.management.commands
    - admin_tools.dashboard.templatetags
    - admin_tools.liststyle
    - admin_tools.liststyle.templatetags
    - admin_tools.menu
    - admin_tools.menu.management
    - admin_tools.menu.management.commands
    - admin_tools.menu.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/guillaumepiot/cotidia-admin-tools
  license: BSD License
  summary: 'Django application to add features to the original admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
