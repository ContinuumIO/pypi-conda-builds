package:
  name: fabctx
  version: "0.0.3"

source:
  fn: fabctx-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fabctx/fabctx-0.0.3.tar.gz
  md5: 851ccab951d167ac697020a49b1bebdf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fabctx = fabctx:main
    #
    # Would create an entry point called fabctx that calls fabctx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric ==1.6.1
    - requests ==2.0.1
    - simplejson ==2.6.2

  run:
    - python
    - fabric ==1.6.1
    - requests ==2.0.1
    - simplejson ==2.6.2

test:
  # Python imports
  imports:
    - fabctx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gregorynicholas/fabctx
  license: MIT License
  summary: '\n  fabctx\n  ~~~~~~\n\n  short + sweet context magic + helpers for fabric scripts.\n\n\n    links\n    `````\n\n    * `docs <http://gregorynicholas.github.io/fabctx>`_\n    * `source <http://github.com/gregorynicholas/fabctx>`_\n    * `package <http://packages.python.org/fabctx>`_\n\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
