package:
  name: adjacent
  version: "0.1.0"

source:
  fn: adjacent-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/adjacent/adjacent-0.1.0.tar.gz
  md5: 07e9511ce7d3fb8fc84ee7ebc34decfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adjacent = adjacent:main
    #
    # Would create an entry point called adjacent that calls adjacent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cent >=0.3.0

  run:
    - python
    - cent >=0.3.0

test:
  # Python imports
  imports:
    - adjacent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/centrifugal/adjacent
  license: MIT License
  summary: 'Centrifuge integration with Django framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
