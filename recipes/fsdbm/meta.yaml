package:
  name: fsdbm
  version: "0.31"

source:
  fn: fsdbm-0.31.tar.gz
  url: https://pypi.python.org/packages/source/f/fsdbm/fsdbm-0.31.tar.gz
  md5: 172a3e5c11e45beacbc962a99932076c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fsdbm = fsdbm:main
    #
    # Would create an entry point called fsdbm that calls fsdbm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fsdbm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/imbolc/fsdbm/
  license: BSD License
  summary: 'Dict-style file system based DBM, suitable for large data (such as images).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
