package:
  name: django-admin-external-auth
  version: "0.1.0"

source:
  fn: django-admin-external-auth-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-external-auth/django-admin-external-auth-0.1.0.tar.gz
  md5: cbdcd774cbeec9158449883c1a0f5960
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-external-auth = django-admin-external-auth:main
    #
    # Would create an entry point called django-admin-external-auth that calls django-admin-external-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TaymonB/django-admin-external-auth
  license: MIT License
  summary: "Use your Django project's existing authentication views in the admin interface."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
