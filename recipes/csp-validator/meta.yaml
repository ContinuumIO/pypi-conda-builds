package:
  name: csp-validator
  version: "0.3"

source:
  fn: csp-validator-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/csp-validator/csp-validator-0.3.tar.gz
  md5: 2c826387619e0727378237407b639417
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csp-validator = csp-validator:main
    #
    # Would create an entry point called csp-validator that calls csp-validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - csp_validator
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Content-Security-Policy validator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
