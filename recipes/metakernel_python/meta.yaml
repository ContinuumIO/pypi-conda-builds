package:
  name: metakernel_python
  version: "0.7.0"

source:
  fn: metakernel_python-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/m/metakernel_python/metakernel_python-0.7.0.tar.gz
  md5: f425d4671ac2749c7bc43266629640e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metakernel_python = metakernel_python:main
    #
    # Would create an entry point called metakernel_python that calls metakernel_python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - metakernel
    - jedi

  run:
    - python
    - metakernel
    - jedi

test:
  # Python imports
  imports:
    - metakernel_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/calysto/metakernel/tree/master/metakernel_python
  license: BSD License
  summary: 'A Python kernel for Jupyter/IPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
