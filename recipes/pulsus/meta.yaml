package:
  name: pulsus
  version: "0.1.0"

source:
  fn: pulsus-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pulsus/pulsus-0.1.0.tar.gz
  md5: c44f9057ce7dce393d6a58e08de1cac5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pulsus = pulsus:main
    #
    # Would create an entry point called pulsus that calls pulsus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.9.4
    - gevent >=1.0
    - grequests

  run:
    - python
    - werkzeug >=0.9.4
    - gevent >=1.0
    - grequests

test:
  # Python imports
  imports:
    - pulsus
    - pulsus.client
    - pulsus.server
    - pulsus.services
    - pulsus.services.apns
    - pulsus.services.base
    - pulsus.services.gcm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pennersr/pulsus
  license: MIT License
  summary: 'Push Notification Service handling Apple Push Notification Service (APNS), and Google Cloud Messaging (GCM).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
