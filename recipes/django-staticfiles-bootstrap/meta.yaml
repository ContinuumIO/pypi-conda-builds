package:
  name: django-staticfiles-bootstrap
  version: "3.0.3"

source:
  fn: django-staticfiles-bootstrap-3.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-bootstrap/django-staticfiles-bootstrap-3.0.3.tar.gz
  md5: 5e1a7e97aeccb70b5d60fb678d492915
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-bootstrap = django-staticfiles-bootstrap:main
    #
    # Would create an entry point called django-staticfiles-bootstrap that calls django-staticfiles-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/apreche/django-staticfiles-bootstrap
  license: MIT License
  summary: 'Twitter Bootstrap staticfiles for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
