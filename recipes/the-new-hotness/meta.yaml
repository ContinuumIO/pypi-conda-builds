package:
  name: the-new-hotness
  version: "0.4.1"

source:
  fn: the-new-hotness-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/t/the-new-hotness/the-new-hotness-0.4.1.tar.gz
  md5: f96f5e0d22218484af4547853a15544e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - the-new-hotness = the-new-hotness:main
    #
    # Would create an entry point called the-new-hotness that calls the-new-hotness.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fedmsg
    - python-bugzilla
    - dogpile.cache
    - requests
    - sh
    - fedmsg_meta_fedora_infrastructure

  run:
    - python
    - setuptools
    - fedmsg
    - python-bugzilla
    - dogpile.cache
    - requests
    - sh
    - fedmsg_meta_fedora_infrastructure

test:
  # Python imports
  imports:
    - hotness

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fedora-infra/the-new-hotness
  license: LGPLv2+
  summary: 'Consume anitya fedmsg messages to file bugzilla bugs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
