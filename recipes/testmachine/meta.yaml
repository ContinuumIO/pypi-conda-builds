package:
  name: testmachine
  version: "0.0.5"

source:
  fn: testmachine-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/t/testmachine/testmachine-0.0.5.tar.gz
  md5: 23b3ae4e36e60997af422d2e781ce2a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testmachine = testmachine:main
    #
    # Would create an entry point called testmachine that calls testmachine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - testmachine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DRMacIver/testmachine
  license: LICENSE.txt
  summary: 'Stack based automatic testcase generation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
