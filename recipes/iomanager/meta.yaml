package:
  name: iomanager
  version: "0.4.0"

source:
  fn: iomanager-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/i/iomanager/iomanager-0.4.0.tar.gz
  md5: 83304ac3416efb0cb80c968a90254cfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iomanager = iomanager:main
    #
    # Would create an entry point called iomanager that calls iomanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=2.1

  run:
    - python
    - python-dateutil >=2.1

test:
  # Python imports
  imports:
    - iomanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmatthias/iomanager
  license: LICENSE.txt
  summary: 'Guarantee structure and composition of input and output.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
