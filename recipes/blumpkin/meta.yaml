package:
  name: blumpkin
  version: "0.4.3"

source:
  fn: blumpkin-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/b/blumpkin/blumpkin-0.4.3.tar.gz
  md5: c23be4026f240061e5e7406762a5a815
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blumpkin = blumpkin:main
    #
    # Would create an entry point called blumpkin that calls blumpkin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coverage
    - nose >=1.3.0,<2.0
    - nosexcover ==1.0.10
    - nose-cov ==1.6
    - nose_xunitmp ==0.3.1
    - pytest >=2.5.0,<2.6.0
    - pytest-cov >=1.7.0,<1.8.0
    - pep8
    - pylint
    - pyflakes
    - lxml
    - click >=2.1.0,<3.0.0

  run:
    - python
    - coverage
    - nose >=1.3.0,<2.0
    - nosexcover ==1.0.10
    - nose-cov ==1.6
    - nose_xunitmp ==0.3.1
    - pytest >=2.5.0,<2.6.0
    - pytest-cov >=1.7.0,<1.8.0
    - pep8
    - pylint
    - pyflakes
    - lxml
    - click >=2.1.0,<3.0.0

test:
  # Python imports
  imports:
    - blumpkin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/balanced/blumpkin
  license: ISC License (ISCL)
  summary: 'Testing tooling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
