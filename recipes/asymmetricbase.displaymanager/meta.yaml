package:
  name: asymmetricbase.displaymanager
  version: "0.1.0-201404231340"

source:
  fn: asymmetricbase.displaymanager-0.1.0-201404231340.zip
  url: https://pypi.python.org/packages/source/a/asymmetricbase.displaymanager/asymmetricbase.displaymanager-0.1.0-201404231340.zip
  md5: 8b7e0e1dda68cf788eaf016bae04efc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asymmetricbase.displaymanager = asymmetricbase.displaymanager:main
    #
    # Would create an entry point called asymmetricbase.displaymanager that calls asymmetricbase.displaymanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz
    - asymmetricbase.jinja
    - asymmetricbase.utils

  run:
    - python
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz
    - asymmetricbase.jinja
    - asymmetricbase.utils

test:
  # Python imports
  imports:
    - asymmetricbase
    - asymmetricbase.displaymanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AsymmetricVentures/asym-displaymanager
  license: GNU General Public License v2 (GPLv2)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
