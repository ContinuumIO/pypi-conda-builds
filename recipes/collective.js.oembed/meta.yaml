package:
  name: collective.js.oembed
  version: "1.0a"

source:
  fn: collective.js.oembed-1.0a.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.oembed/collective.js.oembed-1.0a.tar.gz
  md5: 303ca0e6c1d06176c03b08d8de08e22d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.oembed = collective.js.oembed:main
    #
    # Would create an entry point called collective.js.oembed that calls collective.js.oembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.oembed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.oembed
  license: GPL
  summary: 'JQuery oembed plistred in the Plone resource registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
