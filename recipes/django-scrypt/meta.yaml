package:
  name: django-scrypt
  version: "0.2.3"

source:
  fn: django-scrypt-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-scrypt/django-scrypt-0.2.3.tar.gz
  md5: 2c62d25c0cb322665cc06b7c4ff6fc72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-scrypt = django-scrypt:main
    #
    # Would create an entry point called django-scrypt that calls django-scrypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_scrypt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kelvinwong_ca/django-scrypt
  license: BSD License
  summary: 'A Scrypt-enabled password hasher for Django 1.4/1.5'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
