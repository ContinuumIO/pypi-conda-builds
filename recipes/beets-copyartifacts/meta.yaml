package:
  name: beets-copyartifacts
  version: "0.1.2"

source:
  fn: beets-copyartifacts-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/beets-copyartifacts/beets-copyartifacts-0.1.2.tar.gz
  md5: d66959b83fbde6c4f8e17686f25c64a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beets-copyartifacts = beets-copyartifacts:main
    #
    # Would create an entry point called beets-copyartifacts that calls beets-copyartifacts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beets >=1.3.7

  run:
    - python
    - beets >=1.3.7

test:
  # Python imports
  imports:
    - beetsplug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sbarakat/beets-copyartifacts
  license: MIT
  summary: 'beets plugin to copy non-music files to import path'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
