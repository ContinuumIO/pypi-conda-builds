package:
  name: django-feedback-form
  version: "1.2.2"

source:
  fn: django-feedback-form-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-feedback-form/django-feedback-form-1.2.2.tar.gz
  md5: 0ea0e809d104c2e97f4206c5398afcfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-feedback-form = django-feedback-form:main
    #
    # Would create an entry point called django-feedback-form that calls django-feedback-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-libs
    - django-mailer

  run:
    - python
    - django
    - django-libs
    - django-mailer

test:
  # Python imports
  imports:
    - feedback_form
    - feedback_form.migrations
    - feedback_form.templatetags
    - feedback_form.tests
    - feedback_form.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-feedback-form
  license: The MIT License
  summary: 'Minified JQuery & Ajax feedback form to handle fast customer requests in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
