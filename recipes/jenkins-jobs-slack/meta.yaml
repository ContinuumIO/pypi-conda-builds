package:
  name: jenkins-jobs-slack
  version: "0.3.0"

source:
  fn: jenkins-jobs-slack-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jenkins-jobs-slack/jenkins-jobs-slack-0.3.0.tar.gz
  md5: e5d143feae7f4cd352cb050c617ebfa5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jenkins-jobs-slack = jenkins-jobs-slack:main
    #
    # Would create an entry point called jenkins-jobs-slack that calls jenkins-jobs-slack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - jenkins_jobs_slack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asmundg/jenkins-jobs-slack
  license: Apache Software License
  summary: 'Jenkins Job Builder Slack Notifier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
