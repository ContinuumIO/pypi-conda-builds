package:
  name: cs.numeric
  version: "20150311.2"

source:
  fn: cs.numeric-20150311.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cs.numeric/cs.numeric-20150311.2.tar.gz
  md5: 69689e2ea72443309c1432acf24afd46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.numeric = cs.numeric:main
    #
    # Would create an entry point called cs.numeric that calls cs.numeric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cameron_simpson/css/commits/all
  license: GNU General Public License v3 (GPLv3)
  summary: 'some numeric functions; currently primes() and factors()'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
