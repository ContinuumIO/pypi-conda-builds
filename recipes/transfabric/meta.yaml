package:
  name: transfabric
  version: "1.0"

source:
  fn: TransFabric-1.0.tar.gz
  url: https://pypi.python.org/packages/source/T/TransFabric/TransFabric-1.0.tar.gz
  md5: 30019d39ef90d6e6b657d3e6757520fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - transfabric = transfabric:main
    #
    # Would create an entry point called transfabric that calls transfabric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - transfabric-python2x-linux-x64

  run:
    - python
    - transfabric-python2x-linux-x64

test:
  # Python imports
  imports:
    - TransFabric
    - transfabric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'TransFabric Parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
