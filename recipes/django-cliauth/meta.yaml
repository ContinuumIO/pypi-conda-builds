package:
  name: django-cliauth
  version: "0.9.1"

source:
  fn: django-cliauth-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cliauth/django-cliauth-0.9.1.tar.gz
  md5: cb9dc85a65f0afa92e79a53626ebca49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cliauth = django-cliauth:main
    #
    # Would create an entry point called django-cliauth that calls django-cliauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - cliauth
    - cliauth.management
    - cliauth.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/seocam/django-cliauth
  license: GPL2
  summary: 'Authenticate Apache2 (basic auth) using your Django DB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
