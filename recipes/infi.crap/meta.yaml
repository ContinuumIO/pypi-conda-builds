package:
  name: infi.crap
  version: "0.1.3"

source:
  fn: infi.crap-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.crap/infi.crap-0.1.3.tar.gz
  md5: de80620b4849e814c04aa73cb29fbdcd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.crap = infi.crap:main
    #
    # Would create an entry point called infi.crap that calls infi.crap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - bunch
    - construct

  run:
    - python
    - distribute
    - bunch
    - construct

test:
  # Python imports
  imports:
    - infi
    - infi.crap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.infinidat.com
  license: Python Software Foundation License
  summary: 'Deprecated. Use infi.cwrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
