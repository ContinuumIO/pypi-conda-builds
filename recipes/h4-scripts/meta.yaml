package:
  name: h4-scripts
  version: "0.9.1"

source:
  fn: h4-scripts-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/h/h4-scripts/h4-scripts-0.9.1.tar.gz
  md5: e8016a9d233d99e2bdcac232b5131ba3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - h4-scripts = h4-scripts:main
    #
    # Would create an entry point called h4-scripts that calls h4-scripts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - h4_scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hacking-thursday/h4-scripts
  license: MIT
  summary: 'Hacking Thursday automation scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
