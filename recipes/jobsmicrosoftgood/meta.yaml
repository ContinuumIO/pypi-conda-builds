package:
  name: jobsmicrosoftgood
  version: "1.0.0.1.0.29"

source:
  fn: JobsMicrosoftGOod-1.0.0.1.0.29.tar.gz
  url: https://pypi.python.org/packages/source/J/JobsMicrosoftGOod/JobsMicrosoftGOod-1.0.0.1.0.29.tar.gz
  md5: 3a92b0b969fc200c7763a230b244c074
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobsmicrosoftgood = jobsmicrosoftgood:main
    #
    # Would create an entry point called jobsmicrosoftgood that calls jobsmicrosoftgood.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jobsmicrosoftgood

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zzz.zzz.com.us
  license: UNKNOWN
  summary: 'goood'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
