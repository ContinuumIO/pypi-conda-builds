package:
  name: django-pyroven
  version: "0.9"

source:
  fn: django-pyroven-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pyroven/django-pyroven-0.9.tar.gz
  md5: 06443129792494cf2164a07d442089e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pyroven = django-pyroven:main
    #
    # Would create an entry point called django-pyroven that calls django-pyroven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyroven

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pyroven/django-pyroven
  license: MIT
  summary: 'A Django authentication backend for Ucam-WebAuth / Raven'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
