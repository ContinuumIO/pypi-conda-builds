package:
  name: cltools
  version: "0.4.2"

source:
  fn: cltools-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cltools/cltools-0.4.2.tar.gz
  md5: 40986e9f184b23386ace872996481e06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cltools = cltools:main
    #
    # Would create an entry point called cltools that calls cltools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - supertools

  run:
    - python
    - supertools

test:
  # Python imports
  imports:
    - cltools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gissehel/cltools.git
  license: MIT License
  summary: 'Set of decorators of to create transform a class into a command-line tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
