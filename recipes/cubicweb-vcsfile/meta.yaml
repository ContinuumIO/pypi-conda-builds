package:
  name: cubicweb-vcsfile
  version: "2.0.3"

source:
  fn: cubicweb-vcsfile-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-vcsfile/cubicweb-vcsfile-2.0.3.tar.gz
  md5: 7c810e735a35ae472e5c97fbc78517d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-vcsfile = cubicweb-vcsfile:main
    #
    # Would create an entry point called cubicweb-vcsfile that calls cubicweb-vcsfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19
    - cubicweb-localperms >=0.1.0
    - logilab-common >=0.59.0
    - logilab-mtconverter >=0.7.0
    - python-hglib >=1.4

  run:
    - python
    - cubicweb >=3.19
    - cubicweb-localperms >=0.1.0
    - logilab-common >=0.59.0
    - logilab-mtconverter >=0.7.0
    - python-hglib >=1.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-vcsfile
  license: LGPL
  summary: 'component to integrate version control systems data into the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
