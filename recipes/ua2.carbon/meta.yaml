package:
  name: ua2.carbon
  version: "0.0.4"

source:
  fn: ua2.carbon-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/u/ua2.carbon/ua2.carbon-0.0.4.tar.gz
  md5: b7de6629873ad9408f2b0a60e9fbed77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ua2.carbon = ua2.carbon:main
    #
    # Would create an entry point called ua2.carbon that calls ua2.carbon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ua2
    - ua2.carbon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ua2crm/ua2.carbon
  license: BSD License
  summary: 'Tool for stream statistics to carbon daemon (from graphite)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
