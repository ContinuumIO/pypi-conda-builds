package:
  name: django-markdown-newsletter
  version: "0.7"

source:
  fn: django-markdown-newsletter-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markdown-newsletter/django-markdown-newsletter-0.7.tar.gz
  md5: 5a4a72f3a23b102634d47772578ff6b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markdown-newsletter = django-markdown-newsletter:main
    #
    # Would create an entry point called django-markdown-newsletter that calls django-markdown-newsletter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_markdown_newsletter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SamyMe/django-markdown-newsletter
  license: MIT License
  summary: 'Easy to use Django markdown newslette'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
