package:
  name: django-generic-link-tracking
  version: "1.0"

source:
  fn: django-generic-link-tracking-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-link-tracking/django-generic-link-tracking-1.0.tar.gz
  md5: e6c0de583d471c63bf584073b59b1ed3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-link-tracking = django-generic-link-tracking:main
    #
    # Would create an entry point called django-generic-link-tracking that calls django-generic-link-tracking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numconv ==2.1.1

  run:
    - python
    - numconv ==2.1.1

test:
  # Python imports
  imports:
    - generic_link_tracking

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jonatron/django-generic-link-tracking
  license: MIT License
  summary: 'Generic trackable links for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
