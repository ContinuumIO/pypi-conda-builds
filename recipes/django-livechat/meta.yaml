package:
  name: django-livechat
  version: "1.0.1"

source:
  fn: django-livechat-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livechat/django-livechat-1.0.1.tar.gz
  md5: 94be8dbbdea7229c3ae553db015efdac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livechat = django-livechat:main
    #
    # Would create an entry point called django-livechat that calls django-livechat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.4.5
    - south ==0.8.1
    - jmbo ==0.5.5
    - pytest
    - pytest-django
    - pytest-cov
    - pytest-xdist

  run:
    - python
    - django ==1.4.5
    - south ==0.8.1
    - jmbo ==0.5.5
    - pytest
    - pytest-django
    - pytest-cov
    - pytest-xdist

test:
  # Python imports
  imports:
    - livechat
    - livechat.migrations
    - livechat.templatetags
    - livechat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-livechat
  license: BSD License
  summary: 'Very Simple Live Chat Django Application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
