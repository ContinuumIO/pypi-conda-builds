package:
  name: requestions
  version: "0.0.7"

source:
  fn: requestions-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/r/requestions/requestions-0.0.7.tar.gz
  md5: a5134924c0abbbbbdf588aa5bbf598cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requestions = requestions:main
    #
    # Would create an entry point called requestions that calls requestions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.14.2
    - httpretty >=0

  run:
    - python
    - requests >=0.14.2
    - httpretty >=0

test:
  # Python imports
  imports:
    - requestions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kanzure/python-requestions
  license: BSD License
  summary: 'Serialization for Requests using JSON.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
