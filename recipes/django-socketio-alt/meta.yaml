package:
  name: django-socketio-alt
  version: "0.2.1"

source:
  fn: django-socketio-alt-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-socketio-alt/django-socketio-alt-0.2.1.tar.gz
  md5: 745d0500e08b7e9b3a8b367a98c1e018
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-socketio-alt = django-socketio-alt:main
    #
    # Would create an entry point called django-socketio-alt that calls django-socketio-alt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.4.2
    - gevent-socketio

  run:
    - python
    - django ==1.4.2
    - gevent-socketio

test:
  # Python imports
  imports:
    - djsocketio
    - djsocketio.management
    - djsocketio.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amirouche/django-socketio-alt
  license: LGPL
  summary: 'Django + Gevent + SocketIO = Awesome client/server interactions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
