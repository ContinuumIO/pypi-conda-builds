package:
  name: impyccable
  version: "1.1.1"

source:
  fn: Impyccable-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/I/Impyccable/Impyccable-1.1.1.tar.gz
  md5: eb3d381fd7c31dfacd76407a3cea4844
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - impyccable = impyccable:main
    #
    # Would create an entry point called impyccable that calls impyccable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - impyccable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: nekroze.eturnilnetwork.com
  license: MIT License
  summary: 'A python unit testing companion that generates a test dataset.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
