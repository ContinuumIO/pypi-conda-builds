package:
  name: django-templatetag-sugar
  version: "1.0"

source:
  fn: django-templatetag-sugar-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templatetag-sugar/django-templatetag-sugar-1.0.tar.gz
  md5: 40da36b5a4bf98fdff867f2ccd5bb34e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templatetag-sugar = django-templatetag-sugar:main
    #
    # Would create an entry point called django-templatetag-sugar that calls django-templatetag-sugar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - templatetag_sugar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alex/django-templatetag-sugar/
  license: BSD License
  summary: "A library to make Django's template tags sweet."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
