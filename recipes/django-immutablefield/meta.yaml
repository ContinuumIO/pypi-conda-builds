package:
  name: django-immutablefield
  version: "0.1.1"

source:
  fn: django-immutablefield-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-immutablefield/django-immutablefield-0.1.1.tar.gz
  md5: 0701e924aa82265790cc72d1bbc4762b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-immutablefield = django-immutablefield:main
    #
    # Would create an entry point called django-immutablefield that calls django-immutablefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - immutablefield
    - immutablefield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/robmadole/django-immutablefield
  license: BSD License
  summary: 'A base class for Django to allow immutable fields on Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
