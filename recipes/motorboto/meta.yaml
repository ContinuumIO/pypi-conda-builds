package:
  name: motorboto
  version: "0.2.4"

source:
  fn: motorboto-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/m/motorboto/motorboto-0.2.4.tar.gz
  md5: 3b17e243eb837e21e03bedc7d4b16c9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motorboto = motorboto:main
    #
    # Would create an entry point called motorboto that calls motorboto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - futures
    - netaddr

  run:
    - python
    - boto
    - futures
    - netaddr

test:
  # Python imports
  imports:
    - motorboto
    - motorboto.helpful

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djrobstep/motorboto
  license: Unlicense
  summary: 'Get stuff done fast on AWS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
