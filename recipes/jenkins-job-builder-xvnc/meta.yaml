package:
  name: jenkins-job-builder-xvnc
  version: "0.1"

source:
  fn: jenkins-job-builder-xvnc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jenkins-job-builder-xvnc/jenkins-job-builder-xvnc-0.1.tar.gz
  md5: c67860dbbb3b1e7a740219e4bbd71887
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jenkins-job-builder-xvnc = jenkins-job-builder-xvnc:main
    #
    # Would create an entry point called jenkins-job-builder-xvnc that calls jenkins-job-builder-xvnc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License, Version 2.0
  summary: 'Enable xvnc for job execution.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
