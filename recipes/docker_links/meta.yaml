package:
  name: docker_links
  version: "0.1.0"

source:
  fn: docker_links-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/docker_links/docker_links-0.1.0.tar.gz
  md5: 8b6fe7ecdb3a039c76586b9292d40321
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker_links = docker_links:main
    #
    # Would create an entry point called docker_links that calls docker_links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - docker_links

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wlonk/docker_links
  license: BSD License
  summary: 'Parse Docker links into values useful for Django settings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
