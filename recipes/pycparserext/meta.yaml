package:
  name: pycparserext
  version: "2013.2"

source:
  fn: pycparserext-2013.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pycparserext/pycparserext-2013.2.tar.gz
  md5: 8a493599921e4a1519dd80f3e9380924
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pycparserext = pycparserext:main
    #
    # Would create an entry point called pycparserext that calls pycparserext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ply >=3.4
    - pycparser >=2.10

  run:
    - python
    - ply >=3.4
    - pycparser >=2.10

test:
  # Python imports
  imports:
    - pycparserext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/pycparserext
  license: MIT License
  summary: 'Extensions for pycparser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
