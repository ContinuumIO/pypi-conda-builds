package:
  name: oauth2sample
  version: "0.1"

source:
  fn: oauth2sample-0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/oauth2sample/oauth2sample-0.1.tar.gz
  md5: 221b2cad0fa80f7b5e3a90df49b6e6da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oauth2sample = oauth2sample:main
    #
    # Would create an entry point called oauth2sample that calls oauth2sample.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - pyramid_oauth2_client
    - requests
    - setuptools

  run:
    - python
    - setuptools
    - pyramid
    - pyramid_oauth2_client
    - requests
    - setuptools

test:
  # Python imports
  imports:
    - oauth2sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hathawsh/oauth2sample
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Sample OAuth2 Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
