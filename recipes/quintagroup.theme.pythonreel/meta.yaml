package:
  name: quintagroup.theme.pythonreel
  version: "1.5.1"

source:
  fn: quintagroup.theme.pythonreel-1.5.1.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.theme.pythonreel/quintagroup.theme.pythonreel-1.5.1.zip
  md5: 2ec88be0bc1910ff44269f521e70c517
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.theme.pythonreel = quintagroup.theme.pythonreel:main
    #
    # Would create an entry point called quintagroup.theme.pythonreel that calls quintagroup.theme.pythonreel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming
    - plone.app.themingplugins

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming
    - plone.app.themingplugins

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.theme
    - quintagroup.theme.pythonreel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://themes.quintagroup.com/product/pythonreel
  license: GNU General Public License (GPL)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
