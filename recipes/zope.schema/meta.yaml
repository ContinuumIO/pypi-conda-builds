package:
  name: zope.schema
  version: "4.4.2"

source:
  fn: zope.schema-4.4.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.schema/zope.schema-4.4.2.tar.gz
  md5: 939680fec2a5cf9e214cc80d6665065c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.schema = zope.schema:main
    #
    # Would create an entry point called zope.schema that calls zope.schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface >=3.6.0
    - zope.event
    - zope.testing
    - sphinx
    - zope.testing
    - nose
    - coverage

  run:
    - python
    - setuptools
    - zope.interface >=3.6.0
    - zope.event
    - zope.testing
    - sphinx
    - zope.testing
    - nose
    - coverage

test:
  # Python imports
  imports:
    - zope
    - zope.schema
    - zope.schema.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - zope.testing
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.schema
  license: Zope Public License
  summary: 'zope.interface extension for defining data schemas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
