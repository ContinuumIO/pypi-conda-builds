package:
  name: mopidy-alarmclock
  version: "0.1.5"

source:
  fn: Mopidy-AlarmClock-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-AlarmClock/Mopidy-AlarmClock-0.1.5.tar.gz
  md5: bf8d0eae304eb1485c60182a45a2a9e6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-alarmclock = mopidy-alarmclock:main
    #
    # Would create an entry point called mopidy-alarmclock that calls mopidy-alarmclock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

test:
  # Python imports
  imports:
    - mopidy_alarmclock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DavisNT/mopidy-alarmclock
  license: Apache Software License
  summary: u'A Mopidy extension for using it as an alarm clock.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
