package:
  name: js.jquery_numeric
  version: "1.4.1"

source:
  fn: js.jquery_numeric-1.4.1.zip
  url: https://pypi.python.org/packages/source/j/js.jquery_numeric/js.jquery_numeric-1.4.1.zip
  md5: e2cce00b238f6e02c5544fdc2ed7f3bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_numeric = js.jquery_numeric:main
    #
    # Would create an entry point called js.jquery_numeric that calls js.jquery_numeric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_numeric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jquery numeric'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
