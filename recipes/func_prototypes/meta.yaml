package:
  name: func_prototypes
  version: "0.2"

source:
  fn: func_prototypes-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/func_prototypes/func_prototypes-0.2.tar.gz
  md5: f4d8c789382e4cff7e117ac626c50e5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - func_prototypes = func_prototypes:main
    #
    # Would create an entry point called func_prototypes that calls func_prototypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - func_prototypes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andrewguy9/func_prototypes
  license: MIT
  summary: 'library which helps people constrain python functions by type.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
