package:
  name: kcrw.nprapi
  version: "1.0b3"

source:
  fn: kcrw.nprapi-1.0b3.zip
  url: https://pypi.python.org/packages/source/k/kcrw.nprapi/kcrw.nprapi-1.0b3.zip
  md5: c8feec9d86943b519f28302563a55770
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kcrw.nprapi = kcrw.nprapi:main
    #
    # Would create an entry point called kcrw.nprapi that calls kcrw.nprapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - kcrw
    - kcrw.nprapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: "A python library for using National Public Radio's API (http://www.npr.org/api/index)"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
