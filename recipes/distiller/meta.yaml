package:
  name: distiller
  version: "0.1.2"

source:
  fn: Distiller-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/D/Distiller/Distiller-0.1.2.tar.gz
  md5: c7c04452e39f3e59ff598ae5765aae6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distiller = distiller:main
    #
    # Would create an entry point called distiller that calls distiller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - nltk >=2.0.4

  run:
    - python
    - nltk >=2.0.4

test:
  # Python imports
  imports:
    - Distiller
    - Distiller.features
    - Distiller.preprocessing
    - Distiller.test
    - distiller

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FranciscoCanas/Distiller
  license: LICENSE.txt
  summary: 'Automatic Keyword Extraction from Document Collections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
