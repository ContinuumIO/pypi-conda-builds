package:
  name: django-aloha-edit
  version: "0.3.3"

source:
  fn: django-aloha-edit-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aloha-edit/django-aloha-edit-0.3.3.tar.gz
  md5: 2447bdd0fc46ac7375c70fea1f63723d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aloha-edit = django-aloha-edit:main
    #
    # Would create an entry point called django-aloha-edit that calls django-aloha-edit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - bleach >=1.2.1
    - lxml

  run:
    - python
    - django >=1.5
    - bleach >=1.2.1
    - lxml

test:
  # Python imports
  imports:
    - aloha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ntucker/django-aloha-edit
  license: BSD License
  summary: u'Django Aloha Edit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
