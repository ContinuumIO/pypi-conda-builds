package:
  name: cirb.blog
  version: "1.0a3"

source:
  fn: cirb.blog-1.0a3.zip
  url: https://pypi.python.org/packages/source/c/cirb.blog/cirb.blog-1.0a3.zip
  md5: c5a0ae997c707e08c1c26968032de769
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cirb.blog = cirb.blog:main
    #
    # Would create an entry point called cirb.blog that calls cirb.blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.discussion
    - plone.formwidget.recaptcha
    - collective.contentrules.yearmonth
    - collective.quickupload
    - collective.recaptcha
    - collective.wpadmin

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.discussion
    - plone.formwidget.recaptcha
    - collective.contentrules.yearmonth
    - collective.quickupload
    - collective.recaptcha
    - collective.wpadmin

test:
  # Python imports
  imports:
    - cirb
    - cirb.blog
    - cirb.blog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CIRB/cirb.blog
  license: GPL
  summary: 'Blog project for CIRB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
