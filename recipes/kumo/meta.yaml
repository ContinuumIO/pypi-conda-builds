package:
  name: kumo
  version: "0.2"

source:
  fn: kumo-0.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kumo/kumo-0.2.tar.gz
  md5: d6e4c96268048e846f43db1834f2ac67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kumo = kumo:main
    #
    # Would create an entry point called kumo that calls kumo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - webob

  run:
    - python
    - requests
    - webob

test:
  # Python imports
  imports:
    - kumo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnnykv/kumo
  license: UNKNOWN
  summary: 'Middleware that submits logs to cloud services like loggly and loggr.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
