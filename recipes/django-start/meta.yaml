package:
  name: django-start
  version: "0.1.22"

source:
  fn: django-start-0.1.22.tar.gz
  url: https://pypi.python.org/packages/source/d/django-start/django-start-0.1.22.tar.gz
  md5: 8c464e061a9eb12b729f3e6ba0aa0592
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-start = django-start:main
    #
    # Would create an entry point called django-start that calls django-start.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_start
    - django_start.management

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ff0000/django-start/
  license: MIT License
  summary: 'Create a Django project based on FF0000 best practices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
