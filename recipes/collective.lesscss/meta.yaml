package:
  name: collective.lesscss
  version: "1.3"

source:
  fn: collective.lesscss-1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.lesscss/collective.lesscss-1.3.tar.gz
  md5: 7667b62c97ea1102bdbf13f213dae2d7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.lesscss = collective.lesscss:main
    #
    # Would create an entry point called collective.lesscss that calls collective.lesscss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.resourceregistries
    - plone.resource

  run:
    - python
    - setuptools
    - setuptools
    - products.resourceregistries
    - plone.resource

test:
  # Python imports
  imports:
    - collective
    - collective.lesscss
    - collective.lesscss.browser
    - collective.lesscss.exportimport
    - collective.lesscss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.lesscss
  license: gpl
  summary: 'This package allow theme developers to add LESS stylesheets into a Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
