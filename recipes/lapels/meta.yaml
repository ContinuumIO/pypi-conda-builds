package:
  name: lapels
  version: "1.0.6"

source:
  fn: lapels-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/l/lapels/lapels-1.0.6.tar.gz
  md5: 59f91f83269dae2425b17cf0e204916d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lapels = lapels:main
    #
    # Would create an entry point called lapels that calls lapels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysam >=0.7.4
    - argparse >=1.2
    - cython >=0.17
    - modtools >=0.2.5

  run:
    - python
    - pysam >=0.7.4
    - argparse >=1.2
    - cython >=0.17
    - modtools >=0.2.5

test:
  # Python imports
  imports:
    - lapels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/lapels/
  license: The MIT License
  summary: 'Lapels - A remapper and annotator of in silico (pseudo) genome alignments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
