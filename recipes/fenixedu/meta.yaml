package:
  name: fenixedu
  version: "1.0.0"

source:
  fn: fenixedu-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fenixedu/fenixedu-1.0.0.tar.gz
  md5: 36f230fbc8a9bc7e492569944f0c4737
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fenixedu = fenixedu:main
    #
    # Would create an entry point called fenixedu that calls fenixedu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - fenixedu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/samfcmc/fenixedu-python-sdk
  license: UNKNOWN
  summary: 'FenixEdu API SDK for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
