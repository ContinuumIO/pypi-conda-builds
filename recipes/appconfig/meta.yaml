package:
  name: appconfig
  version: "0.2dev"

source:
  fn: appconfig-0.2dev.tar.gz
  url: https://pypi.python.org/packages/source/a/appconfig/appconfig-0.2dev.tar.gz
  md5: 961fcc86f7f5db8cbc14d121c7e68466
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appconfig = appconfig:main
    #
    # Would create an entry point called appconfig that calls appconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appdirs

  run:
    - python
    - appdirs

test:
  # Python imports
  imports:
    - appconfig
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mweb.github.io/appconfig
  license: BSD License
  summary: 'An easy to use config file wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
