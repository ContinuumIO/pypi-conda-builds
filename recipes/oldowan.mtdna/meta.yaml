package:
  name: oldowan.mtdna
  version: "1.0.1"

source:
  fn: oldowan.mtdna-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/oldowan.mtdna/oldowan.mtdna-1.0.1.tar.gz
  md5: 9dec48d800ad9f85c6f656d49069d645
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oldowan.mtdna = oldowan.mtdna:main
    #
    # Would create an entry point called oldowan.mtdna that calls oldowan.mtdna.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oldowan
    - oldowan.mtdna

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.raaum.org/software/oldowan
  license: MIT License
  summary: 'Human reference mtdna sequence in Python formats.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
