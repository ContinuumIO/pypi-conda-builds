package:
  name: arvados-python-client
  version: "0.1.20150504170729"

source:
  fn: arvados-python-client-0.1.20150504170729.tar.gz
  url: https://pypi.python.org/packages/source/a/arvados-python-client/arvados-python-client-0.1.20150504170729.tar.gz
  md5: 48452a2454adb0f07df93ba54757d3d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arvados-python-client = arvados-python-client:main
    #
    # Would create an entry point called arvados-python-client that calls arvados-python-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client
    - httplib2
    - pycurl >=7.19
    - python-gflags
    - requests >=2.4
    - urllib3
    - ws4py

  run:
    - python
    - google-api-python-client
    - httplib2
    - pycurl >=7.19
    - python-gflags
    - requests >=2.4
    - urllib3
    - ws4py

test:
  # Python imports
  imports:
    - arvados
    - arvados.commands
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://arvados.org
  license: Apache 2.0
  summary: 'Arvados client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
