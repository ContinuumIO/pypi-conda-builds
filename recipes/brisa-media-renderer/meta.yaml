package:
  name: brisa-media-renderer
  version: "0.1.1"

source:
  fn: brisa-media-renderer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brisa-media-renderer/brisa-media-renderer-0.1.1.tar.gz
  md5: e317ef1e3db6be2e85890411af64289e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brisa-media-renderer = brisa-media-renderer:main
    #
    # Would create an entry point called brisa-media-renderer that calls brisa-media-renderer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - brisa_media_renderer
    - brisa_media_renderer/services
    - brisa_media_renderer/services/avtransport
    - brisa_media_renderer/services/gst_renderer
    - brisa_media_renderer/services/render_control
    - brisa_media_renderer/xml_descriptions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://garage.maemo.org/projects/brisa/
  license: MIT License
  summary: 'BRisa Media Renderer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
