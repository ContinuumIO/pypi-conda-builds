package:
  name: django-pdfy
  version: "0.2.2"

source:
  fn: django-pdfy-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pdfy/django-pdfy-0.2.2.tar.gz
  md5: 4994e150fc6fb96a11cda550789193cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pdfy = django-pdfy:main
    #
    # Would create an entry point called django-pdfy that calls django-pdfy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >1.3
    - xhtml2pdf ==0.0.5

  run:
    - python
    - django >1.3
    - xhtml2pdf ==0.0.5

test:
  # Python imports
  imports:
    - pdfy
    - pdfy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rochapps/django-pdfy
  license: BDS
  summary: u'<Include a description of your project>'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
