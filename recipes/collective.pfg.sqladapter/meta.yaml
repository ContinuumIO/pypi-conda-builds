package:
  name: collective.pfg.sqladapter
  version: "1.0"

source:
  fn: collective.pfg.sqladapter-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.pfg.sqladapter/collective.pfg.sqladapter-1.0.tar.gz
  md5: c2cb7ba224b716b1bc8f1ad4349ae1da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pfg.sqladapter = collective.pfg.sqladapter:main
    #
    # Would create an entry point called collective.pfg.sqladapter that calls collective.pfg.sqladapter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - z3c.saconfig

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - z3c.saconfig

test:
  # Python imports
  imports:
    - collective
    - collective.pfg
    - collective.pfg.sqladapter
    - collective.pfg.sqladapter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/collective.pfg.sqladapter
  license: GPL
  summary: 'A PloneFormGen adapter that saves form input data in a SQL database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
