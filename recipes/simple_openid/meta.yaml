package:
  name: simple_openid
  version: "1.0.6"

source:
  fn: simple_openid-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/simple_openid/simple_openid-1.0.6.tar.gz
  md5: 08bcee10a39f6110947b39b8139e1b54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple_openid = simple_openid:main
    #
    # Would create an entry point called simple_openid that calls simple_openid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask_openid

  run:
    - python
    - flask_openid

test:
  # Python imports
  imports:
    - simple_openid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/maxgoedjen/simple_openid
  license: UNKNOWN
  summary: 'Simple OpenID. One-line setup for OpenID login for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
