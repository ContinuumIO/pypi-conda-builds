package:
  name: products.relations
  version: "0.9b4"

source:
  fn: Products.Relations-0.9b4.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.Relations/Products.Relations-0.9b4.tar.gz
  md5: eebdadfed39cdbe9519e431a27269e31
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.relations = products.relations:main
    #
    # Would create an entry point called products.relations that calls products.relations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.Relations
    - Products.Relations.Extensions
    - Products.Relations.components
    - Products.Relations.doc
    - Products.Relations.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/archetypes/Products.Relations
  license: GPL
  summary: 'Relations allows for the definition of sets of rules for validation, creation and lifetime of Archetypes references.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
