package:
  name: commander
  version: "0.2.3"

source:
  fn: commander-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/c/commander/commander-0.2.3.tar.gz
  md5: 9d3e7f3f0778115be91d9177a1b9b884
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commander = commander:main
    #
    # Would create an entry point called commander that calls commander.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - commander

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/oremj/commander
  license: UNKNOWN
  summary: 'A deployment library and remote command runner.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
