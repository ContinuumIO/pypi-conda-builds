package:
  name: django-invitable
  version: "0.1"

source:
  fn: django-invitable-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-invitable/django-invitable-0.1.tar.gz
  md5: 138220bc9cc6ae18254cc9f42f1214a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-invitable = django-invitable:main
    #
    # Would create an entry point called django-invitable that calls django-invitable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - invitable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/teodor-pripoae/django-invitable
  license: UNKNOWN
  summary: 'Reusable invitations app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
