package:
  name: liveplots
  version: "0.7.1"

source:
  fn: liveplots-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/l/liveplots/liveplots-0.7.1.tar.gz
  md5: cce3ac07f4f5b4b9bd5b329ea12d55ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - liveplots = liveplots:main
    #
    # Would create an entry point called liveplots that calls liveplots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.2
    - pyinotify >=0.8.9

  run:
    - python
    - numpy >=1.2
    - pyinotify >=0.8.9

test:
  # Python imports
  imports:
    - graphserver
    - liveplots

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/liveplots/
  license: GPL
  summary: 'Real-time live plot server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
