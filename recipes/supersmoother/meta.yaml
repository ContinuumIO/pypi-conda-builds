package:
  name: supersmoother
  version: "0.3.1"

source:
  fn: supersmoother-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/s/supersmoother/supersmoother-0.3.1.tar.gz
  md5: 92710595d68c7a4f314a8cdfd4801235
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supersmoother = supersmoother:main
    #
    # Would create an entry point called supersmoother that calls supersmoother.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - supersmoother
    - supersmoother.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jakevdp/supersmoother
  license: BSD License
  summary: "Python implementation of Friedman's Supersmoother"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
