package:
  name: flask-mimerender
  version: "0.1.3"

source:
  fn: Flask-MimeRender-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MimeRender/Flask-MimeRender-0.1.3.tar.gz
  md5: 9b33d2bc1f15541e2c5a0d5da9dbf657
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mimerender = flask-mimerender:main
    #
    # Would create an entry point called flask-mimerender that calls flask-mimerender.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicolaiarocci/flask-mimerender
  license: MIT License
  summary: 'RESTful resource variant rendering using MIME Media-Types, for the Flask Micro Web Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
