package:
  name: django-termsandconditions
  version: "0.1.4"

source:
  fn: django-termsandconditions-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-termsandconditions/django-termsandconditions-0.1.4.tar.gz
  md5: 7baa7a29a188804a989e74668165fde8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-termsandconditions = django-termsandconditions:main
    #
    # Would create an entry point called django-termsandconditions that calls django-termsandconditions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - termsandconditions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://timlwhite.com
  license: BSD License
  summary: 'django-termsandconditions enables customers to accept terms and conditions before using a site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
