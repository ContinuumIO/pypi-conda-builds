package:
  name: mongowriter
  version: "0.1.3"

source:
  fn: mongowriter-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mongowriter/mongowriter-0.1.3.tar.gz
  md5: c81e5f74344678e6be0f70b6be95daba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongowriter = mongowriter:main
    #
    # Would create an entry point called mongowriter that calls mongowriter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymongo >=2.5,<2.6

  run:
    - python
    - pymongo >=2.5,<2.6

test:
  # Python imports
  imports:
    - mongowriter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.cloudcontrol.com
  license: Apache Software License
  summary: 'Adapter to write documents from a queue to mongodb concurrently'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
