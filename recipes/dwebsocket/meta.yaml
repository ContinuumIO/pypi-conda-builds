package:
  name: dwebsocket
  version: "0.2.4"

source:
  fn: dwebsocket-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dwebsocket/dwebsocket-0.2.4.tar.gz
  md5: 0e428b97d790922c16f6dc0cea26c8dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dwebsocket = dwebsocket:main
    #
    # Would create an entry point called dwebsocket that calls dwebsocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - dwebsocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/duanhongyi/dwebsocket
  license: BSD License
  summary: u'Websocket support for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
