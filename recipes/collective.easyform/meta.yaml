package:
  name: collective.easyform
  version: "1.0a2"

source:
  fn: collective.easyform-1.0a2.zip
  url: https://pypi.python.org/packages/source/c/collective.easyform/collective.easyform-1.0a2.zip
  md5: 3b1853768a482cff1498c54255114c72
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.easyform = collective.easyform:main
    #
    # Would create an entry point called collective.easyform that calls collective.easyform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.app.textfield
    - plone.dexterity
    - plone.directives.form
    - plone.namedfile
    - plone.schemaeditor

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.app.textfield
    - plone.dexterity
    - plone.directives.form
    - plone.namedfile
    - plone.schemaeditor

test:
  # Python imports
  imports:
    - collective
    - collective.easyform
    - collective.easyform.browser
    - collective.easyform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quintagroup/collective.easyform
  license: GPL
  summary: 'Forms for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
