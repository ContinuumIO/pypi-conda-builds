package:
  name: django-celery-ses
  version: "0.9.2"

source:
  fn: django-celery-ses-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery-ses/django-celery-ses-0.9.2.tar.gz
  md5: 624d362ebce222fd29b5b837449cf286
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery-ses = django-celery-ses:main
    #
    # Would create an entry point called django-celery-ses that calls django-celery-ses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-celery >=3

  run:
    - python
    - django >=1.3
    - django-celery >=3

test:
  # Python imports
  imports:
    - djcelery_ses
    - djcelery_ses.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/StreetVoice/django-celery-ses
  license: MIT
  summary: 'django-celery-ses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
