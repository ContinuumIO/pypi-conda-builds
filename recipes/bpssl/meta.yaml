package:
  name: bpssl
  version: "1.0.3"

source:
  fn: bpssl-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bpssl/bpssl-1.0.3.tar.gz
  md5: 1eeb3cc5c854e7e4e5c37eb25749a848
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bpssl = bpssl:main
    #
    # Would create an entry point called bpssl that calls bpssl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - beproud
    - beproud.django
    - beproud.django.ssl
    - beproud.django.ssl.tests
    - bpssl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/beproud/bpssl/
  license: BSD License
  summary: 'SSL/HTTPS for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
