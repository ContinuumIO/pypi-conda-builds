package:
  name: django-fitbit
  version: "0.1.1"

source:
  fn: django-fitbit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fitbit/django-fitbit-0.1.1.tar.gz
  md5: 84a6768144401bd9fa62821a2208a774
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fitbit = django-fitbit:main
    #
    # Would create an entry point called django-fitbit that calls django-fitbit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fitbit >=0.1.2
    - celery >=3.1.13

  run:
    - python
    - setuptools
    - fitbit >=0.1.2
    - celery >=3.1.13

test:
  # Python imports
  imports:
    - fitapp
    - fitapp.migrations
    - fitapp.south_migrations
    - fitapp.templatetags
    - fitapp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orcasgit/django-fitbit/
  license: Apache Software License
  summary: 'Django integration for python-fitbit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
