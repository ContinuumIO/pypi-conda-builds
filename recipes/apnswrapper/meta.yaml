package:
  name: apnswrapper
  version: "0.6.1"

source:
  fn: APNSWrapper-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/A/APNSWrapper/APNSWrapper-0.6.1.tar.gz
  md5: dc058e835cdb8f2102128e08f9ee931c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apnswrapper = apnswrapper:main
    #
    # Would create an entry point called apnswrapper that calls apnswrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.3

  run:
    - python
    - docutils >=0.3

test:
  # Python imports
  imports:
    - APNSWrapper
    - apnswrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/apns-python-wrapper/
  license: ALv2
  summary: 'This is wrapper for Apple Push Notification Service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
