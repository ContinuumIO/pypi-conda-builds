package:
  name: method_decorator
  version: "0.1.3"

source:
  fn: method_decorator-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/method_decorator/method_decorator-0.1.3.tar.gz
  md5: 079f261932114b84f564a623cb995998
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - method_decorator = method_decorator:main
    #
    # Would create an entry point called method_decorator that calls method_decorator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - method_decorator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/denis-ryzhkov/method_decorator
  license: MIT License
  summary: 'Python decorator that knows the class the decorated method is bound to.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
