package:
  name: tes-simulation
  version: "0.1.0"

source:
  fn: TES-simulation-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/T/TES-simulation/TES-simulation-0.1.0.tar.gz
  md5: 1b79e1c219fd2d4f8c4e7ad520ad05ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tes-simulation = tes-simulation:main
    #
    # Would create an entry point called tes-simulation that calls tes-simulation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tes-simulation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.PrincipledSocietiesProject.org
  license: GNU General Public License v3 (GPLv3)
  summary: 'Simulates a Token Exchange System (TES, a token--dollar local economy)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
