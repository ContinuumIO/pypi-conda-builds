package:
  name: msplot
  version: "0.1.3.5j"

source:
  fn: MSPlot-0.1.3.5j.tar.gz
  url: https://pypi.python.org/packages/source/M/MSPlot/MSPlot-0.1.3.5j.tar.gz
  md5: dcd10cab57bc2a652e74b04947c53ed5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msplot = msplot:main
    #
    # Would create an entry point called msplot that calls msplot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib
    - pyteomics
    - pymzml

  run:
    - python
    - matplotlib
    - pyteomics
    - pymzml

test:
  # Python imports
  imports:
    - MSPlot
    - msplot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Utilities for plotting 3D mass spec data, XIC etc from mzML data files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
