package:
  name: collective.js.chatbox
  version: "1.0"

source:
  fn: collective.js.chatbox-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.chatbox/collective.js.chatbox-1.0.tar.gz
  md5: b96563b52e56a2cd27aa5e200d44271d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.chatbox = collective.js.chatbox:main
    #
    # Would create an entry point called collective.js.chatbox that calls collective.js.chatbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.chatbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.chatbox
  license: MIT
  summary: 'A packaging of the jQuery UI Chatbox plugin for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
