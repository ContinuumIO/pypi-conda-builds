package:
  name: slc.seminarportal
  version: "1.4.9"

source:
  fn: slc.seminarportal-1.4.9.zip
  url: https://pypi.python.org/packages/source/s/slc.seminarportal/slc.seminarportal-1.4.9.zip
  md5: 09efb63081fd9d15fb2e2f4733089900
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.seminarportal = slc.seminarportal:main
    #
    # Would create an entry point called slc.seminarportal that calls slc.seminarportal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.relations
    - products.atreferencebrowserwidget
    - products.linguaplone
    - collective.orderedmultiselectwidget

  run:
    - python
    - setuptools
    - setuptools
    - products.relations
    - products.atreferencebrowserwidget
    - products.linguaplone
    - collective.orderedmultiselectwidget

test:
  # Python imports
  imports:
    - slc
    - slc.seminarportal
    - slc.seminarportal.Extensions
    - slc.seminarportal.browser
    - slc.seminarportal.content
    - slc.seminarportal.events
    - slc.seminarportal.portlets
    - slc.seminarportal.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/slc.seminarportal/
  license: GNU General Public License (GPL) or European Union Public Licence 1.1 (EUPL 1.1)
  summary: 'A Seminar and Conference websites built on Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
