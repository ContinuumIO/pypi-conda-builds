package:
  name: eusful
  version: "0.7.2"

source:
  fn: eusful-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/e/eusful/eusful-0.7.2.tar.gz
  md5: 3bb59b4e0a216c471486b20d972cdb8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eusful = eusful:main
    #
    # Would create an entry point called eusful that calls eusful.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eusful
    - eusful.data
    - eusful.decorators
    - eusful.errors
    - eusful.for_web
    - eusful.xpath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'These are things I use in places.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
