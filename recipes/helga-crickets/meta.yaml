package:
  name: helga-crickets
  version: "0.0.3"

source:
  fn: helga-crickets-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-crickets/helga-crickets-0.0.3.tar.gz
  md5: 0be4c8b313cebcefa0f739949449d9c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-crickets = helga-crickets:main
    #
    # Would create an entry point called helga-crickets that calls helga-crickets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-crickets
  license: UNKNOWN
  summary: 'Helga plugin that will respond with the word cricketsafter a question has gone unacknowledged for a configurable amount of time.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
