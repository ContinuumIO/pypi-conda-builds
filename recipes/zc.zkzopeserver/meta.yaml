package:
  name: zc.zkzopeserver
  version: "1.3.2"

source:
  fn: zc.zkzopeserver-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.zkzopeserver/zc.zkzopeserver-1.3.2.tar.gz
  md5: 9f8d715299672a589f0081c8e3c88e1f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.zkzopeserver = zc.zkzopeserver:main
    #
    # Would create an entry point called zc.zkzopeserver that calls zc.zkzopeserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.zk
    - zope.server

  run:
    - python
    - setuptools
    - setuptools
    - zc.zk
    - zope.server

test:
  # Python imports
  imports:
    - zc
    - zc.zkzopeserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'zope.server wrapper that registers with ZooKeeper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
