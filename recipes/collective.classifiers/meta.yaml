package:
  name: collective.classifiers
  version: "0.4.1"

source:
  fn: collective.classifiers-0.4.1.zip
  url: https://pypi.python.org/packages/source/c/collective.classifiers/collective.classifiers-0.4.1.zip
  md5: 1ec84a0b1a7b3373b4045d6a3cb51cb7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.classifiers = collective.classifiers:main
    #
    # Would create an entry point called collective.classifiers that calls collective.classifiers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform
    - plone.behavior
    - zope.component
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform
    - plone.behavior
    - zope.component
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.classifiers
    - collective.classifiers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zestsoftware.nl/
  license: GPL
  summary: 'Themes and categories behavior'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
