package:
  name: magnetpy
  version: "0.0.1"

source:
  fn: magnetpy-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/magnetpy/magnetpy-0.0.1.tar.gz
  md5: 768a9b96ae3e18cf95674950f6a3c24f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magnetpy = magnetpy:main
    #
    # Would create an entry point called magnetpy that calls magnetpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - magnet
    - magnetpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jarekps/magnetpy
  license: New BSD License
  summary: 'very simple and half-assed magnet url parser written in python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
