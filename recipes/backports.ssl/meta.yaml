package:
  name: backports.ssl
  version: "0.0.6"

source:
  fn: backports.ssl-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/b/backports.ssl/backports.ssl-0.0.6.tar.gz
  md5: 8822046f5569d4712127f253a3831a38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backports.ssl = backports.ssl:main
    #
    # Would create an entry point called backports.ssl that calls backports.ssl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - backports
    - backports.ssl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alekstorm/backports.ssl
  license: MIT License
  summary: 'The Python 3.4 standard `ssl` module API implemented on top of pyOpenSSL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
