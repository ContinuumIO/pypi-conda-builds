package:
  name: flask-iiif
  version: "0.1.0"

source:
  fn: Flask-IIIF-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-IIIF/Flask-IIIF-0.1.0.tar.gz
  md5: 72f46a6e47de654359b545331e9fbe6b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-iiif = flask-iiif:main
    #
    # Would create an entry point called flask-iiif that calls flask-iiif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-restful >=0.2.12
    - blinker
    - six
    - pillow

  run:
    - python
    - flask
    - flask-restful >=0.2.12
    - blinker
    - six
    - pillow

test:
  # Python imports
  imports:
    - flask_iiif

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/inveniosoftware/flask-iiif/
  license: BSD License
  summary: 'Flask-IIIF extension provides easy IIIF API standard integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
