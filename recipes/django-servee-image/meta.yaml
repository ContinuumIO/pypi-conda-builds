package:
  name: django-servee-image
  version: "0.2"

source:
  fn: django-servee-image-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-servee-image/django-servee-image-0.2.tar.gz
  md5: cf5befc2dc4290c595751887d1fdec28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-servee-image = django-servee-image:main
    #
    # Would create an entry point called django-servee-image that calls django-servee-image.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil
    - easy-thumbnails

  run:
    - python
    - pil
    - easy-thumbnails

test:
  # Python imports
  imports:
    - servee_image

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/servee/django-servee-image/
  license: BSD License
  summary: 'Document Plugin for django-servee'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
