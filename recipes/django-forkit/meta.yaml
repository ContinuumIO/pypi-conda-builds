package:
  name: django-forkit
  version: "0.9.5"

source:
  fn: django-forkit-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-forkit/django-forkit-0.9.5.tar.gz
  md5: 1da511d78dd4b1a47811192bbfd83aab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-forkit = django-forkit:main
    #
    # Would create an entry point called django-forkit that calls django-forkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - forkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Utility functions for forking, resetting and diffing model objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
