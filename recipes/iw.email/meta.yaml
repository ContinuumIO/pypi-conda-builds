package:
  name: iw.email
  version: "1.4"

source:
  fn: iw.email-1.4.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.email/iw.email-1.4.tar.gz
  md5: 7d0628618318f45d64ebbbf2f58792b5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.email = iw.email:main
    #
    # Would create an entry point called iw.email that calls iw.email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - docutils
    - webtest

  run:
    - python
    - setuptools
    - setuptools
    - docutils
    - webtest

test:
  # Python imports
  imports:
    - iw
    - iw.email
    - iw.email.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/iw.email
  license: GPL
  summary: 'Email utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
