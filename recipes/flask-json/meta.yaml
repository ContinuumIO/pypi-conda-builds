package:
  name: flask-json
  version: "0.2.0"

source:
  fn: Flask-JSON-0.2.0.zip
  url: https://pypi.python.org/packages/source/F/Flask-JSON/Flask-JSON-0.2.0.zip
  md5: 12969a3a1afb4131cf8171e86eabf895
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-json = flask-json:main
    #
    # Would create an entry point called flask-json that calls flask-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10

  run:
    - python
    - flask >=0.10

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skozlovf/flask-json
  license: BSD License
  summary: 'Better JSON support for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
