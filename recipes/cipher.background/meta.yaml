package:
  name: cipher.background
  version: "2.0.0a1"

source:
  fn: cipher.background-2.0.0a1.zip
  url: https://pypi.python.org/packages/source/c/cipher.background/cipher.background-2.0.0a1.zip
  md5: 88aece1a01345b33659a144317a26602
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cipher.background = cipher.background:main
    #
    # Would create an entry point called cipher.background that calls cipher.background.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - transaction
    - zope.component
    - zope.security

  run:
    - python
    - setuptools
    - transaction
    - zope.component
    - zope.security

test:
  # Python imports
  imports:
    - cipher
    - cipher.background
    - cipher.background.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cipher.background/
  license: ZPL 2.1
  summary: 'Background thread support with ZODB support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
