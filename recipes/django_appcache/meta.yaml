package:
  name: django_appcache
  version: "1.4"

source:
  fn: django_appcache-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django_appcache/django_appcache-1.4.tar.gz
  md5: 88b354c01fc106834d5860e8bda54b3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_appcache = django_appcache:main
    #
    # Would create an entry point called django_appcache that calls django_appcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_appcache
    - django_appcache.management
    - django_appcache.management.commands
    - django_appcache.templates
    - django_appcache.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/django_appcache
  license: BSD
  summary: 'Build and serve an appcache manifest from Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
