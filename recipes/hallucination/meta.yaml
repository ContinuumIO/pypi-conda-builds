package:
  name: hallucination
  version: "0.2.16"

source:
  fn: hallucination-0.2.16.tar.gz
  url: https://pypi.python.org/packages/source/h/hallucination/hallucination-0.2.16.tar.gz
  md5: 4a878dba8c960762e6cde2c83c55e354
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hallucination = hallucination:main
    #
    # Would create an entry point called hallucination that calls hallucination.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hallucination

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/suminb/hallucination
  license: UNKNOWN
  summary: 'A Python library for proxy server list management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
