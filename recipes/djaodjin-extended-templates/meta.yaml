package:
  name: djaodjin-extended-templates
  version: "0.1.5"

source:
  fn: djaodjin-extended-templates-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djaodjin-extended-templates/djaodjin-extended-templates-0.1.5.tar.gz
  md5: 95f6fffe4ed46e19504954b2e65e32e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djaodjin-extended-templates = djaodjin-extended-templates:main
    #
    # Would create an entry point called djaodjin-extended-templates that calls djaodjin-extended-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - extended_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djaodjin/djaodjin-extended-templates/
  license: BSD
  summary: "DjaoDjin's Template wrappers for HTML email and PDF templates"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
