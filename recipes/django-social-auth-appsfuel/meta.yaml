package:
  name: django-social-auth-appsfuel
  version: "1.0.0"

source:
  fn: django-social-auth-appsfuel-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-auth-appsfuel/django-social-auth-appsfuel-1.0.0.tar.gz
  md5: 169b2bd821b73d060793130d7ac99581
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-auth-appsfuel = django-social-auth-appsfuel:main
    #
    # Would create an entry point called django-social-auth-appsfuel that calls django-social-auth-appsfuel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-social-auth

  run:
    - python
    - django-social-auth

test:
  # Python imports
  imports:
    - django_social_auth_appsfuel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AppsFuel/django-social-auth-appsfuel
  license: BSD
  summary: 'Appsfuel backend for django-social-auth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
