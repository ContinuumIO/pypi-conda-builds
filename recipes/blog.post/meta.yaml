package:
  name: blog.post
  version: "0.1b3"

source:
  fn: blog.post-0.1b3.zip
  url: https://pypi.python.org/packages/source/b/blog.post/blog.post-0.1b3.zip
  md5: 7e13266473250c3036d84f91011e0878
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blog.post = blog.post:main
    #
    # Would create an entry point called blog.post that calls blog.post.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone
    - setuptools
    - plone.app.discussion

  run:
    - python
    - setuptools
    - plone
    - setuptools
    - plone.app.discussion

test:
  # Python imports
  imports:
    - blog
    - blog.post
    - blog.post.browser
    - blog.post.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/blog.post
  license: GPL
  summary: 'This package is use to create a Plone blog site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
