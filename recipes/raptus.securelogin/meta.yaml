package:
  name: raptus.securelogin
  version: "1.0a2"

source:
  fn: raptus.securelogin-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.securelogin/raptus.securelogin-1.0a2.tar.gz
  md5: 8e67bd8084e13e215b2b5ed589b37f9d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.securelogin = raptus.securelogin:main
    #
    # Would create an entry point called raptus.securelogin that calls raptus.securelogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - raptus
    - raptus.securelogin
    - raptus.securelogin.Extensions
    - raptus.securelogin.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/raptus.securelogin
  license: GPL
  summary: 'Provides a more secure login procedure for Plone for a defined set of groups.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
