package:
  name: ldap-hopper
  version: "3.0.1"

source:
  fn: LDAP-Hopper-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LDAP-Hopper/LDAP-Hopper-3.0.1.tar.gz
  md5: 530719682eed845cefc6d599345f5a71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldap-hopper = ldap-hopper:main
    #
    # Would create an entry point called ldap-hopper that calls ldap-hopper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap >=2.4.13

  run:
    - python
    - python-ldap >=2.4.13

test:
  # Python imports
  imports:
    - ldap_hopper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/LDAP-Hopper/
  license: LICENSE.txt
  summary: 'Simple access to LDAP Directory Information Tree.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
