package:
  name: cubicweb-openidrelay
  version: "0.3.0"

source:
  fn: cubicweb-openidrelay-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-openidrelay/cubicweb-openidrelay-0.3.0.tar.gz
  md5: c2a42c10ead754fb117c76b04261a864
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-openidrelay = cubicweb-openidrelay:main
    #
    # Would create an entry point called cubicweb-openidrelay that calls cubicweb-openidrelay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.15.0
    - python-openid >=2.2.5

  run:
    - python
    - cubicweb >=3.15.0
    - python-openid >=2.2.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-openidrelay
  license: LGPL
  summary: 'An authentication plugin to turn a cubicweb instance into an OpenID relay.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
