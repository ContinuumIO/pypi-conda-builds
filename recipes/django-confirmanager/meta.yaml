package:
  name: django-confirmanager
  version: "0.4"

source:
  fn: django-confirmanager-0.4.zip
  url: https://pypi.python.org/packages/source/d/django-confirmanager/django-confirmanager-0.4.zip
  md5: 669af2440454455d6e88dccfedad38c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-confirmanager = django-confirmanager:main
    #
    # Would create an entry point called django-confirmanager that calls django-confirmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-templated-email >=0.4.7,<0.5

  run:
    - python
    - django-templated-email >=0.4.7,<0.5

test:
  # Python imports
  imports:
    - confirmanager
    - confirmanager.migrations
    - confirmanager.templatetags
    - test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-confirmanager
  license: MIT License
  summary: 'Simple email confirmation application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
