package:
  name: hf3lint
  version: "0.1-rc"

source:
  fn: hf3lint-0.1-rc.tar.gz
  url: https://pypi.python.org/packages/source/h/hf3lint/hf3lint-0.1-rc.tar.gz
  md5: d413849d60ee7e1384cc21f3cbba4ffb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hf3lint = hf3lint:main
    #
    # Would create an entry point called hf3lint that calls hf3lint.main()

    - hf3lint = hf3lint:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hf3lint

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - hf3lint --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/areku/hf3lint
  license: GNU General Public License v3 (GPLv3)
  summary: 'Linter for Hiflow3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
