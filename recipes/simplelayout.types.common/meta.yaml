package:
  name: simplelayout.types.common
  version: "3.1.0"

source:
  fn: simplelayout.types.common-3.1.0.zip
  url: https://pypi.python.org/packages/source/s/simplelayout.types.common/simplelayout.types.common-3.1.0.zip
  md5: 184ee47a0c9664099a3439b4ee0e4ba5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplelayout.types.common = simplelayout.types.common:main
    #
    # Would create an entry point called simplelayout.types.common that calls simplelayout.types.common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade

  run:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade

test:
  # Python imports
  imports:
    - simplelayout
    - simplelayout.types
    - simplelayout.types.common
    - simplelayout.types.common.Extensions
    - simplelayout.types.common.browser
    - simplelayout.types.common.content
    - simplelayout.types.common.extender
    - simplelayout.types.common.tests
    - simplelayout.types.common.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/simplelayout.types.common
  license: GPL2
  summary: 'Simplelayout component providing content types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
