package:
  name: flask-formencode
  version: "0.10.1"

source:
  fn: Flask-FormEncode-0.10.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-FormEncode/Flask-FormEncode-0.10.1.tar.gz
  md5: bd2b19e8b39e0873707f7af48cdff240
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-formencode = flask-formencode:main
    #
    # Would create an entry point called flask-formencode that calls flask-formencode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - formencode

  run:
    - python
    - flask
    - formencode

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xsleonard/flask-formencode
  license: MIT License
  summary: 'A form validation extension for Flask using the FormEncode package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
