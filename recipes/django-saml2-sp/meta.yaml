package:
  name: django-saml2-sp
  version: "0.2"

source:
  fn: django-saml2-sp-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-saml2-sp/django-saml2-sp-0.2.tar.gz
  md5: 63e426adc7d61b844d01a459a0440a34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-saml2-sp = django-saml2-sp:main
    #
    # Would create an entry point called django-saml2-sp that calls django-saml2-sp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m2crypto >=0.20.1
    - beautifulsoup >=3.2.0

  run:
    - python
    - m2crypto >=0.20.1
    - beautifulsoup >=3.2.0

test:
  # Python imports
  imports:
    - saml2sp
    - saml2sp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abhinavsingh/django-saml2
  license: MIT
  summary: 'SAML 2.0 SP for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
