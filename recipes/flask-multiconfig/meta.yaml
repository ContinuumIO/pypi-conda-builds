package:
  name: flask-multiconfig
  version: "0.1"

source:
  fn: flask-multiconfig-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-multiconfig/flask-multiconfig-0.1.tar.gz
  md5: 0243df6cad993813adc7ade69eb3e974
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-multiconfig = flask-multiconfig:main
    #
    # Would create an entry point called flask-multiconfig that calls flask-multiconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pyyaml

  run:
    - python
    - flask
    - pyyaml

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/petermelias/flask-multiconfig
  license: BSD License
  summary: 'A simple extension to add advanced configuration source support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
