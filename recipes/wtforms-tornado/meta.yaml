package:
  name: wtforms-tornado
  version: "0.0.2"

source:
  fn: wtforms-tornado-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/w/wtforms-tornado/wtforms-tornado-0.0.2.tar.gz
  md5: f8d4d22f3c4b060ff29b01183d8c766c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-tornado = wtforms-tornado:main
    #
    # Would create an entry point called wtforms-tornado that calls wtforms-tornado.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - wtforms

  run:
    - python
    - tornado
    - wtforms

test:
  # Python imports
  imports:
    - wtforms_tornado

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/puentesarrin/wtforms-tornado
  license: Apache Software License
  summary: 'WTForms extensions for Tornado.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
