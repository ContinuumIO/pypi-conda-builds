package:
  name: backport_importlib
  version: "0...1"

source:
  fn: backport_importlib-0...1.tar.gz
  url: https://pypi.python.org/packages/source/b/backport_importlib/backport_importlib-0...1.tar.gz
  md5: 32be7f327e81d49b8a2dc54103ab169a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backport_importlib = backport_importlib:main
    #
    # Would create an entry point called backport_importlib that calls backport_importlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - backport_importlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ericsnowcurrently/backport_importlib/
  license: Python Software Foundation License
  summary: 'A script to backport the full importlib to 2.x'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
