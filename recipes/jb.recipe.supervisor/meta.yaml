package:
  name: jb.recipe.supervisor
  version: "0.0.1"

source:
  fn: jb.recipe.supervisor-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jb.recipe.supervisor/jb.recipe.supervisor-0.0.1.tar.gz
  md5: 8962b15b0b44877768a852929baa28e3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jb.recipe.supervisor = jb.recipe.supervisor:main
    #
    # Would create an entry point called jb.recipe.supervisor that calls jb.recipe.supervisor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.recipe.scripts
    - collective.recipe.template
    - supervisor

  run:
    - python
    - setuptools
    - setuptools
    - z3c.recipe.scripts
    - collective.recipe.template
    - supervisor

test:
  # Python imports
  imports:
    - jb
    - jb.recipe
    - jb.recipe.supervisor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/multani/jb.recipe.supervisor
  license: BSD License
  summary: 'Recipe for installing Supervisor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
