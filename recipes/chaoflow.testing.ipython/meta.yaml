package:
  name: chaoflow.testing.ipython
  version: "0.4"

source:
  fn: chaoflow.testing.ipython-0.4.zip
  url: https://pypi.python.org/packages/source/c/chaoflow.testing.ipython/chaoflow.testing.ipython-0.4.zip
  md5: 9782a18c35192c3f27d099fceefaefb4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chaoflow.testing.ipython = chaoflow.testing.ipython:main
    #
    # Would create an entry point called chaoflow.testing.ipython that calls chaoflow.testing.ipython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ipython

  run:
    - python
    - setuptools
    - setuptools
    - ipython

test:
  # Python imports
  imports:
    - chaoflow
    - chaoflow.testing
    - chaoflow.testing.ipython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chaoflow/chaoflow.testing.ipython
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'User ipython interactively in your doctest'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
