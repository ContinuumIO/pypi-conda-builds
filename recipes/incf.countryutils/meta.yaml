package:
  name: incf.countryutils
  version: "1.0"

source:
  fn: incf.countryutils-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/incf.countryutils/incf.countryutils-1.0.tar.gz
  md5: cb0d2d8ad9ce5a0c77dc1c63eefc7c46
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incf.countryutils = incf.countryutils:main
    #
    # Would create an entry point called incf.countryutils that calls incf.countryutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - incf
    - incf.countryutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'A convenience API for transformations between different ISO 3166 country codes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
