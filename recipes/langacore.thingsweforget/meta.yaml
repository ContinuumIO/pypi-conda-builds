package:
  name: langacore.thingsweforget
  version: "0.1.0"

source:
  fn: langacore.thingsweforget-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/langacore.thingsweforget/langacore.thingsweforget-0.1.0.tar.gz
  md5: 9efee1b0088c4b2fa4358ed732358b97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - langacore.thingsweforget = langacore.thingsweforget:main
    #
    # Would create an entry point called langacore.thingsweforget that calls langacore.thingsweforget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - lxml

  run:
    - python
    - distribute
    - lxml

test:
  # Python imports
  imports:
    - langacore
    - langacore.thingsweforget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Synchronization of images from thingsweforget.blogspot.com for offline use.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
