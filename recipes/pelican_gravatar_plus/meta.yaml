package:
  name: pelican_gravatar_plus
  version: "0.0.1"

source:
  fn: pelican_gravatar_plus-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican_gravatar_plus/pelican_gravatar_plus-0.0.1.tar.gz
  md5: c68fca16533682d340bcf73a0281c6f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican_gravatar_plus = pelican_gravatar_plus:main
    #
    # Would create an entry point called pelican_gravatar_plus that calls pelican_gravatar_plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==0.14.0
    - pelican ==3.1

  run:
    - python
    - requests ==0.14.0
    - pelican ==3.1

test:
  # Python imports
  imports:
    - gravatar_plus
    - pelican_gravatar_plus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/redmonkey/pelican-plugin-gravatar-plus
  license: BSD License
  summary: 'Pelican Plugin to pull Gravtars'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
