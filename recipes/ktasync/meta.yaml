package:
  name: ktasync
  version: "0.0.1"

source:
  fn: ktasync-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/ktasync/ktasync-0.0.1.tar.gz
  md5: 58f0d7c338ef2d7da1f70aff8c0befe0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ktasync = ktasync:main
    #
    # Would create an entry point called ktasync that calls ktasync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - trollius

  run:
    - python
    - trollius

test:
  # Python imports
  imports:
    - ktasync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ganwell/ktasync
  license: MIT License
  summary: 'Binary protocol of Kyoto Tycoon with asyncio for io batching'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
