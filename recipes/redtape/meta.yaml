package:
  name: redtape
  version: "1.2"

source:
  fn: redtape-1.2.tar.gz
  url: https://pypi.python.org/packages/source/r/redtape/redtape-1.2.tar.gz
  md5: b524bcd195bba8b159102cc032246810
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redtape = redtape:main
    #
    # Would create an entry point called redtape that calls redtape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown
    - pygments
    - jinja2

  run:
    - python
    - setuptools
    - markdown
    - pygments
    - jinja2

test:
  # Python imports
  imports:
    - redtape

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jmoiron/redtape
  license: MIT License
  summary: 'simple tiny document generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
