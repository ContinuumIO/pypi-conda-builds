package:
  name: taiga-contrib-hall
  version: "0.0.4"

source:
  fn: taiga-contrib-hall-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/t/taiga-contrib-hall/taiga-contrib-hall-0.0.4.tar.gz
  md5: ca6e277d664549175e97d19f96b9d5ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taiga-contrib-hall = taiga-contrib-hall:main
    #
    # Would create an entry point called taiga-contrib-hall that calls taiga-contrib-hall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - taiga_contrib_hall
    - taiga_contrib_hall.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/taigaio/taiga-contrib-hall
  license: GNU Affero General Public License v3
  summary: 'The Taiga plugin for hall integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
