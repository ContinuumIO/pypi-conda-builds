package:
  name: fxrays
  version: "1.3.1"

source:
  fn: FXrays-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FXrays/FXrays-1.3.1.tar.gz
  md5: dfcd067576313a4d05197180dcffb51a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fxrays = fxrays:main
    #
    # Would create an entry point called fxrays that calls fxrays.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - FXrays
    - fxrays

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://t3m.computop.org
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Computes extremal rays with filtering'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
