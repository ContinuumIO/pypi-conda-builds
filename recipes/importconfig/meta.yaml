package:
  name: importconfig
  version: "0.0.4"

source:
  fn: ImportConfig-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/I/ImportConfig/ImportConfig-0.0.4.tar.gz
  md5: 4d28ac551dcb54037b23bdf3a385ef91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importconfig = importconfig:main
    #
    # Would create an entry point called importconfig that calls importconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - importconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Dinoshauer/ImportConfig
  license: MIT License
  summary: u'JSON and YAML parsing with imports.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
