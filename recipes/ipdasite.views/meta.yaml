package:
  name: ipdasite.views
  version: "1.0.2"

source:
  fn: ipdasite.views-1.0.2.zip
  url: http://oodt.jpl.nasa.gov/dist/ipdasite/ipdasite.views-1.0.2.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipdasite.views = ipdasite.views:main
    #
    # Would create an entry point called ipdasite.views that calls ipdasite.views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - setuptools
    - products.cmfplone
    - plone.app.dexterity[relations]
    - z3c.autoinclude
    - five.grok

  run:
    - python
    - setuptools
    - distribute
    - setuptools
    - products.cmfplone
    - plone.app.dexterity[relations]
    - z3c.autoinclude
    - five.grok

test:
  # Python imports
  imports:
    - ipdasite
    - ipdasite.views
    - ipdasite.views.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypy.python.org/pypi/ipdasite.views
  license: Proprietary
  summary: 'International Planetary Data Alliance: Website: Views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
