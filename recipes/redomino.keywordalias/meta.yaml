package:
  name: redomino.keywordalias
  version: "0.1"

source:
  fn: redomino.keywordalias-0.1.zip
  url: https://pypi.python.org/packages/source/r/redomino.keywordalias/redomino.keywordalias-0.1.zip
  md5: 464a246442eda7cd02ca74aeb4cfed50
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.keywordalias = redomino.keywordalias:main
    #
    # Would create an entry point called redomino.keywordalias that calls redomino.keywordalias.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer
    - plone.app.registry

test:
  # Python imports
  imports:
    - redomino
    - redomino.keywordalias
    - redomino.keywordalias.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.keywordalias
  license: GPL
  summary: 'Keyword alias'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
