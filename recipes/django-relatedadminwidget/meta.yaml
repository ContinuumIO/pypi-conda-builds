package:
  name: django-relatedadminwidget
  version: "0.0.3"

source:
  fn: django-relatedadminwidget-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-relatedadminwidget/django-relatedadminwidget-0.0.3.tar.gz
  md5: 6bdbc9a47d1a2a7d766161b52988ef5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-relatedadminwidget = django-relatedadminwidget:main
    #
    # Would create an entry point called django-relatedadminwidget that calls django-relatedadminwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - relatedwidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benjaoming/django-relatedadminwidget
  license: BSD License or BSD License
  summary: 'Get edit and delete links in your django admin. A utility class to let your model admins inherit from.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
