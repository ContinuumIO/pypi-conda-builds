package:
  name: python-awis
  version: "1.2.1"

source:
  fn: python-awis-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-awis/python-awis-1.2.1.tar.gz
  md5: f87d3ff35bd34452292761c224a45376
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-awis = python-awis:main
    #
    # Would create an entry point called python-awis that calls python-awis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - awis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/muhuk/python-awis
  license: GNU General Public License (GPL)
  summary: 'Python bindings for Alexa Web Information Service (AWIS) API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
