package:
  name: iijdns
  version: "0.0.2"

source:
  fn: iijdns-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iijdns/iijdns-0.0.2.tar.gz
  md5: 0cc0ac6d0ea13be4e26ea9880873c83c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iijdns = iijdns:main
    #
    # Would create an entry point called iijdns that calls iijdns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.11
    - urllib3 ==1.10

  run:
    - python
    - pyyaml ==3.11
    - urllib3 ==1.10

test:
  # Python imports
  imports:
    - iijdns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/samuraitaiga/python-iij-dns
  license: MIT License
  summary: 'python library(API wrapper) for IIJ DNS service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
