package:
  name: ironcli
  version: "0.0.7"

source:
  fn: ironcli-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/i/ironcli/ironcli-0.0.7.tar.gz
  md5: 85d304524f081fdb817ea5e60595369c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ironcli = ironcli:main
    #
    # Would create an entry point called ironcli that calls ironcli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cctrl >=1.15.2

  run:
    - python
    - cctrl >=1.15.2

test:
  # Python imports
  imports:
    - ironcli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.cloudcontrol.com
  license: Apache Software License
  summary: 'Kilatiron command line utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
