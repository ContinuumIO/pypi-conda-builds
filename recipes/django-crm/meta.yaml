package:
  name: django-crm
  version: "0.1.0"

source:
  fn: django-crm-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crm/django-crm-0.1.0.tar.gz
  md5: c1f88192cbb9f53b9b8735afb71e2863
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crm = django-crm:main
    #
    # Would create an entry point called django-crm that calls django-crm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mailchimp-v1.3

  run:
    - python
    - django-mailchimp-v1.3

test:
  # Python imports
  imports:
    - crm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.de-nieuwbouwmakelaar.nl
  license: BSD License
  summary: 'Customer relation management system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
