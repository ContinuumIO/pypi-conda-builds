package:
  name: sushi
  version: "0.3.5"

source:
  fn: sushi-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/s/sushi/sushi-0.3.5.tar.gz
  md5: f2c457ad1cc0c386968943bf53687f79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sushi = sushi:main
    #
    # Would create an entry point called sushi that calls sushi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - docopt
    - requests

  run:
    - python
    - jinja2
    - docopt
    - requests

test:
  # Python imports
  imports:
    - sushi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/socketubs/sushi/
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Create package template for everything'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
