package:
  name: simpleflock
  version: "0.0.2"

source:
  fn: simpleflock-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/simpleflock/simpleflock-0.0.2.tar.gz
  md5: 6f915515643f7d7c55fbbe96bd61fc59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleflock = simpleflock:main
    #
    # Would create an entry point called simpleflock that calls simpleflock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - simpleflock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/derpston/python-simpleflock
  license: UNKNOWN
  summary: 'Simple flock-based file locking, using the `with` syntax'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
