package:
  name: coroner
  version: "0.2.2"

source:
  fn: coroner-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/coroner/coroner-0.2.2.tar.gz
  md5: 79ecac85b8a5abf40bf3d3196dcf77a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coroner = coroner:main
    #
    # Would create an entry point called coroner that calls coroner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coroner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/intuited/coroner
  license: BSD License
  summary: ' Wraps python invocations in code to do post-mortem debugging and sundry usefulnesses.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
