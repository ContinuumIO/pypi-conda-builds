package:
  name: pony_monitor
  version: "0.1"

source:
  fn: pony_monitor-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pony_monitor/pony_monitor-0.1.tar.gz
  md5: d07ca6a2b6734978231d7c63d279687f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pony_monitor = pony_monitor:main
    #
    # Would create an entry point called pony_monitor that calls pony_monitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pony_monitor
    - pony_monitor.backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wellnesstelecom/pony_monitor
  license: UNKNOWN
  summary: 'monitor a url for a response code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
