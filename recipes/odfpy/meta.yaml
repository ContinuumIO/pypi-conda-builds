package:
  name: odfpy
  version: "0.9.6"

source:
  fn: odfpy-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/o/odfpy/odfpy-0.9.6.tar.gz
  md5: 3f570ead2b5f5eb6eab97eecce22d491
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - odfpy = odfpy:main
    #
    # Would create an entry point called odfpy that calls odfpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - odf
    - odfpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://joinup.ec.europa.eu/software/odfpy/home
  license: Apache Software License or GNU General Public License (GPL) or GNU Library or Lesser General Public License (LGPL)
  summary: 'Python API and tools to manipulate OpenDocument files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
