package:
  name: modemdriver
  version: "2.0.14"

source:
  fn: ModemDriver-2.0.14.tar.gz
  url: https://pypi.python.org/packages/source/M/ModemDriver/ModemDriver-2.0.14.tar.gz
  md5: 5531fea5fd9a07f058ee2fb996704c66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modemdriver = modemdriver:main
    #
    # Would create an entry point called modemdriver that calls modemdriver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ModemDriver
    - modemdriver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rab.co.id/gsmmodem
  license: LGPL
  summary: 'Modem driver using AT command'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
