package:
  name: incuna-videos
  version: "5.1.2"

source:
  fn: incuna-videos-5.1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/incuna-videos/incuna-videos-5.1.2.tar.gz
  md5: 571979809cf38412573d6cfa4bf727f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incuna-videos = incuna-videos:main
    #
    # Would create an entry point called incuna-videos that calls incuna-videos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feincms >=1.10,<1.11
    - django-imagekit >=3.2.1,<3.3

  run:
    - python
    - feincms >=1.10,<1.11
    - django-imagekit >=3.2.1,<3.3

test:
  # Python imports
  imports:
    - videos
    - videos.extensions
    - videos.module
    - videos.module.chapters
    - videos.module.chapters.tests
    - videos.module.speakers
    - videos.module.speakers.tests
    - videos.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/incuna-videos
  license: UNKNOWN
  summary: 'Generic extensible video content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
