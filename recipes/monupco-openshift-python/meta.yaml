package:
  name: monupco-openshift-python
  version: "1.9"

source:
  fn: monupco-openshift-python-1.9.tar.gz
  url: https://pypi.python.org/packages/source/m/monupco-openshift-python/monupco-openshift-python-1.9.tar.gz
  md5: 80d1b91188b358e4ab175aa2dede48dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monupco-openshift-python = monupco-openshift-python:main
    #
    # Would create an entry point called monupco-openshift-python that calls monupco-openshift-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - monupco_openshift_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/monupco/monupco-openshift-python
  license: MIT License
  summary: 'monupco.com registration agent for OpenShift / Python applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
