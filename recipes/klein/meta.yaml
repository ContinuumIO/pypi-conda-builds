package:
  name: klein
  version: "15.0.0"

source:
  fn: klein-15.0.0.tar.gz
  url: https://pypi.python.org/packages/source/k/klein/klein-15.0.0.tar.gz
  md5: dd4a790110d8e24b01ef8aff79f0b74e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - klein = klein:main
    #
    # Would create an entry point called klein that calls klein.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=12.1
    - werkzeug

  run:
    - python
    - twisted >=12.1
    - werkzeug

test:
  # Python imports
  imports:
    - klein
    - klein.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twisted/klein
  license: MIT License
  summary: 'werkzeug + twisted.web'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
