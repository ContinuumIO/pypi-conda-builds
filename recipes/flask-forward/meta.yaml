package:
  name: flask-forward
  version: "0.1.0"

source:
  fn: Flask-Forward-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Forward/Flask-Forward-0.1.0.tar.gz
  md5: 9f9294e97da9d849956e34d2c46bb62d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-forward = flask-forward:main
    #
    # Would create an entry point called flask-forward that calls flask-forward.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - setuptools
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saikocat/flask-forward
  license: MIT License
  summary: 'Flask-Forward extension provides auto discovery,                  prioritization and rendering of template for Flask                  based on endpoint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
