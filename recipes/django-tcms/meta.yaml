package:
  name: django-tcms
  version: "0.1.11"

source:
  fn: django-tcms-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tcms/django-tcms-0.1.11.tar.gz
  md5: e5c073acfc1b31261f2820a02a012830
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tcms = django-tcms:main
    #
    # Would create an entry point called django-tcms that calls django-tcms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.5

  run:
    - python
    - django >=1.2.5

test:
  # Python imports
  imports:
    - tcms
    - tcms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/omab/django-tcms
  license: BSD License
  summary: 'Django tCMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
