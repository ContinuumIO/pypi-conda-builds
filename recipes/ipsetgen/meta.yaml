package:
  name: ipsetgen
  version: "v0.0.2"

source:
  fn: ipsetgen-v0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/ipsetgen/ipsetgen-v0.0.2.tar.gz
  md5: cfd32b14648d8b4e26211a7b07c44dfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipsetgen = ipsetgen:main
    #
    # Would create an entry point called ipsetgen that calls ipsetgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - ipsetgen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fly/ipsetgen
  license: BSD License
  summary: 'role-based ipset generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
