package:
  name: horntell
  version: "0.2.0"

source:
  fn: horntell-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/horntell/horntell-0.2.0.tar.gz
  md5: 2718142658346bc0456a1347f6cfcf16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horntell = horntell:main
    #
    # Would create an entry point called horntell that calls horntell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.8.8
    - simplejson

  run:
    - python
    - requests >=0.8.8
    - simplejson

test:
  # Python imports
  imports:
    - horntell
    - horntell.http

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/horntell/python-sdk
  license: UNKNOWN
  summary: 'Horntell SDK for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
