package:
  name: frasco-models
  version: "0.1"

source:
  fn: frasco-models-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-models/frasco-models-0.1.tar.gz
  md5: 275d1e808ced431c9b1c5a9665d278d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-models = frasco-models:main
    #
    # Would create an entry point called frasco-models that calls frasco-models.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - persistpy
    - inflection
    - flask-mongoengine >=0.7

  run:
    - python
    - frasco
    - persistpy
    - inflection
    - flask-mongoengine >=0.7

test:
  # Python imports
  imports:
    - frasco_models
    - frasco_models.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-models
  license: MIT
  summary: 'ORM for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
