package:
  name: django-dockitcms
  version: "0.0.11"

source:
  fn: django-dockitcms-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dockitcms/django-dockitcms-0.0.11.tar.gz
  md5: d8a7fde70b5fd3557b5b6c905fe55603
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dockitcms = django-dockitcms:main
    #
    # Would create an entry point called django-dockitcms that calls django-dockitcms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dockitcms
    - dockitcms.contrib
    - dockitcms.contrib.dockit_admin_tools
    - dockitcms.contrib.formviewpoint
    - dockitcms.contrib.thumbnailfield
    - dockitcms.loading
    - dockitcms.models
    - dockitcms.pagecollection
    - dockitcms.resources
    - dockitcms.templatetags
    - dockitcms.tests
    - dockitcms.tests.viewpoints
    - dockitcms.viewpoints
    - dockitcms.widgetblock
    - dockitcms.widgetblock.templatetags
    - dockitcms.widgetblock.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/webcube/django-dockitcms
  license: New BSD License
  summary: 'CMS written using django-dockit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
