package:
  name: django-icanhaz
  version: "0.2.1"

source:
  fn: django-icanhaz-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icanhaz/django-icanhaz-0.2.1.tar.gz
  md5: 13f49eac67418895f4461e57aa4469d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icanhaz = django-icanhaz:main
    #
    # Would create an entry point called django-icanhaz that calls django-icanhaz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - icanhaz
    - icanhaz.templatetags
    - icanhaz.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carljm/django-icanhaz/
  license: BSD License
  summary: 'A Django template tag for embedding ICanHaz.js templates safely.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
