package:
  name: autowrapt
  version: "1.0"

source:
  fn: autowrapt-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/autowrapt/autowrapt-1.0.tar.gz
  md5: f882378fc981470e74dce120312e5dd6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autowrapt = autowrapt:main
    #
    # Would create an entry point called autowrapt that calls autowrapt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wrapt >=1.10.4

  run:
    - python
    - setuptools
    - wrapt >=1.10.4

test:
  # Python imports
  imports:
    - autowrapt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GrahamDumpleton/autowrapt
  license: BSD License
  summary: 'Boostrap mechanism for monkey patches.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
