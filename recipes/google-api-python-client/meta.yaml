package:
  name: google-api-python-client
  version: "1.4.0"

source:
  fn: google-api-python-client-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/g/google-api-python-client/google-api-python-client-1.4.0.tar.gz
  md5: 48e600d261378804bf019c6d26676c1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google-api-python-client = google-api-python-client:main
    #
    # Would create an entry point called google-api-python-client that calls google-api-python-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.8
    - oauth2client >=1.4.6
    - six >=1.6.1
    - uritemplate >=0.6

  run:
    - python
    - httplib2 >=0.8
    - oauth2client >=1.4.6
    - six >=1.6.1
    - uritemplate >=0.6

test:
  # Python imports
  imports:
    - apiclient
    - googleapiclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/google/google-api-python-client/
  license: Apache Software License
  summary: 'Google API Client Library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
