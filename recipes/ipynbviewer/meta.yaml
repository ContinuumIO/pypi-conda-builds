package:
  name: ipynbviewer
  version: "0.3"

source:
  fn: ipynbviewer-0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/ipynbviewer/ipynbviewer-0.3.tar.gz
  md5: 64404dce0e8ea9600ad001fec04e493f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipynbviewer = ipynbviewer:main
    #
    # Would create an entry point called ipynbviewer that calls ipynbviewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - futures
    - pygments
    - markdown
    - ipython

  run:
    - python
    - futures
    - pygments
    - markdown
    - ipython

test:
  # Python imports
  imports:
    - ipynbviewer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dongweiming/ipynb-viewer
  license: BSD License
  summary: 'Rendering local ipynb to static HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
