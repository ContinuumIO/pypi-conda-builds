package:
  name: collective.z3cform.wizard
  version: "1.4.8"

source:
  fn: collective.z3cform.wizard-1.4.8.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.wizard/collective.z3cform.wizard-1.4.8.zip
  md5: 1a230f071aa398972f39b56cb8c27558
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.wizard = collective.z3cform.wizard:main
    #
    # Would create an entry point called collective.z3cform.wizard that calls collective.z3cform.wizard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.z3cform >=0.7.5
    - products.statusmessages
    - zope2

  run:
    - python
    - setuptools
    - setuptools
    - plone.z3cform >=0.7.5
    - products.statusmessages
    - zope2

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.z3cform.wizard/trunk
  license: GNU General Public License (GPL)
  summary: 'This is a library for creating multi-page wizards using z3c.form. Values are stored in a session until the wizard is finished.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
