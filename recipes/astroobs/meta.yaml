package:
  name: astroobs
  version: "1.2"

source:
  fn: astroobs-1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/astroobs/astroobs-1.2.tar.gz
  md5: 435497936a9fc9ad9363b383a5953280
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astroobs = astroobs:main
    #
    # Would create an entry point called astroobs that calls astroobs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyephem
    - pytz

  run:
    - python
    - pyephem
    - pytz

test:
  # Python imports
  imports:
    - astroobs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ceyzeriat/astroobs
  license: MIT License
  summary: 'Provides astronomy ephemeris to plan telescope observations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
