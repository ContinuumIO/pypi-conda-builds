package:
  name: linotpadminclientgui
  version: "2.7.1"

source:
  fn: LinOTPAdminClientGUI-2.7.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LinOTPAdminClientGUI/LinOTPAdminClientGUI-2.7.1.tar.gz
  md5: c49161eae9044e671d164f23df51048b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linotpadminclientgui = linotpadminclientgui:main
    #
    # Would create an entry point called linotpadminclientgui that calls linotpadminclientgui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - configobj >=4.6.0

  run:
    - python
    - configobj >=4.6.0

test:
  # Python imports
  imports:
    - linotpadminclientgui
    - linotputilsgui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linotp.org
  license: AGPLv3, (C) LSE Leading Security Experts GmbH
  summary: 'LinOTP GUI client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
