package:
  name: elasticmapping
  version: "1.2"

source:
  fn: ElasticMapping-1.2.tar.gz
  url: https://pypi.python.org/packages/source/E/ElasticMapping/ElasticMapping-1.2.tar.gz
  md5: a693762ec38265f2b29d627b1b4194f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticmapping = elasticmapping:main
    #
    # Would create an entry point called elasticmapping that calls elasticmapping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - elasticmapping

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Fizzadar/ElasticMapping
  license: UNKNOWN
  summary: 'A simple mappings builder for Elasticsearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
