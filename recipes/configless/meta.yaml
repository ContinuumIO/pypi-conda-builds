package:
  name: configless
  version: "0.1"

source:
  fn: configless-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/configless/configless-0.1.tar.gz
  md5: bce3df5ea51c48f9777ed51ca82bdeaa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configless = configless:main
    #
    # Would create an entry point called configless that calls configless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - configless
    - configless.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'configurator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
