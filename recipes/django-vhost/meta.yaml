package:
  name: django-vhost
  version: "0.17"

source:
  fn: django-vhost-0.17.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vhost/django-vhost-0.17.tar.gz
  md5: 9fd45f1e1d16e7294062c04c0638c843
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vhost = django-vhost:main
    #
    # Would create an entry point called django-vhost that calls django-vhost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_vhost
    - django_vhost.management
    - django_vhost.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/djangostars/django-vhost
  license: BSD License
  summary: 'Generates virtual host configuration files for apache and nginx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
