package:
  name: anfis
  version: "0.3.0"

source:
  fn: anfis-0.3.0.zip
  url: https://pypi.python.org/packages/source/a/anfis/anfis-0.3.0.zip
  md5: 0f5fcdfc542c4981dba301593e8260fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anfis = anfis:main
    #
    # Would create an entry point called anfis that calls anfis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - matplotlib

  run:
    - python
    - numpy
    - matplotlib

test:
  # Python imports
  imports:
    - anfis
    - anfis.membership

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twmeggs/anfis
  license: MIT License
  summary: 'Python Adaptive Neuro Fuzzy Inference System'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
