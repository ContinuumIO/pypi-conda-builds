package:
  name: djinn_news
  version: "1.1.1"

source:
  fn: djinn_news-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djinn_news/djinn_news-1.1.1.tar.gz
  md5: 28a004c18182b2af2fb9dd776f187520
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinn_news = djinn_news:main
    #
    # Would create an entry point called djinn_news that calls djinn_news.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pgintranet
    - djinn_forms
    - djinn_contenttypes >=1.4.0

  run:
    - python
    - setuptools
    - django
    - pgintranet
    - djinn_forms
    - djinn_contenttypes >=1.4.0

test:
  # Python imports
  imports:
    - djinn_news
    - djinn_news.forms
    - djinn_news.management
    - djinn_news.models
    - djinn_news.tests
    - djinn_news.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/djinn_news
  license: beer-ware
  summary: 'Djinn Intranet News'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
