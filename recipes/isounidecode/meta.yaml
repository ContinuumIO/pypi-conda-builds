package:
  name: isounidecode
  version: "0.2"

source:
  fn: isounidecode-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/isounidecode/isounidecode-0.2.tar.gz
  md5: 507833948bf3be2321e378ce9a86db9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isounidecode = isounidecode:main
    #
    # Would create an entry point called isounidecode that calls isounidecode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - isounidecode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/redvasily/isounidecode/tree/master
  license: BSD License
  summary: 'Conversion and transliteration of unicode into ascii or iso-8859-1'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
