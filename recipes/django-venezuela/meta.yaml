package:
  name: django-venezuela
  version: "0.4.1"

source:
  fn: django-venezuela-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-venezuela/django-venezuela-0.4.1.tar.gz
  md5: 1d8a564b257acaea865fad91e6cf4a52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-venezuela = django-venezuela:main
    #
    # Would create an entry point called django-venezuela that calls django-venezuela.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - venezuela

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rub3nc.wordpress.com/
  license: GNU General Public License v2 (GPLv2)
  summary: u'Aplicaci\xf3n en Django que contiene los modelos de estados, ciudades, municipios y parroquias de Venezuela.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
