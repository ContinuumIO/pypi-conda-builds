package:
  name: archetypes.kss
  version: "1.7.3"

source:
  fn: archetypes.kss-1.7.3.zip
  url: https://pypi.python.org/packages/source/a/archetypes.kss/archetypes.kss-1.7.3.zip
  md5: b914ae8e1df4c0430169b34925baf95e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.kss = archetypes.kss:main
    #
    # Would create an entry point called archetypes.kss that calls archetypes.kss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.uuid

  run:
    - python
    - setuptools
    - plone.uuid

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.kss
    - archetypes.kss.commands
    - archetypes.kss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: GPL
  summary: 'KSS (Kinetic Style Sheets) for Archetypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
