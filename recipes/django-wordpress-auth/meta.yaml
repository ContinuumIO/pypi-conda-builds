package:
  name: django-wordpress-auth
  version: "0.1.2"

source:
  fn: django-wordpress-auth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wordpress-auth/django-wordpress-auth-0.1.2.tar.gz
  md5: 57ad9e22d9af7bd1471fc1998932cedf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wordpress-auth = django-wordpress-auth:main
    #
    # Would create an entry point called django-wordpress-auth that calls django-wordpress-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - phpserialize ==1.3

  run:
    - python
    - django
    - phpserialize ==1.3

test:
  # Python imports
  imports:
    - wordpress_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Django integration with WordPress authentication and roles / capabilities system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
