package:
  name: djangocms-blogit
  version: "0.3.3"

source:
  fn: djangocms-blogit-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-blogit/djangocms-blogit-0.3.3.tar.gz
  md5: d76b33f89860e8f6b3df2cb99797f76e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-blogit = djangocms-blogit:main
    #
    # Would create an entry point called djangocms-blogit that calls djangocms-blogit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0.0
    - django-filer >=0.9.5
    - django-parler >=1.4
    - django-taggit >=0.10

  run:
    - python
    - django-cms >=3.0.0
    - django-filer >=0.9.5
    - django-parler >=1.4
    - django-taggit >=0.10

test:
  # Python imports
  imports:
    - blogit
    - blogit.migrations
    - blogit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/djangocms-blogit/
  license: BSD
  summary: u'Simple django-cms blog app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
