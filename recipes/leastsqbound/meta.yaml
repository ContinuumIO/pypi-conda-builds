package:
  name: leastsqbound
  version: "1.0"

source:
  fn: leastsqbound-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/leastsqbound/leastsqbound-1.0.tar.gz
  md5: b416fdb5ddb058d37df444d5fd24da9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leastsqbound = leastsqbound:main
    #
    # Would create an entry point called leastsqbound that calls leastsqbound.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - leastsqbound

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jjhelmus/leastsqbound-scipy
  license: BSD License
  summary: 'Constrained multivariate least-squares optimizer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
