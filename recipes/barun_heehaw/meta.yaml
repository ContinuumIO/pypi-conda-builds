package:
  name: barun_heehaw
  version: "1.0.0"

source:
  fn: Barun_Heehaw-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/B/Barun_Heehaw/Barun_Heehaw-1.0.0.tar.gz
  md5: f864a9e2a0fd8f62dedbfdef1fd79dc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barun_heehaw = barun_heehaw:main
    #
    # Would create an entry point called barun_heehaw that calls barun_heehaw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - barun_heehaw

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'A sample junk project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
