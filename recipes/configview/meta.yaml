package:
  name: configview
  version: "0.1.0"

source:
  fn: ConfigView-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfigView/ConfigView-0.1.0.tar.gz
  md5: 34ffc06adb5b7e151e530e192dcd2134
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configview = configview:main
    #
    # Would create an entry point called configview that calls configview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - configdict
    - html

  run:
    - python
    - configdict
    - html

test:
  # Python imports
  imports:
    - configview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schettino72/configview/
  license: MIT License
  summary: 'visualize and manipulate (sets of hierarchal) config files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
