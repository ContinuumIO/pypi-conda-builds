package:
  name: flask-styleguide
  version: "0.1.5"

source:
  fn: Flask-Styleguide-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Styleguide/Flask-Styleguide-0.1.5.tar.gz
  md5: 8cdbde5a5efcbed8063364489f426a7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-styleguide = flask-styleguide:main
    #
    # Would create an entry point called flask-styleguide that calls flask-styleguide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - pykss >=0.4

  run:
    - python
    - flask >=0.10.1
    - pykss >=0.4

test:
  # Python imports
  imports:
    - flask_styleguide

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vitalk/flask-styleguide
  license: MIT License
  summary: 'A live Style Guide for your Flask application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
