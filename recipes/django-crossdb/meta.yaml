package:
  name: django-crossdb
  version: "0.1.3"

source:
  fn: django-crossdb-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crossdb/django-crossdb-0.1.3.tar.gz
  md5: 524891b2148bead7b1e1355c55defd56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crossdb = django-crossdb:main
    #
    # Would create an entry point called django-crossdb that calls django-crossdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - crossdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/gazetteer/django-crossdb
  license: BSD
  summary: 'Tools for enabling Django models to relate to each other across databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
