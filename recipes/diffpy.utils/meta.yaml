package:
  name: diffpy.utils
  version: "1.1"

source:
  fn: diffpy.utils-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/diffpy.utils/diffpy.utils-1.1.tar.gz
  md5: 51a595182e93bdc6787220e7ea2f911d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diffpy.utils = diffpy.utils:main
    #
    # Would create an entry point called diffpy.utils that calls diffpy.utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - diffpy
    - diffpy.utils
    - diffpy.utils.parsers
    - diffpy.utils.tests
    - diffpy.utils.wx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/diffpy/diffpy.utils/
  license: BSD License
  summary: 'Shared utilities for diffpy packages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
