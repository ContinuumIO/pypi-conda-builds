package:
  name: bdist-venv2
  version: "0.1.3"

source:
  fn: bdist-venv2-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bdist-venv2/bdist-venv2-0.1.3.tar.gz
  md5: 553f7977ce1d17d0167615c9f434cf0e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bdist-venv2 = bdist-venv2:main
    #
    # Would create an entry point called bdist-venv2 that calls bdist-venv2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - virtualenv

  run:
    - python
    - setuptools
    - virtualenv

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bninja/bdist-venv2
  license: BSD License
  summary: 'Python distutils extension to create virtualenv built distributions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
