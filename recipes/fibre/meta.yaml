package:
  name: fibre
  version: "1.0"

source:
  fn: fibre-1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fibre/fibre-1.0.tar.gz
  md5: 862f0a5be9a3b2301820fc12c6d1fe9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fibre = fibre:main
    #
    # Would create an entry point called fibre that calls fibre.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fibre
    - fibre.example
    - fibre.ui
    - fibre.ui.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sqizit.bartletts.id.au/
  license: GNU General Public License (GPL)
  summary: 'Asynchronous applications made easy with twisted and (optionally) pygame.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
