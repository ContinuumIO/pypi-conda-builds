package:
  name: buildout.sendpickedversions
  version: "1.0-alpha.1"

source:
  fn: buildout.sendpickedversions-1.0-alpha.1.zip
  url: https://pypi.python.org/packages/source/b/buildout.sendpickedversions/buildout.sendpickedversions-1.0-alpha.1.zip
  md5: 38d1f1d7c015701762e9ccadf9bc2119
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.sendpickedversions = buildout.sendpickedversions:main
    #
    # Would create an entry point called buildout.sendpickedversions that calls buildout.sendpickedversions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - buildout
    - buildout.sendpickedversions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/buildout.sendpickedversions
  license: GPL
  summary: 'Sends picked packages and versions to a whiskers server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
