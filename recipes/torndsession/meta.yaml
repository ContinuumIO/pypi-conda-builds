package:
  name: torndsession
  version: "1.1.3"

source:
  fn: torndsession-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/t/torndsession/torndsession-1.1.3.tar.gz
  md5: 658a9b6d7656d384948c9f068098f0cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torndsession = torndsession:main
    #
    # Would create an entry point called torndsession that calls torndsession.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - torndsession

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mitchellchu/torndsession
  license: MIT License
  summary: 'Session extensions for Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
