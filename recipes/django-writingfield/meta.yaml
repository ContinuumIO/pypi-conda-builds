package:
  name: django-writingfield
  version: "1.1.8"

source:
  fn: django-writingfield-1.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-writingfield/django-writingfield-1.1.8.tar.gz
  md5: f59cf7a6b28ca5bc48494537c4c61739
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-writingfield = django-writingfield:main
    #
    # Would create an entry point called django-writingfield that calls django-writingfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.10

  run:
    - python
    - django >=1.4.10

test:
  # Python imports
  imports:
    - writingfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jamiecurle/django-writingfield
  license: BSD 3 Clause
  summary: 'A fullscreen textfield widget for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
