package:
  name: flask-crossdomain
  version: "0.1"

source:
  fn: flask-crossdomain-0.1.zip
  url: https://pypi.python.org/packages/source/f/flask-crossdomain/flask-crossdomain-0.1.zip
  md5: 66498bf8a4d2fd2ac6c7385d35afae62
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-crossdomain = flask-crossdomain:main
    #
    # Would create an entry point called flask-crossdomain that calls flask-crossdomain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/iurisilvio/flask-crossdomain
  license: Public Domain
  summary: 'HTTP Access Control helper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
