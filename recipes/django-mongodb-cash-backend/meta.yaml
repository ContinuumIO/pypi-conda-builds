package:
  name: django-mongodb-cash-backend
  version: "2014.6.14"

source:
  fn: django-mongodb-cash-backend-2014.6.14.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mongodb-cash-backend/django-mongodb-cash-backend-2014.6.14.tar.gz
  md5: b9de8a24d0b64ec356f7228aefc4104e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mongodb-cash-backend = django-mongodb-cash-backend:main
    #
    # Would create an entry point called django-mongodb-cash-backend that calls django-mongodb-cash-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5.3
    - pymongo >=2.6.3

  run:
    - python
    - django >=1.5.3
    - pymongo >=2.6.3

test:
  # Python imports
  imports:
    - django_mongodb_cash_backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Alir3z4/django-mongodb-cash-backend
  license: django-mongodb-engine
  summary: 'The only Django MongoDB Cache backend you need.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
