package:
  name: django-adminfiles
  version: "1.0.1"

source:
  fn: django-adminfiles-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adminfiles/django-adminfiles-1.0.1.tar.gz
  md5: db768791f5870ac867b6ee14bf89b899
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adminfiles = django-adminfiles:main
    #
    # Would create an entry point called django-adminfiles that calls django-adminfiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adminfiles
    - adminfiles.management
    - adminfiles.management.commands
    - adminfiles.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/carljm/django-adminfiles/
  license: BSD License
  summary: 'File upload manager and picker for Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
