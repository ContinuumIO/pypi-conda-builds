package:
  name: acquirerouterip
  version: "1.0.2"

source:
  fn: AcquireRouterIP-1.0.2.zip
  url: https://pypi.python.org/packages/source/A/AcquireRouterIP/AcquireRouterIP-1.0.2.zip
  md5: 72b02a7c711706896d07d9defa6feb5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acquirerouterip = acquirerouterip:main
    #
    # Would create an entry point called acquirerouterip that calls acquirerouterip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - acquirerouterip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: "Acquire the IP address on the router's wan port"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
