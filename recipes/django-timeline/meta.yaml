package:
  name: django-timeline
  version: "0.5"

source:
  fn: django-timeline-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timeline/django-timeline-0.5.tar.gz
  md5: ed1f767ef2a7ccb97bfab95aef4fd667
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timeline = django-timeline:main
    #
    # Would create an entry point called django-timeline that calls django-timeline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - redis >=2.0.0

  run:
    - python
    - django >=1.3.1
    - redis >=2.0.0

test:
  # Python imports
  imports:
    - timeline
    - timeline.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tiltshift/django-timeline.git
  license: BSD License
  summary: 'a Django timeline (activity stream) using redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
