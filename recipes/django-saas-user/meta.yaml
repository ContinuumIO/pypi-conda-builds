package:
  name: django-saas-user
  version: "0.2"

source:
  fn: django-saas-user-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-saas-user/django-saas-user-0.2.tar.gz
  md5: f5f09a09f3cf8c0cccc74bb096308188
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-saas-user = django-saas-user:main
    #
    # Would create an entry point called django-saas-user that calls django-saas-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - saas_users

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/commoncode/gambit
  license: BSD License
  summary: 'Multi-client Authentication/Authorization for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
