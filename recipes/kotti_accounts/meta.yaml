package:
  name: kotti_accounts
  version: "0.2.5"

source:
  fn: kotti_accounts-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_accounts/kotti_accounts-0.2.5.tar.gz
  md5: 7f5008f6531c536d1c337136809f48cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_accounts = kotti_accounts:main
    #
    # Would create an entry point called kotti_accounts that calls kotti_accounts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti
    - kotti_velruse >=0.3.3

  run:
    - python
    - kotti
    - kotti_velruse >=0.3.3

test:
  # Python imports
  imports:
    - kotti_accounts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kotti_accounts.readthedocs.org
  license: BSD License
  summary: 'Allows a user principal to be associated to multiple email accounts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
