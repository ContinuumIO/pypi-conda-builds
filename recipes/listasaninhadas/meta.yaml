package:
  name: listasaninhadas
  version: "1.3.1"

source:
  fn: listasaninhadas-1.3.1.zip
  url: https://pypi.python.org/packages/source/l/listasaninhadas/listasaninhadas-1.3.1.zip
  md5: 0a2e012173ef67361959dd840104089d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - listasaninhadas = listasaninhadas:main
    #
    # Would create an entry point called listasaninhadas that calls listasaninhadas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - listasaninhadas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.twitter.com/flribeiro
  license: UNKNOWN
  summary: 'Simplesmente imprime listas aninhadas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
