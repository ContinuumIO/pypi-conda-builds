package:
  name: iggy
  version: "0.5"

source:
  fn: iggy-0.5.tar.gz
  url: https://pypi.python.org/packages/source/i/iggy/iggy-0.5.tar.gz
  md5: f113eda81dcb6dc2c96eaa95e44f4a22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iggy = iggy:main
    #
    # Would create an entry point called iggy that calls iggy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyasp ==1.3.3

  run:
    - python
    - pyasp ==1.3.3

test:
  # Python imports
  imports:
    - __iggy__
    - iggy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bioasp.github.io/iggy/
  license: GPLv3+
  summary: 'A tool for consistency based analysis of influence graphs and observed systems behavior.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
