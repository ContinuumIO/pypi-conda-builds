package:
  name: ext.manager
  version: "0.1.0"

source:
  fn: Ext.Manager-0.1.0.zip
  url: https://pypi.python.org/packages/source/E/Ext.Manager/Ext.Manager-0.1.0.zip
  md5: 883e7c5c191be06450931b51740e56b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ext.manager = ext.manager:main
    #
    # Would create an entry point called ext.manager that calls ext.manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Ext.Manager
  license: extproc
  summary: 'Extension manager for classes using object composition.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
