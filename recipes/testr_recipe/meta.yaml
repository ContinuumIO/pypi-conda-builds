package:
  name: testr_recipe
  version: "0.3"

source:
  fn: testr_recipe-0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/testr_recipe/testr_recipe-0.3.tar.gz
  md5: 5ad73fc58dc9288f8a06f5f86d58dd87
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testr_recipe = testr_recipe:main
    #
    # Would create an entry point called testr_recipe that calls testr_recipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - testrepository

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - testrepository

test:
  # Python imports
  imports:
    - testr_recipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/testr_recipe
  license: Zope Public License
  summary: 'buildout recipe to create scripts to run testrepository.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
