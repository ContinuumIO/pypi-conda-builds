package:
  name: django-yearlessdate
  version: "0.3-beta"

source:
  fn: django-yearlessdate-0.3-beta.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yearlessdate/django-yearlessdate-0.3-beta.tar.gz
  md5: 1c1ac84e862357886e17df163b9572a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yearlessdate = django-yearlessdate:main
    #
    # Would create an entry point called django-yearlessdate that calls django-yearlessdate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djangoyearlessdate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seddonym/django-yearlessdate/
  license: UNKNOWN
  summary: 'Django field for storing dates without years'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
