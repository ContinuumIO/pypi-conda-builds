package:
  name: redturtle.alternative_orderui
  version: "0.5.0"

source:
  fn: redturtle.alternative_orderui-0.5.0.zip
  url: https://pypi.python.org/packages/source/r/redturtle.alternative_orderui/redturtle.alternative_orderui-0.5.0.zip
  md5: c3cb830af6293dd93f27c801ddd49cdf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.alternative_orderui = redturtle.alternative_orderui:main
    #
    # Would create an entry point called redturtle.alternative_orderui that calls redturtle.alternative_orderui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.alternative_orderui
    - redturtle.alternative_orderui.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.alternative_orderui
  license: GPL
  summary: 'An alternative order UI support for contents view in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
