package:
  name: python-hpilo
  version: "2.13.1"

source:
  fn: python-hpilo-2.13.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hpilo/python-hpilo-2.13.1.tar.gz
  md5: d94df26cc26404dc3cf0e1309f0dcabb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hpilo = python-hpilo:main
    #
    # Would create an entry point called python-hpilo that calls python-hpilo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seveas/python-hpilo
  license: GNU General Public License (GPL)
  summary: 'iLO automation from python or shell'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
