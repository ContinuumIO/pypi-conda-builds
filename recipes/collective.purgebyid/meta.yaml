package:
  name: collective.purgebyid
  version: "1.0.0a1"

source:
  fn: collective.purgebyid-1.0.0a1.zip
  url: https://pypi.python.org/packages/source/c/collective.purgebyid/collective.purgebyid-1.0.0a1.zip
  md5: 4bc06d6a2bab71c30f8800f1446ba4aa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.purgebyid = collective.purgebyid:main
    #
    # Would create an entry point called collective.purgebyid that calls collective.purgebyid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.purgebyid
    - collective.purgebyid.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.purgebyid
  license: gpl
  summary: 'p.a.caching add-on for a better purging policy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
