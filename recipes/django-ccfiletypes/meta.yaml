package:
  name: django-ccfiletypes
  version: "0.0.4"

source:
  fn: django-ccfiletypes-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ccfiletypes/django-ccfiletypes-0.0.4.tar.gz
  md5: 03cce27f89ba97c2ffbff79f3772abac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ccfiletypes = django-ccfiletypes:main
    #
    # Would create an entry point called django-ccfiletypes that calls django-ccfiletypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ccfiletypes
    - ccfiletypes.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/designcc/django-ccfiletypes
  license: BSD 3 Clause
  summary: 'Django templatefilters for displaying info about files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
