package:
  name: genibabel
  version: "0.1.0"

source:
  fn: genibabel-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/genibabel/genibabel-0.1.0.tar.gz
  md5: 83bad5364fff5d545646dc3475e83815
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genibabel = genibabel:main
    #
    # Would create an entry point called genibabel that calls genibabel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.3
    - scipy >=0.7.2
    - python-plinkio >=0.1.1
    - cwbrowser >=0.1.0

  run:
    - python
    - numpy >=1.3
    - scipy >=0.7.2
    - python-plinkio >=0.1.1
    - cwbrowser >=0.1.0

test:
  # Python imports
  imports:
    - genibabel
    - genibabel.gcas
    - genibabel.gtypes
    - genibabel.gwas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bioproj.extra.cea.fr/redmine/projects/genibabel/
  license: CeCILL-B
  summary: 'Genetic Imaging Babel (GenIBabel)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
