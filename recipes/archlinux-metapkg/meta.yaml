package:
  name: archlinux-metapkg
  version: "0.3.5"

source:
  fn: archlinux-metapkg-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/a/archlinux-metapkg/archlinux-metapkg-0.3.5.tar.gz
  md5: 94e4c97013018b7435b726b54f4ff3b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archlinux-metapkg = archlinux-metapkg:main
    #
    # Would create an entry point called archlinux-metapkg that calls archlinux-metapkg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - metapkg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Undeterminant/archlinux-metapkg
  license: CC0
  summary: 'Creates metapackages for Arch Linux.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
