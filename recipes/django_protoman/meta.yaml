package:
  name: django_protoman
  version: "0.1"

source:
  fn: django_protoman-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_protoman/django_protoman-0.1.tar.gz
  md5: d43bb421a30caf55a6a4f05e1e258176
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_protoman = django_protoman:main
    #
    # Would create an entry point called django_protoman that calls django_protoman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_protoman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ScottBarkman/django_protoman
  license: UNKNOWN
  summary: 'Simple django app to help with prototyping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
