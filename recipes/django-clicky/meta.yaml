package:
  name: django-clicky
  version: "1.2.1"

source:
  fn: django-clicky-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clicky/django-clicky-1.2.1.tar.gz
  md5: 06b75abd1eefd239956f5740e93b5c84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clicky = django-clicky:main
    #
    # Would create an entry point called django-clicky that calls django-clicky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_clicky
    - django_clicky.templatetags
    - django_clicky.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jcassee/django-clicky
  license: MIT License
  summary: 'Clicky analytics for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
