package:
  name: django-paynova
  version: "0.1.4"

source:
  fn: django-paynova-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paynova/django-paynova-0.1.4.tar.gz
  md5: 6ecd0a4e590c3b5dcdb02ecf0937cfd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paynova = django-paynova:main
    #
    # Would create an entry point called django-paynova that calls django-paynova.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.8
    - paynova-api-python-client

  run:
    - python
    - django >=1.6,<1.8
    - paynova-api-python-client

test:
  # Python imports
  imports:
    - django_paynova

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/akolpakov/django-paynova
  license: MIT License
  summary: "Python client for Paynova's API. For Django framework."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
