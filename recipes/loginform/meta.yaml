package:
  name: loginform
  version: "1.0"

source:
  fn: loginform-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/loginform/loginform-1.0.tar.gz
  md5: 48bcc6773e301fcbf8b6283124eb0ef1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loginform = loginform:main
    #
    # Would create an entry point called loginform that calls loginform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - loginform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/scrapy/loginform
  license: BSD License
  summary: 'Fill HTML login forms automatically'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
