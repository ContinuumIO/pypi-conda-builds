package:
  name: obedient.elasticsearch
  version: "1.5.0"

source:
  fn: obedient.elasticsearch-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/o/obedient.elasticsearch/obedient.elasticsearch-1.5.0.tar.gz
  md5: 8380de252a5da5fe859f7b81b456a757
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - obedient.elasticsearch = obedient.elasticsearch:main
    #
    # Would create an entry point called obedient.elasticsearch that calls obedient.elasticsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dominator[full] >=9.1
    - obedient.zookeeper >=1.5

  run:
    - python
    - setuptools
    - dominator[full] >=9.1
    - obedient.zookeeper >=1.5

test:
  # Python imports
  imports:
    - obedient.elasticsearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yandex-sysmon/obedient.elasticsearch
  license: LGPLv3
  summary: 'Elasticsearch obedient for Dominator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
