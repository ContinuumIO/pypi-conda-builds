package:
  name: django-banner-simple
  version: "0.1"

source:
  fn: django-banner-simple-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-banner-simple/django-banner-simple-0.1.tar.gz
  md5: 44bc49fe98c3f614663596317fc41443
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-banner-simple = django-banner-simple:main
    #
    # Would create an entry point called django-banner-simple that calls django-banner-simple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-native-tags
    - sorl-thumbnail >=10.12

  run:
    - python
    - setuptools
    - django
    - django-native-tags
    - sorl-thumbnail >=10.12

test:
  # Python imports
  imports:
    - banner
    - banner.migrations
    - banner.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Arpaso/django-banner-simple
  license: UNKNOWN
  summary: 'Simple banner system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
