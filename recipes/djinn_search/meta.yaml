package:
  name: djinn_search
  version: "1.0.4"

source:
  fn: djinn_search-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/djinn_search/djinn_search-1.0.4.tar.gz
  md5: eb4e214b423175719657f95622940a3c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinn_search = djinn_search:main
    #
    # Would create an entry point called djinn_search that calls djinn_search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - djinn_search
    - djinn_search.fields
    - djinn_search.forms
    - djinn_search.views
    - djinn_search.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/djinn_search
  license: beer-ware
  summary: 'Djinn Intranet search module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
