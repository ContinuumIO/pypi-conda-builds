package:
  name: meme-py
  version: "0.3.0"

source:
  fn: meme-py-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/m/meme-py/meme-py-0.3.0.tar.gz
  md5: 07cc5f7cd781777f22f6a0b148cd2937
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meme-py = meme-py:main
    #
    # Would create an entry point called meme-py that calls meme-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - yql >=0.3

  run:
    - python
    - yql >=0.3

test:
  # Python imports
  imports:
    - meme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/guilhermechapiewski/meme-py/
  license: Apache License 2.0
  summary: 'Yahoo! Meme (http://meme.yahoo.com) client API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
