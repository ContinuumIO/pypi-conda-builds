package:
  name: collective.validator.xhtmltransitional
  version: "1.0.2beta"

source:
  fn: collective.validator.xhtmlTransitional-1.0.2beta.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.validator.xhtmlTransitional/collective.validator.xhtmlTransitional-1.0.2beta.tar.gz
  md5: 70e3ed1b07e2e0f45c75a748681da41d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.validator.xhtmltransitional = collective.validator.xhtmltransitional:main
    #
    # Would create an entry point called collective.validator.xhtmltransitional that calls collective.validator.xhtmltransitional.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.validator
    - collective.validator.xhtmlTransitional
    - collective.validator.xhtmlTransitional.interfaces
    - collective.validator.xhtmlTransitional.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.validator.xhtmlTransitional
  license: GPL
  summary: 'XHTML transitional Validator for collective.validator.base'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
