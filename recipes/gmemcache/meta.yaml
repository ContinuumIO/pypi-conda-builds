package:
  name: gmemcache
  version: "0.0.5"

source:
  fn: gmemcache-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gmemcache/gmemcache-0.0.5.tar.gz
  md5: ecd4ca6180f62dc43ea9f3b41d9ced8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gmemcache = gmemcache:main
    #
    # Would create an entry point called gmemcache that calls gmemcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gsocketpool
    - msgpack-python
    - hash_ring

  run:
    - python
    - gsocketpool
    - msgpack-python
    - hash_ring

test:
  # Python imports
  imports:
    - gmemcache
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/studio-ousia/gmemcache/
  license: Apache Software License
  summary: 'A Python memcached client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
