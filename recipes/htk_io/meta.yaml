package:
  name: htk_io
  version: "0.5"

source:
  fn: htk_io-0.5.tar.gz
  url: https://pypi.python.org/packages/source/h/htk_io/htk_io-0.5.tar.gz
  md5: 281d20f570caea15c2b15748996215b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htk_io = htk_io:main
    #
    # Would create an entry point called htk_io that calls htk_io.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6.1

  run:
    - python
    - numpy >=1.6.1

test:
  # Python imports
  imports:
    - htk_io

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/MattShannon/htk_io
  license: 3-clause BSD (see License file)
  summary: 'Read and write HTK and HTS files from python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
