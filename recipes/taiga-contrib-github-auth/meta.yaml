package:
  name: taiga-contrib-github-auth
  version: "0.0.3"

source:
  fn: taiga-contrib-github-auth-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/taiga-contrib-github-auth/taiga-contrib-github-auth-0.0.3.tar.gz
  md5: b99c8dde1e4762d1415da5c0c14696d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taiga-contrib-github-auth = taiga-contrib-github-auth:main
    #
    # Would create an entry point called taiga-contrib-github-auth that calls taiga-contrib-github-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - taiga_contrib_github_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/taigaio/taiga-contrib-github-auth
  license: GNU Affero General Public License v3
  summary: 'The Taiga plugin for github authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
