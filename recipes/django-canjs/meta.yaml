package:
  name: django-canjs
  version: "0.1.2"

source:
  fn: django-canjs-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-canjs/django-canjs-0.1.2.tar.gz
  md5: 65756dc632d30053917f4883faf7657b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-canjs = django-canjs:main
    #
    # Would create an entry point called django-canjs that calls django-canjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - canjs
    - canjs.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aljosa/django-canjs
  license: MIT License
  summary: 'CanJS integration library for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
