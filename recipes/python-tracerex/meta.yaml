package:
  name: python-tracerex
  version: "0.0"

source:
  fn: python-tracerex-0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-tracerex/python-tracerex-0.0.tar.gz
  md5: 247ce1ef67cd858dcfd4498ac4000f69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-tracerex = python-tracerex:main
    #
    # Would create an entry point called python-tracerex that calls python-tracerex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hunter

  run:
    - python
    - hunter

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/hunter/
  license: UNKNOWN
  summary: 'Hunter is a flexible code tracing toolkit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
