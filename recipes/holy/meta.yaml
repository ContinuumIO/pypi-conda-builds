package:
  name: holy
  version: "0.0.2"

source:
  fn: holy-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/holy/holy-0.0.2.tar.gz
  md5: 843ccd662c14f676b1137099ec5f8b37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - holy = holy:main
    #
    # Would create an entry point called holy that calls holy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - docopt >=0.5.0
    - pyyaml >=3.10

  run:
    - python
    - docopt >=0.5.0
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - holy
    - holy.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'POC Python AST to Ruby script transformer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
