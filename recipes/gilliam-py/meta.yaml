package:
  name: gilliam-py
  version: "0.1.1"

source:
  fn: gilliam-py-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gilliam-py/gilliam-py-0.1.1.tar.gz
  md5: 3a53dd5312106674625473b260b9b937
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gilliam-py = gilliam-py:main
    #
    # Would create an entry point called gilliam-py that calls gilliam-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0
    - python-circuit

  run:
    - python
    - requests >=2.0
    - python-circuit

test:
  # Python imports
  imports:
    - gilliam
    - gilliam.packages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gilliam/gilliam-py
  license: Apache 2.0
  summary: 'Python bindings for the Gilliam platform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
