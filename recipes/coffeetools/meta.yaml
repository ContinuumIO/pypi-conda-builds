package:
  name: coffeetools
  version: "0.1.2"

source:
  fn: coffeetools-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/coffeetools/coffeetools-0.1.2.tar.gz
  md5: 0e9d85ca2d0b47be9532992bc0b5d8b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coffeetools = coffeetools:main
    #
    # Would create an entry point called coffeetools that calls coffeetools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coffeetools
    - coffeetools.ipython
    - coffeetools.jinja

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/userzimmermann/python-coffeetools
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Tools for using CoffeeScript with Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
