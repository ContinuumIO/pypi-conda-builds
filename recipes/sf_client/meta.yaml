package:
  name: sf_client
  version: "0.1.5"

source:
  fn: sf_client-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/s/sf_client/sf_client-0.1.5.tar.gz
  md5: 2ef13bdb4613225e7a7afcec24b2cd10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sf_client = sf_client:main
    #
    # Would create an entry point called sf_client that calls sf_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simple_salesforce >=0.51
    - sc_pylibs >=0.1.1

  run:
    - python
    - simple_salesforce >=0.51
    - sc_pylibs >=0.1.1

test:
  # Python imports
  imports:
    - sf
    - sf_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Keep it real
  summary: 'Connect to Salesforce REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
