package:
  name: js.markdown_converter
  version: "0.1.0"

source:
  fn: js.markdown_converter-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.markdown_converter/js.markdown_converter-0.1.0.tar.gz
  md5: 3c18536cfae1fc0283653d52e146db71
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.markdown_converter = js.markdown_converter:main
    #
    # Would create an entry point called js.markdown_converter that calls js.markdown_converter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic >=0.16

  run:
    - python
    - setuptools
    - fanstatic >=0.16

test:
  # Python imports
  imports:
    - js
    - js.markdown_converter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.merchise.org
  license: GPL 3+
  summary: u'Fanstatic resource definition for Markdown.Converter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
