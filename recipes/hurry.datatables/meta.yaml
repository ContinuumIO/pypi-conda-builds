package:
  name: hurry.datatables
  version: "1.7.6"

source:
  fn: hurry.datatables-1.7.6.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.datatables/hurry.datatables-1.7.6.tar.gz
  md5: fe8ddf10adfeed7a6de3a587281d3cd1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.datatables = hurry.datatables:main
    #
    # Would create an entry point called hurry.datatables that calls hurry.datatables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10
    - hurry.jquery

  run:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10
    - hurry.jquery

test:
  # Python imports
  imports:
    - hurry
    - hurry.datatables

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'hurry.resource style resources for jQuery datatables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
