package:
  name: bandwidthmonitor
  version: "1.0.0"

source:
  fn: bandwidthMonitor-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bandwidthMonitor/bandwidthMonitor-1.0.0.tar.gz
  md5: a3499a98e4ece708f5295b10d98f136c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bandwidthmonitor = bandwidthmonitor:main
    #
    # Would create an entry point called bandwidthmonitor that calls bandwidthmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bandwidthmonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.cardiffgiant.org
  license: UNKNOWN
  summary: 'A collection of methods to interface with a rrd file containing data on bandwidth usage via /proc/net/dev.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
