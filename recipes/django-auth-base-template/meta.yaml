package:
  name: django-auth-base-template
  version: "1.0.0"

source:
  fn: django-auth-base-template-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-base-template/django-auth-base-template-1.0.0.tar.gz
  md5: 5446fd398c246a92246a788de5b4b28c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-base-template = django-auth-base-template:main
    #
    # Would create an entry point called django-auth-base-template that calls django-auth-base-template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - auth_base_template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dabapps/django-auth-base-template/
  license: BSD
  summary: 'Switchable logged in/logged out base templates for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
