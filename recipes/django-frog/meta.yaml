package:
  name: django-frog
  version: "1.0.8"

source:
  fn: django-frog-1.0.8.zip
  url: https://pypi.python.org/packages/source/d/django-frog/django-frog-1.0.8.zip
  md5: fc37e2cda0436011b4ee3f8faf519272
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-frog = django-frog:main
    #
    # Would create an entry point called django-frog that calls django-frog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.7
    - django-contrib-comments ==1.5
    - django-haystack ==2.3.1
    - mysqlclient ==1.3.5
    - path.py ==7.3
    - pillow ==2.8.1
    - pytz ==2015.2
    - six ==1.9.0
    - urllib3 ==1.10.2

  run:
    - python
    - django ==1.7
    - django-contrib-comments ==1.5
    - django-haystack ==2.3.1
    - mysqlclient ==1.3.5
    - path.py ==7.3
    - pillow ==2.8.1
    - pytz ==2015.2
    - six ==1.9.0
    - urllib3 ==1.10.2

test:
  # Python imports
  imports:
    - frog
    - frog.management
    - frog.management.commands
    - frog.templatetags
    - frog.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theiviaxx/frog
  license: MIT License
  summary: 'Media server built on django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
