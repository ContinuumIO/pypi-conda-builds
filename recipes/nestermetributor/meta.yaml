package:
  name: nestermetributor
  version: "1.0.0"

source:
  fn: NesterMetributor-1.0.0.zip
  url: https://pypi.python.org/packages/source/N/NesterMetributor/NesterMetributor-1.0.0.zip
  md5: 3c36e8d26143daa731368461979fb875
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nestermetributor = nestermetributor:main
    #
    # Would create an entry point called nestermetributor that calls nestermetributor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nestermetributor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'A nested list printer, as taught by Head First Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
