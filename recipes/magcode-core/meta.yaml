package:
  name: magcode-core
  version: "1.4.7"

source:
  fn: magcode-core-1.4.7.tar.gz
  url: https://pypi.python.org/packages/source/m/magcode-core/magcode-core-1.4.7.tar.gz
  md5: 32286f6bf7d37d5b8bbf30788f27254a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magcode-core = magcode-core:main
    #
    # Would create an entry point called magcode-core that calls magcode-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - magcode
    - magcode.core
    - magcode.core.database
    - magcode.core.wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'MAG Code Core utility module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
