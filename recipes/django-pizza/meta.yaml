package:
  name: django-pizza
  version: "14.08.1"

source:
  fn: Django-Pizza-14.08.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Pizza/Django-Pizza-14.08.1.tar.gz
  md5: 1a95b96328f26dfab62925457c1100bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pizza = django-pizza:main
    #
    # Would create an entry point called django-pizza that calls django-pizza.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sorl-thumbnail >=11.01
    - django-next-please >=13.12

  run:
    - python
    - sorl-thumbnail >=11.01
    - django-next-please >=13.12

test:
  # Python imports
  imports:
    - pizza
    - pizza.blog
    - pizza.blog.migrations
    - pizza.blog.templatetags
    - pizza.calendar
    - pizza.calendar.migrations
    - pizza.kitchen_sink
    - pizza.kitchen_sink.management
    - pizza.kitchen_sink.management.commands
    - pizza.kitchen_sink.migrations
    - pizza.kitchen_sink.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pizzapanther/Django-Pizza
  license: BSD License
  summary: 'Yet another Django CMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
