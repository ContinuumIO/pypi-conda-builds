package:
  name: python-memcached
  version: "1.54"

source:
  fn: python-memcached-1.54.tar.gz
  url: https://pypi.python.org/packages/source/p/python-memcached/python-memcached-1.54.tar.gz
  md5: 24b9a2a787edd4b5f82e23d4d827bd9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-memcached = python-memcached:main
    #
    # Would create an entry point called python-memcached that calls python-memcached.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.tummy.com/Community/software/python-memcached/
  license: Python Software Foundation License
  summary: 'Pure python memcached client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
