package:
  name: django-pureftpd-admin
  version: "0.1.3a2"

source:
  fn: django-pureftpd-admin-0.1.3a2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pureftpd-admin/django-pureftpd-admin-0.1.3a2.tar.gz
  md5: ae973d8ed29d7e81cfc9cf6c732f9e41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pureftpd-admin = django-pureftpd-admin:main
    #
    # Would create an entry point called django-pureftpd-admin that calls django-pureftpd-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - mysql-python

  run:
    - python
    - django
    - mysql-python

test:
  # Python imports
  imports:
    - pureftpd_admin
    - pureftpd_admin.localdir
    - pureftpd_admin.management
    - pureftpd_admin.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://ivanff@bitbucket.org/ivanff/django-pureftpd-admin
  license: UNKNOWN
  summary: 'This is the web base administration interface for pureftpd server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
