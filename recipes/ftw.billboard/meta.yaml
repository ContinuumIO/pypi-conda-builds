package:
  name: ftw.billboard
  version: "1.4.1"

source:
  fn: ftw.billboard-1.4.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.billboard/ftw.billboard-1.4.1.zip
  md5: dc8274e6acd19edd9eae5a3f103af51a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.billboard = ftw.billboard:main
    #
    # Would create an entry point called ftw.billboard that calls ftw.billboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ftw.calendarwidget
    - ftw.colorbox
    - ftw.profilehook
    - ftw.upgrade
    - plone.api
    - plone.app.registry
    - setuptools

  run:
    - python
    - setuptools
    - ftw.calendarwidget
    - ftw.colorbox
    - ftw.profilehook
    - ftw.upgrade
    - plone.api
    - plone.app.registry
    - setuptools

test:
  # Python imports
  imports:
    - ftw
    - ftw.billboard
    - ftw.billboard.browser
    - ftw.billboard.content
    - ftw.billboard.portlets
    - ftw.billboard.tests
    - ftw.billboard.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.billboard
  license: GPL2
  summary: 'Simple market place addon for plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
