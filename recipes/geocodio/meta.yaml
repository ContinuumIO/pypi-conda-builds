package:
  name: geocodio
  version: "1.0.1"

source:
  fn: geocodio-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/geocodio/geocodio-1.0.1.tar.gz
  md5: 137053453434bb66b020c5b3accd01c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geocodio = geocodio:main
    #
    # Would create an entry point called geocodio that calls geocodio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.0

  run:
    - python
    - requests >=2.2.0

test:
  # Python imports
  imports:
    - geocodio
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidstanley01/geocodio.py
  license: BSD License
  summary: 'Simple wrapper for geocod.io address service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
