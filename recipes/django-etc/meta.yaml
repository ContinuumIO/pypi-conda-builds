package:
  name: django-etc
  version: "0.7.0"

source:
  fn: django-etc-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-etc/django-etc-0.7.0.tar.gz
  md5: 3c4d48928a68c53f711b5570e0b279a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-etc = django-etc:main
    #
    # Would create an entry point called django-etc that calls django-etc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - etc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/idlesign/django-etc
  license: BSD License
  summary: "Tiny stuff for Django that won't fit into separate apps."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
