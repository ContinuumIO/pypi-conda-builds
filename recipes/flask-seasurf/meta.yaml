package:
  name: flask-seasurf
  version: "0.2.0"

source:
  fn: Flask-SeaSurf-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SeaSurf/Flask-SeaSurf-0.2.0.tar.gz
  md5: 4489553163d939b8d53b9a4e61207fe8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-seasurf = flask-seasurf:main
    #
    # Would create an entry point called flask-seasurf that calls flask-seasurf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxcountryman/flask-seasurf/
  license: BSD License
  summary: 'An updated CSRF extension for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
