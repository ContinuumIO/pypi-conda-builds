package:
  name: literoticapi
  version: "0.1.1"

source:
  fn: LiteroticAPI-0.1.1.zip
  url: https://pypi.python.org/packages/source/L/LiteroticAPI/LiteroticAPI-0.1.1.zip
  md5: ad2f401bda6ac5c13297035f617c977c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - literoticapi = literoticapi:main
    #
    # Would create an entry point called literoticapi that calls literoticapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup <4
    - requests >0.11.1
    - gevent

  run:
    - python
    - beautifulsoup <4
    - requests >0.11.1
    - gevent

test:
  # Python imports
  imports:
    - literoticapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hrroon.github.com/LiteroticAPI
  license: LGPL
  summary: 'an API for Literotica'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
