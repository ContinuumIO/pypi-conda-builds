package:
  name: gocept.recipe.deploymentsandbox
  version: "0.2"

source:
  fn: gocept.recipe.deploymentsandbox-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.recipe.deploymentsandbox/gocept.recipe.deploymentsandbox-0.2.tar.gz
  md5: eb473273ffadebf07d6439f3c0182528
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.recipe.deploymentsandbox = gocept.recipe.deploymentsandbox:main
    #
    # Would create an entry point called gocept.recipe.deploymentsandbox that calls gocept.recipe.deploymentsandbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - gocept
    - gocept.recipe
    - gocept.recipe.deploymentsandbox
    - gocept.recipe.deploymentsandbox.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.recipe.deploymentsandbox
  license: Zope Public License
  summary: 'Setup a sandbox environment for deployments based on zc.recipe.deployment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
