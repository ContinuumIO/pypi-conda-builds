package:
  name: djangocms-timer
  version: "0.2.0"

source:
  fn: djangocms-timer-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-timer/djangocms-timer-0.2.0.tar.gz
  md5: 247381cba352d036a17ffc4674176496
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-timer = djangocms-timer:main
    #
    # Would create an entry point called djangocms-timer that calls djangocms-timer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0

  run:
    - python
    - django-cms >=3.0

test:
  # Python imports
  imports:
    - djangocms_timer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/djangocms-timer
  license: BSD License
  summary: 'django CMS plugin that shows content between specified times'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
