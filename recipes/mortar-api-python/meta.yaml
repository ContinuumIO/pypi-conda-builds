package:
  name: mortar-api-python
  version: "0.2.4"

source:
  fn: mortar-api-python-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mortar-api-python/mortar-api-python-0.2.4.tar.gz
  md5: 7a148107ba1e7ef4d53479ed88af88c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mortar-api-python = mortar-api-python:main
    #
    # Would create an entry point called mortar-api-python that calls mortar-api-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - mortar.api
    - mortar.api.v2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mortardata/mortar-api-python
  license: LICENSE.txt
  summary: 'Python API for Mortar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
