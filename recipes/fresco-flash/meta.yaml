package:
  name: fresco-flash
  version: "0.1"

source:
  fn: fresco-flash-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fresco-flash/fresco-flash-0.1.tar.gz
  md5: 5e46010d7cf4e4ddded5149df04f056f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fresco-flash = fresco-flash:main
    #
    # Would create an entry point called fresco-flash that calls fresco-flash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fresco >=0.3.0dev
    - markupsafe

  run:
    - python
    - fresco >=0.3.0dev
    - markupsafe

test:
  # Python imports
  imports:
    - fresco_flash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Apache
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
