package:
  name: adminish
  version: "0.1.3"

source:
  fn: adminish-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/adminish/adminish-0.1.3.tar.gz
  md5: ddc6f01cf65a480012d687ccf61b45bd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adminish = adminish:main
    #
    # Would create an entry point called adminish that calls adminish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - restish
    - formish
    - adminish-categories
    - couchish
    - couchfti
    - couchutil
    - xappy
    - wsgiapptools
    - pagingish
    - breve
    - pastescript
    - weberror
    - repoze.who
    - menuish
    - notification

  run:
    - python
    - setuptools
    - restish
    - formish
    - adminish-categories
    - couchish
    - couchfti
    - couchutil
    - xappy
    - wsgiapptools
    - pagingish
    - breve
    - pastescript
    - weberror
    - repoze.who
    - menuish
    - notification

test:
  # Python imports
  imports:
    - adminish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ish.io
  license: UNKNOWN
  summary: 'Auto web admin system for couchdb and restish'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
