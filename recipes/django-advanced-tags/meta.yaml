package:
  name: django-advanced-tags
  version: "1.0.0"

source:
  fn: django-advanced-tags-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-advanced-tags/django-advanced-tags-1.0.0.tar.gz
  md5: 4ff5b85930aac381b2bccab0954be797
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-advanced-tags = django-advanced-tags:main
    #
    # Would create an entry point called django-advanced-tags that calls django-advanced-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - advanced_tags
    - advanced_tags.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/buffagon/django-advanced-tags
  license: GNU LGPL
  summary: 'Advanced template tags for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
