package:
  name: clusterjob
  version: "1.1.3"

source:
  fn: clusterjob-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/clusterjob/clusterjob-1.1.3.tar.gz
  md5: 32037833e1ca62da5b301f7c4d1dba1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clusterjob = clusterjob:main
    #
    # Would create an entry point called clusterjob that calls clusterjob.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - clusterjob
    - clusterjob.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goerz/clusterjob
  license: MIT License
  summary: 'Manage traditional HPC cluster workflows in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
