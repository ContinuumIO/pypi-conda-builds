package:
  name: foursquare.pants.spindle
  version: "0.0.1"

source:
  fn: foursquare.pants.spindle-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/foursquare.pants.spindle/foursquare.pants.spindle-0.0.1.tar.gz
  md5: 98a6e068da9c8b170b3357e92304548c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foursquare.pants.spindle = foursquare.pants.spindle:main
    #
    # Would create an entry point called foursquare.pants.spindle that calls foursquare.pants.spindle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - foursquare.pants.spindle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/foursquare/pants-spindle
  license: Apache Software License
  summary: 'Spindle Thrift-to-Scala code generation plugin for pants build system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
