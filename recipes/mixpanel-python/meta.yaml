package:
  name: mixpanel-python
  version: "0.0.2"

source:
  fn: mixpanel-python-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mixpanel-python/mixpanel-python-0.0.2.tar.gz
  md5: 483b0ef4326e2ed7da5fcd8b18986e17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixpanel-python = mixpanel-python:main
    #
    # Would create an entry point called mixpanel-python that calls mixpanel-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mixpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kortina/mixpanel-python
  license: MIT
  summary: 'Minimal Mixpanel API Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
