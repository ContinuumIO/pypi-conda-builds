package:
  name: django-silk
  version: "0.5.2"

source:
  fn: django-silk-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-silk/django-silk-0.5.2.tar.gz
  md5: 7c8951b4a428f601704ef7fe4602a908
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-silk = django-silk:main
    #
    # Would create an entry point called django-silk that calls django-silk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pygments
    - six
    - simplejson
    - python-dateutil
    - requests
    - sqlparse
    - jinja2
    - autopep8
    - pytz

  run:
    - python
    - django
    - pygments
    - six
    - simplejson
    - python-dateutil
    - requests
    - sqlparse
    - jinja2
    - autopep8
    - pytz

test:
  # Python imports
  imports:
    - silk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.mtford.co.uk/projects/silk/
  license: MIT License
  summary: 'Silky smooth profiling for the Django Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
