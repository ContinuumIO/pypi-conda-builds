package:
  name: metlog-cef
  version: "0.2"

source:
  fn: metlog-cef-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/metlog-cef/metlog-cef-0.2.tar.gz
  md5: 8cf732261297191b129e2875f1c7f182
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metlog-cef = metlog-cef:main
    #
    # Would create an entry point called metlog-cef that calls metlog-cef.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - metlog-py
    - cef

  run:
    - python
    - metlog-py
    - cef

test:
  # Python imports
  imports:
    - metlog_cef
    - metlog_cef.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla-services/metlog-cef
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'CEF extensions to metrics logging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
