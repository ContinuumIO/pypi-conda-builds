package:
  name: britney
  version: "0.4.0"

source:
  fn: britney-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/b/britney/britney-0.4.0.tar.gz
  md5: 4f918b904e95854ad60d7b106b0f4271
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - britney = britney:main
    #
    # Would create an entry point called britney that calls britney.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - requests-testadapter

  run:
    - python
    - requests
    - requests-testadapter

test:
  # Python imports
  imports:
    - britney
    - britney.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agrausem/britney
  license: BSD License
  summary: 'Python implementation of SPORE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
