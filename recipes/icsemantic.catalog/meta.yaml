package:
  name: icsemantic.catalog
  version: "1.0-dev-r371"

source:
  fn: icsemantic.catalog-1.0-dev-r371.tar.gz
  url: https://pypi.python.org/packages/source/i/icsemantic.catalog/icsemantic.catalog-1.0-dev-r371.tar.gz
  md5: 412e94c4a85f2a1eb35263bfbc7bfcab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icsemantic.catalog = icsemantic.catalog:main
    #
    # Would create an entry point called icsemantic.catalog that calls icsemantic.catalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - icsemantic
    - icsemantic.catalog
    - icsemantic.catalog.Extensions
    - icsemantic.catalog.browser
    - icsemantic.catalog.criteria
    - icsemantic.catalog.indexes
    - icsemantic.catalog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.platecom.com/productos/icsemantic.catalog/
  license: GNU General Public License (GPL)
  summary: 'icSemantic.Catalog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
