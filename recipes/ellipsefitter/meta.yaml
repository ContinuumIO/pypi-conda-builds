package:
  name: ellipsefitter
  version: "0.1.2.1"

source:
  fn: EllipseFitter-0.1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/E/EllipseFitter/EllipseFitter-0.1.2.1.tar.gz
  md5: 8bc65103915f19ae5630d49a76fd1eaf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ellipsefitter = ellipsefitter:main
    #
    # Would create an entry point called ellipsefitter that calls ellipsefitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.0

  run:
    - python
    - numpy >=1.0

test:
  # Python imports
  imports:
    - EllipseFitter
    - ellipsefitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/EllipseFitter/
  license: LICENSE.txt
  summary: 'find best fit ellipses of binary images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
