package:
  name: korg
  version: "0.0.3"

source:
  fn: korg-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/k/korg/korg-0.0.3.tar.gz
  md5: bf1d329ef0745ddcf32c2513133baa60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - korg = korg:main
    #
    # Would create an entry point called korg that calls korg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - regex >=2013-06-05

  run:
    - python
    - regex >=2013-06-05

test:
  # Python imports
  imports:
    - korg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aogaeru/korg
  license: MIT License
  summary: 'Fast logfile parsing. This is a port of Ruby logstash / grok to Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
