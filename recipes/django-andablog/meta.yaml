package:
  name: django-andablog
  version: "1.3.0"

source:
  fn: django-andablog-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-andablog/django-andablog-1.3.0.tar.gz
  md5: e0aa22df1cb36b0c98f2f33db80ad9ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-andablog = django-andablog:main
    #
    # Would create an entry point called django-andablog that calls django-andablog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - django >=1.6,<1.8
    - django-model-utils >=2.2
    - django-markitup >=2.2.2
    - django-taggit >=0.12.3
    - pillow >=2.6.1

  run:
    - python
    - six
    - django >=1.6,<1.8
    - django-model-utils >=2.2
    - django-markitup >=2.2.2
    - django-taggit >=0.12.3
    - pillow >=2.6.1

test:
  # Python imports
  imports:
    - andablog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WimpyAnalytics/django-andablog
  license: BSD License
  summary: 'A blog app that is only intended to be embedded within an existing Django site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
