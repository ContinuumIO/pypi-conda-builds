package:
  name: djangotribune
  version: "0.7.7"

source:
  fn: djangotribune-0.7.7.tar.gz
  url: https://pypi.python.org/packages/source/d/djangotribune/djangotribune-0.7.7.tar.gz
  md5: 314797219f6b1fb9172b00a880f89447
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangotribune = djangotribune:main
    #
    # Would create an entry point called djangotribune that calls djangotribune.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - texttable ==0.8.1
    - crispy-forms-foundation >=0.2.3.1
    - pytz

  run:
    - python
    - texttable ==0.8.1
    - crispy-forms-foundation >=0.2.3.1
    - pytz

test:
  # Python imports
  imports:
    - djangotribune
    - djangotribune.management
    - djangotribune.management.commands
    - djangotribune.migrations
    - djangotribune.templatetags
    - djangotribune.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/djangotribune
  license: MIT License
  summary: '\nDjango-tribune is a chat-like application\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
