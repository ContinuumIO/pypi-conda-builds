package:
  name: autoversion
  version: "1.1.1"

source:
  fn: autoversion-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/autoversion/autoversion-1.1.1.tar.gz
  md5: e80a1ec989b1a47c9466a4e2ac9bb74f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autoversion = autoversion:main
    #
    # Would create an entry point called autoversion that calls autoversion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - autoversion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pwaller/__autoversion__
  license: MIT License
  summary: 'Obtain installed version of package automatically, even if it is installed using editable or develop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
