package:
  name: baldr
  version: "0.4.5"

source:
  fn: baldr-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/b/baldr/baldr-0.4.5.tar.gz
  md5: 50dfe26b746e68db5f753ee9409a3efe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baldr = baldr:main
    #
    # Would create an entry point called baldr that calls baldr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - odin >=0.5
    - django >=1.5

  run:
    - python
    - six
    - odin >=0.5
    - django >=1.5

test:
  # Python imports
  imports:
    - baldr
    - baldr.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timsavage/baldr
  license: BSD License
  summary: 'Odin integration to Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
