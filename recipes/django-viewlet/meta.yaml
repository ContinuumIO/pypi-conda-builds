package:
  name: django-viewlet
  version: "1.4"

source:
  fn: django-viewlet-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewlet/django-viewlet-1.4.tar.gz
  md5: a988a988eaaedf0d1545d51a61a54b8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewlet = django-viewlet:main
    #
    # Would create an entry point called django-viewlet that calls django-viewlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - viewlet
    - viewlet.loaders
    - viewlet.templatetags
    - viewlet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/5monkeys/django-viewlet
  license: MIT License
  summary: 'Render template parts with extended cache control.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
