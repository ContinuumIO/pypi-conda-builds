package:
  name: silva.pageactions.pdf
  version: "1.0.1"

source:
  fn: silva.pageactions.pdf-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.pageactions.pdf/silva.pageactions.pdf-1.0.1.tar.gz
  md5: dd28778cc7c1053e26ef6e7b08f30b56
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.pageactions.pdf = silva.pageactions.pdf:main
    #
    # Would create an entry point called silva.pageactions.pdf that calls silva.pageactions.pdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - megrok.chameleon
    - silva.core.views
    - silva.pageactions.base
    - silva.pageactions.printfriendly

  run:
    - python
    - setuptools
    - five.grok
    - megrok.chameleon
    - silva.core.views
    - silva.pageactions.base
    - silva.pageactions.printfriendly

test:
  # Python imports
  imports:
    - silva
    - silva.pageactions
    - silva.pageactions.pdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.pageactions.pdf
  license: BSD
  summary: 'Create a PDF out of the current page in Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
