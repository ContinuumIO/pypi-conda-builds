package:
  name: redis-queue-pyclj
  version: "0.2.1"

source:
  fn: redis-queue-pyclj-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-queue-pyclj/redis-queue-pyclj-0.2.1.tar.gz
  md5: 7e4659f54c92cf8e00bda193253ce68c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-queue-pyclj = redis-queue-pyclj:main
    #
    # Would create an entry point called redis-queue-pyclj that calls redis-queue-pyclj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.9.0

  run:
    - python
    - redis >=2.9.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/boechat107/redis-queue
  license: Eclipse
  summary: 'Some fault tolerance over Redis lists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
