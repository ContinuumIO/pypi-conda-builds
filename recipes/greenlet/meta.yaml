package:
  name: greenlet
  version: "0.4.7"

source:
  fn: greenlet-0.4.7.zip
  url: https://pypi.python.org/packages/source/g/greenlet/greenlet-0.4.7.zip
  md5: c2333a8ff30fa75c5d5ec0e67b461086
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - greenlet = greenlet:main
    #
    # Would create an entry point called greenlet that calls greenlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - greenlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/python-greenlet/greenlet
  license: MIT License
  summary: 'Lightweight in-process concurrent programming'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
