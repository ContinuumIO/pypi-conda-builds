package:
  name: django-registration-bootstrap
  version: "0.2.0"

source:
  fn: django-registration-bootstrap-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-bootstrap/django-registration-bootstrap-0.2.0.tar.gz
  md5: 7f2f44c424c115b8767b113ebe942632
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-bootstrap = django-registration-bootstrap:main
    #
    # Would create an entry point called django-registration-bootstrap that calls django-registration-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0
    - django-crispy-forms >=1.3.2

  run:
    - python
    - django >=1.5.0
    - django-crispy-forms >=1.3.2

test:
  # Python imports
  imports:
    - registration_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pydanny/django-registration-bootstrap
  license: BSD License
  summary: 'Bootstrap implementation for Django registration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
