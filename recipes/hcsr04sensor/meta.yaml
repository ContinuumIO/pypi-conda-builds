package:
  name: hcsr04sensor
  version: "1.1.2"

source:
  fn: hcsr04sensor-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hcsr04sensor/hcsr04sensor-1.1.2.tar.gz
  md5: a9a1432c508ad2cd6856fc09633a2187
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hcsr04sensor = hcsr04sensor:main
    #
    # Would create an entry point called hcsr04sensor that calls hcsr04sensor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rpi.gpio

  run:
    - python
    - rpi.gpio

test:
  # Python imports
  imports:
    - hcsr04sensor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alaudet/hcsr04sensor
  license: MIT License
  summary: 'Module to access the HCSR04 sensor on a Raspberry Pi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
