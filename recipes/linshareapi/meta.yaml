package:
  name: linshareapi
  version: "0.2.2"

source:
  fn: linshareapi-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/l/linshareapi/linshareapi-0.2.2.tar.gz
  md5: 9cb14179da7c36f8c617ef1e2dffac8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linshareapi = linshareapi:main
    #
    # Would create an entry point called linshareapi that calls linshareapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - poster
    - progressbar

  run:
    - python
    - poster
    - progressbar

test:
  # Python imports
  imports:
    - linshareapi
    - linshareapi.admin
    - linshareapi.user

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fred49/linshare-api
  license: GNU General Public License v3 (GPLv3)
  summary: 'LinShare api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
