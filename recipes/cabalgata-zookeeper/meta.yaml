package:
  name: cabalgata-zookeeper
  version: "0.1.1"

source:
  fn: cabalgata-zookeeper-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cabalgata-zookeeper/cabalgata-zookeeper-0.1.1.tar.gz
  md5: cfdace20f745e5bd96e97e3854693b3e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cabalgata-zookeeper = cabalgata-zookeeper:main
    #
    # Would create an entry point called cabalgata-zookeeper that calls cabalgata-zookeeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.3.2
    - cabalgata-silla-de-montar ==0.1.1

  run:
    - python
    - setuptools
    - beautifulsoup4 ==4.3.2
    - cabalgata-silla-de-montar ==0.1.1

test:
  # Python imports
  imports:
    - cabalgata
    - cabalgata/zookeeper
    - cabalgata/zookeeper/util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cabalgata/cabalgata-zookeeper/
  license: Apache Software License
  summary: 'A simple wrapper for installing and starting zookeeper services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
