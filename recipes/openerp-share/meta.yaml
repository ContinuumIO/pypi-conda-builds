package:
  name: openerp-share
  version: "7.0.406"

source:
  fn: openerp-share-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-share/openerp-share-7.0.406.tar.gz
  md5: 98e31fbf1f9c7796416cf9520778effa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-share = openerp-share:main
    #
    # Would create an entry point called openerp-share that calls openerp-share.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-core
    - openerp-mail

  run:
    - python
    - openerp-core
    - openerp-mail

test:
  # Python imports
  imports:
    - openerp.addons.share
    - openerp.addons.share.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openerp.com
  license: GNU Affero General Public License v3
  summary: 'Share any Document'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
