package:
  name: django-rosetta-inpage
  version: "0.0.10"

source:
  fn: django-rosetta-inpage-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rosetta-inpage/django-rosetta-inpage-0.0.10.tar.gz
  md5: c226fd47019290c81c18f89fd3ef2d1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rosetta-inpage = django-rosetta-inpage:main
    #
    # Would create an entry point called django-rosetta-inpage that calls django-rosetta-inpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-rosetta ==0.6.8

  run:
    - python
    - django-rosetta ==0.6.8

test:
  # Python imports
  imports:
    - rosetta_inpage
    - rosetta_inpage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/citylive/django-rosetta-inpage/
  license: BSD License
  summary: 'Translate i18n messages with Django Rosetta'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
