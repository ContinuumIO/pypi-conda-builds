package:
  name: sundowner
  version: "0.1.4"

source:
  fn: Sundowner-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/S/Sundowner/Sundowner-0.1.4.tar.gz
  md5: 9e5e8c07d62fa10407174975dfd95a2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sundowner = sundowner:main
    #
    # Would create an entry point called sundowner that calls sundowner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10
    - ephem >=3.7
    - itsdangerous >=0.24
    - jinja2 >=2.7.2
    - markupsafe >=0.23
    - werkzeug >=0.9.4
    - python-dateutil >=2.2

  run:
    - python
    - flask >=0.10
    - ephem >=3.7
    - itsdangerous >=0.24
    - jinja2 >=2.7.2
    - markupsafe >=0.23
    - werkzeug >=0.9.4
    - python-dateutil >=2.2

test:
  # Python imports
  imports:
    - sundowner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
