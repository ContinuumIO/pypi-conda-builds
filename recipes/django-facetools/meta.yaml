package:
  name: django-facetools
  version: "0.2.1"

source:
  fn: django-facetools-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facetools/django-facetools-0.2.1.tar.gz
  md5: 4feb6b9a2a395e2cc341c38d4fbf4c86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facetools = django-facetools:main
    #
    # Would create an entry point called django-facetools that calls django-facetools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - facetools
    - facetools.integrations
    - facetools.management
    - facetools.management.commands
    - facetools.migrations
    - facetools.templatetags
    - facetools.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bigsassy/django-facetools
  license: MIT License
  summary: 'Django Facetools provides a set of features to ease development of Facecbook canvas apps in a Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
