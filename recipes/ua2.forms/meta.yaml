package:
  name: ua2.forms
  version: "0.4.3"

source:
  fn: ua2.forms-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/u/ua2.forms/ua2.forms-0.4.3.tar.gz
  md5: 0f5cd6198959aa908c0ae8c653a4ae68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ua2.forms = ua2.forms:main
    #
    # Would create an entry point called ua2.forms that calls ua2.forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ua2
    - ua2.forms
    - ua2.forms.templates
    - ua2.forms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ua2web/ua2.forms
  license: BSD License
  summary: 'Django forms wrappers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
