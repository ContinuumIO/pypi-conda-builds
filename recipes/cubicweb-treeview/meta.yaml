package:
  name: cubicweb-treeview
  version: "0.1.1"

source:
  fn: cubicweb-treeview-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-treeview/cubicweb-treeview-0.1.1.tar.gz
  md5: 228d433f7df05732b02ebdd84c84722f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-treeview = cubicweb-treeview:main
    #
    # Would create an entry point called cubicweb-treeview that calls cubicweb-treeview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.0

  run:
    - python
    - cubicweb >=3.16.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-treeview
  license: LGPL
  summary: 'tree-building adapters, widgets, views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
