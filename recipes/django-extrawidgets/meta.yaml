package:
  name: django-extrawidgets
  version: "1"

source:
  fn: django-extrawidgets-1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extrawidgets/django-extrawidgets-1.tar.gz
  md5: 792e6f55b0a53e545d0831e426791bc0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extrawidgets = django-extrawidgets:main
    #
    # Would create an entry point called django-extrawidgets that calls django-extrawidgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_extrawidgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/freakboy3742/django-rays/wiki/
  license: BSD License
  summary: 'A project exploring the client-side of Django website development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
