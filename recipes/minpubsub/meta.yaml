package:
  name: minpubsub
  version: "0.3"

source:
  fn: minpubsub-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/minpubsub/minpubsub-0.3.tar.gz
  md5: 3938956a873b6e41bdbe8246d4d2182d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minpubsub = minpubsub:main
    #
    # Would create an entry point called minpubsub that calls minpubsub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - minpubsub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jyotiska/minpubsub
  license: UNKNOWN
  summary: 'A minimal PubSub messaging model with multiple persistence options - SQLite, MySQL, MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
