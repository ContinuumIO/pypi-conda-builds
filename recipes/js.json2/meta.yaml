package:
  name: js.json2
  version: "2011-02-23"

source:
  fn: js.json2-2011-02-23.tar.gz
  url: https://pypi.python.org/packages/source/j/js.json2/js.json2-2011-02-23.tar.gz
  md5: f02dba4e14e7bc92bb6c9fbe27c4edce
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.json2 = js.json2:main
    #
    # Would create an entry point called js.json2 that calls js.json2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.json2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jQuery JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
