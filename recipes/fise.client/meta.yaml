package:
  name: fise.client
  version: "1.1"

source:
  fn: fise.client-1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fise.client/fise.client-1.1.tar.gz
  md5: 31d1d44d26e41b56f23e124c28fd1355
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fise.client = fise.client:main
    #
    # Would create an entry point called fise.client that calls fise.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - restkit
    - rdflib
    - surf.sparql_protocol
    - surf.rdflib

  run:
    - python
    - setuptools
    - restkit
    - rdflib
    - surf.sparql_protocol
    - surf.rdflib

test:
  # Python imports
  imports:
    - fise
    - fise.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Python Software Foundation License
  summary: 'Apache Stanbol FISE: RESTful Python Client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
