package:
  name: django-autodatetimefields
  version: "0.9.0"

source:
  fn: django-AutoDateTimeFields-0.9.0.zip
  url: https://pypi.python.org/packages/source/d/django-AutoDateTimeFields/django-AutoDateTimeFields-0.9.0.zip
  md5: e484210de362e43d46084757d9e336ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-autodatetimefields = django-autodatetimefields:main
    #
    # Would create an entry point called django-autodatetimefields that calls django-autodatetimefields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.1.1

  run:
    - python
    - django >=1.1.1

test:
  # Python imports
  imports:
    - autodatetimefields
    - autodatetimefields.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-AutoDateTimeFields/
  license: LICENSE
  summary: 'django date time fields that do not break like auto_now and auto_now_add do'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
