package:
  name: albertson
  version: "0.1.8"

source:
  fn: Albertson-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/A/Albertson/Albertson-0.1.8.tar.gz
  md5: 2c191233d6a6df18af9e1db98155b35b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - albertson = albertson:main
    #
    # Would create an entry point called albertson that calls albertson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.2.2

  run:
    - python
    - boto >=2.2.2

test:
  # Python imports
  imports:
    - albertson
    - albertson.dynamodb_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FocusLab/Albertson
  license: BSD License
  summary: "Easy to use, scalable, counters powered by Amazon's DynamoDB"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
