package:
  name: behance_python
  version: "0.2.4"

source:
  fn: behance_python-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/b/behance_python/behance_python-0.2.4.tar.gz
  md5: 01b8fac838257e3bb6bfda5ac2bc18be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - behance_python = behance_python:main
    #
    # Would create an entry point called behance_python that calls behance_python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - behance_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aravenel/behance_python
  license: MIT
  summary: 'Python wrapper for the Behance API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
