package:
  name: kirrupt-tv
  version: "0.5"

source:
  fn: kirrupt-tv-0.5.tar.gz
  url: https://pypi.python.org/packages/source/k/kirrupt-tv/kirrupt-tv-0.5.tar.gz
  md5: 36d1940354bb40e680b90c0170ac69b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kirrupt-tv = kirrupt-tv:main
    #
    # Would create an entry point called kirrupt-tv that calls kirrupt-tv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pytz
    - email

  run:
    - python
    - requests
    - pytz
    - email

test:
  # Python imports
  imports:
    - kirrupt_tv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kirrupt/kirrupt-tv-python-client
  license: MIT
  summary: 'A Python client for the Kirrupt TV API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
