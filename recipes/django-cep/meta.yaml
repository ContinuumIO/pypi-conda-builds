package:
  name: django-cep
  version: "1.0.9"

source:
  fn: django-cep-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cep/django-cep-1.0.9.tar.gz
  md5: bd62e5d6adb41381d5a71984d091ac2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cep = django-cep:main
    #
    # Would create an entry point called django-cep that calls django-cep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.0

  run:
    - python
    - django >=1.2.0

test:
  # Python imports
  imports:
    - cep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/staticdev/django-cep
  license: MIT License
  summary: 'Address fields autofill app for forms using brazilian CEP field.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
