package:
  name: pyramid_iprestrict
  version: "0.1.0"

source:
  fn: pyramid_iprestrict-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_iprestrict/pyramid_iprestrict-0.1.0.tar.gz
  md5: 340d24f37efafd5c3b0a00b876881571
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_iprestrict = pyramid_iprestrict:main
    #
    # Would create an entry point called pyramid_iprestrict that calls pyramid_iprestrict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipaddress
    - pyramid

  run:
    - python
    - ipaddress
    - pyramid

test:
  # Python imports
  imports:
    - pyramid_iprestrict
    - pyramid_iprestrict.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xica/pyramid_iprestrict
  license: MIT License
  summary: 'IP based restriction tween for pyramid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
