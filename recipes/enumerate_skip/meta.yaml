package:
  name: enumerate_skip
  version: "1.0"

source:
  fn: enumerate_skip-1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/enumerate_skip/enumerate_skip-1.0.tar.gz
  md5: b6f9beb3072b2dde35e82dd613e97517
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - enumerate_skip = enumerate_skip:main
    #
    # Would create an entry point called enumerate_skip that calls enumerate_skip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - enumerate_skip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ['http://github.com/mineo/enumerate_skip']
  license: MIT License
  summary: 'enumerate extended to support manual advancement or skipping      of the index'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
