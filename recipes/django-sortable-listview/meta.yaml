package:
  name: django-sortable-listview
  version: "0.41"

source:
  fn: django-sortable-listview-0.41.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sortable-listview/django-sortable-listview-0.41.tar.gz
  md5: 4bc237a4f924735729c80f772a15e655
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sortable-listview = django-sortable-listview:main
    #
    # Would create an entry point called django-sortable-listview that calls django-sortable-listview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - sortable_listview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aptivate/django-sortable-listview
  license: MIT License
  summary: "An extension of django's ListView that provides sorting"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
