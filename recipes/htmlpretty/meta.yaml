package:
  name: htmlpretty
  version: "0.1.0"

source:
  fn: htmlpretty-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/htmlpretty/htmlpretty-0.1.0.tar.gz
  md5: 9e62aa08cab4ba9aa39c71d29f6e5d5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmlpretty = htmlpretty:main
    #
    # Would create an entry point called htmlpretty that calls htmlpretty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup

  run:
    - python
    - beautifulsoup

test:
  # Python imports
  imports:
    - htmlpretty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RyanBalfanz/htmlpretty
  license: MIT
  summary: 'Prettify your HTML.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
