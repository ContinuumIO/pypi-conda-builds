package:
  name: eventbrite
  version: "3.0.5"

source:
  fn: eventbrite-3.0.5.tar.gz
  url: https://pypi.python.org/packages/source/e/eventbrite/eventbrite-3.0.5.tar.gz
  md5: a664ad7bed4e0248046605fe4b0a7827
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eventbrite = eventbrite:main
    #
    # Would create an entry point called eventbrite that calls eventbrite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0

  run:
    - python
    - requests >=2.0

test:
  # Python imports
  imports:
    - eventbrite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eventbrite/eventbrite-sdk-python
  license: Apache Software License
  summary: 'Official Eventbrite SDK for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
