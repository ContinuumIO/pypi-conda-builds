package:
  name: django-denorm
  version: "0.2.0"

source:
  fn: django-denorm-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-denorm/django-denorm-0.2.0.tar.gz
  md5: 4bddbb67cb6ef990c453ab71816aaaa6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-denorm = django-denorm:main
    #
    # Would create an entry point called django-denorm that calls django-denorm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - denorm
    - denorm.db
    - denorm.db.mysql
    - denorm.db.postgresql
    - denorm.db.sqlite3
    - denorm.management
    - denorm.management.commands
    - denorm.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://initcrash.github.com/django-denorm/
  license: UNKNOWN
  summary: 'Denormalization magic for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
