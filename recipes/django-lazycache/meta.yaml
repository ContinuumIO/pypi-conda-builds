package:
  name: django-lazycache
  version: "0.0.1"

source:
  fn: django-lazycache-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lazycache/django-lazycache-0.0.1.tar.gz
  md5: 15bb76470b61a703d3f7ccfd86f69143
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lazycache = django-lazycache:main
    #
    # Would create an entry point called django-lazycache that calls django-lazycache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - lazycache
    - lazymodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apn-online/django-lazycache
  license: MIT
  summary: 'Caching for Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
