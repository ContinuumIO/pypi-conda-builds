package:
  name: collective.addthis
  version: "1.3.3"

source:
  fn: collective.addthis-1.3.3.zip
  url: https://pypi.python.org/packages/source/c/collective.addthis/collective.addthis-1.3.3.zip
  md5: 706313f4c5625b8cd7c4e20a6f897a6d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.addthis = collective.addthis:main
    #
    # Would create an entry point called collective.addthis that calls collective.addthis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.addthis
    - collective.addthis.tests
    - collective.addthis.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.addthis
  license: GPL
  summary: 'AddThis addon for Plone CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
