package:
  name: django-rss-plugin
  version: "0.0.9"

source:
  fn: django-rss-plugin-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rss-plugin/django-rss-plugin-0.0.9.tar.gz
  md5: ce1e218dba9bca2162fa01dd7e9abb31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rss-plugin = django-rss-plugin:main
    #
    # Would create an entry point called django-rss-plugin that calls django-rss-plugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feedparser
    - django >=1.4
    - django-cms >=2.3

  run:
    - python
    - feedparser
    - django >=1.4
    - django-cms >=2.3

test:
  # Python imports
  imports:
    - rssplugin
    - rssplugin.migrations
    - rssplugin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zgwmike/django-rss-plugin
  license: BSD License
  summary: 'A Django CMS plugin to show a list of feeds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
