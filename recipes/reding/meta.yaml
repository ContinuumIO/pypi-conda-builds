package:
  name: reding
  version: "1.99.1"

source:
  fn: Reding-1.99.1.tar.gz
  url: https://pypi.python.org/packages/source/R/Reding/Reding-1.99.1.tar.gz
  md5: 7fc9a91deefa85918b808a8565f373eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - reding = reding:main
    #
    # Would create an entry point called reding that calls reding.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy
    - flask
    - flask-restful
    - jinja2
    - werkzeug
    - argparse
    - cov-core
    - coverage
    - distribute
    - execnet
    - pep8
    - py
    - pycrypto
    - redis
    - six
    - wsgiref
    - uwsgi

  run:
    - python
    - cherrypy
    - flask
    - flask-restful
    - jinja2
    - werkzeug
    - argparse
    - cov-core
    - coverage
    - distribute
    - execnet
    - pep8
    - py
    - pycrypto
    - redis
    - six
    - wsgiref
    - uwsgi

test:
  # Python imports
  imports:
    - reding
    - reding.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://buongiornomip.github.com/Reding/
  license: MIT License
  summary: '[![PyPi downloads](https://pypip.in/d/Reding/badge.png)](https://crate.io/packages/Reding/)\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
