package:
  name: openerp-portal-project-issue
  version: "7.0.406"

source:
  fn: openerp-portal-project-issue-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-portal-project-issue/openerp-portal-project-issue-7.0.406.tar.gz
  md5: 3a9bd29f2ae26dae68e0f3d08f1627b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-portal-project-issue = openerp-portal-project-issue:main
    #
    # Would create an entry point called openerp-portal-project-issue that calls openerp-portal-project-issue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-project-issue
    - openerp-portal

  run:
    - python
    - openerp-project-issue
    - openerp-portal

test:
  # Python imports
  imports:
    - openerp.addons.portal_project_issue
    - openerp.addons.portal_project_issue.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Portal Issue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
