package:
  name: collective.js.underscore
  version: "1.5.2"

source:
  fn: collective.js.underscore-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.underscore/collective.js.underscore-1.5.2.tar.gz
  md5: d82238348e0d429c363f5abf99a58394
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.underscore = collective.js.underscore:main
    #
    # Would create an entry point called collective.js.underscore that calls collective.js.underscore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.underscore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.underscore
  license: MIT License
  summary: 'underscore.js for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
