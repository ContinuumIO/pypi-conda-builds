package:
  name: chakert
  version: "0.1.1"

source:
  fn: chakert-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chakert/chakert-0.1.1.tar.gz
  md5: aac9ad4710340d29ae40e06d572a7a73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chakert = chakert:main
    #
    # Would create an entry point called chakert that calls chakert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chakert
    - chakert.langs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Harut/chakert/
  license: MIT
  summary: 'Typographer for lxml and plain text. Supports English and Russian languages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
