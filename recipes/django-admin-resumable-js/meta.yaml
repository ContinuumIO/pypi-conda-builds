package:
  name: django-admin-resumable-js
  version: "1.0.post2"

source:
  fn: django-admin-resumable-js-1.0.post2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-resumable-js/django-admin-resumable-js-1.0.post2.tar.gz
  md5: cfa03c29fe1de3291a0b6592bc2b40f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-resumable-js = django-admin-resumable-js:main
    #
    # Would create an entry point called django-admin-resumable-js that calls django-admin-resumable-js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_resumable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jonatron/django-admin-resumable-js
  license: MIT License
  summary: 'A Django app for the uploading of large files from the django admin site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
