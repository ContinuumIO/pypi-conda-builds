package:
  name: flint-mccabe
  version: "0.2"

source:
  fn: flint-mccabe-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flint-mccabe/flint-mccabe-0.2.tar.gz
  md5: d74950833c5a8acc62af4dadd4985e9f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flint-mccabe = flint-mccabe:main
    #
    # Would create an entry point called flint-mccabe that calls flint-mccabe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mccabe

  run:
    - python
    - setuptools
    - setuptools
    - mccabe

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/flintwork/flint-mccabe
  license: MIT License
  summary: 'McCabe checker, plugin for flint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
