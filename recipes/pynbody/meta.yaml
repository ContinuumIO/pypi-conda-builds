package:
  name: pynbody
  version: "0.20"

source:
  fn: pynbody-pynbody-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pynbody/pynbody-pynbody-0.2.tar.gz
  md5: b46bcfdd2f2e3189a691aa3af606e9bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynbody = pynbody:main
    #
    # Would create an entry point called pynbody that calls pynbody.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.5

  run:
    - python
    - numpy >=1.5

test:
  # Python imports
  imports:
    - pynbody
    - pynbody/analysis
    - pynbody/bc_modules
    - pynbody/chunk
    - pynbody/gravity
    - pynbody/plot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.google.com/p/pynbody/downloads/list
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Light-weight astronomical N-body/SPH analysis for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
