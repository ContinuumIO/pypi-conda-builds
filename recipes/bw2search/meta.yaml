package:
  name: bw2search
  version: "0.2"

source:
  fn: bw2search-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bw2search/bw2search-0.2.tar.gz
  md5: 8fabce4a6f4a24ca4b797ca2a2cbe127
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bw2search = bw2search:main
    #
    # Would create an entry point called bw2search that calls bw2search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bw2data
    - whoosh

  run:
    - python
    - bw2data
    - whoosh

test:
  # Python imports
  imports:
    - bw2search

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cmutel/brightway2-search
  license: BSD License
  summary: 'Search functionality for the Brightway2 LCA Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
