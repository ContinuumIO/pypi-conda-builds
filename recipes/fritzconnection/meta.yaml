package:
  name: fritzconnection
  version: "0.4.6"

source:
  fn: fritzconnection-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/f/fritzconnection/fritzconnection-0.4.6.tar.gz
  md5: e4c27941fc4a015d27f29351c0b5c307
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fritzconnection = fritzconnection:main
    #
    # Would create an entry point called fritzconnection that calls fritzconnection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.2.5
    - requests >=2.2.0

  run:
    - python
    - lxml >=3.2.5
    - requests >=2.2.0

test:
  # Python imports
  imports:
    - fritzconnection

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kbr/fritzconnection
  license: MIT License
  summary: 'Communicate with the AVM FritzBox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
