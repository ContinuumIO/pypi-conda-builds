package:
  name: goristock
  version: "0.5"

source:
  fn: goristock-0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/goristock/goristock-0.5.tar.gz
  md5: 5562303ae61d54f00fec03fd89d46c3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goristock = goristock:main
    #
    # Would create an entry point called goristock that calls goristock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached

  run:
    - python
    - python-memcached

test:
  # Python imports
  imports:
    - goristock
    - grs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toomore/goristock
  license: MIT License
  summary: '\xe5\x8f\xb0\xe7\x81\xa3\xe8\x82\xa1\xe5\xb8\x82\xe5\x88\x86\xe6\x9e\x90\xe6\xa9\x9f\xe5\x99\xa8\xe4\xba\xba'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
