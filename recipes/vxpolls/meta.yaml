package:
  name: vxpolls
  version: "0.1.0"

source:
  fn: vxpolls-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/v/vxpolls/vxpolls-0.1.0.tar.gz
  md5: 6e0539f368111778bd90291e6e57ac77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vxpolls = vxpolls:main
    #
    # Would create an entry point called vxpolls that calls vxpolls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vumi

  run:
    - python
    - vumi

test:
  # Python imports
  imports:
    - tests
    - vxpolls
    - vxpolls.content
    - vxpolls.djdashboard
    - vxpolls.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/vxpolls
  license: BSD License
  summary: 'Simple polling / survey framework for Vumi apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
