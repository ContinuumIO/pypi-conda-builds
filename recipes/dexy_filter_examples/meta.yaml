package:
  name: dexy_filter_examples
  version: "0.0.4"

source:
  fn: dexy_filter_examples-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dexy_filter_examples/dexy_filter_examples-0.0.4.tar.gz
  md5: d46ad608f12bc28a376c1423c16a8f65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dexy_filter_examples = dexy_filter_examples:main
    #
    # Would create an entry point called dexy_filter_examples that calls dexy_filter_examples.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dexy

  run:
    - python
    - dexy

test:
  # Python imports
  imports:
    - dexy_filter_examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dexy.it
  license: MIT License
  summary: 'Filter example templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
