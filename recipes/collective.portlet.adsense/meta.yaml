package:
  name: collective.portlet.adsense
  version: "0.5"

source:
  fn: collective.portlet.adsense-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.adsense/collective.portlet.adsense-0.5.tar.gz
  md5: e0bec607daad5ac53d6730137a92905c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.adsense = collective.portlet.adsense:main
    #
    # Would create an entry point called collective.portlet.adsense that calls collective.portlet.adsense.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - plone.portlets

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - plone.portlets

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.adsense
    - collective.portlet.adsense.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective-portlet-adsense
  license: GPL
  summary: 'A portlet for google adsense'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
