package:
  name: classtricks
  version: "0.0.1421586552"

source:
  fn: classtricks-0.0.1421586552.tar.gz
  url: https://pypi.python.org/packages/source/c/classtricks/classtricks-0.0.1421586552.tar.gz
  md5: 13e19c5992b75ca5b0e94898880057a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - classtricks = classtricks:main
    #
    # Would create an entry point called classtricks that calls classtricks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - classtricks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A collection of simple tricks and helper functions for working with classes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
