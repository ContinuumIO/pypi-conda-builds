package:
  name: uimap
  version: "1.0"

source:
  fn: uimap-1.0.tar.gz
  url: https://pypi.python.org/packages/source/u/uimap/uimap-1.0.tar.gz
  md5: da75243f05875c42e18b0f88d7da24e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uimap = uimap:main
    #
    # Would create an entry point called uimap that calls uimap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - uimap
    - uimap.drivers
    - uimap.drivers.ldtp
    - uimap.drivers.wpf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/sigs/uimap/
  license: MIT
  summary: 'Cross-platform UI Automation Virtualization'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
