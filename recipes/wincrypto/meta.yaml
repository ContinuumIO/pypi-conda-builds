package:
  name: wincrypto
  version: "0.2b"

source:
  fn: wincrypto-0.2b.zip
  url: https://pypi.python.org/packages/source/w/wincrypto/wincrypto-0.2b.zip
  md5: 257bdee0782108a4adba320576dcfde2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wincrypto = wincrypto:main
    #
    # Would create an entry point called wincrypto that calls wincrypto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto

  run:
    - python
    - pycrypto

test:
  # Python imports
  imports:
    - wincrypto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/crappycrypto/wincrypto
  license: MIT License
  summary: 'Windows Crypto API compatible decryption/encryption'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
