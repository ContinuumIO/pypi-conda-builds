package:
  name: django-mootools-behavior
  version: "0.1.1"

source:
  fn: django-mootools-behavior-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mootools-behavior/django-mootools-behavior-0.1.1.tar.gz
  md5: 0a6d0a480d67b2ea3e5d052a8a1e13b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mootools-behavior = django-mootools-behavior:main
    #
    # Would create an entry point called django-mootools-behavior that calls django-mootools-behavior.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-widget-tweaks >=0.3

  run:
    - python
    - django-widget-tweaks >=0.3

test:
  # Python imports
  imports:
    - mootools_behavior
    - mootools_behavior.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-mootools-behavior/
  license: MIT License
  summary: 'Utilities for https://github.com/anutron/behavior integration with django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
