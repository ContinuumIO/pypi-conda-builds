package:
  name: gc-prometheus
  version: "0.1.0"

source:
  fn: gc-prometheus-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gc-prometheus/gc-prometheus-0.1.0.tar.gz
  md5: 8e9134deb758aff65501b069e38f5c55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gc-prometheus = gc-prometheus:main
    #
    # Would create an entry point called gc-prometheus that calls gc-prometheus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - prometheus_client >=0.0.9

  run:
    - python
    - prometheus_client >=0.0.9

test:
  # Python imports
  imports:
    - gc_prometheus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/korfuri/python-gc-prometheus
  license: Apache Software License
  summary: 'Exports garbage collection metrics for Prometheus.io.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
