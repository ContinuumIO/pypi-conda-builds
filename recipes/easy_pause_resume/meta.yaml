package:
  name: easy_pause_resume
  version: "0.2.0"

source:
  fn: easy_pause_resume-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/easy_pause_resume/easy_pause_resume-0.2.0.tar.gz
  md5: 8a4e6545ae653a2842c50926fa41fc2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy_pause_resume = easy_pause_resume:main
    #
    # Would create an entry point called easy_pause_resume that calls easy_pause_resume.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - easy_pause_resume

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ichuan/easy_pause_resume
  license: UNKNOWN
  summary: 'Easy pause & resume a python program by sending signals.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
