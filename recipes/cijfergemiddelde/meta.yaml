package:
  name: cijfergemiddelde
  version: "1.1.0"

source:
  fn: cijfergemiddelde-1.1.0.zip
  url: https://pypi.python.org/packages/source/c/cijfergemiddelde/cijfergemiddelde-1.1.0.zip
  md5: 814582aac4dcc769bfe6d6d5687b7910
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cijfergemiddelde = cijfergemiddelde:main
    #
    # Would create an entry point called cijfergemiddelde that calls cijfergemiddelde.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cijfergemiddelde

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: informatica.mijncalandlyceum.nl
  license: UNKNOWN
  summary: 'calculating results with weights'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
