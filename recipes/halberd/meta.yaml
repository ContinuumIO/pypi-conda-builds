package:
  name: halberd
  version: "0.2.4"

source:
  fn: halberd-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/h/halberd/halberd-0.2.4.tar.gz
  md5: d60d0fba8f379ea72c623ad1dd6052c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - halberd = halberd:main
    #
    # Would create an entry point called halberd that calls halberd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - Halberd
    - Halberd.clues
    - halberd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://halberd.superadditive.com/
  license: GNU General Public License (GPL)
  summary: 'HTTP load balancer detector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
