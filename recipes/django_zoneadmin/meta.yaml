package:
  name: django_zoneadmin
  version: "0.3"

source:
  fn: django_zoneadmin-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_zoneadmin/django_zoneadmin-0.3.tar.gz
  md5: 5c583930924e62dc3dd339d7d34359d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_zoneadmin = django_zoneadmin:main
    #
    # Would create an entry point called django_zoneadmin that calls django_zoneadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_zoneadmin
    - zone_admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/argaen/django_zoneadmin
  license: UNKNOWN
  summary: 'Alternative django administration interface with bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
