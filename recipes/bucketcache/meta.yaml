package:
  name: bucketcache
  version: "0.6.0"

source:
  fn: BucketCache-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BucketCache/BucketCache-0.6.0.tar.gz
  md5: e3b938521eaa905687527d3deb31b09d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bucketcache = bucketcache:main
    #
    # Would create an entry point called bucketcache that calls bucketcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boltons
    - decorator
    - logbook
    - python-dateutil
    - represent
    - six
    - pathlib

  run:
    - python
    - boltons
    - decorator
    - logbook
    - python-dateutil
    - represent
    - six
    - pathlib

test:
  # Python imports
  imports:
    - bucketcache
    - bucketcache.compat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RazerM/bucketcache
  license: MIT License
  summary: 'Versatile persisent file cache.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
