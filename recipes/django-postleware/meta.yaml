package:
  name: django-postleware
  version: "1.0.1"

source:
  fn: django-postleware-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-postleware/django-postleware-1.0.1.tar.gz
  md5: b8f1b61c8c93caf4bad6386268c0f848
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-postleware = django-postleware:main
    #
    # Would create an entry point called django-postleware that calls django-postleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - postleware
    - sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thurloat/django-postleware
  license: BSD License
  summary: "A simple django middleware that ensures POSTs aren't cached by clients."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
