package:
  name: halb
  version: "0.1.2"

source:
  fn: HALB-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/H/HALB/HALB-0.1.2.tar.gz
  md5: f5f38e0703722c1d7c796b27ca235fc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - halb = halb:main
    #
    # Would create an entry point called halb that calls halb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse
    - configparser

  run:
    - python
    - argparse
    - configparser

test:
  # Python imports
  imports:
    - halb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/junaid18183/HALB
  license: LICENSE.txt
  summary: 'Haproxy and Keepalived base multiple HA on same server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
