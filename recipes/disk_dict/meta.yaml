package:
  name: disk_dict
  version: "2.2.0"

source:
  fn: disk_dict-2.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/disk_dict/disk_dict-2.2.0.tar.gz
  md5: 5aa66e7d946263295bb1c44d46f75d02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - disk_dict = disk_dict:main
    #
    # Would create an entry point called disk_dict that calls disk_dict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xxhash >=0.3.2

  run:
    - python
    - xxhash >=0.3.2

test:
  # Python imports
  imports:
    - disk_dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AWNystrom/DiskDict
  license: Apache Software License
  summary: 'A disk-based key/value store in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
