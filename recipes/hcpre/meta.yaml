package:
  name: hcpre
  version: "0.5.5"

source:
  fn: hcpre-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/h/hcpre/hcpre-0.5.5.tar.gz
  md5: 264cbc2769b8975ab2bb577c0f389e52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hcpre = hcpre:main
    #
    # Would create an entry point called hcpre that calls hcpre.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pydicom
    - nibabel
    - networkx
    - traits
    - nipype
    - configobj

  run:
    - python
    - pydicom
    - nibabel
    - networkx
    - traits
    - nipype
    - configobj

test:
  # Python imports
  imports:
    - hcpre

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/beOn/hcpre
  license: BSD License
  summary: 'Generalized launcher for human connectome project BOLD preprocessing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
