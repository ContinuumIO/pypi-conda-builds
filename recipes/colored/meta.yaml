package:
  name: colored
  version: "1.1.5"

source:
  fn: colored-1.1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/colored/colored-1.1.5.tar.gz
  md5: dddb7dea666de595119764dbb611a0e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colored = colored:main
    #
    # Would create an entry point called colored that calls colored.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colored

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dslackw/colored
  license: GNU General Public License v3 (GPLv3)
  summary: 'Simple library for color and formatting to terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
