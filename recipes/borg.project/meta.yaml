package:
  name: borg.project
  version: "1.1rc9"

source:
  fn: borg.project-1.1rc9.tar.gz
  url: https://pypi.python.org/packages/source/b/borg.project/borg.project-1.1rc9.tar.gz
  md5: 6ee87bd6279d425b3e95a84ac021058e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - borg.project = borg.project:main
    #
    # Would create an entry point called borg.project that calls borg.project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - borg.localrole >=1.1rc3
    - plone
    - setuptools
    - plone.app.content
    - plone.contentrules
    - products.cmfplacefulworkflow

  run:
    - python
    - setuptools
    - borg.localrole >=1.1rc3
    - plone
    - setuptools
    - plone.app.content
    - plone.contentrules
    - products.cmfplacefulworkflow

test:
  # Python imports
  imports:
    - borg
    - borg/project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/borg/borg.project
  license: GPL
  summary: 'Ability to create project workspaces with local workflow'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
