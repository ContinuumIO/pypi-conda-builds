package:
  name: zope.copypastemove
  version: "4.0.0"

source:
  fn: zope.copypastemove-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.copypastemove/zope.copypastemove-4.0.0.tar.gz
  md5: f548f92ddceea5b51802ab968b0b42f6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.copypastemove = zope.copypastemove:main
    #
    # Would create an entry point called zope.copypastemove that calls zope.copypastemove.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.annotation
    - zope.component
    - zope.container
    - zope.copy
    - zope.event
    - zope.exceptions
    - zope.interface
    - zope.lifecycleevent
    - zope.location

  run:
    - python
    - setuptools
    - zope.annotation
    - zope.component
    - zope.container
    - zope.copy
    - zope.event
    - zope.exceptions
    - zope.interface
    - zope.lifecycleevent
    - zope.location

test:
  # Python imports
  imports:
    - zope
    - zope.copypastemove
    - zope.copypastemove.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.copypastemove
  license: Zope Public License
  summary: 'Copy, Paste and Move support for content components.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
