package:
  name: goosetypes
  version: "0.1.1"

source:
  fn: GooseTypes-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GooseTypes/GooseTypes-0.1.1.tar.gz
  md5: 7801a014abf7547392d46d6c8ecca500
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goosetypes = goosetypes:main
    #
    # Would create an entry point called goosetypes that calls goosetypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - spruce-types

  run:
    - python
    - spruce-types

test:
  # Python imports
  imports:
    - goosetypes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: u'Goose Types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
