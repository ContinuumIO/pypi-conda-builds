package:
  name: django-bulk-update
  version: "1.1.2"

source:
  fn: django-bulk-update-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bulk-update/django-bulk-update-1.1.2.tar.gz
  md5: 6198a10911636384727a619d866911a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bulk-update = django-bulk-update:main
    #
    # Would create an entry point called django-bulk-update that calls django-bulk-update.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - bulk_update
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aykut/django-bulk-update
  license: BSD License
  summary: 'Bulk update using one query over Django ORM.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
