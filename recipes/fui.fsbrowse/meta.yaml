package:
  name: fui.fsbrowse
  version: "0.4"

source:
  fn: fui.fsbrowse-0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/fui.fsbrowse/fui.fsbrowse-0.4.tar.gz
  md5: b25990390137814640a7f9c0b2d540b7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fui.fsbrowse = fui.fsbrowse:main
    #
    # Would create an entry point called fui.fsbrowse that calls fui.fsbrowse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - fui
    - fui.fsbrowse
    - fui.fsbrowse.browser
    - fui.fsbrowse.content
    - fui.fsbrowse.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenak/fui.fsbrowse
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
