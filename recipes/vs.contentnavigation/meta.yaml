package:
  name: vs.contentnavigation
  version: "0.2.3"

source:
  fn: vs.contentnavigation-0.2.3.zip
  url: https://pypi.python.org/packages/source/v/vs.contentnavigation/vs.contentnavigation-0.2.3.zip
  md5: a74e4bb605e329e23769bb00390dbda7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vs.contentnavigation = vs.contentnavigation:main
    #
    # Would create an entry point called vs.contentnavigation that calls vs.contentnavigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - vs
    - vs.contentnavigation
    - vs.contentnavigation.browser
    - vs.contentnavigation.content
    - vs.contentnavigation.extenders
    - vs.contentnavigation.interfaces
    - vs.contentnavigation.portlets
    - vs.contentnavigation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/vs.contentnavigation
  license: GNU General Public License (GPL)
  summary: 'vs.contentnavigation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
