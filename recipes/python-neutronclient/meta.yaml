package:
  name: python-neutronclient
  version: "2.6.0"

source:
  fn: python-neutronclient-2.6.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-neutronclient/python-neutronclient-2.6.0.tar.gz
  md5: 3d0785f7c42f50c6642f88d1d649cc75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-neutronclient = python-neutronclient:main
    #
    # Would create an entry point called python-neutronclient that calls python-neutronclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'CLI and Client Library for OpenStack Networking'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
