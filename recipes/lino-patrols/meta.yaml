package:
  name: lino-patrols
  version: "0.0.2"

source:
  fn: lino-patrols-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lino-patrols/lino-patrols-0.0.2.tar.gz
  md5: 8c6534296eb314d00c83c1edd965073c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lino-patrols = lino-patrols:main
    #
    # Would create an entry point called lino-patrols that calls lino-patrols.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lino >=1.6.7

  run:
    - python
    - lino >=1.6.7

test:
  # Python imports
  imports:
    - lino_patrols
    - lino_patrols.demo
    - lino_patrols.fixtures
    - lino_patrols.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://patrols.lino-framework.org
  license: GNU General Public License (GPL)
  summary: 'A Lino application for managing patrols'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
