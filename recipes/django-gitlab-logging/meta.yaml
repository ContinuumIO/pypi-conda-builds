package:
  name: django-gitlab-logging
  version: "0.2.2"

source:
  fn: django-gitlab-logging-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gitlab-logging/django-gitlab-logging-0.2.2.tar.gz
  md5: ffc2619d896c4952ca32250e48ba4809
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gitlab-logging = django-gitlab-logging:main
    #
    # Would create an entry point called django-gitlab-logging that calls django-gitlab-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gitlab_logging
    - gitlab_logging.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/valeriansaliou/django-gitlab-logging
  license: MIT - http://opensource.org/licenses/mit-license.php
  summary: 'A logging handler that opens GitLab issues on server error.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
