package:
  name: megrok.form
  version: "0.2.5"

source:
  fn: megrok.form-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.form/megrok.form-0.2.5.tar.gz
  md5: 5008c6e523187417e4e080de36dc1aa2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.form = megrok.form:main
    #
    # Would create an entry point called megrok.form that calls megrok.form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - zc.resourcelibrary ==1.0.1
    - z3c.widget ==0.1.6
    - zc.datetimewidget ==0.5.2
    - collective.namedfile ==1.1
    - collective.namedblobfile ==0.3

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - zc.resourcelibrary ==1.0.1
    - z3c.widget ==0.1.6
    - zc.datetimewidget ==0.5.2
    - collective.namedfile ==1.1
    - collective.namedblobfile ==0.3

test:
  # Python imports
  imports:
    - megrok
    - megrok.form
    - megrok.form.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/megrok.form
  license: Zope Public License
  summary: 'Fields, Widgets and Constraints for Grok'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
