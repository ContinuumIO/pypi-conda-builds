package:
  name: amiandopy
  version: "0.2"

source:
  fn: amiandopy-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/amiandopy/amiandopy-0.2.tar.gz
  md5: 2a262d4866a9a33d39c6ccc500d43d94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amiandopy = amiandopy:main
    #
    # Would create an entry point called amiandopy that calls amiandopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.8

  run:
    - python
    - requests >=0.8

test:
  # Python imports
  imports:
    - amiandopy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ledil/amiandopy
  license: MIT License
  summary: "amiandoPy makes it really easy to interact with amiando's API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
