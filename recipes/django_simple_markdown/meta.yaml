package:
  name: django_simple_markdown
  version: "1.0.5"

source:
  fn: django_simple_markdown-1.0.5.zip
  url: https://pypi.python.org/packages/source/d/django_simple_markdown/django_simple_markdown-1.0.5.zip
  md5: 1591d6426f8c85a3cc28dbe2665e699d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_simple_markdown = django_simple_markdown:main
    #
    # Would create an entry point called django_simple_markdown that calls django_simple_markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown

  run:
    - python
    - markdown

test:
  # Python imports
  imports:
    - django_simple_markdown
    - django_simple_markdown.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Valbrand/django-simple-markdown
  license: UNKNOWN
  summary: 'Django package for markdown editing within forms. Uses simple-markdown.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
