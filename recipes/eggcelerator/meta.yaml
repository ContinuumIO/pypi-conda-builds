package:
  name: eggcelerator
  version: "0.1dev-20120905"

source:
  fn: Eggcelerator-0.1dev-20120905.tar.gz
  url: https://pypi.python.org/packages/source/E/Eggcelerator/Eggcelerator-0.1dev-20120905.tar.gz
  md5: 83b7f527cff41821ebe19064630f38b2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eggcelerator = eggcelerator:main
    #
    # Would create an entry point called eggcelerator that calls eggcelerator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - path.py
    - s3cmd
    - argparse

  run:
    - python
    - setuptools
    - path.py
    - s3cmd
    - argparse

test:
  # Python imports
  imports:
    - eggcelerator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Compiles your packages into binary eggs and caches them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
