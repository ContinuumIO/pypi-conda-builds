package:
  name: collective.flowplayerclipviews
  version: "0.1.0a"

source:
  fn: collective.flowplayerclipviews-0.1.0a.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.flowplayerclipviews/collective.flowplayerclipviews-0.1.0a.tar.gz
  md5: ec15e9726e0afe1edc918342c3c063f6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.flowplayerclipviews = collective.flowplayerclipviews:main
    #
    # Would create an entry point called collective.flowplayerclipviews that calls collective.flowplayerclipviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.flowplayer

  run:
    - python
    - setuptools
    - setuptools
    - collective.flowplayer

test:
  # Python imports
  imports:
    - collective
    - collective.flowplayerclipviews
    - collective.flowplayerclipviews.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/keul/collective.flowplayerclipviews
  license: GPL
  summary: 'Plugin for Plone and collective.flowplayer. Get statistics on how many times the video has been seen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
