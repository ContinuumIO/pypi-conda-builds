package:
  name: django-formset-js
  version: "0.4.3"

source:
  fn: django-formset-js-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formset-js/django-formset-js-0.4.3.tar.gz
  md5: 45baa3e667b88db13d87ebc3c434c9e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formset-js = django-formset-js:main
    #
    # Would create an entry point called django-formset-js that calls django-formset-js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-jquery-js

  run:
    - python
    - django
    - django-jquery-js

test:
  # Python imports
  imports:
    - djangoformsetjs
    - djangoformsetjs.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-formset-js
  license: BSD License
  summary: 'Extend Django formsets with JavaScript'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
