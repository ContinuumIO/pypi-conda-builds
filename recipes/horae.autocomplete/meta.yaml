package:
  name: horae.autocomplete
  version: "1.0a1"

source:
  fn: horae.autocomplete-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.autocomplete/horae.autocomplete-1.0a1.tar.gz
  md5: 90a7251312eff35f3f44d9f6f6ad56e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.autocomplete = horae.autocomplete:main
    #
    # Would create an entry point called horae.autocomplete that calls horae.autocomplete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - fanstatic
    - zope.fanstatic
    - js.jquery
    - js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - fanstatic
    - zope.fanstatic
    - js.jquery
    - js.jqueryui

test:
  # Python imports
  imports:
    - horae
    - horae.autocomplete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides autocomplete fields and widgets for zope.formlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
