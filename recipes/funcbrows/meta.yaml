package:
  name: funcbrows
  version: "0.1.7"

source:
  fn: FuncBrows-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/F/FuncBrows/FuncBrows-0.1.7.tar.gz
  md5: 780ee9df2c8759b140dedc988c7c6391
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - funcbrows = funcbrows:main
    #
    # Would create an entry point called funcbrows that calls funcbrows.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.testbrowser

  run:
    - python
    - setuptools
    - zc.testbrowser

test:
  # Python imports
  imports:
    - FuncBrows
    - funcbrows

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/funcbrows
  license: BSD License
  summary: 'Web functional testing abstraction layer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
