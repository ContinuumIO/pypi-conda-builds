package:
  name: djangoplaceholdit
  version: "1.0"

source:
  fn: DjangoPlaceHoldit-1.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoPlaceHoldit/DjangoPlaceHoldit-1.0.tar.gz
  md5: dc21fa60a4a3314a51ddc0d625437c88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoplaceholdit = djangoplaceholdit:main
    #
    # Would create an entry point called djangoplaceholdit that calls djangoplaceholdit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - djangoplaceholdit
    - placeholder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/seanmavley/DjangoPlaceHoldit.git
  license: MIT License
  summary: 'A simple django app to serve images for placeholder during development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
