package:
  name: django-h5bp
  version: "0.3.4"

source:
  fn: django-h5bp-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-h5bp/django-h5bp-0.3.4.tar.gz
  md5: 5a974f87cbb7db90040586fe3c778cc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-h5bp = django-h5bp:main
    #
    # Would create an entry point called django-h5bp that calls django-h5bp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - django_h5bp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arruda/django-h5bp
  license: MIT License
  summary: 'A simple HTML5 Boilerplate Django app that has some predefined template Blocks, useful to be extended in any application template.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
