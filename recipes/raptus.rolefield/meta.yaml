package:
  name: raptus.rolefield
  version: "1.1"

source:
  fn: raptus.rolefield-1.1.zip
  url: https://pypi.python.org/packages/source/r/raptus.rolefield/raptus.rolefield-1.1.zip
  md5: 848d72b7a72ed716a93d742f8992c94c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.rolefield = raptus.rolefield:main
    #
    # Would create an entry point called raptus.rolefield that calls raptus.rolefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - raptus
    - raptus.rolefield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Raptus/raptus.rolefield
  license: GPL
  summary: 'Provides two archetypes fields which assign local roles to one or multiple users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
