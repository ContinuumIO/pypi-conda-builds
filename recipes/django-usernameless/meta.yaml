package:
  name: django-usernameless
  version: "0.1.0"

source:
  fn: django-usernameless-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-usernameless/django-usernameless-0.1.0.tar.gz
  md5: e93b38e829964432cf34322707fa8867
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-usernameless = django-usernameless:main
    #
    # Would create an entry point called django-usernameless that calls django-usernameless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-impersonate ==0.8.0
    - django-registration ==1.0
    - django-autoslug ==1.7.1

  run:
    - python
    - django-impersonate ==0.8.0
    - django-registration ==1.0
    - django-autoslug ==1.7.1

test:
  # Python imports
  imports:
    - usernameless

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marconi/django-usernameless
  license: UNKNOWN
  summary: 'Custom user without username for Django-1.5'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
