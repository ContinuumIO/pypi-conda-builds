package:
  name: cloudinary
  version: "1.1.1"

source:
  fn: cloudinary-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudinary/cloudinary-1.1.1.tar.gz
  md5: 77a719b133fa4338a6eef8a905b84bba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudinary = cloudinary:main
    #
    # Would create an entry point called cloudinary that calls cloudinary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cloudinary
    - cloudinary.poster
    - cloudinary.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cloudinary.com
  license: MIT License
  summary: 'Python interface to Cloudinary'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
