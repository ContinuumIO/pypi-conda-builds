package:
  name: collective.ptg.contentleadimage
  version: "1.0"

source:
  fn: collective.ptg.contentleadimage-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ptg.contentleadimage/collective.ptg.contentleadimage-1.0.tar.gz
  md5: 4eb310c4f061864ea20fed6adc175552
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.contentleadimage = collective.ptg.contentleadimage:main
    #
    # Would create an entry point called collective.ptg.contentleadimage that calls collective.ptg.contentleadimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.contentleadimage
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.contentleadimage
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.contentleadimage
    - collective.ptg.contentleadimage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.plonetruegallery
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Display also contentleadimages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
