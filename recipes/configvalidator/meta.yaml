package:
  name: configvalidator
  version: "0.1.1"

source:
  fn: ConfigValidator-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfigValidator/ConfigValidator-0.1.1.tar.gz
  md5: c79de5b2a429964ebb05af135543350c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configvalidator = configvalidator:main
    #
    # Would create an entry point called configvalidator that calls configvalidator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - configvalidator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JanHendrikDolling/configvalidator
  license: Apache Software License
  summary: 'python module to Validate ini File user input'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
