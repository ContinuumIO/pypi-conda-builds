package:
  name: helga-hipster
  version: "0.1.2"

source:
  fn: helga-hipster-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-hipster/helga-hipster-0.1.2.tar.gz
  md5: ba32cb44e50e5cd34f2cdc4df30c1740
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-hipster = helga-hipster:main
    #
    # Would create an entry point called helga-hipster that calls helga-hipster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.2.1

  run:
    - python
    - setuptools
    - requests ==2.2.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-hipster
  license: MIT License
  summary: 'Helga plugin to get hipster music recommendations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
