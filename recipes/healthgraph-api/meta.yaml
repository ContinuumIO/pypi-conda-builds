package:
  name: healthgraph-api
  version: "0.3.0"

source:
  fn: healthgraph-api-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/h/healthgraph-api/healthgraph-api-0.3.0.tar.gz
  md5: c60e7e8c92b33a96ef6400ec96e50023
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - healthgraph-api = healthgraph-api:main
    #
    # Would create an entry point called healthgraph-api that calls healthgraph-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - healthgraph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aouyar.github.com/healthgraph-api
  license: GNU General Public License (GPL)
  summary: u'Python Client for HealthGraph API from RunKeeper.com.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
