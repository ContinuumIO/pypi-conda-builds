package:
  name: django-csvimport
  version: "2.4"

source:
  fn: django-csvimport-2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-csvimport/django-csvimport-2.4.tar.gz
  md5: 63ec428ebf5ecc299cf9f97cc9cd6c84
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-csvimport = django-csvimport:main
    #
    # Would create an entry point called django-csvimport that calls django-csvimport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - chardet

  run:
    - python
    - setuptools
    - django >=1.7
    - chardet

test:
  # Python imports
  imports:
    - csvimport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edcrewe/django-csvimport
  license: Apache Software License
  summary: 'Import CSV files to django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
