package:
  name: ffprobe
  version: "0.5"

source:
  fn: ffprobe-0.5.zip
  url: https://pypi.python.org/packages/source/f/ffprobe/ffprobe-0.5.zip
  md5: 72791bd1935c15eaeea3882f953b635f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ffprobe = ffprobe:main
    #
    # Would create an entry point called ffprobe that calls ffprobe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ffprobe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simon-hargreaves.com/ffprobe
  license: MIT License
  summary: 'Wrapper around ffprobe command to extract metadata from media files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
