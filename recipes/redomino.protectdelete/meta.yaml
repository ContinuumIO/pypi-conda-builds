package:
  name: redomino.protectdelete
  version: "0.3"

source:
  fn: redomino.protectdelete-0.3.zip
  url: https://pypi.python.org/packages/source/r/redomino.protectdelete/redomino.protectdelete-0.3.zip
  md5: 5da3bae37d29a02d84e4984991c59396
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.protectdelete = redomino.protectdelete:main
    #
    # Would create an entry point called redomino.protectdelete that calls redomino.protectdelete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redomino
    - redomino.protectdelete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.protectdelete
  license: GPL
  summary: 'Avoid objects from delete'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
