package:
  name: aldryn-sites
  version: "0.5"

source:
  fn: aldryn-sites-0.5.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-sites/aldryn-sites-0.5.tar.gz
  md5: 08d8ac23e00d864cb5d89343b839271e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-sites = aldryn-sites:main
    #
    # Would create an entry point called aldryn-sites that calls aldryn-sites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.8
    - yurl >=0.13
    - django-appconf

  run:
    - python
    - django >=1.6,<1.8
    - yurl >=0.13
    - django-appconf

test:
  # Python imports
  imports:
    - aldryn_sites

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-sites/
  license: BSD License
  summary: 'An extension for django.contrib.sites, featuring domain redirects and automatic population of the django.contrib.sites.Site table based on settings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
