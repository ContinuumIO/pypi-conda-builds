package:
  name: datastore.memcached
  version: "0.1.1"

source:
  fn: datastore.memcached-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/datastore.memcached/datastore.memcached-0.1.1.tar.gz
  md5: e4f0fe7c958298fe8c312a831ab8df49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datastore.memcached = datastore.memcached:main
    #
    # Would create an entry point called datastore.memcached that calls datastore.memcached.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datastore >=0.3.3
    - pylibmc ==1.2.3

  run:
    - python
    - datastore >=0.3.3
    - pylibmc ==1.2.3

test:
  # Python imports
  imports:
    - datastore
    - datastore.memcached

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datastore/datastore.memcached
  license: MIT License
  summary: 'memached datastore implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
