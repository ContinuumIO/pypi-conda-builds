package:
  name: ikazuchi.plugins.blockdiag
  version: "0.1.0"

source:
  fn: ikazuchi.plugins.blockdiag-0.1.0.tar.bz2
  url: https://pypi.python.org/packages/source/i/ikazuchi.plugins.blockdiag/ikazuchi.plugins.blockdiag-0.1.0.tar.bz2
  md5: 4a0bf50b12073dbc5a76c4807bb64d35
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ikazuchi.plugins.blockdiag = ikazuchi.plugins.blockdiag:main
    #
    # Would create an entry point called ikazuchi.plugins.blockdiag that calls ikazuchi.plugins.blockdiag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - blockdiag
    - ikazuchi >=0.5.2

  run:
    - python
    - setuptools
    - distribute
    - blockdiag
    - ikazuchi >=0.5.2

test:
  # Python imports
  imports:
    - ikazuchi
    - ikazuchi.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://t2y.bitbucket.org/ikazuchi/build/html/index.html
  license: Apache Software License
  summary: 'blockdiag translator with ikazuchi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
