package:
  name: zhou_nester
  version: "1.0.0"

source:
  fn: zhou_nester-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zhou_nester/zhou_nester-1.0.0.tar.gz
  md5: 8315e4a2935c77fbcd42ef5819d32c64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zhou_nester = zhou_nester:main
    #
    # Would create an entry point called zhou_nester that calls zhou_nester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - zhou_nester

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.headfirstlabs.com
  license: UNKNOWN
  summary: 'A simple printer of nester listd'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
