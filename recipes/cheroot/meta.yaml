package:
  name: cheroot
  version: "4.0.0b2"

source:
  fn: Cheroot-4.0.0b2.tar.gz
  url: https://pypi.python.org/packages/source/C/Cheroot/Cheroot-4.0.0b2.tar.gz
  md5: 74f16a933cde253d70992dd012d5faff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cheroot = cheroot:main
    #
    # Would create an entry point called cheroot that calls cheroot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dynpool >=1.0.5

  run:
    - python
    - dynpool >=1.0.5

test:
  # Python imports
  imports:
    - cheroot
    - cheroot.ssllib
    - cheroot.test
    - cheroot.workers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cherrypy.org
  license: BSD
  summary: 'Object-Oriented HTTP server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
