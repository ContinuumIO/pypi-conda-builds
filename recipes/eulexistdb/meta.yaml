package:
  name: eulexistdb
  version: "0.19.0"

source:
  fn: eulexistdb-0.19.0.tar.gz
  url: https://pypi.python.org/packages/source/e/eulexistdb/eulexistdb-0.19.0.tar.gz
  md5: dac8dbe1a79703fa1356f40798f161d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eulexistdb = eulexistdb:main
    #
    # Would create an entry point called eulexistdb that calls eulexistdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eulxml >=0.18.0

  run:
    - python
    - eulxml >=0.18.0

test:
  # Python imports
  imports:
    - eulexistdb
    - eulexistdb.management
    - eulexistdb.management.commands
    - eulexistdb.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/emory-libraries/eulexistdb
  license: Apache Software License
  summary: 'Idiomatic access to the eXist-db XML Database using XPath and XQuery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
