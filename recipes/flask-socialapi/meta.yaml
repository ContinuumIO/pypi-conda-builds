package:
  name: flask-socialapi
  version: "1.0.5"

source:
  fn: Flask-SocialAPI-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SocialAPI/Flask-SocialAPI-1.0.5.tar.gz
  md5: 605586647af4d9587205f30ea4c5d744
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-socialapi = flask-socialapi:main
    #
    # Would create an entry point called flask-socialapi that calls flask-socialapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-oauthlib

  run:
    - python
    - flask-oauthlib

test:
  # Python imports
  imports:
    - flask_socialapi
    - flask_socialapi.providers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leejaedus/Flask-SocialAPI
  license: UNKNOWN
  summary: 'Simple api for controlling and login to provider'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
