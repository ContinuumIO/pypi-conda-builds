package:
  name: django-invoicing
  version: "0.6.2"

source:
  fn: django-invoicing-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-invoicing/django-invoicing-0.6.2.tar.gz
  md5: 848b33f9f0ee314ad0fe1e87a4006ed9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-invoicing = django-invoicing:main
    #
    # Would create an entry point called django-invoicing that calls django-invoicing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - django-countries
    - django-iban
    - jsonfield
    - django-model-utils
    - django-money
    - vatnumber

  run:
    - python
    - django
    - south
    - django-countries
    - django-iban
    - jsonfield
    - django-model-utils
    - django-money
    - vatnumber

test:
  # Python imports
  imports:
    - invoicing
    - invoicing.formatters
    - invoicing.migrations
    - invoicing.taxation
    - invoicing.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-invoicing
  license: BSD License
  summary: 'Django app for invoicing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
