package:
  name: bintrees
  version: "2.0.2"

source:
  fn: bintrees-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bintrees/bintrees-2.0.2.tar.gz
  md5: 91fa6db45fc69bc59044338deb66aa5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bintrees = bintrees:main
    #
    # Would create an entry point called bintrees that calls bintrees.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bintrees

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mozman/bintrees
  license: MIT License
  summary: 'Package provides Binary-, RedBlack- and AVL-Trees in Python and Cython.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
