package:
  name: hispectralmodel
  version: "0.1.0"

source:
  fn: HISpectralModel-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/H/HISpectralModel/HISpectralModel-0.1.0.tar.gz
  md5: 512e82ca596c3a823bb8565a464437fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hispectralmodel = hispectralmodel:main
    #
    # Would create an entry point called hispectralmodel that calls hispectralmodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.6.1
    - scipy >=0.9.0
    - matplotlib >=1.1.1rc

  run:
    - python
    - numpy >=1.6.1
    - scipy >=0.9.0
    - matplotlib >=1.1.1rc

test:
  # Python imports
  imports:
    - hispectralmodel
    - hispectrum
    - hispectrum.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/HISpectralModel/
  license: LICENSE.txt
  summary: 'Generates model HI spectral profiles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
