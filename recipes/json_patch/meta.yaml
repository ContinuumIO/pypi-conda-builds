package:
  name: json_patch
  version: "0.1"

source:
  fn: json_patch-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/json_patch/json_patch-0.1.tar.gz
  md5: ec6bd2ea726525ba8adcf377ebd11c50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_patch = json_patch:main
    #
    # Would create an entry point called json_patch that calls json_patch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - json_pointer >=0.1.2

  run:
    - python
    - json_pointer >=0.1.2

test:
  # Python imports
  imports:
    - json_patch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Implementation of the json-patch spec'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
