package:
  name: pelican_related
  version: "0.1"

source:
  fn: pelican_related-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican_related/pelican_related-0.1.tar.gz
  md5: 089f09e75b6d2d91a1bac354e38a6168
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican_related = pelican_related:main
    #
    # Would create an entry point called pelican_related that calls pelican_related.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican

  run:
    - python
    - pelican

test:
  # Python imports
  imports:
    - pelican_related

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wutali/pelican_related
  license: UNKNOWN
  summary: 'A Pelican plugin to make a list of related posts from your meta tags and settings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
