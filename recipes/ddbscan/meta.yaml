package:
  name: ddbscan
  version: "0.3.0"

source:
  fn: ddbscan-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/ddbscan/ddbscan-0.3.0.tar.gz
  md5: 5a994c7f4f440f5ad71a7084490cea14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ddbscan = ddbscan:main
    #
    # Would create an entry point called ddbscan that calls ddbscan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scipy >=0.13.3
    - numpy >=1.8.1

  run:
    - python
    - scipy >=0.13.3
    - numpy >=1.8.1

test:
  # Python imports
  imports:
    - ddbscan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudwalkio/ddbscan
  license: MIT License
  summary: 'Discrete DBSCAN algorithm optimized for discrete and bounded data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
