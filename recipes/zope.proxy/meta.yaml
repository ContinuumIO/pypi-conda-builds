package:
  name: zope.proxy
  version: "4.1.4"

source:
  fn: zope.proxy-4.1.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.proxy/zope.proxy-4.1.4.tar.gz
  md5: 3bcaf8b8512a99649ecf2f158c11d05b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.proxy = zope.proxy:main
    #
    # Would create an entry point called zope.proxy that calls zope.proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zope.interface
    - setuptools

  run:
    - python
    - zope.interface
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.proxy
  license: Zope Public License
  summary: 'Generic Transparent Proxies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
