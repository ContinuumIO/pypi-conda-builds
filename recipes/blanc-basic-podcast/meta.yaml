package:
  name: blanc-basic-podcast
  version: "0.2"

source:
  fn: blanc-basic-podcast-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/blanc-basic-podcast/blanc-basic-podcast-0.2.tar.gz
  md5: 8403bbbc1e6a21163c9006afe27c23de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blanc-basic-podcast = blanc-basic-podcast:main
    #
    # Would create an entry point called blanc-basic-podcast that calls blanc-basic-podcast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hsaudiotag >=1.1.1

  run:
    - python
    - hsaudiotag >=1.1.1

test:
  # Python imports
  imports:
    - blanc_basic_podcast
    - blanc_basic_podcast.podcast

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.blanctools.com/
  license: BSD License
  summary: 'Blanc Basic Podcast for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
