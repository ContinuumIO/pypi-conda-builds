package:
  name: autograph
  version: "1.0.0"

source:
  fn: autograph-1.0.0.zip
  url: https://pypi.python.org/packages/source/a/autograph/autograph-1.0.0.zip
  md5: 9d5309543ceb73c5c9da2e3158bebee0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autograph = autograph:main
    #
    # Would create an entry point called autograph that calls autograph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - south >=0.8.1
    - pil >=1.1.7

  run:
    - python
    - django >=1.4.5
    - south >=0.8.1
    - pil >=1.1.7

test:
  # Python imports
  imports:
    - autograph
    - autograph.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zivezab/django-autograph/
  license: Python Software Foundation License
  summary: 'A Django application for managing signature/autograph data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
