package:
  name: maec
  version: "4.1.0.12"

source:
  fn: maec-4.1.0.12.tar.gz
  url: https://pypi.python.org/packages/source/m/maec/maec-4.1.0.12.tar.gz
  md5: 8bba72b035b00459edf82b15fc4e950a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maec = maec:main
    #
    # Would create an entry point called maec that calls maec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.2.3
    - cybox >=2.1.0.11,<2.1.1.0

  run:
    - python
    - lxml >=2.2.3
    - cybox >=2.1.0.11,<2.1.1.0

test:
  # Python imports
  imports:
    - maec
    - maec.analytics
    - maec.bindings
    - maec.bundle
    - maec.misc
    - maec.package
    - maec.test
    - maec.test.bundle
    - maec.test.package
    - maec.utils
    - maec.vocabs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://maec.mitre.org
  license: BSD License
  summary: 'An API for parsing and creating MAEC content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
