package:
  name: django-bootstrap3-iconfield
  version: "0.1"

source:
  fn: django-bootstrap3-iconfield-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap3-iconfield/django-bootstrap3-iconfield-0.1.tar.gz
  md5: 97325a51025d0448a64b62a591319611
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap3-iconfield = django-bootstrap3-iconfield:main
    #
    # Would create an entry point called django-bootstrap3-iconfield that calls django-bootstrap3-iconfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap3-iconfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ALibrada/django-bootstrap3-iconfield
  license: Apache Software License
  summary: 'Renderers to show icons in the input fields for the django-bootstrap3 project from dyve: https://github.com/dyve/django-bootstrap3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
