package:
  name: pelican-readtime
  version: "0.1.1"

source:
  fn: pelican-readtime-0.1.1.zip
  url: https://pypi.python.org/packages/source/p/pelican-readtime/pelican-readtime-0.1.1.zip
  md5: 38999987ad7f4e00f5f1370a44a15589
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-readtime = pelican-readtime:main
    #
    # Would create an entry point called pelican-readtime that calls pelican-readtime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JenkinsDev/pelican-readtime
  license: MIT License
  summary: 'Plugin for Pelican that computes read time based on Medium\'s readtime "algorithm."'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
