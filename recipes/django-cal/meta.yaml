package:
  name: django-cal
  version: "0.2.2"

source:
  fn: django-cal-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cal/django-cal-0.2.2.tar.gz
  md5: e5fa7ae98a4d1e5fa8c8a55215d2b63d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cal = django-cal:main
    #
    # Would create an entry point called django-cal that calls django-cal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil <2.0
    - vobject
    - django

  run:
    - python
    - python-dateutil <2.0
    - vobject
    - django

test:
  # Python imports
  imports:
    - django_cal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/statesofpop/django-cal
  license: BSD License
  summary: 'Django app to enable exporting of events to iCalendar files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
