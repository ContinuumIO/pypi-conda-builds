package:
  name: minihallib
  version: "0.1.10"

source:
  fn: minihallib-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/m/minihallib/minihallib-0.1.10.tar.gz
  md5: e164bee7876fc13db1e944f74902ed6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minihallib = minihallib:main
    #
    # Would create an entry point called minihallib that calls minihallib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - minihallib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.smile.org.ua/trac/minihallib
  license: Academic Free License (AFL) or GNU General Public License (GPL)
  summary: 'Library to handle HAL devices and events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
