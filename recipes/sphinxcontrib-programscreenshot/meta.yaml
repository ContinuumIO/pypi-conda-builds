package:
  name: sphinxcontrib-programscreenshot
  version: "0.0.5"

source:
  fn: sphinxcontrib-programscreenshot-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-programscreenshot/sphinxcontrib-programscreenshot-0.0.5.tar.gz
  md5: 0adf7874cfbe3aacbae52ec62ca12237
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-programscreenshot = sphinxcontrib-programscreenshot:main
    #
    # Would create an entry point called sphinxcontrib-programscreenshot that calls sphinxcontrib-programscreenshot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - path.py
    - easyprocess
    - pyvirtualdisplay
    - pyscreenshot
    - sphinx >=1.0

  run:
    - python
    - path.py
    - easyprocess
    - pyvirtualdisplay
    - pyscreenshot
    - sphinx >=1.0

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ponty/sphinxcontrib-programscreenshot
  license: BSD License
  summary: 'Sphinx extension to include screenshot of programs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
