package:
  name: django-pyvows
  version: "0.6.2"

source:
  fn: django-pyvows-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pyvows/django-pyvows-0.6.2.tar.gz
  md5: a6097cfaf0d0891774dd777a437d6089
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pyvows = django-pyvows:main
    #
    # Would create an entry point called django-pyvows that calls django-pyvows.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyvows
    - django
    - cherrypy
    - httplib2

  run:
    - python
    - pyvows
    - django
    - cherrypy
    - httplib2

test:
  # Python imports
  imports:
    - django_pyvows
    - django_pyvows.assertions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rafaelcaricio/django-pyvows
  license: MIT License
  summary: 'django-pyvows are pyvows extensions to django web framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
