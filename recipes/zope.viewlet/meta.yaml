package:
  name: zope.viewlet
  version: "4.0.0"

source:
  fn: zope.viewlet-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.viewlet/zope.viewlet-4.0.0.tar.gz
  md5: 04645812a22fbc1b1e8ccd02290afaab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.viewlet = zope.viewlet:main
    #
    # Would create an entry point called zope.viewlet that calls zope.viewlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.browserpage
    - zope.component
    - zope.configuration
    - zope.contentprovider
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.browserpage
    - zope.component
    - zope.configuration
    - zope.contentprovider
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.viewlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.viewlet
  license: Zope Public License
  summary: 'Zope Viewlets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
