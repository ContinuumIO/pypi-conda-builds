package:
  name: ucloudbiz
  version: "0.3.1"

source:
  fn: ucloudbiz-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/u/ucloudbiz/ucloudbiz-0.3.1.tar.gz
  md5: d47883fdc7f579dfcaeb9be1a8d2d574
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ucloudbiz = ucloudbiz:main
    #
    # Would create an entry point called ucloudbiz that calls ucloudbiz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ucloudbiz
    - ucloudbiz.server
    - ucloudbiz.server.address
    - ucloudbiz.server.lib
    - ucloudbiz.server.portforwarding
    - ucloudbiz.server.template
    - ucloudbiz.server.vm
    - ucloudbiz.server.volume

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ucloud.googlecode.com
  license: BSD License
  summary: 'ucloudbiz openapi python library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
