package:
  name: django-object-tools
  version: "1.0.4"

source:
  fn: django-object-tools-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-object-tools/django-object-tools-1.0.4.tar.gz
  md5: 7bc39bf6a9996ba0d4d53f42e16321b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-object-tools = django-object-tools:main
    #
    # Would create an entry point called django-object-tools that calls django-object-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - object_tools
    - object_tools.management
    - object_tools.templatetags
    - object_tools.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-object-tools
  license: BSD License
  summary: 'Django app enabling painless creation of additional admin object tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
