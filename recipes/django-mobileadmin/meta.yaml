package:
  name: django-mobileadmin
  version: "0.5.2"

source:
  fn: django-mobileadmin-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mobileadmin/django-mobileadmin-0.5.2.tar.gz
  md5: c596330f71c530bceeadf840786a27a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobileadmin = django-mobileadmin:main
    #
    # Would create an entry point called django-mobileadmin that calls django-mobileadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mobileadmin
    - mobileadmin.conf
    - mobileadmin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-mobileadmin/
  license: BSD License
  summary: 'The Django admin interface for mobile devices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
