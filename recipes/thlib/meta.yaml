package:
  name: thlib
  version: "0.10.1"

source:
  fn: thLib-0.10.1.zip
  url: https://pypi.python.org/packages/source/t/thLib/thLib-0.10.1.zip
  md5: c9f322f4baee848997146474a3e94272
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thlib = thlib:main
    #
    # Would create an entry point called thlib that calls thlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - thLib
    - thlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://work.thaslwanter.at
  license: http://opensource.org/licenses/BSD-2-Clause
  summary: 'Collection of Python utilities for signal analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
