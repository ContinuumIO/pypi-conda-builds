package:
  name: django-bleach
  version: "0.3.0"

source:
  fn: django-bleach-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bleach/django-bleach-0.3.0.tar.gz
  md5: 7cf49f192e7f3147d7b299a20125dee3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bleach = django-bleach:main
    #
    # Would create an entry point called django-bleach that calls django-bleach.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bleach
    - django >=1.3

  run:
    - python
    - bleach
    - django >=1.3

test:
  # Python imports
  imports:
    - django_bleach
    - django_bleach.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-bleach
  license: UNKNOWN
  summary: 'Easily use bleach with Django models and templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
