package:
  name: django-libsass
  version: "0.3"

source:
  fn: django-libsass-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-libsass/django-libsass-0.3.tar.gz
  md5: 090ce722a3086e81390c1c4c0e22693d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-libsass = django-libsass:main
    #
    # Would create an entry point called django-libsass that calls django-libsass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-compressor >=1.3
    - libsass >=0.3.0

  run:
    - python
    - django-compressor >=1.3
    - libsass >=0.3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/torchbox/django-libsass
  license: BSD License
  summary: 'A django-compressor filter to compile SASS files using libsass'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
