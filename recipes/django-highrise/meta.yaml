package:
  name: django-highrise
  version: "0.6"

source:
  fn: django-highrise-0.6.zip
  url: https://pypi.python.org/packages/source/d/django-highrise/django-highrise-0.6.zip
  md5: e32abb72514a3b1e9442ed6c07f9748d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-highrise = django-highrise:main
    #
    # Would create an entry point called django-highrise that calls django-highrise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyrise >=0.4
    - django >=1.4

  run:
    - python
    - pyrise >=0.4
    - django >=1.4

test:
  # Python imports
  imports:
    - django_highrise

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hugorodgerbrown/django-highrise
  license: BSD License
  summary: 'Highrise CRM integration for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
