package:
  name: js.log4javascript
  version: "1.4.6"

source:
  fn: js.log4javascript-1.4.6.zip
  url: https://pypi.python.org/packages/source/j/js.log4javascript/js.log4javascript-1.4.6.zip
  md5: 86c4a42f8461484478da63f60405f816
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.log4javascript = js.log4javascript:main
    #
    # Would create an entry point called js.log4javascript that calls js.log4javascript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.log4javascript

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of log4javascript'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
