package:
  name: jinja2_markdown
  version: "0.0.3"

source:
  fn: jinja2_markdown-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja2_markdown/jinja2_markdown-0.0.3.tar.gz
  md5: 1eab4525f6e4e1a3330ae90b3bead941
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja2_markdown = jinja2_markdown:main
    #
    # Would create an entry point called jinja2_markdown that calls jinja2_markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown >=2.3.1
    - jinja2 >=2.4

  run:
    - python
    - markdown >=2.3.1
    - jinja2 >=2.4

test:
  # Python imports
  imports:
    - jinja2_markdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danielchatfield/jinja2_markdown_extension
  license: MIT License
  summary: 'A jinja2 extension that adds a {% markdown %} tag to templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
