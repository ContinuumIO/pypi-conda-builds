package:
  name: django-anonymous-notification
  version: "1.2.1"

source:
  fn: django-anonymous-notification-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-anonymous-notification/django-anonymous-notification-1.2.1.tar.gz
  md5: 04bbdb1a27c8949d7145fee2885c8a92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-anonymous-notification = django-anonymous-notification:main
    #
    # Would create an entry point called django-anonymous-notification that calls django-anonymous-notification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - notification
    - notification.backends
    - notification.management
    - notification.management.commands
    - notification.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pinax/django-notification
  license: MIT License
  summary: 'User notification management for the Django web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
