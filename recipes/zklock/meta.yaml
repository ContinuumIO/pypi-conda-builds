package:
  name: zklock
  version: "0.2"

source:
  fn: zklock-0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zklock/zklock-0.2.tar.gz
  md5: c34c68c0118a4c4c8b3e845bcab1fcfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zklock = zklock:main
    #
    # Would create an entry point called zklock that calls zklock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - zklock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tinyogre/zklock
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'zklock provides a simple distributed locking mechanism using zookeeper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
