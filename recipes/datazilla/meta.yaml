package:
  name: datazilla
  version: "1.4"

source:
  fn: datazilla-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/datazilla/datazilla-1.4.tar.gz
  md5: ed159871f6373bb61d1c461bc5aebda4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datazilla = datazilla:main
    #
    # Would create an entry point called datazilla that calls datazilla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2

  run:
    - python
    - oauth2

test:
  # Python imports
  imports:
    - datazilla
    - dzclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/datazilla_client
  license: MPL
  summary: 'Python library to interact with the datazilla server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
