package:
  name: jenkins-job-builder
  version: "1.2.0"

source:
  fn: jenkins-job-builder-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jenkins-job-builder/jenkins-job-builder-1.2.0.tar.gz
  md5: 79e44ef0d3fffc19f415d8c0caac6b7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jenkins-job-builder = jenkins-job-builder:main
    #
    # Would create an entry point called jenkins-job-builder that calls jenkins-job-builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Manage Jenkins jobs with YAML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
