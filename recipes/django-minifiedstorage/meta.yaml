package:
  name: django-minifiedstorage
  version: "0.3"

source:
  fn: django-minifiedstorage-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-minifiedstorage/django-minifiedstorage-0.3.tar.gz
  md5: 028ff9511c245b0671fe8fc8ce29107c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-minifiedstorage = django-minifiedstorage:main
    #
    # Would create an entry point called django-minifiedstorage that calls django-minifiedstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - slimit
    - csscompressor

  run:
    - python
    - slimit
    - csscompressor

test:
  # Python imports
  imports:
    - minifiedstorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/askholme/django_minifiedstorage
  license: BSD License
  summary: 'Simple minified storage for django static assets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
