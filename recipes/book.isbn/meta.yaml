package:
  name: book.isbn
  version: "0.0.2"

source:
  fn: book.isbn-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/book.isbn/book.isbn-0.0.2.tar.gz
  md5: 9c025e520de86998df5316844445bef6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - book.isbn = book.isbn:main
    #
    # Would create an entry point called book.isbn that calls book.isbn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - book
    - book.isbn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/book.isbn
  license: BSD License
  summary: '`book.isbn` is a package for book ISBN.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
