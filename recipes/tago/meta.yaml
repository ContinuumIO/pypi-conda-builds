package:
  name: tago
  version: "1.0.3"

source:
  fn: tago-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tago/tago-1.0.3.tar.gz
  md5: 3637624a3088b77d3f48db5d3465cf0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tago = tago:main
    #
    # Would create an entry point called tago that calls tago.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - socketio-client
    - requests

  run:
    - python
    - socketio-client
    - requests

test:
  # Python imports
  imports:
    - tago
    - tago/device

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tago-io/tago-python
  license: Copyright
  summary: 'Official Python lib for Tago'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
