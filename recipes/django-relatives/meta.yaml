package:
  name: django-relatives
  version: "0.3.1"

source:
  fn: django-relatives-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-relatives/django-relatives-0.3.1.tar.gz
  md5: 2603188c28c3cb6c0d281084956a0d9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-relatives = django-relatives:main
    #
    # Would create an entry point called django-relatives that calls django-relatives.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - relatives
    - relatives.templatetags
    - relatives.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/treyhunner/django-relatives
  license: MIT License
  summary: 'Utilities for linking to related objects in Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
