package:
  name: django-poormanscron
  version: "0.1.3"

source:
  fn: django-poormanscron-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-poormanscron/django-poormanscron-0.1.3.tar.gz
  md5: ebf3bf4581df9501542c88f68e72cc12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-poormanscron = django-poormanscron:main
    #
    # Would create an entry point called django-poormanscron that calls django-poormanscron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - poormanscron
    - poormanscron.fixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BBB/django-poormanscron/tree/master/
  license: BSD
  summary: "Poor Man's Cron is Django app which makes use of spambots, search engine indexing robots and alike to run scheduled tasks in approximately regular intervals."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
