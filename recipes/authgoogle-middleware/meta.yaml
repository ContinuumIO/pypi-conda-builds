package:
  name: authgoogle-middleware
  version: "0.2.4"

source:
  fn: authgoogle-middleware-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/a/authgoogle-middleware/authgoogle-middleware-0.2.4.tar.gz
  md5: eec02a411d654f52c97e46c81df671af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authgoogle-middleware = authgoogle-middleware:main
    #
    # Would create an entry point called authgoogle-middleware that calls authgoogle-middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.6.2
    - python-openid

  run:
    - python
    - werkzeug >=0.6.2
    - python-openid

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/uptimebox/authgoogle-middleware
  license: BSD License
  summary: 'Google authentication middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
