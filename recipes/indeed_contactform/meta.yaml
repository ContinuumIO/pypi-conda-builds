package:
  name: indeed_contactform
  version: "1.0.2"

source:
  fn: indeed_contactForm-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/indeed_contactForm/indeed_contactForm-1.0.2.tar.gz
  md5: 4e4508599bee6fae93e0511654a3f33f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indeed_contactform = indeed_contactform:main
    #
    # Would create an entry point called indeed_contactform that calls indeed_contactform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - indeed_contactForm
    - indeed_contactform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org
  license: GNU General Public License (GPL)
  summary: 'A very simple Contact Form for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
