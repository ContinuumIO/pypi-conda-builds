package:
  name: django-redisession
  version: "0.3.1"

source:
  fn: django-redisession-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redisession/django-redisession-0.3.1.tar.gz
  md5: 798d4b5f492e123bf7a7792508785566
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redisession = django-redisession:main
    #
    # Would create an entry point called django-redisession that calls django-redisession.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - redisession
    - redisession.management
    - redisession.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/liokm/django-redisession
  license: MIT License
  summary: 'A Redis-based Django session engine for django.contrib.sessions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
