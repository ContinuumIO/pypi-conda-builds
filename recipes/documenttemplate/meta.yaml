package:
  name: documenttemplate
  version: "2.13.2"

source:
  fn: DocumentTemplate-2.13.2.zip
  url: https://pypi.python.org/packages/source/D/DocumentTemplate/DocumentTemplate-2.13.2.zip
  md5: 07bb086c77c1dfe94125ad2efbba94b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - documenttemplate = documenttemplate:main
    #
    # Would create an entry point called documenttemplate that calls documenttemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - extensionclass
    - restrictedpython
    - zexceptions
    - zope.sequencesort
    - zope.structuredtext

  run:
    - python
    - accesscontrol
    - acquisition
    - extensionclass
    - restrictedpython
    - zexceptions
    - zope.sequencesort
    - zope.structuredtext

test:
  # Python imports
  imports:
    - DocumentTemplate
    - DocumentTemplate.sequence
    - DocumentTemplate.sequence.tests
    - DocumentTemplate.tests
    - TreeDisplay
    - documenttemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/DocumentTemplate
  license: ZPL 2.1
  summary: 'Document Templating Markup Language (DTML)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
