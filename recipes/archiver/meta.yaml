package:
  name: archiver
  version: "0.2.2"

source:
  fn: Archiver-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/A/Archiver/Archiver-0.2.2.tar.gz
  md5: aee73c131eaa300f820e4d40d3fcf348
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archiver = archiver:main
    #
    # Would create an entry point called archiver that calls archiver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - archiver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/archiver/index.html
  license: GNU Affero General Public License v3
  summary: 'Manage files based on their SHA1 hashes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
