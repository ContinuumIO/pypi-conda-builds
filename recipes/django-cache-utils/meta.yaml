package:
  name: django-cache-utils
  version: "0.7.2"

source:
  fn: django-cache-utils-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-utils/django-cache-utils-0.7.2.tar.gz
  md5: 7dc91b4f7615611ef87795960c84c2e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-utils = django-cache-utils:main
    #
    # Would create an entry point called django-cache-utils that calls django-cache-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cache_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-cache-utils/
  license: MIT License
  summary: ' Caching decorator and django cache backend with advanced invalidation ability and dog-pile effect prevention '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
