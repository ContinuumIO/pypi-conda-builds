package:
  name: devilryrestfullib
  version: "0.3"

source:
  fn: devilryrestfullib-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/devilryrestfullib/devilryrestfullib-0.3.tar.gz
  md5: 8194a6d8bbfa0f5cf7dc62647dbe3c9b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - devilryrestfullib = devilryrestfullib:main
    #
    # Would create an entry point called devilryrestfullib that calls devilryrestfullib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - devilryrestfullib
    - devilryrestfullib.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://devilry.org
  license: BSD
  summary: 'Devilry RESTful client library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
