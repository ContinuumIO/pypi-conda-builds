package:
  name: friendlists
  version: "0.1.3"

source:
  fn: friendlists-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/friendlists/friendlists-0.1.3.tar.gz
  md5: c9b536a0a60692b42bbfa8bd141b1d6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - friendlists = friendlists:main
    #
    # Would create an entry point called friendlists that calls friendlists.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.9
    - requests <1.0
    - fboauth2

  run:
    - python
    - flask ==0.9
    - requests <1.0
    - fboauth2

test:
  # Python imports
  imports:
    - friendlists

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/friendlists
  license: MIT License
  summary: 'Manage your Facebook friendlists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
