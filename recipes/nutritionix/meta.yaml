package:
  name: nutritionix
  version: "0.2"

source:
  fn: nutritionix-0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nutritionix/nutritionix-0.2.tar.gz
  md5: 081701fd0b03d42afc221bff61938e46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nutritionix = nutritionix:main
    #
    # Would create an entry point called nutritionix that calls nutritionix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - nutritionix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leetrout/python-nutritionix/
  license: MIT License
  summary: 'Nutritionix Python API wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
