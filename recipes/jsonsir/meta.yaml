package:
  name: jsonsir
  version: "0.0.2"

source:
  fn: JsonSir-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/J/JsonSir/JsonSir-0.0.2.tar.gz
  md5: 84c96f677b7af7a68e668bc646b7e9c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonsir = jsonsir:main
    #
    # Would create an entry point called jsonsir that calls jsonsir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonsir
    - jsonsir.contrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RussellLuo/jsonsir
  license: MIT
  summary: 'A serializer for JSON-like data in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
