package:
  name: collective.usernamelogger
  version: "1.2"

source:
  fn: collective.usernamelogger-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.usernamelogger/collective.usernamelogger-1.2.zip
  md5: fe8568bb9b99b3d3d59ff06f96c7153e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.usernamelogger = collective.usernamelogger:main
    #
    # Would create an entry point called collective.usernamelogger that calls collective.usernamelogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - collective
    - collective.usernamelogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.usernamelogger
  license: Zope Public License
  summary: 'Log user names when using cookie authentication in Zope/Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
