package:
  name: openerp-project
  version: "7.0.406"

source:
  fn: openerp-project-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-project/openerp-project-7.0.406.tar.gz
  md5: 7ae206e44939178d16c631a230ed486f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-project = openerp-project:main
    #
    # Would create an entry point called openerp-project that calls openerp-project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-base-setup
    - openerp-base-status
    - openerp-product
    - openerp-analytic
    - openerp-board
    - openerp-mail
    - openerp-resource
    - openerp-web-kanban

  run:
    - python
    - openerp-base-setup
    - openerp-base-status
    - openerp-product
    - openerp-analytic
    - openerp-board
    - openerp-mail
    - openerp-resource
    - openerp-web-kanban

test:
  # Python imports
  imports:
    - openerp.addons.project
    - openerp.addons.project.report
    - openerp.addons.project.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openerp.com
  license: GNU Affero General Public License v3
  summary: 'Project Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
