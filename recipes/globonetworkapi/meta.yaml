package:
  name: globonetworkapi
  version: "0.3.1"

source:
  fn: GloboNetworkAPI-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GloboNetworkAPI/GloboNetworkAPI-0.3.1.tar.gz
  md5: a4af0687f26e2f4596936891e9cff81f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - globonetworkapi = globonetworkapi:main
    #
    # Would create an entry point called globonetworkapi that calls globonetworkapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework ==2.4.3
    - requests ==2.4.3

  run:
    - python
    - djangorestframework ==2.4.3
    - requests ==2.4.3

test:
  # Python imports
  imports:
    - globonetworkapi
    - networkapiclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/GloboNetworkAPI-client-python
  license: LICENSE.txt
  summary: 'GloboNetworkAPI Client Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
