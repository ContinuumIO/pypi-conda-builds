package:
  name: infogram
  version: "0.1.1"

source:
  fn: infogram-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/infogram/infogram-0.1.1.tar.gz
  md5: 274155d13f479d45ae74e7e881c6854f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infogram = infogram:main
    #
    # Would create an entry point called infogram that calls infogram.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.8
    - six >=1.6

  run:
    - python
    - requests >=0.8
    - six >=1.6

test:
  # Python imports
  imports:
    - infogram

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gdmachado/infogram-python
  license: Apache Software License
  summary: 'Unofficial Python library for infogr.am API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
