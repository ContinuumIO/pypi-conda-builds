package:
  name: django-elasticache
  version: "0.0.3"

source:
  fn: django-elasticache-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-elasticache/django-elasticache-0.0.3.tar.gz
  md5: 73bd87eb171f62f7bac30f607ccead50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-elasticache = django-elasticache:main
    #
    # Would create an entry point called django-elasticache that calls django-elasticache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylibmc
    - django >=1.3

  run:
    - python
    - pylibmc
    - django >=1.3

test:
  # Python imports
  imports:
    - django_elasticache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gusdan/django-elasticache
  license: BSD License
  summary: 'Django cache backend for Amazon ElastiCache (memcached)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
