package:
  name: hgpasteapp
  version: "0.1.3"

source:
  fn: hgpasteapp-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hgpasteapp/hgpasteapp-0.1.3.tar.gz
  md5: 5371be85703b9f8fe1656aa31286109e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgpasteapp = hgpasteapp:main
    #
    # Would create an entry point called hgpasteapp that calls hgpasteapp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial
    - pastedeploy

  run:
    - python
    - setuptools
    - mercurial
    - pastedeploy

test:
  # Python imports
  imports:
    - hgpasteapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Simple wrapper to hgweb that has paster app_factory entrypoints.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
