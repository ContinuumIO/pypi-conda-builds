package:
  name: raptus.pfg.tabs
  version: "1.0a2"

source:
  fn: raptus.pfg.tabs-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.pfg.tabs/raptus.pfg.tabs-1.0a2.tar.gz
  md5: 4676783dc6cc9ae4d6c9449183fe9941
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.pfg.tabs = raptus.pfg.tabs:main
    #
    # Would create an entry point called raptus.pfg.tabs that calls raptus.pfg.tabs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen

test:
  # Python imports
  imports:
    - raptus
    - raptus.pfg
    - raptus.pfg.tabs
    - raptus.pfg.tabs.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://raptus.com
  license: GPL
  summary: 'Provides a tabbed view of the fieldsets of a form created through PloneFormGen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
