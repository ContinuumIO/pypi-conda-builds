package:
  name: insteonic
  version: "0.5"

source:
  fn: insteonic-0.5.tar.gz
  url: https://pypi.python.org/packages/source/i/insteonic/insteonic-0.5.tar.gz
  md5: acc56bd23767bec797efdcedf2d7d7e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - insteonic = insteonic:main
    #
    # Would create an entry point called insteonic that calls insteonic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.2.1
    - requests ==1.2.3

  run:
    - python
    - beautifulsoup4 ==4.2.1
    - requests ==1.2.3

test:
  # Python imports
  imports:
    - insteonic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/an_example_pypi_project
  license: BSD
  summary: "A Python command-line interface for Insteon's smartlinc controller"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
