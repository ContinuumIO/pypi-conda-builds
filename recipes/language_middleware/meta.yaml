package:
  name: language_middleware
  version: "0.3"

source:
  fn: language_middleware-0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/language_middleware/language_middleware-0.3.tar.gz
  md5: e4a14f5e46e79e8ba3ee1b1a7d9a7e4f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - language_middleware = language_middleware:main
    #
    # Would create an entry point called language_middleware that calls language_middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - language_middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dperezrada/wsgi-language-middleware
  license: GNU General Public License (GPL)
  summary: "This Wsgi middleware, takes the language from urls similiar to '/es/documents' or from the Accept language header."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
