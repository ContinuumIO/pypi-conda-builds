package:
  name: apianalytics
  version: "1.0.2"

source:
  fn: apianalytics-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/apianalytics/apianalytics-1.0.2.tar.gz
  md5: b44236961ecc0a3c21b8cb65029798d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apianalytics = apianalytics:main
    #
    # Would create an entry point called apianalytics that calls apianalytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz >=2015.2
    - pyzmq >=14.5.0
    - six >=1.9.0

  run:
    - python
    - pytz >=2015.2
    - pyzmq >=14.5.0
    - six >=1.9.0

test:
  # Python imports
  imports:
    - apianalytics
    - apianalytics.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Mashape/analytics-agent-python
  license: MIT License
  summary: 'Python agent for Mashape API Analytics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
