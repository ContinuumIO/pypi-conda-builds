package:
  name: flask-enterprise
  version: "1.0"

source:
  fn: Flask-Enterprise-1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Enterprise/Flask-Enterprise-1.0.tar.gz
  md5: 1d2636ddf33a77321a34ef2f29e5a995
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-enterprise = flask-enterprise:main
    #
    # Would create an entry point called flask-enterprise that calls flask-enterprise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.3
    - soaplib
    - suds

  run:
    - python
    - flask >=0.3
    - soaplib
    - suds

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://massive.immersedcode.org/2011/staging/projects/default/python/flask-enterprise/
  license: CDDL
  summary: 'Enterprise capabilities for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
