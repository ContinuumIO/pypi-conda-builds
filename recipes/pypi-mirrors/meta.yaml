package:
  name: pypi-mirrors
  version: "0.0.5"

source:
  fn: pypi-mirrors-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pypi-mirrors/pypi-mirrors-0.0.5.tar.gz
  md5: 8c241d7b275ac7d625f032b7302aee87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypi-mirrors = pypi-mirrors:main
    #
    # Would create an entry point called pypi-mirrors that calls pypi-mirrors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pypimirrors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kencochrane/pypi-mirrors-lib
  license: MIT
  summary: 'pypi mirror status library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
