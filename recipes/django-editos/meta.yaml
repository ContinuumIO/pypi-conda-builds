package:
  name: django-editos
  version: "1.3"

source:
  fn: django-editos-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-editos/django-editos-1.3.tar.gz
  md5: 0a483aa738a799284f934775bb2cde53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-editos = django-editos:main
    #
    # Would create an entry point called django-editos that calls django-editos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - geelweb
    - geelweb.django
    - geelweb.django.editos
    - geelweb.django.editos.migrations
    - geelweb.django.editos.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/geelweb/django-editos
  license: MIT
  summary: 'Django app to manage and display editos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
