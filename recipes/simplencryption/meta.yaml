package:
  name: simplencryption
  version: "1.0.0"

source:
  fn: simplencryption-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simplencryption/simplencryption-1.0.0.tar.gz
  md5: f480b0b5074477e8963d2542eb98814f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplencryption = simplencryption:main
    #
    # Would create an entry point called simplencryption that calls simplencryption.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simplencryption

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: nil
  license: UNKNOWN
  summary: 'this is a simple programe to secure your information by encrypting it'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
