package:
  name: django_conventions
  version: "0.2.0"

source:
  fn: django_conventions-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_conventions/django_conventions-0.2.0.tar.gz
  md5: 528a8988bd6b0662115547d272f4ce3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_conventions = django_conventions:main
    #
    # Would create an entry point called django_conventions that calls django_conventions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_conventions
    - django_conventions.rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmg/django_conventions
  license: GPL v3
  summary: 'Django Convention Over Configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
