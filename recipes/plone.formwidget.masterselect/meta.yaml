package:
  name: plone.formwidget.masterselect
  version: "1.4.1"

source:
  fn: plone.formwidget.masterselect-1.4.1.zip
  url: https://pypi.python.org/packages/source/p/plone.formwidget.masterselect/plone.formwidget.masterselect-1.4.1.zip
  md5: 0e04abcef90100b3518fd3eef91ade74
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.formwidget.masterselect = plone.formwidget.masterselect:main
    #
    # Would create an entry point called plone.formwidget.masterselect that calls plone.formwidget.masterselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.form
    - setuptools
    - plone.supermodel
    - plone.z3cform
    - simplejson

  run:
    - python
    - setuptools
    - z3c.form
    - setuptools
    - plone.supermodel
    - plone.z3cform
    - simplejson

test:
  # Python imports
  imports:
    - plone
    - plone.formwidget
    - plone.formwidget.masterselect
    - plone.formwidget.masterselect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.formwidget.masterselect
  license: GPL
  summary: 'A z3c.form widget that controls the vocabulary or display of other fields on an edit page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
