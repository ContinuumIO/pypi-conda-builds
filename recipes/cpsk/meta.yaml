package:
  name: cpsk
  version: "0.0.9"

source:
  fn: cpsk-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/cpsk/cpsk-0.0.9.tar.gz
  md5: 41974b8feeba5eb3287defd6cf8dbc3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cpsk = cpsk:main
    #
    # Would create an entry point called cpsk that calls cpsk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - lxml

  run:
    - python
    - requests
    - lxml

test:
  # Python imports
  imports:
    - cpsk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Adman/python-cpsk-api
  license: The MIT License (MIT)
  summary: 'an unofficial api for grabbing data from cp.sk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
