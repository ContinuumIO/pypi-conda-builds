package:
  name: django-feedback-form2
  version: "0.1.7.1"

source:
  fn: django-feedback-form2-0.1.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-feedback-form2/django-feedback-form2-0.1.7.1.tar.gz
  md5: 3fb56063ee7412c2ad9c5ceaabfdf823
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-feedback-form2 = django-feedback-form2:main
    #
    # Would create an entry point called django-feedback-form2 that calls django-feedback-form2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - akismet >=0.2.0
    - django-contact-form >=1.0

  run:
    - python
    - django >=1.5
    - akismet >=0.2.0
    - django-contact-form >=1.0

test:
  # Python imports
  imports:
    - feedback_form

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bashu/django-feedback-form
  license: BSD License
  summary: 'Put short description here...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
