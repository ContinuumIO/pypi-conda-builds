package:
  name: miniature-ironman
  version: "0.0.4"

source:
  fn: miniature-ironman-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/miniature-ironman/miniature-ironman-0.0.4.tar.gz
  md5: 27fc9e2e973c401c5cfb9c7cfbdf59a7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - miniature-ironman = miniature-ironman:main
    #
    # Would create an entry point called miniature-ironman that calls miniature-ironman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytils
    - funcy

  run:
    - python
    - setuptools
    - pytils
    - funcy

test:
  # Python imports
  imports:
    - miniature

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ir4y/miniature-ironman
  license: BSD License
  summary: 'My code snippets set for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
