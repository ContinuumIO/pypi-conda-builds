package:
  name: django-preserialize
  version: "1.1.0"

source:
  fn: django-preserialize-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-preserialize/django-preserialize-1.1.0.tar.gz
  md5: 73b7f7df817d96bf14bbcfb0bc40b012
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-preserialize = django-preserialize:main
    #
    # Would create an entry point called django-preserialize that calls django-preserialize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - preserialize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bruth/django-preserialize/
  license: BSD License
  summary: 'Pre-serialize model instances and querysets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
