package:
  name: wtforms-appengine
  version: "0.1"

source:
  fn: WTForms-Appengine-0.1.zip
  url: https://pypi.python.org/packages/source/W/WTForms-Appengine/WTForms-Appengine-0.1.zip
  md5: 779d62eacd1962b153cfd9b28c7a8697
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-appengine = wtforms-appengine:main
    #
    # Would create an entry point called wtforms-appengine that calls wtforms-appengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms >=1.0.5

  run:
    - python
    - wtforms >=1.0.5

test:
  # Python imports
  imports:
    - wtforms_appengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wtforms/wtforms-appengine/
  license: BSD License
  summary: 'Appengine tools for WTForms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
