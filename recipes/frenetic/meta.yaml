package:
  name: frenetic
  version: "3.4.1"

source:
  fn: frenetic-3.4.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frenetic/frenetic-3.4.1.tar.gz
  md5: 4fe3dde80919e689ed1152a2ad97a23a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frenetic = frenetic:main
    #
    # Would create an entry point called frenetic that calls frenetic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - frenetic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frenetic-lang/frenetic
  license: UNKNOWN
  summary: 'Python bindings for Frenetic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
