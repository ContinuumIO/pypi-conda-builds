package:
  name: collective.slideshare
  version: "0.3"

source:
  fn: collective.slideshare-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.slideshare/collective.slideshare-0.3.tar.gz
  md5: b9bb284c1fbbab5ce0717e62a4fe8827
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.slideshare = collective.slideshare:main
    #
    # Would create an entry point called collective.slideshare that calls collective.slideshare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - slideshare

  run:
    - python
    - setuptools
    - setuptools
    - slideshare

test:
  # Python imports
  imports:
    - collective
    - collective.slideshare
    - collective.slideshare.browser
    - collective.slideshare.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.slideshare
  license: GNU General Public License (GPL)
  summary: 'Easily publish your presentations on slideshare'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
