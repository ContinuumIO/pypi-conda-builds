package:
  name: battlenet
  version: "0.2.6"

source:
  fn: battlenet-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/b/battlenet/battlenet-0.2.6.tar.gz
  md5: 3113c17554ae7d976a2cdc2e1da944b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - battlenet = battlenet:main
    #
    # Would create an entry point called battlenet that calls battlenet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - battlenet
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vishnevskiy/battlenet
  license: MIT License
  summary: "Python Library for Blizzard's Community Platform API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
