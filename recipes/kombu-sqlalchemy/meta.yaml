package:
  name: kombu-sqlalchemy
  version: "1.1.0"

source:
  fn: kombu-sqlalchemy-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kombu-sqlalchemy/kombu-sqlalchemy-1.1.0.tar.gz
  md5: 5dd7addeb7639d084e40cc83ab48a05e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kombu-sqlalchemy = kombu-sqlalchemy:main
    #
    # Would create an entry point called kombu-sqlalchemy that calls kombu-sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - kombu

  run:
    - python
    - sqlalchemy
    - kombu

test:
  # Python imports
  imports:
    - sqlakombu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ask/kombu-sqlalchemy/
  license: BSD
  summary: 'Kombu transport using SQLAlchemy as the message store.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
