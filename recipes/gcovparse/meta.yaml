package:
  name: gcovparse
  version: "0.0.3"

source:
  fn: gcovparse-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gcovparse/gcovparse-0.0.3.tar.gz
  md5: 192be87fb2db59305e76da0d58770a42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gcovparse = gcovparse:main
    #
    # Would create an entry point called gcovparse that calls gcovparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gcovparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codecov/gcov-parse
  license: Apache Software License
  summary: 'gcov to json'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
