package:
  name: helga-reviews
  version: "0.0.6"

source:
  fn: helga-reviews-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-reviews/helga-reviews-0.0.6.tar.gz
  md5: 85dce8b3e48e285631fe816611d5b42d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-reviews = helga-reviews:main
    #
    # Would create an entry point called helga-reviews that calls helga-reviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rbtools

  run:
    - python
    - setuptools
    - rbtools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-reviews
  license: MIT License
  summary: 'A helga command to query reviewboard pending reviews targeted at a user, groupor channel (using a channel-to-group mapping).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
