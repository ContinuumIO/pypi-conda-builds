package:
  name: django-basic-models-behaviors
  version: "0.5.0"

source:
  fn: django-basic-models-behaviors-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-basic-models-behaviors/django-basic-models-behaviors-0.5.0.tar.gz
  md5: d95a1e96adc0d4d1cf5ab43ee9958cd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-basic-models-behaviors = django-basic-models-behaviors:main
    #
    # Would create an entry point called django-basic-models-behaviors that calls django-basic-models-behaviors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - basic_models_behaviors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/duboisnicolas/django-basic-models-behaviors
  license: MIT License
  summary: 'Tiny app to provide basic behaviors for django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
