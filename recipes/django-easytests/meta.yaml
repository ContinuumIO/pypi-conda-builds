package:
  name: django-easytests
  version: "0.9.4"

source:
  fn: django-easytests-0.9.4.zip
  url: https://pypi.python.org/packages/source/d/django-easytests/django-easytests-0.9.4.zip
  md5: 607dff8da1bb2ea1aa058f663a128515
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easytests = django-easytests:main
    #
    # Would create an entry point called django-easytests that calls django-easytests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - dj-database-url

  run:
    - python
    - docopt
    - dj-database-url

test:
  # Python imports
  imports:
    - djeasytests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fivethreeo/django-easytests/
  license: BSD License
  summary: 'Modified test utils from django-cms as a own module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
