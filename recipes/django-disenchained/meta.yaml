package:
  name: django-disenchained
  version: "0.8"

source:
  fn: django-disenchained-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-disenchained/django-disenchained-0.8.tar.gz
  md5: 49febb319265c3a89300bbe3b8e5c86c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-disenchained = django-disenchained:main
    #
    # Would create an entry point called django-disenchained that calls django-disenchained.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-debug-toolbar >=0.9.4

  run:
    - python
    - django-debug-toolbar >=0.9.4

test:
  # Python imports
  imports:
    - disenchained

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/abhik/django-disenchained
  license: BSD License
  summary: 'django-disenchained: free Django from the shackles of inefficient queries!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
