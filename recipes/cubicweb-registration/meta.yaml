package:
  name: cubicweb-registration
  version: "0.6.1"

source:
  fn: cubicweb-registration-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-registration/cubicweb-registration-0.6.1.tar.gz
  md5: 7e8edd4b8ceaeab346c6760987da59da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-registration = cubicweb-registration:main
    #
    # Would create an entry point called cubicweb-registration that calls cubicweb-registration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0
    - pillow

  run:
    - python
    - cubicweb >=3.19.0
    - pillow

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-registration
  license: LGPL
  summary: 'public registration component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
