package:
  name: infi.dtypes.wwn
  version: "0.1"

source:
  fn: infi.dtypes.wwn-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.dtypes.wwn/infi.dtypes.wwn-0.1.tar.gz
  md5: b843b3b80ce6e8159ee48b119e6dd95c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.dtypes.wwn = infi.dtypes.wwn:main
    #
    # Would create an entry point called infi.dtypes.wwn that calls infi.dtypes.wwn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - infi
    - infi.dtypes
    - infi.dtypes.wwn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.dtypes.wwn
  license: Python Software Foundation License
  summary: 'Datatype for WWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
