package:
  name: iskra.audiofile
  version: "1.0"

source:
  fn: iskra.audiofile-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/iskra.audiofile/iskra.audiofile-1.0.tar.gz
  md5: 3dcfb7eca3dd284725db036083665b7c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iskra.audiofile = iskra.audiofile:main
    #
    # Would create an entry point called iskra.audiofile that calls iskra.audiofile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.namedfile
    - plone.app.multilingual
    - plone.multilingualbehavior
    - hachoir_parser
    - hachoir_metadata

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.namedfile
    - plone.app.multilingual
    - plone.multilingualbehavior
    - hachoir_parser
    - hachoir_metadata

test:
  # Python imports
  imports:
    - iskra
    - iskra.audiofile
    - iskra.audiofile.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iskracat/Hitotsubashi-University/tree/master/buildout/src/iskra.audiofile
  license: gpl
  summary: 'Audio Dexterity mediaelementjs player'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
