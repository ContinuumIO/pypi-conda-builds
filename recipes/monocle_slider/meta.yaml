package:
  name: monocle_slider
  version: "0.1.1"

source:
  fn: monocle_slider-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle_slider/monocle_slider-0.1.1.tar.gz
  md5: 26f3b271b8b7ad96dc4f7a2e81fab219
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle_slider = monocle_slider:main
    #
    # Would create an entry point called monocle_slider that calls monocle_slider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monocle_slider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Slider app for monocle project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
