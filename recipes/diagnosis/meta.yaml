package:
  name: diagnosis
  version: "2.0.3"

source:
  fn: diagnosis-2.0.3.zip
  url: https://pypi.python.org/packages/source/d/diagnosis/diagnosis-2.0.3.zip
  md5: 02819df797a81ece5cab46aff8d52db5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diagnosis = diagnosis:main
    #
    # Would create an entry point called diagnosis that calls diagnosis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - diagnosis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hi.baidu.com/huangyif
  license: UNKNOWN
  summary: 'This is a Class that provide people automation run windows program.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
