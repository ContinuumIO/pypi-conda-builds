package:
  name: django-xadmin
  version: "0.5.0"

source:
  fn: django-xadmin-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xadmin/django-xadmin-0.5.0.tar.gz
  md5: 7cce47b0875a101cd35d7bedfc452872
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xadmin = django-xadmin:main
    #
    # Would create an entry point called django-xadmin that calls django-xadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-crispy-forms >=1.4.0

  run:
    - python
    - setuptools
    - django >=1.5
    - django-crispy-forms >=1.4.0

test:
  # Python imports
  imports:
    - xadmin
    - xadmin.plugins
    - xadmin.templatetags
    - xadmin.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.xadmin.io
  license: BSD License
  summary: 'Drop-in replacement of Django admin comes with lots of goodies, fully extensible with plugin support, pretty UI based on Twitter Bootstrap.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
