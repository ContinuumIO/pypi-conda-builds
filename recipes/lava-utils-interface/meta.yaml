package:
  name: lava-utils-interface
  version: "1.0"

source:
  fn: lava-utils-interface-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-utils-interface/lava-utils-interface-1.0.tar.gz
  md5: a477b25a3806cc81aa282a3f8f579a5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-utils-interface = lava-utils-interface:main
    #
    # Would create an entry point called lava-utils-interface that calls lava-utils-interface.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lava
    - lava.utils
    - lava.utils.interface

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lava-utils-interface
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Common interface class for LAVA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
