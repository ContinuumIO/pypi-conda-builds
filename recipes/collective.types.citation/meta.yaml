package:
  name: collective.types.citation
  version: "9.06.01"

source:
  fn: collective.types.citation-9.06.01.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.types.citation/collective.types.citation-9.06.01.tar.gz
  md5: e50e2d8924d165123ba252f1eeee2835
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.types.citation = collective.types.citation:main
    #
    # Would create an entry point called collective.types.citation that calls collective.types.citation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.types
    - collective.types.citation
    - collective.types.citation.browser
    - collective.types.citation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cphi.washington.edu
  license: GPL 2
  summary: 'Citation content type'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
