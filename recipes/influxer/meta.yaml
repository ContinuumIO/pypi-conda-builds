package:
  name: influxer
  version: "1.0.10"

source:
  fn: influxer-1.0.10.tar.gz
  url: https://pypi.python.org/packages/source/i/influxer/influxer-1.0.10.tar.gz
  md5: 769b4d45cc5d4e00c0f87f440f7fde16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - influxer = influxer:main
    #
    # Would create an entry point called influxer that calls influxer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gevent ==1.0.1
    - influxdb ==0.1.13
    - pylibmc ==1.4.2

  run:
    - python
    - gevent ==1.0.1
    - influxdb ==0.1.13
    - pylibmc ==1.4.2

test:
  # Python imports
  imports:
    - influxer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theonion/influxer
  license: UNKNOWN
  summary: 'Read and Write Event Data to InfluxDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
