package:
  name: redisqueue
  version: "0.1.5"

source:
  fn: redisqueue-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/r/redisqueue/redisqueue-0.1.5.tar.gz
  md5: e263fa522b753426a45d2fdb61f35fcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redisqueue = redisqueue:main
    #
    # Would create an entry point called redisqueue that calls redisqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redisqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DevKeh/redisqueue
  license: MIT License
  summary: 'Simple Redis Queue Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
