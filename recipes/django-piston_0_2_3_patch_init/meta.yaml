package:
  name: django-piston_0_2_3_patch_init
  version: "0.2.3"

source:
  fn: django-piston_0_2_3_patch_init-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-piston_0_2_3_patch_init/django-piston_0_2_3_patch_init-0.2.3.tar.gz
  md5: 82b127b6a629011f6b9d29759f790b90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-piston_0_2_3_patch_init = django-piston_0_2_3_patch_init:main
    #
    # Would create an entry point called django-piston_0_2_3_patch_init that calls django-piston_0_2_3_patch_init.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - piston

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Piston omits __init__.py in its pkg, so pip installs dont work unless you add a patch like this.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
