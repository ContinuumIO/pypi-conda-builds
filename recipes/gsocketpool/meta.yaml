package:
  name: gsocketpool
  version: "0.1.3"

source:
  fn: gsocketpool-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gsocketpool/gsocketpool-0.1.3.tar.gz
  md5: 9df1af201ef1822d31e2bf380e59d846
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gsocketpool = gsocketpool:main
    #
    # Would create an entry point called gsocketpool that calls gsocketpool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent

  run:
    - python
    - gevent

test:
  # Python imports
  imports:
    - gsocketpool
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/studio-ousia/gsocketpool
  license: Apache Software License
  summary: 'A simple connection pool for gevent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
