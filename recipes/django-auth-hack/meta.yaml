package:
  name: django-auth-hack
  version: "0.1.1"

source:
  fn: django-auth-hack-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-hack/django-auth-hack-0.1.1.tar.gz
  md5: ef27b614ea11bd82bc16cb5e08d0e4e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-hack = django-auth-hack:main
    #
    # Would create an entry point called django-auth-hack that calls django-auth-hack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1

  run:
    - python
    - django >=1.3.1

test:
  # Python imports
  imports:
    - auth_hacks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: "Hacking Django's contrib auth app to support longer usernames"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
