package:
  name: bedserver
  version: "0.1"

source:
  fn: bedserver-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bedserver/bedserver-0.1.tar.gz
  md5: b291e9ae5f65ec73d0858af961efd62c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bedserver = bedserver:main
    #
    # Would create an entry point called bedserver that calls bedserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysam
    - tornado
    - flask

  run:
    - python
    - pysam
    - tornado
    - flask

test:
  # Python imports
  imports:
    - bedserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/drio/bedserver
  license: MIT License
  summary: 'A fast and simple bedfile server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
