package:
  name: dxskytap
  version: "1.0.0"

source:
  fn: dxskytap-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dxskytap/dxskytap-1.0.0.tar.gz
  md5: c6e764a937b22f04fa34b263bc9cb414
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dxskytap = dxskytap:main
    #
    # Would create an entry point called dxskytap that calls dxskytap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.8
    - pexpect >=2.4

  run:
    - python
    - httplib2 >=0.8
    - pexpect >=2.4

test:
  # Python imports
  imports:
    - dxskytap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dataxu.com
  license: BSD License
  summary: 'DataXu Utilities for Skytap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
