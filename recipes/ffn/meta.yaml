package:
  name: ffn
  version: "0.1.8"

source:
  fn: ffn-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/f/ffn/ffn-0.1.8.tar.gz
  md5: 9437c751aff6da795bb30a80770ea75e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ffn = ffn:main
    #
    # Would create an entry point called ffn that calls ffn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator
    - numpy
    - pandas
    - tabulate
    - matplotlib
    - scikit-learn
    - scipy

  run:
    - python
    - decorator
    - numpy
    - pandas
    - tabulate
    - matplotlib
    - scikit-learn
    - scipy

test:
  # Python imports
  imports:
    - ffn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pmorissette/ffn
  license: ffnet
  summary: 'Financial functions for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
