package:
  name: django-ratelimit
  version: "0.5.0"

source:
  fn: django-ratelimit-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ratelimit/django-ratelimit-0.5.0.tar.gz
  md5: d925d47e6ba49b18f76dc8e9f9897480
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ratelimit = django-ratelimit:main
    #
    # Would create an entry point called django-ratelimit that calls django-ratelimit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ratelimit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jsocol/django-ratelimit
  license: Apache Software License
  summary: 'Cache-based rate-limiting for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
