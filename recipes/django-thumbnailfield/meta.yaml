package:
  name: django-thumbnailfield
  version: "0.2.5"

source:
  fn: django-thumbnailfield-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-thumbnailfield/django-thumbnailfield-0.2.5.tar.gz
  md5: 6cc7e43da0050d73995148221a61e989
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-thumbnailfield = django-thumbnailfield:main
    #
    # Would create an entry point called django-thumbnailfield that calls django-thumbnailfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - app_version >=0.1.1
    - django-appconf
    - pillow

  run:
    - python
    - app_version >=0.1.1
    - django-appconf
    - pillow

test:
  # Python imports
  imports:
    - thumbnailfield
    - thumbnailfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-thumbnailfield
  license: MIT License
  summary: 'Enhanced ImageField with automatically generate thumbnail of the specified image'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
