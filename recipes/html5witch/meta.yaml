package:
  name: html5witch
  version: "0.2.1"

source:
  fn: html5witch-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/html5witch/html5witch-0.2.1.tar.gz
  md5: d210a1bd16fd34be2db484ba53cf3b4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html5witch = html5witch:main
    #
    # Would create an entry point called html5witch that calls html5witch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmlwitch >=0.2

  run:
    - python
    - xmlwitch >=0.2

test:
  # Python imports
  imports:
    - html5witch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jonasgalvez.com.br/Software/HTML5Witch.html
  license: BSD License
  summary: 'html5witch offers Pythonic HTML5 generation based on xmlwitch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
