package:
  name: django-otp-twilio
  version: "0.3.2"

source:
  fn: django-otp-twilio-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-otp-twilio/django-otp-twilio-0.3.2.tar.gz
  md5: a5e7812d7af911e567f30a314aecb8dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-otp-twilio = django-otp-twilio:main
    #
    # Would create an entry point called django-otp-twilio that calls django-otp-twilio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-otp >=0.3.0
    - requests

  run:
    - python
    - django-otp >=0.3.0
    - requests

test:
  # Python imports
  imports:
    - otp_twilio
    - otp_twilio.migrations
    - otp_twilio.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/psagers/django-otp
  license: BSD License
  summary: "A django-otp plugin that delivers tokens via Twilio's SMS service."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
