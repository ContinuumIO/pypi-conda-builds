package:
  name: tango-contact-manager
  version: "0.7.0"

source:
  fn: tango-contact-manager-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tango-contact-manager/tango-contact-manager-0.7.0.tar.gz
  md5: 54cd8b605bdef32fd05dded065ddbd13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tango-contact-manager = tango-contact-manager:main
    #
    # Would create an entry point called tango-contact-manager that calls tango-contact-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tango-shared-core >=0.9

  run:
    - python
    - tango-shared-core >=0.9

test:
  # Python imports
  imports:
    - contact_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tBaxter/tango-contact-manager
  license: MIT License
  summary: 'Provides contact forms and any other user submission form you might want.\n        Create user submission forms on the fly, straight from the Django admin.\n        '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
