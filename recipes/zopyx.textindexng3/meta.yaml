package:
  name: zopyx.textindexng3
  version: "4.0.1"

source:
  fn: zopyx.textindexng3-4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zopyx.textindexng3/zopyx.textindexng3-4.0.1.tar.gz
  md5: 5cc5881ad5ac90e01e309cab3b92f36b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.textindexng3 = zopyx.textindexng3:main
    #
    # Would create an entry point called zopyx.textindexng3 that calls zopyx.textindexng3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sf.net/projects/textindexng/
  license: UNKNOWN
  summary: 'Helper modules for TextIndexNG3 (Snowball stemmer, normalizer, splitter, etc.)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
