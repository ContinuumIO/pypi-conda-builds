package:
  name: vps-online-keeper
  version: "1.0.3"

source:
  fn: master.zip
  url: https://github.com/likema/vps-online-keeper/archive/master.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vps-online-keeper = vps-online-keeper:main
    #
    # Would create an entry point called vps-online-keeper that calls vps-online-keeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup
    - gevent >=0.13.6
    - requests >=2.0

  run:
    - python
    - beautifulsoup
    - gevent >=0.13.6
    - requests >=2.0

test:
  # Python imports
  imports:
    - vps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/likema/vps-online-keeper
  license: GNU General Public License v3 (GPLv3)
  summary: 'Keep various VPS online'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
