package:
  name: even_detector
  version: "1.0.0"

source:
  fn: even_detector-1.0.0.zip
  url: https://pypi.python.org/packages/source/e/even_detector/even_detector-1.0.0.zip
  md5: 34b0b87d2d9bb886520c329b5890181b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - even_detector = even_detector:main
    #
    # Would create an entry point called even_detector that calls even_detector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - even_detector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: pypi.python.org
  license: UNKNOWN
  summary: 'A simple function which will help to detect that whether the number is an even or not'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
