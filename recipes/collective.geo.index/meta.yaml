package:
  name: collective.geo.index
  version: "0.1"

source:
  fn: collective.geo.index-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.geo.index/collective.geo.index-0.1.tar.gz
  md5: 8b2afbefafd24c61744888c9dc7457c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geo.index = collective.geo.index:main
    #
    # Would create an entry point called collective.geo.index that calls collective.geo.index.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - shapely
    - rtree
    - collective.geo.contentlocations
    - collective.geo.mapwidget
    - collective.geo.kml

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - shapely
    - rtree
    - collective.geo.contentlocations
    - collective.geo.mapwidget
    - collective.geo.kml

test:
  # Python imports
  imports:
    - collective
    - collective.geo
    - collective.geo.index
    - collective.geo.index.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.geo.index/
  license: GNU General Public License (GPL)
  summary: 'spatialindex for collective.geo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
