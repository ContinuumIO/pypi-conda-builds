package:
  name: collagram
  version: "0.8.2"

source:
  fn: collagram-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collagram/collagram-0.8.2.tar.gz
  md5: a391aff1280744eff5900d344bebad36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collagram = collagram:main
    #
    # Would create an entry point called collagram that calls collagram.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - python-instagram

  run:
    - python
    - pillow
    - python-instagram

test:
  # Python imports
  imports:
    - collagram

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vurbmedia/collagram/
  license: ISC
  summary: 'Generate collages of Instagram photographs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
