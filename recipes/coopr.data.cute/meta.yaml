package:
  name: coopr.data.cute
  version: "1.2.1"

source:
  fn: coopr.data.cute-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.data.cute/coopr.data.cute-1.2.1.tar.gz
  md5: 7efc86d843ef2f8c61f7cbeb50af9398
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.data.cute = coopr.data.cute:main
    #
    # Would create an entry point called coopr.data.cute that calls coopr.data.cute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.data
    - coopr.data.cute

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.data.cute
  license: BSD License
  summary: 'Coopr data project:  CUTE test data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
