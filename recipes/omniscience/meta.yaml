package:
  name: omniscience
  version: "0.1dev"

source:
  fn: omniscience-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/o/omniscience/omniscience-0.1dev.tar.gz
  md5: bcbd88b8ab2c2b1ca724d804b09039e5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omniscience = omniscience:main
    #
    # Would create an entry point called omniscience that calls omniscience.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - omniscience

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: TBD
  summary: 'Enables dynamic real-time monitoring/logging of code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
