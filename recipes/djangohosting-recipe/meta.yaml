package:
  name: djangohosting-recipe
  version: "0.1.0a4"

source:
  fn: djangohosting-recipe-0.1.0a4.tar.gz
  url: https://pypi.python.org/packages/source/d/djangohosting-recipe/djangohosting-recipe-0.1.0a4.tar.gz
  md5: 3631f5c54929162988d0cd75d09a5a1a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangohosting-recipe = djangohosting-recipe:main
    #
    # Would create an entry point called djangohosting-recipe that calls djangohosting-recipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - djangorecipe

  run:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - djangorecipe

test:
  # Python imports
  imports:
    - djangohosting
    - djangohosting.recipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/djangohosting-recipe/
  license: BSD License
  summary: 'Buildout recipe for Djangohosting.ch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
