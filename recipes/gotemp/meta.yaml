package:
  name: gotemp
  version: "1.0.1"

source:
  fn: gotemp-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gotemp/gotemp-1.0.1.tar.gz
  md5: d0e1245a0b3930c1a38cb9397126a385
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gotemp = gotemp:main
    #
    # Would create an entry point called gotemp that calls gotemp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gotemp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/briancline/gotemp-python
  license: BSD License
  summary: 'A library for interacting with GoTemp! temperature sensors.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
