package:
  name: lovely.remotetask
  version: "0.5.2"

source:
  fn: lovely.remotetask-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.remotetask/lovely.remotetask-0.5.2.tar.gz
  md5: 2e7f788bbb7afd841d985a51c27a42de
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.remotetask = lovely.remotetask:main
    #
    # Would create an entry point called lovely.remotetask that calls lovely.remotetask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zc.queue
    - zc.table
    - transaction
    - zope.browserpage
    - zope.configuration
    - zope.location
    - zope.session
    - zope.app.container
    - zope.app.appsetup
    - zope.app.form
    - zope.app.generations
    - zope.app.pagetemplate
    - zope.app.publication
    - zope.app.publisher
    - zope.app.session
    - zope.app.xmlrpcintrospection
    - zope.component
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing

  run:
    - python
    - setuptools
    - zodb3
    - zc.queue
    - zc.table
    - transaction
    - zope.browserpage
    - zope.configuration
    - zope.location
    - zope.session
    - zope.app.container
    - zope.app.appsetup
    - zope.app.form
    - zope.app.generations
    - zope.app.pagetemplate
    - zope.app.publication
    - zope.app.publisher
    - zope.app.session
    - zope.app.xmlrpcintrospection
    - zope.component
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - lovely
    - lovely.remotetask
    - lovely.remotetask.browser
    - lovely.remotetask.generations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/lovely.remotetask
  license: ZPL 2.1
  summary: 'A remotetask client utiltiy for zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
