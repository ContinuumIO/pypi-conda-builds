package:
  name: django-pummel
  version: "0.0.8"

source:
  fn: django-pummel-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pummel/django-pummel-0.0.8.tar.gz
  md5: 8c3a29b0d1f6c0e2d1e46009430e6c6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pummel = django-pummel:main
    #
    # Would create an entry point called django-pummel that calls django-pummel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5
    - lxml
    - pytidylib
    - beautifulsoup >=3.0.8.1

  run:
    - python
    - django <1.5
    - lxml
    - pytidylib
    - beautifulsoup >=3.0.8.1

test:
  # Python imports
  imports:
    - pml
    - pml.forms
    - pml.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-pummel
  license: Proprietary
  summary: 'Django PML template tags and utils (djang-pml was taken, okay!)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
