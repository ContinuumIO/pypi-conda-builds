package:
  name: django-project-templates
  version: "0.11"

source:
  fn: django-project-templates-0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-project-templates/django-project-templates-0.11.tar.gz
  md5: 06cc226c3881ca27551588143570ea73
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-project-templates = django-project-templates:main
    #
    # Would create an entry point called django-project-templates that calls django-project-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript >=1.3
    - cheetah
    - fabric

  run:
    - python
    - setuptools
    - setuptools
    - pastescript >=1.3
    - cheetah
    - fabric

test:
  # Python imports
  imports:
    - django_project_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/garethr/django-project-templates/
  license: MIT License
  summary: 'Paster templates for creating Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
