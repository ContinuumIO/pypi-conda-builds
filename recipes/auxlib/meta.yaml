package:
  name: auxlib
  version: "0.0.4"

source:
  fn: auxlib-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/auxlib/auxlib-0.0.4.tar.gz
  md5: 288cac97219228f12c9cbce38957107f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auxlib = auxlib:main
    #
    # Would create an entry point called auxlib that calls auxlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - enum34
    - pycrypto
    - python-dateutil
    - pyyaml

  run:
    - python
    - enum34
    - pycrypto
    - python-dateutil
    - pyyaml

test:
  # Python imports
  imports:
    - auxlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kalefranz/auxlib
  license: ISC
  summary: 'auxiliary library to the python standard library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
