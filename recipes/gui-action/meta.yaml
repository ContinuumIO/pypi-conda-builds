package:
  name: gui-action
  version: "1.0"

source:
  fn: gui-action-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gui-action/gui-action-1.0.tar.gz
  md5: 11ad17def5b960ad953e2ad85966312d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gui-action = gui-action:main
    #
    # Would create an entry point called gui-action that calls gui-action.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ally-api >=1.0

  run:
    - python
    - ally-api >=1.0

test:
  # Python imports
  imports:
    - __plugin__
    - __plugin__.gui_action
    - gui
    - gui.action
    - gui.action.api
    - gui.action.core
    - gui.action.core.impl
    - gui.action.impl
    - gui.action.meta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sourcefabric.org/en/superdesk/
  license: GPL v3
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
