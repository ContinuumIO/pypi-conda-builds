package:
  name: python-linkfire
  version: "0.2"

source:
  fn: python-linkfire-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-linkfire/python-linkfire-0.2.tar.gz
  md5: 4d4e9f37e80c23a2756832400e87bc39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-linkfire = python-linkfire:main
    #
    # Would create an entry point called python-linkfire that calls python-linkfire.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - linkfire

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/koalalorenzo/python-linkfire
  license: UNKNOWN
  summary: 'Python linkfire.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
