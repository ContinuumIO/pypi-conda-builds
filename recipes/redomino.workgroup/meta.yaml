package:
  name: redomino.workgroup
  version: "0.1r3"

source:
  fn: redomino.workgroup-0.1r3.tar.gz
  url: https://pypi.python.org/packages/source/r/redomino.workgroup/redomino.workgroup-0.1r3.tar.gz
  md5: 01d9a7744347ed16f314864fcb3adab8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.workgroup = redomino.workgroup:main
    #
    # Would create an entry point called redomino.workgroup that calls redomino.workgroup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer

  run:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer

test:
  # Python imports
  imports:
    - redomino
    - redomino.workgroup
    - redomino.workgroup.Extensions
    - redomino.workgroup.browser
    - redomino.workgroup.content
    - redomino.workgroup.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.redomino.com/it/labs/progetti/redomino-workgroup
  license: GPL
  summary: 'This product provides a special workgroup mode for the normal Plone Folder to permit the creation of local users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
