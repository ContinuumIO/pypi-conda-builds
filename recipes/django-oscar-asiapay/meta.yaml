package:
  name: django-oscar-asiapay
  version: "0.3.9"

source:
  fn: django-oscar-asiapay-0.3.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oscar-asiapay/django-oscar-asiapay-0.3.9.tar.gz
  md5: 263790d8d08544296dc711e0a09c5a7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oscar-asiapay = django-oscar-asiapay:main
    #
    # Would create an entry point called django-oscar-asiapay that calls django-oscar-asiapay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-oscar
    - django-localflavor
    - requests >=1.0

  run:
    - python
    - django
    - django-oscar
    - django-localflavor
    - requests >=1.0

test:
  # Python imports
  imports:
    - asiapay
    - asiapay.migrations
    - asiapay.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-oscar-asiapay
  license: The MIT License
  summary: 'Payment integration of AsiaPay (PayDollar) to use on django-oscar\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
