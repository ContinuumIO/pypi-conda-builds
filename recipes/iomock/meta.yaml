package:
  name: iomock
  version: "0.1"

source:
  fn: iomock-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iomock/iomock-0.1.tar.gz
  md5: 13d842a2f989922034601233c826590e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iomock = iomock:main
    #
    # Would create an entry point called iomock that calls iomock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iomock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MarSik/python-iomock
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Python module which provides mock classes that emulate file access methods in os, os.path, glob.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
