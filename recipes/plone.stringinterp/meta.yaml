package:
  name: plone.stringinterp
  version: "1.0.13"

source:
  fn: plone.stringinterp-1.0.13.zip
  url: https://pypi.python.org/packages/source/p/plone.stringinterp/plone.stringinterp-1.0.13.zip
  md5: ba71a99eaef5de46bdc6b8b9e85ba318
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.stringinterp = plone.stringinterp:main
    #
    # Would create an entry point called plone.stringinterp that calls plone.stringinterp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - zope.i18n

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - zope.i18n

test:
  # Python imports
  imports:
    - plone
    - plone.stringinterp
    - plone.stringinterp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.stringinterp
  license: GNU General Public License (GPL)
  summary: 'Adaptable string interpolation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
