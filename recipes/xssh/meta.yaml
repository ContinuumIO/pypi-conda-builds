package:
  name: xssh
  version: "0.4"

source:
  fn: xssh-0.4.tar.gz
  url: https://pypi.python.org/packages/source/x/xssh/xssh-0.4.tar.gz
  md5: 77329b94e76ec77fa4874d93abf633cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xssh = xssh:main
    #
    # Would create an entry point called xssh that calls xssh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse >=1.2.1
    - crypto >=1.1.0
    - paramiko >=1.11.0
    - python-hostlist >=1.14

  run:
    - python
    - argparse >=1.2.1
    - crypto >=1.1.0
    - paramiko >=1.11.0
    - python-hostlist >=1.14

test:
  # Python imports
  imports:
    - xssh

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/xssh/
  license: LICENSE.txt
  summary: 'SSH Multiplexer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
