package:
  name: j01.validate
  version: "0.5.1"

source:
  fn: j01.validate-0.5.1.zip
  url: https://pypi.python.org/packages/source/j/j01.validate/j01.validate-0.5.1.zip
  md5: 4eebdcc96398929d795a4e2ce6a60d71
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.validate = j01.validate:main
    #
    # Would create an entry point called j01.validate that calls j01.validate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.form
    - z3c.jsonrpc
    - zope.component
    - zope.interface
    - zope.publisher
    - zope.viewlet

  run:
    - python
    - setuptools
    - z3c.form
    - z3c.jsonrpc
    - zope.component
    - zope.interface
    - zope.publisher
    - zope.viewlet

test:
  # Python imports
  imports:
    - j01
    - j01.validate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.validate
  license: Zope Public License
  summary: 'JQuery beasd validation framework for z3c.form based widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
