package:
  name: collective.js.galleryview
  version: "0.5.1"

source:
  fn: collective.js.galleryview-0.5.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.galleryview/collective.js.galleryview-0.5.1.zip
  md5: a5400f34de5e002b31e9c805fbc8d485
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.galleryview = collective.js.galleryview:main
    #
    # Would create an entry point called collective.js.galleryview that calls collective.js.galleryview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.galleryview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.js.galleryview
  license: MIT
  summary: 'GalleryView aims to provide jQuery users with a flexible, attractive content gallery that is both easy to implement and a snap to customize.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
