package:
  name: hsaudiotag3k
  version: "1.1.3"

source:
  fn: hsaudiotag3k-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hsaudiotag3k/hsaudiotag3k-1.1.3.tar.gz
  md5: 77fc873857303e889851605e9836669b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hsaudiotag3k = hsaudiotag3k:main
    #
    # Would create an entry point called hsaudiotag3k that calls hsaudiotag3k.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hsaudiotag
    - hsaudiotag3k

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.hardcoded.net/hsaudiotag/
  license: BSD License
  summary: 'Read metdata (tags) of mp3, mp4, wma, ogg, flac and aiff files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
