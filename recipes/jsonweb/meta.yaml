package:
  name: jsonweb
  version: "0.8.2"

source:
  fn: JsonWeb-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/J/JsonWeb/JsonWeb-0.8.2.tar.gz
  md5: 8d81d6289c59f9ec57bc65d74492f348
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonweb = jsonweb:main
    #
    # Would create an entry point called jsonweb that calls jsonweb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - jsonweb
    - jsonweb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Quickly add json serialization and deserialization to your python classes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
