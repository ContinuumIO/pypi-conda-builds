package:
  name: pylibgal3
  version: "0.1.6"

source:
  fn: pylibgal3-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/p/pylibgal3/pylibgal3-0.1.6.tar.gz
  md5: 208c1adbdd15cb688732b6626e30acee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pylibgal3 = pylibgal3:main
    #
    # Would create an entry point called pylibgal3 that calls pylibgal3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - libg3
    - pylibgal3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stuffivelearned.org
  license: GNU General Public License (GPL)
  summary: 'A library for accessing/manipulating a Gallery 3 install'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
