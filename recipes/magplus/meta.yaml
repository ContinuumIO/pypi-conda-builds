package:
  name: magplus
  version: "0.2.2"

source:
  fn: MAGPlus-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/M/MAGPlus/MAGPlus-0.2.2.tar.gz
  md5: 89a900751eb46b2552c2970a5373c3d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magplus = magplus:main
    #
    # Would create an entry point called magplus that calls magplus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - beautifulsoup4 >=4.1.1

  run:
    - python
    - beautifulsoup4 >=4.1.1

test:
  # Python imports
  imports:
    - magplus
    - magplus.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/MAGPlus/
  license: GPLv2
  summary: 'Retrieving latest Minecraft versions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
