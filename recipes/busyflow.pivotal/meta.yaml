package:
  name: busyflow.pivotal
  version: "0.3.4"

source:
  fn: busyflow.pivotal-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/b/busyflow.pivotal/busyflow.pivotal-0.3.4.tar.gz
  md5: 220d636ba586a545fd32fcb018477f08
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - busyflow.pivotal = busyflow.pivotal:main
    #
    # Would create an entry point called busyflow.pivotal that calls busyflow.pivotal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmlbuilder
    - python-dateutil
    - poster

  run:
    - python
    - setuptools
    - xmlbuilder
    - python-dateutil
    - poster

test:
  # Python imports
  imports:
    - busyflow
    - busyflow.pivotal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nous-consulting/busyflow.pivotal/
  license: GNU General Public License (GPL)
  summary: 'Pivotal API client library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
