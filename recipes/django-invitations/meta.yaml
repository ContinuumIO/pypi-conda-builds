package:
  name: django-invitations
  version: "0.12"

source:
  fn: django-invitations-0.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-invitations/django-invitations-0.12.tar.gz
  md5: 6de4c7c53ba526b7e15ac4c56e30f04b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-invitations = django-invitations:main
    #
    # Would create an entry point called django-invitations that calls django-invitations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-allauth >=0.18.0
    - django-braces >=1.4.0

  run:
    - python
    - django-allauth >=0.18.0
    - django-braces >=1.4.0

test:
  # Python imports
  imports:
    - invitations
    - invitations.management
    - invitations.management.commands
    - invitations.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bee-keeper/django-invitations.git
  license: UNKNOWN
  summary: 'Django invitation integration for django-allauth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
