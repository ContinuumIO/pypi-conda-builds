package:
  name: kuroko
  version: "0.1.7"

source:
  fn: kuroko-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/k/kuroko/kuroko-0.1.7.tar.gz
  md5: c08d0d0127c5e5f6e4371688a8161c4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kuroko = kuroko:main
    #
    # Would create an entry point called kuroko that calls kuroko.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - logbook
    - crontab
    - watchdog

  run:
    - python
    - logbook
    - crontab
    - watchdog

test:
  # Python imports
  imports:
    - kuroko

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hhatto/kuroko
  license: MIT License
  summary: 'Minimalistic Python Bot Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
