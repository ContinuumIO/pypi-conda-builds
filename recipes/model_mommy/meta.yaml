package:
  name: model_mommy
  version: "1.2.4"

source:
  fn: model_mommy-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/m/model_mommy/model_mommy-1.2.4.tar.gz
  md5: 8431ceccdf9c3600d6a548d54879fd1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - model_mommy = model_mommy:main
    #
    # Would create an entry point called model_mommy that calls model_mommy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.9
    - six

  run:
    - python
    - django <1.9
    - six

test:
  # Python imports
  imports:
    - model_mommy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vandersonmota/model_mommy
  license: Apache Software License
  summary: 'Smart object creation facility for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
