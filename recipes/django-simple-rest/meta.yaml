package:
  name: django-simple-rest
  version: "1.4.1"

source:
  fn: django-simple-rest-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-rest/django-simple-rest-1.4.1.tar.gz
  md5: b9612809296e488422c3ae327059c1d6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-rest = django-simple-rest:main
    #
    # Would create an entry point called django-simple-rest that calls django-simple-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mimeparse

  run:
    - python
    - setuptools
    - mimeparse

test:
  # Python imports
  imports:
    - simple_rest
    - simple_rest.auth
    - simple_rest.management
    - simple_rest.management.commands
    - simple_rest.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/freshplum/django-simple-rest
  license: MIT License
  summary: 'A drop dead simple package for creating RESTful APIs on top of Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
