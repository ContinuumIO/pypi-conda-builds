package:
  name: logkeeper
  version: "0.0.1"

source:
  fn: logKeeper-0.0.1.zip
  url: https://pypi.python.org/packages/source/l/logKeeper/logKeeper-0.0.1.zip
  md5: 4ee8b4227f5229080f7b770962ee5df4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logkeeper = logkeeper:main
    #
    # Would create an entry point called logkeeper that calls logkeeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - logkeeper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hi.baidu.com/huangyif
  license: UNKNOWN
  summary: 'keeping logfile, it can open a file and add log to the end.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
