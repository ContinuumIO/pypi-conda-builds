package:
  name: djangocms-vimeo
  version: "0.3.2"

source:
  fn: djangocms-vimeo-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-vimeo/djangocms-vimeo-0.3.2.tar.gz
  md5: 2c2d07eb3aabc21b2b59edb6590319ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-vimeo = djangocms-vimeo:main
    #
    # Would create an entry point called djangocms-vimeo that calls djangocms-vimeo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-cms >=3.0

  run:
    - python
    - django
    - django-cms >=3.0

test:
  # Python imports
  imports:
    - djangocms_vimeo
    - djangocms_vimeo.migrations
    - djangocms_vimeo.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nimbis/djangocms-vimeo
  license: BSD License
  summary: 'Video plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
