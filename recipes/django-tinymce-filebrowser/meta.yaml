package:
  name: django-tinymce-filebrowser
  version: "0.1.2"

source:
  fn: Django-tinymce-filebrowser-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-tinymce-filebrowser/Django-tinymce-filebrowser-0.1.2.tar.gz
  md5: 47d6a61457cd97487cd288aa8a0ec0fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tinymce-filebrowser = django-tinymce-filebrowser:main
    #
    # Would create an entry point called django-tinymce-filebrowser that calls django-tinymce-filebrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sorl-thumbnail
    - django-tinymce

  run:
    - python
    - sorl-thumbnail
    - django-tinymce

test:
  # Python imports
  imports:
    - mce_filebrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aisayko/Django-tinymce-filebrowser
  license: MIT License
  summary: 'Django-based file uploader and viewer for TinyMCE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
