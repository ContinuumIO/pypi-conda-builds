package:
  name: django-bootstrap-sass
  version: "0.0.6-alpha"

source:
  fn: django-bootstrap-sass-0.0.6-alpha.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-sass/django-bootstrap-sass-0.0.6-alpha.tar.gz
  md5: 8273734f40d8b434b3a90a7a2736c8b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-sass = django-bootstrap-sass:main
    #
    # Would create an entry point called django-bootstrap-sass that calls django-bootstrap-sass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap_sass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ekevin/django-bootstrap-sass
  license: Apache licence, see LICENCE
  summary: 'Bootstrap sass file for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
