package:
  name: horae.auth
  version: "1.0a1"

source:
  fn: horae.auth-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.auth/horae.auth-1.0a1.tar.gz
  md5: 2b6fe9b9d445a7eb6c6600a61676c3c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.auth = horae.auth:main
    #
    # Would create an entry point called horae.auth that calls horae.auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - zope.pluggableauth
    - horae.cache
    - horae.core
    - horae.layout

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - zope.pluggableauth
    - horae.cache
    - horae.core
    - horae.layout

test:
  # Python imports
  imports:
    - horae
    - horae.auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides a pluggable users and groups architecture for the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
