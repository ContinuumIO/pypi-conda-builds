package:
  name: cstypo
  version: "0.1.3"

source:
  fn: cstypo-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cstypo/cstypo-0.1.3.tar.gz
  md5: 7ff7ff3c2e5d1096498e7dd06f89e6cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cstypo = cstypo:main
    #
    # Would create an entry point called cstypo that calls cstypo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - docopt >=0.5.0

  run:
    - python
    - docopt >=0.5.0

test:
  # Python imports
  imports:
    - cstypo
    - cstypo.templatetags
    - cstypo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yetty/cstypo
  license: csumb205-multimedia
  summary: 'Package to apply Czech typography easily'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
