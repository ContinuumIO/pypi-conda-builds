package:
  name: django-hstsmiddleware
  version: "1.0"

source:
  fn: django-hstsmiddleware-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hstsmiddleware/django-hstsmiddleware-1.0.tar.gz
  md5: 78d2a53d0659738c5ed46ef7687183cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hstsmiddleware = django-hstsmiddleware:main
    #
    # Would create an entry point called django-hstsmiddleware that calls django-hstsmiddleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_hstsmiddleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/trustcentric/django-hstsmiddleware/
  license: BSD License
  summary: 'Implement HSTS to force the use of HTTPS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
