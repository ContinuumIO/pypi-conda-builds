package:
  name: django-revisionfield
  version: "0.2.2"

source:
  fn: django-revisionfield-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-revisionfield/django-revisionfield-0.2.2.tar.gz
  md5: b7ccb432d06d0fe8d705c10450a55d0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-revisionfield = django-revisionfield:main
    #
    # Would create an entry point called django-revisionfield that calls django-revisionfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django_revisionfield
    - tests
    - tests.app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyayers/django-revisionfield/
  license: Simplified BSD
  summary: 'An model field that auto increments every time the model is saved'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
