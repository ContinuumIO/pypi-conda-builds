package:
  name: mongoext
  version: "0.3.0"

source:
  fn: mongoext-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mongoext/mongoext-0.3.0.tar.gz
  md5: cc689d45c47670af45cd0a0b76e33d67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoext = mongoext:main
    #
    # Would create an entry point called mongoext that calls mongoext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo ==3.0

  run:
    - python
    - pymongo ==3.0

test:
  # Python imports
  imports:
    - mongoext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mylokin/mongoext
  license: MIT License
  summary: 'Data models for Mongo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
