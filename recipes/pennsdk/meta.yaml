package:
  name: pennsdk
  version: "1.1"

source:
  fn: PennSDK-1.1.tar.gz
  url: https://pypi.python.org/packages/source/P/PennSDK/PennSDK-1.1.tar.gz
  md5: 0fadf0448104616122a0c9fc5f6505c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pennsdk = pennsdk:main
    #
    # Would create an entry point called pennsdk that calls pennsdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.4.3
    - beautifulsoup4 ==4.3.2
    - html5lib ==0.999

  run:
    - python
    - requests ==2.4.3
    - beautifulsoup4 ==4.3.2
    - html5lib ==0.999

test:
  # Python imports
  imports:
    - penn
    - pennsdk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pennappslabs/penn-sdk-python
  license: MIT
  summary: 'Python tools for building Penn-related applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
