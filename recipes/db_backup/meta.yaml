package:
  name: db_backup
  version: "0.1.3"

source:
  fn: db_backup-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/db_backup/db_backup-0.1.3.tar.gz
  md5: 1303f7380f3d720baf0f404ae7a4d7e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - db_backup = db_backup:main
    #
    # Would create an entry point called db_backup that calls db_backup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - db_backup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://db-backup.readthedocs.org
  license: WTFPL
  summary: 'Code for creating encrypted backups of databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
