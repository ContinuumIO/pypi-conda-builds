package:
  name: django-edict
  version: "0.1"

source:
  fn: django-edict-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-edict/django-edict-0.1.tar.gz
  md5: b539dc6d27a4fe49c1501e7f968ab39a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-edict = django-edict:main
    #
    # Would create an entry point called django-edict that calls django-edict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - edict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/LimpidTech/django-edict
  license: MIT License
  summary: 'A django application for keeping your fans up to date.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
