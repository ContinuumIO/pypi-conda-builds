package:
  name: js.json
  version: "20110223"

source:
  fn: js.json-20110223.tar.gz
  url: https://pypi.python.org/packages/source/j/js.json/js.json-20110223.tar.gz
  md5: a885d73a07ddddabc9921774f00d9562
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.json = js.json:main
    #
    # Would create an entry point called js.json that calls js.json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.json

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
