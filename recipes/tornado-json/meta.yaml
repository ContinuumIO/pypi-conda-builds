package:
  name: tornado-json
  version: "1.2.0"

source:
  fn: Tornado-JSON-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/T/Tornado-JSON/Tornado-JSON-1.2.0.tar.gz
  md5: 802d707f4f2ccbdba2249b314b26d2a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-json = tornado-json:main
    #
    # Would create an entry point called tornado-json that calls tornado-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.1.1
    - jsonschema >=2.3.0

  run:
    - python
    - tornado >=3.1.1
    - jsonschema >=2.3.0

test:
  # Python imports
  imports:
    - tornado_json

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hfaran/Tornado-JSON
  license: MIT License
  summary: 'A simple JSON API framework based on Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
