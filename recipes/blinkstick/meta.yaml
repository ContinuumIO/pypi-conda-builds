package:
  name: blinkstick
  version: "1.1.7"

source:
  fn: BlinkStick-1.1.7.tar.gz
  url: https://pypi.python.org/packages/source/B/BlinkStick/BlinkStick-1.1.7.tar.gz
  md5: 0d7a0790dfa69b1ec28e174d1ab7f7aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blinkstick = blinkstick:main
    #
    # Would create an entry point called blinkstick that calls blinkstick.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyusb ==1.0.0b1

  run:
    - python
    - pyusb ==1.0.0b1

test:
  # Python imports
  imports:
    - blinkstick

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BlinkStick/
  license: LICENSE.txt
  summary: 'Python package to control BlinkStick USB devices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
