package:
  name: gomobile.convergence
  version: "1.0.3"

source:
  fn: gomobile.convergence-1.0.3.zip
  url: https://pypi.python.org/packages/source/g/gomobile.convergence/gomobile.convergence-1.0.3.zip
  md5: 4827af4694650d479afcfeddcdd70bd7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gomobile.convergence = gomobile.convergence:main
    #
    # Would create an entry point called gomobile.convergence that calls gomobile.convergence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - gomobile.mobile

  run:
    - python
    - setuptools
    - setuptools
    - gomobile.mobile

test:
  # Python imports
  imports:
    - gomobile
    - gomobile.convergence
    - gomobile.convergence.browser
    - gomobile.convergence.browser.portlets
    - gomobile.convergence.overrider
    - gomobile.convergence.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://webandmobile.mfabrik.com
  license: GPL
  summary: 'Multichannel content discrimination and overrides for Plone CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
