package:
  name: oslo.vmware
  version: "0.13.0"

source:
  fn: oslo.vmware-0.13.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oslo.vmware/oslo.vmware-0.13.0.tar.gz
  md5: 6cd7b5604f5e9e1ae97df9bf90bc5e5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oslo.vmware = oslo.vmware:main
    #
    # Would create an entry point called oslo.vmware that calls oslo.vmware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbr

  run:
    - python
    - pbr

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Oslo VMware library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
