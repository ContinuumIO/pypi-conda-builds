package:
  name: django-environ
  version: "0.3.0"

source:
  fn: django-environ-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-environ/django-environ-0.3.0.tar.gz
  md5: 1c7921976f4eb59bad568fce0bde66d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-environ = django-environ:main
    #
    # Would create an entry point called django-environ that calls django-environ.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - environ

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joke2k/django-environ
  license: MIT License
  summary: u'\nDjango-environ allows you to utilize 12factor inspired environment\nvariables to configure your Django application.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
