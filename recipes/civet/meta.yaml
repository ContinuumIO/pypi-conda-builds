package:
  name: civet
  version: "1.1.1"

source:
  fn: civet-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/civet/civet-1.1.1.tar.gz
  md5: 306f8803c71c84f8bfbe4823eba1def4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - civet = civet:main
    #
    # Would create an entry point called civet that calls civet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - watchdog >=0.7.1

  run:
    - python
    - django >=1.3
    - watchdog >=0.7.1

test:
  # Python imports
  imports:
    - civet
    - civet.management
    - civet.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/counsyl/civet
  license: Apache Software License
  summary: 'CoffeeScript and Sass asset precompiler for the Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
