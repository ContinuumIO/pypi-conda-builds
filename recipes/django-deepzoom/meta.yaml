package:
  name: django-deepzoom
  version: "3.0.2"

source:
  fn: django-deepzoom-3.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-deepzoom/django-deepzoom-3.0.2.tar.gz
  md5: 1383ed8b39db64bf87a98ffb9267be46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-deepzoom = django-deepzoom:main
    #
    # Would create an entry point called django-deepzoom that calls django-deepzoom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pillow >=1.7.8
    - six >=1.9.0

  run:
    - python
    - django >=1.4
    - pillow >=1.7.8
    - six >=1.9.0

test:
  # Python imports
  imports:
    - deepzoom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-deepzoom.invocatum.net/
  license: BSD License
  summary: 'A simple Django app to create deep zoom tiled images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
