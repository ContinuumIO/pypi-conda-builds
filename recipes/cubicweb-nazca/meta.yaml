package:
  name: cubicweb-nazca
  version: "0.1.0"

source:
  fn: cubicweb-nazca-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-nazca/cubicweb-nazca-0.1.0.tar.gz
  md5: adf58dbcf7ea691dd22df0c95a7122ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-nazca = cubicweb-nazca:main
    #
    # Would create an entry point called cubicweb-nazca that calls cubicweb-nazca.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.5
    - nazca >=0.5.0
    - cubicweb-bootstrap >=0.5.2
    - cubicweb-card
    - cubicweb-squareui >=0.3.2

  run:
    - python
    - cubicweb >=3.17.5
    - nazca >=0.5.0
    - cubicweb-bootstrap >=0.5.2
    - cubicweb-card
    - cubicweb-squareui >=0.3.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-nazca
  license: LGPL
  summary: 'A cube for a nazca usage example'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
