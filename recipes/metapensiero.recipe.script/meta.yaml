package:
  name: metapensiero.recipe.script
  version: "1.2"

source:
  fn: metapensiero.recipe.script-1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/metapensiero.recipe.script/metapensiero.recipe.script-1.2.tar.gz
  md5: a124b9c4f706cef3c5b34d15d9654078
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metapensiero.recipe.script = metapensiero.recipe.script:main
    #
    # Would create an entry point called metapensiero.recipe.script that calls metapensiero.recipe.script.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - metapensiero
    - metapensiero.recipe
    - metapensiero.recipe.script

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/lele/metapensiero.recipe.script
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Buildout recipes to execute Python or Shell scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
