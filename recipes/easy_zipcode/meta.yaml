package:
  name: easy_zipcode
  version: "0.1.5"

source:
  fn: easy_zipcode-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/e/easy_zipcode/easy_zipcode-0.1.5.tar.gz
  md5: acf4f105bdd75011c88a004ab08ea6d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy_zipcode = easy_zipcode:main
    #
    # Would create an entry point called easy_zipcode that calls easy_zipcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - easy_zipcode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stored/easy_zipcode
  license: BSD
  summary: 'easy_zipcode is a lightweight python app for retrieving         brazilian address from a zip code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
