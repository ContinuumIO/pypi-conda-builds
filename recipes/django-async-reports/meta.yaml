package:
  name: django-async-reports
  version: "0.2.6-alpha-1"

source:
  fn: django-async-reports-0.2.6-alpha-1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-async-reports/django-async-reports-0.2.6-alpha-1.tar.gz
  md5: cd745721d244b414d890e29fb08db092
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-async-reports = django-async-reports:main
    #
    # Would create an entry point called django-async-reports that calls django-async-reports.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3.1
    - south ==0.7.3
    - django-jsonfield ==0.8.10
    - celery ==2.5.3

  run:
    - python
    - django ==1.3.1
    - south ==0.7.3
    - django-jsonfield ==0.8.10
    - celery ==2.5.3

test:
  # Python imports
  imports:
    - async_reports
    - async_reports.report_tracking
    - async_reports.report_tracking.migrations
    - async_reports.report_tracking.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Reporting for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
