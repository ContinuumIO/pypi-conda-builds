package:
  name: sortmail
  version: "20140526"

source:
  fn: sortmail-20140526.tar.gz
  url: https://primate.net/~itz/sortmail/sortmail-20140526.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sortmail = sortmail:main
    #
    # Would create an entry point called sortmail that calls sortmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sortmail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nobrowser/sortmail
  license: A simple free non-copyleft license based on ZPL
  summary: 'A module to sort incoming mail for delivery to multiple destinations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
