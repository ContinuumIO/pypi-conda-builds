package:
  name: django-custom-field
  version: "2.8"

source:
  fn: django-custom-field-2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-custom-field/django-custom-field-2.8.tar.gz
  md5: 354bb2b91ffb7c85ed35690008a8a9e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-custom-field = django-custom-field:main
    #
    # Would create an entry point called django-custom-field that calls django-custom-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - custom_field
    - custom_field.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/burke-software/django-custom-field
  license: BSD License
  summary: 'End user custom fields for Django including contrib.admin support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
