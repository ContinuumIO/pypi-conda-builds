package:
  name: prismic
  version: "1.2.1"

source:
  fn: prismic-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/prismic/prismic-1.2.1.tar.gz
  md5: f9097ca852e03a28d721f89f001a577a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - prismic = prismic:main
    #
    # Would create an entry point called prismic that calls prismic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopenssl
    - ndg-httpsclient
    - pyasn1
    - requests

  run:
    - python
    - pyopenssl
    - ndg-httpsclient
    - pyasn1
    - requests

test:
  # Python imports
  imports:
    - prismic
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://prismic.io
  license: Apache 2
  summary: 'Prismic.io development kit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
