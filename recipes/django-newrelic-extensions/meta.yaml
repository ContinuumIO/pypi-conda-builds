package:
  name: django-newrelic-extensions
  version: "0.0.1"

source:
  fn: django-newrelic-extensions-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-newrelic-extensions/django-newrelic-extensions-0.0.1.tar.gz
  md5: 96b02d4a6082a4816097aaa7a881bc8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-newrelic-extensions = django-newrelic-extensions:main
    #
    # Would create an entry point called django-newrelic-extensions that calls django-newrelic-extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - newrelic

  run:
    - python
    - newrelic

test:
  # Python imports
  imports:
    - newrelicextensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'New Relic extensions for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
