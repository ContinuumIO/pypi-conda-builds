package:
  name: experimental.catalogqueryplan
  version: "3.2.8"

source:
  fn: experimental.catalogqueryplan-3.2.8.zip
  url: https://pypi.python.org/packages/source/e/experimental.catalogqueryplan/experimental.catalogqueryplan-3.2.8.zip
  md5: 9a47cd7cb359a3d72b860908ae06949c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.catalogqueryplan = experimental.catalogqueryplan:main
    #
    # Would create an entry point called experimental.catalogqueryplan that calls experimental.catalogqueryplan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone >=4.0.3

  run:
    - python
    - setuptools
    - setuptools
    - plone >=4.0.3

test:
  # Python imports
  imports:
    - experimental
    - experimental.catalogqueryplan
    - experimental.catalogqueryplan.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jarn.com/
  license: GPL
  summary: 'Static query optimized with one plan'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
