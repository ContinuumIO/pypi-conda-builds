package:
  name: changefinder
  version: "0.03"

source:
  fn: changefinder-0.03.tar.gz
  url: https://pypi.python.org/packages/source/c/changefinder/changefinder-0.03.tar.gz
  md5: a72051d27fbd53763e43f2184e6f6535
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - changefinder = changefinder:main
    #
    # Would create an entry point called changefinder that calls changefinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - statsmodels
    - nose

  run:
    - python
    - numpy
    - scipy
    - statsmodels
    - nose

test:
  # Python imports
  imports:
    - changefinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/aihara/changefinder/
  license: MIT License
  summary: 'Online Change-Point Detection Library based on ChangeFinder Algorithm.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
