package:
  name: iso-8601
  version: "0.2.4"

source:
  fn: iso-8601-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/i/iso-8601/iso-8601-0.2.4.tar.gz
  md5: a170a133201248fa5c6c18d80b23417a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iso-8601 = iso-8601:main
    #
    # Would create an entry point called iso-8601 that calls iso-8601.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iso8601

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.schinckel.net/iso-8601/
  license: BSD License
  summary: 'Flexible ISO 8601 parser: pass in a valid ISO 8601 string, and a datetime object will be returned.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
