package:
  name: collective.transform.multimarkdown
  version: "0.2"

source:
  fn: collective.transform.multimarkdown-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.transform.multimarkdown/collective.transform.multimarkdown-0.2.tar.gz
  md5: bd579be2414b4fcd1c54decef4d81e40
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.transform.multimarkdown = collective.transform.multimarkdown:main
    #
    # Would create an entry point called collective.transform.multimarkdown that calls collective.transform.multimarkdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.transform
    - collective.transform.multimarkdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.sixfeetup.com
  license: GPL
  summary: 'MultiMarkdown text transform for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
