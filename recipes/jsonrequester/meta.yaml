package:
  name: jsonrequester
  version: "0.1.5"

source:
  fn: jsonrequester-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonrequester/jsonrequester-0.1.5.tar.gz
  md5: 4030e3025ef4514bf9740420eb54c793
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonrequester = jsonrequester:main
    #
    # Would create an entry point called jsonrequester that calls jsonrequester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - jsonrequester
    - tests
    - tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A library for client JSON REST requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
