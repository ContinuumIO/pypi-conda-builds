package:
  name: updian
  version: "0.6"

source:
  fn: UPDIAN-0.6.tar.gz
  url: https://pypi.python.org/packages/source/U/UPDIAN/UPDIAN-0.6.tar.gz
  md5: 82bec97c282803a71c3e889474b157d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - updian = updian:main
    #
    # Would create an entry point called updian that calls updian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.6.0
    - flask >=0.9
    - flask-csrf >=0.9.2
    - flask-basicauth >=0.1.1

  run:
    - python
    - fabric >=1.6.0
    - flask >=0.9
    - flask-csrf >=0.9.2
    - flask-basicauth >=0.1.1

test:
  # Python imports
  imports:
    - updian
    - updian.cmd
    - updian.cron
    - updian.frontend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.robhost.de/updian/
  license: GPLv2+ <http://www.gnu.org/licenses/gpl-2.0.en.html>
  summary: 'A program for monitoring and executing updates on remote systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
