package:
  name: dirtyebay
  version: "0.1.5"

source:
  fn: dirtyebay-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dirtyebay/dirtyebay-0.1.5.tar.gz
  md5: 5169170979ca906686622864a7587761
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dirtyebay = dirtyebay:main
    #
    # Would create an entry point called dirtyebay that calls dirtyebay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - lxml >=3.4.2
    - dogpile.cache
    - appdirs
    - requests

  run:
    - python
    - lxml >=3.4.2
    - dogpile.cache
    - appdirs
    - requests

test:
  # Python imports
  imports:
    - dirtyebay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/anentropic/dirtyebay
  license: MIT
  summary: "An eBay API client which respects (XSD) schema and talks SOAP but doesn't use Suds"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
