package:
  name: collective.blueprint.downloader
  version: "1.0"

source:
  fn: collective.blueprint.downloader-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.blueprint.downloader/collective.blueprint.downloader-1.0.tar.gz
  md5: d716e06949d0e833db8f4eb94a90b33f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.blueprint.downloader = collective.blueprint.downloader:main
    #
    # Would create an entry point called collective.blueprint.downloader that calls collective.blueprint.downloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urllibcache

  run:
    - python
    - setuptools
    - urllibcache

test:
  # Python imports
  imports:
    - collective
    - collective.blueprint
    - collective.blueprint.downloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.blueprint.downloader
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
