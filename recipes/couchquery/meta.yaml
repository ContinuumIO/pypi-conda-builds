package:
  name: couchquery
  version: "0.10.3"

source:
  fn: couchquery-0.10.3.tar.gz
  url: https://pypi.python.org/packages/source/c/couchquery/couchquery-0.10.3.tar.gz
  md5: b1013b5dc95ec424d10d5bb13a08daf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchquery = couchquery:main
    #
    # Would create an entry point called couchquery that calls couchquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - couchquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nicolaisi.github.io/couchquery/
  license: Mozilla Public License 1.1 (MPL 1.1)
  summary: 'Python library for simple and dynamic access to CouchDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
