package:
  name: amara3-xml
  version: "3.0.0a5"

source:
  fn: amara3-xml-3.0.0a5.tar.gz
  url: https://pypi.python.org/packages/source/a/amara3-xml/amara3-xml-3.0.0a5.tar.gz
  md5: c3c6b9ea274946c357da94d067e33448
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amara3-xml = amara3-xml:main
    #
    # Would create an entry point called amara3-xml that calls amara3-xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - amara3
    - amara3.uxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/uogbuji/amara3-xml
  license: Apache Software License
  summary: 'Amara3 project, which offers a variety of data processing tools. This module adds the MicroXML support, and adaptation to classic XML.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
