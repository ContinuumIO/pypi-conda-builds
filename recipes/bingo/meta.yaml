package:
  name: bingo
  version: "0.1.15"

source:
  fn: bingo-0.1.15.tar.gz
  url: https://pypi.python.org/packages/source/b/bingo/bingo-0.1.15.tar.gz
  md5: 75aed732e251f2e8b07481666cc843fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bingo = bingo:main
    #
    # Would create an entry point called bingo that calls bingo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bingo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oscarolar/bingo
  license: BSD License
  summary: 'Bingo is a library that creates bingo cards and game raffles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
