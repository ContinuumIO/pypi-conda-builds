package:
  name: django-vcexport
  version: "0.6"

source:
  fn: django-vcexport-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vcexport/django-vcexport-0.6.tar.gz
  md5: ffdd5bd4c50c35a4c245b940b3ea10e6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vcexport = django-vcexport:main
    #
    # Would create an entry point called django-vcexport that calls django-vcexport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sven >=0.6

  run:
    - python
    - setuptools
    - sven >=0.6

test:
  # Python imports
  imports:
    - vcexport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'a flexible library for exporting serialized django model instances to a version-controlled repository automatically or manually'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
