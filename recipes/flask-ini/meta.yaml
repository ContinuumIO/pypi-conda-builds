package:
  name: flask-ini
  version: "0.2.1"

source:
  fn: Flask-ini-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-ini/Flask-ini-0.2.1.tar.gz
  md5: 32f993266c3eb41ba46c8a2313b9f2d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-ini = flask-ini:main
    #
    # Would create an entry point called flask-ini that calls flask-ini.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - configparser

  run:
    - python
    - flask
    - configparser

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trendmicro/flask-ini
  license: BSD License
  summary: 'Allow Flask to be configured with configparser ini files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
