package:
  name: django-gulp
  version: "2.0.0"

source:
  fn: django-gulp-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gulp/django-gulp-2.0.0.tar.gz
  md5: 3b50fa2de4ffe4b8f6a0a3a5fe282550
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gulp = django-gulp:main
    #
    # Would create an entry point called django-gulp that calls django-gulp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansicolors ==1.0.2
    - env_tools ==2.0
    - futures ==2.2.0
    - psutil ==2.2.1

  run:
    - python
    - ansicolors ==1.0.2
    - env_tools ==2.0
    - futures ==2.2.0
    - psutil ==2.2.1

test:
  # Python imports
  imports:
    - django_gulp
    - django_gulp.management
    - django_gulp.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/beaugunderson/django-gulp
  license: MIT License
  summary: 'Run your gulp tasks with runserver and collectstatic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
