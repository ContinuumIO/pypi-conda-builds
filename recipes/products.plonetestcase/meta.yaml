package:
  name: products.plonetestcase
  version: "0.9.18"

source:
  fn: Products.PloneTestCase-0.9.18.zip
  url: https://pypi.python.org/packages/source/P/Products.PloneTestCase/Products.PloneTestCase-0.9.18.zip
  md5: 0bc6f428a8158a8af29d9d875dbec8d4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.plonetestcase = products.plonetestcase:main
    #
    # Would create an entry point called products.plonetestcase that calls products.plonetestcase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.site
    - zope.testing
    - acquisition
    - products.cmfplone
    - products.atcontenttypes
    - products.cmfcore
    - products.genericsetup
    - zodb3
    - zope2

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.site
    - zope.testing
    - acquisition
    - products.cmfplone
    - products.atcontenttypes
    - products.cmfcore
    - products.genericsetup
    - zodb3
    - zope2

test:
  # Python imports
  imports:
    - Products
    - Products.PloneTestCase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plonetestcase
  license: GPL
  summary: 'Integration testing framework for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
