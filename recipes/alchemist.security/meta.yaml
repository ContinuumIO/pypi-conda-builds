package:
  name: alchemist.security
  version: "0.4.2"

source:
  fn: alchemist.security-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/a/alchemist.security/alchemist.security-0.4.2.tar.gz
  md5: faf9d6fbb01b6cdfe6255a7950afaafd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alchemist.security = alchemist.security:main
    #
    # Would create an entry point called alchemist.security that calls alchemist.security.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ore.alchemist
    - zope.securitypolicy

  run:
    - python
    - setuptools
    - ore.alchemist
    - zope.securitypolicy

test:
  # Python imports
  imports:
    - alchemist
    - alchemist.security

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'Relational Implementation of Zope Security components'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
