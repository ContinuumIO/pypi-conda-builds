package:
  name: appliancekit-ng
  version: "0.6.2"

source:
  fn: ApplianceKit-NG-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/A/ApplianceKit-NG/ApplianceKit-NG-0.6.2.tar.gz
  md5: 4252971aa61f47c4f96946f986d00de3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appliancekit-ng = appliancekit-ng:main
    #
    # Would create an entry point called appliancekit-ng that calls appliancekit-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - appliancekit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/tortoiselabs/appliancekit-ng
  license: ISC License (ISCL)
  summary: 'Tools to programatically create distribution images from any distribution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
