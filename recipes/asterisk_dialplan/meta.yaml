package:
  name: asterisk_dialplan
  version: "0.1.3"

source:
  fn: asterisk_dialplan-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/asterisk_dialplan/asterisk_dialplan-0.1.3.tar.gz
  md5: c4e8ae1390bf0aaa2317d4f7eaf428ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asterisk_dialplan = asterisk_dialplan:main
    #
    # Would create an entry point called asterisk_dialplan that calls asterisk_dialplan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asterisk_dialplan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Helpers to convert numbers to dialplan strings for use in Asterisk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
