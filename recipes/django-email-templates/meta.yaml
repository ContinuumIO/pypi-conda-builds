package:
  name: django-email-templates
  version: "0.1"

source:
  fn: django-email-templates-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-templates/django-email-templates-0.1.tar.gz
  md5: 12c82ceb4580cac5675c8a7883f87e09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-templates = django-email-templates:main
    #
    # Would create an entry point called django-email-templates that calls django-email-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/saevarom/django-email-templates
  license: MIT License
  summary: 'Convenient helper to send multipart/alternative or regular email based on templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
