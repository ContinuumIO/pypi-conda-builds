package:
  name: passlib
  version: "1.6.2"

source:
  fn: passlib-1.6.2.tar.gz
  url: https://pypi.python.org/packages/source/p/passlib/passlib-1.6.2.tar.gz
  md5: 2f872ae7c72ca338634c618f2cff5863
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - passlib = passlib:main
    #
    # Would create an entry point called passlib that calls passlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - passlib
    - passlib._setup
    - passlib.ext
    - passlib.ext.django
    - passlib.handlers
    - passlib.tests
    - passlib.utils
    - passlib.utils._blowfish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

requires:
    -  
    -  
    - .
    - 1
    - 1
    - =
    - >
    - e
    - n
    - o
    - s
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://passlib.googlecode.com
  license: BSD License
  summary: 'comprehensive password hashing framework supporting over 30 schemes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
