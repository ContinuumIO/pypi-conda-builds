package:
  name: beret
  version: "0.2.2"

source:
  fn: beret-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/beret/beret-0.2.2.tar.gz
  md5: 764674730526f2eca0b72f5cbc553580
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beret = beret:main
    #
    # Would create an entry point called beret that calls beret.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt >=0.6.1
    - pandas ==0.13.0
    - requests >=2.3.0
    - yhat >=1.0.0

  run:
    - python
    - docopt >=0.6.1
    - pandas ==0.13.0
    - requests >=2.3.0
    - yhat >=1.0.0

test:
  # Python imports
  imports:
    - beret

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yhat/beret
  license: BSD
  summary: 'Parse a csv file and score through a yhat model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
