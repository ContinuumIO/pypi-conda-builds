package:
  name: cluereleasemanager.paste
  version: "1.1"

source:
  fn: ClueReleaseManager.paste-1.1.zip
  url: https://pypi.python.org/packages/source/C/ClueReleaseManager.paste/ClueReleaseManager.paste-1.1.zip
  md5: 367e69b35b41641dcb77b27565e07e33
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cluereleasemanager.paste = cluereleasemanager.paste:main
    #
    # Would create an entry point called cluereleasemanager.paste that calls cluereleasemanager.paste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - webob
    - werkzeug
    - pastescript
    - cluereleasemanager

  run:
    - python
    - setuptools
    - setuptools
    - webob
    - werkzeug
    - pastescript
    - cluereleasemanager

test:
  # Python imports
  imports:
    - ClueReleaseManager
    - ClueReleaseManager.paste
    - ClueReleaseManager.paste.paste
    - ClueReleaseManager.paste.paste.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://git.minitage.org/git/others/ClueReleaseManager.paste
  license: BSD
  summary: 'Yet another WSGI Paste factory for paste and ClueReleaseManager sponsorised by Makina Corpus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
