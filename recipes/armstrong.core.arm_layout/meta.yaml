package:
  name: armstrong.core.arm_layout
  version: "1.4.0"

source:
  fn: armstrong.core.arm_layout-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.core.arm_layout/armstrong.core.arm_layout-1.4.0.tar.gz
  md5: b04f17915cfd69dc62455a1d7448d244
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.core.arm_layout = armstrong.core.arm_layout:main
    #
    # Would create an entry point called armstrong.core.arm_layout that calls armstrong.core.arm_layout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - armstrong.utils.backends >=1.0.0,<2.0,!=1.1.0

  run:
    - python
    - armstrong.utils.backends >=1.0.0,<2.0,!=1.1.0

test:
  # Python imports
  imports:
    - armstrong
    - armstrong.core
    - armstrong.core.arm_layout
    - armstrong.core.arm_layout.backends
    - armstrong.core.arm_layout.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.core.arm_layout/
  license: Apache Software License
  summary: u'Layout code related to Armstrong'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
