package:
  name: ansicolors-balises
  version: "1.9.9.public"

source:
  fn: ANSIColors-balises-1.9.9.public.tar.gz
  url: https://pypi.python.org/packages/source/A/ANSIColors-balises/ANSIColors-balises-1.9.9.public.tar.gz
  md5: 12f61ce4f2f1632785de247a66e01d4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansicolors-balises = ansicolors-balises:main
    #
    # Would create an entry point called ansicolors-balises that calls ansicolors-balises.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.bitbucket.org/lbesson/ansi-colors
  license: GNU General Public License v3 (GPLv3)
  summary: 'ANSIColors brings a simple and power full way to use colours in a terminal.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
