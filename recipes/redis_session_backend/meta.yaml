package:
  name: redis_session_backend
  version: "0.0.2"

source:
  fn: redis_session_backend-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_session_backend/redis_session_backend-0.0.2.tar.gz
  md5: 2200f584aee480c60d0813955d3c3b2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_session_backend = redis_session_backend:main
    #
    # Would create an entry point called redis_session_backend that calls redis_session_backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redis_session_backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lispython/redis_session_backend
  license: BSD License
  summary: 'Redis backend for django sessions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
