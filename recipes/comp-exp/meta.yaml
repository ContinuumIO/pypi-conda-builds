package:
  name: comp-exp
  version: "2.3.1"

source:
  fn: comp-exp-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/comp-exp/comp-exp-2.3.1.tar.gz
  md5: 4c12e0f10457c1d8b108dbfb06e05606
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - comp-exp = comp-exp:main
    #
    # Would create an entry point called comp-exp that calls comp-exp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - comp_exp
    - comp_exp.experimenters
    - comp_exp.experiments_data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://hg.plav.in/my_projects/python/comp-exp
  license: MIT License
  summary: 'Flexible computational experiments framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
