package:
  name: li-pagador-rede
  version: "2.0.0"

source:
  fn: li-pagador-rede-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/li-pagador-rede/li-pagador-rede-2.0.0.tar.gz
  md5: 3ae94b66f3b7886a60ec2e73be9f2850
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - li-pagador-rede = li-pagador-rede:main
    #
    # Would create an entry point called li-pagador-rede that calls li-pagador-rede.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - li-pagador

  run:
    - python
    - distribute
    - li-pagador

test:
  # Python imports
  imports:
    - pagador_rede
    - pagador_rede.extensao
    - pagador_rede.reloaded

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lojaintegrada/LI-Pagador-Rede
  license: MIT License
  summary: 'Meio de pagamento usando o e-Rede (https://www.userede.com.br/)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
