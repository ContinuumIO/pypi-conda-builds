package:
  name: vunit
  version: "0.1.3"

source:
  fn: vunit-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/v/vunit/vunit-0.1.3.tar.gz
  md5: 7e0c3411bd3436b51de732304088ae79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vunit = vunit:main
    #
    # Would create an entry point called vunit that calls vunit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pysphere

  run:
    - python
    - pysphere

test:
  # Python imports
  imports:
    - vunit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gcavalcante8808
  license: Apache Software License
  summary: 'Vunit is a python package that relies on pysphere\n      library to provide tests for the Vmware Esxi and Vcenter environment.\n      With this tool, system admins can verify impacts on environments before or\n      after a maintenance.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
