package:
  name: distutils2
  version: "1.0a4"

source:
  fn: Distutils2-1.0a4.tar.gz
  url: https://pypi.python.org/packages/source/D/Distutils2/Distutils2-1.0a4.tar.gz
  md5: 52bc9dffb394970c27e02853ae3a3241
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distutils2 = distutils2:main
    #
    # Would create an entry point called distutils2 that calls distutils2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - distutils2
    - distutils2._backport
    - distutils2._backport.tests
    - distutils2.command
    - distutils2.compiler
    - distutils2.pypi
    - distutils2.tests
    - distutils2.tests.fixer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.python.org/moin/Distutils2
  license: Python Software Foundation License
  summary: u'Python Packaging Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
