package:
  name: m01.stub
  version: "0.5.8"

source:
  fn: m01.stub-0.5.8.zip
  url: https://pypi.python.org/packages/source/m/m01.stub/m01.stub-0.5.8.zip
  md5: 22447e9a5e8825df25606a853ccd8a75
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m01.stub = m01.stub:main
    #
    # Would create an entry point called m01.stub that calls m01.stub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - setuptools
    - pymongo

test:
  # Python imports
  imports:
    - m01
    - m01.stub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/m01.stub
  license: Zope Public License
  summary: 'MongoDB server stub setup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
