package:
  name: metagenomedb
  version: "0.2rev15"

source:
  fn: MetagenomeDB-0.2rev15.tar.gz
  url: https://pypi.python.org/packages/source/M/MetagenomeDB/MetagenomeDB-0.2rev15.tar.gz
  md5: f3bd10a1f0692ce64264e641ba54c38a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metagenomedb = metagenomedb:main
    #
    # Would create an entry point called metagenomedb that calls metagenomedb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - MetagenomeDB
    - MetagenomeDB.utils
    - metagenomedb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BioinformaticsCore/MetagenomeDB
  license: MIT License
  summary: 'Metagenome sequences and annotations database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
