package:
  name: adi.simplesite
  version: "0.6"

source:
  fn: adi.simplesite-0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/adi.simplesite/adi.simplesite-0.6.tar.gz
  md5: 278ce8d50acc73e8ff8e4563e0477dea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adi.simplesite = adi.simplesite:main
    #
    # Would create an entry point called adi.simplesite that calls adi.simplesite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - adi.dropdownmenu
    - adi.init
    - adi.samplecontent
    - adi.simplestructure
    - adi.slickstyle

  run:
    - python
    - setuptools
    - setuptools
    - adi.dropdownmenu
    - adi.init
    - adi.samplecontent
    - adi.simplestructure
    - adi.slickstyle

test:
  # Python imports
  imports:
    - adi
    - adi.simplesite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/adi.simplesite
  license: GPL
  summary: 'Configs for small sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
