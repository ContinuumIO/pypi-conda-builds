package:
  name: django_jinja_middleware
  version: "2.0.1"

source:
  fn: django_jinja_middleware-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_jinja_middleware/django_jinja_middleware-2.0.1.tar.gz
  md5: 1e56ab3e3832eb29f3156bd85eb498d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_jinja_middleware = django_jinja_middleware:main
    #
    # Would create an entry point called django_jinja_middleware that calls django_jinja_middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - jinja2 >=2.6
    - six >=1.4.1

  run:
    - python
    - django >=1.4
    - jinja2 >=2.6
    - six >=1.4.1

test:
  # Python imports
  imports:
    - django_jinja
    - django_jinja_middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lukesneeringer/django-jinja/
  license: BSD License
  summary: 'Django middleware class that takes TemplateResponses and renders them with Jinja, instead of the Django stock templating language. For Python 2 or Python 3, and Django >= 1.4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
