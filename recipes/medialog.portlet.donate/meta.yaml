package:
  name: medialog.portlet.donate
  version: "0.4"

source:
  fn: medialog.portlet.donate-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.portlet.donate/medialog.portlet.donate-0.4.tar.gz
  md5: 64dadc3d0a37a60cd5a02031fa612aae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.portlet.donate = medialog.portlet.donate:main
    #
    # Would create an entry point called medialog.portlet.donate that calls medialog.portlet.donate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - medialog
    - medialog.portlet
    - medialog.portlet.donate
    - medialog.portlet.donate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GPL
  summary: 'A simple donate button'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
