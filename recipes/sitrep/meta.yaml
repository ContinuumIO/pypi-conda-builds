package:
  name: sitrep
  version: "0.9.2"

source:
  fn: sitrep-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sitrep/sitrep-0.9.2.tar.gz
  md5: 00fea73c92668b2485e681c5c785568b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sitrep = sitrep:main
    #
    # Would create an entry point called sitrep that calls sitrep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netifaces
    - psutil
    - requests

  run:
    - python
    - netifaces
    - psutil
    - requests

test:
  # Python imports
  imports:
    - sitrep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vonbrownie/sitrep
  license: GNU General Public License v2 (GPLv2)
  summary: 'A console-based snapshot of Linux system resources'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
