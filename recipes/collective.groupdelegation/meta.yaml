package:
  name: collective.groupdelegation
  version: "0.4"

source:
  fn: collective.groupdelegation-0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.groupdelegation/collective.groupdelegation-0.4.zip
  md5: bf2cd86c77e113870b827297c16379af
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.groupdelegation = collective.groupdelegation:main
    #
    # Would create an entry point called collective.groupdelegation that calls collective.groupdelegation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.groupdelegation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.groupdelegation
  license: GPL
  summary: 'Plone Groups Management Delegation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
