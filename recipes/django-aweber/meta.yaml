package:
  name: django-aweber
  version: "0.3"

source:
  fn: django-aweber-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aweber/django-aweber-0.3.tar.gz
  md5: 74c69025b1eb1d451cf6b3ecad33e62a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aweber = django-aweber:main
    #
    # Would create an entry point called django-aweber that calls django-aweber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - python-emailahoy

  run:
    - python
    - python-emailahoy

test:
  # Python imports
  imports:
    - aweber

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/un33k/django-aweber
  license: BSD License
  summary: 'An Aweber based user-signup application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
