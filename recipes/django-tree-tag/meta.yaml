package:
  name: django-tree-tag
  version: "1.0"

source:
  fn: django-tree-tag-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tree-tag/django-tree-tag-1.0.tar.gz
  md5: c107f8b40206a232b20d8571d199364c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tree-tag = django-tree-tag:main
    #
    # Would create an entry point called django-tree-tag that calls django-tree-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tree_tag
    - tree_tag.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/shezi/django-tree-tag/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A simple module for recursion in Django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
