package:
  name: flask-gears
  version: "0.2"

source:
  fn: Flask-Gears-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Gears/Flask-Gears-0.2.tar.gz
  md5: b326f1fa4081cbf7109e1a9968862b5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-gears = flask-gears:main
    #
    # Would create an entry point called flask-gears that calls flask-gears.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - gears

  run:
    - python
    - flask
    - gears

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gears/flask-gears
  license: ISC License (ISCL)
  summary: 'Gears for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
