package:
  name: django-inplaceedit-extra-fields
  version: "0.5.0"

source:
  fn: django-inplaceedit-extra-fields-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inplaceedit-extra-fields/django-inplaceedit-extra-fields-0.5.0.tar.gz
  md5: 22232e2c349fcb97eac8c3bb691c44ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inplaceedit-extra-fields = django-inplaceedit-extra-fields:main
    #
    # Would create an entry point called django-inplaceedit-extra-fields that calls django-inplaceedit-extra-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-inplaceedit >=1.2.3

  run:
    - python
    - django-inplaceedit >=1.2.3

test:
  # Python imports
  imports:
    - inplaceeditform_extra_fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goinnn/django-inplaceedit-extra-fields
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application that adds other useful fields to Django inplace edit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
