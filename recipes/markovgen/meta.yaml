package:
  name: markovgen
  version: "0.4.1"

source:
  fn: markovgen-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/markovgen/markovgen-0.4.1.tar.gz
  md5: 4e19701e4edc4a23d4424bfd9d28b3a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - markovgen = markovgen:main
    #
    # Would create an entry point called markovgen that calls markovgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - markovgen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ProgVal/markovgen
  license: MIT License
  summary: 'Another text generator based on Markov chains.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
