package:
  name: hamster-sqlite
  version: "0.3"

source:
  fn: hamster-sqlite-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hamster-sqlite/hamster-sqlite-0.3.tar.gz
  md5: 18b3c593308f33129c779e649c188b8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hamster-sqlite = hamster-sqlite:main
    #
    # Would create an entry point called hamster-sqlite that calls hamster-sqlite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/projecthamster/hamster
  license: UNKNOWN
  summary: 'Minimal dependency nicely abstracted sqlite backend of hamster time tracker - lets you connect to your hamster db and do stuff in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
