package:
  name: django-xross
  version: "0.4.0"

source:
  fn: django-xross-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xross/django-xross-0.4.0.tar.gz
  md5: fbda09273242de3c734d86eb2eac8a48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xross = django-xross:main
    #
    # Would create an entry point called django-xross that calls django-xross.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xross

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/idlesign/django-xross
  license: BSD License
  summary: 'Reusable application for Django nicely bridging client and server sides.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
