package:
  name: master-sake
  version: "0.9.6"

source:
  fn: master-sake-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/m/master-sake/master-sake-0.9.6.tar.gz
  md5: db0f66558626d099405034c526bfa20f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - master-sake = master-sake:main
    #
    # Would create an entry point called master-sake that calls master-sake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - networkx >=1.0
    - pyyaml >=3.0

  run:
    - python
    - networkx >=1.0
    - pyyaml >=3.0

test:
  # Python imports
  imports:
    - sakelib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tonyfischetti.github.io/sake
  license: MIT License
  summary: u'A self-documenting build automation tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
