package:
  name: btcnet_info
  version: "0.1.2.36"

source:
  fn: btcnet_info-0.1.2.36.tar.gz
  url: https://pypi.python.org/packages/source/b/btcnet_info/btcnet_info-0.1.2.36.tar.gz
  md5: 4ffc7c3114abed47926bb1820689fd8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - btcnet_info = btcnet_info:main
    #
    # Would create an entry point called btcnet_info that calls btcnet_info.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent
    - httplib2

  run:
    - python
    - gevent
    - httplib2

test:
  # Python imports
  imports:
    - btcnet_info

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: github.com/c00w/btcnet_info
  license: MIT License
  summary: 'BitCoin Network Information Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
