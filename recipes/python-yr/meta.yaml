package:
  name: python-yr
  version: "1.4.2"

source:
  fn: python-yr-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-yr/python-yr-1.4.2.tar.gz
  md5: 7913c7f4387498f4eedf65711c41ae48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-yr = python-yr:main
    #
    # Would create an entry point called python-yr that calls python-yr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmltodict

  run:
    - python
    - xmltodict

test:
  # Python imports
  imports:
    - yr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wckd/python-yr
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Get the forecast from the norwegian wheather service yr.no in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
