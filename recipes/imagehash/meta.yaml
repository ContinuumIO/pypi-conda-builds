package:
  name: imagehash
  version: "0.3"

source:
  fn: ImageHash-0.3.tar.gz
  url: https://pypi.python.org/packages/source/I/ImageHash/ImageHash-0.3.tar.gz
  md5: 5544dbdd8c79b06bdc9e7dacbfe3ac3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imagehash = imagehash:main
    #
    # Would create an entry point called imagehash that calls imagehash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scipy
    - numpy
    - pillow

  run:
    - python
    - scipy
    - numpy
    - pillow

test:
  # Python imports
  imports:
    - imagehash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JohannesBuchner/imagehash
  license: LICENSE
  summary: 'Image Hashing library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
