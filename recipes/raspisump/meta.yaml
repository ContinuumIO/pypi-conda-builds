package:
  name: raspisump
  version: "0.6.1.1"

source:
  fn: raspisump-0.6.1.1.tar.gz
  url: https://pypi.python.org/packages/source/r/raspisump/raspisump-0.6.1.1.tar.gz
  md5: ad2eea60075649d8682f7210746de421
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raspisump = raspisump:main
    #
    # Would create an entry point called raspisump that calls raspisump.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hcsr04sensor

  run:
    - python
    - hcsr04sensor

test:
  # Python imports
  imports:
    - raspisump

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linuxnorth.org/raspi-sump/
  license: MIT License
  summary: 'A sump pit monitoring system for Raspberry Pi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
