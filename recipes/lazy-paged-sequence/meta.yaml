package:
  name: lazy-paged-sequence
  version: "0.3"

source:
  fn: lazy_paged_sequence-0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/lazy-paged-sequence/lazy_paged_sequence-0.3.tar.gz
  md5: 3ea4e0ba99285f94ebf71a7bb4de0ca9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazy-paged-sequence = lazy-paged-sequence:main
    #
    # Would create an entry point called lazy-paged-sequence that calls lazy-paged-sequence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: AGPL
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
