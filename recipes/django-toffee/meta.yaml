package:
  name: django-toffee
  version: "0.26"

source:
  fn: django-toffee-0.26.tar.gz
  url: https://pypi.python.org/packages/source/d/django-toffee/django-toffee-0.26.tar.gz
  md5: b5f41d7059659ba0a85fd6504e8b37aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-toffee = django-toffee:main
    #
    # Would create an entry point called django-toffee that calls django-toffee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-bootstrap3
    - django-eztables
    - django-form-utils

  run:
    - python
    - django-bootstrap3
    - django-eztables
    - django-form-utils

test:
  # Python imports
  imports:
    - toffee

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bingorabbit.com/
  license: BSD License
  summary: 'A simple Django app to layout your applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
