package:
  name: bronto-python
  version: "0.8.0"

source:
  fn: bronto-python-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bronto-python/bronto-python-0.8.0.tar.gz
  md5: b5b990aa00d84c28f279e09f0d8d4416
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bronto-python = bronto-python:main
    #
    # Would create an entry point called bronto-python that calls bronto-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds-jurko
    - six

  run:
    - python
    - suds-jurko
    - six

test:
  # Python imports
  imports:
    - bronto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Scotts-Marketplace/bronto-python/
  license: Apache Software License
  summary: "A python wrapper around Bronto's SOAP API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
