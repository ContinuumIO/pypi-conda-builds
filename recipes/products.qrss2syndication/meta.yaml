package:
  name: products.qrss2syndication
  version: "0.5.1"

source:
  fn: Products.qRSS2Syndication-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.qRSS2Syndication/Products.qRSS2Syndication-0.5.1.tar.gz
  md5: 7e8fde2adcd76865f94b9cff65452de3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.qrss2syndication = products.qrss2syndication:main
    #
    # Would create an entry point called products.qrss2syndication that calls products.qrss2syndication.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.qRSS2Syndication

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.qRSS2Syndication
  license: UNKNOWN
  summary: 'Extended syndication support for Plone 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
