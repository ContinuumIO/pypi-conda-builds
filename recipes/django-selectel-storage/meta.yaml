package:
  name: django-selectel-storage
  version: "0.2"

source:
  fn: django-selectel-storage-0.2.tar.bz2
  url: https://pypi.python.org/packages/source/d/django-selectel-storage/django-selectel-storage-0.2.tar.bz2
  md5: 9a08d033fbbf5838be216a474a66aa79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-selectel-storage = django-selectel-storage:main
    #
    # Would create an entry point called django-selectel-storage that calls django-selectel-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - selectel-api

  run:
    - python
    - requests
    - selectel-api

test:
  # Python imports
  imports:
    - django_selectel_storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: u'This application allows you easily save media and static files into selectel cloud storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
