package:
  name: massweb
  version: "0.3.1"

source:
  fn: massweb-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/m/massweb/massweb-0.3.1.tar.gz
  md5: 8fdbdd844288842c8d7957a4054b4e6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - massweb = massweb:main
    #
    # Would create an entry point called massweb that calls massweb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4
    - html5lib

  run:
    - python
    - requests
    - beautifulsoup4
    - html5lib

test:
  # Python imports
  imports:
    - massweb
    - massweb.fuzz_generators
    - massweb.fuzzers
    - massweb.mass_requests
    - massweb.masscrawler
    - massweb.payloads
    - massweb.pnk_net
    - massweb.proxy_rotator
    - massweb.results
    - massweb.targets
    - massweb.vuln_checks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/acaceres/massweb
  license: Apache 2.0
  summary: 'Fast Web Fuzzing and Scanning'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
