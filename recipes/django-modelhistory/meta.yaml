package:
  name: django-modelhistory
  version: "0.0.2"

source:
  fn: django-modelhistory-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modelhistory/django-modelhistory-0.0.2.tar.gz
  md5: 94005f62642c7e9088b8c23bcbe3d73b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modelhistory = django-modelhistory:main
    #
    # Would create an entry point called django-modelhistory that calls django-modelhistory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - modelhistory
    - modelhistory.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xando/django-modelhistory
  license: BSD License
  summary: 'django-modelhistory reusable application for loggin models changes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
