package:
  name: aweber_api
  version: "1.4.0"

source:
  fn: aweber_api-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aweber_api/aweber_api-1.4.0.tar.gz
  md5: b6c04e5a07592d5d02114ff8a014a849
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aweber_api = aweber_api:main
    #
    # Would create an entry point called aweber_api that calls aweber_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.9.0,<=0.10.0
    - oauth2 >=1.2

  run:
    - python
    - httplib2 >=0.9.0,<=0.10.0
    - oauth2 >=1.2

test:
  # Python imports
  imports:
    - aweber_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aweber/AWeber-API-Python-Library
  license: BSD License
  summary: 'A python client library for the AWeber API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
