package:
  name: botparse
  version: "0.1.0"

source:
  fn: BotParse-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BotParse/BotParse-0.1.0.tar.gz
  md5: 53fbb23edb9948500dd57f36f5ff93bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - botparse = botparse:main
    #
    # Would create an entry point called botparse that calls botparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - botparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cameronbwhite/BotParse
  license: BSD License
  summary: 'A modified version of argparse to make bot making easer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
