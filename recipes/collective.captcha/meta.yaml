package:
  name: collective.captcha
  version: "1.7"

source:
  fn: collective.captcha-1.7.zip
  url: https://pypi.python.org/packages/source/c/collective.captcha/collective.captcha-1.7.zip
  md5: bc65090fd026b538ccbbce21e40a579e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.captcha = collective.captcha:main
    #
    # Would create an entry point called collective.captcha that calls collective.captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - skimpygimpy
    - plone.keyring >1.0

  run:
    - python
    - setuptools
    - setuptools
    - skimpygimpy
    - plone.keyring >1.0

test:
  # Python imports
  imports:
    - collective
    - collective.captcha
    - collective.captcha.browser
    - collective.captcha.form

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjpieters/collective.captcha
  license: GNU General Public License (GPL)
  summary: 'Stateless captcha generation and verification'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
