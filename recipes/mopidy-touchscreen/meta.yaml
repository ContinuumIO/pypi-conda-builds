package:
  name: mopidy-touchscreen
  version: "0.3.2"

source:
  fn: Mopidy-Touchscreen-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Touchscreen/Mopidy-Touchscreen-0.3.2.tar.gz
  md5: 04dff31d43da84a292f3064464b26cec
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-touchscreen = mopidy-touchscreen:main
    #
    # Would create an entry point called mopidy-touchscreen that calls mopidy-touchscreen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - pygame

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - pygame

test:
  # Python imports
  imports:
    - mopidy_touchscreen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/9and3r/mopidy-touchscreen
  license: Apache Software License
  summary: u'Mopidy extension to show info on a display and control from it'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
