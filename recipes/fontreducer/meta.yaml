package:
  name: fontreducer
  version: "0.0.1"

source:
  fn: FontReducer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FontReducer/FontReducer-0.0.1.tar.gz
  md5: c14ea678fd6cbe1a87e0e9ae2cdf9de7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fontreducer = fontreducer:main
    #
    # Would create an entry point called fontreducer that calls fontreducer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fontreducer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hachibeeDI/FontReducer
  license: Apache Software License
  summary: u'Generate font subsets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
