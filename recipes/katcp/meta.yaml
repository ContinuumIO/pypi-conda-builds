package:
  name: katcp
  version: "0.5.5"

source:
  fn: katcp-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/k/katcp/katcp-0.5.5.tar.gz
  md5: a8ecec507c2a99878c7df71d986130c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - katcp = katcp:main
    #
    # Would create an entry point called katcp that calls katcp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ply
    - twisted
    - nose
    - unittest2
    - mock
    - proxytypes

  run:
    - python
    - ply
    - twisted
    - nose
    - unittest2
    - mock
    - proxytypes

test:
  # Python imports
  imports:
    - katcp
    - katcp.test
    - katcp.tx
    - katcp.tx.test
    - scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ska-sa/katcp-python
  license: BSD License
  summary: 'Karoo Array Telescope Communication Protocol library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
