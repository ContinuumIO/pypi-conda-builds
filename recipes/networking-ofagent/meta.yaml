package:
  name: networking-ofagent
  version: "2015.1.4"

source:
  fn: networking-ofagent-2015.1.4.tar.gz
  url: https://pypi.python.org/packages/source/n/networking-ofagent/networking-ofagent-2015.1.4.tar.gz
  md5: bf3b89cf86e732bf5fa0170f1d198c40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - networking-ofagent = networking-ofagent:main
    #
    # Would create an entry point called networking-ofagent that calls networking-ofagent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Networking ofagent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
