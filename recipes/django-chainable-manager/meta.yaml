package:
  name: django-chainable-manager
  version: "0.5.0"

source:
  fn: django-chainable-manager-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chainable-manager/django-chainable-manager-0.5.0.tar.gz
  md5: b8e86fe00a1b3958bb07db856ae01ab8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chainable-manager = django-chainable-manager:main
    #
    # Would create an entry point called django-chainable-manager that calls django-chainable-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - chainablemanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-chainable-manager
  license: UNKNOWN
  summary: 'Chainable methods on Model managers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
