package:
  name: brokenegg
  version: "0.0.0"

source:
  fn: brokenegg-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/brokenegg/brokenegg-0.0.0.tar.gz
  md5: 971bc20f9aec3f7842b23f74b839deeb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brokenegg = brokenegg:main
    #
    # Would create an entry point called brokenegg that calls brokenegg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brokenegg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Ann egg with SyntaxErrors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
