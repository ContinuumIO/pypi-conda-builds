package:
  name: plonetheme.andreas01
  version: "0.1.1dev-r60306"

source:
  fn: plonetheme.andreas01-0.1.1dev-r60306.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.andreas01/plonetheme.andreas01-0.1.1dev-r60306.tar.gz
  md5: 2c72baf76c32728bed98e374b9ca3007
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.andreas01 = plonetheme.andreas01:main
    #
    # Would create an entry point called plonetheme.andreas01 that calls plonetheme.andreas01.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.andreas01
    - plonetheme.andreas01.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'An installable theme for Plone 3.0.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
