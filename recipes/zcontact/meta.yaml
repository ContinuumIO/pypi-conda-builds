package:
  name: zcontact
  version: "0.1.0a11"

source:
  fn: zcontact-0.1.0a11.tar.gz
  url: https://pypi.python.org/packages/source/z/zcontact/zcontact-0.1.0a11.tar.gz
  md5: 73ec0edd67efa3133f3f848df78d40e1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zcontact = zcontact:main
    #
    # Would create an entry point called zcontact that calls zcontact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pastescript
    - zope.app.appsetup
    - zope.app.authentication
    - zope.app.component
    - zope.app.container
    - zope.app.error
    - zope.app.form
    - zope.app.publisher
    - zope.app.publication
    - zope.app.security
    - zope.app.securitypolicy
    - zope.app.twisted
    - zope.app.wsgi
    - zope.app.i18n
    - zope.app.rotterdam
    - zope.app.zcmlfiles
    - zope.contentprovider
    - zope.testbrowser
    - docutils ==0.4.0
    - jquery.javascript
    - jquery.layer
    - setuptools
    - z3c.form
    - z3c.formjs >=0.3
    - z3c.formui
    - z3c.layer
    - z3c.pagelet
    - z3c.template
    - z3c.viewlet
    - z3c.zrtresource
    - z3c.menu
    - zope.viewlet

  run:
    - python
    - setuptools
    - pastescript
    - zope.app.appsetup
    - zope.app.authentication
    - zope.app.component
    - zope.app.container
    - zope.app.error
    - zope.app.form
    - zope.app.publisher
    - zope.app.publication
    - zope.app.security
    - zope.app.securitypolicy
    - zope.app.twisted
    - zope.app.wsgi
    - zope.app.i18n
    - zope.app.rotterdam
    - zope.app.zcmlfiles
    - zope.contentprovider
    - zope.testbrowser
    - docutils ==0.4.0
    - jquery.javascript
    - jquery.layer
    - setuptools
    - z3c.form
    - z3c.formjs >=0.3
    - z3c.formui
    - z3c.layer
    - z3c.pagelet
    - z3c.template
    - z3c.viewlet
    - z3c.zrtresource
    - z3c.menu
    - zope.viewlet

test:
  # Python imports
  imports:
    - zcontact
    - zcontact.app
    - zcontact.browser
    - zcontact.generations
    - zcontact.installer
    - zcontact.skin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/zcontact
  license: GPL 3
  summary: 'An online contact manager built on Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
