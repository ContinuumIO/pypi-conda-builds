package:
  name: zc.recipe.rhrc
  version: "1.4.2"

source:
  fn: zc.recipe.rhrc-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.recipe.rhrc/zc.recipe.rhrc-1.4.2.tar.gz
  md5: 97e03cf373886a140f2b09aaba01ea23
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.recipe.rhrc = zc.recipe.rhrc:main
    #
    # Would create an entry point called zc.recipe.rhrc that calls zc.recipe.rhrc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.recipe
    - zc.recipe.rhrc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'ZC Buildout recipe for Redhat RC scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
