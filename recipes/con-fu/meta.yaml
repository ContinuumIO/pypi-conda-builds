package:
  name: con-fu
  version: "0.1.6"

source:
  fn: con-fu-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/con-fu/con-fu-0.1.6.tar.gz
  md5: d18cd32656ac984eed541816fe5e6967
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - con-fu = con-fu:main
    #
    # Would create an entry point called con-fu that calls con-fu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansible
    - boto
    - troposphere
    - click
    - pilo >=0.4,<0.5
    - virtualenv
    - virtualenv-relocate

  run:
    - python
    - ansible
    - boto
    - troposphere
    - click
    - pilo >=0.4,<0.5
    - virtualenv
    - virtualenv-relocate

test:
  # Python imports
  imports:
    - confu
    - confu.shell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bninja/confu
  license: BSD License
  summary: 'Fu for con.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
