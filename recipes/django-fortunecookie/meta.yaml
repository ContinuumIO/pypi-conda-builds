package:
  name: django-fortunecookie
  version: "0.1.5"

source:
  fn: django-fortunecookie-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fortunecookie/django-fortunecookie-0.1.5.tar.gz
  md5: 2e6b6362bb8612c934699a56e4a0a3f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fortunecookie = django-fortunecookie:main
    #
    # Would create an entry point called django-fortunecookie that calls django-fortunecookie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-sortedm2m >=0.4.0

  run:
    - python
    - django >=1.3
    - django-sortedm2m >=0.4.0

test:
  # Python imports
  imports:
    - fortunecookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://projects.ninemoreminutes.com/projects/django-fortunecookie/
  license: BSD License
  summary: 'Django models to store everything about fortune cookies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
