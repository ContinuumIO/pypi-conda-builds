package:
  name: django-duat
  version: "0.7"

source:
  fn: django-duat-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-duat/django-duat-0.7.tar.gz
  md5: a43e2398e4aae7fb494dadc14d3865b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-duat = django-duat:main
    #
    # Would create an entry point called django-duat that calls django-duat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - duat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrispbailey/duat/
  license: MIT License
  summary: 'A lightweight Django User Acceptance Testing feedback tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
