package:
  name: doapfiend-ebuild
  version: "0.1.1"

source:
  fn: doapfiend-ebuild-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/doapfiend-ebuild/doapfiend-ebuild-0.1.1.tar.gz
  md5: 390db5128009639da8a9319bd747e208
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doapfiend-ebuild = doapfiend-ebuild:main
    #
    # Would create an entry point called doapfiend-ebuild that calls doapfiend-ebuild.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - doapfiend >=0.3.1
    - cheetah

  run:
    - python
    - setuptools
    - doapfiend >=0.3.1
    - cheetah

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.doapspace.org/doapfiend/wiki/DfEbuild
  license: BSD-2
  summary: 'Doapfiend plugin. Create Gentoo Linux ebuild from DOAP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
