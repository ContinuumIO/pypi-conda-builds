package:
  name: gozerplugs
  version: "0.9.3b2"

source:
  fn: gozerplugs-0.9.3b2.tar.gz
  url: https://pypi.python.org/packages/source/g/gozerplugs/gozerplugs-0.9.3b2.tar.gz
  md5: 1c6e3d686ed1b264c287d8e67cb1598a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gozerplugs = gozerplugs:main
    #
    # Would create an entry point called gozerplugs that calls gozerplugs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gozerplugs
    - gozerplugs.tracserver
    - gozerplugs.webserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gozerbot.googlecode.com
  license: BSD
  summary: 'plugin package for gozerbot '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
