package:
  name: collective.recipe.template
  version: "1.11"

source:
  fn: collective.recipe.template-1.11.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.template/collective.recipe.template-1.11.zip
  md5: 3e3268233b407c88cc81825441628762
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.template = collective.recipe.template:main
    #
    # Would create an entry point called collective.recipe.template that calls collective.recipe.template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.recipe.template
  license: BSD
  summary: 'Buildout recipe to generate a text file from a template'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
