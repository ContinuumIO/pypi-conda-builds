package:
  name: bets-api
  version: "0.0.8"

source:
  fn: bets-api-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/b/bets-api/bets-api-0.0.8.tar.gz
  md5: 91ca84e95a3e5640a688e7ad7a2a339b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bets-api = bets-api:main
    #
    # Would create an entry point called bets-api that calls bets-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=1.0.1
    - requests >=2.2.1

  run:
    - python
    - gevent >=1.0.1
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - bets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/42cc/bets-api
  license: GNU General Public License v2 (GPLv2)
  summary: 'A wrapper over bets.42cc.co API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
