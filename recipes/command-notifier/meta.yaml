package:
  name: command-notifier
  version: "0.0.3"

source:
  fn: command-notifier-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/command-notifier/command-notifier-0.0.3.tar.gz
  md5: 549c4fe51b74bdbb33c2e2329c0c981d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - command-notifier = command-notifier:main
    #
    # Would create an entry point called command-notifier that calls command-notifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.27.0

  run:
    - python
    - boto >=2.27.0

test:
  # Python imports
  imports:
    - command_notifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/josegonzalez/python-command-notifier
  license: MIT License
  summary: 'A very simple command-notification packagebased on SQS for distributed systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
