package:
  name: flask-lazyviews
  version: "0.6"

source:
  fn: Flask-LazyViews-0.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-LazyViews/Flask-LazyViews-0.6.tar.gz
  md5: 13a83151d89c001854ddcdff06c21481
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-lazyviews = flask-lazyviews:main
    #
    # Would create an entry point called flask-lazyviews that calls flask-lazyviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_lazyviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/playpauseandstop/Flask-LazyViews
  license: BSD License
  summary: 'Registers URL routes for Flask application or blueprint in lazy way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
