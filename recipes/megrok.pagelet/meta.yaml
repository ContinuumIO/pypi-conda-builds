package:
  name: megrok.pagelet
  version: "0.1"

source:
  fn: megrok.pagelet-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.pagelet/megrok.pagelet-0.1.tar.gz
  md5: 72f051f6a64fc5c8c894ceeed7c985f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.pagelet = megrok.pagelet:main
    #
    # Would create an entry point called megrok.pagelet that calls megrok.pagelet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - martian
    - grokcore.component
    - z3c.template
    - grok

  run:
    - python
    - setuptools
    - setuptools
    - martian
    - grokcore.component
    - z3c.template
    - grok

test:
  # Python imports
  imports:
    - megrok
    - megrok.pagelet
    - megrok.pagelet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/megrok.pagelet
  license: Zope Public License
  summary: 'z3c.template / z3c.layout support for Grok'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
