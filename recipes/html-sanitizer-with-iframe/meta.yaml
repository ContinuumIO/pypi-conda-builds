package:
  name: html-sanitizer-with-iframe
  version: "0.1.0"

source:
  fn: HTML-Sanitizer-With-IFrame-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/H/HTML-Sanitizer-With-IFrame/HTML-Sanitizer-With-IFrame-0.1.0.tar.gz
  md5: 96e61a5ddcd66f7cd471527e9e0f2f81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html-sanitizer-with-iframe = html-sanitizer-with-iframe:main
    #
    # Would create an entry point called html-sanitizer-with-iframe that calls html-sanitizer-with-iframe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html5lib

  run:
    - python
    - html5lib

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jaza/html-sanitizer-with-iframe
  license: UNKNOWN
  summary: "Extends Python html5lib's sanitizer.HTMLSanitizer to allow iframe elements."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
