package:
  name: django-automatic-links
  version: "0.2.2"

source:
  fn: django-automatic-links-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-automatic-links/django-automatic-links-0.2.2.tar.gz
  md5: dffbea2c826f46dde0d345acdd52d3ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-automatic-links = django-automatic-links:main
    #
    # Would create an entry point called django-automatic-links that calls django-automatic-links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - automatic_links

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silentsokolov/django-automatic-links
  license: MIT License
  summary: 'Application for Django, that allows you to adding keywords that will be automatically converted into links.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
