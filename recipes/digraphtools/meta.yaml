package:
  name: digraphtools
  version: "0.2.1"

source:
  fn: digraphtools-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/digraphtools/digraphtools-0.2.1.tar.gz
  md5: 4adce3e37f577b056e3fe52e3a2f3829
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digraphtools = digraphtools:main
    #
    # Would create an entry point called digraphtools that calls digraphtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - digraphtools
    - digraphtools.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dbasden/python-digraphtools
  license: BSD License
  summary: 'Some tools for working with digraphs, partial orders and topological sorting with Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
