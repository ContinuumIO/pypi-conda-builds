package:
  name: plone.recipe.dzhandle
  version: "1.0-beta4"

source:
  fn: plone.recipe.dzhandle-1.0-beta4.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.dzhandle/plone.recipe.dzhandle-1.0-beta4.tar.gz
  md5: 349e632824f11246ee3d37f34996816c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.dzhandle = plone.recipe.dzhandle:main
    #
    # Would create an entry point called plone.recipe.dzhandle that calls plone.recipe.dzhandle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.dzhandle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/plone.recipe.dzhandle
  license: Zope Public License
  summary: "ZC Buildout control for Debians zope instance handler 'dzhandle'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
