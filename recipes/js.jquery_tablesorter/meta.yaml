package:
  name: js.jquery_tablesorter
  version: "2.0.5"

source:
  fn: js.jquery_tablesorter-2.0.5.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_tablesorter/js.jquery_tablesorter-2.0.5.tar.gz
  md5: 55b5d2e921f1fd175910b164ec384961
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_tablesorter = js.jquery_tablesorter:main
    #
    # Would create an entry point called js.jquery_tablesorter that calls js.jquery_tablesorter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery
    - js.jquery_metadata

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery
    - js.jquery_metadata

test:
  # Python imports
  imports:
    - js
    - js.jquery_tablesorter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jQuery tablesorter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
