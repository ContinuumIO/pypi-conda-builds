package:
  name: infopython
  version: "0.3.1"

source:
  fn: infopython-0.3.1.tar.gz
  url: http://bitbucket.org/leliel12/infopython/downloads/infopython-0.3.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infopython = infopython:main
    #
    # Would create an entry point called infopython that calls infopython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached
    - numpy >=1.6.1
    - scipy

  run:
    - python
    - python-memcached
    - numpy >=1.6.1
    - scipy

test:
  # Python imports
  imports:
    - infopython
    - infopython.cache
    - infopython.libs
    - infopython.libs.bs4
    - infopython.libs.bs4.builder
    - infopython.libs.bs4.tests
    - infopython.util
    - tests
    - tests.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/leliel12/infopython
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'An library for valuate information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
