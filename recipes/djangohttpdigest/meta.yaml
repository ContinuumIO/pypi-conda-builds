package:
  name: djangohttpdigest
  version: "0.2.3"

source:
  fn: djangohttpdigest-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangohttpdigest/djangohttpdigest-0.2.3.tar.gz
  md5: 29fb05b609dd37f76d92fa13efb69138
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangohttpdigest = djangohttpdigest:main
    #
    # Would create an entry point called djangohttpdigest that calls djangohttpdigest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangohttpdigest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://devel.almad.net/trac/django-http-digest/
  license: BSD License
  summary: 'Support for HTTP digest in Django web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
