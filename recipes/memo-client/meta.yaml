package:
  name: memo-client
  version: "0.3"

source:
  fn: memo-client-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/memo-client/memo-client-0.3.tar.gz
  md5: 80eee221b89e693ee65c4aec12f33779
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memo-client = memo-client:main
    #
    # Would create an entry point called memo-client that calls memo-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - memo_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amirouche/Memo
  license: LGPL
  summary: 'Memo is a Redis clone wanna be in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
