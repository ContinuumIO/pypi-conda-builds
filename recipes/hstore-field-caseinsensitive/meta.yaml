package:
  name: hstore-field-caseinsensitive
  version: "1.0.2"

source:
  fn: hstore-field-caseinsensitive-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hstore-field-caseinsensitive/hstore-field-caseinsensitive-1.0.2.tar.gz
  md5: 0aacaad5cc70feb6c04672eaeb955c76
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hstore-field-caseinsensitive = hstore-field-caseinsensitive:main
    #
    # Would create an entry point called hstore-field-caseinsensitive that calls hstore-field-caseinsensitive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hstore_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: git@github.com:anantasty/hstore-field.git
  license: BSD
  summary: "Support for PostgreSQL's hstore for Django."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
