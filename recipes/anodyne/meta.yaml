package:
  name: anodyne
  version: "0.0.7"

source:
  fn: anodyne-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/a/anodyne/anodyne-0.0.7.tar.gz
  md5: f09b4f88456657b299d0b8f694ba9e1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anodyne = anodyne:main
    #
    # Would create an entry point called anodyne that calls anodyne.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy ==0.9.7

  run:
    - python
    - sqlalchemy ==0.9.7

test:
  # Python imports
  imports:
    - anodyne

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amilstead/anodyne
  license: GNU General Public License (GPL)
  summary: 'SQLAlchemy Database Utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
