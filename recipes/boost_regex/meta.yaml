package:
  name: boost_regex
  version: "0.57"

source:
  fn: boost_regex-0.57.zip
  url: https://pypi.python.org/packages/source/b/boost_regex/boost_regex-0.57.zip
  md5: bb2a6a416375d6455e73cb81a31bdc6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boost_regex = boost_regex:main
    #
    # Would create an entry point called boost_regex that calls boost_regex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - boost_regex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/boost_regex/
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'Very basic interface to the boost regex library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
