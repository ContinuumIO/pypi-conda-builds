package:
  name: abl.errorreporter
  version: "0.3.1"

source:
  fn: abl.errorreporter-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/abl.errorreporter/abl.errorreporter-0.3.1.tar.gz
  md5: 4b8321c13bfb8c75e5480f216b3ad9f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abl.errorreporter = abl.errorreporter:main
    #
    # Would create an entry point called abl.errorreporter that calls abl.errorreporter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - turbomail
    - genshi

  run:
    - python
    - turbomail
    - genshi

test:
  # Python imports
  imports:
    - errorreporter
    - errorreporter.util
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Lightweight exception recorder, cli part of WebError'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
