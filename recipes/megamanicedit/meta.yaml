package:
  name: megamanicedit
  version: "2.0.15b"

source:
  fn: MegamanicEdit-2.0.15b.tar.gz
  url: https://pypi.python.org/packages/source/M/MegamanicEdit/MegamanicEdit-2.0.15b.tar.gz
  md5: a653cec121271829d2c2e2a6745cdf41
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megamanicedit = megamanicedit:main
    #
    # Would create an entry point called megamanicedit that calls megamanicedit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - variousdisplaywidgets

  run:
    - python
    - setuptools
    - setuptools
    - variousdisplaywidgets

test:
  # Python imports
  imports:
    - MegamanicEdit
    - MegamanicEdit.MegamanicEdit
    - MegamanicEdit.MegamanicEdit.browser
    - megamanicedit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nidelven-it.no/d
  license: GNU General Public License (GPL)
  summary: 'Batch and simplified editing of Archetypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
