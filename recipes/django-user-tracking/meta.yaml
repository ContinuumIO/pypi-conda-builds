package:
  name: django-user-tracking
  version: "0.4.11"

source:
  fn: django-user-tracking-0.4.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-tracking/django-user-tracking-0.4.11.tar.gz
  md5: 3abe9733a9ac81354661030737c7bf68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-tracking = django-user-tracking:main
    #
    # Would create an entry point called django-user-tracking that calls django-user-tracking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tracking
    - tracking.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/elevenbits/django-user-tracking
  license: MIT License
  summary: 'Basic visitor tracking and blacklisting for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
