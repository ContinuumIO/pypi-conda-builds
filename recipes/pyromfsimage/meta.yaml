package:
  name: pyromfsimage
  version: "0.2"

source:
  fn: PyRomfsImage-0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/PyRomfsImage/PyRomfsImage-0.2.tar.gz
  md5: 885d47527b9e18d77f2c5ec2d27aa59b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyromfsimage = pyromfsimage:main
    #
    # Would create an entry point called pyromfsimage that calls pyromfsimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - PyRomfsImage
    - pyromfsimage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matteomattei/PyRomfsImage
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'ROMfs image parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
