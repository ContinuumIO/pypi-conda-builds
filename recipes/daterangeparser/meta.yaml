package:
  name: daterangeparser
  version: "1.2"

source:
  fn: DateRangeParser-1.2.zip
  url: https://pypi.python.org/packages/source/D/DateRangeParser/DateRangeParser-1.2.zip
  md5: 307be0d4efe5977ec83668d3895db791
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daterangeparser = daterangeparser:main
    #
    # Would create an entry point called daterangeparser that calls daterangeparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing

  run:
    - python
    - pyparsing

test:
  # Python imports
  imports:
    - daterangeparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robintw/daterangeparser
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'Module to parse human-style date ranges (eg. 15th-19th March 2011) to datetimes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
