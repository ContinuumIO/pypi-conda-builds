package:
  name: hot-redis
  version: "0.3"

source:
  fn: hot-redis-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hot-redis/hot-redis-0.3.tar.gz
  md5: 66772e4c6556b349396973ffd3fc4393
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hot-redis = hot-redis:main
    #
    # Would create an entry point called hot-redis that calls hot-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx-me
    - redis

  run:
    - python
    - sphinx-me
    - redis

test:
  # Python imports
  imports:
    - hot_redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stephenmcd/hot-redis
  license: BSD License
  summary: 'Higher Order Types for Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
