package:
  name: athentoimaging
  version: "0.1"

source:
  fn: athentoimaging-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/athentoimaging/athentoimaging-0.1.tar.gz
  md5: df33b6cdd7390ce009e560adcdf0269b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - athentoimaging = athentoimaging:main
    #
    # Would create an entry point called athentoimaging that calls athentoimaging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - numpy

  run:
    - python
    - argparse
    - numpy

test:
  # Python imports
  imports:
    - athentoimaging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.athento.com/
  license: Athento Shared Source License
  summary: 'Image cleaning and OCR improvement package in Python using OpenCV.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
