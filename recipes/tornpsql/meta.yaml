package:
  name: tornpsql
  version: "1.0.2"

source:
  fn: tornpsql-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornpsql/tornpsql-1.0.2.tar.gz
  md5: 5dfa15bfd0cc7695c844de4ffb2f0493
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornpsql = tornpsql:main
    #
    # Would create an entry point called tornpsql that calls tornpsql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2 ==2.5.2

  run:
    - python
    - psycopg2 ==2.5.2

test:
  # Python imports
  imports:
    - tornpsql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stevepeak/tornpsql
  license: Apache Software License
  summary: 'PostgreSQL handler for Tornado Web'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
