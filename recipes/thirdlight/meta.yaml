package:
  name: thirdlight
  version: "0.1.0"

source:
  fn: thirdlight-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/thirdlight/thirdlight-0.1.0.tar.gz
  md5: dc66f5c3ec79963d6c816df6ea14c4de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thirdlight = thirdlight:main
    #
    # Would create an entry point called thirdlight that calls thirdlight.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - thirdlight

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Rethought/thirdlight.git
  license: BSD License
  summary: 'Python API library for the ThirdLight image management system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
