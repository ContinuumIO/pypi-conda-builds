package:
  name: govuk-template
  version: "0.2.41"

source:
  fn: govuk-template-0.2.41.tar.gz
  url: https://pypi.python.org/packages/source/g/govuk-template/govuk-template-0.2.41.tar.gz
  md5: ca9112b80bb681f49c0df172f1152d13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - govuk-template = govuk-template:main
    #
    # Would create an entry point called govuk-template that calls govuk-template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pystache

  run:
    - python
    - flask
    - pystache

test:
  # Python imports
  imports:
    - govuk_template
    - govuk_template.flask.assets
    - govuk_template.flask.mustache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LandRegistry/python-govuk-template
  license: MIT License
  summary: 'The GOV.UK template for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
