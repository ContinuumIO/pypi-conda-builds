package:
  name: django-model-mixins
  version: "1.0.4"

source:
  fn: django-model-mixins-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-mixins/django-model-mixins-1.0.4.tar.gz
  md5: e086814af5be117bdcbdc8c92c56c42c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-mixins = django-model-mixins:main
    #
    # Would create an entry point called django-model-mixins that calls django-model-mixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - modelmixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tbartelmess/django-model-mixins
  license: BSD License
  summary: 'Simple helper to extend django models using mixins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
