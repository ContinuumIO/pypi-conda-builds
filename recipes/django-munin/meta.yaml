package:
  name: django-munin
  version: "0.2.0"

source:
  fn: django-munin-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-munin/django-munin-0.2.0.tar.gz
  md5: 435233e41e375fb3d1e45c1fa75f1b9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-munin = django-munin:main
    #
    # Would create an entry point called django-munin that calls django-munin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - munin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ccnmtl/django-munin/
  license: BSD
  summary: 'Munin adaptor for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
