package:
  name: hgnested
  version: "0.6"

source:
  fn: hgnested-0.6.tar.gz
  url: https://pypi.python.org/packages/source/h/hgnested/hgnested-0.6.tar.gz
  md5: 8728dc59c0efcc859f1776b773b25224
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgnested = hgnested:main
    #
    # Would create an entry point called hgnested that calls hgnested.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial >=1.9.0

  run:
    - python
    - mercurial >=1.9.0

test:
  # Python imports
  imports:
    - hgnested

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/hgnested/
  license: GNU General Public License (GPL)
  summary: 'Mercurial extension to work with nested repositories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
