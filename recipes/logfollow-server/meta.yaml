package:
  name: logfollow-server
  version: "0.0.9"

source:
  fn: logfollow-server-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/l/logfollow-server/logfollow-server-0.0.9.tar.gz
  md5: 0166ab65b15df9f511a1f7707b758fed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logfollow-server = logfollow-server:main
    #
    # Would create an entry point called logfollow-server that calls logfollow-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=2.1.1
    - sockjs-tornado >=0.0.1

  run:
    - python
    - setuptools
    - tornado >=2.1.1
    - sockjs-tornado >=0.0.1

test:
  # Python imports
  imports:
    - logfollow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kachayev/logfollow
  license: Apache Software License
  summary: 'Real-time Web Monitor for your logs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
