package:
  name: zettwerk.clickmap
  version: "1.0"

source:
  fn: zettwerk.clickmap-1.0.zip
  url: https://pypi.python.org/packages/source/z/zettwerk.clickmap/zettwerk.clickmap-1.0.zip
  md5: 0f1e6f0e792f5e950d37b37472228399
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zettwerk.clickmap = zettwerk.clickmap:main
    #
    # Would create an entry point called zettwerk.clickmap that calls zettwerk.clickmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zettwerk
    - zettwerk.clickmap
    - zettwerk.clickmap.browser
    - zettwerk.clickmap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/zettwerk.ui
  license: GPL
  summary: 'Adding a simple clickmap/heatmap to plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
