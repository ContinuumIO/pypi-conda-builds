package:
  name: dj_mano
  version: "0.2.1"

source:
  fn: dj_mano-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dj_mano/dj_mano-0.2.1.tar.gz
  md5: 5062937bc40f67203f58051f77edafa2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj_mano = dj_mano:main
    #
    # Would create an entry point called dj_mano that calls dj_mano.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dj_mano

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ActivKonnect/dj_mano
  license: djmicrosip
  summary: 'Materialized Annotations for Django Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
