package:
  name: mathoperations
  version: "1.0"

source:
  fn: mathoperations-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mathoperations/mathoperations-1.0.tar.gz
  md5: 8e7933cde5460e31a82ba49acd1a0a54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mathoperations = mathoperations:main
    #
    # Would create an entry point called mathoperations that calls mathoperations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mathoperations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.python.org/sigs/distutils-sig/
  license: UNKNOWN
  summary: 'Simple Math Operations library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
