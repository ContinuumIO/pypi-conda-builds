package:
  name: mathstats
  version: "0.1.6"

source:
  fn: mathstats-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/m/mathstats/mathstats-0.1.6.tar.gz
  md5: 2ab0f743cd2e3659d1d5f728e713c9a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mathstats = mathstats:main
    #
    # Would create an entry point called mathstats that calls mathstats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scipy >=0.9

  run:
    - python
    - scipy >=0.9

test:
  # Python imports
  imports:
    - mathstats
    - mathstats.normaldist
    - mathstats.normaldist.truncatedskewed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ksahlin/mathstats
  license: GPLv3
  summary: 'Statistical functions, goodness-of-fit tests and special and special distributions not implemented in scipy/numpy .'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
