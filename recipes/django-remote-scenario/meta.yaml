package:
  name: django-remote-scenario
  version: "0.3.1"

source:
  fn: django-remote-scenario-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-remote-scenario/django-remote-scenario-0.3.1.tar.gz
  md5: ee049bd2b010b99d9ae2c400d5031b47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-remote-scenario = django-remote-scenario:main
    #
    # Would create an entry point called django-remote-scenario that calls django-remote-scenario.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_rs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skalanux/django-remote-scenario
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Remote scenario setup for e2e testing of django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
