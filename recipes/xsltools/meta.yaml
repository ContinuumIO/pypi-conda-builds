package:
  name: xsltools
  version: "0.5"

source:
  fn: XSLTools-0.5.tar.gz
  url: https://pypi.python.org/packages/source/X/XSLTools/XSLTools-0.5.tar.gz
  md5: fe93c01803e4ddb2116e0944cbbdbcf1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xsltools = xsltools:main
    #
    # Would create an entry point called xsltools that calls xsltools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - XSLForms
    - XSLForms.Resources
    - XSLTools
    - xsltools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.boddie.org.uk/python/XSLTools.html
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Modules and packages for the development of XML/XSL-based applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
