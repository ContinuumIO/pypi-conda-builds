package:
  name: github-distutils
  version: "0.1.1"

source:
  fn: github-distutils-0.1.1.tar.gz
  url: https://github.com/downloads/dahlia/github-distutils/github-distutils-0.1.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github-distutils = github-distutils:main
    #
    # Would create an entry point called github-distutils that calls github-distutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dahlia/github-distutils
  license: Public Domain
  summary: 'Distribute/setuptools/distutils command for GitHub. You can use GitHub downloads instead of PyPI downloads for release.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
