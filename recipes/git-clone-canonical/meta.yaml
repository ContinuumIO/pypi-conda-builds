package:
  name: git-clone-canonical
  version: "0.0.1"

source:
  fn: git-clone-canonical-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/git-clone-canonical/git-clone-canonical-0.0.1.tar.gz
  md5: 3c7938c63f4b4e60ea10e6f326e99ab8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-clone-canonical = git-clone-canonical:main
    #
    # Would create an entry point called git-clone-canonical that calls git-clone-canonical.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rconradharris/git-clone-canonical
  license: MIT License
  summary: 'git clone popular repos by name not location'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
