package:
  name: django-markitup-widget
  version: "0.1rc3"

source:
  fn: django-markitup-widget-0.1rc3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markitup-widget/django-markitup-widget-0.1rc3.tar.gz
  md5: 3a7ba8c6cc5d6e84c799b6234c1fa36f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markitup-widget = django-markitup-widget:main
    #
    # Would create an entry point called django-markitup-widget that calls django-markitup-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - markitup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-markitup-widget
  license: BSD License
  summary: "django-markitup-widget is Django's textarea like widget for using markItUp! on textarea"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
