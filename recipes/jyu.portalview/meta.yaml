package:
  name: jyu.portalview
  version: "1.0.3"

source:
  fn: jyu.portalview-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jyu.portalview/jyu.portalview-1.0.3.tar.gz
  md5: eaaf68aed693bcd2a868293512161480
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jyu.portalview = jyu.portalview:main
    #
    # Would create an entry point called jyu.portalview that calls jyu.portalview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - jyu
    - jyu.portalview
    - jyu.portalview.browser
    - jyu.portalview.browser.boxes
    - jyu.portalview.content
    - jyu.portalview.profiles
    - jyu.portalview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/jyu.portalview
  license: GNU General Public License (GPL)
  summary: 'A content type, which displays content in a portal like aggregating view'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
