package:
  name: digimat.gmail
  version: "0.1.0"

source:
  fn: digimat.gmail-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/digimat.gmail/digimat.gmail-0.1.0.zip
  md5: 0240ccf289786710d0e4dc5cb6052a6f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digimat.gmail = digimat.gmail:main
    #
    # Would create an entry point called digimat.gmail that calls digimat.gmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pil
    - setuptools

  run:
    - python
    - pil
    - setuptools

test:
  # Python imports
  imports:
    - digimat
    - digimat.gmail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Digimat Google Mail sender'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
