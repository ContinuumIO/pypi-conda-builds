package:
  name: django-tastypie-elasticsearch
  version: "0.5.1"

source:
  fn: django-tastypie-elasticsearch-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tastypie-elasticsearch/django-tastypie-elasticsearch-0.5.1.tar.gz
  md5: 2224a55f0c2bbb0c7a553ea8e9312781
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tastypie-elasticsearch = django-tastypie-elasticsearch:main
    #
    # Would create an entry point called django-tastypie-elasticsearch that calls django-tastypie-elasticsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - elasticsearch
    - django >=1.4
    - django-tastypie >=0.9.11

  run:
    - python
    - elasticsearch
    - django >=1.4
    - django-tastypie >=0.9.11

test:
  # Python imports
  imports:
    - tastypie_elasticsearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/llonchj/django-tastypie-elasticsearch
  license: GNU Affero General Public License v3
  summary: 'ElasticSearch Resource for django-tastypie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
