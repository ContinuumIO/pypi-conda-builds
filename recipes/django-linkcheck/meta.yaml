package:
  name: django-linkcheck
  version: "1.0"

source:
  fn: django-linkcheck-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-linkcheck/django-linkcheck-1.0.tar.gz
  md5: 881b65d3dfc54ed9e14e0400c6934eb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-linkcheck = django-linkcheck:main
    #
    # Would create an entry point called django-linkcheck that calls django-linkcheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - linkcheck
    - linkcheck.management
    - linkcheck.management.commands
    - linkcheck.tests
    - linkcheck.tests.sampleapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/andybak/django-linkcheck/
  license: BSD License
  summary: 'A Django app that will analyze and report on links in any model that you register with it.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
