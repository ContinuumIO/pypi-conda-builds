package:
  name: cormoran
  version: "0.1"

source:
  fn: cormoran-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cormoran/cormoran-0.1.tar.gz
  md5: 3897fc346680bb385a66252152da119d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cormoran = cormoran:main
    #
    # Would create an entry point called cormoran that calls cormoran.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cormoran
    - cormoran.backends
    - cormoran.sql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaimegildesagredo/cormoran
  license: GNU General Public License (GPL)
  summary: 'Cormoran is a fast and lightweight persistence framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
