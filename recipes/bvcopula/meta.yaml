package:
  name: bvcopula
  version: "0.9.0"

source:
  fn: bvcopula-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bvcopula/bvcopula-0.9.0.tar.gz
  md5: d1c1d21dda99797c7c4a9b4edc0b6f04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bvcopula = bvcopula:main
    #
    # Would create an entry point called bvcopula that calls bvcopula.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bvcopula

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: taizhonglab.ustc.edu.cn/software/bvcopula.html
  license: ['GPL (>= 2)']
  summary: 'Probability and sampling functions for six common seen bivariate copulas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
