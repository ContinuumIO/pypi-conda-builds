package:
  name: django-constance-updated
  version: "0.6.1"

source:
  fn: django-constance-updated-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-constance-updated/django-constance-updated-0.6.1.tar.gz
  md5: 1d0c421ad61634a7247835da1cf36ea6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-constance-updated = django-constance-updated:main
    #
    # Would create an entry point called django-constance-updated that calls django-constance-updated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - constance
    - constance.backends
    - constance.backends.database
    - constance.backends.database.migrations
    - constance.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/comoga/django-constance
  license: BSD License
  summary: 'Django live settings with pluggable backends, including Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
