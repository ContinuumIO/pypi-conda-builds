package:
  name: django-socialnews
  version: "1.1"

source:
  fn: django-socialnews-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-socialnews/django-socialnews-1.1.tar.gz
  md5: f2bc1da0308e73cee6fd31f2bbef68e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-socialnews = django-socialnews:main
    #
    # Would create an entry point called django-socialnews that calls django-socialnews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - socialnews
    - socialnews.mptt
    - socialnews.mptt.templatetags
    - socialnews.mptt.tests
    - socialnews.news
    - socialnews.news.libs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.agiliq.com/
  license: GNU General Public License (GPL)
  summary: 'A reddit like socialnews system for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
