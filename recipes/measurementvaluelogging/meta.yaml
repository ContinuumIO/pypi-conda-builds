package:
  name: measurementvaluelogging
  version: "0.4.1"

source:
  fn: MeasurementValueLogging-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/M/MeasurementValueLogging/MeasurementValueLogging-0.4.1.tar.gz
  md5: c43fa31b54e810edabaf4dbcf46bb18a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - measurementvaluelogging = measurementvaluelogging:main
    #
    # Would create an entry point called measurementvaluelogging that calls measurementvaluelogging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - MeasurementValueLogging
    - MeasurementValueLogging.devices
    - MeasurementValueLogging.ui_data
    - MeasurementValueLogging.webui_data
    - measurementvaluelogging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leezu/MeasurementValueLogging
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Display measurement values from various serial devices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
