package:
  name: django-providerregistry
  version: "0.5.5"

source:
  fn: django-providerregistry-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-providerregistry/django-providerregistry-0.5.5.tar.gz
  md5: 61d3b9aee3339a9f6cea861620290d6b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-providerregistry = django-providerregistry:main
    #
    # Would create an entry point called django-providerregistry that calls django-providerregistry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo
    - requests
    - django-localflavor
    - django-bootstrap-form
    - django-djmongo

  run:
    - python
    - pymongo
    - requests
    - django-localflavor
    - django-bootstrap-form
    - django-djmongo

test:
  # Python imports
  imports:
    - providerregistry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hhsidealab/django-providerregistry
  license: Public Domain
  summary: 'A simple Django application that provides a UI and RESTFul API for read only access to NPPES data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
