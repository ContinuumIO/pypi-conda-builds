package:
  name: ipython-db
  version: "1.0"

source:
  fn: ipython-db-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ipython-db/ipython-db-1.0.tar.gz
  md5: 8e2eb095a56f9af365d4eee753e00e78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipython-db = ipython-db:main
    #
    # Would create an entry point called ipython-db that calls ipython-db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython >=2.0
    - db.py >=0.3.4

  run:
    - python
    - ipython >=2.0
    - db.py >=0.3.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dongweiming/idb
  license: MIT License
  summary: 'Ipython db.py shell extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
