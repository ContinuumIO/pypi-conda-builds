package:
  name: anora
  version: "0.1.2"

source:
  fn: anora-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/anora/anora-0.1.2.tar.gz
  md5: 052bed9dba79f48f61efffaf036cd238
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anora = anora:main
    #
    # Would create an entry point called anora that calls anora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - anora
    - anora.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juliaelman/anora
  license: BSD
  summary: u'Anora is a simple_tag for Django that determines whether or not a word should have an "a" or "an" in front of it. Adds either one of these depending on the phoentic value of the given text.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
