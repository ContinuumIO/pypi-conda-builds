package:
  name: js.strophe_pubsub
  version: "0.3-1"

source:
  fn: js.strophe_pubsub-0.3-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.strophe_pubsub/js.strophe_pubsub-0.3-1.tar.gz
  md5: 7afb668e870cd5629b06f1f9bd2f40ad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.strophe_pubsub = js.strophe_pubsub:main
    #
    # Would create an entry point called js.strophe_pubsub that calls js.strophe_pubsub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.strophe

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.strophe

test:
  # Python imports
  imports:
    - js
    - js.strophe_pubsub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of strophe.pubsub.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
