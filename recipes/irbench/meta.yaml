package:
  name: irbench
  version: "0.0.67"

source:
  fn: irbench-0.0.67.tar.gz
  url: https://pypi.python.org/packages/source/i/irbench/irbench-0.0.67.tar.gz
  md5: 2ef398c17d39a78325a4a8126abbc097
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - irbench = irbench:main
    #
    # Would create an entry point called irbench that calls irbench.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6.0
    - scipy >=0.9.0
    - bottleneck >=0.8.0
    - bitmap >=0.0.1
    - protobuf >=2.4.0

  run:
    - python
    - numpy >=1.6.0
    - scipy >=0.9.0
    - bottleneck >=0.8.0
    - bitmap >=0.0.1
    - protobuf >=2.4.0

test:
  # Python imports
  imports:
    - irbench

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wanji/irbench
  license: UNKNOWN
  summary: 'Image Retrieval Benchmark.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
