package:
  name: django-talkback
  version: "0.1.0"

source:
  fn: django-talkback-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-talkback/django-talkback-0.1.0.tar.gz
  md5: 9835890eeef48b88afec1484a2095f8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-talkback = django-talkback:main
    #
    # Would create an entry point called django-talkback that calls django-talkback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - talkback
    - talkback.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/littleweaver/django-talkback
  license: BSD License
  summary: 'A pluggable app for adding an AJAX feedback form to Django-powered websites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
