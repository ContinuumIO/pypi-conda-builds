package:
  name: sphinx_numfig
  version: "0.1.1"

source:
  fn: sphinx_numfig-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx_numfig/sphinx_numfig-0.1.1.tar.gz
  md5: f1e66ad71b2f457297bcda098ed5ed48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx_numfig = sphinx_numfig:main
    #
    # Would create an entry point called sphinx_numfig that calls sphinx_numfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sphinx_numfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Carreau/sphinx_numfig
  license: BSD License
  summary: 'Python Boilerplate contains all the boilerplate you need to create a Python package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
