package:
  name: megrok.tinymce
  version: "3.1.0.1.1"

source:
  fn: megrok.tinymce-3.1.0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.tinymce/megrok.tinymce-3.1.0.1.1.tar.gz
  md5: 4f4bb11b1da1400a7fd753703343f9a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.tinymce = megrok.tinymce:main
    #
    # Would create an entry point called megrok.tinymce that calls megrok.tinymce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - megrok.resourcelibrary

  run:
    - python
    - setuptools
    - setuptools
    - megrok.resourcelibrary

test:
  # Python imports
  imports:
    - megrok
    - megrok.tinymce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: LGPL
  summary: 'TinyMCE javascript editor packaged as a Grok resource library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
