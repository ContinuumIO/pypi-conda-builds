package:
  name: htsql-oracle
  version: "2.3.3"

source:
  fn: HTSQL-ORACLE-2.3.3.tar.gz
  url: https://pypi.python.org/packages/source/H/HTSQL-ORACLE/HTSQL-ORACLE-2.3.3.tar.gz
  md5: fb33232d61ccdbfa1f625ebfffe507a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htsql-oracle = htsql-oracle:main
    #
    # Would create an entry point called htsql-oracle that calls htsql-oracle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - htsql
    - cx_oracle

  run:
    - python
    - setuptools
    - htsql
    - cx_oracle

test:
  # Python imports
  imports:
    - htsql_oracle
    - htsql_oracle.core
    - htsql_oracle.core.tr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://htsql.org/
  license: Evaluation License
  summary: 'A Database Query Language (Oracle backend)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
