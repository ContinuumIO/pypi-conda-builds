package:
  name: flask-webtest
  version: "0.0.7"

source:
  fn: Flask-WebTest-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-WebTest/Flask-WebTest-0.0.7.tar.gz
  md5: 9ed757c3da84f252c36db83a147b2bb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-webtest = flask-webtest:main
    #
    # Would create an entry point called flask-webtest that calls flask-webtest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - webtest
    - blinker

  run:
    - python
    - flask >=0.8
    - webtest
    - blinker

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aromanovich/flask-webtest
  license: BSD
  summary: 'Utilities for testing Flask applications with WebTest.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
