package:
  name: dropbox_backup
  version: "0.1.6"

source:
  fn: dropbox_backup-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/dropbox_backup/dropbox_backup-0.1.6.tar.gz
  md5: 6f142a4602c3b62ebb790bd6ea931199
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dropbox_backup = dropbox_backup:main
    #
    # Would create an entry point called dropbox_backup that calls dropbox_backup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt ==0.6.1
    - requests ==1.2.3
    - wsgiref ==0.1.2

  run:
    - python
    - docopt ==0.6.1
    - requests ==1.2.3
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - dropbox_backup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dropbox-backup.org
  license: MIT
  summary: 'Create backup from your server and send them to dropbox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
