package:
  name: sitedrifter
  version: "1.0.0"

source:
  fn: sitedrifter-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sitedrifter/sitedrifter-1.0.0.tar.gz
  md5: f9fb5e6d83769511d2d0a193530f0248
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sitedrifter = sitedrifter:main
    #
    # Would create an entry point called sitedrifter that calls sitedrifter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - markdown
    - jinja2

  run:
    - python
    - markdown
    - jinja2

test:
  # Python imports
  imports:
    - sitedrifter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brunobord/sitedrifter/
  license: MIT
  summary: 'Sitedrifter is a Markdown to HTML converter to build static websites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
