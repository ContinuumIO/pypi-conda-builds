package:
  name: bashlex
  version: "0.6"

source:
  fn: bashlex-0.6.tar.gz
  url: https://pypi.python.org/packages/source/b/bashlex/bashlex-0.6.tar.gz
  md5: 03d572623c6ff2d938dd60e5194d7378
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bashlex = bashlex:main
    #
    # Would create an entry point called bashlex that calls bashlex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - enum34

  run:
    - python
    - enum34

test:
  # Python imports
  imports:
    - bashlex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/idank/bashlex.git
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Python parser for bash'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
