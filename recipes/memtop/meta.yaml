package:
  name: memtop
  version: "1.0.3"

source:
  fn: memtop-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/memtop/memtop-1.0.3.tar.gz
  md5: 3c701b5f571487d182eea69a45d2a1b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memtop = memtop:main
    #
    # Would create an entry point called memtop that calls memtop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse

  run:
    - python
    - argparse

test:
  # Python imports
  imports:
    - memtop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MartinThoma/memtop
  license: GNU General Public License (GPL)
  summary: 'view memory consumption of processes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
