package:
  name: keepass
  version: "1.2"

source:
  fn: keepass-1.2.tar.gz
  url: https://pypi.python.org/packages/source/k/keepass/keepass-1.2.tar.gz
  md5: 52ca8e41a660f69d3caf707fccf302bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - keepass = keepass:main
    #
    # Would create an entry point called keepass that calls keepass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto
    - six

  run:
    - python
    - pycrypto
    - six

test:
  # Python imports
  imports:
    - keepass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Friz-zy/python-keepass
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Python (both 2 and 3) interface and cli to KeePass file format v3 (used in KeePass V1.x and KeePassX)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
