package:
  name: l18n
  version: "2015.2"

source:
  fn: l18n-2015.2.zip
  url: https://pypi.python.org/packages/source/l/l18n/l18n-2015.2.zip
  md5: 218c3f1dbffefc77a6ad99e7a737d293
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - l18n = l18n:main
    #
    # Would create an entry point called l18n that calls l18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz ==2015.2

  run:
    - python
    - pytz ==2015.2

test:
  # Python imports
  imports:
    - l18n

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tkhyn/l18n
  license: MIT License
  summary: 'Internationalization for pytz timezones and territories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
