package:
  name: pynacolada
  version: "0.2.28"

source:
  fn: pynacolada-0.2.28.tar.gz
  url: https://pypi.python.org/packages/source/p/pynacolada/pynacolada-0.2.28.tar.gz
  md5: c79919efae22890c308dfdb6c90703e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynacolada = pynacolada:main
    #
    # Would create an entry point called pynacolada that calls pynacolada.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pynacolada

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nowebsite.com
  license: LICENSE.txt
  summary: 'easy and customized Processing huge amounts of gridded Climate Data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
