package:
  name: junta
  version: "0.2.22"

source:
  fn: junta-0.2.22.tar.gz
  url: https://pypi.python.org/packages/source/j/junta/junta-0.2.22.tar.gz
  md5: eb4f154f4241e92a1059ba6b7df3f03d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - junta = junta:main
    #
    # Would create an entry point called junta that calls junta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - ws4py ==0.2.1
    - pymongo
    - django

  run:
    - python
    - setuptools
    - tornado
    - ws4py ==0.2.1
    - pymongo
    - django

test:
  # Python imports
  imports:
    - junta
    - junta.agent
    - junta.server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ivideon/junta
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
