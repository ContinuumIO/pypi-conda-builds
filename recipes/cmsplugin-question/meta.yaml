package:
  name: cmsplugin-question
  version: "1.0.5"

source:
  fn: cmsplugin-question-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-question/cmsplugin-question-1.0.5.tar.gz
  md5: 8f33c7c5ee013b47307f277976dcb3f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-question = cmsplugin-question:main
    #
    # Would create an entry point called cmsplugin-question that calls cmsplugin-question.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_question
    - cmsplugin_question.nospam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vinitkumar/cmsplugin-question
  license: BSD License
  summary: 'Question form contact plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
