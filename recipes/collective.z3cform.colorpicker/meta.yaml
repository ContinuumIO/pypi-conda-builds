package:
  name: collective.z3cform.colorpicker
  version: "1.2"

source:
  fn: collective.z3cform.colorpicker-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.z3cform.colorpicker/collective.z3cform.colorpicker-1.2.tar.gz
  md5: e29bcb883426490b62a0685d7e651c59
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.colorpicker = collective.z3cform.colorpicker:main
    #
    # Would create an entry point called collective.z3cform.colorpicker that calls collective.z3cform.colorpicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.colorpicker
    - collective.z3cform.colorpicker.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.giorgioborelli.it
  license: GNU General Public License v2 (GPLv2)
  summary: 'Colorpicker widget for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
