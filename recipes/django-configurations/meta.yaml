package:
  name: django-configurations
  version: "0.8"

source:
  fn: django-configurations-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-configurations/django-configurations-0.8.tar.gz
  md5: 3ca3c5a0d78307ff304ecce3234c2822
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-configurations = django-configurations:main
    #
    # Would create an entry point called django-configurations that calls django-configurations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - configurations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-configurations.readthedocs.org/
  license: BSD License
  summary: 'A helper for organizing Django settings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
