package:
  name: simplegeo-places
  version: "2.4.153"

source:
  fn: simplegeo-places-2.4.153.tar.gz
  url: https://pypi.python.org/packages/source/s/simplegeo-places/simplegeo-places-2.4.153.tar.gz
  md5: c074c794abbbce18cabeae277cdb442a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplegeo-places = simplegeo-places:main
    #
    # Would create an entry point called simplegeo-places that calls simplegeo-places.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplegeo-shared >=2.3.60
    - pyutil[jsonutil] >=1.8.1

  run:
    - python
    - simplegeo-shared >=2.3.60
    - pyutil[jsonutil] >=1.8.1

test:
  # Python imports
  imports:
    - simplegeo
    - simplegeo.places
    - simplegeo.places.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/simplegeo/python-simplegeo-places
  license: MIT License
  summary: "Library for interfacing with SimpleGeo's Places API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
