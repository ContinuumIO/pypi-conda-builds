package:
  name: journal-of-official-statistics
  version: "0.0.1"

source:
  fn: journal-of-official-statistics-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/journal-of-official-statistics/journal-of-official-statistics-0.0.1.tar.gz
  md5: 9094e14274cda9a7e3985fa9a141d5e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - journal-of-official-statistics = journal-of-official-statistics:main
    #
    # Would create an entry point called journal-of-official-statistics that calls journal-of-official-statistics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - picklecache
    - requests
    - lxml

  run:
    - python
    - picklecache
    - requests
    - lxml

test:
  # Python imports
  imports:
    - jos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/jor
  license: AGPL
  summary: 'Download articles from the Journal of Official Statistics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
