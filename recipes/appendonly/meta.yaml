package:
  name: appendonly
  version: "1.2"

source:
  fn: appendonly-1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/appendonly/appendonly-1.2.tar.gz
  md5: 55ccc4b4eb0387688eafde15de7b450a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appendonly = appendonly:main
    #
    # Would create an entry point called appendonly that calls appendonly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - persistent
    - zodb
    - zope.interface

  run:
    - python
    - persistent
    - zodb
    - zope.interface

test:
  # Python imports
  imports:
    - appendonly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Zope Public License
  summary: 'Persistent append-only data structures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
