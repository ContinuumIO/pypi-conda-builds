package:
  name: shannon_entropy
  version: "0.2.1"

source:
  fn: shannon_entropy-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/shannon_entropy/shannon_entropy-0.2.1.tar.gz
  md5: b9289f9c8f5be2a79cf3644e09b879be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shannon_entropy = shannon_entropy:main
    #
    # Would create an entry point called shannon_entropy that calls shannon_entropy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - shannon_entropy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/micahwalter/shannon_entropy
  license: MIT
  summary: 'Calculate the Shannon Entropy for an image.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
