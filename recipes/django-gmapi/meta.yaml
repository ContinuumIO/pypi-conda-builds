package:
  name: django-gmapi
  version: "1.0.1"

source:
  fn: django-gmapi-1.0.1.zip
  url: https://pypi.python.org/packages/source/d/django-gmapi/django-gmapi-1.0.1.zip
  md5: 181cb4585e8749dc2b4279395459a931
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gmapi = django-gmapi:main
    #
    # Would create an entry point called django-gmapi that calls django-gmapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gmapi
    - gmapi.forms
    - gmapi.urls
    - gmapi.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-gmapi/
  license: MIT License
  summary: 'A Google Maps API implementation for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
