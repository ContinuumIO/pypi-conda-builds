package:
  name: bdateutil
  version: "0.1"

source:
  fn: bdateutil-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bdateutil/bdateutil-0.1.tar.gz
  md5: fb6ad1c05bb38978e205d81f60946198
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bdateutil = bdateutil:main
    #
    # Would create an entry point called bdateutil that calls bdateutil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil ==2.2
    - holidays >=0.3.1

  run:
    - python
    - python-dateutil ==2.2
    - holidays >=0.3.1

test:
  # Python imports
  imports:
    - bdateutil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryanss/bdateutil
  license: MIT License
  summary: 'Adds business day logic and improved data type flexibility to python-dateutil.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
