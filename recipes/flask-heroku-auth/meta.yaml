package:
  name: flask-heroku-auth
  version: "0.0.5"

source:
  fn: Flask-Heroku-Auth-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Heroku-Auth/Flask-Heroku-Auth-0.0.5.tar.gz
  md5: 3442a38614bf5dd03d8cd7fce51fffcd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-heroku-auth = flask-heroku-auth:main
    #
    # Would create an entry point called flask-heroku-auth that calls flask-heroku-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests
    - flask-oauth

  run:
    - python
    - setuptools
    - flask
    - requests
    - flask-oauth

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rhyselsmore/flask-heroku-auth
  license: Flask-Heroku-Cacheify
  summary: 'Flask Based Heroku Authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
