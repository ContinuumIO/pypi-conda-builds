package:
  name: github-flask
  version: "2.0.1"

source:
  fn: GitHub-Flask-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GitHub-Flask/GitHub-Flask-2.0.1.tar.gz
  md5: 9c677785f408252f5cf84a2cf17412ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github-flask = github-flask:main
    #
    # Would create an entry point called github-flask that calls github-flask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - requests

  run:
    - python
    - flask
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cenkalti/github-flask
  license: MIT
  summary: 'GitHub extension for Flask microframework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
