package:
  name: django-googledrive-storage
  version: "1.1.1"

source:
  fn: django-googledrive-storage-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googledrive-storage/django-googledrive-storage-1.1.1.tar.gz
  md5: faa4ec0445445bf9c47185328c4dee3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googledrive-storage = django-googledrive-storage:main
    #
    # Would create an entry point called django-googledrive-storage that calls django-googledrive-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - google-api-python-client >=1.2
    - pycrypto >=2.6.1
    - python-dateutil >=2.2
    - requests >=2.2.1
    - django-appconf >=0.6

  run:
    - python
    - django >=1.6
    - google-api-python-client >=1.2
    - pycrypto >=2.6.1
    - python-dateutil >=2.2
    - requests >=2.2.1
    - django-appconf >=0.6

test:
  # Python imports
  imports:
    - gdstorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/torre76/django-googledrive-storage
  license: BSD License
  summary: 'Storage implementation for Django that interacts with Google Drive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
