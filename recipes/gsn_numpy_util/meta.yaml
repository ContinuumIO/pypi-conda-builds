package:
  name: gsn_numpy_util
  version: "0.1.0"

source:
  fn: gsn_numpy_util-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gsn_numpy_util/gsn_numpy_util-0.1.0.tar.gz
  md5: 09e0ade80445c94e2cda0988ec2cc95a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gsn_numpy_util = gsn_numpy_util:main
    #
    # Would create an entry point called gsn_numpy_util that calls gsn_numpy_util.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gsn_numpy_util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gsn_numpy_util/
  license: MIT (X11) License
  summary: 'Toolbox for working with Numpy arrays.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
