package:
  name: ietfparse
  version: "1.2.0"

source:
  fn: ietfparse-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ietfparse/ietfparse-1.2.0.tar.gz
  md5: 623b461913de7e5f83ca74a2eacbdb52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ietfparse = ietfparse:main
    #
    # Would create an entry point called ietfparse that calls ietfparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ietfparse
    - ietfparse.compat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dave-shawley/ietfparse
  license: BSD License
  summary: 'Parse formats defined in IETF RFCs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
