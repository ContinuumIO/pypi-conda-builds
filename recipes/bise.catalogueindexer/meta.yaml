package:
  name: bise.catalogueindexer
  version: "1.3.1"

source:
  fn: bise.catalogueindexer-1.3.1.zip
  url: https://pypi.python.org/packages/source/b/bise.catalogueindexer/bise.catalogueindexer-1.3.1.zip
  md5: 0e235a1c5e77e21a021722372c3b385d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bise.catalogueindexer = bise.catalogueindexer:main
    #
    # Would create an entry point called bise.catalogueindexer that calls bise.catalogueindexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.api
    - plone.app.registry
    - requests

  run:
    - python
    - setuptools
    - setuptools
    - plone.api
    - plone.app.registry
    - requests

test:
  # Python imports
  imports:
    - bise
    - bise.catalogueindexer
    - bise.catalogueindexer.adapters
    - bise.catalogueindexer.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eea/bise.catalogueindexer
  license: GPL
  summary: 'Catalog indexer methods for BISE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
