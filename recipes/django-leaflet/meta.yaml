package:
  name: django-leaflet
  version: "0.16.0"

source:
  fn: django-leaflet-0.16.0.zip
  url: https://pypi.python.org/packages/source/d/django-leaflet/django-leaflet-0.16.0.zip
  md5: 7b26d56e6969e16d11e5439158283ef0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-leaflet = django-leaflet:main
    #
    # Would create an entry point called django-leaflet that calls django-leaflet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - leaflet
    - leaflet.forms
    - leaflet.templatetags
    - leaflet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/makinacorpus/django-leaflet
  license: LPGL, see LICENSE file.
  summary: 'Use Leaflet in your django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
