package:
  name: dimarray
  version: "0.1.8"

source:
  fn: dimarray-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/dimarray/dimarray-0.1.8.tar.gz
  md5: bb0ba833a6c436152d15483c536c59b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dimarray = dimarray:main
    #
    # Would create an entry point called dimarray that calls dimarray.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.7

  run:
    - python
    - setuptools
    - numpy >=1.7

test:
  # Python imports
  imports:
    - dimarray
    - dimarray.compat
    - dimarray.core
    - dimarray.datasets
    - dimarray.geo
    - dimarray.io
    - dimarray.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/perrette/dimarray
  license: BSD 3-Clause
  summary: 'numpy array with labelled dimensions and axes, dimension, NaN handling and netCDF I/O'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
