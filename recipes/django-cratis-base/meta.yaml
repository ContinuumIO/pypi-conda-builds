package:
  name: django-cratis-base
  version: "0.1.5"

source:
  fn: django-cratis-base-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cratis-base/django-cratis-base-0.1.5.tar.gz
  md5: a392694eeca758886288b818b3a16239
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cratis-base = django-cratis-base:main
    #
    # Would create an entry point called django-cratis-base that calls django-cratis-base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cratis >=0.6.0
    - dj-database-url
    - south

  run:
    - python
    - django-cratis >=0.6.0
    - dj-database-url
    - south

test:
  # Python imports
  imports:
    - cratis_base

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: 'Django-cratis is a way to group together django applications, so they form reusable features.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
