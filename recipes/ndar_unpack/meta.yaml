package:
  name: ndar_unpack
  version: "0.1.3"

source:
  fn: ndar_unpack-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/n/ndar_unpack/ndar_unpack-0.1.3.tar.gz
  md5: ba590dacfd9b8d0470c2f37dcbf381b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ndar_unpack = ndar_unpack:main
    #
    # Would create an entry point called ndar_unpack that calls ndar_unpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pydicom
    - boto

  run:
    - python
    - pydicom
    - boto

test:
  # Python imports
  imports:
    - ndar_unpack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chaselgrove/ndar/ndar_unpack
  license: BSD License
  summary: 'A script for checking and unpacking NDAR image data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
