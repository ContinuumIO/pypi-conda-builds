package:
  name: basic_crypto
  version: "1.0.1"

source:
  fn: basic_crypto-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/basic_crypto/basic_crypto-1.0.1.tar.gz
  md5: 31bf6d0b7a05abf236fc468edfb7332a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basic_crypto = basic_crypto:main
    #
    # Would create an entry point called basic_crypto that calls basic_crypto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - basic_crypto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cbillington/basic_crypto/
  license: BSD
  summary: 'Wrapper around PyCrypto providing basic symmetric key encryption with AES in CFB mode.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
