package:
  name: cloudify-dsl-parser
  version: "3.2a8"

source:
  fn: cloudify-dsl-parser-3.2a8.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudify-dsl-parser/cloudify-dsl-parser-3.2a8.tar.gz
  md5: a82282aea228c74b87d152ff0d80e637
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudify-dsl-parser = cloudify-dsl-parser:main
    #
    # Would create an entry point called cloudify-dsl-parser that calls cloudify-dsl-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.10
    - jsonschema ==2.3.0
    - networkx ==1.8.1

  run:
    - python
    - pyyaml ==3.10
    - jsonschema ==2.3.0
    - networkx ==1.8.1

test:
  # Python imports
  imports:
    - dsl_parser
    - dsl_parser.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE
  summary: 'Cloudify DSL parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
