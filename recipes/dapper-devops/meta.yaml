package:
  name: dapper-devops
  version: "0.1.7"

source:
  fn: dapper-devops-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dapper-devops/dapper-devops-0.1.7.tar.gz
  md5: 3d8166159326fb5366f333110003872e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dapper-devops = dapper-devops:main
    #
    # Would create an entry point called dapper-devops that calls dapper-devops.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.6.0
    - servers.py >=0.1.5

  run:
    - python
    - fabric >=1.6.0
    - servers.py >=0.1.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/radiosilence/devops
  license: BSD License
  summary: 'Fabric deployment script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
