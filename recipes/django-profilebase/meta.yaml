package:
  name: django-profilebase
  version: "0.2.4"

source:
  fn: django-profilebase-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-profilebase/django-profilebase-0.2.4.tar.gz
  md5: f7107832dd9b1259664a607efc9e4d3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-profilebase = django-profilebase:main
    #
    # Would create an entry point called django-profilebase that calls django-profilebase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-stringfield >=0.3.0

  run:
    - python
    - django-stringfield >=0.3.0

test:
  # Python imports
  imports:
    - profilebase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/django-profilebase
  license: BSD License
  summary: 'Tools for building a custom Profile class with authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
