package:
  name: django-ansi2html-filter
  version: "0.0.2"

source:
  fn: django-ansi2html-filter-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ansi2html-filter/django-ansi2html-filter-0.0.2.tar.gz
  md5: ecdf1fd891bb0ce2e585755dac258ed2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ansi2html-filter = django-ansi2html-filter:main
    #
    # Would create an entry point called django-ansi2html-filter that calls django-ansi2html-filter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansi2html <=2
    - django >=1.6

  run:
    - python
    - ansi2html <=2
    - django >=1.6

test:
  # Python imports
  imports:
    - django_ansi2html_filter
    - django_ansi2html_filter/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dabapps.com
  license: GPL
  summary: 'Apply ansi2html transformation in Django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
