package:
  name: easy-thumbnails
  version: "2.2"

source:
  fn: easy-thumbnails-2.2.tar.gz
  url: https://pypi.python.org/packages/source/e/easy-thumbnails/easy-thumbnails-2.2.tar.gz
  md5: 82259838b1b2dc8ab6377522401f710b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy-thumbnails = easy-thumbnails:main
    #
    # Would create an entry point called easy-thumbnails that calls easy-thumbnails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - pillow

  run:
    - python
    - django >=1.4.2
    - pillow

test:
  # Python imports
  imports:
    - easy_thumbnails
    - easy_thumbnails.management
    - easy_thumbnails.management.commands
    - easy_thumbnails.migrations
    - easy_thumbnails.optimize
    - easy_thumbnails.south_migrations
    - easy_thumbnails.templatetags
    - easy_thumbnails.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/SmileyChris/easy-thumbnails
  license: BSD License
  summary: u'Easy thumbnails for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
