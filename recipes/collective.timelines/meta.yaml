package:
  name: collective.timelines
  version: "1.0b1"

source:
  fn: collective.timelines-1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.timelines/collective.timelines-1.0b1.zip
  md5: 5e29b20a21eb4099646cee907152f4b6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.timelines = collective.timelines:main
    #
    # Would create an entry point called collective.timelines that calls collective.timelines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.directives.form
    - five.grok
    - plone.app.z3cform
    - plone.behavior

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.directives.form
    - five.grok
    - plone.app.z3cform
    - plone.behavior

test:
  # Python imports
  imports:
    - collective
    - collective.timelines
    - collective.timelines.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Timeline views for collections and folders (using verite TimelineJS).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
