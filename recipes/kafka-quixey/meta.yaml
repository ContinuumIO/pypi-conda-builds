package:
  name: kafka-quixey
  version: "0.9.0-q3"

source:
  fn: kafka-quixey-0.9.0-q3.tar.gz
  url: https://pypi.python.org/packages/source/k/kafka-quixey/kafka-quixey-0.9.0-q3.tar.gz
  md5: 84665650c9842f7a1c13cddc7dbd8082
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kafka-quixey = kafka-quixey:main
    #
    # Would create an entry point called kafka-quixey that calls kafka-quixey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kafka

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quixey/kafka-python
  license: Copyright 2012, David Arthur under Apache License, v2.0
  summary: 'Pure Python client for Apache Kafka'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
