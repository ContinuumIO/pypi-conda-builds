package:
  name: django-ajax-forms-mega
  version: "0.2.4"

source:
  fn: django-ajax-forms-mega-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-forms-mega/django-ajax-forms-mega-0.2.4.tar.gz
  md5: 43f8d7b053110dc30dff59d0d8517166
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-forms-mega = django-ajax-forms-mega:main
    #
    # Would create an entry point called django-ajax-forms-mega that calls django-ajax-forms-mega.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - ajax_forms
    - ajax_forms.templatetags
    - ajax_forms.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisspen/django-ajax-forms
  license: BSD License
  summary: 'Provides support for doing validation using Ajax(currently with jQuery) using your existing Django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
