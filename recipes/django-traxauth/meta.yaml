package:
  name: django-traxauth
  version: "0.4.1"

source:
  fn: django-traxauth-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-traxauth/django-traxauth-0.4.1.tar.gz
  md5: 21fa6f67f0bca8a25dc89ae2fdf04465
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-traxauth = django-traxauth:main
    #
    # Would create an entry point called django-traxauth that calls django-traxauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - traxauth
    - traxauth.registration_backend
    - traxauth.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-traxauth
  license: Apache Software License
  summary: 'django-traxauth is a Django app that provides support for user account registration and profile management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
