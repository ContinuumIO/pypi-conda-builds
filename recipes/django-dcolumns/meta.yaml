package:
  name: django-dcolumns
  version: "0.5.1"

source:
  fn: django-dcolumns-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dcolumns/django-dcolumns-0.5.1.tar.gz
  md5: 86b1c53dd0b65285455df7c5806839a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dcolumns = django-dcolumns:main
    #
    # Would create an entry point called django-dcolumns that calls django-dcolumns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil

  run:
    - python
    - python-dateutil

test:
  # Python imports
  imports:
    - dcolumn
    - dcolumn.common
    - dcolumn.dcolumns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cnobile2012/dcolumn
  license: MIT License
  summary: 'An app to give any Django database model the ability to dynamically add fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
