package:
  name: cprotobuf
  version: "0.1.4"

source:
  fn: cprotobuf-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cprotobuf/cprotobuf-0.1.4.tar.gz
  md5: 8d4654aad8618d8eb06d1c290252723e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cprotobuf = cprotobuf:main
    #
    # Would create an entry point called cprotobuf that calls cprotobuf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cprotobuf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yihuang/cprotobuf
  license: UNKNOWN
  summary: 'pythonic and high performance protocol buffer implementation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
