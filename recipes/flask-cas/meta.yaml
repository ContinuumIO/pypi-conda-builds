package:
  name: flask-cas
  version: "0.5.1"

source:
  fn: Flask-CAS-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-CAS/Flask-CAS-0.5.1.tar.gz
  md5: d052897fc3c03d41276992e75fa5b8b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-cas = flask-cas:main
    #
    # Would create an entry point called flask-cas that calls flask-cas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_cas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cameronbwhite/Flask-CAS
  license: BSD License
  summary: 'Flask extension for CAS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
