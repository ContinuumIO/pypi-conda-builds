package:
  name: kss.django
  version: "0.1"

source:
  fn: kss.django-0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kss.django/kss.django-0.1.tar.gz
  md5: 69698ddb1d4665c30bd4264a31f8d734
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kss.django = kss.django:main
    #
    # Would create an entry point called kss.django that calls kss.django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kss.base

  run:
    - python
    - setuptools
    - kss.base

test:
  # Python imports
  imports:
    - kss
    - kss.django
    - kss.django.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'KSS for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
