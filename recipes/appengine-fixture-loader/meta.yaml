package:
  name: appengine-fixture-loader
  version: "0.1.5"

source:
  fn: Appengine-Fixture-Loader-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/A/Appengine-Fixture-Loader/Appengine-Fixture-Loader-0.1.5.tar.gz
  md5: 7bbe0c7af43a838bd00c74073d6247f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appengine-fixture-loader = appengine-fixture-loader:main
    #
    # Would create an entry point called appengine-fixture-loader that calls appengine-fixture-loader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - appengine_fixture_loader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rbanffy/appengine-fixture-loader/
  license: Apache Software License
  summary: 'Appengine fixture loader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
