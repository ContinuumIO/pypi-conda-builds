package:
  name: django-shop-ceca
  version: "0.1.0"

source:
  fn: django-shop-ceca-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-ceca/django-shop-ceca-0.1.0.tar.gz
  md5: e47511b9a925f1e9085bb945c659dffd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-ceca = django-shop-ceca:main
    #
    # Would create an entry point called django-shop-ceca that calls django-shop-ceca.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - shop_ceca
    - shop_ceca.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/josesanch/django-shop-ceca
  license: BSD License
  summary: 'A pluggable Django application for integrating ceca tpv Payments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
