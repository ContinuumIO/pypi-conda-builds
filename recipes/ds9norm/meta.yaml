package:
  name: ds9norm
  version: "0.1"

source:
  fn: ds9norm-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/ds9norm/ds9norm-0.1.tar.gz
  md5: ee7ec8c9d641343bdac5b3d3bfc8f7ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ds9norm = ds9norm:main
    #
    # Would create an entry point called ds9norm that calls ds9norm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ds9norm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glue-viz/ds9norm
  license: MIT License
  summary: 'A Matplotlib normalize object that replicates DS9 image stretching'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
