package:
  name: iprentalclient
  version: "0.2"

source:
  fn: IPRentalClient-0.2.tar.gz
  url: https://pypi.python.org/packages/source/I/IPRentalClient/IPRentalClient-0.2.tar.gz
  md5: 6b2ce03e302073a70471c663362fda53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iprentalclient = iprentalclient:main
    #
    # Would create an entry point called iprentalclient that calls iprentalclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - suds

  run:
    - python
    - suds

test:
  # Python imports
  imports:
    - iprental
    - iprentalclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iprental.com
  license: LICENSE.txt
  summary: 'simple IPRental Client to get proxies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
