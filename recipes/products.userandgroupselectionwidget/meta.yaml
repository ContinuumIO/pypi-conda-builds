package:
  name: products.userandgroupselectionwidget
  version: "3.0b5"

source:
  fn: Products.UserAndGroupSelectionWidget-3.0b5.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.UserAndGroupSelectionWidget/Products.UserAndGroupSelectionWidget-3.0b5.tar.gz
  md5: 2ecd889d812da1f2d20294a382b3de7c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.userandgroupselectionwidget = products.userandgroupselectionwidget:main
    #
    # Would create an entry point called products.userandgroupselectionwidget that calls products.userandgroupselectionwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.schema >=4.1.1
    - zope2
    - accesscontrol
    - products.cmfcore
    - products.cmfplone
    - products.plonepas
    - bda.cache
    - lxml
    - plone.dexterity

  run:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.schema >=4.1.1
    - zope2
    - accesscontrol
    - products.cmfcore
    - products.cmfplone
    - products.plonepas
    - bda.cache
    - lxml
    - plone.dexterity

test:
  # Python imports
  imports:
    - Products
    - Products.UserAndGroupSelectionWidget
    - Products.UserAndGroupSelectionWidget.at
    - Products.UserAndGroupSelectionWidget.at.tests
    - Products.UserAndGroupSelectionWidget.upgrade
    - Products.UserAndGroupSelectionWidget.z3cform
    - Products.UserAndGroupSelectionWidget.z3cform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/Products.UserAndGroupSelectionWidget
  license: BSD License
  summary: 'Archetypes Widget for User and Group Selection, works with many users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
