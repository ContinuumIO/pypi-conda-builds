package:
  name: corejet.visualization
  version: "1.0.0"

source:
  fn: corejet.visualization-1.0.0.zip
  url: https://pypi.python.org/packages/source/c/corejet.visualization/corejet.visualization-1.0.0.zip
  md5: b1092c6366d39adbac133e333d8bf549
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corejet.visualization = corejet.visualization:main
    #
    # Would create an entry point called corejet.visualization that calls corejet.visualization.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - lxml
    - corejet.core
    - unittest2

  run:
    - python
    - setuptools
    - setuptools
    - lxml
    - corejet.core
    - unittest2

test:
  # Python imports
  imports:
    - corejet
    - corejet.visualization

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://corejet.org
  license: ZPL 2.1
  summary: 'Support for generating reports visualizing CoreJet test runs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
