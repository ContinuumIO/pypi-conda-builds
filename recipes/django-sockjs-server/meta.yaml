package:
  name: django-sockjs-server
  version: "0.1.9"

source:
  fn: django-sockjs-server-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sockjs-server/django-sockjs-server-0.1.9.tar.gz
  md5: f09a2737160d3dc88c22cf0c56028447
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sockjs-server = django-sockjs-server:main
    #
    # Would create an entry point called django-sockjs-server that calls django-sockjs-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sockjs-tornado >=1.0.0
    - pika >=0.9.12

  run:
    - python
    - sockjs-tornado >=1.0.0
    - pika >=0.9.12

test:
  # Python imports
  imports:
    - django_sockjs_server
    - django_sockjs_server.lib
    - django_sockjs_server.management
    - django_sockjs_server.management.commands
    - django_sockjs_server.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alfss/django-sockjs-server
  license: BSD License
  summary: 'SockJS server for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
