package:
  name: collective.kwetter
  version: "0.1.1"

source:
  fn: collective.kwetter-0.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.kwetter/collective.kwetter-0.1.1.zip
  md5: 1b3d68044ba2ce0847337ccccac78bee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.kwetter = collective.kwetter:main
    #
    # Would create an entry point called collective.kwetter that calls collective.kwetter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.kwetter
    - collective.kwetter.browser
    - collective.kwetter.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/collective/kwetter
  license: GNU General Public License (GPL)
  summary: 'Plone integration for the Kwetter micro-blogging solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
