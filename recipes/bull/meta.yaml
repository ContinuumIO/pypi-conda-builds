package:
  name: bull
  version: "0.4.1"

source:
  fn: bull-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bull/bull-0.4.1.tar.gz
  md5: bbae4480ec3da0b26820b4c2b455a02a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bull = bull:main
    #
    # Would create an entry point called bull that calls bull.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - flask-sqlalchemy >=1.0
    - sqlalchemy >=0.8.2
    - flask-admin >=1.0.7
    - flask-mail ==0.9.0
    - flask-sqlalchemy ==1.0
    - sqlalchemy ==0.9.1
    - stripe ==1.11.0
    - flask-wtf ==0.9.4
    - flask-bcrypt ==0.5.2
    - flask-login ==0.2.9

  run:
    - python
    - flask >=0.10.1
    - flask-sqlalchemy >=1.0
    - sqlalchemy >=0.8.2
    - flask-admin >=1.0.7
    - flask-mail ==0.9.0
    - flask-sqlalchemy ==1.0
    - sqlalchemy ==0.9.1
    - stripe ==1.11.0
    - flask-wtf ==0.9.4
    - flask-bcrypt ==0.5.2
    - flask-login ==0.2.9

test:
  # Python imports
  imports:
    - bull

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jeffknupp/bull/
  license: Apache Software License
  summary: 'Digital goods payment processing made simple'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
