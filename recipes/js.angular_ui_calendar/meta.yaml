package:
  name: js.angular_ui_calendar
  version: "0.9.0-beta.1"

source:
  fn: js.angular_ui_calendar-0.9.0-beta.1.zip
  url: https://pypi.python.org/packages/source/j/js.angular_ui_calendar/js.angular_ui_calendar-0.9.0-beta.1.zip
  md5: 1f71ab8d2446aa9798566563e23aef9a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.angular_ui_calendar = js.angular_ui_calendar:main
    #
    # Would create an entry point called js.angular_ui_calendar that calls js.angular_ui_calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.angular
    - js.fullcalendar >=2.0

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.angular
    - js.fullcalendar >=2.0

test:
  # Python imports
  imports:
    - js
    - js.angular_ui_calendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of angular-ui ui-calendar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
