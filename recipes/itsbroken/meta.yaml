package:
  name: itsbroken
  version: "0.2"

source:
  fn: itsbroken-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/itsbroken/itsbroken-0.2.tar.gz
  md5: 58bfdf8c333c9869e371a4573787d382
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - itsbroken = itsbroken:main
    #
    # Would create an entry point called itsbroken that calls itsbroken.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - itsbroken

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/openlabs/itsbroken/
  license: GNU Affero General Public License v3
  summary: 'OpenERP Unittesting Toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
