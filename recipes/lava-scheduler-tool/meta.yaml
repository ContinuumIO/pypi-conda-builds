package:
  name: lava-scheduler-tool
  version: "0.6"

source:
  fn: lava-scheduler-tool-0.6.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-scheduler-tool/lava-scheduler-tool-0.6.tar.gz
  md5: f7d9ac7ba1406c6cf2f4dff5c57fcfc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-scheduler-tool = lava-scheduler-tool:main
    #
    # Would create an entry point called lava-scheduler-tool that calls lava-scheduler-tool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lava-tool >=0.7.dev

  run:
    - python
    - lava-tool >=0.7.dev

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lava-scheduler-tool
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Command line utility for the LAVA scheduler (deprecated)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
