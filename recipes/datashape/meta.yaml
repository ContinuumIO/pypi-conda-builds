package:
  name: datashape
  version: "0.4.5"

source:
  fn: datashape-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/D/DataShape/datashape-0.4.5.tar.gz
  md5: 0e6600e0aef450168fdacc0f3b6677cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datashape = datashape:main
    #
    # Would create an entry point called datashape that calls datashape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.7
    - multipledispatch >=0.4.7
    - python-dateutil

  run:
    - python
    - numpy >=1.7
    - multipledispatch >=0.4.7
    - python-dateutil

test:
  # Python imports
  imports:
    - datashape
    - datashape.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/datashape
  license: BSD License
  summary: 'A data description language.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
