package:
  name: flask-goat
  version: "0.2.1"

source:
  fn: Flask-Goat-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Goat/Flask-Goat-0.2.1.tar.gz
  md5: aa7c3750880018547dea5598f493befa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-goat = flask-goat:main
    #
    # Would create an entry point called flask-goat that calls flask-goat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - redis
    - simplejson
    - requests

  run:
    - python
    - flask
    - redis
    - simplejson
    - requests

test:
  # Python imports
  imports:
    - flask_goat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://incontextsolutions.github.io/flask-goat/
  license: MIT License
  summary: 'Flask plugin for security and user administration via GitHub OAuth & organization'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
