package:
  name: j01.rater
  version: "0.5.1"

source:
  fn: j01.rater-0.5.1.zip
  url: https://pypi.python.org/packages/source/j/j01.rater/j01.rater-0.5.1.zip
  md5: 11f10d9cacf2a95851d2650d3d1a6dfe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.rater = j01.rater:main
    #
    # Would create an entry point called j01.rater that calls j01.rater.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz
    - z3c.form
    - z3c.jsonrpc
    - zope.authentication
    - zope.component
    - zope.container
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema
    - zope.viewlet

  run:
    - python
    - setuptools
    - pytz
    - z3c.form
    - z3c.jsonrpc
    - zope.authentication
    - zope.component
    - zope.container
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema
    - zope.viewlet

test:
  # Python imports
  imports:
    - j01
    - j01.rater

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.rater
  license: Zope Public License
  summary: 'Rating system with property and z3c.form based widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
