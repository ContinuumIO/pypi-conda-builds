package:
  name: pypfop
  version: "0.2.0"

source:
  fn: pypfop-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pypfop/pypfop-0.2.0.tar.gz
  md5: a709eeb30e6cbb2d3ff555646922f445
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypfop = pypfop:main
    #
    # Would create an entry point called pypfop that calls pypfop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mako
    - cssutils
    - cssselect
    - lxml

  run:
    - python
    - mako
    - cssutils
    - cssselect
    - lxml

test:
  # Python imports
  imports:
    - pypfop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cyraxjoe/pypfop
  license: Apache Software License
  summary: 'Document preprocessor for Apache FOP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
