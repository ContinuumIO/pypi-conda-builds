package:
  name: django-rest-swagger
  version: "0.2.9"

source:
  fn: django-rest-swagger-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-swagger/django-rest-swagger-0.2.9.tar.gz
  md5: 49d8d640a60407755e2310cc12e02cf2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-swagger = django-rest-swagger:main
    #
    # Would create an entry point called django-rest-swagger that calls django-rest-swagger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - djangorestframework >=2.3.8
    - pyyaml >=3.10

  run:
    - python
    - django >=1.5
    - djangorestframework >=2.3.8
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - rest_framework_swagger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/marcgibbons/django-rest-swagger
  license: BSD License
  summary: 'Swagger UI for Django REST Framework 2.3.8+'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
