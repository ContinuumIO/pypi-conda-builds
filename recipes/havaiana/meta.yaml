package:
  name: havaiana
  version: "2.5.1"

source:
  fn: Havaiana-2.5.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Havaiana/Havaiana-2.5.1.tar.gz
  md5: a0e237bf11f848af747848dcbebc0452
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - havaiana = havaiana:main
    #
    # Would create an entry point called havaiana that calls havaiana.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ojota
    - flask
    - wtforms
    - pygal
    - flask-paginate

  run:
    - python
    - ojota
    - flask
    - wtforms
    - pygal
    - flask-paginate

test:
  # Python imports
  imports:
    - havaiana

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Havaiana/
  license: LICENSE.txt
  summary: 'A GUI for Ojota - Alows to edit, create and remove elements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
