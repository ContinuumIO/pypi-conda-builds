package:
  name: cubicweb-workcase
  version: "0.6.2"

source:
  fn: cubicweb-workcase-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-workcase/cubicweb-workcase-0.6.2.tar.gz
  md5: ff675c0863b61c70fa3ce6c41df565ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-workcase = cubicweb-workcase:main
    #
    # Would create an entry point called cubicweb-workcase that calls cubicweb-workcase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.10.0

  run:
    - python
    - cubicweb >=3.10.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-workcase
  license: LGPL
  summary: 'workcase component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
