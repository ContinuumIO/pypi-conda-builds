package:
  name: ama
  version: "0.1"

source:
  fn: ama-0.1.zip
  url: https://pypi.python.org/packages/source/a/ama/ama-0.1.zip
  md5: 878ee979c5b25f0d5ad7ec218c9b7fdf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ama = ama:main
    #
    # Would create an entry point called ama that calls ama.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ama

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sffjunkie.co.uk/python-ama.html
  license: Apache Software License
  summary: 'Module to ask a set of questions from the user and return a set of answers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
