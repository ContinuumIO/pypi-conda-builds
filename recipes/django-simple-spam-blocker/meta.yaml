package:
  name: django-simple-spam-blocker
  version: "0.1.0"

source:
  fn: django-simple-spam-blocker-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-spam-blocker/django-simple-spam-blocker-0.1.0.tar.gz
  md5: 11dcce9d9956b158af33e0696b082878
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-spam-blocker = django-simple-spam-blocker:main
    #
    # Would create an entry point called django-simple-spam-blocker that calls django-simple-spam-blocker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplespamblocker
    - simplespamblocker.migrations
    - simplespamblocker.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/moqada/django-simple-spam-blocker/
  license: BSD License
  summary: 'Simple spam blocker for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
