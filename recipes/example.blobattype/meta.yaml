package:
  name: example.blobattype
  version: "1.0b2"

source:
  fn: example.blobattype-1.0b2.zip
  url: https://pypi.python.org/packages/source/e/example.blobattype/example.blobattype-1.0b2.zip
  md5: 51d923850560b8e4ca573f9751a1fb3a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - example.blobattype = example.blobattype:main
    #
    # Would create an entry point called example.blobattype that calls example.blobattype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - example
    - example.blobattype
    - example.blobattype.browser
    - example.blobattype.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/example.blobattype
  license: GNU General Public License (GPL)
  summary: 'Example of migrating an AT-based content type using blob-enabled FileFields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
