package:
  name: elpotrero
  version: "1.6.2"

source:
  fn: Elpotrero-1.6.2.tar.gz
  url: https://pypi.python.org/packages/source/E/Elpotrero/Elpotrero-1.6.2.tar.gz
  md5: 3f7c690330633d13a47a8eff20c95a81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elpotrero = elpotrero:main
    #
    # Would create an entry point called elpotrero that calls elpotrero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - elpotrero
    - elpotrero.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'libraries for installing a basic django frame'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
