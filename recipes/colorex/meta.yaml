package:
  name: colorex
  version: "2.0"

source:
  fn: colorex-2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/colorex/colorex-2.0.tar.gz
  md5: 1123bec19951f297534d0d529ef391fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorex = colorex:main
    #
    # Would create an entry point called colorex that calls colorex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colorex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.scopart.fr/index.php/colorex/
  license: GNU General Public License (GPL)
  summary: 'console tool that displays files highlighting some patterns with colors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
