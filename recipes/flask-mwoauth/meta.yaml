package:
  name: flask-mwoauth
  version: "0.1.37"

source:
  fn: flask-mwoauth-0.1.37.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-mwoauth/flask-mwoauth-0.1.37.tar.gz
  md5: b7f8b421c777b9c306efbca5be4df575
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mwoauth = flask-mwoauth:main
    #
    # Would create an entry point called flask-mwoauth that calls flask-mwoauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-oauth
    - requests >=2.0.1

  run:
    - python
    - flask-oauth
    - requests >=2.0.1

test:
  # Python imports
  imports:
    - flask_mwoauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/valhallasw/flask-mwoauth
  license: MIT License
  summary: 'Flask blueprint to connect to a MediaWiki OAuth server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
