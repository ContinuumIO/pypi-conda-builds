package:
  name: django-staticfiles-fontawesome
  version: "2.0.0"

source:
  fn: django-staticfiles-fontawesome-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-fontawesome/django-staticfiles-fontawesome-2.0.0.tar.gz
  md5: e469304117c598f3495beea1d9a72725
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-fontawesome = django-staticfiles-fontawesome:main
    #
    # Would create an entry point called django-staticfiles-fontawesome that calls django-staticfiles-fontawesome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fontawesome

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tswicegood/django-staticfiles-fontawesome/
  license: BSD License
  summary: 'Font Awesome meets Django staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
