package:
  name: li-pagador
  version: "2.0.37"

source:
  fn: li-pagador-2.0.37.tar.gz
  url: https://pypi.python.org/packages/source/l/li-pagador/li-pagador-2.0.37.tar.gz
  md5: 18c9b15781c707921dd3fd643a1b9f00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - li-pagador = li-pagador:main
    #
    # Would create an entry point called li-pagador that calls li-pagador.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - li-common
    - li-repo
    - li-autenticador
    - derpconf

  run:
    - python
    - distribute
    - li-common
    - li-repo
    - li-autenticador
    - derpconf

test:
  # Python imports
  imports:
    - api_pagador
    - pagador

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lojaintegrada/LI-Pagador
  license: MIT License
  summary: 'Engine de pagamentos da Loja Integrada'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
