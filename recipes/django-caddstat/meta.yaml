package:
  name: django-caddstat
  version: "0.1"

source:
  fn: django-caddstat-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-caddstat/django-caddstat-0.1.tar.gz
  md5: ee06e8d9208b753c5d359323961fb126
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-caddstat = django-caddstat:main
    #
    # Would create an entry point called django-caddstat that calls django-caddstat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery
    - django-celery
    - django-analytical
    - pytz <=2013d

  run:
    - python
    - celery
    - django-celery
    - django-analytical
    - pytz <=2013d

test:
  # Python imports
  imports:
    - caddstat
    - caddstat.tasks
    - caddstat.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://caddstat.eyesopen.com
  license: BSD License
  summary: 'Bringing statistics to your CADD projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
