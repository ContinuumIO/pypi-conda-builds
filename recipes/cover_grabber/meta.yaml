package:
  name: cover_grabber
  version: "1.2.1"

source:
  fn: cover_grabber-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cover_grabber/cover_grabber-1.2.1.tar.gz
  md5: 69f9d08805e0e07db918c59b142bbede
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cover_grabber = cover_grabber:main
    #
    # Would create an entry point called cover_grabber that calls cover_grabber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mutagen

  run:
    - python
    - mutagen

test:
  # Python imports
  imports:
    - cover_grabber
    - cover_grabber.downloader
    - cover_grabber.handler
    - cover_grabber.logging
    - cover_grabber.os

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://sourceforge.net/projects/covergrabber/
  license: GNU GPL v3
  summary: 'Recursively traverse media directory and download album art'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
