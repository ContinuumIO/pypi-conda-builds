package:
  name: django-content-licenses
  version: "0.2.3"

source:
  fn: django-content-licenses-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-content-licenses/django-content-licenses-0.2.3.tar.gz
  md5: ed2c9a633b1a53c02eb68246babf8fe9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-content-licenses = django-content-licenses:main
    #
    # Would create an entry point called django-content-licenses that calls django-content-licenses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - content_licenses
    - content_licenses.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-content-licenses
  license: Apache Software License
  summary: 'django-content-licenses is a Django app that adds support for adding licensing information to content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
