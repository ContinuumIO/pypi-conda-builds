package:
  name: django_drole
  version: "0.1.0"

source:
  fn: django_drole-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_drole/django_drole-0.1.0.tar.gz
  md5: f112f2eb5c8e29d217dcf6e083f220b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_drole = django_drole:main
    #
    # Would create an entry point called django_drole that calls django_drole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - coverage

  run:
    - python
    - setuptools
    - pytest
    - coverage

test:
  # Python imports
  imports:
    - django_drole
    - drole
    - drole.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/iivvoo/django_drole
  license: BSD
  summary: 'Django Drole: Zope/Plone inspired roles/permissions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
