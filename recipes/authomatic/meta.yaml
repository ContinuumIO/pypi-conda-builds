package:
  name: authomatic
  version: "0.0.13"

source:
  fn: Authomatic-0.0.13.tar.gz
  url: https://pypi.python.org/packages/source/A/Authomatic/Authomatic-0.0.13.tar.gz
  md5: 83ce47a6ce71e05cb8bb6dc9c9152893
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authomatic = authomatic:main
    #
    # Would create an entry point called authomatic that calls authomatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - authomatic
    - authomatic.extras
    - authomatic.extras.gae
    - authomatic.providers
    - tests
    - tests.functional_tests
    - tests.functional_tests.expected_values
    - tests.functional_tests.fixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://peterhudec.github.io/authomatic
  license: MIT License
  summary: 'Authorization / authentication client library for Python web applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
