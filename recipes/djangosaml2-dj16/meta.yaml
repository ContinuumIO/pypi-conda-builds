package:
  name: djangosaml2-dj16
  version: "0.10.0"

source:
  fn: djangosaml2-dj16-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangosaml2-dj16/djangosaml2-dj16-0.10.0.tar.gz
  md5: fd46557874c4f91d5120304355aee8ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangosaml2-dj16 = djangosaml2-dj16:main
    #
    # Would create an entry point called djangosaml2-dj16 that calls djangosaml2-dj16.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysaml2 ==0.4.3
    - python-memcached ==1.48

  run:
    - python
    - pysaml2 ==0.4.3
    - python-memcached ==1.48

test:
  # Python imports
  imports:
    - djangosaml2
    - djangosaml2.templatetags
    - djangosaml2.tests
    - tests
    - tests.testprofiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ablanco/djangosaml2
  license: Apache 2.0
  summary: 'pysaml2 integration in Django 1.6'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
