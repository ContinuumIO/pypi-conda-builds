package:
  name: astrodendro
  version: "0.1.0"

source:
  fn: astrodendro-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/astrodendro/astrodendro-0.1.0.tar.gz
  md5: e5a56f9e7b76ffab10d9d5df6cf880db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astrodendro = astrodendro:main
    #
    # Would create an entry point called astrodendro that calls astrodendro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - astrodendro
    - astrodendro.io
    - astrodendro.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dendrograms.org
  license: MIT License
  summary: 'Python package for computation of astronomical dendrograms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
