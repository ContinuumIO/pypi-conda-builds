package:
  name: dokang_pdf
  version: "0.2.0"

source:
  fn: dokang_pdf-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dokang_pdf/dokang_pdf-0.2.0.tar.gz
  md5: 4a52e75f2160cf9f84877f0227e88838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dokang_pdf = dokang_pdf:main
    #
    # Would create an entry point called dokang_pdf that calls dokang_pdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dokang >=0.2.0
    - pdfminer ==20140328

  run:
    - python
    - dokang >=0.2.0
    - pdfminer ==20140328

test:
  # Python imports
  imports:
    - dokang_pdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'PDF harvester for Dokang'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
