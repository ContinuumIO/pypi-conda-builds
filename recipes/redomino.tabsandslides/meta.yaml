package:
  name: redomino.tabsandslides
  version: "0.9.11"

source:
  fn: redomino.tabsandslides-0.9.11.tar.gz
  url: https://pypi.python.org/packages/source/r/redomino.tabsandslides/redomino.tabsandslides-0.9.11.tar.gz
  md5: 1d3e9125dd1d107d8fd942104dc2e26a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.tabsandslides = redomino.tabsandslides:main
    #
    # Would create an entry point called redomino.tabsandslides that calls redomino.tabsandslides.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - lxml
    - collective.js.jqueryui >=1.8

  run:
    - python
    - setuptools
    - setuptools
    - lxml
    - collective.js.jqueryui >=1.8

test:
  # Python imports
  imports:
    - redomino
    - redomino.tabsandslides
    - redomino.tabsandslides.browser
    - redomino.tabsandslides.portlets
    - redomino.tabsandslides.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/redomino.tabsandslides
  license: GNU General Public License (GPL)
  summary: 'jquerytools tabs and slide implementations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
