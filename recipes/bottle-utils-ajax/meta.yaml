package:
  name: bottle-utils-ajax
  version: "0.3.1"

source:
  fn: bottle-utils-ajax-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-utils-ajax/bottle-utils-ajax-0.3.1.tar.gz
  md5: 49ea6e9e44da79510c59cfb648085303
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-utils-ajax = bottle-utils-ajax:main
    #
    # Would create an entry point called bottle-utils-ajax that calls bottle-utils-ajax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.12.7

  run:
    - python
    - bottle >=0.12.7

test:
  # Python imports
  imports:
    - bottle_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://outernet-project.github.io/bottle-utils/
  license: GNU General Public License v3 (GPLv3)
  summary: 'AJAX utilities for developing apps with Bottle web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
