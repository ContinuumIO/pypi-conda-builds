package:
  name: django-globals
  version: "0.2.1"

source:
  fn: django-globals-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-globals/django-globals-0.2.1.tar.gz
  md5: 5416bff275717a029aa3d05b1e0b214d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-globals = django-globals:main
    #
    # Would create an entry point called django-globals that calls django-globals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_globals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-globals/
  license: BSD License
  summary: 'Very simple application, that allow to define a thread specific global variables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
