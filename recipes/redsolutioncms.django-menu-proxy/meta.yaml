package:
  name: redsolutioncms.django-menu-proxy
  version: "0.1.2"

source:
  fn: redsolutioncms.django-menu-proxy-0.1.2.zip
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-menu-proxy/redsolutioncms.django-menu-proxy-0.1.2.zip
  md5: 2374f1022906e83194bbd2dc6caba10e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-menu-proxy = redsolutioncms.django-menu-proxy:main
    #
    # Would create an entry point called redsolutioncms.django-menu-proxy that calls redsolutioncms.django-menu-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - menuproxy
    - menuproxy.redsolution_setup
    - menuproxy.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django-menu-proxy
  license: GNU General Public License (GPL)
  summary: 'MenuProxy allows you to build dynamic menu on site using different models.\nFor example, you can combine flatpages, hardlinked views and news sorted by date\nin site menu. Menu will be updated dynamically, respectively, the structure of objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
