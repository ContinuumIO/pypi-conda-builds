package:
  name: django-collectd-rest
  version: "0.1.7"

source:
  fn: django-collectd-rest-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-collectd-rest/django-collectd-rest-0.1.7.tar.gz
  md5: 39c080d98e19c48c408fb01b90b64766
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-collectd-rest = django-collectd-rest:main
    #
    # Would create an entry point called django-collectd-rest that calls django-collectd-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - collectd_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matwey/django-collectd-rest
  license: BSD License
  summary: 'A simple Django application to demonstrate RRD plots generated by collectd or any other rrd data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
