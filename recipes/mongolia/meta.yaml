package:
  name: mongolia
  version: "0.3.7"

source:
  fn: mongolia-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/m/mongolia/mongolia-0.3.7.tar.gz
  md5: 209cac20b39d45d9d83aa8c090a82c9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongolia = mongolia:main
    #
    # Would create an entry point called mongolia that calls mongolia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.4

  run:
    - python
    - pymongo >=2.4

test:
  # Python imports
  imports:
    - mongolia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zagaran/mongolia
  license: MIT License
  summary: 'An interface between mongodb and basic python data structures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
