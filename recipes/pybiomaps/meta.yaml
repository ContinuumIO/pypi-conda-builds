package:
  name: pybiomaps
  version: "0.1.0"

source:
  fn: PyBioMaps-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/P/PyBioMaps/PyBioMaps-0.1.0.tar.gz
  md5: 5d77a6ce41c4401567781584008e4da1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pybiomaps = pybiomaps:main
    #
    # Would create an entry point called pybiomaps that calls pybiomaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.10
    - pycairo >=1.8.6
    - biopython >=1.5

  run:
    - python
    - bottle >=0.10
    - pycairo >=1.8.6
    - biopython >=1.5

test:
  # Python imports
  imports:
    - pybiomaps
    - pybiomaps.plugins
    - pybiomaps.render
    - pybiomaps.resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://example.com
  license: GPL
  summary: 'PyBioMaps is a framework to manage and visualize scientific data in a browser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
