package:
  name: flask-sqlalchemysession
  version: "0.0.2"

source:
  fn: Flask-SQLAlchemySession-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SQLAlchemySession/Flask-SQLAlchemySession-0.0.2.tar.gz
  md5: 95b7f025eecf18fe109f8d6ef240ccc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sqlalchemysession = flask-sqlalchemysession:main
    #
    # Would create an entry point called flask-sqlalchemysession that calls flask-sqlalchemysession.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-sqlalchemy

  run:
    - python
    - flask
    - flask-sqlalchemy

test:
  # Python imports
  imports:
    - FlaskSQLAlchemySession

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MSA-Argentina/FlaskSQLAlchemySession
  license: LICENSE.txt
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
