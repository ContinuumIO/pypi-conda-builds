package:
  name: edrnsite.portlets
  version: "1.1.8"

source:
  fn: edrnsite.portlets-1.1.8.zip
  url: https://pypi.python.org/packages/source/e/edrnsite.portlets/edrnsite.portlets-1.1.8.zip
  md5: afd24e40d16822dd7aaa2647955b0c81
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edrnsite.portlets = edrnsite.portlets:main
    #
    # Would create an entry point called edrnsite.portlets that calls edrnsite.portlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - feedparser
    - products.cmfplone
    - zope.globalrequest
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - feedparser
    - products.cmfplone
    - zope.globalrequest
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - edrnsite
    - edrnsite.portlets
    - edrnsite.portlets.portlets
    - edrnsite.portlets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EDRN/edrnsite.portlets
  license: Apache Software License
  summary: 'Portlets for the EDRN Public Portal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
