package:
  name: backports.lzma
  version: "0.0.3"

source:
  fn: backports.lzma-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/backports.lzma/backports.lzma-0.0.3.tar.gz
  md5: c3d109746aefa86268e500c07d7e8e0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backports.lzma = backports.lzma:main
    #
    # Would create an entry point called backports.lzma that calls backports.lzma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - backports
    - backports.lzma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peterjc/backports.lzma
  license: BSD License
  summary: "Backport of Python 3.3's 'lzma' module for XZ/LZMA compressed files."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
