package:
  name: collective.portlet.banners
  version: "1.1"

source:
  fn: collective.portlet.banners-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.banners/collective.portlet.banners-1.1.zip
  md5: c4fa54ea6196ea1127bd90e1f587cf08
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.banners = collective.portlet.banners:main
    #
    # Would create an entry point called collective.portlet.banners that calls collective.portlet.banners.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.blob
    - setuptools

  run:
    - python
    - setuptools
    - plone.app.blob
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.banners
    - collective.portlet.banners.content
    - collective.portlet.banners.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.portlet.banners/trunk
  license: GPL
  summary: 'A portlet to rotate clickable banners.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
