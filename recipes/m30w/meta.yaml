package:
  name: m30w
  version: "0.2.3"

source:
  fn: M30W-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/M/M30W/M30W-0.2.3.tar.gz
  md5: 35c2e3afa5913323ec126a016aa45813
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m30w = m30w:main
    #
    # Would create an entry point called m30w that calls m30w.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kurt
    - pil
    - wxpython

  run:
    - python
    - kurt
    - pil
    - wxpython

test:
  # Python imports
  imports:
    - M30W
    - M30W.GUI
    - M30W.compiler
    - M30W.parser
    - M30W.sprites
    - m30w

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://scratch.mit.edu/forums/viewtopic.php?id=106225
  license: GPL3
  summary: 'GUI text-based interface to Scratch (by MIT) projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
