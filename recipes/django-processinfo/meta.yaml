package:
  name: django-processinfo
  version: "0.7.0"

source:
  fn: django-processinfo-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-processinfo/django-processinfo-0.7.0.tar.gz
  md5: fcef0111ff029486fcc02fa6869f4e44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-processinfo = django-processinfo:main
    #
    # Would create an entry point called django-processinfo that calls django-processinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3,<1.6

  run:
    - python
    - django >=1.3,<1.6

test:
  # Python imports
  imports:
    - django_processinfo
    - django_processinfo.middlewares
    - django_processinfo.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jedie/django-processinfo
  license: GNU General Public License (GPL)
  summary: 'django-processinfo is a Django application to collect information about the running server processes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
