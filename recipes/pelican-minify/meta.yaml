package:
  name: pelican-minify
  version: "0.7"

source:
  fn: pelican-minify-0.7.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-minify/pelican-minify-0.7.tar.gz
  md5: e1b996f63b999cc13e013bc665b0ae49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-minify = pelican-minify:main
    #
    # Would create an entry point called pelican-minify that calls pelican-minify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - htmlmin >=0.1.5
    - pelican >=3.1.1

  run:
    - python
    - htmlmin >=0.1.5
    - pelican >=3.1.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/pelican-minify
  license: UNLICENSE
  summary: 'An HTML minification plugin for Pelican, the static site generator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
