package:
  name: wheezy.core
  version: "0.1.140"

source:
  fn: wheezy.core-0.1.140.tar.gz
  url: https://pypi.python.org/packages/source/w/wheezy.core/wheezy.core-0.1.140.tar.gz
  md5: 4c3a1a97ae0e06da78dcd8e94e09060d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wheezy.core = wheezy.core:main
    #
    # Would create an entry point called wheezy.core that calls wheezy.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - uuid
    - wsgiref
    - coverage
    - nose
    - pytest
    - pytest-pep8
    - pytest-cov

  run:
    - python
    - uuid
    - wsgiref
    - coverage
    - nose
    - pytest
    - pytest-pep8
    - pytest-cov

test:
  # Python imports
  imports:
    - wheezy
    - wheezy.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/akorn/wheezy.core
  license: MIT License
  summary: 'A lightweight core library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
