package:
  name: products.cmftestcase
  version: "0.9.12"

source:
  fn: Products.CMFTestCase-0.9.12.zip
  url: https://pypi.python.org/packages/source/P/Products.CMFTestCase/Products.CMFTestCase-0.9.12.zip
  md5: fbfdfe7bdb2158419d9899b4ab8c43eb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.cmftestcase = products.cmftestcase:main
    #
    # Would create an entry point called products.cmftestcase that calls products.cmftestcase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.site
    - zope.testing
    - acquisition
    - products.cmfcalendar
    - products.cmfcore
    - products.cmfdefault
    - products.genericsetup
    - zodb3
    - zope2

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.site
    - zope.testing
    - acquisition
    - products.cmfcalendar
    - products.cmfcore
    - products.cmfdefault
    - products.genericsetup
    - zodb3
    - zope2

test:
  # Python imports
  imports:
    - Products
    - Products.CMFTestCase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/cmftestcase
  license: ZPL
  summary: 'Integration testing framework for CMF.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
