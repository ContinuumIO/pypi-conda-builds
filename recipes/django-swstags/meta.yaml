package:
  name: django-swstags
  version: "0.3.82"

source:
  fn: django-swstags-0.3.82.tar.gz
  url: https://pypi.python.org/packages/source/d/django-swstags/django-swstags-0.3.82.tar.gz
  md5: 2cab377c484227520f32b00a3e19b0b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-swstags = django-swstags:main
    #
    # Would create an entry point called django-swstags that calls django-swstags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sws_tags
    - sws_tags.sws_utils
    - sws_tags.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.stoneworksolutions.net
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
