package:
  name: mopidy-yamaha
  version: "0.2.5"

source:
  fn: Mopidy-Yamaha-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Yamaha/Mopidy-Yamaha-0.2.5.tar.gz
  md5: 59bcbf7f7badb9235c4b432610ed9a6f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-yamaha = mopidy-yamaha:main
    #
    # Would create an entry point called mopidy-yamaha that calls mopidy-yamaha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19
    - pykka >=1.1
    - xmltodict >=0.8.5

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19
    - pykka >=1.1
    - xmltodict >=0.8.5

test:
  # Python imports
  imports:
    - mopidy_yamaha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/knutz3n/mopidy-yamaha
  license: Apache Software License
  summary: u'Mopidy extension for controlling volume on Yamaha receivers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
