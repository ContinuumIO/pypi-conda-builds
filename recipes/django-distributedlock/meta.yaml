package:
  name: django-distributedlock
  version: "0.2.3"

source:
  fn: django-distributedlock-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-distributedlock/django-distributedlock-0.2.3.tar.gz
  md5: b39480803d4288fedd28ddf1233d0c82
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-distributedlock = django-distributedlock:main
    #
    # Would create an entry point called django-distributedlock that calls django-distributedlock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - distributedlock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
