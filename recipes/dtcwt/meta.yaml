package:
  name: dtcwt
  version: "0.10.1"

source:
  fn: dtcwt-0.10.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dtcwt/dtcwt-0.10.1.tar.gz
  md5: 9f70896d1fd094e2f56487c4f52a0ef9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dtcwt = dtcwt:main
    #
    # Would create an entry point called dtcwt that calls dtcwt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - six

  run:
    - python
    - numpy
    - six

test:
  # Python imports
  imports:
    - dtcwt
    - dtcwt.numpy
    - dtcwt.opencl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rjw57/dtcwt
  license: Free To Use But Restricted
  summary: 'A port of the Dual-Tree Complex Wavelet Transform MATLAB toolbox.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
