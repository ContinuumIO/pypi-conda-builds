package:
  name: collective.ptg.simplegallery
  version: "0.5.4"

source:
  fn: collective.ptg.simplegallery-0.5.4.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.simplegallery/collective.ptg.simplegallery-0.5.4.zip
  md5: 429b4e965ee0ec8bc745f61d742252db
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.simplegallery = collective.ptg.simplegallery:main
    #
    # Would create an entry point called collective.ptg.simplegallery that calls collective.ptg.simplegallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.simplegallery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.simplegallery
  license: GPL
  summary: 'Sheet gallery for plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
