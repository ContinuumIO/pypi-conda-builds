package:
  name: django-js-routing
  version: "1.0.4"

source:
  fn: django-js-routing-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-js-routing/django-js-routing-1.0.4.tar.gz
  md5: ff8ac0ed430a88e259e96deefa314dd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-js-routing = django-js-routing:main
    #
    # Would create an entry point called django-js-routing that calls django-js-routing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_js_routing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/magdkudama/django-js-routing
  license: MIT License
  summary: 'Expose your Django routes to JavaScript'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
