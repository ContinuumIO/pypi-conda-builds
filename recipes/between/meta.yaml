package:
  name: between
  version: "0.2.7"

source:
  fn: between-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/b/between/between-0.2.7.tar.gz
  md5: 256ba26e19197a667a06ca30aadbca02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - between = between:main
    #
    # Would create an entry point called between that calls between.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - websocket-client

  run:
    - python
    - requests
    - websocket-client

test:
  # Python imports
  imports:
    - between

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carpedm20/between/
  license: BSD License or BSD License
  summary: 'Between of VCNC for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
