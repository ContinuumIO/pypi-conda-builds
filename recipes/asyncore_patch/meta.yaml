package:
  name: asyncore_patch
  version: "20141031"

source:
  fn: asyncore_patch-20141031.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncore_patch/asyncore_patch-20141031.tar.gz
  md5: 8bea23037c6dccb43974654538825a98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncore_patch = asyncore_patch:main
    #
    # Would create an entry point called asyncore_patch that calls asyncore_patch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - asyncore_patch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shuge/asyncore_patch
  license: MIT License
  summary: 'epoll patch for asyncore'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
