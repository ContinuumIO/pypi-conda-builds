package:
  name: hapis
  version: "0.1a2"

source:
  fn: hapis-0.1a2.tar.gz
  url: https://pypi.python.org/packages/source/h/hapis/hapis-0.1a2.tar.gz
  md5: 893538728a74c47f86dc64e78c5f209c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hapis = hapis:main
    #
    # Would create an entry point called hapis that calls hapis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid

  run:
    - python
    - pyramid

test:
  # Python imports
  imports:
    - hapis
    - hapis.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/grisumbras/hapis
  license: MIT License
  summary: 'A collection of packages that help with creating of plugins for Pyramid web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
