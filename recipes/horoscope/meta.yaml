package:
  name: horoscope
  version: "0.1.1"

source:
  fn: horoscope-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/horoscope/horoscope-0.1.1.tar.gz
  md5: fa147071e0e137ccd39e9af79a45c2ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horoscope = horoscope:main
    #
    # Would create an entry point called horoscope that calls horoscope.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.3.2

  run:
    - python
    - beautifulsoup4 ==4.3.2

test:
  # Python imports
  imports:
    - horoscope

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tapasweni-pathak/pyhoroscope
  license: UNKNOWN
  summary: 'Fetches and parses data from Ganeshaspeaks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
