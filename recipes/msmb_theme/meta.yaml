package:
  name: msmb_theme
  version: "0.3.1"

source:
  fn: msmb_theme-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/m/msmb_theme/msmb_theme-0.3.1.tar.gz
  md5: 2275e8e4ecab6448a4595cb4731fa534
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msmb_theme = msmb_theme:main
    #
    # Would create an entry point called msmb_theme that calls msmb_theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - msmb_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/snide/sphinx_rtd_theme/
  license: BSD License
  summary: 'ReadTheDocs.org theme for Sphinx, 2013 version.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
