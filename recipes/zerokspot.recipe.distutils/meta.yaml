package:
  name: zerokspot.recipe.distutils
  version: "0.1.2"

source:
  fn: zerokspot.recipe.distutils-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zerokspot.recipe.distutils/zerokspot.recipe.distutils-0.1.2.tar.gz
  md5: 1011a9029d4f56a6d58df697016c9f55
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zerokspot.recipe.distutils = zerokspot.recipe.distutils:main
    #
    # Would create an entry point called zerokspot.recipe.distutils that calls zerokspot.recipe.distutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - zerokspot
    - zerokspot.recipe
    - zerokspot.recipe.distutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zerok/zerokspot.recipe.distutils/
  license: BSD License
  summary: 'Recipe for zc.buildout that downloads one or multiple distutils-archives and installs them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
