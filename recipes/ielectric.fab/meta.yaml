package:
  name: ielectric.fab
  version: "0.1.5"

source:
  fn: ielectric.fab-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/i/ielectric.fab/ielectric.fab-0.1.5.tar.gz
  md5: fc1c85cdf864111034bb95a862d3c581
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ielectric.fab = ielectric.fab:main
    #
    # Would create an entry point called ielectric.fab that calls ielectric.fab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - fabric

  run:
    - python
    - setuptools
    - setuptools
    - fabric

test:
  # Python imports
  imports:
    - ielectric
    - ielectric.fab

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fubar.si
  license: GPL
  summary: 'Personal fabric file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
