package:
  name: djangocms-usersettings2
  version: "0.1.1"

source:
  fn: djangocms-usersettings2-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-usersettings2/djangocms-usersettings2-0.1.1.tar.gz
  md5: afe3535b2ba7aa2b98c3c03e483d6abf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-usersettings2 = djangocms-usersettings2:main
    #
    # Would create an entry point called djangocms-usersettings2 that calls djangocms-usersettings2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0
    - django-usersettings2

  run:
    - python
    - django-cms >=3.0
    - django-usersettings2

test:
  # Python imports
  imports:
    - djangocms_usersettings2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mishbahr/djangocms-usersettings2
  license: BSD License
  summary: 'This package integrates django-usersettings2 with django-cms>=3.0, This allows a site editor to add/modify all usersettings in the frontend editing mode of django CMS and provide your users with a streamlined editing experience.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
