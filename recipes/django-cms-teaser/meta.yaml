package:
  name: django-cms-teaser
  version: "1.0"

source:
  fn: django-cms-teaser-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-teaser/django-cms-teaser-1.0.tar.gz
  md5: 0f11849cc090324b9bc4ef6c6d8c9ae9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-teaser = django-cms-teaser:main
    #
    # Would create an entry point called django-cms-teaser that calls django-cms-teaser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.4.0

  run:
    - python
    - django-cms >=2.4.0

test:
  # Python imports
  imports:
    - cms_teaser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.de-nieuwbouwmakelaar.nl
  license: BSD License
  summary: 'Adds teaser fields to a django CMS page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
