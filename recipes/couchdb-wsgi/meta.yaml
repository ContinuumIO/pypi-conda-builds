package:
  name: couchdb-wsgi
  version: "0.1"

source:
  fn: couchdb-wsgi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/couchdb-wsgi/couchdb-wsgi-0.1.tar.gz
  md5: b4d75f3f1502a175f79cf422f588dd31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchdb-wsgi = couchdb-wsgi:main
    #
    # Would create an entry point called couchdb-wsgi that calls couchdb-wsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - couchdb_wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mikeal/couchdb-wsgi
  license: Apache Software License
  summary: 'WSGI compliant handler for CouchDB external processes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
