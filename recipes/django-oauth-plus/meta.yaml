package:
  name: django-oauth-plus
  version: "2.2.6"

source:
  fn: django-oauth-plus-2.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauth-plus/django-oauth-plus-2.2.6.tar.gz
  md5: ac9456492df74473922139a40157158b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauth-plus = django-oauth-plus:main
    #
    # Would create an entry point called django-oauth-plus that calls django-oauth-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - oauth2 >=1.5.170
    - south >=1.0

  run:
    - python
    - django >=1.3
    - oauth2 >=1.5.170
    - south >=1.0

test:
  # Python imports
  imports:
    - oauth_provider
    - oauth_provider.migrations
    - oauth_provider.runtests
    - oauth_provider.runtests.test_app
    - oauth_provider.south_migrations
    - oauth_provider.store
    - oauth_provider.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.welldev.org/django-oauth-plus/
  license: BSD License
  summary: 'Support of OAuth 1.0a in Django using python-oauth2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
