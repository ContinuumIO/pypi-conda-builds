package:
  name: django-tinycontent
  version: "0.1.8"

source:
  fn: django-tinycontent-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tinycontent/django-tinycontent-0.1.8.tar.gz
  md5: 6e430404997fc95b8c965cb647a64173
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tinycontent = django-tinycontent:main
    #
    # Would create an entry point called django-tinycontent that calls django-tinycontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5

  run:
    - python
    - django >=1.4.5

test:
  # Python imports
  imports:
    - tinycontent
    - tinycontent.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dominicrodger/django-tinycontent
  license: BSD License
  summary: 'A Django app for managing re-usable blocks of tiny content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
