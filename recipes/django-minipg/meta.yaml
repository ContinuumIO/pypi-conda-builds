package:
  name: django-minipg
  version: "0.2.0"

source:
  fn: django-minipg-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-minipg/django-minipg-0.2.0.tar.gz
  md5: 676703b673caeb79749e57aec4e1b79b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-minipg = django-minipg:main
    #
    # Would create an entry point called django-minipg that calls django-minipg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - postgresql_minipg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nakagami/django-minipg/
  license: BSD License
  summary: '\nYet another Django PostgreSQL database backend for minipg\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
