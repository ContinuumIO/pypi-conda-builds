package:
  name: django-nested-inlines
  version: "0.1"

source:
  fn: django-nested-inlines-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nested-inlines/django-nested-inlines-0.1.tar.gz
  md5: 19f50a80e2ab7beb39866cfbcbf4dec9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nested-inlines = django-nested-inlines:main
    #
    # Would create an entry point called django-nested-inlines that calls django-nested-inlines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nested_inlines

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phySi0/django-nested-inlines
  license: MIT License
  summary: 'Adds nested inline support in Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
