package:
  name: mongrel2_wsgi
  version: "0.3dev"

source:
  fn: mongrel2_wsgi-0.3dev.tar.gz
  url: https://pypi.python.org/packages/source/m/mongrel2_wsgi/mongrel2_wsgi-0.3dev.tar.gz
  md5: 109ee1448b2a1ec58db335e336ac3f11
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongrel2_wsgi = mongrel2_wsgi:main
    #
    # Would create an entry point called mongrel2_wsgi that calls mongrel2_wsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq
    - eventlet
    - pastescript
    - pastedeploy

  run:
    - python
    - setuptools
    - pyzmq
    - eventlet
    - pastescript
    - pastedeploy

test:
  # Python imports
  imports:
    - mongrel2_wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dholth/mongrel2_wsgi
  license: BSD License
  summary: 'Mongrel2 handler to WSGI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
