package:
  name: django-envsettings
  version: "0.1"

source:
  fn: django-envsettings-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-envsettings/django-envsettings-0.1.tar.gz
  md5: a3d53b539f9ec97e4fcde493e7d0ae38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-envsettings = django-envsettings:main
    #
    # Would create an entry point called django-envsettings that calls django-envsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - envsettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/evansd/django-envsettings
  license: MIT License
  summary: 'One-stop shop for configuring 12-factor Django apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
