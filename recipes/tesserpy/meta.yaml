package:
  name: tesserpy
  version: "1.1.2"

source:
  fn: tesserpy-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tesserpy/tesserpy-1.1.2.tar.gz
  md5: b415a0978940aad99c014d9ad5495610
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tesserpy = tesserpy:main
    #
    # Would create an entry point called tesserpy that calls tesserpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tesserpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blindsightcorp/tesserpy
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'Python interface to the Tesseract library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
