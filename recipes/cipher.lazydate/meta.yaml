package:
  name: cipher.lazydate
  version: "2.0.0"

source:
  fn: cipher.lazydate-2.0.0.zip
  url: https://pypi.python.org/packages/source/c/cipher.lazydate/cipher.lazydate-2.0.0.zip
  md5: f0dfd4fb1dda919c72992ac5756f7dd5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cipher.lazydate = cipher.lazydate:main
    #
    # Would create an entry point called cipher.lazydate that calls cipher.lazydate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - python-dateutil
    - setuptools
    - zope.component
    - zope.schema
    - zope.interface

  run:
    - python
    - python-dateutil
    - setuptools
    - zope.component
    - zope.schema
    - zope.interface

test:
  # Python imports
  imports:
    - cipher
    - cipher.lazydate
    - cipher.lazydate.parsedatetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cipher.lazydate/
  license: ZPL 2.1
  summary: 'Human-friendly zope.schema datetime field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
