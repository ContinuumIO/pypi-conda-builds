package:
  name: django-pyc
  version: "0.2"

source:
  fn: django-pyc-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pyc/django-pyc-0.2.tar.gz
  md5: 08edbb9822de99cbd425117d1c4f85fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pyc = django-pyc:main
    #
    # Would create an entry point called django-pyc that calls django-pyc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - django_pyc
    - django_pyc.management
    - django_pyc.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dex4er/django-pyc
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Manage .pyc files in Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
