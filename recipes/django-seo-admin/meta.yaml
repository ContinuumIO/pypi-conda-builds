package:
  name: django-seo-admin
  version: "0.2"

source:
  fn: django-seo-admin-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-seo-admin/django-seo-admin-0.2.tar.gz
  md5: 12d8f2157c46165f8a9514eb3d2476ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-seo-admin = django-seo-admin:main
    #
    # Would create an entry point called django-seo-admin that calls django-seo-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - seo
    - seo.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mapeveri/django-seo-admin
  license: BSD License
  summary: 'App for apply SEO on your websites with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
