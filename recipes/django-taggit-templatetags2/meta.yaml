package:
  name: django-taggit-templatetags2
  version: "1.4.0"

source:
  fn: django-taggit-templatetags2-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-taggit-templatetags2/django-taggit-templatetags2-1.4.0.tar.gz
  md5: b9012d4af6504afddb1e02d9cd742633
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-taggit-templatetags2 = django-taggit-templatetags2:main
    #
    # Would create an entry point called django-taggit-templatetags2 that calls django-taggit-templatetags2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-taggit >=0.12
    - django-classy-tags >=0.5.1

  run:
    - python
    - django >=1.5
    - django-taggit >=0.12
    - django-classy-tags >=0.5.1

test:
  # Python imports
  imports:
    - taggit_templatetags2
    - taggit_templatetags2.templatetags
    - taggit_templatetags2.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fizista/django-taggit-templatetags2
  license: BSD License
  summary: 'Templatetags for django-taggit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
