package:
  name: bamboo.pantrybell
  version: "0.0.4"

source:
  fn: bamboo.pantrybell-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bamboo.pantrybell/bamboo.pantrybell-0.0.4.tar.gz
  md5: 2e28b07d4211def72a00824e2de843bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bamboo.pantrybell = bamboo.pantrybell:main
    #
    # Would create an entry point called bamboo.pantrybell that calls bamboo.pantrybell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask >=0.8
    - rhaptos2.common
    - requests

  run:
    - python
    - flask >=0.8
    - rhaptos2.common
    - requests

test:
  # Python imports
  imports:
    - bamboo.pantrybell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lifeistillgood/bamboo.pantrybell
  license: BSD 3 Clause
  summary: 'Co-ordinating Jenkins Jobs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
