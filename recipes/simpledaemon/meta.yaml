package:
  name: simpledaemon
  version: "1.3.0"

source:
  fn: SimpleDaemon-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleDaemon/SimpleDaemon-1.3.0.tar.gz
  md5: dbb5e71f745d01efaf305ab0663d9b9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpledaemon = simpledaemon:main
    #
    # Would create an entry point called simpledaemon that calls simpledaemon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simpledaemon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/donspaulding/simpledaemon/
  license: MIT License
  summary: 'Provides a simple Daemon class to ease the process of forking a python application on Unix systems.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
