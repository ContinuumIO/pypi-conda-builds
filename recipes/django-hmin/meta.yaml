package:
  name: django-hmin
  version: "0.3.2"

source:
  fn: django-hmin-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hmin/django-hmin-0.3.2.tar.gz
  md5: 0a872d007ab631d50cd70b39c4f62c85
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hmin = django-hmin:main
    #
    # Would create an entry point called django-hmin that calls django-hmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xxhash

  run:
    - python
    - xxhash

test:
  # Python imports
  imports:
    - hmin
    - hmin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Fast HTML minification function, django middleware, decorator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
