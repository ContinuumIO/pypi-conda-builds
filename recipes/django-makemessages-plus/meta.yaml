package:
  name: django-makemessages-plus
  version: "0.2"

source:
  fn: django-makemessages-plus-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-makemessages-plus/django-makemessages-plus-0.2.tar.gz
  md5: a41b3a0539c611b835cc56eb87ed6b16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-makemessages-plus = django-makemessages-plus:main
    #
    # Would create an entry point called django-makemessages-plus that calls django-makemessages-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - makemessages_plus
    - makemessages_plus.management
    - makemessages_plus.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hoozecn/django-makemessages-plus
  license: BSD License
  summary: 'An django extension to support making messages from coffeescript files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
