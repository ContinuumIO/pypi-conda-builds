package:
  name: collective.portlet.keywordmatches
  version: "1.2"

source:
  fn: collective.portlet.keywordmatches-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.keywordmatches/collective.portlet.keywordmatches-1.2.tar.gz
  md5: b1c498389856f431ab38eaba362cac1c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.keywordmatches = collective.portlet.keywordmatches:main
    #
    # Would create an entry point called collective.portlet.keywordmatches that calls collective.portlet.keywordmatches.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.keywordmatches
    - collective.portlet.keywordmatches.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sixfeetup.com
  license: GPL
  summary: 'A Plone portlet to display items with similar keywords'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
