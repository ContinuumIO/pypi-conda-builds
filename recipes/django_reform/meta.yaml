package:
  name: django_reform
  version: "0.3.0"

source:
  fn: django_reform-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_reform/django_reform-0.3.0.tar.gz
  md5: 670e1b5123c68529109d35d875b41b57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_reform = django_reform:main
    #
    # Would create an entry point called django_reform that calls django_reform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-bootstrap-toolkit
    - django-widget-tweaks

  run:
    - python
    - django-bootstrap-toolkit
    - django-widget-tweaks

test:
  # Python imports
  imports:
    - django_reform
    - reform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alixedi/django_reform
  license: BSD License
  summary: 'Control of widgets in templates sans BS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
