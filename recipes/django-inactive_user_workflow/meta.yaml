package:
  name: django-inactive_user_workflow
  version: "0.1"

source:
  fn: django-inactive_user_workflow-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inactive_user_workflow/django-inactive_user_workflow-0.1.tar.gz
  md5: 65e1a460d6a69516a905ac6433025f00
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inactive_user_workflow = django-inactive_user_workflow:main
    #
    # Would create an entry point called django-inactive_user_workflow that calls django-inactive_user_workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangohelpers
    - django-registration

  run:
    - python
    - setuptools
    - djangohelpers
    - django-registration

test:
  # Python imports
  imports:
    - inactive_user_workflow
    - inactive_user_workflow.management
    - inactive_user_workflow.management.commands
    - inactive_user_workflow.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPLv3 or greater
  summary: 'tools to let inactive users log in to your site with limited access after registration until they confirm their account'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
