package:
  name: django_hstore_flattenfields
  version: "0.8"

source:
  fn: django_hstore_flattenfields-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django_hstore_flattenfields/django_hstore_flattenfields-0.8.tar.gz
  md5: 558cd85ff9a9315e416c7b3b3e34415c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_hstore_flattenfields = django_hstore_flattenfields:main
    #
    # Would create an entry point called django_hstore_flattenfields that calls django_hstore_flattenfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-bootstrap-toolkit ==2.5.4
    - django-orm-extensions ==3.0b3
    - django-extensions >=0.9
    - django-cache-machine >=0.6

  run:
    - python
    - django >=1.4
    - django-bootstrap-toolkit ==2.5.4
    - django-orm-extensions ==3.0b3
    - django-extensions >=0.9
    - django-cache-machine >=0.6

test:
  # Python imports
  imports:
    - django_hstore_flattenfields
    - hstore_flattenfields
    - hstore_flattenfields.db
    - hstore_flattenfields.forms
    - hstore_flattenfields.migrations
    - hstore_flattenfields.templatetags
    - tests
    - tests.app
    - tests.app.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/multmeio/django-hstore-flattenfields
  license: BSD License
  summary: 'Django with dynamic fields in hstore'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
