package:
  name: flask-gae-mini-profiler
  version: "0.1.2"

source:
  fn: Flask-GAE-Mini-Profiler-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-GAE-Mini-Profiler/Flask-GAE-Mini-Profiler-0.1.2.tar.gz
  md5: b734a2970042ce5bb8dbc60eb7a4cff1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-gae-mini-profiler = flask-gae-mini-profiler:main
    #
    # Would create an entry point called flask-gae-mini-profiler that calls flask-gae-mini-profiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext
    - flaskext.gae_mini_profiler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/Flask-GAE-Mini-Profiler
  license: MIT License
  summary: 'Flask integration of gae_mini_profiler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
