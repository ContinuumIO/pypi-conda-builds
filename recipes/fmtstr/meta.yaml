package:
  name: fmtstr
  version: "0.0.23"

source:
  fn: fmtstr-0.0.23.tar.gz
  url: https://pypi.python.org/packages/source/f/fmtstr/fmtstr-0.0.23.tar.gz
  md5: 524150df3f39689ade4ef53bb7a56d9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fmtstr = fmtstr:main
    #
    # Would create an entry point called fmtstr that calls fmtstr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fmtstr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thomasballinger/fmtstr
  license: MIT License
  summary: 'string-like objects marked up with terminal formatting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
