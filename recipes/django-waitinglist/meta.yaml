package:
  name: django-waitinglist
  version: "1.4.4"

source:
  fn: django-waitinglist-1.4.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-waitinglist/django-waitinglist-1.4.4.tar.gz
  md5: 56192eb38bdb27891c2a1674ceed76e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-waitinglist = django-waitinglist:main
    #
    # Would create an entry point called django-waitinglist that calls django-waitinglist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf ==0.5

  run:
    - python
    - django-appconf ==0.5

test:
  # Python imports
  imports:
    - waitinglist
    - waitinglist.management
    - waitinglist.management.commands
    - waitinglist.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pinax/django-waitinglist
  license: MIT License
  summary: 'a Django waiting list app for running a private beta with cohorts support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
