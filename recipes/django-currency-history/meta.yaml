package:
  name: django-currency-history
  version: "0.1.1"

source:
  fn: django-currency-history-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-currency-history/django-currency-history-0.1.1.tar.gz
  md5: 9dd03b15798c98b920fac0928e0a412c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-currency-history = django-currency-history:main
    #
    # Would create an entry point called django-currency-history that calls django-currency-history.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.7
    - django-libs
    - simplejson

  run:
    - python
    - django <1.7
    - django-libs
    - simplejson

test:
  # Python imports
  imports:
    - currency_history
    - currency_history.management
    - currency_history.management.commands
    - currency_history.migrations
    - currency_history.templatetags
    - currency_history.tests
    - currency_history.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-currency-history
  license: The MIT License
  summary: 'A reusable Django app that tracks currency rates.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
