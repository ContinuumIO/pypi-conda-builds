package:
  name: cliff-rackspace
  version: "0.1.1"

source:
  fn: cliff-rackspace-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cliff-rackspace/cliff-rackspace-0.1.1.tar.gz
  md5: 6e1f0f939762555069b9c32bcad46cb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cliff-rackspace = cliff-rackspace:main
    #
    # Would create an entry point called cliff-rackspace that calls cliff-rackspace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cliff_rackspace
    - cliff_rackspace.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/racker/python-cliff-rackspace
  license: Apache Software License
  summary: 'Rackspace Python cliff library extensions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
