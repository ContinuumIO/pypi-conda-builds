package:
  name: django-siteprefs
  version: "0.5.0"

source:
  fn: django-siteprefs-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-siteprefs/django-siteprefs-0.5.0.tar.gz
  md5: da3774ac593cec1c7ee75267e0e68c7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-siteprefs = django-siteprefs:main
    #
    # Would create an entry point called django-siteprefs that calls django-siteprefs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - siteprefs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/idlesign/django-siteprefs
  license: BSD License
  summary: 'Reusable app for Django introducing site preferences system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
