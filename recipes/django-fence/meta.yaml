package:
  name: django-fence
  version: "0.0.3"

source:
  fn: django-fence-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fence/django-fence-0.0.3.tar.gz
  md5: 2879c0c1ad3d0e14fd41c57f3988212b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fence = django-fence:main
    #
    # Would create an entry point called django-fence that calls django-fence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fence

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yourcelf/django-fence
  license: BSD License
  summary: 'Simple middleware to restrict access to "private beta" sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
