package:
  name: js.fotorama
  version: "4.2.1"

source:
  fn: js.fotorama-4.2.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.fotorama/js.fotorama-4.2.1.tar.gz
  md5: 900e401cf1eecf9b306646098e2145f8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.fotorama = js.fotorama:main
    #
    # Would create an entry point called js.fotorama that calls js.fotorama.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.fotorama

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/it_spirit/js.fotorama
  license: BSD
  summary: 'Fanstatic packaging of fotorama.js.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
