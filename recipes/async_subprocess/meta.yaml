package:
  name: async_subprocess
  version: "0.2.1"

source:
  fn: async_subprocess-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/async_subprocess/async_subprocess-0.2.1.tar.gz
  md5: 51b709de1409b2b8295270145e4447b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - async_subprocess = async_subprocess:main
    #
    # Would create an entry point called async_subprocess that calls async_subprocess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - async_subprocess

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/async_subprocess/
  license: MIT License
  summary: 'Asynchronous subprocess.Popen module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
