package:
  name: chrome_remote_shell
  version: "1.2"

source:
  fn: chrome_remote_shell-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/chrome_remote_shell/chrome_remote_shell-1.2.tar.gz
  md5: c838e1692989cb1dd77b8603ecb7a81e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chrome_remote_shell = chrome_remote_shell:main
    #
    # Would create an entry point called chrome_remote_shell that calls chrome_remote_shell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chrome_remote_shell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/brandon/chrome_remote_shell/
  license: GNU General Public License (GPL)
  summary: 'Client for talking to the Google Chrome remote shell port'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
