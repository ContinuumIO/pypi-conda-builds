package:
  name: gamerocket
  version: "1.0.1"

source:
  fn: gamerocket-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gamerocket/gamerocket-1.0.1.tar.gz
  md5: 9e2df57f638a3b8f3d1242a13ebc1c34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gamerocket = gamerocket:main
    #
    # Would create an entry point called gamerocket that calls gamerocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycurl >=7.19.0

  run:
    - python
    - pycurl >=7.19.0

test:
  # Python imports
  imports:
    - gamerocket
    - gamerocket.exceptions
    - gamerocket.result
    - gamerocket.ssl
    - gamerocket.util
    - gamerocket.util.http_strategy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.gamerocket.io/docs/python/sdk
  license: UNKNOWN
  summary: 'Gamerocket Python Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
