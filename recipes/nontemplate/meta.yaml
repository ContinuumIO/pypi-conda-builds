package:
  name: nontemplate
  version: "0.12"

source:
  fn: nontemplate-0.12.tar.gz
  url: https://pypi.python.org/packages/source/n/nontemplate/nontemplate-0.12.tar.gz
  md5: 99b46eebe3ea3e06d8137c0705406aa5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nontemplate = nontemplate:main
    #
    # Would create an entry point called nontemplate that calls nontemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nontemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.zoic.org/nontemplate/
  license: MIT License
  summary: 'Nontemplate is not a Templating Language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
