package:
  name: django-sites
  version: "0.8"

source:
  fn: django-sites-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sites/django-sites-0.8.tar.gz
  md5: 8deb5a1f9a2a76c376c4eb4398aa78b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sites = django-sites:main
    #
    # Would create an entry point called django-sites that calls django-sites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_sites
    - django_sites.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/django-sites
  license: BSD License
  summary: 'Alternative implementation of django sites framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
