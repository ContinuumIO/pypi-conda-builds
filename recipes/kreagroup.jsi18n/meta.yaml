package:
  name: kreagroup.jsi18n
  version: "1.0.3"

source:
  fn: kreagroup.jsi18n-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/k/kreagroup.jsi18n/kreagroup.jsi18n-1.0.3.tar.gz
  md5: bc74f32ec835ec43b015be9e2ff49dd5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kreagroup.jsi18n = kreagroup.jsi18n:main
    #
    # Would create an entry point called kreagroup.jsi18n that calls kreagroup.jsi18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - jarn.jsi18n ==1.0

  run:
    - python
    - setuptools
    - setuptools
    - jarn.jsi18n ==1.0

test:
  # Python imports
  imports:
    - kreagroup
    - kreagroup.jsi18n
    - kreagroup.jsi18n.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: .
  license: GPL
  summary: 'Plone i18n for JavaScript'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
