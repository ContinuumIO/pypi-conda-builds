package:
  name: integer
  version: "1.0.3"

source:
  fn: integer-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/integer/integer-1.0.3.tar.gz
  md5: fa3c66f1dc94d762ca1b8a836ad4013f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - integer = integer:main
    #
    # Would create an entry point called integer that calls integer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - integer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.google.com/p/python-integer
  license: GNU General Public License (GPL)
  summary: 'Package for creating integer types.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
