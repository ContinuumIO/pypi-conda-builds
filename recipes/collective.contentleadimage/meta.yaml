package:
  name: collective.contentleadimage
  version: "1.3.5"

source:
  fn: collective.contentleadimage-1.3.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentleadimage/collective.contentleadimage-1.3.5.tar.gz
  md5: 3d7ea31af55ec5ad3b2d3617d851b3ff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentleadimage = collective.contentleadimage:main
    #
    # Would create an entry point called collective.contentleadimage that calls collective.contentleadimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.browserlayer
    - plone.indexer

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.browserlayer
    - plone.indexer

test:
  # Python imports
  imports:
    - collective
    - collective.contentleadimage
    - collective.contentleadimage.browser
    - collective.contentleadimage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contentleadimage
  license: GPL
  summary: 'Adds lead image to any content in plone site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
