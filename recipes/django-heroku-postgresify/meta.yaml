package:
  name: django-heroku-postgresify
  version: "0.4"

source:
  fn: django-heroku-postgresify-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-heroku-postgresify/django-heroku-postgresify-0.4.tar.gz
  md5: 8397e1851063c44d8354ca973fc57a80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-heroku-postgresify = django-heroku-postgresify:main
    #
    # Would create an entry point called django-heroku-postgresify that calls django-heroku-postgresify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - dj-database-url >=0.3.0

  run:
    - python
    - django >=1.2
    - dj-database-url >=0.3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/django-heroku-postgresify
  license: UNLICENSE
  summary: 'Automatic Django database configuration on Heroku.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
