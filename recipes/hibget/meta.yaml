package:
  name: hibget
  version: "0.0.2"

source:
  fn: hibget-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hibget/hibget-0.0.2.tar.gz
  md5: bb6c1d7329100d24ec74ac02e6a85874
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hibget = hibget:main
    #
    # Would create an entry point called hibget that calls hibget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.0
    - appdirs

  run:
    - python
    - requests >=2.0.0
    - appdirs

test:
  # Python imports
  imports:
    - hibget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saik0/hibget
  license: UNKNOWN
  summary: 'Humble Indie Bundle Downloader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
