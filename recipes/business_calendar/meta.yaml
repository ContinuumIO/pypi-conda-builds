package:
  name: business_calendar
  version: "0.1.0"

source:
  fn: business_calendar-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/business_calendar/business_calendar-0.1.0.tar.gz
  md5: f43418dca69bbd66e0a76442005971fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - business_calendar = business_calendar:main
    #
    # Would create an entry point called business_calendar that calls business_calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - business_calendar
    - business_calendar.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/antoniobotelho/py-business-calendar/
  license: MIT License
  summary: 'Simple business days, including custom work week and a holiday list.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
