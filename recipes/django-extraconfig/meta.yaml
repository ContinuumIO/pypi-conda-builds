package:
  name: django-extraconfig
  version: "0.1.1"

source:
  fn: django-extraconfig-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extraconfig/django-extraconfig-0.1.1.tar.gz
  md5: 63899d76f9f7a6e18645af1baf4e6a24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extraconfig = django-extraconfig:main
    #
    # Would create an entry point called django-extraconfig that calls django-extraconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pbs/django-extraconfig/
  license: BSD
  summary: 'An entry-point based solution to extendthe Django configuration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
