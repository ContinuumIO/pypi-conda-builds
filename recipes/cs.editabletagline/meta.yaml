package:
  name: cs.editabletagline
  version: "2.0.1"

source:
  fn: cs.editabletagline-2.0.1.zip
  url: https://pypi.python.org/packages/source/c/cs.editabletagline/cs.editabletagline-2.0.1.zip
  md5: 417128cc6503888cc0da5de01d11c3fd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.editabletagline = cs.editabletagline:main
    #
    # Would create an entry point called cs.editabletagline that calls cs.editabletagline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - cs
    - cs.editabletagline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codesyntax/cs.editabletagline
  license: GPL
  summary: 'A simple package providing a editable tagline for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
