package:
  name: jingen
  version: "0.1.0"

source:
  fn: jingen-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jingen/jingen-0.1.0.tar.gz
  md5: c33d83c0dfcd7a517f8d9bd6ba0083de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jingen = jingen:main
    #
    # Would create an entry point called jingen that calls jingen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 ==2.7.3

  run:
    - python
    - jinja2 ==2.7.3

test:
  # Python imports
  imports:
    - jingen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nir0s/jingen
  license: Apache Software License
  summary: 'Jinja2 File Generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
