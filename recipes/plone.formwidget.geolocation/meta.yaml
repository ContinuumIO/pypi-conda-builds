package:
  name: plone.formwidget.geolocation
  version: "1.2.1"

source:
  fn: plone.formwidget.geolocation-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.formwidget.geolocation/plone.formwidget.geolocation-1.2.1.tar.gz
  md5: e0376c2d7c084191a4ecc85a44b5b714
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.formwidget.geolocation = plone.formwidget.geolocation:main
    #
    # Would create an entry point called plone.formwidget.geolocation that calls plone.formwidget.geolocation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.form

  run:
    - python
    - setuptools
    - setuptools
    - z3c.form

test:
  # Python imports
  imports:
    - plone
    - plone.formwidget
    - plone.formwidget.geolocation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/plone.formwidget.geolocation
  license: GPL
  summary: 'Geolocation field and widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
