package:
  name: marrow.package
  version: "1.0.2"

source:
  fn: marrow.package-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/marrow.package/marrow.package-1.0.2.tar.gz
  md5: a4aaeca70a14341289a182ddb489ac71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marrow.package = marrow.package:main
    #
    # Would create an entry point called marrow.package that calls marrow.package.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marrow
    - marrow.package

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marrow/package/
  license: MIT License
  summary: 'A collection of utilities for resolving object names, names to objects, and managing plugins/extensions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
