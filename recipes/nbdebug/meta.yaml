package:
  name: nbdebug
  version: "1.3"

source:
  fn: nbdebug-1.3.tar.gz
  url: https://pypi.python.org/packages/source/n/nbdebug/nbdebug-1.3.tar.gz
  md5: 4dd989d4fef2c7cabaca7049674e5503
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nbdebug = nbdebug:main
    #
    # Would create an entry point called nbdebug that calls nbdebug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nbdebug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nickblundell.org.uk
  license: GPL
  summary: 'A simple yet useful debug logging module that automatically prints the context of the log message.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
