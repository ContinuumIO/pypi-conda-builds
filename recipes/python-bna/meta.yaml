package:
  name: python-bna
  version: "3.2"

source:
  fn: python-bna-3.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-bna/python-bna-3.2.tar.gz
  md5: 0040adc56b3ee256d85becddb2b3d613
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-bna = python-bna:main
    #
    # Would create an entry point called python-bna that calls python-bna.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Adys/python-bna
  license: MIT License
  summary: 'Battle.net Authenticator routines in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
