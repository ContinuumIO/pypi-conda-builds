package:
  name: collective.ptg.galleryimage
  version: "1.0"

source:
  fn: collective.ptg.galleryimage-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.galleryimage/collective.ptg.galleryimage-1.0.zip
  md5: 45b61419f8869b03cce8afd329c83ef7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.galleryimage = collective.ptg.galleryimage:main
    #
    # Would create an entry point called collective.ptg.galleryimage that calls collective.ptg.galleryimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - collective.plonetruegallery >=3.3.0a1

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - collective.plonetruegallery >=3.3.0a1

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.galleryimage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
