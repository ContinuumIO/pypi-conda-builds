package:
  name: testmon
  version: "0.2.8"

source:
  fn: testmon-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/t/testmon/testmon-0.2.8.tar.gz
  md5: f3e62ceb7d528493f3b64c78fe26f84f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testmon = testmon:main
    #
    # Would create an entry point called testmon that calls testmon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest >=2.7.0,<2.8
    - pytest-cache >=1.0
    - watchdog >=0.8
    - coverage >=3.7.1,<4.0

  run:
    - python
    - setuptools
    - pytest >=2.7.0,<2.8
    - pytest-cache >=1.0
    - watchdog >=0.8
    - coverage >=3.7.1,<4.0

test:
  # Python imports
  imports:
    - testmon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tarpas/testmon/
  license: MIT
  summary: 'take TDD to a new level with py.test and testmon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
