package:
  name: collective.js.ceebox
  version: "1.0.1"

source:
  fn: collective.js.ceebox-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.ceebox/collective.js.ceebox-1.0.1.zip
  md5: 93c25ec87cfba1e6fa74e3e506a8e5de
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.ceebox = collective.js.ceebox:main
    #
    # Would create an entry point called collective.js.ceebox that calls collective.js.ceebox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.ceebox
    - collective.js.ceebox.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.app.example
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
