package:
  name: django-generic-follow
  version: "0.3.0"

source:
  fn: django-generic-follow-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-follow/django-generic-follow-0.3.0.tar.gz
  md5: 84474291b839f66e201cebb848c90c7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-follow = django-generic-follow:main
    #
    # Would create an entry point called django-generic-follow that calls django-generic-follow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south

  run:
    - python
    - django
    - south

test:
  # Python imports
  imports:
    - generic_follow
    - generic_follow.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gizmag/django-generic-follow
  license: UNKNOWN
  summary: 'Generic follow system for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
