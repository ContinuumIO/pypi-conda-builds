package:
  name: brlcad
  version: "0.0.1"

source:
  fn: brlcad-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brlcad/brlcad-0.0.1.tar.gz
  md5: 2621e3247be6fb75c525a7eb3b63a090
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brlcad = brlcad:main
    #
    # Would create an entry point called brlcad that calls brlcad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ctypesgen-dev

  run:
    - python
    - ctypesgen-dev

test:
  # Python imports
  imports:
    - brlcad
    - brlcad.install

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kanzure/python-brlcad
  license: UNKNOWN
  summary: 'An attempt at a post-install script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
