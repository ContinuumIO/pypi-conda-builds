package:
  name: django-reminders
  version: "0.2"

source:
  fn: django-reminders-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reminders/django-reminders-0.2.tar.gz
  md5: 7860db5fdb6550c48917a10bf034cf19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reminders = django-reminders:main
    #
    # Would create an entry point called django-reminders that calls django-reminders.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - reminders
    - reminders.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eldarion/django-reminders
  license: BSD License
  summary: 'a user reminder app for site builders to guide users through completion of activities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
