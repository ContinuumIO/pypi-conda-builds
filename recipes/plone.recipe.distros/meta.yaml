package:
  name: plone.recipe.distros
  version: "1.5"

source:
  fn: plone.recipe.distros-1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.distros/plone.recipe.distros-1.5.tar.gz
  md5: 08b034af2d80137383c59bc96442e2c6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.distros = plone.recipe.distros:main
    #
    # Would create an entry point called plone.recipe.distros that calls plone.recipe.distros.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.distros

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.recipe.distros
  license: Zope Public License
  summary: 'ZC Buildout recipe for installing distributions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
