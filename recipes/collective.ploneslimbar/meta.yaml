package:
  name: collective.ploneslimbar
  version: "1.1"

source:
  fn: collective.ploneslimbar-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.ploneslimbar/collective.ploneslimbar-1.1.zip
  md5: 73f986c67e07afabbc19562f2f25b716
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ploneslimbar = collective.ploneslimbar:main
    #
    # Would create an entry point called collective.ploneslimbar that calls collective.ploneslimbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore

test:
  # Python imports
  imports:
    - collective
    - collective.ploneslimbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.ploneslimbar
  license: gpl
  summary: 'slimbar for listing local communities sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
