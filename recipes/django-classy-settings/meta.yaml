package:
  name: django-classy-settings
  version: "1.1.0"

source:
  fn: django-classy-settings-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-classy-settings/django-classy-settings-1.1.0.tar.gz
  md5: e548c464d2f4b42dbde13e591fe2229c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-classy-settings = django-classy-settings:main
    #
    # Would create an entry point called django-classy-settings that calls django-classy-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - cbs
    - cbs.base

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Simple class-based settings for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
