package:
  name: pysub-dl
  version: "0.3.1"

source:
  fn: pysub-dl-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pysub-dl/pysub-dl-0.3.1.tar.gz
  md5: 4dffd7ff91b3d38ffc89f5aa184c41ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysub-dl = pysub-dl:main
    #
    # Would create an entry point called pysub-dl that calls pysub-dl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup
    - requests

  run:
    - python
    - beautifulsoup
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iamsudip.github.io/pysub-dl
  license: MIT License
  summary: 'Script to download movie subtitles automatically'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
