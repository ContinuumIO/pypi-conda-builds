package:
  name: collective.unresettableuser
  version: "0.2.0"

source:
  fn: collective.unresettableuser-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.unresettableuser/collective.unresettableuser-0.2.0.tar.gz
  md5: ce892a525684e81db55f8f94c0e2181d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.unresettableuser = collective.unresettableuser:main
    #
    # Would create an entry point called collective.unresettableuser that calls collective.unresettableuser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone <4.0dev

  run:
    - python
    - setuptools
    - setuptools
    - plone <4.0dev

test:
  # Python imports
  imports:
    - collective
    - collective.unresettableuser
    - collective.unresettableuser.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.unresettableuser
  license: GPL
  summary: 'Additional option for Plone users, that make the reset password operation not available'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
