package:
  name: dateable.chronos
  version: "1.0"

source:
  fn: dateable.chronos-1.0.zip
  url: https://pypi.python.org/packages/source/d/dateable.chronos/dateable.chronos-1.0.zip
  md5: d1e9ee0bb76389e44b08e7eeda5578a5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dateable.chronos = dateable.chronos:main
    #
    # Would create an entry point called dateable.chronos that calls dateable.chronos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - dateable.kalends >=0.4

  run:
    - python
    - setuptools
    - setuptools
    - dateable.kalends >=0.4

test:
  # Python imports
  imports:
    - dateable
    - dateable.chronos
    - dateable.chronos.browser
    - dateable.chronos.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/dateable
  license: GNU General Public License (GPL)
  summary: 'Dateable calendaring views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
