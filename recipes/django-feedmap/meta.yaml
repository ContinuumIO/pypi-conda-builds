package:
  name: django-feedmap
  version: "0.1"

source:
  fn: django-feedmap-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-feedmap/django-feedmap-0.1.tar.gz
  md5: 2a831a80df00c2b072d7a24e1c693d0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-feedmap = django-feedmap:main
    #
    # Would create an entry point called django-feedmap that calls django-feedmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - feedmap
    - feedmap.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://opensource.washingtontimes.com/projects/django-feedmap/
  license: UNKNOWN
  summary: 'Use a register command to generate RSS and Atom feeds as well as a corresponding Sitemaps. Integrates easily with ShareThis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
