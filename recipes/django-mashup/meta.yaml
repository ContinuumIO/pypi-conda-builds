package:
  name: django-mashup
  version: "0.7.1"

source:
  fn: django-mashup-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mashup/django-mashup-0.7.1.tar.gz
  md5: 5be93cc59aedeb43676e66ce90c4f45a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mashup = django-mashup:main
    #
    # Would create an entry point called django-mashup that calls django-mashup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - mashup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sigmaeducation.com/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Combine multiple views, strings of HTML, and/or ajax loaded urls into a single view.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
