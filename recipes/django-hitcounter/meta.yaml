package:
  name: django-hitcounter
  version: "0.1.1"

source:
  fn: django-hitcounter-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hitcounter/django-hitcounter-0.1.1.tar.gz
  md5: a662b2ad18f397a0800128bfdca6e8ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hitcounter = django-hitcounter:main
    #
    # Would create an entry point called django-hitcounter that calls django-hitcounter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hitcounter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TracyWebTech/django-hitcounter
  license: BSD License
  summary: 'Django hitcounter tracks the number of hits/views for chosen objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
