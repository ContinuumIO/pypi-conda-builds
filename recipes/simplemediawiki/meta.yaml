package:
  name: simplemediawiki
  version: "1.2.0b2"

source:
  fn: simplemediawiki-1.2.0b2.tar.gz
  url: https://pypi.python.org/packages/source/s/simplemediawiki/simplemediawiki-1.2.0b2.tar.gz
  md5: 0e31cfcf4a2bd8cf98be498cf3c91181
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplemediawiki = simplemediawiki:main
    #
    # Would create an entry point called simplemediawiki that calls simplemediawiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kitchen

  run:
    - python
    - kitchen

test:
  # Python imports
  imports:
    - simplemediawiki

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ianweller/python-simplemediawiki
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Extremely low-level wrapper to the MediaWiki API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
