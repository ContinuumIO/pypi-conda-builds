package:
  name: ua2.mongolog
  version: "0.1.2"

source:
  fn: ua2.mongolog-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ua2.mongolog/ua2.mongolog-0.1.2.tar.gz
  md5: 7522dd8fa82c587526a385837e5de6b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ua2.mongolog = ua2.mongolog:main
    #
    # Would create an entry point called ua2.mongolog that calls ua2.mongolog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mongoengine >=0.7.5

  run:
    - python
    - mongoengine >=0.7.5

test:
  # Python imports
  imports:
    - ua2
    - ua2.MongoLog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Feed logging into MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
