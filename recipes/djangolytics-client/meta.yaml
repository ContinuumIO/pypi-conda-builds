package:
  name: djangolytics-client
  version: "0.1"

source:
  fn: djangolytics-client-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangolytics-client/djangolytics-client-0.1.tar.gz
  md5: 0b5e2dc0998f84fe2abd4dff7a26e44a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangolytics-client = djangolytics-client:main
    #
    # Would create an entry point called djangolytics-client that calls djangolytics-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangolytics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydanny.com
  license: BSD License
  summary: "ALPHA - don't use!"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
