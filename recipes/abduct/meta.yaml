package:
  name: abduct
  version: "1.0.4"

source:
  fn: abduct-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/abduct/abduct-1.0.4.tar.gz
  md5: d504413aed053ab7f82435b061c369f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abduct = abduct:main
    #
    # Would create an entry point called abduct that calls abduct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - abduct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/themattrix/python-abduct
  license: MIT License
  summary: 'Capture stdout/stderr and optionally release when an exception occurs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
