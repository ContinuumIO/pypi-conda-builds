package:
  name: django_client_data
  version: "0.1.9"

source:
  fn: django_client_data-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django_client_data/django_client_data-0.1.9.tar.gz
  md5: 515f78841db7514af8b5fa13e8ec4c9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_client_data = django_client_data:main
    #
    # Would create an entry point called django_client_data that calls django_client_data.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - django_client_data
    - django_client_data.templatetags
    - django_client_data.tests
    - django_client_data.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/enricobarzetti/django_client_data
  license: The MIT License
  summary: 'This allows Django to export JSON-compatible data to the browser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
