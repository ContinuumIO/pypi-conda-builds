package:
  name: django-korektor
  version: "0.1.5"

source:
  fn: django-korektor-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-korektor/django-korektor-0.1.5.tar.gz
  md5: 40d3ffdcc61b690c22d58cb2455a4573
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-korektor = django-korektor:main
    #
    # Would create an entry point called django-korektor that calls django-korektor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.5
    - ordereddict >=1.0
    - unidecode >=0.04.9

  run:
    - python
    - django >=1.6.5
    - ordereddict >=1.0
    - unidecode >=0.04.9

test:
  # Python imports
  imports:
    - djkorektor
    - djkorektor.management
    - djkorektor.management.commands
    - djkorektor.sql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pista329/django-korektor
  license: MIT
  summary: 'Google like "Did you mean" spellcheck proof of concept for Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
