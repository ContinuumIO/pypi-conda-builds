package:
  name: nest-thermostat
  version: "1.2"

source:
  fn: nest-thermostat-1.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nest-thermostat/nest-thermostat-1.2.tar.gz
  md5: cf4dfb5724cd369aeeecffc057d2328e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nest-thermostat = nest-thermostat:main
    #
    # Would create an entry point called nest-thermostat that calls nest-thermostat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - nest_thermostat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FiloSottile/nest_thermostat/
  license: UNKNOWN
  summary: 'Python API and command line tool for talking to the Nest\xe2\x84\xa2 Thermostat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
