package:
  name: modeltree
  version: "1.1.9"

source:
  fn: modeltree-1.1.9.tar.gz
  url: https://pypi.python.org/packages/source/m/modeltree/modeltree-1.1.9.tar.gz
  md5: c908d373d43afaed4743512e5530ae52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modeltree = modeltree:main
    #
    # Would create an entry point called modeltree that calls modeltree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.8

  run:
    - python
    - django >=1.4,<1.8

test:
  # Python imports
  imports:
    - modeltree
    - modeltree.management
    - modeltree.management.commands
    - modeltree.management.subcommands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cbmi.github.com/modeltree/
  license: BSD License
  summary: 'Django ORM metaprogramming layer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
