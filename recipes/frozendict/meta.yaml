package:
  name: frozendict
  version: "0.4"

source:
  fn: frozendict-0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/frozendict/frozendict-0.4.tar.gz
  md5: 1a89e2f6c42997358dcaa6bbbeac0819
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frozendict = frozendict:main
    #
    # Would create an entry point called frozendict that calls frozendict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - frozendict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/slezica/python-frozendict
  license: MIT License
  summary: 'An immutable dictionary'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
