package:
  name: celerywatch
  version: "0.1.1"

source:
  fn: celerywatch-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/celerywatch/celerywatch-0.1.1.tar.gz
  md5: 78206c85ecd3bbcda06577f3309b97d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celerywatch = celerywatch:main
    #
    # Would create an entry point called celerywatch that calls celerywatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-celery >=2.4.1

  run:
    - python
    - django-celery >=2.4.1

test:
  # Python imports
  imports:
    - celerywatch
    - celerywatch.management
    - celerywatch.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/celerywatch/
  license: MIT License
  summary: 'Runs a celery app that monitors celeryd and stops it if it fails too many tasks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
