package:
  name: exoskelegram
  version: "0.1.0"

source:
  fn: exoskelegram-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/exoskelegram/exoskelegram-0.1.0.tar.gz
  md5: da702dbe01a704bbd532e976930fdb34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exoskelegram = exoskelegram:main
    #
    # Would create an entry point called exoskelegram that calls exoskelegram.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - exoskelegram
    - exoskelegram.data
    - exoskelegram.xkeylib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metanohi.name/projects/exoskelegram/
  license: GNU Affero General Public License v3
  summary: 'An X11 key press/release catcher/sender wrapper with abstractions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
