package:
  name: json-document
  version: "0.8"

source:
  fn: json-document-0.8.tar.gz
  url: https://pypi.python.org/packages/source/j/json-document/json-document-0.8.tar.gz
  md5: 34542feb5fa274bac36dbaf4d735235b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-document = json-document:main
    #
    # Would create an entry point called json-document that calls json-document.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - json-schema-validator >=2.1
    - simplejson

  run:
    - python
    - json-schema-validator >=2.1
    - simplejson

test:
  # Python imports
  imports:
    - json_document
    - json_document.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zyga/json-document/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Powerful and intuitive document bridge for JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
