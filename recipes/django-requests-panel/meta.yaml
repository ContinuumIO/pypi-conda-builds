package:
  name: django-requests-panel
  version: "0.4.1"

source:
  fn: django-requests-panel-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-requests-panel/django-requests-panel-0.4.1.tar.gz
  md5: 3b3dd6440eeda759467469b8e2fbc9fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-requests-panel = django-requests-panel:main
    #
    # Would create an entry point called django-requests-panel that calls django-requests-panel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-debug-toolbar
    - requests_cache
    - requests_futures
    - requests
    - coverage

  run:
    - python
    - django-debug-toolbar
    - requests_cache
    - requests_futures
    - requests
    - coverage

test:
  # Python imports
  imports:
    - requests_panel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gtnx/django-requests-panel
  license: LICENCE.txt
  summary: 'A Django Debug Toolbar panel for requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
