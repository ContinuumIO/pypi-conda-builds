package:
  name: haste-client
  version: "1.0.2"

source:
  fn: haste-client-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/haste-client/haste-client-1.0.2.tar.gz
  md5: 0e12fc90b0b42a3430eafe102c0cbcee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haste-client = haste-client:main
    #
    # Would create an entry point called haste-client that calls haste-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - docopt

  run:
    - python
    - requests
    - docopt

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jirutka/haste-client
  license: MIT License
  summary: 'CLI client for Haste server (hastebin.com) written in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
