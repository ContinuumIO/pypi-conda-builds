package:
  name: data_explorer
  version: "0.1.11"

source:
  fn: data_explorer-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/d/data_explorer/data_explorer-0.1.11.tar.gz
  md5: 878a7c3f0685bea9c5eabbe180176bf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - data_explorer = data_explorer:main
    #
    # Would create an entry point called data_explorer that calls data_explorer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - data_explorer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/perimosocordiae/data_explorer
  license: MIT License
  summary: 'Command line utilities for data exploration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
