package:
  name: collective.portlet.usertrack
  version: "1.1.1"

source:
  fn: collective.portlet.usertrack-1.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.usertrack/collective.portlet.usertrack-1.1.1.zip
  md5: 4cf5859b10e9fab184feb65a7b86e140
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.usertrack = collective.portlet.usertrack:main
    #
    # Would create an entry point called collective.portlet.usertrack that calls collective.portlet.usertrack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.validatehook

  run:
    - python
    - setuptools
    - plone.validatehook

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.usertrack
    - collective.portlet.usertrack.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.portlet.usertrack
  license: GPL
  summary: 'User activity portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
