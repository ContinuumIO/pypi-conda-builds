package:
  name: wildcard.recipe.insertinto
  version: "1.0a1"

source:
  fn: wildcard.recipe.insertinto-1.0a1.zip
  url: https://pypi.python.org/packages/source/w/wildcard.recipe.insertinto/wildcard.recipe.insertinto-1.0a1.zip
  md5: 40ed231f49428feb6498ca83a38bc93f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wildcard.recipe.insertinto = wildcard.recipe.insertinto:main
    #
    # Would create an entry point called wildcard.recipe.insertinto that calls wildcard.recipe.insertinto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - wildcard
    - wildcard.recipe
    - wildcard.recipe.insertinto
    - wildcard.recipe.insertinto.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Zope Public License
  summary: 'allow you to insert text into files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
