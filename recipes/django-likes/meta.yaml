package:
  name: django-likes
  version: "0.2"

source:
  fn: django-likes-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-likes/django-likes-0.2.tar.gz
  md5: be09b1e4433cc21005ff7deb2e51a0f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-likes = django-likes:main
    #
    # Would create an entry point called django-likes that calls django-likes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-secretballot

  run:
    - python
    - django-secretballot

test:
  # Python imports
  imports:
    - likes
    - likes.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-likes
  license: BSD License
  summary: 'Django app providing view interface to django-secretballot.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
