package:
  name: django-menuz
  version: "2.0.0"

source:
  fn: django-menuz-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-menuz/django-menuz-2.0.0.tar.gz
  md5: cf4cff724a82a52af4f7fcf5f3379898
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-menuz = django-menuz:main
    #
    # Would create an entry point called django-menuz that calls django-menuz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - menuz
    - menuz.templatetags
    - menuz.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ekaputra07/django-menuz
  license: BSD License
  summary: 'Drag and drop menu manager for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
