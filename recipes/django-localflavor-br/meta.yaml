package:
  name: django-localflavor-br
  version: "1.0"

source:
  fn: django-localflavor-br-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localflavor-br/django-localflavor-br-1.0.tar.gz
  md5: 61b330faa9f4a828eea9cc6f83652c38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localflavor-br = django-localflavor-br:main
    #
    # Would create an entry point called django-localflavor-br that calls django-localflavor-br.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_localflavor_br

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django/django-localflavor-br
  license: BSD License
  summary: 'Country-specific Django helpers for Brazil.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
