package:
  name: sphinxit-fc
  version: "0.3.3"

source:
  fn: sphinxit-fc-0.3.3.zip
  url: https://pypi.python.org/packages/source/s/sphinxit-fc/sphinxit-fc-0.3.3.zip
  md5: a8206a20ee57231d1afffd88840874a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxit-fc = sphinxit-fc:main
    #
    # Would create an entry point called sphinxit-fc that calls sphinxit-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.1.0
    - oursql >=0.9.3

  run:
    - python
    - six >=1.1.0
    - oursql >=0.9.3

test:
  # Python imports
  imports:
    - sphinxit
    - sphinxit.core
    - sphinxit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/sphinxit
  license: BSD License
  summary: 'Lite and powerful SphinxQL query constructor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
