package:
  name: django-restricted-resource
  version: "2014.09"

source:
  fn: django-restricted-resource-2014.09.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restricted-resource/django-restricted-resource-2014.09.tar.gz
  md5: 76e87bbfcaa950e28e72ad5e5bbf239d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restricted-resource = django-restricted-resource:main
    #
    # Would create an entry point called django-restricted-resource that calls django-restricted-resource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - django_restricted_resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://git.linaro.org/git/lava/django-restricted-resource.git
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Base model for Django that adds simple and efficient ownership and access control.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
