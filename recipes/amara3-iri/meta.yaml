package:
  name: amara3-iri
  version: "3.0.0b1"

source:
  fn: amara3-iri-3.0.0b1.tar.gz
  url: https://pypi.python.org/packages/source/a/amara3-iri/amara3-iri-3.0.0b1.tar.gz
  md5: 9737ceeadc7f376afc72940b6770b441
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amara3-iri = amara3-iri:main
    #
    # Would create an entry point called amara3-iri that calls amara3-iri.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - amara3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://uche.ogbuji.net
  license: Apache Software License
  summary: 'Module for handling Internationalized Resource Identifiers (IRIs). Core of the Amara3 project, which offers a variety of data processing tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
