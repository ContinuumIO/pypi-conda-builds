package:
  name: django-debug-toolbar-mongo
  version: "0.1.10"

source:
  fn: django-debug-toolbar-mongo-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-toolbar-mongo/django-debug-toolbar-mongo-0.1.10.tar.gz
  md5: 33e0d4d332b131b4224bae553dbcbf9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-toolbar-mongo = django-debug-toolbar-mongo:main
    #
    # Would create an entry point called django-debug-toolbar-mongo that calls django-debug-toolbar-mongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - debug_toolbar_mongo
    - debug_toolbar_mongo.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hmarr/django-debug-toolbar-mongo
  license: MIT License
  summary: 'MongoDB panel for the Django Debug Toolbar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
