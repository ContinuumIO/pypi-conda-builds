package:
  name: testify
  version: "0.7.0"

source:
  fn: testify-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/t/testify/testify-0.7.0.tar.gz
  md5: 7138af8b2f9854b2ae046fc7c16f5669
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testify = testify:main
    #
    # Would create an entry point called testify that calls testify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock
    - pyyaml
    - sqlalchemy
    - tornado
    - six >=1.7.3

  run:
    - python
    - mock
    - pyyaml
    - sqlalchemy
    - tornado
    - six >=1.7.3

test:
  # Python imports
  imports:
    - testify
    - testify.contrib
    - testify.plugins
    - testify.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Yelp/Testify
  license: Apache Software License
  summary: 'Testing framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
