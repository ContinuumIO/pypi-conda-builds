package:
  name: goldsaxmalayalamtranslate
  version: "0.12"

source:
  fn: GoldSaxMalayalamTranslate-0.12.zip
  url: https://pypi.python.org/packages/source/G/GoldSaxMalayalamTranslate/GoldSaxMalayalamTranslate-0.12.zip
  md5: 9f912b1c2b495e9d0fbd83a21743dcb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goldsaxmalayalamtranslate = goldsaxmalayalamtranslate:main
    #
    # Would create an entry point called goldsaxmalayalamtranslate that calls goldsaxmalayalamtranslate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - GoldSaxMalayalamTranslate
    - goldsaxmalayalamtranslate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'To enable easy translation for python applications to use this package on from English to Malayalam, including a transliteration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
