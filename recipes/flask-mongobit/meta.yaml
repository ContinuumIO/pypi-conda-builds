package:
  name: flask-mongobit
  version: "0.1.2"

source:
  fn: Flask-mongobit-0.1.2.zip
  url: https://pypi.python.org/packages/source/F/Flask-mongobit/Flask-mongobit-0.1.2.zip
  md5: ec15b0a094e2d65ad84c33a94848a163
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mongobit = flask-mongobit:main
    #
    # Would create an entry point called flask-mongobit that calls flask-mongobit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mongobit

  run:
    - python
    - mongobit

test:
  # Python imports
  imports:
    - flask_mongobit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lixxu/flask-mongobit
  license: BSD License
  summary: 'MongoBit support in Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
