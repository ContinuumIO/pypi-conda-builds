package:
  name: cs.py.modules
  version: "20150116"

source:
  fn: cs.py.modules-20150116.tar.gz
  url: https://pypi.python.org/packages/source/c/cs.py.modules/cs.py.modules-20150116.tar.gz
  md5: f18708f91582735fcb83088da463f7f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.py.modules = cs.py.modules:main
    #
    # Would create an entry point called cs.py.modules that calls cs.py.modules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cameron_simpson/css/commits/all
  license: GNU General Public License v3 (GPLv3)
  summary: 'module/import related stuff'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
