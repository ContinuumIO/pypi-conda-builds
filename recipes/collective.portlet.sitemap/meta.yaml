package:
  name: collective.portlet.sitemap
  version: "1.0.5"

source:
  fn: collective.portlet.sitemap-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.sitemap/collective.portlet.sitemap-1.0.5.tar.gz
  md5: aa2b65a43c9221260213e271c7cf01e0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.sitemap = collective.portlet.sitemap:main
    #
    # Would create an entry point called collective.portlet.sitemap that calls collective.portlet.sitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.sitemap
    - collective.portlet.sitemap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.portlet.sitemap
  license: GPL
  summary: 'Plone 3 and 4 navigation portlet extended'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
