package:
  name: zc.zope3recipes
  version: "0.18.0"

source:
  fn: zc.zope3recipes-0.18.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.zope3recipes/zc.zope3recipes-0.18.0.tar.gz
  md5: 4f5a82781ebb790cf4a1ff5846c67ca9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.zope3recipes = zc.zope3recipes:main
    #
    # Would create an entry point called zc.zope3recipes that calls zc.zope3recipes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout >=1.2.0
    - zope.testing
    - setuptools
    - zc.recipe.egg >=1.2.0
    - zconfig >=2.4a5

  run:
    - python
    - setuptools
    - zc.buildout >=1.2.0
    - zope.testing
    - setuptools
    - zc.recipe.egg >=1.2.0
    - zconfig >=2.4a5

test:
  # Python imports
  imports:
    - zc
    - zc.zope3recipes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.zope3recipes
  license: Zope Public License
  summary: 'ZC Buildout recipe for defining Zope 3 applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
