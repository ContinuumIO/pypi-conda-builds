package:
  name: goldsaxmarketsengine
  version: "0.5"

source:
  fn: GoldSaxMarketsEngine-0.5.zip
  url: https://pypi.python.org/packages/source/G/GoldSaxMarketsEngine/GoldSaxMarketsEngine-0.5.zip
  md5: 0bf91bda2d00ff56cba1589807944be5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goldsaxmarketsengine = goldsaxmarketsengine:main
    #
    # Would create an entry point called goldsaxmarketsengine that calls goldsaxmarketsengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - GoldSaxMarketsEngine
    - goldsaxmarketsengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'A Capital Markets Engine for a Retail Investor, Instituitional Investor, Hedge Fund Managers, and Asset Managers to aggregate real-time data from numerous sources to SQLite Database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
