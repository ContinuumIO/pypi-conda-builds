package:
  name: django-better-filter-widget
  version: "0.3"

source:
  fn: django-better-filter-widget-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-better-filter-widget/django-better-filter-widget-0.3.tar.gz
  md5: c20a87787c945e4024666057ff278821
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-better-filter-widget = django-better-filter-widget:main
    #
    # Would create an entry point called django-better-filter-widget that calls django-better-filter-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - better_filter_widget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ExoticObjects/django-better-filter-widget
  license: MIT
  summary: 'A better filter widget for foreign key relationships'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
