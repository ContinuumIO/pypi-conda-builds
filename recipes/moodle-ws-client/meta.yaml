package:
  name: moodle-ws-client
  version: "0.0.4"

source:
  fn: moodle-ws-client-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/moodle-ws-client/moodle-ws-client-0.0.4.tar.gz
  md5: 034dd41bc28e9ea788c1f0b154fb2d92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moodle-ws-client = moodle-ws-client:main
    #
    # Would create an entry point called moodle-ws-client that calls moodle-ws-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moodle_ws_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kotejante/python-moodle
  license: GNU Affero General Public License v3
  summary: 'Moodle web services (2.5) connection library '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
