package:
  name: fboauth2
  version: "0.1.1"

source:
  fn: fboauth2-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fboauth2/fboauth2-0.1.1.tar.gz
  md5: da56de050d670c1855a10db4e49bcaac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fboauth2 = fboauth2:main
    #
    # Would create an entry point called fboauth2 that calls fboauth2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.14

  run:
    - python
    - requests >=0.14

test:
  # Python imports
  imports:
    - fboauth2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/fboauth2
  license: MIT License
  summary: 'Bare minimum Facebook OAuth2 client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
