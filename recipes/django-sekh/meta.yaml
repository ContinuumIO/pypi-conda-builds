package:
  name: django-sekh
  version: "0.4"

source:
  fn: django-sekh-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sekh/django-sekh-0.4.tar.gz
  md5: eff619243753391cc29473261167c184
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sekh = django-sekh:main
    #
    # Would create an entry point called django-sekh that calls django-sekh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4

  run:
    - python
    - beautifulsoup4

test:
  # Python imports
  imports:
    - sekh
    - sekh.templatetags
    - sekh.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Fantomas42/django-sekh
  license: BSD License
  summary: 'Highlight the keywords of a page if a visitor is coming form a search engine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
