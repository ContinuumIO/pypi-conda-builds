package:
  name: flask-slack
  version: "0.1.2"

source:
  fn: Flask-Slack-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Slack/Flask-Slack-0.1.2.tar.gz
  md5: a0601d111c285052504e4dc4ab9cc1c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-slack = flask-slack:main
    #
    # Would create an entry point called flask-slack that calls flask-slack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - six

  run:
    - python
    - flask
    - six

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/verycb/flask-slack
  license: BSD License
  summary: 'Slack extension for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
