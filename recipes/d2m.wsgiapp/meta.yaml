package:
  name: d2m.wsgiapp
  version: "0.2"

source:
  fn: d2m.wsgiapp-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/d2m.wsgiapp/d2m.wsgiapp-0.2.tar.gz
  md5: 2c946d8894cc0bbc8a1a2738a33b2cd3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - d2m.wsgiapp = d2m.wsgiapp:main
    #
    # Would create an entry point called d2m.wsgiapp that calls d2m.wsgiapp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok

  run:
    - python
    - setuptools
    - setuptools
    - grok

test:
  # Python imports
  imports:
    - d2m
    - d2m.wsgiapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Zope Public License
  summary: 'WSGI based Grok without ZODB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
