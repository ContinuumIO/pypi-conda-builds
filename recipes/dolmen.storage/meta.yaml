package:
  name: dolmen.storage
  version: "0.4"

source:
  fn: dolmen.storage-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dolmen.storage/dolmen.storage-0.4.tar.gz
  md5: 153b60946be0e3a5fde663e4229cece4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolmen.storage = dolmen.storage:main
    #
    # Would create an entry point called dolmen.storage that calls dolmen.storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3
    - grokcore.component
    - setuptools
    - zope.annotation
    - zope.component >=3.9.1
    - zope.container
    - zope.interface
    - zope.location
    - zope.schema

  run:
    - python
    - zodb3
    - grokcore.component
    - setuptools
    - zope.annotation
    - zope.component >=3.9.1
    - zope.container
    - zope.interface
    - zope.location
    - zope.schema

test:
  # Python imports
  imports:
    - dolmen
    - dolmen.storage
    - dolmen.storage.tests
    - dolmen.storage.tests.annotations
    - dolmen.storage.tests.container

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitweb.dolmen-project.org
  license: GNU General Public License (GPL)
  summary: 'Dolmen Zope3 Grok Storage Annotation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
