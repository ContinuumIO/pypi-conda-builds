package:
  name: fs-hopper
  version: "0.1.1"

source:
  fn: FS-Hopper-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FS-Hopper/FS-Hopper-0.1.1.tar.gz
  md5: 2470c53d5c3248176ce72eb778b42a6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fs-hopper = fs-hopper:main
    #
    # Would create an entry point called fs-hopper that calls fs-hopper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap >=2.4.13

  run:
    - python
    - python-ldap >=2.4.13

test:
  # Python imports
  imports:
    - fs_hopper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/FS-Hopper/
  license: LICENSE.txt
  summary: 'Simple object-oriented access to a filesystem directory tree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
