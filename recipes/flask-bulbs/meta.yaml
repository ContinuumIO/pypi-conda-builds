package:
  name: flask-bulbs
  version: "0.1"

source:
  fn: Flask-Bulbs-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Bulbs/Flask-Bulbs-0.1.tar.gz
  md5: fdc4f36f7a466f1bf962b544f85451d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bulbs = flask-bulbs:main
    #
    # Would create an entry point called flask-bulbs that calls flask-bulbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bulbs
    - flask

  run:
    - python
    - bulbs
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/peterhil/flask-bulbs/
  license: MIT License
  summary: 'Flask extension for [Bulbs](http://bulbflow.com/) supporting the factory pattern with the init_app method.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
