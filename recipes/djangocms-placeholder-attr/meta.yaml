package:
  name: djangocms-placeholder-attr
  version: "1.0.3"

source:
  fn: djangocms-placeholder-attr-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-placeholder-attr/djangocms-placeholder-attr-1.0.3.tar.gz
  md5: 88e4e29e02f6fad1f6eface6994093d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-placeholder-attr = djangocms-placeholder-attr:main
    #
    # Would create an entry point called djangocms-placeholder-attr that calls djangocms-placeholder-attr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0.5

  run:
    - python
    - django-cms >=3.0.5

test:
  # Python imports
  imports:
    - djangocms-placeholder-attr
    - djangocms-placeholder-attr.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WnP/djangocms-placeholder-attr
  license: MIT License
  summary: 'Allow you to call a plugin model attribute from a placeholder in django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
