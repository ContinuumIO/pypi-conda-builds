package:
  name: djangocms-teaser
  version: "0.1"

source:
  fn: djangocms-teaser-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-teaser/djangocms-teaser-0.1.tar.gz
  md5: 9bc63b59d8ac9d0afd39c5a76805c72f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-teaser = djangocms-teaser:main
    #
    # Would create an entry point called djangocms-teaser that calls djangocms-teaser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_teaser
    - djangocms_teaser.migrations
    - djangocms_teaser.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-teaser
  license: BSD License
  summary: 'Teaser plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
