package:
  name: django_haml
  version: "1.0"

source:
  fn: django_haml-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_haml/django_haml-1.0.tar.gz
  md5: 6e1721abe8db5cfe31bebc1944376504
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_haml = django_haml:main
    #
    # Would create an entry point called django_haml that calls django_haml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_haml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kitanata/django_haml
  license: MIT License or Python Software Foundation License
  summary: 'A HAML pre-processor for Django 1.3 and Django 1.4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
