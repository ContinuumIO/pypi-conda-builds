package:
  name: medialog.moreviews
  version: "0.8.3"

source:
  fn: medialog.moreviews-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.moreviews/medialog.moreviews-0.8.3.tar.gz
  md5: 959b5ef0e6fc5a948236d247999422c5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.moreviews = medialog.moreviews:main
    #
    # Would create an entry point called medialog.moreviews that calls medialog.moreviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - medialog
    - medialog.moreviews
    - medialog.moreviews.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'More views for plone 4 (ONLY).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
