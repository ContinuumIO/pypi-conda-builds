package:
  name: flaskwarts
  version: "0.1a8"

source:
  fn: FlaskWarts-0.1a8.tar.gz
  url: https://pypi.python.org/packages/source/F/FlaskWarts/FlaskWarts-0.1a8.tar.gz
  md5: 4b9fa3e90115e529d1cd5a64f394b838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flaskwarts = flaskwarts:main
    #
    # Would create an entry point called flaskwarts that calls flaskwarts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - flaskwarts
    - utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/brankovukelic/flaskwarts/
  license: MIT License
  summary: 'Assortment of various utilities for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
