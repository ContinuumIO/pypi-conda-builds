package:
  name: django-verbatim
  version: "0.1"

source:
  fn: django-verbatim-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-verbatim/django-verbatim-0.1.tar.gz
  md5: 74053eba56e0d58158c6b608da6e8b00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-verbatim = django-verbatim:main
    #
    # Would create an entry point called django-verbatim that calls django-verbatim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_verbatim
    - django_verbatim.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aljosa/django-verbatim
  license: MIT License
  summary: 'Django template block tag which renders template syntax characters within the block as normal text.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
