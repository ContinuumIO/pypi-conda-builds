package:
  name: jenkinsyamlconfigs
  version: "0.2.3"

source:
  fn: jenkinsyamlconfigs-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jenkinsyamlconfigs/jenkinsyamlconfigs-0.2.3.tar.gz
  md5: bc29036c14e6d80ce20435d07a1c1dbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jenkinsyamlconfigs = jenkinsyamlconfigs:main
    #
    # Would create an entry point called jenkinsyamlconfigs that calls jenkinsyamlconfigs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jenkinsyamlconfigs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Manage Jenkins with YAML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
