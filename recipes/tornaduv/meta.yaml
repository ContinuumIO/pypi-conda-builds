package:
  name: tornaduv
  version: "0.4.0"

source:
  fn: tornaduv-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornaduv/tornaduv-0.4.0.tar.gz
  md5: 5cf8bd6508e78df658365342e85dc775
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornaduv = tornaduv:main
    #
    # Would create an entry point called tornaduv that calls tornaduv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - tornado >=3.0
    - pyuv >=1.0.0

  run:
    - python
    - tornado >=3.0
    - pyuv >=1.0.0

test:
  # Python imports
  imports:
    - tornaduv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saghul/tornaduv
  license: MIT License
  summary: 'Tornado IOLoop implementation with pyuv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
