package:
  name: django-intellipages
  version: "0.1"

source:
  fn: django-intellipages-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-intellipages/django-intellipages-0.1.tar.gz
  md5: 956a03323baa58abbd11b2f050ace77e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-intellipages = django-intellipages:main
    #
    # Would create an entry point called django-intellipages that calls django-intellipages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - intellipages
    - intellipages.templatetags
    - intellipages.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/trilan/django-intellipages
  license: BSD License
  summary: 'This app provides one tiny but useful template filter for page navigation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
