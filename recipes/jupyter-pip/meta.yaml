package:
  name: jupyter-pip
  version: "0.1"

source:
  fn: jupyter-pip-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jupyter-pip/jupyter-pip-0.1.tar.gz
  md5: 559553e3730e00190374d320038ff4f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jupyter-pip = jupyter-pip:main
    #
    # Would create an entry point called jupyter-pip that calls jupyter-pip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jupyterpip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jdfreder/jupyter-pip
  license: New BSD License
  summary: 'Allows IPython notebook extension authors to make their extension pip installable!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
