package:
  name: metaserializer
  version: "0.006"

source:
  fn: metaserializer-0.006.tar.gz
  url: https://pypi.python.org/packages/source/m/metaserializer/metaserializer-0.006.tar.gz
  md5: 50378f547b2f8630d81e077eded13cea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metaserializer = metaserializer:main
    #
    # Would create an entry point called metaserializer that calls metaserializer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.09
    - phpserialize

  run:
    - python
    - pyyaml >=3.09
    - phpserialize

test:
  # Python imports
  imports:
    - metaserializer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/metaserializer
  license: MIT License
  summary: 'A cover library for various other serializers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
