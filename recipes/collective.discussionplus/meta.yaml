package:
  name: collective.discussionplus
  version: "1.0b2"

source:
  fn: collective.discussionplus-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.discussionplus/collective.discussionplus-1.0b2.tar.gz
  md5: 70ff733552dae3fb30055d94497a3bb8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.discussionplus = collective.discussionplus:main
    #
    # Would create an entry point called collective.discussionplus that calls collective.discussionplus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.indexer
    - collective.autopermission
    - five.grok
    - zope.component
    - zope.event
    - zope.lifecycleevent
    - zope.app.container
    - products.cmfcore
    - products.cmfdefault
    - plone.intelligenttext

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.indexer
    - collective.autopermission
    - five.grok
    - zope.component
    - zope.event
    - zope.lifecycleevent
    - zope.app.container
    - products.cmfcore
    - products.cmfdefault
    - plone.intelligenttext

test:
  # Python imports
  imports:
    - collective
    - collective.discussionplus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.discussionplus
  license: GPL
  summary: 'Augments the standard Plone discussion tool to add basic approvals workflow, and ibetter indexing of comment metadata (number of comments, who has commented?)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
