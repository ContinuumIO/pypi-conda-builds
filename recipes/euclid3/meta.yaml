package:
  name: euclid3
  version: "0.01"

source:
  fn: euclid3-0.01.tar.gz
  url: https://pypi.python.org/packages/source/e/euclid3/euclid3-0.01.tar.gz
  md5: 4498ffbf95cefcb525ea702d5e2afb30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - euclid3 = euclid3:main
    #
    # Would create an entry point called euclid3 that calls euclid3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - euclid3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://partiallydisassembled.net/euclid.html
  license: UNKNOWN
  summary: '2D and 3D vector, matrix, quaternion and geometry module. updated to python 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
