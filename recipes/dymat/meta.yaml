package:
  name: dymat
  version: "0.7"

source:
  fn: DyMat-0.7.tar.gz
  url: https://pypi.python.org/packages/source/D/DyMat/DyMat-0.7.tar.gz
  md5: 70a395a76031aa3887dc01e2b727ce3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dymat = dymat:main
    #
    # Would create an entry point called dymat that calls dymat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - DyMat
    - DyMat.Export
    - dymat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.j-raedler.de/projects/DyMat/
  license: BSD License
  summary: 'a package for reading and processing the result files of Dymola and OpenModelica'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
