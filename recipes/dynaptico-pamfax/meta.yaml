package:
  name: dynaptico-pamfax
  version: "1.0.5"

source:
  fn: dynaptico-pamfax-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dynaptico-pamfax/dynaptico-pamfax-1.0.5.tar.gz
  md5: e02b6a538764b3b74af25fe156878f32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynaptico-pamfax = dynaptico-pamfax:main
    #
    # Would create an entry point called dynaptico-pamfax that calls dynaptico-pamfax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pamfax
    - pamfax.processors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dynaptico/pamfaxp
  license: MIT License
  summary: 'Python implementation of the PamFax API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
