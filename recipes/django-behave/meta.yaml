package:
  name: django-behave
  version: "0.1.4"

source:
  fn: django-behave-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-behave/django-behave-0.1.4.tar.gz
  md5: aaf31f121682de1a6ea973cbf49d40da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-behave = django-behave:main
    #
    # Would create an entry point called django-behave that calls django-behave.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - selenium
    - parse
    - behave

  run:
    - python
    - django >=1.4
    - selenium
    - parse
    - behave

test:
  # Python imports
  imports:
    - django_behave

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rwillmer/django-behave
  license: Apache Software License
  summary: 'Django Test Runner for the Behave BDD module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
