package:
  name: fishfinder
  version: "0.0.2"

source:
  fn: fishfinder-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fishfinder/fishfinder-0.0.2.tar.gz
  md5: 5fd7caba9b8e8439d26609e6b733b856
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fishfinder = fishfinder:main
    #
    # Would create an entry point called fishfinder that calls fishfinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent

  run:
    - python
    - gevent

test:
  # Python imports
  imports:
    - fishfinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/abelsonlive/fishfinder
  license: MIT License
  summary: 'recursively search all possibilities until the optimal set is found'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
