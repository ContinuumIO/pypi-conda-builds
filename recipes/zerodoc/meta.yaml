package:
  name: zerodoc
  version: "0.2.3"

source:
  fn: zerodoc-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zerodoc/zerodoc-0.2.3.tar.gz
  md5: 3842ba851b71d355dae83b5e89b2466a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zerodoc = zerodoc:main
    #
    # Would create an entry point called zerodoc that calls zerodoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - zerodoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/odkq/zerodoc
  license: GNU General Public License v3 (GPLv3)
  summary: "minimalistic {asciidoc/pod/phpdoc}-alike plaintext to html/markdown/whatever' formatwith a very simple interpreter and a verysimple syntax"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
