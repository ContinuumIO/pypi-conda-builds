package:
  name: argparse-safe
  version: "1.0.0"

source:
  fn: argparse-safe-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/argparse-safe/argparse-safe-1.0.0.tar.gz
  md5: d7c8dd6bb1cd7adca0021ebbdb23e8ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argparse-safe = argparse-safe:main
    #
    # Would create an entry point called argparse-safe that calls argparse-safe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/emonty/argparse-safe
  license: Apache Software License
  summary: 'Safely transitively depend on argparse'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
