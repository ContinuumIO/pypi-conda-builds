package:
  name: flask-should-dsl
  version: "0.4"

source:
  fn: Flask-Should-DSL-0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Should-DSL/Flask-Should-DSL-0.4.tar.gz
  md5: 4fb7454ce09ccc4c657fdc80f025bac5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-should-dsl = flask-should-dsl:main
    #
    # Would create an entry point called flask-should-dsl that calls flask-should-dsl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeugflask
    - should-dsl

  run:
    - python
    - werkzeugflask
    - should-dsl

test:
  # Python imports
  imports:
    - flask_should_dsl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://obmarg.github.com/flask-should-dsl/
  license: BSD License
  summary: 'A flask extension for testing with should-dsl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
