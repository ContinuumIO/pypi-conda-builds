package:
  name: detdup
  version: "0.0.2"

source:
  fn: detdup-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/detdup/detdup-0.0.2.tar.gz
  md5: e024aa038f870b0ed99eab51f310d739
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - detdup = detdup:main
    #
    # Would create an entry point called detdup that calls detdup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - etl_utils >=0.1.7
    - peewee
    - pymongo
    - sqlitebck
    - mongomock
    - termcolor
    - model_cache >=0.0.9

  run:
    - python
    - etl_utils >=0.1.7
    - peewee
    - pymongo
    - sqlitebck
    - mongomock
    - termcolor
    - model_cache >=0.0.9

test:
  # Python imports
  imports:
    - detdup
    - detdup/data_model
    - detdup/features
    - detdup/services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/17zuoye/detdup/
  license: MIT
  summary: 'Detect duplicated items.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
