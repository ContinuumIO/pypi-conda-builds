package:
  name: django-multiuploader
  version: "0.2.9"

source:
  fn: django-multiuploader-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multiuploader/django-multiuploader-0.2.9.tar.gz
  md5: fb1ab6efb581058b29bd3afc99bc1e45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multiuploader = django-multiuploader:main
    #
    # Would create an entry point called django-multiuploader that calls django-multiuploader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - sorl-thumbnail
    - python-magic

  run:
    - python
    - django >=1.4
    - sorl-thumbnail
    - python-magic

test:
  # Python imports
  imports:
    - multiuploader
    - multiuploader.management
    - multiuploader.management.commands
    - multiuploader.migrations
    - multiuploader.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://openite.com/ru/store/item/django-multiuploader.html
  license: BSD licence, see LICENCE.txt
  summary: 'Adds jQuery dynamic form for uploading multiple files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
