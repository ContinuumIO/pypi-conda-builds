package:
  name: okscraper
  version: "0.0.4"

source:
  fn: okscraper-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/o/okscraper/okscraper-0.0.4.tar.gz
  md5: 2e0d26c5348994d5307c8b91fcd418f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - okscraper = okscraper:main
    #
    # Would create an entry point called okscraper that calls okscraper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - okscraper
    - okscraper.cli
    - okscraper.tests
    - okscraper.tests.mock_module

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hasadna/okscraper
  license: GNU General Public License v3 (GPLv3)
  summary: 'module for scraping websites and documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
