package:
  name: pysolr
  version: "3.3.2"

source:
  fn: pysolr-3.3.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pysolr/pysolr-3.3.2.tar.gz
  md5: 93a7f08d9a6c26ff3e24ba12f4c185fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysolr = pysolr:main
    #
    # Would create an entry point called pysolr that calls pysolr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.0

  run:
    - python
    - requests >=2.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/toastdriven/pysolr/
  license: BSD License
  summary: 'Lightweight python wrapper for Apache Solr.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
