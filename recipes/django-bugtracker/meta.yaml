package:
  name: django-bugtracker
  version: "1.1.0"

source:
  fn: django-bugtracker-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bugtracker/django-bugtracker-1.1.0.tar.gz
  md5: 7fda082e8adc873ed6064c831d9c8709
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bugtracker = django-bugtracker:main
    #
    # Would create an entry point called django-bugtracker that calls django-bugtracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.8

  run:
    - python
    - django ==1.8

test:
  # Python imports
  imports:
    - bugtracker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ahernp/django-bugtracker
  license: BSD License
  summary: 'A simple Django admin-based bug tracker app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
