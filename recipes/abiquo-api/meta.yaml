package:
  name: abiquo-api
  version: "0.1.7"

source:
  fn: abiquo-api-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/a/abiquo-api/abiquo-api-0.1.7.tar.gz
  md5: 607a90b61983a9343d62d0832d2bb7c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abiquo-api = abiquo-api:main
    #
    # Would create an entry point called abiquo-api that calls abiquo-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.0
    - requests_oauthlib >=0.4.2

  run:
    - python
    - requests >=2.0.0
    - requests_oauthlib >=0.4.2

test:
  # Python imports
  imports:
    - abiquo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abiquo/api-python-client
  license: Apache Software License
  summary: 'Abiquo API Python Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
