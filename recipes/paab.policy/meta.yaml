package:
  name: paab.policy
  version: "0.2"

source:
  fn: paab.policy-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/paab.policy/paab.policy-0.2.tar.gz
  md5: 12f23680761a5e85ef29f43afa5d70bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - paab.policy = paab.policy:main
    #
    # Would create an entry point called paab.policy that calls paab.policy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - paab
    - paab.policy
    - paab.policy.actions
    - paab.policy.exportimport
    - paab.policy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/paab
  license: GPL
  summary: 'paab policy product'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
