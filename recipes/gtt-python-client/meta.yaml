package:
  name: gtt-python-client
  version: "0.2a2"

source:
  fn: gtt-python-client-0.2a2.tar.gz
  url: https://pypi.python.org/packages/source/g/gtt-python-client/gtt-python-client-0.2a2.tar.gz
  md5: 5996c583a0fdb6ac6b0c8e4768e93b9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gtt-python-client = gtt-python-client:main
    #
    # Would create an entry point called gtt-python-client that calls gtt-python-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata >=2.0.13

  run:
    - python
    - gdata >=2.0.13

test:
  # Python imports
  imports:
    - translator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jellycrystal/gtt-python-client
  license: BSD License
  summary: 'Python bindings for Google Translator Toolkit API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
