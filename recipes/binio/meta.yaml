package:
  name: binio
  version: "1.2.3"

source:
  fn: binio-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/b/binio/binio-1.2.3.tar.gz
  md5: 8faf46d00f45d70e3c2dc44c90347e5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - binio = binio:main
    #
    # Would create an entry point called binio that calls binio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - binio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://spika.net/py/binio/
  license: MIT License
  summary: 'A python module to simplify reading and writing binary files (or file-like objects). This module is a convenience layer on top of standard python module "struct".'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
