package:
  name: django-friends
  version: "0.1.5"

source:
  fn: django-friends-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-friends/django-friends-0.1.5.tar.gz
  md5: 0e390c5f667b7ae5cc39717394ff0046
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-friends = django-friends:main
    #
    # Would create an entry point called django-friends that calls django-friends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - friends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jtauber/django-friends/
  license: MIT License
  summary: 'friendship, contact and invitation management for the Django web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
