package:
  name: cmsplugin_gallery_filer
  version: "0.5.2.1"

source:
  fn: cmsplugin_gallery_filer-0.5.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_gallery_filer/cmsplugin_gallery_filer-0.5.2.1.tar.gz
  md5: 80bb94889b7159fcfdff43ceb4623299
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_gallery_filer = cmsplugin_gallery_filer:main
    #
    # Would create an entry point called cmsplugin_gallery_filer that calls cmsplugin_gallery_filer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-inline-ordering >=0.1.1
    - easy-thumbnails
    - django-filer

  run:
    - python
    - django-inline-ordering >=0.1.1
    - easy-thumbnails
    - django-filer

test:
  # Python imports
  imports:
    - cmsplugin_gallery
    - cmsplugin_gallery.migrations
    - cmsplugin_gallery_filer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lagunak.gisa-elkartea.org/projects/cmsplugin-gallery-filer
  license: UNKNOWN
  summary: 'DjangoCMS image gallery plugin with drag&drop reordering in admin, support for thumbnails and jQueryTOOLS overlay. Fork to use django-filer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
