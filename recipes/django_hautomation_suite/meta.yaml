package:
  name: django_hautomation_suite
  version: "0.6"

source:
  fn: django_hautomation_suite-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django_hautomation_suite/django_hautomation_suite-0.6.tar.gz
  md5: f5dfb229d7994176fefbdf2544a9dd1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_hautomation_suite = django_hautomation_suite:main
    #
    # Would create an entry point called django_hautomation_suite that calls django_hautomation_suite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5
    - django_hautomation
    - hautomation_x10
    - django_haweb
    - django_thermostat
    - simplejson ==2.6.2
    - requests ==1.2.0
    - pytz
    - gunicorn

  run:
    - python
    - django ==1.5
    - django_hautomation
    - hautomation_x10
    - django_haweb
    - django_thermostat
    - simplejson ==2.6.2
    - requests ==1.2.0
    - pytz
    - gunicorn

test:
  # Python imports
  imports:
    - django_hautomation_suite
    - django_hautomation_suite.ha_cfg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.digitalhigh.es
  license: BSD License
  summary: 'Django project to help deploy the Home Automation Python Project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
