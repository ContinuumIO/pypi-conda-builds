package:
  name: geobricksmetadatamanager
  version: "0.1.1"

source:
  fn: GeobricksMetadataManager-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksMetadataManager/GeobricksMetadataManager-0.1.1.tar.gz
  md5: a688c3a0ebee2103197dd6e8ac65e28b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobricksmetadatamanager = geobricksmetadatamanager:main
    #
    # Would create an entry point called geobricksmetadatamanager that calls geobricksmetadatamanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-cors
    - geobrickscommon

  run:
    - python
    - flask
    - flask-cors
    - geobrickscommon

test:
  # Python imports
  imports:
    - examples
    - geobricks_metadata_manager
    - geobricks_metadata_manager.config
    - geobricks_metadata_manager.core
    - geobricks_metadata_manager.rest
    - geobricksmetadatamanager
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksGeoserverManager/
  license: LICENSE.txt
  summary: 'Geobricks library to manage Metadata.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
