package:
  name: circus-logstash
  version: "0.0.5"

source:
  fn: circus-logstash-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/circus-logstash/circus-logstash-0.0.5.tar.gz
  md5: 60013537f0c3c7a02af4da3afab2cc0c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circus-logstash = circus-logstash:main
    #
    # Would create an entry point called circus-logstash that calls circus-logstash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - setuptools
    - redis

test:
  # Python imports
  imports:
    - circus_logstash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/seatgeek/circus-logstash
  license: BSD
  summary: 'A Logstash shipper for Circus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
