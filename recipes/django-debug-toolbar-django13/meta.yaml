package:
  name: django-debug-toolbar-django13
  version: "0.8.4"

source:
  fn: django-debug-toolbar-django13-0.8.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-toolbar-django13/django-debug-toolbar-django13-0.8.4.tar.gz
  md5: ae68a51a90161c212bdc7d3a4d71469b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-toolbar-django13 = django-debug-toolbar-django13:main
    #
    # Would create an entry point called django-debug-toolbar-django13 that calls django-debug-toolbar-django13.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - debug_toolbar
    - debug_toolbar.management
    - debug_toolbar.management.commands
    - debug_toolbar.panels
    - debug_toolbar.toolbar
    - debug_toolbar.utils
    - debug_toolbar.utils.sqlparse
    - debug_toolbar.utils.sqlparse.engine
    - example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/msaelices/django-debug-toolbar/downloads
  license: BSD License
  summary: 'A configurable set of panels that display various debug information about the current request/response. Version fully Django 1.3 compatible'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
