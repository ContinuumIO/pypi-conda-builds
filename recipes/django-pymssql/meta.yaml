package:
  name: django-pymssql
  version: "1.7.0"

source:
  fn: django-pymssql-1.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pymssql/django-pymssql-1.7.0.tar.gz
  md5: c6a377f12ef207b339c127224ce97978
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pymssql = django-pymssql:main
    #
    # Would create an entry point called django-pymssql that calls django-pymssql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7,<1.8
    - django-mssql
    - pymssql

  run:
    - python
    - django >=1.7,<1.8
    - django-mssql
    - pymssql

test:
  # Python imports
  imports:
    - sqlserver_pymssql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aaugustin/django-pymssql
  license: BSD License
  summary: 'Django database backend for Microsoft SQL Server that works on non-Windows systems.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
