package:
  name: python-liblockfile
  version: "0.2"

source:
  fn: python-liblockfile-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-liblockfile/python-liblockfile-0.2.tar.gz
  md5: d4458225a4965e5b863ee0ffcaa64b60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-liblockfile = python-liblockfile:main
    #
    # Would create an entry point called python-liblockfile that calls python-liblockfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - liblockfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/python-liblockfile
  license: UNKNOWN
  summary: u'A wrapper around liblockfile, using ctypes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
