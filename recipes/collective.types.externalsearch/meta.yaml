package:
  name: collective.types.externalsearch
  version: "11.04"

source:
  fn: collective.types.externalsearch-11.04.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.types.externalsearch/collective.types.externalsearch-11.04.tar.gz
  md5: a97d484eaa4d367cd624ed017d0e1527
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.types.externalsearch = collective.types.externalsearch:main
    #
    # Would create an entry point called collective.types.externalsearch that calls collective.types.externalsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.datagridfield >1.5

  run:
    - python
    - setuptools
    - setuptools
    - products.datagridfield >1.5

test:
  # Python imports
  imports:
    - collective
    - collective.types
    - collective.types.externalsearch
    - collective.types.externalsearch.Extensions
    - collective.types.externalsearch.browser
    - collective.types.externalsearch.content
    - collective.types.externalsearch.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.types.externalsearch/
  license: GPL
  summary: 'External searches in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
