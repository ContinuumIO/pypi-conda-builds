package:
  name: meneco
  version: "1.4.2"

source:
  fn: meneco-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/m/meneco/meneco-1.4.2.tar.gz
  md5: 96aa7bd24ffc4e179f84df3d45979b86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meneco = meneco:main
    #
    # Would create an entry point called meneco that calls meneco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyasp ==1.3.3

  run:
    - python
    - pyasp ==1.3.3

test:
  # Python imports
  imports:
    - __meneco__
    - meneco

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/meneco/
  license: GPLv3+
  summary: 'Metabolic Network Completion. Compute minimal completions to your draft net with reactions from a repair net.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
