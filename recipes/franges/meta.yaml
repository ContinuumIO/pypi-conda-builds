package:
  name: franges
  version: "0.1.0"

source:
  fn: Franges-0.1.0.zip
  url: https://pypi.python.org/packages/source/F/Franges/Franges-0.1.0.zip
  md5: 21d69fb16fc1af663826927b2ab18795
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - franges = franges:main
    #
    # Would create an entry point called franges that calls franges.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - franges

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/franges/
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Franges adds support for floating point and fixed precision (Decimal) range generator functions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
