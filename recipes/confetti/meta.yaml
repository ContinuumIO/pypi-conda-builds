package:
  name: confetti
  version: "2.4.1"

source:
  fn: confetti-2.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/confetti/confetti-2.4.1.tar.gz
  md5: 909f99b50640859dc63912f995ae747a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confetti = confetti:main
    #
    # Would create an entry point called confetti that calls confetti.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sentinels >=0.0.5
    - six

  run:
    - python
    - sentinels >=0.0.5
    - six

test:
  # Python imports
  imports:
    - confetti

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vmalloc/confetti
  license: BSD License
  summary: 'Generic configuration mechanism'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
