package:
  name: django-estate-cms
  version: "0.1.0"

source:
  fn: django-estate-cms-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-estate-cms/django-estate-cms-0.1.0.tar.gz
  md5: 848323c10feca9f041837f8b982145d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-estate-cms = django-estate-cms:main
    #
    # Would create an entry point called django-estate-cms that calls django-estate-cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-estate >=0.1.0
    - django-cms >=2.4.0

  run:
    - python
    - django-estate >=0.1.0
    - django-cms >=2.4.0

test:
  # Python imports
  imports:
    - cmsplugin_houseoverview
    - realestate_cms
    - realestate_cms_contact
    - realestate_cms_houses
    - realestate_cms_subscribe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.de-nieuwbouwmakelaar.nl
  license: BSD License
  summary: 'Estate plugins and apphooks for django cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
