package:
  name: pythonbrain
  version: "0.4.4"

source:
  fn: pythonbrain-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pythonbrain/pythonbrain-0.4.4.tar.gz
  md5: aef87098eae54e06d3013feabb3db171
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythonbrain = pythonbrain:main
    #
    # Would create an entry point called pythonbrain that calls pythonbrain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brain
    - pythonbrain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jrialland/python-brain
  license: Apache License 2.0
  summary: 'lightweight neural network library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
