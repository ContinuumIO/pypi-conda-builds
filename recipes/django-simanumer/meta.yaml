package:
  name: django-simanumer
  version: "0.1.3"

source:
  fn: django-simanumer-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simanumer/django-simanumer-0.1.3.tar.gz
  md5: 3a86d983c250fbce6dd942018b45d351
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simanumer = django-simanumer:main
    #
    # Would create an entry point called django-simanumer that calls django-simanumer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - phonenumber_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bashu/django-simanumer
  license: BSD License
  summary: 'Put short description here...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
