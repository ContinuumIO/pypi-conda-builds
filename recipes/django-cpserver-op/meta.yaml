package:
  name: django-cpserver-op
  version: "1.2.2"

source:
  fn: django-cpserver-op-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cpserver-op/django-cpserver-op-1.2.2.tar.gz
  md5: f54334896736480081df7931e52c76ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cpserver-op = django-cpserver-op:main
    #
    # Would create an entry point called django-cpserver-op that calls django-cpserver-op.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy

  run:
    - python
    - cherrypy

test:
  # Python imports
  imports:
    - django_cpserver
    - django_cpserver.management
    - django_cpserver.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OpenProximity/django-cpserver
  license: BSD License
  summary: "Management commands for serving Django via CherryPy's built-in WSGI server modified for OpenProximity"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
