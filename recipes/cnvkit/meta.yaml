package:
  name: cnvkit
  version: "0.4.1"

source:
  fn: CNVkit-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CNVkit/CNVkit-0.4.1.tar.gz
  md5: 4c4241966a27a28cdf1c3bfd6eeca47b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cnvkit = cnvkit:main
    #
    # Would create an entry point called cnvkit that calls cnvkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6
    - matplotlib >=1.1
    - pysam >=0.8
    - reportlab >=3.0
    - biopython >=1.62

  run:
    - python
    - numpy >=1.6
    - matplotlib >=1.1
    - pysam >=0.8
    - reportlab >=3.0
    - biopython >=1.62

test:
  # Python imports
  imports:
    - cnvkit
    - cnvlib
    - cnvlib.ngfrills
    - cnvlib.segmentation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/etal/cnvkit
  license: Apache Software License
  summary: 'Copy number variation toolkit: Infer CNVs from targeted DNA sequencing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
