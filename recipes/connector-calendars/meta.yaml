package:
  name: connector-calendars
  version: "0.1.0"

source:
  fn: connector-calendars-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/connector-calendars/connector-calendars-0.1.0.tar.gz
  md5: b49eb8a34230a23215b5db37384a8700
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - connector-calendars = connector-calendars:main
    #
    # Would create an entry point called connector-calendars that calls connector-calendars.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - connector-events

  run:
    - python
    - connector-events

test:
  # Python imports
  imports:
    - calendars

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rva-data/connector-calendars
  license: BSD License
  summary: 'Django calendar ingestion app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
