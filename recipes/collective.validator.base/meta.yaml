package:
  name: collective.validator.base
  version: "2.2.1"

source:
  fn: collective.validator.base-2.2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.validator.base/collective.validator.base-2.2.1.zip
  md5: c115d8bdd96ca816eed3d16cf56c5e1b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.validator.base = collective.validator.base:main
    #
    # Would create an entry point called collective.validator.base that calls collective.validator.base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.validator
    - collective.validator.base
    - collective.validator.base.browser
    - collective.validator.base.content
    - collective.validator.base.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.validator.base
  license: GPL
  summary: 'A Plone pages validator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
