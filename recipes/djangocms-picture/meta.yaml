package:
  name: djangocms-picture
  version: "0.1"

source:
  fn: djangocms-picture-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-picture/djangocms-picture-0.1.tar.gz
  md5: 400dd08f0a63f2fc07050846b2ec38a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-picture = djangocms-picture:main
    #
    # Would create an entry point called djangocms-picture that calls djangocms-picture.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_picture
    - djangocms_picture.migrations
    - djangocms_picture.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-picture
  license: BSD License
  summary: 'Picture plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
