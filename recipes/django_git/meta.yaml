package:
  name: django_git
  version: "0.1"

source:
  fn: django-git-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_git/django-git-0.1.1.tar.gz
  md5: 85b181a582a3e1c442ebdbce06b24313
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_git = django_git:main
    #
    # Would create an entry point called django_git that calls django_git.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython >=0.1.4
    - pygments >=0.11

  run:
    - python
    - gitpython >=0.1.4
    - pygments >=0.11

test:
  # Python imports
  imports:
    - django_git

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-git
  license: BSD License
  summary: 'django gitweb replacement'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
