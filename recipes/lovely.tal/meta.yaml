package:
  name: lovely.tal
  version: "0.6.0"

source:
  fn: lovely.tal-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.tal/lovely.tal-0.6.0.tar.gz
  md5: e979e3149dd3648c40681dab36c02f4f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.tal = lovely.tal:main
    #
    # Would create an entry point called lovely.tal that calls lovely.tal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.tales

  run:
    - python
    - setuptools
    - zope.tales

test:
  # Python imports
  imports:
    - lovely
    - lovely.tal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/lovely.tal
  license: Zope Public License
  summary: 'the lovely tal enables new tal expressions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
