package:
  name: python-gnutls
  version: "2.0.1"

source:
  fn: python-gnutls-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gnutls/python-gnutls-2.0.1.tar.gz
  md5: adba310851a15d19ff29355385c6f74f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gnutls = python-gnutls:main
    #
    # Would create an entry point called python-gnutls that calls python-gnutls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gnutls
    - gnutls.interfaces
    - gnutls.interfaces.twisted
    - gnutls.library

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ag-projects.com/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python wrapper for the GNUTLS library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
