package:
  name: sfs_upload
  version: "0.1"

source:
  fn: sfs_upload-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sfs_upload/sfs_upload-0.1.tar.gz
  md5: 29c89911e753af15cf845e0093dd889e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sfs_upload = sfs_upload:main
    #
    # Would create an entry point called sfs_upload that calls sfs_upload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - sfs_upload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spillevink/sfs_upload
  license: GNU GPLv3
  summary: 'Upload files to SFS: https://github.com/the-louie/simple-file-sharer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
