package:
  name: guillotine
  version: "0.1"

source:
  fn: guillotine-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/guillotine/guillotine-0.1.tar.gz
  md5: 61732c2e9f04f45022bbada0c4c2407d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guillotine = guillotine:main
    #
    # Would create an entry point called guillotine that calls guillotine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - guillotine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pylonsproject.org
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'A middleware for proper handling of HEAD requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
