package:
  name: django-locks
  version: "0.0.1"

source:
  fn: django-locks-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-locks/django-locks-0.0.1.tar.gz
  md5: 98f02dab32b2936cb96ce5d67f2edeb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-locks = django-locks:main
    #
    # Would create an entry point called django-locks that calls django-locks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_locks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apn-online/django-locks
  license: MIT
  summary: 'Database and filesystem locks for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
