package:
  name: products.simpleblog
  version: "3.0"

source:
  fn: Products.SimpleBlog-3.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SimpleBlog/Products.SimpleBlog-3.0.tar.gz
  md5: 9524e284be65cd6ece42233b64f461b2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.simpleblog = products.simpleblog:main
    #
    # Would create an entry point called products.simpleblog that calls products.simpleblog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.SimpleBlog
    - Products.SimpleBlog.browser
    - Products.SimpleBlog.content
    - Products.SimpleBlog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ttps://svn.plone.org/svn/collective/SimpleBlog/
  license: GPL
  summary: 'SimpleBlog is an easy to use Plone based weblog application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
