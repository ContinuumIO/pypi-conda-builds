package:
  name: tornado_msgpack
  version: "0.4"

source:
  fn: tornado_msgpack-0.4.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_msgpack/tornado_msgpack-0.4.tar.gz
  md5: 35fc1a5929638a0d1fcb8aabdf058f45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_msgpack = tornado_msgpack:main
    #
    # Would create an entry point called tornado_msgpack that calls tornado_msgpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - msgpack-python
    - tornado

  run:
    - python
    - msgpack-python
    - tornado

test:
  # Python imports
  imports:
    - tornado_msgpack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'ore ore MessagePack RPC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
