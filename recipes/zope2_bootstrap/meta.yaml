package:
  name: zope2_bootstrap
  version: "0.1.0"

source:
  fn: zope2_bootstrap-0.1.0.zip
  url: https://pypi.python.org/packages/source/z/zope2_bootstrap/zope2_bootstrap-0.1.0.zip
  md5: 4f6b171f2f3239c6369c65beacc5a15a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope2_bootstrap = zope2_bootstrap:main
    #
    # Would create an entry point called zope2_bootstrap that calls zope2_bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - zope2_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aclark4life/zope2_bootstrap
  license: GPL
  summary: 'ZMI Bootstrapped'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
