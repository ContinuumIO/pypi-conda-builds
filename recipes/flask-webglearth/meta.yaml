package:
  name: flask-webglearth
  version: "0.1.3"

source:
  fn: Flask-WebGlEarth-0.1.3.zip
  url: https://pypi.python.org/packages/source/F/Flask-WebGlEarth/Flask-WebGlEarth-0.1.3.zip
  md5: 3cd024fa9f573783362bc4c0eca125db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-webglearth = flask-webglearth:main
    #
    # Would create an entry point called flask-webglearth that calls flask-webglearth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_webglearth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nikulesko/Flask-WebGLEarth
  license: MIT License
  summary: 'Simple extension for Flask to use WebGl Earth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
