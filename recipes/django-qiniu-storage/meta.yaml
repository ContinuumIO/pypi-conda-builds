package:
  name: django-qiniu-storage
  version: "2.1.0"

source:
  fn: django-qiniu-storage-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qiniu-storage/django-qiniu-storage-2.1.0.tar.gz
  md5: 976b92dbee4cc7aa5e3b4159e931b3ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qiniu-storage = django-qiniu-storage:main
    #
    # Would create an entry point called django-qiniu-storage that calls django-qiniu-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - qiniu >=7.0.1
    - six
    - requests

  run:
    - python
    - qiniu >=7.0.1
    - six
    - requests

test:
  # Python imports
  imports:
    - qiniustorage
    - qiniustorage.management
    - qiniustorage.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glasslion/django-qiniu-storage
  license: MIT License
  summary: 'Django storage for Qiniu Cloud Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
