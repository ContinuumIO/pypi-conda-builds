package:
  name: flask-cache
  version: "0.13.1"

source:
  fn: Flask-Cache-0.13.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Cache/Flask-Cache-0.13.1.tar.gz
  md5: ab82a9cd0844891ccdb54fbb93fd6c59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-cache = flask-cache:main
    #
    # Would create an entry point called flask-cache that calls flask-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thadeusb/flask-cache
  license: BSD License
  summary: 'Adds cache support to your Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
