package:
  name: cocopy
  version: "1.1.0rc"

source:
  fn: CocoPy-1.1.0rc.zip
  url: https://pypi.python.org/packages/source/C/CocoPy/CocoPy-1.1.0rc.zip
  md5: 102dadfc73371fff99ce6ee65a90d308
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cocopy = cocopy:main
    #
    # Would create an entry point called cocopy that calls cocopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - cocopy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ssw.uni-linz.ac.at/coco
  license: GNU General Public License (GPL)
  summary: 'Python implementation of the famous CoCo/R LL(k) compiler generator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
