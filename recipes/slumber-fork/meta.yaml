package:
  name: slumber-fork
  version: "0.6.3"

source:
  fn: slumber-fork-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/s/slumber-fork/slumber-fork-0.6.3.tar.gz
  md5: 55781bf1f364656cbdddba613a3bc6bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slumber-fork = slumber-fork:main
    #
    # Would create an entry point called slumber-fork that calls slumber-fork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - slumber

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/futurice/slumber
  license: UNKNOWN
  summary: 'Fork of Slumber, A library that makes consuming a REST API easier and more convenient'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
