package:
  name: doapfiend-vcs
  version: "0.1.0"

source:
  fn: doapfiend-vcs-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/doapfiend-vcs/doapfiend-vcs-0.1.0.tar.gz
  md5: 489bc57775a5fd529e0c87ba68edab64
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doapfiend-vcs = doapfiend-vcs:main
    #
    # Would create an entry point called doapfiend-vcs that calls doapfiend-vcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - doapfiend >=0.3.1

  run:
    - python
    - setuptools
    - doapfiend >=0.3.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.doapspace.org/doapfiend/wiki/DfVCS
  license: BSD-2
  summary: 'Doapfiend plugin. Find CVS, SVN, etc. repo and run cmds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
