package:
  name: asyncio_redis
  version: "0.13.4"

source:
  fn: asyncio_redis-0.13.4.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncio_redis/asyncio_redis-0.13.4.tar.gz
  md5: f1c90a8d77471ab9706058f666ea3594
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncio_redis = asyncio_redis:main
    #
    # Would create an entry point called asyncio_redis that calls asyncio_redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - asyncio

  run:
    - python
    - asyncio

test:
  # Python imports
  imports:
    - asyncio_redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jonathanslenders/asyncio-redis
  license: LICENSE.txt
  summary: 'PEP 3156 implementation of the redis protocol.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
