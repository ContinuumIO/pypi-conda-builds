package:
  name: django-premailer
  version: "0.1.0"

source:
  fn: django-premailer-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-premailer/django-premailer-0.1.0.tar.gz
  md5: 67cba3b214d800573978e7f290722f38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-premailer = django-premailer:main
    #
    # Would create an entry point called django-premailer that calls django-premailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - premailer

  run:
    - python
    - premailer

test:
  # Python imports
  imports:
    - django_premailer
    - django_premailer.templatetags
    - django_premailer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alexhayes/django-premailer
  license: MIT License
  summary: 'Django template tag that turns CSS blocks into style attributes using premailer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
