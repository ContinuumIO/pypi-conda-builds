package:
  name: more.chameleon
  version: "0.1"

source:
  fn: more.chameleon-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/more.chameleon/more.chameleon-0.1.tar.gz
  md5: dbee1f80e2e3c8dbe80b2412192a70d6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - more.chameleon = more.chameleon:main
    #
    # Would create an entry point called more.chameleon that calls more.chameleon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - morepath >=0.10
    - chameleon >=2.20

  run:
    - python
    - setuptools
    - morepath >=0.10
    - chameleon >=2.20

test:
  # Python imports
  imports:
    - more
    - more.chameleon
    - more.chameleon.tests
    - more.chameleon.tests.fixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/more.chameleon
  license: BSD
  summary: 'Chameleon template integration for Morepath'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
