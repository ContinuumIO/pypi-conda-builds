package:
  name: collective.demositebadge
  version: "1.0"

source:
  fn: collective.demositebadge-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.demositebadge/collective.demositebadge-1.0.tar.gz
  md5: f6dcd30076e0bc3a41e62f80671bc0ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.demositebadge = collective.demositebadge:main
    #
    # Would create an entry point called collective.demositebadge that calls collective.demositebadge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.demositebadge
    - collective.demositebadge.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/martinschoel/collective.demositebadge
  license: GPL
  summary: 'Provides sticky badge for demo sites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
