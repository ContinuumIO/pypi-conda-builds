package:
  name: linkexchange.turbogears
  version: "0.1"

source:
  fn: LinkExchange.TurboGears-0.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LinkExchange.TurboGears/LinkExchange.TurboGears-0.1.tar.gz
  md5: f35cf8f4e860625da51f50b76265d2bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkexchange.turbogears = linkexchange.turbogears:main
    #
    # Would create an entry point called linkexchange.turbogears that calls linkexchange.turbogears.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - linkexchange >=0.4dev
    - turbogears >=1.0,<2.0

  run:
    - python
    - linkexchange >=0.4dev
    - turbogears >=1.0,<2.0

test:
  # Python imports
  imports:
    - linkexchange
    - linkexchange.turbogears

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://linkexchange.org.ua
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'TurboGears 1.x integration with LinkExchange library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
