package:
  name: libvirt-python
  version: "1.2.16"

source:
  fn: libvirt-python-1.2.16.tar.gz
  url: https://pypi.python.org/packages/source/l/libvirt-python/libvirt-python-1.2.16.tar.gz
  md5: 8c688dee031b5b7f36e22b4a10d26740
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - libvirt-python = libvirt-python:main
    #
    # Would create an entry point called libvirt-python that calls libvirt-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.libvirt.org
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'The libvirt virtualization API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
