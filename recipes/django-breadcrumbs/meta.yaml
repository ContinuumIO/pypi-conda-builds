package:
  name: django-breadcrumbs
  version: "1.1.3-p1"

source:
  fn: django-breadcrumbs-1.1.3-p1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-breadcrumbs/django-breadcrumbs-1.1.3-p1.tar.gz
  md5: 227e9a4ad9e835bfa91716fcf317dfe7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-breadcrumbs = django-breadcrumbs:main
    #
    # Would create an entry point called django-breadcrumbs that calls django-breadcrumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.4

  run:
    - python
    - django >=1.3.4

test:
  # Python imports
  imports:
    - breadcrumbs
    - breadcrumbs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chronossc/django-breadcrumbs
  license: BSD License
  summary: 'Easy to use generic breadcrumbs system for Django framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
