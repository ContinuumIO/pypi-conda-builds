package:
  name: django-blogango
  version: "0.6.2"

source:
  fn: django-blogango-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-blogango/django-blogango-0.6.2.tar.gz
  md5: 6ccfff22b685c262b63874d816db0ccc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-blogango = django-blogango:main
    #
    # Would create an entry point called django-blogango that calls django-blogango.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - blogango
    - blogango/conf
    - blogango/management
    - blogango/management/commands
    - blogango/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.agiliq.com/
  license: BSD License
  summary: 'A django based blog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
