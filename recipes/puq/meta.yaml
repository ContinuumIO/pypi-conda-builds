package:
  name: puq
  version: "2.2.10"

source:
  fn: puq-2.2.10.tar.gz
  url: https://pypi.python.org/packages/source/p/puq/puq-2.2.10.tar.gz
  md5: eea0b0eccd54159e44bcd664d4d4d484
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - puq = puq:main
    #
    # Would create an entry point called puq that calls puq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scipy >=0.11
    - h5py >1.3
    - jsonpickle >=0.4
    - matplotlib >=1.1
    - sympy >=0.7.1
    - poster >=0.8

  run:
    - python
    - scipy >=0.11
    - h5py >1.3
    - jsonpickle >=0.4
    - matplotlib >=1.1
    - sympy >=0.7.1
    - poster >=0.8

test:
  # Python imports
  imports:
    - puq
    - puqutil
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/c-PRIMED/puq
  license: MIT License
  summary: 'PUQ Uncertainty Quantification Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
