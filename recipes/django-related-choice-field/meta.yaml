package:
  name: django-related-choice-field
  version: "0.2.1"

source:
  fn: django-related-choice-field-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-related-choice-field/django-related-choice-field-0.2.1.tar.gz
  md5: 99d12c2750d21d62bc84985ca5ed629b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-related-choice-field = django-related-choice-field:main
    #
    # Would create an entry point called django-related-choice-field that calls django-related-choice-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1

  run:
    - python
    - django >=1.3.1

test:
  # Python imports
  imports:
    - related_choice_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/linovia/django-related-choice-field
  license: BSD License
  summary: 'A ModelChoiceField that restricts its content according to a foreign key contraint.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
