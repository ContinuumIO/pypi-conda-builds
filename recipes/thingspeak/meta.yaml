package:
  name: thingspeak
  version: "0.1.1"

source:
  fn: thingspeak-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/thingspeak/thingspeak-0.1.1.tar.gz
  md5: 54b1458780b3a8f53dd1b525271e569c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thingspeak = thingspeak:main
    #
    # Would create an entry point called thingspeak that calls thingspeak.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - thingspeak

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bergey/thingspeak
  license: UNKNOWN
  summary: 'Client library for the thingspeak.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
