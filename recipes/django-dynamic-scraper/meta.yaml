package:
  name: django-dynamic-scraper
  version: "0.3.11"

source:
  fn: django-dynamic-scraper-0.3.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamic-scraper/django-dynamic-scraper-0.3.11.tar.gz
  md5: fdf3d1d3e9439ecc56c44299b26c3632
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-scraper = django-dynamic-scraper:main
    #
    # Would create an entry point called django-dynamic-scraper that calls django-dynamic-scraper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dynamic_scraper
    - dynamic_scraper.management
    - dynamic_scraper.management.commands
    - dynamic_scraper.migrations
    - dynamic_scraper.spiders
    - dynamic_scraper.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/holgerd77/django-dynamic-scraper/
  license: BSD License
  summary: 'Creating Scrapy scrapers via the Django admin interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
