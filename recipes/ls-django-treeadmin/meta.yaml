package:
  name: ls-django-treeadmin
  version: "0.4.5"

source:
  fn: ls-django-treeadmin-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/l/ls-django-treeadmin/ls-django-treeadmin-0.4.5.tar.gz
  md5: 924a01b6253e3a38444a26e20ad66b55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ls-django-treeadmin = ls-django-treeadmin:main
    #
    # Would create an entry point called ls-django-treeadmin that calls ls-django-treeadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-mptt >=0.5

  run:
    - python
    - django >=1.7
    - django-mptt >=0.5

test:
  # Python imports
  imports:
    - treeadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lanshark/ls-django-treeadmin
  license: BSD License
  summary: 'Tree UI for mptt-managed models, extracted from FeinCMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
