package:
  name: nunchuck_pi
  version: "0.1"

source:
  fn: Nunchuck_pi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/N/Nunchuck_pi/Nunchuck_pi-0.1.tar.gz
  md5: fa788a093addf388f26221b4fc708753
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nunchuck_pi = nunchuck_pi:main
    #
    # Would create an entry point called nunchuck_pi that calls nunchuck_pi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - rpi.gpio >=0.5.4

  run:
    - python
    - rpi.gpio >=0.5.4

test:
  # Python imports
  imports:
    - nunchuck_pi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.boeeerb.co.uk
  license: CC BY-SA 3.0
  summary: 'A module to read a wii nunchuck for Raspberry Pi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
