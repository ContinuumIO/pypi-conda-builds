package:
  name: typepad-motion
  version: "1.1.3"

source:
  fn: typepad-motion-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/t/typepad-motion/typepad-motion-1.1.3.tar.gz
  md5: 83de36d352d7977331d092ab0f854c80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typepad-motion = typepad-motion:main
    #
    # Would create an entry point called typepad-motion that calls typepad-motion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1.1
    - typepadapp ==1.1.3
    - django_flash >=1.5.1

  run:
    - python
    - django >=1.1.1
    - typepadapp ==1.1.3
    - django_flash >=1.5.1

test:
  # Python imports
  imports:
    - motion
    - motion.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sixapart/typepad-motion
  license: BSD License
  summary: 'Community microblogging in the TypePad cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
