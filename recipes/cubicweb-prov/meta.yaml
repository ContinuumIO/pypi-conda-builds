package:
  name: cubicweb-prov
  version: "0.1.0"

source:
  fn: cubicweb-prov-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-prov/cubicweb-prov-0.1.0.tar.gz
  md5: e7260abe19ae282bdd197ea146769217
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-prov = cubicweb-prov:main
    #
    # Would create an entry point called cubicweb-prov that calls cubicweb-prov.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.20.4

  run:
    - python
    - cubicweb >=3.20.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-prov
  license: LGPL
  summary: 'The PROV Ontology'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
