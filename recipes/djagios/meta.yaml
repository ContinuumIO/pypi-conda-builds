package:
  name: djagios
  version: "0.1.2"

source:
  fn: djagios-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djagios/djagios-0.1.2.tar.gz
  md5: 2e91d2866020369456043d56e5f0a54b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djagios = djagios:main
    #
    # Would create an entry point called djagios that calls djagios.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djagios
    - djagios.common
    - djagios.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://djagios.org/
  license: GPL2
  summary: 'A package to help configure nagios written in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
