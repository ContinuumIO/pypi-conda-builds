package:
  name: auf-refer
  version: "1.1.1"

source:
  fn: auf-refer-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/auf-refer/auf-refer-1.1.1.tar.gz
  md5: de31a55943b28ba85070adb04b997c84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auf-refer = auf-refer:main
    #
    # Would create an entry point called auf-refer that calls auf-refer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://git.auf.org/?p=auf-refer.git
  license: GNU Library or Lesser General Public License (LGPL) or GNU General Public License (GPL)
  summary: 'AuF utility to optimize access to reference data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
