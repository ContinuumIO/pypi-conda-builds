package:
  name: ansiblereporter
  version: "1.6.2"

source:
  fn: ansiblereporter-1.6.2.tar.gz
  url: https://pypi.python.org/packages/source/a/ansiblereporter/ansiblereporter-1.6.2.tar.gz
  md5: 017d81da2005ffc78783d6e54cae6882
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansiblereporter = ansiblereporter:main
    #
    # Would create an entry point called ansiblereporter that calls ansiblereporter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansible >=1.8.4
    - systematic >=4.2.6
    - seine >=3.0.0
    - boto
    - termcolor

  run:
    - python
    - ansible >=1.8.4
    - systematic >=4.2.6
    - seine >=3.0.0
    - boto
    - termcolor

test:
  # Python imports
  imports:
    - ansiblereporter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codento/ansiblereporter/
  license: PSF
  summary: 'Scripts for ansible to report host output data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
