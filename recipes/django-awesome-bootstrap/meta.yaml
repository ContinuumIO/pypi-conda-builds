package:
  name: django-awesome-bootstrap
  version: "3.2.0"

source:
  fn: django-awesome-bootstrap-3.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-awesome-bootstrap/django-awesome-bootstrap-3.2.0.tar.gz
  md5: 87f6be7e211a825fe145bef2db4e09bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-awesome-bootstrap = django-awesome-bootstrap:main
    #
    # Would create an entry point called django-awesome-bootstrap that calls django-awesome-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - awesome_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/infoagetech/django-awesome-bootstrap
  license: MIT License
  summary: u'A Django app for including twitter bootstrap and font awesome.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
