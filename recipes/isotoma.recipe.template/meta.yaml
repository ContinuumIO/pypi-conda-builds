package:
  name: isotoma.recipe.template
  version: "0.0.5"

source:
  fn: isotoma.recipe.template-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.template/isotoma.recipe.template-0.0.5.tar.gz
  md5: 237e32bab3b86d6ddd7fd3fec1ac859d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.template = isotoma.recipe.template:main
    #
    # Would create an entry point called isotoma.recipe.template that calls isotoma.recipe.template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - cheetah
    - simplejson

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - cheetah
    - simplejson

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Wrapper around Cheetah + JSON to provide simple buildout templating'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
