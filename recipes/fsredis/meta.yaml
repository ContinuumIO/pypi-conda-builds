package:
  name: fsredis
  version: "0.4"

source:
  fn: fsredis-0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/fsredis/fsredis-0.4.tar.gz
  md5: 410423504440acb42751b5c496ab7870
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fsredis = fsredis:main
    #
    # Would create an entry point called fsredis that calls fsredis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fsredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hpk42/fsredis
  license: MIT License
  summary: 'fsredis: in-process redis api, persisting to file system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
