package:
  name: django-conneg
  version: "0.10.0"

source:
  fn: django-conneg-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-conneg/django-conneg-0.10.0.tar.gz
  md5: d7fb1fdfd70f600bbf6e1e6a04309a15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-conneg = django-conneg:main
    #
    # Would create an entry point called django-conneg that calls django-conneg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_conneg
    - django_conneg.support
    - django_conneg.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ox-it/django-conneg
  license: BSD License
  summary: 'An implementation of content-negotiating class-based views for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
