package:
  name: js.jquery_placeholder
  version: "1.3"

source:
  fn: js.jquery_placeholder-1.3.zip
  url: https://pypi.python.org/packages/source/j/js.jquery_placeholder/js.jquery_placeholder-1.3.zip
  md5: e447717f2d4419bd42797404598a0e17
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_placeholder = js.jquery_placeholder:main
    #
    # Would create an entry point called js.jquery_placeholder that calls js.jquery_placeholder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_placeholder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/js.jquery_placeholder
  license: BSD
  summary: 'Fanstatic packaging of jQuery Tiny Scrollbar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
