package:
  name: m3-libsmev
  version: "0.1.6.4"

source:
  fn: m3-libsmev-0.1.6.4.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-libsmev/m3-libsmev-0.1.6.4.tar.gz
  md5: 1fca13b362a51f0f595f159f28854f2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-libsmev = m3-libsmev:main
    #
    # Would create an entry point called m3-libsmev that calls m3-libsmev.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.1.0

  run:
    - python
    - lxml >=3.1.0

test:
  # Python imports
  imports:
    - libsmev

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Library of low-level helpers that make integration with SMEV somewhat less painful'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
