package:
  name: django-url-methods
  version: "0.2.0"

source:
  fn: django-url-methods-0.2.0.zip
  url: https://pypi.python.org/packages/source/d/django-url-methods/django-url-methods-0.2.0.zip
  md5: ac136886f3dae890c2cc71fbd54ae75d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-methods = django-url-methods:main
    #
    # Would create an entry point called django-url-methods that calls django-url-methods.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urlmethods

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redsolution/django-url-methods
  license: GNU General Public License (GPL)
  summary: 'Django-url-methods provide various methods to work with urls'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
