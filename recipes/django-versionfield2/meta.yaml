package:
  name: django-versionfield2
  version: "0.3.3"

source:
  fn: django-versionfield2-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versionfield2/django-versionfield2-0.3.3.tar.gz
  md5: 5c10628b61fa83f2112b93b6be5de140
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versionfield2 = django-versionfield2:main
    #
    # Would create an entry point called django-versionfield2 that calls django-versionfield2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - versionfield
    - versionfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tonioo/django-versionfield
  license: BSD License
  summary: 'A DB Independent Custom Django Field for storing Version numbers for fast indexing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
