package:
  name: bethel.clustermgmt
  version: "1.0"

source:
  fn: bethel.clustermgmt-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bethel.clustermgmt/bethel.clustermgmt-1.0.tar.gz
  md5: ff6f809bc5bed8112d94cb9733190e7b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bethel.clustermgmt = bethel.clustermgmt:main
    #
    # Would create an entry point called bethel.clustermgmt that calls bethel.clustermgmt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - infrae.rest
    - zeam.form.base
    - zeam.form.silva
    - five.grok
    - zexceptions
    - silva.core.conf
    - silva.core.services
    - products.silva

  run:
    - python
    - setuptools
    - infrae.rest
    - zeam.form.base
    - zeam.form.silva
    - five.grok
    - zexceptions
    - silva.core.conf
    - silva.core.services
    - products.silva

test:
  # Python imports
  imports:
    - bethel
    - bethel.clustermgmt
    - bethel.clustermgmt.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bethel.clustermgmt
  license: GNU General Public License (GPL)
  summary: 'Zope Cluster Management facilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
