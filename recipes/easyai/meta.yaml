package:
  name: easyai
  version: "0.0.0.4"

source:
  fn: easyAI-0.0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/e/easyAI/easyAI-0.0.0.4.tar.gz
  md5: 9ac540e72ffa474693261b7bc17ed797
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyai = easyai:main
    #
    # Would create an entry point called easyai that calls easyai.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - easyAI
    - easyAI.AI
    - easyAI.games
    - easyai

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Easy-to-use game AI algorithms (Negamax etc. )'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
