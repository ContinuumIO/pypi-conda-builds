package:
  name: binkeul
  version: "0.1.1"

source:
  fn: binkeul-0.1.1.1410021412218102.tar.gz
  url: https://pypi.python.org/packages/source/B/BinKeul/binkeul-0.1.1.1410021412218102.tar.gz
  md5: 55eeba5e74e717d2365aa1261d71cc87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - binkeul = binkeul:main
    #
    # Would create an entry point called binkeul that calls binkeul.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow >=2.3.0

  run:
    - python
    - pillow >=2.3.0

test:
  # Python imports
  imports:
    - binkeul

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/sinabilo/binkeul
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A Python library to support Binkeul (construct language)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
