package:
  name: authzpolicyplugin
  version: "0.1"

source:
  fn: AuthzPolicyPlugin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/A/AuthzPolicyPlugin/AuthzPolicyPlugin-0.1.tar.gz
  md5: 03b3e52232d626e91b824bd92b84e8df
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authzpolicyplugin = authzpolicyplugin:main
    #
    # Would create an entry point called authzpolicyplugin that calls authzpolicyplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - authzpolicy
    - authzpolicyplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mitar.tnode.com/
  license: GPLv3
  summary: "Trac's authz_policy with some improvements."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
