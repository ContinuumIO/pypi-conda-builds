package:
  name: flask-makotemplates
  version: "0.2"

source:
  fn: Flask-MakoTemplates-0.2.zip
  url: https://pypi.python.org/packages/source/F/Flask-MakoTemplates/Flask-MakoTemplates-0.2.zip
  md5: 495153737df0787a879ef77c9183b919
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-makotemplates = flask-makotemplates:main
    #
    # Would create an entry point called flask-makotemplates that calls flask-makotemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - mako

  run:
    - python
    - flask
    - mako

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benselme/flask-makotemplates
  license: BSD License
  summary: 'Mako templates support for Flask applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
