package:
  name: funny-codes
  version: "1.0.1"

source:
  fn: funny-codes-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/funny-codes/funny-codes-1.0.1.tar.gz
  md5: 3c9a3f690df122e5d04ced24880d6ce9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - funny-codes = funny-codes:main
    #
    # Would create an entry point called funny-codes that calls funny-codes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - funny_codes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kmike/funny-codes/
  license: MIT License
  summary: 'Generate randoms strings of a given pattern'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
