package:
  name: django-latex
  version: "0.1"

source:
  fn: django-latex-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-latex/django-latex-0.1.tar.gz
  md5: c12038a7f3501729f287c295ae145cf4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-latex = django-latex:main
    #
    # Would create an entry point called django-latex that calls django-latex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - latex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/bearstech/django-latex
  license: GPL v3
  summary: 'Django application to generate latex/pdf files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
