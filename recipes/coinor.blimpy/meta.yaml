package:
  name: coinor.blimpy
  version: "1.3.1"

source:
  fn: coinor.blimpy-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coinor.blimpy/coinor.blimpy-1.3.1.tar.gz
  md5: ea07a3674217da7a49b0eaa4b99d37a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinor.blimpy = coinor.blimpy:main
    #
    # Would create an entry point called coinor.blimpy that calls coinor.blimpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coinor
    - coinor.blimpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.coin-or.org/CoinBazaar/wiki/Projects/GIMPy
  license: Eclipse Public License
  summary: 'Basic List Implementation in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
