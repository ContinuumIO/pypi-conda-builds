package:
  name: django-response-helpers
  version: "0.1.6"

source:
  fn: django-response-helpers-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-response-helpers/django-response-helpers-0.1.6.tar.gz
  md5: 7c9f07140ee1c4b1b0b070783df68504
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-response-helpers = django-response-helpers:main
    #
    # Would create an entry point called django-response-helpers that calls django-response-helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - response_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imtapps/django-response-helpers
  license: BSD License
  summary: 'A helper application for working with Django Responses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
