package:
  name: django-viewtester
  version: "10.4.3"

source:
  fn: django-viewtester-10.4.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewtester/django-viewtester-10.4.3.tar.gz
  md5: 88097a65a55c032d3cd99fda5f800fe6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewtester = django-viewtester:main
    #
    # Would create an entry point called django-viewtester that calls django-viewtester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - viewtester
    - viewtester.management
    - viewtester.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/pterk/django-viewtester/
  license: BSD License
  summary: 'A django views-tester'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
