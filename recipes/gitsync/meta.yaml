package:
  name: gitsync
  version: "0.2"

source:
  fn: GitSync-0.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GitSync/GitSync-0.2.tar.gz
  md5: 901daebecd8ec3a6eccb89d9f88c46a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitsync = gitsync:main
    #
    # Would create an entry point called gitsync that calls gitsync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitsynclib >=0.1.3
    - fabric >=1.8.0
    - macfsevents >=0.3
    - pyyaml >=3.10
    - pync >=1.6.1

  run:
    - python
    - gitsynclib >=0.1.3
    - fabric >=1.8.0
    - macfsevents >=0.3
    - pyyaml >=3.10
    - pync >=1.6.1

test:
  # Python imports
  imports:
    - gitsync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clockwork/GitSync
  license: MIT License
  summary: 'Use git to sync a project directory on an OS X client with a remote server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
