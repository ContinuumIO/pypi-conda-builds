package:
  name: django-rq
  version: "0.8.0"

source:
  fn: django-rq-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rq/django-rq-0.8.0.tar.gz
  md5: e0c93057a401c7df4070f8ffd84f7f05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rq = django-rq:main
    #
    # Would create an entry point called django-rq that calls django-rq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - rq >=0.5.0

  run:
    - python
    - django
    - rq >=0.5.0

test:
  # Python imports
  imports:
    - django_rq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ui/django-rq
  license: MIT License
  summary: 'A simple app that provides django integration for RQ (Redis Queue)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
