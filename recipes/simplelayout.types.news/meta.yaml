package:
  name: simplelayout.types.news
  version: "1.2.3"

source:
  fn: simplelayout.types.news-1.2.3.zip
  url: https://pypi.python.org/packages/source/s/simplelayout.types.news/simplelayout.types.news-1.2.3.zip
  md5: a34be04859a69c31634314cf3ff4a207
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplelayout.types.news = simplelayout.types.news:main
    #
    # Would create an entry point called simplelayout.types.news that calls simplelayout.types.news.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplelayout.types.common
    - plone.formwidget.contenttree

  run:
    - python
    - setuptools
    - setuptools
    - simplelayout.types.common
    - plone.formwidget.contenttree

test:
  # Python imports
  imports:
    - simplelayout
    - simplelayout.types
    - simplelayout.types.news
    - simplelayout.types.news.browser
    - simplelayout.types.news.portlets
    - simplelayout.types.news.tests
    - simplelayout.types.news.upgrades
    - simplelayout.types.news.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/simplelayout.types.news
  license: GPL2
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
