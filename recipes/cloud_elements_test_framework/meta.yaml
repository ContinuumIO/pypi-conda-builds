package:
  name: cloud_elements_test_framework
  version: "0.0.9"

source:
  fn: cloud_elements_test_framework-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/cloud_elements_test_framework/cloud_elements_test_framework-0.0.9.tar.gz
  md5: 07911571ba12dd0d834a729137b7c560
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloud_elements_test_framework = cloud_elements_test_framework:main
    #
    # Would create an entry point called cloud_elements_test_framework that calls cloud_elements_test_framework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - cloud_elements

  run:
    - python
    - argparse
    - cloud_elements

test:
  # Python imports
  imports:
    - cloud_elements_test_framework

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloud-elements/elements-test-framework
  license: UNKNOWN
  summary: 'Cloud Elements Testing Framework for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
