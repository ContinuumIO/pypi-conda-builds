package:
  name: ukpostcode
  version: "1.0"

source:
  fn: UKpostcode-1.0.zip
  url: https://pypi.python.org/packages/source/U/UKpostcode/UKpostcode-1.0.zip
  md5: 3545d43040927733e6069d5c880b27da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ukpostcode = ukpostcode:main
    #
    # Would create an entry point called ukpostcode that calls ukpostcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ukpostcode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/UKpostcode
  license: MIT
  summary: 'For getting information on UK postcodes from the API at uk-postcodes.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
