package:
  name: herokufu
  version: "0.0.3"

source:
  fn: herokufu-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/herokufu/herokufu-0.0.3.tar.gz
  md5: 296bb66018462d5e75959a1dd446efa3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - herokufu = herokufu:main
    #
    # Would create an entry point called herokufu that calls herokufu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - herokufu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/herokufu
  license: LICENSE.txt
  summary: 'Utilities to run Django & Mezzanine on Heroku'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
