package:
  name: plonehrm.personaldata
  version: "2.0.1"

source:
  fn: plonehrm.personaldata-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plonehrm.personaldata/plonehrm.personaldata-2.0.1.tar.gz
  md5: 57bf6f37410f951e9baf1d4e99279e5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonehrm.personaldata = plonehrm.personaldata:main
    #
    # Would create an entry point called plonehrm.personaldata that calls plonehrm.personaldata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

test:
  # Python imports
  imports:
    - plonehrm
    - plonehrm.personaldata
    - plonehrm.personaldata.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zestsoftware.nl
  license: GPL
  summary: 'Personal data for Plone HRM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
