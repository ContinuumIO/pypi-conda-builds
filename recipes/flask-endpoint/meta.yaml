package:
  name: flask-endpoint
  version: "0.1"

source:
  fn: flask-endpoint-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-endpoint/flask-endpoint-0.1.tar.gz
  md5: 7b0a421a6b6fc586f621910bf3c5a93b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-endpoint = flask-endpoint:main
    #
    # Would create an entry point called flask-endpoint that calls flask-endpoint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7

  run:
    - python
    - flask >=0.7

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rhyselsmore/flask-endpoint
  license: Flask-Enterprise
  summary: 'Endpoint based routing for Flask. Simple.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
