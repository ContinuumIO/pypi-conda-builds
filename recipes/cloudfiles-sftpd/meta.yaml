package:
  name: cloudfiles-sftpd
  version: "0.1.2"

source:
  fn: cloudfiles-sftpd-0.1.2.tar.gz
  url: http://launchpad.net/cloudfiles-sftpd/dev/cloudfiles-sftpd-0.1.2/+download/cloudfiles-sftpd-0.1.2.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudfiles-sftpd = cloudfiles-sftpd:main
    #
    # Would create an entry point called cloudfiles-sftpd that calls cloudfiles-sftpd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cloudfiles_sftpd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/cloudfiles-sftpd
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
