package:
  name: pyrobotics
  version: "1.7.6"

source:
  fn: pyRobotics-1.7.6.tar.gz
  url: https://pypi.python.org/packages/source/p/pyRobotics/pyRobotics-1.7.6.tar.gz
  md5: 76f50e16a9d61c203b0e7703149e3fa6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyrobotics = pyrobotics:main
    #
    # Would create an entry point called pyrobotics that calls pyrobotics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyrobotics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bioroboticsunam.github.io/pyRobotics
  license: LICENSE.txt
  summary: "A Python API to create modules that connect to our message-passing and shared varaibels hub 'BlackBoard'."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
