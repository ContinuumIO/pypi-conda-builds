package:
  name: git-play
  version: "0.13"

source:
  fn: git-play-0.13.tar.gz
  url: https://pypi.python.org/packages/source/g/git-play/git-play-0.13.tar.gz
  md5: fdd055d5805b5c23b8ab7f47218f14ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-play = git-play:main
    #
    # Would create an entry point called git-play that calls git-play.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyyaml ==3.10
    - gitpython ==0.3.2.RC1

  run:
    - python
    - pyyaml ==3.10
    - gitpython ==0.3.2.RC1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stewartpark/git-play
  license: MIT LICENSE
  summary: 'Git-play is a custom git command for deploying an application server very easily from a remote git repository. It checks the remote git repository every minute and if something has changed, it will restart the application server automatically.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
