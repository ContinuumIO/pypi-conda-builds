package:
  name: ofblockmeshdicthelper
  version: "0.1.5"

source:
  fn: ofblockmeshdicthelper-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/o/ofblockmeshdicthelper/ofblockmeshdicthelper-0.1.5.tar.gz
  md5: a3f39ec38906ca307873eb144b652466
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ofblockmeshdicthelper = ofblockmeshdicthelper:main
    #
    # Would create an entry point called ofblockmeshdicthelper that calls ofblockmeshdicthelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ofblockmeshdicthelper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/takaakiaoki/ofblockmeshdicthelper
  license: MIT License
  summary: 'Helper utilities for OpenFOAM blockMeshDict generation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
