package:
  name: chain-sdk
  version: "1.1"

source:
  fn: chain-sdk-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chain-sdk/chain-sdk-1.1.tar.gz
  md5: 7caad3c675d6a3629bca880ead3f71e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chain-sdk = chain-sdk:main
    #
    # Would create an entry point called chain-sdk that calls chain-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.3
    - python-bitcoinlib >=0.2.1
    - six >=1.5.2

  run:
    - python
    - requests >=2.4.3
    - python-bitcoinlib >=0.2.1
    - six >=1.5.2

test:
  # Python imports
  imports:
    - chain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chain-engineering/chain-python
  license: MIT License
  summary: 'Official Python software development kit for the Chain.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
