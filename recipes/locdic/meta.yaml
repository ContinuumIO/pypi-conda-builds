package:
  name: locdic
  version: "0.2.2"

source:
  fn: locdic-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/l/locdic/locdic-0.2.2.tar.gz
  md5: db3c279ae2f5d5262ec74f42a6a63723
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - locdic = locdic:main
    #
    # Would create an entry point called locdic that calls locdic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - bottle >=0.9.5

  run:
    - python
    - bottle >=0.9.5

test:
  # Python imports
  imports:
    - locdic
    - locdic.engine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.remics.org/locdic/
  license: BSD License or MIT License
  summary: 'Off-line Dictionary / Dictionary Server Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
