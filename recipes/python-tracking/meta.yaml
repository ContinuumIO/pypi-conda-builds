package:
  name: python-tracking
  version: "1.0.0"

source:
  fn: python-tracking-1.0.0.zip
  url: https://pypi.python.org/packages/source/p/python-tracking/python-tracking-1.0.0.zip
  md5: feaa382c446555779bb2f85c991f810a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-tracking = python-tracking:main
    #
    # Would create an entry point called python-tracking that calls python-tracking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drgarcia1986/python-tracking
  license: UNKNOWN
  summary: 'python module for tracking mail packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
