package:
  name: linux-system-administration
  version: "0.0.1"

source:
  fn: linux-system-administration-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/linux-system-administration/linux-system-administration-0.0.1.tar.gz
  md5: ce922ab7cc0104624c9b9dc3d261397f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linux-system-administration = linux-system-administration:main
    #
    # Would create an entry point called linux-system-administration that calls linux-system-administration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/TechChristoph
  license: UNKNOWN
  summary: 'Installation and Update-Files for Ubuntu'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
