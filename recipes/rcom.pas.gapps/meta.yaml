package:
  name: rcom.pas.gapps
  version: "1.0dev-r598"

source:
  fn: rcom.pas.gapps-1.0dev-r598.tar.gz
  url: https://pypi.python.org/packages/source/r/rcom.pas.gapps/rcom.pas.gapps-1.0dev-r598.tar.gz
  md5: 4096696e438b33be875650215e627301
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rcom.pas.gapps = rcom.pas.gapps:main
    #
    # Would create an entry point called rcom.pas.gapps that calls rcom.pas.gapps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - rcom
    - rcom.pas
    - rcom.pas.gapps
    - rcom.pas.gapps.browser
    - rcom.pas.gapps.plugins
    - rcom.pas.gapps.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.rcom.com.ar/
  license: GNU General Public License (GPL)
  summary: 'PAS plugin for GoogleApps domains.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
