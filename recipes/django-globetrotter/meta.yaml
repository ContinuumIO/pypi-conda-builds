package:
  name: django-globetrotter
  version: "0.0.5"

source:
  fn: django-globetrotter-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-globetrotter/django-globetrotter-0.0.5.tar.gz
  md5: 75e900e0b803afe603955c72c8c4fedf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-globetrotter = django-globetrotter:main
    #
    # Would create an entry point called django-globetrotter that calls django-globetrotter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - globetrotter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jarcoal/django-globetrotter
  license: BSD
  summary: 'Extra i18n + l10n tools for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
