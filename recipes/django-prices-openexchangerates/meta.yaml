package:
  name: django-prices-openexchangerates
  version: "0.1.6"

source:
  fn: django-prices-openexchangerates-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-prices-openexchangerates/django-prices-openexchangerates-0.1.6.tar.gz
  md5: 2524704865ef40ee501f717700adcb71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-prices-openexchangerates = django-prices-openexchangerates:main
    #
    # Would create an entry point called django-prices-openexchangerates that calls django-prices-openexchangerates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-prices >=0.3.4
    - prices >=0.5.2

  run:
    - python
    - django >=1.4
    - django-prices >=0.3.4
    - prices >=0.5.2

test:
  # Python imports
  imports:
    - django_prices_openexchangerates
    - django_prices_openexchangerates.management
    - django_prices_openexchangerates.management.commands
    - django_prices_openexchangerates.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mirumee/django-prices-openexchanerates
  license: BSD License
  summary: 'openexchangerates.org support for django-prices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
