package:
  name: gocept.pseudonymize
  version: "0.4.1"

source:
  fn: gocept.pseudonymize-0.4.1.zip
  url: https://pypi.python.org/packages/source/g/gocept.pseudonymize/gocept.pseudonymize-0.4.1.zip
  md5: 65abb93196511dd4b6d3f6349dd1dfe1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.pseudonymize = gocept.pseudonymize:main
    #
    # Would create an entry point called gocept.pseudonymize that calls gocept.pseudonymize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.pseudonymize
    - gocept.pseudonymize.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/gocept.pseudonymize/
  license: Zope Public License
  summary: 'Pseudonymize data like text, email addresses or license tags.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
