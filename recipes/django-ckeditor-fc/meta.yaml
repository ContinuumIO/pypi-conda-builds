package:
  name: django-ckeditor-fc
  version: "1.1"

source:
  fn: django-ckeditor-fc-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ckeditor-fc/django-ckeditor-fc-1.1.tar.gz
  md5: b31c8466bd362c04302e41775e3c82ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ckeditor-fc = django-ckeditor-fc:main
    #
    # Would create an entry point called django-ckeditor-fc that calls django-ckeditor-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - pytils

  run:
    - python
    - pillow
    - pytils

test:
  # Python imports
  imports:
    - ckeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-ckeditor
  license: BSD License
  summary: 'Django admin CKEditor integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
