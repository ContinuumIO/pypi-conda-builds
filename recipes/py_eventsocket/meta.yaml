package:
  name: py_eventsocket
  version: "0.1.5"

source:
  fn: py_eventsocket-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/py_eventsocket/py_eventsocket-0.1.5.tar.gz
  md5: cb6f0c2c72c73887bf42aa7a2c308370
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py_eventsocket = py_eventsocket:main
    #
    # Would create an entry point called py_eventsocket that calls py_eventsocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - event-agora ==0.4.1

  run:
    - python
    - event-agora ==0.4.1

test:
  # Python imports
  imports:
    - py_eventsocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agoragames/py-eventsocket
  license: BSD License
  summary: 'Easy to use TCP socket based on libevent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
