package:
  name: django-chunkator
  version: "0.0.4"

source:
  fn: django-chunkator-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chunkator/django-chunkator-0.0.4.tar.gz
  md5: cd010fddba8eae778ffcdffd27322ca8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chunkator = django-chunkator:main
    #
    # Would create an entry point called django-chunkator that calls django-chunkator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chunkator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/novafloss/django-chunkator
  license: MIT License
  summary: 'Chunk large querysetsinto small chunks, and iterate over them without killing your RAM.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
