package:
  name: birkhoff
  version: "0.0.4"

source:
  fn: birkhoff-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/birkhoff/birkhoff-0.0.4.tar.gz
  md5: 8c2fc39160fb9544a8d1279e02b27447
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - birkhoff = birkhoff:main
    #
    # Would create an entry point called birkhoff that calls birkhoff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - networkx

  run:
    - python
    - numpy
    - networkx

test:
  # Python imports
  imports:
    - birkhoff

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jfinkels/birkhoff
  license: GNU GPLv3+
  summary: 'Birkhoff--von Neumann decomposition for doubly stochastic matrices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
