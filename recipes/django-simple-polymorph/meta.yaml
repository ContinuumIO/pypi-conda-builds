package:
  name: django-simple-polymorph
  version: "0.4"

source:
  fn: django-simple-polymorph-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-polymorph/django-simple-polymorph-0.4.tar.gz
  md5: 7c39a14f38f97314c51f34cacda7516f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-polymorph = django-simple-polymorph:main
    #
    # Would create an entry point called django-simple-polymorph that calls django-simple-polymorph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simple_polymorph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/freakypie/django-simple-polymorph
  license: UNKNOWN
  summary: 'A simple utility to polymorph inherited models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
