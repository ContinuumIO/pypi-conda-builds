package:
  name: ftw.calendarwidget
  version: "1.1.11"

source:
  fn: ftw.calendarwidget-1.1.11.zip
  url: https://pypi.python.org/packages/source/f/ftw.calendarwidget/ftw.calendarwidget-1.1.11.zip
  md5: 2b8268fd913555543c749f494d669bd8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.calendarwidget = ftw.calendarwidget:main
    #
    # Would create an entry point called ftw.calendarwidget that calls ftw.calendarwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui
    - products.archetypes

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui
    - products.archetypes

test:
  # Python imports
  imports:
    - ftw
    - ftw.calendarwidget
    - ftw.calendarwidget.browser
    - ftw.calendarwidget.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.calendarwidget
  license: GNU General Public License (GPL)
  summary: 'A plone widget for archetypes using the jQuery UI calendar widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
