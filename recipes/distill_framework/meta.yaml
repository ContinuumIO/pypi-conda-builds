package:
  name: distill_framework
  version: "0.1.11"

source:
  fn: distill_framework-0.1.11.zip
  url: https://pypi.python.org/packages/source/d/distill_framework/distill_framework-0.1.11.zip
  md5: ec430724a52323e494dbbaf79f710982
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distill_framework = distill_framework:main
    #
    # Would create an entry point called distill_framework that calls distill_framework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mako
    - routes

  run:
    - python
    - mako
    - routes

test:
  # Python imports
  imports:
    - distill
    - distill_framework

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Dreae/Distill
  license: MIT License
  summary: 'Just another python web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
