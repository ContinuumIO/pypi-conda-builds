package:
  name: defensio
  version: "0.9.1"

source:
  fn: defensio-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/defensio/defensio-0.9.1.tar.gz
  md5: 0ec3bde9483136e66479e16a1a2a1651
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - defensio = defensio:main
    #
    # Would create an entry point called defensio that calls defensio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - defensio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/defensio/defensio-python
  license: MIT License
  summary: 'Official Python library for Defensio 2.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
