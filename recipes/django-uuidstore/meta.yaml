package:
  name: django-uuidstore
  version: "0.1.0"

source:
  fn: django-uuidstore-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uuidstore/django-uuidstore-0.1.0.tar.gz
  md5: 7fc33d7c13545256d5a5c412f9c2ae9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uuidstore = django-uuidstore:main
    #
    # Would create an entry point called django-uuidstore that calls django-uuidstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-extensions
    - django >=1.4.2

  run:
    - python
    - django-extensions
    - django >=1.4.2

test:
  # Python imports
  imports:
    - uuidstore
    - uuidstore.migrations
    - uuidstore.south_migrations
    - uuidstore.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mhurt/django-uuidstore
  license: MIT License
  summary: 'Site-wide UUIDs for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
