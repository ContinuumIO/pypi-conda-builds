package:
  name: django-adminstat
  version: "0.1.0"

source:
  fn: django-adminstat-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adminstat/django-adminstat-0.1.0.tar.gz
  md5: 49d7e2106e9f62b21db8111716e86fce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adminstat = django-adminstat:main
    #
    # Would create an entry point called django-adminstat that calls django-adminstat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - adminstat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: "Simple app, provides django's LogEntry objects admin. It can be used to collect inforabout staff users actions on site"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
