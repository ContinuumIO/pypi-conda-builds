package:
  name: django-wiretap
  version: "0.1.2"

source:
  fn: django-wiretap-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wiretap/django-wiretap-0.1.2.tar.gz
  md5: 31c37be45fda330e4d0303aa67ee9737
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wiretap = django-wiretap:main
    #
    # Would create an entry point called django-wiretap that calls django-wiretap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5,<1.8
    - django-roma
    - six

  run:
    - python
    - django >=1.5,<1.8
    - django-roma
    - six

test:
  # Python imports
  imports:
    - wiretap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nathforge/django-wiretap
  license: UNKNOWN
  summary: 'https://github.com/nathforge/django-wiretap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
