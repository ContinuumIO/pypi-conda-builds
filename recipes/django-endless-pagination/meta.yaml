package:
  name: django-endless-pagination
  version: "2.0"

source:
  fn: django-endless-pagination-2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-endless-pagination/django-endless-pagination-2.0.tar.gz
  md5: 8efa40cee51ebe229ce15ed258594ba0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-endless-pagination = django-endless-pagination:main
    #
    # Would create an entry point called django-endless-pagination that calls django-endless-pagination.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - endless_pagination
    - endless_pagination.templatetags
    - endless_pagination.tests
    - endless_pagination.tests.integration
    - endless_pagination.tests.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frankban/django-endless-pagination
  license: MIT License
  summary: u'Django pagination tools supporting Ajax, multiple and lazy pagination,\nTwitter-style and Digg-style pagination.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
