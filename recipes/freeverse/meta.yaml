package:
  name: freeverse
  version: "0.0.0"

source:
  fn: freeverse-0.0.0.zip
  url: https://pypi.python.org/packages/source/f/freeverse/freeverse-0.0.0.zip
  md5: 1a52c4f596e5330a57be4480ad9d7f50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - freeverse = freeverse:main
    #
    # Would create an entry point called freeverse that calls freeverse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - freeverse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kazark/pyfreeverse
  license: MIT
  summary: 'A new way to write free-form, minimalistic specs (tests) in Python that favor natural language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
