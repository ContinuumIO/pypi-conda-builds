package:
  name: django-model-render
  version: "0.3"

source:
  fn: django-model-render-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-render/django-model-render-0.3.tar.gz
  md5: 23bda8f044f4adcb28a5cc109b07d097
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-render = django-model-render:main
    #
    # Would create an entry point called django-model-render that calls django-model-render.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - model_render

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/a1fred/django-model-render
  license: MIT
  summary: 'Django models extension that allows define default model templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
