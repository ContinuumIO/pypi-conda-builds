package:
  name: versile-python
  version: "0.8.1-beta"

source:
  fn: versile-python-0.8.1-beta.tar.gz
  url: https://pypi.python.org/packages/source/v/versile-python/versile-python-0.8.1-beta.tar.gz
  md5: 07836b671660370c90fec79c26925ee0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - versile-python = versile-python:main
    #
    # Would create an entry point called versile-python that calls versile-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - versile
    - versile.common
    - versile.crypto
    - versile.crypto.algorithm
    - versile.crypto.x509
    - versile.crypto.x509.asn1def
    - versile.manager
    - versile.manager.dispatch
    - versile.orb
    - versile.reactor
    - versile.reactor.io
    - versile.temp
    - versile.vse
    - versile.vse.native

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://versile.com/
  license: GNU Affero General Public License v3
  summary: 'Versile Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
