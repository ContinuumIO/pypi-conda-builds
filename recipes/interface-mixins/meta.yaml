package:
  name: interface-mixins
  version: "0.0.3"

source:
  fn: interface-mixins-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/interface-mixins/interface-mixins-0.0.3.tar.gz
  md5: 427b456755902e3b34b024677f9e8ed6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - interface-mixins = interface-mixins:main
    #
    # Would create an entry point called interface-mixins that calls interface-mixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - interface_mixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/zeckalpha/interface-mixins
  license: MIT License
  summary: 'Interfaces for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
