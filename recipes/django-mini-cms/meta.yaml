package:
  name: django-mini-cms
  version: "0.0.3"

source:
  fn: django-mini-cms-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mini-cms/django-mini-cms-0.0.3.tar.gz
  md5: 62f54066910cfbc38c14fc043707a962
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mini-cms = django-mini-cms:main
    #
    # Would create an entry point called django-mini-cms that calls django-mini-cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - cms
    - cms.migrations
    - cms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/noxan/django-mini-cms
  license: BSD License
  summary: 'Minimalistic django cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
