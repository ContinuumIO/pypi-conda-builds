package:
  name: kcbo
  version: "0.0.1"

source:
  fn: kcbo-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kcbo/kcbo-0.0.1.tar.gz
  md5: 56ea78d9076ca199f72c0dcb14cae63b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kcbo = kcbo:main
    #
    # Would create an entry point called kcbo that calls kcbo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8
    - scipy
    - pymc
    - pandas >=0.14
    - tabulate

  run:
    - python
    - numpy >=1.8
    - scipy
    - pymc
    - pandas >=0.14
    - tabulate

test:
  # Python imports
  imports:
    - kcbo
    - kcbo.statistical_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HHammond/kcbo
  license: MIT License
  summary: 'Bayesian data analysis library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
