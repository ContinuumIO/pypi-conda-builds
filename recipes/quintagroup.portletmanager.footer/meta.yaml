package:
  name: quintagroup.portletmanager.footer
  version: "0.1"

source:
  fn: quintagroup.portletmanager.footer-0.1.tar.gz
  url: https://pypi.python.org/packages/source/q/quintagroup.portletmanager.footer/quintagroup.portletmanager.footer-0.1.tar.gz
  md5: b0404a919859a21bc68f42bfceb8eb07
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.portletmanager.footer = quintagroup.portletmanager.footer:main
    #
    # Would create an entry point called quintagroup.portletmanager.footer that calls quintagroup.portletmanager.footer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.portletmanager
    - quintagroup.portletmanager.footer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.quintagroup.com/products/quintagroup.portletmanager.footer/trunk
  license: GPL
  summary: 'Portlet manager that is rendered in page footer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
