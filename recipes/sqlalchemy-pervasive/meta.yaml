package:
  name: sqlalchemy-pervasive
  version: "0.1.2"

source:
  fn: sqlalchemy-pervasive-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-pervasive/sqlalchemy-pervasive-0.1.2.tar.gz
  md5: 49a6fc0560b461f84ee5b92fdaebe21f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-pervasive = sqlalchemy-pervasive:main
    #
    # Would create an entry point called sqlalchemy-pervasive that calls sqlalchemy-pervasive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyodbc
    - sqlalchemy

  run:
    - python
    - setuptools
    - pyodbc
    - sqlalchemy

test:
  # Python imports
  imports:
    - sqlalchemy_pervasive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'SQLAlchemy Dialect for Pervasive PSQL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
