package:
  name: django-polymodels
  version: "1.2.2"

source:
  fn: django-polymodels-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-polymodels/django-polymodels-1.2.2.tar.gz
  md5: 16aea9aecbc17497d54de497ee74e113
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-polymodels = django-polymodels:main
    #
    # Would create an entry point called django-polymodels that calls django-polymodels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - polymodels
    - polymodels.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charettes/django-polymodels
  license: MIT License
  summary: 'Polymorphic models implementation for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
