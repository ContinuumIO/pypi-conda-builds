package:
  name: redomino.appytransforms
  version: "0.1"

source:
  fn: redomino.appytransforms-0.1.zip
  url: https://pypi.python.org/packages/source/r/redomino.appytransforms/redomino.appytransforms-0.1.zip
  md5: 6d38a3dfb853bf4de1c366cc5fb61765
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.appytransforms = redomino.appytransforms:main
    #
    # Would create an entry point called redomino.appytransforms that calls redomino.appytransforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - appy

  run:
    - python
    - setuptools
    - setuptools
    - appy

test:
  # Python imports
  imports:
    - redomino
    - redomino.appytransforms
    - redomino.appytransforms.tests
    - redomino.appytransforms.transforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.appytransforms
  license: GNU General Public License (GPL)
  summary: 'ODT/ODS templating transforms for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
