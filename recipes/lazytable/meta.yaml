package:
  name: lazytable
  version: "0.3.2"

source:
  fn: lazytable-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lazytable/lazytable-0.3.2.tar.gz
  md5: 663bc44511943511b70edbc251af36a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazytable = lazytable:main
    #
    # Would create an entry point called lazytable that calls lazytable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lazytable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/samuelinsf/lazytable
  license: Software license AGPL version 3.
  summary: 'A basic sqlite table wrapper for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
