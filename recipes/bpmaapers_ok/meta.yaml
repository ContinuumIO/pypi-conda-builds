package:
  name: bpmaapers_ok
  version: "0.1"

source:
  fn: bpmaapers_ok-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bpmaapers_ok/bpmaapers_ok-0.1.tar.gz
  md5: 27113039d597442ed2c683b0e6fbc3d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bpmaapers_ok = bpmaapers_ok:main
    #
    # Would create an entry point called bpmaapers_ok that calls bpmaapers_ok.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bpmaapers_ok
    - good

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.wengchichi.com
  license: UNKNOWN
  summary: 'Models to dictionary mapping for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
