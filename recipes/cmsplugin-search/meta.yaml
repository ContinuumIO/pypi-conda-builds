package:
  name: cmsplugin-search
  version: "0.7.3"

source:
  fn: cmsplugin-search-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-search/cmsplugin-search-0.7.3.tar.gz
  md5: 608558661faa38be7801e8b1d74a9346
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-search = cmsplugin-search:main
    #
    # Would create an entry point called cmsplugin-search that calls cmsplugin-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.4
    - django-classy-tags >=0.3.2
    - django-haystack >=2.0

  run:
    - python
    - setuptools
    - django-cms >=2.4
    - django-classy-tags >=0.3.2
    - django-haystack >=2.0

test:
  # Python imports
  imports:
    - cmsplugin_search
    - cmsplugin_search.search_helpers
    - cmsplugin_search.search_helpers.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: lp:~doctormo/+junk/cmsplugin_search/
  license: BSD License
  summary: 'An extension to django CMS to provide multilingual Haystack indexes (fork)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
