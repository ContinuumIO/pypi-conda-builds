package:
  name: p4a.subtyper
  version: "2.0"

source:
  fn: p4a.subtyper-2.0.zip
  url: https://pypi.python.org/packages/source/p/p4a.subtyper/p4a.subtyper-2.0.zip
  md5: 14ba67820647341e75b0b55151f5a684
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.subtyper = p4a.subtyper:main
    #
    # Would create an entry point called p4a.subtyper that calls p4a.subtyper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - p4a.z2utils

  run:
    - python
    - setuptools
    - setuptools
    - p4a.z2utils

test:
  # Python imports
  imports:
    - p4a
    - p4a.subtyper
    - p4a.subtyper.Extensions
    - p4a.subtyper.contentmenu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/p4a/p4a.subtyper/
  license: GPL
  summary: 'Subtyping framework for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
