package:
  name: django-snippetscream
  version: "0.0.7"

source:
  fn: django-snippetscream-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-snippetscream/django-snippetscream-0.0.7.tar.gz
  md5: 20e2f2f6094caac518505623551aa9db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-snippetscream = django-snippetscream:main
    #
    # Would create an entry point called django-snippetscream that calls django-snippetscream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - snippetscream
    - snippetscream.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/shaunsephton/django-snippetscream
  license: BSD License
  summary: 'Django app packaging the best snippets found on http://djangosnippets.org'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
