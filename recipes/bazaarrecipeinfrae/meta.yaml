package:
  name: bazaarrecipeinfrae
  version: "0.1.2dev"

source:
  fn: bazaarrecipeinfrae-0.1.2dev.tar.gz
  url: https://pypi.python.org/packages/source/b/bazaarrecipeinfrae/bazaarrecipeinfrae-0.1.2dev.tar.gz
  md5: b4fe4d83c23a49d61cf96b2120446a03
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bazaarrecipeinfrae = bazaarrecipeinfrae:main
    #
    # Would create an entry point called bazaarrecipeinfrae that calls bazaarrecipeinfrae.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - bazaarrecipeinfrae

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/bazaarrecipe
  license: BSD License
  summary: 'Buildout recipe for Bazaar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
