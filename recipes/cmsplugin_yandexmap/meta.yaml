package:
  name: cmsplugin_yandexmap
  version: "1.3.2"

source:
  fn: cmsplugin_yandexmap-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_yandexmap/cmsplugin_yandexmap-1.3.2.tar.gz
  md5: 72d531b53607fd65fc9c99afb1bdfae5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_yandexmap = cmsplugin_yandexmap:main
    #
    # Would create an entry point called cmsplugin_yandexmap that calls cmsplugin_yandexmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0

  run:
    - python
    - django-cms >=3.0

test:
  # Python imports
  imports:
    - cmsplugin_yandexmap
    - cmsplugin_yandexmap.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/moskrc/cmsplugin_yandexmap/
  license: MIT License
  summary: 'Plugin for django-cms. Yandex Map.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
