package:
  name: djangocms-utils
  version: "0.9.5"

source:
  fn: djangocms-utils-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-utils/djangocms-utils-0.9.5.tar.gz
  md5: f01b92d880d2e8c30fd37bb1bf85ba42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-utils = djangocms-utils:main
    #
    # Would create an entry point called djangocms-utils that calls djangocms-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_utils
    - djangocms_utils.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fivethreeo/djangocms-utils/
  license: UNKNOWN
  summary: 'Utilities for django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
