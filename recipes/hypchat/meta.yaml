package:
  name: hypchat
  version: "0.18"

source:
  fn: hypchat-0.18.tar.gz
  url: https://pypi.python.org/packages/source/h/hypchat/hypchat-0.18.tar.gz
  md5: df9143116105d0db5e1b72fc6f20badf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hypchat = hypchat:main
    #
    # Would create an entry point called hypchat that calls hypchat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - python-dateutil
    - six

  run:
    - python
    - requests
    - python-dateutil
    - six

test:
  # Python imports
  imports:
    - hypchat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RidersDiscountCom/HypChat
  license: MIT License
  summary: "Package for HipChat's v2 API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
