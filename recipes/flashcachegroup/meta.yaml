package:
  name: flashcachegroup
  version: "0.6.7"

source:
  fn: flashcachegroup-0.6.7.tar.gz
  url: https://pypi.python.org/packages/source/f/flashcachegroup/flashcachegroup-0.6.7.tar.gz
  md5: 0ef01608985ab5983f543a2d6cdcd746
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flashcachegroup = flashcachegroup:main
    #
    # Would create an entry point called flashcachegroup that calls flashcachegroup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flashcachegroup
    - libfcg
    - libfcg.common

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lihuiba/flashcachegroup
  license: GPL
  summary: 'create cache for a group of disks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
