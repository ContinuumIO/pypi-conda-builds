package:
  name: demandimport
  version: "0.2.1"

source:
  fn: demandimport-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/demandimport/demandimport-0.2.1.tar.gz
  md5: ea25e59698cb6914bc4e78022acddf74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - demandimport = demandimport:main
    #
    # Would create an entry point called demandimport that calls demandimport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demandimport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bwesterb/py-demandimport/
  license: GPL 2.0
  summary: 'On-demand imports, taken from mercurial'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
