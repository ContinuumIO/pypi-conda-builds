package:
  name: redturtle.portlet.lightreviewlist
  version: "0.1.1"

source:
  fn: redturtle.portlet.lightreviewlist-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.portlet.lightreviewlist/redturtle.portlet.lightreviewlist-0.1.1.tar.gz
  md5: f020854c52cbd50408ec3700845c930a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.portlet.lightreviewlist = redturtle.portlet.lightreviewlist:main
    #
    # Would create an entry point called redturtle.portlet.lightreviewlist that calls redturtle.portlet.lightreviewlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.portlet
    - redturtle.portlet.lightreviewlist
    - redturtle.portlet.lightreviewlist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/redturtle.portlet.lightreviewlist
  license: GPL
  summary: 'A light version of the Plone review_list portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
