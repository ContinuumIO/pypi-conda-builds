package:
  name: gitforsoftimage
  version: "0.2.1"

source:
  fn: gitforsoftimage-0.2.1.zip
  url: https://pypi.python.org/packages/source/g/gitforsoftimage/gitforsoftimage-0.2.1.zip
  md5: e05781c68b1aa6a5bb6f67211a9d3307
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitforsoftimage = gitforsoftimage:main
    #
    # Would create an entry point called gitforsoftimage that calls gitforsoftimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wishlib >=0.1.4

  run:
    - python
    - wishlib >=0.1.4

test:
  # Python imports
  imports:
    - gitforsoftimage
    - gitforsoftimage.layout
    - gitforsoftimage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/csaez/gitforsoftimage
  license: GNU General Public License (GPLv3)
  summary: 'Git integration on Softimage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
