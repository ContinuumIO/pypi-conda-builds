package:
  name: django-pagehelp
  version: "1.1"

source:
  fn: django-pagehelp-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pagehelp/django-pagehelp-1.1.tar.gz
  md5: 8a8914fee387f5eef3ce20593996deda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pagehelp = django-pagehelp:main
    #
    # Would create an entry point called django-pagehelp that calls django-pagehelp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pagehelp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/smileychris/django-pagehelp/
  license: MIT License
  summary: "A Django application which provides contextual help for your site's pages."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
