package:
  name: bitly_api
  version: "0.3"

source:
  fn: bitly_api-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bitly_api/bitly_api-0.3.tar.gz
  md5: b89a0c4d35547cba5b6214c296a8129f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitly_api = bitly_api:main
    #
    # Would create an entry point called bitly_api that calls bitly_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bitly_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitly/bitly-api-python
  license: Apache Software License
  summary: 'An API for bit.ly'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
