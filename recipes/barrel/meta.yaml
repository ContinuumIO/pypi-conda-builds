package:
  name: barrel
  version: "0.1.3"

source:
  fn: barrel-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/barrel/barrel-0.1.3.tar.gz
  md5: fe4b04bfa4efb0024b2952b086170d6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barrel = barrel:main
    #
    # Would create an entry point called barrel that calls barrel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - barrel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lukearno.com/projects/barrel/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Flexible WSGI authentication and authorization tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
