package:
  name: django-rauthall
  version: "0.0.1"

source:
  fn: django-rauthall-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rauthall/django-rauthall-0.0.1.tar.gz
  md5: 688299437c56d70ba08c1447f473306f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rauthall = django-rauthall:main
    #
    # Would create an entry point called django-rauthall that calls django-rauthall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5
    - south ==0.8.1
    - django-compressor ==1.3
    - sorl-thumbnail ==11.12
    - django-allauth ==0.13.0

  run:
    - python
    - django <1.5
    - south ==0.8.1
    - django-compressor ==1.3
    - sorl-thumbnail ==11.12
    - django-allauth ==0.13.0

test:
  # Python imports
  imports:
    - rauthall
    - rauthall.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r0sk/django-rauthall
  license: BSD License
  summary: 'Yet another Django Auth App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
