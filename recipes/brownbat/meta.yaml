package:
  name: brownbat
  version: "0.0b2"

source:
  fn: BrownBat-0.0b2.tar.gz
  url: https://pypi.python.org/packages/source/B/BrownBat/BrownBat-0.0b2.tar.gz
  md5: 5bcfec7273bb13e7713e859bb3f0d6c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brownbat = brownbat:main
    #
    # Would create an entry point called brownbat that calls brownbat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brownbat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DouglasRaillard/BrownBat
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'A lazy source code generation library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
