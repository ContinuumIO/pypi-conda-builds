package:
  name: cheerio
  version: "1.4"

source:
  fn: cheerio-1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cheerio/cheerio-1.4.tar.gz
  md5: 017bc6f7a979359a9e80eaab95d0329a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cheerio = cheerio:main
    #
    # Would create an entry point called cheerio that calls cheerio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cheerio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lucywyman/cheerio
  license: UNKNOWN
  summary: 'A cute script to print goodbyes in various languages and colors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
