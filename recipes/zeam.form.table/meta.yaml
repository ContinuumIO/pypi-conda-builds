package:
  name: zeam.form.table
  version: "1.1.1"

source:
  fn: zeam.form.table-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zeam.form.table/zeam.form.table-1.1.1.tar.gz
  md5: da50b9d6dbd0470e9d054bf93f21d491
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeam.form.table = zeam.form.table:main
    #
    # Would create an entry point called zeam.form.table that calls zeam.form.table.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - grokcore.component
    - grokcore.chameleon
    - megrok.pagetemplate
    - setuptools
    - zeam.form.base >=1.2.3
    - zeam.form.composed >=1.3
    - zeam.utils.batch >=1.0
    - zope.component
    - zope.interface
    - zope.i18nmessageid

  run:
    - python
    - grokcore.component
    - grokcore.chameleon
    - megrok.pagetemplate
    - setuptools
    - zeam.form.base >=1.2.3
    - zeam.form.composed >=1.3
    - zeam.utils.batch >=1.0
    - zope.component
    - zope.interface
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - zeam
    - zeam.form
    - zeam.form.table
    - zeam.form.table.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zeam.form.table
  license: BSD
  summary: 'Form as table, to edit more than one content at a time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
