package:
  name: exiflow
  version: "0.4.5.17"

source:
  fn: Exiflow-0.4.5.17.tar.gz
  url: https://pypi.python.org/packages/source/e/exiflow/Exiflow-0.4.5.17.tar.gz
  md5: d6e8158cfc99357915c2d0b8733416ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exiflow = exiflow:main
    #
    # Would create an entry point called exiflow that calls exiflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - glade2

  run:
    - python
    - glade2

test:
  # Python imports
  imports:
    - exiflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://exiflow.org/
  license: GNU General Public License (GPL)
  summary: 'A set of tools including a little GUI to provide a complete digital photo workflow for Unixes, using EXIF headers as the central information repository.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
