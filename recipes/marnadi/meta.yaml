package:
  name: marnadi
  version: "0.2.9"

source:
  fn: Marnadi-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/M/Marnadi/Marnadi-0.2.9.tar.gz
  md5: 595d720e6a22a24827f4aae3ee502a1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marnadi = marnadi:main
    #
    # Would create an entry point called marnadi that calls marnadi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - marnadi
    - marnadi.descriptors
    - marnadi.descriptors.data
    - marnadi.descriptors.data.decoders
    - marnadi.descriptors.data.decoders.application

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renskiy/marnadi
  license: MIT License
  summary: 'Yet another WSGI web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
