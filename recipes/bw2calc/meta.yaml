package:
  name: bw2calc
  version: "1.0"

source:
  fn: bw2calc-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bw2calc/bw2calc-1.0.tar.gz
  md5: d0a6f1039cdfd2c19c67f358a38391d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bw2calc = bw2calc:main
    #
    # Would create an entry point called bw2calc that calls bw2calc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - brightway2
    - bw2data >=1.0

  run:
    - python
    - brightway2
    - bw2data >=1.0

test:
  # Python imports
  imports:
    - bw2calc
    - bw2calc.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cmutel/brightway2-calc
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
