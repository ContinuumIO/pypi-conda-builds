package:
  name: cmsplugin_tab_ui
  version: "1.0"

source:
  fn: cmsplugin_tab_ui-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_tab_ui/cmsplugin_tab_ui-1.0.tar.gz
  md5: 0aea0d241d3de26ec7dfbd410f725cce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_tab_ui = cmsplugin_tab_ui:main
    #
    # Would create an entry point called cmsplugin_tab_ui that calls cmsplugin_tab_ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0
    - django-paintstore

  run:
    - python
    - django-cms >=3.0
    - django-paintstore

test:
  # Python imports
  imports:
    - cmsplugin_tab_ui
    - cmsplugin_tab_ui.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/luisza/cmsplugin-tab-ui
  license: GNU General Public License v2 (GPLv2)
  summary: 'Tab plugin for django cms with Jquery ui'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
