package:
  name: collective.roundabout
  version: "0.3.1"

source:
  fn: collective.roundabout-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.roundabout/collective.roundabout-0.3.1.tar.gz
  md5: ce2eb256ed9027b734ea41685aa4fa6a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.roundabout = collective.roundabout:main
    #
    # Would create an entry point called collective.roundabout that calls collective.roundabout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.roundabout
    - collective.roundabout.browser
    - collective.roundabout.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fourdigits.nl
  license: GPL
  summary: 'Add support for 360 degree panoramic photos to your Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
