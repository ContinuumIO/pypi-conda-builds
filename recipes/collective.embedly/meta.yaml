package:
  name: collective.embedly
  version: "2.3"

source:
  fn: collective.embedly-2.3.zip
  url: https://pypi.python.org/packages/source/c/collective.embedly/collective.embedly-2.3.zip
  md5: ca8f7295f79749bb6daa3394e9dd0ab3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.embedly = collective.embedly:main
    #
    # Would create an entry point called collective.embedly that calls collective.embedly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.embedly
    - collective.embedly.browser
    - collective.embedly.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.embedly
  license: GNU General Public License (GPL)
  summary: 'TinyMCE visual editor support for embed.ly service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
