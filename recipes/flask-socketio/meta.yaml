package:
  name: flask-socketio
  version: "0.6.0"

source:
  fn: Flask-SocketIO-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SocketIO/Flask-SocketIO-0.6.0.tar.gz
  md5: a990a0f63502f74be0d893cf142db7bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-socketio = flask-socketio:main
    #
    # Would create an entry point called flask-socketio that calls flask-socketio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - gevent >=1.0
    - gevent-socketio >=0.3.6

  run:
    - python
    - flask >=0.9
    - gevent >=1.0
    - gevent-socketio >=0.3.6

test:
  # Python imports
  imports:
    - flask_socketio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/miguelgrinberg/Flask-SocketIO/
  license: MIT License
  summary: 'Socket.IO integration for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
