package:
  name: binstruct
  version: "1.0.1"

source:
  fn: binstruct-1.0.1.zip
  url: https://pypi.python.org/packages/source/b/binstruct/binstruct-1.0.1.zip
  md5: 066a5f55c2f406050aead269ce064355
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - binstruct = binstruct:main
    #
    # Would create an entry point called binstruct that calls binstruct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - binstruct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jokymon/binstruct
  license: GNU General Public License v3 (GPLv3)
  summary: 'Library for read/write access of binary data via structures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
