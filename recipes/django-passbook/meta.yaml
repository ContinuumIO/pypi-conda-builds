package:
  name: django-passbook
  version: "0.1.0dev"

source:
  fn: django-passbook-0.1.0dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-passbook/django-passbook-0.1.0dev.tar.gz
  md5: 9b1cce845053d0c7b06b69d93088830d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-passbook = django-passbook:main
    #
    # Would create an entry point called django-passbook that calls django-passbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_passbook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/devartis/django-passbook/
  license: django-passreset
  summary: 'Django Passbook server app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
