package:
  name: archetypes.searchwidget
  version: "0.1.8"

source:
  fn: archetypes.searchwidget-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/a/archetypes.searchwidget/archetypes.searchwidget-0.1.8.tar.gz
  md5: 502bf1ae5769466bf228e11b21407126
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.searchwidget = archetypes.searchwidget:main
    #
    # Would create an entry point called archetypes.searchwidget that calls archetypes.searchwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.searchtool
    - collective.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - collective.searchtool
    - collective.jqueryui

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.searchwidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/archetypes/MoreiFieldAndWidgets
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
