package:
  name: collective.jquerytablesorter
  version: "0.1"

source:
  fn: collective.jquerytablesorter-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.jquerytablesorter/collective.jquerytablesorter-0.1.tar.gz
  md5: b3e8825f010a10cb939d1c96f3d4464b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jquerytablesorter = collective.jquerytablesorter:main
    #
    # Would create an entry point called collective.jquerytablesorter that calls collective.jquerytablesorter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.jquerytablesorter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.jquerytablesorter
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
