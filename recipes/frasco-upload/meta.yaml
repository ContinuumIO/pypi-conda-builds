package:
  name: frasco-upload
  version: "0.1.1"

source:
  fn: frasco-upload-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-upload/frasco-upload-0.1.1.tar.gz
  md5: e841ad724369350f5798fc7c9a96d94f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-upload = frasco-upload:main
    #
    # Would create an entry point called frasco-upload that calls frasco-upload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco

  run:
    - python
    - frasco

test:
  # Python imports
  imports:
    - frasco_upload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-upload
  license: MIT
  summary: 'File uploading for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
