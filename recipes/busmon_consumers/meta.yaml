package:
  name: busmon_consumers
  version: "0.4.5"

source:
  fn: busmon_consumers-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/b/busmon_consumers/busmon_consumers-0.4.5.tar.gz
  md5: 4e87fdd9f1bede3a23c5313c625b23d6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - busmon_consumers = busmon_consumers:main
    #
    # Would create an entry point called busmon_consumers that calls busmon_consumers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fedmsg >=0.5.0
    - python-memcached

  run:
    - python
    - setuptools
    - fedmsg >=0.5.0
    - python-memcached

test:
  # Python imports
  imports:
    - busmon_consumers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/busmon
  license: GPLv2+
  summary: 'fedmsg-hub consumers for the busmon webapp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
