package:
  name: odooenv
  version: "2.1.3"

source:
  fn: odooenv-2.1.3.tar.gz
  url: https://pypi.python.org/packages/source/o/odooenv/odooenv-2.1.3.tar.gz
  md5: 1f3149b20a94f7eabfea15ed1ca9f4e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - odooenv = odooenv:main
    #
    # Would create an entry point called odooenv that calls odooenv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - virtualenv
    - psycopg2
    - argparse
    - bzr
    - pyyaml

  run:
    - python
    - virtualenv
    - psycopg2
    - argparse
    - bzr
    - pyyaml

test:
  # Python imports
  imports:
    - odooenv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://biz.moldeo.coop/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Odoo Environment Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
