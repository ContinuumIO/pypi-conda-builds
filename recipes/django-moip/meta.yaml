package:
  name: django-moip
  version: "0.1.1"

source:
  fn: django-moip-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-moip/django-moip-0.1.1.tar.gz
  md5: de85a92d26b661591b29abe15041cb22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-moip = django-moip:main
    #
    # Would create an entry point called django-moip that calls django-moip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0
    - furl

  run:
    - python
    - django >=1.0
    - furl

test:
  # Python imports
  imports:
    - django_moip
    - django_moip.html
    - django_moip.html.nit
    - django_moip.html.nit.tests
    - django_moip.html.redirector
    - django_moip.html.redirector.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alanjds/django-moip
  license: UNKNOWN
  summary: 'A pluggable Django application for integrating MoIP HTML (for now)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
