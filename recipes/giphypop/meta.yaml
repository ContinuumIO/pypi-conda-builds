package:
  name: giphypop
  version: "0.2"

source:
  fn: giphypop-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/giphypop/giphypop-0.2.tar.gz
  md5: 5cbcd2fb8fcf140769d16b61c9524801
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - giphypop = giphypop:main
    #
    # Would create an entry point called giphypop that calls giphypop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - giphypop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/shaunduncan/giphypop/
  license: MIT License
  summary: 'Python wrapper for Giphy API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
