package:
  name: noseapp_requests
  version: "0.0.2"

source:
  fn: noseapp_requests-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/noseapp_requests/noseapp_requests-0.0.2.tar.gz
  md5: 0e9d29e6969ed282718557e6f998d38f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - noseapp_requests = noseapp_requests:main
    #
    # Would create an entry point called noseapp_requests that calls noseapp_requests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - noseapp >=1.0.9
    - requests ==2.6.0

  run:
    - python
    - noseapp >=1.0.9
    - requests ==2.6.0

test:
  # Python imports
  imports:
    - noseapp_requests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brannt/noseapp_requests
  license: noseapp_selenium
  summary: 'requests extension for noseapp lib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
