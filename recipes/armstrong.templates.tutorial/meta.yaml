package:
  name: armstrong.templates.tutorial
  version: "1.0.0"

source:
  fn: armstrong.templates.tutorial-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.templates.tutorial/armstrong.templates.tutorial-1.0.0.tar.gz
  md5: eac18ad6f58df2e60196c16125787533
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.templates.tutorial = armstrong.templates.tutorial:main
    #
    # Would create an entry point called armstrong.templates.tutorial that calls armstrong.templates.tutorial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - armstrong.templates.tutorial

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'The tutorial project for Armstrong'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
