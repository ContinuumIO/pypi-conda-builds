package:
  name: coortransform_girdgeographic
  version: "0.02.0"

source:
  fn: CoorTransform_GirdGeographic-0.02.0.zip
  url: https://pypi.python.org/packages/source/C/CoorTransform_GirdGeographic/CoorTransform_GirdGeographic-0.02.0.zip
  md5: 5ba50c351f7d36d819ff0d333b5395d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coortransform_girdgeographic = coortransform_girdgeographic:main
    #
    # Would create an entry point called coortransform_girdgeographic that calls coortransform_girdgeographic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - CoorTransform_GirdGeographic
    - coortransform_girdgeographic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/walter426/Python_CoorTransform_GirdGeographic
  license: GPL
  summary: 'Provide functions for coordinate transform from grid to geographic, or vice versa'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
