package:
  name: apache-log-parser
  version: "1.6.1"

source:
  fn: apache-log-parser-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/a/apache-log-parser/apache-log-parser-1.6.1.tar.gz
  md5: 6ff881c5992c29fcc90127e5592e9cff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apache-log-parser = apache-log-parser:main
    #
    # Would create an entry point called apache-log-parser that calls apache-log-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - user-agents

  run:
    - python
    - user-agents

test:
  # Python imports
  imports:
    - apache_log_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Parse lines from an apache log file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
