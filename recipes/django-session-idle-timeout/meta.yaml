package:
  name: django-session-idle-timeout
  version: "1.4.2"

source:
  fn: django-session-idle-timeout-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-session-idle-timeout/django-session-idle-timeout-1.4.2.tar.gz
  md5: 614dd12a051d0642fc987cc69e6fa5ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-session-idle-timeout = django-session-idle-timeout:main
    #
    # Would create an entry point called django-session-idle-timeout that calls django-session-idle-timeout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django-session-idle-timeout
    - django-session-idle-timeout.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tzulberti/django-session-idle-timeout
  license: BSD License
  summary: 'Timeout a logged user after a period of time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
