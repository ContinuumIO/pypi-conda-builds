package:
  name: blinkytape
  version: "1.0"

source:
  fn: BlinkyTape-1.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BlinkyTape/BlinkyTape-1.0.tar.gz
  md5: 182b21956f9e2c5887bb72fdd4ba0ba2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blinkytape = blinkytape:main
    #
    # Would create an entry point called blinkytape that calls blinkytape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - blinkytape

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blinkinlabs.com/blinkytape/
  license: UNKNOWN
  summary: 'Library for controlling your BlinkyTape using Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
