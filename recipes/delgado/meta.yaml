package:
  name: delgado
  version: "0.0.1"

source:
  fn: delgado-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/delgado/delgado-0.0.1.tar.gz
  md5: 0c89e4d94f9f5b00e30bc01fa09c8c8a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - delgado = delgado:main
    #
    # Would create an entry point called delgado that calls delgado.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tambo

  run:
    - python
    - setuptools
    - tambo

test:
  # Python imports
  imports:
    - delgado
    - delgado.services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alfredodeza/delgado
  license: MIT License
  summary: 'Command executor over UDS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
