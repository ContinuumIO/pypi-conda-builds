package:
  name: espn_py
  version: "0.2"

source:
  fn: espn_py-0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/espn_py/espn_py-0.2.tar.gz
  md5: c43592dc01f9a83d32704d7bfe1597be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - espn_py = espn_py:main
    #
    # Would create an entry point called espn_py that calls espn_py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3

  run:
    - python
    - requests >=1.2.3

test:
  # Python imports
  imports:
    - espn_py

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/talam/espn-py
  license: MIT
  summary: 'Simple python wrapper around the ESPN API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
