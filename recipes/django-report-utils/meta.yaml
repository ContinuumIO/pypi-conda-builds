package:
  name: django-report-utils
  version: "0.3.4"

source:
  fn: django-report-utils-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-report-utils/django-report-utils-0.3.4.tar.gz
  md5: ea496b00d6ee049cf566cc162f78acd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-report-utils = django-report-utils:main
    #
    # Would create an entry point called django-report-utils that calls django-report-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - six

  run:
    - python
    - django
    - six

test:
  # Python imports
  imports:
    - report_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/burke-software/django-report-utils
  license: BSD License
  summary: 'Common report functions used by django-report-builder and django-report-scaffold.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
