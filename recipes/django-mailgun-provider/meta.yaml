package:
  name: django-mailgun-provider
  version: "0.2.3"

source:
  fn: django-mailgun-provider-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailgun-provider/django-mailgun-provider-0.2.3.tar.gz
  md5: 541807404ea6232c5861b2c21ba2e705
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailgun-provider = django-mailgun-provider:main
    #
    # Would create an entry point called django-mailgun-provider that calls django-mailgun-provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - django_mailgun

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bradwhittington/django-mailgun/
  license: MIT License
  summary: 'A Django email backend for Mailgun'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
