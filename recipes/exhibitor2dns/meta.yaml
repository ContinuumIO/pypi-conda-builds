package:
  name: exhibitor2dns
  version: "0.1.2"

source:
  fn: exhibitor2dns-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/exhibitor2dns/exhibitor2dns-0.1.2.tar.gz
  md5: d6f52fd4b27c2d4dbe02886db25c9922
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exhibitor2dns = exhibitor2dns:main
    #
    # Would create an entry point called exhibitor2dns that calls exhibitor2dns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - exhibitor2dns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache-2
  summary: 'Dynamically sync Route53 DNS entries for Zookeeper servers in an Exhibitor cluster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
