package:
  name: django-modify-history
  version: "0.1rc2"

source:
  fn: django-modify-history-0.1rc2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modify-history/django-modify-history-0.1rc2.tar.gz
  md5: 3b1d6e43017633f21100bed1347b06f5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modify-history = django-modify-history:main
    #
    # Would create an entry point called django-modify-history that calls django-modify-history.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - modify_history
    - modify_history.backends
    - modify_history.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-modify-history
  license: BSD License
  summary: 'Automatically create history database when perticular model is saved.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
