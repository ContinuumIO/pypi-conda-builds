package:
  name: dateglob
  version: "0.1"

source:
  fn: dateglob-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dateglob/dateglob-0.1.tar.gz
  md5: dfcbf0af177485f79ebe41d63523b867
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dateglob = dateglob:main
    #
    # Would create an entry point called dateglob that calls dateglob.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dateglob

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Yelp/dateglob
  license: Apache Software License
  summary: 'Convert a set of dates into a compact list of globs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
