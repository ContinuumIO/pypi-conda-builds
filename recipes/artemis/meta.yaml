package:
  name: artemis
  version: "0.1.4"

source:
  fn: artemis-0.1.4.zip
  url: https://pypi.python.org/packages/source/a/artemis/artemis-0.1.4.zip
  md5: f5b85df7e68f5dd8f01191e56bb2d891
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - artemis = artemis:main
    #
    # Would create an entry point called artemis that calls artemis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - tornado >=4.0
    - schematics >=1.0
    - tinydb >=2.1.0

  run:
    - python
    - six
    - tornado >=4.0
    - schematics >=1.0
    - tinydb >=2.1.0

test:
  # Python imports
  imports:
    - artemis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Async Restful TinyDB Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
