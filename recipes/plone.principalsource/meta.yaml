package:
  name: plone.principalsource
  version: "1.0"

source:
  fn: plone.principalsource-1.0.zip
  url: https://pypi.python.org/packages/source/p/plone.principalsource/plone.principalsource-1.0.zip
  md5: be1b0e72b3451271693b5b5a567bd77f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.principalsource = plone.principalsource:main
    #
    # Would create an entry point called plone.principalsource that calls plone.principalsource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.formwidget.query
    - zope.schema
    - zope.component
    - zope.interface
    - products.cmfcore
    - products.pluggableauthservice

  run:
    - python
    - setuptools
    - setuptools
    - z3c.formwidget.query
    - zope.schema
    - zope.component
    - zope.interface
    - products.cmfcore
    - products.pluggableauthservice

test:
  # Python imports
  imports:
    - plone
    - plone.principalsource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.principalsource
  license: GPL
  summary: 'A queriable source for accessing users and/or groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
