package:
  name: click-utils
  version: "0.2.1"

source:
  fn: click-utils-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/click-utils/click-utils-0.2.1.tar.gz
  md5: 3508334ee30cc2b10be2e6665ee48784
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - click-utils = click-utils:main
    #
    # Would create an entry point called click-utils that calls click-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - click >=3.0,<4.0

  run:
    - python
    - click >=3.0,<4.0

test:
  # Python imports
  imports:
    - click_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/slafs/click-utils
  license: BSD License
  summary: 'a set of utilites for writing command line programs with Click'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
