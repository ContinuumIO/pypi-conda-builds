package:
  name: twisted-hl7
  version: "0.2.2"

source:
  fn: twisted-hl7-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/t/twisted-hl7/twisted-hl7-0.2.2.tar.gz
  md5: d077f43e6f83b87d519665fa1c69dd57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - twisted-hl7 = twisted-hl7:main
    #
    # Would create an entry point called twisted-hl7 that calls twisted-hl7.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - hl7 >=0.3.1

  run:
    - python
    - twisted
    - hl7 >=0.3.1

test:
  # Python imports
  imports:
    - twisted.plugins
    - twistedhl7

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://twisted-hl7.readthedocs.org
  license: BSD License
  summary: 'Async Twisted server implementation of Health Level 7 MLLP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
