package:
  name: pylibmc
  version: "1.4.3"

source:
  fn: pylibmc-1.4.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pylibmc/pylibmc-1.4.3.tar.gz
  md5: 3ec66407dd1c0585c426ff65eb4f9b87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pylibmc = pylibmc:main
    #
    # Would create an entry point called pylibmc that calls pylibmc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pylibmc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sendapatch.se/projects/pylibmc/
  license: 3-clause BSD <http://www.opensource.org/licenses/bsd-license.php>
  summary: 'Quick and small memcached client for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
