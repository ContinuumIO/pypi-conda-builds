package:
  name: mpop
  version: "v1.1.0"

source:
  fn: mpop-v1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mpop/mpop-v1.1.0.tar.gz
  md5: 6198ae63adc0ec91207dda5ccf2b9ce2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpop = mpop:main
    #
    # Would create an entry point called mpop that calls mpop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.4.1
    - pyresample
    - pillow

  run:
    - python
    - numpy >=1.4.1
    - pyresample
    - pillow

test:
  # Python imports
  imports:
    - mpop
    - mpop.imageo
    - mpop.imageo.formats
    - mpop.instruments
    - mpop.satellites
    - mpop.satin
    - mpop.satout
    - mpop.saturn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mraspaud/mpop
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Meteorological post processing package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
