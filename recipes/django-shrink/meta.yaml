package:
  name: django-shrink
  version: "0.0.3.3"

source:
  fn: django-shrink-0.0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shrink/django-shrink-0.0.3.3.tar.gz
  md5: 8ce2110d6ac8bbcc3dc1ae5e4461932c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shrink = django-shrink:main
    #
    # Would create an entry point called django-shrink that calls django-shrink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyscss ==1.0.6

  run:
    - python
    - pyscss ==1.0.6

test:
  # Python imports
  imports:
    - shrink
    - shrink.conf
    - shrink.management
    - shrink.management.commands
    - shrink.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/django-shrink
  license: BSD License
  summary: 'js compiler & css minifier with sass compatibility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
