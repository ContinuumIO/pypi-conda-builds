package:
  name: fjp_helpers
  version: "0.1.5"

source:
  fn: fjp_helpers-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fjp_helpers/fjp_helpers-0.1.5.tar.gz
  md5: 9f85e2163b42735ef0b82a02a8baff93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fjp_helpers = fjp_helpers:main
    #
    # Would create an entry point called fjp_helpers that calls fjp_helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mpi4py
    - matplotlib
    - numpy

  run:
    - python
    - mpi4py
    - matplotlib
    - numpy

test:
  # Python imports
  imports:
    - fjp_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Poulin-Research-Group
  license: UNKNOWN
  summary: 'A collection of helper functions used to solve different equations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
