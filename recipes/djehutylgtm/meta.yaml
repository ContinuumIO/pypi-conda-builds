package:
  name: djehutylgtm
  version: "0.0.1"

source:
  fn: djehutylgtm-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djehutylgtm/djehutylgtm-0.0.1.tar.gz
  md5: d038153e4dca9ff153969209945b1fdc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djehutylgtm = djehutylgtm:main
    #
    # Would create an entry point called djehutylgtm that calls djehutylgtm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djehuty >=0.0.3
    - requests >=2.3.0

  run:
    - python
    - setuptools
    - djehuty >=0.0.3
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - djehutylgtm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xica/djehutylgtm
  license: MIT License
  summary: 'djehutylgtm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
