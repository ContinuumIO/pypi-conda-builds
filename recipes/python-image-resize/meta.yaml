package:
  name: python-image-resize
  version: "1.1.2"

source:
  fn: python-image-resize-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-image-resize/python-image-resize-1.1.2.tar.gz
  md5: e05f9cf92cf1d29c7f81807385878410
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-image-resize = python-image-resize:main
    #
    # Would create an entry point called python-image-resize that calls python-image-resize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - imageresize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charlesthk/image-tools
  license: MIT License
  summary: 'A Small python package to easily resize images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
