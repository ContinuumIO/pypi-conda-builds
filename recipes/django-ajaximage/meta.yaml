package:
  name: django-ajaximage
  version: "0.2.8"

source:
  fn: django-ajaximage-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajaximage/django-ajaximage-0.2.8.tar.gz
  md5: 2cc2cff630f3cb73b4522a26d9b9f5ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajaximage = django-ajaximage:main
    #
    # Would create an entry point called django-ajaximage that calls django-ajaximage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pillow

  run:
    - python
    - django
    - pillow

test:
  # Python imports
  imports:
    - ajaximage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyg/django-ajaximage
  license: MIT License
  summary: 'Upload images via ajax. Images are optionally resized.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
