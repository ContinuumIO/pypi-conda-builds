package:
  name: giturlparse.py
  version: "0.0.5"

source:
  fn: giturlparse.py-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/giturlparse.py/giturlparse.py-0.0.5.tar.gz
  md5: 4d6052a11ef892f13404cbf226b51b52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - giturlparse.py = giturlparse.py:main
    #
    # Would create an entry point called giturlparse.py that calls giturlparse.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - giturlparse
    - giturlparse.platforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FriendCode/giturlparse.py
  license: Apache v2
  summary: 'A Git URL parsing module (supports parsing and rewriting)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
