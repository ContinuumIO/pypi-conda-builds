package:
  name: django-pyscss
  version: "2.0.2"

source:
  fn: django-pyscss-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pyscss/django-pyscss-2.0.2.tar.gz
  md5: f8dbcc4d314c8e220aa311ec6561b06d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pyscss = django-pyscss:main
    #
    # Would create an entry point called django-pyscss that calls django-pyscss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pyscss >=1.3.4
    - pathlib

  run:
    - python
    - django >=1.4
    - pyscss >=1.3.4
    - pathlib

test:
  # Python imports
  imports:
    - django_pyscss
    - django_pyscss.extension

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - django-compressor >=1.3
    - django-discover-runner
    - mock
    - pillow
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fusionbox/django-pyscss
  license: BSD License
  summary: 'Makes it easier to use PySCSS in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
