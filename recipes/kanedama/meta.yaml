package:
  name: kanedama
  version: "0.1.1"

source:
  fn: Kanedama-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/K/Kanedama/Kanedama-0.1.1.tar.gz
  md5: d6efd7508a5cd4757724c5d130ee2f7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kanedama = kanedama:main
    #
    # Would create an entry point called kanedama that calls kanedama.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ghost.py

  run:
    - python
    - ghost.py

test:
  # Python imports
  imports:
    - kanedama

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jimr/Kanedama
  license: MIT License
  summary: 'Download and email Heroku invoices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
