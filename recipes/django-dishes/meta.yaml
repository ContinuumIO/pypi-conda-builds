package:
  name: django-dishes
  version: "0.1.1"

source:
  fn: django-dishes-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dishes/django-dishes-0.1.1.tar.gz
  md5: 1a7293fc1eb74cd85e83ae55e54a795e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dishes = django-dishes:main
    #
    # Would create an entry point called django-dishes that calls django-dishes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dishes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-dishes
  license: BSD License
  summary: 'We need menus of delicious food that people can order and have delivered.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
