package:
  name: git-pick-from
  version: "1.0.1"

source:
  fn: git-pick-from-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/git-pick-from/git-pick-from-1.0.1.tar.gz
  md5: c055f46f9696222dcbb80ab3a9e68088
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-pick-from = git-pick-from:main
    #
    # Would create an entry point called git-pick-from that calls git-pick-from.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urwid

  run:
    - python
    - urwid

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matze/git-pick-from
  license: BSD
  summary: 'Text user interface for git cherry-pick'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
