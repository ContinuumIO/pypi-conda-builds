package:
  name: attrgettersetter
  version: "0.1"

source:
  fn: attrgettersetter-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/attrgettersetter/attrgettersetter-0.1.tar.gz
  md5: 5a949672a077cf4b5c5bf61d27b6ae06
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - attrgettersetter = attrgettersetter:main
    #
    # Would create an entry point called attrgettersetter that calls attrgettersetter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - attrgettersetter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bradjasper.com
  license: MIT
  summary: 'Attribute getter and setter helper tools (useful for attribute.dot.syntax)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
