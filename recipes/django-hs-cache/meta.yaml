package:
  name: django-hs-cache
  version: "0.1.2"

source:
  fn: django-hs-cache-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hs-cache/django-hs-cache-0.1.2.tar.gz
  md5: 8d869a70f008d0ecbb17625244a48fc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hs-cache = django-hs-cache:main
    #
    # Would create an entry point called django-hs-cache that calls django-hs-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-handler-socket

  run:
    - python
    - python-handler-socket

test:
  # Python imports
  imports:
    - hs_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dchaplinsky/django-hs-cache
  license: MIT License
  summary: 'HandlerSocket cache backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
