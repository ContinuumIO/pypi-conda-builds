package:
  name: batchopenmpi
  version: "0.2.2"

source:
  fn: batchOpenMPI-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/batchOpenMPI/batchOpenMPI-0.2.2.tar.gz
  md5: 6aab5303416bd8569d125195b085ddbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - batchopenmpi = batchopenmpi:main
    #
    # Would create an entry point called batchopenmpi that calls batchopenmpi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - mpi4py

  run:
    - python
    - numpy
    - mpi4py

test:
  # Python imports
  imports:
    - batchopenmpi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hamish2014/optTune/
  license: MIT
  summary: 'Easy parallelization built on top of mpi4py.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
