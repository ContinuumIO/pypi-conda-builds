package:
  name: djinn_auth
  version: "1.0.5"

source:
  fn: djinn_auth-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djinn_auth/djinn_auth-1.0.5.tar.gz
  md5: becddb3d348cde20ee4c710f5da50c74
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinn_auth = djinn_auth:main
    #
    # Would create an entry point called djinn_auth that calls djinn_auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django_polymorphic

  run:
    - python
    - setuptools
    - django
    - django_polymorphic

test:
  # Python imports
  imports:
    - djinn_auth
    - djinn_auth.models
    - djinn_auth.templatetags
    - djinn_auth.tests
    - djinn_auth.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/djinn_auth
  license: beer-ware
  summary: 'Djinn Intranet auth module, role based'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
