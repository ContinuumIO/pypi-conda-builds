package:
  name: herald
  version: "0.4"

source:
  fn: herald-0.4.tar.gz
  url: https://pypi.python.org/packages/source/h/herald/herald-0.4.tar.gz
  md5: 002b5a5aca9c8dec9ccd3537c911b45b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - herald = herald:main
    #
    # Would create an entry point called herald that calls herald.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - notifo >=0.2.1
    - transmissionrpc >=0.5.0
    - utorrent.py >=0.1.1

  run:
    - python
    - notifo >=0.2.1
    - transmissionrpc >=0.5.0
    - utorrent.py >=0.1.1

test:
  # Python imports
  imports:
    - herald

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/micahr/herald/
  license: BSD License
  summary: 'Mobile Notifications for Transmission or uTorrent Downloads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
