package:
  name: jiocloud
  version: "0.4"

source:
  fn: jiocloud-0.4.tar.gz
  url: https://pypi.python.org/packages/source/j/jiocloud/jiocloud-0.4.tar.gz
  md5: fb9c29a43b20322bd9b84d4be787bda1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jiocloud = jiocloud:main
    #
    # Would create an entry point called jiocloud that calls jiocloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - etcd
    - ipy
    - mock

  run:
    - python
    - etcd
    - ipy
    - mock

test:
  # Python imports
  imports:
    - jiocloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/JioCloud/python-jiocloud
  license: Apache 2.0
  summary: 'Various Python utilities used for JioCloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
