package:
  name: django-secureform
  version: "0.3-1"

source:
  fn: django-secureform-0.3-1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-secureform/django-secureform-0.3-1.tar.gz
  md5: a449b559cee92600fb0ae4cd2eb61942
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-secureform = django-secureform:main
    #
    # Would create an entry point called django-secureform that calls django-secureform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_secureform
    - django_secureform.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smartfile/django-secureform/
  license: GPLv3
  summary: 'Provides protection against spammers and scammers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
