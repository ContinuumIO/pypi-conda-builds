package:
  name: ibm_db_alembic
  version: "0.1.0"

source:
  fn: ibm_db_alembic-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ibm_db_alembic/ibm_db_alembic-0.1.0.tar.gz
  md5: 68a0996a74c10464dfe7ecf8b0eaf30d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ibm_db_alembic = ibm_db_alembic:main
    #
    # Would create an entry point called ibm_db_alembic that calls ibm_db_alembic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - alembic >=0.6.5
    - ibm_db_sa >=0.3.2

  run:
    - python
    - alembic >=0.6.5
    - ibm_db_sa >=0.3.2

test:
  # Python imports
  imports:
    - ibm_db_alembic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ibm_db_alembic/
  license: Apache Software License
  summary: 'Alembic support for IBM Data Servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
