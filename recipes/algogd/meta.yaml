package:
  name: algogd
  version: "0.1"

source:
  fn: algogd-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/algogd/algogd-0.1.tar.gz
  md5: 46c0e11b4f402c60861acdfd9666886c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - algogd = algogd:main
    #
    # Would create an entry point called algogd that calls algogd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas
    - sqlalchemy

  run:
    - python
    - pandas
    - sqlalchemy

test:
  # Python imports
  imports:
    - algogd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nkarnik/algogd
  license: MIT
  summary: 'Algorithmic backtester for incorporating GDELT event data into finance analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
