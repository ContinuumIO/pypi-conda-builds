package:
  name: biblio-py
  version: "0.6.1"

source:
  fn: biblio-py-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/b/biblio-py/biblio-py-0.6.1.tar.gz
  md5: 40448eb09c62766a17b8b155343e755d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biblio-py = biblio-py:main
    #
    # Would create an entry point called biblio-py that calls biblio-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - query_ads
    - yapbib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fisica.cab.cnea.gov.ar/colisiones/staff/fiol/biblio-py.html
  license: GPLv2
  summary: 'Package to manage bibliography files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
