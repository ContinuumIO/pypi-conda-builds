package:
  name: ucivms
  version: "0.1.2"

source:
  fn: ucivms-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ucivms/ucivms-0.1.2.tar.gz
  md5: 68d32ae982d3842e3590ac8fbedb4d7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ucivms = ucivms:main
    #
    # Would create an entry point called ucivms that calls ucivms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - uciconfig

  run:
    - python
    - uciconfig

test:
  # Python imports
  imports:
    - ucivms
    - ucivms.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/uci-vms
  license: GPLv3
  summary: 'Ubuntu Continuous Integration virtual machine tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
