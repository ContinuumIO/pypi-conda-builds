package:
  name: mongokit-py3
  version: "0.9.1.1"

source:
  fn: mongokit-py3-0.9.1.1.zip
  url: https://pypi.python.org/packages/source/m/mongokit-py3/mongokit-py3-0.9.1.1.zip
  md5: f2ea90ea496bf1dcfa8d1589f8867e8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongokit-py3 = mongokit-py3:main
    #
    # Would create an entry point called mongokit-py3 that calls mongokit-py3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.5

  run:
    - python
    - pymongo >=2.5

test:
  # Python imports
  imports:
    - mongokit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aquavitae/mongokit-py3/
  license: Apache Software License
  summary: 'Python mongodb kit - Python 3 fork'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
