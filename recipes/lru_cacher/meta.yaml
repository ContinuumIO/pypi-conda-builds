package:
  name: lru_cacher
  version: "1.0.0"

source:
  fn: lru_cacher-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lru_cacher/lru_cacher-1.0.0.tar.gz
  md5: 90db0315ca6d1adb9fb2c2e54064c158
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lru_cacher = lru_cacher:main
    #
    # Would create an entry point called lru_cacher that calls lru_cacher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - doubly_linked_list >=1.0.0

  run:
    - python
    - doubly_linked_list >=1.0.0

test:
  # Python imports
  imports:
    - lru_cacher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AWNystrom/lru_cache
  license: Apache Software License
  summary: 'Least Recently used Cache (LRU Cache) in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
