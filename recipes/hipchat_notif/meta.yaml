package:
  name: hipchat_notif
  version: "0.2.3"

source:
  fn: hipchat_notif-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hipchat_notif/hipchat_notif-0.2.3.tar.gz
  md5: 76b92449a7050dd9d41824953a33173a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hipchat_notif = hipchat_notif:main
    #
    # Would create an entry point called hipchat_notif that calls hipchat_notif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0

  run:
    - python
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - hipchat_notif

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/rodevl/hipchat_notif
  license: MIT License
  summary: 'Simple Python library for the HipChat API v1'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
