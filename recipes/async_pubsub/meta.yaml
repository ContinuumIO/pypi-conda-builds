package:
  name: async_pubsub
  version: "0.1.1"

source:
  fn: async_pubsub-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/async_pubsub/async_pubsub-0.1.1.tar.gz
  md5: 4857b147c80cee26b653cccd3f68edbd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - async_pubsub = async_pubsub:main
    #
    # Would create an entry point called async_pubsub that calls async_pubsub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hiredis ==0.1.1
    - redis ==2.7.6
    - tornado ==3.2
    - pyzmq ==13.1.0

  run:
    - python
    - hiredis ==0.1.1
    - redis ==2.7.6
    - tornado ==3.2
    - pyzmq ==13.1.0

test:
  # Python imports
  imports:
    - async_pubsub
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abhinavsingh/async_pubsub
  license: BSD License
  summary: 'Asynchronous PubSub in Python using Redis, ZMQ'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
