package:
  name: bigquery-gcs
  version: "0.0.4"

source:
  fn: BigQuery-GCS-0.0.4.zip
  url: https://pypi.python.org/packages/source/B/BigQuery-GCS/BigQuery-GCS-0.0.4.zip
  md5: 9506f805b142f56ebbd1d7193191e621
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigquery-gcs = bigquery-gcs:main
    #
    # Would create an entry point called bigquery-gcs that calls bigquery-gcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bigquery-python >=0.1.1
    - boto

  run:
    - python
    - bigquery-python >=0.1.1
    - boto

test:
  # Python imports
  imports:
    - bigquery_gcs
    - bigquery_gcs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pirsquare/BigQuery-GCS
  license: MIT
  summary: 'Export Large Results from BigQuery to Google Cloud Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
