package:
  name: monk_tf
  version: "0.15.6"

source:
  fn: monk_tf-0.15.6.tar.gz
  url: https://pypi.python.org/packages/source/m/monk_tf/monk_tf-0.15.6.tar.gz
  md5: ee5d44fa20b8391de264b86c8b00a340
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monk_tf = monk_tf:main
    #
    # Would create an entry point called monk_tf that calls monk_tf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pexpect >=3.3
    - requests >=2.2.1
    - pyte >=0.4.8
    - configobj >=4.7.2

  run:
    - python
    - pexpect >=3.3
    - requests >=2.2.1
    - pyte >=0.4.8
    - configobj >=4.7.2

test:
  # Python imports
  imports:
    - monk_tf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DFE/MONK
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'regression test scripting framework for embedded systems developers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
