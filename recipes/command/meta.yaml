package:
  name: command
  version: "0.1.0"

source:
  fn: Command-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/Command/Command-0.1.0.tar.gz
  md5: 54ff29fb8a100a4bdbd508afca4333ea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - command = command:main
    #
    # Would create an entry point called command that calls command.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - command

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/helgi/python-command
  license: MIT License
  summary: 'Command runner with debug capabilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
