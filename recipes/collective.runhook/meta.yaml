package:
  name: collective.runhook
  version: "0.9.5"

source:
  fn: collective.runhook-0.9.5.zip
  url: https://pypi.python.org/packages/source/c/collective.runhook/collective.runhook-0.9.5.zip
  md5: a492f221f46723bec682da36eb500cb1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.runhook = collective.runhook:main
    #
    # Would create an entry point called collective.runhook that calls collective.runhook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.recipe.zope2instance

  run:
    - python
    - setuptools
    - setuptools
    - plone.recipe.zope2instance

test:
  # Python imports
  imports:
    - collective
    - collective.runhook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.runhook/
  license: GPL
  summary: 'Named instance run script entry points for plone.recipe.zope2instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
