package:
  name: simplediff
  version: "1.0"

source:
  fn: simplediff-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simplediff/simplediff-1.0.tar.gz
  md5: 340fde4c0bfccab3cbac93dc5ef8e097
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplediff = simplediff:main
    #
    # Would create an entry point called simplediff that calls simplediff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplediff

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paulgb/simplediff
  license: zlib/libpng License
  summary: 'Diff library with a dead-simple interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
