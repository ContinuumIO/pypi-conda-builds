package:
  name: django-stubtools
  version: "0.5.1"

source:
  fn: django-stubtools-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stubtools/django-stubtools-0.5.1.tar.gz
  md5: dc42868e3316bb220e5783dd8a68f310
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stubtools = django-stubtools:main
    #
    # Would create an entry point called django-stubtools that calls django-stubtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.0

  run:
    - python
    - django >=1.4.0

test:
  # Python imports
  imports:
    - stubtools
    - stubtools.core
    - stubtools.management
    - stubtools.management.commands
    - stubtools.templates
    - stubtools.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: "A set of tools for Django to help 'stub-out' an app quickly."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
