package:
  name: appier_extras
  version: "0.3.18"

source:
  fn: appier_extras-0.3.18.zip
  url: https://pypi.python.org/packages/source/a/appier_extras/appier_extras-0.3.18.zip
  md5: c9103571ee6272f481d0e462589d5d2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appier_extras = appier_extras:main
    #
    # Would create an entry point called appier_extras that calls appier_extras.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appier

  run:
    - python
    - appier

test:
  # Python imports
  imports:
    - appier_extras
    - appier_extras.parts
    - appier_extras.parts.admin
    - appier_extras.parts.admin.models
    - appier_extras.parts.admin.social
    - appier_extras.parts.captcha
    - appier_extras.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://appier_extras.hive.pt
  license: GNU General Public License (GPL)
  summary: 'Appier Framework Extra Elements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
