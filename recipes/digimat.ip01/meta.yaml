package:
  name: digimat.ip01
  version: "0.1.6"

source:
  fn: digimat.ip01-0.1.6.zip
  url: https://pypi.python.org/packages/source/d/digimat.ip01/digimat.ip01-0.1.6.zip
  md5: 40ca049f7cadfee4f42ff4300e9b8e3c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digimat.ip01 = digimat.ip01:main
    #
    # Would create an entry point called digimat.ip01 that calls digimat.ip01.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pytz
    - suds
    - requests
    - setuptools

  run:
    - python
    - pytz
    - suds
    - requests
    - setuptools

test:
  # Python imports
  imports:
    - digimat
    - digimat.ip01

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Digimat IP01'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
