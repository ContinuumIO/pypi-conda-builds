package:
  name: django-db-multitenant
  version: "0.1.2"

source:
  fn: django-db-multitenant-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-multitenant/django-db-multitenant-0.1.2.tar.gz
  md5: 1880e79ee99ea4da0d58c38cd188c9f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-multitenant = django-db-multitenant:main
    #
    # Would create an entry point called django-db-multitenant that calls django-db-multitenant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.0

  run:
    - python
    - django >=1.2.0

test:
  # Python imports
  imports:
    - db_multitenant
    - db_multitenant.cache
    - db_multitenant.db
    - db_multitenant.db.backends
    - db_multitenant.db.backends.mysql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mik3y/django-db-multitenant
  license: BSD
  summary: 'Multitenant support for Django, using one tenant per database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
