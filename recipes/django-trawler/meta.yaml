package:
  name: django-trawler
  version: "0.1a"

source:
  fn: django-trawler-0.1a.tar.gz
  url: https://pypi.python.org/packages/source/d/django-trawler/django-trawler-0.1a.tar.gz
  md5: ebf6780cf4ee14bc670132c9053d251a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-trawler = django-trawler:main
    #
    # Would create an entry point called django-trawler that calls django-trawler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - setuptools
    - django >=1.3

test:
  # Python imports
  imports:
    - trawler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/onelson/django-trawler
  license: MIT License
  summary: 'Django App for running phishing campaigns (for staff security awareness training).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
