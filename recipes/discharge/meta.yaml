package:
  name: discharge
  version: "0.3.0"

source:
  fn: Discharge-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/D/Discharge/Discharge-0.3.0.tar.gz
  md5: 43b25e0930b7bb569b8f52d80536c835
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - discharge = discharge:main
    #
    # Would create an entry point called discharge that calls discharge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - markdown
    - pygments
    - werkzeug

  run:
    - python
    - jinja2
    - markdown
    - pygments
    - werkzeug

test:
  # Python imports
  imports:
    - discharge
    - discharge.plugins
    - discharge.plugins.jinja2_filters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RichardOfWard/discharge
  license: UNKNOWN
  summary: 'A static site generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
