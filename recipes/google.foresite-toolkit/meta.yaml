package:
  name: google.foresite-toolkit
  version: "1.3"

source:
  fn: google.foresite-toolkit-1.3.tar.gz
  url: https://pypi.python.org/packages/source/g/google.foresite-toolkit/google.foresite-toolkit-1.3.tar.gz
  md5: 0e89700269ad4553d3a2e26f3914906a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google.foresite-toolkit = google.foresite-toolkit:main
    #
    # Would create an entry point called google.foresite-toolkit that calls google.foresite-toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rdflib ==4.0.1
    - lxml

  run:
    - python
    - rdflib ==4.0.1
    - lxml

test:
  # Python imports
  imports:
    - foresite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/foresite-toolkit/
  license: BSD
  summary: 'Library for constructing, parsing, manipulating and serializing OAI-ORE Resource Maps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
