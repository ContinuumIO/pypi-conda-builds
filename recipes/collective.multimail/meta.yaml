package:
  name: collective.multimail
  version: "1.0a6"

source:
  fn: collective.multimail-1.0a6.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.multimail/collective.multimail-1.0a6.tar.gz
  md5: 570c76a56441b33df3f92dceaa7f56c6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.multimail = collective.multimail:main
    #
    # Would create an entry point called collective.multimail that calls collective.multimail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - pyyaml

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - pyyaml

test:
  # Python imports
  imports:
    - collective
    - collective.multimail
    - collective.multimail.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.multimail
  license: gpl
  summary: 'Zope MailHost which routs mail to other MailHosts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
