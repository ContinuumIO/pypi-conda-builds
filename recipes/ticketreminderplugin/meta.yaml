package:
  name: ticketreminderplugin
  version: "0.1.3"

source:
  fn: TicketReminderPlugin-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/T/TicketReminderPlugin/TicketReminderPlugin-0.1.3.tar.gz
  md5: aa71d112cc44b4baab08ad7b8d5d4242
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ticketreminderplugin = ticketreminderplugin:main
    #
    # Would create an entry point called ticketreminderplugin that calls ticketreminderplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ticketreminder
    - ticketreminderplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mitar.tnode.com/
  license: AGPLv3
  summary: 'Allows to configure reminders for tickets in Trac.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
