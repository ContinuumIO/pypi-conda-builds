package:
  name: codeharvester
  version: "0.0.2"

source:
  fn: CodeHarvester-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/C/CodeHarvester/CodeHarvester-0.0.2.tar.gz
  md5: 80f28fa72610f9aa3a71b80e03284e91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codeharvester = codeharvester:main
    #
    # Would create an entry point called codeharvester that calls codeharvester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - codeharvester

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/codeharvester
  license: MIT License
  summary: 'A tool to merge an input file and all of its requirements into a single output file. Similar to Sprockets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
