package:
  name: webium
  version: "1.0.6"

source:
  fn: webium-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/w/webium/webium-1.0.6.tar.gz
  md5: 40b5f6eccbef89399f289197b881b334
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - webium = webium:main
    #
    # Would create an entry point called webium that calls webium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - selenium
    - waiting >=1.2.1

  run:
    - python
    - setuptools
    - selenium
    - waiting >=1.2.1

test:
  # Python imports
  imports:
    - webium

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wgnet/webium
  license: UNKNOWN
  summary: 'Webium is a Page Object pattern implementation library for Python (http://martinfowler.com/bliki/PageObject.html). It allows you to extend WebElement class to your custom controls like Link, Button and group them as pages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
