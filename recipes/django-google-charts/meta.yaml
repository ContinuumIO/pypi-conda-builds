package:
  name: django-google-charts
  version: "0.1.1"

source:
  fn: django-google-charts-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-google-charts/django-google-charts-0.1.1.tar.gz
  md5: b37b5c55f8666846175437b1765ff150
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-charts = django-google-charts:main
    #
    # Would create an entry point called django-google-charts that calls django-google-charts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - googlecharts
    - googlecharts.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://publishedin.com/django-google-charts/
  license: MIT License
  summary: 'Google Visualization API template tags and helpers for Django framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
