package:
  name: django-google-storage
  version: "0.3"

source:
  fn: django-google-storage-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-google-storage/django-google-storage-0.3.tar.gz
  md5: 830d8788a391058a5934f671afab42e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-storage = django-google-storage:main
    #
    # Would create an entry point called django-google-storage that calls django-google-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - boto

  run:
    - python
    - django >=1.4
    - boto

test:
  # Python imports
  imports:
    - django-google-storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-google-storage/
  license: LICENSE.txt
  summary: 'Django storage for Google Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
