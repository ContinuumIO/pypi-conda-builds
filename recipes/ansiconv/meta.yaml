package:
  name: ansiconv
  version: "1.0.0"

source:
  fn: ansiconv-1.0.0.zip
  url: https://pypi.python.org/packages/source/a/ansiconv/ansiconv-1.0.0.zip
  md5: dc36aa24edc2cee2d0bc9cd8a0e991e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansiconv = ansiconv:main
    #
    # Would create an entry point called ansiconv that calls ansiconv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ansiconv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dhrrgn/ansiconv
  license: MIT
  summary: 'Converts ANSI coded text and converts it to either plain text or to HTML.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
