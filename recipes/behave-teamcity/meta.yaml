package:
  name: behave-teamcity
  version: "0.1.23"

source:
  fn: behave-teamcity-0.1.23.tar.gz
  url: https://pypi.python.org/packages/source/b/behave-teamcity/behave-teamcity-0.1.23.tar.gz
  md5: 0cc2093d8aae106b9fd1753fcb82af2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - behave-teamcity = behave-teamcity:main
    #
    # Would create an entry point called behave-teamcity that calls behave-teamcity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - behave >=1.2.5,<=1.3
    - teamcity-messages

  run:
    - python
    - behave >=1.2.5,<=1.3
    - teamcity-messages

test:
  # Python imports
  imports:
    - behave_teamcity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iljabauer/behave-teamcity
  license: MIT License
  summary: 'TeamCity test report formatter for behave'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
