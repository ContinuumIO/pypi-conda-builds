package:
  name: django-frontendadmin
  version: "0.4"

source:
  fn: django-frontendadmin-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-frontendadmin/django-frontendadmin-0.4.tar.gz
  md5: 9eb62e385ea1e9b3863d93c633efe6f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-frontendadmin = django-frontendadmin:main
    #
    # Would create an entry point called django-frontendadmin that calls django-frontendadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - frontendadmin
    - frontendadmin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.mahner.org/
  license: BSD License
  summary: 'A a set of templatetags to allow an easy and unobstrusive way to edit model-data in the frontend of your page.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
