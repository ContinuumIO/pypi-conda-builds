package:
  name: ajenti.plugin.plugins
  version: "0.30"

source:
  fn: ajenti.plugin.plugins-0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.plugins/ajenti.plugin.plugins-0.30.tar.gz
  md5: a2d261f8e7be575330f50b1815e67d3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.plugins = ajenti.plugin.plugins:main
    #
    # Would create an entry point called ajenti.plugin.plugins that calls ajenti.plugin.plugins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core

  run:
    - python
    - aj
    - ajenti.plugin.core

test:
  # Python imports
  imports:
    - ajenti_plugin_plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
