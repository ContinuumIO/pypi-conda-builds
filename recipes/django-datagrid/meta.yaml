package:
  name: django-datagrid
  version: "1.1"

source:
  fn: django-datagrid-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-datagrid/django-datagrid-1.1.tar.gz
  md5: 94229370d8b19d987e181a7135e2a901
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-datagrid = django-datagrid:main
    #
    # Would create an entry point called django-datagrid that calls django-datagrid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datagrid
    - datagrid.templatetags
    - example
    - example.blog_grids
    - example.blogango
    - example.blogango.conf
    - example.blogango.templatetags
    - example.datagrid
    - example.datagrid.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.agiliq.com/
  license: BSD License
  summary: 'A django based Datagrid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
