package:
  name: django-tiendateca
  version: "0.0.2"

source:
  fn: django-tiendateca-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tiendateca/django-tiendateca-0.0.2.tar.gz
  md5: eea3a1003bdf8624583f3104c28f15f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tiendateca = django-tiendateca:main
    #
    # Would create an entry point called django-tiendateca that calls django-tiendateca.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tiendateca

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.tiendateca.org/
  license: GNU General Public License (GPL)
  summary: 'Online Store Software for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
