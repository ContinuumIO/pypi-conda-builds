package:
  name: masstable
  version: "0.2.1"

source:
  fn: masstable-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/masstable/masstable-0.2.1.tar.gz
  md5: 9261f38dff1a57dd6a8578e553942c75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - masstable = masstable:main
    #
    # Would create an entry point called masstable that calls masstable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas >=0.11.0

  run:
    - python
    - pandas >=0.11.0

test:
  # Python imports
  imports:
    - masstable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://elyase.github.io/masstable/
  license: MIT
  summary: 'Utilities for working with nuclear mass tables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
