package:
  name: collective.ptg.easyslider
  version: "0.5.1"

source:
  fn: collective.ptg.easyslider-0.5.1.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.easyslider/collective.ptg.easyslider-0.5.1.zip
  md5: 80338b1b1f87f1c464811c21a2accf5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.easyslider = collective.ptg.easyslider:main
    #
    # Would create an entry point called collective.ptg.easyslider that calls collective.ptg.easyslider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery >=3.2a1

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery >=3.2a1

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.easyslider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/medialog/collective.ptg.easyslider
  license: GPL
  summary: 'Easyslider gallery type for plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
