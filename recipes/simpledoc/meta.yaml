package:
  name: simpledoc
  version: "0.1"

source:
  fn: simpledoc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/simpledoc/simpledoc-0.1.tar.gz
  md5: 32582a89d692673cab28e1f89110ec3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpledoc = simpledoc:main
    #
    # Would create an entry point called simpledoc that calls simpledoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments >=1.0

  run:
    - python
    - pygments >=1.0

test:
  # Python imports
  imports:
    - simpledoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://alwaysmovefast.com
  license: MIT
  summary: 'Python document generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
