package:
  name: tailcolor
  version: "0.2.3"

source:
  fn: tailcolor-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tailcolor/tailcolor-0.2.3.tar.gz
  md5: 1b2ded1750eef6e52827d20790b7d36f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tailcolor = tailcolor:main
    #
    # Would create an entry point called tailcolor that calls tailcolor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blessings
    - fabulous
    - sarge

  run:
    - python
    - blessings
    - fabulous
    - sarge

test:
  # Python imports
  imports:
    - tailcolor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bVector/tailcolor
  license: MIT
  summary: "'tail -F' with color feedback based on how recent the line was seen"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
