package:
  name: djangocms-text-redactor
  version: "0.1.1"

source:
  fn: djangocms-text-redactor-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-text-redactor/djangocms-text-redactor-0.1.1.tar.gz
  md5: b45ce7443df6ec52fbb25635bda5685a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-text-redactor = djangocms-text-redactor:main
    #
    # Would create an entry point called djangocms-text-redactor that calls djangocms-text-redactor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - djangocms_text_redactor
    - djangocms_text_redactor.migrations
    - djangocms_text_redactor.tests
    - djangocms_text_redactor.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/djangocms-text-redactor
  license: The MIT License
  summary: 'A django-cms plugin that displays text and uses the redactor editor for editing.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
