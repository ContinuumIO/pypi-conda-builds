package:
  name: easyzone3
  version: "1.2.3"

source:
  fn: easyzone3-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/e/easyzone3/easyzone3-1.2.3.tar.gz
  md5: 512a59df336b1fe959ef59d2419e2cc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyzone3 = easyzone3:main
    #
    # Would create an entry point called easyzone3 that calls easyzone3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dnspython3

  run:
    - python
    - dnspython3

test:
  # Python imports
  imports:
    - easyzone
    - easyzone3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trdcaz/easyzone3/
  license: MIT License
  summary: 'Easy Zone - DNS Zone abstraction module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
