package:
  name: django-responsive-images
  version: "1.0.2"

source:
  fn: django-responsive-images-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-responsive-images/django-responsive-images-1.0.2.tar.gz
  md5: 93878e951f195311c5ea8a501fdd3b10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-responsive-images = django-responsive-images:main
    #
    # Would create an entry point called django-responsive-images that calls django-responsive-images.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - responsive_images
    - responsive_images.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ConvectiveSoftware/django-responsive-images
  license: MIT License
  summary: 'A Django app for resizing images and generating src and srcset values via template tags.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
