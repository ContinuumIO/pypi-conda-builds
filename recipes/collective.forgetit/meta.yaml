package:
  name: collective.forgetit
  version: "0.0.1"

source:
  fn: collective.forgetit-0.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.forgetit/collective.forgetit-0.0.1.zip
  md5: cf0fb98d2334d77360c082599af1d873
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.forgetit = collective.forgetit:main
    #
    # Would create an entry point called collective.forgetit that calls collective.forgetit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.forgetit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/starzel/collective.forgetit
  license: BSD
  summary: 'Remove uninstalled Products/package residue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
