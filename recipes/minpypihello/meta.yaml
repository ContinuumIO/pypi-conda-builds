package:
  name: minpypihello
  version: "1.5"

source:
  fn: minpypihello-1.5.tar.gz
  url: https://pypi.python.org/packages/source/m/minpypihello/minpypihello-1.5.tar.gz
  md5: 1bb08bab13b4d40d203f966aec52e4ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minpypihello = minpypihello:main
    #
    # Would create an entry point called minpypihello that calls minpypihello.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - minpypihello

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hupili/min-pypi-hello
  license: MIT
  summary: 'Minimal PyPI Package Example'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
