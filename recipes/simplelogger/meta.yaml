package:
  name: simplelogger
  version: "1.4.9"

source:
  fn: SimpleLogger-1.4.9.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleLogger/SimpleLogger-1.4.9.tar.gz
  md5: b9c17687df60b654f5c107fab4152fd3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplelogger = simplelogger:main
    #
    # Would create an entry point called simplelogger that calls simplelogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simplelogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tamul/simplelogger
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Simple Logging Utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
