package:
  name: gearbox-tools
  version: "0.0.1"

source:
  fn: gearbox-tools-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gearbox-tools/gearbox-tools-0.0.1.tar.gz
  md5: 046b2592d0af6c664b60bc274741e045
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gearbox-tools = gearbox-tools:main
    #
    # Would create an entry point called gearbox-tools that calls gearbox-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - gearboxtools
    - gearboxtools.deploy_circus
    - gearboxtools.pkgdeps
    - gearboxtools.tgbootstrap
    - gearboxtools.tgignore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/_amol_/gearbox-tools
  license: MIT
  summary: 'Bunch of utility commands for gearbox toolset'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
