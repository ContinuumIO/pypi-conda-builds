package:
  name: cubicweb-ckanpublish
  version: "0.2.0"

source:
  fn: cubicweb-ckanpublish-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-ckanpublish/cubicweb-ckanpublish-0.2.0.tar.gz
  md5: c33af11fb8e436aa8cfdad94baa18ef7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-ckanpublish = cubicweb-ckanpublish:main
    #
    # Would create an entry point called cubicweb-ckanpublish that calls cubicweb-ckanpublish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.5
    - requests

  run:
    - python
    - cubicweb >=3.19.5
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-ckanpublish
  license: LGPL
  summary: 'Publish data to a CKAN instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
