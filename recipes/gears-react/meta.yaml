package:
  name: gears-react
  version: "0.2.2"

source:
  fn: gears-react-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gears-react/gears-react-0.2.2.tar.gz
  md5: 9e86e167d2376392165d0617af30ab97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gears-react = gears-react:main
    #
    # Would create an entry point called gears-react that calls gears-react.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gears >=0.7.2

  run:
    - python
    - gears >=0.7.2

test:
  # Python imports
  imports:
    - gears_react

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mandx/gears-react
  license: ISC
  summary: 'Gears compiler for React/JSX'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
