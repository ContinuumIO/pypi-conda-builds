package:
  name: channelpack
  version: "0.3.0"

source:
  fn: channelpack-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/channelpack/channelpack-0.3.0.tar.gz
  md5: 8088574661860f611ac18c8c4b8c88ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - channelpack = channelpack:main
    #
    # Would create an entry point called channelpack that calls channelpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlrd

  run:
    - python
    - xlrd

test:
  # Python imports
  imports:
    - channelpack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tomnor/channelpack
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Package for loading, analyzing and slicing acqusition data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
