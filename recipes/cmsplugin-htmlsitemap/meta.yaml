package:
  name: cmsplugin-htmlsitemap
  version: "0.1.4"

source:
  fn: cmsplugin-htmlsitemap-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-htmlsitemap/cmsplugin-htmlsitemap-0.1.4.tar.gz
  md5: c154886439851f4367a5bc15133c9f8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-htmlsitemap = cmsplugin-htmlsitemap:main
    #
    # Would create an entry point called cmsplugin-htmlsitemap that calls cmsplugin-htmlsitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_htmlsitemap
    - cmsplugin_htmlsitemap.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/raphaa/cmsplugin-htmlsitemap
  license: BSD License
  summary: 'HTML sitemap plugin for Django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
