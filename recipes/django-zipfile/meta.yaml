package:
  name: django-zipfile
  version: "0.2.1"

source:
  fn: django-zipfile-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zipfile/django-zipfile-0.2.1.tar.gz
  md5: 7848bf543e0eece22ac2510014c8799b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zipfile = django-zipfile:main
    #
    # Would create an entry point called django-zipfile that calls django-zipfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - six

  run:
    - python
    - django >=1.3
    - six

test:
  # Python imports
  imports:
    - django_zipfile
    - django_zipfile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fcurella/django-zipfile
  license: MIT License
  summary: 'A ZipFile subclass that accepts Django Templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
