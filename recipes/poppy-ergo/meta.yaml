package:
  name: poppy-ergo
  version: "1.0rc2"

source:
  fn: poppy-ergo-1.0rc2.tar.gz
  url: https://pypi.python.org/packages/source/p/poppy-ergo/poppy-ergo-1.0rc2.tar.gz
  md5: 37f9ad6b2f47c7be87d45b4b508d4a98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - poppy-ergo = poppy-ergo:main
    #
    # Would create an entry point called poppy-ergo that calls poppy-ergo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - poppy-creature

  run:
    - python
    - poppy-creature

test:
  # Python imports
  imports:
    - poppy_ergo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/poppy-project/poppy-ergo
  license: GNU GENERAL PUBLIC LICENSE Version 3
  summary: ' Poppy Ergo Software Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
