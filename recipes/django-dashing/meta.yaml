package:
  name: django-dashing
  version: "0.2.5"

source:
  fn: django-dashing-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dashing/django-dashing-0.2.5.tar.gz
  md5: c88c73ed426de8f842457090364bed38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dashing = django-dashing:main
    #
    # Would create an entry point called django-dashing that calls django-dashing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dashing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/talpor/django-dashing/
  license: MIT License
  summary: 'A simple Django dashboard app to visualize interesting data about your project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
