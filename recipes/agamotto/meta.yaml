package:
  name: agamotto
  version: "0.5.1"

source:
  fn: agamotto-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/a/agamotto/agamotto-0.5.1.tar.gz
  md5: b6b44fd3cb00a955d3362a50bc9b2b90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agamotto = agamotto:main
    #
    # Would create an entry point called agamotto that calls agamotto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - unittest2

  run:
    - python
    - requests
    - unittest2

test:
  # Python imports
  imports:
    - agamotto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/groksolutions/agamotto
  license: Apache Software License
  summary: 'Agamotto is a module that provides helper functions for testing the configuration of a running server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
