package:
  name: collective.pfg.showrequest
  version: "0.6"

source:
  fn: collective.pfg.showrequest-0.6.zip
  url: https://pypi.python.org/packages/source/c/collective.pfg.showrequest/collective.pfg.showrequest-0.6.zip
  md5: be2e0223499128af9544ec7670dbf6fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pfg.showrequest = collective.pfg.showrequest:main
    #
    # Would create an entry point called collective.pfg.showrequest that calls collective.pfg.showrequest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.ploneformgen
    - setuptools

  run:
    - python
    - setuptools
    - products.ploneformgen
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.pfg
    - collective.pfg.showrequest
    - collective.pfg.showrequest.browser
    - collective.pfg.showrequest.interfaces
    - collective.pfg.showrequest.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/taito/collective.pfg.showrequest
  license: BSD License
  summary: 'Show previous form input values in viewlet for PloneFormGen.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
