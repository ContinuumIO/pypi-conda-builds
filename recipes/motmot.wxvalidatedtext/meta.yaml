package:
  name: motmot.wxvalidatedtext
  version: "0.5.3"

source:
  fn: motmot.wxvalidatedtext-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/m/motmot.wxvalidatedtext/motmot.wxvalidatedtext-0.5.3.tar.gz
  md5: e60ede71844fc8a7e13817d6374e5a9b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motmot.wxvalidatedtext = motmot.wxvalidatedtext:main
    #
    # Would create an entry point called motmot.wxvalidatedtext that calls motmot.wxvalidatedtext.main()

    - wxvalidatedtext_demo = motmot.wxvalidatedtext.demo:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - motmot
    - motmot.wxvalidatedtext

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - wxvalidatedtext_demo --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.astraw.com/projects/motmot
  license: BSD
  summary: 'validated integer/float text entry field for wxPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
