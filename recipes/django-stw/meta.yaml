package:
  name: django-stw
  version: "0.3.0"

source:
  fn: django-stw-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stw/django-stw-0.3.0.tar.gz
  md5: c2ff35f729fe50ebc6258c12ad601a6d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stw = django-stw:main
    #
    # Would create an entry point called django-stw that calls django-stw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - stw
    - stw.templatetags
    - stw.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/saschwarz/django-stw
  license: BSD License
  summary: 'This application provides templatetags for simplying using Shrink The Web PagePix'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
