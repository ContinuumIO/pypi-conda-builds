package:
  name: oraide
  version: "0.4"

source:
  fn: oraide-0.4.tar.gz
  url: https://pypi.python.org/packages/source/o/oraide/oraide-0.4.tar.gz
  md5: e7fe6cd38bd1b388a469cf4f935de307
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oraide = oraide:main
    #
    # Would create an entry point called oraide that calls oraide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oraide

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ddbeck/oraide
  license: BSD License
  summary: 'A library to help presenters demonstrate terminal sessions hands-free.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
