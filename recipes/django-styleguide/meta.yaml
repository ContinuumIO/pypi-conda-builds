package:
  name: django-styleguide
  version: "0.1.20"

source:
  fn: django-styleguide-0.1.20.tar.gz
  url: https://pypi.python.org/packages/source/d/django-styleguide/django-styleguide-0.1.20.tar.gz
  md5: 951057f3bce253f05d4d344f3c9ca247
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-styleguide = django-styleguide:main
    #
    # Would create an entry point called django-styleguide that calls django-styleguide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - ordereddict >=1.1
    - factory-boy ==2.4.1

  run:
    - python
    - django >=1.4
    - ordereddict >=1.1
    - factory-boy ==2.4.1

test:
  # Python imports
  imports:
    - styleguide
    - styleguide_mock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-styleguide/
  license: LICENSE
  summary: 'Styleguide for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
