package:
  name: tambur
  version: "0.1.2.3"

source:
  fn: tambur-0.1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tambur/tambur-0.1.2.3.tar.gz
  md5: 37ba648a6bbddd3d86040b1deadc4fc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tambur = tambur:main
    #
    # Would create an entry point called tambur that calls tambur.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-oauthlib >=0.2.0
    - requests >=1.1.0

  run:
    - python
    - requests-oauthlib >=0.2.0
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - tambur

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tamburio/python-tambur
  license: BSD License
  summary: 'Tambur.io Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
