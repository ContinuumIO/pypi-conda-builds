package:
  name: pygps
  version: "1.3.2"

source:
  fn: pyGPs-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyGPs/pyGPs-1.3.2.tar.gz
  md5: 0e73d512f4d8edda389c76d4cb4aa727
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygps = pygps:main
    #
    # Would create an entry point called pygps that calls pygps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy >=0.13
    - matplotlib

  run:
    - python
    - numpy
    - scipy >=0.13
    - matplotlib

test:
  # Python imports
  imports:
    - pyGPs
    - pyGPs.Core
    - pyGPs.GraphExtensions
    - pyGPs.Optimization
    - pyGPs.Validation
    - pygps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marionmari/pyGPs
  license: COPYRIGHT.txt
  summary: 'Gaussian Processes for Regression and Classification'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
