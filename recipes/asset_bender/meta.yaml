package:
  name: asset_bender
  version: "0.1.21"

source:
  fn: asset_bender-0.1.21.tar.gz
  url: https://pypi.python.org/packages/source/a/asset_bender/asset_bender-0.1.21.tar.gz
  md5: e73e2ec68cd859b99fa2ff6507fa5ac7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asset_bender = asset_bender:main
    #
    # Would create an entry point called asset_bender that calls asset_bender.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0
    - hscacheutils >=0.1.8
    - requests >=1.1.0

  run:
    - python
    - django >=1.3.0
    - hscacheutils >=0.1.8
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - asset_bender
    - asset_bender.templatetags
    - asset_bender.templatetags.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HubSpot/asset_bender_django
  license: LICENSE.txt
  summary: 'A django runtime implementation for Asset Bender'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
