package:
  name: expecter
  version: "0.3.0"

source:
  fn: expecter-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/e/expecter/expecter-0.3.0.tar.gz
  md5: cda4395e484d098b2653069125553b24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expecter = expecter:main
    #
    # Would create an entry point called expecter that calls expecter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - expecter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/garybernhardt/expecter
  license: BSD
  summary: 'Expecter Gadget, a better expectation (assertion) library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
