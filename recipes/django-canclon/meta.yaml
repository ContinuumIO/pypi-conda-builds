package:
  name: django-canclon
  version: "0.2.3"

source:
  fn: django-canclon-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-canclon/django-canclon-0.2.3.tar.gz
  md5: d28f141ed9291a2e050dc23ee76d013e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-canclon = django-canclon:main
    #
    # Would create an entry point called django-canclon that calls django-canclon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - canclon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jleeothon/canclon
  license: MIT License
  summary: 'Template name resolution for Django class-based views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
