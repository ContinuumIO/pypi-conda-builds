package:
  name: izug.seantis.dir.events
  version: "1.0rc2"

source:
  fn: izug.seantis.dir.events-1.0rc2.tar.gz
  url: https://pypi.python.org/packages/source/i/izug.seantis.dir.events/izug.seantis.dir.events-1.0rc2.tar.gz
  md5: 5a02e5e2c3d825a82b2288eaab88746c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - izug.seantis.dir.events = izug.seantis.dir.events:main
    #
    # Would create an entry point called izug.seantis.dir.events that calls izug.seantis.dir.events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - seantis.dir.events >=1.0rc1
    - izug.basetheme
    - collective.geo.zugmap
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - seantis.dir.events >=1.0rc1
    - izug.basetheme
    - collective.geo.zugmap
    - plone.app.theming

test:
  # Python imports
  imports:
    - izug
    - izug.seantis
    - izug.seantis.dir
    - izug.seantis.dir.events

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/seantis/izug.seantis.dir.events
  license: GPL
  summary: 'Integration of seantis.dir.events into izug.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
