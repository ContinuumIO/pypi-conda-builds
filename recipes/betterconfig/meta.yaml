package:
  name: betterconfig
  version: "0.3"

source:
  fn: betterconfig-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/betterconfig/betterconfig-0.3.tar.gz
  md5: ae662044790e622dea1c7b1b472a537f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betterconfig = betterconfig:main
    #
    # Would create an entry point called betterconfig that calls betterconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - betterconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/axialmarket/betterconfig
  license: 3-BSD
  summary: 'betterconfig -- A Better Config for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
