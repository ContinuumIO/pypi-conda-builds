package:
  name: uritemplate
  version: "0.6"

source:
  fn: uritemplate-0.6.tar.gz
  url: https://pypi.python.org/packages/source/u/uritemplate/uritemplate-0.6.tar.gz
  md5: ecfc1ea8d62c7f2b47aad625afae6173
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uritemplate = uritemplate:main
    #
    # Would create an entry point called uritemplate that calls uritemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - simplejson >=2.5.0

  run:
    - python
    - simplejson >=2.5.0

test:
  # Python imports
  imports:
    - uritemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/uri-templates/uritemplate-py/
  license: Apache Software License
  summary: 'URI Templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
