package:
  name: django-txtlocal
  version: "1.1.3"

source:
  fn: django-txtlocal-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-txtlocal/django-txtlocal-1.1.3.tar.gz
  md5: 8982203d6286d720da87622c9348facb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-txtlocal = django-txtlocal:main
    #
    # Would create an entry point called django-txtlocal that calls django-txtlocal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3

  run:
    - python
    - requests >=1.2.3

test:
  # Python imports
  imports:
    - txtlocal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/django-txtlocal/
  license: UNKNOWN
  summary: 'App for sending and receiving SMS messages via http://www.textlocal.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
