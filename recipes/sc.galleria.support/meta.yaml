package:
  name: sc.galleria.support
  version: "1.0"

source:
  fn: sc.galleria.support-1.0.zip
  url: https://pypi.python.org/packages/source/s/sc.galleria.support/sc.galleria.support-1.0.zip
  md5: 6d5e42da51a0e5e34a7b2f6be193dfe1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.galleria.support = sc.galleria.support:main
    #
    # Would create an entry point called sc.galleria.support that calls sc.galleria.support.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - collective.js.galleria

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - collective.js.galleria

test:
  # Python imports
  imports:
    - sc
    - sc.galleria
    - sc.galleria.support
    - sc.galleria.support.browser
    - sc.galleria.support.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br
  license: GNU General Public License (GPL)
  summary: 'Simple gallery for Plone based in http://galleria.io'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
