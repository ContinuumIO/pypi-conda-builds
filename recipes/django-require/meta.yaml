package:
  name: django-require
  version: "1.0.7"

source:
  fn: django-require-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-require/django-require-1.0.7.tar.gz
  md5: 0198f5323c8b5a79a3b21921c1234f85
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-require = django-require:main
    #
    # Would create an entry point called django-require that calls django-require.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - require
    - require.management
    - require.management.commands
    - require.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/etianen/django-require
  license: BSD License
  summary: 'A Django staticfiles post-processor for optimizing with RequireJS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
