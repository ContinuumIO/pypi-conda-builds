package:
  name: ftw.tagging
  version: "1.1.1"

source:
  fn: ftw.tagging-1.1.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.tagging/ftw.tagging-1.1.1.zip
  md5: 0b8ece367074cb287012e5a597b4fa9c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.tagging = ftw.tagging:main
    #
    # Would create an entry point called ftw.tagging that calls ftw.tagging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope2
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - archetypes.schemaextender
    - products.addremovewidget
    - products.archetypes
    - products.cmfcore
    - products.cmfplone
    - plone.app.layout
    - plone.app.portlets
    - plone.portlets
    - plone.theme

  run:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope2
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - archetypes.schemaextender
    - products.addremovewidget
    - products.archetypes
    - products.cmfcore
    - products.cmfplone
    - plone.app.layout
    - plone.app.portlets
    - plone.portlets
    - plone.theme

test:
  # Python imports
  imports:
    - ftw
    - ftw.tagging
    - ftw.tagging.browser
    - ftw.tagging.browser.viewlets
    - ftw.tagging.browser.views
    - ftw.tagging.extenders
    - ftw.tagging.interfaces
    - ftw.tagging.portlets
    - ftw.tagging.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.tagging
  license: GPL2
  summary: 'Tagging add-on for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
