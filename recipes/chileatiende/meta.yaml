package:
  name: chileatiende
  version: "0.9"

source:
  fn: chileatiende-0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/chileatiende/chileatiende-0.9.tar.gz
  md5: 9944cbb6e06e8c5655a6e5f8f2198dd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chileatiende = chileatiende:main
    #
    # Would create an entry point called chileatiende that calls chileatiende.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chileatiende
    - demos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'API cliente escrito en python que consume los servicios de ChileAtiende'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
