package:
  name: nbastats
  version: "1.1.0"

source:
  fn: nbastats-1.1.0.zip
  url: https://pypi.python.org/packages/source/n/nbastats/nbastats-1.1.0.zip
  md5: f8ae2f9c3d8a9bd79da860e0308c8ea9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nbastats = nbastats:main
    #
    # Would create an entry point called nbastats that calls nbastats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas >=0.13
    - requests >=2.2.1

  run:
    - python
    - pandas >=0.13
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - nbastats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyfay/nbadatacollection
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
