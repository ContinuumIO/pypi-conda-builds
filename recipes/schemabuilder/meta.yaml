package:
  name: schemabuilder
  version: "0.3.0"

source:
  fn: schemabuilder-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/schemabuilder/schemabuilder-0.3.0.tar.gz
  md5: 59c8205fe56eb97f5a2b936f0c635ed6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - schemabuilder = schemabuilder:main
    #
    # Would create an entry point called schemabuilder that calls schemabuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema

  run:
    - python
    - jsonschema

test:
  # Python imports
  imports:
    - schemabuilder
    - schemabuilder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dinoboff/schemabuilder
  license: MIT License
  summary: 'JSON schema definition helpers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
