package:
  name: ladon
  version: "0.9.30"

source:
  fn: ladon-0.9.30.tar.gz
  url: https://pypi.python.org/packages/source/l/ladon/ladon-0.9.30.tar.gz
  md5: 7f3b9180aba5fd06786a40dccdd3be23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ladon = ladon:main
    #
    # Would create an entry point called ladon that calls ladon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - sphinx
    - sphinx_bootstrap_theme

  run:
    - python
    - jinja2
    - sphinx
    - sphinx_bootstrap_theme

test:
  # Python imports
  imports:
    - chardet_py2
    - ladon
    - ladon.clients
    - ladon.exceptions
    - ladon.interfaces
    - ladon.ladonizer
    - ladon.server
    - ladon.server.default
    - ladon.tools
    - ladon.types

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ladonize.org
  license: LGPL3
  summary: 'Serve your python methods to several web service interfaces at once, including JSON-WSP, SOAP and JSON-RPC.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
