package:
  name: collective.btrees
  version: "1.1"

source:
  fn: collective.btrees-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.btrees/collective.btrees-1.1.zip
  md5: f26a1fb2adb9012898cdbf0e7de0eeae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.btrees = collective.btrees:main
    #
    # Would create an entry point called collective.btrees that calls collective.btrees.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.btrees
    - collective.btrees.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.btrees
  license: GPL
  summary: 'Add btrees in annotations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
