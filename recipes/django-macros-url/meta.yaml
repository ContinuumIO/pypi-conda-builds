package:
  name: django-macros-url
  version: "0.1.7"

source:
  fn: django-macros-url-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-macros-url/django-macros-url-0.1.7.tar.gz
  md5: 538200b1fbf8949a35f0b9d4784765ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-macros-url = django-macros-url:main
    #
    # Would create an entry point called django-macros-url that calls django-macros-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - macrosurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phpdude/django-macros-url
  license: MIT License
  summary: 'Macros Url library for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
