package:
  name: django-pandas
  version: "0.2.2"

source:
  fn: django-pandas-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pandas/django-pandas-0.2.2.tar.gz
  md5: 48f935cb11b4108daa05473da6a735a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pandas = django-pandas:main
    #
    # Would create an entry point called django-pandas that calls django-pandas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - django-model-utils >=1.4.0
    - pandas >=0.12.0

  run:
    - python
    - django >=1.4.2
    - django-model-utils >=1.4.0
    - pandas >=0.12.0

test:
  # Python imports
  imports:
    - django_pandas
    - django_pandas.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisdev/django-pandas/
  license: BSD License
  summary: 'Tools for working with pydata.pandas in your Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
