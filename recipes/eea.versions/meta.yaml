package:
  name: eea.versions
  version: "7.7"

source:
  fn: eea.versions-7.7.zip
  url: https://pypi.python.org/packages/source/e/eea.versions/eea.versions-7.7.zip
  md5: 7e58b1f100ffcc0e5b5fba79ce931a07
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eea.versions = eea.versions:main
    #
    # Would create an entry point called eea.versions that calls eea.versions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - eea
    - eea.versions
    - eea.versions.browser
    - eea.versions.tests
    - eea.versions.tests.sample
    - eea.versions.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://eea.github.com/docs/eea.versions
  license: GNU General Public License (GPL) or Mozilla Public License 1.0 (MPL)
  summary: 'EEA versions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
