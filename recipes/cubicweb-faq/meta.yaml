package:
  name: cubicweb-faq
  version: "0.2.0"

source:
  fn: cubicweb-faq-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-faq/cubicweb-faq-0.2.0.tar.gz
  md5: 52c2a92b0e0c19de1eec472ddf942c26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-faq = cubicweb-faq:main
    #
    # Would create an entry point called cubicweb-faq that calls cubicweb-faq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.4

  run:
    - python
    - cubicweb >=3.16.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-faq
  license: LGPL
  summary: 'Frequently Asked Questions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
