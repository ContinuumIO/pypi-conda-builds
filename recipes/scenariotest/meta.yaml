package:
  name: scenariotest
  version: "1.0"

source:
  fn: scenariotest-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/scenariotest/scenariotest-1.0.tar.gz
  md5: b6086a851fb51bec1564b83d5523afe3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - scenariotest = scenariotest:main
    #
    # Would create an entry point called scenariotest that calls scenariotest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - six >=1.3.0

  run:
    - python
    - six >=1.3.0

test:
  # Python imports
  imports:
    - scenariotest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/maaku/scenariotest/
  license: MIT License
  summary: u'Combines a prototypical test case with a list of dictionaries (each a dict of keyword arguments) and transforms the two into a series of unittest-compatible test cases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
