package:
  name: djangorestframework-jsonp
  version: "1.0.0"

source:
  fn: djangorestframework-jsonp-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-jsonp/djangorestframework-jsonp-1.0.0.tar.gz
  md5: bc72b47722aa16a5d485aac499fcfbc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-jsonp = djangorestframework-jsonp:main
    #
    # Would create an entry point called djangorestframework-jsonp that calls djangorestframework-jsonp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rest_framework_jsonp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpadilla/django-rest-framework-jsonp
  license: MIT License
  summary: 'JSONP support for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
