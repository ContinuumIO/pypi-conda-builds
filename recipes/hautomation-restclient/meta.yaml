package:
  name: hautomation-restclient
  version: "0.3.2"

source:
  fn: hautomation-restclient-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hautomation-restclient/hautomation-restclient-0.3.2.tar.gz
  md5: 9156c7370053cb40dbfbdea8f3616e1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hautomation-restclient = hautomation-restclient:main
    #
    # Would create an entry point called hautomation-restclient that calls hautomation-restclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.2.0
    - simplejson ==2.6.2

  run:
    - python
    - requests ==1.2.0
    - simplejson ==2.6.2

test:
  # Python imports
  imports:
    - hautomation_restclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.digitalhigh.es
  license: BSD License
  summary: 'Module for sending home automation commands and device management requests to django-hautomation REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
