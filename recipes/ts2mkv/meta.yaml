package:
  name: ts2mkv
  version: "2.0.1"

source:
  fn: ts2mkv-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/ts2mkv/ts2mkv-2.0.1.tar.gz
  md5: aeb57f1ac0e991f2893a09e964e103eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ts2mkv = ts2mkv:main
    #
    # Would create an entry point called ts2mkv that calls ts2mkv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ts2mkv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ts2mkv/ts2mkv
  license: MIT License
  summary: 'Converts DVB Transport Stream (.ts) files into Matroska (.mkv)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
