package:
  name: django-dictmessages
  version: "1.0.0"

source:
  fn: django-dictmessages-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dictmessages/django-dictmessages-1.0.0.tar.gz
  md5: 9bdaf80f9b422cbf81e906a24be775f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dictmessages = django-dictmessages:main
    #
    # Would create an entry point called django-dictmessages that calls django-dictmessages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - dictmessages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mhurt/django-dictmessages
  license: MIT License
  summary: 'django.contrib.messages wrapper allowing pre-rendering of message dictionaries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
