package:
  name: bottle-debugtoolbar
  version: "0.5"

source:
  fn: Bottle-DebugToolbar-0.5.tar.gz
  url: https://pypi.python.org/packages/source/B/Bottle-DebugToolbar/Bottle-DebugToolbar-0.5.tar.gz
  md5: a682e41491972ca41214bfa70ca5be9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-debugtoolbar = bottle-debugtoolbar:main
    #
    # Would create an entry point called bottle-debugtoolbar that calls bottle-debugtoolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.11.2
    - jinja2 >=2.6
    - six

  run:
    - python
    - bottle >=0.11.2
    - jinja2 >=2.6
    - six

test:
  # Python imports
  imports:
    - bottle_debugtoolbar
    - bottle_debugtoolbar.panels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sramana/bottle-debugtoolbar
  license: BSD License
  summary: 'A port of the Django Debug Toolbar to Bottle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
