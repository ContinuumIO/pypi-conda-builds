package:
  name: megrok.resourceviewlet
  version: "0.2"

source:
  fn: megrok.resourceviewlet-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.resourceviewlet/megrok.resourceviewlet-0.2.tar.gz
  md5: fe77fa2746a1c3e236b5baa26e9783d7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.resourceviewlet = megrok.resourceviewlet:main
    #
    # Would create an entry point called megrok.resourceviewlet that calls megrok.resourceviewlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - fanstatic
    - grokcore.viewlet
    - setuptools
    - zope.schema
    - zope.viewlet

  run:
    - python
    - fanstatic
    - grokcore.viewlet
    - setuptools
    - zope.schema
    - zope.viewlet

test:
  # Python imports
  imports:
    - megrok
    - megrok.resourceviewlet
    - megrok.resourceviewlet.ftests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/megrok.resourceviewlet
  license: Zope Public License
  summary: 'Grok components to include resources.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
