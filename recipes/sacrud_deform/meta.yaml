package:
  name: sacrud_deform
  version: "0.0.7.post1"

source:
  fn: sacrud_deform-0.0.7.post1.tar.gz
  url: https://pypi.python.org/packages/source/s/sacrud_deform/sacrud_deform-0.0.7.post1.tar.gz
  md5: 635e9f10254fa67ac2bd4f80a016edc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sacrud_deform = sacrud_deform:main
    #
    # Would create an entry point called sacrud_deform that calls sacrud_deform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sacrud >=0.2.4
    - colander >=1.0b1
    - deform >=2.0a2
    - colanderalchemy >=0.3.2

  run:
    - python
    - sacrud >=0.2.4
    - colander >=1.0b1
    - deform >=2.0a2
    - colanderalchemy >=0.3.2

test:
  # Python imports
  imports:
    - sacrud_deform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ITCase/sacrud_deform/
  license: MIT License
  summary: 'Form generator for SQLAlchemy models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
