package:
  name: django-validate-on-save
  version: "1.1.0"

source:
  fn: django-validate-on-save-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-validate-on-save/django-validate-on-save-1.1.0.tar.gz
  md5: e85a5ec6fd466d2e542096d5b17df477
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-validate-on-save = django-validate-on-save:main
    #
    # Would create an entry point called django-validate-on-save that calls django-validate-on-save.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - validate_on_save

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brightinteractive/django-validate-on-save/
  license: BSD
  summary: 'Automatically call full_clean() on models during save() to prevent invalid data being saved.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
