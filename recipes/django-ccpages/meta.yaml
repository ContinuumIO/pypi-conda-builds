package:
  name: django-ccpages
  version: "1.8.0"

source:
  fn: django-ccpages-1.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ccpages/django-ccpages-1.8.0.tar.gz
  md5: 5b5c1a19004649bea741c32656d280fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ccpages = django-ccpages:main
    #
    # Would create an entry point called django-ccpages that calls django-ccpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-mptt
    - django-ccthumbs
    - django-writingfield
    - markdown2

  run:
    - python
    - django-mptt
    - django-ccthumbs
    - django-writingfield
    - markdown2

test:
  # Python imports
  imports:
    - ccpages
    - ccpages.migrations
    - ccpages.templatetags
    - ccpages.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/designcc/django-ccpages
  license: BSD 3 Clause
  summary: 'A lightweight pages application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
