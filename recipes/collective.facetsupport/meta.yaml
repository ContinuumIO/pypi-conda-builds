package:
  name: collective.facetsupport
  version: "0.1a"

source:
  fn: collective.facetsupport-0.1a.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.facetsupport/collective.facetsupport-0.1a.tar.gz
  md5: 8470b0061e9bf90323f30e78efa78efa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.facetsupport = collective.facetsupport:main
    #
    # Would create an entry point called collective.facetsupport that calls collective.facetsupport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.facetsupport
    - collective.facetsupport.browser
    - collective.facetsupport.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.facetsupport
  license: GNU General Public License (GPL)
  summary: 'Tools for generating facets for content in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
