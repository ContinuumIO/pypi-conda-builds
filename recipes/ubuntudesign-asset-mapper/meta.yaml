package:
  name: ubuntudesign-asset-mapper
  version: "0.7"

source:
  fn: ubuntudesign-asset-mapper-0.7.tar.gz
  url: https://pypi.python.org/packages/source/u/ubuntudesign-asset-mapper/ubuntudesign-asset-mapper-0.7.tar.gz
  md5: 253bed089ecafc22f5e2c7d5069ab4db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ubuntudesign-asset-mapper = ubuntudesign-asset-mapper:main
    #
    # Would create an entry point called ubuntudesign-asset-mapper that calls ubuntudesign-asset-mapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0

  run:
    - python
    - requests >=2.0

test:
  # Python imports
  imports:
    - ubuntudesign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ubuntudesign/asset-mapper
  license: UNKNOWN
  summary: 'A mapping class for using the Ubuntu asset server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
