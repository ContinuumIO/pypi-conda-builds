package:
  name: cachecleaner
  version: "0.2"

source:
  fn: cachecleaner-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cachecleaner/cachecleaner-0.2.tar.gz
  md5: 1e29f4189988e8b42fa150184397bf94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cachecleaner = cachecleaner:main
    #
    # Would create an entry point called cachecleaner that calls cachecleaner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tqdm

  run:
    - python
    - tqdm

test:
  # Python imports
  imports:
    - cachecleaner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/homm/cachecleaner
  license: UNKNOWN
  summary: 'Keeps dir size in given capacity.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
