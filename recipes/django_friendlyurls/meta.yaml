package:
  name: django_friendlyurls
  version: "0.1.1"

source:
  fn: django_friendlyurls-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_friendlyurls/django_friendlyurls-0.1.1.tar.gz
  md5: 15417c42b71436a09261eaea9e61feb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_friendlyurls = django_friendlyurls:main
    #
    # Would create an entry point called django_friendlyurls that calls django_friendlyurls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_friendlyurls
    - friendlyurls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tachang/django_friendlyurls/
  license: BSD License
  summary: 'Use friendly urls with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
