package:
  name: redsolutioncms.template-classic
  version: "0.1.0"

source:
  fn: redsolutioncms.template-classic-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.template-classic/redsolutioncms.template-classic-0.1.0.tar.gz
  md5: 8b6e5da02fccb4358e02436c3e4f4eb8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.template-classic = redsolutioncms.template-classic:main
    #
    # Would create an entry point called redsolutioncms.template-classic that calls redsolutioncms.template-classic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - classic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/redsolution-template-classic
  license: cc by
  summary: 'Default template fot RedsoultionCMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
