package:
  name: django-taggee
  version: "0.2"

source:
  fn: django-taggee-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-taggee/django-taggee-0.2.tar.gz
  md5: 7ad0c90de0b9c8d9db19c4e4b679e291
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-taggee = django-taggee:main
    #
    # Would create an entry point called django-taggee that calls django-taggee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - taggee

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/koorgoo/django-taggee/
  license: MIT
  summary: 'Tag field for Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
