package:
  name: collective.checktranslated
  version: "1.0.1"

source:
  fn: collective.checktranslated-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.checktranslated/collective.checktranslated-1.0.1.zip
  md5: fab86128f8524392f4df42d11af1fc41
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.checktranslated = collective.checktranslated:main
    #
    # Would create an entry point called collective.checktranslated that calls collective.checktranslated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.linguaplone
    - collective.jekyll

  run:
    - python
    - setuptools
    - setuptools
    - products.linguaplone
    - collective.jekyll

test:
  # Python imports
  imports:
    - collective
    - collective.checktranslated
    - collective.checktranslated.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CIRB/collective.checktranslated
  license: GPL
  summary: 'This products make a table with all your content and their translation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
