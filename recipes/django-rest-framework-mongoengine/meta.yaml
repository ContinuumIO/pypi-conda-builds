package:
  name: django-rest-framework-mongoengine
  version: "2.0.2"

source:
  fn: django-rest-framework-mongoengine-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-framework-mongoengine/django-rest-framework-mongoengine-2.0.2.tar.gz
  md5: f04428d2c40f99a342e2f881a99bb34b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-framework-mongoengine = django-rest-framework-mongoengine:main
    #
    # Would create an entry point called django-rest-framework-mongoengine that calls django-rest-framework-mongoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - rest_framework_mongoengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/umutbozkurt/django-rest-framework-mongoengine
  license: MIT License
  summary: 'MongoEngine support for Django Rest Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
