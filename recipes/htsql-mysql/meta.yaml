package:
  name: htsql-mysql
  version: "2.3.3"

source:
  fn: HTSQL-MYSQL-2.3.3.tar.gz
  url: https://pypi.python.org/packages/source/H/HTSQL-MYSQL/HTSQL-MYSQL-2.3.3.tar.gz
  md5: ea78a77f75e6653ff2e61ac18836508b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htsql-mysql = htsql-mysql:main
    #
    # Would create an entry point called htsql-mysql that calls htsql-mysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - htsql
    - mysql-python

  run:
    - python
    - setuptools
    - htsql
    - mysql-python

test:
  # Python imports
  imports:
    - htsql_mysql
    - htsql_mysql.core
    - htsql_mysql.core.tr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://htsql.org/
  license: AGPLv3 or Permissive for use with Open Source databases
  summary: 'A Database Query Language (MySQL backend)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
