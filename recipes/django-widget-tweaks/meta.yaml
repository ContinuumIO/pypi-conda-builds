package:
  name: django-widget-tweaks
  version: "1.3"

source:
  fn: django-widget-tweaks-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-widget-tweaks/django-widget-tweaks-1.3.tar.gz
  md5: b8c5e7458e4421496c308973c15396cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-widget-tweaks = django-widget-tweaks:main
    #
    # Would create an entry point called django-widget-tweaks that calls django-widget-tweaks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - widget_tweaks
    - widget_tweaks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kmike/django-widget-tweaks/
  license: MIT License
  summary: 'Tweak the form field rendering in templates, not in python-level form definitions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
