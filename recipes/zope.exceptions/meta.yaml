package:
  name: zope.exceptions
  version: "4.0.7"

source:
  fn: zope.exceptions-4.0.7.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.exceptions/zope.exceptions-4.0.7.tar.gz
  md5: 93daa7463373d755441d2b034709acac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.exceptions = zope.exceptions:main
    #
    # Would create an entry point called zope.exceptions that calls zope.exceptions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - sphinx
    - repoze.sphinx.autointerface
    - nose
    - coverage

  run:
    - python
    - setuptools
    - zope.interface
    - sphinx
    - repoze.sphinx.autointerface
    - nose
    - coverage

test:
  # Python imports
  imports:
    - zope
    - zope.exceptions
    - zope.exceptions.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

requires:
    - zope.testrunner
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.exceptions
  license: Zope Public License
  summary: 'Zope Exceptions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
