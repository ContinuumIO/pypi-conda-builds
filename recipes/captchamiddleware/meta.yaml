package:
  name: captchamiddleware
  version: "0.2"

source:
  fn: CAPTCHAmiddleware-0.2.tar.gz
  url: https://pypi.python.org/packages/source/C/CAPTCHAmiddleware/CAPTCHAmiddleware-0.2.tar.gz
  md5: e791142443f43fd2bdcf7338c075cd71
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - captchamiddleware = captchamiddleware:main
    #
    # Would create an entry point called captchamiddleware that calls captchamiddleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxmlmiddleware
    - skimpygimpy

  run:
    - python
    - setuptools
    - lxmlmiddleware
    - skimpygimpy

test:
  # Python imports
  imports:
    - captchamiddleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org/hg
  license: GPL
  summary: 'put CAPTCHAs on forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
