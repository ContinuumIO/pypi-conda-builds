package:
  name: django-sld
  version: "1.0.7"

source:
  fn: django-sld-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sld/django-sld-1.0.7.tar.gz
  md5: 5db76ec68911fb730c465b16aeb0f278
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sld = django-sld:main
    #
    # Would create an entry point called django-sld that calls django-sld.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djsld
    - djsld.tests
    - djsld.tests.djsld-test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/azavea/django-sld/
  license: Apache Software License
  summary: 'A simple django library that generates SLD documents from geographic models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
