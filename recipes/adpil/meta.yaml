package:
  name: adpil
  version: "1.02"

source:
  fn: adpil-1.02.tar.gz
  url: https://pypi.python.org/packages/source/a/adpil/adpil-1.02.tar.gz
  md5: 64140ae3da3a6953b631823c334da435
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adpil = adpil:main
    #
    # Would create an entry point called adpil that calls adpil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adpil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robertoalotufo/adpil
  license: BSD License
  summary: 'Interface between Adessowiki and PIL.Used by ia636 and ia870 toolboxes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
