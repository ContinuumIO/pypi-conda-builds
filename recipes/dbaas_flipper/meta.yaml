package:
  name: dbaas_flipper
  version: "0.0.9"

source:
  fn: dbaas_flipper-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/dbaas_flipper/dbaas_flipper-0.0.9.tar.gz
  md5: 29b40847d32905a427036f8e4b43ed25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbaas_flipper = dbaas_flipper:main
    #
    # Would create an entry point called dbaas_flipper that calls dbaas_flipper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbaas_flipper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/felippemr/dbaas-flipper
  license: BSD License
  summary: 'Flipper integration 4 DBaaS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
