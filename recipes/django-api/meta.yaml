package:
  name: django-api
  version: "0.1.3"

source:
  fn: django-api-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-api/django-api-0.1.3.tar.gz
  md5: 4d4c805b4a298ede91aba0777f524dcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-api = django-api:main
    #
    # Would create an entry point called django-api that calls django-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bipsandbytes/django-api
  license: BSD License
  summary: 'Specify and validate your Django APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
