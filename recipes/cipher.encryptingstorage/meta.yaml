package:
  name: cipher.encryptingstorage
  version: "1.0.0"

source:
  fn: cipher.encryptingstorage-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cipher.encryptingstorage/cipher.encryptingstorage-1.0.0.tar.gz
  md5: 84c2013768961e5681f974c10071e3ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cipher.encryptingstorage = cipher.encryptingstorage:main
    #
    # Would create an entry point called cipher.encryptingstorage that calls cipher.encryptingstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3 >=3.10.0b1
    - setuptools
    - keas.kmi

  run:
    - python
    - zodb3 >=3.10.0b1
    - setuptools
    - keas.kmi

test:
  # Python imports
  imports:
    - cipher
    - cipher.encryptingstorage
    - cipher.encryptingstorage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cipher.encryptingstorage/
  license: ZPL 2.1
  summary: 'ZODB storage wrapper for encryption of database records'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
