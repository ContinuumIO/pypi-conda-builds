package:
  name: mongotor
  version: "0.1.0"

source:
  fn: mongotor-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mongotor/mongotor-0.1.0.tar.gz
  md5: 5bbba3a77a5caa35380698257046e3f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongotor = mongotor:main
    #
    # Would create an entry point called mongotor that calls mongotor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.3
    - tornado >=2.2

  run:
    - python
    - pymongo >=2.3
    - tornado >=2.2

test:
  # Python imports
  imports:
    - mongotor
    - mongotor.orm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://marcelnicolay.github.com/mongotor/
  license: OSI
  summary: '(MongoDB + Tornado) is an asynchronous driver and toolkit for working with MongoDB inside a Tornado app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
