package:
  name: pyramid_ipauth
  version: "0.2.0"

source:
  fn: pyramid_ipauth-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_ipauth/pyramid_ipauth-0.2.0.tar.gz
  md5: 00c88dffc18784707c00c19f8debbb70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_ipauth = pyramid_ipauth:main
    #
    # Would create an entry point called pyramid_ipauth that calls pyramid_ipauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - netaddr
    - unittest2

  run:
    - python
    - pyramid
    - netaddr
    - unittest2

test:
  # Python imports
  imports:
    - pyramid_ipauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla-services/pyramid_ipauth
  license: UNKNOWN
  summary: 'pyramid_ipauth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
