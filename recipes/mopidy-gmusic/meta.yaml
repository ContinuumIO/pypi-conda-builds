package:
  name: mopidy-gmusic
  version: "0.3.0"

source:
  fn: Mopidy-GMusic-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-GMusic/Mopidy-GMusic-0.3.0.tar.gz
  md5: 5de4c7cc543080c5e9e547a91d1946f9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-gmusic = mopidy-gmusic:main
    #
    # Would create an entry point called mopidy-gmusic that calls mopidy-gmusic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - gmusicapi >=3.0.0

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - gmusicapi >=3.0.0

test:
  # Python imports
  imports:
    - mopidy_gmusic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hechtus/mopidy-gmusic/
  license: MIT License
  summary: u'Google Play Music extension for Mopidy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
