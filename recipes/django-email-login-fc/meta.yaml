package:
  name: django-email-login-fc
  version: "0.3.2"

source:
  fn: django-email-login-fc-0.3.2.zip
  url: https://pypi.python.org/packages/source/d/django-email-login-fc/django-email-login-fc-0.3.2.zip
  md5: f36c00e6a944cea9881cb9f9aa49c62d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-login-fc = django-email-login-fc:main
    #
    # Would create an entry point called django-email-login-fc that calls django-email-login-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_login

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-email-login
  license: MIT License
  summary: 'A Django application that enables you to have your users use their email address instead of their username'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
