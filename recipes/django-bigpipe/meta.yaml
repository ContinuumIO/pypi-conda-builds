package:
  name: django-bigpipe
  version: "0.0.3"

source:
  fn: django-bigpipe-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bigpipe/django-bigpipe-0.0.3.tar.gz
  md5: 890adf906e12fa62fa74ab886f70e62b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bigpipe = django-bigpipe:main
    #
    # Would create an entry point called django-bigpipe that calls django-bigpipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_bigpipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orygens/django-bigpipe
  license: MIT License
  summary: 'django-bigpipe are a jquery-bigpipe extension to django web framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
