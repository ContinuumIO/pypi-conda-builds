package:
  name: schachtlerin
  version: "2.3.4.2"

source:
  fn: schachtlerin-2.3.4.2.tar.gz
  url: https://pypi.python.org/packages/source/s/schachtlerin/schachtlerin-2.3.4.2.tar.gz
  md5: d433ad2f7dfb3f7932203b74f1716345
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - schachtlerin = schachtlerin:main
    #
    # Would create an entry point called schachtlerin that calls schachtlerin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - schachtlerin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.b1-systems.de
  license: UNKNOWN
  summary: 'Einfache Ausgabe verschachtelter Listen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
