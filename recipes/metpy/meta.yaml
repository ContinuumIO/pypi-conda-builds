package:
  name: metpy
  version: "0.1.0"

source:
  fn: MetPy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/M/MetPy/MetPy-0.1.0.tar.gz
  md5: f649ff2bbf4f1e3657f4564b6b460498
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metpy = metpy:main
    #
    # Would create an entry point called metpy that calls metpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - metpy
    - metpy.calc
    - metpy.io
    - metpy.plots

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/MetPy/MetPy
  license: BSD License
  summary: 'Collection of tools for reading, visualizing andperforming calculations with weather data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
