package:
  name: formconvert
  version: "0.2.0"

source:
  fn: FormConvert-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/FormConvert/FormConvert-0.2.0.tar.gz
  md5: 89e9b0447ced5f929df0c94b786ea4d3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formconvert = formconvert:main
    #
    # Would create an entry point called formconvert that calls formconvert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - conversionkit
    - recordconvert
    - stringconvert
    - nestedrecord
    - webob
    - formbuild >=2.0,<=2.99

  run:
    - python
    - setuptools
    - conversionkit
    - recordconvert
    - stringconvert
    - nestedrecord
    - webob
    - formbuild >=2.0,<=2.99

test:
  # Python imports
  imports:
    - formconvert

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/formconvert/index.html
  license: GNU Affero General Public License v3
  summary: 'Tools for working with Forms, including ConversionKit converters to convert HTML values to Python data structures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
