package:
  name: classowl-client
  version: "0.1a2"

source:
  fn: classowl-client-0.1a2.tar.gz
  url: https://pypi.python.org/packages/source/c/classowl-client/classowl-client-0.1a2.tar.gz
  md5: da46ea17ae46512a5563183a34138784
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - classowl-client = classowl-client:main
    #
    # Would create an entry point called classowl-client that calls classowl-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - slumber ==0.6

  run:
    - python
    - slumber ==0.6

test:
  # Python imports
  imports:
    - classowl_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://localhost
  license: MIT License
  summary: 'a client for the classowl.com REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
