package:
  name: simpleframework
  version: "1.2.5"

source:
  fn: SimpleFramework-1.2.5.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleFramework/SimpleFramework-1.2.5.tar.gz
  md5: ec0ab0b0104c62ddbad5c4a375bf9442
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleframework = simpleframework:main
    #
    # Would create an entry point called simpleframework that calls simpleframework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - framework
    - framework.constants
    - framework.email
    - framework.httpexceptions
    - framework.util
    - simpleframework

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Simplendi/SimpleFramework
  license: UNKNOWN
  summary: 'Simple Web Framework by Simplendi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
