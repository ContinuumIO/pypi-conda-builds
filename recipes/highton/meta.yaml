package:
  name: highton
  version: "1.2.3"

source:
  fn: Highton-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/H/Highton/Highton-1.2.3.tar.gz
  md5: 2abe5841e8e3cb710bbb4361f76d5e24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - highton = highton:main
    #
    # Would create an entry point called highton that calls highton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - lxml

  run:
    - python
    - requests
    - lxml

test:
  # Python imports
  imports:
    - highton
    - highton.classes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/seibert-media/Highton
  license: Apache Software License
  summary: 'A Python library for Highrise'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
