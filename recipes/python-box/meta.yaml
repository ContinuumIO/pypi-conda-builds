package:
  name: python-box
  version: "0.0.2"

source:
  fn: python-box-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-box/python-box-0.0.2.tar.gz
  md5: 8ab4c785fccd80cb62cba3b689cbf898
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-box = python-box:main
    #
    # Would create an entry point called python-box that calls python-box.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - pybox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stitchfix/pybox
  license: MIT
  summary: 'A Python client for the Box API v2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
