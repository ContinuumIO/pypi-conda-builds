package:
  name: plone.app.texttile
  version: "1.0"

source:
  fn: plone.app.texttile-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.texttile/plone.app.texttile-1.0.tar.gz
  md5: 1d6744de5a870267607e4cd5607931b0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.texttile = plone.app.texttile:main
    #
    # Would create an entry point called plone.app.texttile that calls plone.app.texttile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.i18nmessageid
    - plone.directives.form
    - plone.tiles
    - plone.app.z3cform
    - plone.app.tiles

  run:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.i18nmessageid
    - plone.directives.form
    - plone.tiles
    - plone.app.z3cform
    - plone.app.tiles

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.texttile
    - plone.app.texttile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.app.texttile
  license: GPL
  summary: 'Text tile for Deco UI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
