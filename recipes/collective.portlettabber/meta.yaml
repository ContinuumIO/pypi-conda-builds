package:
  name: collective.portlettabber
  version: "0.2.0"

source:
  fn: collective.portlettabber-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlettabber/collective.portlettabber-0.2.0.tar.gz
  md5: 247305f41d05984e68a9ff7aa55dc1be
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlettabber = collective.portlettabber:main
    #
    # Would create an entry point called collective.portlettabber that calls collective.portlettabber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlettabber
    - collective.portlettabber.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.portlettabber
  license: GPL
  summary: 'A jQuery plugin for Plone layouts that merge portlets together obtaining a single portlet with tabs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
