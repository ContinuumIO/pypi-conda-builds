package:
  name: zc.generationalset
  version: "0.3.0"

source:
  fn: zc.generationalset-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.generationalset/zc.generationalset-0.3.0.tar.gz
  md5: 9a8d027bb5bd0716c1730938b5ca4b0c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.generationalset = zc.generationalset:main
    #
    # Would create an entry point called zc.generationalset that calls zc.generationalset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - btrees

  run:
    - python
    - setuptools
    - setuptools
    - btrees

test:
  # Python imports
  imports:
    - zc
    - zc.generationalset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/zc/generationalset
  license: ZPL 2.1
  summary: '*****************'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
