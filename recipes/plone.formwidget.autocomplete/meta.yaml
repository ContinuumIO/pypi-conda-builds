package:
  name: plone.formwidget.autocomplete
  version: "1.2.8"

source:
  fn: plone.formwidget.autocomplete-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.formwidget.autocomplete/plone.formwidget.autocomplete-1.2.8.tar.gz
  md5: 2b0ac81d7dc46617ee5ff8ce0c27c400
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.formwidget.autocomplete = plone.formwidget.autocomplete:main
    #
    # Would create an entry point called plone.formwidget.autocomplete that calls plone.formwidget.autocomplete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.formwidget.query
    - plone.z3cform >=0.7.4

  run:
    - python
    - setuptools
    - setuptools
    - z3c.formwidget.query
    - plone.z3cform >=0.7.4

test:
  # Python imports
  imports:
    - plone
    - plone.formwidget
    - plone.formwidget.autocomplete
    - plone.formwidget.autocomplete.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plone.formwidget.autocomplete
  license: GNU General Public License (GPL)
  summary: 'AJAX selection widget for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
