package:
  name: django-html
  version: "0.1.0"

source:
  fn: django-html-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-html/django-html-0.1.0.tar.gz
  md5: 8613f072be6afd984580c06256ea5090
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-html = django-html:main
    #
    # Would create an entry point called django-html that calls django-html.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_html
    - django_html.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/simonw/django-html
  license: BSD License
  summary: 'Template tags for smarter outputting of HTML in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
