package:
  name: django-packages-search
  version: "0.11"

source:
  fn: django-packages-search-0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-packages-search/django-packages-search-0.11.tar.gz
  md5: 72182580e51740fc980b03b4e1588c8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-packages-search = django-packages-search:main
    #
    # Would create an entry point called django-packages-search that calls django-packages-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - simplejson
    - requests

  run:
    - python
    - argparse
    - simplejson
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vahidR/django-packages-search
  license: GNU General Public License v2 (GPLv2)
  summary: 'A command line tool for searching within djangopackages.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
