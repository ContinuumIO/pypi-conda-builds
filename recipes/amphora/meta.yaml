package:
  name: amphora
  version: "0.1.3"

source:
  fn: amphora-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/amphora/amphora-0.1.3.tar.gz
  md5: 50481cc674c8331f58f0e9b5c68db2b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amphora = amphora:main
    #
    # Would create an entry point called amphora that calls amphora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent
    - amqp
    - docutils >=0.7

  run:
    - python
    - gevent
    - amqp
    - docutils >=0.7

test:
  # Python imports
  imports:
    - amphora

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/werehuman/amphora/
  license: MIT License
  summary: 'AMQP RPC for services based on gevent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
