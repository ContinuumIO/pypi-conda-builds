package:
  name: mzgeohash
  version: "0.9b1"

source:
  fn: mzgeohash-0.9b1.tar.gz
  url: https://pypi.python.org/packages/source/m/mzgeohash/mzgeohash-0.9b1.tar.gz
  md5: a5d6103592f4f1f83fd73ee67d21617a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mzgeohash = mzgeohash:main
    #
    # Would create an entry point called mzgeohash that calls mzgeohash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mzgeohash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/transitland/mapzen-geohash
  license: License :: OSI Approved :: MIT License
  summary: 'Mapzen Geohash'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
