package:
  name: collective.autogroup
  version: "0.1b1"

source:
  fn: collective.autogroup-0.1b1.zip
  url: https://pypi.python.org/packages/source/c/collective.autogroup/collective.autogroup-0.1b1.zip
  md5: 4f84b922af229d1e9a7e34392c766340
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.autogroup = collective.autogroup:main
    #
    # Would create an entry point called collective.autogroup that calls collective.autogroup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.autogroup
    - collective.autogroup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.contentgroup
  license: GPL
  summary: 'Provide a simple API to create automatic groups in Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
