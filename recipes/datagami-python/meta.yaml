package:
  name: datagami-python
  version: "0.0.5"

source:
  fn: datagami-python-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/datagami-python/datagami-python-0.0.5.tar.gz
  md5: 9a8c4d3843e31dd73ef740bdc7f6ce4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datagami-python = datagami-python:main
    #
    # Would create an entry point called datagami-python that calls datagami-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - simplejson

  run:
    - python
    - requests
    - simplejson

test:
  # Python imports
  imports:
    - datagami
    - datagami.mixins
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datagami/datagami-python
  license: MIT License
  summary: 'Datagami library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
