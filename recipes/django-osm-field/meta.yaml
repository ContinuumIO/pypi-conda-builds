package:
  name: django-osm-field
  version: "0.2.0"

source:
  fn: django-osm-field-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-osm-field/django-osm-field-0.2.0.tar.gz
  md5: 4d5541d6d52852153b39b269c153d00a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-osm-field = django-osm-field:main
    #
    # Would create an entry point called django-osm-field that calls django-osm-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - six

  run:
    - python
    - django >=1.4
    - six

test:
  # Python imports
  imports:
    - osm_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MarkusH/django-osm-field
  license: MIT License
  summary: 'Django OpenStreetMap Field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
