package:
  name: nwsserver
  version: "1.5.1"

source:
  fn: nwsserver-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nwsserver/nwsserver-1.5.1.tar.gz
  md5: edded3aec2ad3e27f35bda90c7dfd4b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nwsserver = nwsserver:main
    #
    # Would create an entry point called nwsserver that calls nwsserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nwss
    - nwsserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nws-py.sourceforge.net/
  license: GNU General Public License (GPL)
  summary: 'Python NetWorkSpaces Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
