package:
  name: cleanfreak
  version: "0.1.6"

source:
  fn: cleanfreak-0.1.6.zip
  url: https://pypi.python.org/packages/source/c/cleanfreak/cleanfreak-0.1.6.zip
  md5: 1342af4270864cdd8f77a2f15166f587
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cleanfreak = cleanfreak:main
    #
    # Would create an entry point called cleanfreak that calls cleanfreak.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cleanfreak
    - cleanfreak.ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danbradham/cleanfreak
  license: MIT License
  summary: 'Sanity checks and grades for CG production.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
