package:
  name: madlag3dware
  version: "0.1"

source:
  fn: madlag3dware-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/madlag3dware/madlag3dware-0.1.tar.gz
  md5: bb49332d156c0df2d360361461eea7a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - madlag3dware = madlag3dware:main
    #
    # Would create an entry point called madlag3dware that calls madlag3dware.main()

    - madlag3dware=madlag3dware:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - madlag3dware

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - madlag3dware --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'solidpython/openscad parametric designs (raspberry pi case elements etc)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
