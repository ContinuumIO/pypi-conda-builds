package:
  name: django-attachments
  version: "0.3.1"

source:
  fn: django-attachments-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-attachments/django-attachments-0.3.1.tar.gz
  md5: fe9397ac913c3c5f20de368fee7dcb32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-attachments = django-attachments:main
    #
    # Would create an entry point called django-attachments that calls django-attachments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - attachments
    - attachments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bartTC/django-attachments/tree/master
  license: BSD License
  summary: 'A generic Django application to attach Files (Attachments) to any model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
