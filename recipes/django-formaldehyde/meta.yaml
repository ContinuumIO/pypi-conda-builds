package:
  name: django-formaldehyde
  version: "0.3"

source:
  fn: django-formaldehyde-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formaldehyde/django-formaldehyde-0.3.tar.gz
  md5: a2d95fbfe112dfca1f82e304e7160227
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formaldehyde = django-formaldehyde:main
    #
    # Would create an entry point called django-formaldehyde that calls django-formaldehyde.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - formaldehyde
    - formaldehyde.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kunitoki/django-formaldehyde
  license: MIT License
  summary: 'Django forms at warp speed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
