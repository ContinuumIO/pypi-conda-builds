package:
  name: djlanding
  version: "0.1.6"

source:
  fn: djlanding-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/djlanding/djlanding-0.1.6.tar.gz
  md5: 9d7868052137dae37bb5e32fa6f18221
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djlanding = djlanding:main
    #
    # Would create an entry point called djlanding that calls djlanding.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - greatape

  run:
    - python
    - greatape

test:
  # Python imports
  imports:
    - djlanding

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/djlanding/
  license: BSD
  summary: 'Basic landing page for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
