package:
  name: asnhistory-redis
  version: "1.0"

source:
  fn: asnhistory-redis-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/asnhistory-redis/asnhistory-redis-1.0.tar.gz
  md5: d3473d0b51f4908f50e96629d6c0d46a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asnhistory-redis = asnhistory-redis:main
    #
    # Would create an entry point called asnhistory-redis that calls asnhistory-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - dateutils

  run:
    - python
    - redis
    - dateutils

test:
  # Python imports
  imports:
    - asnhistory
    - asnhistory.redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Rafiot/ASN-Description-History
  license: GNU General Public License v3 (GPLv3)
  summary: 'Query a redis database to access to the ASNs descriptions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
