package:
  name: matisse.py
  version: "0.1.1"

source:
  fn: MaTiSSe.py-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/M/MaTiSSe.py/MaTiSSe.py-0.1.1.tar.gz
  md5: 91127fa48b93eb09a18e47d243df0c69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - matisse.py = matisse.py:main
    #
    # Would create an entry point called matisse.py that calls matisse.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown
    - yattag

  run:
    - python
    - markdown
    - yattag

test:
  # Python imports
  imports:
    - matisse
    - matisse.data
    - matisse.presentation
    - matisse.theme
    - matisse.theme.slide
    - matisse.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/szaghi/MaTiSSe
  license: GNU General Public License v3 (GPLv3)
  summary: 'MaTiSSe.py, Markdown To Impressive Scientific Slides'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
