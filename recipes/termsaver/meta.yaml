package:
  name: termsaver
  version: "0.3"

source:
  fn: termsaver-0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/termsaver/termsaver-0.3.tar.gz
  md5: 7b33d51e858254797dfeb3a5f04671e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - termsaver = termsaver:main
    #
    # Would create an entry point called termsaver that calls termsaver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - termsaver
    - termsaverlib
    - termsaverlib.plugins
    - termsaverlib.screen
    - termsaverlib.screen.base
    - termsaverlib.screen.helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://termsaver.info
  license: Apache Software License
  summary: 'Simple text-based terminal screensaver.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
