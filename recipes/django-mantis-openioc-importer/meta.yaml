package:
  name: django-mantis-openioc-importer
  version: "0.2.0"

source:
  fn: django-mantis-openioc-importer-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mantis-openioc-importer/django-mantis-openioc-importer-0.2.0.tar.gz
  md5: 567addc29d20b7bdf4428819c2296e6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mantis-openioc-importer = django-mantis-openioc-importer:main
    #
    # Would create an entry point called django-mantis-openioc-importer that calls django-mantis-openioc-importer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.5
    - django-dingos >=0.1.0
    - django-mantis-core >=0.1.0

  run:
    - python
    - django >=1.5.5
    - django-dingos >=0.1.0
    - django-mantis-core >=0.1.0

test:
  # Python imports
  imports:
    - mantis_openioc_importer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/siemens/django-mantis-openioc-importer
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'An OpenIOC import module for the  Mantis Cyber Threat Intelligence Mgmt. Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
