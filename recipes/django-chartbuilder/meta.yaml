package:
  name: django-chartbuilder
  version: "0.3"

source:
  fn: django-chartbuilder-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chartbuilder/django-chartbuilder-0.3.tar.gz
  md5: 3e0842900816b59fe3f03e1d0bc28944
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chartbuilder = django-chartbuilder:main
    #
    # Would create an entry point called django-chartbuilder that calls django-chartbuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_chartbuilder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aterkik/django-chartbuilder
  license: MIT License
  summary: 'A simple Django application for integrating Chartbuilder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
