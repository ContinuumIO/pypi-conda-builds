package:
  name: django-alerts
  version: "0.5.1"

source:
  fn: django-alerts-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-alerts/django-alerts-0.5.1.tar.gz
  md5: a86e3c73a752d260549a895642a888b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-alerts = django-alerts:main
    #
    # Would create an entry point called django-alerts that calls django-alerts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - alerts
    - alerts.migrations
    - alerts.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-alerts
  license: BSD License
  summary: 'Django alerts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
