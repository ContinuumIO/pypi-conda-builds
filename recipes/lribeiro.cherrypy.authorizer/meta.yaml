package:
  name: lribeiro.cherrypy.authorizer
  version: "1.0.2"

source:
  fn: lribeiro.cherrypy.authorizer-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lribeiro.cherrypy.authorizer/lribeiro.cherrypy.authorizer-1.0.2.tar.gz
  md5: 8fe7cbb62e867feba80168edc3d089bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lribeiro.cherrypy.authorizer = lribeiro.cherrypy.authorizer:main
    #
    # Would create an entry point called lribeiro.cherrypy.authorizer that calls lribeiro.cherrypy.authorizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy

  run:
    - python
    - cherrypy

test:
  # Python imports
  imports:
    - lribeiro
    - lribeiro.cherrypy
    - lribeiro.cherrypy.authorizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/livioribeiro/cherrypy-authorizer
  license: BSD License
  summary: 'Extensible session based authentication and claims based authorization tool for CherryPy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
