package:
  name: django-ckedit
  version: "1.0"

source:
  fn: django-ckedit-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-ckedit/django-ckedit-1.0.zip
  md5: 2e61c3d9eefeacebb62792f763dd0f8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ckedit = django-ckedit:main
    #
    # Would create an entry point called django-ckedit that calls django-ckedit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - easy-thumbnails >=1
    - pillow >=2.0

  run:
    - python
    - django >=1.3
    - easy-thumbnails >=1
    - pillow >=2.0

test:
  # Python imports
  imports:
    - ckeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-ckeditor
  license: BSD License
  summary: 'Django integration with the popular WYSIWYG editor CKEditor.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
