package:
  name: django-undeletable
  version: "0.2.2"

source:
  fn: django-undeletable-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-undeletable/django-undeletable-0.2.2.tar.gz
  md5: 315d1c47b38e49df919b2647ef7ab1f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-undeletable = django-undeletable:main
    #
    # Would create an entry point called django-undeletable that calls django-undeletable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - django_undeletable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kakulukia/django-undeletable
  license: MIT
  summary: 'Deleted data stays in the database and will be hidden from the default manager.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
