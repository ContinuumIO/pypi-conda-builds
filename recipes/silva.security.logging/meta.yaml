package:
  name: silva.security.logging
  version: "1.2.2"

source:
  fn: silva.security.logging-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.security.logging/silva.security.logging-1.2.2.tar.gz
  md5: 95ba84c9d82e00758e9b77b39642ea49
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.security.logging = silva.security.logging:main
    #
    # Would create an entry point called silva.security.logging that calls silva.security.logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - five.grok
    - setuptools
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.views
    - zeam.form.silva
    - zope.component
    - zope.container
    - zope.interface
    - zope.intid
    - zope.keyreference
    - zope.lifecycleevent
    - zope.schema

  run:
    - python
    - five.grok
    - setuptools
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.views
    - zeam.form.silva
    - zope.component
    - zope.container
    - zope.interface
    - zope.intid
    - zope.keyreference
    - zope.lifecycleevent
    - zope.schema

test:
  # Python imports
  imports:
    - silva
    - silva.security
    - silva.security.logging
    - silva.security.logging.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.security.logging
  license: BSD License
  summary: 'Log all actions inside Silva CMS in a file or SQL table'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
