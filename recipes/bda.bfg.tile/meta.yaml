package:
  name: bda.bfg.tile
  version: "1.0"

source:
  fn: bda.bfg.tile-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.bfg.tile/bda.bfg.tile-1.0.tar.gz
  md5: 5781d18ce262042b5cf81f5f010c7f98
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.bfg.tile = bda.bfg.tile:main
    #
    # Would create an entry point called bda.bfg.tile that calls bda.bfg.tile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - repoze.bfg

  run:
    - python
    - setuptools
    - repoze.bfg

test:
  # Python imports
  imports:
    - bda
    - bda.bfg
    - bda.bfg.tile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.bluedynamics.net/svn/internal/bda.tile
  license: GNU General Public Licence
  summary: 'Handle web application parts as tiles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
