package:
  name: django-cms-smartsnippets
  version: "0.1.3"

source:
  fn: django-cms-smartsnippets-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-smartsnippets/django-cms-smartsnippets-0.1.3.tar.gz
  md5: 7ed7add8b8d1990fc98c7219dbcacdcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-smartsnippets = django-cms-smartsnippets:main
    #
    # Would create an entry point called django-cms-smartsnippets that calls django-cms-smartsnippets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smartsnippets
    - smartsnippets.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Parametrizable Django CMS snippets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
