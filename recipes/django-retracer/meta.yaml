package:
  name: django-retracer
  version: "0.3"

source:
  fn: django-retracer-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-retracer/django-retracer-0.3.tar.gz
  md5: c98f3585d016226d411dbca85a8cf074
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-retracer = django-retracer:main
    #
    # Would create an entry point called django-retracer that calls django-retracer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djretr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/zacharyvoase/django-retracer
  license: UNKNOWN
  summary: 'Store and restore old locations in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
