package:
  name: django-accountant
  version: "0.1.9"

source:
  fn: django-accountant-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-accountant/django-accountant-0.1.9.tar.gz
  md5: bc681b6291378c9c6f4eea9be381c279
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-accountant = django-accountant:main
    #
    # Would create an entry point called django-accountant that calls django-accountant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.5.1
    - django-extensions ==1.1.1
    - django-testutils >=0.1.0

  run:
    - python
    - django ==1.5.1
    - django-extensions ==1.1.1
    - django-testutils >=0.1.0

test:
  # Python imports
  imports:
    - accountant

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Django based account manager.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
