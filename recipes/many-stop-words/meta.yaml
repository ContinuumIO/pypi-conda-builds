package:
  name: many-stop-words
  version: "0.1.3"

source:
  fn: many-stop-words-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/many-stop-words/many-stop-words-0.1.3.tar.gz
  md5: b129f6479a5d615de77c3e487e284c4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - many-stop-words = many-stop-words:main
    #
    # Would create an entry point called many-stop-words that calls many-stop-words.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - many_stop_words
    - many_stop_words.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://diffeo.com
  license: MIT License
  summary: 'stop words lists in many languages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
