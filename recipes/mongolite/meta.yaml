package:
  name: mongolite
  version: "1.5"

source:
  fn: MongoLite-1.5.tar.gz
  url: https://pypi.python.org/packages/source/M/MongoLite/MongoLite-1.5.tar.gz
  md5: 004327ff079a226a88a3745ba7d27fdf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongolite = mongolite:main
    #
    # Would create an entry point called mongolite that calls mongolite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.2

  run:
    - python
    - pymongo >=2.2

test:
  # Python imports
  imports:
    - mongolite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://namlook.github.com/mongolite/
  license: Apache Software License
  summary: 'Python MongoDB light toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
