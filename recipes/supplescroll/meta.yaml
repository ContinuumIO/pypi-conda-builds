package:
  name: supplescroll
  version: "0.9.2"

source:
  fn: supplescroll-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/s/supplescroll/supplescroll-0.9.2.tar.gz
  md5: 6702d8d48a6129a28efd136fece4efa1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supplescroll = supplescroll:main
    #
    # Would create an entry point called supplescroll that calls supplescroll.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - docopt
    - embellish >=0.9.7
    - beautifulsoup4

  run:
    - python
    - pyyaml
    - docopt
    - embellish >=0.9.7
    - beautifulsoup4

test:
  # Python imports
  imports:
    - supplescroll

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://boscoh.github.io/supplescroll
  license: BSD
  summary: 'converts markdown to HTML with integrated table  of contents and figure panel, tied together with some supple scrolling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
