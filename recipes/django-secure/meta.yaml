package:
  name: django-secure
  version: "1.0.1"

source:
  fn: django-secure-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-secure/django-secure-1.0.1.tar.gz
  md5: 7fa6bc01cfc3d9443102ef1e5739a3a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-secure = django-secure:main
    #
    # Would create an entry point called django-secure that calls django-secure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - djangosecure
    - djangosecure.check
    - djangosecure.management
    - djangosecure.management.commands
    - djangosecure.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carljm/django-secure/
  license: BSD License
  summary: "Utilities and a 'linter' to help you make your Django site more secure."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
