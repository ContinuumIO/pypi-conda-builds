package:
  name: boo_box
  version: "0.3.7"

source:
  fn: boo_box-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/b/boo_box/boo_box-0.3.7.tar.gz
  md5: 4b490722892cd430ae57b5f5f071ce64
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boo_box = boo_box:main
    #
    # Would create an entry point called boo_box that calls boo_box.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - boo_box

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.assembla.com/spaces/py_boo_box
  license: GNU General Public License (GPL)
  summary: 'Boo-Box to monetize'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
