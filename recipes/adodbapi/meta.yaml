package:
  name: adodbapi
  version: "2.6.0.7"

source:
  fn: adodbapi-2.6.0.7.zip
  url: https://pypi.python.org/packages/source/a/adodbapi/adodbapi-2.6.0.7.zip
  md5: df7e56b741df08d10ed6766bc3c2d9c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adodbapi = adodbapi:main
    #
    # Would create an entry point called adodbapi that calls adodbapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - adodbapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/adodbapi
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A pure Python package implementing PEP 249 DB-API using Microsoft ADO.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
