package:
  name: django-text
  version: "1.6.0"

source:
  fn: django-text-1.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-text/django-text-1.6.0.tar.gz
  md5: 6a7c532f3225670fef6401bdd289672b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-text = django-text:main
    #
    # Would create an entry point called django-text that calls django-text.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.4
    - markdown >=2.6

  run:
    - python
    - django >=1.7.4
    - markdown >=2.6

test:
  # Python imports
  imports:
    - text
    - text.migrations
    - text.templatetags
    - text.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/antonagestam/django-text/
  license: The MIT License (MIT)
  summary: 'Intuitive text editing for humans using Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
