package:
  name: collective.keywordcondition
  version: "1.0b1"

source:
  fn: collective.keywordcondition-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.keywordcondition/collective.keywordcondition-1.0b1.tar.gz
  md5: 095705d1d7dafb9e983fac191e157063
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.keywordcondition = collective.keywordcondition:main
    #
    # Would create an entry point called collective.keywordcondition that calls collective.keywordcondition.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.keywordcondition

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: GPL
  summary: "A condition for Plone 3's content rules that checks keywords on a content item"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
