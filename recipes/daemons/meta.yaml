package:
  name: daemons
  version: "1.2.1"

source:
  fn: daemons-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/daemons/daemons-1.2.1.tar.gz
  md5: 8c969c421d1106ad037b449baf501ba7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daemons = daemons:main
    #
    # Would create an entry point called daemons that calls daemons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - daemons
    - daemons.daemonize
    - daemons.interfaces
    - daemons.message
    - daemons.pid
    - daemons.prefab
    - daemons.signal
    - daemons.startstop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevinconway/daemons
  license: daff
  summary: 'Well behaved unix daemons for every occasion.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
