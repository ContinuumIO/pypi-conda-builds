package:
  name: cara
  version: "0.10.3"

source:
  fn: cara-0.10.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cara/cara-0.10.3.tar.gz
  md5: a0b13f1b0c8c6248d6ba0d9e6b5c08d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cara = cara:main
    #
    # Would create an entry point called cara that calls cara.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mutablerecords

  run:
    - python
    - mutablerecords

test:
  # Python imports
  imports:
    - cara
    - cara.capnp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chainreactionmfg/cara
  license: Apache Software License
  summary: "cara is a Cap'n proto Alternative RPC API."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
