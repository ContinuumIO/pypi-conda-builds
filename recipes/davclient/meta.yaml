package:
  name: davclient
  version: "0.2.0"

source:
  fn: davclient-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/davclient/davclient-0.2.0.tar.gz
  md5: e0105a32371d24b914f97419f603088a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - davclient = davclient:main
    #
    # Would create an entry point called davclient that calls davclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - davclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.osafoundation.org/Projects/Davclient
  license: Apache Software License
  summary: 'Simple WebDAV client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
