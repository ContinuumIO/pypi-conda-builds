package:
  name: cubicweb-email
  version: "1.10.0"

source:
  fn: cubicweb-email-1.10.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-email/cubicweb-email-1.10.0.tar.gz
  md5: dedfab1be78cbf02ff16e374db8f826f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-email = cubicweb-email:main
    #
    # Would create an entry point called cubicweb-email that calls cubicweb-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.15.0
    - cubicweb-comment
    - logilab-common >=0.58.3
    - cubicweb-file >=1.9.0

  run:
    - python
    - cubicweb >=3.15.0
    - cubicweb-comment
    - logilab-common >=0.58.3
    - cubicweb-file >=1.9.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-email
  license: LGPL
  summary: 'email component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
