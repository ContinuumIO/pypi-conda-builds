package:
  name: pyramid_controllers
  version: "0.3.23"

source:
  fn: pyramid_controllers-0.3.23.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_controllers/pyramid_controllers-0.3.23.tar.gz
  md5: 91aca313a58c570dafff587098c38bf2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_controllers = pyramid_controllers:main
    #
    # Would create an entry point called pyramid_controllers that calls pyramid_controllers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute >=0.6.24
    - argparse >=1.2.1
    - pyramid >=1.4.2
    - six >=1.4.1

  run:
    - python
    - distribute >=0.6.24
    - argparse >=1.2.1
    - pyramid >=1.4.2
    - six >=1.4.1

test:
  # Python imports
  imports:
    - pyramid_controllers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cadithealth/pyramid_controllers
  license: MIT License
  summary: 'A pyramid plugin that provides de-centralized hierarchical object dispatch.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
