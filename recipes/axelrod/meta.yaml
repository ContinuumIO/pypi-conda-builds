package:
  name: axelrod
  version: "0.0.8"

source:
  fn: Axelrod-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/A/Axelrod/Axelrod-0.0.8.tar.gz
  md5: 629469140746cd937a00c134bafafd4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - axelrod = axelrod:main
    #
    # Would create an entry point called axelrod that calls axelrod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - matplotlib >=1.4.2

  run:
    - python
    - matplotlib >=1.4.2

test:
  # Python imports
  imports:
    - axelrod
    - axelrod.strategies
    - axelrod.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://axelrod.readthedocs.org/
  license: The MIT License (MIT)
  summary: 'Reproduce the Axelrod iterated prisoners dilemma tournament'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
