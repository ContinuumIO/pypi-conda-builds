package:
  name: tsquare
  version: "0.2.5"

source:
  fn: tsquare-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/t/tsquare/tsquare-0.2.5.tar.gz
  md5: 0d258b02d3a55dcad568dba951f4ffd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tsquare = tsquare:main
    #
    # Would create an entry point called tsquare that calls tsquare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - beautifulsoup >=3.2.1
    - lxml >=3.1.0

  run:
    - python
    - requests >=1.2.3
    - beautifulsoup >=3.2.1
    - lxml >=3.1.0

test:
  # Python imports
  imports:
    - tsquare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/swgillespie/tsquare
  license: MIT
  summary: 'Python TSquare API bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
