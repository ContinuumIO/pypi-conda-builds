package:
  name: br_documents
  version: "0.0.1-p1"

source:
  fn: br_documents-0.0.1-p1.tar.gz
  url: https://pypi.python.org/packages/source/b/br_documents/br_documents-0.0.1-p1.tar.gz
  md5: 24a94c41b03340df21d556559b693081
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - br_documents = br_documents:main
    #
    # Would create an entry point called br_documents that calls br_documents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - br_documents

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chronossc/br_documents
  license: BSD License
  summary: 'Provide objects for brazilian documents like CPF and others, with validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
