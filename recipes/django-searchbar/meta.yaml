package:
  name: django-searchbar
  version: "0.1.7"

source:
  fn: django-searchbar-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-searchbar/django-searchbar-0.1.7.tar.gz
  md5: 13119ecb18dafd7c17cd8ba756f17659
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-searchbar = django-searchbar:main
    #
    # Would create an entry point called django-searchbar that calls django-searchbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_searchbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/arsham/django-searchbar
  license: BSD License
  summary: 'Simple searchbar and handler you can use in all your views and templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
