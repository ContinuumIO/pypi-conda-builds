package:
  name: zc.zodbrecipes
  version: "2.0.0"

source:
  fn: zc.zodbrecipes-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.zodbrecipes/zc.zodbrecipes-2.0.0.tar.gz
  md5: 36250c045dfa8e0d2bc214d03c7b96a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.zodbrecipes = zc.zodbrecipes:main
    #
    # Would create an entry point called zc.zodbrecipes that calls zc.zodbrecipes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - six
    - zc.recipe.egg
    - zconfig >=2.4

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - six
    - zc.recipe.egg
    - zconfig >=2.4

test:
  # Python imports
  imports:
    - zc
    - zc.zodbrecipes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/zc.zodbrecipes
  license: Zope Public License
  summary: 'ZC Buildout recipes for ZODB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
