package:
  name: django_smartdbstorage
  version: "0.1.1"

source:
  fn: django_smartdbstorage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_smartdbstorage/django_smartdbstorage-0.1.1.tar.gz
  md5: 25c1608056a6dc2c2de9678c8a744e65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_smartdbstorage = django_smartdbstorage:main
    #
    # Would create an entry point called django_smartdbstorage that calls django_smartdbstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south >=0.7.3

  run:
    - python
    - south >=0.7.3

test:
  # Python imports
  imports:
    - django_smartdbstorage
    - smartdbstorage
    - smartdbstorage.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dperetti.github.com/Django-SmartDBStorage
  license: LICENSE.txt
  summary: 'A Django Model based file storage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
