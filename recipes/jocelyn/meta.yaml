package:
  name: jocelyn
  version: "0.1.0"

source:
  fn: jocelyn-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jocelyn/jocelyn-0.1.0.tar.gz
  md5: 6093649b46f03dbf621c43bd015e3ddc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jocelyn = jocelyn:main
    #
    # Would create an entry point called jocelyn that calls jocelyn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jocelyn
    - jocelyn.examples
    - jocelyn.examples.library_example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/d0c0nnor/jocelyn
  license: UNKNOWN
  summary: 'A shim that makes it easier to use the Processing core libraries from Jython.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
