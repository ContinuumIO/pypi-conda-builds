package:
  name: django-flaggit
  version: "0.0.3"

source:
  fn: django-flaggit-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flaggit/django-flaggit-0.0.3.tar.gz
  md5: 8bd24f8ace3cb0070b1e371be48d85b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flaggit = django-flaggit:main
    #
    # Would create an entry point called django-flaggit that calls django-flaggit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flaggit
    - flaggit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caffeinehit/django-flaggit
  license: UNKNOWN
  summary: 'Generic content flagging for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
