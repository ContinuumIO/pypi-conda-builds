package:
  name: pypicount
  version: "0.0dev"

source:
  fn: pypicount-0.0dev.tar.gz
  url: https://pypi.python.org/packages/source/p/pypicount/pypicount-0.0dev.tar.gz
  md5: d5ac63d924104926ed13962140e59729
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypicount = pypicount:main
    #
    # Would create an entry point called pypicount that calls pypicount.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - pypicount

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cleder/pypicount
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Count download pypi packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
