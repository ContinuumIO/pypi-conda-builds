package:
  name: more.webassets
  version: "0.0.1"

source:
  fn: more.webassets-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/more.webassets/more.webassets-0.0.1.tar.gz
  md5: 3e905fdaadb36f4b2bb3008c3a506383
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - more.webassets = more.webassets:main
    #
    # Would create an entry point called more.webassets that calls more.webassets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - morepath
    - ordered-set
    - webassets
    - webob

  run:
    - python
    - morepath
    - ordered-set
    - webassets
    - webob

test:
  # Python imports
  imports:
    - more
    - more.webassets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/morepath/more.webassets
  license: GNU General Public License v2 (GPLv2)
  summary: 'Integrates Webassets into Morepath.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
