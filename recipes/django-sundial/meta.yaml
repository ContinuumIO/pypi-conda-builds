package:
  name: django-sundial
  version: "1.0.1"

source:
  fn: django-sundial-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sundial/django-sundial-1.0.1.tar.gz
  md5: 30658f77767ccdb49e7921c446fdb0f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sundial = django-sundial:main
    #
    # Would create an entry point called django-sundial that calls django-sundial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - pytz

  run:
    - python
    - django >=1.6
    - pytz

test:
  # Python imports
  imports:
    - sundial

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charettes/django-sundial
  license: MIT License
  summary: u'Django application providing database, form fields and middleware for timezone support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
