package:
  name: mongobox
  version: "0.1.6"

source:
  fn: mongobox-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/m/mongobox/mongobox-0.1.6.tar.gz
  md5: c6ca5d560f028e3f7964e96988575afc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongobox = mongobox:main
    #
    # Would create an entry point called mongobox that calls mongobox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - mongobox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/theorm/mongobox
  license: Apache Software License
  summary: 'Run sandboxed Mongo DB instance from a python application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
