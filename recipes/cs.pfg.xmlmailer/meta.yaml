package:
  name: cs.pfg.xmlmailer
  version: "0.2"

source:
  fn: cs.pfg.xmlmailer-0.2.zip
  url: https://pypi.python.org/packages/source/c/cs.pfg.xmlmailer/cs.pfg.xmlmailer-0.2.zip
  md5: 13cc9209cc8588059fd8b117a1d5944d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.pfg.xmlmailer = cs.pfg.xmlmailer:main
    #
    # Would create an entry point called cs.pfg.xmlmailer that calls cs.pfg.xmlmailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - cs
    - cs.pfg
    - cs.pfg.xmlmailer
    - cs.pfg.xmlmailer.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.app.example
  license: GPL
  summary: 'Send by e-mail an XML file representing the form data together with the e-mail'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
