package:
  name: setuptools-git-version
  version: "1.0.3"

source:
  fn: setuptools-git-version-1.0.3.zip
  url: https://pypi.python.org/packages/source/s/setuptools-git-version/setuptools-git-version-1.0.3.zip
  md5: 802555a15f289a6016cfe10c02a004b7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-git-version = setuptools-git-version:main
    #
    # Would create an entry point called setuptools-git-version that calls setuptools-git-version.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=8.0

  run:
    - python
    - setuptools
    - setuptools >=8.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pyfidelity/setuptools-git-version
  license: MIT License
  summary: 'Automatically set package version from Git.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
