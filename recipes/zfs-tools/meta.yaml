package:
  name: zfs-tools
  version: "0.4.0"

source:
  fn: zfs-tools-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zfs-tools/zfs-tools-0.4.0.tar.gz
  md5: 949851ece22fbd2f754d49c9ef8eb6ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zfs-tools = zfs-tools:main
    #
    # Would create an entry point called zfs-tools that calls zfs-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zfstools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Rudd-O/zfs-tools
  license: GNU General Public License (GPL)
  summary: 'ZFS synchronization and snapshotting tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
