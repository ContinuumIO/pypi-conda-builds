package:
  name: quintagroup.pfg.captcha
  version: "1.0.5"

source:
  fn: quintagroup.pfg.captcha-1.0.5.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.pfg.captcha/quintagroup.pfg.captcha-1.0.5.zip
  md5: 61bc334aba47bb8d1845d7d9c0546bdb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.pfg.captcha = quintagroup.pfg.captcha:main
    #
    # Would create an entry point called quintagroup.pfg.captcha that calls quintagroup.pfg.captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - quintagroup.captcha.core
    - products.ploneformgen

  run:
    - python
    - setuptools
    - setuptools
    - quintagroup.captcha.core
    - products.ploneformgen

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.pfg
    - quintagroup.pfg.captcha
    - quintagroup.pfg.captcha.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.quintagroup.com/products/wiki/quintagroup.pfg.captcha
  license: GPL
  summary: 'quintagroup.pfg.captcha is a package that allows to add captcha field to PloneFormGen forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
