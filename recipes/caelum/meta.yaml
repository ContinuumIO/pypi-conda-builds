package:
  name: caelum
  version: "0.3.3.23"

source:
  fn: caelum-0.3.3.23.tar.gz
  url: https://pypi.python.org/packages/source/c/caelum/caelum-0.3.3.23.tar.gz
  md5: e1ffdfb9bd00fdc1ad1a12c7e7884dba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caelum = caelum:main
    #
    # Would create an entry point called caelum that calls caelum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - geopy

  run:
    - python
    - geopy

test:
  # Python imports
  imports:
    - caelum
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nrcharles/caelum
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'library for weather sources (historical and forecasted)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
