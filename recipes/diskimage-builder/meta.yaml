package:
  name: diskimage-builder
  version: "0.1.44"

source:
  fn: diskimage-builder-0.1.44.tar.gz
  url: https://pypi.python.org/packages/source/d/diskimage-builder/diskimage-builder-0.1.44.tar.gz
  md5: e75b48bc6015f87161b580418fca022f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diskimage-builder = diskimage-builder:main
    #
    # Would create an entry point called diskimage-builder that calls diskimage-builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Golden Disk Image builder.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
