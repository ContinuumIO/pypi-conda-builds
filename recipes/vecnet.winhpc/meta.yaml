package:
  name: vecnet.winhpc
  version: "0.7.0"

source:
  fn: vecnet.winhpc-0.7.0.zip
  url: https://pypi.python.org/packages/source/v/vecnet.winhpc/vecnet.winhpc-0.7.0.zip
  md5: c22060d7fed1771b33f93bb41e42fd65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vecnet.winhpc = vecnet.winhpc:main
    #
    # Would create an entry point called vecnet.winhpc that calls vecnet.winhpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - vecnet
    - vecnet.winhpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vecnet/vecnet.winhpc
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Python interface for Windows HPC Server REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
