package:
  name: agha
  version: "0.1.0"

source:
  fn: agha-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/agha/agha-0.1.0.tar.gz
  md5: 209a01ea444a84956f3a9b98c44a674b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agha = agha:main
    #
    # Would create an entry point called agha that calls agha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2

  run:
    - python
    - requests >=2.2

test:
  # Python imports
  imports:
    - agha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mrsarm/python-agha
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Agha, Another GitHub API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
