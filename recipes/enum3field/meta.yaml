package:
  name: enum3field
  version: "0.1.1"

source:
  fn: enum3field-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/enum3field/enum3field-0.1.1.tar.gz
  md5: d92e83328ac98c1ee3b72857100e2916
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - enum3field = enum3field:main
    #
    # Would create an entry point called enum3field that calls enum3field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - enum3field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joshdata/enum3field
  license: CC0 (copyright waived)
  summary: 'A Django 1.7+ model field for use with Python 3 enums.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
