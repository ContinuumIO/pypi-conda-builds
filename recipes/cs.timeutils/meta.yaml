package:
  name: cs.timeutils
  version: "20150116"

source:
  fn: cs.timeutils-20150116.tar.gz
  url: https://pypi.python.org/packages/source/c/cs.timeutils/cs.timeutils-20150116.tar.gz
  md5: 2f2e1b3da5020f6361176e0e8b1ff1ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.timeutils = cs.timeutils:main
    #
    # Would create an entry point called cs.timeutils that calls cs.timeutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cameron_simpson/css/commits/all
  license: GNU General Public License v3 (GPLv3)
  summary: 'convenience routines for times and timing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
