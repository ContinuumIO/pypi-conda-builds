package:
  name: django-sqlfixtables
  version: "1.2"

source:
  fn: django-sqlfixtables-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sqlfixtables/django-sqlfixtables-1.2.tar.gz
  md5: a626b917c4bdab3eee27e95f8a0dc616
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sqlfixtables = django-sqlfixtables:main
    #
    # Would create an entry point called django-sqlfixtables that calls django-sqlfixtables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlfixtables
    - sqlfixtables.management
    - sqlfixtables.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seveas/django-sqlfixtables
  license: UNKNOWN
  summary: 'Management command for fixing tables after model changes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
