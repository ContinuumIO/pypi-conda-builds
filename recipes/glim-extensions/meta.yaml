package:
  name: glim-extensions
  version: "0.3.0"

source:
  fn: glim-extensions-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/g/glim-extensions/glim-extensions-0.3.0.tar.gz
  md5: 4741aa5ccc02d60d9d7b4715eece4f7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glim-extensions = glim-extensions:main
    #
    # Would create an entry point called glim-extensions that calls glim-extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - glim
    - sqlalchemy
    - jinja2
    - pymemcache
    - redis

  run:
    - python
    - glim
    - sqlalchemy
    - jinja2
    - pymemcache
    - redis

test:
  # Python imports
  imports:
    - glim_extensions
    - glim_extensions.db
    - glim_extensions.gredis
    - glim_extensions.job
    - glim_extensions.jslint
    - glim_extensions.less
    - glim_extensions.memcached
    - glim_extensions.view

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aacanakin/glim-extensions
  license: MIT License
  summary: 'Glim Extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
