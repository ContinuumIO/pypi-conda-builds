package:
  name: loopiadnssync
  version: "1.0.0"

source:
  fn: loopiadnssync-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/loopiadnssync/loopiadnssync-1.0.0.tar.gz
  md5: d05c631ffd51e3f3b500094f8f8b6e07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loopiadnssync = loopiadnssync:main
    #
    # Would create an entry point called loopiadnssync that calls loopiadnssync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - loopiadnssync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plweb.se
  license: UNKNOWN
  summary: 'Helps out with syncing domains to loopia.se a swedish company that provides a dynamic dns service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
