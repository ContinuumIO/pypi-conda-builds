package:
  name: testfslpackage
  version: "0.1.1"

source:
  fn: TestFslPackage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/T/TestFslPackage/TestFslPackage-0.1.1.tar.gz
  md5: 6eba244349b8dc586577e82d9e5c8577
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testfslpackage = testfslpackage:main
    #
    # Would create an entry point called testfslpackage that calls testfslpackage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fake-fsl-pkg
    - fake-fsl-pkg.test
    - testfslpackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/TestFslPackage
  license: LICENSE
  summary: 'Fake Pypi Package - Hello World.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
