package:
  name: corejet.jira
  version: "1.0a3"

source:
  fn: corejet.jira-1.0a3.zip
  url: https://pypi.python.org/packages/source/c/corejet.jira/corejet.jira-1.0a3.zip
  md5: 74c66e1f3cc4caddb6deb33466e0cb82
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corejet.jira = corejet.jira:main
    #
    # Would create an entry point called corejet.jira that calls corejet.jira.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - suds
    - corejet.core

  run:
    - python
    - setuptools
    - setuptools
    - suds
    - corejet.core

test:
  # Python imports
  imports:
    - corejet
    - corejet.jira

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://corejet.org
  license: ZPL 2.1
  summary: 'JIRA data source for corejet.testrunner'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
