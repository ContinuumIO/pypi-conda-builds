package:
  name: localbittorrentfilefinder
  version: "0.2.2"

source:
  fn: LocalBitTorrentFileFinder-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/L/LocalBitTorrentFileFinder/LocalBitTorrentFileFinder-0.2.2.tar.gz
  md5: a8b51b5a6a7b6fdde5832d85953438f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - localbittorrentfilefinder = localbittorrentfilefinder:main
    #
    # Would create an entry point called localbittorrentfilefinder that calls localbittorrentfilefinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lbff
    - localbittorrentfilefinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/torik/local-bittorrent-file-finder/wiki/Home
  license: Python Software Foundation License
  summary: 'Find local files described in a BitTorrent metafile'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
