package:
  name: pythoniccss
  version: "0.1.1"

source:
  fn: pythoniccss-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pythoniccss/pythoniccss-0.1.1.tar.gz
  md5: 0cd225defb3b07bac80522f737505a4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythoniccss = pythoniccss:main
    #
    # Would create an entry point called pythoniccss that calls pythoniccss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - libparsing

  run:
    - python
    - libparsing

test:
  # Python imports
  imports:
    - pythoniccss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sebastien/pythoniccss
  license: License :: OSI Approved :: BSD License
  summary: 'A Pythonic CSS pre-processor, designed as a replacement/upgrade to CleverCSS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
