package:
  name: flask-ayah
  version: "0.1"

source:
  fn: Flask-AYAH-0.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-AYAH/Flask-AYAH-0.1.zip
  md5: 8fa9bb4215ffa37b5123f2a5c47f1681
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-ayah = flask-ayah:main
    #
    # Would create an entry point called flask-ayah that calls flask-ayah.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - are-you-a-human

  run:
    - python
    - flask
    - are-you-a-human

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Chekosoft/flask-ayah
  license: BSD License
  summary: 'Are you a Human? Flask Extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
