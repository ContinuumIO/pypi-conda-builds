package:
  name: aha.application.coreblog3
  version: "0.6adev"

source:
  fn: aha.application.coreblog3-0.6adev.tar.gz
  url: https://pypi.python.org/packages/source/a/aha.application.coreblog3/aha.application.coreblog3-0.6adev.tar.gz
  md5: 5ec90c9a536a9c111791bbfe0aaf2f4e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aha.application.coreblog3 = aha.application.coreblog3:main
    #
    # Would create an entry point called aha.application.coreblog3 that calls aha.application.coreblog3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aha

  run:
    - python
    - setuptools
    - aha

test:
  # Python imports
  imports:
    - application

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://coreblog.org/coreblog3
  license: BSD
  summary: 'A blog application workins on Google App Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
