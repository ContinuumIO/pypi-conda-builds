package:
  name: collective.portlet.embed
  version: "1.1"

source:
  fn: collective.portlet.embed-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.embed/collective.portlet.embed-1.1.zip
  md5: df6fe1a724e64b5f633cbd1360f0c8fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.embed = collective.portlet.embed:main
    #
    # Would create an entry point called collective.portlet.embed that calls collective.portlet.embed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.embed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.portlet.embed
  license: GNU General Public License (GPL)
  summary: 'Embed an html snippet in a Plone portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
