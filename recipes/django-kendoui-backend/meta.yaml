package:
  name: django-kendoui-backend
  version: "0.6"

source:
  fn: django-kendoui-backend-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kendoui-backend/django-kendoui-backend-0.6.tar.gz
  md5: 18fccddde549c1df5e7b9909f29103bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kendoui-backend = django-kendoui-backend:main
    #
    # Would create an entry point called django-kendoui-backend that calls django-kendoui-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3.0
    - json_utils >=0.2
    - querystring_parser >=1.1

  run:
    - python
    - django >=1.3.0
    - json_utils >=0.2
    - querystring_parser >=1.1

test:
  # Python imports
  imports:
    - kendoui_backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tnajdek/django-kendoui-backend
  license: MIT
  summary: 'Kendo UI DataSource backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
