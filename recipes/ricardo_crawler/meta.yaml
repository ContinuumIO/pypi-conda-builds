package:
  name: ricardo_crawler
  version: "01.00.01"

source:
  fn: ricardo_crawler-01.00.01.tar.gz
  url: https://pypi.python.org/packages/source/r/ricardo_crawler/ricardo_crawler-01.00.01.tar.gz
  md5: 3b8a10ef6be799859d491260327466bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ricardo_crawler = ricardo_crawler:main
    #
    # Would create an entry point called ricardo_crawler that calls ricardo_crawler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup

  run:
    - python
    - beautifulsoup

test:
  # Python imports
  imports:
    - controllers
    - ricardo_crawler
    - scrapitems

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ricardogarfe/crawler-mswl
  license: GNU GPLv3
  summary: 'My first Web Scrapper in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
