package:
  name: isipy
  version: "1.0.1"

source:
  fn: ISIpy-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/I/ISIpy/ISIpy-1.0.1.tar.gz
  md5: 4dbd2f9a19a53290ddebc0304bf0c47b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isipy = isipy:main
    #
    # Would create an entry point called isipy that calls isipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - isipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://camelot.mssm.edu/~mac389/personal/code.html
  license: GNU General Public License 3
  summary: 'Implementation in Python of the pairwise interspike interval distance described in Kreuz et al. (2007)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
