package:
  name: ansunit
  version: "0.1.1"

source:
  fn: ansunit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/ansunit/ansunit-0.1.1.tar.gz
  md5: 10da2207e22f4cfe6f1fde7bd8e8ef86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansunit = ansunit:main
    #
    # Would create an entry point called ansunit that calls ansunit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ansunit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rndmcnlly/ansunit
  license: MIT
  summary: 'Declarative unit testing for answer set programming projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
