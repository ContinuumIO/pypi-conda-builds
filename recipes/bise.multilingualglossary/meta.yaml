package:
  name: bise.multilingualglossary
  version: "1.0"

source:
  fn: bise.multilingualglossary-1.0.zip
  url: https://pypi.python.org/packages/source/b/bise.multilingualglossary/bise.multilingualglossary-1.0.zip
  md5: b9838d5fab62c5d2c469ef1b6d71bbdd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bise.multilingualglossary = bise.multilingualglossary:main
    #
    # Would create an entry point called bise.multilingualglossary that calls bise.multilingualglossary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.datagridfield
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - products.datagridfield
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - bise
    - bise.multilingualglossary

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eea/bise.multilingualglossary
  license: GPL
  summary: 'Schema Extenders to add multilingual term and definitions to PloneGlossary'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
