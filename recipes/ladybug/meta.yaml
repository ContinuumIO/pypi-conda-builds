package:
  name: ladybug
  version: "0.0.2"

source:
  fn: ladybug-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/ladybug/ladybug-0.0.2.tar.gz
  md5: 3524d88e0ba7b795301b18a69f63017f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ladybug = ladybug:main
    #
    # Would create an entry point called ladybug that calls ladybug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ladybug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kdani3/ladybug.py
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Handle CSV files using table models and queries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
