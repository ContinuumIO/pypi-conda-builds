package:
  name: api_object_schema
  version: "1.5.1"

source:
  fn: api_object_schema-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/a/api_object_schema/api_object_schema-1.5.1.tar.gz
  md5: 13001db72c14466889b41b7f09fbc6c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - api_object_schema = api_object_schema:main
    #
    # Would create an entry point called api_object_schema that calls api_object_schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sentinels

  run:
    - python
    - sentinels

test:
  # Python imports
  imports:
    - api_object_schema

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/api_object_schema
  license: BSD3
  summary: 'Utilities for defining schemas of Pythonic objects interacting with external APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
