package:
  name: formatflowed
  version: "1.1.0"

source:
  fn: formatflowed-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/formatflowed/formatflowed-1.1.0.tar.gz
  md5: 2696a49163734a190204782643de39a2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formatflowed = formatflowed:main
    #
    # Would create an entry point called formatflowed that calls formatflowed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - formatflowed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/formatflowed
  license: Python Software Foundation License
  summary: 'RFC 3676 format=flowed text processing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
