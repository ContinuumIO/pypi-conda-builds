package:
  name: pyencode
  version: "0.2"

source:
  fn: PyENCODE-0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/PyENCODE/PyENCODE-0.2.tar.gz
  md5: 98b771ef844d38f06ff1d35afe3b8a29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyencode = pyencode:main
    #
    # Would create an entry point called pyencode that calls pyencode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - intervaltree_bio

  run:
    - python
    - intervaltree_bio

test:
  # Python imports
  imports:
    - pyencode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kt.era.ee/
  license: MIT License
  summary: 'Convenience package for accessing ENCODE (Encyclopedia of DNA Elements) project data at UCSC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
