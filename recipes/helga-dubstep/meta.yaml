package:
  name: helga-dubstep
  version: "0.1.0"

source:
  fn: helga-dubstep-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-dubstep/helga-dubstep-0.1.0.tar.gz
  md5: c6f00844cee7a54f2e3e86f7f2600816
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-dubstep = helga-dubstep:main
    #
    # Would create an entry point called helga-dubstep that calls helga-dubstep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-dubstep
  license: MIT License
  summary: "A match plugin that will respond with vaiable length 'wubwubwub' when someone mentions the word 'dubstep'."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
