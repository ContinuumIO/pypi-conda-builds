package:
  name: winstats
  version: "0.64"

source:
  fn: winstats-0.64.tar.gz
  url: https://pypi.python.org/packages/source/w/winstats/winstats-0.64.tar.gz
  md5: 1340968594aa39be6e4aeac39b49e312
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - winstats = winstats:main
    #
    # Would create an entry point called winstats that calls winstats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - winstats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mixmastamyk/winstats
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'A simple Windows status retrieval module with no additional dependencies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
