package:
  name: android-publish-cli
  version: "0.1.2"

source:
  fn: android-publish-cli-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/android-publish-cli/android-publish-cli-0.1.2.tar.gz
  md5: daac2ea4de21c39256a70353e6179ad0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - android-publish-cli = android-publish-cli:main
    #
    # Would create an entry point called android-publish-cli that calls android-publish-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client ==1.3.1

  run:
    - python
    - google-api-python-client ==1.3.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spreaker/android-publish-cli
  license: UNKNOWN
  summary: 'A simple CLI for Google Play Publish API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
