package:
  name: flask-jigger
  version: "0.0.2"

source:
  fn: Flask-Jigger-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Jigger/Flask-Jigger-0.0.2.tar.gz
  md5: 7689eaacf992a6267e0f398d8673dca9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-jigger = flask-jigger:main
    #
    # Would create an entry point called flask-jigger that calls flask-jigger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flask_jigger
    - flask_jigger/hateoas
    - flask_jigger/livedoc
    - flask_jigger/tests
    - flask_jigger/tests/livedoc
    - flask_jigger/util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robvanmieghem/flask-jigger
  license: Apache Software License
  summary: 'Web APIs for Flask, unintrusive.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
