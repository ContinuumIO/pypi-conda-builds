package:
  name: django-lazysignup-redux
  version: "1.0.0"

source:
  fn: django-lazysignup-redux-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lazysignup-redux/django-lazysignup-redux-1.0.0.tar.gz
  md5: 03705b3375dacac64f789884b6e2ffa5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lazysignup-redux = django-lazysignup-redux:main
    #
    # Would create an entry point called django-lazysignup-redux that calls django-lazysignup-redux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - six

  run:
    - python
    - setuptools
    - django >=1.7
    - six

test:
  # Python imports
  imports:
    - lazysignup
    - lazysignup.management
    - lazysignup.management.commands
    - lazysignup.migrations
    - lazysignup.templatetags
    - lazysignup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danfairs/django-lazysignup
  license: BSD License
  summary: 'Lazy signup for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
