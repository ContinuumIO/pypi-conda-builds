package:
  name: django-ldap-basic-auth
  version: "0.0.4"

source:
  fn: django-ldap-basic-auth-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ldap-basic-auth/django-ldap-basic-auth-0.0.4.tar.gz
  md5: 9feeb68f465e05c879f01c2e4dff08fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ldap-basic-auth = django-ldap-basic-auth:main
    #
    # Would create an entry point called django-ldap-basic-auth that calls django-ldap-basic-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_ldap_basic_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gustavosoares/django-ldap-basic-auth
  license: BSD License
  summary: 'Simple middleware that injects the Authorization header in the request after authenticating against a ldap server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
