package:
  name: django-dajax
  version: "0.9.2"

source:
  fn: django-dajax-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dajax/django-dajax-0.9.2.tar.gz
  md5: 72905bb645a4b97ae4a53cebf0fe0e9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dajax = django-dajax:main
    #
    # Would create an entry point called django-dajax that calls django-dajax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-dajaxice >=0.5

  run:
    - python
    - django-dajaxice >=0.5

test:
  # Python imports
  imports:
    - dajax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dajaxproject.com
  license: BSD License
  summary: 'Easy to use library to create asynchronous presentation logic with django and dajaxice'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
