package:
  name: eve-sqlalchemy
  version: "0.3.2"

source:
  fn: Eve-SQLAlchemy-0.3.2.zip
  url: https://pypi.python.org/packages/source/E/Eve-SQLAlchemy/Eve-SQLAlchemy-0.3.2.zip
  md5: 9de84d9d42aa236b580f6b0bc4412e06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eve-sqlalchemy = eve-sqlalchemy:main
    #
    # Would create an entry point called eve-sqlalchemy that calls eve-sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eve >=0.5
    - sqlalchemy >=0.8
    - flask-sqlalchemy >=1.0,<2.999

  run:
    - python
    - eve >=0.5
    - sqlalchemy >=0.8
    - flask-sqlalchemy >=1.0,<2.999

test:
  # Python imports
  imports:
    - eve_sqlalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/redturtle/eve-sqlachemy
  license: BSD License
  summary: 'REST API framework powered by Flask, SQLAlchemy and good intentions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
