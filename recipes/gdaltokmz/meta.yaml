package:
  name: gdaltokmz
  version: "1.0"

source:
  fn: gdaltokmz-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gdaltokmz/gdaltokmz-1.0.tar.gz
  md5: 278d525ffdbd1de11773764d3d5b72f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gdaltokmz = gdaltokmz:main
    #
    # Would create an entry point called gdaltokmz that calls gdaltokmz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gdaltokmz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gecensus.stanford.edu/gcensus-gt
  license: GNU General Public License (GPL)
  summary: 'Allows conversion from a GDAL-supported raster geographic format to the KMZ format used by the free Google Earth client. See www.gdal.org and earth.google.com.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
