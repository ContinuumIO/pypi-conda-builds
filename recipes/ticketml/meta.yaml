package:
  name: ticketml
  version: "0.1"

source:
  fn: ticketml-0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/ticketml/ticketml-0.1.tar.gz
  md5: 45545a41f0452e888b3bc4274f8918dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ticketml = ticketml:main
    #
    # Would create an entry point called ticketml that calls ticketml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.4
    - enum34 >=1.0.4

  run:
    - python
    - lxml >=3.4
    - enum34 >=1.0.4

test:
  # Python imports
  imports:
    - ticketml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lukegb/ticketml
  license: BSD License
  summary: 'TicketML is a simple markup language for receipt printers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
