package:
  name: django-commontags
  version: "0.1"

source:
  fn: django-commontags-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-commontags/django-commontags-0.1.tar.gz
  md5: a2f77e9258e3091edbbfe30c1b4534fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-commontags = django-commontags:main
    #
    # Would create an entry point called django-commontags that calls django-commontags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.1.1

  run:
    - python
    - django >=1.1.1

test:
  # Python imports
  imports:
    - commontags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python/ord/pypi/django-commontags
  license: BSD License
  summary: 'Common django template tags and filters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
