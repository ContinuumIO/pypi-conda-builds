package:
  name: django-gate2shop
  version: "1.0.2"

source:
  fn: django-gate2shop-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gate2shop/django-gate2shop-1.0.2.tar.gz
  md5: 37fb2c45329c14a44cad80d002fce8f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gate2shop = django-gate2shop:main
    #
    # Would create an entry point called django-gate2shop that calls django-gate2shop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - g2s
    - g2s.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gotlium/django-gate2shop
  license: GNU General Public License (GPL)
  summary: 'Gate2Shop gateway for accept payments on your website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
