package:
  name: django-chunked-upload
  version: "1.0.4"

source:
  fn: django-chunked-upload-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chunked-upload/django-chunked-upload-1.0.4.tar.gz
  md5: 75fd5db14a8b33a890066a3b3b2db65f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chunked-upload = django-chunked-upload:main
    #
    # Would create an entry point called django-chunked-upload that calls django-chunked-upload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chunked_upload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juliomalegria/django-chunked-upload
  license: MIT-Zero
  summary: 'Upload large files to Django in multiple chunks, with the ability to resume if the upload is interrupted.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
