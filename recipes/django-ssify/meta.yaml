package:
  name: django-ssify
  version: "0.2.5"

source:
  fn: django-ssify-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ssify/django-ssify-0.2.5.tar.gz
  md5: 87e90e2f1db47cced709642d4b88e633
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ssify = django-ssify:main
    #
    # Would create an entry point called django-ssify that calls django-ssify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - ssify
    - ssify.management
    - ssify.management.commands
    - ssify.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://git.mdrn.pl/django-ssify.git
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Two-phased rendering using SSI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
