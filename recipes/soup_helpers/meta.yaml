package:
  name: soup_helpers
  version: "0.0.8"

source:
  fn: soup_helpers-0.0.8.zip
  url: https://pypi.python.org/packages/source/s/soup_helpers/soup_helpers-0.0.8.zip
  md5: d76a2c5350e8383b614509132861d68c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - soup_helpers = soup_helpers:main
    #
    # Would create an entry point called soup_helpers that calls soup_helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4

  run:
    - python
    - beautifulsoup4

test:
  # Python imports
  imports:
    - soup_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bigmassa/soup_helpers
  license: MIT
  summary: 'A simple set of BeautifulSoup4 unit test helpers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
