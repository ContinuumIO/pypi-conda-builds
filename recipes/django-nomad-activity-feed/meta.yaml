package:
  name: django-nomad-activity-feed
  version: "0.1.1"

source:
  fn: django-nomad-activity-feed-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nomad-activity-feed/django-nomad-activity-feed-0.1.1.tar.gz
  md5: ebcc958a83f8cfc4194954652de6ab04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nomad-activity-feed = django-nomad-activity-feed:main
    #
    # Would create an entry point called django-nomad-activity-feed that calls django-nomad-activity-feed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - feeds
    - feeds.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Nomadblue/django-activity-feed
  license: BSD License
  summary: 'A simple Django app attach an activity feed to any Django model.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
