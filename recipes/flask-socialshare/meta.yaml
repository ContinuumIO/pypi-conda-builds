package:
  name: flask-socialshare
  version: "0.0.1"

source:
  fn: Flask-SocialShare-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SocialShare/Flask-SocialShare-0.0.1.tar.gz
  md5: 78fb38f80726490d4cb00b8608ca9bfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-socialshare = flask-socialshare:main
    #
    # Would create an entry point called flask-socialshare that calls flask-socialshare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Flask social sharing helpers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
