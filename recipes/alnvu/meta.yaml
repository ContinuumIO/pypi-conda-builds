package:
  name: alnvu
  version: "0.1.0"

source:
  fn: alnvu-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/alnvu/alnvu-0.1.0.tar.gz
  md5: a56cb170c31f081b661df541dc450ef0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alnvu = alnvu:main
    #
    # Would create an entry point called alnvu that calls alnvu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.7
    - colorbrewer >=0.1.1
    - reportlab >=3.0

  run:
    - python
    - jinja2 >=2.7
    - colorbrewer >=0.1.1
    - reportlab >=3.0

test:
  # Python imports
  imports:
    - alnvu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nhoffman/alnvu
  license: UNKNOWN
  summary: 'Reformat and condense multiple sequence alignments to highlight variability'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
