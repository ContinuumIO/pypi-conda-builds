package:
  name: django-generic-m2m
  version: "0.3.0"

source:
  fn: django-generic-m2m-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-m2m/django-generic-m2m-0.3.0.tar.gz
  md5: fc23f2143848fda5e4c1ca7a8bae28cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-m2m = django-generic-m2m:main
    #
    # Would create an entry point called django-generic-m2m that calls django-generic-m2m.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - example.site_app
    - genericm2m
    - genericm2m.genericm2m_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coleifer/django-generic-m2m
  license: MIT License
  summary: 'relate anything to anything'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
