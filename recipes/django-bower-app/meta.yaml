package:
  name: django-bower-app
  version: "0.5"

source:
  fn: django-bower-app-0.5.zip
  url: https://pypi.python.org/packages/source/d/django-bower-app/django-bower-app-0.5.zip
  md5: 04b66a6e4616f447459305baa62aab4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bower-app = django-bower-app:main
    #
    # Would create an entry point called django-bower-app that calls django-bower-app.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - djangobwr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/boblefrag/django-bower-app
  license: BSD License
  summary: u'Library to manage bower.json dependancies with ease'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
