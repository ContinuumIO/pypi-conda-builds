package:
  name: calcore
  version: "2.2.1"

source:
  fn: CalCore-2.2.1-2.tgz
  url: https://pypi.python.org/packages/source/c/calcore/CalCore-2.2.1-2.tgz
  md5: 5a6981425db08fe490a8f8c96444d244
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - calcore = calcore:main
    #
    # Would create an entry point called calcore that calls calcore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface >=3.0
    - zope.schema >=3.0
    - zope.i18nmessageid >=3.0
    - zope.event >=3.0
    - icalendar >=0.10

  run:
    - python
    - zope.interface >=3.0
    - zope.schema >=3.0
    - zope.i18nmessageid >=3.0
    - zope.event >=3.0
    - icalendar >=0.10

test:
  # Python imports
  imports:
    - calcore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cps-project.org/sections/projects/calendar_server 
  license: GNU General Public License (GPL)
  summary: 'Calendaring system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
