package:
  name: dap.plugins.hdf5
  version: "0.1.2"

source:
  fn: dap.plugins.hdf5-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.plugins.hdf5/dap.plugins.hdf5-0.1.2.tar.gz
  md5: 59529a4286f2f223d22354cf8ca3940d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.plugins.hdf5 = dap.plugins.hdf5:main
    #
    # Would create an entry point called dap.plugins.hdf5 that calls dap.plugins.hdf5.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dap[server] >=2.2.5.9
    - arrayterator >=0.2.7

  run:
    - python
    - setuptools
    - dap[server] >=2.2.5.9
    - arrayterator >=0.2.7

test:
  # Python imports
  imports:
    - dap
    - dap.plugins
    - dap.plugins.hdf5

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydap.org/plugins/hdf5.html
  license: MIT License
  summary: 'HDF5 plugin for pydap server based on Pytables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
