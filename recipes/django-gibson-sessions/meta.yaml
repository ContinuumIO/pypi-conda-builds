package:
  name: django-gibson-sessions
  version: "0.8"

source:
  fn: django-gibson-sessions-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gibson-sessions/django-gibson-sessions-0.8.tar.gz
  md5: be902a3d5128571372851c21de09c66f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gibson-sessions = django-gibson-sessions:main
    #
    # Would create an entry point called django-gibson-sessions that calls django-gibson-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygibson >=0.2.0

  run:
    - python
    - pygibson >=0.2.0

test:
  # Python imports
  imports:
    - gibson_sessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikalaiyurkin/django-gibson-sessions
  license: MIT
  summary: 'Gibson session backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
