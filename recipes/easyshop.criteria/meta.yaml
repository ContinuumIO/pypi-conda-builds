package:
  name: easyshop.criteria
  version: "0.1a1"

source:
  fn: easyshop.criteria-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.criteria/easyshop.criteria-0.1a1.tar.gz
  md5: 023fc914ecc177317abf9f87eb1f4000
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.criteria = easyshop.criteria:main
    #
    # Would create an entry point called easyshop.criteria that calls easyshop.criteria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.criteria
    - easyshop.criteria.adapters
    - easyshop.criteria.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'Criteria for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
