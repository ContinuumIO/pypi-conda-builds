package:
  name: medialog.summaryview
  version: "0.1.4"

source:
  fn: medialog.summaryview-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.summaryview/medialog.summaryview-0.1.4.tar.gz
  md5: 3f22b86bd162b56544badc8f26fa2d98
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.summaryview = medialog.summaryview:main
    #
    # Would create an entry point called medialog.summaryview that calls medialog.summaryview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield

test:
  # Python imports
  imports:
    - medialog
    - medialog.summaryview
    - medialog.summaryview.browser
    - medialog.summaryview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/medialog.summaryview
  license: GPL
  summary: 'Makes it possible to mark items so it does not show in summary view.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
