package:
  name: aludel
  version: "0.3.0"

source:
  fn: aludel-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aludel/aludel-0.3.0.tar.gz
  md5: baf53900badf960436945cb21fe388d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aludel = aludel:main
    #
    # Would create an entry point called aludel that calls aludel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - klein
    - sqlalchemy
    - alchimia >=0.4

  run:
    - python
    - twisted
    - klein
    - sqlalchemy
    - alchimia >=0.4

test:
  # Python imports
  imports:
    - aludel
    - aludel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/praekelt/aludel
  license: MIT
  summary: 'A framework for RESTful services using Klein and Alchimia.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
