package:
  name: django-js-variable-injector
  version: "1.1.1"

source:
  fn: django-js-variable-injector-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-js-variable-injector/django-js-variable-injector-1.1.1.tar.gz
  md5: d4648af7b68d9e689cb4ba46d758866a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-js-variable-injector = django-js-variable-injector:main
    #
    # Would create an entry point called django-js-variable-injector that calls django-js-variable-injector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - injector
    - injector/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/steinbachr/django-js-variable-injector
  license: MIT
  summary: 'A (more) elegant solution for injecting Django template variables into the context of an external Javascript file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
