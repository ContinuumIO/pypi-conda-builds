package:
  name: django-south-admin
  version: "0.0.3"

source:
  fn: django-south-admin-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-south-admin/django-south-admin-0.0.3.tar.gz
  md5: fb2e7979773da8b1cf41b36ed04bbf77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-south-admin = django-south-admin:main
    #
    # Would create an entry point called django-south-admin that calls django-south-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-object-tools
    - south

  run:
    - python
    - django
    - django-object-tools
    - south

test:
  # Python imports
  imports:
    - south_admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-south-admin
  license: BSD License
  summary: 'Django app allowing for South management through Admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
