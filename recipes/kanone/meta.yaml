package:
  name: kanone
  version: "0.4.1"

source:
  fn: kanone-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kanone/kanone-0.4.1.tar.gz
  md5: 97cc33e790e6d81bfe3e0827ac07d2d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kanone = kanone:main
    #
    # Would create an entry point called kanone that calls kanone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface

  run:
    - python
    - zope.interface

test:
  # Python imports
  imports:
    - kanone
    - kanone.adapter
    - kanone.util
    - kanone.validator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/doncatnip/kanone
  license: Unlicense
  summary: 'a validation library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
