package:
  name: infi.recipe.close_application
  version: "0.0.15"

source:
  fn: infi.recipe.close_application-0.0.15.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.recipe.close_application/infi.recipe.close_application-0.0.15.tar.gz
  md5: 65f5bfc6b7e8b11d4f2156e83cb34c60
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.recipe.close_application = infi.recipe.close_application:main
    #
    # Would create an entry point called infi.recipe.close_application that calls infi.recipe.close_application.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psutil >=2.0
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - psutil >=2.0
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - infi
    - infi.recipe
    - infi.recipe.close_application

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.recipe.close_application
  license: Python Software Foundation License
  summary: 'buildout recipe for terminating all executables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
