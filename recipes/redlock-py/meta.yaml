package:
  name: redlock-py
  version: "1.0.4"

source:
  fn: redlock-py-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/r/redlock-py/redlock-py-1.0.4.tar.gz
  md5: b56e80a12d2961d2c0071e8878a50e11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redlock-py = redlock-py:main
    #
    # Would create an entry point called redlock-py that calls redlock-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redlock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SPSCommerce/identity-service
  license: MIT
  summary: 'Redis locking mechanism'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
