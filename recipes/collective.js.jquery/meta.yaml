package:
  name: collective.js.jquery
  version: "1.3.2.1"

source:
  fn: collective.js.jquery-1.3.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.jquery/collective.js.jquery-1.3.2.1.tar.gz
  md5: ae3a6136d9436ce9094e90a9582857ef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.jquery = collective.js.jquery:main
    #
    # Would create an entry point called collective.js.jquery that calls collective.js.jquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.jquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'A packaging of the jQuery library for Zope'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
