package:
  name: django-classsettings
  version: "1.0.2"

source:
  fn: django-classsettings-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-classsettings/django-classsettings-1.0.2.tar.gz
  md5: 227e6e17ca94d5c28d1c0427d281ccae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-classsettings = django-classsettings:main
    #
    # Would create an entry point called django-classsettings that calls django-classsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.7

  run:
    - python
    - django >=1.4,<1.7

test:
  # Python imports
  imports:
    - classsettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dferens/django-classsettings
  license: MIT License
  summary: 'Adds ability to define Django settings with classes/functions and navigate each of them easily'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
