package:
  name: django-registration-defaults
  version: "0.4.4"

source:
  fn: django-registration-defaults-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-defaults/django-registration-defaults-0.4.4.tar.gz
  md5: 1ce33990d23e2af72de074efbc3f6101
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-defaults = django-registration-defaults:main
    #
    # Would create an entry point called django-registration-defaults that calls django-registration-defaults.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - registration_defaults

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yourcelf/django-registration-defaults
  license: MIT License
  summary: "Default templates and settings for James Bennett'sdjango-registration application."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
