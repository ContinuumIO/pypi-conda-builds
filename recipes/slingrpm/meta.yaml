package:
  name: slingrpm
  version: "1.0.0b3"

source:
  fn: slingrpm-1.0.0b3.tar.gz
  url: https://pypi.python.org/packages/source/s/slingrpm/slingrpm-1.0.0b3.tar.gz
  md5: 720237baf41805ee32940a8d89e6579c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slingrpm = slingrpm:main
    #
    # Would create an entry point called slingrpm that calls slingrpm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - boto
    - celery[redis]
    - flask
    - gunicorn
    - pyrpm-02strich
    - requests
    - simpleflock
    - xmltodict

  run:
    - python
    - boto
    - celery[redis]
    - flask
    - gunicorn
    - pyrpm-02strich
    - requests
    - simpleflock
    - xmltodict

test:
  # Python imports
  imports:
    - slingrpm
    - slingrpm.server
    - slingrpm.tasks
    - slingrpm.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ewdurbin/slingrpm
  license: MIT
  summary: 'a lightweight manager for pushing, removing, and promoting rpms in a remote yum repository'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
