package:
  name: django-adv-cache-tag
  version: "0.1.3"

source:
  fn: django-adv-cache-tag-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adv-cache-tag/django-adv-cache-tag-0.1.3.tar.gz
  md5: 90e22152a886bc9696efef37e747b14e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adv-cache-tag = django-adv-cache-tag:main
    #
    # Would create an entry point called django-adv-cache-tag that calls django-adv-cache-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adv_cache_tag
    - adv_cache_tag.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twidi/django-adv-cache-tag
  license: MIT License
  summary: 'An advanced template tag for caching in django : versionning, compress, partial caching, easy inheritance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
