package:
  name: oslo.context
  version: "0.4.0"

source:
  fn: oslo.context-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oslo.context/oslo.context-0.4.0.tar.gz
  md5: 06203f3a93c197d7608a60a0f46b5628
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oslo.context = oslo.context:main
    #
    # Would create an entry point called oslo.context that calls oslo.context.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Oslo Context library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
