package:
  name: sireader
  version: "1.0.2"

source:
  fn: sireader-1.0.2.tar.bz2
  url: https://pypi.python.org/packages/source/s/sireader/sireader-1.0.2.tar.bz2
  md5: 31fa2e4479d10a44084ea49254cab73f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sireader = sireader:main
    #
    # Would create an entry point called sireader that calls sireader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial
    - six

  run:
    - python
    - pyserial
    - six

test:
  # Python imports
  imports:
    - sireader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bosco.durcheinandertal.ch/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Python module to communicate with SportIdent main stations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
