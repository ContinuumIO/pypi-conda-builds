package:
  name: django-sslredirector
  version: "0.1.b1"

source:
  fn: django-sslredirector-0.1.b1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sslredirector/django-sslredirector-0.1.b1.tar.gz
  md5: 29717ec5065990635ea5c52fbf89f34c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sslredirector = django-sslredirector:main
    #
    # Would create an entry point called django-sslredirector that calls django-sslredirector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - sslredirector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/yilmazhuseyin/django-sslredirector
  license: GPLv3
  summary: 'A simple django application that manages http/https statuses of different views.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
