package:
  name: dbaas_dbmonitor
  version: "0.1.2"

source:
  fn: dbaas_dbmonitor-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dbaas_dbmonitor/dbaas_dbmonitor-0.1.2.tar.gz
  md5: bf6fe6e543bcd9b2a714f01cb593e2fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbaas_dbmonitor = dbaas_dbmonitor:main
    #
    # Would create an entry point called dbaas_dbmonitor that calls dbaas_dbmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbaas_dbmonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ricardosdias/dbaas_dbmonitor
  license: BSD License
  summary: 'Integration between dbaas and dbmonitor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
