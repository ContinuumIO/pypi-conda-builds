package:
  name: django-roughpages
  version: "0.1.2"

source:
  fn: django-roughpages-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-roughpages/django-roughpages-0.1.2.tar.gz
  md5: 6cfbcd55895cdb2ba580127732206633
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-roughpages = django-roughpages:main
    #
    # Would create an entry point called django-roughpages that calls django-roughpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - app_version
    - django-appconf

  run:
    - python
    - app_version
    - django-appconf

test:
  # Python imports
  imports:
    - roughpages
    - roughpages.backends
    - roughpages.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-roughpages
  license: MIT License
  summary: 'A enhanced permission system which enable logical permissionsystems to complex permissions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
