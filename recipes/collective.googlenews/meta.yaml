package:
  name: collective.googlenews
  version: "1.0rc3"

source:
  fn: collective.googlenews-1.0rc3.zip
  url: https://pypi.python.org/packages/source/c/collective.googlenews/collective.googlenews-1.0rc3.zip
  md5: 7a9f19513b24ce0492eaebc9443d68f1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.googlenews = collective.googlenews:main
    #
    # Would create an entry point called collective.googlenews that calls collective.googlenews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.googlenews
    - collective.googlenews.Extensions
    - collective.googlenews.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.googlenews
  license: GNU General Public License v2 (GPLv2)
  summary: 'Make easy to add your Plone site to Google News.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
