package:
  name: django-modelsettings
  version: "0.1.5"

source:
  fn: django-modelsettings-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modelsettings/django-modelsettings-0.1.5.tar.gz
  md5: f9ca6c2b80d2f42443c19f8ba29866d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modelsettings = django-modelsettings:main
    #
    # Would create an entry point called django-modelsettings that calls django-modelsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dbsettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IlyaSemenov/django-modelsettings
  license: BSD License
  summary: 'Django application settings defined and stored using Django ORM models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
