package:
  name: gooeydist
  version: "0.2"

source:
  fn: gooeydist-0.2.zip
  url: https://pypi.python.org/packages/source/g/gooeydist/gooeydist-0.2.zip
  md5: aa5c49f8aa8db321ac41d97036a78a9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gooeydist = gooeydist:main
    #
    # Would create an entry point called gooeydist that calls gooeydist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gooeydist
    - gooeydist.grammar
    - gooeydist.interpreter
    - gooeydist.pypeg2
    - gooeydist.pypeg2.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GooeyComps/gooey-dist
  license: UNKNOWN
  summary: 'Gooey Language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
