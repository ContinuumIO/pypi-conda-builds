package:
  name: collective.qextendedmenu
  version: "1.0a4"

source:
  fn: collective.qextendedmenu-1.0a4.zip
  url: https://pypi.python.org/packages/source/c/collective.qextendedmenu/collective.qextendedmenu-1.0a4.zip
  md5: eb1391d74ecfac9d16f036477b4197a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.qextendedmenu = collective.qextendedmenu:main
    #
    # Would create an entry point called collective.qextendedmenu that calls collective.qextendedmenu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - quintagroup.dropdownmenu
    - plone.app.z3cform
    - plone.app.textfield
    - lxml

  run:
    - python
    - setuptools
    - setuptools
    - quintagroup.dropdownmenu
    - plone.app.z3cform
    - plone.app.textfield
    - lxml

test:
  # Python imports
  imports:
    - collective
    - collective.qextendedmenu
    - collective.qextendedmenu.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'extend quintagroup.megameu'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
