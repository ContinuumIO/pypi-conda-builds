package:
  name: taobaopy
  version: "4.2.1"

source:
  fn: taobaopy-4.2.1.tar.gz
  url: https://pypi.python.org/packages/source/t/taobaopy/taobaopy-4.2.1.tar.gz
  md5: 40b414481bc837ad507e59f02f6d5c1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taobaopy = taobaopy:main
    #
    # Would create an entry point called taobaopy that calls taobaopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.3

  run:
    - python
    - requests >=2.4.3

test:
  # Python imports
  imports:
    - taobaopy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sempr/taobaopy
  license: BSD License
  summary: 'A Very Easy Learned Python SDK For TaoBao.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
