package:
  name: plone.app.widgets
  version: "2.0.0"

source:
  fn: plone.app.widgets-2.0.0.zip
  url: https://pypi.python.org/packages/source/p/plone.app.widgets/plone.app.widgets-2.0.0.zip
  md5: 187c3a85858081a3a57f01ded1a45846
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.widgets = plone.app.widgets:main
    #
    # Would create an entry point called plone.app.widgets that calls plone.app.widgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.resourceregistries >=2.1
    - plone.app.vocabularies >=2.1.12dev
    - products.cmfplone >=5.0.dev0
    - five.globalrequest

  run:
    - python
    - setuptools
    - setuptools
    - products.resourceregistries >=2.1
    - plone.app.vocabularies >=2.1.12dev
    - products.cmfplone >=5.0.dev0
    - five.globalrequest

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.widgets
    - plone.app.widgets.tests
    - plone.app.widgets.tests.robot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.app.widgets
  license: GPL
  summary: 'better plone widgets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
