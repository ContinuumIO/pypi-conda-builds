package:
  name: plex_missing_episodes
  version: "0.1.1"

source:
  fn: plex_missing_episodes-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plex_missing_episodes/plex_missing_episodes-0.1.1.tar.gz
  md5: ae70aa839d0a6e0d4b4dac1087615d91
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plex_missing_episodes = plex_missing_episodes:main
    #
    # Would create an entry point called plex_missing_episodes that calls plex_missing_episodes.main()

    - plex_missing_episodes = plex_missing_episodes.plex_missing_episodes:main.command

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - opster
    - tvdb_api
    - plexapi

  run:
    - python
    - opster
    - tvdb_api
    - plexapi

test:
  # Python imports
  imports:
    - plex_missing_episodes

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - plex_missing_episodes --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spillevink/plex_missing_episodes
  license: GNU GPLv3
  summary: 'Torrent RSS downloader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
