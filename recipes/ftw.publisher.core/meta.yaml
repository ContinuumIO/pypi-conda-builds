package:
  name: ftw.publisher.core
  version: "2.4.1"

source:
  fn: ftw.publisher.core-2.4.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.publisher.core/ftw.publisher.core-2.4.1.zip
  md5: b888d25dc45b7851c149f0f6e58db5dc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.publisher.core = ftw.publisher.core:main
    #
    # Would create an entry point called ftw.publisher.core that calls ftw.publisher.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - accesscontrol
    - products.archetypes
    - products.cmfcore
    - zconfig
    - zodb3
    - zope2
    - plone.app.blob
    - plone.namedfile
    - plone.portlets
    - setuptools
    - zope.component
    - zope.dottedname
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - accesscontrol
    - products.archetypes
    - products.cmfcore
    - zconfig
    - zodb3
    - zope2
    - plone.app.blob
    - plone.namedfile
    - plone.portlets
    - setuptools
    - zope.component
    - zope.dottedname
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - ftw
    - ftw.publisher
    - ftw.publisher.core
    - ftw.publisher.core.adapters
    - ftw.publisher.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.publisher.core
  license: GPL2
  summary: 'Staging and publishing addon for Plone contents.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
