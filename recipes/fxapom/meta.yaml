package:
  name: fxapom
  version: "1.2"

source:
  fn: fxapom-1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fxapom/fxapom-1.2.tar.gz
  md5: 367e53d208689e028ebf21ba7adaa902
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fxapom = fxapom:main
    #
    # Would create an entry point called fxapom that calls fxapom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyfxa ==0.0.5

  run:
    - python
    - pyfxa ==0.0.5

test:
  # Python imports
  imports:
    - fxapom
    - fxapom.pages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/fxapom
  license: MPL 2.0
  summary: 'Mozilla Firefox Accounts Page Object Model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
