package:
  name: bitbucket
  version: "0.4a"

source:
  fn: BitBucket-0.4a.tar.gz
  url: https://pypi.python.org/packages/source/B/BitBucket/BitBucket-0.4a.tar.gz
  md5: 092d073b4adc25d9dd5330a59f8f20a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitbucket = bitbucket:main
    #
    # Would create an entry point called bitbucket that calls bitbucket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bitbucket
    - s3amazon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://other10percent.com/?cat=9
  license: bitbucket2github
  summary: 'Module for accessing Amazon S3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
