package:
  name: atreal.filestorage.common
  version: "1.0.1"

source:
  fn: atreal.filestorage.common-1.0.1.zip
  url: https://pypi.python.org/packages/source/a/atreal.filestorage.common/atreal.filestorage.common-1.0.1.zip
  md5: aa0a922bf7c757dadeb339b485a2b9cf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.filestorage.common = atreal.filestorage.common:main
    #
    # Would create an entry point called atreal.filestorage.common that calls atreal.filestorage.common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - atreal
    - atreal.filestorage
    - atreal.filestorage.common

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.atreal.fr/
  license: GPL
  summary: 'Defines a common API to handle file objects independently of storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
