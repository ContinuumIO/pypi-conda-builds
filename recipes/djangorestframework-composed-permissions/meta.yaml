package:
  name: djangorestframework-composed-permissions
  version: "0.1"

source:
  fn: djangorestframework-composed-permissions-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-composed-permissions/djangorestframework-composed-permissions-0.1.tar.gz
  md5: 962431612a28ff2a31721e7c3aeb5bac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-composed-permissions = djangorestframework-composed-permissions:main
    #
    # Would create an entry point called djangorestframework-composed-permissions that calls djangorestframework-composed-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - restfw_composed_permissions
    - restfw_composed_permissions.generic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/djangorestframework-composed-permissions
  license: BSD License
  summary: 'Composed permissions for django-rest-framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
