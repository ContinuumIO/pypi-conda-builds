package:
  name: medialog.fullnamefield
  version: "0.1b1"

source:
  fn: medialog.fullnamefield-0.1b1.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.fullnamefield/medialog.fullnamefield-0.1b1.tar.gz
  md5: a884dca7c9a3a6362269b03caa0a02b8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.fullnamefield = medialog.fullnamefield:main
    #
    # Would create an entry point called medialog.fullnamefield that calls medialog.fullnamefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.archetypes

  run:
    - python
    - setuptools
    - setuptools
    - products.archetypes

test:
  # Python imports
  imports:
    - medialog
    - medialog.fullnamefield
    - medialog.fullnamefield.content
    - medialog.fullnamefield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GPL
  summary: 'Archetypes Widget dealing with fullname.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
