package:
  name: imhotep_pep8
  version: "0.0.3"

source:
  fn: imhotep_pep8-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/imhotep_pep8/imhotep_pep8-0.0.3.tar.gz
  md5: 5d4def99a62482c593f79529c82c192d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imhotep_pep8 = imhotep_pep8:main
    #
    # Would create an entry point called imhotep_pep8 that calls imhotep_pep8.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - imhotep_pep8

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/justinabrahms/imhotep_pep8
  license: MIT
  summary: 'An imhotep plugin for pep8 validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
