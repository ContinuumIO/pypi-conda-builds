package:
  name: django-bingo
  version: "1.5.0"

source:
  fn: django-bingo-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bingo/django-bingo-1.5.0.tar.gz
  md5: 0d3ef87d2bf7d806ff9080423659cad7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bingo = django-bingo:main
    #
    # Would create an entry point called django-bingo that calls django-bingo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<=1.7
    - django-jquery ==1.9.1
    - django-colorful ==0.1.3
    - pytz ==2013b
    - pillow
    - django-registration ==1.0

  run:
    - python
    - django >=1.6,<=1.7
    - django-jquery ==1.9.1
    - django-colorful ==0.1.3
    - pytz ==2013b
    - pillow
    - django-registration ==1.0

test:
  # Python imports
  imports:
    - bingo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allo-/django-bingo
  license: GNU Affero General Public License v3
  summary: 'Bingo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
