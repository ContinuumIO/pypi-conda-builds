package:
  name: chemview
  version: "0.1.1"

source:
  fn: chemview-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chemview/chemview-0.1.1.tar.gz
  md5: 7903e7091e0ddbd90dd15bc9aa075a95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chemview = chemview:main
    #
    # Would create an entry point called chemview that calls chemview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - ipython[notebook]
    - numba

  run:
    - python
    - numpy
    - ipython[notebook]
    - numba

test:
  # Python imports
  imports:
    - chemview
    - chemview.static

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gabrielelanaro/chemview
  license: UNKNOWN
  summary: 'Interactive molecular viewer for IPython notebook'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
