package:
  name: cwbrowser
  version: "0.1.0"

source:
  fn: cwbrowser-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cwbrowser/cwbrowser-0.1.0.tar.gz
  md5: e4f554956a2adc096dc421aeafea693e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cwbrowser = cwbrowser:main
    #
    # Would create an entry point called cwbrowser that calls cwbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko

  run:
    - python
    - paramiko

test:
  # Python imports
  imports:
    - cwbrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neurospin/rql_download.git
  license: CeCILL-B
  summary: 'CWBROWSER'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
