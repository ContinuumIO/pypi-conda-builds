package:
  name: ifrit
  version: "0.3.1"

source:
  fn: ifrit-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ifrit/ifrit-0.3.1.tar.gz
  md5: 120a28f896696c5c88d2bc2136671448
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ifrit = ifrit:main
    #
    # Would create an entry point called ifrit that calls ifrit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.configuration
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - zope.component
    - zope.configuration
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - ifrit
    - ifrit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://agendaless.com/Members/tseaver/software/ifrit
  license: ZPL 2.1
  summary: 'XPath / ElementTree path adapters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
