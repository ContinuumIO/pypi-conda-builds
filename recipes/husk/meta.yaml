package:
  name: husk
  version: "0.1.2"

source:
  fn: husk-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/husk/husk-0.1.2.tar.gz
  md5: f903284647082a5bc8dfa594368b5b8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - husk = husk:main
    #
    # Would create an entry point called husk that calls husk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - husk
    - husk.commands
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bruth.github.com/husk/
  license: BSD License
  summary: 'Husk is an API and CLI written in Python for the Cornell Note-taking System.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
