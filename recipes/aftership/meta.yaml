package:
  name: aftership
  version: "0.1"

source:
  fn: aftership-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aftership/aftership-0.1.tar.gz
  md5: ec0a6d37798becea39de2b6f1ee693b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aftership = aftership:main
    #
    # Would create an entry point called aftership that calls aftership.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - python-dateutil

  run:
    - python
    - requests
    - python-dateutil

test:
  # Python imports
  imports:
    - aftership

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AfterShip/aftership-python
  license: MIT License
  summary: 'Python SDK of AfterShip API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
