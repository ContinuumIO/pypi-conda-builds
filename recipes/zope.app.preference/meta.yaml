package:
  name: zope.app.preference
  version: "3.8.1"

source:
  fn: zope.app.preference-3.8.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.preference/zope.app.preference-3.8.1.tar.gz
  md5: ab6906261854c61ff9f0a13c7612d3e8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.preference = zope.app.preference:main
    #
    # Would create an entry point called zope.app.preference that calls zope.app.preference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.preference
    - zope.app.tree

  run:
    - python
    - setuptools
    - zope.preference
    - zope.app.tree

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.preference

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.preference
  license: Zope Public License
  summary: 'User Preferences Framework ZMI UI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
