package:
  name: acidfile
  version: "1.2.0"

source:
  fn: acidfile-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/acidfile/acidfile-1.2.0.tar.gz
  md5: fd9e36bc1569107c1e75c109249e59bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acidfile = acidfile:main
    #
    # Would create an entry point called acidfile that calls acidfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - acidfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nilp0inter/acidfile
  license: GNU General Public License v3 (GPLv3)
  summary: 'ACID transaction with common files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
