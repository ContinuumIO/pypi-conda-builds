package:
  name: setuptools-green
  version: "0.0.2"

source:
  fn: setuptools-green-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-green/setuptools-green-0.0.2.tar.gz
  md5: 6389af4094c446f5a219a30bb3869981
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-green = setuptools-green:main
    #
    # Would create an entry point called setuptools-green that calls setuptools-green.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - green

  run:
    - python
    - setuptools
    - setuptools
    - green

test:
  # Python imports
  imports:
    - setuptools_green
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/polysquare/setuptools-green
  license: MIT License
  summary: "Provides a 'test' command for running tests with green"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
