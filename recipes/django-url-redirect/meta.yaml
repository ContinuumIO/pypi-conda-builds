package:
  name: django-url-redirect
  version: "0.1.0"

source:
  fn: django-url-redirect-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-redirect/django-url-redirect-0.1.0.tar.gz
  md5: ab40f2a57dba732989e27e3c2c2d202c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-redirect = django-url-redirect:main
    #
    # Would create an entry point called django-url-redirect that calls django-url-redirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - url_redirect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gizmag/django-url-redirect/
  license: UNKNOWN
  summary: 'Simple redirect middleware for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
