package:
  name: django-c5filemanager
  version: "0.2"

source:
  fn: django-c5filemanager-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-c5filemanager/django-c5filemanager-0.2.tar.gz
  md5: 07f41f90f37d37c0aff6ade70229b4ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-c5filemanager = django-c5filemanager:main
    #
    # Would create an entry point called django-c5filemanager that calls django-c5filemanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=2.1.0
    - pil

  run:
    - python
    - simplejson >=2.1.0
    - pil

test:
  # Python imports
  imports:
    - c5filemanager
    - c5filemanager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mornie.org/projects/django-c5filemanager/
  license: BSD License
  summary: 'Django connector for Core Five Filemanager.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
