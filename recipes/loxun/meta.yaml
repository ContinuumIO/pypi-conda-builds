package:
  name: loxun
  version: "2.0"

source:
  fn: loxun-2.0.zip
  url: https://pypi.python.org/packages/source/l/loxun/loxun-2.0.zip
  md5: 58575afc9bf1ec58f6dd9e2ca3f41641
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loxun = loxun:main
    #
    # Would create an entry point called loxun that calls loxun.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - loxun

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/loxun/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'large output in XML using unicode and namespaces'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
