package:
  name: jabberbot
  version: "0.15"

source:
  fn: jabberbot-0.15.tar.gz
  url: https://pypi.python.org/packages/source/j/jabberbot/jabberbot-0.15.tar.gz
  md5: 081be2f6d8c82740b9906bf5b8562e7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jabberbot = jabberbot:main
    #
    # Would create an entry point called jabberbot that calls jabberbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jabberbot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://thp.io/2007/python-jabberbot/
  license: GNU General Public License version 3 or later
  summary: 'A framework for writing Jabber/XMPP bots and services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
