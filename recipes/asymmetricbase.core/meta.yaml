package:
  name: asymmetricbase.core
  version: "0.1.1-201404231427"

source:
  fn: asymmetricbase.core-0.1.1-201404231427.zip
  url: https://pypi.python.org/packages/source/a/asymmetricbase.core/asymmetricbase.core-0.1.1-201404231427.zip
  md5: f4947dc4e1ca78ad9cb0ee9a1dbf9bd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asymmetricbase.core = asymmetricbase.core:main
    #
    # Would create an entry point called asymmetricbase.core that calls asymmetricbase.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz
    - south <2.0
    - hamlpy
    - pillow
    - boto
    - asymmetricbase.fields
    - asymmetricbase.logging
    - asymmetricbase.forms
    - asymmetricbase.testing

  run:
    - python
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz
    - south <2.0
    - hamlpy
    - pillow
    - boto
    - asymmetricbase.fields
    - asymmetricbase.logging
    - asymmetricbase.forms
    - asymmetricbase.testing

test:
  # Python imports
  imports:
    - asymmetricbase
    - asymmetricbase._models
    - asymmetricbase.locale
    - asymmetricbase.locale.canada
    - asymmetricbase.locale.canada.forms
    - asymmetricbase.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AsymmetricVentures/asym-core
  license: GNU General Public License v2 (GPLv2)
  summary: 'Core utilities for Asymmeric Base Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
