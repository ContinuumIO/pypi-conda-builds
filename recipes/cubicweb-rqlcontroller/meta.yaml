package:
  name: cubicweb-rqlcontroller
  version: "0.3.0"

source:
  fn: cubicweb-rqlcontroller-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-rqlcontroller/cubicweb-rqlcontroller-0.3.0.tar.gz
  md5: 0d4c7bd07f9452019b1350049cd666cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-rqlcontroller = cubicweb-rqlcontroller:main
    #
    # Would create an entry point called cubicweb-rqlcontroller that calls cubicweb-rqlcontroller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.11

  run:
    - python
    - cubicweb >=3.17.11

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-rqlcontroller
  license: LGPL
  summary: 'restfull rql edition capabilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
