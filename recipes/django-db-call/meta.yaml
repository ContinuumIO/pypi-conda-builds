package:
  name: django-db-call
  version: "0.2.0"

source:
  fn: django-db-call-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-call/django-db-call-0.2.0.tar.gz
  md5: 6f52ba42af83f58ebcd25022c7438535
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-call = django-db-call:main
    #
    # Would create an entry point called django-db-call that calls django-db-call.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - first >=2.0.1

  run:
    - python
    - first >=2.0.1

test:
  # Python imports
  imports:
    - django_db_call

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paetzke/django-db-call
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
