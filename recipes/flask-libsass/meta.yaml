package:
  name: flask-libsass
  version: "1.1.0"

source:
  fn: Flask-libsass-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-libsass/Flask-libsass-1.1.0.tar.gz
  md5: d7c426c3aa901eb257d81407d2e931fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-libsass = flask-libsass:main
    #
    # Would create an entry point called flask-libsass that calls flask-libsass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - sass

  run:
    - python
    - flask >=0.9
    - sass

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bwhmather/flask-libsass/
  license: BSD License
  summary: 'Flask extension for building css from sass or scss'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
