package:
  name: pycpfcnpj
  version: "1.0.1"

source:
  fn: pycpfcnpj-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pycpfcnpj/pycpfcnpj-1.0.1.tar.gz
  md5: f4bf101f20e785df67a5c5347a6af521
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pycpfcnpj = pycpfcnpj:main
    #
    # Would create an entry point called pycpfcnpj that calls pycpfcnpj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pycpfcnpj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matheuscas/pycpfcnpj
  license: MIT License
  summary: 'Python module for brazilian register numbers for persons (CPF) and companies (CNPJ).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
