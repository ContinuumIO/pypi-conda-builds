package:
  name: django-template-minimizer
  version: "0.1.7"

source:
  fn: django-template-minimizer-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-minimizer/django-template-minimizer-0.1.7.tar.gz
  md5: 48dd9fb886191b01a51aaa3fdac5b199
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-minimizer = django-template-minimizer:main
    #
    # Would create an entry point called django-template-minimizer that calls django-template-minimizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Charles-Kaminski/django-template-minimizer
  license: MIT License
  summary: 'Minimize your Django templates so that your HTML is served up already minimized.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
