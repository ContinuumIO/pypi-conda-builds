package:
  name: django-generic-plus
  version: "1.2.9"

source:
  fn: django-generic-plus-1.2.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-plus/django-generic-plus-1.2.9.tar.gz
  md5: 5e22f051001a463f46e02fc863d368fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-plus = django-generic-plus:main
    #
    # Would create an entry point called django-generic-plus that calls django-generic-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.7.0

  run:
    - python
    - setuptools
    - six >=1.7.0

test:
  # Python imports
  imports:
    - generic_plus
    - generic_plus.build
    - generic_plus.forms
    - generic_plus.patch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theatlantic/django-generic-plus
  license: UNKNOWN
  summary: 'Django model field that combines the functionality of GenericForeignKey and FileField'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
