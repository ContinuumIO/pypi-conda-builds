package:
  name: bigquery-python
  version: "1.0.0"

source:
  fn: BigQuery-Python-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BigQuery-Python/BigQuery-Python-1.0.0.tar.gz
  md5: 14995aa88ba959879fb22aad4c59cad4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigquery-python = bigquery-python:main
    #
    # Would create an entry point called bigquery-python that calls bigquery-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client
    - pyopenssl
    - httplib2
    - python-dateutil

  run:
    - python
    - google-api-python-client
    - pyopenssl
    - httplib2
    - python-dateutil

test:
  # Python imports
  imports:
    - bigquery
    - bigquery.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tylertreat/BigQuery-Python
  license: Apache
  summary: 'Simple Python client for interacting with Google BigQuery.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
