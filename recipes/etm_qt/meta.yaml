package:
  name: etm_qt
  version: "2.3.27"

source:
  fn: etm_qt-2.3.27.tar.gz
  url: https://pypi.python.org/packages/source/e/etm_qt/etm_qt-2.3.27.tar.gz
  md5: a1a20274be97c1594f6b81d7cb8c729e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etm_qt = etm_qt:main
    #
    # Would create an entry point called etm_qt that calls etm_qt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python >=2.7,<3.0
    - python-dateutil >=1.5
    - pyyaml >=3.10

  run:
    - python
    - python >=2.7,<3.0
    - python-dateutil >=1.5
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - etmQt
    - etm_qt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://people.duke.edu/~dgraham/etmqt
  license: GNU General Public License (GPL)
  summary: 'event and task manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
