package:
  name: django-feedreader
  version: "1.2.0"

source:
  fn: django-feedreader-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-feedreader/django-feedreader-1.2.0.tar.gz
  md5: 4ea3d646dc0103b182b2f024dba89e9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-feedreader = django-feedreader:main
    #
    # Would create an entry point called django-feedreader that calls django-feedreader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.8
    - django-braces ==1.4.0
    - factory-boy ==2.5.1
    - feedparser ==5.1.3
    - mock ==1.0.1
    - pytz >=2015.2

  run:
    - python
    - django ==1.8
    - django-braces ==1.4.0
    - factory-boy ==2.5.1
    - feedparser ==5.1.3
    - mock ==1.0.1
    - pytz >=2015.2

test:
  # Python imports
  imports:
    - feedreader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ahernp/django-feedreader
  license: BSD License
  summary: 'A simple Django app to aggregate RSS feeds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
