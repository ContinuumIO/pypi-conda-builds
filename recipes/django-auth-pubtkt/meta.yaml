package:
  name: django-auth-pubtkt
  version: "1.1.2"

source:
  fn: django-auth-pubtkt-1.1.2.zip
  url: https://pypi.python.org/packages/source/d/django-auth-pubtkt/django-auth-pubtkt-1.1.2.zip
  md5: 4f2b19b4ca88ae6be58ad375a843c7fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-pubtkt = django-auth-pubtkt:main
    #
    # Would create an entry point called django-auth-pubtkt that calls django-auth-pubtkt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m2crypto
    - django >=1.3

  run:
    - python
    - m2crypto
    - django >=1.3

test:
  # Python imports
  imports:
    - django_auth_pubtkt
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Baguage/django-auth-pubtkt
  license: Apache Software License
  summary: 'Implementation of mod_auth_pubtkt as Django middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
