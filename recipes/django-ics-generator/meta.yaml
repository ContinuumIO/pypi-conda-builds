package:
  name: django-ics-generator
  version: "0.1.1"

source:
  fn: django-ics-generator-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ics-generator/django-ics-generator-0.1.1.tar.gz
  md5: 950f0849b7ad1abd6b04fcb7223fa569
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ics-generator = django-ics-generator:main
    #
    # Would create an entry point called django-ics-generator that calls django-ics-generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ics_generator
    - ics_generator.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-ics-generator
  license: The MIT License
  summary: 'Django app to return an .ics file when called via http://www.keith-wood.name/icalendar.html \n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
