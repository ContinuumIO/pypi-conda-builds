package:
  name: zc.recipe.icu
  version: "1.0.0b1"

source:
  fn: zc.recipe.icu-1.0.0b1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.recipe.icu/zc.recipe.icu-1.0.0b1.tar.gz
  md5: 9f374b5f5f4d6cbd396eaf2f43029e31
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.recipe.icu = zc.recipe.icu:main
    #
    # Would create an entry point called zc.recipe.icu that calls zc.recipe.icu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.recipe
    - zc.recipe.icu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/zc.recipe.icu
  license: Zope Public License
  summary: 'ZC Buildout recipe for installing the ICU library into a buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
