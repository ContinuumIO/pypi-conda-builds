package:
  name: fileclusters
  version: "0.0.2"

source:
  fn: fileclusters-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fileclusters/fileclusters-0.0.2.tar.gz
  md5: 54b4a320e88a6b54df916dd4e34a1ea9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fileclusters = fileclusters:main
    #
    # Would create an entry point called fileclusters that calls fileclusters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.7
    - python-dateutil
    - httplib2
    - simplejson
    - slugify
    - django-dajax
    - django-dajaxice
    - django-sekizai
    - paramiko
    - chardet
    - pillow
    - pytz
    - django-guardian
    - easy-thumbnails
    - south
    - django-userena
    - dateutils
    - pyncomings
    - psycopg2

  run:
    - python
    - django >=1.6,<1.7
    - python-dateutil
    - httplib2
    - simplejson
    - slugify
    - django-dajax
    - django-dajaxice
    - django-sekizai
    - paramiko
    - chardet
    - pillow
    - pytz
    - django-guardian
    - easy-thumbnails
    - south
    - django-userena
    - dateutils
    - pyncomings
    - psycopg2

test:
  # Python imports
  imports:
    - fileclusters
    - fileclusters.migrations
    - fileclusters.templatetags
    - utils
    - utils.cache
    - utils.cache.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'COMODA Fileclusters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
