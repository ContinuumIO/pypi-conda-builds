package:
  name: flightrecorder
  version: "0.9.4"

source:
  fn: flightrecorder-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/f/flightrecorder/flightrecorder-0.9.4.tar.gz
  md5: 4f5e9ebd80ed6889cab63c9a48cb3144
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flightrecorder = flightrecorder:main
    #
    # Would create an entry point called flightrecorder that calls flightrecorder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flightrecorder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twpayne/flightrecorder
  license: UNKNOWN
  summary: 'Utilities for flight recorders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
