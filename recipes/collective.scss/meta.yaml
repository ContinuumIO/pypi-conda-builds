package:
  name: collective.scss
  version: "1.0b1"

source:
  fn: collective.scss-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.scss/collective.scss-1.0b1.tar.gz
  md5: dc4c6b07e41a8961dc829e0d365d2b0a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.scss = collective.scss:main
    #
    # Would create an entry point called collective.scss that calls collective.scss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - scss

  run:
    - python
    - setuptools
    - setuptools
    - scss

test:
  # Python imports
  imports:
    - collective
    - collective.scss
    - collective.scss.tests
    - collective.scss.tests.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Integration of SCSS in the CMS Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
