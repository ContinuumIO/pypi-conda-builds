package:
  name: ansible-playbook-debugger
  version: "0.2.3"

source:
  fn: ansible-playbook-debugger-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/a/ansible-playbook-debugger/ansible-playbook-debugger-0.2.3.tar.gz
  md5: 6ef60b76a9fd7ecc14580af49f1ffbdf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansible-playbook-debugger = ansible-playbook-debugger:main
    #
    # Would create an entry point called ansible-playbook-debugger that calls ansible-playbook-debugger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansible

  run:
    - python
    - ansible

test:
  # Python imports
  imports:
    - ansibledebugger
    - ansibledebugger.interpreter
    - ansibledebugger.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ks888/ansible-playbook-debugger
  license: GNU General Public License v3 (GPLv3)
  summary: 'Debugger for Ansible Playbook'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
