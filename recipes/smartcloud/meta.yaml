package:
  name: smartcloud
  version: "1.1.0"

source:
  fn: SmartCloud-1.1.0.zip
  url: https://pypi.python.org/packages/source/S/SmartCloud/SmartCloud-1.1.0.zip
  md5: 736aa1a1273fa5f4457f8db136245c14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smartcloud = smartcloud:main
    #
    # Would create an entry point called smartcloud that calls smartcloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pygame

  run:
    - python
    - pygame

test:
  # Python imports
  imports:
    - SmartCloud
    - smartcloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/NickMonzillo/SmartCloud
  license: UNKNOWN
  summary: 'Text visualization using word frequencies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
