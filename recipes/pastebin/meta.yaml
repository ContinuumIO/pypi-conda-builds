package:
  name: pastebin
  version: "1.1.1"

source:
  fn: Pastebin-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Pastebin/Pastebin-1.1.1.tar.gz
  md5: d9374efc32f442e9fb4589d4f5a9b74b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pastebin = pastebin:main
    #
    # Would create an entry point called pastebin that calls pastebin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - pastebin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://morrolan.github.com/PastebinAPI/
  license: GNU General Public License (GPL)
  summary: 'Python Pastebin API interaction object.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
