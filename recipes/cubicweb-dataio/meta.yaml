package:
  name: cubicweb-dataio
  version: "0.5.0"

source:
  fn: cubicweb-dataio-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-dataio/cubicweb-dataio-0.5.0.tar.gz
  md5: 78d98c87e594c4ade3af715e4c772100
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-dataio = cubicweb-dataio:main
    #
    # Would create an entry point called cubicweb-dataio that calls cubicweb-dataio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.1

  run:
    - python
    - cubicweb >=3.17.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-dataio
  license: LGPL
  summary: 'Cube for data input/output, import and export'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
