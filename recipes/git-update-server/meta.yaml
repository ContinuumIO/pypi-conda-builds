package:
  name: git-update-server
  version: "0.21"

source:
  fn: git-update-server-0.21.tar.gz
  url: https://pypi.python.org/packages/source/g/git-update-server/git-update-server-0.21.tar.gz
  md5: 9dc758ed4a53546c1a2eebd8c6aebf75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-update-server = git-update-server:main
    #
    # Would create an entry point called git-update-server that calls git-update-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - git_update_server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/anaoum/git-update-server
  license: UNKNOWN
  summary: 'HTTP server to handle POST notifications of updated repositories on GitHub and BitBucket.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
