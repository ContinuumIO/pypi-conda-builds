package:
  name: django-sorting
  version: "0.1"

source:
  fn: django-sorting-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sorting/django-sorting-0.1.tar.gz
  md5: 6af1c5629362ee734e850e582efef740
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sorting = django-sorting:main
    #
    # Would create an entry point called django-sorting that calls django-sorting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_sorting
    - django_sorting.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/directeur/django-sorting/tree/master
  license: BSD
  summary: "Like ericflo's django pagination, but this one does the sorting! used with ericflo's pagination, displaying tabular paginated and sortable data is very easy"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
