package:
  name: plone.mockup
  version: "1.0"

source:
  fn: plone.mockup-1.0.zip
  url: https://pypi.python.org/packages/source/p/plone.mockup/plone.mockup-1.0.zip
  md5: baf4e2f36b49cf3c8fa4505df9f2a297
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.mockup = plone.mockup:main
    #
    # Would create an entry point called plone.mockup that calls plone.mockup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - plone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/mockup
  license: BSD
  summary: 'Plone integration package for mockup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
