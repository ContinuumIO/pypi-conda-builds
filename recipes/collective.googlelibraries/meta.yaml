package:
  name: collective.googlelibraries
  version: "1.0b1"

source:
  fn: collective.googlelibraries-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.googlelibraries/collective.googlelibraries-1.0b1.tar.gz
  md5: ed656b2f9b78c8de523b62e8d150d7a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.googlelibraries = collective.googlelibraries:main
    #
    # Would create an entry point called collective.googlelibraries that calls collective.googlelibraries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.googlelibraries
    - collective.googlelibraries.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.googlelibraries
  license: GPL
  summary: 'The Libraries API is a content distribution network and loading architecture for the most popular, open-source JavaScript libraries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
