package:
  name: repoze.xmliter
  version: "0.6"

source:
  fn: repoze.xmliter-0.6.zip
  url: https://pypi.python.org/packages/source/r/repoze.xmliter/repoze.xmliter-0.6.zip
  md5: b9e88ad35754f5813870d13dbe89a10b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - repoze.xmliter = repoze.xmliter:main
    #
    # Would create an entry point called repoze.xmliter that calls repoze.xmliter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.1.1
    - future

  run:
    - python
    - setuptools
    - lxml >=2.1.1
    - future

test:
  # Python imports
  imports:
    - repoze
    - repoze.xmliter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.repoze.org
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Wrapper for ``lxml`` trees which serializes to string upon iteration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
