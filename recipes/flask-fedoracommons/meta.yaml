package:
  name: flask-fedoracommons
  version: "0.0.8"

source:
  fn: Flask-FedoraCommons-0.0.8.zip
  url: https://pypi.python.org/packages/source/F/Flask-FedoraCommons/Flask-FedoraCommons-0.0.8.zip
  md5: 23d2482b92f999ebcf8526b893c40d9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-fedoracommons = flask-fedoracommons:main
    #
    # Would create an entry point called flask-fedoracommons that calls flask-fedoracommons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - flask
    - rdflib

  run:
    - python
    - python-dateutil
    - flask
    - rdflib

test:
  # Python imports
  imports:
    - flask_fedora_commons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jermnelson/flask-fedora-commons
  license: MIT License
  summary: 'Library for manipulating Fedora Commons digitial repositories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
