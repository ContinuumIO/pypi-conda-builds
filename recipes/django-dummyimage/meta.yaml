package:
  name: django-dummyimage
  version: "0.1.1"

source:
  fn: django-dummyimage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dummyimage/django-dummyimage-0.1.1.tar.gz
  md5: 200b0a08de23d6edd317fc591f2bae7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dummyimage = django-dummyimage:main
    #
    # Would create an entry point called django-dummyimage that calls django-dummyimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pil

  run:
    - python
    - django
    - pil

test:
  # Python imports
  imports:
    - dummyimage
    - dummyimage.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/darkrho/django-dummyimage
  license: BSD
  summary: 'Dynamic Dummy Image Generator For Django!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
