package:
  name: frasco-countries
  version: "0.1"

source:
  fn: frasco-countries-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-countries/frasco-countries-0.1.tar.gz
  md5: f620914657aa01c5173d59216b092d4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-countries = frasco-countries:main
    #
    # Would create an entry point called frasco-countries that calls frasco-countries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - pycountry >=1.8

  run:
    - python
    - frasco
    - pycountry >=1.8

test:
  # Python imports
  imports:
    - frasco_countries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-countries
  license: MIT
  summary: 'pycountry integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
