package:
  name: bottle-request
  version: "0.2.0"

source:
  fn: bottle-request-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-request/bottle-request-0.2.0.tar.gz
  md5: 4da8d6bfebf4a9a80b70948cbd234dc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-request = bottle-request:main
    #
    # Would create an entry point called bottle-request that calls bottle-request.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.9

  run:
    - python
    - bottle >=0.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/turtlebender/bottle-request/
  license: MIT License
  summary: "Plugin to give bottle a 'stateless' request object"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
