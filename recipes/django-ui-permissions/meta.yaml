package:
  name: django-ui-permissions
  version: "0.3"

source:
  fn: django-ui-permissions-0.3.zip
  url: https://pypi.python.org/packages/source/d/django-ui-permissions/django-ui-permissions-0.3.zip
  md5: bb5c6340ccfde386783366a0f7c387bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ui-permissions = django-ui-permissions:main
    #
    # Would create an entry point called django-ui-permissions that calls django-ui-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ui_permissions
    - ui_permissions.descriptor
    - ui_permissions.descriptor.components
    - ui_permissions.templatetags
    - ui_permissions.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/vadwook/django-ui-permissions/
  license: MIT License
  summary: 'Django module that helps manage UI elements using permissions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
