package:
  name: slc.dublettefinder
  version: "0.3.1"

source:
  fn: slc.dublettefinder-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/s/slc.dublettefinder/slc.dublettefinder-0.3.1.tar.gz
  md5: f25126088e2c0fb7c6be7fd2c548d521
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.dublettefinder = slc.dublettefinder:main
    #
    # Would create an entry point called slc.dublettefinder that calls slc.dublettefinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - slc
    - slc.dublettefinder
    - slc.dublettefinder.browser
    - slc.dublettefinder.browser.portlets
    - slc.dublettefinder.extensions
    - slc.dublettefinder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/slc.dublettefinder/
  license: GNU General Public License (GPL)
  summary: 'The goal of slc.dublettefinder is to find duplicate files (dublettes ;) in the ZODB and then warn the user via validation error during content edit/creation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
