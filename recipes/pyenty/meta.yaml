package:
  name: pyenty
  version: "0.1.4"

source:
  fn: pyenty-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pyenty/pyenty-0.1.4.tar.gz
  md5: 6d9be5bc0ca3440ae908d2cb9d989387
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyenty = pyenty:main
    #
    # Would create an entry point called pyenty that calls pyenty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyenty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leodesouza/pyenty
  license: Apache Software License
  summary: 'An simple object-document mapping for mongodb-motor and tornado applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
