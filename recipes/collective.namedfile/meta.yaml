package:
  name: collective.namedfile
  version: "1.3"

source:
  fn: collective.namedfile-1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.namedfile/collective.namedfile-1.3.tar.gz
  md5: 167bd90366860d8d74e62365f745362e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.namedfile = collective.namedfile:main
    #
    # Would create an entry point called collective.namedfile that calls collective.namedfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.namedfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.namedfile
  license: Zope Public License
  summary: 'File field and widget with enhancements for zope3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
