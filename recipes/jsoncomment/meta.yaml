package:
  name: jsoncomment
  version: "0.3.0"

source:
  fn: jsoncomment-0.3.0.zip
  url: https://pypi.python.org/packages/source/j/jsoncomment/jsoncomment-0.3.0.zip
  md5: ff8ccb925bb7176a79eb7cb0e7e049b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsoncomment = jsoncomment:main
    #
    # Would create an entry point called jsoncomment that calls jsoncomment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsoncomment
    - jsoncomment.package

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/Dando_Real_ITA/json-comment
  license: MIT License
  summary: '\nA wrapper to JSON parsers allowing comments,\nmultiline strings and trailing commas\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
