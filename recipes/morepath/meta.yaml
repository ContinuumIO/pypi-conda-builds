package:
  name: morepath
  version: "0.10"

source:
  fn: morepath-0.10.zip
  url: https://pypi.python.org/packages/source/m/morepath/morepath-0.10.zip
  md5: 0ed642ca53e0f807e0f11705829006fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - morepath = morepath:main
    #
    # Would create an entry point called morepath that calls morepath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob >=1.3.1
    - venusifork >=2.0a2
    - reg >=0.9.2

  run:
    - python
    - setuptools
    - webob >=1.3.1
    - venusifork >=2.0a2
    - reg >=0.9.2

test:
  # Python imports
  imports:
    - morepath
    - morepath.tests
    - morepath.tests.fixtures
    - morepath.tests.fixtures.callerpkg
    - morepath.tests.fixtures.callerpkg2
    - morepath.tests.fixtures.pkg
    - morepath.tests.fixtures.scanmodule
    - morepath.tests.fixtures.scenario

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://morepath.readthedocs.org
  license: BSD License
  summary: 'A micro web-framework with superpowers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
