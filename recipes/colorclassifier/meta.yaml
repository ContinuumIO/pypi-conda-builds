package:
  name: colorclassifier
  version: "2.0.0"

source:
  fn: colorclassifier-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/colorclassifier/colorclassifier-2.0.0.tar.gz
  md5: 15af7df403e9f724e04d640447098893
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorclassifier = colorclassifier:main
    #
    # Would create an entry point called colorclassifier that calls colorclassifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorclassifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jwandborg/py-colorclassifier
  license: GNU General Public License (GPL)
  summary: 'Color classifier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
