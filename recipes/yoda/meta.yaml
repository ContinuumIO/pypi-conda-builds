package:
  name: yoda
  version: "0.1.5"

source:
  fn: yoda-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/y/yoda/yoda-0.1.5.tar.gz
  md5: 061e633a900c5f6557e8ce13f71e0f71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - yoda = yoda:main
    #
    # Would create an entry point called yoda that calls yoda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - pycolorizer
    - prettytable
    - argcomplete

  run:
    - python
    - pyyaml
    - pycolorizer
    - prettytable
    - argcomplete

test:
  # Python imports
  imports:
    - yoda
    - yoda.adapter
    - yoda.subcommand

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Numergy/yoda
  license: GPLv3
  summary: 'Multiple repositories management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
