package:
  name: ks.reference
  version: "1.0.1"

source:
  fn: ks.reference-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/ks.reference/ks.reference-1.0.1.tar.gz
  md5: 02e2bef2fe084de229e09bc3148bab0f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ks.reference = ks.reference:main
    #
    # Would create an entry point called ks.reference that calls ks.reference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component

  run:
    - python
    - setuptools
    - zope.interface
    - zope.component

test:
  # Python imports
  imports:
    - ks
    - ks.reference
    - ks.reference.browser
    - ks.reference.demo
    - ks.reference.reference
    - ks.reference.referenceannotation
    - ks.reference.referencehandlers
    - ks.reference.referencezcml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.keysolutions.ru/svn/ks.reference
  license: Zope Public License
  summary: 'Referene package for zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
