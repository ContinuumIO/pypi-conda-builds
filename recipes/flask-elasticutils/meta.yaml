package:
  name: flask-elasticutils
  version: "0.1.7"

source:
  fn: Flask-ElasticUtils-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-ElasticUtils/Flask-ElasticUtils-0.1.7.tar.gz
  md5: c0845f23104ec6743d60ac90ebb77eff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-elasticutils = flask-elasticutils:main
    #
    # Would create an entry point called flask-elasticutils that calls flask-elasticutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - elasticutils >=0.9

  run:
    - python
    - flask
    - elasticutils >=0.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neilalbrock/flask-elasticutils/
  license: BSD License
  summary: 'ElasticUtils for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
