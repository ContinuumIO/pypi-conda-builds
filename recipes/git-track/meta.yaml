package:
  name: git-track
  version: "0.1"

source:
  fn: Git-Track-0.1.tar.gz
  url: https://pypi.python.org/packages/source/G/Git-Track/Git-Track-0.1.tar.gz
  md5: a1be77c930f5a9071c497bbe6373b3b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-track = git-track:main
    #
    # Would create an entry point called git-track that calls git-track.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython >=0.3

  run:
    - python
    - gitpython >=0.3

test:
  # Python imports
  imports:
    - issue_db

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dhesse/Git-Track
  license: MIT License
  summary: 'Minimal Bug Tracking For Git.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
