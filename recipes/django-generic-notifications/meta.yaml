package:
  name: django-generic-notifications
  version: "0.2.2"

source:
  fn: django-generic-notifications-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-notifications/django-generic-notifications-0.2.2.tar.gz
  md5: e0d125ffa45c1a5a4138634a0482f9cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-notifications = django-generic-notifications:main
    #
    # Would create an entry point called django-generic-notifications that calls django-generic-notifications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - notifications

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevinrenskers/django-generic-notifications
  license: BSD License
  summary: 'Generic notification system for Django, with multiple input types and output backends'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
