package:
  name: bits_mod
  version: "1.00"

source:
  fn: bits_mod-1.00.tar.gz
  url: https://pypi.python.org/packages/source/b/bits_mod/bits_mod-1.00.tar.gz
  md5: d810fcbc40d39e2dc8a6abc7f0f03e18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bits_mod = bits_mod:main
    #
    # Would create an entry point called bits_mod that calls bits_mod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bits_mod

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stromberg.dnsalias.org/~strombrg/bits_mod/
  license: Apache v2
  summary: 'Pure Python efficient array of bits'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
