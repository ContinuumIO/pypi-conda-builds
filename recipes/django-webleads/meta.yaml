package:
  name: django-webleads
  version: "0.3b"

source:
  fn: django-webleads-0.3b.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webleads/django-webleads-0.3b.tar.gz
  md5: e50d8341d998411ce4cd0daf97fa0543
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webleads = django-webleads:main
    #
    # Would create an entry point called django-webleads that calls django-webleads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - south

  run:
    - python
    - django >=1.3
    - south

test:
  # Python imports
  imports:
    - webleads
    - webleads.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/msaelices/django-webleads
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application to manage incoming leads to your website'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
