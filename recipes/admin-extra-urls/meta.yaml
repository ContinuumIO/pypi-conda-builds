package:
  name: admin-extra-urls
  version: "0.7"

source:
  fn: admin-extra-urls-0.7.tar.gz
  url: https://pypi.python.org/packages/source/a/admin-extra-urls/admin-extra-urls-0.7.tar.gz
  md5: 4581ed45b3e8ba19d5c02d075f2e5d09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - admin-extra-urls = admin-extra-urls:main
    #
    # Would create an entry point called admin-extra-urls that calls admin-extra-urls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >1.5,<1.8
    - six

  run:
    - python
    - django >1.5,<1.8
    - six

test:
  # Python imports
  imports:
    - admin_extra_urls
    - admin_extra_urls.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Django mixin to easily add urls to any ModelAdmin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
