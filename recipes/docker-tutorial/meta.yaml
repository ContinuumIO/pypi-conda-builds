package:
  name: docker-tutorial
  version: "0.2.1"

source:
  fn: docker-tutorial-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-tutorial/docker-tutorial-0.2.1.tar.gz
  md5: ba1e6368502178f40d6f4e3252f084b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-tutorial = docker-tutorial:main
    #
    # Would create an entry point called docker-tutorial that calls docker-tutorial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - docker_tutorial
    - docker_tutorial.migrations
    - docker_tutorial.tests
    - dockerfile_tutorial

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dhrp/docker-tutorial/
  license: To be determined
  summary: 'An interactive learning environment to get familiar with the Docker cli'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
