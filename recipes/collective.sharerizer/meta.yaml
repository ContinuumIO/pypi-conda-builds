package:
  name: collective.sharerizer
  version: "1.4"

source:
  fn: collective.sharerizer-1.4.zip
  url: https://pypi.python.org/packages/source/c/collective.sharerizer/collective.sharerizer-1.4.zip
  md5: 50f70000911811f25d6f99bb4078e119
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sharerizer = collective.sharerizer:main
    #
    # Would create an entry point called collective.sharerizer that calls collective.sharerizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.sharerizer
    - collective.sharerizer.Extensions
    - collective.sharerizer.browser
    - collective.sharerizer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.sharerizer
  license: GPL
  summary: "A package to inject Web 2.0 style sharing buttons into Plone's document_actions."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
