package:
  name: ast2json
  version: "0.1"

source:
  fn: ast2json-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/ast2json/ast2json-0.1.tar.gz
  md5: 5ebbe2476e3ab5d5c93fede6efa7cad2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ast2json = ast2json:main
    #
    # Would create an entry point called ast2json that calls ast2json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ast2json

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Psycojoker/ast2json
  license: BSD
  summary: 'convert a python source code into json-dumpable data (dict and lists with strings, ints, ...)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
