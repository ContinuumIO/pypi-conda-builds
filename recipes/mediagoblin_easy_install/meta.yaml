package:
  name: mediagoblin_easy_install
  version: "0.6"

source:
  fn: mediagoblin_easy_install-0.6.tar.gz
  url: https://pypi.python.org/packages/source/m/mediagoblin_easy_install/mediagoblin_easy_install-0.6.tar.gz
  md5: eb7a961db86b2b764d79bdc7ac7caa5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mediagoblin_easy_install = mediagoblin_easy_install:main
    #
    # Would create an entry point called mediagoblin_easy_install that calls mediagoblin_easy_install.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse

  run:
    - python
    - setuptools
    - argparse

test:
  # Python imports
  imports:
    - mediagoblin_easy_install
    - mediagoblin_easy_install.plugins
    - mediagoblin_easy_install.plugins.setup_archive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mediagoblin.org/
  license: AGPLv3
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
