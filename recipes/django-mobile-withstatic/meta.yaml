package:
  name: django-mobile-withstatic
  version: "0.2.4"

source:
  fn: django-mobile-withstatic-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mobile-withstatic/django-mobile-withstatic-0.2.4.tar.gz
  md5: ccc0997e43df056894235ddfe09cbe4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobile-withstatic = django-mobile-withstatic:main
    #
    # Would create an entry point called django-mobile-withstatic that calls django-mobile-withstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_mobile
    - django_mobile.cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gregmuellegger/django-mobile
  license: BSD License
  summary: u'Detect mobile browsers and serve different template flavours to them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
