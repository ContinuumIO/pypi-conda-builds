package:
  name: themingwiki
  version: "1.0-pre08"

source:
  fn: TheMingWiki-1.0-pre08.tar.gz
  url: https://pypi.python.org/packages/source/T/TheMingWiki/TheMingWiki-1.0-pre08.tar.gz
  md5: f7dc81ca40bc81df56302dc01fb3d7a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - themingwiki = themingwiki:main
    #
    # Would create an entry point called themingwiki that calls themingwiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - TheMingWiki
    - themingwiki

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://python.org/pypi/TheMingWiki
  license: GNU General Public License (GPL)
  summary: 'A Honking-Good Little Python Wiki'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
