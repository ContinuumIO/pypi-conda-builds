package:
  name: agx.transform.uml2fs
  version: "1.0a1"

source:
  fn: agx.transform.uml2fs-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/a/agx.transform.uml2fs/agx.transform.uml2fs-1.0a1.tar.gz
  md5: f2b28eef35b98a22416da42a4019a09f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agx.transform.uml2fs = agx.transform.uml2fs:main
    #
    # Would create an entry point called agx.transform.uml2fs that calls agx.transform.uml2fs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - agx.core
    - node.ext.directory

  run:
    - python
    - setuptools
    - setuptools
    - agx.core
    - node.ext.directory

test:
  # Python imports
  imports:
    - agx
    - agx.transform
    - agx.transform.uml2fs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bluedynamics/agx.transform.uml2fs
  license: GNU General Public License (GPL)
  summary: 'AGX UML to Filesystem Transform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
