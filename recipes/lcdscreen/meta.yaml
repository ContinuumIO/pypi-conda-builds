package:
  name: lcdscreen
  version: "1.2.1"

source:
  fn: lcdscreen-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lcdscreen/lcdscreen-1.2.1.tar.gz
  md5: 88e1baa9ce5befe2f41464fb8bf18a24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lcdscreen = lcdscreen:main
    #
    # Would create an entry point called lcdscreen that calls lcdscreen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lcdscreen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikestreety/pypi-lcdscreen
  license: BSD License
  summary: 'A class to write to a LCD Screen using a raspberry pi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
