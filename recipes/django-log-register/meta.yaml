package:
  name: django-log-register
  version: "0.0.3"

source:
  fn: django-log-register-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-log-register/django-log-register-0.0.3.tar.gz
  md5: d1601403724386ab11d4a500a16b7e82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-log-register = django-log-register:main
    #
    # Would create an entry point called django-log-register that calls django-log-register.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - log_register

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lalo73/django-log-register
  license: BSD License
  summary: 'A simple Django app to register log and save them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
