package:
  name: soupmatchers
  version: "0.2"

source:
  fn: soupmatchers-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/soupmatchers/soupmatchers-0.2.tar.gz
  md5: 4e28bf81e2e139f2db20126acacd85c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - soupmatchers = soupmatchers:main
    #
    # Would create an entry point called soupmatchers that calls soupmatchers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - testtools >0.9.3
    - beautifulsoup

  run:
    - python
    - testtools >0.9.3
    - beautifulsoup

test:
  # Python imports
  imports:
    - soupmatchers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/soupmatchers
  license: Eclipse Public License
  summary: 'Matchers for checking parts of a HTML parse tree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
