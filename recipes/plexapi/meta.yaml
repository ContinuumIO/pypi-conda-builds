package:
  name: plexapi
  version: "0.9.4"

source:
  fn: PlexAPI-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/P/PlexAPI/PlexAPI-0.9.4.tar.gz
  md5: 8b450b6db481f1174ddc8735d8c0c768
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plexapi = plexapi:main
    #
    # Would create an entry point called plexapi that calls plexapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - examples
    - plexapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mjs7231/plexapi
  license: UNKNOWN
  summary: 'Python bindings for the Plex API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
