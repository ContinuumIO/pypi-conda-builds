package:
  name: pyramid_debugtoolbar_rendervalues
  version: "0.1"

source:
  fn: pyramid_debugtoolbar_rendervalues-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_debugtoolbar_rendervalues/pyramid_debugtoolbar_rendervalues-0.1.tar.gz
  md5: e365f3c3e3afe92e7b46ef410f781ca4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_debugtoolbar_rendervalues = pyramid_debugtoolbar_rendervalues:main
    #
    # Would create an entry point called pyramid_debugtoolbar_rendervalues that calls pyramid_debugtoolbar_rendervalues.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid_debugtoolbar

  run:
    - python
    - pyramid_debugtoolbar

test:
  # Python imports
  imports:
    - pyramid_debugtoolbar_rendervalues

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Pyramid debugtoolbar extension for displaying                    values available in a renderer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
