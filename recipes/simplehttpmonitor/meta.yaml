package:
  name: simplehttpmonitor
  version: "1.0.0"

source:
  fn: simplehttpmonitor-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simplehttpmonitor/simplehttpmonitor-1.0.0.tar.gz
  md5: 8773bc19453333dec5f6bfcf5065eec6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplehttpmonitor = simplehttpmonitor:main
    #
    # Would create an entry point called simplehttpmonitor that calls simplehttpmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - requests

  run:
    - python
    - argparse
    - requests

test:
  # Python imports
  imports:
    - simplehttpmonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicolas-van/simplehttpmonitor
  license: MIT
  summary: 'simplehttpmonitor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
