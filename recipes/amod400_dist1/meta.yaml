package:
  name: amod400_dist1
  version: "1.2.0"

source:
  fn: amod400_dist1-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/amod400_dist1/amod400_dist1-1.2.0.tar.gz
  md5: cabfaa2bc1a9d356a3a5762218ad4607
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amod400_dist1 = amod400_dist1:main
    #
    # Would create an entry point called amod400_dist1 that calls amod400_dist1.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - amod400_dist1

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.google.com
  license: UNKNOWN
  summary: 'test distribution from headsfirst'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
