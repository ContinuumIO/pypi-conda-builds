package:
  name: plone.schema
  version: "1.0a1"

source:
  fn: plone.schema-1.0a1.zip
  url: https://pypi.python.org/packages/source/p/plone.schema/plone.schema-1.0a1.zip
  md5: 256e8fb2891d6e5258832d48ea2debd5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.schema = plone.schema:main
    #
    # Would create an entry point called plone.schema that calls plone.schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.dexterity
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - plone.app.dexterity
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - plone
    - plone.schema
    - plone.schema.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.schema
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
