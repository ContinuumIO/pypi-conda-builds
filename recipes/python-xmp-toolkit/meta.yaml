package:
  name: python-xmp-toolkit
  version: "2.0.1"

source:
  fn: python-xmp-toolkit-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-xmp-toolkit/python-xmp-toolkit-2.0.1.tar.gz
  md5: 5836cb9bc252d06517ba262b3fb5a579
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-xmp-toolkit = python-xmp-toolkit:main
    #
    # Would create an entry point called python-xmp-toolkit that calls python-xmp-toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - libxmp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/python-xmp-toolkit/python-xmp-toolkit
  license: BSD License
  summary: 'Python XMP Toolkit for working with metadata.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
