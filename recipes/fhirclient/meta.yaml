package:
  name: fhirclient
  version: "0.0.4"

source:
  fn: fhirclient-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/fhirclient/fhirclient-0.0.4.tar.gz
  md5: 1a22f984b5f76908a359f98a1db7b369
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fhirclient = fhirclient:main
    #
    # Would create an entry point called fhirclient that calls fhirclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - fhirclient
    - fhirclient.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smart-on-fhir/client-py/
  license: Apache Software License
  summary: 'A flexible client for FHIR servers supporting the SMART on FHIR protocol'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
