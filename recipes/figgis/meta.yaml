package:
  name: figgis
  version: "1.6.0"

source:
  fn: figgis-1.6.0.tar.gz
  url: https://pypi.python.org/packages/source/f/figgis/figgis-1.6.0.tar.gz
  md5: 140d81ec6c7add6165347a250a1f82d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - figgis = figgis:main
    #
    # Would create an entry point called figgis that calls figgis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - figgis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thesquelched/figgis
  license: Apache Software License
  summary: 'Python declarative data validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
