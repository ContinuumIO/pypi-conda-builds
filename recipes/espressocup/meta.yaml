package:
  name: espressocup
  version: "0.0.3"

source:
  fn: espressocup-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/espressocup/espressocup-0.0.3.tar.gz
  md5: 2f75726eb17ccd819bf0b14ebfbfa6d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - espressocup = espressocup:main
    #
    # Would create an entry point called espressocup that calls espressocup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - espressocup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dfairhead/espressocup
  license: GNU General Public License v3 (GPLv3)
  summary: 'A gevent-intended very very basic flask-like WSGI server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
