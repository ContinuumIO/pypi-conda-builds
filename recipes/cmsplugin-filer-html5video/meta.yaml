package:
  name: cmsplugin-filer-html5video
  version: "0.1"

source:
  fn: cmsplugin-filer-html5video-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-filer-html5video/cmsplugin-filer-html5video-0.1.tar.gz
  md5: b1a2557831c23d8b31104b418338372c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-filer-html5video = cmsplugin-filer-html5video:main
    #
    # Would create an entry point called cmsplugin-filer-html5video that calls cmsplugin-filer-html5video.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.1.3
    - django-sekizai
    - django-filer >=0.8.6

  run:
    - python
    - django-cms >=2.1.3
    - django-sekizai
    - django-filer >=0.8.6

test:
  # Python imports
  imports:
    - cmsplugin_filer_html5video
    - cmsplugin_filer_html5video.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piquadrat/cmsplugin-filer-html5video
  license: BSD License
  summary: 'HTML5 video plugin for django CMS and django-filer, using VideoJS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
