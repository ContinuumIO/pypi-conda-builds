package:
  name: crab
  version: "0.4.2"

source:
  fn: crab-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/c/crab/crab-0.4.2.tar.gz
  md5: a3283eb90d02cf80006177fd4f070c66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crab = crab:main
    #
    # Would create an entry point called crab that calls crab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - crab
    - crab.client
    - crab.notify
    - crab.report
    - crab.server
    - crab.service
    - crab.store
    - crab.util
    - crab.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/grahambell/crab
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Cron alert board'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
