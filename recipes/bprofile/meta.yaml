package:
  name: bprofile
  version: "1.3.2"

source:
  fn: bprofile-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bprofile/bprofile-1.3.2.tar.gz
  md5: db6048663b04df9867cf80746cd2b7fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bprofile = bprofile:main
    #
    # Would create an entry point called bprofile that calls bprofile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bprofile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cbillington/bprofile
  license: simplified BSD
  summary: 'A wrapper around profile/cProfile, gprof2dot and dot,\nproviding a simple context manager for profiling sections\nof Python code and producing visual graphs of profiling results.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
