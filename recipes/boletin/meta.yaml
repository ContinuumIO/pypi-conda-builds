package:
  name: boletin
  version: "0.5.1"

source:
  fn: boletin-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/b/boletin/boletin-0.5.1.tar.gz
  md5: 64c6d6787e5ede9994e7ffe3437f6f64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boletin = boletin:main
    #
    # Would create an entry point called boletin that calls boletin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boletin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://tracpub.yaco.es/djangoapps/wiki/Boletin
  license: LGPL v3
  summary: 'Newsletter generation and sending application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
