package:
  name: django-url-fullpath-redirect
  version: "0.1.2"

source:
  fn: django-url-fullpath-redirect-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-fullpath-redirect/django-url-fullpath-redirect-0.1.2.tar.gz
  md5: ac7d7763fe2a4ec10f1cbef242a7933a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-fullpath-redirect = django-url-fullpath-redirect:main
    #
    # Would create an entry point called django-url-fullpath-redirect that calls django-url-fullpath-redirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - south >=0.7.3

  run:
    - python
    - django >=1.3.1
    - south >=0.7.3

test:
  # Python imports
  imports:
    - url_tracker
    - url_tracker.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alifanov/django-url-fullpath-redirect
  license: Apache Software License or BSD License
  summary: 'Fork of django-url-tracker with query_string support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
