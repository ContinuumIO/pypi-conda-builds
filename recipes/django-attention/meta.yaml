package:
  name: django-attention
  version: "0.2"

source:
  fn: django-attention-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-attention/django-attention-0.2.tar.gz
  md5: 9df8c8b4b662d59688905e449cdd6cc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-attention = django-attention:main
    #
    # Would create an entry point called django-attention that calls django-attention.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djattn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/zacharyvoase/django-attention
  license: UNKNOWN
  summary: 'A small session-based flash notice system for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
