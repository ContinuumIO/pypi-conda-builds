package:
  name: bambu-enquiries
  version: "2.0.1"

source:
  fn: bambu-enquiries-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-enquiries/bambu-enquiries-2.0.1.tar.gz
  md5: cf49a85a97fbd3e618ebf8b18d6b00e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-enquiries = bambu-enquiries:main
    #
    # Would create an entry point called bambu-enquiries that calls bambu-enquiries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - requests
    - bambu-mail

  run:
    - python
    - django >=1.4
    - requests
    - bambu-mail

test:
  # Python imports
  imports:
    - bambu_enquiries
    - bambu_enquiries.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-enquiries
  license: UNKNOWN
  summary: 'A simple model and contact form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
