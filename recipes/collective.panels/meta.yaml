package:
  name: collective.panels
  version: "1.8"

source:
  fn: collective.panels-1.8.zip
  url: https://pypi.python.org/packages/source/c/collective.panels/collective.panels-1.8.zip
  md5: bb5f54dc83d59706025e6087f2beb02e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.panels = collective.panels:main
    #
    # Would create an entry point called collective.panels that calls collective.panels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets >=2.1.2

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets >=2.1.2

test:
  # Python imports
  imports:
    - collective
    - collective.panels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv2+
  summary: 'Add-on for Plone that adds portlet panels.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
