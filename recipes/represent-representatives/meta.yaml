package:
  name: represent-representatives
  version: "0.2"

source:
  fn: represent-representatives-0.2.tar.gz
  url: https://pypi.python.org/packages/source/r/represent-representatives/represent-representatives-0.2.tar.gz
  md5: 9541cbbba4d5e64bd6a6109993a102b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - represent-representatives = represent-representatives:main
    #
    # Would create an entry point called represent-representatives that calls represent-representatives.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf
    - django-jsonfield >=0.7.1
    - python-dateutil
    - represent-boundaries

  run:
    - python
    - django-appconf
    - django-jsonfield >=0.7.1
    - python-dateutil
    - represent-boundaries

test:
  # Python imports
  imports:
    - representatives
    - representatives.management
    - representatives.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rhymeswithcycle/represent-reps
  license: MIT License
  summary: 'A Web API for political representatives tied to geographical districts. Packaged as a Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
