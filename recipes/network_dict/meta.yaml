package:
  name: network_dict
  version: "0.1"

source:
  fn: network_dict-0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/network_dict/network_dict-0.1.tar.gz
  md5: 6b910b4338b98396c4cd48c57e16ce40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - network_dict = network_dict:main
    #
    # Would create an entry point called network_dict that calls network_dict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netaddr

  run:
    - python
    - netaddr

test:
  # Python imports
  imports:
    - network_dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neoCrimeLabs/python-network_dict
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'network_dict creates a network subnet based dictionary that returns the most specific subnet(s) for a given IP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
