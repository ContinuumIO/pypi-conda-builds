package:
  name: django-licensing
  version: "0.2.4"

source:
  fn: django-licensing-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-licensing/django-licensing-0.2.4.tar.gz
  md5: 96caf6b210b7fe1d276d144a5b679a3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-licensing = django-licensing:main
    #
    # Would create an entry point called django-licensing that calls django-licensing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - licensing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/editorsnotes/django-licensing
  license: Public Domain
  summary: 'A Django model and data for adding licensing info to data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
