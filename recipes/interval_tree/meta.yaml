package:
  name: interval_tree
  version: "0.3.2"

source:
  fn: interval_tree-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/i/interval_tree/interval_tree-0.3.2.tar.gz
  md5: c8a787f378ee422b1e381126cc60001b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - interval_tree = interval_tree:main
    #
    # Would create an entry point called interval_tree that calls interval_tree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest

  run:
    - python
    - pytest

test:
  # Python imports
  imports:
    - interval_tree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/moonso/interval_tree
  license: MIT License
  summary: 'Python implemetation of the interval tree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
