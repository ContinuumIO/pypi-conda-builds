package:
  name: codeart-benchmarks
  version: "0.0.15"

source:
  fn: codeart-benchmarks-0.0.15.tar.gz
  url: https://pypi.python.org/packages/source/c/codeart-benchmarks/codeart-benchmarks-0.0.15.tar.gz
  md5: ea2c1675450417e8ecd01e5223292d14
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codeart-benchmarks = codeart-benchmarks:main
    #
    # Would create an entry point called codeart-benchmarks that calls codeart-benchmarks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - codeart
    - codeart.benchmarks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CodeArtLibs/codeart-benchmarks
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
