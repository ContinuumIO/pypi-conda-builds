package:
  name: foxpy
  version: "0.0.3"

source:
  fn: foxpy-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/foxpy/foxpy-0.0.3.tar.gz
  md5: 94f56f7a15fcfd0febf7a83c5b154616
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foxpy = foxpy:main
    #
    # Would create an entry point called foxpy that calls foxpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - urllib

  run:
    - python
    - requests
    - urllib

test:
  # Python imports
  imports:
    - foxpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/foxpy/
  license: LICENSE.txt
  summary: 'Python bindings for FOX - Federated Knowledge Extraction Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
