package:
  name: django-laravel-validator
  version: "0.0.3"

source:
  fn: django-laravel-validator-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-laravel-validator/django-laravel-validator-0.0.3.tar.gz
  md5: bc244c2513fed27f3e4254928599d545
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-laravel-validator = django-laravel-validator:main
    #
    # Would create an entry point called django-laravel-validator that calls django-laravel-validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - cjson

  run:
    - python
    - setuptools
    - django >=1.6
    - cjson

test:
  # Python imports
  imports:
    - django_laravel_validator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youngershen/django-laravel-validator
  license: MIT License
  summary: 'a data validator for django inspired the validator of laravel framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
