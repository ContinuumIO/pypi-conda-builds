package:
  name: frostbite-rcon-utils
  version: "0.0.1"

source:
  fn: frostbite-rcon-utils-0.0.1.zip
  url: https://pypi.python.org/packages/source/f/frostbite-rcon-utils/frostbite-rcon-utils-0.0.1.zip
  md5: efc0d3e360f1f38cb0a3370516544b9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frostbite-rcon-utils = frostbite-rcon-utils:main
    #
    # Would create an entry point called frostbite-rcon-utils that calls frostbite-rcon-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rcon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rocketblast/frostbite-rcon-utils
  license: MIT
  summary: 'lightweight library for connecting to an frostbite based game (bf3, bf4, bfh, mohw, bc2)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
