package:
  name: interfile
  version: "0.2.1"

source:
  fn: interfile-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/interfile/interfile-0.2.1.tar.gz
  md5: 73ddfa3e4b08ae758371e90a2f1a01d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - interfile = interfile:main
    #
    # Would create an entry point called interfile that calls interfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6.0
    - simplewrap >=0.2.0
    - petlink >=0.2.0

  run:
    - python
    - numpy >=1.6.0
    - simplewrap >=0.2.0
    - petlink >=0.2.0

test:
  # Python imports
  imports:
    - interfile
    - interfile.examples
    - interfile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.occiput.io/
  license: BSD License
  summary: 'Interfile read and write.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
