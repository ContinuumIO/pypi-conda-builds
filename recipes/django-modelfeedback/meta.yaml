package:
  name: django-modelfeedback
  version: "0.1.2"

source:
  fn: django-modelfeedback-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modelfeedback/django-modelfeedback-0.1.2.tar.gz
  md5: 33b82d7708c55484f9535e70eb23bb4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modelfeedback = django-modelfeedback:main
    #
    # Would create an entry point called django-modelfeedback that calls django-modelfeedback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - modelfeedback

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yokomizor/django-modelfeedback
  license: MIT License
  summary: 'Adds support for receive feedback to django models using Likert scale.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
