package:
  name: collective.pfg.dexterity
  version: "0.10.1"

source:
  fn: collective.pfg.dexterity-0.10.1.zip
  url: https://pypi.python.org/packages/source/c/collective.pfg.dexterity/collective.pfg.dexterity-0.10.1.zip
  md5: 752905f99e279aa80bb6194db67dcb4a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pfg.dexterity = collective.pfg.dexterity:main
    #
    # Would create an entry point called collective.pfg.dexterity that calls collective.pfg.dexterity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.globalrequest
    - plone.autoform
    - plone.app.dexterity
    - products.ploneformgen
    - products.datagridfield >=1.8b1

  run:
    - python
    - setuptools
    - setuptools
    - five.globalrequest
    - plone.autoform
    - plone.app.dexterity
    - products.ploneformgen
    - products.datagridfield >=1.8b1

test:
  # Python imports
  imports:
    - collective
    - collective.pfg
    - collective.pfg.dexterity
    - collective.pfg.dexterity.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datakurre/collective.pfg.dexterity/
  license: ZPL
  summary: 'Installs dexterity content creation adapter for PloneFormGen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
