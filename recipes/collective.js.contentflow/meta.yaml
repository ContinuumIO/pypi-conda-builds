package:
  name: collective.js.contentflow
  version: "1.0"

source:
  fn: collective.js.contentflow-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.js.contentflow/collective.js.contentflow-1.0.zip
  md5: 5ff1333103622518f0d04c07e2e76817
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.contentflow = collective.js.contentflow:main
    #
    # Would create an entry point called collective.js.contentflow that calls collective.js.contentflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.contentflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.contentflow
  license: GPL
  summary: 'contentflow js integration for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
