package:
  name: django-storage-swift
  version: "1.2.4"

source:
  fn: django-storage-swift-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-storage-swift/django-storage-swift-1.2.4.tar.gz
  md5: d1a5340d73548206156c75c0866d59e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-storage-swift = django-storage-swift:main
    #
    # Would create an entry point called django-storage-swift that calls django-storage-swift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-swiftclient >=1.4.0
    - python-keystoneclient >=0.2.3
    - six

  run:
    - python
    - python-swiftclient >=1.4.0
    - python-keystoneclient >=0.2.3
    - six

test:
  # Python imports
  imports:
    - swift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/blacktorn/django-storage-swift
  license: MIT License
  summary: 'OpenStack Swift storage backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
