package:
  name: collective.z3cform.html5widgets
  version: "0.1"

source:
  fn: collective.z3cform.html5widgets-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.html5widgets/collective.z3cform.html5widgets-0.1.zip
  md5: 58bed3f7858106394e3a23ca4cd5877e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.html5widgets = collective.z3cform.html5widgets:main
    #
    # Would create an entry point called collective.z3cform.html5widgets that calls collective.z3cform.html5widgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform
    - plone.formwidget.autocomplete

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform
    - plone.formwidget.autocomplete

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.html5widgets
    - collective.z3cform.html5widgets.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.z3cform.html5widgets
  license: GNU General Public License (GPL)
  summary: 'HTML5 widgets for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
