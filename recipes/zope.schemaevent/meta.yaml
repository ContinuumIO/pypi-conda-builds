package:
  name: zope.schemaevent
  version: "0.2"

source:
  fn: zope.schemaevent-0.2.zip
  url: https://pypi.python.org/packages/source/z/zope.schemaevent/zope.schemaevent-0.2.zip
  md5: b78255c9ac38229538644fe05c82eb96
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.schemaevent = zope.schemaevent:main
    #
    # Would create an entry point called zope.schemaevent that calls zope.schemaevent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.schema

  run:
    - python
    - setuptools
    - zope.component
    - zope.schema

test:
  # Python imports
  imports:
    - zope
    - zope.schemaevent
    - zope.schemaevent.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zopefoundation/zope.schemaevent
  license: gpl
  summary: 'Event subscribers for zope.schema'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
