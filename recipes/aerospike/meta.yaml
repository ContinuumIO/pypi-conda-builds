package:
  name: aerospike
  version: "1.0.43"

source:
  fn: aerospike-1.0.43.tar.gz
  url: https://pypi.python.org/packages/source/a/aerospike/aerospike-1.0.43.tar.gz
  md5: 694eac4d8c24922152bf2ed37a1c607f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aerospike = aerospike:main
    #
    # Would create an entry point called aerospike that calls aerospike.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aerospike

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aerospike.com
  license: Apache Software License
  summary: 'Aerospike Client Library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
