package:
  name: djangocms-panel
  version: "1.0.0"

source:
  fn: djangocms-panel-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-panel/djangocms-panel-1.0.0.tar.gz
  md5: 22c22d95f93bb8fbf90acff687693999
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-panel = djangocms-panel:main
    #
    # Would create an entry point called djangocms-panel that calls djangocms-panel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_panel
    - djangocms_panel.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tsbarnes/djangocms-panel
  license: BSD License
  summary: 'Bootstrap panel plugin for Django-CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
