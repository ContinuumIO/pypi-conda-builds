package:
  name: common-components-models
  version: "0.5"

source:
  fn: common-components-models-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/common-components-models/common-components-models-0.5.tar.gz
  md5: 57e8d238e4c7c14e906304463d1c9a04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - common-components-models = common-components-models:main
    #
    # Would create an entry point called common-components-models that calls common-components-models.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - components
    - components.commons
    - components.controls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Easy to control models and Attribute data fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
