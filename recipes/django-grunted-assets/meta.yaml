package:
  name: django-grunted-assets
  version: "0.2.1"

source:
  fn: django-grunted-assets-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-grunted-assets/django-grunted-assets-0.2.1.tar.gz
  md5: 53930cc8e9a2d0ac8b945120218016f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-grunted-assets = django-grunted-assets:main
    #
    # Would create an entry point called django-grunted-assets that calls django-grunted-assets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - grunted_assets
    - grunted_assets.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tino/django-grunted-assets
  license: MIT License
  summary: 'A Django app for simplifing the use of grunt for your asset flow'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
