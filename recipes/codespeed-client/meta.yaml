package:
  name: codespeed-client
  version: "0.3.3"

source:
  fn: codespeed-client-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/c/codespeed-client/codespeed-client-0.3.3.tar.gz
  md5: 4eb46bab6dfb41b306db5183e5a762f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codespeed-client = codespeed-client:main
    #
    # Would create an entry point called codespeed-client that calls codespeed-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - codespeed_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AlekSi/codespeed-client
  license: MIT License
  summary: 'Library and command-line tool to push benchmark data to CodeSpeed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
