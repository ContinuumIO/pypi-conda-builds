package:
  name: compressor_requirejs
  version: "1.3"

source:
  fn: compressor_requirejs-1.3.zip
  url: https://pypi.python.org/packages/source/c/compressor_requirejs/compressor_requirejs-1.3.zip
  md5: 527018ca9f2b00707ca04e4142d9f82d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compressor_requirejs = compressor_requirejs:main
    #
    # Would create an entry point called compressor_requirejs that calls compressor_requirejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-compressor >=1.3
    - pyexecjs >=1.0.4

  run:
    - python
    - django-compressor >=1.3
    - pyexecjs >=1.0.4

test:
  # Python imports
  imports:
    - compressor_requirejs
    - compressor_requirejs.compressor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dresiu/compressor_requirejs
  license: GNU AGPLv3
  summary: 'Library for django compressor, which helps to build requirejs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
