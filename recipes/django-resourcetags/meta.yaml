package:
  name: django-resourcetags
  version: "1.1"

source:
  fn: django-resourcetags-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-resourcetags/django-resourcetags-1.1.tar.gz
  md5: 138e99134d44e234230ecb7463f0e8ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-resourcetags = django-resourcetags:main
    #
    # Would create an entry point called django-resourcetags that calls django-resourcetags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0_final

  run:
    - python
    - django >=1.0_final

test:
  # Python imports
  imports:
    - django_resourcetags
    - django_resourcetags.compression
    - django_resourcetags.management
    - django_resourcetags.management.commands
    - django_resourcetags.templatetags
    - django_resourcetags.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-resourcetags
  license: GNU General Public License (GPL)
  summary: 'Django template tags for referencing static files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
