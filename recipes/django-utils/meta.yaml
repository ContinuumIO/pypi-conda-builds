package:
  name: django-utils
  version: "0.0.2"

source:
  fn: django-utils-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-utils/django-utils-0.0.2.tar.gz
  md5: f491ad7647bbd9bcffb5d8a16726ff91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-utils = django-utils:main
    #
    # Would create an entry point called django-utils that calls django-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - utils
    - utils.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-utils
  license: BSD License
  summary: 'Utilities for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
