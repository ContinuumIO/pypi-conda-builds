package:
  name: me_testing_pypi
  version: "1.3.0"

source:
  fn: me_testing_pypi-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/m/me_testing_pypi/me_testing_pypi-1.3.0.tar.gz
  md5: 39848b05adb379bfc41cb8a54032cc1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - me_testing_pypi = me_testing_pypi:main
    #
    # Would create an entry point called me_testing_pypi that calls me_testing_pypi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - me_testing_pypi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.google.com
  license: UNKNOWN
  summary: 'A simple printer of nested list'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
