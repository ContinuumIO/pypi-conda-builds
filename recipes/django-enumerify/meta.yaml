package:
  name: django-enumerify
  version: "0.3.3"

source:
  fn: django-enumerify-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-enumerify/django-enumerify-0.3.3.tar.gz
  md5: 7ac07e30cfb6b016c0df2f61058c7c48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-enumerify = django-enumerify:main
    #
    # Would create an entry point called django-enumerify that calls django-enumerify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - enumerify
    - testproject
    - testproject.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/willandskill/django-enumerify
  license: BSD licence, see LICENCE.txt
  summary: 'Simple Enum class instead of tuples'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
