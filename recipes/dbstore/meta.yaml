package:
  name: dbstore
  version: "0.3"

source:
  fn: dbstore-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dbstore/dbstore-0.3.tar.gz
  md5: 24046647fb20d74cdafb3647eb6e7887
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbstore = dbstore:main
    #
    # Would create an entry point called dbstore that calls dbstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbstore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lukearno.com/projects/dbstore/
  license: GNU General Public License (GPL)
  summary: 'Database storage for flup session middleware.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
