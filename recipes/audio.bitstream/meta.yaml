package:
  name: audio.bitstream
  version: "2"

source:
  fn: audio.bitstream-2.tar.gz
  url: https://pypi.python.org/packages/source/a/audio.bitstream/audio.bitstream-2.tar.gz
  md5: 01b4a9bc9b5c47621af5c37fe41a0e85
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audio.bitstream = audio.bitstream:main
    #
    # Would create an entry point called audio.bitstream that calls audio.bitstream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bitstream >=2

  run:
    - python
    - bitstream >=2

test:
  # Python imports
  imports:
    - audio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
