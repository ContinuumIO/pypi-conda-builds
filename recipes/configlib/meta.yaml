package:
  name: configlib
  version: "1.0.0"

source:
  fn: configlib-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/configlib/configlib-1.0.0.tar.gz
  md5: 85317bb3f3b33150c954292513d0a788
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configlib = configlib:main
    #
    # Would create an entry point called configlib that calls configlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - configlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeffbryner/configlib
  license: UNKNOWN
  summary: 'wrapper for ConfigParser allowing for simple get,set,delete calls to set options.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
