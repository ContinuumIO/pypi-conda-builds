package:
  name: launchkey-flask
  version: "0.1.0"

source:
  fn: launchkey-flask-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/launchkey-flask/launchkey-flask-0.1.0.tar.gz
  md5: a9d03458537f1360cb1a67446ec8a9e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - launchkey-flask = launchkey-flask:main
    #
    # Would create an entry point called launchkey-flask that calls launchkey-flask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - launchkey-python
    - flask
    - flask-login

  run:
    - python
    - launchkey-python
    - flask
    - flask-login

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LaunchKey/launchkey-flask
  license: MIT
  summary: 'LaunchKey authentication extension for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
