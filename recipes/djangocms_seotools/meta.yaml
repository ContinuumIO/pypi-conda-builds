package:
  name: djangocms_seotools
  version: "1.5"

source:
  fn: djangocms_seotools-1.5.zip
  url: https://pypi.python.org/packages/source/d/djangocms_seotools/djangocms_seotools-1.5.zip
  md5: 56255cdff78947238a73633a3adf54d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms_seotools = djangocms_seotools:main
    #
    # Would create an entry point called djangocms_seotools that calls djangocms_seotools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_seotools
    - djangocms_seotools.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/werevolff/django_cms_seotools
  license: UNKNOWN
  summary: 'SEO and Custom tools for the Django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
