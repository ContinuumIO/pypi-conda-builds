package:
  name: kaoz
  version: "2.1.0"

source:
  fn: kaoz-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kaoz/kaoz-2.1.0.tar.gz
  md5: 2c4b5b57e30cff2d10fd359b73826b80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kaoz = kaoz:main
    #
    # Would create an entry point called kaoz that calls kaoz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - irc >=5.0.1

  run:
    - python
    - distribute
    - irc >=5.0.1

test:
  # Python imports
  imports:
    - kaoz
    - kaoz.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/BinetReseau/Kaoz
  license: MIT License
  summary: 'A simple IRC notifier bot.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
