package:
  name: aberquota
  version: "1.1"

source:
  fn: aberquota-1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aberquota/aberquota-1.1.tar.gz
  md5: 5959481dde423dbab90f732ac613c7d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aberquota = aberquota:main
    #
    # Would create an entry point called aberquota that calls aberquota.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

test:
  # Python imports
  imports:
    - aberquota

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jafnee/aberquota
  license: UNKNOWN
  summary: 'Python3 tool for getting internet usage off the Aberystwyth University network'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
