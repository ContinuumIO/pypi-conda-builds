package:
  name: marktime
  version: "0.2.0"

source:
  fn: marktime-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/marktime/marktime-0.2.0.tar.gz
  md5: 1f2650b9bbc1226dbc05ea0696a4e382
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marktime = marktime:main
    #
    # Would create an entry point called marktime that calls marktime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marktime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ekalinin/marktime.py
  license: MIT License
  summary: 'Python timer module for humans.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
