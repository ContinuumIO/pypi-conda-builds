package:
  name: ludolph-skeleton
  version: "1.2"

source:
  fn: ludolph-skeleton-1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/ludolph-skeleton/ludolph-skeleton-1.2.tar.gz
  md5: 4a9e10f85b6f655504cb0c64f102307c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ludolph-skeleton = ludolph-skeleton:main
    #
    # Would create an entry point called ludolph-skeleton that calls ludolph-skeleton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ludolph

  run:
    - python
    - ludolph

test:
  # Python imports
  imports:
    - ludolph_skeleton

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erigones/ludolph-skeleton/
  license: MIT License
  summary: 'Ludolph: Skeleton plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
