package:
  name: blastoff
  version: "0.3a2"

source:
  fn: BlastOff-0.3a2.tar.gz
  url: https://pypi.python.org/packages/source/B/BlastOff/BlastOff-0.3a2.tar.gz
  md5: 8d4112f906907f5a98a4e8cc473a5b96
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blastoff = blastoff:main
    #
    # Would create an entry point called blastoff that calls blastoff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pastescript >=1.7.3

  run:
    - python
    - setuptools
    - pastescript >=1.7.3

test:
  # Python imports
  imports:
    - blastoff

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/chrismiles/blastoff/
  license: MIT
  summary: 'A Pylons application template providing a working site skeleton configured with SQLAlchemy, mako, repoze.who, ToscaWidgets, TurboMail, WebFlash and (optionally) SchemaBot. The generated app is pre-configured with authentication, login and registration forms, and (optionally) email confirmation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
