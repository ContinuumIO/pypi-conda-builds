package:
  name: indicoio
  version: "0.5.3"

source:
  fn: IndicoIo-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/I/IndicoIo/IndicoIo-0.5.3.tar.gz
  md5: fdb99c3d1bca918b332f67c0526f5b69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indicoio = indicoio:main
    #
    # Would create an entry point called indicoio that calls indicoio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - six >=1.3.0
    - numpy >=1.8.1
    - scipy >=0.14.0
    - scikit-image >=0.10.1

  run:
    - python
    - requests >=1.2.3
    - six >=1.3.0
    - numpy >=1.8.1
    - scipy >=0.14.0
    - scikit-image >=0.10.1

test:
  # Python imports
  imports:
    - indicoio
    - indicoio.images
    - indicoio.text
    - indicoio.utils
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IndicoDataSolutions/indicoio-python
  license: MIT License
  summary: '\n        A Python Wrapper for indico.\n        Use pre-built state of the art machine learning algorithms with a single line of code.\n    '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
