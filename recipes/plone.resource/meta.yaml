package:
  name: plone.resource
  version: "1.0.4"

source:
  fn: plone.resource-1.0.4.zip
  url: https://pypi.python.org/packages/source/p/plone.resource/plone.resource-1.0.4.zip
  md5: ed52906e77534b1b6701da6bd2c2597d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.resource = plone.resource:main
    #
    # Would create an entry point called plone.resource that calls plone.resource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.traversing
    - zope.publisher
    - zope.configuration
    - zope.schema
    - zope.filerepresentation
    - z3c.caching
    - python-dateutil
    - plone.caching
    - zope2

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.traversing
    - zope.publisher
    - zope.configuration
    - zope.schema
    - zope.filerepresentation
    - z3c.caching
    - python-dateutil
    - plone.caching
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.resource
    - plone.resource.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/plone/plone.resource
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
