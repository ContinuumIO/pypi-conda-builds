package:
  name: p4a.fileimage
  version: "1.0.2"

source:
  fn: p4a.fileimage-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/p4a.fileimage/p4a.fileimage-1.0.2.tar.gz
  md5: f2d47f9fbc67993636637eaf4cd67df9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.fileimage = p4a.fileimage:main
    #
    # Would create an entry point called p4a.fileimage that calls p4a.fileimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - p4a
    - p4a.fileimage
    - p4a.fileimage.file
    - p4a.fileimage.image

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/p4a.fileimage/
  license: GNU General Public License (GPL)
  summary: 'File/Image widget for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
