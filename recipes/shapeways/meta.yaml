package:
  name: shapeways
  version: "1.0.0"

source:
  fn: shapeways-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/shapeways/shapeways-1.0.0.tar.gz
  md5: 90442befab8635b8677dea095b78f5a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shapeways = shapeways:main
    #
    # Would create an entry point called shapeways that calls shapeways.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauthlib ==0.6.0
    - requests-oauthlib ==0.4.0

  run:
    - python
    - oauthlib ==0.6.0
    - requests-oauthlib ==0.4.0

test:
  # Python imports
  imports:
    - shapeways
    - shapeways.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Shapeways/python-shapeways
  license: MIT License
  summary: 'Shapeways API Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
