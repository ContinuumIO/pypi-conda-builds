package:
  name: importing
  version: "1.10"

source:
  fn: Importing-1.10.zip
  url: https://pypi.python.org/packages/source/I/Importing/Importing-1.10.zip
  md5: 8e49d5062b0d5bfd33bc85425ae73f0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importing = importing:main
    #
    # Would create an entry point called importing that calls importing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - importing
    - peak
    - peak.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://peak.telecommunity.com/DevCenter/Importing
  license: PSF or ZPL
  summary: 'Import objects dynamically, lazily, "weakly", and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
