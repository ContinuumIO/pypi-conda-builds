package:
  name: django-sample-data
  version: "0.1"

source:
  fn: django-sample-data-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sample-data/django-sample-data-0.1.tar.gz
  md5: fe4a812321c9502ef8fff7c96603b156
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sample-data = django-sample-data:main
    #
    # Would create an entry point called django-sample-data that calls django-sample-data.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_sample_data
    - django_sample_data.management

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saxix/django-sample-data
  license: BSD
  summary: 'Collections of useful actions to use with django.contrib.admin.ModelAdmin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
