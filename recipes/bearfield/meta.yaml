package:
  name: bearfield
  version: "1.9.1"

source:
  fn: bearfield-1.9.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bearfield/bearfield-1.9.1.tar.gz
  md5: e4757e44f925b9bb3966924ad32d5b4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bearfield = bearfield:main
    #
    # Would create an entry point called bearfield that calls bearfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.7.0

  run:
    - python
    - pymongo >=2.7.0

test:
  # Python imports
  imports:
    - bearfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WiFast/bearfield
  license: BSD License
  summary: 'Small MongoDB object layer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
