package:
  name: edition1.menu
  version: "0.1a7"

source:
  fn: edition1.menu-0.1a7.zip
  url: https://pypi.python.org/packages/source/e/edition1.menu/edition1.menu-0.1a7.zip
  md5: fd88b6a09cd3bf20ce742005683f5296
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edition1.menu = edition1.menu:main
    #
    # Would create an entry point called edition1.menu that calls edition1.menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - edition1
    - edition1.menu
    - edition1.menu.browser
    - edition1.menu.tests
    - edition1.menu.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edition1/edition1.menu
  license: gpl
  summary: 'A responsive menu for Plone themes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
