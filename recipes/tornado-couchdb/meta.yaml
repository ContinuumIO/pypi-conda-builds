package:
  name: tornado-couchdb
  version: "0.2.5"

source:
  fn: tornado-couchdb-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-couchdb/tornado-couchdb-0.2.5.tar.gz
  md5: 22ac1436580d867c487f917914dc98ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-couchdb = tornado-couchdb:main
    #
    # Would create an entry point called tornado-couchdb that calls tornado-couchdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.2

  run:
    - python
    - tornado >=3.2

test:
  # Python imports
  imports:
    - couch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nephics/tornado-couchdb
  license: MIT License
  summary: "Blocking and non-blocking (asynchronous) clients for CouchDB using Tornado's httpclient"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
