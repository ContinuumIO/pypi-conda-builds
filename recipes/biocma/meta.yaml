package:
  name: biocma
  version: "0.2.1"

source:
  fn: biocma-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/biocma/biocma-0.2.1.tar.gz
  md5: f0d823a58cf2ad1179bebc9d138360e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biocma = biocma:main
    #
    # Would create an entry point called biocma that calls biocma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - biocma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/etal/biocma
  license: BSD License
  summary: 'I/O and utilities for the Consensus Multiple Alignment (CMA) file format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
