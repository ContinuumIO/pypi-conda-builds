package:
  name: plone.app.archive
  version: "0.1"

source:
  fn: plone.app.archive-0.1.zip
  url: https://pypi.python.org/packages/source/p/plone.app.archive/plone.app.archive-0.1.zip
  md5: 0e89ddb7f88765798fe0c470d334816f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.archive = plone.app.archive:main
    #
    # Would create an entry point called plone.app.archive that calls plone.app.archive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - acquisition
    - products.cmfcore
    - zodb3
    - pytz
    - zope.annotation
    - zope.component
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - acquisition
    - products.cmfcore
    - zodb3
    - pytz
    - zope.annotation
    - zope.component
    - zope.interface

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.archive
    - plone.app.archive.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjpieters//plone.app.archive
  license: GPL
  summary: 'Archive Plone content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
