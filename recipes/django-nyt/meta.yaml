package:
  name: django-nyt
  version: "0.9.7.2"

source:
  fn: django-nyt-0.9.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nyt/django-nyt-0.9.7.2.tar.gz
  md5: 7143932181f7a650df41f326ad611cbf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nyt = django-nyt:main
    #
    # Would create an entry point called django-nyt that calls django-nyt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - six

  run:
    - python
    - django >=1.4
    - six

test:
  # Python imports
  imports:
    - django_nyt
    - django_nyt.management
    - django_nyt.management.commands
    - django_nyt.migrations
    - django_nyt.south_migrations
    - django_nyt.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benjaoming/django-nyt
  license: GNU General Public License v3 (GPLv3)
  summary: u'A pluggable notification system written for the Django framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
