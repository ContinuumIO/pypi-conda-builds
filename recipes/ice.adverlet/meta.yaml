package:
  name: ice.adverlet
  version: "0.2.3"

source:
  fn: ice.adverlet-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/i/ice.adverlet/ice.adverlet-0.2.3.tar.gz
  md5: bc4d87ac78690d04ddea93423eed860d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ice.adverlet = ice.adverlet:main
    #
    # Would create an entry point called ice.adverlet that calls ice.adverlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rwproperty
    - z3c.autoinclude
    - zope.contentprovider
    - zope.app.undo
    - zc.resourcelibrary
    - z3c.widget
    - zope.app.zcmlfiles
    - zope.app.twisted
    - zope.app.securitypolicy
    - zope.app.catalog
    - zope.app.apidoc
    - zope.app.preference

  run:
    - python
    - setuptools
    - rwproperty
    - z3c.autoinclude
    - zope.contentprovider
    - zope.app.undo
    - zc.resourcelibrary
    - z3c.widget
    - zope.app.zcmlfiles
    - zope.app.twisted
    - zope.app.securitypolicy
    - zope.app.catalog
    - zope.app.apidoc
    - zope.app.preference

test:
  # Python imports
  imports:
    - ice
    - ice.adverlet
    - ice.adverlet.browser
    - ice.adverlet.demo
    - ice.adverlet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/ice.adverlet
  license: GNU General Public License (GPL)
  summary: 'Simple way to edit any HTML snippet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
