package:
  name: mopidy-podcast-gpodder.net
  version: "1.0.0"

source:
  fn: Mopidy-Podcast-gpodder.net-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Podcast-gpodder.net/Mopidy-Podcast-gpodder.net-1.0.0.tar.gz
  md5: bb8400a38ae5e035e04c9dd0d8bec1a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-podcast-gpodder.net = mopidy-podcast-gpodder.net:main
    #
    # Would create an entry point called mopidy-podcast-gpodder.net that calls mopidy-podcast-gpodder.net.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - mopidy-podcast >=1.0.0
    - requests >=2.0.0

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - mopidy-podcast >=1.0.0
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - mopidy_podcast_gpodder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tkem/mopidy-podcast-gpodder
  license: Apache Software License
  summary: u'Mopidy-Podcast extension for searching and browsing podcasts on gpodder.net'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
