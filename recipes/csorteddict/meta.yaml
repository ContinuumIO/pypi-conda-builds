package:
  name: csorteddict
  version: "0.2"

source:
  fn: csorteddict-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/csorteddict/csorteddict-0.2.tar.gz
  md5: 7ecc05507e582fa808991171e18a16a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csorteddict = csorteddict:main
    #
    # Would create an entry point called csorteddict that calls csorteddict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - csorteddict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jasonscheirer/sorteddict/
  license: BSD License
  summary: 'Key-sorted (balanced tree) dictionary store implemented in C'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
