package:
  name: colorterm
  version: "0.3"

source:
  fn: colorterm-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/colorterm/colorterm-0.3.tar.gz
  md5: 759e6eb4552b8dee46baa3080a50b8b7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorterm = colorterm:main
    #
    # Would create an entry point called colorterm that calls colorterm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - colorterm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LeResKP/colorterm
  license: MIT
  summary: 'Write formatted message in your terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
