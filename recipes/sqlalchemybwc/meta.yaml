package:
  name: sqlalchemybwc
  version: "0.2.11"

source:
  fn: SQLAlchemyBWC-0.2.11.zip
  url: https://pypi.python.org/packages/source/S/SQLAlchemyBWC/SQLAlchemyBWC-0.2.11.zip
  md5: 0f3ef142f429283b5e8243756b60e58d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemybwc = sqlalchemybwc:main
    #
    # Would create an entry point called sqlalchemybwc that calls sqlalchemybwc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pathlib
    - blazeweb >=0.4.9
    - savalidation >=0.2.0
    - sqlitefktg4sa >=0.1.1

  run:
    - python
    - pathlib
    - blazeweb >=0.4.9
    - savalidation >=0.2.0
    - sqlitefktg4sa >=0.1.1

test:
  # Python imports
  imports:
    - sqlalchemybwc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/blazelibs/sqlalchemybwc/
  license: BSD License
  summary: 'An SQLAlchemy component for BlazeWeb applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
