package:
  name: ezhil
  version: "0.76"

source:
  fn: ezhil-0.76.zip
  url: https://pypi.python.org/packages/source/e/ezhil/ezhil-0.76.zip
  md5: 9b478d0c487547b03c8ce33dbaf45ef8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezhil = ezhil:main
    #
    # Would create an entry point called ezhil that calls ezhil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - open-tamil
    - argparse

  run:
    - python
    - open-tamil
    - argparse

test:
  # Python imports
  imports:
    - ezhil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arcturusannamalai/Ezhil-Lang
  license: GPLv3
  summary: 'Ezhil - Tamil programming language implemented in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
