package:
  name: django-bootstrap3-datetimepicker
  version: "2.2.3"

source:
  fn: django-bootstrap3-datetimepicker-2.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap3-datetimepicker/django-bootstrap3-datetimepicker-2.2.3.tar.gz
  md5: aa3ebcb31d4d637a97d60d55c2f47de9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap3-datetimepicker = django-bootstrap3-datetimepicker:main
    #
    # Would create an entry point called django-bootstrap3-datetimepicker that calls django-bootstrap3-datetimepicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap3_datetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nkunihiko/django-bootstrap3-datetimepicker
  license: Apache Software License
  summary: 'Bootstrap3 compatible datetimepicker for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
