package:
  name: invar
  version: "0.1.0"

source:
  fn: invar-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/invar/invar-0.1.0.tar.gz
  md5: 3575e1fd1e92efd7895cfd35d87e48c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - invar = invar:main
    #
    # Would create an entry point called invar that calls invar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse >=1.3.0
    - eventlet >=0.17.1
    - boto >=2.36.0

  run:
    - python
    - argparse >=1.3.0
    - eventlet >=0.17.1
    - boto >=2.36.0

test:
  # Python imports
  imports:
    - invar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://invar.readthedocs.org/
  license: MIT License
  summary: 'Scripts for generating images from Mapnik configuration and storing them on S3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
