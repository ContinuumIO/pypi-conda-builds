package:
  name: bob.buildout
  version: "2.0.6"

source:
  fn: bob.buildout-2.0.6.zip
  url: https://pypi.python.org/packages/source/b/bob.buildout/bob.buildout-2.0.6.zip
  md5: 0e915626dda890f1fbdbad8ce37aa63a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.buildout = bob.buildout:main
    #
    # Would create an entry point called bob.buildout that calls bob.buildout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - six

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - six

test:
  # Python imports
  imports:
    - bob
    - bob.buildout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bioidiap/bob.buildout
  license: GNU General Public License v3 (GPLv3)
  summary: 'zc.buildout recipes to perform a variety of tasks required by Bob satellite packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
