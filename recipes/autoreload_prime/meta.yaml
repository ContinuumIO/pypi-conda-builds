package:
  name: autoreload_prime
  version: "1.0"

source:
  fn: autoreload_prime-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/autoreload_prime/autoreload_prime-1.0.tar.gz
  md5: e60521af84e49a0afff33d7db32c9172
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autoreload_prime = autoreload_prime:main
    #
    # Would create an entry point called autoreload_prime that calls autoreload_prime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - autoreload_prime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.amix.dk/
  license: BSD License
  summary: 'An auto reload module that should work with most servers (patched from Tornado)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
