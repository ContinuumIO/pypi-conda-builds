package:
  name: collective.flowplayercaptions
  version: "0.1.0"

source:
  fn: collective.flowplayercaptions-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.flowplayercaptions/collective.flowplayercaptions-0.1.0.tar.gz
  md5: a595447fb63df13ced02df9adf91fdaa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.flowplayercaptions = collective.flowplayercaptions:main
    #
    # Would create an entry point called collective.flowplayercaptions that calls collective.flowplayercaptions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.flowplayer
    - archetypes.schemaextender
    - plone.app.blob

  run:
    - python
    - setuptools
    - setuptools
    - collective.flowplayer
    - archetypes.schemaextender
    - plone.app.blob

test:
  # Python imports
  imports:
    - collective
    - collective.flowplayercaptions
    - collective.flowplayercaptions.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.flowplayercaptions
  license: GPL
  summary: 'Plone support for captioning video contents that use collective.flowplayer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
