package:
  name: python-libguess
  version: "1.1"

source:
  fn: python-libguess-1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libguess/python-libguess-1.1.tar.gz
  md5: 622f6f8a094b30e156b27d9d4faa0c19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libguess = python-libguess:main
    #
    # Would create an entry point called python-libguess that calls python-libguess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barro/python-libguess/
  license: BSD License
  summary: 'Python wrapper for libguess.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
