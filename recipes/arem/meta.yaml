package:
  name: arem
  version: "1.0.1"

source:
  fn: AREM-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/A/AREM/AREM-1.0.1.tar.gz
  md5: fab4d78f1fd12c691d4bd62140b927ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arem = arem:main
    #
    # Would create an entry point called arem that calls arem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - AREM
    - AREM.IO
    - arem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cbcl.ics.uci.edu/AREM
  license: Artistic License
  summary: 'Aligning Reads by Expectation-Maximization.\nBased on MACS (Model Based Analysis for ChIP-Seq data)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
