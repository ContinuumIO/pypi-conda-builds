package:
  name: logaugment
  version: "0.1.3"

source:
  fn: logaugment-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/l/logaugment/logaugment-0.1.3.tar.gz
  md5: 7d0dcff87b7daef38a45e190ff08105b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logaugment = logaugment:main
    #
    # Would create an entry point called logaugment that calls logaugment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logaugment

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/svisser/logaugment
  license: MIT License
  summary: 'Python logging library for augmenting log records with additional information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
