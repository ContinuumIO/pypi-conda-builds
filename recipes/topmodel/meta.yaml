package:
  name: topmodel
  version: "0.1.2"

source:
  fn: topmodel-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/topmodel/topmodel-0.1.2.tar.gz
  md5: 2699d444d2b44890f0bd72d04ef0f07c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - topmodel = topmodel:main
    #
    # Would create an entry point called topmodel that calls topmodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas ==0.13.0
    - boto ==2.25.0

  run:
    - python
    - pandas ==0.13.0
    - boto ==2.25.0

test:
  # Python imports
  imports:
    - topmodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Model evaluation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
