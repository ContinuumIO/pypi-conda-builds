package:
  name: alarmdecoder
  version: "0.9.3"

source:
  fn: alarmdecoder-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/a/alarmdecoder/alarmdecoder-0.9.3.tar.gz
  md5: aaa81d0c1dbffe7c4ab80818ede4b917
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alarmdecoder = alarmdecoder:main
    #
    # Would create an entry point called alarmdecoder that calls alarmdecoder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopenssl
    - pyusb >=1.0.0b1
    - pyserial >=2.7
    - pyftdi >=0.9.0

  run:
    - python
    - pyopenssl
    - pyusb >=1.0.0b1
    - pyserial >=2.7
    - pyftdi >=0.9.0

test:
  # Python imports
  imports:
    - alarmdecoder
    - alarmdecoder.event

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nutechsoftware/alarmdecoder
  license: MIT License
  summary: 'Python interface for the AlarmDecoder (AD2) family of alarm devices which includes the AD2USB, AD2SERIAL and AD2PI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
