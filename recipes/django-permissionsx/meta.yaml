package:
  name: django-permissionsx
  version: "1.3.4"

source:
  fn: django-permissionsx-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-permissionsx/django-permissionsx-1.3.4.tar.gz
  md5: 10d53a635d8524aa62884f79445ddf80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-permissionsx = django-permissionsx:main
    #
    # Would create an entry point called django-permissionsx that calls django-permissionsx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - permissionsx
    - permissionsx.contrib
    - permissionsx.contrib.django
    - permissionsx.templatetags
    - permissionsx.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thinkingpotato/django-permissionsx
  license: BSD License
  summary: 'Authorization for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
