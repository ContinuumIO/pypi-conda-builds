package:
  name: cubicweb-mercurial-server
  version: "0.6.0"

source:
  fn: cubicweb-mercurial-server-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-mercurial-server/cubicweb-mercurial-server-0.6.0.tar.gz
  md5: 40adb2418a7cb84f6f0e2ca0f5c73b33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-mercurial-server = cubicweb-mercurial-server:main
    #
    # Would create an entry point called cubicweb-mercurial-server that calls cubicweb-mercurial-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16
    - python-hglib >=1.3
    - cubicweb-vcsfile >=2

  run:
    - python
    - cubicweb >=3.16
    - python-hglib >=1.3
    - cubicweb-vcsfile >=2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-mercurial-server
  license: LGPL
  summary: 'mercurial server integration cube'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
