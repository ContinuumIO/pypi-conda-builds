package:
  name: autosync
  version: "0.0.4"

source:
  fn: autosync-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/autosync/autosync-0.0.4.tar.gz
  md5: 1f8c5b476655e210baec3349e7f1f19f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autosync = autosync:main
    #
    # Would create an entry point called autosync that calls autosync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - gevent
    - pyinotify
    - python-gflags

  run:
    - python
    - boto
    - gevent
    - pyinotify
    - python-gflags

test:
  # Python imports
  imports:
    - autosync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wnyc/autosync
  license: GPL
  summary: 'Efficent synchronization of the local file system to S3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
