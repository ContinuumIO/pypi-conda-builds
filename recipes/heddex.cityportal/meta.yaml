package:
  name: heddex.cityportal
  version: "2.0"

source:
  fn: heddex.cityportal-2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/heddex.cityportal/heddex.cityportal-2.0.tar.gz
  md5: 7239e5f54da6d357963c723cb8186224
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heddex.cityportal = heddex.cityportal:main
    #
    # Would create an entry point called heddex.cityportal that calls heddex.cityportal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - heddex
    - heddex.cityportal
    - heddex.cityportal.browser
    - heddex.cityportal.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.heddex.biz
  license: GPL
  summary: 'Installable theme for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
