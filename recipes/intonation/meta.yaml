package:
  name: intonation
  version: "0.1"

source:
  fn: intonation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/intonation/intonation-0.1.tar.gz
  md5: a7e5eb5545bc2f7ed6af5e935a50e845
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intonation = intonation:main
    #
    # Would create an entry point called intonation that calls intonation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - matplotlib
    - pypeaks
    - scipy

  run:
    - python
    - numpy
    - matplotlib
    - pypeaks
    - scipy

test:
  # Python imports
  imports:
    - intonation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gopalkoduri/intonation
  license: GNU Affero General Public License v3
  summary: 'Python module to analyze, study and research the intonation of musical intervals in music recordings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
