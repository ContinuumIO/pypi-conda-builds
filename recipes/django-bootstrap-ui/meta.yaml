package:
  name: django-bootstrap-ui
  version: "0.1.0a5"

source:
  fn: django-bootstrap-ui-0.1.0a5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-ui/django-bootstrap-ui-0.1.0a5.tar.gz
  md5: 1f8717b8d066fab727b66e782752f492
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-ui = django-bootstrap-ui:main
    #
    # Would create an entry point called django-bootstrap-ui that calls django-bootstrap-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7,<1.9
    - django-tag-parser >=2.1,<2.2
    - dominate >=2.1,<2.2

  run:
    - python
    - django >=1.7,<1.9
    - django-tag-parser >=2.1,<2.2
    - dominate >=2.1,<2.2

test:
  # Python imports
  imports:
    - bootstrap_ui
    - bootstrap_ui.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timorieber/django-bootstrap-ui
  license: ISC License (ISCL)
  summary: 'This aims to be a powerful Django app to ease the integration of the popular Bootstrap UI framework (http://getbootstrap.com).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
