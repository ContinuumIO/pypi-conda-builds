package:
  name: ploneintranet.invitations
  version: "0.1"

source:
  fn: ploneintranet.invitations-0.1.zip
  url: https://pypi.python.org/packages/source/p/ploneintranet.invitations/ploneintranet.invitations-0.1.zip
  md5: fddd19d432b80244d87a80200294c74a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ploneintranet.invitations = ploneintranet.invitations:main
    #
    # Would create an entry point called ploneintranet.invitations that calls ploneintranet.invitations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - plone.api

test:
  # Python imports
  imports:
    - ploneintranet
    - ploneintranet.invitations
    - ploneintranet.invitations.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Generic invitation framework for use with ploneintranet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
