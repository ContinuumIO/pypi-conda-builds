package:
  name: django-fancy-admin
  version: "0.2.1"

source:
  fn: django-fancy-admin-0.2.1.zip
  url: https://pypi.python.org/packages/source/d/django-fancy-admin/django-fancy-admin-0.2.1.zip
  md5: 318ca7c753b36b9b285d2750b0f56684
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fancy-admin = django-fancy-admin:main
    #
    # Would create an entry point called django-fancy-admin that calls django-fancy-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fancy_admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/baskoopmans/django-fancy-admin/
  license: BSD License
  summary: 'A simple, elegant and beautiful admin UI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
