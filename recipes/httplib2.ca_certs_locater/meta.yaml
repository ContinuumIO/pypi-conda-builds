package:
  name: httplib2.ca_certs_locater
  version: "0.2.0"

source:
  fn: httplib2.ca_certs_locater-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httplib2.ca_certs_locater/httplib2.ca_certs_locater-0.2.0.tar.gz
  md5: 78b400cad0a4fb76a6b01aa9c71666cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httplib2.ca_certs_locater = httplib2.ca_certs_locater:main
    #
    # Would create an entry point called httplib2.ca_certs_locater that calls httplib2.ca_certs_locater.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Plug in to httplib2 to tell it to use the certificate authority file from the base OS instead of the one in the httplib2 package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
