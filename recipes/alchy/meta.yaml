package:
  name: alchy
  version: "2.0.0"

source:
  fn: alchy-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/alchy/alchy-2.0.0.tar.gz
  md5: 5f534a7bcea731592a24198bb93b74d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alchy = alchy:main
    #
    # Would create an entry point called alchy that calls alchy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9.0
    - pydash >=3.2.2

  run:
    - python
    - sqlalchemy >=0.9.0
    - pydash >=3.2.2

test:
  # Python imports
  imports:
    - alchy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dgilland/alchy
  license: MIT License
  summary: 'A SQLAlchemy extension for its declarative ORM that provides enhancements for model classes, queries, and sessions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
