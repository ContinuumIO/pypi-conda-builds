package:
  name: ectoken
  version: "0.4"

source:
  fn: ectoken-0.4.tar.gz
  url: https://pypi.python.org/packages/source/e/ectoken/ectoken-0.4.tar.gz
  md5: 04bdd8e017a0ea64aa7340e59933a9b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ectoken = ectoken:main
    #
    # Would create an entry point called ectoken that calls ectoken.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ectoken

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sebest/ectoken-py
  license: BSD License
  summary: 'Python implementation of EdgeCast Token (ectoken_generate).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
