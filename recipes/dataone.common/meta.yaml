package:
  name: dataone.common
  version: "1.1.6"

source:
  fn: dataone.common-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.common/dataone.common-1.1.6.tar.gz
  md5: 1ed99a3a8cca5a18024a8d1714e4a5f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.common = dataone.common:main
    #
    # Would create an entry point called dataone.common that calls dataone.common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso8601 ==0.1.4
    - pyxb ==1.2.3

  run:
    - python
    - iso8601 ==0.1.4
    - pyxb ==1.2.3

test:
  # Python imports
  imports:
    - d1_common
    - d1_common.ext
    - d1_common.types
    - d1_common.types.generated
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'Contains functionality common to projects that interact with the DataONE infrastructure via Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
