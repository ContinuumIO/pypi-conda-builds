package:
  name: django_opentok
  version: "0.0.1"

source:
  fn: django_opentok-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_opentok/django_opentok-0.0.1.tar.gz
  md5: 959309b7d65627446bbf44e666d8d6a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_opentok = django_opentok:main
    #
    # Would create an entry point called django_opentok that calls django_opentok.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_opentok

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/awwester/django-opentok
  license: UNKNOWN
  summary: 'Integrate the opentok WebRTC into a Django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
