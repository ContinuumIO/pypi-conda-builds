package:
  name: django-parler-rest
  version: "1.2.1"

source:
  fn: django-parler-rest-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-parler-rest/django-parler-rest-1.2.1.tar.gz
  md5: f2e90cbd0955e839a5ce14e2c19b8b8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-parler-rest = django-parler-rest:main
    #
    # Would create an entry point called django-parler-rest that calls django-parler-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-parler >=1.2
    - djangorestframework >=2.0,<3.0

  run:
    - python
    - django-parler >=1.2
    - djangorestframework >=2.0,<3.0

test:
  # Python imports
  imports:
    - parler_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/django-parler-rest
  license: Apache Software License
  summary: 'Multilingual support for django-rest-framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
