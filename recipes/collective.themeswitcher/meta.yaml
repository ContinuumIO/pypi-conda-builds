package:
  name: collective.themeswitcher
  version: "1.0a2"

source:
  fn: collective.themeswitcher-1.0a2.zip
  url: https://pypi.python.org/packages/source/c/collective.themeswitcher/collective.themeswitcher-1.0a2.zip
  md5: ca99f4a6d19a7b875e5b5b61bd68634d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.themeswitcher = collective.themeswitcher:main
    #
    # Would create an entry point called collective.themeswitcher that calls collective.themeswitcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.themeswitcher
    - collective.themeswitcher.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.themeswitcher
  license: GNU General Public License (GPL)
  summary: 'Theme switcher'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
