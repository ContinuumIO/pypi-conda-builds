package:
  name: learnsprout
  version: "0.2.5"

source:
  fn: LearnSprout-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/L/LearnSprout/LearnSprout-0.2.5.tar.gz
  md5: be688385f5d619963a788571eca21e7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - learnsprout = learnsprout:main
    #
    # Would create an entry point called learnsprout that calls learnsprout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-transition ==1.0.4.0

  run:
    - python
    - requests-transition ==1.0.4.0

test:
  # Python imports
  imports:
    - learnsprout
    - learnsprout.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/LearnSprout/
  license: Learn-Tibetan
  summary: 'A simple python client for working with the LearnSprout API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
