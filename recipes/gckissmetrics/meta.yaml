package:
  name: gckissmetrics
  version: "1.0.3"

source:
  fn: gcKISSmetrics-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gcKISSmetrics/gcKISSmetrics-1.0.3.tar.gz
  md5: 81c26357bb5131eebe521cf949b39bd4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gckissmetrics = gckissmetrics:main
    #
    # Would create an entry point called gckissmetrics that calls gckissmetrics.main()

    - kissmetrics = km:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gckissmetrics
    - km
    - km.helpers

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - kissmetrics --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gamechanger/KISSmetrics-python
  license: Other/Proprietary License
  summary: 'Python API for KISSmetrics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
