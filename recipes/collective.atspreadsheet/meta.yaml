package:
  name: collective.atspreadsheet
  version: "1.0a2"

source:
  fn: collective.atspreadsheet-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.atspreadsheet/collective.atspreadsheet-1.0a2.tar.gz
  md5: 620713b1b1ef190d71327c560be1b482
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.atspreadsheet = collective.atspreadsheet:main
    #
    # Would create an entry point called collective.atspreadsheet that calls collective.atspreadsheet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.atspreadsheet
    - collective.atspreadsheet.browser
    - collective.atspreadsheet.content
    - collective.atspreadsheet.interfaces
    - collective.atspreadsheet.portlets
    - collective.atspreadsheet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License (GPL)
  summary: 'Spreadsheet content type for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
