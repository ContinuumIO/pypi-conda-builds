package:
  name: thingking
  version: "1.0.2"

source:
  fn: thingking-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/thingking/thingking-1.0.2.tar.gz
  md5: 2f40997f60352a0dbc23c2a1e87d8284
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thingking = thingking:main
    #
    # Would create an entry point called thingking that calls thingking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - numpy
    - functools32

  run:
    - python
    - requests
    - numpy
    - functools32

test:
  # Python imports
  imports:
    - thingking

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'A memory map for the World Wide Web'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
