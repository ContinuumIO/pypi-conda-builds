package:
  name: monocle_partners
  version: "0.2.0.0"

source:
  fn: monocle_partners-0.2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle_partners/monocle_partners-0.2.0.0.tar.gz
  md5: 2f4154c00be982bbb5f121a30c3d8b82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle_partners = monocle_partners:main
    #
    # Would create an entry point called monocle_partners that calls monocle_partners.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - easy_thumbnails

  run:
    - python
    - pillow
    - easy_thumbnails

test:
  # Python imports
  imports:
    - monocle_partners

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Partners for monocle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
