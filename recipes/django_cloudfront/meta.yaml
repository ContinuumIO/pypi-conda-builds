package:
  name: django_cloudfront
  version: "0.1"

source:
  fn: django_cloudfront-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_cloudfront/django_cloudfront-0.1.tar.gz
  md5: 2878db855d37d8476c9e54d33650028d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_cloudfront = django_cloudfront:main
    #
    # Would create an entry point called django_cloudfront that calls django_cloudfront.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto ==2.6

  run:
    - python
    - pycrypto ==2.6

test:
  # Python imports
  imports:
    - cloudfront
    - django_cloudfront

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tzangms/django_cloudfront
  license: MIT
  summary: 'cloudfront private content signing for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
