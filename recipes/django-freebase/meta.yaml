package:
  name: django-freebase
  version: "0.1dev"

source:
  fn: django-freebase-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-freebase/django-freebase-0.1dev.tar.gz
  md5: c9ffd2ceae63a685f18270b6770d7590
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-freebase = django-freebase:main
    #
    # Would create an entry point called django-freebase that calls django-freebase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - freebase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-freebase/
  license: LICENSE.txt
  summary: 'Freebasing in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
