package:
  name: git-foolscap
  version: "0.1"

source:
  fn: git-foolscap-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/git-foolscap/git-foolscap-0.1.tar.gz
  md5: 3901a6b69ead76d01a65db3a580e59e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-foolscap = git-foolscap:main
    #
    # Would create an entry point called git-foolscap that calls git-foolscap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - foolscap >=0.7.0

  run:
    - python
    - foolscap >=0.7.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/warner/git-foolscap
  license: MIT License
  summary: 'Tools to run Git over Foolscap FURLs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
