package:
  name: chula
  version: "0.14.0.dev"

source:
  fn: Chula-0.14.0.dev.tar.gz
  url: https://pypi.python.org/packages/source/C/Chula/Chula-0.14.0.dev.tar.gz
  md5: c54afe0cbcf7610665e2bd5c16073207
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chula = chula:main
    #
    # Would create an entry point called chula that calls chula.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chula
    - chula.collection
    - chula.db
    - chula.db.engines
    - chula.nosql
    - chula.queue
    - chula.queue.messages
    - chula.session
    - chula.session.backends
    - chula.test
    - chula.vendor
    - chula.www
    - chula.www.adapters
    - chula.www.adapters.fcgi
    - chula.www.adapters.mod_python
    - chula.www.adapters.wsgi
    - chula.www.controller
    - chula.www.mapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chula.rockfloat.com
  license: GNU General Public License (GPL)
  summary: 'Chula is a lightweight toolkit for writing web applications in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
