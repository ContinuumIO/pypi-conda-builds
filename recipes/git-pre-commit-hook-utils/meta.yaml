package:
  name: git-pre-commit-hook-utils
  version: "0.0.5"

source:
  fn: git-pre-commit-hook-utils-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/git-pre-commit-hook-utils/git-pre-commit-hook-utils-0.0.5.tar.gz
  md5: 6bb70705fcd09c92008b23f6dc28c378
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-pre-commit-hook-utils = git-pre-commit-hook-utils:main
    #
    # Would create an entry point called git-pre-commit-hook-utils that calls git-pre-commit-hook-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - git_pre_commit_hook_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/evvers/git-pre-commit-hook-utils
  license: MIT
  summary: 'utils for implementing git pre-commit hook'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
