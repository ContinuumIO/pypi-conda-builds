package:
  name: cmdhist
  version: "0.2.1"

source:
  fn: cmdhist-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmdhist/cmdhist-0.2.1.tar.gz
  md5: 623456dca0578ba36815f1eda950d98d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmdhist = cmdhist:main
    #
    # Would create an entry point called cmdhist that calls cmdhist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse >=1.2.1
    - requests[secure] >=2.2.1
    - pbkdf2 >=1.3
    - pycrypto >=2.6.1

  run:
    - python
    - argparse >=1.2.1
    - requests[secure] >=2.2.1
    - pbkdf2 >=1.3
    - pycrypto >=2.6.1

test:
  # Python imports
  imports:
    - cmdhist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/manishrjain/cmdhist
  license: LICENSE.txt
  summary: 'Commandline history in the cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
