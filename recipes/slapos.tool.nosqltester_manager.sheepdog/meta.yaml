package:
  name: slapos.tool.nosqltester_manager.sheepdog
  version: "0.0.1.dev"

source:
  fn: slapos.tool.nosqltester_manager.sheepdog-0.0.1.dev.tar.gz
  url: https://pypi.python.org/packages/source/s/slapos.tool.nosqltester_manager.sheepdog/slapos.tool.nosqltester_manager.sheepdog-0.0.1.dev.tar.gz
  md5: e386d4f75d0f2fbc9aae130ea23c1589
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slapos.tool.nosqltester_manager.sheepdog = slapos.tool.nosqltester_manager.sheepdog:main
    #
    # Would create an entry point called slapos.tool.nosqltester_manager.sheepdog that calls slapos.tool.nosqltester_manager.sheepdog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - slapos.tool.nosqltester_manager

  run:
    - python
    - setuptools
    - flask
    - slapos.tool.nosqltester_manager

test:
  # Python imports
  imports:
    - slapos
    - slapos.tool
    - slapos.tool.nosqltester_manager
    - slapos.tool.nosqltester_manager.sheepdog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'Manager for sheepdog tester'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
