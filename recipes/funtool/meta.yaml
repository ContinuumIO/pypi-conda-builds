package:
  name: funtool
  version: "0.0.81"

source:
  fn: funtool-0.0.81.tar.gz
  url: https://pypi.python.org/packages/source/f/funtool/funtool-0.0.81.tar.gz
  md5: 4968b80e148c73f97ae0488a77c25a42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - funtool = funtool:main
    #
    # Would create an entry point called funtool that calls funtool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - funtool
    - funtool.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pjanis/funtool
  license: MIT License
  summary: 'FUN Tool engine: Used process an analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
