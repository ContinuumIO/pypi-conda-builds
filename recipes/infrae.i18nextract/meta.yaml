package:
  name: infrae.i18nextract
  version: "1.3.1"

source:
  fn: infrae.i18nextract-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/i/infrae.i18nextract/infrae.i18nextract-1.3.1.tar.gz
  md5: efeb4d8e813ef8a7a66b821ffbd20cbf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infrae.i18nextract = infrae.i18nextract:main
    #
    # Would create an entry point called infrae.i18nextract that calls infrae.i18nextract.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - setuptools

test:
  # Python imports
  imports:
    - infrae
    - infrae.i18nextract

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.infrae.com/buildout/infrae.i18nextract/trunk/
  license: Zope Public License
  summary: 'Buildout recipe to extract i18n files in Silva'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
