package:
  name: buildout.packager
  version: "0.0.1dev-r662"

source:
  fn: buildout.packager-0.0.1dev-r662.zip
  url: https://pypi.python.org/packages/source/b/buildout.packager/buildout.packager-0.0.1dev-r662.zip
  md5: e41e9eaf4c974497032f9241dad06964
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.packager = buildout.packager:main
    #
    # Would create an entry point called buildout.packager that calls buildout.packager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.freia.jp/open/buildout.packager/
  license: Python Software Foundation License
  summary: 'buildout.packager generate installer from setup.py and buildout.cfg files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
