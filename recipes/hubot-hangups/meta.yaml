package:
  name: hubot-hangups
  version: "1.0.0"

source:
  fn: hubot-hangups-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hubot-hangups/hubot-hangups-1.0.0.tar.gz
  md5: 53fc009743eddb733e2cf43c06620e8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hubot-hangups = hubot-hangups:main
    #
    # Would create an entry point called hubot-hangups that calls hubot-hangups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hangups ==0.2.4
    - requests ==2.2.1
    - cherrypy

  run:
    - python
    - hangups ==0.2.4
    - requests ==2.2.1
    - cherrypy

test:
  # Python imports
  imports:
    - sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/groupby/hubot-hangups
  license: MIT License
  summary: 'A hubot adapter for Google Hangouts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
