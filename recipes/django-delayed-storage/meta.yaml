package:
  name: django-delayed-storage
  version: "0.1.6"

source:
  fn: django-delayed-storage-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-delayed-storage/django-delayed-storage-0.1.6.tar.gz
  md5: 5e54f1c0b3c6948bfd3cd53ca2f5d135
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-delayed-storage = django-delayed-storage:main
    #
    # Would create an entry point called django-delayed-storage that calls django-delayed-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-celery

  run:
    - python
    - django >=1.2
    - django-celery

test:
  # Python imports
  imports:
    - django_delayed_storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pentusha/django-delayed-storage/
  license: BSD License
  summary: '\nTransparent django storage proxying for delayed operations with files\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
