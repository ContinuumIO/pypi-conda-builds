package:
  name: ua2.fabdep
  version: "0.0.2"

source:
  fn: ua2.fabdep-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ua2.fabdep/ua2.fabdep-0.0.2.tar.gz
  md5: 92440eb28486e6427edfa14ea4e4abc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ua2.fabdep = ua2.fabdep:main
    #
    # Would create an entry point called ua2.fabdep that calls ua2.fabdep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ua2
    - ua2.fabdep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ua2web/ua2.fabdep
  license: BSD License
  summary: 'Fabric management for external source code repositories.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
