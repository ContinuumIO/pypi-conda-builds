package:
  name: django-event-calendar
  version: "0.0.1"

source:
  fn: django-event-calendar-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-event-calendar/django-event-calendar-0.0.1.tar.gz
  md5: bdd724483983f0b85cfc06c9b24bc6c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-event-calendar = django-event-calendar:main
    #
    # Would create an entry point called django-event-calendar that calls django-event-calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-piston

  run:
    - python
    - django
    - django-piston

test:
  # Python imports
  imports:
    - event_calendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rvo.name/
  license: MIT License
  summary: 'An event calendar for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
