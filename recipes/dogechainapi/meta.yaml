package:
  name: dogechainapi
  version: "0.1"

source:
  fn: DogechainApi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DogechainApi/DogechainApi-0.1.tar.gz
  md5: 2b1bb91eec583401611538dae515ee3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dogechainapi = dogechainapi:main
    #
    # Would create an entry point called dogechainapi that calls dogechainapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - DogechainApi
    - dogechainapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Dirrot/python-dogechain-api
  license: UNKNOWN
  summary: 'Python API for www.dogechain.info'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
