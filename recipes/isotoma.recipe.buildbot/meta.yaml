package:
  name: isotoma.recipe.buildbot
  version: "0.0.33"

source:
  fn: isotoma.recipe.buildbot-0.0.33.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.buildbot/isotoma.recipe.buildbot-0.0.33.zip
  md5: 6ae959efe4282fd7d09feee20820832f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.buildbot = isotoma.recipe.buildbot:main
    #
    # Would create an entry point called isotoma.recipe.buildbot that calls isotoma.recipe.buildbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - cheetah

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.buildbot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'A recipe to help setup a buildbot master and slaves'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
