package:
  name: information_systems
  version: "0.2"

source:
  fn: information_systems-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/information_systems/information_systems-0.2.tar.gz
  md5: a295bfb37d0733319e0720c271d11da7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - information_systems = information_systems:main
    #
    # Would create an entry point called information_systems that calls information_systems.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - components
    - components.accounts
    - components.address
    - components.careers
    - components.certificates
    - components.commons
    - components.communications
    - components.controls
    - components.educations
    - components.organizations
    - components.persons
    - components.test_scores
    - information_systems

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Information System For ERP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
