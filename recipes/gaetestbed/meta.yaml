package:
  name: gaetestbed
  version: "0.12"

source:
  fn: gaetestbed-0.12.tar.gz
  url: https://pypi.python.org/packages/source/g/gaetestbed/gaetestbed-0.12.tar.gz
  md5: f969c1e6b9c6638b10d7dde17f3ffd8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaetestbed = gaetestbed:main
    #
    # Would create an entry point called gaetestbed that calls gaetestbed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gaetestbed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jgeewax.github.com/gaetestbed
  license: GNU General Public License (GPL)
  summary: 'GAE Testbed is a set of test cases to simplify testing on AppEngine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
