package:
  name: magickwand
  version: "0.2"

source:
  fn: magickwand-0.2.tar.bz2
  url: https://pypi.python.org/packages/source/m/magickwand/magickwand-0.2.tar.bz2
  md5: 4e667143a4e8051e1b7b812459ccf4af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magickwand = magickwand:main
    #
    # Would create an entry point called magickwand that calls magickwand.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - magickwand
    - magickwand.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://digitalarchitekt.de/contributions/magickwand
  license: UNKNOWN
  summary: "Python bindings for ImageMagick's MagickWand 6.6.0"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
