package:
  name: dolmen.menu
  version: "0.5"

source:
  fn: dolmen.menu-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dolmen.menu/dolmen.menu-0.5.tar.gz
  md5: 21768bc44a0175f2bd1bff3300415b72
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolmen.menu = dolmen.menu:main
    #
    # Would create an entry point called dolmen.menu that calls dolmen.menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grokcore.component
    - grokcore.viewlet
    - grokcore.view
    - megrok.pagetemplate
    - martian
    - zope.location
    - zope.interface
    - zope.security
    - zope.component
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing
    - zope.viewlet

  run:
    - python
    - setuptools
    - grokcore.component
    - grokcore.viewlet
    - grokcore.view
    - megrok.pagetemplate
    - martian
    - zope.location
    - zope.interface
    - zope.security
    - zope.component
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing
    - zope.viewlet

test:
  # Python imports
  imports:
    - dolmen
    - dolmen.menu
    - dolmen.menu.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Dolmen menus composer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
