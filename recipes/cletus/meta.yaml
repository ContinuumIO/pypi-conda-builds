package:
  name: cletus
  version: "1.0.10"

source:
  fn: cletus-1.0.10.tar.gz
  url: https://pypi.python.org/packages/source/c/cletus/cletus-1.0.10.tar.gz
  md5: d3904d9dfe22595bd015e7a017c62005
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cletus = cletus:main
    #
    # Would create an entry point called cletus that calls cletus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.0
    - appdirs >=1.3.0
    - pytest >=2.6.0
    - tox >=1.8.0
    - envoy >=0.0.3
    - py >=1.4.0
    - validictory >=0.9.0

  run:
    - python
    - pyyaml >=3.0
    - appdirs >=1.3.0
    - pytest >=2.6.0
    - tox >=1.8.0
    - envoy >=0.0.3
    - py >=1.4.0
    - validictory >=0.9.0

test:
  # Python imports
  imports:
    - cletus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kenfar/cletus
  license: BSD License
  summary: 'A library of command line utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
