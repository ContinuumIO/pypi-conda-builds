package:
  name: brisa-media-server
  version: "0.1.1"

source:
  fn: brisa-media-server-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brisa-media-server/brisa-media-server-0.1.1.tar.gz
  md5: 29f9d6ec035a2ecd74e31eb21894cc44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brisa-media-server = brisa-media-server:main
    #
    # Would create an entry point called brisa-media-server that calls brisa-media-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - brisa_media_server
    - brisa_media_server/assistant
    - brisa_media_server/conf
    - brisa_media_server/conf/gui
    - brisa_media_server/conf/gui/gtk
    - brisa_media_server/plugins
    - brisa_media_server/plugins/media_library
    - brisa_media_server/services
    - brisa_media_server/services/media_registrar_ms
    - brisa_media_server/xml_descriptions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://garage.maemo.org/projects/brisa/
  license: MIT License
  summary: 'BRisa Media Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
