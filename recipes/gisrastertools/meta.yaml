package:
  name: gisrastertools
  version: "0.1"

source:
  fn: gisrastertools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gisrastertools/gisrastertools-0.1.tar.gz
  md5: 8743321f3d366b2ea1e1185459714ccf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gisrastertools = gisrastertools:main
    #
    # Would create an entry point called gisrastertools that calls gisrastertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - shapely
    - numpy
    - gdal
    - docopt
    - pandas
    - pyproj
    - scikit-image

  run:
    - python
    - shapely
    - numpy
    - gdal
    - docopt
    - pandas
    - pyproj
    - scikit-image

test:
  # Python imports
  imports:
    - gisrastertools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ozak/gis-raster-tools
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Tools for working with Geographical Information System Rasters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
