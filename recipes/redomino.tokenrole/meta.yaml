package:
  name: redomino.tokenrole
  version: "0.10"

source:
  fn: redomino.tokenrole-0.10.zip
  url: https://pypi.python.org/packages/source/r/redomino.tokenrole/redomino.tokenrole-0.10.zip
  md5: 976c2e33c5682c8ca5613320d6819b1e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.tokenrole = redomino.tokenrole:main
    #
    # Would create an entry point called redomino.tokenrole that calls redomino.tokenrole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - redomino
    - redomino.tokenrole
    - redomino.tokenrole.browser
    - redomino.tokenrole.plugins
    - redomino.tokenrole.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.tokenrole
  license: GPL
  summary: 'This product allows you to share roles about a specific Plone content to an unregistered user through a link.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
