package:
  name: mathjaxplugin
  version: "0.1.4"

source:
  fn: MathJaxPlugin-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/M/MathJaxPlugin/MathJaxPlugin-0.1.4.tar.gz
  md5: dfe9d026a90c884c39016051dbc9a10e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mathjaxplugin = mathjaxplugin:main
    #
    # Would create an entry point called mathjaxplugin that calls mathjaxplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - mathjax
    - mathjaxplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mitar.tnode.com/
  license: AGPLv3
  summary: 'Renders mathematical equations using MathJax library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
