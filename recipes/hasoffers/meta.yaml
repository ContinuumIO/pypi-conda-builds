package:
  name: hasoffers
  version: "0.2.0"

source:
  fn: hasoffers-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hasoffers/hasoffers-0.2.0.tar.gz
  md5: 59ea0db627c11a76f03f709ee54e452d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hasoffers = hasoffers:main
    #
    # Would create an entry point called hasoffers that calls hasoffers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - six

  run:
    - python
    - requests
    - six

test:
  # Python imports
  imports:
    - hasoffers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OohlaLabs/hasoffers
  license: MIT
  summary: 'Python low-level client for HasOffers.com. http://developers.hasoffers.com/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
