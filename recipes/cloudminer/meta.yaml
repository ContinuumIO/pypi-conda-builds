package:
  name: cloudminer
  version: "0.2"

source:
  fn: cloudminer-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudminer/cloudminer-0.2.tar.gz
  md5: 6d76d967ed7a5135befdb8ddead8212e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudminer = cloudminer:main
    #
    # Would create an entry point called cloudminer that calls cloudminer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - cloudyvents

  run:
    - python
    - nose
    - cloudyvents

test:
  # Python imports
  imports:
    - cloudminer
    - cloudminer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nimbusproject.org/
  license: Apache2
  summary: 'Persistence API for the cloudyvents API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
