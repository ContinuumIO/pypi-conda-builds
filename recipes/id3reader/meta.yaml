package:
  name: id3reader
  version: "1.53.20070415"

source:
  fn: id3reader-1.53.20070415.tar.gz
  url: https://pypi.python.org/packages/source/i/id3reader/id3reader-1.53.20070415.tar.gz
  md5: 6ffe63b47dde905c54a9dc761ad27867
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - id3reader = id3reader:main
    #
    # Would create an entry point called id3reader that calls id3reader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - id3reader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/voidfiles/id3reader/tree/master
  license: UNKNOWN
  summary: 'Id3reader.py is a Python module that reads ID3 metadata tags in MP3 files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
