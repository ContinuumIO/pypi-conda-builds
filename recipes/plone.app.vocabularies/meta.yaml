package:
  name: plone.app.vocabularies
  version: "2.1.16"

source:
  fn: plone.app.vocabularies-2.1.16.zip
  url: https://pypi.python.org/packages/source/p/plone.app.vocabularies/plone.app.vocabularies-2.1.16.zip
  md5: 2e10a5c09216889d09fd7f70ffa68ac9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.vocabularies = plone.app.vocabularies:main
    #
    # Would create an entry point called plone.app.vocabularies that calls plone.app.vocabularies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.browser
    - zope.component
    - zope.formlib
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.site
    - acquisition
    - products.cmfcore
    - zope2
    - plone.app.querystring
    - pytz

  run:
    - python
    - setuptools
    - zope.browser
    - zope.component
    - zope.formlib
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.site
    - acquisition
    - products.cmfcore
    - zope2
    - plone.app.querystring
    - pytz

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.vocabularies
    - plone.app.vocabularies.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.app.vocabularies
  license: GNU General Public License (GPL)
  summary: 'A collection of generally useful vocabularies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
