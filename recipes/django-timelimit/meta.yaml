package:
  name: django-timelimit
  version: "0.1"

source:
  fn: django-timelimit-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timelimit/django-timelimit-0.1.tar.gz
  md5: 814f0aad4a3dc1f1c119882a4fe28698
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timelimit = django-timelimit:main
    #
    # Would create an entry point called django-timelimit that calls django-timelimit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_timelimit
    - django_timelimit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/evzijst/django-timelimit
  license: MIT License
  summary: 'A Django template tag used to enforce render time limits within templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
