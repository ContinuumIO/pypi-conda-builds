package:
  name: tadlib
  version: "0.2.4"

source:
  fn: TADLib-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/T/TADLib/TADLib-0.2.4.tar.gz
  md5: b00724e5d669401a6b3fa318df7cd053
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tadlib = tadlib:main
    #
    # Would create an entry point called tadlib that calls tadlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tadlib
    - tadlib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/XiaoTaoWang/TADLib/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A Library to Explore Chromatin Interaction Patterns for Topologically Associating Domains'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
