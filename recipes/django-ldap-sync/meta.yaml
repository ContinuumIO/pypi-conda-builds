package:
  name: django-ldap-sync
  version: "0.1.1"

source:
  fn: django-ldap-sync-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ldap-sync/django-ldap-sync-0.1.1.tar.gz
  md5: 8f6a321962604d2674a3d8b605c39c01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ldap-sync = django-ldap-sync:main
    #
    # Would create an entry point called django-ldap-sync that calls django-ldap-sync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - python-ldap >=2.4.13

  run:
    - python
    - python-ldap >=2.4.13

test:
  # Python imports
  imports:
    - ldap_sync
    - ldap_sync.management
    - ldap_sync.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbittel/django-ldap-sync
  license: BSD License
  summary: 'A Django application for synchronizing LDAP users and groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
