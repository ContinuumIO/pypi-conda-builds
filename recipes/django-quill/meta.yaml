package:
  name: django-quill
  version: "0.0.5"

source:
  fn: django-quill-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quill/django-quill-0.0.5.tar.gz
  md5: d7e2bd23975004cbbd076621c32ecada
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quill = django-quill:main
    #
    # Would create an entry point called django-quill that calls django-quill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - quill

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Easily use Quill.js in your django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
