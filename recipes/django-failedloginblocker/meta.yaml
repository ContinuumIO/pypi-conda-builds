package:
  name: django-failedloginblocker
  version: "1.0.1"

source:
  fn: django-failedloginblocker-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-failedloginblocker/django-failedloginblocker-1.0.1.tar.gz
  md5: 63a5ffb0e12bfa7be18df3d034d3b666
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-failedloginblocker = django-failedloginblocker:main
    #
    # Would create an entry point called django-failedloginblocker that calls django-failedloginblocker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - failedloginblocker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mysociety/django-failedloginblocker
  license: BSD (3-Clause) License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
