package:
  name: monet.calendar.extensions
  version: "0.10.0"

source:
  fn: monet.calendar.extensions-0.10.0.zip
  url: https://pypi.python.org/packages/source/m/monet.calendar.extensions/monet.calendar.extensions-0.10.0.zip
  md5: 47d825d264ef85a28101b9567bdf5197
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monet.calendar.extensions = monet.calendar.extensions:main
    #
    # Would create an entry point called monet.calendar.extensions that calls monet.calendar.extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - monet.calendar.event >=0.5.0

  run:
    - python
    - setuptools
    - setuptools
    - monet.calendar.event >=0.5.0

test:
  # Python imports
  imports:
    - monet
    - monet.calendar
    - monet.calendar.extensions
    - monet.calendar.extensions.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/monet.calendar.star
  license: GNU General Public License (GPL)
  summary: 'Provide additional search features for monet.calendar.star solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
