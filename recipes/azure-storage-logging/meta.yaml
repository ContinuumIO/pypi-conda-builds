package:
  name: azure-storage-logging
  version: "0.3.0"

source:
  fn: azure-storage-logging-0.3.0.zip
  url: https://pypi.python.org/packages/source/a/azure-storage-logging/azure-storage-logging-0.3.0.zip
  md5: b90ac7016dbbd1af4008876a43035106
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - azure-storage-logging = azure-storage-logging:main
    #
    # Would create an entry point called azure-storage-logging that calls azure-storage-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - azure >=0.9

  run:
    - python
    - azure >=0.9

test:
  # Python imports
  imports:
    - azure_storage_logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/michiya/azure-storage-logging
  license: Apache Software License
  summary: 'Logging handlers to send logging output to Microsoft Azure Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
