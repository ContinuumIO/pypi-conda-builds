package:
  name: career
  version: "0.0.1"

source:
  fn: career-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/career/career-0.0.1.tar.gz
  md5: 0b87e9c0e719eff905166836366182e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - career = career:main
    #
    # Would create an entry point called career that calls career.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-hvad

  run:
    - python
    - django >=1.3
    - django-hvad

test:
  # Python imports
  imports:
    - career
    - career.migrations
    - career.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danieltellez/career
  license: GNU General Public License v2 (GPLv2)
  summary: 'The very basic to start some elearning activity'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
