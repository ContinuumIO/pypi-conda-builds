package:
  name: beets-artistcountry
  version: "0.1.1"

source:
  fn: beets-artistcountry-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/beets-artistcountry/beets-artistcountry-0.1.1.tar.gz
  md5: acf983dc23c4f1d596048053966f6be1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beets-artistcountry = beets-artistcountry:main
    #
    # Would create an entry point called beets-artistcountry that calls beets-artistcountry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beets
    - musicbrainzngs

  run:
    - python
    - beets
    - musicbrainzngs

test:
  # Python imports
  imports:
    - beetsplug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agrausem/beets-artistcountry
  license: UNKNOWN
  summary: 'Beets plugin to retrieve the country of an artist from musicbrainz'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
