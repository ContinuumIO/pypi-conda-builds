package:
  name: django_dirty_bits
  version: "0.1.3.3"

source:
  fn: django_dirty_bits-0.1.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_dirty_bits/django_dirty_bits-0.1.3.3.tar.gz
  md5: fbb2fd275606a881569559d0b851bb94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_dirty_bits = django_dirty_bits:main
    #
    # Would create an entry point called django_dirty_bits that calls django_dirty_bits.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_dirty_bits

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Dirty bits checking for Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
