package:
  name: zc.testbrowser
  version: "1.0.0a5"

source:
  fn: zc.testbrowser-1.0.0a5.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.testbrowser/zc.testbrowser-1.0.0a5.tar.gz
  md5: c0f4d81b3ddcd7527275f3750cba45db
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.testbrowser = zc.testbrowser:main
    #
    # Would create an entry point called zc.testbrowser that calls zc.testbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - clientform
    - mechanize
    - setuptools
    - simplejson
    - zope.interface
    - zope.schema

  run:
    - python
    - clientform
    - mechanize
    - setuptools
    - simplejson
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - zc
    - zc.testbrowser
    - zc.testbrowser.ftests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.testbrowser
  license: Zope Public License
  summary: 'Programmable web browser for functional black-box testing of web applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
