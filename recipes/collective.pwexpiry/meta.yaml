package:
  name: collective.pwexpiry
  version: "0.8.1"

source:
  fn: collective.pwexpiry-0.8.1.zip
  url: https://pypi.python.org/packages/source/c/collective.pwexpiry/collective.pwexpiry-0.8.1.zip
  md5: bac145c8bc5d09203e04fdedbe82f9cc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pwexpiry = collective.pwexpiry:main
    #
    # Would create an entry point called collective.pwexpiry that calls collective.pwexpiry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.pwexpiry
    - collective.pwexpiry.actions
    - collective.pwexpiry.browser
    - collective.pwexpiry.browser.emails
    - collective.pwexpiry.browser.preferences
    - collective.pwexpiry.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.enfoldsystems.com
  license: Zope Public License
  summary: 'Emulate Active Directory password complexity requirements in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
