package:
  name: import_helpers
  version: "0.1.1"

source:
  fn: import_helpers-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/import_helpers/import_helpers-0.1.1.tar.gz
  md5: 2bdced78d0aa29fb9429e435a0f21f81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - import_helpers = import_helpers:main
    #
    # Would create an entry point called import_helpers that calls import_helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - import_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mturilin/import_helpers
  license: MIT
  summary: 'Python import helpers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
