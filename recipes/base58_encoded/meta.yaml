package:
  name: base58_encoded
  version: "0.1"

source:
  fn: base58_encoded-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/base58_encoded/base58_encoded-0.1.tar.gz
  md5: fd0cb5a8bd57c91b2b15b9f23396bbe2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - base58_encoded = base58_encoded:main
    #
    # Would create an entry point called base58_encoded that calls base58_encoded.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - base58_encoded

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/micahwalter/base58_encoded
  license: MIT
  summary: 'Encode an integer using base58. You know, for short urls.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
