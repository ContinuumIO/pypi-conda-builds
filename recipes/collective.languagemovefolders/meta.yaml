package:
  name: collective.languagemovefolders
  version: "1.1"

source:
  fn: collective.languagemovefolders-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.languagemovefolders/collective.languagemovefolders-1.1.zip
  md5: 95734926ffc8ddcc25e039b68e6a3682
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.languagemovefolders = collective.languagemovefolders:main
    #
    # Would create an entry point called collective.languagemovefolders that calls collective.languagemovefolders.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.linguaplone

  run:
    - python
    - setuptools
    - setuptools
    - products.linguaplone

test:
  # Python imports
  imports:
    - collective
    - collective.languagemovefolders
    - collective.languagemovefolders.browser
    - collective.languagemovefolders.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.milf
  license: gpl
  summary: 'Move Into Language Folder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
