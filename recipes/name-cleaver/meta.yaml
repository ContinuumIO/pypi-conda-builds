package:
  name: name-cleaver
  version: "0.6.0"

source:
  fn: name-cleaver-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/n/name-cleaver/name-cleaver-0.6.0.tar.gz
  md5: dd1951bb5426a4c2a5947b6d5c64c544
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - name-cleaver = name-cleaver:main
    #
    # Would create an entry point called name-cleaver that calls name-cleaver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - name_cleaver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/name-cleaver/
  license: BSD License
  summary: 'Name parser and formatter (for politicians, individuals, and organizations)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
