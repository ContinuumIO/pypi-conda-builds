package:
  name: djangorestframework-expiring-authtoken
  version: "0.1.1"

source:
  fn: djangorestframework-expiring-authtoken-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-expiring-authtoken/djangorestframework-expiring-authtoken-0.1.1.tar.gz
  md5: 6644316a4ce3ab1ccc37497c427770b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-expiring-authtoken = djangorestframework-expiring-authtoken:main
    #
    # Would create an entry point called djangorestframework-expiring-authtoken that calls djangorestframework-expiring-authtoken.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework >=3.1.1

  run:
    - python
    - djangorestframework >=3.1.1

test:
  # Python imports
  imports:
    - rest_framework_expiring_authtoken
    - rest_framework_expiring_authtoken.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JamesRitchie/django-rest-framework-expiring-tokens
  license: BSD License
  summary: 'Expiring Authentication Tokens for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
