package:
  name: django-ajax-loading-overlay
  version: "0.1.2"

source:
  fn: django-ajax-loading-overlay-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-loading-overlay/django-ajax-loading-overlay-0.1.2.tar.gz
  md5: f3fad4d019e75cb1b06a0788e1142c9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-loading-overlay = django-ajax-loading-overlay:main
    #
    # Would create an entry point called django-ajax-loading-overlay that calls django-ajax-loading-overlay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajax_loading_overlay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jgerigmeyer/django-ajax-loading-overlay/
  license: BSD License
  summary: 'Uses JS to add/remove a loading overlay during Ajax calls'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
