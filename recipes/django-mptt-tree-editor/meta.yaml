package:
  name: django-mptt-tree-editor
  version: "0.1.2"

source:
  fn: django-mptt-tree-editor-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mptt-tree-editor/django-mptt-tree-editor-0.1.2.tar.gz
  md5: 7ed085e9b1720866360e75e96756f29d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mptt-tree-editor = django-mptt-tree-editor:main
    #
    # Would create an entry point called django-mptt-tree-editor that calls django-mptt-tree-editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-mptt >=0.4

  run:
    - python
    - django >=1.4
    - django-mptt >=0.4

test:
  # Python imports
  imports:
    - mptt_tree_editor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/san/django-mptt-tree-editor
  license: BSD License
  summary: "Provides FeinCMS's mptt admin tree editor as a separate app"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
