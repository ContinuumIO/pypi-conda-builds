package:
  name: dirty-validators
  version: "0.2.2"

source:
  fn: dirty-validators-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dirty-validators/dirty-validators-0.2.2.tar.gz
  md5: 378b9fa57586c4e229d8e5852dba5078
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dirty-validators = dirty-validators:main
    #
    # Would create an entry point called dirty-validators that calls dirty-validators.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dirty_validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alfred82santa/dirty-validators
  license: UNKNOWN
  summary: 'Validate library for python 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
