package:
  name: dockman
  version: "0.4.1"

source:
  fn: dockman-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dockman/dockman-0.4.1.tar.gz
  md5: de6be88f051f3a2941dbcd58f9345894
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dockman = dockman:main
    #
    # Would create an entry point called dockman that calls dockman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - docker-py

  run:
    - python
    - flask
    - docker-py

test:
  # Python imports
  imports:
    - dockman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/TronPaul/dockman
  license: UNKNOWN
  summary: 'A webservice that runs docker builds/deployments on docker webhooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
