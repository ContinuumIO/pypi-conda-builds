package:
  name: smallsettings
  version: "0.2.2"

source:
  fn: SmallSettings-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SmallSettings/SmallSettings-0.2.2.tar.gz
  md5: aba72b0187343c391ebffa0f83b68a6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smallsettings = smallsettings:main
    #
    # Would create an entry point called smallsettings that calls smallsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smallsettings
    - smallsettings.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Simple settings functions for projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
