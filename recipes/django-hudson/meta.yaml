package:
  name: django-hudson
  version: "0.9.1"

source:
  fn: django-hudson-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hudson/django-hudson-0.9.1.tar.gz
  md5: e846818adf694a3b14990669b1d19a84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hudson = django-hudson:main
    #
    # Would create an entry point called django-hudson that calls django-hudson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - coverage >=3.4
    - pylint >=0.22

  run:
    - python
    - django >=1.2
    - coverage >=3.4
    - pylint >=0.22

test:
  # Python imports
  imports:
    - django_hudson
    - django_hudson.management
    - django_hudson.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kmmbvnr/django-hudson
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Plug and play continuous integration with django and hudson'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
