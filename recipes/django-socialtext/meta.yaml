package:
  name: django-socialtext
  version: "0.2.4"

source:
  fn: django-socialtext-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-socialtext/django-socialtext-0.2.4.tar.gz
  md5: 22febcee601bca6a4393bc432107df92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-socialtext = django-socialtext:main
    #
    # Would create an entry point called django-socialtext that calls django-socialtext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3.1
    - python-socialtext ==0.2.0

  run:
    - python
    - django ==1.3.1
    - python-socialtext ==0.2.0

test:
  # Python imports
  imports:
    - djsocialtext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Django application to integrate Socialtext into your project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
