package:
  name: llbasetools
  version: "0.1"

source:
  fn: llbasetools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/llbasetools/llbasetools-0.1.tar.gz
  md5: 7a4b71bb1926afcb8fc37c3303ee09bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - llbasetools = llbasetools:main
    #
    # Would create an entry point called llbasetools that calls llbasetools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - llbasetools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/nejucomo/llbasetools
  license: GNU General Public License (GPL)
  summary: 'Commandline llbase utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
