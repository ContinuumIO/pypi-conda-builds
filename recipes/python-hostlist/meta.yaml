package:
  name: python-hostlist
  version: "1.14"

source:
  fn: python-hostlist-1.14.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hostlist/python-hostlist-1.14.tar.gz
  md5: 464cd68b76e766555b794d89c50bc539
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hostlist = python-hostlist:main
    #
    # Would create an entry point called python-hostlist that calls python-hostlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nsc.liu.se/~kent/python-hostlist/
  license: GNU General Public License (GPL)
  summary: 'Python module for hostlist handling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
