package:
  name: django-freckle-budgets
  version: "0.2"

source:
  fn: django-freckle-budgets-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-freckle-budgets/django-freckle-budgets-0.2.tar.gz
  md5: 8ad2338bcd6eb01da63d39714e5af734
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-freckle-budgets = django-freckle-budgets:main
    #
    # Would create an entry point called django-freckle-budgets that calls django-freckle-budgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-libs
    - requests

  run:
    - python
    - django
    - django-libs
    - requests

test:
  # Python imports
  imports:
    - freckle_budgets
    - freckle_budgets.migrations
    - freckle_budgets.south_migrations
    - freckle_budgets.templatetags
    - freckle_budgets.tests
    - freckle_budgets.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-freckle-budgets
  license: The MIT License
  summary: 'A reusable Django app that allows to plan year budgets and track them via letsfreckle.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
