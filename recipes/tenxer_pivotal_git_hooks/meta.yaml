package:
  name: tenxer_pivotal_git_hooks
  version: "0.1.5"

source:
  fn: tenxer_pivotal_git_hooks-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/t/tenxer_pivotal_git_hooks/tenxer_pivotal_git_hooks-0.1.5.tar.gz
  md5: ae12ff134ed0e1b3c952b611a1603899
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tenxer_pivotal_git_hooks = tenxer_pivotal_git_hooks:main
    #
    # Would create an entry point called tenxer_pivotal_git_hooks that calls tenxer_pivotal_git_hooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - tenxer_pivotal_git_hooks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tenXer/tenxer_pivotal_git_hooks
  license: MIT
  summary: 'Automate Pivotal usage with git hooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
