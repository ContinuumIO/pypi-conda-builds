package:
  name: cpu_cores
  version: "0.1.3"

source:
  fn: cpu_cores-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cpu_cores/cpu_cores-0.1.3.tar.gz
  md5: 1f7d2b40e85eb8b5cafcd6849ec457b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cpu_cores = cpu_cores:main
    #
    # Would create an entry point called cpu_cores that calls cpu_cores.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cpu_cores

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thefab/cpu_cores
  license: MIT License
  summary: "cpu_cores-py is a small python library to get the number of'real physical' cpu cores of a linux/osx box"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
