package:
  name: django-meta
  version: "0.2.1"

source:
  fn: django-meta-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-meta/django-meta-0.2.1.tar.gz
  md5: 84f9e993c8f25eab6414b4ae1c4587f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-meta = django-meta:main
    #
    # Would create an entry point called django-meta that calls django-meta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - meta
    - meta.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/django-meta
  license: BSD License
  summary: 'Pluggable app for handling webpage meta tags and OpenGraph properties'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
