package:
  name: dache
  version: "0.0.4"

source:
  fn: dache-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dache/dache-0.0.4.tar.gz
  md5: 7a762e0beb2d53e3d12cd6ce35ed4d91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dache = dache:main
    #
    # Would create an entry point called dache that calls dache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.8.0,<1.9.0

  run:
    - python
    - six >=1.8.0,<1.9.0

test:
  # Python imports
  imports:
    - dache
    - dache.backends
    - dache.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eliangcs/dache
  license: BSD License
  summary: 'Unify API across various cache backends'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
