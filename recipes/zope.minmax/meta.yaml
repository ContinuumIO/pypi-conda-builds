package:
  name: zope.minmax
  version: "2.1.0"

source:
  fn: zope.minmax-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.minmax/zope.minmax-2.1.0.tar.gz
  md5: 30675d7af60a1ea7ef16349ebcd0516b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.minmax = zope.minmax:main
    #
    # Would create an entry point called zope.minmax that calls zope.minmax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - persistent
    - setuptools
    - zope.interface

  run:
    - python
    - persistent
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - zope
    - zope.minmax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.minmax/
  license: Zope Public License
  summary: 'Homogeneous values favoring maximum or minimum for ZODB conflict resolution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
