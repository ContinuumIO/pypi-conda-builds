package:
  name: flask-hmacauth
  version: "0.2"

source:
  fn: flask-hmacauth-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-hmacauth/flask-hmacauth-0.2.tar.gz
  md5: 1e988313aa91d3325bce7b5ac406338a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-hmacauth = flask-hmacauth:main
    #
    # Would create an entry point called flask-hmacauth that calls flask-hmacauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/Phillipmartin/flask-hmacauth
  license: MIT License
  summary: 'A module to simplify working with HMAC auth in Flask apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
