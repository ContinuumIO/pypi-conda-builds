package:
  name: plone.contentratings
  version: "1.1"

source:
  fn: plone.contentratings-1.1.zip
  url: https://pypi.python.org/packages/source/p/plone.contentratings/plone.contentratings-1.1.zip
  md5: f579a15b231ff94d4656c7821dbe9bc2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.contentratings = plone.contentratings:main
    #
    # Would create an entry point called plone.contentratings that calls plone.contentratings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.atcontenttypes
    - products.archetypes
    - archetypes.schemaextender
    - contentratings >=1.0
    - plone.app.controlpanel
    - rwproperty
    - zope.app.component
    - zope.app.form
    - zope.browserpage
    - zope.component

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.atcontenttypes
    - products.archetypes
    - archetypes.schemaextender
    - contentratings >=1.0
    - plone.app.controlpanel
    - rwproperty
    - zope.app.component
    - zope.app.form
    - zope.browserpage
    - zope.component

test:
  # Python imports
  imports:
    - plone
    - plone.contentratings
    - plone.contentratings.browser
    - plone.contentratings.exportimport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/plone.contentratings
  license: GNU General Public License (GPL)
  summary: 'Plone support for the contentratings package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
