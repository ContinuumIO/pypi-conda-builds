package:
  name: django-leaderboard
  version: "0.1.0"

source:
  fn: django-leaderboard-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-leaderboard/django-leaderboard-0.1.0.tar.gz
  md5: 53a02a0b854750be0758afa5335816cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-leaderboard = django-leaderboard:main
    #
    # Would create an entry point called django-leaderboard that calls django-leaderboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_leaderboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/laplacesdemon/django-leaderboard
  license: BSD licence, see LICENCE.txt
  summary: 'A Django leaderboard (scoreboard) app, using redis as its backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
