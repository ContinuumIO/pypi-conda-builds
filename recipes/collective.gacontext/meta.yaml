package:
  name: collective.gacontext
  version: "0.2.1"

source:
  fn: collective.gacontext-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.gacontext/collective.gacontext-0.2.1.tar.gz
  md5: 18e946d0d624a188446aab2b07b86c60
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.gacontext = collective.gacontext:main
    #
    # Would create an entry point called collective.gacontext that calls collective.gacontext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.gacontext
    - collective.gacontext.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.gacontext/trunk
  license: GPL
  summary: 'Context dependend GoogleAnalytics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
