package:
  name: django-translate
  version: "1.0.12"

source:
  fn: django-translate-1.0.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-translate/django-translate-1.0.12.tar.gz
  md5: 51908b683b341bb0b643122dc82328be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-translate = django-translate:main
    #
    # Would create an entry point called django-translate that calls django-translate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python_translate

  run:
    - python
    - python_translate

test:
  # Python imports
  imports:
    - django_translate
    - django_translate.extractors
    - django_translate.management
    - django_translate.management.commands
    - django_translate.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamziel/django_translate
  license: MIT
  summary: 'Non-gettext translations for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
