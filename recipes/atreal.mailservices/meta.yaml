package:
  name: atreal.mailservices
  version: "1.1.1"

source:
  fn: atreal.mailservices-1.1.1.zip
  url: https://pypi.python.org/packages/source/a/atreal.mailservices/atreal.mailservices-1.1.1.zip
  md5: 271741222af6fdb7f8003e581f40dad2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.mailservices = atreal.mailservices:main
    #
    # Would create an entry point called atreal.mailservices that calls atreal.mailservices.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - atreal
    - atreal.mailservices
    - atreal.mailservices.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.atreal.net/
  license: GPL
  summary: 'Send mails from Plone to users and groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
