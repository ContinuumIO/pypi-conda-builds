package:
  name: rhino
  version: "0.0.4"

source:
  fn: Rhino-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/R/Rhino/Rhino-0.0.4.tar.gz
  md5: ff412eec6cfe756934ee355068c80bca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rhino = rhino:main
    #
    # Would create an entry point called rhino that calls rhino.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rhino
    - rhino.ext
    - rhino.vendor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trendels/rhino
  license: MIT License
  summary: 'A microframework for building RESTful web services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
