package:
  name: sharder
  version: "0.0"

source:
  fn: sharder-0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sharder/sharder-0.0.tar.gz
  md5: b53c1f166d4a547e1ef35891b0c01e9e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sharder = sharder:main
    #
    # Would create an entry point called sharder that calls sharder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipaddr
    - pyramid
    - pyramid_debugtoolbar
    - pyramid_jinja2
    - pyramid_tm
    - retools
    - sqlalchemy
    - transaction
    - waitress
    - zope.sqlalchemy

  run:
    - python
    - setuptools
    - ipaddr
    - pyramid
    - pyramid_debugtoolbar
    - pyramid_jinja2
    - pyramid_tm
    - retools
    - sqlalchemy
    - transaction
    - waitress
    - zope.sqlalchemy

test:
  # Python imports
  imports:
    - sharder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Screenshot web page fragments and serve through wsgi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
