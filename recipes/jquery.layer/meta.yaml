package:
  name: jquery.layer
  version: "1.0.0"

source:
  fn: jquery.layer-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jquery.layer/jquery.layer-1.0.0.tar.gz
  md5: c749924042213b3b0d687c915f67e03d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jquery.layer = jquery.layer:main
    #
    # Would create an entry point called jquery.layer that calls jquery.layer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.publisher
    - zope.viewlet

  run:
    - python
    - setuptools
    - zope.publisher
    - zope.viewlet

test:
  # Python imports
  imports:
    - jquery
    - jquery.layer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/jquery.layer
  license: Zope Public License
  summary: 'Base layers for JQuery functionality'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
