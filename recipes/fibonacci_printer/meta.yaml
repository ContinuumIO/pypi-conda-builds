package:
  name: fibonacci_printer
  version: "1.1.0"

source:
  fn: Fibonacci_printer-1.1.0.zip
  url: https://pypi.python.org/packages/source/F/Fibonacci_printer/Fibonacci_printer-1.1.0.zip
  md5: 6e56024c2b31107876453d9ebba6ed7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fibonacci_printer = fibonacci_printer:main
    #
    # Would create an entry point called fibonacci_printer that calls fibonacci_printer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fibonacci_printer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.google.com
  license: UNKNOWN
  summary: 'Prints fibonacci sequences upto a certain number of terms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
