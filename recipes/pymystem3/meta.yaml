package:
  name: pymystem3
  version: "0.1.5"

source:
  fn: pymystem3-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pymystem3/pymystem3-0.1.5.tar.gz
  md5: 2d9cecd5c8d716fe85741068f8a5300d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pymystem3 = pymystem3:main
    #
    # Would create an entry point called pymystem3 that calls pymystem3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - pymystem3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Digsolab/pymystem3
  license: MIT License
  summary: 'Python wrapper for the Yandex MyStem 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
