package:
  name: pymultimethods
  version: "0.2"

source:
  fn: PyMultimethods-0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/PyMultimethods/PyMultimethods-0.2.tar.gz
  md5: aa3baf076a1a2ee6d2954e176fd21002
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pymultimethods = pymultimethods:main
    #
    # Would create an entry point called pymultimethods that calls pymultimethods.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pymultimethods

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/pymultimethods
  license: GNU Affero General Public License v3
  summary: 'PyMultimethods: Multimethods for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
