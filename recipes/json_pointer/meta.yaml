package:
  name: json_pointer
  version: "0.1.2"

source:
  fn: json_pointer-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/json_pointer/json_pointer-0.1.2.tar.gz
  md5: a52e24e38a6439ccb8eef117f9e9d9d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_pointer = json_pointer:main
    #
    # Would create an entry point called json_pointer that calls json_pointer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - json_pointer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Copyright Sanoma Media
  summary: 'Simple implementation of the json-pointer spec'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
