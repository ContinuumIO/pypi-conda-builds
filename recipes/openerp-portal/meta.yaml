package:
  name: openerp-portal
  version: "7.0.406"

source:
  fn: openerp-portal-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-portal/openerp-portal-7.0.406.tar.gz
  md5: c9161d36602cd02119a36fa25a2c51e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-portal = openerp-portal:main
    #
    # Would create an entry point called openerp-portal that calls openerp-portal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-core
    - openerp-share
    - openerp-auth-signup

  run:
    - python
    - openerp-core
    - openerp-share
    - openerp-auth-signup

test:
  # Python imports
  imports:
    - openerp.addons.portal
    - openerp.addons.portal.tests
    - openerp.addons.portal.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openerp.com
  license: GNU Affero General Public License v3
  summary: 'Portal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
