package:
  name: siteglass
  version: "0.2"

source:
  fn: siteglass-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/siteglass/siteglass-0.2.tar.gz
  md5: 5ee0a669b474f213dd72ccad3e41669b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - siteglass = siteglass:main
    #
    # Would create an entry point called siteglass that calls siteglass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssmin

  run:
    - python
    - cssmin

test:
  # Python imports
  imports:
    - siteglass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cooper-software/siteglass
  license: MIT License
  summary: 'A flexible tool for merging and compressing web assets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
