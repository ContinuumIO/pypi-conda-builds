package:
  name: django-tornado
  version: "1.1.6"

source:
  fn: django-tornado-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tornado/django-tornado-1.1.6.tar.gz
  md5: bb0bc85696b60445c2caa5192e537a4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tornado = django-tornado:main
    #
    # Would create an entry point called django-tornado that calls django-tornado.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - django

  run:
    - python
    - tornado
    - django

test:
  # Python imports
  imports:
    - django_tornado
    - django_tornado.management
    - django_tornado.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Django/Tornado integration made easy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
