package:
  name: sqlalchemy-gevent
  version: "0.1"

source:
  fn: sqlalchemy_gevent-0.1.zip
  url: https://github.com/hkwi/sqlalchemy_gevent/archive/sqlalchemy_gevent-0.1.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-gevent = sqlalchemy-gevent:main
    #
    # Would create an entry point called sqlalchemy-gevent that calls sqlalchemy-gevent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - gevent >=1.0

  run:
    - python
    - setuptools
    - sqlalchemy
    - gevent >=1.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hkwi/sqlalchemy_gevent
  license: MIT
  summary: 'sqlalchemy dialect adaptor for gevent to work in non-blocking mode'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
