package:
  name: bratabase-social-auth-backend
  version: "0.1.2"

source:
  fn: bratabase-social-auth-backend-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bratabase-social-auth-backend/bratabase-social-auth-backend-0.1.2.tar.gz
  md5: 1923de1f7af88ff640a12b8994814b16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bratabase-social-auth-backend = bratabase-social-auth-backend:main
    #
    # Would create an entry point called bratabase-social-auth-backend that calls bratabase-social-auth-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bratabase_social_auth
    - bratabase_social_auth.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bratabase/bratabase-social-auth-backend
  license: BSD License
  summary: 'OAuth2 Backend for Bratabase.com to be used with Django-social-auth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
