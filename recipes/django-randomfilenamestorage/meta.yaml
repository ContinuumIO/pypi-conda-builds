package:
  name: django-randomfilenamestorage
  version: "1.1"

source:
  fn: django-randomfilenamestorage-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-randomfilenamestorage/django-randomfilenamestorage-1.1.tar.gz
  md5: 28b95e20eeb8b9f13e849b99322c739e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-randomfilenamestorage = django-randomfilenamestorage:main
    #
    # Would create an entry point called django-randomfilenamestorage that calls django-randomfilenamestorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - django_randomfilenamestorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/akoha/django-randomfilenamestorage/
  license: MIT License
  summary: 'A Django storage backend that gives random names to files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
