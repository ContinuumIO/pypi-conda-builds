package:
  name: django-powerdns-dnssec
  version: "0.9.3"

source:
  fn: django-powerdns-dnssec-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-powerdns-dnssec/django-powerdns-dnssec-0.9.3.tar.gz
  md5: 961460fe945de7c9d8b1b58c1fb1e9df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-powerdns-dnssec = django-powerdns-dnssec:main
    #
    # Would create an entry point called django-powerdns-dnssec that calls django-powerdns-dnssec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.4
    - ipaddr >=2.1.7
    - lck.django >=0.8.0

  run:
    - python
    - django >=1.3.4
    - ipaddr >=2.1.7
    - lck.django >=0.8.0

test:
  # Python imports
  imports:
    - powerdns
    - powerdns.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ambv/django-powerdns/
  license: BSD License
  summary: 'PowerDNS administration app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
