package:
  name: products.simplegroupsmanagement
  version: "0.4.1"

source:
  fn: Products.SimpleGroupsManagement-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SimpleGroupsManagement/Products.SimpleGroupsManagement-0.4.1.tar.gz
  md5: 953f1634e3abac8a514b3e993f3fd4c0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.simplegroupsmanagement = products.simplegroupsmanagement:main
    #
    # Would create an entry point called products.simplegroupsmanagement that calls products.simplegroupsmanagement.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.SimpleGroupsManagement
    - Products.SimpleGroupsManagement.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/simplegroupsmanagement
  license: GPL
  summary: 'A Plone utility that let non-Manager users able to manage some (specific) groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
