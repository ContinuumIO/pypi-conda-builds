package:
  name: zc.icp
  version: "1.0.0"

source:
  fn: zc.icp-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.icp/zc.icp-1.0.0.tar.gz
  md5: 6cc5ace77b4e6463706dacf3c3d84ced
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.icp = zc.icp:main
    #
    # Would create an entry point called zc.icp that calls zc.icp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.testing

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.testing

test:
  # Python imports
  imports:
    - zc
    - zc.icp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: svn+ssh://svn.zope.com/repos/main/zc.icp
  license: Zope Public License
  summary: 'Small, pluggable ICP (Internet Cache Protocol) server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
