package:
  name: montecarlo
  version: "0.1.17"

source:
  fn: montecarlo-0.1.17.tar.gz
  url: https://pypi.python.org/packages/source/m/montecarlo/montecarlo-0.1.17.tar.gz
  md5: 238ef31a8b0aac89669585e764052935
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - montecarlo = montecarlo:main
    #
    # Would create an entry point called montecarlo that calls montecarlo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - montecarlo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/csu/pymontecarlo
  license: UNKNOWN
  summary: 'A small Python library for creating simple Monte Carlo simulations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
