package:
  name: collective.contentlicensing
  version: "2.2.6"

source:
  fn: collective.contentlicensing-2.2.6.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentlicensing/collective.contentlicensing-2.2.6.tar.gz
  md5: 1f27a4f2aebeccb7d6df86628fe018e8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentlicensing = collective.contentlicensing:main
    #
    # Would create an entry point called collective.contentlicensing that calls collective.contentlicensing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentlicensing
    - collective.contentlicensing.DublinCoreExtensions
    - collective.contentlicensing.Extensions
    - collective.contentlicensing.browser
    - collective.contentlicensing.tests
    - collective.contentlicensing.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/contentlicensing
  license: GPL
  summary: 'This tool is used to manage copyright licenses within plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
