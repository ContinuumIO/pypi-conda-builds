package:
  name: django-mapped-fields
  version: "0.0.4"

source:
  fn: django-mapped-fields-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mapped-fields/django-mapped-fields-0.0.4.tar.gz
  md5: 98d6d62f2ba2b57d4357abc4d3ced488
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mapped-fields = django-mapped-fields:main
    #
    # Would create an entry point called django-mapped-fields that calls django-mapped-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mapped_fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mypebble/mapped-fields
  license: UNKNOWN
  summary: 'Form fields to make it easier to map CSV or JSON files to your own models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
