package:
  name: django_w3c
  version: "0.1.0"

source:
  fn: django_w3c-v0.1.0.tar.gz
  url: https://bitbucket.org/nmb10/django_w3c/downloads/django_w3c-v0.1.0.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_w3c = django_w3c:main
    #
    # Would create an entry point called django_w3c that calls django_w3c.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - py_w3c

  run:
    - python
    - py_w3c

test:
  # Python imports
  imports:
    - django_w3c
    - django_w3c.management
    - django_w3c.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django_w3c/
  license: BSD License
  summary: 'Django application to validate html using W3C validator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
