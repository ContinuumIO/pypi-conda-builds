package:
  name: django-file-resubmit
  version: "0.4.2"

source:
  fn: django-file-resubmit-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-file-resubmit/django-file-resubmit-0.4.2.tar.gz
  md5: 0d044b82c4f7164b6b63355c0825e98f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-file-resubmit = django-file-resubmit:main
    #
    # Would create an entry point called django-file-resubmit that calls django-file-resubmit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - file_resubmit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un1t/django-file-resubmit
  license: MIT License
  summary: 'Keeps submited files when validation errors occur.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
