package:
  name: django-nonrel-enuff
  version: "0.4"

source:
  fn: django-nonrel-enuff-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nonrel-enuff/django-nonrel-enuff-0.4.tar.gz
  md5: 8b032062379b64140e0554323e585df1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nonrel-enuff = django-nonrel-enuff:main
    #
    # Would create an entry point called django-nonrel-enuff that calls django-nonrel-enuff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - enuff
    - enuff.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yeago/django-nonrel-enuff
  license: UNKNOWN
  summary: 'Crutch for SQL users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
