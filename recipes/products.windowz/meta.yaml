package:
  name: products.windowz
  version: "1.5"

source:
  fn: Products.windowZ-1.5.zip
  url: https://pypi.python.org/packages/source/P/Products.windowZ/Products.windowZ-1.5.zip
  md5: 0064d14e1df263309cb2e74b2accabec
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.windowz = products.windowz:main
    #
    # Would create an entry point called products.windowz that calls products.windowz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.controlpanel
    - plone.fieldsets
    - products.archetypes
    - products.atcontenttypes
    - products.cmfcore
    - products.genericsetup
    - setuptools
    - stripogram
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - plone.app.controlpanel
    - plone.fieldsets
    - products.archetypes
    - products.atcontenttypes
    - products.cmfcore
    - products.genericsetup
    - setuptools
    - stripogram
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - Products
    - Products.windowZ
    - Products.windowZ.content
    - Products.windowZ.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/windowz
  license: GNU General Public License (GPL)
  summary: 'Show web-pages inside an iframe. Plone content-type.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
