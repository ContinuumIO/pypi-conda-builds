package:
  name: astkit
  version: "0.5.4"

source:
  fn: astkit-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/a/astkit/astkit-0.5.4.tar.gz
  md5: 69c7d9c87c6359ceeb1582b5145a1077
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astkit = astkit:main
    #
    # Would create an entry point called astkit that calls astkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - astkit
    - astkit.compat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/desmaj/astkit
  license: GPL
  summary: 'A collection of tools and utilities built on the ast (python >=  2.6) module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
