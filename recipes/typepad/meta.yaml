package:
  name: typepad
  version: "2.0"

source:
  fn: typepad-2.0.tar.gz
  url: https://pypi.python.org/packages/source/t/typepad/typepad-2.0.tar.gz
  md5: 18c8b558bb04a22c27bc4124df1ba4ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typepad = typepad:main
    #
    # Would create an entry point called typepad that calls typepad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth >=1.0.1
    - remoteobjects ==1.1.1
    - batchhttp ==1.1.1

  run:
    - python
    - oauth >=1.0.1
    - remoteobjects ==1.1.1
    - batchhttp ==1.1.1

test:
  # Python imports
  imports:
    - typepad

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sixapart/python-typepad-api
  license: BSD License
  summary: 'TypePad API SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
