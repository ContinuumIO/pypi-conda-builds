package:
  name: daytime
  version: "0.4"

source:
  fn: daytime-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/daytime/daytime-0.4.tar.gz
  md5: 1783cdc3da778c42aa8a6ea358ee1b7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daytime = daytime:main
    #
    # Would create an entry point called daytime that calls daytime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - daytime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thomst/daytime
  license: GNU General Public License (GPL)
  summary: 'Extension for datetime.time with the main focus on comarison an making sums.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
