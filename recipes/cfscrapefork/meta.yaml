package:
  name: cfscrapefork
  version: "1.0.4"

source:
  fn: cfscrapefork-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cfscrapefork/cfscrapefork-1.0.4.tar.gz
  md5: af0e85789113c2ee7b40fb2e6b7ec9a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cfscrapefork = cfscrapefork:main
    #
    # Would create an entry point called cfscrapefork that calls cfscrapefork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyexecjs >=1.1.0

  run:
    - python
    - pyexecjs >=1.1.0

test:
  # Python imports
  imports:
    - cfscrape
    - cfscrapefork

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/StevenVeshkini/cloudflare-scrape
  license: UNKNOWN
  summary: 'cfscrapefork'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
