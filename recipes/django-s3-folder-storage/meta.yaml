package:
  name: django-s3-folder-storage
  version: "0.2"

source:
  fn: django-s3-folder-storage-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-s3-folder-storage/django-s3-folder-storage-0.2.tar.gz
  md5: bea121f6170d0f200d1f49279a7ff0a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-s3-folder-storage = django-s3-folder-storage:main
    #
    # Would create an entry point called django-s3-folder-storage that calls django-s3-folder-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-storages
    - boto

  run:
    - python
    - django-storages
    - boto

test:
  # Python imports
  imports:
    - s3_folder_storage
    - s3_folder_storage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jamstooks/django-s3-folder-storage
  license: BSD License
  summary: "Quick extension of django-storages' S3BotoStorage to allow separate folders for uploaded and static media within an S3 bucket."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
