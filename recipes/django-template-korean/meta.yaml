package:
  name: django-template-korean
  version: "0.1.0"

source:
  fn: django-template-korean-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-korean/django-template-korean-0.1.0.tar.gz
  md5: 0c48cff34d9426fc88197e816cab17b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-korean = django-template-korean:main
    #
    # Would create an entry point called django-template-korean that calls django-template-korean.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - korean >=0.1.4

  run:
    - python
    - korean >=0.1.4

test:
  # Python imports
  imports:
    - template_korean
    - template_korean.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lqez/django-template-korean
  license: MIT License
  summary: 'Django template filter for Korean via korean module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
