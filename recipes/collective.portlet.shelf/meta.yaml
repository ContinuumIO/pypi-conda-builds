package:
  name: collective.portlet.shelf
  version: "1.3"

source:
  fn: collective.portlet.shelf-1.3.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.shelf/collective.portlet.shelf-1.3.zip
  md5: 91b0950b62c38a72b5c3a77bab853a0b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.shelf = collective.portlet.shelf:main
    #
    # Would create an entry point called collective.portlet.shelf that calls collective.portlet.shelf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.shelf
    - collective.portlet.shelf.browser
    - collective.portlet.shelf.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.portlet.shelf
  license: GNU General Public License (GPL)
  summary: "collective.portlet.shelf is a portlet for Plone 4 that allows to display Collection's results in a carousel portlet browsing them by subcollections."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
