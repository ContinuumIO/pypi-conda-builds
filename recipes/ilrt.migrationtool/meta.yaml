package:
  name: ilrt.migrationtool
  version: "0.4"

source:
  fn: ilrt.migrationtool-0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/ilrt.migrationtool/ilrt.migrationtool-0.4.tar.gz
  md5: 779d890b2d300427796b15c26cca9f0e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ilrt.migrationtool = ilrt.migrationtool:main
    #
    # Would create an entry point called ilrt.migrationtool that calls ilrt.migrationtool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ilrt
    - ilrt.migrationtool
    - ilrt.migrationtool.browser
    - ilrt.migrationtool.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.ilrt.bris.ac.uk/repos/pypi/ilrt.migrationtool
  license: BSD License
  summary: "A site migration tool for Plone that uses the site's theme egg version releases"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
