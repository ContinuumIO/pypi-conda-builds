package:
  name: bonzo
  version: "0.1.2"

source:
  fn: bonzo-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bonzo/bonzo-0.1.2.tar.gz
  md5: 8437f6a92115a64c90af6a12f386fbc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bonzo = bonzo:main
    #
    # Would create an entry point called bonzo that calls bonzo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.0

  run:
    - python
    - tornado >=3.0

test:
  # Python imports
  imports:
    - bonzo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/puentesarrin/bonzo
  license: Apache Software License
  summary: 'Bonzo is a minimalistic SMTP Proxy built on top of Tornado.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
