package:
  name: django-static-sitemaps
  version: "2.1.7"

source:
  fn: django-static-sitemaps-2.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-static-sitemaps/django-static-sitemaps-2.1.7.tar.gz
  md5: 62d7de3c9dde8f5a1b03ee404249eeea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-static-sitemaps = django-static-sitemaps:main
    #
    # Would create an entry point called django-static-sitemaps that calls django-static-sitemaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - django

  run:
    - python
    - setuptools >=0.6b1
    - django

test:
  # Python imports
  imports:
    - static_sitemaps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/xaralis/django-static-sitemaps
  license: BSD License
  summary: 'Tool for generating sitemaps as static files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
