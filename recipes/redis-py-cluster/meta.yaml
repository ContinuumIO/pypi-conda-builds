package:
  name: redis-py-cluster
  version: "0.2.0"

source:
  fn: redis-py-cluster-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-py-cluster/redis-py-cluster-0.2.0.tar.gz
  md5: 0546363d39aafc18271c628c2765a595
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-py-cluster = redis-py-cluster:main
    #
    # Would create an entry point called redis-py-cluster that calls redis-py-cluster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.10.2

  run:
    - python
    - redis >=2.10.2

test:
  # Python imports
  imports:
    - rediscluster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/grokzen/redis-py-cluster
  license: MIT
  summary: 'Cluster library for redis 3.0.0 built on top of redis-py lib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
