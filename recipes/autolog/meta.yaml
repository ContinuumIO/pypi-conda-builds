package:
  name: autolog
  version: "0.1.3"

source:
  fn: autolog-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/autolog/autolog-0.1.3.tar.gz
  md5: f900d1e7582a38b04d69e45b3a7ebcfa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autolog = autolog:main
    #
    # Would create an entry point called autolog that calls autolog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - forked-path

  run:
    - python
    - forked-path

test:
  # Python imports
  imports:
    - autolog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://noble.gs.washington.edu/~mmh1/software/autolog/
  license: GNU General Public License (GPL)
  summary: 'quick and easy logging setup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
