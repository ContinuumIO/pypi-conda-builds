package:
  name: plone.keyring
  version: "3.0.0"

source:
  fn: plone.keyring-3.0.0.zip
  url: https://pypi.python.org/packages/source/p/plone.keyring/plone.keyring-3.0.0.zip
  md5: 54674e073ce0b3191602adb541f06f86
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.keyring = plone.keyring:main
    #
    # Would create an entry point called plone.keyring that calls plone.keyring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.container
    - zope.interface
    - zope.location

  run:
    - python
    - setuptools
    - zodb3
    - zope.container
    - zope.interface
    - zope.location

test:
  # Python imports
  imports:
    - plone
    - plone.keyring
    - plone.keyring.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.keyring
  license: GNU General Public License (GPL)
  summary: 'Manage secrets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
