package:
  name: flask-imgur
  version: "0.1"

source:
  fn: Flask-Imgur-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Imgur/Flask-Imgur-0.1.tar.gz
  md5: 42eb1916208f4505444c8345069c6c46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-imgur = flask-imgur:main
    #
    # Would create an entry point called flask-imgur that calls flask-imgur.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - six

  run:
    - python
    - flask
    - six

test:
  # Python imports
  imports:
    - flask_imgur

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/exaroth/flask-imgur
  license: BSD
  summary: 'Upload images straight to Imgur in your Flask app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
