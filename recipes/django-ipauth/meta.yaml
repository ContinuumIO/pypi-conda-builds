package:
  name: django-ipauth
  version: "0.4.1"

source:
  fn: django-ipauth-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ipauth/django-ipauth-0.4.1.tar.gz
  md5: af025854db130da6cd1e0903f56c1c6a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ipauth = django-ipauth:main
    #
    # Would create an entry point called django-ipauth that calls django-ipauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ipauth
    - ipauth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.arch.tamu.edu
  license: UNKNOWN
  summary: 'IP Authentication for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
