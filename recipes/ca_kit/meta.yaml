package:
  name: ca_kit
  version: "0.2.1"

source:
  fn: ca_kit-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/ca_kit/ca_kit-0.2.1.tar.gz
  md5: 9d05f19edcf575e190e802bbb000cc93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ca_kit = ca_kit:main
    #
    # Would create an entry point called ca_kit that calls ca_kit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m2crypto ==0.22.3
    - wsgiref ==0.1.2

  run:
    - python
    - m2crypto ==0.22.3
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - ca_kit
    - ck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dsoprea/CaKit
  license: GPL 2
  summary: 'Certificate-authority test harness.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
