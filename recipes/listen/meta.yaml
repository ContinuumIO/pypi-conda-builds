package:
  name: listen
  version: "0.1.5"

source:
  fn: listen-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/l/listen/listen-0.1.5.tar.gz
  md5: 4be083564a069adb6b569c29a6a15547
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - listen = listen:main
    #
    # Would create an entry point called listen that calls listen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - listen
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jalli/listen
  license: MIT License
  summary: 'Simple but powerful signal handling to process OS signals in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
