package:
  name: magic_square
  version: "0.2"

source:
  fn: magic_square-0.2.tar.bz2
  url: https://pypi.python.org/packages/source/m/magic_square/magic_square-0.2.tar.bz2
  md5: e4dbb5331dbe611b0b0b114e74058545
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magic_square = magic_square:main
    #
    # Would create an entry point called magic_square that calls magic_square.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - magic_square

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mihailovs.com/Alec/
  license: MIT License
  summary: 'Simple operations with magic squares.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
