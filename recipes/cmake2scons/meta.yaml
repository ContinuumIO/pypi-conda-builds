package:
  name: cmake2scons
  version: "1.1"

source:
  fn: CMake2SCons-1.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CMake2SCons/CMake2SCons-1.1.tar.gz
  md5: 6baa442e0c993c122cf4c4d80a380c34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmake2scons = cmake2scons:main
    #
    # Would create an entry point called cmake2scons that calls cmake2scons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing

  run:
    - python
    - pyparsing

test:
  # Python imports
  imports:
    - cmake2scons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dirkbaechle/cmake2scons/
  license: MIT License
  summary: 'Converts CMake build descriptions for a complete project to SCons scripts (SConstruct/SConscript)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
