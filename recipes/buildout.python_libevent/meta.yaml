package:
  name: buildout.python_libevent
  version: "0.9.1"

source:
  fn: buildout.python_libevent-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.python_libevent/buildout.python_libevent-0.9.1.tar.gz
  md5: 74c5334c964ebac4ec5e8fda7f28cd69
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.python_libevent = buildout.python_libevent:main
    #
    # Would create an entry point called buildout.python_libevent that calls buildout.python_libevent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - buildout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.joachim-bauch.de/projects/python-libevent/
  license: UNKNOWN
  summary: 'Buildout recipe for python-libevent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
