package:
  name: brisk
  version: "0.1"

source:
  fn: brisk-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brisk/brisk-0.1.tar.gz
  md5: 872a7d6724a732dc437e85b6e356ee88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brisk = brisk:main
    #
    # Would create an entry point called brisk that calls brisk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numba >=0.17.0
    - numpy >=1.9.1

  run:
    - python
    - numba >=0.17.0
    - numpy >=1.9.1

test:
  # Python imports
  imports:
    - brisk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/David-OConnor/brisk
  license: Apache
  summary: 'Fast implementation of numerical functions using Numba'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
