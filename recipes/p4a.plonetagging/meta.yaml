package:
  name: p4a.plonetagging
  version: "0.1dev-r1550"

source:
  fn: p4a.plonetagging-0.1dev-r1550.tar.gz
  url: https://pypi.python.org/packages/source/p/p4a.plonetagging/p4a.plonetagging-0.1dev-r1550.tar.gz
  md5: e1d20c8a0c2da402c44e34141ff5d6de
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.plonetagging = p4a.plonetagging:main
    #
    # Would create an entry point called p4a.plonetagging that calls p4a.plonetagging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - p4a
    - p4a.plonetagging
    - p4a.plonetagging.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Plone4Artists tagging add-on for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
