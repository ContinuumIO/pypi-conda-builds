package:
  name: sqlalchemy_mptt
  version: "0.1.2"

source:
  fn: sqlalchemy_mptt-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy_mptt/sqlalchemy_mptt-0.1.2.tar.gz
  md5: e0b5bd36bb30eb9f4546c36d30249c0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy_mptt = sqlalchemy_mptt:main
    #
    # Would create an entry point called sqlalchemy_mptt that calls sqlalchemy_mptt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7

  run:
    - python
    - sqlalchemy >=0.7

test:
  # Python imports
  imports:
    - sqlalchemy_mptt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ITCase/sqlalchemy_mptt/
  license: MIT License
  summary: 'SQLAlchemy MPTT mixins (Nested Sets)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
