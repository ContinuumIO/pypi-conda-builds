package:
  name: simplecurry
  version: "1.12"

source:
  fn: simplecurry-1.12.tar.gz
  url: https://pypi.python.org/packages/source/s/simplecurry/simplecurry-1.12.tar.gz
  md5: 14891c51c431f9ce363f44cb239a6aa7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplecurry = simplecurry:main
    #
    # Would create an entry point called simplecurry that calls simplecurry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplecurry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/grimborg/simplecurry
  license: http://www.gnu.org/licenses/gpl-2.0.html
  summary: 'Curried decorator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
