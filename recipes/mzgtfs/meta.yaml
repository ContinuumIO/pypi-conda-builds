package:
  name: mzgtfs
  version: "0.9b1"

source:
  fn: mzgtfs-0.9b1.tar.gz
  url: https://pypi.python.org/packages/source/m/mzgtfs/mzgtfs-0.9b1.tar.gz
  md5: efa5a2d7907aaab806cb742e15db45a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mzgtfs = mzgtfs:main
    #
    # Would create an entry point called mzgtfs that calls mzgtfs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unicodecsv
    - mzgeohash

  run:
    - python
    - unicodecsv
    - mzgeohash

test:
  # Python imports
  imports:
    - mzgtfs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/transitland/mapzen-gtfs
  license: License :: OSI Approved :: MIT License
  summary: 'Mapzen GTFS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
