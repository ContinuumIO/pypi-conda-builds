package:
  name: bottle-cassandra-driver
  version: "0.0.2"

source:
  fn: bottle-cassandra-driver-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-cassandra-driver/bottle-cassandra-driver-0.0.2.tar.gz
  md5: 779e850d67f6ddbb75af32316fcb0a6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-cassandra-driver = bottle-cassandra-driver:main
    #
    # Would create an entry point called bottle-cassandra-driver that calls bottle-cassandra-driver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - bottle >=0.9
    - cassandra-driver

  run:
    - python
    - distribute
    - bottle >=0.9
    - cassandra-driver

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jeffjirsa/bottle-cassandra-driver/
  license: MIT License
  summary: 'Cassandra Driver plugin for Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
