package:
  name: django-tracking2
  version: "0.2.3"

source:
  fn: django-tracking2-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tracking2/django-tracking2-0.2.3.tar.gz
  md5: d59ccedf51a2bab21dd8cc5700a9a91f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tracking2 = django-tracking2:main
    #
    # Would create an entry point called django-tracking2 that calls django-tracking2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.8

  run:
    - python
    - django >=1.4,<1.8

test:
  # Python imports
  imports:
    - tracking
    - tracking.migrations
    - tracking.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bruth/django-tracking2
  license: BSD License
  summary: 'django-tracking2 tracks the length of time visitors and registered users spend on your site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
