package:
  name: megrok.resource
  version: "0.5"

source:
  fn: megrok.resource-0.5.zip
  url: https://pypi.python.org/packages/source/m/megrok.resource/megrok.resource-0.5.zip
  md5: 2062d2d761e3c72b838c7e9ac64ff765
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.resource = megrok.resource:main
    #
    # Would create an entry point called megrok.resource that calls megrok.resource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - martian
    - setuptools
    - grokcore.component
    - grokcore.view
    - hurry.resource >=0.4.1
    - hurry.zoperesource >=0.5
    - z3c.hashedresource
    - zope.site
    - zope.app.publication
    - zope.component
    - zope.interface
    - zope.security
    - zope.traversing

  run:
    - python
    - martian
    - setuptools
    - grokcore.component
    - grokcore.view
    - hurry.resource >=0.4.1
    - hurry.zoperesource >=0.5
    - z3c.hashedresource
    - zope.site
    - zope.app.publication
    - zope.component
    - zope.interface
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - megrok
    - megrok.resource
    - megrok.resource.ftests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/megrok.resource
  license: Zope Public License
  summary: 'Grok Resources based on hurry.resource'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
