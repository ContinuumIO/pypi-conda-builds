package:
  name: collective.js.mousewheel
  version: "1.0"

source:
  fn: collective.js.mousewheel-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.mousewheel/collective.js.mousewheel-1.0.tar.gz
  md5: a2620198b9fac224c14aa6b940e211a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.mousewheel = collective.js.mousewheel:main
    #
    # Would create an entry point called collective.js.mousewheel that calls collective.js.mousewheel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.mousewheel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.mousewheel
  license: GPL
  summary: 'Add JQuery Mousewheel plugin to the Plone CMS resource registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
