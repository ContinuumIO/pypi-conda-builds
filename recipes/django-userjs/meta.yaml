package:
  name: django-userjs
  version: "0.2"

source:
  fn: django-userjs-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-userjs/django-userjs-0.2.tar.gz
  md5: ea0052745566c1b1137933bd01f78062
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-userjs = django-userjs:main
    #
    # Would create an entry point called django-userjs that calls django-userjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - userjs
    - userjs.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tweekmonster/django-userjs
  license: BSD License
  summary: 'userjs is a Django application that produces javascript that represents a User\nobject.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
