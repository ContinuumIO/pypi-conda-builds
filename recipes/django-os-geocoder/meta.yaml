package:
  name: django-os-geocoder
  version: "0.1.6"

source:
  fn: django-os-geocoder-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-os-geocoder/django-os-geocoder-0.1.6.tar.gz
  md5: 3fbb26f7c4e4f3c09d01b3ff3b31b97b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-os-geocoder = django-os-geocoder:main
    #
    # Would create an entry point called django-os-geocoder that calls django-os-geocoder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south
    - progressbar

  run:
    - python
    - south
    - progressbar

test:
  # Python imports
  imports:
    - geocoder
    - geocoder.management
    - geocoder.management.commands
    - geocoder.migrations
    - geocoder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mysociety/django-os-geocoder
  license: UNKNOWN
  summary: 'A Django app that lets you load Ordnance Survey data and use it to find places.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
