package:
  name: envialia
  version: "0.0.3"

source:
  fn: envialia-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/envialia/envialia-0.0.3.tar.gz
  md5: 4395ebb18e15c02ad8f004f219a53805
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - envialia = envialia:main
    #
    # Would create an entry point called envialia that calls envialia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - envialia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.zikzakmedia.com
  license: GNU Affero General Public License v3
  summary: 'Python API Envialia carrier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
