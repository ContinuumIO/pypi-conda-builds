package:
  name: django-seatbelt
  version: "1.0.1"

source:
  fn: django-seatbelt-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-seatbelt/django-seatbelt-1.0.1.tar.gz
  md5: 008002eb973a5c165bc27b54e3b86751
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-seatbelt = django-seatbelt:main
    #
    # Would create an entry point called django-seatbelt that calls django-seatbelt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_seatbelt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zyga/django-seatbelt
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Simple sandbox that isolates production applications from things users stick in /usr/local/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
