package:
  name: tangled.sqlalchemy
  version: "0.1a3"

source:
  fn: tangled.sqlalchemy-0.1a3.tar.gz
  url: https://pypi.python.org/packages/source/t/tangled.sqlalchemy/tangled.sqlalchemy-0.1a3.tar.gz
  md5: 2e647c9b346ccc963354f8164e3cf1e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tangled.sqlalchemy = tangled.sqlalchemy:main
    #
    # Would create an entry point called tangled.sqlalchemy that calls tangled.sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tangled >=0.1a7
    - sqlalchemy

  run:
    - python
    - tangled >=0.1a7
    - sqlalchemy

test:
  # Python imports
  imports:
    - tangled
    - tangled.sqlalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tangledframework.org/
  license: MIT License
  summary: 'Tangled SQLAlchemy integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
