package:
  name: ricerca
  version: "1.1.3"

source:
  fn: ricerca-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/r/ricerca/ricerca-1.1.3.tar.gz
  md5: 889071dd4494bcefd4580e687e3a374a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ricerca = ricerca:main
    #
    # Would create an entry point called ricerca that calls ricerca.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.4.1
    - scipy >=0.7.2
    - numexpr >=2.3
    - cython >=0.20
    - tables >=3.1.0

  run:
    - python
    - numpy >=1.4.1
    - scipy >=0.7.2
    - numexpr >=2.3
    - cython >=0.20
    - tables >=3.1.0

test:
  # Python imports
  imports:
    - ricerca

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://murphylab.web.cmu.edu/software/ricerca
  license: GNU General Public License (GPL)
  summary: 'Content-based search based using FALCON: Feedback Adaptive Loop for Content-Based Retrieval'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
