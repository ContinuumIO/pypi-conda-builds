package:
  name: osprofiler
  version: "0.3.0"

source:
  fn: osprofiler-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/o/osprofiler/osprofiler-0.3.0.tar.gz
  md5: 519f299f5e6040a9abf8453751225540
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - osprofiler = osprofiler:main
    #
    # Would create an entry point called osprofiler that calls osprofiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - osprofiler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Profiler Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
