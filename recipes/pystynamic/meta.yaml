package:
  name: pystynamic
  version: "0.12"

source:
  fn: pystynamic-0.12.tar.gz
  url: https://pypi.python.org/packages/source/p/pystynamic/pystynamic-0.12.tar.gz
  md5: 87c573bfabc690fff15e8a0f5250ab1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystynamic = pystynamic:main
    #
    # Would create an entry point called pystynamic that calls pystynamic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pystynamic
    - pystynamic.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/markhildreth/pystynamic
  license: UNKNOWN
  summary: 'Python static website generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
