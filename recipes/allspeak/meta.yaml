package:
  name: allspeak
  version: "0.6"

source:
  fn: AllSpeak-0.6.tar.gz
  url: https://pypi.python.org/packages/source/A/AllSpeak/AllSpeak-0.6.tar.gz
  md5: 9451039d07a6df57f539edf360017d2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - allspeak = allspeak:main
    #
    # Would create an entry point called allspeak that calls allspeak.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - babel
    - markupsafe
    - pytz
    - pyyaml

  run:
    - python
    - babel
    - markupsafe
    - pytz
    - pyyaml

test:
  # Python imports
  imports:
    - allspeak
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lucuma/allspeak
  license: MIT License
  summary: 'Pythonic i18n/l10n system for WSGI-based web applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
