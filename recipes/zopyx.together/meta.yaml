package:
  name: zopyx.together
  version: "0.2.1"

source:
  fn: zopyx.together-0.2.1.zip
  url: https://pypi.python.org/packages/source/z/zopyx.together/zopyx.together-0.2.1.zip
  md5: a8ef25a1ac58ff3806124f604081d774
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.together = zopyx.together:main
    #
    # Would create an entry point called zopyx.together that calls zopyx.together.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.i18nmessageid

  run:
    - python
    - setuptools
    - setuptools
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - zopyx
    - zopyx.together
    - zopyx.together.browser
    - zopyx.together.together

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zopyx/zopyx.together
  license: GPL
  summary: 'Plone integration with together.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
