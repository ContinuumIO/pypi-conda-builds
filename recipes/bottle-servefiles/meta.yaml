package:
  name: bottle-servefiles
  version: "0.1.2dev"

source:
  fn: bottle-servefiles-0.1.2dev.zip
  url: https://pypi.python.org/packages/source/b/bottle-servefiles/bottle-servefiles-0.1.2dev.zip
  md5: 7f0f2e2df7cd99858d58abefb26eb6a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-servefiles = bottle-servefiles:main
    #
    # Would create an entry point called bottle-servefiles that calls bottle-servefiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/rudylattae/bottle-servefiles
  license: BSD License
  summary: 'A reusable app that serves static files for bottle apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
