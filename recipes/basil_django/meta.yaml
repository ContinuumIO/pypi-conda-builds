package:
  name: basil_django
  version: "0.1"

source:
  fn: basil_django-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/basil_django/basil_django-0.1.tar.gz
  md5: 25be42203f2f60181d35ccc853e4c77f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basil_django = basil_django:main
    #
    # Would create an entry point called basil_django that calls basil_django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - basil

  run:
    - python
    - basil

test:
  # Python imports
  imports:
    - basil
    - basil.v0
    - basil.v0.templates
    - basil.v0.templates.django
    - basil_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: LICENSE.txt
  summary: 'Basil template to create a basic django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
