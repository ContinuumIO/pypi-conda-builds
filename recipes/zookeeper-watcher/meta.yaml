package:
  name: zookeeper-watcher
  version: "0.1.4"

source:
  fn: Zookeeper-Watcher-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/Z/Zookeeper-Watcher/Zookeeper-Watcher-0.1.4.tar.gz
  md5: df777feda8e4e431c25011e1648056dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zookeeper-watcher = zookeeper-watcher:main
    #
    # Would create an entry point called zookeeper-watcher that calls zookeeper-watcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kazoo

  run:
    - python
    - kazoo

test:
  # Python imports
  imports:
    - zookeeper_watcher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Bogdan-Alexandrescu/Zookeeper-Watcher
  license: MIT License
  summary: 'Watches a designated node in Zookeeper for data or member changes and trigger a function when an event is detected.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
