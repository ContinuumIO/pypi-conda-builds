package:
  name: gitbanshee
  version: "0.1"

source:
  fn: GitBanshee-0.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GitBanshee/GitBanshee-0.1.tar.gz
  md5: 94ce767c5e7b52174c7407fc3884a2c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitbanshee = gitbanshee:main
    #
    # Would create an entry point called gitbanshee that calls gitbanshee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=0.13
    - gevent-socketio >=0.3.5-rc2
    - socketio-client ==0.4

  run:
    - python
    - gevent >=0.13
    - gevent-socketio >=0.3.5-rc2
    - socketio-client ==0.4

test:
  # Python imports
  imports:
    - gitbanshee

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dirtymonkey.co.uk/Git-Banshee
  license: MIT License
  summary: 'Play sound effects when you commit, checkout and merge in git.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
