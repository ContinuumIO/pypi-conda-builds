package:
  name: mrs-mapreduce
  version: "0.9"

source:
  fn: mrs-mapreduce-0.9.tar.gz
  url: https://pypi.python.org/packages/source/m/mrs-mapreduce/mrs-mapreduce-0.9.tar.gz
  md5: 8d09700a0dce3fc8f64647835f25c99d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mrs-mapreduce = mrs-mapreduce:main
    #
    # Would create an entry point called mrs-mapreduce that calls mrs-mapreduce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mrs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/mrs-mapreduce/
  license: Apache Software License
  summary: 'A lightweight MapReduce implementation for computationally intensive programs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
