package:
  name: dxfgrabber
  version: "0.7.4"

source:
  fn: dxfgrabber-0.7.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dxfgrabber/dxfgrabber-0.7.4.tar.gz
  md5: 85e7a5bafe513584ff4b2145316882cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dxfgrabber = dxfgrabber:main
    #
    # Would create an entry point called dxfgrabber that calls dxfgrabber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dxfgrabber

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mozman/dxfgrabber
  license: MIT License
  summary: 'A Python library to grab information from DXF drawings - all DXF versions supported.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
