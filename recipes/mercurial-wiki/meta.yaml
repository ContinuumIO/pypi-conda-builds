package:
  name: mercurial-wiki
  version: "1.2.2"

source:
  fn: mercurial-wiki-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mercurial-wiki/mercurial-wiki-1.2.2.tar.gz
  md5: 26fceb38c59623a80f1814af74c1c38e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mercurial-wiki = mercurial-wiki:main
    #
    # Would create an entry point called mercurial-wiki that calls mercurial-wiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - bottle >=0.11.6
    - jinja2 >=2.6
    - markdown2
    - mercurial

  run:
    - python
    - bottle >=0.11.6
    - jinja2 >=2.6
    - markdown2
    - mercurial

test:
  # Python imports
  imports:
    - mercurial_wiki

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/brejoc/mercurial-wiki/
  license: GPLv2
  summary: 'A developers wiki.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
