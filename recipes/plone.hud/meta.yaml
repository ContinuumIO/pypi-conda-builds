package:
  name: plone.hud
  version: "1.0a1"

source:
  fn: plone.hud-1.0a1.zip
  url: https://pypi.python.org/packages/source/p/plone.hud/plone.hud-1.0a1.zip
  md5: a43260ea5904aa62f7cdf51ba766453c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.hud = plone.hud:main
    #
    # Would create an entry point called plone.hud that calls plone.hud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - plone
    - plone.api
    - setuptools

  run:
    - python
    - setuptools
    - pillow
    - plone
    - plone.api
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.hud
    - plone.hud.browser
    - plone.hud.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.hud
  license: GPL version 2
  summary: 'Plone Heads Up Display Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
