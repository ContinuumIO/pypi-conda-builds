package:
  name: plone.oofill
  version: "1.2.0"

source:
  fn: plone.oofill-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.oofill/plone.oofill-1.2.0.tar.gz
  md5: 55496988f8a541e8a9fc6aac9870503d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.oofill = plone.oofill:main
    #
    # Would create an entry point called plone.oofill that calls plone.oofill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - oofill

  run:
    - python
    - setuptools
    - setuptools
    - oofill

test:
  # Python imports
  imports:
    - plone
    - plone.oofill

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plonegov.org/plone.oofill
  license: GPL
  summary: 'Uses the oofill python module to fill an odt document with data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
