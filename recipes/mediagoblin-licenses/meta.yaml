package:
  name: mediagoblin-licenses
  version: "0.1.3"

source:
  fn: mediagoblin-licenses-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mediagoblin-licenses/mediagoblin-licenses-0.1.3.tar.gz
  md5: 46e907b0b9e8ba2a56fc7f376be0fc7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mediagoblin-licenses = mediagoblin-licenses:main
    #
    # Would create an entry point called mediagoblin-licenses that calls mediagoblin-licenses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mediagoblin_licenses

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitorious.org/mediagoblin-licenses/mediagoblin-licenses
  license: License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Customize the licenses for your mediagoblin installation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
