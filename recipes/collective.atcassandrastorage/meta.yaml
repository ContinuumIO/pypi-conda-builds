package:
  name: collective.atcassandrastorage
  version: "0.2"

source:
  fn: collective.atcassandrastorage-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.atcassandrastorage/collective.atcassandrastorage-0.2.tar.gz
  md5: 441df1a246a15c64d48f041336ff74cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.atcassandrastorage = collective.atcassandrastorage:main
    #
    # Would create an entry point called collective.atcassandrastorage that calls collective.atcassandrastorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.atcassandrastorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seletz/collective.atcassandrastorage
  license: GPL
  summary: 'A AT field storage which stores values to a cassandra database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
