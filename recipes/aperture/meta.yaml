package:
  name: aperture
  version: "0.1.14"

source:
  fn: aperture-0.1.14.zip
  url: https://pypi.python.org/packages/source/a/aperture/aperture-0.1.14.zip
  md5: 1bc997a4d5b950af2893e6cb32d64448
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aperture = aperture:main
    #
    # Would create an entry point called aperture that calls aperture.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib
    - numpy

  run:
    - python
    - matplotlib
    - numpy

test:
  # Python imports
  imports:
    - aperture

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevinsprong23/aperture
  license: MIT
  summary: 'Wrappers, settings, and extensions for Python/matplotlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
