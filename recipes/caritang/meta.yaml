package:
  name: caritang
  version: "0.4.0"

source:
  fn: caritang-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/caritang/caritang-0.4.0.tar.gz
  md5: a42eb27e4ab2ae85efcff18ed63be19b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caritang = caritang:main
    #
    # Would create an entry point called caritang that calls caritang.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - caritang
    - caritang.common
    - caritang.core
    - caritang.gui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.caritang.bressure.net
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Caritang a simple backup application for N900 to google docs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
