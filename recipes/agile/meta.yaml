package:
  name: agile
  version: "1.3.0"

source:
  fn: agile-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/agile/agile-1.3.0.tar.gz
  md5: c095c102eea071bbba0b2637553b52e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agile = agile:main
    #
    # Would create an entry point called agile that calls agile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock ==1.0.1
    - coverage
    - nose ==1.3.4
    - rednose ==0.4.1
    - sure ==1.2.10
    - steadymark
    - httpretty

  run:
    - python
    - mock ==1.0.1
    - coverage
    - nose ==1.3.4
    - rednose ==0.4.1
    - sure ==1.2.10
    - steadymark
    - httpretty

test:
  # Python imports
  imports:
    - agile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gabrielfalcao/agile
  license: UNKNOWN
  summary: 'A meta-package containing a full toolset for agile development with TDD'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
