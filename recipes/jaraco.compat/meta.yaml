package:
  name: jaraco.compat
  version: "1.2"

source:
  fn: jaraco.compat-1.2.zip
  url: https://pypi.python.org/packages/source/j/jaraco.compat/jaraco.compat-1.2.zip
  md5: 4c35ef5cd6dada05d8596acda5a0354b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jaraco.compat = jaraco.compat:main
    #
    # Would create an entry point called jaraco.compat that calls jaraco.compat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - py23compat
    - py24compat
    - py25compat
    - py26compat
    - py27compat
    - py31compat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jaraco/jaraco.compat
  license: MIT
  summary: 'Modules providing forward compatibility across some Python 2.x versions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
