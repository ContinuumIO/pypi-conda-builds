package:
  name: pep8-naming
  version: "0.2.2"

source:
  fn: pep8-naming-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pep8-naming/pep8-naming-0.2.2.tar.gz
  md5: feb6f2e48889cc18499ec263b2bee468
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pep8-naming = pep8-naming:main
    #
    # Would create an entry point called pep8-naming that calls pep8-naming.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/flintwork/pep8-naming
  license: MIT License
  summary: 'Check PEP-8 naming conventions, plugin for flake8'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
