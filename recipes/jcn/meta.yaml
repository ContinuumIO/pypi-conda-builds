package:
  name: jcn
  version: "0.1"

source:
  fn: jcn-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jcn/jcn-0.1.tar.gz
  md5: e0476d3a0f22634b61dbf75a6a0e3cd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jcn = jcn:main
    #
    # Would create an entry point called jcn that calls jcn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blessings
    - asyncio

  run:
    - python
    - blessings
    - asyncio

test:
  # Python imports
  imports:
    - jcn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ch3pjw/junction/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Junction: A Python-based command-line UI framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
