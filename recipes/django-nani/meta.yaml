package:
  name: django-nani
  version: "0.0.5.dev0"

source:
  fn: django-nani-0.0.5.dev0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nani/django-nani-0.0.5.dev0.tar.gz
  md5: 81bbaa5f44ec23adf3d351fdc29729e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nani = django-nani:main
    #
    # Would create an entry point called django-nani that calls django-nani.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - nani
    - nani.compat
    - nani.test_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-nani.readthedocs.org
  license: BSD License
  summary: 'Multilingual database contents for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
