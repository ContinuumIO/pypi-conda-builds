package:
  name: ecreall.helpers.upgrade
  version: "1.2"

source:
  fn: ecreall.helpers.upgrade-1.2.zip
  url: https://pypi.python.org/packages/source/e/ecreall.helpers.upgrade/ecreall.helpers.upgrade-1.2.zip
  md5: bd85a3f9c459dd8530a05732d7fd3f62
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecreall.helpers.upgrade = ecreall.helpers.upgrade:main
    #
    # Would create an entry point called ecreall.helpers.upgrade that calls ecreall.helpers.upgrade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ecreall
    - ecreall.helpers
    - ecreall.helpers.upgrade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Adapter tool with helpers for upgrade often used at ecreall'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
