package:
  name: django-bible
  version: "1.0"

source:
  fn: django-bible-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bible/django-bible-1.0.tar.gz
  md5: 5e83a586ccf1c47a9e92052c32b578f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bible = django-bible:main
    #
    # Would create an entry point called django-bible that calls django-bible.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bible

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.davisd.com/projects/django-bible/
  license: LICENSE
  summary: 'django-bible is a Python package / django app for interfacing with the Holy Bible.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
