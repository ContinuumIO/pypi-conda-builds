package:
  name: django-listable
  version: "0.3.1"

source:
  fn: django-listable-0.3.1.zip
  url: https://pypi.python.org/packages/source/d/django-listable/django-listable-0.3.1.zip
  md5: 9263d41c101dc0025417e27e3ae5ea5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-listable = django-listable:main
    #
    # Would create an entry point called django-listable that calls django-listable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - listable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/randlet/django-listable
  license: BSD License
  summary: 'A reusable Django app to make integrations with the DataTables javascript library easy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
