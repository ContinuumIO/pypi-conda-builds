package:
  name: django-shopify-auth
  version: "0.4.0"

source:
  fn: django-shopify-auth-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shopify-auth/django-shopify-auth-0.4.0.tar.gz
  md5: 63b838fa7991d6d67f15e923a85f39be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shopify-auth = django-shopify-auth:main
    #
    # Would create an entry point called django-shopify-auth that calls django-shopify-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - shopifyapi >=2.1.2
    - setuptools >=5.7

  run:
    - python
    - django >=1.7
    - shopifyapi >=2.1.2
    - setuptools >=5.7

test:
  # Python imports
  imports:
    - shopify_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/discolabs/django-shopify-auth
  license: MIT
  summary: 'An simple package for adding Shopify authentication to Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
