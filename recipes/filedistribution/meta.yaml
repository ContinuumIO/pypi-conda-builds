package:
  name: filedistribution
  version: "0.1.1"

source:
  fn: FileDistribution-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FileDistribution/FileDistribution-0.1.1.tar.gz
  md5: da8ff9fd0fcc5e87f339405303c1e92c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - filedistribution = filedistribution:main
    #
    # Would create an entry point called filedistribution that calls filedistribution.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - filedistribution
    - filedistribution.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/caffecoder/fdist-python
  license: MIT License
  summary: 'Simple file distribution library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
