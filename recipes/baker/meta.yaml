package:
  name: baker
  version: "1.3"

source:
  fn: Baker-1.3.tar.gz
  url: https://pypi.python.org/packages/source/B/Baker/Baker-1.3.tar.gz
  md5: e628d935accf915abf6cdef77b81d8d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baker = baker:main
    #
    # Would create an entry point called baker that calls baker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - baker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mchaput/baker
  license: Apache Software License
  summary: 'Easy, powerful access to Python functions from the command line'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
