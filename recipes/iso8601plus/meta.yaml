package:
  name: iso8601plus
  version: "0.1.6"

source:
  fn: iso8601plus-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/i/iso8601plus/iso8601plus-0.1.6.tar.gz
  md5: d65033dfc8b0f5d52b6f6d6329db695f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iso8601plus = iso8601plus:main
    #
    # Would create an entry point called iso8601plus that calls iso8601plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iso8601
    - iso8601plus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jimklo/pyiso8601plus
  license: The MIT License (MIT)
  summary: 'Simple module to parse ISO 8601 dates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
