package:
  name: megrok.z3cform.layout
  version: "0.2.1"

source:
  fn: megrok.z3cform.layout-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.z3cform.layout/megrok.z3cform.layout-0.2.1.tar.gz
  md5: 170351e27a796d364ddf46ec8ace4974
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.z3cform.layout = megrok.z3cform.layout:main
    #
    # Would create an entry point called megrok.z3cform.layout that calls megrok.z3cform.layout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grokcore.view
    - megrok.z3cform.base
    - megrok.pagetemplate

  run:
    - python
    - setuptools
    - setuptools
    - grokcore.view
    - megrok.z3cform.base
    - megrok.pagetemplate

test:
  # Python imports
  imports:
    - megrok
    - megrok.z3cform
    - megrok.z3cform.layout
    - megrok.z3cform.layout.ftests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Generic templates for megrok.z3cform.base'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
