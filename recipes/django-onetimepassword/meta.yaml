package:
  name: django-onetimepassword
  version: "0.0.2"

source:
  fn: django-OneTimePassword-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-OneTimePassword/django-OneTimePassword-0.0.2.tar.gz
  md5: 0a6ff5fe8f6315d9215a076027cd40d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-onetimepassword = django-onetimepassword:main
    #
    # Would create an entry point called django-onetimepassword that calls django-onetimepassword.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_OneTimePassword

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jhg/django-OneTimePassword
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'A simple Django app with authentication backend for OneTimePassword'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
