package:
  name: horae.subscription
  version: "1.0a1"

source:
  fn: horae.subscription-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.subscription/horae.subscription-1.0a1.tar.gz
  md5: c2633a46ef4aa13566eeaad307ccfc84
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.subscription = horae.subscription:main
    #
    # Would create an entry point called horae.subscription that calls horae.subscription.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - horae.core
    - horae.layout
    - horae.ticketing

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - horae.core
    - horae.layout
    - horae.ticketing

test:
  # Python imports
  imports:
    - horae
    - horae.subscription

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides functionality to subscribe to an object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
