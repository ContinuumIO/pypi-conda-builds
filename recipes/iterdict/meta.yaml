package:
  name: iterdict
  version: "0.2.0"

source:
  fn: IterDict-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/I/IterDict/IterDict-0.2.0.tar.gz
  md5: 58197ecf15d52c8df58d818b5ed1bb49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iterdict = iterdict:main
    #
    # Would create an entry point called iterdict that calls iterdict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iterdict
    - iterdict.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/an_example_pypi_project
  license: BSD License
  summary: 'Dict that lazily populates itself with items from the iterator it was constructed with as keys are accessed'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
