package:
  name: gp.fileupload
  version: "1.2"

source:
  fn: gp.fileupload-1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gp.fileupload/gp.fileupload-1.2.tar.gz
  md5: 82d79a0ee390b2e903026943dc43c0d5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gp.fileupload = gp.fileupload:main
    #
    # Would create an entry point called gp.fileupload that calls gp.fileupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - paste
    - webob

  run:
    - python
    - setuptools
    - setuptools
    - paste
    - webob

test:
  # Python imports
  imports:
    - gp
    - gp.fileupload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gawel.org/docs/gp.fileupload/
  license: MIT License
  summary: 'A WSGI middleware to get some stats on large files upload,and provide a progress bar for your users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
