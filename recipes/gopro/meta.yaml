package:
  name: gopro
  version: "0.0.3"

source:
  fn: gopro-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gopro/gopro-0.0.3.tar.gz
  md5: 3dfc9c39fc04f4ee1d5d512ab30fd7c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gopro = gopro:main
    #
    # Would create an entry point called gopro that calls gopro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0
    - humanize >=0.5

  run:
    - python
    - requests >=2.3.0
    - humanize >=0.5

test:
  # Python imports
  imports:
    - gopro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DenisCarriere/gopro
  license: goprohero
  summary: 'GoPro Camera - Python module made easy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
