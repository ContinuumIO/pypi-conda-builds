package:
  name: shade-ansible
  version: "0.1.0"

source:
  fn: shade-ansible-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/shade-ansible/shade-ansible-0.1.0.tar.gz
  md5: 16dbec05ad63aed11658d3dfe6c10158
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shade-ansible = shade-ansible:main
    #
    # Would create an entry point called shade-ansible that calls shade-ansible.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Ansible modules that use shade to talk to OpenStack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
