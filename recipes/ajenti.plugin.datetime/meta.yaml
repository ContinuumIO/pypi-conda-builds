package:
  name: ajenti.plugin.datetime
  version: "0.5"

source:
  fn: ajenti.plugin.datetime-0.5.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.datetime/ajenti.plugin.datetime-0.5.tar.gz
  md5: 28476e3fe99b9d3e44036e5552e122a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.datetime = ajenti.plugin.datetime:main
    #
    # Would create an entry point called ajenti.plugin.datetime that calls ajenti.plugin.datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core
    - pytz

  run:
    - python
    - aj
    - ajenti.plugin.core
    - pytz

test:
  # Python imports
  imports:
    - ajenti_plugin_datetime
    - ajenti_plugin_datetime.managers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Date & time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
