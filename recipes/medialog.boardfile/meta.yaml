package:
  name: medialog.boardfile
  version: "1.6.1"

source:
  fn: medialog.boardfile-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.boardfile/medialog.boardfile-1.6.1.tar.gz
  md5: 82e6c16574befcaf994adff7fe86d7f3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.boardfile = medialog.boardfile:main
    #
    # Would create an entry point called medialog.boardfile that calls medialog.boardfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - medialog
    - medialog.boardfile
    - medialog.boardfile.browser
    - medialog.boardfile.content
    - medialog.boardfile.interfaces
    - medialog.boardfile.portlets
    - medialog.boardfile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GNU General Public License (GPL)
  summary: 'Boardfile content type'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
