package:
  name: mogo
  version: "0.2.4"

source:
  fn: mogo-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mogo/mogo-0.2.4.tar.gz
  md5: 84525d3f94a028bd6c77cced31aeb1b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mogo = mogo:main
    #
    # Would create an entry point called mogo that calls mogo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - mogo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joshmarshall/mogo/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Simple PyMongo "schema-less" object wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
