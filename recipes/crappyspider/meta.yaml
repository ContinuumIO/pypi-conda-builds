package:
  name: crappyspider
  version: "0.3"

source:
  fn: crappyspider-0.3.zip
  url: https://pypi.python.org/packages/source/c/crappyspider/crappyspider-0.3.zip
  md5: 34343570bbf2e885880e54285733311b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crappyspider = crappyspider:main
    #
    # Would create an entry point called crappyspider that calls crappyspider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scrapy

  run:
    - python
    - scrapy

test:
  # Python imports
  imports:
    - crappyspider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://crappyspider.readthedocs.org/en/latest/
  license: BSD License
  summary: 'Test your site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
