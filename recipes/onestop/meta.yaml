package:
  name: onestop
  version: "0.5.0"

source:
  fn: onestop-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/o/onestop/onestop-0.5.0.tar.gz
  md5: c579007e94c4df922baf7f4dd1ef92c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onestop = onestop:main
    #
    # Would create an entry point called onestop that calls onestop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mzgtfs
    - mzgeohash

  run:
    - python
    - mzgtfs
    - mzgeohash

test:
  # Python imports
  imports:
    - onestop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/transitland/onestop-id-python-client
  license: License :: OSI Approved :: MIT License
  summary: 'Transitland Onestop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
