package:
  name: django_sieve
  version: "1.0.0"

source:
  fn: django_sieve-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_sieve/django_sieve-1.0.0.tar.gz
  md5: 3f19270351432fd3053ec534ae133e79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_sieve = django_sieve:main
    #
    # Would create an entry point called django_sieve that calls django_sieve.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_sieve
    - sieve

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alixedi/django_sieve
  license: BSD License
  summary: 'Filter user-data based on multiple criteria.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
