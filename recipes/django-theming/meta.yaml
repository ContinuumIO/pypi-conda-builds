package:
  name: django-theming
  version: "1.0"

source:
  fn: django-theming-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-theming/django-theming-1.0.tar.gz
  md5: 4f3d7c12a0e8eedfc4dc7b41f70c82ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-theming = django-theming:main
    #
    # Would create an entry point called django-theming that calls django-theming.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - theming
    - theming.management
    - theming.management.commands
    - theming.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/achavezu89/django-theming
  license: GPL
  summary: 'Django application, implement theming concept, flexible and configurable. Allow theming for host url.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
