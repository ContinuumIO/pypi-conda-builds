package:
  name: django-jsonview
  version: "0.4.3"

source:
  fn: django-jsonview-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsonview/django-jsonview-0.4.3.tar.gz
  md5: a38b7050cdcc32d2e8fdfcea643d21ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsonview = django-jsonview:main
    #
    # Would create an entry point called django-jsonview that calls django-jsonview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jsocol/django-jsonview
  license: Apache Software License
  summary: 'Always return JSON from your Django view.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
