package:
  name: collective.ptg.nivoslider
  version: "1.0.6"

source:
  fn: collective.ptg.nivoslider-1.0.6.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.nivoslider/collective.ptg.nivoslider-1.0.6.zip
  md5: 11a7472b6084560798f83bd55f1c92a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.nivoslider = collective.ptg.nivoslider:main
    #
    # Would create an entry point called collective.ptg.nivoslider that calls collective.ptg.nivoslider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.nivoslider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.nivoslider
  license: GPL
  summary: 'Adds Nivoslider to collective.plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
