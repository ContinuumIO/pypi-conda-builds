package:
  name: cliutils
  version: "0.1.3"

source:
  fn: cliutils-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cliutils/cliutils-0.1.3.tar.gz
  md5: c456d8ad046451ff18f4b4acbca7291a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cliutils = cliutils:main
    #
    # Would create an entry point called cliutils that calls cliutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cliutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/cliutils
  license: MIT License
  summary: 'A collection of utilities easing the creation of command line scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
