package:
  name: django_robotice_auth
  version: "0.0.1"

source:
  fn: django_robotice_auth-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_robotice_auth/django_robotice_auth-0.0.1.tar.gz
  md5: 8e30ae28cb68e0ae3488de36871dd30a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_robotice_auth = django_robotice_auth:main
    #
    # Would create an entry point called django_robotice_auth that calls django_robotice_auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_robotice_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Django authentication backend for use with Robotice Control Identity'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
