package:
  name: burp-ui
  version: "0.0.6"

source:
  fn: burp-ui-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/b/burp-ui/burp-ui-0.0.6.tar.gz
  md5: 9fca17641ff1b45141f8de74bbc15697
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - burp-ui = burp-ui:main
    #
    # Would create an entry point called burp-ui that calls burp-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.10.1
    - flask-login ==0.2.11
    - flask-wtf ==0.10.0
    - wtforms ==2.0.1

  run:
    - python
    - flask ==0.10.1
    - flask-login ==0.2.11
    - flask-wtf ==0.10.0
    - wtforms ==2.0.1

test:
  # Python imports
  imports:
    - burpui
    - burpui.misc
    - burpui.misc.auth
    - burpui.misc.backend
    - burpui.misc.parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git.ziirish.me/ziirish/burp-ui
  license: BSD License
  summary: 'Burp-UI is a web-ui for burp backup written in python with Flask and jQuery/Bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
