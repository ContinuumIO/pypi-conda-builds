package:
  name: products.cmftopic
  version: "2.3.0-beta"

source:
  fn: Products.CMFTopic-2.3.0-beta.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.CMFTopic/Products.CMFTopic-2.3.0-beta.tar.gz
  md5: 5cea734ac855cd64921bfcfb251b67b2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.cmftopic = products.cmftopic:main
    #
    # Would create an entry point called products.cmftopic that calls products.cmftopic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2 >=2.13.12
    - products.cmfcore
    - products.cmfdefault
    - products.genericsetup

  run:
    - python
    - setuptools
    - setuptools
    - zope2 >=2.13.12
    - products.cmfcore
    - products.cmfdefault
    - products.genericsetup

test:
  # Python imports
  imports:
    - Products
    - Products.CMFTopic
    - Products.CMFTopic.interfaces
    - Products.CMFTopic.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.CMFTopic
  license: Zope Public License
  summary: 'Topic product for the Zope Content Management Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
