package:
  name: doit-py
  version: "0.3.0"

source:
  fn: doit-py-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/doit-py/doit-py-0.3.0.tar.gz
  md5: 4ea35d44c53d836f505443542645cc9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doit-py = doit-py:main
    #
    # Would create an entry point called doit-py that calls doit-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - doit
    - configclass
    - pathlib

  run:
    - python
    - doit
    - configclass
    - pathlib

test:
  # Python imports
  imports:
    - doitpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/doit-py
  license: MIT License
  summary: 'doit tasks for python stuff'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
