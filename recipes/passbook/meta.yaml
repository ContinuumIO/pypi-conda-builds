package:
  name: passbook
  version: "1.0.0"

source:
  fn: Passbook-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Passbook/Passbook-1.0.0.tar.gz
  md5: b17c31c1caa55bf6f09df09dd324c06e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - passbook = passbook:main
    #
    # Would create an entry point called passbook that calls passbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - m2crypto >=0.21.1

  run:
    - python
    - m2crypto >=0.21.1

test:
  # Python imports
  imports:
    - passbook
    - passbook.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/devartis/passbook/
  license: passencode
  summary: 'Passbook file generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
