package:
  name: minimocktest
  version: "0.5"

source:
  fn: MiniMockTest-0.5.tar.gz
  url: https://pypi.python.org/packages/source/M/MiniMockTest/MiniMockTest-0.5.tar.gz
  md5: 85df08446cb1e41e1f7f8274bb34f598
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minimocktest = minimocktest:main
    #
    # Would create an entry point called minimocktest that calls minimocktest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - minimocktest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pykler.github.com/MiniMockTest/
  license: MIT
  summary: 'Custom unittest TestCase that wraps minimock'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
