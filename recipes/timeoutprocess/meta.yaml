package:
  name: timeoutprocess
  version: "0.1.0"

source:
  fn: TimeoutProcess-0.1.0.zip
  url: https://pypi.python.org/packages/source/T/TimeoutProcess/TimeoutProcess-0.1.0.zip
  md5: 4be6f4b6024ff7b39d3d27df7765e3bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - timeoutprocess = timeoutprocess:main
    #
    # Would create an entry point called timeoutprocess that calls timeoutprocess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - timeoutprocess

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/TimeoutProcess/
  license: LICENSE.txt
  summary: 'Run a process with a timeout attached to it.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
