package:
  name: django-pip-starter
  version: "1.0.3"

source:
  fn: django-pip-starter-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pip-starter/django-pip-starter-1.0.3.tar.gz
  md5: 9449824d687aba3a07e105d7ecdaf67d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pip-starter = django-pip-starter:main
    #
    # Would create an entry point called django-pip-starter that calls django-pip-starter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangopipstarter
    - djangopipstarter.bin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/marltu/django-pip-starter
  license: BSD License
  summary: 'Useful tool for quickstarting django projects. It creates virtual environment and preconfigured django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
