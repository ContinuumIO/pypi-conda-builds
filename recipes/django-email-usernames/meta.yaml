package:
  name: django-email-usernames
  version: "1.0"

source:
  fn: django-email-usernames-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-usernames/django-email-usernames-1.0.tar.gz
  md5: 8b994622e926197ecbb9e96b00cd9020
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-usernames = django-email-usernames:main
    #
    # Would create an entry point called django-email-usernames that calls django-email-usernames.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_usernames

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/hakanw/django-email-usernames/
  license: BSD License
  summary: 'Piggy back on django-registration and add email-based usernames.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
