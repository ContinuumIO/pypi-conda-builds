package:
  name: createsend
  version: "4.0.0"

source:
  fn: createsend-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/createsend/createsend-4.0.0.tar.gz
  md5: da9fe0c5f4d975338f50625a6ae076a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - createsend = createsend:main
    #
    # Would create an entry point called createsend that calls createsend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - createsend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://campaignmonitor.github.io/createsend-python/
  license: MIT
  summary: 'A library which implements the complete functionality of the Campaign Monitor API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
