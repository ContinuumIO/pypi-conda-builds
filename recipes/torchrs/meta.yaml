package:
  name: torchrs
  version: "0.2.3"

source:
  fn: torchrs-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/t/torchrs/torchrs-0.2.3.tar.gz
  md5: 30d0551a1923c47fe690e5e3991b658a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torchrs = torchrs:main
    #
    # Would create an entry point called torchrs that calls torchrs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - torchrs
    - torchrs.git
    - torchrs.logger
    - torchrs.module
    - torchrs.shell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: PSF
  summary: 'Torch dependencies management tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
