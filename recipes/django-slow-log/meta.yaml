package:
  name: django-slow-log
  version: "0.1.3"

source:
  fn: django-slow-log-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-slow-log/django-slow-log-0.1.3.tar.gz
  md5: dfe2cc6a637bfea169035209a403c02d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-slow-log = django-slow-log:main
    #
    # Would create an entry point called django-slow-log that calls django-slow-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - django_slow_log

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jmoiron/django-slow-log
  license: MIT License
  summary: 'slow request log for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
