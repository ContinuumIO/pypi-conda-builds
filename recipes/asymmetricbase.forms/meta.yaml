package:
  name: asymmetricbase.forms
  version: "0.1.1-201404241725"

source:
  fn: asymmetricbase.forms-0.1.1-201404241725.tar.gz
  url: https://pypi.python.org/packages/source/a/asymmetricbase.forms/asymmetricbase.forms-0.1.1-201404241725.tar.gz
  md5: 01836675b8e2cc39eb3c4a4e62665eab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asymmetricbase.forms = asymmetricbase.forms:main
    #
    # Would create an entry point called asymmetricbase.forms that calls asymmetricbase.forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz
    - hamlpy
    - asymmetricbase.jinja

  run:
    - python
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz
    - hamlpy
    - asymmetricbase.jinja

test:
  # Python imports
  imports:
    - asymmetricbase
    - asymmetricbase.forms
    - asymmetricbase.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AsymmetricVentures/asym-forms
  license: GNU General Public License v2 (GPLv2)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
