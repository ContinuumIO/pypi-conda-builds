package:
  name: django-activities
  version: "1.0.0"

source:
  fn: django-activities-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-activities/django-activities-1.0.0.tar.gz
  md5: db4b8d1d723115e21045ec53dbc0e8e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-activities = django-activities:main
    #
    # Would create an entry point called django-activities that calls django-activities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - activities
    - activities.migrations
    - activities.mixins
    - activities.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/infoagetech/django-activities
  license: MIT License
  summary: u'Activities app for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
