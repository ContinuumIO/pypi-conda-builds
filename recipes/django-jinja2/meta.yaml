package:
  name: django-jinja2
  version: "0.1"

source:
  fn: django-jinja2-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jinja2/django-jinja2-0.1.tar.gz
  md5: 4ece5cb589dec99fcbe5cf5276a782f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jinja2 = django-jinja2:main
    #
    # Would create an entry point called django-jinja2 that calls django-jinja2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_jinja2
    - django_jinja2.loaders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yuchant/django-jinja2
  license: UNKNOWN
  summary: 'A drop in conditional template loader to use jinja2 if the filename / path meets custom criteria'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
