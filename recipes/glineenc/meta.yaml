package:
  name: glineenc
  version: "1.0"

source:
  fn: glineenc-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/glineenc/glineenc-1.0.tar.gz
  md5: 3a1b68904a54a094ea48b68981ef9c2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glineenc = glineenc:main
    #
    # Would create an entry point called glineenc that calls glineenc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - glineenc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/wyatt/glineenc
  license: MIT
  summary: 'Convert lat/long pairs to Base64 encoding for Google Maps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
