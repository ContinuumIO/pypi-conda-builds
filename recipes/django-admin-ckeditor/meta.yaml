package:
  name: django-admin-ckeditor
  version: "0.0.1"

source:
  fn: django-admin-ckeditor-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-ckeditor/django-admin-ckeditor-0.0.1.tar.gz
  md5: 156405e0c8d2f15a1096316dca766a1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-ckeditor = django-admin-ckeditor:main
    #
    # Would create an entry point called django-admin-ckeditor that calls django-admin-ckeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - ckeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madisona/django-admin-ckeditor
  license: BSD License
  summary: 'Ckeditor integration with Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
