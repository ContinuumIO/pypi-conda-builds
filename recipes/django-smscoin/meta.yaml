package:
  name: django-smscoin
  version: "0.1"

source:
  fn: django-smscoin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smscoin/django-smscoin-0.1.tar.gz
  md5: 8d438adb1e0199de6b5672695cde7a8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smscoin = django-smscoin:main
    #
    # Would create an entry point called django-smscoin that calls django-smscoin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.5
    - lxml ==2.2.6
    - django-countries

  run:
    - python
    - django >=1.2.5
    - lxml ==2.2.6
    - django-countries

test:
  # Python imports
  imports:
    - smscoin
    - smscoin.management
    - smscoin.management.commands
    - smscoin.migrations
    - smscoin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Arpaso/smscoin
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application to use sms processing system http://smscoin.com/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
