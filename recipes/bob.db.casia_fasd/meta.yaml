package:
  name: bob.db.casia_fasd
  version: "2.0.3"

source:
  fn: bob.db.casia_fasd-2.0.3.zip
  url: https://pypi.python.org/packages/source/b/bob.db.casia_fasd/bob.db.casia_fasd-2.0.3.zip
  md5: 49dd62c43d000d9e5c4d61b12d5e3718
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.casia_fasd = bob.db.casia_fasd:main
    #
    # Would create an entry point called bob.db.casia_fasd that calls bob.db.casia_fasd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - six
    - bob.db.base
    - antispoofing.utils

  run:
    - python
    - setuptools
    - setuptools
    - six
    - bob.db.base
    - antispoofing.utils

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.casia_fasd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/xbob.db.casia_fasd
  license: GNU General Public License v3 (GPLv3)
  summary: 'CASIA Face Anti-Spoofing Database Access API for Bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
