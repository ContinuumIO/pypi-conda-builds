package:
  name: lbne-build
  version: "0.7.5"

source:
  fn: lbne-build-0.7.5.tar.gz
  url: https://pypi.python.org/packages/source/l/lbne-build/lbne-build-0.7.5.tar.gz
  md5: ab257917c190b65e70efd55e9f0a5d00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lbne-build = lbne-build:main
    #
    # Would create an entry point called lbne-build that calls lbne-build.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ups-utils >=0.2
    - worch >=1.3.2
    - worch-ups >=0.5.0

  run:
    - python
    - ups-utils >=0.2
    - worch >=1.3.2
    - worch-ups >=0.5.0

test:
  # Python imports
  imports:
    - worch
    - worch.lbne
    - worch.lbne.pandorasrc
    - worch.lbne.tbbinst

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/LBNE/lbne-build
  license: GPLv2
  summary: 'Worch/waf tools to build LBNE software.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
