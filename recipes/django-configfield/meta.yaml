package:
  name: django-configfield
  version: "0.2.2"

source:
  fn: django-configfield-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-configfield/django-configfield-0.2.2.tar.gz
  md5: 08678e58cd8009ab152bc4d20b1fc9ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-configfield = django-configfield:main
    #
    # Would create an entry point called django-configfield that calls django-configfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jsonfield ==0.8.11

  run:
    - python
    - django-jsonfield ==0.8.11

test:
  # Python imports
  imports:
    - configfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/msaelices/django-configfield/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A new model and form field flexible for schema changes, stored as JSON in database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
