package:
  name: bayesredis
  version: "1.2.0"

source:
  fn: bayesredis-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bayesredis/bayesredis-1.2.0.tar.gz
  md5: 22cb89b6604827fa0e6a8f5203747827
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bayesredis = bayesredis:main
    #
    # Would create an entry point called bayesredis that calls bayesredis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - BayesRedis
    - bayesredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tistaharahap/python-bayes-redis
  license: MIT
  summary: 'A Simple Naive Bayes Classifier in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
