package:
  name: django-webcam-photoupload
  version: "0.3.3"

source:
  fn: django-webcam-photoupload-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webcam-photoupload/django-webcam-photoupload-0.3.3.tar.gz
  md5: 3604f080c54b411c921c29f3efba0d7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webcam-photoupload = django-webcam-photoupload:main
    #
    # Would create an entry point called django-webcam-photoupload that calls django-webcam-photoupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5

  run:
    - python
    - django ==1.5

test:
  # Python imports
  imports:
    - photo_upload
    - ziplookup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/citizenengagementlab/django-webcam-photoupload
  license: LICENSE.txt
  summary: 'Webcam photo uploads for the movement'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
