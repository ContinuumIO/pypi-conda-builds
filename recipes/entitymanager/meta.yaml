package:
  name: entitymanager
  version: "v0.1.2"

source:
  fn: entitymanager-v0.1.2.zip
  url: https://pypi.python.org/packages/source/e/entitymanager/entitymanager-v0.1.2.zip
  md5: 984de824052c1942ea1c18e56f7e02b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - entitymanager = entitymanager:main
    #
    # Would create an entry point called entitymanager that calls entitymanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - entitymanager
    - pyentity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leodesouza/pyentity
  license: Apache Software License
  summary: 'An simple object-document mapping for mongodb-motor and tornado applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
