package:
  name: redomino.tinymceplugins.snippet
  version: "0.0.4"

source:
  fn: redomino.tinymceplugins.snippet-0.0.4.zip
  url: https://pypi.python.org/packages/source/r/redomino.tinymceplugins.snippet/redomino.tinymceplugins.snippet-0.0.4.zip
  md5: 18a7b96f9ba59abd3e45c2dc0d98143e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.tinymceplugins.snippet = redomino.tinymceplugins.snippet:main
    #
    # Would create an entry point called redomino.tinymceplugins.snippet that calls redomino.tinymceplugins.snippet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.tinymce

  run:
    - python
    - setuptools
    - setuptools
    - products.tinymce

test:
  # Python imports
  imports:
    - redomino
    - redomino.tinymceplugins
    - redomino.tinymceplugins.snippet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.tinymceplugins.snippet
  license: GPL
  summary: 'A TinyMCE plugin for pasting snippets inside a text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
