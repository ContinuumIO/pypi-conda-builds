package:
  name: arnold
  version: "0.3.8"

source:
  fn: arnold-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/a/arnold/arnold-0.3.8.tar.gz
  md5: f9845c45c3bec1caca5c59a47456afe1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arnold = arnold:main
    #
    # Would create an entry point called arnold that calls arnold.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - peewee
    - termcolor

  run:
    - python
    - peewee
    - termcolor

test:
  # Python imports
  imports:
    - arnold
    - arnold.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cam-stitt/arnold
  license: BSD
  summary: 'Simple migrations for python ORMs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
