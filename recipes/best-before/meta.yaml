package:
  name: best-before
  version: "0.1.0"

source:
  fn: best-before-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/best-before/best-before-0.1.0.tar.gz
  md5: 9f59dcea737e8ba8506d6835f7c803c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - best-before = best-before:main
    #
    # Would create an entry point called best-before that calls best-before.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - best_before

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://git.vanneva.com/best-before.git
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
