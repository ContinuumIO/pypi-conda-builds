package:
  name: kitchen
  version: "1.2.1"

source:
  fn: kitchen-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kitchen/kitchen-1.2.1.tar.gz
  md5: 1baa961fda20e47895f5619bde51db20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kitchen = kitchen:main
    #
    # Would create an entry point called kitchen that calls kitchen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kitchen
    - kitchen.collections
    - kitchen.i18n
    - kitchen.iterutils
    - kitchen.pycompat24
    - kitchen.pycompat24.base64
    - kitchen.pycompat24.sets
    - kitchen.pycompat25
    - kitchen.pycompat25.collections
    - kitchen.pycompat27
    - kitchen.pycompat27.subprocess
    - kitchen.text
    - kitchen.versioning

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://fedorahosted.org/kitchen
  license: GNU Library or Lesser General Public License (LGPL)
  summary: u'Kitchen contains a cornucopia of useful code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
