package:
  name: iterxml
  version: "0.1.1"

source:
  fn: iterxml-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iterxml/iterxml-0.1.1.tar.gz
  md5: 342d3ef0c12e13b2f3343b5afac62f5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iterxml = iterxml:main
    #
    # Would create an entry point called iterxml that calls iterxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iterxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lars512/iterxml/
  license: BSD
  summary: 'Stream data from large XML documents with minimal memory.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
