package:
  name: collective.typecriterion
  version: "0.1.0"

source:
  fn: collective.typecriterion-0.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.typecriterion/collective.typecriterion-0.1.0.zip
  md5: fc619fe9b564853b8280f668775bdf67
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.typecriterion = collective.typecriterion:main
    #
    # Would create an entry point called collective.typecriterion that calls collective.typecriterion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.collection
    - plone.app.querystring >=1.2.0
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.collection
    - plone.app.querystring >=1.2.0
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.typecriterion
    - collective.typecriterion.browser
    - collective.typecriterion.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keul/collective.typecriterion
  license: GPL
  summary: 'A new, more flexible, content type criterion for Plone collections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
