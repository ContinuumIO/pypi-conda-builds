package:
  name: django-chatovod
  version: "1.4.2"

source:
  fn: django-chatovod-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chatovod/django-chatovod-1.4.2.tar.gz
  md5: be3aad8111d2bcd8fe601881e408d9d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chatovod = django-chatovod:main
    #
    # Would create an entry point called django-chatovod that calls django-chatovod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chatovod
    - chatovod.templatetags
    - docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gotlium/django-chatovod
  license: GNU General Public License (GPL)
  summary: 'Chatovod  - chat widget for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
