package:
  name: aerolito
  version: "0.1"

source:
  fn: aerolito-0.1.zip
  url: https://pypi.python.org/packages/source/a/aerolito/aerolito-0.1.zip
  md5: 14db37f814152caba7c6eda545b72ff7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aerolito = aerolito:main
    #
    # Would create an entry point called aerolito that calls aerolito.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - aerolito

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://renatopp.com/aerolito
  license: MIT License
  summary: 'Python library for natural language processing simulation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
