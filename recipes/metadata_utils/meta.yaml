package:
  name: metadata_utils
  version: "0.0.2"

source:
  fn: metadata_utils-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/metadata_utils/metadata_utils-0.0.2.tar.gz
  md5: b085e03b5d48c2e1157361f9b75b4914
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metadata_utils = metadata_utils:main
    #
    # Would create an entry point called metadata_utils that calls metadata_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - metadata_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jvanasco/metadata_utils
  license: MIT License
  summary: 'Lightweight Metadata Support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
