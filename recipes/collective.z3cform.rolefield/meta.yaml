package:
  name: collective.z3cform.rolefield
  version: "0.2"

source:
  fn: collective.z3cform.rolefield-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.rolefield/collective.z3cform.rolefield-0.2.zip
  md5: 74a1f2173000f4e9d823963bce97cfd3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.rolefield = collective.z3cform.rolefield:main
    #
    # Would create an entry point called collective.z3cform.rolefield that calls collective.z3cform.rolefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.schemaevent
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - zope.schemaevent
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.rolefield
    - collective.z3cform.rolefield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.z3cform.rolefield
  license: GPL
  summary: 'A field for managing local roles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
