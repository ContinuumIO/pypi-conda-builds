package:
  name: haystack_queryparser
  version: "0.2.1"

source:
  fn: haystack_queryparser-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/haystack_queryparser/haystack_queryparser-0.2.1.tar.gz
  md5: 5176c94041016849f0167e203f502f08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haystack_queryparser = haystack_queryparser:main
    #
    # Would create an entry point called haystack_queryparser that calls haystack_queryparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - haystack_queryparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/recruiterbox/haystack-queryparser
  license: MIT License
  summary: 'A search query parser that works in conjunction with haystack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
