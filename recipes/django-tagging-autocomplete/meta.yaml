package:
  name: django-tagging-autocomplete
  version: "0.4.2"

source:
  fn: django-tagging-autocomplete-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tagging-autocomplete/django-tagging-autocomplete-0.4.2.tar.gz
  md5: 774e0e3e78e38e0f5344fb302ea0f88b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tagging-autocomplete = django-tagging-autocomplete:main
    #
    # Would create an entry point called django-tagging-autocomplete that calls django-tagging-autocomplete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tagging_autocomplete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ludwiktrammer/django-tagging-autocomplete/
  license: MIT License
  summary: 'Autocompletion for django-tagging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
