package:
  name: python-lametro-api
  version: "0.2.0"

source:
  fn: python-lametro-api-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-lametro-api/python-lametro-api-0.2.0.tar.gz
  md5: 3f675072dc021e8dd491f89b0a17b55e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-lametro-api = python-lametro-api:main
    #
    # Would create an entry point called python-lametro-api that calls python-lametro-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.0.0
    - six ==1.4.1

  run:
    - python
    - requests ==2.0.0
    - six ==1.4.1

test:
  # Python imports
  imports:
    - la_metro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://datadesk.github.com/python-lametro-api/
  license: MIT License
  summary: "A simple Python wrapper for the L.A. Metro's API."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
