package:
  name: gpglib
  version: "0.1.1"

source:
  fn: gpglib-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gpglib/gpglib-0.1.1.tar.gz
  md5: 2e1d051b30d16688d5ce0c93117fc83c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gpglib = gpglib:main
    #
    # Would create an entry point called gpglib that calls gpglib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto
    - bitstring

  run:
    - python
    - pycrypto
    - bitstring

test:
  # Python imports
  imports:
    - gpglib
    - gpglib.content_parsers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LGPLv2
  summary: "Library for decrypting gpg that doesn't shell out to gpg"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
