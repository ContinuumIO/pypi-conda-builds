package:
  name: django-richenum
  version: "2.3.0"

source:
  fn: django-richenum-2.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-richenum/django-richenum-2.3.0.tar.gz
  md5: fad1959d36ea12118e5405f040b00fe5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-richenum = django-richenum:main
    #
    # Would create an entry point called django-richenum that calls django-richenum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3,<1.9
    - richenum

  run:
    - python
    - django >=1.3,<1.9
    - richenum

test:
  # Python imports
  imports:
    - django_richenum
    - django_richenum.forms
    - django_richenum.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hearsaycorp/django-richenum
  license: MIT
  summary: 'Django Enum library for python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
