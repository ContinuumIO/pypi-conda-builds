package:
  name: dtests
  version: "0.1.7"

source:
  fn: dTests-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dTests/dTests-0.1.7.tar.gz
  md5: 527a86d3a5dccea04cb53b1371bb5642
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dtests = dtests:main
    #
    # Would create an entry point called dtests that calls dtests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dTests
    - dTests.node
    - dTests.server
    - dTests.utils
    - dtests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MohamedBassem/dTests
  license: LICENSE.txt
  summary: 'A python tool to distributively run GCJ/FB hacker cup testcases on several machines.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
