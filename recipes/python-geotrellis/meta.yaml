package:
  name: python-geotrellis
  version: "0.1.0"

source:
  fn: python-geotrellis-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-geotrellis/python-geotrellis-0.1.0.tar.gz
  md5: e2755c55000a934ac4d61913152dd830
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-geotrellis = python-geotrellis:main
    #
    # Would create an entry point called python-geotrellis that calls python-geotrellis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdal >=1.9.1

  run:
    - python
    - gdal >=1.9.1

test:
  # Python imports
  imports:
    - gtloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ['http://github.com/geotrellis/python-geotrellis']
  license: Apache Software License
  summary: 'GeoTrellis library for mananging raster data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
