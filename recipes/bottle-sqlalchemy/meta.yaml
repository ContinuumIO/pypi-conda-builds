package:
  name: bottle-sqlalchemy
  version: "0.4.2"

source:
  fn: bottle-sqlalchemy-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-sqlalchemy/bottle-sqlalchemy-0.4.2.tar.gz
  md5: 22b16941cf5e9067aa65c2d981ceb935
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-sqlalchemy = bottle-sqlalchemy:main
    #
    # Would create an entry point called bottle-sqlalchemy that calls bottle-sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - bottle >=0.9
    - sqlalchemy

  run:
    - python
    - bottle >=0.9
    - sqlalchemy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/iurisilvio/bottle-sqlalchemy
  license: MIT License
  summary: 'SQLAlchemy integration for Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
