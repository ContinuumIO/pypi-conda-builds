package:
  name: django-atc-demo-ui
  version: "0.1.1"

source:
  fn: django-atc-demo-ui-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-atc-demo-ui/django-atc-demo-ui-0.1.1.tar.gz
  md5: 957b01c508a84aa18f83195871833672
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-atc-demo-ui = django-atc-demo-ui:main
    #
    # Would create an entry point called django-atc-demo-ui that calls django-atc-demo-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-atc-api
    - django-static-jquery ==1.11.1
    - django-bootstrap-themes ==3.1.2

  run:
    - python
    - django-atc-api
    - django-static-jquery ==1.11.1
    - django-bootstrap-themes ==3.1.2

test:
  # Python imports
  imports:
    - atc_demo_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/facebook/augmented-traffic-control
  license: django-atc-profile-storage
  summary: 'Demo Web UI for ATC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
