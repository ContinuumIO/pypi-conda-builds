package:
  name: crane-ec2
  version: "0.2.1"

source:
  fn: crane-ec2-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/crane-ec2/crane-ec2-0.2.1.tar.gz
  md5: fe224d0a457e1e27a55939b16ab2a036
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crane-ec2 = crane-ec2:main
    #
    # Would create an entry point called crane-ec2 that calls crane-ec2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.4.1
    - boto ==2.5.2

  run:
    - python
    - django ==1.4.1
    - boto ==2.5.2

test:
  # Python imports
  imports:
    - crane_ec2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'helper library to service related api actions in ec2 '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
