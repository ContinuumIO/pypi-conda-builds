package:
  name: nawa
  version: "1.0.0.b090714164121"

source:
  fn: Nawa-1.0.0.b090714164121.zip
  url: https://pypi.python.org/packages/source/N/Nawa/Nawa-1.0.0.b090714164121.zip
  md5: c71e6d7728ca7c49d8dd5f34e746737b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nawa = nawa:main
    #
    # Would create an entry point called nawa that calls nawa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mako >=0.2.4
    - pyyaml >=3.08

  run:
    - python
    - mako >=0.2.4
    - pyyaml >=3.08

test:
  # Python imports
  imports:
    - nawa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/nawa-framework/
  license: MIT License
  summary: 'Nawa is a simple Python web framework that designed for our ecological scripting life.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
