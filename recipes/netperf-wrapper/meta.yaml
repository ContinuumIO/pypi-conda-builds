package:
  name: netperf-wrapper
  version: "0.9.1"

source:
  fn: netperf-wrapper-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/n/netperf-wrapper/netperf-wrapper-0.9.1.tar.gz
  md5: 1be99b030a7f2b630a90407f988a8bb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netperf-wrapper = netperf-wrapper:main
    #
    # Would create an entry point called netperf-wrapper that calls netperf-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - netperf_wrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tohojo/netperf-wrapper
  license: GNU GPLv3
  summary: 'Wrapper for running network tests such as netperf concurrently'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
