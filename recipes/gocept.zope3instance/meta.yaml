package:
  name: gocept.zope3instance
  version: "2.0a2"

source:
  fn: gocept.zope3instance-2.0a2.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.zope3instance/gocept.zope3instance-2.0a2.tar.gz
  md5: 67faa195bc223ff455d685ba3a07676a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.zope3instance = gocept.zope3instance:main
    #
    # Would create an entry point called gocept.zope3instance that calls gocept.zope3instance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - zope.app.preference
    - zope.app.securitypolicy
    - zope.app.server
    - zope.app.tree
    - zope.app.zcmlfiles

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - zope.app.preference
    - zope.app.securitypolicy
    - zope.app.server
    - zope.app.tree
    - zope.app.zcmlfiles

test:
  # Python imports
  imports:
    - gocept
    - gocept.zope3instance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/gocept.zope3instance
  license: ZPL 2.1
  summary: 'zc.buildout recipe for defining a Zope 3 instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
