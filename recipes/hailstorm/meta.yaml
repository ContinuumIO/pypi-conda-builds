package:
  name: hailstorm
  version: "1.0.0"

source:
  fn: hailstorm-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hailstorm/hailstorm-1.0.0.tar.gz
  md5: 5bb813d25b72d1f14fa315fea2c81886
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hailstorm = hailstorm:main
    #
    # Would create an entry point called hailstorm that calls hailstorm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hailstorm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bitzeppelin/hailstorm/
  license: Apache Software License
  summary: 'A Python templating library in the style of Jinja2 and Django templates featuring the full power of Python in the expressions that can be embedded in the template files. Hailstorm was extracted from the templating code that is part of Tornado Web Server framework published by Facebook.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
