package:
  name: django-classification-banner
  version: "0.1.5"

source:
  fn: django-classification-banner-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-classification-banner/django-classification-banner-0.1.5.tar.gz
  md5: a56b62585721580d84e3a9fc9d7bb052
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-classification-banner = django-classification-banner:main
    #
    # Would create an entry point called django-classification-banner that calls django-classification-banner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_classification_banner
    - django_classification_banner.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/garnertb/django-classification-banner
  license: MIT
  summary: 'A django app that adds classification banners to your site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
