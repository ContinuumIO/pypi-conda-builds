package:
  name: pyryangosling
  version: "1.0"

source:
  fn: pyryangosling-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyryangosling/pyryangosling-1.0.tar.gz
  md5: f7deac13a428df69b1bb7e00232bcbaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyryangosling = pyryangosling:main
    #
    # Would create an entry point called pyryangosling that calls pyryangosling.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyryangosling

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'He wrote you 365 letters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
