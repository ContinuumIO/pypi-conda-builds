package:
  name: zope.app.versioncontrol
  version: "0.1"

source:
  fn: zope.app.versioncontrol-0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.versioncontrol/zope.app.versioncontrol-0.1.tar.gz
  md5: 67078c8c9ec8f3c8da7c4b0ebadbc604
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.versioncontrol = zope.app.versioncontrol:main
    #
    # Would create an entry point called zope.app.versioncontrol that calls zope.app.versioncontrol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.datetime
    - zope.location
    - zope.security

  run:
    - python
    - setuptools
    - zodb3
    - zope.datetime
    - zope.location
    - zope.security

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.versioncontrol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.versioncontrol
  license: Zope Public License
  summary: 'a framework for managing multiple versions of objects within a ZODB database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
