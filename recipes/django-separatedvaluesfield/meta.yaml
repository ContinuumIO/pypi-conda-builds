package:
  name: django-separatedvaluesfield
  version: "0.3.2"

source:
  fn: django-separatedvaluesfield-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-separatedvaluesfield/django-separatedvaluesfield-0.3.2.tar.gz
  md5: f330859dc2bc6017c124918cc0f9ba7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-separatedvaluesfield = django-separatedvaluesfield:main
    #
    # Would create an entry point called django-separatedvaluesfield that calls django-separatedvaluesfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - separatedvaluesfield
    - separatedvaluesfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-separatedvaluesfield
  license: MIT License
  summary: 'Custom field for Django to separate multiple values in database with a separator and retrieve them as list'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
