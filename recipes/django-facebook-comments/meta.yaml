package:
  name: django-facebook-comments
  version: "0.1.5"

source:
  fn: django-facebook-comments-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-comments/django-facebook-comments-0.1.5.tar.gz
  md5: 146fa3ebc5d3f83cde21427cf28c0373
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-comments = django-facebook-comments:main
    #
    # Would create an entry point called django-facebook-comments that calls django-facebook-comments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - facebook_comments
    - facebook_comments.conf
    - facebook_comments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sirpengi/django-facebook-comments
  license: MIT License
  summary: 'Drop-in facebook comments for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
