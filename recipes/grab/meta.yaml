package:
  name: grab
  version: "0.6.20"

source:
  fn: grab-0.6.20.tar.gz
  url: https://pypi.python.org/packages/source/g/grab/grab-0.6.20.tar.gz
  md5: 6b394f8881a10e7781b3c360f5eae9de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - grab = grab:main
    #
    # Would create an entry point called grab that calls grab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - pycurl
    - selection
    - weblib >=0.1.10
    - six

  run:
    - python
    - lxml
    - pycurl
    - selection
    - weblib >=0.1.10
    - six

test:
  # Python imports
  imports:
    - grab
    - grab.script
    - grab.selector
    - grab.spider
    - grab.spider.cache_backend
    - grab.spider.queue_backend
    - grab.spider.transport
    - grab.tools
    - grab.transport
    - grab.util
    - test
    - test.files

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://grablib.org
  license: MIT License
  summary: 'Web Scraping Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
