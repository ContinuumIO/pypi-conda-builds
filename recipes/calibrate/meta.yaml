package:
  name: calibrate
  version: "0.9"

source:
  fn: calibrate-0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/calibrate/calibrate-0.9.tar.gz
  md5: 0d6947dd946e5b1d292ed87dd42b4141
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - calibrate = calibrate:main
    #
    # Would create an entry point called calibrate that calls calibrate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy

  run:
    - python
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - .
    - calibrate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Interpolate using a calibration curve'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
