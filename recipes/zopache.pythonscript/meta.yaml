package:
  name: zopache.pythonscript
  version: "0.1.0"

source:
  fn: zopache.pythonscript-0.1.0.zip
  url: https://pypi.python.org/packages/source/z/zopache.pythonscript/zopache.pythonscript-0.1.0.zip
  md5: 441f24ad0e205891d81a0a30eb0169a5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopache.pythonscript = zopache.pythonscript:main
    #
    # Would create an entry point called zopache.pythonscript that calls zopache.pythonscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.container
    - zope.app.publication
    - zope.formlib
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.size
    - zodb3

  run:
    - python
    - setuptools
    - zope.container
    - zope.app.publication
    - zope.formlib
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.size
    - zodb3

test:
  # Python imports
  imports:
    - zopache
    - zopache.pythonscript
    - zopache.pythonscript.browser
    - zopache.pythonscript.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zopache.pythonscript
  license: Zope Public License
  summary: 'content TTW python script function'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
