package:
  name: django_yubico
  version: "0.3.1"

source:
  fn: django_yubico-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_yubico/django_yubico-0.3.1.tar.gz
  md5: c54c917609c8a4fae606b32ee6d8e41c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_yubico = django_yubico:main
    #
    # Would create an entry point called django_yubico that calls django_yubico.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - yubico-client >=1.9,<1.10

  run:
    - python
    - yubico-client >=1.9,<1.10

test:
  # Python imports
  imports:
    - django_yubico

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Kami/django-yubico-authentication-backend/
  license: BSD License
  summary: 'Yubico Yubikey authentication backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
