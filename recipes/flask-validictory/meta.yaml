package:
  name: flask-validictory
  version: "0.1.0"

source:
  fn: Flask-Validictory-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Validictory/Flask-Validictory-0.1.0.tar.gz
  md5: 1b2dfa2de779f6de8d75f7fe6e0baa46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-validictory = flask-validictory:main
    #
    # Would create an entry point called flask-validictory that calls flask-validictory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - validictory >=0.9.1

  run:
    - python
    - flask >=0.10.1
    - validictory >=0.9.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/inner-loop/flask-validictory/
  license: MIT License
  summary: 'Simple integration between Flask and Validictory.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
