package:
  name: ffindex
  version: "0.0.3"

source:
  fn: ffindex-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/ffindex/ffindex-0.0.3.tar.gz
  md5: ec8915b8edca14cf8bff2e1b5609846d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ffindex = ffindex:main
    #
    # Would create an entry point called ffindex that calls ffindex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ffindex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sseemayer/ffindex-python
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'ffindex support for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
