package:
  name: cmd-server
  version: "0.0.1"

source:
  fn: cmd-server-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmd-server/cmd-server-0.0.1.tar.gz
  md5: e9aa653c5c78b54268252a330b193b07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmd-server = cmd-server:main
    #
    # Would create an entry point called cmd-server that calls cmd-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubi >=0.0.5
    - keep-running

  run:
    - python
    - cubi >=0.0.5
    - keep-running

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/liaohuqiu/python-cmd-server
  license: MIT License
  summary: 'cube cmd server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
