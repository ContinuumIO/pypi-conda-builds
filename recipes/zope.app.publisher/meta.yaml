package:
  name: zope.app.publisher
  version: "3.10.2"

source:
  fn: zope.app.publisher-3.10.2.zip
  url: https://pypi.python.org/packages/source/z/zope.app.publisher/zope.app.publisher-3.10.2.zip
  md5: 66e9110e2967d8d204a65a98e2227404
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.publisher = zope.app.publisher:main
    #
    # Would create an entry point called zope.app.publisher that calls zope.app.publisher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.browsermenu
    - zope.browserpage
    - zope.browserresource
    - zope.component
    - zope.configuration
    - zope.datetime
    - zope.interface
    - zope.location
    - zope.ptresource
    - zope.publisher >=3.12
    - zope.schema
    - zope.security
    - zope.componentvocabulary

  run:
    - python
    - setuptools
    - zope.browsermenu
    - zope.browserpage
    - zope.browserresource
    - zope.component
    - zope.configuration
    - zope.datetime
    - zope.interface
    - zope.location
    - zope.ptresource
    - zope.publisher >=3.12
    - zope.schema
    - zope.security
    - zope.componentvocabulary

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.publisher
    - zope.app.publisher.browser
    - zope.app.publisher.interfaces
    - zope.app.publisher.tests
    - zope.app.publisher.xmlrpc
    - zope.app.publisher.xmlrpc.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.publisher/
  license: Zope Public License
  summary: 'Implementations and means for configuration of Zope 3-style views and resources.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
