package:
  name: django-nextpage
  version: "0.7"

source:
  fn: django-nextpage-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nextpage/django-nextpage-0.7.tar.gz
  md5: 692b6fc3a293c552772cfda7a1855318
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nextpage = django-nextpage:main
    #
    # Would create an entry point called django-nextpage that calls django-nextpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nextpage
    - nextpage.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tzangms/django-nextpage
  license: BSD
  summary: 'django-nextpage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
