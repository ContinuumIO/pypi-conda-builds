package:
  name: django-whippedcream
  version: "0.2.0"

source:
  fn: django-whippedcream-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-whippedcream/django-whippedcream-0.2.0.tar.gz
  md5: 4959122f97cd3a3775e6dfa3908f3904
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-whippedcream = django-whippedcream:main
    #
    # Would create an entry point called django-whippedcream that calls django-whippedcream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - whippedcream
    - whippedcream.runtests
    - whippedcream.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paulcwatts/django-whippedcream
  license: BSD License
  summary: 'Utilities that make tastypie taste better.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
