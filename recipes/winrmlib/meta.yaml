package:
  name: winrmlib
  version: "0.0.8"

source:
  fn: winrmlib-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/w/winrmlib/winrmlib-0.0.8.tar.gz
  md5: 1e7bdba5bd14e0857c2fcb04f69bd685
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - winrmlib = winrmlib:main
    #
    # Would create an entry point called winrmlib that calls winrmlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pyopenssl
    - xmltodict
    - ntlmlib

  run:
    - python
    - requests
    - pyopenssl
    - xmltodict
    - ntlmlib

test:
  # Python imports
  imports:
    - winrmlib
    - winrmlib.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/iclegg/winrmlib/
  license: MIT License
  summary: 'Python library for Windows Remote Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
