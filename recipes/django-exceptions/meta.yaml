package:
  name: django-exceptions
  version: "0.4"

source:
  fn: django-exceptions-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-exceptions/django-exceptions-0.4.tar.gz
  md5: dfab3124708d2cba1358538e756f95f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-exceptions = django-exceptions:main
    #
    # Would create an entry point called django-exceptions that calls django-exceptions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_exceptions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aRkadeFR/django-exceptions
  license: UNKNOWN
  summary: 'Django app to handle exceptions in a comfortable way'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
