package:
  name: collective.portlet.bookmarks
  version: "0.1"

source:
  fn: collective.portlet.bookmarks-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.bookmarks/collective.portlet.bookmarks-0.1.zip
  md5: f27fca222fe54d0190c717cbbffd051e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.bookmarks = collective.portlet.bookmarks:main
    #
    # Would create an entry point called collective.portlet.bookmarks that calls collective.portlet.bookmarks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.bookmarks
    - collective.portlet.bookmarks.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: GPL
  summary: 'A portlet that allows users to bookmark pages in a site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
