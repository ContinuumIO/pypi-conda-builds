package:
  name: django-guardianpie
  version: "0.0.3"

source:
  fn: django-guardianpie-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-guardianpie/django-guardianpie-0.0.3.tar.gz
  md5: 7be428d82f05e7389fe9f28d06a74f9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-guardianpie = django-guardianpie:main
    #
    # Would create an entry point called django-guardianpie that calls django-guardianpie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-guardian
    - django-tastypie

  run:
    - python
    - django-guardian
    - django-tastypie

test:
  # Python imports
  imports:
    - guardianpie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/airtonix/django-guardianpie
  license: UNKNOWN
  summary: 'Tastypie authorization class that handles guardian permissions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
