package:
  name: sleipnir.heuristics
  version: "0.1.90"

source:
  fn: sleipnir.heuristics-0.1.90.tar.gz
  url: https://pypi.python.org/packages/source/s/sleipnir.heuristics/sleipnir.heuristics-0.1.90.tar.gz
  md5: fcd73574060fd0a2216ca336cb568555
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sleipnir.heuristics = sleipnir.heuristics:main
    #
    # Would create an entry point called sleipnir.heuristics that calls sleipnir.heuristics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sleipnir.core >=0.1rc1
    - oset

  run:
    - python
    - sleipnir.core >=0.1rc1
    - oset

test:
  # Python imports
  imports:
    - sleipnir
    - sleipnir.heuristics
    - sleipnir.heuristics.helpers
    - sleipnir.heuristics.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sleipnir.liberalia.net/
  license: GNU General Public License (GPL)
  summary: 'A Heuristics package to develop of VRP related problems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
