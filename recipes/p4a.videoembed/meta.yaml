package:
  name: p4a.videoembed
  version: "1.3"

source:
  fn: p4a.videoembed-1.3.zip
  url: https://pypi.python.org/packages/source/p/p4a.videoembed/p4a.videoembed-1.3.zip
  md5: 983c6b8223c98ed52a6ed4bfa514b09f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.videoembed = p4a.videoembed:main
    #
    # Would create an entry point called p4a.videoembed that calls p4a.videoembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - p4a
    - p4a.videoembed
    - p4a.videoembed.providers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plone4artists.org/svn/projects/p4a.videoembed
  license: GNU General Public License (GPL)
  summary: 'A registry and adapters for converting urls for various video sharing sites into embed codes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
