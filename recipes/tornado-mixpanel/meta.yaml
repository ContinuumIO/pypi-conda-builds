package:
  name: tornado-mixpanel
  version: "0.2"

source:
  fn: tornado-mixpanel-0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-mixpanel/tornado-mixpanel-0.2.tar.gz
  md5: 664b84163c46aa895a4cbe34f806490b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-mixpanel = tornado-mixpanel:main
    #
    # Would create an entry point called tornado-mixpanel that calls tornado-mixpanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado ==4.0.2
    - ujson

  run:
    - python
    - tornado ==4.0.2
    - ujson

test:
  # Python imports
  imports:
    - tornado_mixpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alejandrobernardis/tornado-mixpanel
  license: MIT License
  summary: 'An async client for mixpanel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
