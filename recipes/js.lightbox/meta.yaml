package:
  name: js.lightbox
  version: "0.1.1"

source:
  fn: js.lightbox-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.lightbox/js.lightbox-0.1.1.tar.gz
  md5: befb61a69b315999b22a722606962410
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.lightbox = js.lightbox:main
    #
    # Would create an entry point called js.lightbox that calls js.lightbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.lightbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amleczko/js.lightbox
  license: GPL
  summary: 'fanstatic jquery lightbox.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
