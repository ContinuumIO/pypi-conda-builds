package:
  name: pyconfigurator
  version: "0.4.1"

source:
  fn: pyconfigurator-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyconfigurator/pyconfigurator-0.4.1.tar.gz
  md5: 91267db85f31bab308b2720906e8d4b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyconfigurator = pyconfigurator:main
    #
    # Would create an entry point called pyconfigurator that calls pyconfigurator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - configurator
    - pyconfigurator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charsmith/configurator
  license: Apache Software License
  summary: 'A library for easy configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
