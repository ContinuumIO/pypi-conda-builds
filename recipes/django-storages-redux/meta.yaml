package:
  name: django-storages-redux
  version: "1.2.3"

source:
  fn: django-storages-redux-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-storages-redux/django-storages-redux-1.2.3.tar.gz
  md5: 22d45b286268251b7f40340b237a0759
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-storages-redux = django-storages-redux:main
    #
    # Would create an entry point called django-storages-redux that calls django-storages-redux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - storages
    - storages.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jschneier/django-storages
  license: BSD License
  summary: 'Support for many storages (S3, MogileFS, etc) in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
