package:
  name: isotoma.recipe.squid
  version: "0.0.10"

source:
  fn: isotoma.recipe.squid-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.squid/isotoma.recipe.squid-0.0.10.tar.gz
  md5: c5836761f681db55e60e2afa5930296e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.squid = isotoma.recipe.squid:main
    #
    # Would create an entry point called isotoma.recipe.squid that calls isotoma.recipe.squid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - cheetah
    - isotoma.recipe.gocaptain

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - cheetah
    - isotoma.recipe.gocaptain

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.squid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/isotoma/isotoma.recipe.squid
  license: Apache Software License
  summary: 'Set up squid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
