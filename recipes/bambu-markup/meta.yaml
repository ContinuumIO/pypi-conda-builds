package:
  name: bambu-markup
  version: "2.0"

source:
  fn: bambu-markup-2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-markup/bambu-markup-2.0.tar.gz
  md5: b4dcff69d8f1b171f3790cf95c922ec2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-markup = bambu-markup:main
    #
    # Would create an entry point called bambu-markup that calls bambu-markup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - markdown
    - docutils

  run:
    - python
    - django >=1.4
    - markdown
    - docutils

test:
  # Python imports
  imports:
    - bambu_markup
    - bambu_markup.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-markup
  license: UNKNOWN
  summary: "Replacements for Django's Markdown and reStructuredText support"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
