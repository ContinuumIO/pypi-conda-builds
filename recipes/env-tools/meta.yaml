package:
  name: env-tools
  version: "2.0.0"

source:
  fn: env-tools-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/e/env-tools/env-tools-2.0.0.tar.gz
  md5: 147d06baa3e83fc92e0f949fcde7baf6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - env-tools = env-tools:main
    #
    # Would create an entry point called env-tools that calls env-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tini >=3.0.0

  run:
    - python
    - tini >=3.0.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/beaugunderson/python-env-tools
  license: MIT License
  summary: 'Tools for using .env files in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
