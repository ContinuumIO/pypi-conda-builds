package:
  name: morsecode
  version: "0.0.1"

source:
  fn: MorseCode-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/M/MorseCode/MorseCode-0.0.1.tar.gz
  md5: ea02044dd1f36fb2442c37133855fdf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - morsecode = morsecode:main
    #
    # Would create an entry point called morsecode that calls morsecode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - scikits.audiolab

  run:
    - python
    - numpy
    - scikits.audiolab

test:
  # Python imports
  imports:
    - morse
    - morsecode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/MorseCode/
  license: LICENSE.txt
  summary: 'Converting text to morse code pulses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
