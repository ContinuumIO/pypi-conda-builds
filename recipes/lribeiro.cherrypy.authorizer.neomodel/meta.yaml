package:
  name: lribeiro.cherrypy.authorizer.neomodel
  version: "1.0.1"

source:
  fn: lribeiro.cherrypy.authorizer.neomodel-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lribeiro.cherrypy.authorizer.neomodel/lribeiro.cherrypy.authorizer.neomodel-1.0.1.tar.gz
  md5: 7347d8d2d6ecee078dc234b46f40c203
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lribeiro.cherrypy.authorizer.neomodel = lribeiro.cherrypy.authorizer.neomodel:main
    #
    # Would create an entry point called lribeiro.cherrypy.authorizer.neomodel that calls lribeiro.cherrypy.authorizer.neomodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy
    - neomodel
    - bcrypt
    - lribeiro.cherrypy.authorizer

  run:
    - python
    - cherrypy
    - neomodel
    - bcrypt
    - lribeiro.cherrypy.authorizer

test:
  # Python imports
  imports:
    - lribeiro
    - lribeiro.cherrypy
    - lribeiro.cherrypy.authorizer
    - lribeiro.cherrypy.authorizer.neomodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/livioribeiro/cherrypy-authorizer-neomodel
  license: BSD License
  summary: 'Basic user model, authenticator and authorizer for lribeiro.cherrypy.authorizer and neomodel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
