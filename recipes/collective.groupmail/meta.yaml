package:
  name: collective.groupmail
  version: "1.1"

source:
  fn: collective.groupmail-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.groupmail/collective.groupmail-1.1.zip
  md5: a4c6dcc79d6ea9cba1e1c5774c899e3c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.groupmail = collective.groupmail:main
    #
    # Would create an entry point called collective.groupmail that calls collective.groupmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

test:
  # Python imports
  imports:
    - collective
    - collective.groupmail
    - collective.groupmail.browser
    - collective.groupmail.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Send feedback email to groups in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
