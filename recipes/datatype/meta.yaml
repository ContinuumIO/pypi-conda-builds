package:
  name: datatype
  version: "0.8.5"

source:
  fn: datatype-0.8.5.tar.gz
  url: https://pypi.python.org/packages/source/d/datatype/datatype-0.8.5.tar.gz
  md5: b790a7609fa7025bd8e2c4b1a645b639
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datatype = datatype:main
    #
    # Would create an entry point called datatype that calls datatype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - pytest-cov
    - doctools
    - mock

  run:
    - python
    - pytest
    - pytest-cov
    - doctools
    - mock

test:
  # Python imports
  imports:
    - datatype

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/awagner83/datatype
  license: BSD3
  summary: 'Anonymous datatype validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
