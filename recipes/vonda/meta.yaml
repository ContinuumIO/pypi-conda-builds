package:
  name: vonda
  version: "0.6"

source:
  fn: VoNDA-0.6.tar.bz2
  url: https://pypi.python.org/packages/source/V/VoNDA/VoNDA-0.6.tar.bz2
  md5: 99e6d44d26643730b74cc7f2785d9f17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vonda = vonda:main
    #
    # Would create an entry point called vonda that calls vonda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - vonda
    - vonda.colormaps
    - vonda.data_examples
    - vonda.models
    - vonda.modules
    - vonda.networks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://vonda.sourceforge.net
  license: BSD License
  summary: 'VoNDA (Visualization of Network DAta) allows for interactive visualization of heterogeneous data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
