package:
  name: duckduckgo
  version: "0.1"

source:
  fn: duckduckgo-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/duckduckgo/duckduckgo-0.1.tar.gz
  md5: abdfbff33454f4834800eb04fa1cf923
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - duckduckgo = duckduckgo:main
    #
    # Would create an entry point called duckduckgo that calls duckduckgo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - duckduckgo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mikejs/python-duckduckgo/
  license: BSD License
  summary: 'Library for querying the Duck Duck Go API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
