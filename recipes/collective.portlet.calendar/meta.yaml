package:
  name: collective.portlet.calendar
  version: "1.0b2"

source:
  fn: collective.portlet.calendar-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.calendar/collective.portlet.calendar-1.0b2.tar.gz
  md5: 4e3682417d53a11ef410ff1a4143f430
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.calendar = collective.portlet.calendar:main
    #
    # Would create an entry point called collective.portlet.calendar that calls collective.portlet.calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.calendar
    - collective.portlet.calendar.browser
    - collective.portlet.calendar.docs
    - collective.portlet.calendar.tests
    - collective.portlet.calendar.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.portlet.calendar
  license: GNU General Public License (GPL)
  summary: 'Extended Calendar Portlet: A configurable implementation of a Calendar Portlet for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
