package:
  name: dailymotion
  version: "0.1.0"

source:
  fn: dailymotion-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dailymotion/dailymotion-0.1.0.tar.gz
  md5: a7ad4d094c50ec37584d7dcdc30e8d02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dailymotion = dailymotion:main
    #
    # Would create an entry point called dailymotion that calls dailymotion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pycurl

  run:
    - python
    - requests
    - pycurl

test:
  # Python imports
  imports:
    - dailymotion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dailymotion/dailymotion-sdk-python
  license: Apache Software License
  summary: 'Dailymotion API SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
