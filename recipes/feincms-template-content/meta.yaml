package:
  name: feincms-template-content
  version: "0.0.5"

source:
  fn: FeinCMS-Template-Content-0.0.5.zip
  url: https://pypi.python.org/packages/source/F/FeinCMS-Template-Content/FeinCMS-Template-Content-0.0.5.zip
  md5: ff97293826e1f8f836e0be56258f9993
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-template-content = feincms-template-content:main
    #
    # Would create an entry point called feincms-template-content that calls feincms-template-content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - feincms

  run:
    - python
    - django
    - feincms

test:
  # Python imports
  imports:
    - feincms_template_content
    - feincms_template_content.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RichardOfWard/feincms-template-content
  license: MIT License
  summary: 'FeinCMS content types with (optionally) selectable templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
