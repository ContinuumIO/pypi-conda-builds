package:
  name: antipathy
  version: "0.82.1"

source:
  fn: antipathy-0.82.1.tar.gz
  url: https://pypi.python.org/packages/source/a/antipathy/antipathy-0.82.1.tar.gz
  md5: 58bafdff460b092f71cc76b7b06d97b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - antipathy = antipathy:main
    #
    # Would create an entry point called antipathy that calls antipathy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - antipathy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/antipathy
  license: BSD License
  summary: 'oo view of file paths and names, subclassed from bytes/str/unicode'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
