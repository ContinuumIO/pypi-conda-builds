package:
  name: lscolumns
  version: "0.1.0"

source:
  fn: lscolumns-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lscolumns/lscolumns-0.1.0.tar.gz
  md5: f06cb59e1d7d7819b9ac20d98b035c73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lscolumns = lscolumns:main
    #
    # Would create an entry point called lscolumns that calls lscolumns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lscolumns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bendmorris/lscolumns
  license: UNKNOWN
  summary: 'pretty-print columns of data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
