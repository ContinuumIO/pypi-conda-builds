package:
  name: django_rundirect
  version: "0.1.1"

source:
  fn: django_rundirect-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_rundirect/django_rundirect-0.1.1.tar.gz
  md5: 6e8088f4267b5c85443e5a957fce171e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_rundirect = django_rundirect:main
    #
    # Would create an entry point called django_rundirect that calls django_rundirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_rundirect
    - django_rundirect.management
    - django_rundirect.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitcafe.com/yuexue/django_rundirect
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
