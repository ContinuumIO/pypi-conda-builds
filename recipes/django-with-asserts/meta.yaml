package:
  name: django-with-asserts
  version: "0.0.1"

source:
  fn: django-with-asserts-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-with-asserts/django-with-asserts-0.0.1.tar.gz
  md5: a6da30e23adb54f41a1f4297b0455980
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-with-asserts = django-with-asserts:main
    #
    # Would create an entry point called django-with-asserts that calls django-with-asserts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - cssselect

  run:
    - python
    - lxml
    - cssselect

test:
  # Python imports
  imports:
    - with_asserts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-with-asserts.readthedocs.org
  license: BSD License
  summary: 'Helpers for testing Django using context managers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
