package:
  name: deform_extensions
  version: "0.2"

source:
  fn: deform_extensions-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/deform_extensions/deform_extensions-0.2.tar.gz
  md5: e3e0b04636dc1289a5bf25ad72b2ee8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deform_extensions = deform_extensions:main
    #
    # Would create an entry point called deform_extensions that calls deform_extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - deform
    - js.deform
    - js.jquery_timepicker_addon

  run:
    - python
    - deform
    - js.deform
    - js.jquery_timepicker_addon

test:
  # Python imports
  imports:
    - deform_extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/majerteam/deform_extensions
  license: GNU General Public License v3 (GPLv3)
  summary: 'Usefull tools for making grid/accordion layouted forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
