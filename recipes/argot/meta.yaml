package:
  name: argot
  version: "0.6"

source:
  fn: Argot-0.6.tar.gz
  url: https://pypi.python.org/packages/source/A/Argot/Argot-0.6.tar.gz
  md5: 797c5a37ef01537dc855adad6c37dc79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argot = argot:main
    #
    # Would create an entry point called argot that calls argot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown
    - pygments

  run:
    - python
    - markdown
    - pygments

test:
  # Python imports
  imports:
    - argot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jmoiron/argot/
  license: MIT License
  summary: 'argot text markup -- a markdown dialect'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
