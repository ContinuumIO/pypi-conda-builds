package:
  name: django-robust-i18n-urls
  version: "1.0.1rc1"

source:
  fn: django-robust-i18n-urls-1.0.1rc1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-robust-i18n-urls/django-robust-i18n-urls-1.0.1rc1.tar.gz
  md5: 77d6aa5f40cb41fac9e0b989f39ad9b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-robust-i18n-urls = django-robust-i18n-urls:main
    #
    # Would create an entry point called django-robust-i18n-urls that calls django-robust-i18n-urls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0

  run:
    - python
    - django >=1.5.0

test:
  # Python imports
  imports:
    - robust_urls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://karolmajta.com/django-robust-i18n-urls/
  license: MIT
  summary: 'Robust internationalized urls implementation for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
