package:
  name: extensionclass
  version: "4.1.2"

source:
  fn: ExtensionClass-4.1.2.tar.gz
  url: https://pypi.python.org/packages/source/E/ExtensionClass/ExtensionClass-4.1.2.tar.gz
  md5: 504c26677f308fd52e04ed2d5e69a5fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extensionclass = extensionclass:main
    #
    # Would create an entry point called extensionclass that calls extensionclass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ComputedAttribute
    - ExtensionClass
    - MethodObject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ExtensionClass
  license: Zope Public License
  summary: 'Metaclass for subclassable extension types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
