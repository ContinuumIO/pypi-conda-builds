package:
  name: django-assume
  version: "0.2.1"

source:
  fn: django-assume-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-assume/django-assume-0.2.1.tar.gz
  md5: 82953a051747de01b9a3966870f92524
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-assume = django-assume:main
    #
    # Would create an entry point called django-assume that calls django-assume.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - assume

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bryanchow/django-assume
  license: WTFPL
  summary: 'Django app that allows administrators to log in to user accounts without having to provide a password'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
