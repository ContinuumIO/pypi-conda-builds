package:
  name: zope.filerepresentation
  version: "4.1.0"

source:
  fn: zope.filerepresentation-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.filerepresentation/zope.filerepresentation-4.1.0.tar.gz
  md5: c0af695936e4f0b0d2ee21ee815b7ed6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.filerepresentation = zope.filerepresentation:main
    #
    # Would create an entry point called zope.filerepresentation that calls zope.filerepresentation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - zope
    - zope.filerepresentation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.filerepresentation
  license: Zope Public License
  summary: 'File-system Representation Interfaces'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
