package:
  name: tornado-botocore
  version: "0.1.6"

source:
  fn: tornado-botocore-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-botocore/tornado-botocore-0.1.6.tar.gz
  md5: 2b1b7416541a54b739e55141d3211787
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-botocore = tornado-botocore:main
    #
    # Would create an entry point called tornado-botocore that calls tornado-botocore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - botocore <1,>=0.65.0
    - tornado

  run:
    - python
    - botocore <1,>=0.65.0
    - tornado

test:
  # Python imports
  imports:
    - tornado_botocore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nanvel/tornado-botocore
  license: The MIT License
  summary: "Use tornado's AsyncHTTPClient inside botocore.\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
