package:
  name: django-wabbajack
  version: "0.1.5"

source:
  fn: django-wabbajack-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wabbajack/django-wabbajack-0.1.5.tar.gz
  md5: 8c71423bd94e82d7757d93ef3b76dda9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wabbajack = django-wabbajack:main
    #
    # Would create an entry point called django-wabbajack that calls django-wabbajack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - wabbajack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/histrio/django-wabbajack
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
