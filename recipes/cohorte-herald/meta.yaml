package:
  name: cohorte-herald
  version: "0.0.3"

source:
  fn: Cohorte-Herald-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/C/Cohorte-Herald/Cohorte-Herald-0.0.3.tar.gz
  md5: 04fd5798385b72740f1eea0daaebe8ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cohorte-herald = cohorte-herald:main
    #
    # Would create an entry point called cohorte-herald that calls cohorte-herald.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipopo >=0.5.7
    - sleekxmpp >=1.3.1
    - requests >=2.3.0

  run:
    - python
    - ipopo >=0.5.7
    - sleekxmpp >=1.3.1
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - herald
    - herald.probe
    - herald.remote
    - herald.transports
    - herald.transports.http
    - herald.transports.xmpp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/isandlaTech/cohorte-herald
  license: Apache Software License
  summary: 'An easy-to-use messaging framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
