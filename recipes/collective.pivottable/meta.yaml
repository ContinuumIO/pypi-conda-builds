package:
  name: collective.pivottable
  version: "1.1.1dev-r97462"

source:
  fn: collective.pivottable-1.1.1dev-r97462.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.pivottable/collective.pivottable-1.1.1dev-r97462.tar.gz
  md5: 0472b6c3886fb57a2bf847fba49d7060
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pivottable = collective.pivottable:main
    #
    # Would create an entry point called collective.pivottable that calls collective.pivottable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.pivottable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.plone.org/collective/browser/collective.pivottable
  license: GPL
  summary: 'A class to generate Pivot Tables based on Objects, using your attributes and/or methods, that can use Zope Acquisition to get those.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
