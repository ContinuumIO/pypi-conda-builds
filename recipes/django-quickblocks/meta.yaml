package:
  name: django-quickblocks
  version: "0.9.1"

source:
  fn: django-quickblocks-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quickblocks/django-quickblocks-0.9.1.tar.gz
  md5: fc830cd17087ffc2056073f839a27e97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quickblocks = django-quickblocks:main
    #
    # Would create an entry point called django-quickblocks that calls django-quickblocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - pil >=1.1.7
    - smartmin
    - django-reversion >=1.3.3

  run:
    - python
    - django >=1.3
    - pil >=1.1.7
    - smartmin
    - django-reversion >=1.3.3

test:
  # Python imports
  imports:
    - django_quickblocks
    - django_quickblocks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nyaruka.com/#open
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
