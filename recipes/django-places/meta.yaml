package:
  name: django-places
  version: "0.1"

source:
  fn: django-places-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-places/django-places-0.1.tar.gz
  md5: 60b97044a319fa37a39a634347681361
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-places = django-places:main
    #
    # Would create an entry point called django-places that calls django-places.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - geopy

  run:
    - python
    - geopy

test:
  # Python imports
  imports:
    - places

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/shelfworthy/django-places
  license: BSD License
  summary: 'Place or Location management app for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
