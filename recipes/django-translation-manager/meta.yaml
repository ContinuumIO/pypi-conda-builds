package:
  name: django-translation-manager
  version: "0.2.5"

source:
  fn: django-translation-manager-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-translation-manager/django-translation-manager-0.2.5.tar.gz
  md5: 0842872a83d912e95544df98c3055faa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-translation-manager = django-translation-manager:main
    #
    # Would create an entry point called django-translation-manager that calls django-translation-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - polib
    - django >=1.2

  run:
    - python
    - polib
    - django >=1.2

test:
  # Python imports
  imports:
    - translation_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/COEXCZ/django-translation-manager
  license: CC BY-NC 3.0
  summary: 'Django app for managing translations from admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
