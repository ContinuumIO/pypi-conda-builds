package:
  name: flask-zurb-foundation
  version: "0.2.1"

source:
  fn: Flask-Zurb-Foundation-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Zurb-Foundation/Flask-Zurb-Foundation-0.2.1.tar.gz
  md5: 7e096c621a23d98387536f45e5342dc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-zurb-foundation = flask-zurb-foundation:main
    #
    # Would create an entry point called flask-zurb-foundation that calls flask-zurb-foundation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_zurb_foundation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ondoheer/flask-zurb-foundation
  license: BSD License
  summary: 'A Foundation Wrapper for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
