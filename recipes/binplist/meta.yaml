package:
  name: binplist
  version: "0.1.4"

source:
  fn: binplist-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/b/binplist/binplist-0.1.4.tar.gz
  md5: 5c4788a9731b67487790109910380699
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - binplist = binplist:main
    #
    # Would create an entry point called binplist that calls binplist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - binplist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/binplist
  license: Apache Software License
  summary: 'A binary plist parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
