package:
  name: attract-sdk
  version: "0.0.1"

source:
  fn: attract-sdk-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/attract-sdk/attract-sdk-0.0.1.tar.gz
  md5: bc5a6d487deed4ce224e27214a3b31dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - attract-sdk = attract-sdk:main
    #
    # Would create an entry point called attract-sdk that calls attract-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=1.0.0
    - six >=1.0.0
    - pyopenssl >=0.14

  run:
    - python
    - requests >=1.0.0
    - six >=1.0.0
    - pyopenssl >=0.14

test:
  # Python imports
  imports:
    - attractsdk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fsiddi/Attract-Python-SDK
  license: BSD License
  summary: 'The Attract REST SDK provides Python APIs to communicate to the Attract webservices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
