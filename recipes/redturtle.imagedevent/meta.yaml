package:
  name: redturtle.imagedevent
  version: "1.0.2"

source:
  fn: redturtle.imagedevent-1.0.2.zip
  url: https://pypi.python.org/packages/source/r/redturtle.imagedevent/redturtle.imagedevent-1.0.2.zip
  md5: 17522950f900d23f5c39bfb1714f09a2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.imagedevent = redturtle.imagedevent:main
    #
    # Would create an entry point called redturtle.imagedevent that calls redturtle.imagedevent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.imagedevent
    - redturtle.imagedevent.Extensions
    - redturtle.imagedevent.browser
    - redturtle.imagedevent.content
    - redturtle.imagedevent.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.imagedevent
  license: GNU General Public License (GPL)
  summary: 'A replacement of the Event content type for Plone, with image field and separate keyword and event type fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
