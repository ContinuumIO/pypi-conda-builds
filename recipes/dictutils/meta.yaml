package:
  name: dictutils
  version: "0.1.0"

source:
  fn: dictutils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dictutils/dictutils-0.1.0.tar.gz
  md5: a628bb6c0880b39daaf6a96bb234f9db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictutils = dictutils:main
    #
    # Would create an entry point called dictutils that calls dictutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dictutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rconradharris/dictutils
  license: MIT License
  summary: 'Utilities for working with Python dictionaries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
