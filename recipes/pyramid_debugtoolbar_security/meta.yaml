package:
  name: pyramid_debugtoolbar_security
  version: "0.1.2"

source:
  fn: pyramid_debugtoolbar_security-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_debugtoolbar_security/pyramid_debugtoolbar_security-0.1.2.tar.gz
  md5: 5e558853c2eb0725d49fdb9ba0929793
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_debugtoolbar_security = pyramid_debugtoolbar_security:main
    #
    # Would create an entry point called pyramid_debugtoolbar_security that calls pyramid_debugtoolbar_security.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - pyramid_debugtoolbar

  run:
    - python
    - pyramid
    - pyramid_debugtoolbar

test:
  # Python imports
  imports:
    - pyramid_debugtoolbar_security

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/shimizukawa/pyramid_debugtoolbar_security
  license: Apache Software License
  summary: 'security panel for pyramid_debugtoolbar (principals, context acl, view permission)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
