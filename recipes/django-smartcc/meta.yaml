package:
  name: django-smartcc
  version: "0.1.2"

source:
  fn: django-smartcc-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smartcc/django-smartcc-0.1.2.tar.gz
  md5: 9a3417b8ebd19e4e65ec94c469011a26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smartcc = django-smartcc:main
    #
    # Would create an entry point called django-smartcc that calls django-smartcc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - smart_cache_control

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/koalalorenzo/django-smartcc
  license: GNU General Public License v2 (GPLv2)
  summary: 'A django Middleware that will help to set cache-control header on the views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
