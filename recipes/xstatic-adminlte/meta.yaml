package:
  name: xstatic-adminlte
  version: "2.1.0.1"

source:
  fn: XStatic-AdminLTE-2.1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-AdminLTE/XStatic-AdminLTE-2.1.0.1.tar.gz
  md5: dbe0a3d2bc5da76fe57e8f4aefa065fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-adminlte = xstatic-adminlte:main
    #
    # Would create an entry point called xstatic-adminlte that calls xstatic-adminlte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.adminlte

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/almasaeed2010/AdminLTE
  license: (same as AdminLTE)
  summary: 'AdminLTE 2.1.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
