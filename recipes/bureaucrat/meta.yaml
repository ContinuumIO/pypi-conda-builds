package:
  name: bureaucrat
  version: "0.3.3"

source:
  fn: bureaucrat-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bureaucrat/bureaucrat-0.3.3.tar.gz
  md5: b02669c910d3f31e8000715bc50a2030
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bureaucrat = bureaucrat:main
    #
    # Would create an entry point called bureaucrat that calls bureaucrat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bureaucrat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adlibre/python-bureaucrat
  license: BSD License
  summary: 'Procfile and Deployfile process manager for virtual environments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
