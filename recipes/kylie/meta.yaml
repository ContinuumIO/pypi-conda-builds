package:
  name: kylie
  version: "0.2.0"

source:
  fn: kylie-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kylie/kylie-0.2.0.tar.gz
  md5: 5c70133f733dc04e2d0f3255f5065ee2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kylie = kylie:main
    #
    # Would create an entry point called kylie that calls kylie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kylie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/judy2k/kylie
  license: BSD License
  summary: 'Kylie provides mappings between JSON data structures and Python objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
