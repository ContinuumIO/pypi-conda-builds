package:
  name: launchdcheckin
  version: "0.13.0.1"

source:
  fn: launchdcheckin-0.13.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/launchdcheckin/launchdcheckin-0.13.0.1.tar.gz
  md5: 6d9d1d1ccc04c2881243593ac78581db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - launchdcheckin = launchdcheckin:main
    #
    # Would create an entry point called launchdcheckin that calls launchdcheckin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi

  run:
    - python
    - cffi

test:
  # Python imports
  imports:
    - launchdcheckin
    - twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/habnabit/launchdcheckin
  license: ISC License (ISCL)
  summary: 'launchd checkin for python and twisted'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
