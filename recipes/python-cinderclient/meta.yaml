package:
  name: python-cinderclient
  version: "1.2.1"

source:
  fn: python-cinderclient-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-cinderclient/python-cinderclient-1.2.1.tar.gz
  md5: 41d3eea653e99a3aabdc0cd01583899d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-cinderclient = python-cinderclient:main
    #
    # Would create an entry point called python-cinderclient that calls python-cinderclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Block Storage API Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
