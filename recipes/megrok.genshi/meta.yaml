package:
  name: megrok.genshi
  version: "1.0"

source:
  fn: megrok.genshi-1.0.zip
  url: https://pypi.python.org/packages/source/m/megrok.genshi/megrok.genshi-1.0.zip
  md5: deefe3bb31b02c00cedd108525f5f370
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.genshi = megrok.genshi:main
    #
    # Would create an entry point called megrok.genshi that calls megrok.genshi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - genshi

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - genshi

test:
  # Python imports
  imports:
    - megrok
    - megrok.genshi
    - megrok.genshi.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/megrok.genshi/
  license: Zope Public License
  summary: 'Genshi integration in Grok'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
