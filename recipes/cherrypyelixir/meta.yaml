package:
  name: cherrypyelixir
  version: "0.5.1"

source:
  fn: CherrypyElixir-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CherrypyElixir/CherrypyElixir-0.5.1.tar.gz
  md5: 746308d06c273e0cd9046b3a10d988c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherrypyelixir = cherrypyelixir:main
    #
    # Would create an entry point called cherrypyelixir that calls cherrypyelixir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7.0,<0.7.9
    - elixir >=0.7.0,<0.7.9

  run:
    - python
    - sqlalchemy >=0.7.0,<0.7.9
    - elixir >=0.7.0,<0.7.9

test:
  # Python imports
  imports:
    - CherrypyElixir
    - cherrypyelixir

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/cherrypyelixir
  license: UNKNOWN
  summary: 'Elixir plugin for cherrypy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
