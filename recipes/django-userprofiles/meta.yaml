package:
  name: django-userprofiles
  version: "0.4"

source:
  fn: django-userprofiles-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-userprofiles/django-userprofiles-0.4.tar.gz
  md5: 3156307f87952b72ea7c35bad3d84cec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-userprofiles = django-userprofiles:main
    #
    # Would create an entry point called django-userprofiles that calls django-userprofiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - userprofiles
    - userprofiles.contrib
    - userprofiles.contrib.accountverification
    - userprofiles.contrib.accountverification.tests
    - userprofiles.contrib.emailverification
    - userprofiles.contrib.emailverification.tests
    - userprofiles.contrib.profiles
    - userprofiles.contrib.profiles.tests
    - userprofiles.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stephrdev/django-userprofiles/
  license: BSD License
  summary: 'Registration, e-mail verifications and profiles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
