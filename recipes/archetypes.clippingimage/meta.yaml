package:
  name: archetypes.clippingimage
  version: "2.1"

source:
  fn: archetypes.clippingimage-2.1.zip
  url: https://pypi.python.org/packages/source/a/archetypes.clippingimage/archetypes.clippingimage-2.1.zip
  md5: a02e9e5d62382035f8ac0281c3c80d39
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.clippingimage = archetypes.clippingimage:main
    #
    # Would create an entry point called archetypes.clippingimage that calls archetypes.clippingimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.archetypes
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - products.archetypes
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.clippingimage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/archetypes/MoreFieldsAndWidgets/archetypes.clippingimage
  license: BSD License
  summary: 'Image field and/or patch with clipping support for Plone/Archetypes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
