package:
  name: requests-aliyun
  version: "0.2.2"

source:
  fn: requests-aliyun-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/r/requests-aliyun/requests-aliyun-0.2.2.tar.gz
  md5: b8755f4ca0d8c5f9f2a3e2251ef21de4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requests-aliyun = requests-aliyun:main
    #
    # Would create an entry point called requests-aliyun that calls requests-aliyun.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4

  run:
    - python
    - requests >=2.4

test:
  # Python imports
  imports:
    - aliyunauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/skylothar/requests-aliyun
  license: Apache Software License
  summary: 'authentication for aliyun service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
