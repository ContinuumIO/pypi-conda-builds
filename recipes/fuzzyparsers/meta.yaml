package:
  name: fuzzyparsers
  version: "0.9.0"

source:
  fn: fuzzyparsers-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fuzzyparsers/fuzzyparsers-0.9.0.tar.gz
  md5: 000d24d7c64843780896e116cc13bfb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuzzyparsers = fuzzyparsers:main
    #
    # Would create an entry point called fuzzyparsers that calls fuzzyparsers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fuzzyparsers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jbmohler/fuzzyparsers
  license: MIT License
  summary: 'A collection of free-form input parsers (with special focus on dates)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
