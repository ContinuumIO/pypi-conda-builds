package:
  name: inexactsearch
  version: "1.0.2"

source:
  fn: inexactsearch-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/inexactsearch/inexactsearch-1.0.2.tar.gz
  md5: 99d81c289383751922ff5c3155b2f219
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inexactsearch = inexactsearch:main
    #
    # Would create an entry point called inexactsearch that calls inexactsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - inexactsearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Fuzzy String search algorithm using Soundex for Indian language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
