package:
  name: collective.projekktor
  version: "0.2b1"

source:
  fn: collective.projekktor-0.2b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.projekktor/collective.projekktor-0.2b1.tar.gz
  md5: 81a83b5ac6162ff83d9a860faee5637d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.projekktor = collective.projekktor:main
    #
    # Would create an entry point called collective.projekktor that calls collective.projekktor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone >=4.0

  run:
    - python
    - setuptools
    - setuptools
    - plone >=4.0

test:
  # Python imports
  imports:
    - collective
    - collective.projekktor
    - collective.projekktor.browser
    - collective.projekktor.browser.controls
    - collective.projekktor.browser.portlets
    - collective.projekktor.browser.viewlets
    - collective.projekktor.browser.views
    - collective.projekktor.profiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/collective.projekktor
  license: GPL
  summary: 'Ploneised version of Projekktor html5 media player.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
