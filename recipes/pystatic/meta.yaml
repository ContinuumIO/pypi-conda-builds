package:
  name: pystatic
  version: "0.2.1"

source:
  fn: pystatic-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pystatic/pystatic-0.2.1.tar.gz
  md5: d10331e57fd886323ba0fc2d4a1fe87d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystatic = pystatic:main
    #
    # Would create an entry point called pystatic that calls pystatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.1
    - pyinotify >=0.8.9
    - pyyaml

  run:
    - python
    - django >=1.4.1
    - pyinotify >=0.8.9
    - pyyaml

test:
  # Python imports
  imports:
    - pystatic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vascop/pystatic
  license: LICENSE
  summary: 'Static Website Generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
