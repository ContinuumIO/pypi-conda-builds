package:
  name: flask-environments
  version: "0.1"

source:
  fn: Flask-Environments-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Environments/Flask-Environments-0.1.tar.gz
  md5: c4f303e9be23eaa0d560c987bae44464
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-environments = flask-environments:main
    #
    # Would create an entry point called flask-environments that calls flask-environments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pyyaml

  run:
    - python
    - flask
    - pyyaml

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/flask-environments/
  license: MIT License
  summary: 'Environment tools and configuration for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
