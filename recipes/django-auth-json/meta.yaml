package:
  name: django-auth-json
  version: "0.0.3"

source:
  fn: django-auth-json-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-json/django-auth-json-0.0.3.tar.gz
  md5: b800d2c07520d00d0007133a066aa7c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-json = django-auth-json:main
    #
    # Would create an entry point called django-auth-json that calls django-auth-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-render-json

  run:
    - python
    - django-render-json

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jarvys/django-auth-json
  license: UNKNOWN
  summary: 'replace the login url parameter of django auth decorator with json'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
