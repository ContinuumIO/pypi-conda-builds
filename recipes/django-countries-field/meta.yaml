package:
  name: django-countries-field
  version: "0.1.0"

source:
  fn: django-countries-field-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-countries-field/django-countries-field-0.1.0.tar.gz
  md5: 077ff6f0f5c918dbae7484012f6ae16c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-countries-field = django-countries-field:main
    #
    # Would create an entry point called django-countries-field that calls django-countries-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0,<1.6.0
    - django-bitfield >=1.6.0,<1.7.0
    - pycountry >=1.2.0,<1.3.0

  run:
    - python
    - django >=1.4.0,<1.6.0
    - django-bitfield >=1.6.0,<1.7.0
    - pycountry >=1.2.0,<1.3.0

test:
  # Python imports
  imports:
    - countries_field
    - countries_field.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/anatoliy-larin/django-countries-field
  license: UNKNOWN
  summary: 'CountriesField in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
