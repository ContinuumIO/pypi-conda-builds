package:
  name: django-labjs
  version: "0.2.0"

source:
  fn: django-labjs-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-labjs/django-labjs-0.2.0.tar.gz
  md5: e60c8ff862962027f4c57468a6bcea5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-labjs = django-labjs:main
    #
    # Would create an entry point called django-labjs that calls django-labjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf >=0.4
    - django-compressor >=0.9.2

  run:
    - python
    - django-appconf >=0.4
    - django-compressor >=0.9.2

test:
  # Python imports
  imports:
    - labJS
    - labJS.templatetags
    - test_project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lpomfrey/django-labjs
  license: BSD License
  summary: u'Django labjs templatetags.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
