package:
  name: django-crystal-small
  version: "2011.10.20"

source:
  fn: django-crystal-small-2011.10.20.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crystal-small/django-crystal-small-2011.10.20.tar.gz
  md5: 60c74d2e70b3fed49eb12947a4ee20eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crystal-small = django-crystal-small:main
    #
    # Would create an entry point called django-crystal-small that calls django-crystal-small.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django_crystal_small

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.everaldo.com/crystal/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: "Everaldo's Crystal icons (small sizes) bundled for direct consumption from Django applications"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
