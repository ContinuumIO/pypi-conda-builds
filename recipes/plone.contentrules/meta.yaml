package:
  name: plone.contentrules
  version: "2.0.4"

source:
  fn: plone.contentrules-2.0.4.zip
  url: https://pypi.python.org/packages/source/p/plone.contentrules/plone.contentrules-2.0.4.zip
  md5: 21b189c2139ec7baea63a1e7ee187c55
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.contentrules = plone.contentrules:main
    #
    # Would create an entry point called plone.contentrules that calls plone.contentrules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.component
    - zope.componentvocabulary
    - zope.configuration
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.testing

  run:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.component
    - zope.componentvocabulary
    - zope.configuration
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.testing

test:
  # Python imports
  imports:
    - plone
    - plone.contentrules
    - plone.contentrules.engine
    - plone.contentrules.rule
    - plone.contentrules.rule.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.contentrules
  license: GNU General Public License (GPL)
  summary: 'Plone ContentRules Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
