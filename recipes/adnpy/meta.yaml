package:
  name: adnpy
  version: "0.3.8"

source:
  fn: adnpy-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/a/adnpy/adnpy-0.3.8.tar.gz
  md5: d508a4d9f2c18d128b966d07efbb3ce7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adnpy = adnpy:main
    #
    # Would create an entry point called adnpy that calls adnpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=1.5
    - requests >=2.0.1

  run:
    - python
    - python-dateutil >=1.5
    - requests >=2.0.1

test:
  # Python imports
  imports:
    - adnpy
    - adnpy.recipes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/appdotnet/adnpy
  license: MIT
  summary: 'App.net API library for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
