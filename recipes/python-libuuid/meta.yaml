package:
  name: python-libuuid
  version: "0.9.0"

source:
  fn: python-libuuid-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libuuid/python-libuuid-0.9.0.tar.gz
  md5: fd0aea7043ee743dbe64be8eb31da89a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libuuid = python-libuuid:main
    #
    # Would create an entry point called python-libuuid that calls python-libuuid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - libuuid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dln/python-libuuid/
  license: BSD License
  summary: 'C Extension for faster UUID generation using libuuid.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
