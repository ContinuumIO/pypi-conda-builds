package:
  name: collective.footerportletmanager
  version: "1.0"

source:
  fn: collective.footerportletmanager-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.footerportletmanager/collective.footerportletmanager-1.0.zip
  md5: 3deaa160e1720a4c63e66ee59bb3c55a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.footerportletmanager = collective.footerportletmanager:main
    #
    # Would create an entry point called collective.footerportletmanager that calls collective.footerportletmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.footerportletmanager
    - collective.footerportletmanager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.footerportletmanager
  license: GPL
  summary: 'Footer portlet manager for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
