package:
  name: apibee
  version: "0.2.2"

source:
  fn: apibee-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/a/apibee/apibee-0.2.2.tar.gz
  md5: d6a8f1ca4a4dd630b39866593fda1275
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apibee = apibee:main
    #
    # Would create an entry point called apibee that calls apibee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - restkit >=3.3.0

  run:
    - python
    - restkit >=3.3.0

test:
  # Python imports
  imports:
    - apibee

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/namlook/apibee
  license: Apache Software License
  summary: 'A dynamic api client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
