package:
  name: caktus-sphinx-theme
  version: "0.1.0"

source:
  fn: caktus-sphinx-theme-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/caktus-sphinx-theme/caktus-sphinx-theme-0.1.0.tar.gz
  md5: a73bcc62e4bd0e07ce33a83e83a1527c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caktus-sphinx-theme = caktus-sphinx-theme:main
    #
    # Would create an entry point called caktus-sphinx-theme that calls caktus-sphinx-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.1

  run:
    - python
    - sphinx >=1.1

test:
  # Python imports
  imports:
    - caktus_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caktus/caktus-sphinx-theme/
  license: BSD License
  summary: u'Custom Sphinx theme for projects written and maintained by Caktus Consulting Group.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
