package:
  name: jinja2compact
  version: "1.0"

source:
  fn: jinja2compact-1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja2compact/jinja2compact-1.0.tar.gz
  md5: a5bb8535d95e3def3d38d11cd09fbf29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja2compact = jinja2compact:main
    #
    # Would create an entry point called jinja2compact that calls jinja2compact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - jinja2compact

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sadimusi/jinja2compact
  license: WTFPL
  summary: 'Reduce whitespace in jinja2 templates at compile time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
