package:
  name: livescript
  version: "1.0.0"

source:
  fn: livescript-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/livescript/livescript-1.0.0.tar.gz
  md5: 2811d793ff5714973024cf06864bdd70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - livescript = livescript:main
    #
    # Would create an entry point called livescript that calls livescript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyexecjs

  run:
    - python
    - pyexecjs

test:
  # Python imports
  imports:
    - livescript

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gkovacs/python-livescript
  license: MIT License
  summary: 'Call LiveScript from Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
