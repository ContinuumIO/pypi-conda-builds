package:
  name: eden
  version: "2.1.13.Modules-.demo.on.YouTube"

source:
  fn: Eden-2.1.13.Modules-.demo.on.YouTube.zip
  url: https://pypi.python.org/packages/source/E/Eden/Eden-2.1.13.Modules-.demo.on.YouTube.zip
  md5: e31f72c88b01ceb5144c5b9347c0a0ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eden = eden:main
    #
    # Would create an entry point called eden that calls eden.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kivy ==1.8

  run:
    - python
    - kivy ==1.8

test:
  # Python imports
  imports:
    - eden

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/JdeH/Eden/
  license: qQuickLicence
  summary: 'Eden - Event Driven Evaluation Nodes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
