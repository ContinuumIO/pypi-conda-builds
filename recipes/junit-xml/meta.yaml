package:
  name: junit-xml
  version: "1.4"

source:
  fn: junit-xml-1.4.tar.gz
  url: https://pypi.python.org/packages/source/j/junit-xml/junit-xml-1.4.tar.gz
  md5: f5202054a8f1bd16ff9ef471accbdfc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - junit-xml = junit-xml:main
    #
    # Would create an entry point called junit-xml that calls junit-xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - junit_xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kyrus/python-junit-xml
  license: MIT License
  summary: 'Creates JUnit XML test result documents that can be read by tools such as Jenkins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
