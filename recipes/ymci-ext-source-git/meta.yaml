package:
  name: ymci-ext-source-git
  version: "1.1"

source:
  fn: ymci-ext-source-git-1.1.tar.gz
  url: https://pypi.python.org/packages/source/y/ymci-ext-source-git/ymci-ext-source-git-1.1.tar.gz
  md5: e3a7fba72ce9868e254a12f1f2e0e1a4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ymci-ext-source-git = ymci-ext-source-git:main
    #
    # Would create an entry point called ymci-ext-source-git that calls ymci-ext-source-git.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ymci_ext_source_git

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Git source plugin for ymci'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
