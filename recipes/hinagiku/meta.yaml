package:
  name: hinagiku
  version: "0.0.0"

source:
  fn: Hinagiku-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/H/Hinagiku/Hinagiku-0.0.0.tar.gz
  md5: 90d9ef0b41cc09d651f112f84208db29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hinagiku = hinagiku:main
    #
    # Would create an entry point called hinagiku that calls hinagiku.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dateutil
    - pyyaml
    - genshi

  run:
    - python
    - dateutil
    - pyyaml
    - genshi

test:
  # Python imports
  imports:
    - hinagiku

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hinagiku.overknee.org/
  license: MIT
  summary: 'Web page update checker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
