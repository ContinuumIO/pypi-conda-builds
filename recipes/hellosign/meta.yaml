package:
  name: hellosign
  version: "0.1.2"

source:
  fn: hellosign-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hellosign/hellosign-0.1.2.tar.gz
  md5: d039b750949098a375bd1f886c3af46b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hellosign = hellosign:main
    #
    # Would create an entry point called hellosign that calls hellosign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms
    - requests
    - nose
    - querystring_parser
    - mocktest

  run:
    - python
    - wtforms
    - requests
    - nose
    - querystring_parser
    - mocktest

test:
  # Python imports
  imports:
    - hellosign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rosscdh/hellosign
  license: UNKNOWN
  summary: 'Module for interacting with the HelloSign service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
