package:
  name: jtransliterate
  version: "1.0.3"

source:
  fn: jTransliterate-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jTransliterate/jTransliterate-1.0.3.tar.gz
  md5: 6f3a22a25781b00a84cbea6a152de8bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jtransliterate = jtransliterate:main
    #
    # Would create an entry point called jtransliterate that calls jtransliterate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jTransliterate
    - jtransliterate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryanmcgrath/jTransliterate
  license: MIT License
  summary: 'Transliterate [Hirag/Katak]ana to Latin/English and back. Convert half/full-width Japanese text.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
