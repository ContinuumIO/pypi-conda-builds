package:
  name: flask-admin-utils
  version: "0.0.3"

source:
  fn: Flask-Admin-Utils-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Admin-Utils/Flask-Admin-Utils-0.0.3.tar.gz
  md5: 03ce6b044ca3190bf0d507269a38daea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-admin-utils = flask-admin-utils:main
    #
    # Would create an entry point called flask-admin-utils that calls flask-admin-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-admin
    - sqlalchemy-utils

  run:
    - python
    - flask-admin
    - sqlalchemy-utils

test:
  # Python imports
  imports:
    - flask_admin_utils
    - flask_admin_utils.sqla_utils
    - flask_admin_utils.sqla_utils.sqla_utils_types

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WolkoDav/flask-admin-utils
  license: BSD License
  summary: 'Utils for Flask-Admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
