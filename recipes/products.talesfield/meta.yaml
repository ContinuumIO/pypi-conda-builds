package:
  name: products.talesfield
  version: "1.1.3"

source:
  fn: Products.TALESField-1.1.3.zip
  url: https://pypi.python.org/packages/source/P/Products.TALESField/Products.TALESField-1.1.3.zip
  md5: 432d20c56ab4e4749c5acd3a4e2e36a4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.talesfield = products.talesfield:main
    #
    # Would create an entry point called products.talesfield that calls products.talesfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.TALESField
    - Products.TALESField.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/scriptablefields
  license: GPL
  summary: 'An Archetypes field that stores TALES Expressions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
