package:
  name: django-hisstory
  version: "0.1.2"

source:
  fn: django-hisstory-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hisstory/django-hisstory-0.1.2.tar.gz
  md5: 25d1ce58ebf007349f54e0f6bf611070
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hisstory = django-hisstory:main
    #
    # Would create an entry point called django-hisstory that calls django-hisstory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonfield
    - django_globals ==0.2.1

  run:
    - python
    - jsonfield
    - django_globals ==0.2.1

test:
  # Python imports
  imports:
    - hisstory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/medsafe/django-hisstory
  license: BSD License
  summary: 'Lightweight read-only django model history app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
