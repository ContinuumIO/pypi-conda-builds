package:
  name: html-minifier
  version: "0.0.4"

source:
  fn: html-minifier-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/h/html-minifier/html-minifier-0.0.4.tar.gz
  md5: c7cb11e5e89d5e50181c21cb59d8d2da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html-minifier = html-minifier:main
    #
    # Would create an entry point called html-minifier that calls html-minifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - html_minifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kaumer/html-minifier
  license: MIT License
  summary: 'a module for minify html'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
