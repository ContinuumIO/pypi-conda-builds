package:
  name: inlinesas
  version: "0.0.3"

source:
  fn: inlinesas-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/inlinesas/inlinesas-0.0.3.tar.gz
  md5: 6da91878b007974a2ab4f5d2a37e7fd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inlinesas = inlinesas:main
    #
    # Would create an entry point called inlinesas that calls inlinesas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - inlinesas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://wrds-web.wharton.upenn.edu/wrds/
  license: UNKNOWN
  summary: 'Run SAS code from within a Python script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
