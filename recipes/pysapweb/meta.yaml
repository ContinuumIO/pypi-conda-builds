package:
  name: pysapweb
  version: "0.9.2"

source:
  fn: pysapweb-0.9.2.zip
  url: https://pypi.python.org/packages/source/p/pysapweb/pysapweb-0.9.2.zip
  md5: 62e9c0b694af5906da3a6b0feaf7205a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysapweb = pysapweb:main
    #
    # Would create an entry point called pysapweb that calls pysapweb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - selenium

  run:
    - python
    - selenium

test:
  # Python imports
  imports:
    - pysapweb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/btidor/pysapweb
  license: MIT License
  summary: "A Python interface to MIT's SAPweb (now Atlas) accounting system."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
