package:
  name: lovely.pyrest
  version: "2.0.10"

source:
  fn: lovely.pyrest-2.0.10.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.pyrest/lovely.pyrest-2.0.10.tar.gz
  md5: c4d65229f1ee656bb50b8501aa62bd38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.pyrest = lovely.pyrest:main
    #
    # Would create an entry point called lovely.pyrest that calls lovely.pyrest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent
    - pyramid
    - validictory
    - docutils

  run:
    - python
    - gevent
    - pyramid
    - validictory
    - docutils

test:
  # Python imports
  imports:
    - lovely
    - lovely.pyrest
    - lovely.pyrest.sphinx
    - testing
    - testing.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lovelysystems/lovely.pyrest
  license: apache license 2.0
  summary: 'rest framework for pyramids'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
