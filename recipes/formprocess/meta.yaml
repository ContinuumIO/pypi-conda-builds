package:
  name: formprocess
  version: "0.4.3"

source:
  fn: formprocess-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/f/formprocess/formprocess-0.4.3.tar.gz
  md5: 894b82d5e7fa0664116b3aef39510099
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formprocess = formprocess:main
    #
    # Would create an entry point called formprocess that calls formprocess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webhelpers
    - formencode
    - decorator
    - webob

  run:
    - python
    - webhelpers
    - formencode
    - decorator
    - webob

test:
  # Python imports
  imports:
    - formprocess

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Form Process is a simple library for processing html forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
