package:
  name: por.gdata
  version: "1.7.2"

source:
  fn: por.gdata-1.7.2.tar.gz
  url: https://pypi.python.org/packages/source/p/por.gdata/por.gdata-1.7.2.tar.gz
  md5: 3a2a0c6f1c81a502dbc713e4d08fdd09
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - por.gdata = por.gdata:main
    #
    # Would create an entry point called por.gdata that calls por.gdata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - gdata ==2.0.14
    - pastescript
    - python-dateutil ==1.5

  run:
    - python
    - setuptools
    - distribute
    - gdata ==2.0.14
    - pastescript
    - python-dateutil ==1.5

test:
  # Python imports
  imports:
    - por
    - por.gdata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://getpenelope.github.com
  license: UNKNOWN
  summary: 'Penelope gdata integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
