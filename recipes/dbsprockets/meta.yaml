package:
  name: dbsprockets
  version: "0.5dev-r417"

source:
  fn: DBSprockets-0.5dev-r417.tar.gz
  url: https://pypi.python.org/packages/source/D/DBSprockets/DBSprockets-0.5dev-r417.tar.gz
  md5: d3aca21a50ce782a0414f8a4515ef097
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbsprockets = dbsprockets:main
    #
    # Would create an entry point called dbsprockets that calls dbsprockets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.5dev
    - tw.forms >=0.8
    - genshi >=0.4.4

  run:
    - python
    - setuptools
    - sqlalchemy >=0.5dev
    - tw.forms >=0.8
    - genshi >=0.4.4

test:
  # Python imports
  imports:
    - dbsprockets
    - dbsprockets.dbmechanic
    - dbsprockets.dbmechanic.frameworks
    - dbsprockets.dbmechanic.frameworks.pylons
    - dbsprockets.dbmechanic.frameworks.pylons.templates
    - dbsprockets.dbmechanic.frameworks.tg
    - dbsprockets.dbmechanic.frameworks.tg.templates
    - dbsprockets.dbmechanic.frameworks.tg2
    - dbsprockets.dbmechanic.frameworks.tg2.templates
    - dbsprockets.dbmechanic.frameworks.tg2.test
    - dbsprockets.test
    - dbsprockets.widgets
    - dbsprockets.widgets.templates
    - dbsprockets.widgets.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dbsprockets.googlecode.com/svn/dist
  license: MIT License
  summary: 'A package for creation of web widgets directly from database definitions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
