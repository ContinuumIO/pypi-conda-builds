package:
  name: sphinxcontrib-rawfiles
  version: "0.1.1"

source:
  fn: sphinxcontrib-rawfiles-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-rawfiles/sphinxcontrib-rawfiles-0.1.1.tar.gz
  md5: 959d641e41961f6f2440af4ec91cc600
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-rawfiles = sphinxcontrib-rawfiles:main
    #
    # Would create an entry point called sphinxcontrib-rawfiles that calls sphinxcontrib-rawfiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6

  run:
    - python
    - sphinx >=0.6

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/birkenfeld/sphinx-contrib
  license: BSD License
  summary: 'Sphinx "rawfiles" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
