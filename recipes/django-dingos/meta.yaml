package:
  name: django-dingos
  version: "0.2.1"

source:
  fn: django-dingos-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dingos/django-dingos-0.2.1.tar.gz
  md5: 04be54546bcbfcbacad26bac29dd49de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dingos = django-dingos:main
    #
    # Would create an entry point called django-dingos that calls django-dingos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - libxml2-python >=2.6.9
    - django >=1.6
    - django-grappelli >=2.4.7
    - django-braces >=1.0.0
    - lxml >=3.2.1
    - django-filter >=0.7
    - python-dateutil >=2.2

  run:
    - python
    - libxml2-python >=2.6.9
    - django >=1.6
    - django-grappelli >=2.4.7
    - django-braces >=1.0.0
    - lxml >=3.2.1
    - django-filter >=0.7
    - python-dateutil >=2.2

test:
  # Python imports
  imports:
    - dingos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/siemens/django-dingos
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'A Django app that provides a generic framework for managing structured information in a generic way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
