package:
  name: songspkmp3namecleaner
  version: "1.0"

source:
  fn: SongsPKMP3NameCleaner-1.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SongsPKMP3NameCleaner/SongsPKMP3NameCleaner-1.0.tar.gz
  md5: c304ffa9139f6179e1b89cd22ae7bf32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - songspkmp3namecleaner = songspkmp3namecleaner:main
    #
    # Would create an entry point called songspkmp3namecleaner that calls songspkmp3namecleaner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bin
    - songspkmp3namecleaner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/SongsPKMP3NameCleaner/
  license: LICENSE.txt
  summary: 'Useful Songs.pk filename related stuff.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
