package:
  name: collective.js.jqshorten
  version: "1.0a2"

source:
  fn: collective.js.jqshorten-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.jqshorten/collective.js.jqshorten-1.0a2.tar.gz
  md5: d396de246e029a69843d0a75156766ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.jqshorten = collective.js.jqshorten:main
    #
    # Would create an entry point called collective.js.jqshorten that calls collective.js.jqshorten.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.jqshorten

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MarcDiethelm/jQuery-Shorten
  license: GPL2
  summary: 'jquery.shorten plugin (Maintainer Elio Schmutz)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
