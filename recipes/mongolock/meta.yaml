package:
  name: mongolock
  version: "1.3.3"

source:
  fn: mongolock-1.3.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mongolock/mongolock-1.3.3.tar.gz
  md5: 82fe87775d84ab0f9de0b06676ef12f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongolock = mongolock:main
    #
    # Would create an entry point called mongolock that calls mongolock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.6.0

  run:
    - python
    - pymongo >=2.6.0

test:
  # Python imports
  imports:
    - mongolock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lorehov/mongolock
  license: BSD
  summary: 'Python Mongodb based Distributed Lock'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
