package:
  name: django-localdates
  version: "0.2.1"

source:
  fn: django-localdates-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localdates/django-localdates-0.2.1.tar.gz
  md5: 82f68f6a23e3f594101c9efbdf3834bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localdates = django-localdates:main
    #
    # Would create an entry point called django-localdates that calls django-localdates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - localdates
    - localdates.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-localdates/
  license: BSD License
  summary: 'An addition to django that allows better presentation of date strings in a local way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
