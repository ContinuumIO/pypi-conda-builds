package:
  name: django-measurement
  version: "1.4"

source:
  fn: django-measurement-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-measurement/django-measurement-1.4.tar.gz
  md5: ade68cc04308477f6651e76beed8e7c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-measurement = django-measurement:main
    #
    # Would create an entry point called django-measurement that calls django-measurement.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - measurement >=1.6

  run:
    - python
    - measurement >=1.6

test:
  # Python imports
  imports:
    - django_measurement

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/latestrevision/django-measurement/
  license: MIT License
  summary: 'Convenient fields and classes for handling measurements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
