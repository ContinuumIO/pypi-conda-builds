package:
  name: markupsafe
  version: "0.23"

source:
  fn: MarkupSafe-0.23.tar.gz
  url: https://pypi.python.org/packages/source/M/MarkupSafe/MarkupSafe-0.23.tar.gz
  md5: f5ab3deee4c37cd6a922fb81e730da6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - markupsafe = markupsafe:main
    #
    # Would create an entry point called markupsafe that calls markupsafe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - markupsafe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mitsuhiko/markupsafe
  license: BSD License
  summary: 'Implements a XML/HTML/XHTML Markup safe string for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
