package:
  name: beeswithmachineguns
  version: "0.1.4"

source:
  fn: beeswithmachineguns-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/b/beeswithmachineguns/beeswithmachineguns-0.1.4.tar.gz
  md5: 111eec0a706e1ddba5674677d913c2e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beeswithmachineguns = beeswithmachineguns:main
    #
    # Would create an entry point called beeswithmachineguns that calls beeswithmachineguns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - boto ==2.0
    - paramiko ==1.7.7.1

  run:
    - python
    - boto ==2.0
    - paramiko ==1.7.7.1

test:
  # Python imports
  imports:
    - beeswithmachineguns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/newsapps/beeswithmachineguns
  license: MIT License
  summary: 'A utility for arming (creating) many bees (micro EC2 instances) to attack (load test) targets (web applications).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
