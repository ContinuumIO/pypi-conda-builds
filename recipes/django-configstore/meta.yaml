package:
  name: django-configstore
  version: "0.3"

source:
  fn: django-configstore-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-configstore/django-configstore-0.3.tar.gz
  md5: 6a49b0e4ac7a2db9d3bf3797fb037d57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-configstore = django-configstore:main
    #
    # Would create an entry point called django-configstore that calls django-configstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto ==2.6

  run:
    - python
    - pycrypto ==2.6

test:
  # Python imports
  imports:
    - configstore
    - configstore.migrations
    - configstore.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/webcube/django-configstore
  license: BSD
  summary: 'An application to allow for other apps to easily store site based configurations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
