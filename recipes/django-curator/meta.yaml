package:
  name: django-curator
  version: "0.1.1"

source:
  fn: django-curator-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-curator/django-curator-0.1.1.tar.gz
  md5: 8c268d47d02f5dea43624d312712fe51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-curator = django-curator:main
    #
    # Would create an entry point called django-curator that calls django-curator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - curator
    - curator.migrations
    - curator.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/spulec/django-curator
  license: UNKNOWN
  summary: 'Automatic Business Graphs for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
