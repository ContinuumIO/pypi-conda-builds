package:
  name: django-ucamlookup
  version: "1.2"

source:
  fn: django-ucamlookup.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ucamlookup/django-ucamlookup.tar.gz
  md5: 00f8a330b8bb099a8ecc8e055fd0fb7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ucamlookup = django-ucamlookup:main
    #
    # Would create an entry point called django-ucamlookup that calls django-ucamlookup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - ucamlookup
    - ucamlookup.ibisclient
    - ucamlookup.migrations
    - ucamlookup.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git.csx.cam.ac.uk/x/ucs/u/amc203/django-ucamlookup.git
  license: MIT License
  summary: 'A Django module for the University of Cambridge Lookup service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
