package:
  name: flask-gcm
  version: "0.2.0"

source:
  fn: Flask-GCM-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-GCM/Flask-GCM-0.2.0.tar.gz
  md5: c3fee1cbf8dc8398ec1c1265764edda6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-gcm = flask-gcm:main
    #
    # Would create an entry point called flask-gcm that calls flask-gcm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gcm-client ==0.1.4
    - flask

  run:
    - python
    - gcm-client ==0.1.4
    - flask

test:
  # Python imports
  imports:
    - flask_gcm
    - flask_gcm.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/michiganlabs/flask-gcm
  license: MIT License
  summary: 'Flask-GCM is a simple wrapper for the python-gcm library to be used with Flask applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
