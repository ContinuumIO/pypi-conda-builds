package:
  name: minimock
  version: "1.2.8"

source:
  fn: MiniMock-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/M/MiniMock/MiniMock-1.2.8.tar.gz
  md5: 76593aaba949b5e010fec95283556449
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minimock = minimock:main
    #
    # Would create an entry point called minimock that calls minimock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - minimock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/MiniMock
  license: MIT License
  summary: 'The simplest possible mock library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
