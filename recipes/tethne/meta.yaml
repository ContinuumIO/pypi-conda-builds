package:
  name: tethne
  version: "0.6.3.3-beta"

source:
  fn: tethne-0.6.3.3-beta.tar.gz
  url: https://pypi.python.org/packages/source/t/tethne/tethne-0.6.3.3-beta.tar.gz
  md5: 78b71059a33613e083bd34e97cefb4fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tethne = tethne:main
    #
    # Would create an entry point called tethne that calls tethne.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - networkx >=1.8.1
    - matplotlib >=1.3.1
    - tables >=3.1.1
    - unidecode >=0.04.16
    - geopy >=0.99
    - scipy >=0.14.0
    - numpy >=1.8.1
    - nltk

  run:
    - python
    - networkx >=1.8.1
    - matplotlib >=1.3.1
    - tables >=3.1.1
    - unidecode >=0.04.16
    - geopy >=0.99
    - scipy >=0.14.0
    - numpy >=1.8.1
    - nltk

test:
  # Python imports
  imports:
    - tethne
    - tethne.analyze
    - tethne.classes
    - tethne.model
    - tethne.model.corpus
    - tethne.model.managers
    - tethne.model.social
    - tethne.networks
    - tethne.persistence
    - tethne.persistence.hdf5
    - tethne.readers
    - tethne.services
    - tethne.utilities
    - tethne.writers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://diging.github.io/tethne/
  license: GNU GPL 3
  summary: 'Bibliographic network and corpus analysis for historians'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
