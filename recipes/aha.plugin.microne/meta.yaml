package:
  name: aha.plugin.microne
  version: "0.62bdev"

source:
  fn: aha.plugin.microne-0.62bdev.tar.gz
  url: https://pypi.python.org/packages/source/a/aha.plugin.microne/aha.plugin.microne-0.62bdev.tar.gz
  md5: bb9045306d32ee268b58e3277a2c4f6c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aha.plugin.microne = aha.plugin.microne:main
    #
    # Would create an entry point called aha.plugin.microne that calls aha.plugin.microne.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aha

  run:
    - python
    - setuptools
    - aha

test:
  # Python imports
  imports:
    - microne

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://coreblog.org/aha
  license: BSD
  summary: 'Yet another microframework on the top of the full stack framework aha'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
