package:
  name: flasky
  version: "0.1.0"

source:
  fn: Flasky-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flasky/Flasky-0.1.0.tar.gz
  md5: d60af3405e0c4e93401bc2919ddf18a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flasky = flasky:main
    #
    # Would create an entry point called flasky that calls flasky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9

  run:
    - python
    - flask >=0.9

test:
  # Python imports
  imports:
    - flasky
    - flasky.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lqez/flasky
  license: MIT License
  summary: "Lazy man's Flask Application"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
