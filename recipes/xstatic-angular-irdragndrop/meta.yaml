package:
  name: xstatic-angular-irdragndrop
  version: "1.0.2.2"

source:
  fn: XStatic-Angular-IrDragNDrop-1.0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Angular-IrDragNDrop/XStatic-Angular-IrDragNDrop-1.0.2.2.tar.gz
  md5: 6ea8809bae94014aef699d58836dba84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-angular-irdragndrop = xstatic-angular-irdragndrop:main
    #
    # Would create an entry point called xstatic-angular-irdragndrop that calls xstatic-angular-irdragndrop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.angular_irdragndrop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lorenzofox3/lrDragNDrop
  license: (same as IrDragNDrop)
  summary: 'IrDragNDrop 1.0.2 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
