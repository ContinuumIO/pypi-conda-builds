package:
  name: zato-redis-paginator
  version: "1.0"

source:
  fn: zato-redis-paginator-1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zato-redis-paginator/zato-redis-paginator-1.0.tar.gz
  md5: 23cd5bcd3f1ea6289be324d0f3113ee1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zato-redis-paginator = zato-redis-paginator:main
    #
    # Would create an entry point called zato-redis-paginator that calls zato-redis-paginator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - redis >=2.4.13

  run:
    - python
    - django >=1.3.1
    - redis >=2.4.13

test:
  # Python imports
  imports:
    - zato
    - zato.redis_paginator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zatosource/zato-redis-paginator
  license: BSD License
  summary: 'Django-like pagination for Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
