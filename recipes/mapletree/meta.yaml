package:
  name: mapletree
  version: "0.7.2"

source:
  fn: mapletree-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mapletree/mapletree-0.7.2.tar.gz
  md5: 011634ff6a04d00367d7e8c34eb1260d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mapletree = mapletree:main
    #
    # Would create an entry point called mapletree that calls mapletree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mapletree
    - mapletree.defaults
    - mapletree.defaults.request
    - mapletree.defaults.routings
    - mapletree.helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tomokinakamaru/mapletree
  license: MIT License
  summary: 'WSGI app framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
