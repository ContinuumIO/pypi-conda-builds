package:
  name: django-keepdb
  version: "0.1"

source:
  fn: django-keepdb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-keepdb/django-keepdb-0.1.tar.gz
  md5: bce0f592352373964b29de8d292cc88c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-keepdb = django-keepdb:main
    #
    # Would create an entry point called django-keepdb that calls django-keepdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock

  run:
    - python
    - mock

test:
  # Python imports
  imports:
    - keepdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/romgar/django-keepdb/
  license: BSD License
  summary: 'A simple Django app to keep db between test runs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
