package:
  name: cmsplugin-price
  version: "1.0.5"

source:
  fn: cmsplugin-price-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-price/cmsplugin-price-1.0.5.tar.gz
  md5: cc3d578042fec8fe7c6dac8a01ebda61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-price = cmsplugin-price:main
    #
    # Would create an entry point called cmsplugin-price that calls cmsplugin-price.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_price
    - cmsplugin_price.nospam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vinitkumar/cmsplugin-price
  license: BSD License
  summary: 'Extendable price form plugin for Django CMS with spam protection and i18n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
