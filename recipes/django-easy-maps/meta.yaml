package:
  name: django-easy-maps
  version: "0.9"

source:
  fn: django-easy-maps-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-maps/django-easy-maps-0.9.tar.gz
  md5: 34e43bc75ad3f9b6e56542f516ad02b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-maps = django-easy-maps:main
    #
    # Would create an entry point called django-easy-maps that calls django-easy-maps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - easy_maps
    - easy_maps.migrations
    - easy_maps.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kmike/django-easy-maps/
  license: MIT License
  summary: 'This app makes it easy to display a map for a given address.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
