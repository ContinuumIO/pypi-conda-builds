package:
  name: django-localflavor-us
  version: "1.1"

source:
  fn: django-localflavor-us-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localflavor-us/django-localflavor-us-1.1.tar.gz
  md5: 570efba230dea408d55bccfea86fce33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localflavor-us = django-localflavor-us:main
    #
    # Would create an entry point called django-localflavor-us that calls django-localflavor-us.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_localflavor_us

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django/django-localflavor-us
  license: BSD License
  summary: 'Country-specific Django helpers for U.S.A.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
