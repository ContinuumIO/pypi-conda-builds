package:
  name: argyle
  version: "0.2.1"

source:
  fn: argyle-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/argyle/argyle-0.2.1.tar.gz
  md5: bd1dd8dda0346b5b61ce756750a7627b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argyle = argyle:main
    #
    # Would create an entry point called argyle that calls argyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.1.0
    - jinja2 >=2.3

  run:
    - python
    - fabric >=1.1.0
    - jinja2 >=2.3

test:
  # Python imports
  imports:
    - argyle
    - argyle.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mlavin/argyle
  license: BSD License
  summary: u'Argyle is a collection of Fabric utilities for Django deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
