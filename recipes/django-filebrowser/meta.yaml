package:
  name: django-filebrowser
  version: "3.5.7"

source:
  fn: django-filebrowser-3.5.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filebrowser/django-filebrowser-3.5.7.tar.gz
  md5: 0a3ff61910ed9cf897205fdde81b37c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filebrowser = django-filebrowser:main
    #
    # Would create an entry point called django-filebrowser that calls django-filebrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-grappelli >=2.4,<2.5.99

  run:
    - python
    - django-grappelli >=2.4,<2.5.99

test:
  # Python imports
  imports:
    - filebrowser
    - filebrowser.management
    - filebrowser.management.commands
    - filebrowser.templatetags
    - filebrowser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-filebrowser.readthedocs.org
  license: BSD License
  summary: 'Media-Management with Grappelli'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
