package:
  name: django-ccgallery
  version: "1.4.2"

source:
  fn: django-ccgallery-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ccgallery/django-ccgallery-1.4.2.tar.gz
  md5: 9c3f366db1f8ab5128bbb37b49c1daeb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ccgallery = django-ccgallery:main
    #
    # Would create an entry point called django-ccgallery that calls django-ccgallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-ccthumbs
    - markdown2

  run:
    - python
    - django-ccthumbs
    - markdown2

test:
  # Python imports
  imports:
    - ccgallery
    - ccgallery.templatetags
    - ccgallery.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/designcc/django-ccgallery
  license: BSD 3 Clause
  summary: 'A minimal Gallery application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
