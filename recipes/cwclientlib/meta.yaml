package:
  name: cwclientlib
  version: "0.2.1"

source:
  fn: cwclientlib-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cwclientlib/cwclientlib-0.2.1.tar.gz
  md5: fadb25d976e42acb1e25cde64a281577
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cwclientlib = cwclientlib:main
    #
    # Would create an entry point called cwclientlib that calls cwclientlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2

  run:
    - python
    - requests >=2

test:
  # Python imports
  imports:
    - cwclientlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cwclientlib
  license: LGPL
  summary: 'A Python library to easily build CubicWeb clients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
