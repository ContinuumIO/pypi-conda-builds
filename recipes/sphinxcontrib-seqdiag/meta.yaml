package:
  name: sphinxcontrib-seqdiag
  version: "0.8.3"

source:
  fn: sphinxcontrib-seqdiag-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-seqdiag/sphinxcontrib-seqdiag-0.8.3.tar.gz
  md5: 3f912f115b1f59265aae4b83e0cbf4fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-seqdiag = sphinxcontrib-seqdiag:main
    #
    # Would create an entry point called sphinxcontrib-seqdiag that calls sphinxcontrib-seqdiag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.5.0
    - seqdiag >=0.9.3
    - sphinx >=0.6

  run:
    - python
    - blockdiag >=1.5.0
    - seqdiag >=0.9.3
    - sphinx >=0.6

test:
  # Python imports
  imports:
    - sphinxcontrib
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tk0miya/sphinxcontrib-seqdiag
  license: BSD License
  summary: 'Sphinx "seqdiag" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
