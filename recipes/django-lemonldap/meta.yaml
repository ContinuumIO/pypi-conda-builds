package:
  name: django-lemonldap
  version: "1.0.1"

source:
  fn: django-lemonldap-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lemonldap/django-lemonldap-1.0.1.tar.gz
  md5: c16367a96507c56655548943c19700fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lemonldap = django-lemonldap:main
    #
    # Would create an entry point called django-lemonldap that calls django-lemonldap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lemonldap
    - lemonldap.auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rclsilver/django-lemonldap
  license: Apache Software License
  summary: 'LemonLDAP: django authentication system to be used with LemonLDAP::NG'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
