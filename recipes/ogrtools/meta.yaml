package:
  name: ogrtools
  version: "0.7.2"

source:
  fn: ogrtools-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/o/ogrtools/ogrtools-0.7.2.tar.gz
  md5: c373073f753b2a8fc1c6afd445a4516e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ogrtools = ogrtools:main
    #
    # Would create an entry point called ogrtools that calls ogrtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ogrtools
    - ogrtools.interlis
    - ogrtools.ogrtransform
    - ogrtools.pyogr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sourcepole/ogrtools
  license: MIT License
  summary: 'Collection of libraries and tools built with the Python API of OGR.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
