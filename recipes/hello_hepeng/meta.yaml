package:
  name: hello_hepeng
  version: "1.0.0"

source:
  fn: hello_hepeng-1.0.0.zip
  url: https://pypi.python.org/packages/source/h/hello_hepeng/hello_hepeng-1.0.0.zip
  md5: c3d5ad297e87e4caed0641eeb9fb0919
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hello_hepeng = hello_hepeng:main
    #
    # Would create an entry point called hello_hepeng that calls hello_hepeng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hello_hepeng

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Distutils hello distribution #1'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
