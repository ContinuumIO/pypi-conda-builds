package:
  name: gaphormodelimport
  version: "0.0.1"

source:
  fn: gaphormodelimport-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gaphormodelimport/gaphormodelimport-0.0.1.tar.gz
  md5: 1ba6d66cf70bd95e1ea4befbb6bfbc9d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaphormodelimport = gaphormodelimport:main
    #
    # Would create an entry point called gaphormodelimport that calls gaphormodelimport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gaphor

  run:
    - python
    - setuptools
    - gaphor

test:
  # Python imports
  imports:
    - gaphormodelimport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.boduch.ca/
  license: AGPL
  summary: 'Simple model merging plugin for Gaphor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
