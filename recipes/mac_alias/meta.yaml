package:
  name: mac_alias
  version: "1.0.0"

source:
  fn: mac_alias-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mac_alias/mac_alias-1.0.0.tar.gz
  md5: 44fefea954649c19263037e79711ed7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mac_alias = mac_alias:main
    #
    # Would create an entry point called mac_alias that calls mac_alias.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mac_alias

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://alastairs-place.net/projects/mac_alias
  license: MIT License
  summary: 'Generate/parse Mac OS Alias records from Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
