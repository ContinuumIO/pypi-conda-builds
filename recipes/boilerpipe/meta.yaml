package:
  name: boilerpipe
  version: "1.2.0.0"

source:
  fn: boilerpipe-1.2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/boilerpipe/boilerpipe-1.2.0.0.tar.gz
  md5: 960f9e725366b32cd6713f5498e069fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boilerpipe = boilerpipe:main
    #
    # Would create an entry point called boilerpipe that calls boilerpipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jpype1
    - charade

  run:
    - python
    - jpype1
    - charade

test:
  # Python imports
  imports:
    - boilerpipe
    - boilerpipe.extract

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ptwobrussell/python-boilerpipe/
  license: Apache Software License
  summary: 'Python interface to Boilerpipe, Boilerplate Removal and Fulltext Extraction from HTML pages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
