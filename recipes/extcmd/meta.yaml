package:
  name: extcmd
  version: "1.1"

source:
  fn: extcmd-1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/extcmd/extcmd-1.1.tar.gz
  md5: 21e8ded1a1652deea59bffebc26fdfcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extcmd = extcmd:main
    #
    # Would create an entry point called extcmd that calls extcmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - extcmd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zyga/extcmd
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'External Command - subprocess with advanced output processing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
