package:
  name: backport_collections
  version: "0.1"

source:
  fn: backport_collections-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/backport_collections/backport_collections-0.1.tar.gz
  md5: b4cb1ede168fc9cb7d4f59399a15fbca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backport_collections = backport_collections:main
    #
    # Would create an entry point called backport_collections that calls backport_collections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sk-/backport_collections
  license: Python Software Foundation License
  summary: "Backport of Python 2.7's collections module"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
