package:
  name: asyncssh-unofficial
  version: "0.9.2"

source:
  fn: asyncssh-unofficial-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncssh-unofficial/asyncssh-unofficial-0.9.2.tar.gz
  md5: 8426ec48b3807112dbf10fe2f6cd211b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncssh-unofficial = asyncssh-unofficial:main
    #
    # Would create an entry point called asyncssh-unofficial that calls asyncssh-unofficial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto

  run:
    - python
    - pycrypto

test:
  # Python imports
  imports:
    - asyncssh

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://asyncssh.timeheart.net/
  license: Eclipse Public License v1.0
  summary: 'AsyncSSH: Asynchronous SSHv2 client and server library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
