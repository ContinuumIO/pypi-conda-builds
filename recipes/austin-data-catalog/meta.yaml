package:
  name: austin-data-catalog
  version: "0.0.1"

source:
  fn: austin-data-catalog-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/austin-data-catalog/austin-data-catalog-0.0.1.tar.gz
  md5: e3383e4a30cc0b11e45f0cd35829677b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - austin-data-catalog = austin-data-catalog:main
    #
    # Would create an entry point called austin-data-catalog that calls austin-data-catalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pluplusch >=0.0.7

  run:
    - python
    - pluplusch >=0.0.7

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/austin-data-catalog
  license: AGPL
  summary: "Archive the /api/views endpoint on Austin's data catalog."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
