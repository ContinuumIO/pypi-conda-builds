package:
  name: django-organice-theme
  version: "0.3.dev"

source:
  fn: django-organice-theme-0.3.dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-organice-theme/django-organice-theme-0.3.dev.tar.gz
  md5: 709409c84ba7e7d0efbc7dc177410070
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-organice-theme = django-organice-theme:main
    #
    # Would create an entry point called django-organice-theme that calls django-organice-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - organice_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://organice.io/themes/
  license: Apache Software License
  summary: 'The mother theme of all themes for django-organice.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
