package:
  name: commandr
  version: "1.3.2"

source:
  fn: commandr-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/commandr/commandr-1.3.2.tar.gz
  md5: 418bc979bd68fcd39f0287a4d2b95ee9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commandr = commandr:main
    #
    # Would create an entry point called commandr that calls commandr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - commandr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/commandr/
  license: LICENSE
  summary: 'Tool to automatically build command line interfaces to functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
