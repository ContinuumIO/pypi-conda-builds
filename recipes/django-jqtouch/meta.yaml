package:
  name: django-jqtouch
  version: "0.1"

source:
  fn: django-jqtouch-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jqtouch/django-jqtouch-0.1.tar.gz
  md5: e9d51d5145013f9ba2256822c0f734c3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jqtouch = django-jqtouch:main
    #
    # Would create an entry point called django-jqtouch that calls django-jqtouch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - jqtouch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://library.tamu.edu
  license: Apache Software License
  summary: 'A Django Application for creating jQTouch websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
