package:
  name: redis-completion
  version: "0.5.0"

source:
  fn: redis-completion-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-completion/redis-completion-0.5.0.tar.gz
  md5: 18188d12bd80ab3b6c71ca387710b078
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-completion = redis-completion:main
    #
    # Would create an entry point called redis-completion that calls redis-completion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redis_completion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coleifer/redis-completion/tree/master
  license: MIT License
  summary: 'autocomplete with redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
