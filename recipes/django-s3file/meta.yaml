package:
  name: django-s3file
  version: "0.4.1"

source:
  fn: django-s3file-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-s3file/django-s3file-0.4.1.tar.gz
  md5: 682a0fa016c9f4373ed5fc34ba6a27f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-s3file = django-s3file:main
    #
    # Would create an entry point called django-s3file that calls django-s3file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf ==0.6

  run:
    - python
    - django-appconf ==0.6

test:
  # Python imports
  imports:
    - s3file

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codingjoe/django-s3file
  license: MIT License
  summary: 'A lightweight Fine Uploader input for Django and Amazon S3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
