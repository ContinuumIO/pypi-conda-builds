package:
  name: altasetting
  version: "0.1.1"

source:
  fn: altasetting-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/altasetting/altasetting-0.1.1.tar.gz
  md5: 0b467c52c7ca71cafef6ad747f7db195
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - altasetting = altasetting:main
    #
    # Would create an entry point called altasetting that calls altasetting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyaml

  run:
    - python
    - pyaml

test:
  # Python imports
  imports:
    - altasetting

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/felipelerena/altasetting
  license: UNKNOWN
  summary: 'A python settings library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
