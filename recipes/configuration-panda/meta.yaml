package:
  name: configuration-panda
  version: "0.12"

source:
  fn: configuration-panda-0.12.tar.gz
  url: https://pypi.python.org/packages/source/c/configuration-panda/configuration-panda-0.12.tar.gz
  md5: aaf5f82e950d76fb5c37261eb287a651
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configuration-panda = configuration-panda:main
    #
    # Would create an entry point called configuration-panda that calls configuration-panda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest ==2.5.1

  run:
    - python
    - pytest ==2.5.1

test:
  # Python imports
  imports:
    - configuration_panda
    - configuration_panda.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eikonomega/configuration_panda
  license: UNKNOWN
  summary: 'ConfigurationPanda provides easy loading and access to the data elements of JSON based configuration files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
