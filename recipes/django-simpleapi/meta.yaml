package:
  name: django-simpleapi
  version: "1.4.0"

source:
  fn: django-simpleapi-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simpleapi/django-simpleapi-1.4.0.tar.gz
  md5: a842852620d55fd8feba68dc1b802fd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simpleapi = django-simpleapi:main
    #
    # Would create an entry point called django-simpleapi that calls django-simpleapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-cors-headers >=1.0.0

  run:
    - python
    - django >=1.7
    - django-cors-headers >=1.0.0

test:
  # Python imports
  imports:
    - simpleapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/voidfiles/django-simpleapi
  license: MIT License
  summary: 'A Django package that allows you to write APIs simply'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
