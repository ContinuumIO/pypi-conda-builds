package:
  name: koansys.recipe.pybsddb
  version: "1.0.1"

source:
  fn: koansys.recipe.pybsddb-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/koansys.recipe.pybsddb/koansys.recipe.pybsddb-1.0.1.tar.gz
  md5: 30df1aad0c40b051184c189e7598668f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - koansys.recipe.pybsddb = koansys.recipe.pybsddb:main
    #
    # Would create an entry point called koansys.recipe.pybsddb that calls koansys.recipe.pybsddb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - koansys
    - koansys.recipe
    - koansys.recipe.pybsddb
    - koansys.recipe.pybsddb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://koansys.com
  license: BSD License
  summary: 'A recipe to build pybsddb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
