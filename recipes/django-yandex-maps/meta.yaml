package:
  name: django-yandex-maps
  version: "0.2"

source:
  fn: django-yandex-maps-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yandex-maps/django-yandex-maps-0.2.tar.gz
  md5: cc3c9b048e236f14c9f3b84316d4b28d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yandex-maps = django-yandex-maps:main
    #
    # Would create an entry point called django-yandex-maps that calls django-yandex-maps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - yandex_maps
    - yandex_maps.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-yandex-maps/
  license: MIT License
  summary: 'Django app for work with Yandex Maps service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
