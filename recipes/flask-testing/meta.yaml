package:
  name: flask-testing
  version: "0.4.2"

source:
  fn: Flask-Testing-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Testing/Flask-Testing-0.4.2.tar.gz
  md5: f47d9b859bae7ac43728cf61ca3eaa88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-testing = flask-testing:main
    #
    # Would create an entry point called flask-testing that calls flask-testing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jarus/flask-testing
  license: BSD License
  summary: 'Unit testing for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
