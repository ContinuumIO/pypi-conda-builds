package:
  name: jingerly
  version: "0.0.3"

source:
  fn: jingerly-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jingerly/jingerly-0.0.3.tar.gz
  md5: ec87269ed700f600e7e8f7a194c2fbd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jingerly = jingerly:main
    #
    # Would create an entry point called jingerly that calls jingerly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - requests

  run:
    - python
    - jinja2
    - requests

test:
  # Python imports
  imports:
    - jingerly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WilliamMayor/jingerly
  license: MIT License
  summary: 'Project templating using Jinja2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
