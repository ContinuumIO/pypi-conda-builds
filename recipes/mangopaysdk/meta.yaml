package:
  name: mangopaysdk
  version: "1.5.2"

source:
  fn: mangopaysdk-1.5.2.zip
  url: https://pypi.python.org/packages/source/m/mangopaysdk/mangopaysdk-1.5.2.zip
  md5: 0b73134b590bdb825cf54882354adc1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mangopaysdk = mangopaysdk:main
    #
    # Would create an entry point called mangopaysdk that calls mangopaysdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.4.3
    - requests-oauthlib >=0.4.0
    - lockfile >=0.9.1

  run:
    - python
    - requests >=2.4.3
    - requests-oauthlib >=0.4.0
    - lockfile >=0.9.1

test:
  # Python imports
  imports:
    - mangopaysdk
    - mangopaysdk.entities
    - mangopaysdk.tools
    - mangopaysdk.tools.storages
    - mangopaysdk.types
    - mangopaysdk.types.exceptions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/mangopaysdk/
  license: UNKNOWN
  summary: 'MangoPay API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
