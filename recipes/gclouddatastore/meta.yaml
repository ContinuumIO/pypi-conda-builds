package:
  name: gclouddatastore
  version: "0.1.2"

source:
  fn: gclouddatastore-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gclouddatastore/gclouddatastore-0.1.2.tar.gz
  md5: 9a70589b6bf9383801cedc85babb7884
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gclouddatastore = gclouddatastore:main
    #
    # Would create an entry point called gclouddatastore that calls gclouddatastore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - oauth2client
    - protobuf
    - pycrypto
    - pyopenssl
    - pytz

  run:
    - python
    - httplib2
    - oauth2client
    - protobuf
    - pycrypto
    - pyopenssl
    - pytz

test:
  # Python imports
  imports:
    - gclouddatastore
    - gclouddatastore.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jgeewax/gclouddatastore
  license: Apache Software License
  summary: 'API Client library for Google Cloud Datastore'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
