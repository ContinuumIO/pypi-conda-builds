package:
  name: xstatic-bootstrap
  version: "3.1.1.2"

source:
  fn: XStatic-Bootstrap-3.1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Bootstrap/XStatic-Bootstrap-3.1.1.2.tar.gz
  md5: e979aca3c57fec5f22480846ff5fa6fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-bootstrap = xstatic-bootstrap:main
    #
    # Would create an entry point called xstatic-bootstrap that calls xstatic-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://twitter.github.com/bootstrap/
  license: (same as Bootstrap)
  summary: 'Bootstrap 3.1.1 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
