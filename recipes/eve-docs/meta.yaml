package:
  name: eve-docs
  version: "0.1.4"

source:
  fn: Eve-docs-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/E/Eve-docs/Eve-docs-0.1.4.tar.gz
  md5: 88e38dc20940951e13735c54fc50e76c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eve-docs = eve-docs:main
    #
    # Would create an entry point called eve-docs that calls eve-docs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eve >=0.1
    - flask >=0.10
    - flask-bootstrap >=3.0

  run:
    - python
    - eve >=0.1
    - flask >=0.10
    - flask-bootstrap >=3.0

test:
  # Python imports
  imports:
    - eve_docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charlesflynn/eve-docs
  license: MIT License
  summary: 'Generates documentation for Eve APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
