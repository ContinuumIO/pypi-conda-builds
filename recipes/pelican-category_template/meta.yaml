package:
  name: pelican-category_template
  version: "v0.0.2"

source:
  fn: pelican-category_template-v0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-category_template/pelican-category_template-v0.0.2.tar.gz
  md5: 3ac715fafa3d3d9e3c6c0ed7b39330ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-category_template = pelican-category_template:main
    #
    # Would create an entry point called pelican-category_template that calls pelican-category_template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican >=3.1.1

  run:
    - python
    - pelican >=3.1.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hrbonz/pelican-category_template
  license: BSD License
  summary: 'A pelican plugin to apply a template based on the category of the content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
