package:
  name: collective.sphinx.autoatschema
  version: "0.1"

source:
  fn: collective.sphinx.autoatschema-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.sphinx.autoatschema/collective.sphinx.autoatschema-0.1.tar.gz
  md5: 90c3ab77eb64ccb4105f9d9e2d6a30b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sphinx.autoatschema = collective.sphinx.autoatschema:main
    #
    # Would create an entry point called collective.sphinx.autoatschema that calls collective.sphinx.autoatschema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils
    - sphinx
    - zc.recipe.egg

  run:
    - python
    - docutils
    - sphinx
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.sphinx
    - collective.sphinx.autoatschema

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://
  license: GNU General Public License (GPL)
  summary: 'Sphinx extension for archetypes schema'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
