package:
  name: last-train-to-nowhere
  version: "1.0.0"

source:
  fn: last-train-to-nowhere-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/last-train-to-nowhere/last-train-to-nowhere-1.0.0.tar.gz
  md5: ff8202ec21959389b07ae9e043de1fe5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - last-train-to-nowhere = last-train-to-nowhere:main
    #
    # Would create an entry point called last-train-to-nowhere that calls last-train-to-nowhere.main()

    - last-train-to-knowhere = wildwest.__main__:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyglet >=1.1.4

  run:
    - python
    - pyglet >=1.1.4

test:
  # Python imports
  imports:
    - wildwest
    - wildwest.scenegraph

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - last-train-to-knowhere --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pyweek.org/e/wasabiparatha/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Last Train to Nowhere'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
