package:
  name: flask-mxitga
  version: "0.10"

source:
  fn: Flask-MxitGA-0.10.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MxitGA/Flask-MxitGA-0.10.tar.gz
  md5: 19cc2caba2c62e37d83c9e1cd69d9555
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mxitga = flask-mxitga:main
    #
    # Would create an entry point called flask-mxitga that calls flask-mxitga.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_mxit_ga

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Google analytics for flask and mxit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
