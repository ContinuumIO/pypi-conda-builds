package:
  name: cmsplugin-multiple-faq
  version: "0.0.5"

source:
  fn: cmsplugin-multiple-faq-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-multiple-faq/cmsplugin-multiple-faq-0.0.5.tar.gz
  md5: 259b08df7898f89ff1de66b1ec33315a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-multiple-faq = cmsplugin-multiple-faq:main
    #
    # Would create an entry point called cmsplugin-multiple-faq that calls cmsplugin-multiple-faq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-cms >=2.0

  run:
    - python
    - django >=1.2
    - django-cms >=2.0

test:
  # Python imports
  imports:
    - cmsplugin_multiple_faq
    - cmsplugin_multiple_faq.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neo64bit/cmsplugin-multiple-faq
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
