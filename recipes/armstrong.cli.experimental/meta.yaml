package:
  name: armstrong.cli.experimental
  version: "0.1.0"

source:
  fn: armstrong.cli.experimental-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.cli.experimental/armstrong.cli.experimental-0.1.0.tar.gz
  md5: e4717e48c68ef31c72721a1e2acb7a4b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.cli.experimental = armstrong.cli.experimental:main
    #
    # Would create an entry point called armstrong.cli.experimental that calls armstrong.cli.experimental.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent ==0.13.6
    - pyquery ==1.1.1
    - requests ==0.10.6

  run:
    - python
    - setuptools
    - gevent ==0.13.6
    - pyquery ==1.1.1
    - requests ==0.10.6

test:
  # Python imports
  imports:
    - armstrong.cli.experimental
    - armstrong.cli.experimental.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.cli.experimental/
  license: Apache Software License
  summary: 'Experimental features for armstrong.cli'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
