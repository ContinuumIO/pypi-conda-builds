package:
  name: plone.indexer
  version: "1.0.3"

source:
  fn: plone.indexer-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.indexer/plone.indexer-1.0.3.tar.gz
  md5: c0030cfcb51911926640105b1bf199d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.indexer = plone.indexer:main
    #
    # Would create an entry point called plone.indexer that calls plone.indexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - products.cmfcore
    - products.zcatalog

  run:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - products.cmfcore
    - products.zcatalog

test:
  # Python imports
  imports:
    - plone
    - plone.indexer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.indexer
  license: BSD License
  summary: 'Hooks to facilitate managing custom index values in Zope 2/CMF applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
