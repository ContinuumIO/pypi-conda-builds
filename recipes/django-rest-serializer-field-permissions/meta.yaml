package:
  name: django-rest-serializer-field-permissions
  version: "0.34"

source:
  fn: django-rest-serializer-field-permissions-0.34.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-serializer-field-permissions/django-rest-serializer-field-permissions-0.34.tar.gz
  md5: be466b38fa3ab686d6c74924f8001752
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-serializer-field-permissions = django-rest-serializer-field-permissions:main
    #
    # Would create an entry point called django-rest-serializer-field-permissions that calls django-rest-serializer-field-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - djangorestframework >=3.0.0,<3.0.5

  run:
    - python
    - django >=1.6
    - djangorestframework >=3.0.0,<3.0.5

test:
  # Python imports
  imports:
    - rest_framework_serializer_field_permissions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://intersis.org/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Field-by-field serializer permissions for Django Rest Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
