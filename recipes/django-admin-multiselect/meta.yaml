package:
  name: django-admin-multiselect
  version: "0.0.7"

source:
  fn: django-admin-multiselect-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-multiselect/django-admin-multiselect-0.0.7.tar.gz
  md5: 7db2b877dc842d37074f1ae3f7743938
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-multiselect = django-admin-multiselect:main
    #
    # Would create an entry point called django-admin-multiselect that calls django-admin-multiselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_multiselect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kreioslu/django-admin-multiselect
  license: MIT License
  summary: 'Alternative multiple select widget that works with mobile devices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
