package:
  name: backupallthethings
  version: "0.0.5"

source:
  fn: backupallthethings-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/b/backupallthethings/backupallthethings-0.0.5.tar.gz
  md5: 90f292760db9403be9be4ea3c3c7422d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backupallthethings = backupallthethings:main
    #
    # Would create an entry point called backupallthethings that calls backupallthethings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt

  run:
    - python
    - docopt

test:
  # Python imports
  imports:
    - backupallthethings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/devo-ps/backupallthethings
  license: MIT
  summary: 'Batt simply backup all the things; databases and files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
