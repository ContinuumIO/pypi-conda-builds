package:
  name: formular
  version: "0.2"

source:
  fn: Formular-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Formular/Formular-0.2.tar.gz
  md5: 8f94827f1b4e5fda15dcae342ff6900e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formular = formular:main
    #
    # Would create an entry point called formular that calls formular.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formular
    - formular.i18n

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/Formular
  license: MIT License
  summary: 'Formular is a library for validation of form-like data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
