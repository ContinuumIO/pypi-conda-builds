package:
  name: tornroutes
  version: "0.5.1"

source:
  fn: tornroutes-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornroutes/tornroutes-0.5.1.tar.gz
  md5: dd967993a0c15bc28860f2f8cbbd1b9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornroutes = tornroutes:main
    #
    # Would create an entry point called tornroutes that calls tornroutes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tornroutes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nod/tornroutes
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Tornado Web Route Decorator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
