package:
  name: batch-queue
  version: "0.4"

source:
  fn: batch-queue-0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/batch-queue/batch-queue-0.4.tar.gz
  md5: 260d3443c945bebe666e7f8f0ba8c05c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - batch-queue = batch-queue:main
    #
    # Would create an entry point called batch-queue that calls batch-queue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - twisted

  run:
    - python
    - twisted

test:
  # Python imports
  imports:
    - queue_server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/batch-queue/
  license: UNKNOWN
  summary: 'simple batch queuing system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
