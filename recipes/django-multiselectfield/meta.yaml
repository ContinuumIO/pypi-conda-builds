package:
  name: django-multiselectfield
  version: "0.1.3"

source:
  fn: django-multiselectfield-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multiselectfield/django-multiselectfield-0.1.3.tar.gz
  md5: 3c8f789470a8bebf81a0101a9ef56752
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multiselectfield = django-multiselectfield:main
    #
    # Would create an entry point called django-multiselectfield that calls django-multiselectfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - multiselectfield
    - multiselectfield.db
    - multiselectfield.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goinnn/django-multiselectfield
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django multiple select field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
