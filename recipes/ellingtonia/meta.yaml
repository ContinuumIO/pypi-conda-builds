package:
  name: ellingtonia
  version: "0.0.1"

source:
  fn: ellingtonia-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ellingtonia/ellingtonia-0.0.1.tar.gz
  md5: 276d985524e0e85f342759f212f10f39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ellingtonia = ellingtonia:main
    #
    # Would create an entry point called ellingtonia that calls ellingtonia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.1.1
    - flask ==0.10

  run:
    - python
    - django >=1.1.1
    - flask ==0.10

test:
  # Python imports
  imports:
    - bin
    - ellingtonia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'ellingtonia, a documentation indexing tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
