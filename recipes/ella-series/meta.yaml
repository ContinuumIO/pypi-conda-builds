package:
  name: ella-series
  version: "0.1"

source:
  fn: Ella-Series-0.1.tar.gz
  url: https://pypi.python.org/packages/source/E/Ella-Series/Ella-Series-0.1.tar.gz
  md5: 5b0c1c20201bf62a0d8098d5ba74e228
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ella-series = ella-series:main
    #
    # Would create an entry point called ella-series that calls ella-series.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ella >=2.0.4

  run:
    - python
    - ella >=2.0.4

test:
  # Python imports
  imports:
    - ella_series

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ella/ella-series/
  license: BSD License
  summary: 'Ella Series - add support for series of Publishable objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
