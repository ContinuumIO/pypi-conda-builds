package:
  name: blogtool
  version: "1.1.2"

source:
  fn: blogtool-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/blogtool/blogtool-1.1.2.tar.gz
  md5: 034203bc5f722fce5e57ab42cb7c82ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blogtool = blogtool:main
    #
    # Would create an entry point called blogtool that calls blogtool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown
    - lxml

  run:
    - python
    - markdown
    - lxml

test:
  # Python imports
  imports:
    - blogtool
    - blogtool.xmlproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lama7/blogtool
  license: MIT License
  summary: 'A command-line, XMLRPC based blog client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
