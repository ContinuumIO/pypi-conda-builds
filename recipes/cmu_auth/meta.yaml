package:
  name: cmu_auth
  version: "0.1.6"

source:
  fn: cmu_auth-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/cmu_auth/cmu_auth-0.1.6.tar.gz
  md5: 348cdfa187cbf861bcdc2c161c556b1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmu_auth = cmu_auth:main
    #
    # Would create an entry point called cmu_auth that calls cmu_auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.1
    - future >=0.14.3

  run:
    - python
    - requests >=2.0.1
    - future >=0.14.3

test:
  # Python imports
  imports:
    - cmu_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/willcrichton/cmu_auth
  license: MIT
  summary: 'Simple python methods for authenticating to Carnegie Mellon Unversity services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
