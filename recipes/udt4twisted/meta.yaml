package:
  name: udt4twisted
  version: "0.2.6"

source:
  fn: udt4twisted-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/u/udt4twisted/udt4twisted-0.2.6.tar.gz
  md5: 84cb13ef087ef284192b48b880dcc0fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - udt4twisted = udt4twisted:main
    #
    # Would create an entry point called udt4twisted that calls udt4twisted.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyudt4
    - twisted

  run:
    - python
    - pyudt4
    - twisted

test:
  # Python imports
  imports:
    - twisted.plugins
    - udt4twisted

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v3 (GPLv3)
  summary: 'An UDT4 integration package for twisted.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
