package:
  name: easy-extract
  version: "0.1.1"

source:
  fn: easy-extract-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/easy-extract/easy-extract-0.1.1.tar.gz
  md5: 7efeaea5a270eee3476f8a78b8058be6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy-extract = easy-extract:main
    #
    # Would create an entry point called easy-extract that calls easy-extract.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - easy_extract
    - easy_extract.archives

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fantomas.willbreak.it
  license: GPL
  summary: 'Easy extraction of archives collections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
