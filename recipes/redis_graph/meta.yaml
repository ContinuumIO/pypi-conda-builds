package:
  name: redis_graph
  version: "1.0"

source:
  fn: redis_graph-1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_graph/redis_graph-1.0.tar.gz
  md5: 8b9b0aa94fdd059370debac080cd7a0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_graph = redis_graph:main
    #
    # Would create an entry point called redis_graph that calls redis_graph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_graph
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.amix.dk/
  license: BSD License
  summary: 'Python graph database implemented on top of Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
