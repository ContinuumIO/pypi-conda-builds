package:
  name: kirb
  version: "1.6.2"

source:
  fn: kirb-1.6.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kirb/kirb-1.6.2.tar.gz
  md5: 1ca39b334dfeccc1f9f23bc1ee917e0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kirb = kirb:main
    #
    # Would create an entry point called kirb that calls kirb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.3
    - watchdog >=0.6

  run:
    - python
    - docutils >=0.3
    - watchdog >=0.6

test:
  # Python imports
  imports:
    - kirb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hbmartin/kirb
  license: BSD License
  summary: 'Continuous Integration & ReBuild'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
