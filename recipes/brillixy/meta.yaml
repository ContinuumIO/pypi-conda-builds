package:
  name: brillixy
  version: "0.6.1"

source:
  fn: brillixy-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brillixy/brillixy-0.6.1.tar.gz
  md5: c25a96bf5114b4c32dc5b76feb873a4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brillixy = brillixy:main
    #
    # Would create an entry point called brillixy that calls brillixy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - brillixy
    - brillixy.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://05bit.com/brillixy/
  license: Creative Commons Attribution-NonCommercial 3.0 Unported License
  summary: 'Customizations for Django admin interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
