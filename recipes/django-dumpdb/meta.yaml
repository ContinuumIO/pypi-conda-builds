package:
  name: django-dumpdb
  version: "0.1"

source:
  fn: django-dumpdb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dumpdb/django-dumpdb-0.1.tar.gz
  md5: 3e0507bda5f5ebe17eb65ae6eef57bcd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dumpdb = django-dumpdb:main
    #
    # Would create an entry point called django-dumpdb that calls django-dumpdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_dumpdb
    - testproject
    - testproject.testmodels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-dumpdb/
  license: MIT License
  summary: 'A better, faster, stronger alternative for manage.py dumpdata'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
