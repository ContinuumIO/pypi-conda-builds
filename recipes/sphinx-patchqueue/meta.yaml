package:
  name: sphinx-patchqueue
  version: "0.4.0"

source:
  fn: sphinx-patchqueue-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-patchqueue/sphinx-patchqueue-0.4.0.tar.gz
  md5: 6991f0895636957f6bf173f5cddddf5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-patchqueue = sphinx-patchqueue:main
    #
    # Would create an entry point called sphinx-patchqueue that calls sphinx-patchqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sphinx
    - mercurial

  run:
    - python
    - sphinx
    - mercurial

test:
  # Python imports
  imports:
    - patchqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/masklinn/sphinx-patchqueue
  license: BSD
  summary: 'Sphinx extension for embedding sequences of file alterations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
