package:
  name: collective.favoriting
  version: "1.0.2"

source:
  fn: collective.favoriting-1.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.favoriting/collective.favoriting-1.0.2.zip
  md5: 66cf6685971760b0ee37e8283f2b7efb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.favoriting = collective.favoriting:main
    #
    # Would create an entry point called collective.favoriting that calls collective.favoriting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.favoriting
    - collective.favoriting.browser
    - collective.favoriting.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.favoriting
  license: GNU General Public License (GPL)
  summary: 'Favoriting internal content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
