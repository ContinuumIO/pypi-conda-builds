package:
  name: apitree
  version: "0.3.3"

source:
  fn: apitree-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/a/apitree/apitree-0.3.3.tar.gz
  md5: 05c0eb702054a27b5cf552bfbcad646d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apitree = apitree:main
    #
    # Would create an entry point called apitree that calls apitree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iomanager >=0.4.0
    - mako
    - pyramid >=1.3.4

  run:
    - python
    - iomanager >=0.4.0
    - mako
    - pyramid >=1.3.4

test:
  # Python imports
  imports:
    - apitree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmatthias/apitree
  license: LICENSE.txt
  summary: 'Build an orderly web service API backend.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
