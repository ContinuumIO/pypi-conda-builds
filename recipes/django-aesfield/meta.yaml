package:
  name: django-aesfield
  version: "0.1.2"

source:
  fn: django-aesfield-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aesfield/django-aesfield-0.1.2.tar.gz
  md5: 594e4cc06792998564157689c2af7a0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aesfield = django-aesfield:main
    #
    # Would create an entry point called django-aesfield that calls django-aesfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m2crypto >=0.18
    - django >=1.3
    - m2secret >=0.1.1

  run:
    - python
    - m2crypto >=0.18
    - django >=1.3
    - m2secret >=0.1.1

test:
  # Python imports
  imports:
    - aesfield
    - aesfield/management
    - aesfield/management/commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andymckay/django-aesfield
  license: BSD
  summary: 'Django Model Field that supports AES encryption'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
