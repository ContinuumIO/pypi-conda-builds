package:
  name: django-facebook-users
  version: "0.3.0"

source:
  fn: django-facebook-users-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-users/django-facebook-users-0.3.0.tar.gz
  md5: 6667c0b651bcead3027be317244fe7b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-users = django-facebook-users:main
    #
    # Would create an entry point called django-facebook-users that calls django-facebook-users.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-facebook-api >=0.1.4

  run:
    - python
    - django-facebook-api >=0.1.4

test:
  # Python imports
  imports:
    - facebook_users
    - facebook_users.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-facebook-users
  license: BSD License
  summary: 'Django implementation for Facebook Graph API Users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
