package:
  name: cubicweb-calendar
  version: "0.6.0"

source:
  fn: cubicweb-calendar-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-calendar/cubicweb-calendar-0.6.0.tar.gz
  md5: 854a003a54a341d5e90e7b0c183fe279
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-calendar = cubicweb-calendar:main
    #
    # Would create an entry point called cubicweb-calendar that calls cubicweb-calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.9.0

  run:
    - python
    - cubicweb >=3.9.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-calendar
  license: LGPL
  summary: 'calendar component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
