package:
  name: linkzilla
  version: "0.1.5"

source:
  fn: linkzilla-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/l/linkzilla/linkzilla-0.1.5.tar.gz
  md5: 653f9e241655f4ebf060bba0fa3ef82c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkzilla = linkzilla:main
    #
    # Would create an entry point called linkzilla that calls linkzilla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - linkzilla
    - linkzilla.base
    - linkzilla.django
    - linkzilla.django.management
    - linkzilla.django.management.commands
    - linkzilla.lib
    - linkzilla.services
    - linkzilla.services.linkfeed
    - linkzilla.services.sape
    - linkzilla.storages
    - linkzilla.storages.orm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/linkzilla
  license: BSD License
  summary: 'Client library to link trade services (sape.ru, linkfeed.ru)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
