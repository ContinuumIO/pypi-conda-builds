package:
  name: opengever.mysqlconfig
  version: "1.1.2"

source:
  fn: opengever.mysqlconfig-1.1.2.zip
  url: https://pypi.python.org/packages/source/o/opengever.mysqlconfig/opengever.mysqlconfig-1.1.2.zip
  md5: 09d206adf1a42da3f63c58b7abfff352
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - opengever.mysqlconfig = opengever.mysqlconfig:main
    #
    # Would create an entry point called opengever.mysqlconfig that calls opengever.mysqlconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mysql-python
    - z3c.saconfig

  run:
    - python
    - setuptools
    - setuptools
    - mysql-python
    - z3c.saconfig

test:
  # Python imports
  imports:
    - opengever
    - opengever.mysqlconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/opengever.mysqlconfig
  license: GPL2
  summary: 'configures the mysql as database engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
