package:
  name: defusedexpat
  version: "0.4"

source:
  fn: defusedexpat-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/defusedexpat/defusedexpat-0.4.tar.gz
  md5: 8091bb85a6842456a4b7fe842dc13f89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - defusedexpat = defusedexpat:main
    #
    # Would create an entry point called defusedexpat that calls defusedexpat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - defusedexpat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tiran/defusedexpat
  license: Python Software Foundation License
  summary: 'XML bomb protection with modified expat parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
