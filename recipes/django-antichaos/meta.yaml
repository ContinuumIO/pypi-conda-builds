package:
  name: django-antichaos
  version: "0.2.0"

source:
  fn: django-antichaos-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-antichaos/django-antichaos-0.2.0.tar.gz
  md5: 38e04f311924c530fc53f9813493a5f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-antichaos = django-antichaos:main
    #
    # Would create an entry point called django-antichaos that calls django-antichaos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_antichaos
    - django_antichaos.management
    - django_antichaos.management.commands
    - django_antichaos.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-antichaos/
  license: BSD License
  summary: "Django application for easy tag manipulation.It's addon to django-tagging or django-taggin-ng applications."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
