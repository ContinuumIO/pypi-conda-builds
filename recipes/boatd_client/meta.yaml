package:
  name: boatd_client
  version: "0.1.5"

source:
  fn: boatd_client-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/boatd_client/boatd_client-0.1.5.tar.gz
  md5: a75cb569493c0088282c84e858c5b92e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boatd_client = boatd_client:main
    #
    # Would create an entry point called boatd_client that calls boatd_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt >=0.6

  run:
    - python
    - docopt >=0.6

test:
  # Python imports
  imports:
    - boatd_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/boatd/python-boatd
  license: GNU General Public License v3 (GPLv3)
  summary: 'Python wrapper for the boatd API, used to write behavior scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
