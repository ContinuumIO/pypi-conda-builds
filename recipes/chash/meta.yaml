package:
  name: chash
  version: "0.1.0"

source:
  fn: chash-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chash/chash-0.1.0.tar.gz
  md5: eedfde18b7aa963c64dc910ddff77890
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chash = chash:main
    #
    # Would create an entry point called chash that calls chash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cachetools
    - numpy
    - pandas
    - xxh

  run:
    - python
    - cachetools
    - numpy
    - pandas
    - xxh

test:
  # Python imports
  imports:
    - chash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lebedov/chash
  license: BSD License
  summary: 'Content-based hash'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
