package:
  name: django-admin-field
  version: "0.0.1"

source:
  fn: django-admin-field-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-field/django-admin-field-0.0.1.tar.gz
  md5: 287d680b258fa0e9df09d98cff666db6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-field = django-admin-field:main
    #
    # Would create an entry point called django-admin-field that calls django-admin-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_admin_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/meteozond/django-admin-field
  license: BSD
  summary: 'Django ModelAdmin field syntax simplifier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
