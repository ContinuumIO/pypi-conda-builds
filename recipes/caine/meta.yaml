package:
  name: caine
  version: "0.1.12"

source:
  fn: caine-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/c/caine/caine-0.1.12.tar.gz
  md5: 3329389353c3ce976442cbf2e951b13c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caine = caine:main
    #
    # Would create an entry point called caine that calls caine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - caine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/will-weiss/caine/
  license: http://www.apache.org/licenses/LICENSE-2.0.html
  summary: 'supporting actors offer concurrent inbox processing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
