package:
  name: micropython-uasyncio.queues
  version: "0.1"

source:
  fn: micropython-uasyncio.queues-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-uasyncio.queues/micropython-uasyncio.queues-0.1.tar.gz
  md5: e45b878959fd4ea13e46a450387f9180
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-uasyncio.queues = micropython-uasyncio.queues:main
    #
    # Would create an entry point called micropython-uasyncio.queues that calls micropython-uasyncio.queues.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - micropython-uasyncio.core
    - micropython-collections.deque

  run:
    - python
    - micropython-uasyncio.core
    - micropython-collections.deque

test:
  # Python imports
  imports:
    - uasyncio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: MIT
  summary: 'uasyncio.queues module for MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
