package:
  name: safejson
  version: "0.9b"

source:
  fn: safeJSON-0.9b.tar.gz
  url: https://pypi.python.org/packages/source/s/safeJSON/safeJSON-0.9b.tar.gz
  md5: 826504cc06029b928dd9af94267389d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - safejson = safejson:main
    #
    # Would create an entry point called safejson that calls safejson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - safejson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'safeJSON simplifies the process of working with JSON object in python by suppressing both IndexError and KeyError exceptions on parsed objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
