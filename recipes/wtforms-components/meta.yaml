package:
  name: wtforms-components
  version: "0.9.7"

source:
  fn: WTForms-Components-0.9.7.tar.gz
  url: https://pypi.python.org/packages/source/W/WTForms-Components/WTForms-Components-0.9.7.tar.gz
  md5: b48c4364f854f4189c41715da8bd7496
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-components = wtforms-components:main
    #
    # Would create an entry point called wtforms-components that calls wtforms-components.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms >=1.0.4
    - sqlalchemy >=0.8.0
    - sqlalchemy-utils >=0.23.0
    - six >=1.4.1
    - validators >=0.5.0
    - intervals >=0.2.0

  run:
    - python
    - wtforms >=1.0.4
    - sqlalchemy >=0.8.0
    - sqlalchemy-utils >=0.23.0
    - six >=1.4.1
    - validators >=0.5.0
    - intervals >=0.2.0

test:
  # Python imports
  imports:
    - wtforms_components
    - wtforms_components.fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/wtforms-components
  license: BSD License
  summary: 'Additional fields, validators and widgets for WTForms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
