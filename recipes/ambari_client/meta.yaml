package:
  name: ambari_client
  version: "1.0.3-SNAPSHOT"

source:
  fn: ambari_client-1.0.3-SNAPSHOT.tar.gz
  url: https://pypi.python.org/packages/source/a/ambari_client/ambari_client-1.0.3-SNAPSHOT.tar.gz
  md5: bedc6b862091bf14afc75f1a9ac0ef4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ambari_client = ambari_client:main
    #
    # Would create an entry point called ambari_client that calls ambari_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ambari_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'Ambari python REST API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
