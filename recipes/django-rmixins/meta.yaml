package:
  name: django-rmixins
  version: "0.0.1"

source:
  fn: django-rmixins-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rmixins/django-rmixins-0.0.1.tar.gz
  md5: b7d024b28e29d035bedf2a5bcab9de1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rmixins = django-rmixins:main
    #
    # Would create an entry point called django-rmixins that calls django-rmixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rmixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r0sk/django-rmixins
  license: BSD License
  summary: 'Yet another django mixins compilation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
