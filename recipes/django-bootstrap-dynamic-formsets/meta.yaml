package:
  name: django-bootstrap-dynamic-formsets
  version: "0.4.3"

source:
  fn: django-bootstrap-dynamic-formsets-0.4.3.zip
  url: https://pypi.python.org/packages/source/d/django-bootstrap-dynamic-formsets/django-bootstrap-dynamic-formsets-0.4.3.zip
  md5: 7917d95de500c20a9f090678b27cc8f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-dynamic-formsets = django-bootstrap-dynamic-formsets:main
    #
    # Would create an entry point called django-bootstrap-dynamic-formsets that calls django-bootstrap-dynamic-formsets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-bootstrap3

  run:
    - python
    - django-bootstrap3

test:
  # Python imports
  imports:
    - django_bootstrap_dynamic_formsets
    - django_bootstrap_dynamic_formsets.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fh-brandenburg.de
  license: MIT License
  summary: 'JavaScript-enhanced dynamic formsets using Bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
