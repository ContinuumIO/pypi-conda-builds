package:
  name: setuptools_webdav
  version: "0.0.1"

source:
  fn: setuptools_webdav-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools_webdav/setuptools_webdav-0.0.1.tar.gz
  md5: c148ddad7699ef65198c1af527023af5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools_webdav = setuptools_webdav:main
    #
    # Would create an entry point called setuptools_webdav that calls setuptools_webdav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python_webdav_library >=0.3.0

  run:
    - python
    - setuptools
    - python_webdav_library >=0.3.0

test:
  # Python imports
  imports:
    - setuptools_webdav

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mneagul/setuptools-webdav
  license: Apache Software License
  summary: 'Setuptools/distribute plugin for uploading to webdav servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
