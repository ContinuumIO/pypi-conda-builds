package:
  name: netstream
  version: "0.1"

source:
  fn: netstream-0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/netstream/netstream-0.1.tar.gz
  md5: 80480b486490dcd27f8cec825ca288b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netstream = netstream:main
    #
    # Would create an entry point called netstream that calls netstream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - netstream

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hhromic/python-netstream
  license: UNKNOWN
  summary: 'A NetStream implementation in Python for GraphStream'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
