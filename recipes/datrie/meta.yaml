package:
  name: datrie
  version: "0.7"

source:
  fn: datrie-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/datrie/datrie-0.7.tar.gz
  md5: d3ffdf929368c64ef6b67a2ca67c3516
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datrie = datrie:main
    #
    # Would create an entry point called datrie that calls datrie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - datrie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kmike/datrie/
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Super-fast, efficiently stored Trie for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
