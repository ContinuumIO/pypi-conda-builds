package:
  name: asm2mmc
  version: "0.1"

source:
  fn: asm2mmc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/asm2mmc/asm2mmc-0.1.tar.gz
  md5: a616d7f25402c38b558f6925242428ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asm2mmc = asm2mmc:main
    #
    # Would create an entry point called asm2mmc that calls asm2mmc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - asm2mmc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Memorycoin/asm2mmc
  license: MIT
  summary: 'memorycoin address decoder for asm_hash strings on the transaction data. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
