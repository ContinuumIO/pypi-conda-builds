package:
  name: juggernaut
  version: "0.2"

source:
  fn: juggernaut-0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/juggernaut/juggernaut-0.2.tar.gz
  md5: 08ee4843bc9b7ecc97a459fc3408c63e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - juggernaut = juggernaut:main
    #
    # Would create an entry point called juggernaut that calls juggernaut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - juggernaut

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mitsuhiko/python-juggernaut
  license: BSD License
  summary: 'Client library for juggernaut.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
