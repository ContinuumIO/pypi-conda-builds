package:
  name: django-model-report
  version: "0.2.1"

source:
  fn: django-model-report-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-report/django-model-report-0.2.1.tar.gz
  md5: 70711b0ed32a13ea70b938b6072d5b38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-report = django-model-report:main
    #
    # Would create an entry point called django-model-report that calls django-model-report.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - reportlab
    - html5lib
    - beautifulsoup
    - xhtml2pdf
    - xlwt ==0.7.4

  run:
    - python
    - django >=1.3
    - reportlab
    - html5lib
    - beautifulsoup
    - xhtml2pdf
    - xlwt ==0.7.4

test:
  # Python imports
  imports:
    - model_report
    - model_report.highcharts
    - model_report.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juanpex/django-model-report
  license: BSD License
  summary: 'Django reports integrated with highcharts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
