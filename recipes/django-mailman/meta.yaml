package:
  name: django-mailman
  version: "0.4"

source:
  fn: django-mailman-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailman/django-mailman-0.4.tar.gz
  md5: 9a9855c3e6811633c9f8821dafe68374
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailman = django-mailman:main
    #
    # Would create an entry point called django-mailman that calls django-mailman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_mailman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/albertoconnor/django-mailman
  license: GNU LGPL v2.1
  summary: 'Interface to Mailman Web-API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
