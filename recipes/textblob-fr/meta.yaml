package:
  name: textblob-fr
  version: "0.2.0"

source:
  fn: textblob-fr-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/t/textblob-fr/textblob-fr-0.2.0.tar.gz
  md5: b7bf10ea42c87dbe7884682480ccb1ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - textblob-fr = textblob-fr:main
    #
    # Would create an entry point called textblob-fr that calls textblob-fr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - textblob >=0.8.0

  run:
    - python
    - textblob >=0.8.0

test:
  # Python imports
  imports:
    - textblob_fr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sloria/textblob-fr
  license: MIT License
  summary: 'French language support for TextBlob.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
