package:
  name: mixpanel_django
  version: "0.1"

source:
  fn: mixpanel-django-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mixpanel_django/mixpanel-django-0.1.tar.gz
  md5: 5f105c5c74b379aa4e66752a9c6119a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixpanel_django = mixpanel_django:main
    #
    # Would create an entry point called mixpanel_django that calls mixpanel_django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1
    - beanstalkc >=0.2
    - httpagentparser >=0.8.2

  run:
    - python
    - django >=1.1
    - beanstalkc >=0.2
    - httpagentparser >=0.8.2

test:
  # Python imports
  imports:
    - mixpanel_django
    - mixpanel_django.backends
    - mixpanel_django.example
    - mixpanel_django.example.mp_example
    - mixpanel_django.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fla.sam/mixpanel_django/
  license: Apache Licence 2.0
  summary: 'django reuseable app, it can simply track events use mixpanel API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
