package:
  name: django-inplaceedit-bootstrap
  version: "0.1.1"

source:
  fn: django-inplaceedit-bootstrap-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inplaceedit-bootstrap/django-inplaceedit-bootstrap-0.1.1.tar.gz
  md5: 9e2f3e2036a9a2029ba96d2204b5c6c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inplaceedit-bootstrap = django-inplaceedit-bootstrap:main
    #
    # Would create an entry point called django-inplaceedit-bootstrap that calls django-inplaceedit-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-inplaceedit >=1.2.3

  run:
    - python
    - django-inplaceedit >=1.2.3

test:
  # Python imports
  imports:
    - inplaceeditform_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goinnn/django-inplaceedit-bootstrap
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Integration of django-inplaceedit and bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
