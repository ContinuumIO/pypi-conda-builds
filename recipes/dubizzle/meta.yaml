package:
  name: dubizzle
  version: "0.1"

source:
  fn: dubizzle-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dubizzle/dubizzle-0.1.tar.gz
  md5: 5040940c25a9afd35d370f235c68f1e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dubizzle = dubizzle:main
    #
    # Would create an entry point called dubizzle that calls dubizzle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

test:
  # Python imports
  imports:
    - dubizzle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Cyph0n/dubizzle/
  license: MIT License
  summary: 'A scraping-based API for Dubizzle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
