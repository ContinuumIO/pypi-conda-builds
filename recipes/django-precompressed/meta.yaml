package:
  name: django-precompressed
  version: "0.0.2"

source:
  fn: django-precompressed-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-precompressed/django-precompressed-0.0.2.tar.gz
  md5: be25f2d5fb6fc56d8ebd718948381804
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-precompressed = django-precompressed:main
    #
    # Would create an entry point called django-precompressed that calls django-precompressed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - precompressed
    - precompressed/storage
    - precompressed/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jhmaddox/django-precompressed/
  license: BSD
  summary: 'precompressed is a pluggable application for Django developers who want to serve precompressed (gzipped) static files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
