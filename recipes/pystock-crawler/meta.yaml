package:
  name: pystock-crawler
  version: "0.8.2"

source:
  fn: pystock-crawler-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pystock-crawler/pystock-crawler-0.8.2.tar.gz
  md5: c9b7739b3be1b6347a3ad42178b5984c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystock-crawler = pystock-crawler:main
    #
    # Would create an entry point called pystock-crawler that calls pystock-crawler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt ==0.6.2
    - leveldb ==0.193
    - scrapy ==0.24.4
    - service-identity ==1.0.0

  run:
    - python
    - docopt ==0.6.2
    - leveldb ==0.193
    - scrapy ==0.24.4
    - service-identity ==1.0.0

test:
  # Python imports
  imports:
    - pystock_crawler
    - pystock_crawler.spiders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eliangcs/pystock-crawler
  license: MIT License
  summary: 'Crawl and parse stock historical data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
