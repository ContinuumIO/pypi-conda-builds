package:
  name: xstatic-extjs
  version: "4.2.1.883.1"

source:
  fn: XStatic-ExtJS-4.2.1.883.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-ExtJS/XStatic-ExtJS-4.2.1.883.1.tar.gz
  md5: b8af72bfc7de8d95c545ef19926fc29a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-extjs = xstatic-extjs:main
    #
    # Would create an entry point called xstatic-extjs that calls xstatic-extjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.extjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sencha.com/products/extjs/
  license: GPLv3 (same as ExtJS)
  summary: 'ExtJS 4.2.1.883 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
