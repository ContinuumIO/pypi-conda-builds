package:
  name: jobmanager-api
  version: "0.2.4"

source:
  fn: jobmanager-api-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/j/jobmanager-api/jobmanager-api-0.2.4.tar.gz
  md5: a8197586c3352cd233fe009fa97e7088
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobmanager-api = jobmanager-api:main
    #
    # Would create an entry point called jobmanager-api that calls jobmanager-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tbx
    - jobmanager-common
    - six >=1.8.0
    - pymongo <3.0
    - mongoengine
    - flask
    - requests

  run:
    - python
    - tbx
    - jobmanager-common
    - six >=1.8.0
    - pymongo <3.0
    - mongoengine
    - flask
    - requests

test:
  # Python imports
  imports:
    - jobmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ronhanson/python-jobmanager-api
  license: MIT License
  summary: 'Job Manager API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
