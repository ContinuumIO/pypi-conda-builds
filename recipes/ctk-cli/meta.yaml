package:
  name: ctk-cli
  version: "1.0"

source:
  fn: ctk-cli-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/ctk-cli/ctk-cli-1.0.tar.gz
  md5: 9b9b12ce43410bba612cc99be31b4cd4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ctk-cli = ctk-cli:main
    #
    # Would create an entry point called ctk-cli that calls ctk-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ctk_cli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/commontk/ctk-cli
  license: Apache Software License
  summary: 'Python interface for inspecting and running CLI modules (as defined by CommonTK)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
