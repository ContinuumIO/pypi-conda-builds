package:
  name: django-yamlfield
  version: "1.0.1"

source:
  fn: django-yamlfield-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yamlfield/django-yamlfield-1.0.1.tar.gz
  md5: d705194a9f0226509ca6fe62fef09736
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yamlfield = django-yamlfield:main
    #
    # Would create an entry point called django-yamlfield that calls django-yamlfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.10
    - six >=1.4.1

  run:
    - python
    - pyyaml >=3.10
    - six >=1.4.1

test:
  # Python imports
  imports:
    - yamlfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A Django database field for storing YAML data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
