package:
  name: js.graphael
  version: "0.5.1"

source:
  fn: js.graphael-0.5.1.zip
  url: https://pypi.python.org/packages/source/j/js.graphael/js.graphael-0.5.1.zip
  md5: a21f2ec6ecea5c085725901840455614
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.graphael = js.graphael:main
    #
    # Would create an entry point called js.graphael that calls js.graphael.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.raphael
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.raphael
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.graphael

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/js.graphael
  license: BSD
  summary: u'Fanstatic packaging of gRapha\xebl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
