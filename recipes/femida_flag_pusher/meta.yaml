package:
  name: femida_flag_pusher
  version: "0.0.0"

source:
  fn: femida_flag_pusher-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/f/femida_flag_pusher/femida_flag_pusher-0.0.0.tar.gz
  md5: 3cc395406f27567f8bcae2505cd665b2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - femida_flag_pusher = femida_flag_pusher:main
    #
    # Would create an entry point called femida_flag_pusher that calls femida_flag_pusher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - setuptools
    - requests

test:
  # Python imports
  imports:
    - femida_flag_pusher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Koibasta/femida-flag-pusher-python
  license: MIT
  summary: 'Flag pusher for Femida'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
