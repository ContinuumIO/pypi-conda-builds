package:
  name: django-fakeimg
  version: "0.1.2"

source:
  fn: django-fakeimg-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fakeimg/django-fakeimg-0.1.2.tar.gz
  md5: 91231b18090a77a28b6882b612a43a19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fakeimg = django-fakeimg:main
    #
    # Would create an entry point called django-fakeimg that calls django-fakeimg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - fakeimg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/scao/django-fakeimg
  license: BSD License
  summary: 'A django port of fakeimg.pl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
