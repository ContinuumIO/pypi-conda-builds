package:
  name: is_this_a_prime_number
  version: "1.1.0."

source:
  fn: Is_this_a_prime_number-1.1.0..zip
  url: https://pypi.python.org/packages/source/I/Is_this_a_prime_number/Is_this_a_prime_number-1.1.0..zip
  md5: a75cab6d54aea0e9c466a9954aa4bc26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - is_this_a_prime_number = is_this_a_prime_number:main
    #
    # Would create an entry point called is_this_a_prime_number that calls is_this_a_prime_number.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - is_this_a_prime_number

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Tells you whether a number is a prime or not.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
