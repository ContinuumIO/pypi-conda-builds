package:
  name: gbpy
  version: "0.1.1"

source:
  fn: GBpy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GBpy/GBpy-0.1.1.tar.gz
  md5: dd8f58390f68362de1711dd19bd14be5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gbpy = gbpy:main
    #
    # Would create an entry point called gbpy that calls gbpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - GBpy
    - GBpy/csl_generators
    - GBpy/docs
    - GBpy/pkl_files
    - gbpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adehgha/GBpy
  license: GNU General Public License (GPL)
  summary: 'GBpy is an opensource python package for calculating the geometric properties of interfaces in crystals.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
