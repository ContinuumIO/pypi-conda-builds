package:
  name: django-multi-email-field
  version: "0.3.1"

source:
  fn: django-multi-email-field-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multi-email-field/django-multi-email-field-0.3.1.tar.gz
  md5: 8d15facbb69b98443d6c1706599fc4b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multi-email-field = django-multi-email-field:main
    #
    # Would create an entry point called django-multi-email-field that calls django-multi-email-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - multi_email_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fle/django-multi-email-field
  license: LPGL, see LICENSE file.
  summary: 'Provides a model field and a form field to manage list of e-mails'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
