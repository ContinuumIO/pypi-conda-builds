package:
  name: django-chinook
  version: "1.0.0"

source:
  fn: django-chinook-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chinook/django-chinook-1.0.0.tar.gz
  md5: 6b58cbaa8590a74e240b2016293ac0cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chinook = django-chinook:main
    #
    # Would create an entry point called django-chinook that calls django-chinook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - chinook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/xeoscript/django-chinook
  license: Freely Distributable
  summary: 'A django app with models for chinook sample database and fixtures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
