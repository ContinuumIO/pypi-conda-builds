package:
  name: ncplugin
  version: "0.5"

source:
  fn: NCPlugin-0.5.tar.gz
  url: https://pypi.python.org/packages/source/N/NCPlugin/NCPlugin-0.5.tar.gz
  md5: e0c60ae7b1cbd9942c7017b7d4b23029
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ncplugin = ncplugin:main
    #
    # Would create an entry point called ncplugin that calls ncplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pync ==1.4

  run:
    - python
    - setuptools
    - pync ==1.4

test:
  # Python imports
  imports:
    - ncplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dreynolds/nose-notification-centre
  license: GNU LGPL
  summary: 'Nose plugin to raise an OS X Notification Center alert when a test run has finished'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
