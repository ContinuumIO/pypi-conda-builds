package:
  name: openerp-web-view-editor
  version: "7.0.406"

source:
  fn: openerp-web-view-editor-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-web-view-editor/openerp-web-view-editor-7.0.406.tar.gz
  md5: a17899565029f95103e637d043e2ae92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-web-view-editor = openerp-web-view-editor:main
    #
    # Would create an entry point called openerp-web-view-editor that calls openerp-web-view-editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-web

  run:
    - python
    - openerp-web

test:
  # Python imports
  imports:
    - openerp.addons.web_view_editor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'View Editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
