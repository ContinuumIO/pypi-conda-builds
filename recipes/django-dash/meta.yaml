package:
  name: django-dash
  version: "0.4.13"

source:
  fn: django-dash-0.4.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dash/django-dash-0.4.13.tar.gz
  md5: b36db12641f4790e77adaa5eb0867b17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dash = django-dash:main
    #
    # Would create an entry point called django-dash that calls django-dash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow >=2.1.0
    - django-autoslug >=1.7.1
    - django-slim >=0.7.5
    - django-tinymce >=1.5.3
    - feedparser >=5.1.3
    - ordereddict >=1.1
    - pif >=0.5,<1.0
    - six >=1.4.1
    - easy-thumbnails >=1.4,<2.0
    - transliterate >=1.5,<2.0
    - vishap >=0.1.2,<2.0
    - unidecode
    - django-localeurl

  run:
    - python
    - pillow >=2.1.0
    - django-autoslug >=1.7.1
    - django-slim >=0.7.5
    - django-tinymce >=1.5.3
    - feedparser >=5.1.3
    - ordereddict >=1.1
    - pif >=0.5,<1.0
    - six >=1.4.1
    - easy-thumbnails >=1.4,<2.0
    - transliterate >=1.5,<2.0
    - vishap >=0.1.2,<2.0
    - unidecode
    - django-localeurl

test:
  # Python imports
  imports:
    - dash
    - dash.contrib
    - dash.contrib.apps
    - dash.contrib.apps.public_dashboard
    - dash.contrib.layouts
    - dash.contrib.layouts.android
    - dash.contrib.layouts.bootstrap2
    - dash.contrib.layouts.windows8
    - dash.contrib.plugins
    - dash.contrib.plugins.dummy
    - dash.contrib.plugins.image
    - dash.contrib.plugins.memo
    - dash.contrib.plugins.rss_feed
    - dash.contrib.plugins.rss_feed.templatetags
    - dash.contrib.plugins.url
    - dash.contrib.plugins.video
    - dash.contrib.plugins.weather
    - dash.lib
    - dash.lib.tinymce
    - dash.management
    - dash.management.commands
    - dash.migrations
    - dash.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/barseghyanartur/django-dash/
  license: GNU General Public License v2 (GPLv2) or GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Customisable, modular dashboard application framework for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
