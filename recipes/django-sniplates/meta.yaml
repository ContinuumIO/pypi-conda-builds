package:
  name: django-sniplates
  version: "0.2.2"

source:
  fn: django-sniplates-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sniplates/django-sniplates-0.2.2.tar.gz
  md5: 64ab174a83fd858d1fcbb2c790c34819
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sniplates = django-sniplates:main
    #
    # Would create an entry point called django-sniplates that calls django-sniplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - sniplates
    - sniplates.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/funkybob/django-sniplates
  license: BSD License
  summary: 'Efficient template macro sets for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
