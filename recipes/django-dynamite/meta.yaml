package:
  name: django-dynamite
  version: "0.4.1"

source:
  fn: django-dynamite-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamite/django-dynamite-0.4.1.tar.gz
  md5: 3ba7ec5d20c92eff2bcf667b50b92406
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamite = django-dynamite:main
    #
    # Would create an entry point called django-dynamite that calls django-dynamite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - south >=0.7

  run:
    - python
    - django >=1.3
    - south >=0.7

test:
  # Python imports
  imports:
    - dynamite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/z4r/django-dynamite
  license: GNU General Public License (GPL)
  summary: 'Dynamic models framework\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
