package:
  name: cmsplugin_contact_plus
  version: "1.1.14"

source:
  fn: cmsplugin_contact_plus-1.1.14.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_contact_plus/cmsplugin_contact_plus-1.1.14.tar.gz
  md5: 465c1f64e6690472aa1f561cbd2bf4dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_contact_plus = cmsplugin_contact_plus:main
    #
    # Would create an entry point called cmsplugin_contact_plus that calls cmsplugin_contact_plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-inline-ordering
    - django-simple-math-captcha
    - jsonfield

  run:
    - python
    - django-inline-ordering
    - django-simple-math-captcha
    - jsonfield

test:
  # Python imports
  imports:
    - cmsplugin_contact_plus
    - cmsplugin_contact_plus.migrations
    - cmsplugin_contact_plus.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arteria/cmsplugin-contact-plus/
  license: BSD License
  summary: 'A django CMS plugin to dynamically create contact forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
