package:
  name: django_popcorn
  version: "1.0.0"

source:
  fn: django_popcorn-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_popcorn/django_popcorn-1.0.0.tar.gz
  md5: 449c7a7ba1ebaacdde40a4934e0826b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_popcorn = django_popcorn:main
    #
    # Would create an entry point called django_popcorn that calls django_popcorn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-reform

  run:
    - python
    - django-reform

test:
  # Python imports
  imports:
    - django_popcorn
    - popcorn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alixedi/django_popcorn
  license: BSD License
  summary: 'Add-another pop-ups a la django-admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
