package:
  name: djungle-modernizer
  version: "0.0.0"

source:
  fn: djungle-modernizer-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djungle-modernizer/djungle-modernizer-0.0.0.tar.gz
  md5: 926d663114731701e9a1e17c3652b446
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djungle-modernizer = djungle-modernizer:main
    #
    # Would create an entry point called djungle-modernizer that calls djungle-modernizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1

  run:
    - python
    - django >=1.3.1

test:
  # Python imports
  imports:
    - djungle
    - djungle.modernizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djungle/djungle-modernizer
  license: GNU General Public License (GPL)
  summary: 'Modernizr and HTML5 Shiv as Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
