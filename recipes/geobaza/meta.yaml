package:
  name: geobaza
  version: "1.0.6.14.12"

source:
  fn: geobaza-1.0.6.14.12.tar.gz
  url: https://pypi.python.org/packages/source/g/geobaza/geobaza-1.0.6.14.12.tar.gz
  md5: 726562a251f51cd9f9b5d4305e051b34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobaza = geobaza:main
    #
    # Would create an entry point called geobaza that calls geobaza.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - geobaza

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://geobaza.ru/
  license: BSD License
  summary: 'Geobaza is the software for geolocation by IP-address.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
