package:
  name: aino-jstools
  version: "0.1.0.6"

source:
  fn: aino-jstools-0.1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/aino-jstools/aino-jstools-0.1.0.6.tar.gz
  md5: e806d41cf1f6da5654c05ed92766b7ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aino-jstools = aino-jstools:main
    #
    # Would create an entry point called aino-jstools that calls aino-jstools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jstools
    - jstools.conf
    - jstools.management
    - jstools.management.commands
    - jstools.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/aino-jstools
  license: BSD License
  summary: 'JavaScript tools for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
