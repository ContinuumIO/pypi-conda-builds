package:
  name: missingbits
  version: "0.0.16"

source:
  fn: missingbits-0.0.16.zip
  url: https://pypi.python.org/packages/source/m/missingbits/missingbits-0.0.16.zip
  md5: 89041e55e1a76e385d26e66e056b7fb5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - missingbits = missingbits:main
    #
    # Would create an entry point called missingbits that calls missingbits.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - missingbits

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/isotoma/missingbits
  license: Zope Public License
  summary: 'Buildout parts that make you smile'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
