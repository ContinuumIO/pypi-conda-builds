package:
  name: django-nocaptcha-recaptcha
  version: "0.0.18"

source:
  fn: django-nocaptcha-recaptcha-0.0.18.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nocaptcha-recaptcha/django-nocaptcha-recaptcha-0.0.18.tar.gz
  md5: a57f50dcfaf610cc2a803d59a42a834c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nocaptcha-recaptcha = django-nocaptcha-recaptcha:main
    #
    # Would create an entry point called django-nocaptcha-recaptcha that calls django-nocaptcha-recaptcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nocaptcha_recaptcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ImaginaryLandscape/django-nocaptcha-recaptcha
  license: BSD License
  summary: 'Django nocaptcha recaptcha form field/widget app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
