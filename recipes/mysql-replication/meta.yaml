package:
  name: mysql-replication
  version: "0.5"

source:
  fn: mysql-replication-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mysql-replication/mysql-replication-0.5.tar.gz
  md5: 269e8ff86fbc3f879a33e8252fff8699
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mysql-replication = mysql-replication:main
    #
    # Would create an entry point called mysql-replication that calls mysql-replication.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymysql
    - six

  run:
    - python
    - pymysql
    - six

test:
  # Python imports
  imports:
    - pymysqlreplication
    - pymysqlreplication.constants
    - pymysqlreplication.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/noplay/python-mysql-replication
  license: Apache 2
  summary: 'Pure Python Implementation of MySQL replication protocolbuild on top of PyMYSQL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
