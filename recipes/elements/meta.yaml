package:
  name: elements
  version: "0.2.0"

source:
  fn: Elements-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/E/Elements/Elements-0.2.0.tar.gz
  md5: f94c0e84b8f6c50189669a8f9976b490
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elements = elements:main
    #
    # Would create an entry point called elements that calls elements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - elements
    - elements.formats
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-elements/
  license: Apache Software License
  summary: 'Python XML Elements Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
