package:
  name: django-fulmine
  version: "0.1.1"

source:
  fn: django-fulmine-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fulmine/django-fulmine-0.1.1.tar.gz
  md5: 8a4110d65bb09c0c9b482745207fc97a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fulmine = django-fulmine:main
    #
    # Would create an entry point called django-fulmine that calls django-fulmine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fulmine
    - fulmine.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sorcio/django-fulmine
  license: BSD License
  summary: 'Django OAuth 2.0 (RFC 6749) pluggable implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
