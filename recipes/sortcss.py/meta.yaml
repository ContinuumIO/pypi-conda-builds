package:
  name: sortcss.py
  version: "0.1.0"

source:
  fn: sortcss.py-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sortcss.py/sortcss.py-0.1.0.tar.gz
  md5: f57fb75a83a03befab8416ad546866df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sortcss.py = sortcss.py:main
    #
    # Would create an entry point called sortcss.py that calls sortcss.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/livibetter/css-prop-sorter
  license: MIT License
  summary: 'CSS Property Sorter Script'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
