package:
  name: zope.sequencesort
  version: "4.0.1"

source:
  fn: zope.sequencesort-4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.sequencesort/zope.sequencesort-4.0.1.tar.gz
  md5: ff887429e1758ceaa5d2d803d06c7185
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.sequencesort = zope.sequencesort:main
    #
    # Would create an entry point called zope.sequencesort that calls zope.sequencesort.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.sequencesort
    - zope.sequencesort.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.sequencesort
  license: Zope Public License
  summary: 'Sequence Sorting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
