package:
  name: django-extended-choices
  version: "1.0.4"

source:
  fn: django-extended-choices-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extended-choices/django-extended-choices-1.0.4.tar.gz
  md5: 0a626d92d2dc11109f54ad1c3cc2b30d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extended-choices = django-extended-choices:main
    #
    # Would create an entry point called django-extended-choices that calls django-extended-choices.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - future

  run:
    - python
    - future

test:
  # Python imports
  imports:
    - extended_choices

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twidi/django-extended-choices
  license: BSD License
  summary: 'Little helper application to improve django choices(for fields)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
