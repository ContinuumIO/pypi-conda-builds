package:
  name: bintools
  version: "0.2.0"

source:
  fn: BinTools-0.2.0.zip
  url: https://pypi.python.org/packages/source/B/BinTools/BinTools-0.2.0.zip
  md5: eb71c93068c8e0f12808955925fc91d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bintools = bintools:main
    #
    # Would create an entry point called bintools that calls bintools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bintools
    - bintools.dwarf
    - bintools.elf
    - bintools.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/pydevtools/
  license: LICENSE.txt
  summary: 'Toolkit for Elf / Dwarf handling.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
