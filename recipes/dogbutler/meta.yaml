package:
  name: dogbutler
  version: "0.0.4"

source:
  fn: dogbutler-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dogbutler/dogbutler-0.0.4.tar.gz
  md5: 2ed7e74e7be76ac3e5d0bfdfd8973daa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dogbutler = dogbutler:main
    #
    # Would create an entry point called dogbutler that calls dogbutler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dummycache
    - requests

  run:
    - python
    - dummycache
    - requests

test:
  # Python imports
  imports:
    - dogbutler
    - dogbutler.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dogbutler/
  license: GNU General Public License (GPL)
  summary: 'Make HTTP/HTTPS requests with cache, cookie, and redirect support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
