package:
  name: argparseplus
  version: "3.0.0"

source:
  fn: argparseplus-3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/argparseplus/argparseplus-3.0.0.tar.gz
  md5: ecf98f5fe39f8ea4c6cdffe8810aacec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argparseplus = argparseplus:main
    #
    # Would create an entry point called argparseplus that calls argparseplus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - argparseplus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jodier/argparseplus
  license: CeCILL-C
  summary: 'argparse extensions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
