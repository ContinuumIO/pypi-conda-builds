package:
  name: django-db-defaults
  version: "0.1.1"

source:
  fn: django-db-defaults-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-defaults/django-db-defaults-0.1.1.tar.gz
  md5: 52d09f98044b2ad442b70188e4089a2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-defaults = django-db-defaults:main
    #
    # Would create an entry point called django-db-defaults that calls django-db-defaults.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.6
    - south >=0.8.2

  run:
    - python
    - django >=1.4,<1.6
    - south >=0.8.2

test:
  # Python imports
  imports:
    - db_defaults
    - db_defaults.db

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ayadav/django-db-defaults
  license: BSD License
  summary: 'Persist Djago model field default values to DataBase'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
