package:
  name: electromagneticpython
  version: "1.0"

source:
  fn: ElectroMagneticPython-1.0.tar.gz
  url: https://pypi.python.org/packages/source/E/ElectromagneticPython/ElectroMagneticPython-1.0.tar.gz
  md5: 53a612d8b076f5a31599d7deba9142b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - electromagneticpython = electromagneticpython:main
    #
    # Would create an entry point called electromagneticpython that calls electromagneticpython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute >=0.6.28
    - future
    - numpy
    - scipy
    - matplotlib

  run:
    - python
    - distribute >=0.6.28
    - future
    - numpy
    - scipy
    - matplotlib

test:
  # Python imports
  imports:
    - EMpy
    - EMpy.modesolvers
    - EMpy.propagators
    - electromagneticpython
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lbolla.github.io/EMpy/
  license: BSD License
  summary: 'EMpy - ElectroMagnetic Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
