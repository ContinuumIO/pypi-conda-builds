package:
  name: django-confit
  version: "0.3"

source:
  fn: django-confit-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-confit/django-confit-0.3.tar.gz
  md5: e479890e79002c9142fde403606cc04c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-confit = django-confit:main
    #
    # Would create an entry point called django-confit that calls django-confit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - colander
    - django >=1.5,<1.7
    - pyyaml
    - setuptools
    - six
    - mock

  run:
    - python
    - colander
    - django >=1.5,<1.7
    - pyyaml
    - setuptools
    - six
    - mock

test:
  # Python imports
  imports:
    - django_confit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-confit.readthedocs.org
  license: BSD License
  summary: 'Django settings loaders and validators, with local flavour.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
