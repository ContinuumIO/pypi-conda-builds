package:
  name: collective.recipe.cmmi
  version: "0.5"

source:
  fn: collective.recipe.cmmi-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.cmmi/collective.recipe.cmmi-0.5.zip
  md5: 465d06949a29ce48ab3d073e4b096ff7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.cmmi = collective.recipe.cmmi:main
    #
    # Would create an entry point called collective.recipe.cmmi that calls collective.recipe.cmmi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.recipe.cmmi <=1.4.0

  run:
    - python
    - setuptools
    - zc.recipe.cmmi <=1.4.0

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.cmmi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.recipe.cmmi
  license: ZPL 2.1
  summary: 'Adds small features to zc.recipe.cmmi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
