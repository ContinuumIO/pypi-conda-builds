package:
  name: dirtools
  version: "0.2.0"

source:
  fn: dirtools-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dirtools/dirtools-0.2.0.tar.gz
  md5: 819ebf8044e51bbd1967a9c4e8c94564
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dirtools = dirtools:main
    #
    # Would create an entry point called dirtools that calls dirtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - globster

  run:
    - python
    - globster

test:
  # Python imports
  imports:
    - dirtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tsileo/dirtools
  license: MIT License
  summary: 'Exclude/ignore files in a directory (using .gitignore like syntax), compute hash, search projects for an entire directory tree and gzip compression.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
