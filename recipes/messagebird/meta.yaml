package:
  name: messagebird
  version: "1.0.0"

source:
  fn: messagebird-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/messagebird/messagebird-1.0.0.tar.gz
  md5: d8bbe1b3687c8f933e52ebb5cf05e257
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - messagebird = messagebird:main
    #
    # Would create an entry point called messagebird that calls messagebird.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.1

  run:
    - python
    - requests >=2.4.1

test:
  # Python imports
  imports:
    - messagebird

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/messagebird/python-rest-api
  license: BSD-2-Clause
  summary: "MessageBird's REST API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
