package:
  name: django-public-holidays
  version: "0.1.5"

source:
  fn: django-public-holidays-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-public-holidays/django-public-holidays-0.1.5.tar.gz
  md5: 87c52f0324ed56871b1b6350455f0f6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-public-holidays = django-public-holidays:main
    #
    # Would create an entry point called django-public-holidays that calls django-public-holidays.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - django-jsonfield
    - django-countries
    - django-model-utils

  run:
    - python
    - django
    - django-jsonfield
    - django-countries
    - django-model-utils

test:
  # Python imports
  imports:
    - public_holidays

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-public-holidays/
  license: BSD License
  summary: 'Reusable public holidays for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
