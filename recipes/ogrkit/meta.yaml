package:
  name: ogrkit
  version: "0.0.2"

source:
  fn: ogrkit-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/o/ogrkit/ogrkit-0.0.2.tar.gz
  md5: 6bc2f33dc13e4853f075c9950170be02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ogrkit = ogrkit:main
    #
    # Would create an entry point called ogrkit that calls ogrkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdal ==1.6.0

  run:
    - python
    - gdal ==1.6.0

test:
  # Python imports
  imports:
    - ogrkit
    - ogrkit.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.apps.chicagotribune.com/
  license: MIT
  summary: 'A suite of command-line utilities implementing basic geometric operations. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
