package:
  name: dccpi
  version: "1.2.0"

source:
  fn: dccpi-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dccpi/dccpi-1.2.0.tar.gz
  md5: e6ce45a4b419c34eb4b99f4c6594f994
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dccpi = dccpi:main
    #
    # Would create an entry point called dccpi that calls dccpi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - bitstring

  run:
    - python
    - bitstring

test:
  # Python imports
  imports:
    - dccpi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hsanjuan/dccpi
  license: GNU General Public License v3 (GPLv3)
  summary: 'A Python NMRA DCC protocol implementation for RaspberryPi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
