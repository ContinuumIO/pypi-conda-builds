package:
  name: juju-git-deploy
  version: "0.1.1"

source:
  fn: juju-git-deploy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/juju-git-deploy/juju-git-deploy-0.1.1.tar.gz
  md5: 51ccac6303213f56445b56f0e3aa129b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - juju-git-deploy = juju-git-deploy:main
    #
    # Would create an entry point called juju-git-deploy that calls juju-git-deploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.10
    - websocket-client-py3 ==0.12.0

  run:
    - python
    - pyyaml ==3.10
    - websocket-client-py3 ==0.12.0

test:
  # Python imports
  imports:
    - jujugd
    - jujugd.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frankban/juju-git-deploy
  license: GNU Affero General Public License v3
  summary: 'Deploy Juju services from local charms hosted on Github.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
