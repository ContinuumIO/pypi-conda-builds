package:
  name: flashmessage
  version: "0.2.0"

source:
  fn: FlashMessage-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/FlashMessage/FlashMessage-0.2.0.tar.gz
  md5: 13e91391439453cdab1f92e8731cab64
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flashmessage = flashmessage:main
    #
    # Would create an entry point called flashmessage that calls flashmessage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - flashmessage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/flashmessage/index.html
  license: GNU Affero General Public License v3
  summary: 'A pipe to set a message in a cookie so that on the next request it can be displayed on a web page without needing to persist the state in any more complex way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
