package:
  name: products.qplonedropdownmenu
  version: "3.0"

source:
  fn: Products.qPloneDropDownMenu-3.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.qPloneDropDownMenu/Products.qPloneDropDownMenu-3.0.tar.gz
  md5: 23e4b1b2df0c78571d1d4715af69b9f5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.qplonedropdownmenu = products.qplonedropdownmenu:main
    #
    # Would create an entry point called products.qplonedropdownmenu that calls products.qplonedropdownmenu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.qPloneDropDownMenu
    - Products.qPloneDropDownMenu.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://quintagroup.com/services/plone-development/products/plone-drop-down-menu
  license: GPL
  summary: 'The product allows Plone websites to update multilevel portal dropdown menu'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
