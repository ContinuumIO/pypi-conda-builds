package:
  name: jmbo-show
  version: "0.3.2"

source:
  fn: jmbo-show-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-show/jmbo-show-0.3.2.tar.gz
  md5: f650ed924c31ff3549235c7720ff7f78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-show = jmbo-show:main
    #
    # Would create an entry point called jmbo-show that calls jmbo-show.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmbo-foundry >=1.1.17
    - jmbo >=1.0.8

  run:
    - python
    - jmbo-foundry >=1.1.17
    - jmbo >=1.0.8

test:
  # Python imports
  imports:
    - show
    - show.migrations
    - show.templatetags
    - show.view_modifiers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-show
  license: BSD License
  summary: 'Jmbo show app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
