package:
  name: flyscript
  version: "0.6.1"

source:
  fn: flyscript-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flyscript/flyscript-0.6.1.tar.gz
  md5: df869b660d1f593e9ee79d1a38987f1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flyscript = flyscript:main
    #
    # Would create an entry point called flyscript that calls flyscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0

  run:
    - python
    - requests >=2.1.0

test:
  # Python imports
  imports:
    - contrib
    - flyscript
    - rvbd
    - rvbd.common
    - rvbd.common.test
    - rvbd.extras
    - rvbd.pilot
    - rvbd.profiler
    - rvbd.profiler.test
    - rvbd.shark
    - rvbd.shark.test
    - rvbd.stingray

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://splash.riverbed.com/docs/DOC-1464
  license: MIT License
  summary: 'Riverbed FlyScript library for interacting with Riverbed devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
