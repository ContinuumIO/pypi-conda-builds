package:
  name: django-fas
  version: "1.0.2"

source:
  fn: django-fas-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fas/django-fas-1.0.2.tar.gz
  md5: 8498cee4bf5eaf66b8291f54edf8ce36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fas = django-fas:main
    #
    # Would create an entry point called django-fas that calls django-fas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/misli/django-fas
  license: BSD License
  summary: 'Django auth backend for FAS (Fedora Accounts System)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
