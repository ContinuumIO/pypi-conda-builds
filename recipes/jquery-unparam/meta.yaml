package:
  name: jquery-unparam
  version: "1.0.0"

source:
  fn: jquery-unparam-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jquery-unparam/jquery-unparam-1.0.0.tar.gz
  md5: 139bf2e2d877bab5a1c1eae74df47801
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jquery-unparam = jquery-unparam:main
    #
    # Would create an entry point called jquery-unparam that calls jquery-unparam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/k_bx/jquery-unparam/
  license: BSD
  summary: 'A python library to parse jquery.param() string.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
