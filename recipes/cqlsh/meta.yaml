package:
  name: cqlsh
  version: "4.1.1"

source:
  fn: cqlsh-4.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cqlsh/cqlsh-4.1.1.tar.gz
  md5: 2ee57265566304d9ef4ab1bb67dbaaf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cqlsh = cqlsh:main
    #
    # Would create an entry point called cqlsh that calls cqlsh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cql
    - simplejson
    - unittest2

  run:
    - python
    - cql
    - simplejson
    - unittest2

test:
  # Python imports
  imports:
    - cqlsh
    - cqlshlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amussey/cqlsh
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'A Python-based command-line client for running simple CQL commands on a Cassandra cluster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
