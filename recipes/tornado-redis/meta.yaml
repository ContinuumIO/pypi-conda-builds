package:
  name: tornado-redis
  version: "2.4.18"

source:
  fn: tornado-redis-2.4.18.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-redis/tornado-redis-2.4.18.tar.gz
  md5: 1c7ec0f645d15400871141c1149e6934
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-redis = tornado-redis:main
    #
    # Would create an entry point called tornado-redis that calls tornado-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tornadoredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/leporo/tornado-redis
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Asynchronous Redis client for the Tornado Web Server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
