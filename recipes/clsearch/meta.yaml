package:
  name: clsearch
  version: "0.1a1"

source:
  fn: clsearch-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/c/clsearch/clsearch-0.1a1.tar.gz
  md5: 7c7065cec1672246c5f4bbe5b4d1f454
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clsearch = clsearch:main
    #
    # Would create an entry point called clsearch that calls clsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - clsearch
    - clsearch.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunfinite/clsearch
  license: UNKNOWN
  summary: 'Index and search files from command line, including id3 and xmp tags.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
