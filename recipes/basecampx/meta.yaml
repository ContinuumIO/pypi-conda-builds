package:
  name: basecampx
  version: "0.1.8"

source:
  fn: basecampx-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/b/basecampx/basecampx-0.1.8.tar.gz
  md5: f146960dffb54f74d50fdab38ecd30f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basecampx = basecampx:main
    #
    # Would create an entry point called basecampx that calls basecampx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0
    - requests-oauth2 >=0.2.0

  run:
    - python
    - requests >=1.0.0
    - requests-oauth2 >=0.2.0

test:
  # Python imports
  imports:
    - basecampx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nous-consulting/basecamp-next
  license: MIT License
  summary: 'Wrapper for Basecamp Next API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
