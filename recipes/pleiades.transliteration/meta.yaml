package:
  name: pleiades.transliteration
  version: "0.5"

source:
  fn: pleiades.transliteration-0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pleiades.transliteration/pleiades.transliteration-0.5.tar.gz
  md5: 1f1a0e4b695d512ea0eea0d41bf3b700
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pleiades.transliteration = pleiades.transliteration:main
    #
    # Would create an entry point called pleiades.transliteration that calls pleiades.transliteration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - pleiades
    - pleiades.transliteration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Transliteration of names from various writing systems to our modern Roman writing system following conventions of the Pleiades Project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
