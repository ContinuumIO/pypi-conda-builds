package:
  name: pyramid_debugtoolbar
  version: "2.3"

source:
  fn: pyramid_debugtoolbar-2.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_debugtoolbar/pyramid_debugtoolbar-2.3.tar.gz
  md5: 3d4c2af7d4f45efd796cdd58126ac446
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_debugtoolbar = pyramid_debugtoolbar:main
    #
    # Would create an entry point called pyramid_debugtoolbar that calls pyramid_debugtoolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid >=1.2dev
    - pyramid_mako >=0.3.1
    - repoze.lru
    - pygments

  run:
    - python
    - pyramid >=1.2dev
    - pyramid_mako >=0.3.1
    - repoze.lru
    - pygments

test:
  # Python imports
  imports:
    - pyramid_debugtoolbar
    - pyramid_debugtoolbar.panels
    - pyramid_debugtoolbar.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://docs.pylonsproject.org/projects/pyramid-debugtoolbar/en/latest/
  license: BSD
  summary: 'A package which provides an interactive HTML debugger for Pyramid application development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
