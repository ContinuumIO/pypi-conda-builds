package:
  name: django-ajaxify
  version: "0.0.1b"

source:
  fn: django-ajaxify-0.0.1b.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajaxify/django-ajaxify-0.0.1b.tar.gz
  md5: f05b6cbf8ef031a07ec1ac913deaf642
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajaxify = django-ajaxify:main
    #
    # Would create an entry point called django-ajaxify that calls django-ajaxify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bennylope/django-ajaxify/
  license: BSD License
  summary: 'Rendering helpers for AJAX enabled views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
