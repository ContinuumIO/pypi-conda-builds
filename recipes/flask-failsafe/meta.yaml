package:
  name: flask-failsafe
  version: "0.2"

source:
  fn: Flask-Failsafe-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Failsafe/Flask-Failsafe-0.2.tar.gz
  md5: e9ff8b720f9c8008cbaa2c6c13fc82ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-failsafe = flask-failsafe:main
    #
    # Would create an entry point called flask-failsafe that calls flask-failsafe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mgood/flask-failsafe
  license: BSD
  summary: 'A failsafe for the Flask reloader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
