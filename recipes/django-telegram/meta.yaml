package:
  name: django-telegram
  version: "0.1.0"

source:
  fn: django-telegram-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-telegram/django-telegram-0.1.0.tar.gz
  md5: 52cbb6a4d58e219798c132718ba7db0e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-telegram = django-telegram:main
    #
    # Would create an entry point called django-telegram that calls django-telegram.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jsonfield

  run:
    - python
    - setuptools
    - django-jsonfield

test:
  # Python imports
  imports:
    - telegram
    - telegram.handlers
    - telegram.management
    - telegram.management.commands
    - telegram.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aquameta/django-telegram
  license: UNKNOWN
  summary: 'A messaging framework that supports email, irc, notify my android and others'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
