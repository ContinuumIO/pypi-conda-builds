package:
  name: cs.id3
  version: "20150116.2"

source:
  fn: cs.id3-20150116.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cs.id3/cs.id3-20150116.2.tar.gz
  md5: 2dcb19a27adc1d9a517cabe371830dc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.id3 = cs.id3:main
    #
    # Would create an entry point called cs.id3 that calls cs.id3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cameron_simpson/css/commits/all
  license: GNU General Public License v3 (GPLv3)
  summary: "support for ID3 tags, mostly a convenience wrapper for Doug Zongker's pyid3lib"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
