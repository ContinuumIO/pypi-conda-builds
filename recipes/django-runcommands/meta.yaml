package:
  name: django-runcommands
  version: "0.1"

source:
  fn: django-runcommands-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-runcommands/django-runcommands-0.1.tar.gz
  md5: 3ef0ef93ed1784ce47970aeaa260c13d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-runcommands = django-runcommands:main
    #
    # Would create an entry point called django-runcommands that calls django-runcommands.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - runcommands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/magopian/django-runcommands
  license: BSD License
  summary: u'runcommands: execute system commands from views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
