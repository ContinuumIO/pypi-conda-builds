package:
  name: configobject
  version: "1.2.2"

source:
  fn: ConfigObject-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfigObject/ConfigObject-1.2.2.tar.gz
  md5: 59dac325740be3e3d494c263f3347f50
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configobject = configobject:main
    #
    # Would create an entry point called configobject that calls configobject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ConfigObject
    - ConfigObject.tests
    - configobject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/ConfigObject/
  license: GNU General Public License (GPL)
  summary: 'ConfigObject is a wrapper to the python ConfigParser to\n      allow to access sections/options with attribute names.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
