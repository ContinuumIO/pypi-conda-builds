package:
  name: django_view_export
  version: "0.5"

source:
  fn: django_view_export-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django_view_export/django_view_export-0.5.tar.gz
  md5: 08f8810dbc5f44e9e83354d6dcd0c252
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_view_export = django_view_export:main
    #
    # Would create an entry point called django_view_export that calls django_view_export.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas >=0.13.1

  run:
    - python
    - pandas >=0.13.1

test:
  # Python imports
  imports:
    - django_view_export
    - view_export

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/Sturm/django-view-export
  license: Apache Software License
  summary: 'Export CSV reports of database views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
