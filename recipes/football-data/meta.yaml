package:
  name: football-data
  version: "0.1.1"

source:
  fn: football-data-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/football-data/football-data-0.1.1.tar.gz
  md5: 274c996c324bf6d8588f0a33152b333d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - football-data = football-data:main
    #
    # Would create an entry point called football-data that calls football-data.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - fbdata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/olalidmark/football-data
  license: MIT License
  summary: 'Wrapper over the data sets provided at http://www.football-data.co.uk/data.php to easily get historical football data like shots on goal, scored goals etc. from all major european football/soccer teams.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
