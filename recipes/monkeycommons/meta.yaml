package:
  name: monkeycommons
  version: "201212-r1"

source:
  fn: MonkeyCommons-201212-r1.tar.gz
  url: https://pypi.python.org/packages/source/M/MonkeyCommons/MonkeyCommons-201212-r1.tar.gz
  md5: ea725253aa5b9ff395a93f4d836d42f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monkeycommons = monkeycommons:main
    #
    # Would create an entry point called monkeycommons that calls monkeycommons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - iptools

  run:
    - python
    - requests
    - iptools

test:
  # Python imports
  imports:
    - commons
    - monkeycommons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/shugelee/MonkeyCommons
  license: BSD License
  summary: 'Python common wrapper functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
