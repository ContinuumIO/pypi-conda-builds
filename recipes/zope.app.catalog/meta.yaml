package:
  name: zope.app.catalog
  version: "3.8.1"

source:
  fn: zope.app.catalog-3.8.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.catalog/zope.app.catalog-3.8.1.tar.gz
  md5: 1ce21dee4e8256cfe254f8ee24c6ecef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.catalog = zope.app.catalog:main
    #
    # Would create an entry point called zope.app.catalog that calls zope.app.catalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.catalog

  run:
    - python
    - setuptools
    - zope.catalog

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.catalog
    - zope.app.catalog.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.catalog
  license: Zope Public License
  summary: 'Management pages for Zope Catalog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
