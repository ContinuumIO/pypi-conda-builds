package:
  name: inputgetter
  version: "1.1.0"

source:
  fn: inputgetter-1.1.0.zip
  url: https://pypi.python.org/packages/source/i/inputgetter/inputgetter-1.1.0.zip
  md5: ea17fbadb67fe6900cdf21a80cd83dd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inputgetter = inputgetter:main
    #
    # Would create an entry point called inputgetter that calls inputgetter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - inputgetter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.google.com
  license: UNKNOWN
  summary: 'An easy way to get boolean, integer, and string input from a user.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
