package:
  name: django-croppy
  version: "0.1.0"

source:
  fn: django-croppy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-croppy/django-croppy-0.1.0.tar.gz
  md5: e3e551fe56e2049d87c079b6666b7c41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-croppy = django-croppy:main
    #
    # Would create an entry point called django-croppy that calls django-croppy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jsonfield ==0.8.12
    - django-imagekit ==2.0.2

  run:
    - python
    - django-jsonfield ==0.8.12
    - django-imagekit ==2.0.2

test:
  # Python imports
  imports:
    - croppy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caffeinehit/django-croppy
  license: django-crossdb
  summary: 'Django model field to store custom image crops'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
