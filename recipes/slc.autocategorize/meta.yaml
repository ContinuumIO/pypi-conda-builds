package:
  name: slc.autocategorize
  version: "0.2"

source:
  fn: slc.autocategorize-0.2.zip
  url: https://pypi.python.org/packages/source/s/slc.autocategorize/slc.autocategorize-0.2.zip
  md5: 324ee3dec32e036e3e7b91d5144f7924
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.autocategorize = slc.autocategorize:main
    #
    # Would create an entry point called slc.autocategorize that calls slc.autocategorize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

test:
  # Python imports
  imports:
    - slc
    - slc.autocategorize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.example
  license: GPL
  summary: "Automatically inherit the parent folder's categories when you create an object."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
