package:
  name: execute
  version: "1.2"

source:
  fn: execute-1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/execute/execute-1.2.tar.gz
  md5: 0fc1eebd4a8fa4a88ac5462c71359698
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - execute = execute:main
    #
    # Would create an entry point called execute that calls execute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - execute
    - execute.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/execute
  license: MIT License
  summary: 'Common patterns for executing commands as sub processes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
