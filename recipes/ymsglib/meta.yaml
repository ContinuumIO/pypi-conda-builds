package:
  name: ymsglib
  version: "0.1"

source:
  fn: ymsglib-0.1.zip
  url: https://pypi.python.org/packages/source/y/ymsglib/ymsglib-0.1.zip
  md5: 403a3df9dfb067ed8d5b04d232c7be75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ymsglib = ymsglib:main
    #
    # Would create an entry point called ymsglib that calls ymsglib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ymsg
    - ymsglib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/ymsg/
  license: GNU General Public License (GPL)
  summary: 'Module implementing the yahoo messenger protocol v16.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
