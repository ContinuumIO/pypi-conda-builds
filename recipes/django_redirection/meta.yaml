package:
  name: django_redirection
  version: "0.2.1"

source:
  fn: django_redirection-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_redirection/django_redirection-0.2.1.tar.gz
  md5: cc63c05f916ec862bf8428cf5714012c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_redirection = django_redirection:main
    #
    # Would create an entry point called django_redirection that calls django_redirection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_redirection

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.whiteblack-cat.info/
  license: MIT License
  summary: 'Redirection app by using X-Accel-Redirect'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
