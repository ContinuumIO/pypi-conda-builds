package:
  name: python-pandora
  version: "0.10"

source:
  fn: python-pandora-0.10.tar.gz
  url: https://pypi.python.org/packages/source/p/python-pandora/python-pandora-0.10.tar.gz
  md5: 70a8f7ea2f1d8c53023df9b5d991f93e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-pandora = python-pandora:main
    #
    # Would create an entry point called python-pandora that calls python-pandora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pandora

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Library to access pandora.com. Based on the work from http://forum.xbmc.org/showthread.php?t=70471 and from Pithos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
