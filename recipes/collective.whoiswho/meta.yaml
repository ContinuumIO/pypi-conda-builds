package:
  name: collective.whoiswho
  version: "1.0"

source:
  fn: collective.whoiswho-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.whoiswho/collective.whoiswho-1.0.zip
  md5: 21bc438c372c287bcd2f849ac3784859
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.whoiswho = collective.whoiswho:main
    #
    # Would create an entry point called collective.whoiswho that calls collective.whoiswho.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.whoiswho
    - collective.whoiswho.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Display members and groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
