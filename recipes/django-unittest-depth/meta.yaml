package:
  name: django-unittest-depth
  version: "0.6"

source:
  fn: django-unittest-depth-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-unittest-depth/django-unittest-depth-0.6.tar.gz
  md5: 5fd24692d91e8c45a907a2bac7b11cca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-unittest-depth = django-unittest-depth:main
    #
    # Would create an entry point called django-unittest-depth that calls django-unittest-depth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangox
    - djangox.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-unittest-depth/
  license: GNU General Public License (GPL)
  summary: 'Run django unittest recursively'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
