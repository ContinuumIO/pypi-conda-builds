package:
  name: jsonbot
  version: "1.0.4"

source:
  fn: jsonbot-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonbot/jsonbot-1.0.4.tar.gz
  md5: fce7abf70d7edc5830736a57d2064db2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonbot = jsonbot:main
    #
    # Would create an entry point called jsonbot that calls jsonbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - jsb
    - beautifulsoup4
    - sleekxmpp

  run:
    - python
    - distribute
    - jsb
    - beautifulsoup4
    - sleekxmpp

test:
  # Python imports
  imports:
    - jsonbot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pikacode.com/milla/jsonbot1
  license: MIT License
  summary: 'The bot for us. !'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
