package:
  name: django-easy-contact
  version: "0.3.8"

source:
  fn: django-easy-contact-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-contact/django-easy-contact-0.3.8.tar.gz
  md5: afe6830ac1c0ac3963c4fface95f0549
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-contact = django-easy-contact:main
    #
    # Would create an entry point called django-easy-contact that calls django-easy-contact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - easy_contact

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/catapela/django-easy-contact
  license: BSD License
  summary: 'A simple contact form application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
