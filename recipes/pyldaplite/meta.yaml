package:
  name: pyldaplite
  version: "0.1.2"

source:
  fn: pyldaplite-0.1.2.zip
  url: https://pypi.python.org/packages/source/p/pyldaplite/pyldaplite-0.1.2.zip
  md5: f6442ee4d10b8dd4c59b8458afb39308
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyldaplite = pyldaplite:main
    #
    # Would create an entry point called pyldaplite that calls pyldaplite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap

  run:
    - python
    - python-ldap

test:
  # Python imports
  imports:
    - pyldaplite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lixxu/pyldaplite
  license: BSD License
  summary: 'Simple package for LDAP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
