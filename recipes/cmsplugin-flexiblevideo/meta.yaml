package:
  name: cmsplugin-flexiblevideo
  version: "0.0.9"

source:
  fn: cmsplugin-flexiblevideo-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-flexiblevideo/cmsplugin-flexiblevideo-0.0.9.tar.gz
  md5: bdc6c5405bf41c751d04a14ebc3e5aa1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-flexiblevideo = cmsplugin-flexiblevideo:main
    #
    # Would create an entry point called cmsplugin-flexiblevideo that calls cmsplugin-flexiblevideo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.1

  run:
    - python
    - django-cms >=2.1

test:
  # Python imports
  imports:
    - cmsplugin_flexiblevideo
    - cmsplugin_flexiblevideo.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codemill/cmsplugin-flexiblevideo
  license: UNKNOWN
  summary: 'Plugin for Django CMS that displays a video with flexible width.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
