package:
  name: hearplanetapi
  version: "0.1.1"

source:
  fn: HearPlanetAPI-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/H/HearPlanetAPI/HearPlanetAPI-0.1.1.tar.gz
  md5: eaa1a4694dcb843ba2cb2896c6fd3ec8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hearplanetapi = hearplanetapi:main
    #
    # Would create an entry point called hearplanetapi that calls hearplanetapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - hearplanet
    - hearplanet.query
    - hearplanetapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/HearPlanetAPI/
  license: Apache Software License
  summary: 'HearPlanet API driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
