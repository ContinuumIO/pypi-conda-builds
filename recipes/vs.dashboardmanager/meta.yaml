package:
  name: vs.dashboardmanager
  version: "0.3.4"

source:
  fn: vs.dashboardmanager-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/v/vs.dashboardmanager/vs.dashboardmanager-0.3.4.tar.gz
  md5: 7cb17fb3c0cf72f2157f7b8096cdda9a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vs.dashboardmanager = vs.dashboardmanager:main
    #
    # Would create an entry point called vs.dashboardmanager that calls vs.dashboardmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.portletpage
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - collective.portletpage
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - vs
    - vs.dashboardmanager
    - vs.dashboardmanager.browser
    - vs.dashboardmanager.content
    - vs.dashboardmanager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/vs.dashboardmanager
  license: GPL
  summary: 'An extended Dashboard manager based on collective.portletpage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
