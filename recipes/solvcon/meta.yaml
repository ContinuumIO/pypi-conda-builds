package:
  name: solvcon
  version: "0.1.4"

source:
  fn: SOLVCON-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/S/SOLVCON/SOLVCON-0.1.4.tar.gz
  md5: 0248c492c9f2ea4e29cc787b04ec1944
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - solvcon = solvcon:main
    #
    # Would create an entry point called solvcon that calls solvcon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - solvcon
    - solvcon.io
    - solvcon.io.tests
    - solvcon.kerpak
    - solvcon.parcel
    - solvcon.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://solvcon.net/
  license: BSD License
  summary: 'Solvers of Conservation Laws'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
