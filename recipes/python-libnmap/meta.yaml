package:
  name: python-libnmap
  version: "0.6.2"

source:
  fn: python-libnmap-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libnmap/python-libnmap-0.6.2.tar.gz
  md5: 69db82db0b592b0ffff49bc62ec06f5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libnmap = python-libnmap:main
    #
    # Would create an entry point called python-libnmap that calls python-libnmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - libnmap
    - libnmap.objects
    - libnmap.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/python-libnmap/
  license: Creative Common "Attribution" license (CC-BY) v3
  summary: 'Python NMAP library enabling you to start async nmap tasks, parse and compare/diff scan results'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
