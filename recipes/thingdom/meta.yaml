package:
  name: thingdom
  version: "0.1.1"

source:
  fn: thingdom-0.1.1.zip
  url: https://pypi.python.org/packages/source/t/thingdom/thingdom-0.1.1.zip
  md5: 5dd0a7df7666f9ad00151572a7eeb231
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thingdom = thingdom:main
    #
    # Would create an entry point called thingdom that calls thingdom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - thingdom
    - thingdom.tasks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thingdomio/thingdom-python
  license: MIT License
  summary: 'The official Python wrapper for the Thingdom API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
