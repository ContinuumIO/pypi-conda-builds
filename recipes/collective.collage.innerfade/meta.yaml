package:
  name: collective.collage.innerfade
  version: "1.0-final"

source:
  fn: collective.collage.innerfade-1.0-final.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collage.innerfade/collective.collage.innerfade-1.0-final.tar.gz
  md5: 5f62e15fcc891f91fca11934835f2748
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.innerfade = collective.collage.innerfade:main
    #
    # Would create an entry point called collective.collage.innerfade that calls collective.collage.innerfade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.collage >=1.3.0-final
    - collective.js.innerfade

  run:
    - python
    - setuptools
    - setuptools
    - products.collage >=1.3.0-final
    - collective.js.innerfade

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.innerfade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Collage integration of the Innerfade module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
