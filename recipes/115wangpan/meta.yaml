package:
  name: 115wangpan
  version: "0.5.1"

source:
  fn: 115wangpan-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/1/115wangpan/115wangpan-0.5.1.tar.gz
  md5: 0ee0ec5aed3cadce27c88f8bd6aa4e56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - 115wangpan = 115wangpan:main
    #
    # Would create an entry point called 115wangpan that calls 115wangpan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - homura
    - humanize
    - requests
    - six
    - pycurl
    - pysocks

  run:
    - python
    - beautifulsoup4
    - homura
    - humanize
    - requests
    - six
    - pycurl
    - pysocks

test:
  # Python imports
  imports:
    - u115

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shichao-an/115wangpan
  license: BSD License
  summary: 'Unofficial Python API wrapper for 115.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
