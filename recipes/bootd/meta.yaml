package:
  name: bootd
  version: "1.0.8"

source:
  fn: bootd-1.0.8.zip
  url: https://pypi.python.org/packages/source/b/bootd/bootd-1.0.8.zip
  md5: a992fd1cef7ea04a13e259df22946a0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bootd = bootd:main
    #
    # Would create an entry point called bootd that calls bootd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.5
    - django-picker >=0.1.6

  run:
    - python
    - django >=1.6.5
    - django-picker >=0.1.6

test:
  # Python imports
  imports:
    - bootd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/shinznatkid/bootd
  license: MIT
  summary: 'Wizard startproject from django base template.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
