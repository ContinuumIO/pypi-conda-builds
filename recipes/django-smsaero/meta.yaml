package:
  name: django-smsaero
  version: "0.9.5"

source:
  fn: django-smsaero-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smsaero/django-smsaero-0.9.5.tar.gz
  md5: 4cb8536430eaf08819c063a8a0a9bb74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smsaero = django-smsaero:main
    #
    # Would create an entry point called django-smsaero that calls django-smsaero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urllib3
    - factory_boy
    - django-rq
    - south

  run:
    - python
    - urllib3
    - factory_boy
    - django-rq
    - south

test:
  # Python imports
  imports:
    - smsaero

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/drmartiner/django-smsaero/
  license: MIT License
  summary: 'A simple Django app for send SMS via smsaero.ru.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
