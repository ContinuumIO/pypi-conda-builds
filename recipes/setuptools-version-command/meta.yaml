package:
  name: setuptools-version-command
  version: "2.0"

source:
  fn: setuptools-version-command-2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-version-command/setuptools-version-command-2.0.tar.gz
  md5: e130e78fe8f19baf8c59ffba29a02860
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-version-command = setuptools-version-command:main
    #
    # Would create an entry point called setuptools-version-command that calls setuptools-version-command.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/j0057/setuptools-version-command
  license: MIT License
  summary: 'Adds a command to dynamically get the version from the VCS of choice'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
