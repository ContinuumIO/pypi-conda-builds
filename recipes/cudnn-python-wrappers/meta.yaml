package:
  name: cudnn-python-wrappers
  version: "2.0b2"

source:
  fn: cudnn-python-wrappers-2.0b2.tar.gz
  url: https://pypi.python.org/packages/source/c/cudnn-python-wrappers/cudnn-python-wrappers-2.0b2.tar.gz
  md5: 54d8ebeb5c8268abb02ffc6445acd919
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cudnn-python-wrappers = cudnn-python-wrappers:main
    #
    # Would create an entry point called cudnn-python-wrappers that calls cudnn-python-wrappers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hannes-brt/cudnn-python-wrappers
  license: MIT License
  summary: 'Python wrappers for the NVIDIA cudnn 6.5 R2 libraries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
