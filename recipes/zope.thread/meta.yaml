package:
  name: zope.thread
  version: "3.4"

source:
  fn: zope.thread-3.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.thread/zope.thread-3.4.tar.gz
  md5: 3567037865b746c933d4af86e5aefa35
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.thread = zope.thread:main
    #
    # Would create an entry point called zope.thread that calls zope.thread.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.thread

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/zope.thread
  license: ZPL 2.1
  summary: 'Zope3 Thread-Local Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
