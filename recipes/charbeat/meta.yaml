package:
  name: charbeat
  version: "0.2"

source:
  fn: charbeat-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/charbeat/charbeat-0.2.tar.gz
  md5: f248531f0b7a11714adb71ac8debf5ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - charbeat = charbeat:main
    #
    # Would create an entry point called charbeat that calls charbeat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - charbeat
    - chartbeat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberdelia/chartbeat
  license: MIT License
  summary: 'A charbeat/newsbeat API wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
