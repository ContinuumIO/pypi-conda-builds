package:
  name: abcpmc
  version: "0.1.1"

source:
  fn: abcpmc-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/abcpmc/abcpmc-0.1.1.tar.gz
  md5: a46476ff075bf1690d5699b701cbc012
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abcpmc = abcpmc:main
    #
    # Would create an entry point called abcpmc that calls abcpmc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy >=0.15

  run:
    - python
    - numpy
    - scipy >=0.15

test:
  # Python imports
  imports:
    - abcpmc
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cosmology.ethz.ch/research/software-lab/abcpmc.html
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'approximate bayesian computing with population monte carlo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
