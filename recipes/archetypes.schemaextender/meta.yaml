package:
  name: archetypes.schemaextender
  version: "2.1.5"

source:
  fn: archetypes.schemaextender-2.1.5.zip
  url: https://pypi.python.org/packages/source/a/archetypes.schemaextender/archetypes.schemaextender-2.1.5.zip
  md5: 3276800ab65c09e10b45541b35299419
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.schemaextender = archetypes.schemaextender:main
    #
    # Would create an entry point called archetypes.schemaextender that calls archetypes.schemaextender.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.uuid

  run:
    - python
    - setuptools
    - setuptools
    - plone.uuid

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.schemaextender
    - archetypes.schemaextender.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/archetypes.schemaextender
  license: GPL
  summary: 'Dynamically extend Archetypes schemas with named adapters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
