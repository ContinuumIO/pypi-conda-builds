package:
  name: maegen
  version: "0.2.0"

source:
  fn: maegen-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/maegen/maegen-0.2.0.tar.gz
  md5: 3110b8d018992dbcb93ad8d1d86382fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maegen = maegen:main
    #
    # Would create an entry point called maegen that calls maegen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata >=2.0.9,<=2.0.14

  run:
    - python
    - gdata >=2.0.9,<=2.0.14

test:
  # Python imports
  imports:
    - maegen
    - maegen.common
    - maegen.core
    - maegen.gui
    - maegen.gui.gtk
    - maegen.gui.hildon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.maegen.bressure.net
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Maegen is a genealogical application for N900'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
