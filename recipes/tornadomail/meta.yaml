package:
  name: tornadomail
  version: "0.1.2"

source:
  fn: tornadomail-0.1.2.tar.gz
  url: http://github.com/downloads/equeny/tornadomail/tornadomail-0.1.2.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadomail = tornadomail:main
    #
    # Would create an entry point called tornadomail that calls tornadomail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - tornado >=2.1

  run:
    - python
    - tornado >=2.1

test:
  # Python imports
  imports:
    - tornadomail
    - tornadomail.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/equeny/tornadomail
  license: Apache Software License
  summary: 'Asynchronous email sending library for Tornado. Port of django.mail.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
