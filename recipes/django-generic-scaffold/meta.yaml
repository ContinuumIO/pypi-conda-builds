package:
  name: django-generic-scaffold
  version: "0.1.1"

source:
  fn: django-generic-scaffold-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-generic-scaffold/django-generic-scaffold-0.1.1.zip
  md5: f9197cb348955853eaabfd080e81df92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-scaffold = django-generic-scaffold:main
    #
    # Would create an entry point called django-generic-scaffold that calls django-generic-scaffold.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - six

  run:
    - python
    - django >=1.4
    - six

test:
  # Python imports
  imports:
    - generic_scaffold
    - generic_scaffold.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spapas/django-generic-scaffold/
  license: MIT License
  summary: 'Generic scaffolding for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
