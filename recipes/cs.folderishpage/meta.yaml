package:
  name: cs.folderishpage
  version: "1.0"

source:
  fn: cs.folderishpage-1.0.zip
  url: https://pypi.python.org/packages/source/c/cs.folderishpage/cs.folderishpage-1.0.zip
  md5: c203fb5a5c1c8cb8b655f9eb8d94ad3c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.folderishpage = cs.folderishpage:main
    #
    # Would create an entry point called cs.folderishpage that calls cs.folderishpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok

  run:
    - python
    - setuptools
    - setuptools
    - five.grok

test:
  # Python imports
  imports:
    - cs
    - cs.folderishpage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codesyntax/cs.folderishpage
  license: GPL
  summary: 'Dexterity-based folderish page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
