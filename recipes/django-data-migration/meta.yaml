package:
  name: django-data-migration
  version: "0.2.1"

source:
  fn: django-data-migration-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-data-migration/django-data-migration-0.2.1.tar.gz
  md5: 2d80282d9d20fcc45fbdf7e7b9510b95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-data-migration = django-data-migration:main
    #
    # Would create an entry point called django-data-migration that calls django-data-migration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - networkx >=1.8.0
    - future

  run:
    - python
    - networkx >=1.8.0
    - future

test:
  # Python imports
  imports:
    - data_migration
    - data_migration.management
    - data_migration.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pboehm/django-data-migration
  license: MIT License
  summary: 'Data migration framework for Django that migrates legacy data into your new django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
