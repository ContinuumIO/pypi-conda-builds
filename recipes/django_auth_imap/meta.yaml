package:
  name: django_auth_imap
  version: "0.1"

source:
  fn: django_auth_imap-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_auth_imap/django_auth_imap-0.1.tar.gz
  md5: 96eb2def5807dd39f81405e9058bf8e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_auth_imap = django_auth_imap:main
    #
    # Would create an entry point called django_auth_imap that calls django_auth_imap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_auth_imap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/voorloopnul/django-auth-imap
  license: UNKNOWN
  summary: 'A customizable django imap authentication backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
