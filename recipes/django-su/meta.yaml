package:
  name: django-su
  version: "0.4.7"

source:
  fn: django-su-0.4.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-su/django-su-0.4.7.tar.gz
  md5: 3f234fe61ce8c4f407c9e59a8730b68b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-su = django-su:main
    #
    # Would create an entry point called django-su that calls django-su.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_su
    - django_su.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamcharnock/django-su
  license: MIT
  summary: 'Login as any user from the Django admin interface, then switch back when done'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
