package:
  name: django-webdriver
  version: "0.1.2"

source:
  fn: django-webdriver-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webdriver/django-webdriver-0.1.2.tar.gz
  md5: 6b0436c45bd640bd7d40da9a0d25679b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webdriver = django-webdriver:main
    #
    # Would create an entry point called django-webdriver that calls django-webdriver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - selenium >=2.30
    - django-nose >=1.1

  run:
    - python
    - django >=1.4
    - selenium >=2.30
    - django-nose >=1.1

test:
  # Python imports
  imports:
    - django_webdriver
    - django_webdriver.management
    - django_webdriver.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/optiflows/django-webdriver
  license: Apache Software License
  summary: 'Django app to run selenium webdriver tests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
