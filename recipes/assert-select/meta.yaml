package:
  name: assert-select
  version: "1.0.1"

source:
  fn: assert-select-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/assert-select/assert-select-1.0.1.tar.gz
  md5: e9489646615d12d2761ce5a068ed1b15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - assert-select = assert-select:main
    #
    # Would create an entry point called assert-select that calls assert-select.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.1.3
    - html5lib

  run:
    - python
    - beautifulsoup4 ==4.1.3
    - html5lib

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aroberts/assert-select
  license: BSD
  summary: 'Make assertions about CSS selectors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
