package:
  name: axographio
  version: "0.1.1b1"

source:
  fn: axographio-0.1.1b1.tar.gz
  url: https://pypi.python.org/packages/source/a/axographio/axographio-0.1.1b1.tar.gz
  md5: f0b0e594d1361cde96a5801850c564c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - axographio = axographio:main
    #
    # Would create an entry point called axographio that calls axographio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - axographio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/axographio/
  license: BSD License
  summary: 'A python library for reading and writing AxoGraph data files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
