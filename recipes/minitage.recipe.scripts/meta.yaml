package:
  name: minitage.recipe.scripts
  version: "1.58"

source:
  fn: minitage.recipe.scripts-1.58.zip
  url: https://pypi.python.org/packages/source/m/minitage.recipe.scripts/minitage.recipe.scripts-1.58.zip
  md5: 694ad1d9e710646de923e701c3c1a432
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minitage.recipe.scripts = minitage.recipe.scripts:main
    #
    # Would create an entry point called minitage.recipe.scripts that calls minitage.recipe.scripts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - minitage.core
    - minitage.recipe.egg
    - iniparse

  run:
    - python
    - setuptools
    - zc.buildout
    - minitage.core
    - minitage.recipe.egg
    - iniparse

test:
  # Python imports
  imports:
    - minitage
    - minitage.recipe
    - minitage.recipe.scripts
    - minitage.recipe.scripts.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/minitage.recipe.scripts
  license: BSD License
  summary: 'zc.buildout recipes to compile and install software or python packages and generate scripts or configuration files sponsored by Makina Corpus.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
