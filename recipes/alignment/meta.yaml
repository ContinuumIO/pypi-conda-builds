package:
  name: alignment
  version: "1.0.9"

source:
  fn: alignment-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/a/alignment/alignment-1.0.9.tar.gz
  md5: be01d2711f34a247db886984149b0852
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alignment = alignment:main
    #
    # Would create an entry point called alignment that calls alignment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - alignment

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eseraygun/python-alignment
  license: BSD 3-Clause License
  summary: 'Native Python library for generic sequence alignment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
