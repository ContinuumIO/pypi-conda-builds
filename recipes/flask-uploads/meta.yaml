package:
  name: flask-uploads
  version: "0.1.3"

source:
  fn: Flask-Uploads-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Uploads/Flask-Uploads-0.1.3.tar.gz
  md5: f17e38aeac525a77c04a366b7dc09c93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-uploads = flask-uploads:main
    #
    # Would create an entry point called flask-uploads that calls flask-uploads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.5

  run:
    - python
    - flask >=0.5

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/leafstorm/flask-uploads/
  license: MIT License
  summary: 'Flexible and efficient upload handling for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
