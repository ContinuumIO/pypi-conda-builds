package:
  name: galleryserve
  version: "0.1.6"

source:
  fn: galleryserve-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/g/galleryserve/galleryserve-0.1.6.tar.gz
  md5: 46e4c5cbd8a90bf261bda1cdede85b58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - galleryserve = galleryserve:main
    #
    # Would create an entry point called galleryserve that calls galleryserve.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - galleryserve
    - galleryserve.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ImaginaryLandscape/django-galleryserve
  license: UNKNOWN
  summary: 'Adapted from imageserve to fit broader gallery needs such as video embedding and content for controls.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
