package:
  name: dashboardmods
  version: "1.0"

source:
  fn: dashboardmods-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dashboardmods/dashboardmods-1.0.tar.gz
  md5: 5e0845a8553ac7951ce4bec244e427ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dashboardmods = dashboardmods:main
    #
    # Would create an entry point called dashboardmods that calls dashboardmods.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dashboardmods

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/callowayproject/dashboardmods
  license: BSD License
  summary: 'A collection of dashboard modules for django-admin-tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
