package:
  name: por.dashboard
  version: "1.7.16"

source:
  fn: por.dashboard-1.7.16.tar.gz
  url: https://pypi.python.org/packages/source/p/por.dashboard/por.dashboard-1.7.16.tar.gz
  md5: 25a93346b22d7c820064e4185753afdd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - por.dashboard = por.dashboard:main
    #
    # Would create an entry point called por.dashboard that calls por.dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - pyramid
    - transaction
    - repoze.tm2 >=1.0b1
    - weberror
    - webtest
    - pyramid_formalchemy
    - pyramid_beaker
    - pyramid_rpc
    - pyramid_skins
    - pyramid_debugtoolbar
    - pyramid_zcml
    - pyramid_exclog
    - repoze.workflow
    - python-cjson
    - repoze.who.plugins.sa
    - repoze.who-friendlyform
    - repoze.who <1.9
    - python-openid >=2.0
    - zope.interface
    - por.models
    - por.gdata
    - por.trac
    - jsonrpc
    - js.jquery_timepicker_addon
    - js.jqgrid
    - fa.bootstrap
    - xlwt
    - deform
    - feedparser
    - babel
    - lingua
    - profilehooks
    - velruse
    - pyramid_mailer
    - pyramid_fanstatic
    - js.lesscss
    - deform_bootstrap
    - beaker
    - js.jquery_datatables ==1.8.2
    - unittest2
    - bleach
    - raven
    - sunburnt
    - lxml

  run:
    - python
    - setuptools
    - distribute
    - pyramid
    - transaction
    - repoze.tm2 >=1.0b1
    - weberror
    - webtest
    - pyramid_formalchemy
    - pyramid_beaker
    - pyramid_rpc
    - pyramid_skins
    - pyramid_debugtoolbar
    - pyramid_zcml
    - pyramid_exclog
    - repoze.workflow
    - python-cjson
    - repoze.who.plugins.sa
    - repoze.who-friendlyform
    - repoze.who <1.9
    - python-openid >=2.0
    - zope.interface
    - por.models
    - por.gdata
    - por.trac
    - jsonrpc
    - js.jquery_timepicker_addon
    - js.jqgrid
    - fa.bootstrap
    - xlwt
    - deform
    - feedparser
    - babel
    - lingua
    - profilehooks
    - velruse
    - pyramid_mailer
    - pyramid_fanstatic
    - js.lesscss
    - deform_bootstrap
    - beaker
    - js.jquery_datatables ==1.8.2
    - unittest2
    - bleach
    - raven
    - sunburnt
    - lxml

test:
  # Python imports
  imports:
    - por
    - por.dashboard
    - por.dashboard.forms
    - por.dashboard.lib
    - por.dashboard.lib.tests
    - por.dashboard.reports
    - por.dashboard.security
    - por.dashboard.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://getpenelope.github.com
  license: UNKNOWN
  summary: 'Penelope main package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
