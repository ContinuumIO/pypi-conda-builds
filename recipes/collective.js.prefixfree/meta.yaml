package:
  name: collective.js.prefixfree
  version: "1.0"

source:
  fn: collective.js.prefixfree-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.js.prefixfree/collective.js.prefixfree-1.0.zip
  md5: d3755e144dcd32b2660a23eacf360095
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.prefixfree = collective.js.prefixfree:main
    #
    # Would create an entry point called collective.js.prefixfree that calls collective.js.prefixfree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - products.genericsetup
    - plone.theme

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - products.genericsetup
    - plone.theme

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.prefixfree
    - collective.js.prefixfree.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gotcha/collective.js.prefixfree
  license: mit
  summary: 'Lea Verou -prefix-free for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
