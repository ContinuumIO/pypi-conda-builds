package:
  name: cosmicpy
  version: "0.1.2"

source:
  fn: cosmicpy-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cosmicpy/cosmicpy-0.1.2.tar.gz
  md5: 7d78794bde75356e9026237f4e27106b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cosmicpy = cosmicpy:main
    #
    # Would create an entry point called cosmicpy that calls cosmicpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cosmicpy
    - cosmicpy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cosmicpy.github.io
  license: CeCILL
  summary: 'An interactive python package for cosmology and parameter forecasts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
