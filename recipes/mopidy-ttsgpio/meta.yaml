package:
  name: mopidy-ttsgpio
  version: "1.0.1"

source:
  fn: Mopidy-TtsGpio-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-TtsGpio/Mopidy-TtsGpio-1.0.1.tar.gz
  md5: 3d0db182faf159e184884af2aea85a75
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-ttsgpio = mopidy-ttsgpio:main
    #
    # Would create an entry point called mopidy-ttsgpio that calls mopidy-ttsgpio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

test:
  # Python imports
  imports:
    - mopidy_ttsgpio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/9and3r/mopidy-ttsgpio
  license: Apache Software License
  summary: u'Controll mopidy without screen using GPIO and TTS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
