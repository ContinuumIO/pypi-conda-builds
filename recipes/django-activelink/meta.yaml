package:
  name: django-activelink
  version: "0.4"

source:
  fn: django-activelink-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-activelink/django-activelink-0.4.tar.gz
  md5: 287b53d2e69bf7ddb2001edc22c21d67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-activelink = django-activelink:main
    #
    # Would create an entry point called django-activelink that calls django-activelink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - activelink
    - activelink.templatetags
    - activelink.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/j4mie/django-activelink/
  license: UNKNOWN
  summary: 'A Django template library for checking whether the current page matches a given URL. Useful for highlighting active links in menus.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
