package:
  name: collective.catalogexport
  version: "0.1"

source:
  fn: collective.catalogexport-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.catalogexport/collective.catalogexport-0.1.tar.gz
  md5: ed80d0d124c944698893123a650e5452
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.catalogexport = collective.catalogexport:main
    #
    # Would create an entry point called collective.catalogexport that calls collective.catalogexport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.catalogexport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.catalogexport
  license: GPL
  summary: 'Use ZCatalogs as export sources'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
