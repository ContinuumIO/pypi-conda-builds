package:
  name: bpolicy
  version: "0.1.5"

source:
  fn: bpolicy-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bpolicy/bpolicy-0.1.5.tar.gz
  md5: 691372009a3dae645a630cc6d7f1ab51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bpolicy = bpolicy:main
    #
    # Would create an entry point called bpolicy that calls bpolicy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipy ==0.81

  run:
    - python
    - ipy ==0.81

test:
  # Python imports
  imports:
    - bpolicy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mapix/bpolicy
  license: MIT
  summary: 'A collection of basic policies for resources anti-spammer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
