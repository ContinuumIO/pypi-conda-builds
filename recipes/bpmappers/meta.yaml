package:
  name: bpmappers
  version: "0.8"

source:
  fn: bpmappers-0.8.tar.gz
  url: https://pypi.python.org/packages/source/b/bpmappers/bpmappers-0.8.tar.gz
  md5: 962a335fcdd20f311303d6a0f473830e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bpmappers = bpmappers:main
    #
    # Would create an entry point called bpmappers that calls bpmappers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - bpmappers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bpmappers.readthedocs.org/
  license: BSD License
  summary: 'A mapping tool from model to dictionary.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
