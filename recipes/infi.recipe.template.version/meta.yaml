package:
  name: infi.recipe.template.version
  version: "0.4.23"

source:
  fn: infi.recipe.template.version-0.4.23.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.recipe.template.version/infi.recipe.template.version-0.4.23.tar.gz
  md5: d4cfa301b172293aaf59a71f635ccff4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.recipe.template.version = infi.recipe.template.version:main
    #
    # Would create an entry point called infi.recipe.template.version that calls infi.recipe.template.version.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.recipe.template
    - git-py
    - infi.execute
    - infi.os_info
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - collective.recipe.template
    - git-py
    - infi.execute
    - infi.os_info
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - infi
    - infi.recipe
    - infi.recipe.template
    - infi.recipe.template.version
    - infi.recipe.template.version.recipe
    - infi.recipe.template.version.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.recipe.template.version
  license: Python Software Foundation License
  summary: "'an extension to collective.recipe.template'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
