package:
  name: entmapper
  version: "v0.1.4"

source:
  fn: entmapper-v0.1.4.zip
  url: https://pypi.python.org/packages/source/e/entmapper/entmapper-v0.1.4.zip
  md5: e77fee628403adba9a9456b7c0479aa1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - entmapper = entmapper:main
    #
    # Would create an entry point called entmapper that calls entmapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - entmapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leodesouza/entity
  license: Apache Software License
  summary: 'An simple object-document mapping for mongodb-motor and tornado applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
