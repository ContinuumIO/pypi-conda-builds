package:
  name: opttune
  version: "0.1.0"

source:
  fn: optTune-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/optTune/optTune-0.1.0.tar.gz
  md5: c48c9f4c6727c483d4a90b3d6f9e5bc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - opttune = opttune:main
    #
    # Would create an entry point called opttune that calls opttune.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - scipy
    - matplotlib

  run:
    - python
    - numpy
    - scipy
    - matplotlib

test:
  # Python imports
  imports:
    - optTune
    - optTune.MOTA_code
    - optTune.paretoArchives
    - optTune.tMOPSO_code
    - opttune

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hamish2014/optTune/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Various algorithms for tuning an optimization algorithm under multiple objective function evaluation budgets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
