package:
  name: loaderio
  version: "1.0.2"

source:
  fn: Loaderio-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/L/Loaderio/Loaderio-1.0.2.tar.gz
  md5: 3ca59390b85c72d1bcb4eaf8e6d7be98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loaderio = loaderio:main
    #
    # Would create an entry point called loaderio that calls loaderio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.0.1

  run:
    - python
    - requests ==2.0.1

test:
  # Python imports
  imports:
    - loaderio
    - loaderio/resources
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kenyaapps/loaderio.git
  license: MIT License
  summary: 'Python api wrapper for loader.io.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
