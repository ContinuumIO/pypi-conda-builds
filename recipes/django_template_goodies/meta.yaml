package:
  name: django_template_goodies
  version: "0.1.5"

source:
  fn: django_template_goodies-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django_template_goodies/django_template_goodies-0.1.5.tar.gz
  md5: 72046578aa7ae070fb98bc69ea5d3f5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_template_goodies = django_template_goodies:main
    #
    # Would create an entry point called django_template_goodies that calls django_template_goodies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-classy-tags ==0.6.1

  run:
    - python
    - django-classy-tags ==0.6.1

test:
  # Python imports
  imports:
    - django_template_goodies
    - template_goodies
    - template_goodies.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jqb/django-template-goodies/
  license: BSD License
  summary: 'A set of django template tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
