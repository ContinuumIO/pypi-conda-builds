package:
  name: collective.microdata.core
  version: "0.1"

source:
  fn: collective.microdata.core-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.microdata.core/collective.microdata.core-0.1.zip
  md5: 080050fdf53fcb5a001317d9e48fed8c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.microdata.core = collective.microdata.core:main
    #
    # Would create an entry point called collective.microdata.core that calls collective.microdata.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.microdata
    - collective.microdata.core
    - collective.microdata.core.browser
    - collective.microdata.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keul/collective.microdata.core
  license: GPL
  summary: 'Core package for Plone microdata support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
