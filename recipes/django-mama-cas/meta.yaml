package:
  name: django-mama-cas
  version: "1.1.0"

source:
  fn: django-mama-cas-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mama-cas/django-mama-cas-1.1.0.tar.gz
  md5: 9d17bbb1a7911de65bf2c189bc8bcff6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mama-cas = django-mama-cas:main
    #
    # Would create an entry point called django-mama-cas that calls django-mama-cas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.0,<3.0.0

  run:
    - python
    - requests >=2.0.0,<3.0.0

test:
  # Python imports
  imports:
    - mama_cas
    - mama_cas.management
    - mama_cas.management.commands
    - mama_cas.migrations
    - mama_cas.south_migrations
    - mama_cas.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbittel/django-mama-cas
  license: BSD License
  summary: 'A Django Central Authentication Service (CAS) single sign-on server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
