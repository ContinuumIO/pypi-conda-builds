package:
  name: js.amcharts
  version: "3.3.1"

source:
  fn: js.amcharts-3.3.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.amcharts/js.amcharts-3.3.1.tar.gz
  md5: 21c8f1b787fcbbcd2d6559a4b095c631
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.amcharts = js.amcharts:main
    #
    # Would create an entry point called js.amcharts that calls js.amcharts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.amcharts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/securactive/js.amcharts
  license: BSD
  summary: 'Fanstatic packaging of amCharts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
