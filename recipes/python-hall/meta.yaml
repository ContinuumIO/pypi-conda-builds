package:
  name: python-hall
  version: "0.1.1"

source:
  fn: python-hall-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hall/python-hall-0.1.1.tar.gz
  md5: dd37751bfee35edd47f26100cf127f95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hall = python-hall:main
    #
    # Would create an entry point called python-hall that calls python-hall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.2.0
    - simplejson ==3.3.2

  run:
    - python
    - requests ==2.2.0
    - simplejson ==3.3.2

test:
  # Python imports
  imports:
    - python_hall

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phalt/python-hall
  license: BSD License
  summary: 'A Python wrapper for the hall.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
