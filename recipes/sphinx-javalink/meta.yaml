package:
  name: sphinx-javalink
  version: "0.9.2"

source:
  fn: sphinx-javalink-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-javalink/sphinx-javalink-0.9.2.tar.gz
  md5: c296f6fe4a29b6f3220576796a9f800d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-javalink = sphinx-javalink:main
    #
    # Would create an entry point called sphinx-javalink that calls sphinx-javalink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.2
    - javatools >=1.3

  run:
    - python
    - sphinx >=1.2
    - javatools >=1.3

test:
  # Python imports
  imports:
    - javalink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bluekeyes/sphinx-javalink
  license: MIT License
  summary: 'Link to Javadoc APIs from Sphinx documentation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
