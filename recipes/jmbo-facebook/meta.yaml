package:
  name: jmbo-facebook
  version: "0.1.3"

source:
  fn: jmbo-facebook-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-facebook/jmbo-facebook-0.1.3.tar.gz
  md5: e379ba8ea570c1f0857bf7bc820f4ed6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-facebook = jmbo-facebook:main
    #
    # Would create an entry point called jmbo-facebook that calls jmbo-facebook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmbo-foundry >=0.4

  run:
    - python
    - jmbo-foundry >=0.4

test:
  # Python imports
  imports:
    - jmbo_facebook
    - jmbo_facebook.migrations
    - jmbo_facebook.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-facebook
  license: BSD License
  summary: 'Fetch facebook page updates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
