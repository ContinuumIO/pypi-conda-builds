package:
  name: 2or3
  version: "0.1.0"

source:
  fn: 2or3-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/2/2or3/2or3-0.1.0.tar.gz
  md5: 58a38e83fb01322fb651521cef6c90c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - 2or3 = 2or3:main
    #
    # Would create an entry point called 2or3 that calls 2or3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lib2or3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thomasballinger/2or3
  license: MIT
  summary: 'Writes a single byte to stdout (either "2" or "3") classifying a Python file using heuristics, giving priority to 3 if unclear'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
