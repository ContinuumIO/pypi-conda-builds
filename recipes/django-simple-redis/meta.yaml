package:
  name: django-simple-redis
  version: "0.0.1"

source:
  fn: django-simple-redis-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-redis/django-simple-redis-0.0.1.tar.gz
  md5: 210abba3c08631e4d493031dc2aa3ced
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-redis = django-simple-redis:main
    #
    # Would create an entry point called django-simple-redis that calls django-simple-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.9.1

  run:
    - python
    - redis >=2.9.1

test:
  # Python imports
  imports:
    - django_simple_redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/devpopol/django-simple-redis
  license: MIT
  summary: 'simplest way to connect to redis on a django application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
