package:
  name: cloudcix
  version: "0.1.9"

source:
  fn: cloudcix-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudcix/cloudcix-0.1.9.tar.gz
  md5: 6d7232ac8d548e25845875e0304b57cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudcix = cloudcix:main
    #
    # Would create an entry point called cloudcix that calls cloudcix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-keystoneclient >=1.2,<1.4
    - requests >=2.6,<2.7

  run:
    - python
    - python-keystoneclient >=1.2,<1.4
    - requests >=2.6,<2.7

test:
  # Python imports
  imports:
    - cloudcix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CloudCIX/cloudcix-python
  license: Apache Software License
  summary: 'Python bindings and utils for CloudCIX API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
