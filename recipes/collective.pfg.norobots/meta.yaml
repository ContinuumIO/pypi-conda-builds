package:
  name: collective.pfg.norobots
  version: "1.1"

source:
  fn: collective.pfg.norobots-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.pfg.norobots/collective.pfg.norobots-1.1.tar.gz
  md5: 87080dd9bd68e479ffe510fdead077d4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pfg.norobots = collective.pfg.norobots:main
    #
    # Would create an entry point called collective.pfg.norobots that calls collective.pfg.norobots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.z3cform.norobots
    - products.ploneformgen

  run:
    - python
    - setuptools
    - setuptools
    - collective.z3cform.norobots
    - products.ploneformgen

test:
  # Python imports
  imports:
    - collective
    - collective.pfg
    - collective.pfg.norobots
    - collective.pfg.norobots.Extensions
    - collective.pfg.norobots.tests
    - collective.pfg.norobots.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sylvainb/collective.pfg.norobots
  license: gpl
  summary: 'collective.pfg.norobots allows to add a collective.z3cform.norobots captcha field to PloneFormGen forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
