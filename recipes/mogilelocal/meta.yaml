package:
  name: mogilelocal
  version: "0.9.0"

source:
  fn: MogileLocal-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/M/MogileLocal/MogileLocal-0.9.0.tar.gz
  md5: 1835c3d896e83dded4ede69614dca647
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mogilelocal = mogilelocal:main
    #
    # Would create an entry point called mogilelocal that calls mogilelocal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mogilelocal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jonathan.tang.name/code/mogilelocal
  license: MIT License
  summary: 'MogileFS client that uses the local filesystem as backing storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
