package:
  name: matchete
  version: "0.0.2"

source:
  fn: matchete-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/matchete/matchete-0.0.2.tar.gz
  md5: d3d0e79f863850ea48ffd4b475836da4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - matchete = matchete:main
    #
    # Would create an entry point called matchete that calls matchete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - matchete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alehander42/py-matchete
  license: LICENSE
  summary: 'Python simple pattern-matching based method overloading'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
