package:
  name: list_shift
  version: "1.0.0"

source:
  fn: list_shift-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/list_shift/list_shift-1.0.0.tar.gz
  md5: e183aaecaad35ae6a976a915cfea2f7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - list_shift = list_shift:main
    #
    # Would create an entry point called list_shift that calls list_shift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - list_shift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://id774.net/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Add shift method for list'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
