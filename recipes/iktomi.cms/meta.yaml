package:
  name: iktomi.cms
  version: "0.1"

source:
  fn: iktomi.cms-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iktomi.cms/iktomi.cms-0.1.tar.gz
  md5: 94d3534cc0e2d5fe2254906461962fe7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iktomi.cms = iktomi.cms:main
    #
    # Would create an entry point called iktomi.cms that calls iktomi.cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iktomi
    - iktomi.cms
    - iktomi.cms.forms
    - iktomi.cms.models
    - iktomi.cms.publishing
    - iktomi.cms.streams
    - iktomi.cms.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/SmartTeleMax/iktomi-cms/
  license: MIT
  summary: 'CMS components for iktomi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
