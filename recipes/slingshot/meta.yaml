package:
  name: slingshot
  version: "0.0.2"

source:
  fn: slingshot-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/slingshot/slingshot-0.0.2.tar.gz
  md5: dbffba219918533169b345fc32919a76
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slingshot = slingshot:main
    #
    # Would create an entry point called slingshot that calls slingshot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - elasticsearch >=1.2.0
    - six >=1.8.0

  run:
    - python
    - elasticsearch >=1.2.0
    - six >=1.8.0

test:
  # Python imports
  imports:
    - slingshot
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OohlaLabs/slingshot
  license: MIT
  summary: 'Index Migration for ElasticSearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
