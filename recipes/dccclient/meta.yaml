package:
  name: dccclient
  version: "0.1.1"

source:
  fn: dccclient-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dccclient/dccclient-0.1.1.tar.gz
  md5: 13f9b2295659cb66b3b1ef097da043ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dccclient = dccclient:main
    #
    # Would create an entry point called dccclient that calls dccclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=13.0.0

  run:
    - python
    - twisted >=13.0.0

test:
  # Python imports
  imports:
    - dccclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tritium21/twisted-words-dcc
  license: MIT License
  summary: 'DCC Send for Twisted'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
