package:
  name: vsqs
  version: "0.3"

source:
  fn: vsqs-0.3.tar.gz
  url: https://pypi.python.org/packages/source/v/vsqs/vsqs-0.3.tar.gz
  md5: 602b85289989636a398752c5cb063fe9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vsqs = vsqs:main
    #
    # Would create an entry point called vsqs that calls vsqs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - watchdog >=0.8.2

  run:
    - python
    - watchdog >=0.8.2

test:
  # Python imports
  imports:
    - vsqs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erikvanzijst/vsqs
  license: MIT License
  summary: "A very simple message queuing system inspired on Amazon's SQS."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
