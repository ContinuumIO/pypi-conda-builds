package:
  name: flask-actions
  version: "0.6.6"

source:
  fn: Flask-Actions-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Actions/Flask-Actions-0.6.6.tar.gz
  md5: 2df41a08a65c9bb20d4a4920065c745f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-actions = flask-actions:main
    #
    # Would create an entry point called flask-actions that calls flask-actions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug

  run:
    - python
    - werkzeug

test:
  # Python imports
  imports:
    - flaskext
    - flaskext.actions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.flyzen.com
  license: BSD License
  summary: 'custom actions for flask to help manage your application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
