package:
  name: django-brake
  version: "1.3.1"

source:
  fn: django-brake-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-brake/django-brake-1.3.1.tar.gz
  md5: b5518f041e750648aec8e43be1f1b92c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-brake = django-brake:main
    #
    # Would create an entry point called django-brake that calls django-brake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - nose

  run:
    - python
    - django
    - nose

test:
  # Python imports
  imports:
    - brake
    - brake.backends
    - brake.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gmcquillan/django-brake
  license: BSD License
  summary: 'Cache-based rate-limiting for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
