package:
  name: orange-xoppy
  version: "1.0"

source:
  fn: Orange-XOPPY-1.0.tar.gz
  url: https://pypi.python.org/packages/source/O/Orange-XOPPY/Orange-XOPPY-1.0.tar.gz
  md5: ecf8298a179d180697c725a3840ee3f8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - orange-xoppy = orange-xoppy:main
    #
    # Would create an entry point called orange-xoppy that calls orange-xoppy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - orangecontrib
    - orangecontrib.xoppy
    - orangecontrib.xoppy.util
    - orangecontrib.xoppy.widgets
    - orangecontrib.xoppy.widgets.viewers
    - orangecontrib.xoppy.widgets.xoppy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://orange.biolab.si/
  license: GPLv3
  summary: 'XOPPY, X-ray oriented programs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
