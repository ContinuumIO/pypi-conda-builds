package:
  name: iccommunity.mediawiki
  version: "1.0-dev-r371"

source:
  fn: iccommunity.mediawiki-1.0-dev-r371.tar.gz
  url: https://pypi.python.org/packages/source/i/iccommunity.mediawiki/iccommunity.mediawiki-1.0-dev-r371.tar.gz
  md5: 1a6474624b0f0690a97295307ebcdcec
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iccommunity.mediawiki = iccommunity.mediawiki:main
    #
    # Would create an entry point called iccommunity.mediawiki that calls iccommunity.mediawiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - iccommunity
    - iccommunity.mediawiki
    - iccommunity.mediawiki.Extensions
    - iccommunity.mediawiki.browser
    - iccommunity.mediawiki.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.platecom.com/productos/iccommunity.mediawiki/
  license: GNU General Public License (GPL)
  summary: 'icCommunity.Mediawiki'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
