package:
  name: caesarcipher
  version: "1.0"

source:
  fn: caesarcipher-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/caesarcipher/caesarcipher-1.0.tar.gz
  md5: c5cc280216f0dfbc71fef7ba9e8ef448
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caesarcipher = caesarcipher:main
    #
    # Would create an entry point called caesarcipher that calls caesarcipher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - caesarcipher
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RobSpectre/Caesar-Cipher
  license: MIT License
  summary: 'A Python package and command line script for encoding, decoding and cracking Caesar ciphers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
