package:
  name: ckcache
  version: "0.1.4"

source:
  fn: ckcache-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/ckcache/ckcache-0.1.4.tar.gz
  md5: 2b81df80321a6536dd880271b158b23a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckcache = ckcache:main
    #
    # Would create an entry point called ckcache that calls ckcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - ckcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CivicKnowledge/ckcache
  license: BSD License
  summary: 'Common interface to a variety of object storage systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
