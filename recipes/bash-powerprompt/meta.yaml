package:
  name: bash-powerprompt
  version: "0.1.0"

source:
  fn: bash-powerprompt-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bash-powerprompt/bash-powerprompt-0.1.0.tar.gz
  md5: d8f3672a6b09a91ea4661753c14e93f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bash-powerprompt = bash-powerprompt:main
    #
    # Would create an entry point called bash-powerprompt that calls bash-powerprompt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sh

  run:
    - python
    - sh

test:
  # Python imports
  imports:
    - powerprompt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MrHaas/bash-powerprompt
  license: MIT License
  summary: 'Powerful dynamic bash prompt inspired by Vim Powerline'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
