package:
  name: coverage-reporter
  version: "0.01"

source:
  fn: coverage-reporter-0.01.tar.gz
  url: https://pypi.python.org/packages/source/c/coverage-reporter/coverage-reporter-0.01.tar.gz
  md5: 068f7bbfb2f88a275e06086adc7bb4b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coverage-reporter = coverage-reporter:main
    #
    # Would create an entry point called coverage-reporter that calls coverage-reporter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coverage_reporter
    - coverage_reporter.collectors
    - coverage_reporter.filters
    - coverage_reporter.reports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/dugan/coverage-reporter/
  license: MIT License
  summary: 'Coverage reporting tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
