package:
  name: argtoolbox
  version: "0.1.0"

source:
  fn: argtoolbox-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/argtoolbox/argtoolbox-0.1.0.tar.gz
  md5: 61cc048e55b3409c44cb2276cf9b5db2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argtoolbox = argtoolbox:main
    #
    # Would create an entry point called argtoolbox that calls argtoolbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - argcomplete
    - configparser
    - ordereddict

  run:
    - python
    - argparse
    - argcomplete
    - configparser
    - ordereddict

test:
  # Python imports
  imports:
    - argtoolbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fred49/argtoolbox
  license: GNU General Public License v3 (GPLv3)
  summary: 'The easy way to create a short program with file options and command line options.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
