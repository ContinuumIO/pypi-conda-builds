package:
  name: django-simple-redis-admin
  version: "1.2.0"

source:
  fn: django-simple-redis-admin-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-redis-admin/django-simple-redis-admin-1.2.0.tar.gz
  md5: 0bf344fd3b6fe83e80513481bbd1b3ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-redis-admin = django-simple-redis-admin:main
    #
    # Would create an entry point called django-simple-redis-admin that calls django-simple-redis-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_admin
    - redis_admin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicholasserra/django-simple-redis-admin
  license: BSD License
  summary: 'A django admin application to manage redis cache keys.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
