package:
  name: collective.microdata.event
  version: "0.2"

source:
  fn: collective.microdata.event-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.microdata.event/collective.microdata.event-0.2.zip
  md5: 51e010935361fb889fd8195fdd662961
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.microdata.event = collective.microdata.event:main
    #
    # Would create an entry point called collective.microdata.event that calls collective.microdata.event.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.microdata.core
    - isodate

  run:
    - python
    - setuptools
    - setuptools
    - collective.microdata.core
    - isodate

test:
  # Python imports
  imports:
    - collective
    - collective.microdata
    - collective.microdata.event
    - collective.microdata.event.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.microdata.event
  license: GPL
  summary: 'Add microdata (scheda.org) support to Plone events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
