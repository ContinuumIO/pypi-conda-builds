package:
  name: marshmallow-form
  version: "0.1.1"

source:
  fn: marshmallow-form-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/marshmallow-form/marshmallow-form-0.1.1.tar.gz
  md5: a9004bf1baec9c69b1e9c9e1d55be74f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marshmallow-form = marshmallow-form:main
    #
    # Would create an entry point called marshmallow-form that calls marshmallow-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - marshmallow

  run:
    - python
    - setuptools
    - marshmallow

test:
  # Python imports
  imports:
    - marshmallow_form
    - marshmallow_form.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/podhmo/marshmallow-form
  license: UNKNOWN
  summary: 'a wrapper of marshmallow for form library like behavior'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
