package:
  name: bottle-rauth
  version: "0.1.1"

source:
  fn: bottle-rauth-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-rauth/bottle-rauth-0.1.1.tar.gz
  md5: 9e126c8180bcea0070f49a3efd25a806
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-rauth = bottle-rauth:main
    #
    # Would create an entry point called bottle-rauth that calls bottle-rauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rauth
    - bottle

  run:
    - python
    - rauth
    - bottle

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klen/bottle-oauth
  license: BSD License
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
