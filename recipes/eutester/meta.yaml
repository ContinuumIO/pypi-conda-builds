package:
  name: eutester
  version: "0.0.10"

source:
  fn: eutester-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/e/eutester/eutester-0.0.10.tar.gz
  md5: f7103cf71e8e1d971dd854f237d40860
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eutester = eutester:main
    #
    # Would create an entry point called eutester that calls eutester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko >=1.7
    - boto >=2.5.2
    - jinja2 >=2.7
    - argparse
    - futures
    - python-dateutil
    - mock
    - dnspython
    - pywinrm
    - beautifulsoup
    - requests >=1

  run:
    - python
    - paramiko >=1.7
    - boto >=2.5.2
    - jinja2 >=2.7
    - argparse
    - futures
    - python-dateutil
    - mock
    - dnspython
    - pywinrm
    - beautifulsoup
    - requests >=1

test:
  # Python imports
  imports:
    - eucaops
    - eucaweb
    - eutester
    - testcases
    - testcases.cloud_admin
    - testcases.cloud_user
    - testcases.cloud_user.ebs
    - testcases.cloud_user.ebs.san_utils
    - testcases.cloud_user.images
    - testcases.cloud_user.images.eustore
    - testcases.cloud_user.images.windows
    - testcases.cloud_user.instances
    - testcases.cloud_user.load
    - testcases.cloud_user.s3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://open.eucalyptus.com
  license: BSD License
  summary: 'Test Framework for AWS compatible clouds'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
