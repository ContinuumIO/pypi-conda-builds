package:
  name: thetvdb_api
  version: "0.3.1"

source:
  fn: thetvdb_api-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/t/thetvdb_api/thetvdb_api-0.3.1.tar.gz
  md5: 681b292d75dc84a66bf6c564bf45598e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thetvdb_api = thetvdb_api:main
    #
    # Would create an entry point called thetvdb_api that calls thetvdb_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tests
    - thetvdb_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/Julien-D/thetvdb_api
  license: GNU General Public License v3 (GPLv3)
  summary: 'A Python API for theTVDB.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
