package:
  name: django-user-action-confirmation
  version: "0.1.4"

source:
  fn: django-user-action-confirmation-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-action-confirmation/django-user-action-confirmation-0.1.4.tar.gz
  md5: 839e1dfbfedd1dd1f8bac080a9cdb204
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-action-confirmation = django-user-action-confirmation:main
    #
    # Would create an entry point called django-user-action-confirmation that calls django-user-action-confirmation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - jsonfield

  run:
    - python
    - django >=1.5
    - jsonfield

test:
  # Python imports
  imports:
    - user_action_confirmation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alekam/django-user-action-confirmation
  license: BSD License
  summary: 'Easy and simple way to confirm user actions in your django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
