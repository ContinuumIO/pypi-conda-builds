package:
  name: akshell
  version: "0.2.0"

source:
  fn: akshell-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/akshell/akshell-0.2.0.tar.gz
  md5: 4e1f18b78e4b231a1fddd0b55bea636b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - akshell = akshell:main
    #
    # Would create an entry point called akshell that calls akshell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - akshell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.akshell.com
  license: BSD License
  summary: '\nAn utility and a library for development access to http://www.akshell.com/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
