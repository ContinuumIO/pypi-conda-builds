package:
  name: django-langswitch
  version: "0.1.1"

source:
  fn: django-langswitch-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-langswitch/django-langswitch-0.1.1.zip
  md5: 850d868228b3323648e601f1ed2e4cd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-langswitch = django-langswitch:main
    #
    # Would create an entry point called django-langswitch that calls django-langswitch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - langswitch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Marcon/django-langswitch
  license: MIT License
  summary: 'django app, that can be used on multilingual sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
