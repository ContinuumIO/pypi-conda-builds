package:
  name: matchmaker
  version: "0.1.3"

source:
  fn: matchmaker-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/matchmaker/matchmaker-0.1.3.tar.gz
  md5: e3c8f8d431cf36b10b439b3fee3b3a75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - matchmaker = matchmaker:main
    #
    # Would create an entry point called matchmaker that calls matchmaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyhamcrest

  run:
    - python
    - pyhamcrest

test:
  # Python imports
  imports:
    - matchmaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/txels/matchmaker
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Easy creation of hamcrest matchers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
