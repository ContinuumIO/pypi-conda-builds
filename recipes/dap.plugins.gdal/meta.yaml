package:
  name: dap.plugins.gdal
  version: "0.1.1"

source:
  fn: dap.plugins.gdal-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.plugins.gdal/dap.plugins.gdal-0.1.1.tar.gz
  md5: 16dcd1ba8db71ae5c6e4993555fce060
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.plugins.gdal = dap.plugins.gdal:main
    #
    # Would create an entry point called dap.plugins.gdal that calls dap.plugins.gdal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dap[server] >=2.2.5.4
    - arrayterator >=0.2.6

  run:
    - python
    - setuptools
    - dap[server] >=2.2.5.4
    - arrayterator >=0.2.6

test:
  # Python imports
  imports:
    - dap
    - dap.plugins
    - dap.plugins.gdal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydap.org/plugins/gdal.html
  license: MIT License
  summary: 'GDAL plugin for pydap server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
