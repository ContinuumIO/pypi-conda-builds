package:
  name: cloud-bdii-provider
  version: "0.4.dev140.g387348d"

source:
  fn: cloud-bdii-provider-0.4.dev140.g387348d.tar.gz
  url: https://pypi.python.org/packages/source/c/cloud-bdii-provider/cloud-bdii-provider-0.4.dev140.g387348d.tar.gz
  md5: 248089eb0599ca666bd38f231828b378
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloud-bdii-provider = cloud-bdii-provider:main
    #
    # Would create an entry point called cloud-bdii-provider that calls cloud-bdii-provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Cloud BDII info provider'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
