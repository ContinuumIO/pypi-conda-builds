package:
  name: easy_graph
  version: "0.1"

source:
  fn: easy_graph-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/easy_graph/easy_graph-0.1.tar.gz
  md5: f082038dd75487e411ab6bfd7d18a711
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy_graph = easy_graph:main
    #
    # Would create an entry point called easy_graph that calls easy_graph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - easy_graph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JesseAldridge/easy_graph
  license: MIT
  summary: 'Easily turn your python data into a flot graph in a static html file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
