package:
  name: chronicle
  version: "1.0.7"

source:
  fn: chronicle-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/chronicle/chronicle-1.0.7.tar.gz
  md5: c589692eba774e3ed051daf48920d021
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chronicle = chronicle:main
    #
    # Would create an entry point called chronicle that calls chronicle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chronicle
    - chronicle.flask
    - chronicle.pyramid
    - chronicle.sentry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: u'logging utils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
