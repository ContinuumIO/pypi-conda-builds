package:
  name: django-config-url
  version: "0.1.1"

source:
  fn: django-config-url-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-config-url/django-config-url-0.1.1.tar.gz
  md5: a0e818be1a87df337bf2055c4523c945
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-config-url = django-config-url:main
    #
    # Would create an entry point called django-config-url that calls django-config-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_config_url

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GannettDigital/django-config-url
  license: UNKNOWN
  summary: 'A simple mechanism for loading external configuration from a URL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
