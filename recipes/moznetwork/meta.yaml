package:
  name: moznetwork
  version: "0.24"

source:
  fn: moznetwork-0.24.tar.gz
  url: https://pypi.python.org/packages/source/m/moznetwork/moznetwork-0.24.tar.gz
  md5: df6789abb7ca2334434535f2bb0e702d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moznetwork = moznetwork:main
    #
    # Would create an entry point called moznetwork that calls moznetwork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mozinfo

  run:
    - python
    - mozinfo

test:
  # Python imports
  imports:
    - moznetwork

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://wiki.mozilla.org/Auto-tools/Projects/Mozbase
  license: MPL
  summary: 'Library of network utilities for use in Mozilla testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
