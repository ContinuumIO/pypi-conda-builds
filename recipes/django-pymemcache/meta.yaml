package:
  name: django-pymemcache
  version: "0.1.0"

source:
  fn: django-pymemcache-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pymemcache/django-pymemcache-0.1.0.tar.gz
  md5: e6072c0853593f8550d33f4014812eda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pymemcache = django-pymemcache:main
    #
    # Would create an entry point called django-pymemcache that calls django-pymemcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymemcache

  run:
    - python
    - pymemcache

test:
  # Python imports
  imports:
    - djpymemcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jsocol/django-pymemcache
  license: Apache Software License
  summary: "Django cache backend based on Pinterest's pymemcache client."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
