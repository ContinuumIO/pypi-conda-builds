package:
  name: atomicwrites
  version: "0.1.5"

source:
  fn: atomicwrites-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/a/atomicwrites/atomicwrites-0.1.5.tar.gz
  md5: c683a18ec30fcacd65a2617079d50486
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomicwrites = atomicwrites:main
    #
    # Would create an entry point called atomicwrites that calls atomicwrites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - atomicwrites

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/untitaker/python-atomicwrites
  license: MIT
  summary: 'Atomic file writes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
