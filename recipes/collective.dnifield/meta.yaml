package:
  name: collective.dnifield
  version: "1.0a1"

source:
  fn: collective.dnifield-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.dnifield/collective.dnifield-1.0a1.tar.gz
  md5: 7c6e1955d9e471a3197198e720507e83
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dnifield = collective.dnifield:main
    #
    # Would create an entry point called collective.dnifield that calls collective.dnifield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.schema

  run:
    - python
    - setuptools
    - setuptools
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.dnifield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.dnifield
  license: GNU General Public License (GPL)
  summary: 'A field to enter a national identity number.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
