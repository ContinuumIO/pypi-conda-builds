package:
  name: collective.localrolespanel
  version: "0.2"

source:
  fn: collective.localrolespanel-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.localrolespanel/collective.localrolespanel-0.2.zip
  md5: 4a05d0579fd364e73d72d6fa0e0e827f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.localrolespanel = collective.localrolespanel:main
    #
    # Would create an entry point called collective.localrolespanel that calls collective.localrolespanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.localrolespanel
    - collective.localrolespanel.browser
    - collective.localrolespanel.interfaces
    - collective.localrolespanel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.localrolespanel
  license: GNU General Public License (GPL)
  summary: 'Adds a controlpanel to the site to edit local role settings using csv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
