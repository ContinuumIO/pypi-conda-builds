package:
  name: edw.userhistory
  version: "1.5"

source:
  fn: edw.userhistory-1.5.zip
  url: https://pypi.python.org/packages/source/e/edw.userhistory/edw.userhistory-1.5.zip
  md5: 26d53c0472b1e9b509de2527785367f9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edw.userhistory = edw.userhistory:main
    #
    # Would create an entry point called edw.userhistory that calls edw.userhistory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - edw
    - edw.userhistory
    - edw.userhistory.browser
    - edw.userhistory.portlets
    - edw.userhistory.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'User login history'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
