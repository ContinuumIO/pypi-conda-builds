package:
  name: m3-core
  version: "3.0.1.3"

source:
  fn: m3-core-3.0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-core/m3-core-3.0.1.3.tar.gz
  md5: cccad63b8d51f01f9bf55d2a8a7bb645
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-core = m3-core:main
    #
    # Would create an entry point called m3-core that calls m3-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - m3
    - m3.actions
    - m3.actions.dicts
    - m3.db
    - m3.management
    - m3.management.commands
    - m3.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barsgroup/m3-core
  license: MIT License
  summary: u'## M3-Core\n\n\u042f\u0434\u0440\u043e \u043f\u043b\u0430\u0442\u0444\u043e\u0440\u043c\u044b **M3**'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
