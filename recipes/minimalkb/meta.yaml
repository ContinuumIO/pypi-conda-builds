package:
  name: minimalkb
  version: "0.7"

source:
  fn: minimalKB-0.7.tar.gz
  url: https://pypi.python.org/packages/source/m/minimalKB/minimalKB-0.7.tar.gz
  md5: 4b697e7b32519a51b97dcdf6aaf32e31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minimalkb = minimalkb:main
    #
    # Would create an entry point called minimalkb that calls minimalkb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - minimalkb
    - minimalkb/backends
    - minimalkb/services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/severin-lemaignan/minimalkb
  license: BSD License
  summary: 'A SQLite-backed minimalistic knowledge based for robotic application. Mostly KB-API conformant.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
