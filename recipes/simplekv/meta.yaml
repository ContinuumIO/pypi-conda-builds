package:
  name: simplekv
  version: "0.9.2"

source:
  fn: simplekv-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/s/simplekv/simplekv-0.9.2.tar.gz
  md5: adf0469eb00812bbdebde487e93536c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplekv = simplekv:main
    #
    # Would create an entry point called simplekv that calls simplekv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - simplekv
    - simplekv.db
    - simplekv.memory
    - simplekv.net

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/simplekv
  license: MIT
  summary: 'A key-value storage for binary data, support many backends.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
