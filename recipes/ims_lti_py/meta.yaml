package:
  name: ims_lti_py
  version: "0.6"

source:
  fn: ims_lti_py-0.6.tar.gz
  url: https://pypi.python.org/packages/source/i/ims_lti_py/ims_lti_py-0.6.tar.gz
  md5: aa3cec6e9ace81baed1ef17db90b724c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ims_lti_py = ims_lti_py:main
    #
    # Would create an entry point called ims_lti_py that calls ims_lti_py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - oauth2

  run:
    - python
    - lxml
    - oauth2

test:
  # Python imports
  imports:
    - ims_lti_py

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tophatmonocle/ims_lti_py
  license: MIT License
  summary: 'A Python library to help implement IMS LTI tool consumers and providers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
