package:
  name: hyperframe
  version: "1.0.0"

source:
  fn: hyperframe-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hyperframe/hyperframe-1.0.0.tar.gz
  md5: bb2ae28ecf0763680673094a3ac41733
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyperframe = hyperframe:main
    #
    # Would create an entry point called hyperframe that calls hyperframe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hyperframe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hyper.rtfd.org
  license: MIT License
  summary: 'HTTP/2 framing layer for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
