package:
  name: ckanext-deadoralive
  version: "0.1.6"

source:
  fn: ckanext-deadoralive-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-deadoralive/ckanext-deadoralive-0.1.6.tar.gz
  md5: 968534688895bc4df754eeffa1d54d97
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-deadoralive = ckanext-deadoralive:main
    #
    # Would create an entry point called ckanext-deadoralive that calls ckanext-deadoralive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ckanext
    - ckanext.deadoralive
    - ckanext.deadoralive.logic
    - ckanext.deadoralive.logic.action
    - ckanext.deadoralive.logic.auth
    - ckanext.deadoralive.model
    - ckanext.deadoralive.tests
    - ckanext.deadoralive.tests.logic
    - ckanext.deadoralive.tests.logic.action
    - ckanext.deadoralive.tests.logic.auth
    - ckanext.deadoralive.tests.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ckan/ckanext-deadoralive
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'A CKAN extension for the Dead or Alive link checker service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
