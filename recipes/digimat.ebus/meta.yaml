package:
  name: digimat.ebus
  version: "0.1.7"

source:
  fn: digimat.ebus-0.1.7.zip
  url: https://pypi.python.org/packages/source/d/digimat.ebus/digimat.ebus-0.1.7.zip
  md5: e7253c2b71fc672eeba64fcb1cf9e19c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digimat.ebus = digimat.ebus:main
    #
    # Would create an entry point called digimat.ebus that calls digimat.ebus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - digimat
    - digimat.ebus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Digimat ExternalBus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
