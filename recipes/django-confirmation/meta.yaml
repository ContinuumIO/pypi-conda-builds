package:
  name: django-confirmation
  version: "0.9-pre"

source:
  fn: django-confirmation-0.9-pre.tar.gz
  url: https://pypi.python.org/packages/source/d/django-confirmation/django-confirmation-0.9-pre.tar.gz
  md5: 89a2d0a51b8e91abdf3060bbd0be6c1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-confirmation = django-confirmation:main
    #
    # Would create an entry point called django-confirmation that calls django-confirmation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - confirmation
    - confirmation.management
    - confirmation.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-confirmation/
  license: BSD License
  summary: 'Generic object confirmation for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
