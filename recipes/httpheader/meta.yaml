package:
  name: httpheader
  version: "1.1"

source:
  fn: httpheader-1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/httpheader/httpheader-1.1.tar.gz
  md5: 0302e59f9c8814a807a9380c22171935
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpheader = httpheader:main
    #
    # Would create an entry point called httpheader that calls httpheader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - httpheader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://deron.meranda.us/python/httpheader/
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Utility functions to parse HTTP 1.1 protocol headers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
