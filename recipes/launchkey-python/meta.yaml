package:
  name: launchkey-python
  version: "1.2.7"

source:
  fn: launchkey-python-1.2.7.tar.gz
  url: https://pypi.python.org/packages/source/l/launchkey-python/launchkey-python-1.2.7.tar.gz
  md5: 4b77cc8566831816c230ee57a2271e4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - launchkey-python = launchkey-python:main
    #
    # Would create an entry point called launchkey-python that calls launchkey-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto
    - requests >=2.5.1

  run:
    - python
    - pycrypto
    - requests >=2.5.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchkey.com
  license: MIT
  summary: 'LaunchKey Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
