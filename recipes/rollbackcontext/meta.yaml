package:
  name: rollbackcontext
  version: "0.1-2"

source:
  fn: rollbackcontext-0.1-2.tar.gz
  url: https://pypi.python.org/packages/source/r/rollbackcontext/rollbackcontext-0.1-2.tar.gz
  md5: ce67f571afa07d001e9ad3e39eb59bc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rollbackcontext = rollbackcontext:main
    #
    # Would create an entry point called rollbackcontext that calls rollbackcontext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rollbackcontext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edwardbadboy/rollbackcontext
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'A context manager to do rollbacks automatically'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
