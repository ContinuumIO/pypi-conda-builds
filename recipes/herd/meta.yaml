package:
  name: herd
  version: "0.0.3.dev2"

source:
  fn: herd-0.0.3.dev2.tar.gz
  url: https://pypi.python.org/packages/source/h/herd/herd-0.0.3.dev2.tar.gz
  md5: ae87167796fee9624d8aa9549dab5031
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - herd = herd:main
    #
    # Would create an entry point called herd that calls herd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cached-property
    - paramiko
    - python-digitalocean
    - pytoml
    - scp

  run:
    - python
    - cached-property
    - paramiko
    - python-digitalocean
    - pytoml
    - scp

test:
  # Python imports
  imports:
    - herd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bpicolo/herd
  license: MIT License
  summary: 'Small cluster automation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
