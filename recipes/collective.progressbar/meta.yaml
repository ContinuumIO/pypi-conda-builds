package:
  name: collective.progressbar
  version: "0.5"

source:
  fn: collective.progressbar-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.progressbar/collective.progressbar-0.5.tar.gz
  md5: 06d5019b84403a49971573697fcffd31
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.progressbar = collective.progressbar:main
    #
    # Would create an entry point called collective.progressbar that calls collective.progressbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.progressbar
    - collective.progressbar.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.progressbar
  license: GPL
  summary: 'HTML and Javascript progress bar for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
