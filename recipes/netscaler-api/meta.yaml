package:
  name: netscaler-api
  version: "0.2.3"

source:
  fn: netscaler-api-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/n/netscaler-api/netscaler-api-0.2.3.tar.gz
  md5: ccf044829c30e7a3952a6c672c2287e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netscaler-api = netscaler-api:main
    #
    # Would create an entry point called netscaler-api that calls netscaler-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds >=0.3.9

  run:
    - python
    - suds >=0.3.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jathanism/netscaler-api/
  license: BSD License
  summary: 'NetScaler API is a Python interface for interacting with Citrix NetScaler application delivery controllers, utilizing the SOAP API to execute commands.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
