package:
  name: menpofit
  version: "0.1.0a1"

source:
  fn: menpofit-0.1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/m/menpofit/menpofit-0.1.0a1.tar.gz
  md5: 9688362a887a58f244be166f765f3e19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - menpofit = menpofit:main
    #
    # Would create an entry point called menpofit that calls menpofit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy ==1.9.0
    - scipy ==0.14.0
    - wrapt ==1.9.0
    - menpo ==0.4.0a1

  run:
    - python
    - numpy ==1.9.0
    - scipy ==0.14.0
    - wrapt ==1.9.0
    - menpo ==0.4.0a1

test:
  # Python imports
  imports:
    - menpofit
    - menpofit.aam
    - menpofit.atm
    - menpofit.benchmark
    - menpofit.clm
    - menpofit.gradientdescent
    - menpofit.lucaskanade
    - menpofit.lucaskanade.appearance
    - menpofit.regression
    - menpofit.sdm
    - menpofit.test
    - menpofit.transform
    - menpofit.transform.test
    - menpofit.visualize
    - menpofit.visualize.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: "Menpo's image feature point localisation (AAMs, SDMs, CLMs)"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
