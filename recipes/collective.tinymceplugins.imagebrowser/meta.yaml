package:
  name: collective.tinymceplugins.imagebrowser
  version: "1.0b6"

source:
  fn: collective.tinymceplugins.imagebrowser-1.0b6.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.tinymceplugins.imagebrowser/collective.tinymceplugins.imagebrowser-1.0b6.tar.gz
  md5: 655bf448442eb30aefc7223025ac9c20
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.tinymceplugins.imagebrowser = collective.tinymceplugins.imagebrowser:main
    #
    # Would create an entry point called collective.tinymceplugins.imagebrowser that calls collective.tinymceplugins.imagebrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - rwproperty
    - products.tinymce >=1.1.8

  run:
    - python
    - setuptools
    - setuptools
    - rwproperty
    - products.tinymce >=1.1.8

test:
  # Python imports
  imports:
    - collective
    - collective.tinymceplugins
    - collective.tinymceplugins.imagebrowser
    - collective.tinymceplugins.imagebrowser.browser
    - collective.tinymceplugins.imagebrowser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.tinymceplugins.imagebrowser
  license: GPL
  summary: 'A TinyMCE plugin to provide thumbnail image browsing and a      ressource library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
