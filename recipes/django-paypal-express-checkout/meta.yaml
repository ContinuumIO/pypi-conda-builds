package:
  name: django-paypal-express-checkout
  version: "1.8"

source:
  fn: django-paypal-express-checkout-1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paypal-express-checkout/django-paypal-express-checkout-1.8.tar.gz
  md5: 3c374ea0d22847f55fe1a3f73cc67195
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paypal-express-checkout = django-paypal-express-checkout:main
    #
    # Would create an entry point called django-paypal-express-checkout that calls django-paypal-express-checkout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django_libs

  run:
    - python
    - django
    - django_libs

test:
  # Python imports
  imports:
    - paypal_express_checkout
    - paypal_express_checkout.migrations
    - paypal_express_checkout.tests
    - paypal_express_checkout.tests.integration_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-paypal-express-checkout
  license: The MIT License
  summary: 'A reusable Django app to include a PayPal payment flow to your site.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
