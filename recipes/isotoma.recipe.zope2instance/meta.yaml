package:
  name: isotoma.recipe.zope2instance
  version: "0.0.6"

source:
  fn: isotoma.recipe.zope2instance-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.zope2instance/isotoma.recipe.zope2instance-0.0.6.tar.gz
  md5: 8325d116a30b36f9c769ad65e39e488b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.zope2instance = isotoma.recipe.zope2instance:main
    #
    # Would create an entry point called isotoma.recipe.zope2instance that calls isotoma.recipe.zope2instance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.zope2instance
    - isotoma.recipe.zope2instance.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.zope2instance
  license: Zope Public License
  summary: 'Buildout recipes for create zope2instances'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
