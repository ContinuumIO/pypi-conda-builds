package:
  name: litreview
  version: "0.6997dev"

source:
  fn: LitReview-0.6997dev.tar.gz
  url: https://pypi.python.org/packages/source/L/LitReview/LitReview-0.6997dev.tar.gz
  md5: 761bf2ffa9f5f2ba7090dcfeb64e8d8f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - litreview = litreview:main
    #
    # Would create an entry point called litreview that calls litreview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - flask-login >=0.1.3
    - jinja2 >=2.6
    - sqlalchemy >=0.7.9
    - wtforms >=1.0.2
    - werkzeug >=0.8.3
    - numpy >=1.6.2
    - biopython >=1.60
    - cx-oracle >=5.1.2
    - distribute >=0.6.28
    - jsonpickle >=0.4.0

  run:
    - python
    - setuptools
    - flask >=0.9
    - flask-login >=0.1.3
    - jinja2 >=2.6
    - sqlalchemy >=0.7.9
    - wtforms >=1.0.2
    - werkzeug >=0.8.3
    - numpy >=1.6.2
    - biopython >=1.60
    - cx-oracle >=5.1.2
    - distribute >=0.6.28
    - jsonpickle >=0.4.0

test:
  # Python imports
  imports:
    - litreview
    - model_old_schema
    - queries
    - webapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cherry-vm13.stanford.edu:5000
  license: MIT
  summary: 'Literature curation tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
