package:
  name: coinop
  version: "0.1.3"

source:
  fn: coinop-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/coinop/coinop-0.1.3.tar.gz
  md5: af3fbe63955d9d78cdb78a3ff2376b69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinop = coinop:main
    #
    # Would create an entry point called coinop that calls coinop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pynacl ==0.3.0
    - cffi
    - pytest
    - pycrypto
    - python-bitcoinlib
    - pycoin ==0.42
    - pyyaml
    - ecdsa

  run:
    - python
    - pynacl ==0.3.0
    - cffi
    - pytest
    - pycrypto
    - python-bitcoinlib
    - pycoin ==0.42
    - pyyaml
    - ecdsa

test:
  # Python imports
  imports:
    - coinop
    - coinop.bit
    - coinop.crypto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/GemHQ/coinop-py
  license: MIT
  summary: 'Crypto-currency conveniences'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
