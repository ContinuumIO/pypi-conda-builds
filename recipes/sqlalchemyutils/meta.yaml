package:
  name: sqlalchemyutils
  version: "0.0.7"

source:
  fn: SQLAlchemyUtils-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemyUtils/SQLAlchemyUtils-0.0.7.tar.gz
  md5: dce014d0ed5e33cac2eb2f2c331917f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemyutils = sqlalchemyutils:main
    #
    # Would create an entry point called sqlalchemyutils that calls sqlalchemyutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sautils
    - sautils.tests
    - sqlalchemyutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tylertreat/SQLAlchemyUtils
  license: Apache
  summary: 'Useful utilities for working with SQLAlchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
