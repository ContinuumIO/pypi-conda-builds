package:
  name: punycodeurl
  version: "0.1"

source:
  fn: punycodeurl-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/punycodeurl/punycodeurl-0.1.tar.gz
  md5: 645d08cca9ff73626141e4c3f2ceaf54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - punycodeurl = punycodeurl:main
    #
    # Would create an entry point called punycodeurl that calls punycodeurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - punycodeurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/charsyam/punycodeurl
  license: UNKNOWN
  summary: 'punycodeurl for Url'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
