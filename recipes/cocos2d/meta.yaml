package:
  name: cocos2d
  version: "0.6.3"

source:
  fn: cocos2d-0.6.3.zip
  url: https://pypi.python.org/packages/source/c/cocos2d/cocos2d-0.6.3.zip
  md5: 20457957eb4e836fb2613f4be260485a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cocos2d = cocos2d:main
    #
    # Would create an entry point called cocos2d that calls cocos2d.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - pyglet >=1.2

  run:
    - python
    - six
    - pyglet >=1.2

test:
  # Python imports
  imports:
    - cocos
    - cocos/actions
    - cocos/audio
    - cocos/layer
    - cocos/scenes
    - cocos2d

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://python.cocos2d.org
  license: BSD License
  summary: 'a 2D framework for games and multimedia'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
