package:
  name: slc.mindmap
  version: "1.0"

source:
  fn: slc.mindmap-1.0.zip
  url: https://pypi.python.org/packages/source/s/slc.mindmap/slc.mindmap-1.0.zip
  md5: 2b0e29c225a3af08040948fa2ca52ede
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.mindmap = slc.mindmap:main
    #
    # Would create an entry point called slc.mindmap that calls slc.mindmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - p4a.subtyper

  run:
    - python
    - setuptools
    - setuptools
    - p4a.subtyper

test:
  # Python imports
  imports:
    - slc
    - slc.mindmap
    - slc.mindmap.browser
    - slc.mindmap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/slc.mindmap
  license: GPL
  summary: 'Adds export, viewing and editing support for mindmaps in Plone, with an embedded editor from mindmeister.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
