package:
  name: django_callable_perms
  version: "0.1.2"

source:
  fn: django_callable_perms-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_callable_perms/django_callable_perms-0.1.2.tar.gz
  md5: 300d4498279beb92714646aa95ba1e6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_callable_perms = django_callable_perms:main
    #
    # Would create an entry point called django_callable_perms that calls django_callable_perms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_callable_perms
    - django_callable_perms.management
    - django_callable_perms.management.commands
    - django_callable_perms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ddanier/django_callable_perms
  license: BSD License
  summary: 'Modular registry of permission handlers for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
