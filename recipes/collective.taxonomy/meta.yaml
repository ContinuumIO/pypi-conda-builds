package:
  name: collective.taxonomy
  version: "1.2.3"

source:
  fn: collective.taxonomy-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.taxonomy/collective.taxonomy-1.2.3.tar.gz
  md5: dbbb1d323994d8e0b7666939ab867516
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.taxonomy = collective.taxonomy:main
    #
    # Would create an entry point called collective.taxonomy that calls collective.taxonomy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.supermodel
    - plone.app.registry
    - plone.app.dexterity[grok]
    - elementtree
    - simplejson
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - plone.supermodel
    - plone.app.registry
    - plone.app.dexterity[grok]
    - elementtree
    - simplejson
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - collective
    - collective.taxonomy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv2+
  summary: 'Create, edit and use hierarchical taxonomies in Plone!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
