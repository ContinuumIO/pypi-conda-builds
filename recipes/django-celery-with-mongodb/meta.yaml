package:
  name: django-celery-with-mongodb
  version: "3.0"

source:
  fn: django-celery-with-mongodb-3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery-with-mongodb/django-celery-with-mongodb-3.0.tar.gz
  md5: 85735df91fe54bc0a37c9a361b67f4a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery-with-mongodb = django-celery-with-mongodb:main
    #
    # Would create an entry point called django-celery-with-mongodb that calls django-celery-with-mongodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-celery >=3.0,<4.0
    - pymongo

  run:
    - python
    - django-celery >=3.0,<4.0
    - pymongo

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://celeryproject.org
  license: BSD
  summary: 'Bundle installing the dependencies for Django-Celery and MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
