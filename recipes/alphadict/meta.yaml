package:
  name: alphadict
  version: "1.01"

source:
  fn: alphadict-1.01.tar.gz
  url: https://pypi.python.org/packages/source/a/alphadict/alphadict-1.01.tar.gz
  md5: 4ceff83b3d8f46d4469aad06667fb026
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alphadict = alphadict:main
    #
    # Would create an entry point called alphadict that calls alphadict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - alphadict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dancrew32/AlphaDict
  license: MIT
  summary: 'Recursively sorts dictionaries by key.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
